var e="The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## The structure\n\n- Last in, first out. Only the top is reachable, and that restriction is the\n  point.\n- `Deque<Integer> st = new ArrayDeque<>()`. Never `java.util.Stack` — it is\n  synchronised, extends `Vector`, and iterates bottom-to-top.\n- `push` / `pop` / `peek`, all O(1). `pop` throws when empty; `poll` returns\n  `null`.\n- `ArrayDeque` rejects `null`, because `peek()` uses `null` to mean empty.\n- Popped `Integer` values compare with `.equals`, not `==` — boxing is only\n  cached to 127.\n- Brackets: opener push, closer pop and check the kinds match. Two failure\n  modes — a closer on an empty stack, and a non-empty stack at the end.\n\n## The monotonic stack\n\n![The answer is a distance, which only an index on the stack can give](diagrams/stacks-revision-indices-not-values.jpg)\n\n```java\nDeque<Integer> st = new ArrayDeque<>();\nfor (int i = 0; i < a.length; i++) {\n    while (!st.isEmpty() && a[st.peek()] < a[i]) ans[st.pop()] = a[i];\n    st.push(i);\n}\n```\n\n- Push **indices**, not values, whenever the answer is a width, a distance or a\n  position.\n- `while`, not `if` — one arriving element can settle several waiting ones.\n- Pre-fill `ans` with `-1`; whatever is still on the stack at the end never\n  found an answer.\n- Flip the comparison to change the question: `<` next greater, `>` next\n  smaller, `<=` / `>=` decide how ties are treated.\n- Previous smaller is the same loop read at a different moment: pop until the\n  top is smaller, and the top that survives is the answer for `i`.\n\n## Why it is O(n)\n\n![The nested loop that is linear, against the scan it replaces](diagrams/stacks-revision-amortised.jpg)\n\n- Each index is pushed once and popped at most once: at most n pushes and n pops\n  over the whole run. One step can be expensive; the total cannot be.\n- Same amortised argument as the two-stack queue and the monotonic deque.\n- Space O(n) — a sorted input keeps every index on the stack.\n\n## Largest Rectangle in Histogram\n\n- Fix the shortest bar in the rectangle; its width runs from the previous\n  smaller bar to the next smaller bar.\n- One increasing stack gives both bounds at the moment of the pop: the bar\n  underneath is the left bound, the arriving bar is the right one.\n- `width = i - left - 1`, where `left` is `-1` when the stack empties.\n- Sentinel: run `i` to `h.length` inclusive with height 0. It flushes the stack\n  and removes the duplicate clean-up loop.\n\n## The other shapes\n\n- **RPN**: push operands, and on an operator pop two, combine, push. The second\n  pop is the left operand — `-` and `/` care.\n- **Min Stack**: store `{ value, min at or below }` in an `int[]`, or a second\n  stack pushed on `<=`. The `<=` matters for duplicate minima.\n- **Decode String**: on `[` push the count and the string so far; on `]` pop and\n  append the inner string that many times. `k = k * 10 + (c - '0')` for\n  multi-digit counts.\n- **Recursion to a loop**: push what the call would have remembered. Push the\n  child you want visited last, first.\n\n## The bugs\n\n- `pop` on an empty stack — put `!st.isEmpty()` first in the `&&`.\n- `if` where the loop needed `while`.\n- Values pushed where indices were needed.\n- Leftovers on the stack given no answer.\n- `<` versus `<=` decided by accident rather than by the question.\n- Operand order for `-` and `/`.\n- Push order reversed in an iterative traversal.\n\n## The API\n\n| Want | Write |\n|---|---|\n| A stack | `Deque<Integer> st = new ArrayDeque<>()` |\n| Top on / off | `st.push(x)` / `st.pop()` |\n| Look, or `null` | `st.peek()` |\n| Safe remove | `st.poll()` — `null` instead of throwing |\n| Pairs | `Deque<int[]> st` with `new int[] { a, b }` |\n| Fill an answer | `Arrays.fill(ans, -1)` |\n| Digit value | `c - '0'` |\n\n## Worth remembering\n\n- If the brute force is \"for each element, scan backwards until something beats\n  it\", the answer is a monotonic stack.\n- The pop is the moment both bounds are known. Do the work there, not later.\n";export{e as default};