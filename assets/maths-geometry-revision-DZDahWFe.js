var e="The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## The identities\n\n```java\nlong gcd(long a, long b) { return b == 0 ? a : gcd(b, a % b); }\nlong lcm(long a, long b) { return a / gcd(a, b) * b; }   // divide first\n```\n\n- `gcd(a, 0) == a`, so a zero argument needs no special case.\n- Reduce `(dy, dx)` by their gcd to get a slope with no division.\n- `n!` trailing zeroes = `n/5 + n/25 + n/125 + …` — count the 5s, the 2s are\n  never scarcer.\n\n## Modular arithmetic\n\n![Reducing after every step against reducing once at the end](diagrams/maths-geometry-revision-modular-steps.jpg)\n\n- Reduce **after every step**, never once at the end.\n- `(a + b) % m`, `(a × b) % m` distribute; subtraction needs `+ m` first;\n  division does not distribute at all.\n- 10⁹ + 7 is prime, fits an `int`, and its square fits a `long` — so a product of\n  two residues is safe in `long`.\n- `(long) a * b`, not `(long) (a * b)`. Java multiplies at the operand width.\n- Java's `%` follows the left operand's sign. `Math.floorMod` for a residue.\n- Divide by `a` modulo prime `p` = multiply by `modPow(a, p - 2, p)`.\n\n## Fast exponentiation\n\n```java\nlong modPow(long base, long exp, long m) {\n    long result = 1;\n    base %= m;\n    while (exp > 0) {\n        if ((exp & 1) == 1) result = result * base % m;\n        base = base * base % m;\n        exp >>= 1;\n    }\n    return result;\n}\n```\n\n- O(log e). One squaring per bit; multiply in only when the bit is set.\n\n## Primes and combinatorics\n\n- One number prime? Trial division while `f * f <= n`. Never `f <= Math.sqrt(n)`.\n- Primes below n? Sieve, crossing off from `p × p`, outer loop to `√n`.\n  O(n log log n).\n- `C(n, r) = C(n-1, r-1) + C(n-1, r)` — Pascal, addition only.\n- `C(n, r) = Π (n - r + i) / i` for i = 1..r. Every partial division is exact.\n  Use `r = min(r, n - r)`.\n\n## Geometry, in integers\n\n![The three things the sign of a cross product can tell you](diagrams/maths-geometry-revision-cross-sign.jpg)\n\n```java\nlong cross = (b.x - a.x) * (c.y - a.y) - (b.y - a.y) * (c.x - a.x);\n```\n\n| Sign | Meaning |\n|---|---|\n| positive | `c` left of `a→b`, anticlockwise |\n| negative | `c` right of `a→b`, clockwise |\n| zero | collinear |\n\n- `|cross|` is **twice** the triangle's area. Keep it doubled; halve last.\n- Shoelace: `2 × area = Σ (xᵢyᵢ₊₁ − xᵢ₊₁yᵢ)`, wrapping the last vertex to the\n  first. Sign gives the winding direction.\n- Segments cross when each straddles the other — `turn` differs at both ends —\n  plus four collinear-and-on-segment cases.\n- Rectangle overlap: `max(0, min(x2) - max(x1)) × max(0, min(y2) - max(y1))`.\n\n## Avoiding floating point\n\n| Instead of | Write |\n|---|---|\n| slope `dy / dx` | reduced pair `(dy/g, dx/g)`, sign-normalised |\n| `sqrt(dx² + dy²)` | `dx*dx + dy*dy` — ranks identically |\n| `area = b × h / 2` | twice the area, as an integer |\n| `a / b == c / d` | `a * d == c * b` in `long` |\n| `Math.pow(10, k)` | a `long` loop |\n\nIf a `double` is unavoidable, compare with `Math.abs(x - y) < 1e-9`, never `==`.\n\n## Costs\n\n- gcd and fast power O(log n) · primality and factorising O(√n) · sieve\n  O(n log log n) · `nCr` multiplicatively O(min(r, n − r)) · shoelace O(vertices)\n  · max points on a line O(n²).\n\n## The bugs\n\n- `int` overflow past 2.1 billion, silently.\n- `a * b / gcd` instead of `a / gcd * b`.\n- Negative remainder used as an index.\n- Slope as a `double` — vertical lines divide by zero, near-equal slopes collide.\n- `Math.abs(Integer.MIN_VALUE)` is still negative.\n- `(long) Math.pow(10, 18)` is not 10¹⁸.\n- Zero and negative inputs in a digit loop, and overflow while reversing an\n  integer.\n";export{e as default};