var e=Object.create,t=Object.defineProperty,n=Object.getOwnPropertyDescriptor,r=Object.getOwnPropertyNames,i=Object.getPrototypeOf,a=Object.prototype.hasOwnProperty,o=(e,t)=>()=>(t||(e((t={exports:{}}).exports,t),e=null),t.exports),s=(e,i,o,s)=>{if(i&&typeof i==`object`||typeof i==`function`)for(var c=r(i),l=0,u=c.length,d;l<u;l++)d=c[l],!a.call(e,d)&&d!==o&&t(e,d,{get:(e=>i[e]).bind(null,d),enumerable:!(s=n(i,d))||s.enumerable});return e},c=(n,r,a)=>(a=n==null?{}:e(i(n)),s(r||!n||!n.__esModule?t(a,`default`,{value:n,enumerable:!0}):a,n));(function(){let e=document.createElement(`link`).relList;if(e&&e.supports&&e.supports(`modulepreload`))return;for(let e of document.querySelectorAll(`link[rel="modulepreload"]`))n(e);new MutationObserver(e=>{for(let t of e)if(t.type===`childList`)for(let e of t.addedNodes)e.tagName===`LINK`&&e.rel===`modulepreload`&&n(e)}).observe(document,{childList:!0,subtree:!0});function t(e){let t={};return e.integrity&&(t.integrity=e.integrity),e.referrerPolicy&&(t.referrerPolicy=e.referrerPolicy),e.crossOrigin===`use-credentials`?t.credentials=`include`:e.crossOrigin===`anonymous`?t.credentials=`omit`:t.credentials=`same-origin`,t}function n(e){if(e.ep)return;e.ep=!0;let n=t(e);fetch(e.href,n)}})();var l=o((e=>{var t=Symbol.for(`react.transitional.element`),n=Symbol.for(`react.portal`),r=Symbol.for(`react.fragment`),i=Symbol.for(`react.strict_mode`),a=Symbol.for(`react.profiler`),o=Symbol.for(`react.consumer`),s=Symbol.for(`react.context`),c=Symbol.for(`react.forward_ref`),l=Symbol.for(`react.suspense`),u=Symbol.for(`react.memo`),d=Symbol.for(`react.lazy`),f=Symbol.for(`react.activity`),p=Symbol.iterator;function m(e){return typeof e!=`object`||!e?null:(e=p&&e[p]||e[`@@iterator`],typeof e==`function`?e:null)}var h={isMounted:function(){return!1},enqueueForceUpdate:function(){},enqueueReplaceState:function(){},enqueueSetState:function(){}},g=Object.assign,_={};function v(e,t,n){this.props=e,this.context=t,this.refs=_,this.updater=n||h}v.prototype.isReactComponent={},v.prototype.setState=function(e,t){if(typeof e!=`object`&&typeof e!=`function`&&e!=null)throw Error(`takes an object of state variables to update or a function which returns an object of state variables.`);this.updater.enqueueSetState(this,e,t,`setState`)},v.prototype.forceUpdate=function(e){this.updater.enqueueForceUpdate(this,e,`forceUpdate`)};function y(){}y.prototype=v.prototype;function b(e,t,n){this.props=e,this.context=t,this.refs=_,this.updater=n||h}var x=b.prototype=new y;x.constructor=b,g(x,v.prototype),x.isPureReactComponent=!0;var S=Array.isArray;function C(){}var w={H:null,A:null,T:null,S:null},T=Object.prototype.hasOwnProperty;function ee(e,n,r){var i=r.ref;return{$$typeof:t,type:e,key:n,ref:i===void 0?null:i,props:r}}function E(e,t){return ee(e.type,t,e.props)}function D(e){return typeof e==`object`&&!!e&&e.$$typeof===t}function te(e){var t={"=":`=0`,":":`=2`};return`$`+e.replace(/[=:]/g,function(e){return t[e]})}var O=/\/+/g;function ne(e,t){return typeof e==`object`&&e&&e.key!=null?te(``+e.key):t.toString(36)}function k(e){switch(e.status){case`fulfilled`:return e.value;case`rejected`:throw e.reason;default:switch(typeof e.status==`string`?e.then(C,C):(e.status=`pending`,e.then(function(t){e.status===`pending`&&(e.status=`fulfilled`,e.value=t)},function(t){e.status===`pending`&&(e.status=`rejected`,e.reason=t)})),e.status){case`fulfilled`:return e.value;case`rejected`:throw e.reason}}throw e}function re(e,r,i,a,o){var s=typeof e;(s===`undefined`||s===`boolean`)&&(e=null);var c=!1;if(e===null)c=!0;else switch(s){case`bigint`:case`string`:case`number`:c=!0;break;case`object`:switch(e.$$typeof){case t:case n:c=!0;break;case d:return c=e._init,re(c(e._payload),r,i,a,o)}}if(c)return o=o(e),c=a===``?`.`+ne(e,0):a,S(o)?(i=``,c!=null&&(i=c.replace(O,`$&/`)+`/`),re(o,r,i,``,function(e){return e})):o!=null&&(D(o)&&(o=E(o,i+(o.key==null||e&&e.key===o.key?``:(``+o.key).replace(O,`$&/`)+`/`)+c)),r.push(o)),1;c=0;var l=a===``?`.`:a+`:`;if(S(e))for(var u=0;u<e.length;u++)a=e[u],s=l+ne(a,u),c+=re(a,r,i,s,o);else if(u=m(e),typeof u==`function`)for(e=u.call(e),u=0;!(a=e.next()).done;)a=a.value,s=l+ne(a,u++),c+=re(a,r,i,s,o);else if(s===`object`){if(typeof e.then==`function`)return re(k(e),r,i,a,o);throw r=String(e),Error(`Objects are not valid as a React child (found: `+(r===`[object Object]`?`object with keys {`+Object.keys(e).join(`, `)+`}`:r)+`). If you meant to render a collection of children, use an array instead.`)}return c}function A(e,t,n){if(e==null)return e;var r=[],i=0;return re(e,r,``,``,function(e){return t.call(n,e,i++)}),r}function ie(e){if(e._status===-1){var t=e._result;t=t(),t.then(function(t){(e._status===0||e._status===-1)&&(e._status=1,e._result=t)},function(t){(e._status===0||e._status===-1)&&(e._status=2,e._result=t)}),e._status===-1&&(e._status=0,e._result=t)}if(e._status===1)return e._result.default;throw e._result}var j=typeof reportError==`function`?reportError:function(e){if(typeof window==`object`&&typeof window.ErrorEvent==`function`){var t=new window.ErrorEvent(`error`,{bubbles:!0,cancelable:!0,message:typeof e==`object`&&e&&typeof e.message==`string`?String(e.message):String(e),error:e});if(!window.dispatchEvent(t))return}else if(typeof process==`object`&&typeof process.emit==`function`){process.emit(`uncaughtException`,e);return}console.error(e)},M={map:A,forEach:function(e,t,n){A(e,function(){t.apply(this,arguments)},n)},count:function(e){var t=0;return A(e,function(){t++}),t},toArray:function(e){return A(e,function(e){return e})||[]},only:function(e){if(!D(e))throw Error(`React.Children.only expected to receive a single React element child.`);return e}};e.Activity=f,e.Children=M,e.Component=v,e.Fragment=r,e.Profiler=a,e.PureComponent=b,e.StrictMode=i,e.Suspense=l,e.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE=w,e.__COMPILER_RUNTIME={__proto__:null,c:function(e){return w.H.useMemoCache(e)}},e.cache=function(e){return function(){return e.apply(null,arguments)}},e.cacheSignal=function(){return null},e.cloneElement=function(e,t,n){if(e==null)throw Error(`The argument must be a React element, but you passed `+e+`.`);var r=g({},e.props),i=e.key;if(t!=null)for(a in t.key!==void 0&&(i=``+t.key),t)!T.call(t,a)||a===`key`||a===`__self`||a===`__source`||a===`ref`&&t.ref===void 0||(r[a]=t[a]);var a=arguments.length-2;if(a===1)r.children=n;else if(1<a){for(var o=Array(a),s=0;s<a;s++)o[s]=arguments[s+2];r.children=o}return ee(e.type,i,r)},e.createContext=function(e){return e={$$typeof:s,_currentValue:e,_currentValue2:e,_threadCount:0,Provider:null,Consumer:null},e.Provider=e,e.Consumer={$$typeof:o,_context:e},e},e.createElement=function(e,t,n){var r,i={},a=null;if(t!=null)for(r in t.key!==void 0&&(a=``+t.key),t)T.call(t,r)&&r!==`key`&&r!==`__self`&&r!==`__source`&&(i[r]=t[r]);var o=arguments.length-2;if(o===1)i.children=n;else if(1<o){for(var s=Array(o),c=0;c<o;c++)s[c]=arguments[c+2];i.children=s}if(e&&e.defaultProps)for(r in o=e.defaultProps,o)i[r]===void 0&&(i[r]=o[r]);return ee(e,a,i)},e.createRef=function(){return{current:null}},e.forwardRef=function(e){return{$$typeof:c,render:e}},e.isValidElement=D,e.lazy=function(e){return{$$typeof:d,_payload:{_status:-1,_result:e},_init:ie}},e.memo=function(e,t){return{$$typeof:u,type:e,compare:t===void 0?null:t}},e.startTransition=function(e){var t=w.T,n={};w.T=n;try{var r=e(),i=w.S;i!==null&&i(n,r),typeof r==`object`&&r&&typeof r.then==`function`&&r.then(C,j)}catch(e){j(e)}finally{t!==null&&n.types!==null&&(t.types=n.types),w.T=t}},e.unstable_useCacheRefresh=function(){return w.H.useCacheRefresh()},e.use=function(e){return w.H.use(e)},e.useActionState=function(e,t,n){return w.H.useActionState(e,t,n)},e.useCallback=function(e,t){return w.H.useCallback(e,t)},e.useContext=function(e){return w.H.useContext(e)},e.useDebugValue=function(){},e.useDeferredValue=function(e,t){return w.H.useDeferredValue(e,t)},e.useEffect=function(e,t){return w.H.useEffect(e,t)},e.useEffectEvent=function(e){return w.H.useEffectEvent(e)},e.useId=function(){return w.H.useId()},e.useImperativeHandle=function(e,t,n){return w.H.useImperativeHandle(e,t,n)},e.useInsertionEffect=function(e,t){return w.H.useInsertionEffect(e,t)},e.useLayoutEffect=function(e,t){return w.H.useLayoutEffect(e,t)},e.useMemo=function(e,t){return w.H.useMemo(e,t)},e.useOptimistic=function(e,t){return w.H.useOptimistic(e,t)},e.useReducer=function(e,t,n){return w.H.useReducer(e,t,n)},e.useRef=function(e){return w.H.useRef(e)},e.useState=function(e){return w.H.useState(e)},e.useSyncExternalStore=function(e,t,n){return w.H.useSyncExternalStore(e,t,n)},e.useTransition=function(){return w.H.useTransition()},e.version=`19.2.7`})),u=o(((e,t)=>{t.exports=l()})),d=o((e=>{function t(e,t){var n=e.length;e.push(t);a:for(;0<n;){var r=n-1>>>1,a=e[r];if(0<i(a,t))e[r]=t,e[n]=a,n=r;else break a}}function n(e){return e.length===0?null:e[0]}function r(e){if(e.length===0)return null;var t=e[0],n=e.pop();if(n!==t){e[0]=n;a:for(var r=0,a=e.length,o=a>>>1;r<o;){var s=2*(r+1)-1,c=e[s],l=s+1,u=e[l];if(0>i(c,n))l<a&&0>i(u,c)?(e[r]=u,e[l]=n,r=l):(e[r]=c,e[s]=n,r=s);else if(l<a&&0>i(u,n))e[r]=u,e[l]=n,r=l;else break a}}return t}function i(e,t){var n=e.sortIndex-t.sortIndex;return n===0?e.id-t.id:n}if(e.unstable_now=void 0,typeof performance==`object`&&typeof performance.now==`function`){var a=performance;e.unstable_now=function(){return a.now()}}else{var o=Date,s=o.now();e.unstable_now=function(){return o.now()-s}}var c=[],l=[],u=1,d=null,f=3,p=!1,m=!1,h=!1,g=!1,_=typeof setTimeout==`function`?setTimeout:null,v=typeof clearTimeout==`function`?clearTimeout:null,y=typeof setImmediate<`u`?setImmediate:null;function b(e){for(var i=n(l);i!==null;){if(i.callback===null)r(l);else if(i.startTime<=e)r(l),i.sortIndex=i.expirationTime,t(c,i);else break;i=n(l)}}function x(e){if(h=!1,b(e),!m)if(n(c)!==null)m=!0,S||(S=!0,D());else{var t=n(l);t!==null&&ne(x,t.startTime-e)}}var S=!1,C=-1,w=5,T=-1;function ee(){return g?!0:!(e.unstable_now()-T<w)}function E(){if(g=!1,S){var t=e.unstable_now();T=t;var i=!0;try{a:{m=!1,h&&(h=!1,v(C),C=-1),p=!0;var a=f;try{b:{for(b(t),d=n(c);d!==null&&!(d.expirationTime>t&&ee());){var o=d.callback;if(typeof o==`function`){d.callback=null,f=d.priorityLevel;var s=o(d.expirationTime<=t);if(t=e.unstable_now(),typeof s==`function`){d.callback=s,b(t),i=!0;break b}d===n(c)&&r(c),b(t)}else r(c);d=n(c)}if(d!==null)i=!0;else{var u=n(l);u!==null&&ne(x,u.startTime-t),i=!1}}break a}finally{d=null,f=a,p=!1}i=void 0}}finally{i?D():S=!1}}}var D;if(typeof y==`function`)D=function(){y(E)};else if(typeof MessageChannel<`u`){var te=new MessageChannel,O=te.port2;te.port1.onmessage=E,D=function(){O.postMessage(null)}}else D=function(){_(E,0)};function ne(t,n){C=_(function(){t(e.unstable_now())},n)}e.unstable_IdlePriority=5,e.unstable_ImmediatePriority=1,e.unstable_LowPriority=4,e.unstable_NormalPriority=3,e.unstable_Profiling=null,e.unstable_UserBlockingPriority=2,e.unstable_cancelCallback=function(e){e.callback=null},e.unstable_forceFrameRate=function(e){0>e||125<e?console.error(`forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported`):w=0<e?Math.floor(1e3/e):5},e.unstable_getCurrentPriorityLevel=function(){return f},e.unstable_next=function(e){switch(f){case 1:case 2:case 3:var t=3;break;default:t=f}var n=f;f=t;try{return e()}finally{f=n}},e.unstable_requestPaint=function(){g=!0},e.unstable_runWithPriority=function(e,t){switch(e){case 1:case 2:case 3:case 4:case 5:break;default:e=3}var n=f;f=e;try{return t()}finally{f=n}},e.unstable_scheduleCallback=function(r,i,a){var o=e.unstable_now();switch(typeof a==`object`&&a?(a=a.delay,a=typeof a==`number`&&0<a?o+a:o):a=o,r){case 1:var s=-1;break;case 2:s=250;break;case 5:s=1073741823;break;case 4:s=1e4;break;default:s=5e3}return s=a+s,r={id:u++,callback:i,priorityLevel:r,startTime:a,expirationTime:s,sortIndex:-1},a>o?(r.sortIndex=a,t(l,r),n(c)===null&&r===n(l)&&(h?(v(C),C=-1):h=!0,ne(x,a-o))):(r.sortIndex=s,t(c,r),m||p||(m=!0,S||(S=!0,D()))),r},e.unstable_shouldYield=ee,e.unstable_wrapCallback=function(e){var t=f;return function(){var n=f;f=t;try{return e.apply(this,arguments)}finally{f=n}}}})),f=o(((e,t)=>{t.exports=d()})),p=o((e=>{var t=u();function n(e){var t=`https://react.dev/errors/`+e;if(1<arguments.length){t+=`?args[]=`+encodeURIComponent(arguments[1]);for(var n=2;n<arguments.length;n++)t+=`&args[]=`+encodeURIComponent(arguments[n])}return`Minified React error #`+e+`; visit `+t+` for the full message or use the non-minified dev environment for full errors and additional helpful warnings.`}function r(){}var i={d:{f:r,r:function(){throw Error(n(522))},D:r,C:r,L:r,m:r,X:r,S:r,M:r},p:0,findDOMNode:null},a=Symbol.for(`react.portal`);function o(e,t,n){var r=3<arguments.length&&arguments[3]!==void 0?arguments[3]:null;return{$$typeof:a,key:r==null?null:``+r,children:e,containerInfo:t,implementation:n}}var s=t.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;function c(e,t){if(e===`font`)return``;if(typeof t==`string`)return t===`use-credentials`?t:``}e.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE=i,e.createPortal=function(e,t){var r=2<arguments.length&&arguments[2]!==void 0?arguments[2]:null;if(!t||t.nodeType!==1&&t.nodeType!==9&&t.nodeType!==11)throw Error(n(299));return o(e,t,null,r)},e.flushSync=function(e){var t=s.T,n=i.p;try{if(s.T=null,i.p=2,e)return e()}finally{s.T=t,i.p=n,i.d.f()}},e.preconnect=function(e,t){typeof e==`string`&&(t?(t=t.crossOrigin,t=typeof t==`string`?t===`use-credentials`?t:``:void 0):t=null,i.d.C(e,t))},e.prefetchDNS=function(e){typeof e==`string`&&i.d.D(e)},e.preinit=function(e,t){if(typeof e==`string`&&t&&typeof t.as==`string`){var n=t.as,r=c(n,t.crossOrigin),a=typeof t.integrity==`string`?t.integrity:void 0,o=typeof t.fetchPriority==`string`?t.fetchPriority:void 0;n===`style`?i.d.S(e,typeof t.precedence==`string`?t.precedence:void 0,{crossOrigin:r,integrity:a,fetchPriority:o}):n===`script`&&i.d.X(e,{crossOrigin:r,integrity:a,fetchPriority:o,nonce:typeof t.nonce==`string`?t.nonce:void 0})}},e.preinitModule=function(e,t){if(typeof e==`string`)if(typeof t==`object`&&t){if(t.as==null||t.as===`script`){var n=c(t.as,t.crossOrigin);i.d.M(e,{crossOrigin:n,integrity:typeof t.integrity==`string`?t.integrity:void 0,nonce:typeof t.nonce==`string`?t.nonce:void 0})}}else t??i.d.M(e)},e.preload=function(e,t){if(typeof e==`string`&&typeof t==`object`&&t&&typeof t.as==`string`){var n=t.as,r=c(n,t.crossOrigin);i.d.L(e,n,{crossOrigin:r,integrity:typeof t.integrity==`string`?t.integrity:void 0,nonce:typeof t.nonce==`string`?t.nonce:void 0,type:typeof t.type==`string`?t.type:void 0,fetchPriority:typeof t.fetchPriority==`string`?t.fetchPriority:void 0,referrerPolicy:typeof t.referrerPolicy==`string`?t.referrerPolicy:void 0,imageSrcSet:typeof t.imageSrcSet==`string`?t.imageSrcSet:void 0,imageSizes:typeof t.imageSizes==`string`?t.imageSizes:void 0,media:typeof t.media==`string`?t.media:void 0})}},e.preloadModule=function(e,t){if(typeof e==`string`)if(t){var n=c(t.as,t.crossOrigin);i.d.m(e,{as:typeof t.as==`string`&&t.as!==`script`?t.as:void 0,crossOrigin:n,integrity:typeof t.integrity==`string`?t.integrity:void 0})}else i.d.m(e)},e.requestFormReset=function(e){i.d.r(e)},e.unstable_batchedUpdates=function(e,t){return e(t)},e.useFormState=function(e,t,n){return s.H.useFormState(e,t,n)},e.useFormStatus=function(){return s.H.useHostTransitionStatus()},e.version=`19.2.7`})),m=o(((e,t)=>{function n(){if(!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__>`u`||typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE!=`function`))try{__REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(n)}catch(e){console.error(e)}}n(),t.exports=p()})),h=o((e=>{var t=f(),n=u(),r=m();function i(e){var t=`https://react.dev/errors/`+e;if(1<arguments.length){t+=`?args[]=`+encodeURIComponent(arguments[1]);for(var n=2;n<arguments.length;n++)t+=`&args[]=`+encodeURIComponent(arguments[n])}return`Minified React error #`+e+`; visit `+t+` for the full message or use the non-minified dev environment for full errors and additional helpful warnings.`}function a(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11)}function o(e){var t=e,n=e;if(e.alternate)for(;t.return;)t=t.return;else{e=t;do t=e,t.flags&4098&&(n=t.return),e=t.return;while(e)}return t.tag===3?n:null}function s(e){if(e.tag===13){var t=e.memoizedState;if(t===null&&(e=e.alternate,e!==null&&(t=e.memoizedState)),t!==null)return t.dehydrated}return null}function c(e){if(e.tag===31){var t=e.memoizedState;if(t===null&&(e=e.alternate,e!==null&&(t=e.memoizedState)),t!==null)return t.dehydrated}return null}function l(e){if(o(e)!==e)throw Error(i(188))}function d(e){var t=e.alternate;if(!t){if(t=o(e),t===null)throw Error(i(188));return t===e?e:null}for(var n=e,r=t;;){var a=n.return;if(a===null)break;var s=a.alternate;if(s===null){if(r=a.return,r!==null){n=r;continue}break}if(a.child===s.child){for(s=a.child;s;){if(s===n)return l(a),e;if(s===r)return l(a),t;s=s.sibling}throw Error(i(188))}if(n.return!==r.return)n=a,r=s;else{for(var c=!1,u=a.child;u;){if(u===n){c=!0,n=a,r=s;break}if(u===r){c=!0,r=a,n=s;break}u=u.sibling}if(!c){for(u=s.child;u;){if(u===n){c=!0,n=s,r=a;break}if(u===r){c=!0,r=s,n=a;break}u=u.sibling}if(!c)throw Error(i(189))}}if(n.alternate!==r)throw Error(i(190))}if(n.tag!==3)throw Error(i(188));return n.stateNode.current===n?e:t}function p(e){var t=e.tag;if(t===5||t===26||t===27||t===6)return e;for(e=e.child;e!==null;){if(t=p(e),t!==null)return t;e=e.sibling}return null}var h=Object.assign,g=Symbol.for(`react.element`),_=Symbol.for(`react.transitional.element`),v=Symbol.for(`react.portal`),y=Symbol.for(`react.fragment`),b=Symbol.for(`react.strict_mode`),x=Symbol.for(`react.profiler`),S=Symbol.for(`react.consumer`),C=Symbol.for(`react.context`),w=Symbol.for(`react.forward_ref`),T=Symbol.for(`react.suspense`),ee=Symbol.for(`react.suspense_list`),E=Symbol.for(`react.memo`),D=Symbol.for(`react.lazy`),te=Symbol.for(`react.activity`),O=Symbol.for(`react.memo_cache_sentinel`),ne=Symbol.iterator;function k(e){return typeof e!=`object`||!e?null:(e=ne&&e[ne]||e[`@@iterator`],typeof e==`function`?e:null)}var re=Symbol.for(`react.client.reference`);function A(e){if(e==null)return null;if(typeof e==`function`)return e.$$typeof===re?null:e.displayName||e.name||null;if(typeof e==`string`)return e;switch(e){case y:return`Fragment`;case x:return`Profiler`;case b:return`StrictMode`;case T:return`Suspense`;case ee:return`SuspenseList`;case te:return`Activity`}if(typeof e==`object`)switch(e.$$typeof){case v:return`Portal`;case C:return e.displayName||`Context`;case S:return(e._context.displayName||`Context`)+`.Consumer`;case w:var t=e.render;return e=e.displayName,e||=(e=t.displayName||t.name||``,e===``?`ForwardRef`:`ForwardRef(`+e+`)`),e;case E:return t=e.displayName||null,t===null?A(e.type)||`Memo`:t;case D:t=e._payload,e=e._init;try{return A(e(t))}catch{}}return null}var ie=Array.isArray,j=n.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE,M=r.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE,ae={pending:!1,data:null,method:null,action:null},oe=[],se=-1;function ce(e){return{current:e}}function le(e){0>se||(e.current=oe[se],oe[se]=null,se--)}function N(e,t){se++,oe[se]=e.current,e.current=t}var ue=ce(null),de=ce(null),fe=ce(null),pe=ce(null);function me(e,t){switch(N(fe,t),N(de,e),N(ue,null),t.nodeType){case 9:case 11:e=(e=t.documentElement)&&(e=e.namespaceURI)?Vd(e):0;break;default:if(e=t.tagName,t=t.namespaceURI)t=Vd(t),e=Hd(t,e);else switch(e){case`svg`:e=1;break;case`math`:e=2;break;default:e=0}}le(ue),N(ue,e)}function he(){le(ue),le(de),le(fe)}function ge(e){e.memoizedState!==null&&N(pe,e);var t=ue.current,n=Hd(t,e.type);t!==n&&(N(de,e),N(ue,n))}function _e(e){de.current===e&&(le(ue),le(de)),pe.current===e&&(le(pe),Qf._currentValue=ae)}var ve,ye;function be(e){if(ve===void 0)try{throw Error()}catch(e){var t=e.stack.trim().match(/\n( *(at )?)/);ve=t&&t[1]||``,ye=-1<e.stack.indexOf(`
    at`)?` (<anonymous>)`:-1<e.stack.indexOf(`@`)?`@unknown:0:0`:``}return`
`+ve+e+ye}var xe=!1;function Se(e,t){if(!e||xe)return``;xe=!0;var n=Error.prepareStackTrace;Error.prepareStackTrace=void 0;try{var r={DetermineComponentFrameRoot:function(){try{if(t){var n=function(){throw Error()};if(Object.defineProperty(n.prototype,"props",{set:function(){throw Error()}}),typeof Reflect==`object`&&Reflect.construct){try{Reflect.construct(n,[])}catch(e){var r=e}Reflect.construct(e,[],n)}else{try{n.call()}catch(e){r=e}e.call(n.prototype)}}else{try{throw Error()}catch(e){r=e}(n=e())&&typeof n.catch==`function`&&n.catch(function(){})}}catch(e){if(e&&r&&typeof e.stack==`string`)return[e.stack,r.stack]}return[null,null]}};r.DetermineComponentFrameRoot.displayName=`DetermineComponentFrameRoot`;var i=Object.getOwnPropertyDescriptor(r.DetermineComponentFrameRoot,`name`);i&&i.configurable&&Object.defineProperty(r.DetermineComponentFrameRoot,"name",{value:`DetermineComponentFrameRoot`});var a=r.DetermineComponentFrameRoot(),o=a[0],s=a[1];if(o&&s){var c=o.split(`
`),l=s.split(`
`);for(i=r=0;r<c.length&&!c[r].includes(`DetermineComponentFrameRoot`);)r++;for(;i<l.length&&!l[i].includes(`DetermineComponentFrameRoot`);)i++;if(r===c.length||i===l.length)for(r=c.length-1,i=l.length-1;1<=r&&0<=i&&c[r]!==l[i];)i--;for(;1<=r&&0<=i;r--,i--)if(c[r]!==l[i]){if(r!==1||i!==1)do if(r--,i--,0>i||c[r]!==l[i]){var u=`
`+c[r].replace(` at new `,` at `);return e.displayName&&u.includes(`<anonymous>`)&&(u=u.replace(`<anonymous>`,e.displayName)),u}while(1<=r&&0<=i);break}}}finally{xe=!1,Error.prepareStackTrace=n}return(n=e?e.displayName||e.name:``)?be(n):``}function Ce(e,t){switch(e.tag){case 26:case 27:case 5:return be(e.type);case 16:return be(`Lazy`);case 13:return e.child!==t&&t!==null?be(`Suspense Fallback`):be(`Suspense`);case 19:return be(`SuspenseList`);case 0:case 15:return Se(e.type,!1);case 11:return Se(e.type.render,!1);case 1:return Se(e.type,!0);case 31:return be(`Activity`);default:return``}}function we(e){try{var t=``,n=null;do t+=Ce(e,n),n=e,e=e.return;while(e);return t}catch(e){return`
Error generating stack: `+e.message+`
`+e.stack}}var Te=Object.prototype.hasOwnProperty,Ee=t.unstable_scheduleCallback,De=t.unstable_cancelCallback,Oe=t.unstable_shouldYield,ke=t.unstable_requestPaint,Ae=t.unstable_now,je=t.unstable_getCurrentPriorityLevel,Me=t.unstable_ImmediatePriority,Ne=t.unstable_UserBlockingPriority,Pe=t.unstable_NormalPriority,Fe=t.unstable_LowPriority,Ie=t.unstable_IdlePriority,Le=t.log,Re=t.unstable_setDisableYieldValue,ze=null,Be=null;function Ve(e){if(typeof Le==`function`&&Re(e),Be&&typeof Be.setStrictMode==`function`)try{Be.setStrictMode(ze,e)}catch{}}var P=Math.clz32?Math.clz32:F,He=Math.log,Ue=Math.LN2;function F(e){return e>>>=0,e===0?32:31-(He(e)/Ue|0)|0}var We=256,Ge=262144,Ke=4194304;function qe(e){var t=e&42;if(t!==0)return t;switch(e&-e){case 1:return 1;case 2:return 2;case 4:return 4;case 8:return 8;case 16:return 16;case 32:return 32;case 64:return 64;case 128:return 128;case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:return e&261888;case 262144:case 524288:case 1048576:case 2097152:return e&3932160;case 4194304:case 8388608:case 16777216:case 33554432:return e&62914560;case 67108864:return 67108864;case 134217728:return 134217728;case 268435456:return 268435456;case 536870912:return 536870912;case 1073741824:return 0;default:return e}}function Je(e,t,n){var r=e.pendingLanes;if(r===0)return 0;var i=0,a=e.suspendedLanes,o=e.pingedLanes;e=e.warmLanes;var s=r&134217727;return s===0?(s=r&~a,s===0?o===0?n||(n=r&~e,n!==0&&(i=qe(n))):i=qe(o):i=qe(s)):(r=s&~a,r===0?(o&=s,o===0?n||(n=s&~e,n!==0&&(i=qe(n))):i=qe(o)):i=qe(r)),i===0?0:t!==0&&t!==i&&(t&a)===0&&(a=i&-i,n=t&-t,a>=n||a===32&&n&4194048)?t:i}function Ye(e,t){return(e.pendingLanes&~(e.suspendedLanes&~e.pingedLanes)&t)===0}function Xe(e,t){switch(e){case 1:case 2:case 4:case 8:case 64:return t+250;case 16:case 32:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return t+5e3;case 4194304:case 8388608:case 16777216:case 33554432:return-1;case 67108864:case 134217728:case 268435456:case 536870912:case 1073741824:return-1;default:return-1}}function Ze(){var e=Ke;return Ke<<=1,!(Ke&62914560)&&(Ke=4194304),e}function Qe(e){for(var t=[],n=0;31>n;n++)t.push(e);return t}function $e(e,t){e.pendingLanes|=t,t!==268435456&&(e.suspendedLanes=0,e.pingedLanes=0,e.warmLanes=0)}function et(e,t,n,r,i,a){var o=e.pendingLanes;e.pendingLanes=n,e.suspendedLanes=0,e.pingedLanes=0,e.warmLanes=0,e.expiredLanes&=n,e.entangledLanes&=n,e.errorRecoveryDisabledLanes&=n,e.shellSuspendCounter=0;var s=e.entanglements,c=e.expirationTimes,l=e.hiddenUpdates;for(n=o&~n;0<n;){var u=31-P(n),d=1<<u;s[u]=0,c[u]=-1;var f=l[u];if(f!==null)for(l[u]=null,u=0;u<f.length;u++){var p=f[u];p!==null&&(p.lane&=-536870913)}n&=~d}r!==0&&tt(e,r,0),a!==0&&i===0&&e.tag!==0&&(e.suspendedLanes|=a&~(o&~t))}function tt(e,t,n){e.pendingLanes|=t,e.suspendedLanes&=~t;var r=31-P(t);e.entangledLanes|=t,e.entanglements[r]=e.entanglements[r]|1073741824|n&261930}function nt(e,t){var n=e.entangledLanes|=t;for(e=e.entanglements;n;){var r=31-P(n),i=1<<r;i&t|e[r]&t&&(e[r]|=t),n&=~i}}function rt(e,t){var n=t&-t;return n=n&42?1:it(n),(n&(e.suspendedLanes|t))===0?n:0}function it(e){switch(e){case 2:e=1;break;case 8:e=4;break;case 32:e=16;break;case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:case 4194304:case 8388608:case 16777216:case 33554432:e=128;break;case 268435456:e=134217728;break;default:e=0}return e}function at(e){return e&=-e,2<e?8<e?e&134217727?32:268435456:8:2}function ot(){var e=M.p;return e===0?(e=window.event,e===void 0?32:mp(e.type)):e}function st(e,t){var n=M.p;try{return M.p=e,t()}finally{M.p=n}}var I=Math.random().toString(36).slice(2),ct=`__reactFiber$`+I,lt=`__reactProps$`+I,ut=`__reactContainer$`+I,dt=`__reactEvents$`+I,ft=`__reactListeners$`+I,pt=`__reactHandles$`+I,mt=`__reactResources$`+I,ht=`__reactMarker$`+I;function gt(e){delete e[ct],delete e[lt],delete e[dt],delete e[ft],delete e[pt]}function _t(e){var t=e[ct];if(t)return t;for(var n=e.parentNode;n;){if(t=n[ut]||n[ct]){if(n=t.alternate,t.child!==null||n!==null&&n.child!==null)for(e=df(e);e!==null;){if(n=e[ct])return n;e=df(e)}return t}e=n,n=e.parentNode}return null}function vt(e){if(e=e[ct]||e[ut]){var t=e.tag;if(t===5||t===6||t===13||t===31||t===26||t===27||t===3)return e}return null}function yt(e){var t=e.tag;if(t===5||t===26||t===27||t===6)return e.stateNode;throw Error(i(33))}function bt(e){var t=e[mt];return t||=e[mt]={hoistableStyles:new Map,hoistableScripts:new Map},t}function xt(e){e[ht]=!0}var St=new Set,Ct={};function wt(e,t){Tt(e,t),Tt(e+`Capture`,t)}function Tt(e,t){for(Ct[e]=t,e=0;e<t.length;e++)St.add(t[e])}var Et=RegExp(`^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$`),Dt={},Ot={};function kt(e){return Te.call(Ot,e)?!0:Te.call(Dt,e)?!1:Et.test(e)?Ot[e]=!0:(Dt[e]=!0,!1)}function At(e,t,n){if(kt(t))if(n===null)e.removeAttribute(t);else{switch(typeof n){case`undefined`:case`function`:case`symbol`:e.removeAttribute(t);return;case`boolean`:var r=t.toLowerCase().slice(0,5);if(r!==`data-`&&r!==`aria-`){e.removeAttribute(t);return}}e.setAttribute(t,``+n)}}function jt(e,t,n){if(n===null)e.removeAttribute(t);else{switch(typeof n){case`undefined`:case`function`:case`symbol`:case`boolean`:e.removeAttribute(t);return}e.setAttribute(t,``+n)}}function Mt(e,t,n,r){if(r===null)e.removeAttribute(n);else{switch(typeof r){case`undefined`:case`function`:case`symbol`:case`boolean`:e.removeAttribute(n);return}e.setAttributeNS(t,n,``+r)}}function Nt(e){switch(typeof e){case`bigint`:case`boolean`:case`number`:case`string`:case`undefined`:return e;case`object`:return e;default:return``}}function Pt(e){var t=e.type;return(e=e.nodeName)&&e.toLowerCase()===`input`&&(t===`checkbox`||t===`radio`)}function Ft(e,t,n){var r=Object.getOwnPropertyDescriptor(e.constructor.prototype,t);if(!e.hasOwnProperty(t)&&r!==void 0&&typeof r.get==`function`&&typeof r.set==`function`){var i=r.get,a=r.set;return Object.defineProperty(e,t,{configurable:!0,get:function(){return i.call(this)},set:function(e){n=``+e,a.call(this,e)}}),Object.defineProperty(e,t,{enumerable:r.enumerable}),{getValue:function(){return n},setValue:function(e){n=``+e},stopTracking:function(){e._valueTracker=null,delete e[t]}}}}function It(e){if(!e._valueTracker){var t=Pt(e)?`checked`:`value`;e._valueTracker=Ft(e,t,``+e[t])}}function Lt(e){if(!e)return!1;var t=e._valueTracker;if(!t)return!0;var n=t.getValue(),r=``;return e&&(r=Pt(e)?e.checked?`true`:`false`:e.value),e=r,e===n?!1:(t.setValue(e),!0)}function Rt(e){if(e||=typeof document<`u`?document:void 0,e===void 0)return null;try{return e.activeElement||e.body}catch{return e.body}}var zt=/[\n"\\]/g;function Bt(e){return e.replace(zt,function(e){return`\\`+e.charCodeAt(0).toString(16)+` `})}function Vt(e,t,n,r,i,a,o,s){e.name=``,o!=null&&typeof o!=`function`&&typeof o!=`symbol`&&typeof o!=`boolean`?e.type=o:e.removeAttribute(`type`),t==null?o!==`submit`&&o!==`reset`||e.removeAttribute(`value`):o===`number`?(t===0&&e.value===``||e.value!=t)&&(e.value=``+Nt(t)):e.value!==``+Nt(t)&&(e.value=``+Nt(t)),t==null?n==null?r!=null&&e.removeAttribute(`value`):Ut(e,o,Nt(n)):Ut(e,o,Nt(t)),i==null&&a!=null&&(e.defaultChecked=!!a),i!=null&&(e.checked=i&&typeof i!=`function`&&typeof i!=`symbol`),s!=null&&typeof s!=`function`&&typeof s!=`symbol`&&typeof s!=`boolean`?e.name=``+Nt(s):e.removeAttribute(`name`)}function Ht(e,t,n,r,i,a,o,s){if(a!=null&&typeof a!=`function`&&typeof a!=`symbol`&&typeof a!=`boolean`&&(e.type=a),t!=null||n!=null){if(!(a!==`submit`&&a!==`reset`||t!=null)){It(e);return}n=n==null?``:``+Nt(n),t=t==null?n:``+Nt(t),s||t===e.value||(e.value=t),e.defaultValue=t}r??=i,r=typeof r!=`function`&&typeof r!=`symbol`&&!!r,e.checked=s?e.checked:!!r,e.defaultChecked=!!r,o!=null&&typeof o!=`function`&&typeof o!=`symbol`&&typeof o!=`boolean`&&(e.name=o),It(e)}function Ut(e,t,n){t===`number`&&Rt(e.ownerDocument)===e||e.defaultValue===``+n||(e.defaultValue=``+n)}function Wt(e,t,n,r){if(e=e.options,t){t={};for(var i=0;i<n.length;i++)t[`$`+n[i]]=!0;for(n=0;n<e.length;n++)i=t.hasOwnProperty(`$`+e[n].value),e[n].selected!==i&&(e[n].selected=i),i&&r&&(e[n].defaultSelected=!0)}else{for(n=``+Nt(n),t=null,i=0;i<e.length;i++){if(e[i].value===n){e[i].selected=!0,r&&(e[i].defaultSelected=!0);return}t!==null||e[i].disabled||(t=e[i])}t!==null&&(t.selected=!0)}}function Gt(e,t,n){if(t!=null&&(t=``+Nt(t),t!==e.value&&(e.value=t),n==null)){e.defaultValue!==t&&(e.defaultValue=t);return}e.defaultValue=n==null?``:``+Nt(n)}function Kt(e,t,n,r){if(t==null){if(r!=null){if(n!=null)throw Error(i(92));if(ie(r)){if(1<r.length)throw Error(i(93));r=r[0]}n=r}n??=``,t=n}n=Nt(t),e.defaultValue=n,r=e.textContent,r===n&&r!==``&&r!==null&&(e.value=r),It(e)}function qt(e,t){if(t){var n=e.firstChild;if(n&&n===e.lastChild&&n.nodeType===3){n.nodeValue=t;return}}e.textContent=t}var Jt=new Set(`animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp`.split(` `));function Yt(e,t,n){var r=t.indexOf(`--`)===0;n==null||typeof n==`boolean`||n===``?r?e.setProperty(t,``):t===`float`?e.cssFloat=``:e[t]=``:r?e.setProperty(t,n):typeof n!=`number`||n===0||Jt.has(t)?t===`float`?e.cssFloat=n:e[t]=(``+n).trim():e[t]=n+`px`}function Xt(e,t,n){if(t!=null&&typeof t!=`object`)throw Error(i(62));if(e=e.style,n!=null){for(var r in n)!n.hasOwnProperty(r)||t!=null&&t.hasOwnProperty(r)||(r.indexOf(`--`)===0?e.setProperty(r,``):r===`float`?e.cssFloat=``:e[r]=``);for(var a in t)r=t[a],t.hasOwnProperty(a)&&n[a]!==r&&Yt(e,a,r)}else for(var o in t)t.hasOwnProperty(o)&&Yt(e,o,t[o])}function Zt(e){if(e.indexOf(`-`)===-1)return!1;switch(e){case`annotation-xml`:case`color-profile`:case`font-face`:case`font-face-src`:case`font-face-uri`:case`font-face-format`:case`font-face-name`:case`missing-glyph`:return!1;default:return!0}}var Qt=new Map([[`acceptCharset`,`accept-charset`],[`htmlFor`,`for`],[`httpEquiv`,`http-equiv`],[`crossOrigin`,`crossorigin`],[`accentHeight`,`accent-height`],[`alignmentBaseline`,`alignment-baseline`],[`arabicForm`,`arabic-form`],[`baselineShift`,`baseline-shift`],[`capHeight`,`cap-height`],[`clipPath`,`clip-path`],[`clipRule`,`clip-rule`],[`colorInterpolation`,`color-interpolation`],[`colorInterpolationFilters`,`color-interpolation-filters`],[`colorProfile`,`color-profile`],[`colorRendering`,`color-rendering`],[`dominantBaseline`,`dominant-baseline`],[`enableBackground`,`enable-background`],[`fillOpacity`,`fill-opacity`],[`fillRule`,`fill-rule`],[`floodColor`,`flood-color`],[`floodOpacity`,`flood-opacity`],[`fontFamily`,`font-family`],[`fontSize`,`font-size`],[`fontSizeAdjust`,`font-size-adjust`],[`fontStretch`,`font-stretch`],[`fontStyle`,`font-style`],[`fontVariant`,`font-variant`],[`fontWeight`,`font-weight`],[`glyphName`,`glyph-name`],[`glyphOrientationHorizontal`,`glyph-orientation-horizontal`],[`glyphOrientationVertical`,`glyph-orientation-vertical`],[`horizAdvX`,`horiz-adv-x`],[`horizOriginX`,`horiz-origin-x`],[`imageRendering`,`image-rendering`],[`letterSpacing`,`letter-spacing`],[`lightingColor`,`lighting-color`],[`markerEnd`,`marker-end`],[`markerMid`,`marker-mid`],[`markerStart`,`marker-start`],[`overlinePosition`,`overline-position`],[`overlineThickness`,`overline-thickness`],[`paintOrder`,`paint-order`],[`panose-1`,`panose-1`],[`pointerEvents`,`pointer-events`],[`renderingIntent`,`rendering-intent`],[`shapeRendering`,`shape-rendering`],[`stopColor`,`stop-color`],[`stopOpacity`,`stop-opacity`],[`strikethroughPosition`,`strikethrough-position`],[`strikethroughThickness`,`strikethrough-thickness`],[`strokeDasharray`,`stroke-dasharray`],[`strokeDashoffset`,`stroke-dashoffset`],[`strokeLinecap`,`stroke-linecap`],[`strokeLinejoin`,`stroke-linejoin`],[`strokeMiterlimit`,`stroke-miterlimit`],[`strokeOpacity`,`stroke-opacity`],[`strokeWidth`,`stroke-width`],[`textAnchor`,`text-anchor`],[`textDecoration`,`text-decoration`],[`textRendering`,`text-rendering`],[`transformOrigin`,`transform-origin`],[`underlinePosition`,`underline-position`],[`underlineThickness`,`underline-thickness`],[`unicodeBidi`,`unicode-bidi`],[`unicodeRange`,`unicode-range`],[`unitsPerEm`,`units-per-em`],[`vAlphabetic`,`v-alphabetic`],[`vHanging`,`v-hanging`],[`vIdeographic`,`v-ideographic`],[`vMathematical`,`v-mathematical`],[`vectorEffect`,`vector-effect`],[`vertAdvY`,`vert-adv-y`],[`vertOriginX`,`vert-origin-x`],[`vertOriginY`,`vert-origin-y`],[`wordSpacing`,`word-spacing`],[`writingMode`,`writing-mode`],[`xmlnsXlink`,`xmlns:xlink`],[`xHeight`,`x-height`]]),$t=/^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;function en(e){return $t.test(``+e)?`javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')`:e}function tn(){}var nn=null;function rn(e){return e=e.target||e.srcElement||window,e.correspondingUseElement&&(e=e.correspondingUseElement),e.nodeType===3?e.parentNode:e}var an=null,on=null;function sn(e){var t=vt(e);if(t&&(e=t.stateNode)){var n=e[lt]||null;a:switch(e=t.stateNode,t.type){case`input`:if(Vt(e,n.value,n.defaultValue,n.defaultValue,n.checked,n.defaultChecked,n.type,n.name),t=n.name,n.type===`radio`&&t!=null){for(n=e;n.parentNode;)n=n.parentNode;for(n=n.querySelectorAll(`input[name="`+Bt(``+t)+`"][type="radio"]`),t=0;t<n.length;t++){var r=n[t];if(r!==e&&r.form===e.form){var a=r[lt]||null;if(!a)throw Error(i(90));Vt(r,a.value,a.defaultValue,a.defaultValue,a.checked,a.defaultChecked,a.type,a.name)}}for(t=0;t<n.length;t++)r=n[t],r.form===e.form&&Lt(r)}break a;case`textarea`:Gt(e,n.value,n.defaultValue);break a;case`select`:t=n.value,t!=null&&Wt(e,!!n.multiple,t,!1)}}}var cn=!1;function ln(e,t,n){if(cn)return e(t,n);cn=!0;try{return e(t)}finally{if(cn=!1,(an!==null||on!==null)&&(bu(),an&&(t=an,e=on,on=an=null,sn(t),e)))for(t=0;t<e.length;t++)sn(e[t])}}function un(e,t){var n=e.stateNode;if(n===null)return null;var r=n[lt]||null;if(r===null)return null;n=r[t];a:switch(t){case`onClick`:case`onClickCapture`:case`onDoubleClick`:case`onDoubleClickCapture`:case`onMouseDown`:case`onMouseDownCapture`:case`onMouseMove`:case`onMouseMoveCapture`:case`onMouseUp`:case`onMouseUpCapture`:case`onMouseEnter`:(r=!r.disabled)||(e=e.type,r=!(e===`button`||e===`input`||e===`select`||e===`textarea`)),e=!r;break a;default:e=!1}if(e)return null;if(n&&typeof n!=`function`)throw Error(i(231,t,typeof n));return n}var dn=!(typeof window>`u`||window.document===void 0||window.document.createElement===void 0),fn=!1;if(dn)try{var pn={};Object.defineProperty(pn,"passive",{get:function(){fn=!0}}),window.addEventListener(`test`,pn,pn),window.removeEventListener(`test`,pn,pn)}catch{fn=!1}var mn=null,hn=null,gn=null;function _n(){if(gn)return gn;var e,t=hn,n=t.length,r,i=`value`in mn?mn.value:mn.textContent,a=i.length;for(e=0;e<n&&t[e]===i[e];e++);var o=n-e;for(r=1;r<=o&&t[n-r]===i[a-r];r++);return gn=i.slice(e,1<r?1-r:void 0)}function vn(e){var t=e.keyCode;return`charCode`in e?(e=e.charCode,e===0&&t===13&&(e=13)):e=t,e===10&&(e=13),32<=e||e===13?e:0}function yn(){return!0}function bn(){return!1}function xn(e){function t(t,n,r,i,a){for(var o in this._reactName=t,this._targetInst=r,this.type=n,this.nativeEvent=i,this.target=a,this.currentTarget=null,e)e.hasOwnProperty(o)&&(t=e[o],this[o]=t?t(i):i[o]);return this.isDefaultPrevented=(i.defaultPrevented==null?!1===i.returnValue:i.defaultPrevented)?yn:bn,this.isPropagationStopped=bn,this}return h(t.prototype,{preventDefault:function(){this.defaultPrevented=!0;var e=this.nativeEvent;e&&(e.preventDefault?e.preventDefault():typeof e.returnValue!=`unknown`&&(e.returnValue=!1),this.isDefaultPrevented=yn)},stopPropagation:function(){var e=this.nativeEvent;e&&(e.stopPropagation?e.stopPropagation():typeof e.cancelBubble!=`unknown`&&(e.cancelBubble=!0),this.isPropagationStopped=yn)},persist:function(){},isPersistent:yn}),t}var Sn={eventPhase:0,bubbles:0,cancelable:0,timeStamp:function(e){return e.timeStamp||Date.now()},defaultPrevented:0,isTrusted:0},Cn=xn(Sn),wn=h({},Sn,{view:0,detail:0}),Tn=xn(wn),En,Dn,On,kn=h({},wn,{screenX:0,screenY:0,clientX:0,clientY:0,pageX:0,pageY:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,getModifierState:zn,button:0,buttons:0,relatedTarget:function(e){return e.relatedTarget===void 0?e.fromElement===e.srcElement?e.toElement:e.fromElement:e.relatedTarget},movementX:function(e){return`movementX`in e?e.movementX:(e!==On&&(On&&e.type===`mousemove`?(En=e.screenX-On.screenX,Dn=e.screenY-On.screenY):Dn=En=0,On=e),En)},movementY:function(e){return`movementY`in e?e.movementY:Dn}}),An=xn(kn),jn=xn(h({},kn,{dataTransfer:0})),Mn=xn(h({},wn,{relatedTarget:0})),Nn=xn(h({},Sn,{animationName:0,elapsedTime:0,pseudoElement:0})),Pn=xn(h({},Sn,{clipboardData:function(e){return`clipboardData`in e?e.clipboardData:window.clipboardData}})),Fn=xn(h({},Sn,{data:0})),In={Esc:`Escape`,Spacebar:` `,Left:`ArrowLeft`,Up:`ArrowUp`,Right:`ArrowRight`,Down:`ArrowDown`,Del:`Delete`,Win:`OS`,Menu:`ContextMenu`,Apps:`ContextMenu`,Scroll:`ScrollLock`,MozPrintableKey:`Unidentified`},Ln={8:`Backspace`,9:`Tab`,12:`Clear`,13:`Enter`,16:`Shift`,17:`Control`,18:`Alt`,19:`Pause`,20:`CapsLock`,27:`Escape`,32:` `,33:`PageUp`,34:`PageDown`,35:`End`,36:`Home`,37:`ArrowLeft`,38:`ArrowUp`,39:`ArrowRight`,40:`ArrowDown`,45:`Insert`,46:`Delete`,112:`F1`,113:`F2`,114:`F3`,115:`F4`,116:`F5`,117:`F6`,118:`F7`,119:`F8`,120:`F9`,121:`F10`,122:`F11`,123:`F12`,144:`NumLock`,145:`ScrollLock`,224:`Meta`},L={Alt:`altKey`,Control:`ctrlKey`,Meta:`metaKey`,Shift:`shiftKey`};function Rn(e){var t=this.nativeEvent;return t.getModifierState?t.getModifierState(e):(e=L[e])?!!t[e]:!1}function zn(){return Rn}var Bn=xn(h({},wn,{key:function(e){if(e.key){var t=In[e.key]||e.key;if(t!==`Unidentified`)return t}return e.type===`keypress`?(e=vn(e),e===13?`Enter`:String.fromCharCode(e)):e.type===`keydown`||e.type===`keyup`?Ln[e.keyCode]||`Unidentified`:``},code:0,location:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,repeat:0,locale:0,getModifierState:zn,charCode:function(e){return e.type===`keypress`?vn(e):0},keyCode:function(e){return e.type===`keydown`||e.type===`keyup`?e.keyCode:0},which:function(e){return e.type===`keypress`?vn(e):e.type===`keydown`||e.type===`keyup`?e.keyCode:0}})),Vn=xn(h({},kn,{pointerId:0,width:0,height:0,pressure:0,tangentialPressure:0,tiltX:0,tiltY:0,twist:0,pointerType:0,isPrimary:0})),Hn=xn(h({},wn,{touches:0,targetTouches:0,changedTouches:0,altKey:0,metaKey:0,ctrlKey:0,shiftKey:0,getModifierState:zn})),Un=xn(h({},Sn,{propertyName:0,elapsedTime:0,pseudoElement:0})),Wn=xn(h({},kn,{deltaX:function(e){return`deltaX`in e?e.deltaX:`wheelDeltaX`in e?-e.wheelDeltaX:0},deltaY:function(e){return`deltaY`in e?e.deltaY:`wheelDeltaY`in e?-e.wheelDeltaY:`wheelDelta`in e?-e.wheelDelta:0},deltaZ:0,deltaMode:0})),Gn=xn(h({},Sn,{newState:0,oldState:0})),Kn=[9,13,27,32],qn=dn&&`CompositionEvent`in window,Jn=null;dn&&`documentMode`in document&&(Jn=document.documentMode);var Yn=dn&&`TextEvent`in window&&!Jn,Xn=dn&&(!qn||Jn&&8<Jn&&11>=Jn),Zn=` `,Qn=!1;function $n(e,t){switch(e){case`keyup`:return Kn.indexOf(t.keyCode)!==-1;case`keydown`:return t.keyCode!==229;case`keypress`:case`mousedown`:case`focusout`:return!0;default:return!1}}function er(e){return e=e.detail,typeof e==`object`&&`data`in e?e.data:null}var tr=!1;function nr(e,t){switch(e){case`compositionend`:return er(t);case`keypress`:return t.which===32?(Qn=!0,Zn):null;case`textInput`:return e=t.data,e===Zn&&Qn?null:e;default:return null}}function rr(e,t){if(tr)return e===`compositionend`||!qn&&$n(e,t)?(e=_n(),gn=hn=mn=null,tr=!1,e):null;switch(e){case`paste`:return null;case`keypress`:if(!(t.ctrlKey||t.altKey||t.metaKey)||t.ctrlKey&&t.altKey){if(t.char&&1<t.char.length)return t.char;if(t.which)return String.fromCharCode(t.which)}return null;case`compositionend`:return Xn&&t.locale!==`ko`?null:t.data;default:return null}}var ir={color:!0,date:!0,datetime:!0,"datetime-local":!0,email:!0,month:!0,number:!0,password:!0,range:!0,search:!0,tel:!0,text:!0,time:!0,url:!0,week:!0};function ar(e){var t=e&&e.nodeName&&e.nodeName.toLowerCase();return t===`input`?!!ir[e.type]:t===`textarea`}function or(e,t,n,r){an?on?on.push(r):on=[r]:an=r,t=Ed(t,`onChange`),0<t.length&&(n=new Cn(`onChange`,`change`,null,n,r),e.push({event:n,listeners:t}))}var sr=null,cr=null;function lr(e){yd(e,0)}function ur(e){if(Lt(yt(e)))return e}function dr(e,t){if(e===`change`)return t}var fr=!1;if(dn){var pr;if(dn){var mr=`oninput`in document;if(!mr){var hr=document.createElement(`div`);hr.setAttribute(`oninput`,`return;`),mr=typeof hr.oninput==`function`}pr=mr}else pr=!1;fr=pr&&(!document.documentMode||9<document.documentMode)}function gr(){sr&&(sr.detachEvent(`onpropertychange`,_r),cr=sr=null)}function _r(e){if(e.propertyName===`value`&&ur(cr)){var t=[];or(t,cr,e,rn(e)),ln(lr,t)}}function vr(e,t,n){e===`focusin`?(gr(),sr=t,cr=n,sr.attachEvent(`onpropertychange`,_r)):e===`focusout`&&gr()}function yr(e){if(e===`selectionchange`||e===`keyup`||e===`keydown`)return ur(cr)}function br(e,t){if(e===`click`)return ur(t)}function xr(e,t){if(e===`input`||e===`change`)return ur(t)}function Sr(e,t){return e===t&&(e!==0||1/e==1/t)||e!==e&&t!==t}var Cr=typeof Object.is==`function`?Object.is:Sr;function wr(e,t){if(Cr(e,t))return!0;if(typeof e!=`object`||!e||typeof t!=`object`||!t)return!1;var n=Object.keys(e),r=Object.keys(t);if(n.length!==r.length)return!1;for(r=0;r<n.length;r++){var i=n[r];if(!Te.call(t,i)||!Cr(e[i],t[i]))return!1}return!0}function Tr(e){for(;e&&e.firstChild;)e=e.firstChild;return e}function Er(e,t){var n=Tr(e);e=0;for(var r;n;){if(n.nodeType===3){if(r=e+n.textContent.length,e<=t&&r>=t)return{node:n,offset:t-e};e=r}a:{for(;n;){if(n.nextSibling){n=n.nextSibling;break a}n=n.parentNode}n=void 0}n=Tr(n)}}function Dr(e,t){return e&&t?e===t?!0:e&&e.nodeType===3?!1:t&&t.nodeType===3?Dr(e,t.parentNode):`contains`in e?e.contains(t):e.compareDocumentPosition?!!(e.compareDocumentPosition(t)&16):!1:!1}function Or(e){e=e!=null&&e.ownerDocument!=null&&e.ownerDocument.defaultView!=null?e.ownerDocument.defaultView:window;for(var t=Rt(e.document);t instanceof e.HTMLIFrameElement;){try{var n=typeof t.contentWindow.location.href==`string`}catch{n=!1}if(n)e=t.contentWindow;else break;t=Rt(e.document)}return t}function kr(e){var t=e&&e.nodeName&&e.nodeName.toLowerCase();return t&&(t===`input`&&(e.type===`text`||e.type===`search`||e.type===`tel`||e.type===`url`||e.type===`password`)||t===`textarea`||e.contentEditable===`true`)}var Ar=dn&&`documentMode`in document&&11>=document.documentMode,jr=null,Mr=null,Nr=null,Pr=!1;function Fr(e,t,n){var r=n.window===n?n.document:n.nodeType===9?n:n.ownerDocument;Pr||jr==null||jr!==Rt(r)||(r=jr,`selectionStart`in r&&kr(r)?r={start:r.selectionStart,end:r.selectionEnd}:(r=(r.ownerDocument&&r.ownerDocument.defaultView||window).getSelection(),r={anchorNode:r.anchorNode,anchorOffset:r.anchorOffset,focusNode:r.focusNode,focusOffset:r.focusOffset}),Nr&&wr(Nr,r)||(Nr=r,r=Ed(Mr,`onSelect`),0<r.length&&(t=new Cn(`onSelect`,`select`,null,t,n),e.push({event:t,listeners:r}),t.target=jr)))}function Ir(e,t){var n={};return n[e.toLowerCase()]=t.toLowerCase(),n[`Webkit`+e]=`webkit`+t,n[`Moz`+e]=`moz`+t,n}var Lr={animationend:Ir(`Animation`,`AnimationEnd`),animationiteration:Ir(`Animation`,`AnimationIteration`),animationstart:Ir(`Animation`,`AnimationStart`),transitionrun:Ir(`Transition`,`TransitionRun`),transitionstart:Ir(`Transition`,`TransitionStart`),transitioncancel:Ir(`Transition`,`TransitionCancel`),transitionend:Ir(`Transition`,`TransitionEnd`)},Rr={},zr={};dn&&(zr=document.createElement(`div`).style,`AnimationEvent`in window||(delete Lr.animationend.animation,delete Lr.animationiteration.animation,delete Lr.animationstart.animation),`TransitionEvent`in window||delete Lr.transitionend.transition);function Br(e){if(Rr[e])return Rr[e];if(!Lr[e])return e;var t=Lr[e],n;for(n in t)if(t.hasOwnProperty(n)&&n in zr)return Rr[e]=t[n];return e}var Vr=Br(`animationend`),Hr=Br(`animationiteration`),Ur=Br(`animationstart`),Wr=Br(`transitionrun`),Gr=Br(`transitionstart`),Kr=Br(`transitioncancel`),qr=Br(`transitionend`),Jr=new Map,Yr=`abort auxClick beforeToggle cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel`.split(` `);Yr.push(`scrollEnd`);function Xr(e,t){Jr.set(e,t),wt(t,[e])}var Zr=typeof reportError==`function`?reportError:function(e){if(typeof window==`object`&&typeof window.ErrorEvent==`function`){var t=new window.ErrorEvent(`error`,{bubbles:!0,cancelable:!0,message:typeof e==`object`&&e&&typeof e.message==`string`?String(e.message):String(e),error:e});if(!window.dispatchEvent(t))return}else if(typeof process==`object`&&typeof process.emit==`function`){process.emit(`uncaughtException`,e);return}console.error(e)},Qr=[],$r=0,ei=0;function ti(){for(var e=$r,t=ei=$r=0;t<e;){var n=Qr[t];Qr[t++]=null;var r=Qr[t];Qr[t++]=null;var i=Qr[t];Qr[t++]=null;var a=Qr[t];if(Qr[t++]=null,r!==null&&i!==null){var o=r.pending;o===null?i.next=i:(i.next=o.next,o.next=i),r.pending=i}a!==0&&ai(n,i,a)}}function ni(e,t,n,r){Qr[$r++]=e,Qr[$r++]=t,Qr[$r++]=n,Qr[$r++]=r,ei|=r,e.lanes|=r,e=e.alternate,e!==null&&(e.lanes|=r)}function ri(e,t,n,r){return ni(e,t,n,r),oi(e)}function ii(e,t){return ni(e,null,null,t),oi(e)}function ai(e,t,n){e.lanes|=n;var r=e.alternate;r!==null&&(r.lanes|=n);for(var i=!1,a=e.return;a!==null;)a.childLanes|=n,r=a.alternate,r!==null&&(r.childLanes|=n),a.tag===22&&(e=a.stateNode,e===null||e._visibility&1||(i=!0)),e=a,a=a.return;return e.tag===3?(a=e.stateNode,i&&t!==null&&(i=31-P(n),e=a.hiddenUpdates,r=e[i],r===null?e[i]=[t]:r.push(t),t.lane=n|536870912),a):null}function oi(e){if(50<du)throw du=0,fu=null,Error(i(185));for(var t=e.return;t!==null;)e=t,t=e.return;return e.tag===3?e.stateNode:null}var si={};function ci(e,t,n,r){this.tag=e,this.key=n,this.sibling=this.child=this.return=this.stateNode=this.type=this.elementType=null,this.index=0,this.refCleanup=this.ref=null,this.pendingProps=t,this.dependencies=this.memoizedState=this.updateQueue=this.memoizedProps=null,this.mode=r,this.subtreeFlags=this.flags=0,this.deletions=null,this.childLanes=this.lanes=0,this.alternate=null}function li(e,t,n,r){return new ci(e,t,n,r)}function ui(e){return e=e.prototype,!(!e||!e.isReactComponent)}function di(e,t){var n=e.alternate;return n===null?(n=li(e.tag,t,e.key,e.mode),n.elementType=e.elementType,n.type=e.type,n.stateNode=e.stateNode,n.alternate=e,e.alternate=n):(n.pendingProps=t,n.type=e.type,n.flags=0,n.subtreeFlags=0,n.deletions=null),n.flags=e.flags&65011712,n.childLanes=e.childLanes,n.lanes=e.lanes,n.child=e.child,n.memoizedProps=e.memoizedProps,n.memoizedState=e.memoizedState,n.updateQueue=e.updateQueue,t=e.dependencies,n.dependencies=t===null?null:{lanes:t.lanes,firstContext:t.firstContext},n.sibling=e.sibling,n.index=e.index,n.ref=e.ref,n.refCleanup=e.refCleanup,n}function R(e,t){e.flags&=65011714;var n=e.alternate;return n===null?(e.childLanes=0,e.lanes=t,e.child=null,e.subtreeFlags=0,e.memoizedProps=null,e.memoizedState=null,e.updateQueue=null,e.dependencies=null,e.stateNode=null):(e.childLanes=n.childLanes,e.lanes=n.lanes,e.child=n.child,e.subtreeFlags=0,e.deletions=null,e.memoizedProps=n.memoizedProps,e.memoizedState=n.memoizedState,e.updateQueue=n.updateQueue,e.type=n.type,t=n.dependencies,e.dependencies=t===null?null:{lanes:t.lanes,firstContext:t.firstContext}),e}function fi(e,t,n,r,a,o){var s=0;if(r=e,typeof e==`function`)ui(e)&&(s=1);else if(typeof e==`string`)s=Uf(e,n,ue.current)?26:e===`html`||e===`head`||e===`body`?27:5;else a:switch(e){case te:return e=li(31,n,t,a),e.elementType=te,e.lanes=o,e;case y:return pi(n.children,a,o,t);case b:s=8,a|=24;break;case x:return e=li(12,n,t,a|2),e.elementType=x,e.lanes=o,e;case T:return e=li(13,n,t,a),e.elementType=T,e.lanes=o,e;case ee:return e=li(19,n,t,a),e.elementType=ee,e.lanes=o,e;default:if(typeof e==`object`&&e)switch(e.$$typeof){case C:s=10;break a;case S:s=9;break a;case w:s=11;break a;case E:s=14;break a;case D:s=16,r=null;break a}s=29,n=Error(i(130,e===null?`null`:typeof e,``)),r=null}return t=li(s,n,t,a),t.elementType=e,t.type=r,t.lanes=o,t}function pi(e,t,n,r){return e=li(7,e,r,t),e.lanes=n,e}function mi(e,t,n){return e=li(6,e,null,t),e.lanes=n,e}function hi(e){var t=li(18,null,null,0);return t.stateNode=e,t}function gi(e,t,n){return t=li(4,e.children===null?[]:e.children,e.key,t),t.lanes=n,t.stateNode={containerInfo:e.containerInfo,pendingChildren:null,implementation:e.implementation},t}var _i=new WeakMap;function vi(e,t){if(typeof e==`object`&&e){var n=_i.get(e);return n===void 0?(t={value:e,source:t,stack:we(t)},_i.set(e,t),t):n}return{value:e,source:t,stack:we(t)}}var yi=[],bi=0,xi=null,Si=0,Ci=[],wi=0,Ti=null,Ei=1,Di=``;function Oi(e,t){yi[bi++]=Si,yi[bi++]=xi,xi=e,Si=t}function ki(e,t,n){Ci[wi++]=Ei,Ci[wi++]=Di,Ci[wi++]=Ti,Ti=e;var r=Ei;e=Di;var i=32-P(r)-1;r&=~(1<<i),n+=1;var a=32-P(t)+i;if(30<a){var o=i-i%5;a=(r&(1<<o)-1).toString(32),r>>=o,i-=o,Ei=1<<32-P(t)+i|n<<i|r,Di=a+e}else Ei=1<<a|n<<i|r,Di=e}function Ai(e){e.return!==null&&(Oi(e,1),ki(e,1,0))}function ji(e){for(;e===xi;)xi=yi[--bi],yi[bi]=null,Si=yi[--bi],yi[bi]=null;for(;e===Ti;)Ti=Ci[--wi],Ci[wi]=null,Di=Ci[--wi],Ci[wi]=null,Ei=Ci[--wi],Ci[wi]=null}function Mi(e,t){Ci[wi++]=Ei,Ci[wi++]=Di,Ci[wi++]=Ti,Ei=t.id,Di=t.overflow,Ti=e}var Ni=null,z=null,B=!1,Pi=null,Fi=!1,Ii=Error(i(519));function Li(e){throw Ui(vi(Error(i(418,1<arguments.length&&arguments[1]!==void 0&&arguments[1]?`text`:`HTML`,``)),e)),Ii}function Ri(e){var t=e.stateNode,n=e.type,r=e.memoizedProps;switch(t[ct]=e,t[lt]=r,n){case`dialog`:Q(`cancel`,t),Q(`close`,t);break;case`iframe`:case`object`:case`embed`:Q(`load`,t);break;case`video`:case`audio`:for(n=0;n<_d.length;n++)Q(_d[n],t);break;case`source`:Q(`error`,t);break;case`img`:case`image`:case`link`:Q(`error`,t),Q(`load`,t);break;case`details`:Q(`toggle`,t);break;case`input`:Q(`invalid`,t),Ht(t,r.value,r.defaultValue,r.checked,r.defaultChecked,r.type,r.name,!0);break;case`select`:Q(`invalid`,t);break;case`textarea`:Q(`invalid`,t),Kt(t,r.value,r.defaultValue,r.children)}n=r.children,typeof n!=`string`&&typeof n!=`number`&&typeof n!=`bigint`||t.textContent===``+n||!0===r.suppressHydrationWarning||Md(t.textContent,n)?(r.popover!=null&&(Q(`beforetoggle`,t),Q(`toggle`,t)),r.onScroll!=null&&Q(`scroll`,t),r.onScrollEnd!=null&&Q(`scrollend`,t),r.onClick!=null&&(t.onclick=tn),t=!0):t=!1,t||Li(e,!0)}function zi(e){for(Ni=e.return;Ni;)switch(Ni.tag){case 5:case 31:case 13:Fi=!1;return;case 27:case 3:Fi=!0;return;default:Ni=Ni.return}}function Bi(e){if(e!==Ni)return!1;if(!B)return zi(e),B=!0,!1;var t=e.tag,n;if((n=t!==3&&t!==27)&&((n=t===5)&&(n=e.type,n=!(n!==`form`&&n!==`button`)||Ud(e.type,e.memoizedProps)),n=!n),n&&z&&Li(e),zi(e),t===13){if(e=e.memoizedState,e=e===null?null:e.dehydrated,!e)throw Error(i(317));z=uf(e)}else if(t===31){if(e=e.memoizedState,e=e===null?null:e.dehydrated,!e)throw Error(i(317));z=uf(e)}else t===27?(t=z,Zd(e.type)?(e=lf,lf=null,z=e):z=t):z=Ni?cf(e.stateNode.nextSibling):null;return!0}function Vi(){z=Ni=null,B=!1}function Hi(){var e=Pi;return e!==null&&(Zl===null?Zl=e:Zl.push.apply(Zl,e),Pi=null),e}function Ui(e){Pi===null?Pi=[e]:Pi.push(e)}var Wi=ce(null),Gi=null,Ki=null;function qi(e,t,n){N(Wi,t._currentValue),t._currentValue=n}function Ji(e){e._currentValue=Wi.current,le(Wi)}function Yi(e,t,n){for(;e!==null;){var r=e.alternate;if((e.childLanes&t)===t?r!==null&&(r.childLanes&t)!==t&&(r.childLanes|=t):(e.childLanes|=t,r!==null&&(r.childLanes|=t)),e===n)break;e=e.return}}function Xi(e,t,n,r){var a=e.child;for(a!==null&&(a.return=e);a!==null;){var o=a.dependencies;if(o!==null){var s=a.child;o=o.firstContext;a:for(;o!==null;){var c=o;o=a;for(var l=0;l<t.length;l++)if(c.context===t[l]){o.lanes|=n,c=o.alternate,c!==null&&(c.lanes|=n),Yi(o.return,n,e),r||(s=null);break a}o=c.next}}else if(a.tag===18){if(s=a.return,s===null)throw Error(i(341));s.lanes|=n,o=s.alternate,o!==null&&(o.lanes|=n),Yi(s,n,e),s=null}else s=a.child;if(s!==null)s.return=a;else for(s=a;s!==null;){if(s===e){s=null;break}if(a=s.sibling,a!==null){a.return=s.return,s=a;break}s=s.return}a=s}}function Zi(e,t,n,r){e=null;for(var a=t,o=!1;a!==null;){if(!o){if(a.flags&524288)o=!0;else if(a.flags&262144)break}if(a.tag===10){var s=a.alternate;if(s===null)throw Error(i(387));if(s=s.memoizedProps,s!==null){var c=a.type;Cr(a.pendingProps.value,s.value)||(e===null?e=[c]:e.push(c))}}else if(a===pe.current){if(s=a.alternate,s===null)throw Error(i(387));s.memoizedState.memoizedState!==a.memoizedState.memoizedState&&(e===null?e=[Qf]:e.push(Qf))}a=a.return}e!==null&&Xi(t,e,n,r),t.flags|=262144}function Qi(e){for(e=e.firstContext;e!==null;){if(!Cr(e.context._currentValue,e.memoizedValue))return!0;e=e.next}return!1}function $i(e){Gi=e,Ki=null,e=e.dependencies,e!==null&&(e.firstContext=null)}function ea(e){return na(Gi,e)}function ta(e,t){return Gi===null&&$i(e),na(e,t)}function na(e,t){var n=t._currentValue;if(t={context:t,memoizedValue:n,next:null},Ki===null){if(e===null)throw Error(i(308));Ki=t,e.dependencies={lanes:0,firstContext:t},e.flags|=524288}else Ki=Ki.next=t;return n}var ra=typeof AbortController<`u`?AbortController:function(){var e=[],t=this.signal={aborted:!1,addEventListener:function(t,n){e.push(n)}};this.abort=function(){t.aborted=!0,e.forEach(function(e){return e()})}},ia=t.unstable_scheduleCallback,aa=t.unstable_NormalPriority,oa={$$typeof:C,Consumer:null,Provider:null,_currentValue:null,_currentValue2:null,_threadCount:0};function sa(){return{controller:new ra,data:new Map,refCount:0}}function ca(e){e.refCount--,e.refCount===0&&ia(aa,function(){e.controller.abort()})}var la=null,ua=0,da=0,fa=null;function pa(e,t){if(la===null){var n=la=[];ua=0,da=dd(),fa={status:`pending`,value:void 0,then:function(e){n.push(e)}}}return ua++,t.then(ma,ma),t}function ma(){if(--ua===0&&la!==null){fa!==null&&(fa.status=`fulfilled`);var e=la;la=null,da=0,fa=null;for(var t=0;t<e.length;t++)(0,e[t])()}}function ha(e,t){var n=[],r={status:`pending`,value:null,reason:null,then:function(e){n.push(e)}};return e.then(function(){r.status=`fulfilled`,r.value=t;for(var e=0;e<n.length;e++)(0,n[e])(t)},function(e){for(r.status=`rejected`,r.reason=e,e=0;e<n.length;e++)(0,n[e])(void 0)}),r}var ga=j.S;j.S=function(e,t){eu=Ae(),typeof t==`object`&&t&&typeof t.then==`function`&&pa(e,t),ga!==null&&ga(e,t)};var _a=ce(null);function va(){var e=_a.current;return e===null?q.pooledCache:e}function ya(e,t){t===null?N(_a,_a.current):N(_a,t.pool)}function ba(){var e=va();return e===null?null:{parent:oa._currentValue,pool:e}}var xa=Error(i(460)),Sa=Error(i(474)),Ca=Error(i(542)),wa={then:function(){}};function Ta(e){return e=e.status,e===`fulfilled`||e===`rejected`}function Ea(e,t,n){switch(n=e[n],n===void 0?e.push(t):n!==t&&(t.then(tn,tn),t=n),t.status){case`fulfilled`:return t.value;case`rejected`:throw e=t.reason,ka(e),e;default:if(typeof t.status==`string`)t.then(tn,tn);else{if(e=q,e!==null&&100<e.shellSuspendCounter)throw Error(i(482));e=t,e.status=`pending`,e.then(function(e){if(t.status===`pending`){var n=t;n.status=`fulfilled`,n.value=e}},function(e){if(t.status===`pending`){var n=t;n.status=`rejected`,n.reason=e}})}switch(t.status){case`fulfilled`:return t.value;case`rejected`:throw e=t.reason,ka(e),e}throw V=t,xa}}function Da(e){try{var t=e._init;return t(e._payload)}catch(e){throw typeof e==`object`&&e&&typeof e.then==`function`?(V=e,xa):e}}var V=null;function Oa(){if(V===null)throw Error(i(459));var e=V;return V=null,e}function ka(e){if(e===xa||e===Ca)throw Error(i(483))}var Aa=null,ja=0;function Ma(e){var t=ja;return ja+=1,Aa===null&&(Aa=[]),Ea(Aa,e,t)}function Na(e,t){t=t.props.ref,e.ref=t===void 0?null:t}function Pa(e,t){throw t.$$typeof===g?Error(i(525)):(e=Object.prototype.toString.call(t),Error(i(31,e===`[object Object]`?`object with keys {`+Object.keys(t).join(`, `)+`}`:e)))}function Fa(e){function t(t,n){if(e){var r=t.deletions;r===null?(t.deletions=[n],t.flags|=16):r.push(n)}}function n(n,r){if(!e)return null;for(;r!==null;)t(n,r),r=r.sibling;return null}function r(e){for(var t=new Map;e!==null;)e.key===null?t.set(e.index,e):t.set(e.key,e),e=e.sibling;return t}function a(e,t){return e=di(e,t),e.index=0,e.sibling=null,e}function o(t,n,r){return t.index=r,e?(r=t.alternate,r===null?(t.flags|=67108866,n):(r=r.index,r<n?(t.flags|=67108866,n):r)):(t.flags|=1048576,n)}function s(t){return e&&t.alternate===null&&(t.flags|=67108866),t}function c(e,t,n,r){return t===null||t.tag!==6?(t=mi(n,e.mode,r),t.return=e,t):(t=a(t,n),t.return=e,t)}function l(e,t,n,r){var i=n.type;return i===y?d(e,t,n.props.children,r,n.key):t!==null&&(t.elementType===i||typeof i==`object`&&i&&i.$$typeof===D&&Da(i)===t.type)?(t=a(t,n.props),Na(t,n),t.return=e,t):(t=fi(n.type,n.key,n.props,null,e.mode,r),Na(t,n),t.return=e,t)}function u(e,t,n,r){return t===null||t.tag!==4||t.stateNode.containerInfo!==n.containerInfo||t.stateNode.implementation!==n.implementation?(t=gi(n,e.mode,r),t.return=e,t):(t=a(t,n.children||[]),t.return=e,t)}function d(e,t,n,r,i){return t===null||t.tag!==7?(t=pi(n,e.mode,r,i),t.return=e,t):(t=a(t,n),t.return=e,t)}function f(e,t,n){if(typeof t==`string`&&t!==``||typeof t==`number`||typeof t==`bigint`)return t=mi(``+t,e.mode,n),t.return=e,t;if(typeof t==`object`&&t){switch(t.$$typeof){case _:return n=fi(t.type,t.key,t.props,null,e.mode,n),Na(n,t),n.return=e,n;case v:return t=gi(t,e.mode,n),t.return=e,t;case D:return t=Da(t),f(e,t,n)}if(ie(t)||k(t))return t=pi(t,e.mode,n,null),t.return=e,t;if(typeof t.then==`function`)return f(e,Ma(t),n);if(t.$$typeof===C)return f(e,ta(e,t),n);Pa(e,t)}return null}function p(e,t,n,r){var i=t===null?null:t.key;if(typeof n==`string`&&n!==``||typeof n==`number`||typeof n==`bigint`)return i===null?c(e,t,``+n,r):null;if(typeof n==`object`&&n){switch(n.$$typeof){case _:return n.key===i?l(e,t,n,r):null;case v:return n.key===i?u(e,t,n,r):null;case D:return n=Da(n),p(e,t,n,r)}if(ie(n)||k(n))return i===null?d(e,t,n,r,null):null;if(typeof n.then==`function`)return p(e,t,Ma(n),r);if(n.$$typeof===C)return p(e,t,ta(e,n),r);Pa(e,n)}return null}function m(e,t,n,r,i){if(typeof r==`string`&&r!==``||typeof r==`number`||typeof r==`bigint`)return e=e.get(n)||null,c(t,e,``+r,i);if(typeof r==`object`&&r){switch(r.$$typeof){case _:return e=e.get(r.key===null?n:r.key)||null,l(t,e,r,i);case v:return e=e.get(r.key===null?n:r.key)||null,u(t,e,r,i);case D:return r=Da(r),m(e,t,n,r,i)}if(ie(r)||k(r))return e=e.get(n)||null,d(t,e,r,i,null);if(typeof r.then==`function`)return m(e,t,n,Ma(r),i);if(r.$$typeof===C)return m(e,t,n,ta(t,r),i);Pa(t,r)}return null}function h(i,a,s,c){for(var l=null,u=null,d=a,h=a=0,g=null;d!==null&&h<s.length;h++){d.index>h?(g=d,d=null):g=d.sibling;var _=p(i,d,s[h],c);if(_===null){d===null&&(d=g);break}e&&d&&_.alternate===null&&t(i,d),a=o(_,a,h),u===null?l=_:u.sibling=_,u=_,d=g}if(h===s.length)return n(i,d),B&&Oi(i,h),l;if(d===null){for(;h<s.length;h++)d=f(i,s[h],c),d!==null&&(a=o(d,a,h),u===null?l=d:u.sibling=d,u=d);return B&&Oi(i,h),l}for(d=r(d);h<s.length;h++)g=m(d,i,h,s[h],c),g!==null&&(e&&g.alternate!==null&&d.delete(g.key===null?h:g.key),a=o(g,a,h),u===null?l=g:u.sibling=g,u=g);return e&&d.forEach(function(e){return t(i,e)}),B&&Oi(i,h),l}function g(a,s,c,l){if(c==null)throw Error(i(151));for(var u=null,d=null,h=s,g=s=0,_=null,v=c.next();h!==null&&!v.done;g++,v=c.next()){h.index>g?(_=h,h=null):_=h.sibling;var y=p(a,h,v.value,l);if(y===null){h===null&&(h=_);break}e&&h&&y.alternate===null&&t(a,h),s=o(y,s,g),d===null?u=y:d.sibling=y,d=y,h=_}if(v.done)return n(a,h),B&&Oi(a,g),u;if(h===null){for(;!v.done;g++,v=c.next())v=f(a,v.value,l),v!==null&&(s=o(v,s,g),d===null?u=v:d.sibling=v,d=v);return B&&Oi(a,g),u}for(h=r(h);!v.done;g++,v=c.next())v=m(h,a,g,v.value,l),v!==null&&(e&&v.alternate!==null&&h.delete(v.key===null?g:v.key),s=o(v,s,g),d===null?u=v:d.sibling=v,d=v);return e&&h.forEach(function(e){return t(a,e)}),B&&Oi(a,g),u}function b(e,r,o,c){if(typeof o==`object`&&o&&o.type===y&&o.key===null&&(o=o.props.children),typeof o==`object`&&o){switch(o.$$typeof){case _:a:{for(var l=o.key;r!==null;){if(r.key===l){if(l=o.type,l===y){if(r.tag===7){n(e,r.sibling),c=a(r,o.props.children),c.return=e,e=c;break a}}else if(r.elementType===l||typeof l==`object`&&l&&l.$$typeof===D&&Da(l)===r.type){n(e,r.sibling),c=a(r,o.props),Na(c,o),c.return=e,e=c;break a}n(e,r);break}else t(e,r);r=r.sibling}o.type===y?(c=pi(o.props.children,e.mode,c,o.key),c.return=e,e=c):(c=fi(o.type,o.key,o.props,null,e.mode,c),Na(c,o),c.return=e,e=c)}return s(e);case v:a:{for(l=o.key;r!==null;){if(r.key===l)if(r.tag===4&&r.stateNode.containerInfo===o.containerInfo&&r.stateNode.implementation===o.implementation){n(e,r.sibling),c=a(r,o.children||[]),c.return=e,e=c;break a}else{n(e,r);break}else t(e,r);r=r.sibling}c=gi(o,e.mode,c),c.return=e,e=c}return s(e);case D:return o=Da(o),b(e,r,o,c)}if(ie(o))return h(e,r,o,c);if(k(o)){if(l=k(o),typeof l!=`function`)throw Error(i(150));return o=l.call(o),g(e,r,o,c)}if(typeof o.then==`function`)return b(e,r,Ma(o),c);if(o.$$typeof===C)return b(e,r,ta(e,o),c);Pa(e,o)}return typeof o==`string`&&o!==``||typeof o==`number`||typeof o==`bigint`?(o=``+o,r!==null&&r.tag===6?(n(e,r.sibling),c=a(r,o),c.return=e,e=c):(n(e,r),c=mi(o,e.mode,c),c.return=e,e=c),s(e)):n(e,r)}return function(e,t,n,r){try{ja=0;var i=b(e,t,n,r);return Aa=null,i}catch(t){if(t===xa||t===Ca)throw t;var a=li(29,t,null,e.mode);return a.lanes=r,a.return=e,a}}}var Ia=Fa(!0),La=Fa(!1),Ra=!1;function za(e){e.updateQueue={baseState:e.memoizedState,firstBaseUpdate:null,lastBaseUpdate:null,shared:{pending:null,lanes:0,hiddenCallbacks:null},callbacks:null}}function Ba(e,t){e=e.updateQueue,t.updateQueue===e&&(t.updateQueue={baseState:e.baseState,firstBaseUpdate:e.firstBaseUpdate,lastBaseUpdate:e.lastBaseUpdate,shared:e.shared,callbacks:null})}function Va(e){return{lane:e,tag:0,payload:null,callback:null,next:null}}function Ha(e,t,n){var r=e.updateQueue;if(r===null)return null;if(r=r.shared,K&2){var i=r.pending;return i===null?t.next=t:(t.next=i.next,i.next=t),r.pending=t,t=oi(e),ai(e,null,n),t}return ni(e,r,t,n),oi(e)}function Ua(e,t,n){if(t=t.updateQueue,t!==null&&(t=t.shared,n&4194048)){var r=t.lanes;r&=e.pendingLanes,n|=r,t.lanes=n,nt(e,n)}}function Wa(e,t){var n=e.updateQueue,r=e.alternate;if(r!==null&&(r=r.updateQueue,n===r)){var i=null,a=null;if(n=n.firstBaseUpdate,n!==null){do{var o={lane:n.lane,tag:n.tag,payload:n.payload,callback:null,next:null};a===null?i=a=o:a=a.next=o,n=n.next}while(n!==null);a===null?i=a=t:a=a.next=t}else i=a=t;n={baseState:r.baseState,firstBaseUpdate:i,lastBaseUpdate:a,shared:r.shared,callbacks:r.callbacks},e.updateQueue=n;return}e=n.lastBaseUpdate,e===null?n.firstBaseUpdate=t:e.next=t,n.lastBaseUpdate=t}var Ga=!1;function Ka(){if(Ga){var e=fa;if(e!==null)throw e}}function qa(e,t,n,r){Ga=!1;var i=e.updateQueue;Ra=!1;var a=i.firstBaseUpdate,o=i.lastBaseUpdate,s=i.shared.pending;if(s!==null){i.shared.pending=null;var c=s,l=c.next;c.next=null,o===null?a=l:o.next=l,o=c;var u=e.alternate;u!==null&&(u=u.updateQueue,s=u.lastBaseUpdate,s!==o&&(s===null?u.firstBaseUpdate=l:s.next=l,u.lastBaseUpdate=c))}if(a!==null){var d=i.baseState;o=0,u=l=c=null,s=a;do{var f=s.lane&-536870913,p=f!==s.lane;if(p?(Y&f)===f:(r&f)===f){f!==0&&f===da&&(Ga=!0),u!==null&&(u=u.next={lane:0,tag:s.tag,payload:s.payload,callback:null,next:null});a:{var m=e,g=s;f=t;var _=n;switch(g.tag){case 1:if(m=g.payload,typeof m==`function`){d=m.call(_,d,f);break a}d=m;break a;case 3:m.flags=m.flags&-65537|128;case 0:if(m=g.payload,f=typeof m==`function`?m.call(_,d,f):m,f==null)break a;d=h({},d,f);break a;case 2:Ra=!0}}f=s.callback,f!==null&&(e.flags|=64,p&&(e.flags|=8192),p=i.callbacks,p===null?i.callbacks=[f]:p.push(f))}else p={lane:f,tag:s.tag,payload:s.payload,callback:s.callback,next:null},u===null?(l=u=p,c=d):u=u.next=p,o|=f;if(s=s.next,s===null){if(s=i.shared.pending,s===null)break;p=s,s=p.next,p.next=null,i.lastBaseUpdate=p,i.shared.pending=null}}while(1);u===null&&(c=d),i.baseState=c,i.firstBaseUpdate=l,i.lastBaseUpdate=u,a===null&&(i.shared.lanes=0),Gl|=o,e.lanes=o,e.memoizedState=d}}function Ja(e,t){if(typeof e!=`function`)throw Error(i(191,e));e.call(t)}function Ya(e,t){var n=e.callbacks;if(n!==null)for(e.callbacks=null,e=0;e<n.length;e++)Ja(n[e],t)}var Xa=ce(null),Za=ce(0);function Qa(e,t){e=Ul,N(Za,e),N(Xa,t),Ul=e|t.baseLanes}function $a(){N(Za,Ul),N(Xa,Xa.current)}function eo(){Ul=Za.current,le(Xa),le(Za)}var to=ce(null),no=null;function ro(e){var t=e.alternate;N(co,co.current&1),N(to,e),no===null&&(t===null||Xa.current!==null||t.memoizedState!==null)&&(no=e)}function io(e){N(co,co.current),N(to,e),no===null&&(no=e)}function ao(e){e.tag===22?(N(co,co.current),N(to,e),no===null&&(no=e)):oo(e)}function oo(){N(co,co.current),N(to,to.current)}function so(e){le(to),no===e&&(no=null),le(co)}var co=ce(0);function lo(e){for(var t=e;t!==null;){if(t.tag===13){var n=t.memoizedState;if(n!==null&&(n=n.dehydrated,n===null||af(n)||of(n)))return t}else if(t.tag===19&&(t.memoizedProps.revealOrder===`forwards`||t.memoizedProps.revealOrder===`backwards`||t.memoizedProps.revealOrder===`unstable_legacy-backwards`||t.memoizedProps.revealOrder===`together`)){if(t.flags&128)return t}else if(t.child!==null){t.child.return=t,t=t.child;continue}if(t===e)break;for(;t.sibling===null;){if(t.return===null||t.return===e)return null;t=t.return}t.sibling.return=t.return,t=t.sibling}return null}var uo=0,H=null,U=null,fo=null,po=!1,mo=!1,ho=!1,go=0,_o=0,vo=null,yo=0;function bo(){throw Error(i(321))}function xo(e,t){if(t===null)return!1;for(var n=0;n<t.length&&n<e.length;n++)if(!Cr(e[n],t[n]))return!1;return!0}function So(e,t,n,r,i,a){return uo=a,H=t,t.memoizedState=null,t.updateQueue=null,t.lanes=0,j.H=e===null||e.memoizedState===null?Rs:zs,ho=!1,a=n(r,i),ho=!1,mo&&(a=wo(t,n,r,i)),Co(e),a}function Co(e){j.H=Ls;var t=U!==null&&U.next!==null;if(uo=0,fo=U=H=null,po=!1,_o=0,vo=null,t)throw Error(i(300));e===null||nc||(e=e.dependencies,e!==null&&Qi(e)&&(nc=!0))}function wo(e,t,n,r){H=e;var a=0;do{if(mo&&(vo=null),_o=0,mo=!1,25<=a)throw Error(i(301));if(a+=1,fo=U=null,e.updateQueue!=null){var o=e.updateQueue;o.lastEffect=null,o.events=null,o.stores=null,o.memoCache!=null&&(o.memoCache.index=0)}j.H=Bs,o=t(n,r)}while(mo);return o}function To(){var e=j.H,t=e.useState()[0];return t=typeof t.then==`function`?Mo(t):t,e=e.useState()[0],(U===null?null:U.memoizedState)!==e&&(H.flags|=1024),t}function Eo(){var e=go!==0;return go=0,e}function Do(e,t,n){t.updateQueue=e.updateQueue,t.flags&=-2053,e.lanes&=~n}function Oo(e){if(po){for(e=e.memoizedState;e!==null;){var t=e.queue;t!==null&&(t.pending=null),e=e.next}po=!1}uo=0,fo=U=H=null,mo=!1,_o=go=0,vo=null}function ko(){var e={memoizedState:null,baseState:null,baseQueue:null,queue:null,next:null};return fo===null?H.memoizedState=fo=e:fo=fo.next=e,fo}function Ao(){if(U===null){var e=H.alternate;e=e===null?null:e.memoizedState}else e=U.next;var t=fo===null?H.memoizedState:fo.next;if(t!==null)fo=t,U=e;else{if(e===null)throw H.alternate===null?Error(i(467)):Error(i(310));U=e,e={memoizedState:U.memoizedState,baseState:U.baseState,baseQueue:U.baseQueue,queue:U.queue,next:null},fo===null?H.memoizedState=fo=e:fo=fo.next=e}return fo}function jo(){return{lastEffect:null,events:null,stores:null,memoCache:null}}function Mo(e){var t=_o;return _o+=1,vo===null&&(vo=[]),e=Ea(vo,e,t),t=H,(fo===null?t.memoizedState:fo.next)===null&&(t=t.alternate,j.H=t===null||t.memoizedState===null?Rs:zs),e}function No(e){if(typeof e==`object`&&e){if(typeof e.then==`function`)return Mo(e);if(e.$$typeof===C)return ea(e)}throw Error(i(438,String(e)))}function Po(e){var t=null,n=H.updateQueue;if(n!==null&&(t=n.memoCache),t==null){var r=H.alternate;r!==null&&(r=r.updateQueue,r!==null&&(r=r.memoCache,r!=null&&(t={data:r.data.map(function(e){return e.slice()}),index:0})))}if(t??={data:[],index:0},n===null&&(n=jo(),H.updateQueue=n),n.memoCache=t,n=t.data[t.index],n===void 0)for(n=t.data[t.index]=Array(e),r=0;r<e;r++)n[r]=O;return t.index++,n}function Fo(e,t){return typeof t==`function`?t(e):t}function Io(e){return Lo(Ao(),U,e)}function Lo(e,t,n){var r=e.queue;if(r===null)throw Error(i(311));r.lastRenderedReducer=n;var a=e.baseQueue,o=r.pending;if(o!==null){if(a!==null){var s=a.next;a.next=o.next,o.next=s}t.baseQueue=a=o,r.pending=null}if(o=e.baseState,a===null)e.memoizedState=o;else{t=a.next;var c=s=null,l=null,u=t,d=!1;do{var f=u.lane&-536870913;if(f===u.lane?(uo&f)===f:(Y&f)===f){var p=u.revertLane;if(p===0)l!==null&&(l=l.next={lane:0,revertLane:0,gesture:null,action:u.action,hasEagerState:u.hasEagerState,eagerState:u.eagerState,next:null}),f===da&&(d=!0);else if((uo&p)===p){u=u.next,p===da&&(d=!0);continue}else f={lane:0,revertLane:u.revertLane,gesture:null,action:u.action,hasEagerState:u.hasEagerState,eagerState:u.eagerState,next:null},l===null?(c=l=f,s=o):l=l.next=f,H.lanes|=p,Gl|=p;f=u.action,ho&&n(o,f),o=u.hasEagerState?u.eagerState:n(o,f)}else p={lane:f,revertLane:u.revertLane,gesture:u.gesture,action:u.action,hasEagerState:u.hasEagerState,eagerState:u.eagerState,next:null},l===null?(c=l=p,s=o):l=l.next=p,H.lanes|=f,Gl|=f;u=u.next}while(u!==null&&u!==t);if(l===null?s=o:l.next=c,!Cr(o,e.memoizedState)&&(nc=!0,d&&(n=fa,n!==null)))throw n;e.memoizedState=o,e.baseState=s,e.baseQueue=l,r.lastRenderedState=o}return a===null&&(r.lanes=0),[e.memoizedState,r.dispatch]}function Ro(e){var t=Ao(),n=t.queue;if(n===null)throw Error(i(311));n.lastRenderedReducer=e;var r=n.dispatch,a=n.pending,o=t.memoizedState;if(a!==null){n.pending=null;var s=a=a.next;do o=e(o,s.action),s=s.next;while(s!==a);Cr(o,t.memoizedState)||(nc=!0),t.memoizedState=o,t.baseQueue===null&&(t.baseState=o),n.lastRenderedState=o}return[o,r]}function zo(e,t,n){var r=H,a=Ao(),o=B;if(o){if(n===void 0)throw Error(i(407));n=n()}else n=t();var s=!Cr((U||a).memoizedState,n);if(s&&(a.memoizedState=n,nc=!0),a=a.queue,ls(Ho.bind(null,r,a,e),[e]),a.getSnapshot!==t||s||fo!==null&&fo.memoizedState.tag&1){if(r.flags|=2048,is(9,{destroy:void 0},Vo.bind(null,r,a,n,t),null),q===null)throw Error(i(349));o||uo&127||Bo(r,t,n)}return n}function Bo(e,t,n){e.flags|=16384,e={getSnapshot:t,value:n},t=H.updateQueue,t===null?(t=jo(),H.updateQueue=t,t.stores=[e]):(n=t.stores,n===null?t.stores=[e]:n.push(e))}function Vo(e,t,n,r){t.value=n,t.getSnapshot=r,Uo(t)&&Wo(e)}function Ho(e,t,n){return n(function(){Uo(t)&&Wo(e)})}function Uo(e){var t=e.getSnapshot;e=e.value;try{var n=t();return!Cr(e,n)}catch{return!0}}function Wo(e){var t=ii(e,2);t!==null&&hu(t,e,2)}function Go(e){var t=ko();if(typeof e==`function`){var n=e;if(e=n(),ho){Ve(!0);try{n()}finally{Ve(!1)}}}return t.memoizedState=t.baseState=e,t.queue={pending:null,lanes:0,dispatch:null,lastRenderedReducer:Fo,lastRenderedState:e},t}function Ko(e,t,n,r){return e.baseState=n,Lo(e,U,typeof r==`function`?r:Fo)}function qo(e,t,n,r,a){if(Ps(e))throw Error(i(485));if(e=t.action,e!==null){var o={payload:a,action:e,next:null,isTransition:!0,status:`pending`,value:null,reason:null,listeners:[],then:function(e){o.listeners.push(e)}};j.T===null?o.isTransition=!1:n(!0),r(o),n=t.pending,n===null?(o.next=t.pending=o,Jo(t,o)):(o.next=n.next,t.pending=n.next=o)}}function Jo(e,t){var n=t.action,r=t.payload,i=e.state;if(t.isTransition){var a=j.T,o={};j.T=o;try{var s=n(i,r),c=j.S;c!==null&&c(o,s),Yo(e,t,s)}catch(n){Zo(e,t,n)}finally{a!==null&&o.types!==null&&(a.types=o.types),j.T=a}}else try{a=n(i,r),Yo(e,t,a)}catch(n){Zo(e,t,n)}}function Yo(e,t,n){typeof n==`object`&&n&&typeof n.then==`function`?n.then(function(n){Xo(e,t,n)},function(n){return Zo(e,t,n)}):Xo(e,t,n)}function Xo(e,t,n){t.status=`fulfilled`,t.value=n,Qo(t),e.state=n,t=e.pending,t!==null&&(n=t.next,n===t?e.pending=null:(n=n.next,t.next=n,Jo(e,n)))}function Zo(e,t,n){var r=e.pending;if(e.pending=null,r!==null){r=r.next;do t.status=`rejected`,t.reason=n,Qo(t),t=t.next;while(t!==r)}e.action=null}function Qo(e){e=e.listeners;for(var t=0;t<e.length;t++)(0,e[t])()}function $o(e,t){return t}function W(e,t){if(B){var n=q.formState;if(n!==null){a:{var r=H;if(B){if(z){b:{for(var i=z,a=Fi;i.nodeType!==8;){if(!a){i=null;break b}if(i=cf(i.nextSibling),i===null){i=null;break b}}a=i.data,i=a===`F!`||a===`F`?i:null}if(i){z=cf(i.nextSibling),r=i.data===`F!`;break a}}Li(r)}r=!1}r&&(t=n[0])}}return n=ko(),n.memoizedState=n.baseState=t,r={pending:null,lanes:0,dispatch:null,lastRenderedReducer:$o,lastRenderedState:t},n.queue=r,n=js.bind(null,H,r),r.dispatch=n,r=Go(!1),a=Ns.bind(null,H,!1,r.queue),r=ko(),i={state:t,dispatch:null,action:e,pending:null},r.queue=i,n=qo.bind(null,H,i,a,n),i.dispatch=n,r.memoizedState=e,[t,n,!1]}function es(e){return ts(Ao(),U,e)}function ts(e,t,n){if(t=Lo(e,t,$o)[0],e=Io(Fo)[0],typeof t==`object`&&t&&typeof t.then==`function`)try{var r=Mo(t)}catch(e){throw e===xa?Ca:e}else r=t;t=Ao();var i=t.queue,a=i.dispatch;return n!==t.memoizedState&&(H.flags|=2048,is(9,{destroy:void 0},ns.bind(null,i,n),null)),[r,a,e]}function ns(e,t){e.action=t}function rs(e){var t=Ao(),n=U;if(n!==null)return ts(t,n,e);Ao(),t=t.memoizedState,n=Ao();var r=n.queue.dispatch;return n.memoizedState=e,[t,r,!1]}function is(e,t,n,r){return e={tag:e,create:n,deps:r,inst:t,next:null},t=H.updateQueue,t===null&&(t=jo(),H.updateQueue=t),n=t.lastEffect,n===null?t.lastEffect=e.next=e:(r=n.next,n.next=e,e.next=r,t.lastEffect=e),e}function as(){return Ao().memoizedState}function os(e,t,n,r){var i=ko();H.flags|=e,i.memoizedState=is(1|t,{destroy:void 0},n,r===void 0?null:r)}function ss(e,t,n,r){var i=Ao();r=r===void 0?null:r;var a=i.memoizedState.inst;U!==null&&r!==null&&xo(r,U.memoizedState.deps)?i.memoizedState=is(t,a,n,r):(H.flags|=e,i.memoizedState=is(1|t,a,n,r))}function cs(e,t){os(8390656,8,e,t)}function ls(e,t){ss(2048,8,e,t)}function us(e){H.flags|=4;var t=H.updateQueue;if(t===null)t=jo(),H.updateQueue=t,t.events=[e];else{var n=t.events;n===null?t.events=[e]:n.push(e)}}function ds(e){var t=Ao().memoizedState;return us({ref:t,nextImpl:e}),function(){if(K&2)throw Error(i(440));return t.impl.apply(void 0,arguments)}}function fs(e,t){return ss(4,2,e,t)}function ps(e,t){return ss(4,4,e,t)}function ms(e,t){if(typeof t==`function`){e=e();var n=t(e);return function(){typeof n==`function`?n():t(null)}}if(t!=null)return e=e(),t.current=e,function(){t.current=null}}function hs(e,t,n){n=n==null?null:n.concat([e]),ss(4,4,ms.bind(null,t,e),n)}function gs(){}function _s(e,t){var n=Ao();t=t===void 0?null:t;var r=n.memoizedState;return t!==null&&xo(t,r[1])?r[0]:(n.memoizedState=[e,t],e)}function vs(e,t){var n=Ao();t=t===void 0?null:t;var r=n.memoizedState;if(t!==null&&xo(t,r[1]))return r[0];if(r=e(),ho){Ve(!0);try{e()}finally{Ve(!1)}}return n.memoizedState=[r,t],r}function ys(e,t,n){return n===void 0||uo&1073741824&&!(Y&261930)?e.memoizedState=t:(e.memoizedState=n,e=mu(),H.lanes|=e,Gl|=e,n)}function bs(e,t,n,r){return Cr(n,t)?n:Xa.current===null?!(uo&42)||uo&1073741824&&!(Y&261930)?(nc=!0,e.memoizedState=n):(e=mu(),H.lanes|=e,Gl|=e,t):(e=ys(e,n,r),Cr(e,t)||(nc=!0),e)}function xs(e,t,n,r,i){var a=M.p;M.p=a!==0&&8>a?a:8;var o=j.T,s={};j.T=s,Ns(e,!1,t,n);try{var c=i(),l=j.S;l!==null&&l(s,c),typeof c==`object`&&c&&typeof c.then==`function`?Ms(e,t,ha(c,r),pu(e)):Ms(e,t,r,pu(e))}catch(n){Ms(e,t,{then:function(){},status:`rejected`,reason:n},pu())}finally{M.p=a,o!==null&&s.types!==null&&(o.types=s.types),j.T=o}}function Ss(){}function Cs(e,t,n,r){if(e.tag!==5)throw Error(i(476));var a=ws(e).queue;xs(e,a,t,ae,n===null?Ss:function(){return Ts(e),n(r)})}function ws(e){var t=e.memoizedState;if(t!==null)return t;t={memoizedState:ae,baseState:ae,baseQueue:null,queue:{pending:null,lanes:0,dispatch:null,lastRenderedReducer:Fo,lastRenderedState:ae},next:null};var n={};return t.next={memoizedState:n,baseState:n,baseQueue:null,queue:{pending:null,lanes:0,dispatch:null,lastRenderedReducer:Fo,lastRenderedState:n},next:null},e.memoizedState=t,e=e.alternate,e!==null&&(e.memoizedState=t),t}function Ts(e){var t=ws(e);t.next===null&&(t=e.alternate.memoizedState),Ms(e,t.next.queue,{},pu())}function Es(){return ea(Qf)}function Ds(){return Ao().memoizedState}function Os(){return Ao().memoizedState}function ks(e){for(var t=e.return;t!==null;){switch(t.tag){case 24:case 3:var n=pu();e=Va(n);var r=Ha(t,e,n);r!==null&&(hu(r,t,n),Ua(r,t,n)),t={cache:sa()},e.payload=t;return}t=t.return}}function As(e,t,n){var r=pu();n={lane:r,revertLane:0,gesture:null,action:n,hasEagerState:!1,eagerState:null,next:null},Ps(e)?Fs(t,n):(n=ri(e,t,n,r),n!==null&&(hu(n,e,r),Is(n,t,r)))}function js(e,t,n){Ms(e,t,n,pu())}function Ms(e,t,n,r){var i={lane:r,revertLane:0,gesture:null,action:n,hasEagerState:!1,eagerState:null,next:null};if(Ps(e))Fs(t,i);else{var a=e.alternate;if(e.lanes===0&&(a===null||a.lanes===0)&&(a=t.lastRenderedReducer,a!==null))try{var o=t.lastRenderedState,s=a(o,n);if(i.hasEagerState=!0,i.eagerState=s,Cr(s,o))return ni(e,t,i,0),q===null&&ti(),!1}catch{}if(n=ri(e,t,i,r),n!==null)return hu(n,e,r),Is(n,t,r),!0}return!1}function Ns(e,t,n,r){if(r={lane:2,revertLane:dd(),gesture:null,action:r,hasEagerState:!1,eagerState:null,next:null},Ps(e)){if(t)throw Error(i(479))}else t=ri(e,n,r,2),t!==null&&hu(t,e,2)}function Ps(e){var t=e.alternate;return e===H||t!==null&&t===H}function Fs(e,t){mo=po=!0;var n=e.pending;n===null?t.next=t:(t.next=n.next,n.next=t),e.pending=t}function Is(e,t,n){if(n&4194048){var r=t.lanes;r&=e.pendingLanes,n|=r,t.lanes=n,nt(e,n)}}var Ls={readContext:ea,use:No,useCallback:bo,useContext:bo,useEffect:bo,useImperativeHandle:bo,useLayoutEffect:bo,useInsertionEffect:bo,useMemo:bo,useReducer:bo,useRef:bo,useState:bo,useDebugValue:bo,useDeferredValue:bo,useTransition:bo,useSyncExternalStore:bo,useId:bo,useHostTransitionStatus:bo,useFormState:bo,useActionState:bo,useOptimistic:bo,useMemoCache:bo,useCacheRefresh:bo};Ls.useEffectEvent=bo;var Rs={readContext:ea,use:No,useCallback:function(e,t){return ko().memoizedState=[e,t===void 0?null:t],e},useContext:ea,useEffect:cs,useImperativeHandle:function(e,t,n){n=n==null?null:n.concat([e]),os(4194308,4,ms.bind(null,t,e),n)},useLayoutEffect:function(e,t){return os(4194308,4,e,t)},useInsertionEffect:function(e,t){os(4,2,e,t)},useMemo:function(e,t){var n=ko();t=t===void 0?null:t;var r=e();if(ho){Ve(!0);try{e()}finally{Ve(!1)}}return n.memoizedState=[r,t],r},useReducer:function(e,t,n){var r=ko();if(n!==void 0){var i=n(t);if(ho){Ve(!0);try{n(t)}finally{Ve(!1)}}}else i=t;return r.memoizedState=r.baseState=i,e={pending:null,lanes:0,dispatch:null,lastRenderedReducer:e,lastRenderedState:i},r.queue=e,e=e.dispatch=As.bind(null,H,e),[r.memoizedState,e]},useRef:function(e){var t=ko();return e={current:e},t.memoizedState=e},useState:function(e){e=Go(e);var t=e.queue,n=js.bind(null,H,t);return t.dispatch=n,[e.memoizedState,n]},useDebugValue:gs,useDeferredValue:function(e,t){return ys(ko(),e,t)},useTransition:function(){var e=Go(!1);return e=xs.bind(null,H,e.queue,!0,!1),ko().memoizedState=e,[!1,e]},useSyncExternalStore:function(e,t,n){var r=H,a=ko();if(B){if(n===void 0)throw Error(i(407));n=n()}else{if(n=t(),q===null)throw Error(i(349));Y&127||Bo(r,t,n)}a.memoizedState=n;var o={value:n,getSnapshot:t};return a.queue=o,cs(Ho.bind(null,r,o,e),[e]),r.flags|=2048,is(9,{destroy:void 0},Vo.bind(null,r,o,n,t),null),n},useId:function(){var e=ko(),t=q.identifierPrefix;if(B){var n=Di,r=Ei;n=(r&~(1<<32-P(r)-1)).toString(32)+n,t=`_`+t+`R_`+n,n=go++,0<n&&(t+=`H`+n.toString(32)),t+=`_`}else n=yo++,t=`_`+t+`r_`+n.toString(32)+`_`;return e.memoizedState=t},useHostTransitionStatus:Es,useFormState:W,useActionState:W,useOptimistic:function(e){var t=ko();t.memoizedState=t.baseState=e;var n={pending:null,lanes:0,dispatch:null,lastRenderedReducer:null,lastRenderedState:null};return t.queue=n,t=Ns.bind(null,H,!0,n),n.dispatch=t,[e,t]},useMemoCache:Po,useCacheRefresh:function(){return ko().memoizedState=ks.bind(null,H)},useEffectEvent:function(e){var t=ko(),n={impl:e};return t.memoizedState=n,function(){if(K&2)throw Error(i(440));return n.impl.apply(void 0,arguments)}}},zs={readContext:ea,use:No,useCallback:_s,useContext:ea,useEffect:ls,useImperativeHandle:hs,useInsertionEffect:fs,useLayoutEffect:ps,useMemo:vs,useReducer:Io,useRef:as,useState:function(){return Io(Fo)},useDebugValue:gs,useDeferredValue:function(e,t){return bs(Ao(),U.memoizedState,e,t)},useTransition:function(){var e=Io(Fo)[0],t=Ao().memoizedState;return[typeof e==`boolean`?e:Mo(e),t]},useSyncExternalStore:zo,useId:Ds,useHostTransitionStatus:Es,useFormState:es,useActionState:es,useOptimistic:function(e,t){return Ko(Ao(),U,e,t)},useMemoCache:Po,useCacheRefresh:Os};zs.useEffectEvent=ds;var Bs={readContext:ea,use:No,useCallback:_s,useContext:ea,useEffect:ls,useImperativeHandle:hs,useInsertionEffect:fs,useLayoutEffect:ps,useMemo:vs,useReducer:Ro,useRef:as,useState:function(){return Ro(Fo)},useDebugValue:gs,useDeferredValue:function(e,t){var n=Ao();return U===null?ys(n,e,t):bs(n,U.memoizedState,e,t)},useTransition:function(){var e=Ro(Fo)[0],t=Ao().memoizedState;return[typeof e==`boolean`?e:Mo(e),t]},useSyncExternalStore:zo,useId:Ds,useHostTransitionStatus:Es,useFormState:rs,useActionState:rs,useOptimistic:function(e,t){var n=Ao();return U===null?(n.baseState=e,[e,n.queue.dispatch]):Ko(n,U,e,t)},useMemoCache:Po,useCacheRefresh:Os};Bs.useEffectEvent=ds;function Vs(e,t,n,r){t=e.memoizedState,n=n(r,t),n=n==null?t:h({},t,n),e.memoizedState=n,e.lanes===0&&(e.updateQueue.baseState=n)}var Hs={enqueueSetState:function(e,t,n){e=e._reactInternals;var r=pu(),i=Va(r);i.payload=t,n!=null&&(i.callback=n),t=Ha(e,i,r),t!==null&&(hu(t,e,r),Ua(t,e,r))},enqueueReplaceState:function(e,t,n){e=e._reactInternals;var r=pu(),i=Va(r);i.tag=1,i.payload=t,n!=null&&(i.callback=n),t=Ha(e,i,r),t!==null&&(hu(t,e,r),Ua(t,e,r))},enqueueForceUpdate:function(e,t){e=e._reactInternals;var n=pu(),r=Va(n);r.tag=2,t!=null&&(r.callback=t),t=Ha(e,r,n),t!==null&&(hu(t,e,n),Ua(t,e,n))}};function Us(e,t,n,r,i,a,o){return e=e.stateNode,typeof e.shouldComponentUpdate==`function`?e.shouldComponentUpdate(r,a,o):t.prototype&&t.prototype.isPureReactComponent?!wr(n,r)||!wr(i,a):!0}function Ws(e,t,n,r){e=t.state,typeof t.componentWillReceiveProps==`function`&&t.componentWillReceiveProps(n,r),typeof t.UNSAFE_componentWillReceiveProps==`function`&&t.UNSAFE_componentWillReceiveProps(n,r),t.state!==e&&Hs.enqueueReplaceState(t,t.state,null)}function Gs(e,t){var n=t;if(`ref`in t)for(var r in n={},t)r!==`ref`&&(n[r]=t[r]);if(e=e.defaultProps)for(var i in n===t&&(n=h({},n)),e)n[i]===void 0&&(n[i]=e[i]);return n}function Ks(e){Zr(e)}function qs(e){console.error(e)}function Js(e){Zr(e)}function Ys(e,t){try{var n=e.onUncaughtError;n(t.value,{componentStack:t.stack})}catch(e){setTimeout(function(){throw e})}}function Xs(e,t,n){try{var r=e.onCaughtError;r(n.value,{componentStack:n.stack,errorBoundary:t.tag===1?t.stateNode:null})}catch(e){setTimeout(function(){throw e})}}function Zs(e,t,n){return n=Va(n),n.tag=3,n.payload={element:null},n.callback=function(){Ys(e,t)},n}function Qs(e){return e=Va(e),e.tag=3,e}function $s(e,t,n,r){var i=n.type.getDerivedStateFromError;if(typeof i==`function`){var a=r.value;e.payload=function(){return i(a)},e.callback=function(){Xs(t,n,r)}}var o=n.stateNode;o!==null&&typeof o.componentDidCatch==`function`&&(e.callback=function(){Xs(t,n,r),typeof i!=`function`&&(ru===null?ru=new Set([this]):ru.add(this));var e=r.stack;this.componentDidCatch(r.value,{componentStack:e===null?``:e})})}function ec(e,t,n,r,a){if(n.flags|=32768,typeof r==`object`&&r&&typeof r.then==`function`){if(t=n.alternate,t!==null&&Zi(t,n,a,!0),n=to.current,n!==null){switch(n.tag){case 31:case 13:return no===null?Du():n.alternate===null&&Wl===0&&(Wl=3),n.flags&=-257,n.flags|=65536,n.lanes=a,r===wa?n.flags|=16384:(t=n.updateQueue,t===null?n.updateQueue=new Set([r]):t.add(r),Gu(e,r,a)),!1;case 22:return n.flags|=65536,r===wa?n.flags|=16384:(t=n.updateQueue,t===null?(t={transitions:null,markerInstances:null,retryQueue:new Set([r])},n.updateQueue=t):(n=t.retryQueue,n===null?t.retryQueue=new Set([r]):n.add(r)),Gu(e,r,a)),!1}throw Error(i(435,n.tag))}return Gu(e,r,a),Du(),!1}if(B)return t=to.current,t===null?(r!==Ii&&(t=Error(i(423),{cause:r}),Ui(vi(t,n))),e=e.current.alternate,e.flags|=65536,a&=-a,e.lanes|=a,r=vi(r,n),a=Zs(e.stateNode,r,a),Wa(e,a),Wl!==4&&(Wl=2)):(!(t.flags&65536)&&(t.flags|=256),t.flags|=65536,t.lanes=a,r!==Ii&&(e=Error(i(422),{cause:r}),Ui(vi(e,n)))),!1;var o=Error(i(520),{cause:r});if(o=vi(o,n),Xl===null?Xl=[o]:Xl.push(o),Wl!==4&&(Wl=2),t===null)return!0;r=vi(r,n),n=t;do{switch(n.tag){case 3:return n.flags|=65536,e=a&-a,n.lanes|=e,e=Zs(n.stateNode,r,e),Wa(n,e),!1;case 1:if(t=n.type,o=n.stateNode,!(n.flags&128)&&(typeof t.getDerivedStateFromError==`function`||o!==null&&typeof o.componentDidCatch==`function`&&(ru===null||!ru.has(o))))return n.flags|=65536,a&=-a,n.lanes|=a,a=Qs(a),$s(a,e,n,r),Wa(n,a),!1}n=n.return}while(n!==null);return!1}var tc=Error(i(461)),nc=!1;function rc(e,t,n,r){t.child=e===null?La(t,null,n,r):Ia(t,e.child,n,r)}function ic(e,t,n,r,i){n=n.render;var a=t.ref;if(`ref`in r){var o={};for(var s in r)s!==`ref`&&(o[s]=r[s])}else o=r;return $i(t),r=So(e,t,n,o,a,i),s=Eo(),e!==null&&!nc?(Do(e,t,i),Oc(e,t,i)):(B&&s&&Ai(t),t.flags|=1,rc(e,t,r,i),t.child)}function ac(e,t,n,r,i){if(e===null){var a=n.type;return typeof a==`function`&&!ui(a)&&a.defaultProps===void 0&&n.compare===null?(t.tag=15,t.type=a,oc(e,t,a,r,i)):(e=fi(n.type,null,r,t,t.mode,i),e.ref=t.ref,e.return=t,t.child=e)}if(a=e.child,!kc(e,i)){var o=a.memoizedProps;if(n=n.compare,n=n===null?wr:n,n(o,r)&&e.ref===t.ref)return Oc(e,t,i)}return t.flags|=1,e=di(a,r),e.ref=t.ref,e.return=t,t.child=e}function oc(e,t,n,r,i){if(e!==null){var a=e.memoizedProps;if(wr(a,r)&&e.ref===t.ref)if(nc=!1,t.pendingProps=r=a,kc(e,i))e.flags&131072&&(nc=!0);else return t.lanes=e.lanes,Oc(e,t,i)}return mc(e,t,n,r,i)}function sc(e,t,n,r){var i=r.children,a=e===null?null:e.memoizedState;if(e===null&&t.stateNode===null&&(t.stateNode={_visibility:1,_pendingMarkers:null,_retryCache:null,_transitions:null}),r.mode===`hidden`){if(t.flags&128){if(a=a===null?n:a.baseLanes|n,e!==null){for(r=t.child=e.child,i=0;r!==null;)i=i|r.lanes|r.childLanes,r=r.sibling;r=i&~a}else r=0,t.child=null;return lc(e,t,a,n,r)}if(n&536870912)t.memoizedState={baseLanes:0,cachePool:null},e!==null&&ya(t,a===null?null:a.cachePool),a===null?$a():Qa(t,a),ao(t);else return r=t.lanes=536870912,lc(e,t,a===null?n:a.baseLanes|n,n,r)}else a===null?(e!==null&&ya(t,null),$a(),oo(t)):(ya(t,a.cachePool),Qa(t,a),oo(t),t.memoizedState=null);return rc(e,t,i,n),t.child}function cc(e,t){return e!==null&&e.tag===22||t.stateNode!==null||(t.stateNode={_visibility:1,_pendingMarkers:null,_retryCache:null,_transitions:null}),t.sibling}function lc(e,t,n,r,i){var a=va();return a=a===null?null:{parent:oa._currentValue,pool:a},t.memoizedState={baseLanes:n,cachePool:a},e!==null&&ya(t,null),$a(),ao(t),e!==null&&Zi(e,t,r,!0),t.childLanes=i,null}function uc(e,t){return t=Cc({mode:t.mode,children:t.children},e.mode),t.ref=e.ref,e.child=t,t.return=e,t}function dc(e,t,n){return Ia(t,e.child,null,n),e=uc(t,t.pendingProps),e.flags|=2,so(t),t.memoizedState=null,e}function fc(e,t,n){var r=t.pendingProps,a=(t.flags&128)!=0;if(t.flags&=-129,e===null){if(B){if(r.mode===`hidden`)return e=uc(t,r),t.lanes=536870912,cc(null,e);if(io(t),(e=z)?(e=rf(e,Fi),e=e!==null&&e.data===`&`?e:null,e!==null&&(t.memoizedState={dehydrated:e,treeContext:Ti===null?null:{id:Ei,overflow:Di},retryLane:536870912,hydrationErrors:null},n=hi(e),n.return=t,t.child=n,Ni=t,z=null)):e=null,e===null)throw Li(t);return t.lanes=536870912,null}return uc(t,r)}var o=e.memoizedState;if(o!==null){var s=o.dehydrated;if(io(t),a)if(t.flags&256)t.flags&=-257,t=dc(e,t,n);else if(t.memoizedState!==null)t.child=e.child,t.flags|=128,t=null;else throw Error(i(558));else if(nc||Zi(e,t,n,!1),a=(n&e.childLanes)!==0,nc||a){if(r=q,r!==null&&(s=rt(r,n),s!==0&&s!==o.retryLane))throw o.retryLane=s,ii(e,s),hu(r,e,s),tc;Du(),t=dc(e,t,n)}else e=o.treeContext,z=cf(s.nextSibling),Ni=t,B=!0,Pi=null,Fi=!1,e!==null&&Mi(t,e),t=uc(t,r),t.flags|=4096;return t}return e=di(e.child,{mode:r.mode,children:r.children}),e.ref=t.ref,t.child=e,e.return=t,e}function pc(e,t){var n=t.ref;if(n===null)e!==null&&e.ref!==null&&(t.flags|=4194816);else{if(typeof n!=`function`&&typeof n!=`object`)throw Error(i(284));(e===null||e.ref!==n)&&(t.flags|=4194816)}}function mc(e,t,n,r,i){return $i(t),n=So(e,t,n,r,void 0,i),r=Eo(),e!==null&&!nc?(Do(e,t,i),Oc(e,t,i)):(B&&r&&Ai(t),t.flags|=1,rc(e,t,n,i),t.child)}function hc(e,t,n,r,i,a){return $i(t),t.updateQueue=null,n=wo(t,r,n,i),Co(e),r=Eo(),e!==null&&!nc?(Do(e,t,a),Oc(e,t,a)):(B&&r&&Ai(t),t.flags|=1,rc(e,t,n,a),t.child)}function gc(e,t,n,r,i){if($i(t),t.stateNode===null){var a=si,o=n.contextType;typeof o==`object`&&o&&(a=ea(o)),a=new n(r,a),t.memoizedState=a.state!==null&&a.state!==void 0?a.state:null,a.updater=Hs,t.stateNode=a,a._reactInternals=t,a=t.stateNode,a.props=r,a.state=t.memoizedState,a.refs={},za(t),o=n.contextType,a.context=typeof o==`object`&&o?ea(o):si,a.state=t.memoizedState,o=n.getDerivedStateFromProps,typeof o==`function`&&(Vs(t,n,o,r),a.state=t.memoizedState),typeof n.getDerivedStateFromProps==`function`||typeof a.getSnapshotBeforeUpdate==`function`||typeof a.UNSAFE_componentWillMount!=`function`&&typeof a.componentWillMount!=`function`||(o=a.state,typeof a.componentWillMount==`function`&&a.componentWillMount(),typeof a.UNSAFE_componentWillMount==`function`&&a.UNSAFE_componentWillMount(),o!==a.state&&Hs.enqueueReplaceState(a,a.state,null),qa(t,r,a,i),Ka(),a.state=t.memoizedState),typeof a.componentDidMount==`function`&&(t.flags|=4194308),r=!0}else if(e===null){a=t.stateNode;var s=t.memoizedProps,c=Gs(n,s);a.props=c;var l=a.context,u=n.contextType;o=si,typeof u==`object`&&u&&(o=ea(u));var d=n.getDerivedStateFromProps;u=typeof d==`function`||typeof a.getSnapshotBeforeUpdate==`function`,s=t.pendingProps!==s,u||typeof a.UNSAFE_componentWillReceiveProps!=`function`&&typeof a.componentWillReceiveProps!=`function`||(s||l!==o)&&Ws(t,a,r,o),Ra=!1;var f=t.memoizedState;a.state=f,qa(t,r,a,i),Ka(),l=t.memoizedState,s||f!==l||Ra?(typeof d==`function`&&(Vs(t,n,d,r),l=t.memoizedState),(c=Ra||Us(t,n,c,r,f,l,o))?(u||typeof a.UNSAFE_componentWillMount!=`function`&&typeof a.componentWillMount!=`function`||(typeof a.componentWillMount==`function`&&a.componentWillMount(),typeof a.UNSAFE_componentWillMount==`function`&&a.UNSAFE_componentWillMount()),typeof a.componentDidMount==`function`&&(t.flags|=4194308)):(typeof a.componentDidMount==`function`&&(t.flags|=4194308),t.memoizedProps=r,t.memoizedState=l),a.props=r,a.state=l,a.context=o,r=c):(typeof a.componentDidMount==`function`&&(t.flags|=4194308),r=!1)}else{a=t.stateNode,Ba(e,t),o=t.memoizedProps,u=Gs(n,o),a.props=u,d=t.pendingProps,f=a.context,l=n.contextType,c=si,typeof l==`object`&&l&&(c=ea(l)),s=n.getDerivedStateFromProps,(l=typeof s==`function`||typeof a.getSnapshotBeforeUpdate==`function`)||typeof a.UNSAFE_componentWillReceiveProps!=`function`&&typeof a.componentWillReceiveProps!=`function`||(o!==d||f!==c)&&Ws(t,a,r,c),Ra=!1,f=t.memoizedState,a.state=f,qa(t,r,a,i),Ka();var p=t.memoizedState;o!==d||f!==p||Ra||e!==null&&e.dependencies!==null&&Qi(e.dependencies)?(typeof s==`function`&&(Vs(t,n,s,r),p=t.memoizedState),(u=Ra||Us(t,n,u,r,f,p,c)||e!==null&&e.dependencies!==null&&Qi(e.dependencies))?(l||typeof a.UNSAFE_componentWillUpdate!=`function`&&typeof a.componentWillUpdate!=`function`||(typeof a.componentWillUpdate==`function`&&a.componentWillUpdate(r,p,c),typeof a.UNSAFE_componentWillUpdate==`function`&&a.UNSAFE_componentWillUpdate(r,p,c)),typeof a.componentDidUpdate==`function`&&(t.flags|=4),typeof a.getSnapshotBeforeUpdate==`function`&&(t.flags|=1024)):(typeof a.componentDidUpdate!=`function`||o===e.memoizedProps&&f===e.memoizedState||(t.flags|=4),typeof a.getSnapshotBeforeUpdate!=`function`||o===e.memoizedProps&&f===e.memoizedState||(t.flags|=1024),t.memoizedProps=r,t.memoizedState=p),a.props=r,a.state=p,a.context=c,r=u):(typeof a.componentDidUpdate!=`function`||o===e.memoizedProps&&f===e.memoizedState||(t.flags|=4),typeof a.getSnapshotBeforeUpdate!=`function`||o===e.memoizedProps&&f===e.memoizedState||(t.flags|=1024),r=!1)}return a=r,pc(e,t),r=(t.flags&128)!=0,a||r?(a=t.stateNode,n=r&&typeof n.getDerivedStateFromError!=`function`?null:a.render(),t.flags|=1,e!==null&&r?(t.child=Ia(t,e.child,null,i),t.child=Ia(t,null,n,i)):rc(e,t,n,i),t.memoizedState=a.state,e=t.child):e=Oc(e,t,i),e}function _c(e,t,n,r){return Vi(),t.flags|=256,rc(e,t,n,r),t.child}var vc={dehydrated:null,treeContext:null,retryLane:0,hydrationErrors:null};function yc(e){return{baseLanes:e,cachePool:ba()}}function bc(e,t,n){return e=e===null?0:e.childLanes&~n,t&&(e|=Jl),e}function xc(e,t,n){var r=t.pendingProps,a=!1,o=(t.flags&128)!=0,s;if((s=o)||(s=e!==null&&e.memoizedState===null?!1:(co.current&2)!=0),s&&(a=!0,t.flags&=-129),s=(t.flags&32)!=0,t.flags&=-33,e===null){if(B){if(a?ro(t):oo(t),(e=z)?(e=rf(e,Fi),e=e!==null&&e.data!==`&`?e:null,e!==null&&(t.memoizedState={dehydrated:e,treeContext:Ti===null?null:{id:Ei,overflow:Di},retryLane:536870912,hydrationErrors:null},n=hi(e),n.return=t,t.child=n,Ni=t,z=null)):e=null,e===null)throw Li(t);return of(e)?t.lanes=32:t.lanes=536870912,null}var c=r.children;return r=r.fallback,a?(oo(t),a=t.mode,c=Cc({mode:`hidden`,children:c},a),r=pi(r,a,n,null),c.return=t,r.return=t,c.sibling=r,t.child=c,r=t.child,r.memoizedState=yc(n),r.childLanes=bc(e,s,n),t.memoizedState=vc,cc(null,r)):(ro(t),Sc(t,c))}var l=e.memoizedState;if(l!==null&&(c=l.dehydrated,c!==null)){if(o)t.flags&256?(ro(t),t.flags&=-257,t=wc(e,t,n)):t.memoizedState===null?(oo(t),c=r.fallback,a=t.mode,r=Cc({mode:`visible`,children:r.children},a),c=pi(c,a,n,null),c.flags|=2,r.return=t,c.return=t,r.sibling=c,t.child=r,Ia(t,e.child,null,n),r=t.child,r.memoizedState=yc(n),r.childLanes=bc(e,s,n),t.memoizedState=vc,t=cc(null,r)):(oo(t),t.child=e.child,t.flags|=128,t=null);else if(ro(t),of(c)){if(s=c.nextSibling&&c.nextSibling.dataset,s)var u=s.dgst;s=u,r=Error(i(419)),r.stack=``,r.digest=s,Ui({value:r,source:null,stack:null}),t=wc(e,t,n)}else if(nc||Zi(e,t,n,!1),s=(n&e.childLanes)!==0,nc||s){if(s=q,s!==null&&(r=rt(s,n),r!==0&&r!==l.retryLane))throw l.retryLane=r,ii(e,r),hu(s,e,r),tc;af(c)||Du(),t=wc(e,t,n)}else af(c)?(t.flags|=192,t.child=e.child,t=null):(e=l.treeContext,z=cf(c.nextSibling),Ni=t,B=!0,Pi=null,Fi=!1,e!==null&&Mi(t,e),t=Sc(t,r.children),t.flags|=4096);return t}return a?(oo(t),c=r.fallback,a=t.mode,l=e.child,u=l.sibling,r=di(l,{mode:`hidden`,children:r.children}),r.subtreeFlags=l.subtreeFlags&65011712,u===null?(c=pi(c,a,n,null),c.flags|=2):c=di(u,c),c.return=t,r.return=t,r.sibling=c,t.child=r,cc(null,r),r=t.child,c=e.child.memoizedState,c===null?c=yc(n):(a=c.cachePool,a===null?a=ba():(l=oa._currentValue,a=a.parent===l?a:{parent:l,pool:l}),c={baseLanes:c.baseLanes|n,cachePool:a}),r.memoizedState=c,r.childLanes=bc(e,s,n),t.memoizedState=vc,cc(e.child,r)):(ro(t),n=e.child,e=n.sibling,n=di(n,{mode:`visible`,children:r.children}),n.return=t,n.sibling=null,e!==null&&(s=t.deletions,s===null?(t.deletions=[e],t.flags|=16):s.push(e)),t.child=n,t.memoizedState=null,n)}function Sc(e,t){return t=Cc({mode:`visible`,children:t},e.mode),t.return=e,e.child=t}function Cc(e,t){return e=li(22,e,null,t),e.lanes=0,e}function wc(e,t,n){return Ia(t,e.child,null,n),e=Sc(t,t.pendingProps.children),e.flags|=2,t.memoizedState=null,e}function Tc(e,t,n){e.lanes|=t;var r=e.alternate;r!==null&&(r.lanes|=t),Yi(e.return,t,n)}function Ec(e,t,n,r,i,a){var o=e.memoizedState;o===null?e.memoizedState={isBackwards:t,rendering:null,renderingStartTime:0,last:r,tail:n,tailMode:i,treeForkCount:a}:(o.isBackwards=t,o.rendering=null,o.renderingStartTime=0,o.last=r,o.tail=n,o.tailMode=i,o.treeForkCount=a)}function Dc(e,t,n){var r=t.pendingProps,i=r.revealOrder,a=r.tail;r=r.children;var o=co.current,s=(o&2)!=0;if(s?(o=o&1|2,t.flags|=128):o&=1,N(co,o),rc(e,t,r,n),r=B?Si:0,!s&&e!==null&&e.flags&128)a:for(e=t.child;e!==null;){if(e.tag===13)e.memoizedState!==null&&Tc(e,n,t);else if(e.tag===19)Tc(e,n,t);else if(e.child!==null){e.child.return=e,e=e.child;continue}if(e===t)break a;for(;e.sibling===null;){if(e.return===null||e.return===t)break a;e=e.return}e.sibling.return=e.return,e=e.sibling}switch(i){case`forwards`:for(n=t.child,i=null;n!==null;)e=n.alternate,e!==null&&lo(e)===null&&(i=n),n=n.sibling;n=i,n===null?(i=t.child,t.child=null):(i=n.sibling,n.sibling=null),Ec(t,!1,i,n,a,r);break;case`backwards`:case`unstable_legacy-backwards`:for(n=null,i=t.child,t.child=null;i!==null;){if(e=i.alternate,e!==null&&lo(e)===null){t.child=i;break}e=i.sibling,i.sibling=n,n=i,i=e}Ec(t,!0,n,null,a,r);break;case`together`:Ec(t,!1,null,null,void 0,r);break;default:t.memoizedState=null}return t.child}function Oc(e,t,n){if(e!==null&&(t.dependencies=e.dependencies),Gl|=t.lanes,(n&t.childLanes)===0)if(e!==null){if(Zi(e,t,n,!1),(n&t.childLanes)===0)return null}else return null;if(e!==null&&t.child!==e.child)throw Error(i(153));if(t.child!==null){for(e=t.child,n=di(e,e.pendingProps),t.child=n,n.return=t;e.sibling!==null;)e=e.sibling,n=n.sibling=di(e,e.pendingProps),n.return=t;n.sibling=null}return t.child}function kc(e,t){return(e.lanes&t)===0?(e=e.dependencies,!!(e!==null&&Qi(e))):!0}function Ac(e,t,n){switch(t.tag){case 3:me(t,t.stateNode.containerInfo),qi(t,oa,e.memoizedState.cache),Vi();break;case 27:case 5:ge(t);break;case 4:me(t,t.stateNode.containerInfo);break;case 10:qi(t,t.type,t.memoizedProps.value);break;case 31:if(t.memoizedState!==null)return t.flags|=128,io(t),null;break;case 13:var r=t.memoizedState;if(r!==null)return r.dehydrated===null?(n&t.child.childLanes)===0?(ro(t),e=Oc(e,t,n),e===null?null:e.sibling):xc(e,t,n):(ro(t),t.flags|=128,null);ro(t);break;case 19:var i=(e.flags&128)!=0;if(r=(n&t.childLanes)!==0,r||=(Zi(e,t,n,!1),(n&t.childLanes)!==0),i){if(r)return Dc(e,t,n);t.flags|=128}if(i=t.memoizedState,i!==null&&(i.rendering=null,i.tail=null,i.lastEffect=null),N(co,co.current),r)break;return null;case 22:return t.lanes=0,sc(e,t,n,t.pendingProps);case 24:qi(t,oa,e.memoizedState.cache)}return Oc(e,t,n)}function jc(e,t,n){if(e!==null)if(e.memoizedProps!==t.pendingProps)nc=!0;else{if(!kc(e,n)&&!(t.flags&128))return nc=!1,Ac(e,t,n);nc=!!(e.flags&131072)}else nc=!1,B&&t.flags&1048576&&ki(t,Si,t.index);switch(t.lanes=0,t.tag){case 16:a:{var r=t.pendingProps;if(e=Da(t.elementType),t.type=e,typeof e==`function`)ui(e)?(r=Gs(e,r),t.tag=1,t=gc(null,t,e,r,n)):(t.tag=0,t=mc(null,t,e,r,n));else{if(e!=null){var a=e.$$typeof;if(a===w){t.tag=11,t=ic(null,t,e,r,n);break a}else if(a===E){t.tag=14,t=ac(null,t,e,r,n);break a}}throw t=A(e)||e,Error(i(306,t,``))}}return t;case 0:return mc(e,t,t.type,t.pendingProps,n);case 1:return r=t.type,a=Gs(r,t.pendingProps),gc(e,t,r,a,n);case 3:a:{if(me(t,t.stateNode.containerInfo),e===null)throw Error(i(387));r=t.pendingProps;var o=t.memoizedState;a=o.element,Ba(e,t),qa(t,r,null,n);var s=t.memoizedState;if(r=s.cache,qi(t,oa,r),r!==o.cache&&Xi(t,[oa],n,!0),Ka(),r=s.element,o.isDehydrated)if(o={element:r,isDehydrated:!1,cache:s.cache},t.updateQueue.baseState=o,t.memoizedState=o,t.flags&256){t=_c(e,t,r,n);break a}else if(r!==a){a=vi(Error(i(424)),t),Ui(a),t=_c(e,t,r,n);break a}else{switch(e=t.stateNode.containerInfo,e.nodeType){case 9:e=e.body;break;default:e=e.nodeName===`HTML`?e.ownerDocument.body:e}for(z=cf(e.firstChild),Ni=t,B=!0,Pi=null,Fi=!0,n=La(t,null,r,n),t.child=n;n;)n.flags=n.flags&-3|4096,n=n.sibling}else{if(Vi(),r===a){t=Oc(e,t,n);break a}rc(e,t,r,n)}t=t.child}return t;case 26:return pc(e,t),e===null?(n=kf(t.type,null,t.pendingProps,null))?t.memoizedState=n:B||(n=t.type,e=t.pendingProps,r=Bd(fe.current).createElement(n),r[ct]=t,r[lt]=e,Pd(r,n,e),xt(r),t.stateNode=r):t.memoizedState=kf(t.type,e.memoizedProps,t.pendingProps,e.memoizedState),null;case 27:return ge(t),e===null&&B&&(r=t.stateNode=ff(t.type,t.pendingProps,fe.current),Ni=t,Fi=!0,a=z,Zd(t.type)?(lf=a,z=cf(r.firstChild)):z=a),rc(e,t,t.pendingProps.children,n),pc(e,t),e===null&&(t.flags|=4194304),t.child;case 5:return e===null&&B&&((a=r=z)&&(r=tf(r,t.type,t.pendingProps,Fi),r===null?a=!1:(t.stateNode=r,Ni=t,z=cf(r.firstChild),Fi=!1,a=!0)),a||Li(t)),ge(t),a=t.type,o=t.pendingProps,s=e===null?null:e.memoizedProps,r=o.children,Ud(a,o)?r=null:s!==null&&Ud(a,s)&&(t.flags|=32),t.memoizedState!==null&&(a=So(e,t,To,null,null,n),Qf._currentValue=a),pc(e,t),rc(e,t,r,n),t.child;case 6:return e===null&&B&&((e=n=z)&&(n=nf(n,t.pendingProps,Fi),n===null?e=!1:(t.stateNode=n,Ni=t,z=null,e=!0)),e||Li(t)),null;case 13:return xc(e,t,n);case 4:return me(t,t.stateNode.containerInfo),r=t.pendingProps,e===null?t.child=Ia(t,null,r,n):rc(e,t,r,n),t.child;case 11:return ic(e,t,t.type,t.pendingProps,n);case 7:return rc(e,t,t.pendingProps,n),t.child;case 8:return rc(e,t,t.pendingProps.children,n),t.child;case 12:return rc(e,t,t.pendingProps.children,n),t.child;case 10:return r=t.pendingProps,qi(t,t.type,r.value),rc(e,t,r.children,n),t.child;case 9:return a=t.type._context,r=t.pendingProps.children,$i(t),a=ea(a),r=r(a),t.flags|=1,rc(e,t,r,n),t.child;case 14:return ac(e,t,t.type,t.pendingProps,n);case 15:return oc(e,t,t.type,t.pendingProps,n);case 19:return Dc(e,t,n);case 31:return fc(e,t,n);case 22:return sc(e,t,n,t.pendingProps);case 24:return $i(t),r=ea(oa),e===null?(a=va(),a===null&&(a=q,o=sa(),a.pooledCache=o,o.refCount++,o!==null&&(a.pooledCacheLanes|=n),a=o),t.memoizedState={parent:r,cache:a},za(t),qi(t,oa,a)):((e.lanes&n)!==0&&(Ba(e,t),qa(t,null,null,n),Ka()),a=e.memoizedState,o=t.memoizedState,a.parent===r?(r=o.cache,qi(t,oa,r),r!==a.cache&&Xi(t,[oa],n,!0)):(a={parent:r,cache:r},t.memoizedState=a,t.lanes===0&&(t.memoizedState=t.updateQueue.baseState=a),qi(t,oa,r))),rc(e,t,t.pendingProps.children,n),t.child;case 29:throw t.pendingProps}throw Error(i(156,t.tag))}function Mc(e){e.flags|=4}function Nc(e,t,n,r,i){if((t=(e.mode&32)!=0)&&(t=!1),t){if(e.flags|=16777216,(i&335544128)===i)if(e.stateNode.complete)e.flags|=8192;else if(wu())e.flags|=8192;else throw V=wa,Sa}else e.flags&=-16777217}function Pc(e,t){if(t.type!==`stylesheet`||t.state.loading&4)e.flags&=-16777217;else if(e.flags|=16777216,!Wf(t))if(wu())e.flags|=8192;else throw V=wa,Sa}function Fc(e,t){t!==null&&(e.flags|=4),e.flags&16384&&(t=e.tag===22?536870912:Ze(),e.lanes|=t,Yl|=t)}function Ic(e,t){if(!B)switch(e.tailMode){case`hidden`:t=e.tail;for(var n=null;t!==null;)t.alternate!==null&&(n=t),t=t.sibling;n===null?e.tail=null:n.sibling=null;break;case`collapsed`:n=e.tail;for(var r=null;n!==null;)n.alternate!==null&&(r=n),n=n.sibling;r===null?t||e.tail===null?e.tail=null:e.tail.sibling=null:r.sibling=null}}function G(e){var t=e.alternate!==null&&e.alternate.child===e.child,n=0,r=0;if(t)for(var i=e.child;i!==null;)n|=i.lanes|i.childLanes,r|=i.subtreeFlags&65011712,r|=i.flags&65011712,i.return=e,i=i.sibling;else for(i=e.child;i!==null;)n|=i.lanes|i.childLanes,r|=i.subtreeFlags,r|=i.flags,i.return=e,i=i.sibling;return e.subtreeFlags|=r,e.childLanes=n,t}function Lc(e,t,n){var r=t.pendingProps;switch(ji(t),t.tag){case 16:case 15:case 0:case 11:case 7:case 8:case 12:case 9:case 14:return G(t),null;case 1:return G(t),null;case 3:return n=t.stateNode,r=null,e!==null&&(r=e.memoizedState.cache),t.memoizedState.cache!==r&&(t.flags|=2048),Ji(oa),he(),n.pendingContext&&(n.context=n.pendingContext,n.pendingContext=null),(e===null||e.child===null)&&(Bi(t)?Mc(t):e===null||e.memoizedState.isDehydrated&&!(t.flags&256)||(t.flags|=1024,Hi())),G(t),null;case 26:var a=t.type,o=t.memoizedState;return e===null?(Mc(t),o===null?(G(t),Nc(t,a,null,r,n)):(G(t),Pc(t,o))):o?o===e.memoizedState?(G(t),t.flags&=-16777217):(Mc(t),G(t),Pc(t,o)):(e=e.memoizedProps,e!==r&&Mc(t),G(t),Nc(t,a,e,r,n)),null;case 27:if(_e(t),n=fe.current,a=t.type,e!==null&&t.stateNode!=null)e.memoizedProps!==r&&Mc(t);else{if(!r){if(t.stateNode===null)throw Error(i(166));return G(t),null}e=ue.current,Bi(t)?Ri(t,e):(e=ff(a,r,n),t.stateNode=e,Mc(t))}return G(t),null;case 5:if(_e(t),a=t.type,e!==null&&t.stateNode!=null)e.memoizedProps!==r&&Mc(t);else{if(!r){if(t.stateNode===null)throw Error(i(166));return G(t),null}if(o=ue.current,Bi(t))Ri(t,o);else{var s=Bd(fe.current);switch(o){case 1:o=s.createElementNS(`http://www.w3.org/2000/svg`,a);break;case 2:o=s.createElementNS(`http://www.w3.org/1998/Math/MathML`,a);break;default:switch(a){case`svg`:o=s.createElementNS(`http://www.w3.org/2000/svg`,a);break;case`math`:o=s.createElementNS(`http://www.w3.org/1998/Math/MathML`,a);break;case`script`:o=s.createElement(`div`),o.innerHTML=`<script><\/script>`,o=o.removeChild(o.firstChild);break;case`select`:o=typeof r.is==`string`?s.createElement(`select`,{is:r.is}):s.createElement(`select`),r.multiple?o.multiple=!0:r.size&&(o.size=r.size);break;default:o=typeof r.is==`string`?s.createElement(a,{is:r.is}):s.createElement(a)}}o[ct]=t,o[lt]=r;a:for(s=t.child;s!==null;){if(s.tag===5||s.tag===6)o.appendChild(s.stateNode);else if(s.tag!==4&&s.tag!==27&&s.child!==null){s.child.return=s,s=s.child;continue}if(s===t)break a;for(;s.sibling===null;){if(s.return===null||s.return===t)break a;s=s.return}s.sibling.return=s.return,s=s.sibling}t.stateNode=o;a:switch(Pd(o,a,r),a){case`button`:case`input`:case`select`:case`textarea`:r=!!r.autoFocus;break a;case`img`:r=!0;break a;default:r=!1}r&&Mc(t)}}return G(t),Nc(t,t.type,e===null?null:e.memoizedProps,t.pendingProps,n),null;case 6:if(e&&t.stateNode!=null)e.memoizedProps!==r&&Mc(t);else{if(typeof r!=`string`&&t.stateNode===null)throw Error(i(166));if(e=fe.current,Bi(t)){if(e=t.stateNode,n=t.memoizedProps,r=null,a=Ni,a!==null)switch(a.tag){case 27:case 5:r=a.memoizedProps}e[ct]=t,e=!!(e.nodeValue===n||r!==null&&!0===r.suppressHydrationWarning||Md(e.nodeValue,n)),e||Li(t,!0)}else e=Bd(e).createTextNode(r),e[ct]=t,t.stateNode=e}return G(t),null;case 31:if(n=t.memoizedState,e===null||e.memoizedState!==null){if(r=Bi(t),n!==null){if(e===null){if(!r)throw Error(i(318));if(e=t.memoizedState,e=e===null?null:e.dehydrated,!e)throw Error(i(557));e[ct]=t}else Vi(),!(t.flags&128)&&(t.memoizedState=null),t.flags|=4;G(t),e=!1}else n=Hi(),e!==null&&e.memoizedState!==null&&(e.memoizedState.hydrationErrors=n),e=!0;if(!e)return t.flags&256?(so(t),t):(so(t),null);if(t.flags&128)throw Error(i(558))}return G(t),null;case 13:if(r=t.memoizedState,e===null||e.memoizedState!==null&&e.memoizedState.dehydrated!==null){if(a=Bi(t),r!==null&&r.dehydrated!==null){if(e===null){if(!a)throw Error(i(318));if(a=t.memoizedState,a=a===null?null:a.dehydrated,!a)throw Error(i(317));a[ct]=t}else Vi(),!(t.flags&128)&&(t.memoizedState=null),t.flags|=4;G(t),a=!1}else a=Hi(),e!==null&&e.memoizedState!==null&&(e.memoizedState.hydrationErrors=a),a=!0;if(!a)return t.flags&256?(so(t),t):(so(t),null)}return so(t),t.flags&128?(t.lanes=n,t):(n=r!==null,e=e!==null&&e.memoizedState!==null,n&&(r=t.child,a=null,r.alternate!==null&&r.alternate.memoizedState!==null&&r.alternate.memoizedState.cachePool!==null&&(a=r.alternate.memoizedState.cachePool.pool),o=null,r.memoizedState!==null&&r.memoizedState.cachePool!==null&&(o=r.memoizedState.cachePool.pool),o!==a&&(r.flags|=2048)),n!==e&&n&&(t.child.flags|=8192),Fc(t,t.updateQueue),G(t),null);case 4:return he(),e===null&&Sd(t.stateNode.containerInfo),G(t),null;case 10:return Ji(t.type),G(t),null;case 19:if(le(co),r=t.memoizedState,r===null)return G(t),null;if(a=(t.flags&128)!=0,o=r.rendering,o===null)if(a)Ic(r,!1);else{if(Wl!==0||e!==null&&e.flags&128)for(e=t.child;e!==null;){if(o=lo(e),o!==null){for(t.flags|=128,Ic(r,!1),e=o.updateQueue,t.updateQueue=e,Fc(t,e),t.subtreeFlags=0,e=n,n=t.child;n!==null;)R(n,e),n=n.sibling;return N(co,co.current&1|2),B&&Oi(t,r.treeForkCount),t.child}e=e.sibling}r.tail!==null&&Ae()>tu&&(t.flags|=128,a=!0,Ic(r,!1),t.lanes=4194304)}else{if(!a)if(e=lo(o),e!==null){if(t.flags|=128,a=!0,e=e.updateQueue,t.updateQueue=e,Fc(t,e),Ic(r,!0),r.tail===null&&r.tailMode===`hidden`&&!o.alternate&&!B)return G(t),null}else 2*Ae()-r.renderingStartTime>tu&&n!==536870912&&(t.flags|=128,a=!0,Ic(r,!1),t.lanes=4194304);r.isBackwards?(o.sibling=t.child,t.child=o):(e=r.last,e===null?t.child=o:e.sibling=o,r.last=o)}return r.tail===null?(G(t),null):(e=r.tail,r.rendering=e,r.tail=e.sibling,r.renderingStartTime=Ae(),e.sibling=null,n=co.current,N(co,a?n&1|2:n&1),B&&Oi(t,r.treeForkCount),e);case 22:case 23:return so(t),eo(),r=t.memoizedState!==null,e===null?r&&(t.flags|=8192):e.memoizedState!==null!==r&&(t.flags|=8192),r?n&536870912&&!(t.flags&128)&&(G(t),t.subtreeFlags&6&&(t.flags|=8192)):G(t),n=t.updateQueue,n!==null&&Fc(t,n.retryQueue),n=null,e!==null&&e.memoizedState!==null&&e.memoizedState.cachePool!==null&&(n=e.memoizedState.cachePool.pool),r=null,t.memoizedState!==null&&t.memoizedState.cachePool!==null&&(r=t.memoizedState.cachePool.pool),r!==n&&(t.flags|=2048),e!==null&&le(_a),null;case 24:return n=null,e!==null&&(n=e.memoizedState.cache),t.memoizedState.cache!==n&&(t.flags|=2048),Ji(oa),G(t),null;case 25:return null;case 30:return null}throw Error(i(156,t.tag))}function Rc(e,t){switch(ji(t),t.tag){case 1:return e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 3:return Ji(oa),he(),e=t.flags,e&65536&&!(e&128)?(t.flags=e&-65537|128,t):null;case 26:case 27:case 5:return _e(t),null;case 31:if(t.memoizedState!==null){if(so(t),t.alternate===null)throw Error(i(340));Vi()}return e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 13:if(so(t),e=t.memoizedState,e!==null&&e.dehydrated!==null){if(t.alternate===null)throw Error(i(340));Vi()}return e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 19:return le(co),null;case 4:return he(),null;case 10:return Ji(t.type),null;case 22:case 23:return so(t),eo(),e!==null&&le(_a),e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 24:return Ji(oa),null;case 25:return null;default:return null}}function zc(e,t){switch(ji(t),t.tag){case 3:Ji(oa),he();break;case 26:case 27:case 5:_e(t);break;case 4:he();break;case 31:t.memoizedState!==null&&so(t);break;case 13:so(t);break;case 19:le(co);break;case 10:Ji(t.type);break;case 22:case 23:so(t),eo(),e!==null&&le(_a);break;case 24:Ji(oa)}}function Bc(e,t){try{var n=t.updateQueue,r=n===null?null:n.lastEffect;if(r!==null){var i=r.next;n=i;do{if((n.tag&e)===e){r=void 0;var a=n.create,o=n.inst;r=a(),o.destroy=r}n=n.next}while(n!==i)}}catch(e){Z(t,t.return,e)}}function Vc(e,t,n){try{var r=t.updateQueue,i=r===null?null:r.lastEffect;if(i!==null){var a=i.next;r=a;do{if((r.tag&e)===e){var o=r.inst,s=o.destroy;if(s!==void 0){o.destroy=void 0,i=t;var c=n,l=s;try{l()}catch(e){Z(i,c,e)}}}r=r.next}while(r!==a)}}catch(e){Z(t,t.return,e)}}function Hc(e){var t=e.updateQueue;if(t!==null){var n=e.stateNode;try{Ya(t,n)}catch(t){Z(e,e.return,t)}}}function Uc(e,t,n){n.props=Gs(e.type,e.memoizedProps),n.state=e.memoizedState;try{n.componentWillUnmount()}catch(n){Z(e,t,n)}}function Wc(e,t){try{var n=e.ref;if(n!==null){switch(e.tag){case 26:case 27:case 5:var r=e.stateNode;break;case 30:r=e.stateNode;break;default:r=e.stateNode}typeof n==`function`?e.refCleanup=n(r):n.current=r}}catch(n){Z(e,t,n)}}function Gc(e,t){var n=e.ref,r=e.refCleanup;if(n!==null)if(typeof r==`function`)try{r()}catch(n){Z(e,t,n)}finally{e.refCleanup=null,e=e.alternate,e!=null&&(e.refCleanup=null)}else if(typeof n==`function`)try{n(null)}catch(n){Z(e,t,n)}else n.current=null}function Kc(e){var t=e.type,n=e.memoizedProps,r=e.stateNode;try{a:switch(t){case`button`:case`input`:case`select`:case`textarea`:n.autoFocus&&r.focus();break a;case`img`:n.src?r.src=n.src:n.srcSet&&(r.srcset=n.srcSet)}}catch(t){Z(e,e.return,t)}}function qc(e,t,n){try{var r=e.stateNode;Fd(r,e.type,n,t),r[lt]=t}catch(t){Z(e,e.return,t)}}function Jc(e){return e.tag===5||e.tag===3||e.tag===26||e.tag===27&&Zd(e.type)||e.tag===4}function Yc(e){a:for(;;){for(;e.sibling===null;){if(e.return===null||Jc(e.return))return null;e=e.return}for(e.sibling.return=e.return,e=e.sibling;e.tag!==5&&e.tag!==6&&e.tag!==18;){if(e.tag===27&&Zd(e.type)||e.flags&2||e.child===null||e.tag===4)continue a;e.child.return=e,e=e.child}if(!(e.flags&2))return e.stateNode}}function Xc(e,t,n){var r=e.tag;if(r===5||r===6)e=e.stateNode,t?(n.nodeType===9?n.body:n.nodeName===`HTML`?n.ownerDocument.body:n).insertBefore(e,t):(t=n.nodeType===9?n.body:n.nodeName===`HTML`?n.ownerDocument.body:n,t.appendChild(e),n=n._reactRootContainer,n!=null||t.onclick!==null||(t.onclick=tn));else if(r!==4&&(r===27&&Zd(e.type)&&(n=e.stateNode,t=null),e=e.child,e!==null))for(Xc(e,t,n),e=e.sibling;e!==null;)Xc(e,t,n),e=e.sibling}function Zc(e,t,n){var r=e.tag;if(r===5||r===6)e=e.stateNode,t?n.insertBefore(e,t):n.appendChild(e);else if(r!==4&&(r===27&&Zd(e.type)&&(n=e.stateNode),e=e.child,e!==null))for(Zc(e,t,n),e=e.sibling;e!==null;)Zc(e,t,n),e=e.sibling}function Qc(e){var t=e.stateNode,n=e.memoizedProps;try{for(var r=e.type,i=t.attributes;i.length;)t.removeAttributeNode(i[0]);Pd(t,r,n),t[ct]=e,t[lt]=n}catch(t){Z(e,e.return,t)}}var $c=!1,el=!1,tl=!1,nl=typeof WeakSet==`function`?WeakSet:Set,rl=null;function il(e,t){if(e=e.containerInfo,Rd=sp,e=Or(e),kr(e)){if(`selectionStart`in e)var n={start:e.selectionStart,end:e.selectionEnd};else a:{n=(n=e.ownerDocument)&&n.defaultView||window;var r=n.getSelection&&n.getSelection();if(r&&r.rangeCount!==0){n=r.anchorNode;var a=r.anchorOffset,o=r.focusNode;r=r.focusOffset;try{n.nodeType,o.nodeType}catch{n=null;break a}var s=0,c=-1,l=-1,u=0,d=0,f=e,p=null;b:for(;;){for(var m;f!==n||a!==0&&f.nodeType!==3||(c=s+a),f!==o||r!==0&&f.nodeType!==3||(l=s+r),f.nodeType===3&&(s+=f.nodeValue.length),(m=f.firstChild)!==null;)p=f,f=m;for(;;){if(f===e)break b;if(p===n&&++u===a&&(c=s),p===o&&++d===r&&(l=s),(m=f.nextSibling)!==null)break;f=p,p=f.parentNode}f=m}n=c===-1||l===-1?null:{start:c,end:l}}else n=null}n||={start:0,end:0}}else n=null;for(zd={focusedElem:e,selectionRange:n},sp=!1,rl=t;rl!==null;)if(t=rl,e=t.child,t.subtreeFlags&1028&&e!==null)e.return=t,rl=e;else for(;rl!==null;){switch(t=rl,o=t.alternate,e=t.flags,t.tag){case 0:if(e&4&&(e=t.updateQueue,e=e===null?null:e.events,e!==null))for(n=0;n<e.length;n++)a=e[n],a.ref.impl=a.nextImpl;break;case 11:case 15:break;case 1:if(e&1024&&o!==null){e=void 0,n=t,a=o.memoizedProps,o=o.memoizedState,r=n.stateNode;try{var h=Gs(n.type,a);e=r.getSnapshotBeforeUpdate(h,o),r.__reactInternalSnapshotBeforeUpdate=e}catch(e){Z(n,n.return,e)}}break;case 3:if(e&1024){if(e=t.stateNode.containerInfo,n=e.nodeType,n===9)ef(e);else if(n===1)switch(e.nodeName){case`HEAD`:case`HTML`:case`BODY`:ef(e);break;default:e.textContent=``}}break;case 5:case 26:case 27:case 6:case 4:case 17:break;default:if(e&1024)throw Error(i(163))}if(e=t.sibling,e!==null){e.return=t.return,rl=e;break}rl=t.return}}function al(e,t,n){var r=n.flags;switch(n.tag){case 0:case 11:case 15:bl(e,n),r&4&&Bc(5,n);break;case 1:if(bl(e,n),r&4)if(e=n.stateNode,t===null)try{e.componentDidMount()}catch(e){Z(n,n.return,e)}else{var i=Gs(n.type,t.memoizedProps);t=t.memoizedState;try{e.componentDidUpdate(i,t,e.__reactInternalSnapshotBeforeUpdate)}catch(e){Z(n,n.return,e)}}r&64&&Hc(n),r&512&&Wc(n,n.return);break;case 3:if(bl(e,n),r&64&&(e=n.updateQueue,e!==null)){if(t=null,n.child!==null)switch(n.child.tag){case 27:case 5:t=n.child.stateNode;break;case 1:t=n.child.stateNode}try{Ya(e,t)}catch(e){Z(n,n.return,e)}}break;case 27:t===null&&r&4&&Qc(n);case 26:case 5:bl(e,n),t===null&&r&4&&Kc(n),r&512&&Wc(n,n.return);break;case 12:bl(e,n);break;case 31:bl(e,n),r&4&&dl(e,n);break;case 13:bl(e,n),r&4&&fl(e,n),r&64&&(e=n.memoizedState,e!==null&&(e=e.dehydrated,e!==null&&(n=Ju.bind(null,n),sf(e,n))));break;case 22:if(r=n.memoizedState!==null||$c,!r){t=t!==null&&t.memoizedState!==null||el,i=$c;var a=el;$c=r,(el=t)&&!a?Sl(e,n,(n.subtreeFlags&8772)!=0):bl(e,n),$c=i,el=a}break;case 30:break;default:bl(e,n)}}function ol(e){var t=e.alternate;t!==null&&(e.alternate=null,ol(t)),e.child=null,e.deletions=null,e.sibling=null,e.tag===5&&(t=e.stateNode,t!==null&&gt(t)),e.stateNode=null,e.return=null,e.dependencies=null,e.memoizedProps=null,e.memoizedState=null,e.pendingProps=null,e.stateNode=null,e.updateQueue=null}var sl=null,cl=!1;function ll(e,t,n){for(n=n.child;n!==null;)ul(e,t,n),n=n.sibling}function ul(e,t,n){if(Be&&typeof Be.onCommitFiberUnmount==`function`)try{Be.onCommitFiberUnmount(ze,n)}catch{}switch(n.tag){case 26:el||Gc(n,t),ll(e,t,n),n.memoizedState?n.memoizedState.count--:n.stateNode&&(n=n.stateNode,n.parentNode.removeChild(n));break;case 27:el||Gc(n,t);var r=sl,i=cl;Zd(n.type)&&(sl=n.stateNode,cl=!1),ll(e,t,n),pf(n.stateNode),sl=r,cl=i;break;case 5:el||Gc(n,t);case 6:if(r=sl,i=cl,sl=null,ll(e,t,n),sl=r,cl=i,sl!==null)if(cl)try{(sl.nodeType===9?sl.body:sl.nodeName===`HTML`?sl.ownerDocument.body:sl).removeChild(n.stateNode)}catch(e){Z(n,t,e)}else try{sl.removeChild(n.stateNode)}catch(e){Z(n,t,e)}break;case 18:sl!==null&&(cl?(e=sl,Qd(e.nodeType===9?e.body:e.nodeName===`HTML`?e.ownerDocument.body:e,n.stateNode),Np(e)):Qd(sl,n.stateNode));break;case 4:r=sl,i=cl,sl=n.stateNode.containerInfo,cl=!0,ll(e,t,n),sl=r,cl=i;break;case 0:case 11:case 14:case 15:Vc(2,n,t),el||Vc(4,n,t),ll(e,t,n);break;case 1:el||(Gc(n,t),r=n.stateNode,typeof r.componentWillUnmount==`function`&&Uc(n,t,r)),ll(e,t,n);break;case 21:ll(e,t,n);break;case 22:el=(r=el)||n.memoizedState!==null,ll(e,t,n),el=r;break;default:ll(e,t,n)}}function dl(e,t){if(t.memoizedState===null&&(e=t.alternate,e!==null&&(e=e.memoizedState,e!==null))){e=e.dehydrated;try{Np(e)}catch(e){Z(t,t.return,e)}}}function fl(e,t){if(t.memoizedState===null&&(e=t.alternate,e!==null&&(e=e.memoizedState,e!==null&&(e=e.dehydrated,e!==null))))try{Np(e)}catch(e){Z(t,t.return,e)}}function pl(e){switch(e.tag){case 31:case 13:case 19:var t=e.stateNode;return t===null&&(t=e.stateNode=new nl),t;case 22:return e=e.stateNode,t=e._retryCache,t===null&&(t=e._retryCache=new nl),t;default:throw Error(i(435,e.tag))}}function ml(e,t){var n=pl(e);t.forEach(function(t){if(!n.has(t)){n.add(t);var r=Yu.bind(null,e,t);t.then(r,r)}})}function hl(e,t){var n=t.deletions;if(n!==null)for(var r=0;r<n.length;r++){var a=n[r],o=e,s=t,c=s;a:for(;c!==null;){switch(c.tag){case 27:if(Zd(c.type)){sl=c.stateNode,cl=!1;break a}break;case 5:sl=c.stateNode,cl=!1;break a;case 3:case 4:sl=c.stateNode.containerInfo,cl=!0;break a}c=c.return}if(sl===null)throw Error(i(160));ul(o,s,a),sl=null,cl=!1,o=a.alternate,o!==null&&(o.return=null),a.return=null}if(t.subtreeFlags&13886)for(t=t.child;t!==null;)_l(t,e),t=t.sibling}var gl=null;function _l(e,t){var n=e.alternate,r=e.flags;switch(e.tag){case 0:case 11:case 14:case 15:hl(t,e),vl(e),r&4&&(Vc(3,e,e.return),Bc(3,e),Vc(5,e,e.return));break;case 1:hl(t,e),vl(e),r&512&&(el||n===null||Gc(n,n.return)),r&64&&$c&&(e=e.updateQueue,e!==null&&(r=e.callbacks,r!==null&&(n=e.shared.hiddenCallbacks,e.shared.hiddenCallbacks=n===null?r:n.concat(r))));break;case 26:var a=gl;if(hl(t,e),vl(e),r&512&&(el||n===null||Gc(n,n.return)),r&4){var o=n===null?null:n.memoizedState;if(r=e.memoizedState,n===null)if(r===null)if(e.stateNode===null){a:{r=e.type,n=e.memoizedProps,a=a.ownerDocument||a;b:switch(r){case`title`:o=a.getElementsByTagName(`title`)[0],(!o||o[ht]||o[ct]||o.namespaceURI===`http://www.w3.org/2000/svg`||o.hasAttribute(`itemprop`))&&(o=a.createElement(r),a.head.insertBefore(o,a.querySelector(`head > title`))),Pd(o,r,n),o[ct]=e,xt(o),r=o;break a;case`link`:var s=Vf(`link`,`href`,a).get(r+(n.href||``));if(s){for(var c=0;c<s.length;c++)if(o=s[c],o.getAttribute(`href`)===(n.href==null||n.href===``?null:n.href)&&o.getAttribute(`rel`)===(n.rel==null?null:n.rel)&&o.getAttribute(`title`)===(n.title==null?null:n.title)&&o.getAttribute(`crossorigin`)===(n.crossOrigin==null?null:n.crossOrigin)){s.splice(c,1);break b}}o=a.createElement(r),Pd(o,r,n),a.head.appendChild(o);break;case`meta`:if(s=Vf(`meta`,`content`,a).get(r+(n.content||``))){for(c=0;c<s.length;c++)if(o=s[c],o.getAttribute(`content`)===(n.content==null?null:``+n.content)&&o.getAttribute(`name`)===(n.name==null?null:n.name)&&o.getAttribute(`property`)===(n.property==null?null:n.property)&&o.getAttribute(`http-equiv`)===(n.httpEquiv==null?null:n.httpEquiv)&&o.getAttribute(`charset`)===(n.charSet==null?null:n.charSet)){s.splice(c,1);break b}}o=a.createElement(r),Pd(o,r,n),a.head.appendChild(o);break;default:throw Error(i(468,r))}o[ct]=e,xt(o),r=o}e.stateNode=r}else Hf(a,e.type,e.stateNode);else e.stateNode=If(a,r,e.memoizedProps);else o===r?r===null&&e.stateNode!==null&&qc(e,e.memoizedProps,n.memoizedProps):(o===null?n.stateNode!==null&&(n=n.stateNode,n.parentNode.removeChild(n)):o.count--,r===null?Hf(a,e.type,e.stateNode):If(a,r,e.memoizedProps))}break;case 27:hl(t,e),vl(e),r&512&&(el||n===null||Gc(n,n.return)),n!==null&&r&4&&qc(e,e.memoizedProps,n.memoizedProps);break;case 5:if(hl(t,e),vl(e),r&512&&(el||n===null||Gc(n,n.return)),e.flags&32){a=e.stateNode;try{qt(a,``)}catch(t){Z(e,e.return,t)}}r&4&&e.stateNode!=null&&(a=e.memoizedProps,qc(e,a,n===null?a:n.memoizedProps)),r&1024&&(tl=!0);break;case 6:if(hl(t,e),vl(e),r&4){if(e.stateNode===null)throw Error(i(162));r=e.memoizedProps,n=e.stateNode;try{n.nodeValue=r}catch(t){Z(e,e.return,t)}}break;case 3:if(Bf=null,a=gl,gl=gf(t.containerInfo),hl(t,e),gl=a,vl(e),r&4&&n!==null&&n.memoizedState.isDehydrated)try{Np(t.containerInfo)}catch(t){Z(e,e.return,t)}tl&&(tl=!1,yl(e));break;case 4:r=gl,gl=gf(e.stateNode.containerInfo),hl(t,e),vl(e),gl=r;break;case 12:hl(t,e),vl(e);break;case 31:hl(t,e),vl(e),r&4&&(r=e.updateQueue,r!==null&&(e.updateQueue=null,ml(e,r)));break;case 13:hl(t,e),vl(e),e.child.flags&8192&&e.memoizedState!==null!=(n!==null&&n.memoizedState!==null)&&($l=Ae()),r&4&&(r=e.updateQueue,r!==null&&(e.updateQueue=null,ml(e,r)));break;case 22:a=e.memoizedState!==null;var l=n!==null&&n.memoizedState!==null,u=$c,d=el;if($c=u||a,el=d||l,hl(t,e),el=d,$c=u,vl(e),r&8192)a:for(t=e.stateNode,t._visibility=a?t._visibility&-2:t._visibility|1,a&&(n===null||l||$c||el||xl(e)),n=null,t=e;;){if(t.tag===5||t.tag===26){if(n===null){l=n=t;try{if(o=l.stateNode,a)s=o.style,typeof s.setProperty==`function`?s.setProperty(`display`,`none`,`important`):s.display=`none`;else{c=l.stateNode;var f=l.memoizedProps.style,p=f!=null&&f.hasOwnProperty(`display`)?f.display:null;c.style.display=p==null||typeof p==`boolean`?``:(``+p).trim()}}catch(e){Z(l,l.return,e)}}}else if(t.tag===6){if(n===null){l=t;try{l.stateNode.nodeValue=a?``:l.memoizedProps}catch(e){Z(l,l.return,e)}}}else if(t.tag===18){if(n===null){l=t;try{var m=l.stateNode;a?$d(m,!0):$d(l.stateNode,!1)}catch(e){Z(l,l.return,e)}}}else if((t.tag!==22&&t.tag!==23||t.memoizedState===null||t===e)&&t.child!==null){t.child.return=t,t=t.child;continue}if(t===e)break a;for(;t.sibling===null;){if(t.return===null||t.return===e)break a;n===t&&(n=null),t=t.return}n===t&&(n=null),t.sibling.return=t.return,t=t.sibling}r&4&&(r=e.updateQueue,r!==null&&(n=r.retryQueue,n!==null&&(r.retryQueue=null,ml(e,n))));break;case 19:hl(t,e),vl(e),r&4&&(r=e.updateQueue,r!==null&&(e.updateQueue=null,ml(e,r)));break;case 30:break;case 21:break;default:hl(t,e),vl(e)}}function vl(e){var t=e.flags;if(t&2){try{for(var n,r=e.return;r!==null;){if(Jc(r)){n=r;break}r=r.return}if(n==null)throw Error(i(160));switch(n.tag){case 27:var a=n.stateNode;Zc(e,Yc(e),a);break;case 5:var o=n.stateNode;n.flags&32&&(qt(o,``),n.flags&=-33),Zc(e,Yc(e),o);break;case 3:case 4:var s=n.stateNode.containerInfo;Xc(e,Yc(e),s);break;default:throw Error(i(161))}}catch(t){Z(e,e.return,t)}e.flags&=-3}t&4096&&(e.flags&=-4097)}function yl(e){if(e.subtreeFlags&1024)for(e=e.child;e!==null;){var t=e;yl(t),t.tag===5&&t.flags&1024&&t.stateNode.reset(),e=e.sibling}}function bl(e,t){if(t.subtreeFlags&8772)for(t=t.child;t!==null;)al(e,t.alternate,t),t=t.sibling}function xl(e){for(e=e.child;e!==null;){var t=e;switch(t.tag){case 0:case 11:case 14:case 15:Vc(4,t,t.return),xl(t);break;case 1:Gc(t,t.return);var n=t.stateNode;typeof n.componentWillUnmount==`function`&&Uc(t,t.return,n),xl(t);break;case 27:pf(t.stateNode);case 26:case 5:Gc(t,t.return),xl(t);break;case 22:t.memoizedState===null&&xl(t);break;case 30:xl(t);break;default:xl(t)}e=e.sibling}}function Sl(e,t,n){for(n&&=(t.subtreeFlags&8772)!=0,t=t.child;t!==null;){var r=t.alternate,i=e,a=t,o=a.flags;switch(a.tag){case 0:case 11:case 15:Sl(i,a,n),Bc(4,a);break;case 1:if(Sl(i,a,n),r=a,i=r.stateNode,typeof i.componentDidMount==`function`)try{i.componentDidMount()}catch(e){Z(r,r.return,e)}if(r=a,i=r.updateQueue,i!==null){var s=r.stateNode;try{var c=i.shared.hiddenCallbacks;if(c!==null)for(i.shared.hiddenCallbacks=null,i=0;i<c.length;i++)Ja(c[i],s)}catch(e){Z(r,r.return,e)}}n&&o&64&&Hc(a),Wc(a,a.return);break;case 27:Qc(a);case 26:case 5:Sl(i,a,n),n&&r===null&&o&4&&Kc(a),Wc(a,a.return);break;case 12:Sl(i,a,n);break;case 31:Sl(i,a,n),n&&o&4&&dl(i,a);break;case 13:Sl(i,a,n),n&&o&4&&fl(i,a);break;case 22:a.memoizedState===null&&Sl(i,a,n),Wc(a,a.return);break;case 30:break;default:Sl(i,a,n)}t=t.sibling}}function Cl(e,t){var n=null;e!==null&&e.memoizedState!==null&&e.memoizedState.cachePool!==null&&(n=e.memoizedState.cachePool.pool),e=null,t.memoizedState!==null&&t.memoizedState.cachePool!==null&&(e=t.memoizedState.cachePool.pool),e!==n&&(e!=null&&e.refCount++,n!=null&&ca(n))}function wl(e,t){e=null,t.alternate!==null&&(e=t.alternate.memoizedState.cache),t=t.memoizedState.cache,t!==e&&(t.refCount++,e!=null&&ca(e))}function Tl(e,t,n,r){if(t.subtreeFlags&10256)for(t=t.child;t!==null;)El(e,t,n,r),t=t.sibling}function El(e,t,n,r){var i=t.flags;switch(t.tag){case 0:case 11:case 15:Tl(e,t,n,r),i&2048&&Bc(9,t);break;case 1:Tl(e,t,n,r);break;case 3:Tl(e,t,n,r),i&2048&&(e=null,t.alternate!==null&&(e=t.alternate.memoizedState.cache),t=t.memoizedState.cache,t!==e&&(t.refCount++,e!=null&&ca(e)));break;case 12:if(i&2048){Tl(e,t,n,r),e=t.stateNode;try{var a=t.memoizedProps,o=a.id,s=a.onPostCommit;typeof s==`function`&&s(o,t.alternate===null?`mount`:`update`,e.passiveEffectDuration,-0)}catch(e){Z(t,t.return,e)}}else Tl(e,t,n,r);break;case 31:Tl(e,t,n,r);break;case 13:Tl(e,t,n,r);break;case 23:break;case 22:a=t.stateNode,o=t.alternate,t.memoizedState===null?a._visibility&2?Tl(e,t,n,r):(a._visibility|=2,Dl(e,t,n,r,(t.subtreeFlags&10256)!=0||!1)):a._visibility&2?Tl(e,t,n,r):Ol(e,t),i&2048&&Cl(o,t);break;case 24:Tl(e,t,n,r),i&2048&&wl(t.alternate,t);break;default:Tl(e,t,n,r)}}function Dl(e,t,n,r,i){for(i&&=(t.subtreeFlags&10256)!=0||!1,t=t.child;t!==null;){var a=e,o=t,s=n,c=r,l=o.flags;switch(o.tag){case 0:case 11:case 15:Dl(a,o,s,c,i),Bc(8,o);break;case 23:break;case 22:var u=o.stateNode;o.memoizedState===null?(u._visibility|=2,Dl(a,o,s,c,i)):u._visibility&2?Dl(a,o,s,c,i):Ol(a,o),i&&l&2048&&Cl(o.alternate,o);break;case 24:Dl(a,o,s,c,i),i&&l&2048&&wl(o.alternate,o);break;default:Dl(a,o,s,c,i)}t=t.sibling}}function Ol(e,t){if(t.subtreeFlags&10256)for(t=t.child;t!==null;){var n=e,r=t,i=r.flags;switch(r.tag){case 22:Ol(n,r),i&2048&&Cl(r.alternate,r);break;case 24:Ol(n,r),i&2048&&wl(r.alternate,r);break;default:Ol(n,r)}t=t.sibling}}var kl=8192;function Al(e,t,n){if(e.subtreeFlags&kl)for(e=e.child;e!==null;)jl(e,t,n),e=e.sibling}function jl(e,t,n){switch(e.tag){case 26:Al(e,t,n),e.flags&kl&&e.memoizedState!==null&&Gf(n,gl,e.memoizedState,e.memoizedProps);break;case 5:Al(e,t,n);break;case 3:case 4:var r=gl;gl=gf(e.stateNode.containerInfo),Al(e,t,n),gl=r;break;case 22:e.memoizedState===null&&(r=e.alternate,r!==null&&r.memoizedState!==null?(r=kl,kl=16777216,Al(e,t,n),kl=r):Al(e,t,n));break;default:Al(e,t,n)}}function Ml(e){var t=e.alternate;if(t!==null&&(e=t.child,e!==null)){t.child=null;do t=e.sibling,e.sibling=null,e=t;while(e!==null)}}function Nl(e){var t=e.deletions;if(e.flags&16){if(t!==null)for(var n=0;n<t.length;n++){var r=t[n];rl=r,Il(r,e)}Ml(e)}if(e.subtreeFlags&10256)for(e=e.child;e!==null;)Pl(e),e=e.sibling}function Pl(e){switch(e.tag){case 0:case 11:case 15:Nl(e),e.flags&2048&&Vc(9,e,e.return);break;case 3:Nl(e);break;case 12:Nl(e);break;case 22:var t=e.stateNode;e.memoizedState!==null&&t._visibility&2&&(e.return===null||e.return.tag!==13)?(t._visibility&=-3,Fl(e)):Nl(e);break;default:Nl(e)}}function Fl(e){var t=e.deletions;if(e.flags&16){if(t!==null)for(var n=0;n<t.length;n++){var r=t[n];rl=r,Il(r,e)}Ml(e)}for(e=e.child;e!==null;){switch(t=e,t.tag){case 0:case 11:case 15:Vc(8,t,t.return),Fl(t);break;case 22:n=t.stateNode,n._visibility&2&&(n._visibility&=-3,Fl(t));break;default:Fl(t)}e=e.sibling}}function Il(e,t){for(;rl!==null;){var n=rl;switch(n.tag){case 0:case 11:case 15:Vc(8,n,t);break;case 23:case 22:if(n.memoizedState!==null&&n.memoizedState.cachePool!==null){var r=n.memoizedState.cachePool.pool;r!=null&&r.refCount++}break;case 24:ca(n.memoizedState.cache)}if(r=n.child,r!==null)r.return=n,rl=r;else a:for(n=e;rl!==null;){r=rl;var i=r.sibling,a=r.return;if(ol(r),r===n){rl=null;break a}if(i!==null){i.return=a,rl=i;break a}rl=a}}}var Ll={getCacheForType:function(e){var t=ea(oa),n=t.data.get(e);return n===void 0&&(n=e(),t.data.set(e,n)),n},cacheSignal:function(){return ea(oa).controller.signal}},Rl=typeof WeakMap==`function`?WeakMap:Map,K=0,q=null,J=null,Y=0,X=0,zl=null,Bl=!1,Vl=!1,Hl=!1,Ul=0,Wl=0,Gl=0,Kl=0,ql=0,Jl=0,Yl=0,Xl=null,Zl=null,Ql=!1,$l=0,eu=0,tu=1/0,nu=null,ru=null,iu=0,au=null,ou=null,su=0,cu=0,lu=null,uu=null,du=0,fu=null;function pu(){return K&2&&Y!==0?Y&-Y:j.T===null?ot():dd()}function mu(){if(Jl===0)if(!(Y&536870912)||B){var e=Ge;Ge<<=1,!(Ge&3932160)&&(Ge=262144),Jl=e}else Jl=536870912;return e=to.current,e!==null&&(e.flags|=32),Jl}function hu(e,t,n){(e===q&&(X===2||X===9)||e.cancelPendingCommit!==null)&&(Su(e,0),yu(e,Y,Jl,!1)),$e(e,n),(!(K&2)||e!==q)&&(e===q&&(!(K&2)&&(Kl|=n),Wl===4&&yu(e,Y,Jl,!1)),rd(e))}function gu(e,t,n){if(K&6)throw Error(i(327));var r=!n&&(t&127)==0&&(t&e.expiredLanes)===0||Ye(e,t),a=r?Au(e,t):Ou(e,t,!0),o=r;do{if(a===0){Vl&&!r&&yu(e,t,0,!1);break}else{if(n=e.current.alternate,o&&!vu(n)){a=Ou(e,t,!1),o=!1;continue}if(a===2){if(o=t,e.errorRecoveryDisabledLanes&o)var s=0;else s=e.pendingLanes&-536870913,s=s===0?s&536870912?536870912:0:s;if(s!==0){t=s;a:{var c=e;a=Xl;var l=c.current.memoizedState.isDehydrated;if(l&&(Su(c,s).flags|=256),s=Ou(c,s,!1),s!==2){if(Hl&&!l){c.errorRecoveryDisabledLanes|=o,Kl|=o,a=4;break a}o=Zl,Zl=a,o!==null&&(Zl===null?Zl=o:Zl.push.apply(Zl,o))}a=s}if(o=!1,a!==2)continue}}if(a===1){Su(e,0),yu(e,t,0,!0);break}a:{switch(r=e,o=a,o){case 0:case 1:throw Error(i(345));case 4:if((t&4194048)!==t)break;case 6:yu(r,t,Jl,!Bl);break a;case 2:Zl=null;break;case 3:case 5:break;default:throw Error(i(329))}if((t&62914560)===t&&(a=$l+300-Ae(),10<a)){if(yu(r,t,Jl,!Bl),Je(r,0,!0)!==0)break a;su=t,r.timeoutHandle=Kd(_u.bind(null,r,n,Zl,nu,Ql,t,Jl,Kl,Yl,Bl,o,`Throttled`,-0,0),a);break a}_u(r,n,Zl,nu,Ql,t,Jl,Kl,Yl,Bl,o,null,-0,0)}}break}while(1);rd(e)}function _u(e,t,n,r,i,a,o,s,c,l,u,d,f,p){if(e.timeoutHandle=-1,d=t.subtreeFlags,d&8192||(d&16785408)==16785408){d={stylesheets:null,count:0,imgCount:0,imgBytes:0,suspenseyImages:[],waitingForImages:!0,waitingForViewTransition:!1,unsuspend:tn},jl(t,a,d);var m=(a&62914560)===a?$l-Ae():(a&4194048)===a?eu-Ae():0;if(m=qf(d,m),m!==null){su=a,e.cancelPendingCommit=m(Lu.bind(null,e,t,a,n,r,i,o,s,c,u,d,null,f,p)),yu(e,a,o,!l);return}}Lu(e,t,a,n,r,i,o,s,c)}function vu(e){for(var t=e;;){var n=t.tag;if((n===0||n===11||n===15)&&t.flags&16384&&(n=t.updateQueue,n!==null&&(n=n.stores,n!==null)))for(var r=0;r<n.length;r++){var i=n[r],a=i.getSnapshot;i=i.value;try{if(!Cr(a(),i))return!1}catch{return!1}}if(n=t.child,t.subtreeFlags&16384&&n!==null)n.return=t,t=n;else{if(t===e)break;for(;t.sibling===null;){if(t.return===null||t.return===e)return!0;t=t.return}t.sibling.return=t.return,t=t.sibling}}return!0}function yu(e,t,n,r){t&=~ql,t&=~Kl,e.suspendedLanes|=t,e.pingedLanes&=~t,r&&(e.warmLanes|=t),r=e.expirationTimes;for(var i=t;0<i;){var a=31-P(i),o=1<<a;r[a]=-1,i&=~o}n!==0&&tt(e,n,t)}function bu(){return K&6?!0:(id(0,!1),!1)}function xu(){if(J!==null){if(X===0)var e=J.return;else e=J,Ki=Gi=null,Oo(e),Aa=null,ja=0,e=J;for(;e!==null;)zc(e.alternate,e),e=e.return;J=null}}function Su(e,t){var n=e.timeoutHandle;n!==-1&&(e.timeoutHandle=-1,qd(n)),n=e.cancelPendingCommit,n!==null&&(e.cancelPendingCommit=null,n()),su=0,xu(),q=e,J=n=di(e.current,null),Y=t,X=0,zl=null,Bl=!1,Vl=Ye(e,t),Hl=!1,Yl=Jl=ql=Kl=Gl=Wl=0,Zl=Xl=null,Ql=!1,t&8&&(t|=t&32);var r=e.entangledLanes;if(r!==0)for(e=e.entanglements,r&=t;0<r;){var i=31-P(r),a=1<<i;t|=e[i],r&=~a}return Ul=t,ti(),n}function Cu(e,t){H=null,j.H=Ls,t===xa||t===Ca?(t=Oa(),X=3):t===Sa?(t=Oa(),X=4):X=t===tc?8:typeof t==`object`&&t&&typeof t.then==`function`?6:1,zl=t,J===null&&(Wl=1,Ys(e,vi(t,e.current)))}function wu(){var e=to.current;return e===null?!0:(Y&4194048)===Y?no===null:(Y&62914560)===Y||Y&536870912?e===no:!1}function Tu(){var e=j.H;return j.H=Ls,e===null?Ls:e}function Eu(){var e=j.A;return j.A=Ll,e}function Du(){Wl=4,Bl||(Y&4194048)!==Y&&to.current!==null||(Vl=!0),!(Gl&134217727)&&!(Kl&134217727)||q===null||yu(q,Y,Jl,!1)}function Ou(e,t,n){var r=K;K|=2;var i=Tu(),a=Eu();(q!==e||Y!==t)&&(nu=null,Su(e,t)),t=!1;var o=Wl;a:do try{if(X!==0&&J!==null){var s=J,c=zl;switch(X){case 8:xu(),o=6;break a;case 3:case 2:case 9:case 6:to.current===null&&(t=!0);var l=X;if(X=0,zl=null,Pu(e,s,c,l),n&&Vl){o=0;break a}break;default:l=X,X=0,zl=null,Pu(e,s,c,l)}}ku(),o=Wl;break}catch(t){Cu(e,t)}while(1);return t&&e.shellSuspendCounter++,Ki=Gi=null,K=r,j.H=i,j.A=a,J===null&&(q=null,Y=0,ti()),o}function ku(){for(;J!==null;)Mu(J)}function Au(e,t){var n=K;K|=2;var r=Tu(),a=Eu();q!==e||Y!==t?(nu=null,tu=Ae()+500,Su(e,t)):Vl=Ye(e,t);a:do try{if(X!==0&&J!==null){t=J;var o=zl;b:switch(X){case 1:X=0,zl=null,Pu(e,t,o,1);break;case 2:case 9:if(Ta(o)){X=0,zl=null,Nu(t);break}t=function(){X!==2&&X!==9||q!==e||(X=7),rd(e)},o.then(t,t);break a;case 3:X=7;break a;case 4:X=5;break a;case 7:Ta(o)?(X=0,zl=null,Nu(t)):(X=0,zl=null,Pu(e,t,o,7));break;case 5:var s=null;switch(J.tag){case 26:s=J.memoizedState;case 5:case 27:var c=J;if(s?Wf(s):c.stateNode.complete){X=0,zl=null;var l=c.sibling;if(l!==null)J=l;else{var u=c.return;u===null?J=null:(J=u,Fu(u))}break b}}X=0,zl=null,Pu(e,t,o,5);break;case 6:X=0,zl=null,Pu(e,t,o,6);break;case 8:xu(),Wl=6;break a;default:throw Error(i(462))}}ju();break}catch(t){Cu(e,t)}while(1);return Ki=Gi=null,j.H=r,j.A=a,K=n,J===null?(q=null,Y=0,ti(),Wl):0}function ju(){for(;J!==null&&!Oe();)Mu(J)}function Mu(e){var t=jc(e.alternate,e,Ul);e.memoizedProps=e.pendingProps,t===null?Fu(e):J=t}function Nu(e){var t=e,n=t.alternate;switch(t.tag){case 15:case 0:t=hc(n,t,t.pendingProps,t.type,void 0,Y);break;case 11:t=hc(n,t,t.pendingProps,t.type.render,t.ref,Y);break;case 5:Oo(t);default:zc(n,t),t=J=R(t,Ul),t=jc(n,t,Ul)}e.memoizedProps=e.pendingProps,t===null?Fu(e):J=t}function Pu(e,t,n,r){Ki=Gi=null,Oo(t),Aa=null,ja=0;var i=t.return;try{if(ec(e,i,t,n,Y)){Wl=1,Ys(e,vi(n,e.current)),J=null;return}}catch(t){if(i!==null)throw J=i,t;Wl=1,Ys(e,vi(n,e.current)),J=null;return}t.flags&32768?(B||r===1?e=!0:Vl||Y&536870912?e=!1:(Bl=e=!0,(r===2||r===9||r===3||r===6)&&(r=to.current,r!==null&&r.tag===13&&(r.flags|=16384))),Iu(t,e)):Fu(t)}function Fu(e){var t=e;do{if(t.flags&32768){Iu(t,Bl);return}e=t.return;var n=Lc(t.alternate,t,Ul);if(n!==null){J=n;return}if(t=t.sibling,t!==null){J=t;return}J=t=e}while(t!==null);Wl===0&&(Wl=5)}function Iu(e,t){do{var n=Rc(e.alternate,e);if(n!==null){n.flags&=32767,J=n;return}if(n=e.return,n!==null&&(n.flags|=32768,n.subtreeFlags=0,n.deletions=null),!t&&(e=e.sibling,e!==null)){J=e;return}J=e=n}while(e!==null);Wl=6,J=null}function Lu(e,t,n,r,a,o,s,c,l){e.cancelPendingCommit=null;do Hu();while(iu!==0);if(K&6)throw Error(i(327));if(t!==null){if(t===e.current)throw Error(i(177));if(o=t.lanes|t.childLanes,o|=ei,et(e,n,o,s,c,l),e===q&&(J=q=null,Y=0),ou=t,au=e,su=n,cu=o,lu=a,uu=r,t.subtreeFlags&10256||t.flags&10256?(e.callbackNode=null,e.callbackPriority=0,Xu(Pe,function(){return Uu(),null})):(e.callbackNode=null,e.callbackPriority=0),r=(t.flags&13878)!=0,t.subtreeFlags&13878||r){r=j.T,j.T=null,a=M.p,M.p=2,s=K,K|=4;try{il(e,t,n)}finally{K=s,M.p=a,j.T=r}}iu=1,Ru(),zu(),Bu()}}function Ru(){if(iu===1){iu=0;var e=au,t=ou,n=(t.flags&13878)!=0;if(t.subtreeFlags&13878||n){n=j.T,j.T=null;var r=M.p;M.p=2;var i=K;K|=4;try{_l(t,e);var a=zd,o=Or(e.containerInfo),s=a.focusedElem,c=a.selectionRange;if(o!==s&&s&&s.ownerDocument&&Dr(s.ownerDocument.documentElement,s)){if(c!==null&&kr(s)){var l=c.start,u=c.end;if(u===void 0&&(u=l),`selectionStart`in s)s.selectionStart=l,s.selectionEnd=Math.min(u,s.value.length);else{var d=s.ownerDocument||document,f=d&&d.defaultView||window;if(f.getSelection){var p=f.getSelection(),m=s.textContent.length,h=Math.min(c.start,m),g=c.end===void 0?h:Math.min(c.end,m);!p.extend&&h>g&&(o=g,g=h,h=o);var _=Er(s,h),v=Er(s,g);if(_&&v&&(p.rangeCount!==1||p.anchorNode!==_.node||p.anchorOffset!==_.offset||p.focusNode!==v.node||p.focusOffset!==v.offset)){var y=d.createRange();y.setStart(_.node,_.offset),p.removeAllRanges(),h>g?(p.addRange(y),p.extend(v.node,v.offset)):(y.setEnd(v.node,v.offset),p.addRange(y))}}}}for(d=[],p=s;p=p.parentNode;)p.nodeType===1&&d.push({element:p,left:p.scrollLeft,top:p.scrollTop});for(typeof s.focus==`function`&&s.focus(),s=0;s<d.length;s++){var b=d[s];b.element.scrollLeft=b.left,b.element.scrollTop=b.top}}sp=!!Rd,zd=Rd=null}finally{K=i,M.p=r,j.T=n}}e.current=t,iu=2}}function zu(){if(iu===2){iu=0;var e=au,t=ou,n=(t.flags&8772)!=0;if(t.subtreeFlags&8772||n){n=j.T,j.T=null;var r=M.p;M.p=2;var i=K;K|=4;try{al(e,t.alternate,t)}finally{K=i,M.p=r,j.T=n}}iu=3}}function Bu(){if(iu===4||iu===3){iu=0,ke();var e=au,t=ou,n=su,r=uu;t.subtreeFlags&10256||t.flags&10256?iu=5:(iu=0,ou=au=null,Vu(e,e.pendingLanes));var i=e.pendingLanes;if(i===0&&(ru=null),at(n),t=t.stateNode,Be&&typeof Be.onCommitFiberRoot==`function`)try{Be.onCommitFiberRoot(ze,t,void 0,(t.current.flags&128)==128)}catch{}if(r!==null){t=j.T,i=M.p,M.p=2,j.T=null;try{for(var a=e.onRecoverableError,o=0;o<r.length;o++){var s=r[o];a(s.value,{componentStack:s.stack})}}finally{j.T=t,M.p=i}}su&3&&Hu(),rd(e),i=e.pendingLanes,n&261930&&i&42?e===fu?du++:(du=0,fu=e):du=0,id(0,!1)}}function Vu(e,t){(e.pooledCacheLanes&=t)===0&&(t=e.pooledCache,t!=null&&(e.pooledCache=null,ca(t)))}function Hu(){return Ru(),zu(),Bu(),Uu()}function Uu(){if(iu!==5)return!1;var e=au,t=cu;cu=0;var n=at(su),r=j.T,a=M.p;try{M.p=32>n?32:n,j.T=null,n=lu,lu=null;var o=au,s=su;if(iu=0,ou=au=null,su=0,K&6)throw Error(i(331));var c=K;if(K|=4,Pl(o.current),El(o,o.current,s,n),K=c,id(0,!1),Be&&typeof Be.onPostCommitFiberRoot==`function`)try{Be.onPostCommitFiberRoot(ze,o)}catch{}return!0}finally{M.p=a,j.T=r,Vu(e,t)}}function Wu(e,t,n){t=vi(n,t),t=Zs(e.stateNode,t,2),e=Ha(e,t,2),e!==null&&($e(e,2),rd(e))}function Z(e,t,n){if(e.tag===3)Wu(e,e,n);else for(;t!==null;){if(t.tag===3){Wu(t,e,n);break}else if(t.tag===1){var r=t.stateNode;if(typeof t.type.getDerivedStateFromError==`function`||typeof r.componentDidCatch==`function`&&(ru===null||!ru.has(r))){e=vi(n,e),n=Qs(2),r=Ha(t,n,2),r!==null&&($s(n,r,t,e),$e(r,2),rd(r));break}}t=t.return}}function Gu(e,t,n){var r=e.pingCache;if(r===null){r=e.pingCache=new Rl;var i=new Set;r.set(t,i)}else i=r.get(t),i===void 0&&(i=new Set,r.set(t,i));i.has(n)||(Hl=!0,i.add(n),e=Ku.bind(null,e,t,n),t.then(e,e))}function Ku(e,t,n){var r=e.pingCache;r!==null&&r.delete(t),e.pingedLanes|=e.suspendedLanes&n,e.warmLanes&=~n,q===e&&(Y&n)===n&&(Wl===4||Wl===3&&(Y&62914560)===Y&&300>Ae()-$l?!(K&2)&&Su(e,0):ql|=n,Yl===Y&&(Yl=0)),rd(e)}function qu(e,t){t===0&&(t=Ze()),e=ii(e,t),e!==null&&($e(e,t),rd(e))}function Ju(e){var t=e.memoizedState,n=0;t!==null&&(n=t.retryLane),qu(e,n)}function Yu(e,t){var n=0;switch(e.tag){case 31:case 13:var r=e.stateNode,a=e.memoizedState;a!==null&&(n=a.retryLane);break;case 19:r=e.stateNode;break;case 22:r=e.stateNode._retryCache;break;default:throw Error(i(314))}r!==null&&r.delete(t),qu(e,n)}function Xu(e,t){return Ee(e,t)}var Zu=null,Qu=null,$u=!1,ed=!1,td=!1,nd=0;function rd(e){e!==Qu&&e.next===null&&(Qu===null?Zu=Qu=e:Qu=Qu.next=e),ed=!0,$u||($u=!0,ud())}function id(e,t){if(!td&&ed){td=!0;do for(var n=!1,r=Zu;r!==null;){if(!t)if(e!==0){var i=r.pendingLanes;if(i===0)var a=0;else{var o=r.suspendedLanes,s=r.pingedLanes;a=(1<<31-P(42|e)+1)-1,a&=i&~(o&~s),a=a&201326741?a&201326741|1:a?a|2:0}a!==0&&(n=!0,ld(r,a))}else a=Y,a=Je(r,r===q?a:0,r.cancelPendingCommit!==null||r.timeoutHandle!==-1),!(a&3)||Ye(r,a)||(n=!0,ld(r,a));r=r.next}while(n);td=!1}}function ad(){od()}function od(){ed=$u=!1;var e=0;nd!==0&&Gd()&&(e=nd);for(var t=Ae(),n=null,r=Zu;r!==null;){var i=r.next,a=sd(r,t);a===0?(r.next=null,n===null?Zu=i:n.next=i,i===null&&(Qu=n)):(n=r,(e!==0||a&3)&&(ed=!0)),r=i}iu!==0&&iu!==5||id(e,!1),nd!==0&&(nd=0)}function sd(e,t){for(var n=e.suspendedLanes,r=e.pingedLanes,i=e.expirationTimes,a=e.pendingLanes&-62914561;0<a;){var o=31-P(a),s=1<<o,c=i[o];c===-1?((s&n)===0||(s&r)!==0)&&(i[o]=Xe(s,t)):c<=t&&(e.expiredLanes|=s),a&=~s}if(t=q,n=Y,n=Je(e,e===t?n:0,e.cancelPendingCommit!==null||e.timeoutHandle!==-1),r=e.callbackNode,n===0||e===t&&(X===2||X===9)||e.cancelPendingCommit!==null)return r!==null&&r!==null&&De(r),e.callbackNode=null,e.callbackPriority=0;if(!(n&3)||Ye(e,n)){if(t=n&-n,t===e.callbackPriority)return t;switch(r!==null&&De(r),at(n)){case 2:case 8:n=Ne;break;case 32:n=Pe;break;case 268435456:n=Ie;break;default:n=Pe}return r=cd.bind(null,e),n=Ee(n,r),e.callbackPriority=t,e.callbackNode=n,t}return r!==null&&r!==null&&De(r),e.callbackPriority=2,e.callbackNode=null,2}function cd(e,t){if(iu!==0&&iu!==5)return e.callbackNode=null,e.callbackPriority=0,null;var n=e.callbackNode;if(Hu()&&e.callbackNode!==n)return null;var r=Y;return r=Je(e,e===q?r:0,e.cancelPendingCommit!==null||e.timeoutHandle!==-1),r===0?null:(gu(e,r,t),sd(e,Ae()),e.callbackNode!=null&&e.callbackNode===n?cd.bind(null,e):null)}function ld(e,t){if(Hu())return null;gu(e,t,!0)}function ud(){Yd(function(){K&6?Ee(Me,ad):od()})}function dd(){if(nd===0){var e=da;e===0&&(e=We,We<<=1,!(We&261888)&&(We=256)),nd=e}return nd}function fd(e){return e==null||typeof e==`symbol`||typeof e==`boolean`?null:typeof e==`function`?e:en(``+e)}function pd(e,t){var n=t.ownerDocument.createElement(`input`);return n.name=t.name,n.value=t.value,e.id&&n.setAttribute(`form`,e.id),t.parentNode.insertBefore(n,t),e=new FormData(e),n.parentNode.removeChild(n),e}function md(e,t,n,r,i){if(t===`submit`&&n&&n.stateNode===i){var a=fd((i[lt]||null).action),o=r.submitter;o&&(t=(t=o[lt]||null)?fd(t.formAction):o.getAttribute(`formAction`),t!==null&&(a=t,o=null));var s=new Cn(`action`,`action`,null,r,i);e.push({event:s,listeners:[{instance:null,listener:function(){if(r.defaultPrevented){if(nd!==0){var e=o?pd(i,o):new FormData(i);Cs(n,{pending:!0,data:e,method:i.method,action:a},null,e)}}else typeof a==`function`&&(s.preventDefault(),e=o?pd(i,o):new FormData(i),Cs(n,{pending:!0,data:e,method:i.method,action:a},a,e))},currentTarget:i}]})}}for(var hd=0;hd<Yr.length;hd++){var gd=Yr[hd];Xr(gd.toLowerCase(),`on`+(gd[0].toUpperCase()+gd.slice(1)))}Xr(Vr,`onAnimationEnd`),Xr(Hr,`onAnimationIteration`),Xr(Ur,`onAnimationStart`),Xr(`dblclick`,`onDoubleClick`),Xr(`focusin`,`onFocus`),Xr(`focusout`,`onBlur`),Xr(Wr,`onTransitionRun`),Xr(Gr,`onTransitionStart`),Xr(Kr,`onTransitionCancel`),Xr(qr,`onTransitionEnd`),Tt(`onMouseEnter`,[`mouseout`,`mouseover`]),Tt(`onMouseLeave`,[`mouseout`,`mouseover`]),Tt(`onPointerEnter`,[`pointerout`,`pointerover`]),Tt(`onPointerLeave`,[`pointerout`,`pointerover`]),wt(`onChange`,`change click focusin focusout input keydown keyup selectionchange`.split(` `)),wt(`onSelect`,`focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange`.split(` `)),wt(`onBeforeInput`,[`compositionend`,`keypress`,`textInput`,`paste`]),wt(`onCompositionEnd`,`compositionend focusout keydown keypress keyup mousedown`.split(` `)),wt(`onCompositionStart`,`compositionstart focusout keydown keypress keyup mousedown`.split(` `)),wt(`onCompositionUpdate`,`compositionupdate focusout keydown keypress keyup mousedown`.split(` `));var _d=`abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting`.split(` `),vd=new Set(`beforetoggle cancel close invalid load scroll scrollend toggle`.split(` `).concat(_d));function yd(e,t){t=(t&4)!=0;for(var n=0;n<e.length;n++){var r=e[n],i=r.event;r=r.listeners;a:{var a=void 0;if(t)for(var o=r.length-1;0<=o;o--){var s=r[o],c=s.instance,l=s.currentTarget;if(s=s.listener,c!==a&&i.isPropagationStopped())break a;a=s,i.currentTarget=l;try{a(i)}catch(e){Zr(e)}i.currentTarget=null,a=c}else for(o=0;o<r.length;o++){if(s=r[o],c=s.instance,l=s.currentTarget,s=s.listener,c!==a&&i.isPropagationStopped())break a;a=s,i.currentTarget=l;try{a(i)}catch(e){Zr(e)}i.currentTarget=null,a=c}}}}function Q(e,t){var n=t[dt];n===void 0&&(n=t[dt]=new Set);var r=e+`__bubble`;n.has(r)||(Cd(t,e,2,!1),n.add(r))}function bd(e,t,n){var r=0;t&&(r|=4),Cd(n,e,r,t)}var xd=`_reactListening`+Math.random().toString(36).slice(2);function Sd(e){if(!e[xd]){e[xd]=!0,St.forEach(function(t){t!==`selectionchange`&&(vd.has(t)||bd(t,!1,e),bd(t,!0,e))});var t=e.nodeType===9?e:e.ownerDocument;t===null||t[xd]||(t[xd]=!0,bd(`selectionchange`,!1,t))}}function Cd(e,t,n,r){switch(mp(t)){case 2:var i=cp;break;case 8:i=lp;break;default:i=up}n=i.bind(null,t,n,e),i=void 0,!fn||t!==`touchstart`&&t!==`touchmove`&&t!==`wheel`||(i=!0),r?i===void 0?e.addEventListener(t,n,!0):e.addEventListener(t,n,{capture:!0,passive:i}):i===void 0?e.addEventListener(t,n,!1):e.addEventListener(t,n,{passive:i})}function wd(e,t,n,r,i){var a=r;if(!(t&1)&&!(t&2)&&r!==null)a:for(;;){if(r===null)return;var s=r.tag;if(s===3||s===4){var c=r.stateNode.containerInfo;if(c===i)break;if(s===4)for(s=r.return;s!==null;){var l=s.tag;if((l===3||l===4)&&s.stateNode.containerInfo===i)return;s=s.return}for(;c!==null;){if(s=_t(c),s===null)return;if(l=s.tag,l===5||l===6||l===26||l===27){r=a=s;continue a}c=c.parentNode}}r=r.return}ln(function(){var r=a,i=rn(n),s=[];a:{var c=Jr.get(e);if(c!==void 0){var l=Cn,u=e;switch(e){case`keypress`:if(vn(n)===0)break a;case`keydown`:case`keyup`:l=Bn;break;case`focusin`:u=`focus`,l=Mn;break;case`focusout`:u=`blur`,l=Mn;break;case`beforeblur`:case`afterblur`:l=Mn;break;case`click`:if(n.button===2)break a;case`auxclick`:case`dblclick`:case`mousedown`:case`mousemove`:case`mouseup`:case`mouseout`:case`mouseover`:case`contextmenu`:l=An;break;case`drag`:case`dragend`:case`dragenter`:case`dragexit`:case`dragleave`:case`dragover`:case`dragstart`:case`drop`:l=jn;break;case`touchcancel`:case`touchend`:case`touchmove`:case`touchstart`:l=Hn;break;case Vr:case Hr:case Ur:l=Nn;break;case qr:l=Un;break;case`scroll`:case`scrollend`:l=Tn;break;case`wheel`:l=Wn;break;case`copy`:case`cut`:case`paste`:l=Pn;break;case`gotpointercapture`:case`lostpointercapture`:case`pointercancel`:case`pointerdown`:case`pointermove`:case`pointerout`:case`pointerover`:case`pointerup`:l=Vn;break;case`toggle`:case`beforetoggle`:l=Gn}var d=(t&4)!=0,f=!d&&(e===`scroll`||e===`scrollend`),p=d?c===null?null:c+`Capture`:c;d=[];for(var m=r,h;m!==null;){var g=m;if(h=g.stateNode,g=g.tag,g!==5&&g!==26&&g!==27||h===null||p===null||(g=un(m,p),g!=null&&d.push(Td(m,g,h))),f)break;m=m.return}0<d.length&&(c=new l(c,u,null,n,i),s.push({event:c,listeners:d}))}}if(!(t&7)){a:{if(c=e===`mouseover`||e===`pointerover`,l=e===`mouseout`||e===`pointerout`,c&&n!==nn&&(u=n.relatedTarget||n.fromElement)&&(_t(u)||u[ut]))break a;if((l||c)&&(c=i.window===i?i:(c=i.ownerDocument)?c.defaultView||c.parentWindow:window,l?(u=n.relatedTarget||n.toElement,l=r,u=u?_t(u):null,u!==null&&(f=o(u),d=u.tag,u!==f||d!==5&&d!==27&&d!==6)&&(u=null)):(l=null,u=r),l!==u)){if(d=An,g=`onMouseLeave`,p=`onMouseEnter`,m=`mouse`,(e===`pointerout`||e===`pointerover`)&&(d=Vn,g=`onPointerLeave`,p=`onPointerEnter`,m=`pointer`),f=l==null?c:yt(l),h=u==null?c:yt(u),c=new d(g,m+`leave`,l,n,i),c.target=f,c.relatedTarget=h,g=null,_t(i)===r&&(d=new d(p,m+`enter`,u,n,i),d.target=h,d.relatedTarget=f,g=d),f=g,l&&u)b:{for(d=Dd,p=l,m=u,h=0,g=p;g;g=d(g))h++;g=0;for(var _=m;_;_=d(_))g++;for(;0<h-g;)p=d(p),h--;for(;0<g-h;)m=d(m),g--;for(;h--;){if(p===m||m!==null&&p===m.alternate){d=p;break b}p=d(p),m=d(m)}d=null}else d=null;l!==null&&Od(s,c,l,d,!1),u!==null&&f!==null&&Od(s,f,u,d,!0)}}a:{if(c=r?yt(r):window,l=c.nodeName&&c.nodeName.toLowerCase(),l===`select`||l===`input`&&c.type===`file`)var v=dr;else if(ar(c))if(fr)v=xr;else{v=yr;var y=vr}else l=c.nodeName,!l||l.toLowerCase()!==`input`||c.type!==`checkbox`&&c.type!==`radio`?r&&Zt(r.elementType)&&(v=dr):v=br;if(v&&=v(e,r)){or(s,v,n,i);break a}y&&y(e,c,r),e===`focusout`&&r&&c.type===`number`&&r.memoizedProps.value!=null&&Ut(c,`number`,c.value)}switch(y=r?yt(r):window,e){case`focusin`:(ar(y)||y.contentEditable===`true`)&&(jr=y,Mr=r,Nr=null);break;case`focusout`:Nr=Mr=jr=null;break;case`mousedown`:Pr=!0;break;case`contextmenu`:case`mouseup`:case`dragend`:Pr=!1,Fr(s,n,i);break;case`selectionchange`:if(Ar)break;case`keydown`:case`keyup`:Fr(s,n,i)}var b;if(qn)b:{switch(e){case`compositionstart`:var x=`onCompositionStart`;break b;case`compositionend`:x=`onCompositionEnd`;break b;case`compositionupdate`:x=`onCompositionUpdate`;break b}x=void 0}else tr?$n(e,n)&&(x=`onCompositionEnd`):e===`keydown`&&n.keyCode===229&&(x=`onCompositionStart`);x&&(Xn&&n.locale!==`ko`&&(tr||x!==`onCompositionStart`?x===`onCompositionEnd`&&tr&&(b=_n()):(mn=i,hn=`value`in mn?mn.value:mn.textContent,tr=!0)),y=Ed(r,x),0<y.length&&(x=new Fn(x,e,null,n,i),s.push({event:x,listeners:y}),b?x.data=b:(b=er(n),b!==null&&(x.data=b)))),(b=Yn?nr(e,n):rr(e,n))&&(x=Ed(r,`onBeforeInput`),0<x.length&&(y=new Fn(`onBeforeInput`,`beforeinput`,null,n,i),s.push({event:y,listeners:x}),y.data=b)),md(s,e,r,n,i)}yd(s,t)})}function Td(e,t,n){return{instance:e,listener:t,currentTarget:n}}function Ed(e,t){for(var n=t+`Capture`,r=[];e!==null;){var i=e,a=i.stateNode;if(i=i.tag,i!==5&&i!==26&&i!==27||a===null||(i=un(e,n),i!=null&&r.unshift(Td(e,i,a)),i=un(e,t),i!=null&&r.push(Td(e,i,a))),e.tag===3)return r;e=e.return}return[]}function Dd(e){if(e===null)return null;do e=e.return;while(e&&e.tag!==5&&e.tag!==27);return e||null}function Od(e,t,n,r,i){for(var a=t._reactName,o=[];n!==null&&n!==r;){var s=n,c=s.alternate,l=s.stateNode;if(s=s.tag,c!==null&&c===r)break;s!==5&&s!==26&&s!==27||l===null||(c=l,i?(l=un(n,a),l!=null&&o.unshift(Td(n,l,c))):i||(l=un(n,a),l!=null&&o.push(Td(n,l,c)))),n=n.return}o.length!==0&&e.push({event:t,listeners:o})}var kd=/\r\n?/g,Ad=/\u0000|\uFFFD/g;function jd(e){return(typeof e==`string`?e:``+e).replace(kd,`
`).replace(Ad,``)}function Md(e,t){return t=jd(t),jd(e)===t}function $(e,t,n,r,a,o){switch(n){case`children`:typeof r==`string`?t===`body`||t===`textarea`&&r===``||qt(e,r):(typeof r==`number`||typeof r==`bigint`)&&t!==`body`&&qt(e,``+r);break;case`className`:jt(e,`class`,r);break;case`tabIndex`:jt(e,`tabindex`,r);break;case`dir`:case`role`:case`viewBox`:case`width`:case`height`:jt(e,n,r);break;case`style`:Xt(e,r,o);break;case`data`:if(t!==`object`){jt(e,`data`,r);break}case`src`:case`href`:if(r===``&&(t!==`a`||n!==`href`)){e.removeAttribute(n);break}if(r==null||typeof r==`function`||typeof r==`symbol`||typeof r==`boolean`){e.removeAttribute(n);break}r=en(``+r),e.setAttribute(n,r);break;case`action`:case`formAction`:if(typeof r==`function`){e.setAttribute(n,`javascript:throw new Error('A React form was unexpectedly submitted. If you called form.submit() manually, consider using form.requestSubmit() instead. If you\\'re trying to use event.stopPropagation() in a submit event handler, consider also calling event.preventDefault().')`);break}else typeof o==`function`&&(n===`formAction`?(t!==`input`&&$(e,t,`name`,a.name,a,null),$(e,t,`formEncType`,a.formEncType,a,null),$(e,t,`formMethod`,a.formMethod,a,null),$(e,t,`formTarget`,a.formTarget,a,null)):($(e,t,`encType`,a.encType,a,null),$(e,t,`method`,a.method,a,null),$(e,t,`target`,a.target,a,null)));if(r==null||typeof r==`symbol`||typeof r==`boolean`){e.removeAttribute(n);break}r=en(``+r),e.setAttribute(n,r);break;case`onClick`:r!=null&&(e.onclick=tn);break;case`onScroll`:r!=null&&Q(`scroll`,e);break;case`onScrollEnd`:r!=null&&Q(`scrollend`,e);break;case`dangerouslySetInnerHTML`:if(r!=null){if(typeof r!=`object`||!(`__html`in r))throw Error(i(61));if(n=r.__html,n!=null){if(a.children!=null)throw Error(i(60));e.innerHTML=n}}break;case`multiple`:e.multiple=r&&typeof r!=`function`&&typeof r!=`symbol`;break;case`muted`:e.muted=r&&typeof r!=`function`&&typeof r!=`symbol`;break;case`suppressContentEditableWarning`:case`suppressHydrationWarning`:case`defaultValue`:case`defaultChecked`:case`innerHTML`:case`ref`:break;case`autoFocus`:break;case`xlinkHref`:if(r==null||typeof r==`function`||typeof r==`boolean`||typeof r==`symbol`){e.removeAttribute(`xlink:href`);break}n=en(``+r),e.setAttributeNS(`http://www.w3.org/1999/xlink`,`xlink:href`,n);break;case`contentEditable`:case`spellCheck`:case`draggable`:case`value`:case`autoReverse`:case`externalResourcesRequired`:case`focusable`:case`preserveAlpha`:r!=null&&typeof r!=`function`&&typeof r!=`symbol`?e.setAttribute(n,``+r):e.removeAttribute(n);break;case`inert`:case`allowFullScreen`:case`async`:case`autoPlay`:case`controls`:case`default`:case`defer`:case`disabled`:case`disablePictureInPicture`:case`disableRemotePlayback`:case`formNoValidate`:case`hidden`:case`loop`:case`noModule`:case`noValidate`:case`open`:case`playsInline`:case`readOnly`:case`required`:case`reversed`:case`scoped`:case`seamless`:case`itemScope`:r&&typeof r!=`function`&&typeof r!=`symbol`?e.setAttribute(n,``):e.removeAttribute(n);break;case`capture`:case`download`:!0===r?e.setAttribute(n,``):!1!==r&&r!=null&&typeof r!=`function`&&typeof r!=`symbol`?e.setAttribute(n,r):e.removeAttribute(n);break;case`cols`:case`rows`:case`size`:case`span`:r!=null&&typeof r!=`function`&&typeof r!=`symbol`&&!isNaN(r)&&1<=r?e.setAttribute(n,r):e.removeAttribute(n);break;case`rowSpan`:case`start`:r==null||typeof r==`function`||typeof r==`symbol`||isNaN(r)?e.removeAttribute(n):e.setAttribute(n,r);break;case`popover`:Q(`beforetoggle`,e),Q(`toggle`,e),At(e,`popover`,r);break;case`xlinkActuate`:Mt(e,`http://www.w3.org/1999/xlink`,`xlink:actuate`,r);break;case`xlinkArcrole`:Mt(e,`http://www.w3.org/1999/xlink`,`xlink:arcrole`,r);break;case`xlinkRole`:Mt(e,`http://www.w3.org/1999/xlink`,`xlink:role`,r);break;case`xlinkShow`:Mt(e,`http://www.w3.org/1999/xlink`,`xlink:show`,r);break;case`xlinkTitle`:Mt(e,`http://www.w3.org/1999/xlink`,`xlink:title`,r);break;case`xlinkType`:Mt(e,`http://www.w3.org/1999/xlink`,`xlink:type`,r);break;case`xmlBase`:Mt(e,`http://www.w3.org/XML/1998/namespace`,`xml:base`,r);break;case`xmlLang`:Mt(e,`http://www.w3.org/XML/1998/namespace`,`xml:lang`,r);break;case`xmlSpace`:Mt(e,`http://www.w3.org/XML/1998/namespace`,`xml:space`,r);break;case`is`:At(e,`is`,r);break;case`innerText`:case`textContent`:break;default:(!(2<n.length)||n[0]!==`o`&&n[0]!==`O`||n[1]!==`n`&&n[1]!==`N`)&&(n=Qt.get(n)||n,At(e,n,r))}}function Nd(e,t,n,r,a,o){switch(n){case`style`:Xt(e,r,o);break;case`dangerouslySetInnerHTML`:if(r!=null){if(typeof r!=`object`||!(`__html`in r))throw Error(i(61));if(n=r.__html,n!=null){if(a.children!=null)throw Error(i(60));e.innerHTML=n}}break;case`children`:typeof r==`string`?qt(e,r):(typeof r==`number`||typeof r==`bigint`)&&qt(e,``+r);break;case`onScroll`:r!=null&&Q(`scroll`,e);break;case`onScrollEnd`:r!=null&&Q(`scrollend`,e);break;case`onClick`:r!=null&&(e.onclick=tn);break;case`suppressContentEditableWarning`:case`suppressHydrationWarning`:case`innerHTML`:case`ref`:break;case`innerText`:case`textContent`:break;default:if(!Ct.hasOwnProperty(n))a:{if(n[0]===`o`&&n[1]===`n`&&(a=n.endsWith(`Capture`),t=n.slice(2,a?n.length-7:void 0),o=e[lt]||null,o=o==null?null:o[n],typeof o==`function`&&e.removeEventListener(t,o,a),typeof r==`function`)){typeof o!=`function`&&o!==null&&(n in e?e[n]=null:e.hasAttribute(n)&&e.removeAttribute(n)),e.addEventListener(t,r,a);break a}n in e?e[n]=r:!0===r?e.setAttribute(n,``):At(e,n,r)}}}function Pd(e,t,n){switch(t){case`div`:case`span`:case`svg`:case`path`:case`a`:case`g`:case`p`:case`li`:break;case`img`:Q(`error`,e),Q(`load`,e);var r=!1,a=!1,o;for(o in n)if(n.hasOwnProperty(o)){var s=n[o];if(s!=null)switch(o){case`src`:r=!0;break;case`srcSet`:a=!0;break;case`children`:case`dangerouslySetInnerHTML`:throw Error(i(137,t));default:$(e,t,o,s,n,null)}}a&&$(e,t,`srcSet`,n.srcSet,n,null),r&&$(e,t,`src`,n.src,n,null);return;case`input`:Q(`invalid`,e);var c=o=s=a=null,l=null,u=null;for(r in n)if(n.hasOwnProperty(r)){var d=n[r];if(d!=null)switch(r){case`name`:a=d;break;case`type`:s=d;break;case`checked`:l=d;break;case`defaultChecked`:u=d;break;case`value`:o=d;break;case`defaultValue`:c=d;break;case`children`:case`dangerouslySetInnerHTML`:if(d!=null)throw Error(i(137,t));break;default:$(e,t,r,d,n,null)}}Ht(e,o,c,l,u,s,a,!1);return;case`select`:for(a in Q(`invalid`,e),r=s=o=null,n)if(n.hasOwnProperty(a)&&(c=n[a],c!=null))switch(a){case`value`:o=c;break;case`defaultValue`:s=c;break;case`multiple`:r=c;default:$(e,t,a,c,n,null)}t=o,n=s,e.multiple=!!r,t==null?n!=null&&Wt(e,!!r,n,!0):Wt(e,!!r,t,!1);return;case`textarea`:for(s in Q(`invalid`,e),o=a=r=null,n)if(n.hasOwnProperty(s)&&(c=n[s],c!=null))switch(s){case`value`:r=c;break;case`defaultValue`:a=c;break;case`children`:o=c;break;case`dangerouslySetInnerHTML`:if(c!=null)throw Error(i(91));break;default:$(e,t,s,c,n,null)}Kt(e,r,a,o);return;case`option`:for(l in n)if(n.hasOwnProperty(l)&&(r=n[l],r!=null))switch(l){case`selected`:e.selected=r&&typeof r!=`function`&&typeof r!=`symbol`;break;default:$(e,t,l,r,n,null)}return;case`dialog`:Q(`beforetoggle`,e),Q(`toggle`,e),Q(`cancel`,e),Q(`close`,e);break;case`iframe`:case`object`:Q(`load`,e);break;case`video`:case`audio`:for(r=0;r<_d.length;r++)Q(_d[r],e);break;case`image`:Q(`error`,e),Q(`load`,e);break;case`details`:Q(`toggle`,e);break;case`embed`:case`source`:case`link`:Q(`error`,e),Q(`load`,e);case`area`:case`base`:case`br`:case`col`:case`hr`:case`keygen`:case`meta`:case`param`:case`track`:case`wbr`:case`menuitem`:for(u in n)if(n.hasOwnProperty(u)&&(r=n[u],r!=null))switch(u){case`children`:case`dangerouslySetInnerHTML`:throw Error(i(137,t));default:$(e,t,u,r,n,null)}return;default:if(Zt(t)){for(d in n)n.hasOwnProperty(d)&&(r=n[d],r!==void 0&&Nd(e,t,d,r,n,void 0));return}}for(c in n)n.hasOwnProperty(c)&&(r=n[c],r!=null&&$(e,t,c,r,n,null))}function Fd(e,t,n,r){switch(t){case`div`:case`span`:case`svg`:case`path`:case`a`:case`g`:case`p`:case`li`:break;case`input`:var a=null,o=null,s=null,c=null,l=null,u=null,d=null;for(m in n){var f=n[m];if(n.hasOwnProperty(m)&&f!=null)switch(m){case`checked`:break;case`value`:break;case`defaultValue`:l=f;default:r.hasOwnProperty(m)||$(e,t,m,null,r,f)}}for(var p in r){var m=r[p];if(f=n[p],r.hasOwnProperty(p)&&(m!=null||f!=null))switch(p){case`type`:o=m;break;case`name`:a=m;break;case`checked`:u=m;break;case`defaultChecked`:d=m;break;case`value`:s=m;break;case`defaultValue`:c=m;break;case`children`:case`dangerouslySetInnerHTML`:if(m!=null)throw Error(i(137,t));break;default:m!==f&&$(e,t,p,m,r,f)}}Vt(e,s,c,l,u,d,o,a);return;case`select`:for(o in m=s=c=p=null,n)if(l=n[o],n.hasOwnProperty(o)&&l!=null)switch(o){case`value`:break;case`multiple`:m=l;default:r.hasOwnProperty(o)||$(e,t,o,null,r,l)}for(a in r)if(o=r[a],l=n[a],r.hasOwnProperty(a)&&(o!=null||l!=null))switch(a){case`value`:p=o;break;case`defaultValue`:c=o;break;case`multiple`:s=o;default:o!==l&&$(e,t,a,o,r,l)}t=c,n=s,r=m,p==null?!!r!=!!n&&(t==null?Wt(e,!!n,n?[]:``,!1):Wt(e,!!n,t,!0)):Wt(e,!!n,p,!1);return;case`textarea`:for(c in m=p=null,n)if(a=n[c],n.hasOwnProperty(c)&&a!=null&&!r.hasOwnProperty(c))switch(c){case`value`:break;case`children`:break;default:$(e,t,c,null,r,a)}for(s in r)if(a=r[s],o=n[s],r.hasOwnProperty(s)&&(a!=null||o!=null))switch(s){case`value`:p=a;break;case`defaultValue`:m=a;break;case`children`:break;case`dangerouslySetInnerHTML`:if(a!=null)throw Error(i(91));break;default:a!==o&&$(e,t,s,a,r,o)}Gt(e,p,m);return;case`option`:for(var h in n)if(p=n[h],n.hasOwnProperty(h)&&p!=null&&!r.hasOwnProperty(h))switch(h){case`selected`:e.selected=!1;break;default:$(e,t,h,null,r,p)}for(l in r)if(p=r[l],m=n[l],r.hasOwnProperty(l)&&p!==m&&(p!=null||m!=null))switch(l){case`selected`:e.selected=p&&typeof p!=`function`&&typeof p!=`symbol`;break;default:$(e,t,l,p,r,m)}return;case`img`:case`link`:case`area`:case`base`:case`br`:case`col`:case`embed`:case`hr`:case`keygen`:case`meta`:case`param`:case`source`:case`track`:case`wbr`:case`menuitem`:for(var g in n)p=n[g],n.hasOwnProperty(g)&&p!=null&&!r.hasOwnProperty(g)&&$(e,t,g,null,r,p);for(u in r)if(p=r[u],m=n[u],r.hasOwnProperty(u)&&p!==m&&(p!=null||m!=null))switch(u){case`children`:case`dangerouslySetInnerHTML`:if(p!=null)throw Error(i(137,t));break;default:$(e,t,u,p,r,m)}return;default:if(Zt(t)){for(var _ in n)p=n[_],n.hasOwnProperty(_)&&p!==void 0&&!r.hasOwnProperty(_)&&Nd(e,t,_,void 0,r,p);for(d in r)p=r[d],m=n[d],!r.hasOwnProperty(d)||p===m||p===void 0&&m===void 0||Nd(e,t,d,p,r,m);return}}for(var v in n)p=n[v],n.hasOwnProperty(v)&&p!=null&&!r.hasOwnProperty(v)&&$(e,t,v,null,r,p);for(f in r)p=r[f],m=n[f],!r.hasOwnProperty(f)||p===m||p==null&&m==null||$(e,t,f,p,r,m)}function Id(e){switch(e){case`css`:case`script`:case`font`:case`img`:case`image`:case`input`:case`link`:return!0;default:return!1}}function Ld(){if(typeof performance.getEntriesByType==`function`){for(var e=0,t=0,n=performance.getEntriesByType(`resource`),r=0;r<n.length;r++){var i=n[r],a=i.transferSize,o=i.initiatorType,s=i.duration;if(a&&s&&Id(o)){for(o=0,s=i.responseEnd,r+=1;r<n.length;r++){var c=n[r],l=c.startTime;if(l>s)break;var u=c.transferSize,d=c.initiatorType;u&&Id(d)&&(c=c.responseEnd,o+=u*(c<s?1:(s-l)/(c-l)))}if(--r,t+=8*(a+o)/(i.duration/1e3),e++,10<e)break}}if(0<e)return t/e/1e6}return navigator.connection&&(e=navigator.connection.downlink,typeof e==`number`)?e:5}var Rd=null,zd=null;function Bd(e){return e.nodeType===9?e:e.ownerDocument}function Vd(e){switch(e){case`http://www.w3.org/2000/svg`:return 1;case`http://www.w3.org/1998/Math/MathML`:return 2;default:return 0}}function Hd(e,t){if(e===0)switch(t){case`svg`:return 1;case`math`:return 2;default:return 0}return e===1&&t===`foreignObject`?0:e}function Ud(e,t){return e===`textarea`||e===`noscript`||typeof t.children==`string`||typeof t.children==`number`||typeof t.children==`bigint`||typeof t.dangerouslySetInnerHTML==`object`&&t.dangerouslySetInnerHTML!==null&&t.dangerouslySetInnerHTML.__html!=null}var Wd=null;function Gd(){var e=window.event;return e&&e.type===`popstate`?e===Wd?!1:(Wd=e,!0):(Wd=null,!1)}var Kd=typeof setTimeout==`function`?setTimeout:void 0,qd=typeof clearTimeout==`function`?clearTimeout:void 0,Jd=typeof Promise==`function`?Promise:void 0,Yd=typeof queueMicrotask==`function`?queueMicrotask:Jd===void 0?Kd:function(e){return Jd.resolve(null).then(e).catch(Xd)};function Xd(e){setTimeout(function(){throw e})}function Zd(e){return e===`head`}function Qd(e,t){var n=t,r=0;do{var i=n.nextSibling;if(e.removeChild(n),i&&i.nodeType===8)if(n=i.data,n===`/$`||n===`/&`){if(r===0){e.removeChild(i),Np(t);return}r--}else if(n===`$`||n===`$?`||n===`$~`||n===`$!`||n===`&`)r++;else if(n===`html`)pf(e.ownerDocument.documentElement);else if(n===`head`){n=e.ownerDocument.head,pf(n);for(var a=n.firstChild;a;){var o=a.nextSibling,s=a.nodeName;a[ht]||s===`SCRIPT`||s===`STYLE`||s===`LINK`&&a.rel.toLowerCase()===`stylesheet`||n.removeChild(a),a=o}}else n===`body`&&pf(e.ownerDocument.body);n=i}while(n);Np(t)}function $d(e,t){var n=e;e=0;do{var r=n.nextSibling;if(n.nodeType===1?t?(n._stashedDisplay=n.style.display,n.style.display=`none`):(n.style.display=n._stashedDisplay||``,n.getAttribute(`style`)===``&&n.removeAttribute(`style`)):n.nodeType===3&&(t?(n._stashedText=n.nodeValue,n.nodeValue=``):n.nodeValue=n._stashedText||``),r&&r.nodeType===8)if(n=r.data,n===`/$`){if(e===0)break;e--}else n!==`$`&&n!==`$?`&&n!==`$~`&&n!==`$!`||e++;n=r}while(n)}function ef(e){var t=e.firstChild;for(t&&t.nodeType===10&&(t=t.nextSibling);t;){var n=t;switch(t=t.nextSibling,n.nodeName){case`HTML`:case`HEAD`:case`BODY`:ef(n),gt(n);continue;case`SCRIPT`:case`STYLE`:continue;case`LINK`:if(n.rel.toLowerCase()===`stylesheet`)continue}e.removeChild(n)}}function tf(e,t,n,r){for(;e.nodeType===1;){var i=n;if(e.nodeName.toLowerCase()!==t.toLowerCase()){if(!r&&(e.nodeName!==`INPUT`||e.type!==`hidden`))break}else if(!r)if(t===`input`&&e.type===`hidden`){var a=i.name==null?null:``+i.name;if(i.type===`hidden`&&e.getAttribute(`name`)===a)return e}else return e;else if(!e[ht])switch(t){case`meta`:if(!e.hasAttribute(`itemprop`))break;return e;case`link`:if(a=e.getAttribute(`rel`),a===`stylesheet`&&e.hasAttribute(`data-precedence`)||a!==i.rel||e.getAttribute(`href`)!==(i.href==null||i.href===``?null:i.href)||e.getAttribute(`crossorigin`)!==(i.crossOrigin==null?null:i.crossOrigin)||e.getAttribute(`title`)!==(i.title==null?null:i.title))break;return e;case`style`:if(e.hasAttribute(`data-precedence`))break;return e;case`script`:if(a=e.getAttribute(`src`),(a!==(i.src==null?null:i.src)||e.getAttribute(`type`)!==(i.type==null?null:i.type)||e.getAttribute(`crossorigin`)!==(i.crossOrigin==null?null:i.crossOrigin))&&a&&e.hasAttribute(`async`)&&!e.hasAttribute(`itemprop`))break;return e;default:return e}if(e=cf(e.nextSibling),e===null)break}return null}function nf(e,t,n){if(t===``)return null;for(;e.nodeType!==3;)if((e.nodeType!==1||e.nodeName!==`INPUT`||e.type!==`hidden`)&&!n||(e=cf(e.nextSibling),e===null))return null;return e}function rf(e,t){for(;e.nodeType!==8;)if((e.nodeType!==1||e.nodeName!==`INPUT`||e.type!==`hidden`)&&!t||(e=cf(e.nextSibling),e===null))return null;return e}function af(e){return e.data===`$?`||e.data===`$~`}function of(e){return e.data===`$!`||e.data===`$?`&&e.ownerDocument.readyState!==`loading`}function sf(e,t){var n=e.ownerDocument;if(e.data===`$~`)e._reactRetry=t;else if(e.data!==`$?`||n.readyState!==`loading`)t();else{var r=function(){t(),n.removeEventListener(`DOMContentLoaded`,r)};n.addEventListener(`DOMContentLoaded`,r),e._reactRetry=r}}function cf(e){for(;e!=null;e=e.nextSibling){var t=e.nodeType;if(t===1||t===3)break;if(t===8){if(t=e.data,t===`$`||t===`$!`||t===`$?`||t===`$~`||t===`&`||t===`F!`||t===`F`)break;if(t===`/$`||t===`/&`)return null}}return e}var lf=null;function uf(e){e=e.nextSibling;for(var t=0;e;){if(e.nodeType===8){var n=e.data;if(n===`/$`||n===`/&`){if(t===0)return cf(e.nextSibling);t--}else n!==`$`&&n!==`$!`&&n!==`$?`&&n!==`$~`&&n!==`&`||t++}e=e.nextSibling}return null}function df(e){e=e.previousSibling;for(var t=0;e;){if(e.nodeType===8){var n=e.data;if(n===`$`||n===`$!`||n===`$?`||n===`$~`||n===`&`){if(t===0)return e;t--}else n!==`/$`&&n!==`/&`||t++}e=e.previousSibling}return null}function ff(e,t,n){switch(t=Bd(n),e){case`html`:if(e=t.documentElement,!e)throw Error(i(452));return e;case`head`:if(e=t.head,!e)throw Error(i(453));return e;case`body`:if(e=t.body,!e)throw Error(i(454));return e;default:throw Error(i(451))}}function pf(e){for(var t=e.attributes;t.length;)e.removeAttributeNode(t[0]);gt(e)}var mf=new Map,hf=new Set;function gf(e){return typeof e.getRootNode==`function`?e.getRootNode():e.nodeType===9?e:e.ownerDocument}var _f=M.d;M.d={f:vf,r:yf,D:Sf,C:Cf,L:wf,m:Tf,X:Df,S:Ef,M:Of};function vf(){var e=_f.f(),t=bu();return e||t}function yf(e){var t=vt(e);t!==null&&t.tag===5&&t.type===`form`?Ts(t):_f.r(e)}var bf=typeof document>`u`?null:document;function xf(e,t,n){var r=bf;if(r&&typeof t==`string`&&t){var i=Bt(t);i=`link[rel="`+e+`"][href="`+i+`"]`,typeof n==`string`&&(i+=`[crossorigin="`+n+`"]`),hf.has(i)||(hf.add(i),e={rel:e,crossOrigin:n,href:t},r.querySelector(i)===null&&(t=r.createElement(`link`),Pd(t,`link`,e),xt(t),r.head.appendChild(t)))}}function Sf(e){_f.D(e),xf(`dns-prefetch`,e,null)}function Cf(e,t){_f.C(e,t),xf(`preconnect`,e,t)}function wf(e,t,n){_f.L(e,t,n);var r=bf;if(r&&e&&t){var i=`link[rel="preload"][as="`+Bt(t)+`"]`;t===`image`&&n&&n.imageSrcSet?(i+=`[imagesrcset="`+Bt(n.imageSrcSet)+`"]`,typeof n.imageSizes==`string`&&(i+=`[imagesizes="`+Bt(n.imageSizes)+`"]`)):i+=`[href="`+Bt(e)+`"]`;var a=i;switch(t){case`style`:a=Af(e);break;case`script`:a=Pf(e)}mf.has(a)||(e=h({rel:`preload`,href:t===`image`&&n&&n.imageSrcSet?void 0:e,as:t},n),mf.set(a,e),r.querySelector(i)!==null||t===`style`&&r.querySelector(jf(a))||t===`script`&&r.querySelector(Ff(a))||(t=r.createElement(`link`),Pd(t,`link`,e),xt(t),r.head.appendChild(t)))}}function Tf(e,t){_f.m(e,t);var n=bf;if(n&&e){var r=t&&typeof t.as==`string`?t.as:`script`,i=`link[rel="modulepreload"][as="`+Bt(r)+`"][href="`+Bt(e)+`"]`,a=i;switch(r){case`audioworklet`:case`paintworklet`:case`serviceworker`:case`sharedworker`:case`worker`:case`script`:a=Pf(e)}if(!mf.has(a)&&(e=h({rel:`modulepreload`,href:e},t),mf.set(a,e),n.querySelector(i)===null)){switch(r){case`audioworklet`:case`paintworklet`:case`serviceworker`:case`sharedworker`:case`worker`:case`script`:if(n.querySelector(Ff(a)))return}r=n.createElement(`link`),Pd(r,`link`,e),xt(r),n.head.appendChild(r)}}}function Ef(e,t,n){_f.S(e,t,n);var r=bf;if(r&&e){var i=bt(r).hoistableStyles,a=Af(e);t||=`default`;var o=i.get(a);if(!o){var s={loading:0,preload:null};if(o=r.querySelector(jf(a)))s.loading=5;else{e=h({rel:`stylesheet`,href:e,"data-precedence":t},n),(n=mf.get(a))&&Rf(e,n);var c=o=r.createElement(`link`);xt(c),Pd(c,`link`,e),c._p=new Promise(function(e,t){c.onload=e,c.onerror=t}),c.addEventListener(`load`,function(){s.loading|=1}),c.addEventListener(`error`,function(){s.loading|=2}),s.loading|=4,Lf(o,t,r)}o={type:`stylesheet`,instance:o,count:1,state:s},i.set(a,o)}}}function Df(e,t){_f.X(e,t);var n=bf;if(n&&e){var r=bt(n).hoistableScripts,i=Pf(e),a=r.get(i);a||(a=n.querySelector(Ff(i)),a||(e=h({src:e,async:!0},t),(t=mf.get(i))&&zf(e,t),a=n.createElement(`script`),xt(a),Pd(a,`link`,e),n.head.appendChild(a)),a={type:`script`,instance:a,count:1,state:null},r.set(i,a))}}function Of(e,t){_f.M(e,t);var n=bf;if(n&&e){var r=bt(n).hoistableScripts,i=Pf(e),a=r.get(i);a||(a=n.querySelector(Ff(i)),a||(e=h({src:e,async:!0,type:`module`},t),(t=mf.get(i))&&zf(e,t),a=n.createElement(`script`),xt(a),Pd(a,`link`,e),n.head.appendChild(a)),a={type:`script`,instance:a,count:1,state:null},r.set(i,a))}}function kf(e,t,n,r){var a=(a=fe.current)?gf(a):null;if(!a)throw Error(i(446));switch(e){case`meta`:case`title`:return null;case`style`:return typeof n.precedence==`string`&&typeof n.href==`string`?(t=Af(n.href),n=bt(a).hoistableStyles,r=n.get(t),r||(r={type:`style`,instance:null,count:0,state:null},n.set(t,r)),r):{type:`void`,instance:null,count:0,state:null};case`link`:if(n.rel===`stylesheet`&&typeof n.href==`string`&&typeof n.precedence==`string`){e=Af(n.href);var o=bt(a).hoistableStyles,s=o.get(e);if(s||(a=a.ownerDocument||a,s={type:`stylesheet`,instance:null,count:0,state:{loading:0,preload:null}},o.set(e,s),(o=a.querySelector(jf(e)))&&!o._p&&(s.instance=o,s.state.loading=5),mf.has(e)||(n={rel:`preload`,as:`style`,href:n.href,crossOrigin:n.crossOrigin,integrity:n.integrity,media:n.media,hrefLang:n.hrefLang,referrerPolicy:n.referrerPolicy},mf.set(e,n),o||Nf(a,e,n,s.state))),t&&r===null)throw Error(i(528,``));return s}if(t&&r!==null)throw Error(i(529,``));return null;case`script`:return t=n.async,n=n.src,typeof n==`string`&&t&&typeof t!=`function`&&typeof t!=`symbol`?(t=Pf(n),n=bt(a).hoistableScripts,r=n.get(t),r||(r={type:`script`,instance:null,count:0,state:null},n.set(t,r)),r):{type:`void`,instance:null,count:0,state:null};default:throw Error(i(444,e))}}function Af(e){return`href="`+Bt(e)+`"`}function jf(e){return`link[rel="stylesheet"][`+e+`]`}function Mf(e){return h({},e,{"data-precedence":e.precedence,precedence:null})}function Nf(e,t,n,r){e.querySelector(`link[rel="preload"][as="style"][`+t+`]`)?r.loading=1:(t=e.createElement(`link`),r.preload=t,t.addEventListener(`load`,function(){return r.loading|=1}),t.addEventListener(`error`,function(){return r.loading|=2}),Pd(t,`link`,n),xt(t),e.head.appendChild(t))}function Pf(e){return`[src="`+Bt(e)+`"]`}function Ff(e){return`script[async]`+e}function If(e,t,n){if(t.count++,t.instance===null)switch(t.type){case`style`:var r=e.querySelector(`style[data-href~="`+Bt(n.href)+`"]`);if(r)return t.instance=r,xt(r),r;var a=h({},n,{"data-href":n.href,"data-precedence":n.precedence,href:null,precedence:null});return r=(e.ownerDocument||e).createElement(`style`),xt(r),Pd(r,`style`,a),Lf(r,n.precedence,e),t.instance=r;case`stylesheet`:a=Af(n.href);var o=e.querySelector(jf(a));if(o)return t.state.loading|=4,t.instance=o,xt(o),o;r=Mf(n),(a=mf.get(a))&&Rf(r,a),o=(e.ownerDocument||e).createElement(`link`),xt(o);var s=o;return s._p=new Promise(function(e,t){s.onload=e,s.onerror=t}),Pd(o,`link`,r),t.state.loading|=4,Lf(o,n.precedence,e),t.instance=o;case`script`:return o=Pf(n.src),(a=e.querySelector(Ff(o)))?(t.instance=a,xt(a),a):(r=n,(a=mf.get(o))&&(r=h({},n),zf(r,a)),e=e.ownerDocument||e,a=e.createElement(`script`),xt(a),Pd(a,`link`,r),e.head.appendChild(a),t.instance=a);case`void`:return null;default:throw Error(i(443,t.type))}else t.type===`stylesheet`&&!(t.state.loading&4)&&(r=t.instance,t.state.loading|=4,Lf(r,n.precedence,e));return t.instance}function Lf(e,t,n){for(var r=n.querySelectorAll(`link[rel="stylesheet"][data-precedence],style[data-precedence]`),i=r.length?r[r.length-1]:null,a=i,o=0;o<r.length;o++){var s=r[o];if(s.dataset.precedence===t)a=s;else if(a!==i)break}a?a.parentNode.insertBefore(e,a.nextSibling):(t=n.nodeType===9?n.head:n,t.insertBefore(e,t.firstChild))}function Rf(e,t){e.crossOrigin??=t.crossOrigin,e.referrerPolicy??=t.referrerPolicy,e.title??=t.title}function zf(e,t){e.crossOrigin??=t.crossOrigin,e.referrerPolicy??=t.referrerPolicy,e.integrity??=t.integrity}var Bf=null;function Vf(e,t,n){if(Bf===null){var r=new Map,i=Bf=new Map;i.set(n,r)}else i=Bf,r=i.get(n),r||(r=new Map,i.set(n,r));if(r.has(e))return r;for(r.set(e,null),n=n.getElementsByTagName(e),i=0;i<n.length;i++){var a=n[i];if(!(a[ht]||a[ct]||e===`link`&&a.getAttribute(`rel`)===`stylesheet`)&&a.namespaceURI!==`http://www.w3.org/2000/svg`){var o=a.getAttribute(t)||``;o=e+o;var s=r.get(o);s?s.push(a):r.set(o,[a])}}return r}function Hf(e,t,n){e=e.ownerDocument||e,e.head.insertBefore(n,t===`title`?e.querySelector(`head > title`):null)}function Uf(e,t,n){if(n===1||t.itemProp!=null)return!1;switch(e){case`meta`:case`title`:return!0;case`style`:if(typeof t.precedence!=`string`||typeof t.href!=`string`||t.href===``)break;return!0;case`link`:if(typeof t.rel!=`string`||typeof t.href!=`string`||t.href===``||t.onLoad||t.onError)break;switch(t.rel){case`stylesheet`:return e=t.disabled,typeof t.precedence==`string`&&e==null;default:return!0}case`script`:if(t.async&&typeof t.async!=`function`&&typeof t.async!=`symbol`&&!t.onLoad&&!t.onError&&t.src&&typeof t.src==`string`)return!0}return!1}function Wf(e){return!(e.type===`stylesheet`&&!(e.state.loading&3))}function Gf(e,t,n,r){if(n.type===`stylesheet`&&(typeof r.media!=`string`||!1!==matchMedia(r.media).matches)&&!(n.state.loading&4)){if(n.instance===null){var i=Af(r.href),a=t.querySelector(jf(i));if(a){t=a._p,typeof t==`object`&&t&&typeof t.then==`function`&&(e.count++,e=Jf.bind(e),t.then(e,e)),n.state.loading|=4,n.instance=a,xt(a);return}a=t.ownerDocument||t,r=Mf(r),(i=mf.get(i))&&Rf(r,i),a=a.createElement(`link`),xt(a);var o=a;o._p=new Promise(function(e,t){o.onload=e,o.onerror=t}),Pd(a,`link`,r),n.instance=a}e.stylesheets===null&&(e.stylesheets=new Map),e.stylesheets.set(n,t),(t=n.state.preload)&&!(n.state.loading&3)&&(e.count++,n=Jf.bind(e),t.addEventListener(`load`,n),t.addEventListener(`error`,n))}}var Kf=0;function qf(e,t){return e.stylesheets&&e.count===0&&Xf(e,e.stylesheets),0<e.count||0<e.imgCount?function(n){var r=setTimeout(function(){if(e.stylesheets&&Xf(e,e.stylesheets),e.unsuspend){var t=e.unsuspend;e.unsuspend=null,t()}},6e4+t);0<e.imgBytes&&Kf===0&&(Kf=62500*Ld());var i=setTimeout(function(){if(e.waitingForImages=!1,e.count===0&&(e.stylesheets&&Xf(e,e.stylesheets),e.unsuspend)){var t=e.unsuspend;e.unsuspend=null,t()}},(e.imgBytes>Kf?50:800)+t);return e.unsuspend=n,function(){e.unsuspend=null,clearTimeout(r),clearTimeout(i)}}:null}function Jf(){if(this.count--,this.count===0&&(this.imgCount===0||!this.waitingForImages)){if(this.stylesheets)Xf(this,this.stylesheets);else if(this.unsuspend){var e=this.unsuspend;this.unsuspend=null,e()}}}var Yf=null;function Xf(e,t){e.stylesheets=null,e.unsuspend!==null&&(e.count++,Yf=new Map,t.forEach(Zf,e),Yf=null,Jf.call(e))}function Zf(e,t){if(!(t.state.loading&4)){var n=Yf.get(e);if(n)var r=n.get(null);else{n=new Map,Yf.set(e,n);for(var i=e.querySelectorAll(`link[data-precedence],style[data-precedence]`),a=0;a<i.length;a++){var o=i[a];(o.nodeName===`LINK`||o.getAttribute(`media`)!==`not all`)&&(n.set(o.dataset.precedence,o),r=o)}r&&n.set(null,r)}i=t.instance,o=i.getAttribute(`data-precedence`),a=n.get(o)||r,a===r&&n.set(null,i),n.set(o,i),this.count++,r=Jf.bind(this),i.addEventListener(`load`,r),i.addEventListener(`error`,r),a?a.parentNode.insertBefore(i,a.nextSibling):(e=e.nodeType===9?e.head:e,e.insertBefore(i,e.firstChild)),t.state.loading|=4}}var Qf={$$typeof:C,Provider:null,Consumer:null,_currentValue:ae,_currentValue2:ae,_threadCount:0};function $f(e,t,n,r,i,a,o,s,c){this.tag=1,this.containerInfo=e,this.pingCache=this.current=this.pendingChildren=null,this.timeoutHandle=-1,this.callbackNode=this.next=this.pendingContext=this.context=this.cancelPendingCommit=null,this.callbackPriority=0,this.expirationTimes=Qe(-1),this.entangledLanes=this.shellSuspendCounter=this.errorRecoveryDisabledLanes=this.expiredLanes=this.warmLanes=this.pingedLanes=this.suspendedLanes=this.pendingLanes=0,this.entanglements=Qe(0),this.hiddenUpdates=Qe(null),this.identifierPrefix=r,this.onUncaughtError=i,this.onCaughtError=a,this.onRecoverableError=o,this.pooledCache=null,this.pooledCacheLanes=0,this.formState=c,this.incompleteTransitions=new Map}function ep(e,t,n,r,i,a,o,s,c,l,u,d){return e=new $f(e,t,n,o,c,l,u,d,s),t=1,!0===a&&(t|=24),a=li(3,null,null,t),e.current=a,a.stateNode=e,t=sa(),t.refCount++,e.pooledCache=t,t.refCount++,a.memoizedState={element:r,isDehydrated:n,cache:t},za(a),e}function tp(e){return e?(e=si,e):si}function np(e,t,n,r,i,a){i=tp(i),r.context===null?r.context=i:r.pendingContext=i,r=Va(t),r.payload={element:n},a=a===void 0?null:a,a!==null&&(r.callback=a),n=Ha(e,r,t),n!==null&&(hu(n,e,t),Ua(n,e,t))}function rp(e,t){if(e=e.memoizedState,e!==null&&e.dehydrated!==null){var n=e.retryLane;e.retryLane=n!==0&&n<t?n:t}}function ip(e,t){rp(e,t),(e=e.alternate)&&rp(e,t)}function ap(e){if(e.tag===13||e.tag===31){var t=ii(e,67108864);t!==null&&hu(t,e,67108864),ip(e,67108864)}}function op(e){if(e.tag===13||e.tag===31){var t=pu();t=it(t);var n=ii(e,t);n!==null&&hu(n,e,t),ip(e,t)}}var sp=!0;function cp(e,t,n,r){var i=j.T;j.T=null;var a=M.p;try{M.p=2,up(e,t,n,r)}finally{M.p=a,j.T=i}}function lp(e,t,n,r){var i=j.T;j.T=null;var a=M.p;try{M.p=8,up(e,t,n,r)}finally{M.p=a,j.T=i}}function up(e,t,n,r){if(sp){var i=dp(r);if(i===null)wd(e,t,r,fp,n),Cp(e,r);else if(Tp(i,e,t,n,r))r.stopPropagation();else if(Cp(e,r),t&4&&-1<Sp.indexOf(e)){for(;i!==null;){var a=vt(i);if(a!==null)switch(a.tag){case 3:if(a=a.stateNode,a.current.memoizedState.isDehydrated){var o=qe(a.pendingLanes);if(o!==0){var s=a;for(s.pendingLanes|=2,s.entangledLanes|=2;o;){var c=1<<31-P(o);s.entanglements[1]|=c,o&=~c}rd(a),!(K&6)&&(tu=Ae()+500,id(0,!1))}}break;case 31:case 13:s=ii(a,2),s!==null&&hu(s,a,2),bu(),ip(a,2)}if(a=dp(r),a===null&&wd(e,t,r,fp,n),a===i)break;i=a}i!==null&&r.stopPropagation()}else wd(e,t,r,null,n)}}function dp(e){return e=rn(e),pp(e)}var fp=null;function pp(e){if(fp=null,e=_t(e),e!==null){var t=o(e);if(t===null)e=null;else{var n=t.tag;if(n===13){if(e=s(t),e!==null)return e;e=null}else if(n===31){if(e=c(t),e!==null)return e;e=null}else if(n===3){if(t.stateNode.current.memoizedState.isDehydrated)return t.tag===3?t.stateNode.containerInfo:null;e=null}else t!==e&&(e=null)}}return fp=e,null}function mp(e){switch(e){case`beforetoggle`:case`cancel`:case`click`:case`close`:case`contextmenu`:case`copy`:case`cut`:case`auxclick`:case`dblclick`:case`dragend`:case`dragstart`:case`drop`:case`focusin`:case`focusout`:case`input`:case`invalid`:case`keydown`:case`keypress`:case`keyup`:case`mousedown`:case`mouseup`:case`paste`:case`pause`:case`play`:case`pointercancel`:case`pointerdown`:case`pointerup`:case`ratechange`:case`reset`:case`resize`:case`seeked`:case`submit`:case`toggle`:case`touchcancel`:case`touchend`:case`touchstart`:case`volumechange`:case`change`:case`selectionchange`:case`textInput`:case`compositionstart`:case`compositionend`:case`compositionupdate`:case`beforeblur`:case`afterblur`:case`beforeinput`:case`blur`:case`fullscreenchange`:case`focus`:case`hashchange`:case`popstate`:case`select`:case`selectstart`:return 2;case`drag`:case`dragenter`:case`dragexit`:case`dragleave`:case`dragover`:case`mousemove`:case`mouseout`:case`mouseover`:case`pointermove`:case`pointerout`:case`pointerover`:case`scroll`:case`touchmove`:case`wheel`:case`mouseenter`:case`mouseleave`:case`pointerenter`:case`pointerleave`:return 8;case`message`:switch(je()){case Me:return 2;case Ne:return 8;case Pe:case Fe:return 32;case Ie:return 268435456;default:return 32}default:return 32}}var hp=!1,gp=null,_p=null,vp=null,yp=new Map,bp=new Map,xp=[],Sp=`mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset`.split(` `);function Cp(e,t){switch(e){case`focusin`:case`focusout`:gp=null;break;case`dragenter`:case`dragleave`:_p=null;break;case`mouseover`:case`mouseout`:vp=null;break;case`pointerover`:case`pointerout`:yp.delete(t.pointerId);break;case`gotpointercapture`:case`lostpointercapture`:bp.delete(t.pointerId)}}function wp(e,t,n,r,i,a){return e===null||e.nativeEvent!==a?(e={blockedOn:t,domEventName:n,eventSystemFlags:r,nativeEvent:a,targetContainers:[i]},t!==null&&(t=vt(t),t!==null&&ap(t)),e):(e.eventSystemFlags|=r,t=e.targetContainers,i!==null&&t.indexOf(i)===-1&&t.push(i),e)}function Tp(e,t,n,r,i){switch(t){case`focusin`:return gp=wp(gp,e,t,n,r,i),!0;case`dragenter`:return _p=wp(_p,e,t,n,r,i),!0;case`mouseover`:return vp=wp(vp,e,t,n,r,i),!0;case`pointerover`:var a=i.pointerId;return yp.set(a,wp(yp.get(a)||null,e,t,n,r,i)),!0;case`gotpointercapture`:return a=i.pointerId,bp.set(a,wp(bp.get(a)||null,e,t,n,r,i)),!0}return!1}function Ep(e){var t=_t(e.target);if(t!==null){var n=o(t);if(n!==null){if(t=n.tag,t===13){if(t=s(n),t!==null){e.blockedOn=t,st(e.priority,function(){op(n)});return}}else if(t===31){if(t=c(n),t!==null){e.blockedOn=t,st(e.priority,function(){op(n)});return}}else if(t===3&&n.stateNode.current.memoizedState.isDehydrated){e.blockedOn=n.tag===3?n.stateNode.containerInfo:null;return}}}e.blockedOn=null}function Dp(e){if(e.blockedOn!==null)return!1;for(var t=e.targetContainers;0<t.length;){var n=dp(e.nativeEvent);if(n===null){n=e.nativeEvent;var r=new n.constructor(n.type,n);nn=r,n.target.dispatchEvent(r),nn=null}else return t=vt(n),t!==null&&ap(t),e.blockedOn=n,!1;t.shift()}return!0}function Op(e,t,n){Dp(e)&&n.delete(t)}function kp(){hp=!1,gp!==null&&Dp(gp)&&(gp=null),_p!==null&&Dp(_p)&&(_p=null),vp!==null&&Dp(vp)&&(vp=null),yp.forEach(Op),bp.forEach(Op)}function Ap(e,n){e.blockedOn===n&&(e.blockedOn=null,hp||(hp=!0,t.unstable_scheduleCallback(t.unstable_NormalPriority,kp)))}var jp=null;function Mp(e){jp!==e&&(jp=e,t.unstable_scheduleCallback(t.unstable_NormalPriority,function(){jp===e&&(jp=null);for(var t=0;t<e.length;t+=3){var n=e[t],r=e[t+1],i=e[t+2];if(typeof r!=`function`){if(pp(r||n)===null)continue;break}var a=vt(n);a!==null&&(e.splice(t,3),t-=3,Cs(a,{pending:!0,data:i,method:n.method,action:r},r,i))}}))}function Np(e){function t(t){return Ap(t,e)}gp!==null&&Ap(gp,e),_p!==null&&Ap(_p,e),vp!==null&&Ap(vp,e),yp.forEach(t),bp.forEach(t);for(var n=0;n<xp.length;n++){var r=xp[n];r.blockedOn===e&&(r.blockedOn=null)}for(;0<xp.length&&(n=xp[0],n.blockedOn===null);)Ep(n),n.blockedOn===null&&xp.shift();if(n=(e.ownerDocument||e).$$reactFormReplay,n!=null)for(r=0;r<n.length;r+=3){var i=n[r],a=n[r+1],o=i[lt]||null;if(typeof a==`function`)o||Mp(n);else if(o){var s=null;if(a&&a.hasAttribute(`formAction`)){if(i=a,o=a[lt]||null)s=o.formAction;else if(pp(i)!==null)continue}else s=o.action;typeof s==`function`?n[r+1]=s:(n.splice(r,3),r-=3),Mp(n)}}}function Pp(){function e(e){e.canIntercept&&e.info===`react-transition`&&e.intercept({handler:function(){return new Promise(function(e){return i=e})},focusReset:`manual`,scroll:`manual`})}function t(){i!==null&&(i(),i=null),r||setTimeout(n,20)}function n(){if(!r&&!navigation.transition){var e=navigation.currentEntry;e&&e.url!=null&&navigation.navigate(e.url,{state:e.getState(),info:`react-transition`,history:`replace`})}}if(typeof navigation==`object`){var r=!1,i=null;return navigation.addEventListener(`navigate`,e),navigation.addEventListener(`navigatesuccess`,t),navigation.addEventListener(`navigateerror`,t),setTimeout(n,100),function(){r=!0,navigation.removeEventListener(`navigate`,e),navigation.removeEventListener(`navigatesuccess`,t),navigation.removeEventListener(`navigateerror`,t),i!==null&&(i(),i=null)}}}function Fp(e){this._internalRoot=e}Ip.prototype.render=Fp.prototype.render=function(e){var t=this._internalRoot;if(t===null)throw Error(i(409));var n=t.current;np(n,pu(),e,t,null,null)},Ip.prototype.unmount=Fp.prototype.unmount=function(){var e=this._internalRoot;if(e!==null){this._internalRoot=null;var t=e.containerInfo;np(e.current,2,null,e,null,null),bu(),t[ut]=null}};function Ip(e){this._internalRoot=e}Ip.prototype.unstable_scheduleHydration=function(e){if(e){var t=ot();e={blockedOn:null,target:e,priority:t};for(var n=0;n<xp.length&&t!==0&&t<xp[n].priority;n++);xp.splice(n,0,e),n===0&&Ep(e)}};var Lp=n.version;if(Lp!==`19.2.7`)throw Error(i(527,Lp,`19.2.7`));M.findDOMNode=function(e){var t=e._reactInternals;if(t===void 0)throw typeof e.render==`function`?Error(i(188)):(e=Object.keys(e).join(`,`),Error(i(268,e)));return e=d(t),e=e===null?null:p(e),e=e===null?null:e.stateNode,e};var Rp={bundleType:0,version:`19.2.7`,rendererPackageName:`react-dom`,currentDispatcherRef:j,reconcilerVersion:`19.2.7`};if(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__<`u`){var zp=__REACT_DEVTOOLS_GLOBAL_HOOK__;if(!zp.isDisabled&&zp.supportsFiber)try{ze=zp.inject(Rp),Be=zp}catch{}}e.createRoot=function(e,t){if(!a(e))throw Error(i(299));var n=!1,r=``,o=Ks,s=qs,c=Js;return t!=null&&(!0===t.unstable_strictMode&&(n=!0),t.identifierPrefix!==void 0&&(r=t.identifierPrefix),t.onUncaughtError!==void 0&&(o=t.onUncaughtError),t.onCaughtError!==void 0&&(s=t.onCaughtError),t.onRecoverableError!==void 0&&(c=t.onRecoverableError)),t=ep(e,1,!1,null,null,n,r,null,o,s,c,Pp),e[ut]=t.current,Sd(e),new Fp(t)}})),g=o(((e,t)=>{function n(){if(!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__>`u`||typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE!=`function`))try{__REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(n)}catch(e){console.error(e)}}n(),t.exports=h()})),_=u(),v=g(),y=m(),b=/[\p{Extended_Pictographic}\u{1F3FB}-\u{1F3FF}]/gu,x=/[\u{FE0F}\u{20E3}]/gu,S=e=>e.replace(b,``).replace(x,``).replace(/\*\*/g,``).replace(/\s{2,}/g,` `).replace(/\s+([:.,])/g,`$1`).trim(),C=e=>e.replace(/^\uFEFF/,``).replace(/\r\n/g,`
`).replace(/^#[ \t][^\n]*\n+/,``).replace(/\n+---[ \t]*\n+-[ \t]*by[^\n]*\s*$/i,``).replace(/^(#{2,6})[ \t]+(.+)$/gm,(e,t,n)=>`${t} ${S(n)}`).replace(/\n{2,}-{3,}[ \t]*(?=\n{2,})/g,``).trim(),w=e=>t=>({...t,content:C(t.markdown),route:`${e}/${t.slug}`}),T=[{slug:`hello-world`,title:`Hello World`,subject:`Java`,stage:`Fundamentals`,chapter:1,accent:`#34d399`,summary:`This is the very first program that every Java developer writes. The "Hello World" program is a tradition in programming that dates back to the 1970s. The purpose of this program is to verify that your development environment is set up correctly and to introduce you to the basic structure of a Java program.`,parts:{notes:`## Introduction\r
\r
This is the very first program that every Java developer writes. The "Hello World" program is a tradition in programming that dates back to the 1970s. The purpose of this program is to verify that your development environment is set up correctly and to introduce you to the basic structure of a Java program.\r
\r
In this example, the program simply prints a greeting message to the console.\r
\r
---\r
\r
## Prerequisites\r
\r
Before running this program, make sure you have the following installed on your system:\r
\r
- **JDK (Java Development Kit)**: Version 8 or above. You can download it from the official Oracle website or use OpenJDK.\r
- **A Text Editor or IDE**: Notepad, VS Code, IntelliJ IDEA, or Eclipse.\r
- **Command Line Access**: Terminal (macOS/Linux) or Command Prompt/PowerShell (Windows).\r
\r
To verify your installation, open a terminal and run:\r
\r
\`\`\`bash\r
java -version\r
javac -version\r
\`\`\`\r
\r
Both commands should display a version number without any errors.\r
\r
---\r
\r
## What is Java\r
\r
Java is a high-level, object-oriented, platform-independent programming language developed by Sun Microsystems (now owned by Oracle) in 1995. Java follows the principle of "Write Once, Run Anywhere" (WORA), meaning code compiled on one platform can run on any other platform that has a Java Virtual Machine (JVM).\r
\r
Key characteristics of Java:\r
\r
- **Object-Oriented**: Everything in Java revolves around classes and objects.\r
- **Platform-Independent**: Java code is compiled into bytecode, which runs on the JVM regardless of the operating system.\r
- **Strongly Typed**: Every variable must have a declared data type.\r
- **Compiled and Interpreted**: Java source code is first compiled into bytecode (.class file), and then interpreted by the JVM at runtime.\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java Hello.java @run-hello-world-hello\r
class Hello {\r
\r
    public static void main(String args[]) {\r
        System.out.println("Hi this is Aditya");\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-hello-world-hello\r
Hi this is Aditya\r
\`\`\`\r
\r
### Structure Overview\r
\r
Every Java program must have at least one class, and the execution starts from the \`main\` method. Think of a class as a container that holds your code, and the \`main\` method as the entry door through which the program begins execution.\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### Line 1: \`class Hello {\`\r
\r
- **\`class\`** is a keyword in Java used to declare a class.\r
- **\`Hello\`** is the name of the class. In Java, class names should start with an uppercase letter by convention.\r
- **\`{\`** marks the beginning of the class body.\r
\r
### Line 2: \`public static void main(String args[]) {\`\r
\r
This is the most important line for any beginner. Let us break down every word:\r
\r
| Keyword    | Meaning                                                                                          |\r
|------------|--------------------------------------------------------------------------------------------------|\r
| \`public\`   | An access modifier. It means this method can be accessed from anywhere, including the JVM.       |\r
| \`static\`   | This means the method belongs to the class itself, not to any specific object of the class.      |\r
| \`void\`     | The return type. \`void\` means this method does not return any value.                             |\r
| \`main\`     | The name of the method. The JVM looks specifically for a method named \`main\` to start execution. |\r
| \`String args[]\` | An array of Strings that can accept command-line arguments when the program is run.         |\r
\r
If even one of these keywords is missing or misspelled, the program will not run.\r
\r
![What each word in the main signature buys, and what fails without it](diagrams/01-hello-world-notes-main-signature.jpg)\r
\r
### Line 3: \`System.out.println("Hi this is Aditya");\`\r
\r
- **\`System\`** is a built-in class in the \`java.lang\` package.\r
- **\`out\`** is a static member of the \`System\` class. It is an object of type \`PrintStream\`.\r
- **\`println\`** is a method of \`PrintStream\` that prints the given text followed by a new line.\r
- **\`"Hi this is Aditya"\`** is a String literal, which is the text that will be displayed on the console.\r
- The semicolon **\`;\`** at the end marks the end of the statement. Every statement in Java must end with a semicolon.\r
\r
![System.out.println is a class, a field and a method, not one name](diagrams/01-hello-world-notes-println-chain.jpg)\r
\r
### Line 4 and 5: Closing braces \`}\`\r
\r
- The first \`}\` closes the \`main\` method.\r
- The second \`}\` closes the \`Hello\` class.\r
\r
---\r
\r
## How to Compile and Run\r
\r
### Step 1: Save the file\r
\r
Save the file as \`Hello.java\`. The file name must match the class inside it whenever that class is \`public\` — and matching it always is the habit worth having.\r
\r
### Step 2: Compile\r
\r
From the parent directory, compile using the package path:\r
\r
\`\`\`bash\r
javac Hello.java\r
\`\`\`\r
\r
This command invokes the Java Compiler (\`javac\`), which converts the \`.java\` source file into a \`.class\` bytecode file.\r
\r
### Step 3: Run\r
\r
Run using the fully qualified name (\`package.ClassName\`):\r
\r
\`\`\`bash\r
java Hello\r
\`\`\`\r
\r
This command invokes the Java Virtual Machine (JVM), which reads the \`Hello.class\` file and executes the \`main\` method.\r
\r
### Expected Output\r
\r
\`\`\`\r
Hi this is Aditya\r
\`\`\`\r
\r
---\r
\r
## What Happens Behind the Scenes\r
\r
1. You write the source code in a \`.java\` file (human-readable).\r
2. The Java Compiler (\`javac\`) converts it into bytecode stored in a \`.class\` file.\r
3. The JVM loads the \`.class\` file and interprets the bytecode.\r
4. The JVM finds the \`main\` method and begins executing the instructions inside it.\r
5. \`System.out.println()\` sends the text to the standard output stream, which is your console/terminal.\r
\r
![What Happens Behind the Scenes](diagrams/01-hello-world-notes-1196663d.jpg)\r
\r
---\r
\r
## Common Errors Beginners Face\r
\r
![Which beginner mistakes javac catches and which only appear at run time](diagrams/01-hello-world-notes-error-timing.jpg)\r
\r
### Error 1: Class name does not match file name (when class is public)\r
\r
If you declare the class as \`public class Hello\`, the file name MUST be \`Hello.java\`. Otherwise, you will get a compilation error.\r
\r
### Error 2: Missing semicolon\r
\r
\`\`\`\r
error: ';' expected\r
\`\`\`\r
\r
Every statement in Java must end with a semicolon.\r
\r
### Error 3: \`main\` method signature is wrong\r
\r
If you write \`public static void main(String args)\` (without the square brackets), the JVM will not recognize it as the entry point.\r
\r
### Error 4: Case sensitivity\r
\r
Java is case-sensitive. \`System\` is not the same as \`system\`. \`String\` is not the same as \`string\`.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`Hello.java\`\r
\r
The whole program is one class holding one method. \`main\` is where the JVM\r
starts â€” its exact signature, \`public static void main(String[] args)\`, is what\r
makes it findable, and a program with no method matching it will compile but\r
refuse to launch.\r
\r
Everything else in the file exists because Java requires code to live inside a\r
class.\r
\r
\`\`\`java Hello.java @run-hello-world-hello\r
class Hello {\r
\r
    public static void main(String args[]) {\r
        System.out.println("Hi this is Aditya");\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-hello-world-hello\r
Hi this is Aditya\r
\`\`\`\r
`,revision:'The chapter, compressed. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## The two commands\n\n![javac takes the file name, java takes the class name](diagrams/01-hello-world-revision-two-commands.jpg)\n\n- `javac Hello.java` compiles — **file** name, with the extension. `java Hello`\n  runs — **class** name, no extension.\n- The file name must match the `public class` inside it, capitals included.\n- `javac` writes `Hello.class` next to the source. That file holds **bytecode**:\n  not machine code, and not something the processor runs directly.\n- The JVM reads the bytecode and executes it. That indirection is the whole of\n  "write once, run anywhere" — the bytecode is portable, the JVM is not.\n\n## The `main` method\n\n![The parts of the main signature the JVM matches on, and the parts it ignores](diagrams/01-hello-world-revision-main-fixed-and-free.jpg)\n\n- The signature is fixed: `public static void main(String[] args)`.\n- **`public`** so the JVM can call it from outside the class. **`static`** so it\n  can be called before any object exists — something has to run first, and at\n  that point nothing has been constructed. **`void`** because the JVM makes no\n  use of a return value; an exit status comes from `System.exit`.\n- `String[] args` holds the command-line arguments. With none, it is an empty\n  array rather than `null`.\n- The order of the modifiers does not matter — `static public void main` compiles\n  and runs. The name, the return type and the parameter type do.\n- Several classes may each have a `main`; the one that runs is the class you\n  named on the command line. Overloading `main` with other parameters is legal,\n  and the JVM ignores every version except `String[]`.\n\n## Getting it wrong\n\n- No `main` with exactly that signature and the class loads, then fails with\n  `NoSuchMethodError: main` — at run time, not compile time.\n- `println` adds a newline, `print` does not. Two `print` calls land on one line.\n- `Could not find or load main class` is nearly always `java` given the file name\n  instead of the class name, or the wrong directory.\n',interview:"## Interview questions\n\n### Q1: Why is the `main` method declared as `public static void`?\n\n**Answer:** The `main` method is `public` so that the JVM can access it from outside the class. It is `static` because the JVM calls it without creating an object of the class. It is `void` because it does not return any value to the JVM.\n\n### Q2: Can we run a Java program without the `main` method?\n\n**Answer:** Starting from Java 7, the answer is no. In Java 6 and earlier, you could use a static block to execute code without a `main` method, but this is no longer supported in modern versions of Java.\n\n### Q3: What is the difference between `System.out.println()` and `System.out.print()`?\n\n**Answer:** `println()` prints the text and then moves the cursor to the next line. `print()` prints the text but keeps the cursor on the same line.\n\n![Where print and println leave the console cursor](diagrams/01-hello-world-interview-print-vs-println.jpg)\n\n### Q4: What is bytecode?\n\n**Answer:** Bytecode is the intermediate code generated by the Java compiler. It is not machine code specific to any operating system. Instead, it is designed to be executed by the JVM, which makes Java platform-independent.\n\n### Q5: Can we have multiple `main` methods in a Java program?\n\n**Answer:** Yes, you can have multiple `main` methods in different classes within the same program. However, the JVM will only execute the `main` method of the class you specify when running the program.\n\n![Three classes each with a main, only the named one is entered](diagrams/01-hello-world-interview-which-main-runs.jpg)\n\n### Q6: What happens if we write `static public void main` instead of `public static void main`?\n\n**Answer:** The program will still compile and run correctly. The order of `public` and `static` does not matter in Java. However, the conventional and recommended order is `public static void main`.\n"}},{slug:`jvm-and-memory`,title:`JVM, JDK, JRE and memory`,subject:`Java`,stage:`Fundamentals`,chapter:2,accent:`#f97316`,summary:`In Chapter 1 you compiled and ran a program with javac and java. But what are those tools, and where do your variables and objects actually live while the program runs? This chapter explains the three things people often confuse — JDK, JRE, and JVM — and the memory model (Stack, Heap, and the String Pool) that the rest of this course relies on.`,parts:{notes:`## Introduction\r
\r
In Chapter 1 you compiled and ran a program with \`javac\` and \`java\`. But what *are* those tools, and where do your variables and objects actually live while the program runs? This chapter explains the three things people often confuse — **JDK, JRE, and JVM** — and the **memory model** (Stack, Heap, and the String Pool) that the rest of this course relies on.\r
\r
This is a concepts chapter, with one small runnable demo (\`MemoryDemo.java\`) that lets you *see* the memory behaviour.\r
\r
---\r
\r
## How Java Runs: Compile Once, Run Anywhere\r
\r
Java's famous promise is **"Write Once, Run Anywhere" (WORA)**. It works in two steps:\r
\r
![How Java Runs: Compile Once, Run Anywhere](diagrams/02-jvm-and-memory-notes-750d6ffb.jpg)\r
\r
1. The **compiler** (\`javac\`) turns your human-readable source code into **bytecode** (\`.class\` files). Bytecode is not machine code; it is an intermediate language the JVM understands.\r
2. The **JVM** reads that bytecode and executes it on the current machine.\r
\r
Because every platform (Windows, macOS, Linux) has its own JVM, the *same* \`.class\` file runs everywhere without recompiling. The bytecode is portable; the JVM is platform-specific.\r
\r
---\r
\r
## JDK, JRE, and JVM\r
\r
These three are nested, like boxes inside boxes:\r
\r
![JDK, JRE, and JVM](diagrams/02-jvm-and-memory-notes-c70dc042.jpg)\r
\r
| Term | Stands for | Contains | You need it to… |\r
|------|------------|----------|-----------------|\r
| **JDK** | Java Development Kit | JRE **+** developer tools (\`javac\`, \`jar\`, \`javadoc\`, debugger) | **write and compile** Java programs |\r
| **JRE** | Java Runtime Environment | JVM **+** core class libraries | **run** Java programs (no compiling) |\r
| **JVM** | Java Virtual Machine | The engine that executes bytecode | **execute** the \`.class\` bytecode |\r
\r
**One-line summary:** JDK is for developers (it includes the JRE); the JRE is for running apps (it includes the JVM); the JVM is the engine that actually runs the bytecode.\r
\r
> As a learner you install the **JDK**, because you need to both compile (\`javac\`) and run (\`java\`).\r
\r
---\r
\r
## What the JVM Does\r
\r
The JVM is more than a simple interpreter. Its main jobs are:\r
\r
- **Class Loading** – finds and loads \`.class\` files into memory.\r
- **Bytecode Verification** – checks the bytecode is safe and valid.\r
- **Execution** – interprets bytecode and, for "hot" code, uses the **JIT (Just-In-Time) compiler** to turn frequently used bytecode into native machine code for speed.\r
- **Memory Management** – allocates objects on the heap and reclaims unused ones via the **Garbage Collector (GC)**, so you rarely free memory manually.\r
\r
![Load, verify, interpret, and JIT-compile the code that runs often](diagrams/02-jvm-and-memory-notes-jvm-jobs.jpg)\r
\r
---\r
\r
## Java Memory Areas\r
\r
When the JVM runs your program, it divides memory into areas. The two you must understand first are the **Stack** and the **Heap**.\r
\r
![Java Memory Areas](diagrams/02-jvm-and-memory-notes-67132182.jpg)\r
\r
---\r
\r
## Stack Memory\r
\r
- Stores **local variables**, **method parameters**, and **references** to objects.\r
- Organised as **frames** — one frame per method call, removed when the method returns (LIFO: Last In, First Out).\r
- **Primitives are stored by value** here. Copying a primitive copies the value, so the copies are independent.\r
- Fast, automatically managed, and limited in size (a runaway recursion causes a \`StackOverflowError\`).\r
\r
\`\`\`java\r
int a = 10;\r
int b = a;\r
b = 20;\r
\`\`\`\r
\r
---\r
\r
## Heap Memory\r
\r
- Stores **all objects** created with \`new\` (and arrays, and Strings).\r
- **Shared** across the whole application; multiple references can point to the same object.\r
- Reclaimed automatically by the **Garbage Collector** when no references remain.\r
- Copying a **reference** does not copy the object — both references point to the *same* object.\r
\r
![Assigning a primitive copies the value, assigning an object copies the address](diagrams/02-jvm-and-memory-notes-copy-value-vs-reference.jpg)\r
\r
\`\`\`java\r
int[] x = {1, 2, 3};\r
int[] y = x;\r
y[0] = 99;\r
\`\`\`\r
\r
---\r
\r
## The String Pool\r
\r
The **String Pool** (also called the String Constant Pool) is a special area the JVM uses to **reuse** String literals and save memory.\r
\r
- When you write a literal like \`"Java"\`, the JVM checks the pool. If \`"Java"\` already exists there, it **reuses** the same object instead of creating a new one.\r
- Therefore two identical literals refer to the **same** object, and \`==\` is \`true\`.\r
- Using \`new String("Java")\` **forces** a brand-new object on the heap (outside the pool), so \`==\` is \`false\` even though the text is equal.\r
- \`intern()\` returns the pooled instance for any String.\r
\r
![Pooled literals share one object, new String allocates a second one](diagrams/02-jvm-and-memory-notes-string-pool.jpg)\r
\r
\`\`\`java\r
String s1 = "Java";\r
String s2 = "Java";\r
String s3 = new String("Java");\r
\r
s1 == s2\r
s1 == s3\r
s1.equals(s3)\r
s1 == s3.intern()\r
\`\`\`\r
\r
> This is exactly why you compare String **text** with \`.equals()\`, not \`==\`.\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java\r
int a = 10;\r
int b = a; b = 20;\r
\r
int[] x = {1, 2, 3};\r
int[] y = x; y[0] = 99;\r
\r
String s1 = "Java";\r
String s2 = "Java";\r
String s3 = new String("Java");\r
\`\`\`\r
\r
Running the demo prints \`a = 10, b = 20\` (the copy is independent), \`x[0] = 99\` (both references share one array), and the String comparisons that prove pooling.\r
\r
---\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac MemoryDemo.java\r
java MemoryDemo\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
a = 10, b = 20\r
x[0] = 99\r
s1 == s2      : true\r
s1 == s3      : false\r
s1.equals(s3) : true\r
s1 == s3.intern(): true\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Thinking the JVM compiles \`.java\` files\r
\r
The **compiler** \`javac\` (part of the JDK) compiles \`.java\` to \`.class\`. The **JVM** only *runs* the resulting bytecode.\r
\r
### Mistake 2: Installing only the JRE, then trying to compile\r
\r
The JRE can run programs but has no \`javac\`. To develop, install the **JDK**.\r
\r
### Mistake 3: Comparing Strings with \`==\`\r
\r
\`==\` compares references. Two Strings with the same text can be different objects (especially with \`new String(...)\`). Use \`.equals()\` for text.\r
\r
### Mistake 4: Assuming assigning an object copies it\r
\r
\`\`\`java\r
int[] x = {1, 2, 3};\r
int[] y = x;\r
y[0] = 99;\r
\`\`\`\r
\r
\`\`\`expected\r
y is not a second array — it is a second name for the same one.\r
x[0] is now 99 as well, because there is only one array.\r
Use x.clone() or Arrays.copyOf(x, x.length) when you wanted a copy.\r
\`\`\`\r
\r
Both variables now point to the same object; changes are visible through both.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`MemoryDemo.java\`\r
\r
Four printed lines, each one showing a different consequence of where a value\r
actually lives.\r
\r
- **\`b = a\` copies the value.** \`a\` and \`b\` are primitives on the stack, so\r
  changing \`b\` leaves \`a\` at \`10\`.\r
- **\`y = x\` copies the reference.** Both variables now point at the *same* array\r
  on the heap, so writing through \`y\` changes what \`x\` sees.\r
- **\`s1\` and \`s2\` are the same object.** Identical string literals are pooled, so\r
  \`==\` is true. \`new String("Java")\` deliberately skips the pool, so \`s1 == s3\`\r
  is false while \`s1.equals(s3)\` is true.\r
- **\`intern()\` puts it back.** It returns the pooled instance, so \`s1 ==\r
  s3.intern()\` is true again.\r
\r
\`\`\`java MemoryDemo.java @run-jvm-and-memory-memory-demo\r
public class MemoryDemo {\r
\r
    public static void main(String[] args) {\r
\r
        int a = 10;\r
        int b = a;\r
        b = 20;\r
        System.out.println("a = " + a + ", b = " + b);\r
\r
        int[] x = { 1, 2, 3 };\r
        int[] y = x;\r
        y[0] = 99;\r
        System.out.println("x[0] = " + x[0]);\r
\r
        String s1 = "Java";\r
        String s2 = "Java";\r
        String s3 = new String("Java");\r
\r
        System.out.println("s1 == s2      : " + (s1 == s2));\r
        System.out.println("s1 == s3      : " + (s1 == s3));\r
        System.out.println("s1.equals(s3) : " + s1.equals(s3));\r
        System.out.println("s1 == s3.intern(): " + (s1 == s3.intern()));\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-jvm-and-memory-memory-demo\r
a = 10, b = 20\r
x[0] = 99\r
s1 == s2      : true\r
s1 == s3      : false\r
s1.equals(s3) : true\r
s1 == s3.intern(): true\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## The three acronyms\r
\r
- **JDK contains the JRE, which contains the JVM.** JDK to write, JRE to run,\r
  JVM to execute. Install the JDK — the JRE alone has no \`javac\`.\r
- \`javac\` produces **bytecode**, not machine code. Bytecode is portable; the JVM\r
  is the part built per platform. That is "write once, run anywhere".\r
- The JVM does four jobs beyond executing: **class loading**, **bytecode\r
  verification**, **JIT compilation** and **garbage collection**.\r
- The **JIT** watches which methods run often and compiles those to machine code\r
  while the program runs. It is why a long-running Java program gets faster after\r
  the first few seconds.\r
\r
## Where things live\r
\r
![Stack holds frames per thread, heap holds every object](diagrams/02-jvm-and-memory-revision-stack-vs-heap.jpg)\r
\r
- **Stack** — locals, parameters and references. One frame per method call,\r
  discarded on return, one stack per thread. Fast and small.\r
- **Heap** — every object made with \`new\`. Shared by all threads, cleaned by the\r
  garbage collector, and the only place an object can be.\r
- A **primitive** variable holds its value. A **reference** variable holds the\r
  address of something on the heap.\r
- Assigning an object copies the **reference**, not the object. Two names then\r
  point at one object, and a change through either is visible through both.\r
- The **garbage collector** frees objects nothing can still reach. You never free\r
  anything by hand, and you cannot force a collection — \`System.gc()\` is a\r
  request the JVM may ignore.\r
\r
## The String Pool\r
\r
![What == and .equals each ask about two Strings](diagrams/02-jvm-and-memory-revision-identity-vs-contents.jpg)\r
\r
- String literals are stored once and shared, which is why \`"a" == "a"\` is true.\r
- \`new String("a")\` deliberately allocates outside the pool, so \`==\` is false\r
  even though the characters match.\r
- \`==\` on references asks "the same object"; \`.equals()\` asks "the same\r
  characters". For Strings you almost always want \`.equals()\`.\r
`,interview:`## Interview questions

### Q1: What is the difference between JDK, JRE, and JVM?

**Answer:** The JVM is the engine that executes Java bytecode and manages memory. The JRE is the JVM plus the core libraries needed to *run* Java applications. The JDK is the JRE plus development tools such as \`javac\`, so it is used to *write and compile* programs. They are nested: JDK ⊃ JRE ⊃ JVM.

![JDK contains the JRE, which contains the JVM](diagrams/02-jvm-and-memory-interview-jdk-jre-jvm.jpg)

### Q2: What is bytecode, and why does it make Java portable?

**Answer:** Bytecode is the platform-independent intermediate code produced by \`javac\` (the \`.class\` files). Because every platform has its own JVM that understands the same bytecode, one compiled file runs on any OS — "Write Once, Run Anywhere."

### Q3: What is the difference between stack and heap memory?

**Answer:** The stack stores local variables, parameters, and references in per-method frames that are removed when the method returns; primitives are stored by value. The heap stores all objects created with \`new\` and is shared across the program; objects are reclaimed by the garbage collector when unreferenced.

### Q4: What is the String Pool?

**Answer:** A special memory area where the JVM stores String literals and reuses them. Identical literals point to the same pooled object, which saves memory and is why \`"Java" == "Java"\` is \`true\`.

### Q5: Why is \`new String("Java") == "Java"\` false?

**Answer:** \`new String(...)\` always creates a separate object on the heap, distinct from the pooled literal \`"Java"\`. The references differ, so \`==\` is \`false\`, even though \`.equals()\` is \`true\` because the characters match.

### Q6: What is the Garbage Collector?

**Answer:** A part of the JVM that automatically frees heap memory occupied by objects that are no longer reachable by any reference, so Java programmers usually do not free memory manually.

### Q7: What does the JIT compiler do?

**Answer:** The Just-In-Time compiler is part of the JVM. It compiles frequently executed bytecode ("hot" code) into native machine code at runtime to make the program run faster than pure interpretation.

![Interpreted at first, compiled to native code once a method turns hot](diagrams/02-jvm-and-memory-interview-jit.jpg)
`}},{slug:`type-casting`,title:`Type casting`,subject:`Java`,stage:`Fundamentals`,chapter:3,accent:`#a78bfa`,summary:`Type casting is one of the fundamental concepts in Java that every developer must understand. When you work with different data types (such as int, float, double, long), there are situations where you need to convert a value from one type to another. This process is called type casting.`,parts:{notes:`## Introduction\r
\r
Type casting is one of the fundamental concepts in Java that every developer must understand. When you work with different data types (such as \`int\`, \`float\`, \`double\`, \`long\`), there are situations where you need to convert a value from one type to another. This process is called type casting.\r
\r
This program demonstrates explicit type casting, where a \`float\` value is manually converted to an \`int\` before performing a mathematical operation.\r
\r
---\r
\r
## What is Type Casting\r
\r
Type casting is the process of converting a value from one data type to another. In Java, every variable has a specific data type, and the compiler enforces strict type rules. If you try to assign a value of one type to a variable of a different type, the compiler will either do it automatically (if it is safe) or ask you to do it manually (if there is a risk of data loss).\r
\r
Think of it like pouring water from a large bucket into a small glass. If the glass is big enough, the water fits perfectly (automatic/widening casting). If the glass is too small, some water will spill (manual/narrowing casting with data loss).\r
\r
---\r
\r
## Types of Type Casting\r
\r
### 1. Widening Casting (Implicit / Automatic)\r
\r
This happens automatically when you convert a smaller data type to a larger data type. There is no risk of data loss.\r
\r
![1. Widening Casting (Implicit / Automatic)](diagrams/03-type-casting-notes-3f9dd935.jpg)\r
\r
**Example:**\r
\r
\`\`\`java\r
int a = 100;\r
double b = a;\r
\`\`\`\r
\r
The compiler does this on its own because a \`double\` can hold any \`int\` value without losing any information.\r
\r
### 2. Narrowing Casting (Explicit / Manual)\r
\r
This must be done manually by the programmer when you convert a larger data type to a smaller data type. There IS a risk of data loss.\r
\r
![2. Narrowing Casting (Explicit / Manual)](diagrams/03-type-casting-notes-31e0a14c.jpg)\r
\r
**Example:**\r
\r
\`\`\`java\r
double a = 9.78;\r
int b = (int) a;\r
\`\`\`\r
\r
You must explicitly write \`(int)\` to tell the compiler: "I know this might lose data, but I want to do it anyway."\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java TypeCasting.java @run-type-casting-type-casting\r
public class TypeCasting {\r
\r
    public static void main(String[] args) {\r
        int a = 5;\r
        float b = 6.1f;\r
\r
        int c = a * (int) b;\r
\r
        System.out.println(c);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-type-casting-type-casting\r
30\r
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### \`int a = 5;\`\r
\r
- Declares an integer variable \`a\` and assigns it the value \`5\`.\r
- \`int\` is a 32-bit data type that can store whole numbers from -2,147,483,648 to 2,147,483,647.\r
\r
### \`float b = 6.1f;\`\r
\r
- Declares a floating-point variable \`b\` and assigns it the value \`6.1\`.\r
- The \`f\` suffix is mandatory for \`float\` literals in Java. Without it, the compiler treats \`6.1\` as a \`double\` (which is 64-bit), and assigning a \`double\` to a \`float\` (which is 32-bit) would cause a compilation error.\r
- \`float\` is a 32-bit data type that stores decimal numbers with approximately 6-7 significant digits of precision.\r
\r
### \`int c = a * (int) b;\`\r
\r
This is the most important line in this program. Let us break it down step by step:\r
\r
1. **\`(int) b\`** -- This is explicit type casting. The \`float\` value \`6.1\` is being converted to an \`int\`. When this happens, the decimal part (\`.1\`) is completely discarded (truncated, NOT rounded). So \`(int) 6.1\` becomes \`6\`.\r
\r
2. **\`a * (int) b\`** -- This becomes \`5 * 6\`, which equals \`30\`.\r
\r
3. **\`int c = 30\`** -- The result \`30\` is stored in the integer variable \`c\`.\r
\r
### \`System.out.println(c);\`\r
\r
- Prints the value of \`c\` to the console, which is \`30\`.\r
\r
---\r
\r
## What Happens When You Convert Between Types\r
\r
### int to float (Widening - Safe)\r
\r
\`\`\`java\r
int x = 100;\r
float y = x;\r
\`\`\`\r
\r
- No data loss occurs for most values.\r
- However, for very large \`int\` values (above 16,777,216), there can be a loss of precision because \`float\` has only 23 bits for the significand.\r
\r
### float to int (Narrowing - Data Loss)\r
\r
\`\`\`java\r
float x = 9.99f;\r
int y = (int) x;\r
\`\`\`\r
\r
- The decimal part is completely removed (truncated).\r
- Important: This is truncation, NOT rounding. \`9.99\` becomes \`9\`, not \`10\`.\r
\r
![A cast truncates towards zero while Math.round rounds](diagrams/03-type-casting-notes-truncate-not-round.jpg)\r
\r
### int to double (Widening - Safe)\r
\r
\`\`\`java\r
int x = 100;\r
double y = x;\r
\`\`\`\r
\r
- \`double\` is 64-bit and can hold any \`int\` value without any loss.\r
\r
### double to int (Narrowing - Data Loss)\r
\r
\`\`\`java\r
double x = 99.99;\r
int y = (int) x;\r
\`\`\`\r
\r
- Same as float to int: the decimal part is truncated.\r
\r
### int to long (Widening - Safe)\r
\r
\`\`\`java\r
int x = 100;\r
long y = x;\r
\`\`\`\r
\r
- \`long\` is 64-bit and can hold any \`int\` value.\r
\r
### long to int (Narrowing - Possible Data Loss)\r
\r
\`\`\`java\r
long x = 3000000000L;\r
int y = (int) x;\r
\`\`\`\r
\r
- If the \`long\` value exceeds the \`int\` range, the result will overflow and produce an incorrect value.\r
\r
---\r
\r
## Data Loss in Type Casting\r
\r
Here is a detailed table showing what can go wrong with narrowing conversions:\r
\r
![Narrowing keeps only the low bits, so the top kept bit becomes the sign](diagrams/03-type-casting-notes-narrowing-bits.jpg)\r
\r
| Conversion      | What Happens                                      | Example                    | Result   |\r
|-----------------|--------------------------------------------------|----------------------------|----------|\r
| float to int    | Decimal part is truncated                        | \`(int) 6.9f\`              | \`6\`      |\r
| double to int   | Decimal part is truncated                        | \`(int) 99.99\`             | \`99\`     |\r
| double to float | Precision may be reduced                         | \`(float) 3.141592653589\`  | \`3.1415927\` |\r
| long to int     | Higher bits are discarded, may cause overflow    | \`(int) 3000000000L\`       | \`-1294967296\` |\r
| int to byte     | Only the lowest 8 bits are kept                  | \`(byte) 130\`              | \`-126\`   |\r
| int to short    | Only the lowest 16 bits are kept                 | \`(short) 40000\`           | \`-25536\` |\r
\r
---\r
\r
## Type Promotion in Expressions\r
\r
When different data types are used together in an expression, Java automatically promotes the smaller type to the larger type before performing the operation. This is called type promotion.\r
\r
**Rules of Type Promotion:**\r
\r
1. \`byte\`, \`short\`, and \`char\` are promoted to \`int\` in any expression.\r
2. If any operand is \`long\`, the entire expression is promoted to \`long\`.\r
3. If any operand is \`float\`, the entire expression is promoted to \`float\`.\r
4. If any operand is \`double\`, the entire expression is promoted to \`double\`.\r
\r
![An expression takes the type of its widest operand](diagrams/03-type-casting-notes-promotion.jpg)\r
\r
**Example:**\r
\r
\`\`\`java\r
byte a = 10;\r
byte b = 20;\r
// byte c = a + b;  // ERROR: a + b is promoted to int\r
int c = a + b;      // Correct\r
\`\`\`\r
\r
---\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac TypeCasting.java\r
java TypeCasting\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
30\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Forgetting the \`f\` suffix for float literals\r
\r
\`\`\`java\r
float b = 6.1;  // ERROR: 6.1 is treated as double\r
float b = 6.1f; // CORRECT\r
\`\`\`\r
\r
\`\`\`expected\r
error: incompatible types: possible lossy conversion from double to float\r
A decimal literal is a double unless it is suffixed. 6.1f compiles.\r
\`\`\`\r
\r
### Mistake 2: Assuming narrowing happens automatically\r
\r
\`\`\`java\r
float x = 6.1f;\r
int y = x;        // ERROR: cannot convert float to int automatically\r
int y = (int) x;  // CORRECT: explicit cast required\r
\`\`\`\r
\r
\`\`\`expected\r
error: incompatible types: possible lossy conversion from float to int\r
Narrowing never happens on its own — the compiler makes you write the cast.\r
\`\`\`\r
\r
### Mistake 3: Expecting rounding instead of truncation\r
\r
\`\`\`java\r
int y = (int) 6.9f;\r
\`\`\`\r
\r
\`\`\`expected\r
y is 6\r
A cast truncates towards zero. It does not round: (int) -6.9f is -6.\r
\`\`\`\r
\r
If you want rounding, use \`Math.round()\`:\r
\r
\`\`\`java\r
int y = Math.round(6.9f);\r
\`\`\`\r
\r
\`\`\`expected\r
y is 7\r
Math.round is what you want when you meant rounding.\r
\`\`\`\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`TypeCasting.java\`\r
\r
One explicit cast, and what it costs. \`(int) 6.1f\` is \`6\` â€” the cast to a\r
narrower type drops the fractional part rather than rounding it â€” so the\r
multiplication that follows is \`5 * 6\` and prints \`30\`.\r
\r
Without the cast the expression would not compile at all: Java will not quietly\r
turn a \`float\` into an \`int\`, because doing so loses data.\r
\r
\`\`\`java TypeCasting.java @run-type-casting-type-casting\r
public class TypeCasting {\r
\r
    public static void main(String[] args) {\r
        int a = 5;\r
        float b = 6.1f;\r
\r
        int c = a * (int) b;\r
\r
        System.out.println(c);\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-type-casting-type-casting\r
30\r
\`\`\`\r
`,revision:"The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## The two directions\r\n\r\n![Widening is implicit and safe, narrowing is explicit and can overflow](diagrams/03-type-casting-revision-widening-vs-narrowing.jpg)\r\n\r\n- **Widening** is automatic — smaller type into larger, no cast written:\r\n  `byte → short → int → long → float → double`.\r\n- **Narrowing** needs an explicit cast, because it can lose information and Java\r\n  makes you say you accept that: `int i = (int) 3.9;`.\r\n- `char` sits in the chain too: it widens to `int` freely, and anything narrowing\r\n  to `char` needs the cast.\r\n\r\n## What gets lost\r\n\r\n- Casting a floating-point value to an integer **truncates towards zero**. It\r\n  does not round: `(int) 3.9` is `3`, and `(int) -3.9` is `-3`.\r\n- Narrowing an integer type keeps the low bits and discards the rest, so a value\r\n  too large for the target comes out as something unrelated rather than as an\r\n  error. `(byte) 130` is `-126`.\r\n- `long` to `float` and `int` to `float` widen without a cast but can still lose\r\n  **precision** — `float` has fewer significant digits than `int` has values.\r\n\r\n## Promotion in expressions\r\n\r\n![The division happens in int before any widening to double](diagrams/03-type-casting-revision-result-type.jpg)\r\n\r\n- Arithmetic on `byte`, `short` or `char` promotes both operands to `int` before\r\n  it happens. `byte a = 10, b = 20; byte c = a + b;` does not compile — the sum\r\n  is an `int`.\r\n- If either operand is `double`, the whole expression is `double`; failing that\r\n  `float`, then `long`, then `int`. The result type follows the widest operand.\r\n- `5 / 2` is `2` because both operands are `int`. Make one of them a `double` and\r\n  it is `2.5`.\r\n\r\n## Getting it wrong\r\n\r\n- A decimal literal is a `double` unless suffixed. `float f = 3.14;` fails;\r\n  `float f = 3.14f;` compiles.\r\n- Expecting narrowing to happen on its own. It never does — the compiler asks.\r\n- Expecting a cast to round. Use `Math.round` when you want rounding.\r\n",interview:"## Interview questions\n\n### Q1: What is type casting in Java?\n\n**Answer:** Type casting is the process of converting a value from one data type to another. There are two types: widening (automatic, from smaller to larger type) and narrowing (manual, from larger to smaller type).\n\n### Q2: What is the difference between implicit and explicit type casting?\n\n**Answer:** Implicit casting (widening) is done automatically by the compiler when there is no risk of data loss, such as converting `int` to `double`. Explicit casting (narrowing) must be done manually by the programmer using the cast operator, such as converting `double` to `int`, because there is a risk of losing data.\n\n### Q3: What happens when you cast a `float` to an `int`?\n\n**Answer:** The decimal (fractional) part is completely discarded through truncation. For example, `(int) 6.9f` results in `6`, not `7`. The value is NOT rounded.\n\n### Q4: Why do we need to add `f` at the end of a float literal?\n\n**Answer:** By default, Java treats all decimal literals as `double`. Since `double` is 64-bit and `float` is 32-bit, assigning a `double` to a `float` would be a narrowing conversion. The `f` suffix explicitly tells the compiler that the literal is a `float`, not a `double`.\n\n### Q5: What is type promotion in Java?\n\n**Answer:** Type promotion is the automatic conversion of smaller data types to larger data types within an expression. For example, when you add a `byte` and a `short`, both are automatically promoted to `int` before the addition is performed. This prevents overflow during intermediate calculations.\n\n### Q6: Can you cast a `boolean` to an `int` in Java?\n\n**Answer:** No. In Java, `boolean` is not considered a numeric type. You cannot cast between `boolean` and any other data type. This is different from languages like C and C++, where `true` and `false` can be treated as `1` and `0`.\n\n![boolean sits outside the numeric conversion family](diagrams/03-type-casting-interview-boolean-not-numeric.jpg)\n\n### Q7: What is the output of `(int) -9.7`?\n\n**Answer:** The output is `-9`. The casting truncates toward zero, discarding the decimal part. It does not round to `-10`.\n\n### Q8: What happens if you cast a large `long` value to `int`?\n\n**Answer:** If the `long` value exceeds the range of `int` (-2,147,483,648 to 2,147,483,647), the higher-order bits are discarded, and the result wraps around due to overflow, producing an unexpected and incorrect value.\n\n### Q9: What is the difference between `float` and `double`?\n\n**Answer:**\n\n| Feature       | float          | double          |\n|---------------|----------------|-----------------|\n| Size          | 32 bits        | 64 bits         |\n| Precision     | ~6-7 digits    | ~15-16 digits   |\n| Default       | No (needs `f`) | Yes             |\n| Memory Usage  | Less           | More            |\n| Use Case      | Memory-critical applications | General-purpose decimal calculations |\n\n### Q10: Is type casting applicable to objects in Java?\n\n**Answer:** Yes. In Java, you can also cast objects. Upcasting (child to parent) is implicit and always safe. Downcasting (parent to child) is explicit and can throw a `ClassCastException` at runtime if the object is not actually an instance of the target class.\n\n![Upcasting is implicit and safe, downcasting is checked at run time](diagrams/03-type-casting-interview-upcast-downcast.jpg)\n"}},{slug:`data-types`,title:`Data types`,subject:`Java`,stage:`Fundamentals`,chapter:4,accent:`#f472b6`,summary:`Every variable in Java has a data type. The data type tells the compiler what kind of value the variable can hold, how much memory to reserve for it, and what operations are allowed on it. Choosing the right data type is the very first decision you make when storing any value.`,parts:{notes:`## Introduction\r
\r
Every variable in Java has a **data type**. The data type tells the compiler what kind of value the variable can hold, how much memory to reserve for it, and what operations are allowed on it. Choosing the right data type is the very first decision you make when storing any value.\r
\r
This chapter is split into five small programs:\r
\r
| File | Topic |\r
|------|-------|\r
| \`DatatypesPrimitive.java\`     | The eight primitive types |\r
| \`DatatypesNonPrimitive.java\` | Reference types (String, arrays) |\r
| \`DatatypesChar.java\`          | The \`char\` type |\r
| \`DatatypesStrings.java\`       | Strings and String methods |\r
| \`DatatypesLiterals.java\`      | Literals |\r
\r
---\r
\r
## What is a Data Type\r
\r
A data type is a classification that specifies:\r
\r
- **What kind of value** a variable can store (a whole number, a decimal, a character, text, true/false).\r
- **How much memory** the variable occupies.\r
- **What operations** can be performed on it.\r
\r
Java is a **statically typed** language. This means every variable must have a declared type, and that type is checked at compile time. You cannot put a decimal into an \`int\` variable without conversion.\r
\r
\`\`\`java\r
int age = 25;\r
int age = "twenty";  // ERROR: a String is not an int\r
\`\`\`\r
\r
---\r
\r
## Two Categories of Data Types\r
\r
![Two Categories of Data Types](diagrams/04-data-types-notes-7c6aa0d0.jpg)\r
\r
| Aspect          | Primitive                          | Non-Primitive (Reference)                |\r
|-----------------|------------------------------------|------------------------------------------|\r
| Stores          | The actual value                   | A reference (address) to an object       |\r
| Memory          | Usually on the stack               | Object lives on the heap                 |\r
| Default value   | \`0\`, \`0.0\`, \`false\`, \`'\\u0000'\`    | \`null\`                                   |\r
| Can be \`null\`?  | No                                 | Yes                                      |\r
| Has methods?    | No                                 | Yes                                      |\r
| Examples        | \`int\`, \`double\`, \`char\`, \`boolean\` | \`String\`, \`int[]\`, any class             |\r
\r
---\r
\r
## Primitive Data Types\r
\r
Java has exactly **eight** primitive types.\r
\r
| Type      | Size    | Range / Values                                  | Example          |\r
|-----------|---------|-------------------------------------------------|------------------|\r
| \`byte\`    | 1 byte  | -128 to 127                                     | \`byte b = 100;\`  |\r
| \`short\`   | 2 bytes | -32,768 to 32,767                               | \`short s = 2000;\`|\r
| \`int\`     | 4 bytes | about -2.1 billion to 2.1 billion               | \`int i = 100000;\`|\r
| \`long\`    | 8 bytes | very large whole numbers (needs \`L\` suffix)     | \`long l = 15000000000L;\` |\r
| \`float\`   | 4 bytes | single-precision decimal (needs \`f\` suffix)     | \`float f = 5.75f;\` |\r
| \`double\`  | 8 bytes | double-precision decimal (default for decimals) | \`double d = 19.99;\` |\r
| \`char\`    | 2 bytes | a single Unicode character                      | \`char c = 'A';\`  |\r
| \`boolean\` | 1 bit*  | \`true\` or \`false\`                               | \`boolean ok = true;\` |\r
\r
\\*The JVM does not precisely define the size of a \`boolean\`; conceptually it holds one bit of information.\r
\r
**Key points:**\r
\r
- \`int\` is the default type for whole numbers.\r
- \`double\` is the default type for decimal numbers.\r
- A \`long\` literal needs an \`L\` suffix, and a \`float\` literal needs an \`f\` suffix, otherwise the compiler treats the number as \`int\` or \`double\`.\r
\r
![The eight primitives drawn to their size in bytes](diagrams/04-data-types-notes-primitive-sizes.jpg)\r
\r
---\r
\r
## Non-Primitive (Reference) Data Types\r
\r
Reference types do not store the value directly. They store a **reference** (a memory address) that points to an actual object on the heap.\r
\r
\`\`\`java\r
String name = "Java";\r
int[] numbers = {10, 20};\r
String empty = null;\r
\`\`\`\r
\r
Because they are objects, reference types:\r
\r
- Have **methods** you can call (e.g. \`name.toUpperCase()\`).\r
- Can be \`null\` (pointing to no object).\r
- Are compared with \`.equals()\` for content, not \`==\`.\r
\r
---\r
\r
## The \`char\` Type in Detail\r
\r
A \`char\` holds a **single** character written in single quotes: \`'A'\`, \`'9'\`, \`'$'\`.\r
\r
Internally a \`char\` is just a number — its Unicode (and for English letters, ASCII) code point. This is why a \`char\` can take part in arithmetic.\r
\r
\`\`\`java\r
char letter = 'A';\r
int code = letter;\r
char next = (char)(letter + 1);\r
\`\`\`\r
\r
| Character | Code |\r
|-----------|------|\r
| \`'A'\`     | 65   |\r
| \`'B'\`     | 66   |\r
| \`'a'\`     | 97   |\r
| \`'0'\`     | 48   |\r
\r
A cast \`(char)\` is needed when converting back from \`int\`, because \`char + int\` produces an \`int\`.\r
\r
---\r
\r
## Strings in Detail\r
\r
![A String method returns a new object and leaves the original alone](diagrams/04-data-types-notes-string-immutable.jpg)\r
\r
A \`String\` is a sequence of characters written in **double quotes**: \`"Hello"\`. It is a class, not a primitive, and it is **immutable** — once created its contents can never change. Methods that look like they modify a String actually return a brand-new String.\r
\r
Common String methods:\r
\r
| Method                | Purpose                                  | Example result for \`"Hello World"\` |\r
|-----------------------|------------------------------------------|------------------------------------|\r
| \`length()\`            | number of characters                     | \`11\`                               |\r
| \`toUpperCase()\`       | upper-case copy                          | \`"HELLO WORLD"\`                    |\r
| \`toLowerCase()\`       | lower-case copy                          | \`"hello world"\`                    |\r
| \`charAt(i)\`           | character at index \`i\`                   | \`charAt(0)\` -> \`'H'\`               |\r
| \`indexOf("x")\`        | index of a substring (or -1)             | \`indexOf("World")\` -> \`6\`          |\r
| \`substring(a, b)\`     | part of the string                       | \`substring(0,5)\` -> \`"Hello"\`      |\r
| \`replace('l','L')\`    | replace characters                       | \`"HeLLo WorLd"\`                    |\r
| \`contains("x")\`       | does it contain the text?                | \`true\`                             |\r
| \`equals(other)\`       | compare contents                         | \`true\` / \`false\`                   |\r
\r
> **Always use \`equals()\` to compare text, not \`==\`.** The \`==\` operator compares references (addresses), not the actual characters.\r
\r
---\r
\r
## Literals in Java\r
\r
A **literal** is a fixed value written directly in source code.\r
\r
| Literal kind | Examples                              |\r
|--------------|---------------------------------------|\r
| Integer      | \`100\`, \`0b1100100\` (binary), \`0144\` (octal), \`0x64\` (hex) |\r
| Long         | \`10000000000L\`                        |\r
| Floating     | \`3.14\`, \`2.5f\`, \`1.5e3\`               |\r
| Character    | \`'A'\`, \`'\\n'\`, \`'A'\`             |\r
| String       | \`"Hello"\`                             |\r
| Boolean      | \`true\`, \`false\`                       |\r
\r
Helpful details:\r
\r
- **Underscores** improve readability and are ignored by the compiler: \`1_000_000\` is the same as \`1000000\`.\r
- **Prefixes** choose the number system: \`0b\` (binary), \`0\` (octal), \`0x\` (hexadecimal).\r
\r
![The same int written in decimal, binary, octal and hexadecimal](diagrams/04-data-types-notes-literal-bases.jpg)\r
- **Suffixes** choose the type: \`L\` (long), \`f\` (float).\r
- **Escape sequences** inside characters/strings: \`\\n\` (newline), \`\\t\` (tab), \`\\\\\` (backslash), \`\\"\` (double quote).\r
\r
---\r
\r
## How to Compile and Run\r
\r
Each file has its own \`main\` method, so run them one at a time.\r
\r
\`\`\`bash\r
javac DatatypesPrimitive.java\r
java DatatypesPrimitive\r
\r
javac DatatypesNonPrimitive.java\r
java DatatypesNonPrimitive\r
\r
javac DatatypesChar.java\r
java DatatypesChar\r
\r
javac DatatypesStrings.java\r
java DatatypesStrings\r
\r
javac DatatypesLiterals.java\r
java DatatypesLiterals\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Forgetting the \`L\` or \`f\` suffix\r
\r
\`\`\`java\r
long big = 10000000000;   // ERROR: integer number too large\r
float f = 5.75;           // ERROR: 5.75 is a double, not a float\r
\`\`\`\r
\r
\`\`\`expected\r
error: integer number too large\r
error: incompatible types: possible lossy conversion from double to float\r
\r
A whole literal is an int and a decimal literal is a double, whatever you\r
assign them to. Write 10000000000L and 5.75f.\r
\`\`\`\r
\r
Use \`10000000000L\` and \`5.75f\`.\r
\r
### Mistake 2: Using \`==\` to compare Strings\r
\r
\`\`\`java\r
String a = "Java";\r
String b = new String("Java");\r
System.out.println(a == b);\r
System.out.println(a.equals(b));\r
\`\`\`\r
\r
\`\`\`expected\r
false\r
true\r
\r
== compares references, and new String deliberately allocates outside the\r
pool. .equals compares the characters, which is what you meant.\r
\`\`\`\r
\r
### Mistake 3: Assigning a larger type to a smaller one\r
\r
\`\`\`java\r
int i = 100;\r
byte b = i;   // ERROR: possible loss of precision\r
byte b2 = (byte) i;  // OK with explicit cast\r
\`\`\`\r
\r
\`\`\`expected\r
error: incompatible types: possible lossy conversion from int to byte\r
100 fits in a byte, but the compiler checks the type and not the value.\r
\`\`\`\r
\r
### Mistake 4: Using double quotes for a \`char\`\r
\r
\`\`\`java\r
char c = "A";   // ERROR: "A" is a String\r
char c = 'A';   // correct\r
\`\`\`\r
\r
\`\`\`expected\r
error: incompatible types: String cannot be converted to char\r
Single quotes are a char, double quotes are a String. One character is still\r
a String if you write it with double quotes.\r
\`\`\`\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`DatatypesPrimitive.java\`\r
\r
All eight primitives in one place. A primitive stores its value **directly** â€”\r
on the stack, for a local variable â€” and is not an object. The size of the type\r
is what fixes the range of values it can hold.\r
\r
Two literals need a suffix, and the file would not compile without them: \`L\`\r
marks a \`long\` (otherwise the number is read as an \`int\`), and \`f\` marks a\r
\`float\` (otherwise a decimal literal is a \`double\`).\r
\r
\`\`\`java DatatypesPrimitive.java @run-data-types-datatypes-primitive\r
public class DatatypesPrimitive {\r
\r
    public static void main(String[] args) {\r
\r
        byte myByte = 100;\r
\r
        short myShort = 20000;\r
\r
        int myInt = 100000;\r
\r
        long myLong = 15000000000L;\r
\r
        float myFloat = 5.75f;\r
\r
        double myDouble = 19.99;\r
\r
        char myChar = 'A';\r
\r
        boolean myBoolean = true;\r
\r
        System.out.println("byte    : " + myByte);\r
        System.out.println("short   : " + myShort);\r
        System.out.println("int     : " + myInt);\r
        System.out.println("long    : " + myLong);\r
        System.out.println("float   : " + myFloat);\r
        System.out.println("double  : " + myDouble);\r
        System.out.println("char    : " + myChar);\r
        System.out.println("boolean : " + myBoolean);\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-data-types-datatypes-primitive\r
byte    : 100\r
short   : 20000\r
int     : 100000\r
long    : 15000000000\r
float   : 5.75\r
double  : 19.99\r
char    : A\r
boolean : true\r
\`\`\`\r
\r
### \`DatatypesNonPrimitive.java\`\r
\r
A non-primitive â€” a *reference* type â€” does not hold the value at all. It holds\r
the address of an object on the heap. \`String\`, arrays, classes and interfaces\r
are all reference types.\r
\r
That difference shows up twice in the output. A reference can be **\`null\`**,\r
meaning it points at no object, which a primitive can never be. And because the\r
thing it points at is an object, it has **methods** you can call â€” which is why\r
\`toUpperCase()\` works on a \`String\` and nothing of the sort exists for an \`int\`.\r
\r
\`\`\`java DatatypesNonPrimitive.java @run-data-types-datatypes-non-primitive\r
public class DatatypesNonPrimitive {\r
\r
    public static void main(String[] args) {\r
\r
        String name = "Java";\r
\r
        int[] numbers = { 10, 20, 30 };\r
\r
        String empty = null;\r
\r
        System.out.println("String        : " + name);\r
        System.out.println("Array length  : " + numbers.length);\r
        System.out.println("Array[0]      : " + numbers[0]);\r
        System.out.println("Null reference: " + empty);\r
\r
        System.out.println("Uppercase     : " + name.toUpperCase());\r
        System.out.println("Char count    : " + name.length());\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-data-types-datatypes-non-primitive\r
String        : Java\r
Array length  : 3\r
Array[0]      : 10\r
Null reference: null\r
Uppercase     : JAVA\r
Char count    : 4\r
\`\`\`\r
\r
### \`DatatypesChar.java\`\r
\r
A \`char\` holds one 16-bit Unicode character, written in single quotes. But\r
underneath it is a **number** â€” the character's code point â€” and that is what\r
makes the rest of the file possible.\r
\r
\`'A'\` is \`65\`, so assigning it to an \`int\` prints \`65\`. Going the other way,\r
\`char fromCode = 66\` gives \`'B'\`. Arithmetic moves along the character set:\r
\`letter + 1\` is \`66\`, which is \`'B'\` â€” and the \`(char)\` cast is required,\r
because \`char + int\` produces an \`int\`.\r
\r
\`\`\`java DatatypesChar.java @run-data-types-datatypes-char\r
public class DatatypesChar {\r
\r
    public static void main(String[] args) {\r
\r
        char letter = 'A';\r
\r
        int code = letter;\r
\r
        char fromCode = 66;\r
\r
        char unicodeChar = 'A';\r
\r
        System.out.println("letter        : " + letter);\r
        System.out.println("code of 'A'   : " + code);\r
        System.out.println("char of 66    : " + fromCode);\r
        System.out.println("unicode esc   : " + unicodeChar);\r
\r
        char next = (char) (letter + 1);\r
        System.out.println("letter + 1    : " + next);\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-data-types-datatypes-char\r
letter        : A\r
code of 'A'   : 65\r
char of 66    : B\r
unicode esc   : A\r
letter + 1    : B\r
\`\`\`\r
\r
### \`DatatypesStrings.java\`\r
\r
A \`String\` is a class, not a primitive, and it is **immutable**: once created\r
its contents can never change. Every "modifying" method here â€” \`toUpperCase\`,\r
\`replace\`, \`substring\` â€” leaves the original alone and hands back a brand new\r
\`String\`.\r
\r
The comparison at the end is the one to remember. \`equals()\` compares the\r
**text**; \`==\` compares **references**, i.e. whether the two variables point at\r
the same object. For strings you almost always want \`equals()\`.\r
\r
\`\`\`java DatatypesStrings.java @run-data-types-datatypes-strings\r
public class DatatypesStrings {\r
\r
    public static void main(String[] args) {\r
\r
        String first = "Hello";\r
        String second = "World";\r
\r
        String message = first + " " + second;\r
\r
        System.out.println("message        : " + message);\r
        System.out.println("length         : " + message.length());\r
        System.out.println("upper case     : " + message.toUpperCase());\r
        System.out.println("lower case     : " + message.toLowerCase());\r
        System.out.println("char at 0      : " + message.charAt(0));\r
        System.out.println("index of World : " + message.indexOf("World"));\r
        System.out.println("substring(0,5) : " + message.substring(0, 5));\r
        System.out.println("replace l->L   : " + message.replace('l', 'L'));\r
        System.out.println("contains 'World': " + message.contains("World"));\r
\r
        String a = "Java";\r
        String b = "Java";\r
        System.out.println("a.equals(b)    : " + a.equals(b));\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-data-types-datatypes-strings\r
message        : Hello World\r
length         : 11\r
upper case     : HELLO WORLD\r
lower case     : hello world\r
char at 0      : H\r
index of World : 6\r
substring(0,5) : Hello\r
replace l->L   : HeLLo WorLd\r
contains 'World': true\r
a.equals(b)    : true\r
\`\`\`\r
\r
### \`DatatypesLiterals.java\`\r
\r
A literal is a fixed value written straight into the source. The first four are\r
worth reading together: \`100\`, \`0b1100100\`, \`0144\` and \`0x64\` are the **same\r
number**, written in base 10, base 2 (prefix \`0b\`), base 8 (a leading \`0\`) and\r
base 16 (prefix \`0x\`).\r
\r
The rest are the notations that trip people up. Underscores group digits purely\r
for readability (\`1_000_000\`). \`1.5e3\` is scientific notation for \`1500.0\`. The\r
\`f\` and \`L\` suffixes pin a literal to \`float\` and \`long\`. And \`'\\n'\` is an\r
escape sequence â€” one character, written as two.\r
\r
\`\`\`java DatatypesLiterals.java @run-data-types-datatypes-literals\r
public class DatatypesLiterals {\r
\r
    public static void main(String[] args) {\r
\r
        int decimal = 100;\r
        int binary = 0b1100100;\r
        int octal = 0144;\r
        int hex = 0x64;\r
\r
        int million = 1_000_000;\r
\r
        double d = 3.14;\r
        double scientific = 1.5e3;\r
        float f = 2.5f;\r
        long big = 10000000000L;\r
\r
        char ch = 'Z';\r
        char newline = '\\n';\r
        String text = "Literals in Java";\r
\r
        boolean flag = true;\r
\r
        System.out.println("decimal    : " + decimal);\r
        System.out.println("binary     : " + binary);\r
        System.out.println("octal      : " + octal);\r
        System.out.println("hex        : " + hex);\r
        System.out.println("million    : " + million);\r
        System.out.println("double     : " + d);\r
        System.out.println("scientific : " + scientific);\r
        System.out.println("float      : " + f);\r
        System.out.println("long       : " + big);\r
        System.out.println("char       : " + ch);\r
        System.out.println("string     : " + text);\r
        System.out.println("boolean    : " + flag);\r
        System.out.print("escape used: line1" + newline + "line2");\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-data-types-datatypes-literals\r
decimal    : 100\r
binary     : 100\r
octal      : 100\r
hex        : 100\r
million    : 1000000\r
double     : 3.14\r
scientific : 1500.0\r
float      : 2.5\r
long       : 10000000000\r
char       : Z\r
string     : Literals in Java\r
boolean    : true\r
escape used: line1\r
line2\r
\`\`\`\r
`,revision:"The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## The eight primitives\r\n\r\n![Fields are given a default value, locals are not](diagrams/04-data-types-revision-defaults-and-locals.jpg)\r\n\r\n- `byte` (1 byte), `short` (2), `int` (4), `long` (8), `float` (4), `double` (8),\r\n  `char` (2), `boolean` (size unspecified by the JVM).\r\n- Defaults for **fields** are `0`, `0.0`, `'\\u0000'` and `false`. **Locals have\r\n  no default** — the compiler refuses to read an uninitialised local, which is a\r\n  whole class of bug it removes rather than warns about.\r\n- `int` and `double` are the defaults for whole and decimal literals. `long`\r\n  literals need `L`, `float` literals need `f`.\r\n- Primitives cannot be `null` and cannot hold anything but their own kind of\r\n  value. Their wrapper classes (`Integer`, `Double`, …) can be `null`.\r\n\r\n## Reference types\r\n\r\n![Only reference types can be null](diagrams/04-data-types-revision-null-boundary.jpg)\r\n\r\n- Everything that is not one of the eight: `String`, arrays, every class you\r\n  write, every class in the library.\r\n- A reference variable holds an address, so its default is `null` and comparing\r\n  two with `==` compares addresses.\r\n- `String` is **immutable** — every operation that looks like a change returns a\r\n  new String. That is what makes literals safe to share in the pool and Strings\r\n  safe to hand between threads.\r\n- `==` on Strings compares identity, `.equals()` compares contents. Use\r\n  `.equals()` unless you specifically mean identity.\r\n\r\n## `char`, and literals\r\n\r\n- `char` is a **numeric** type holding a UTF-16 code unit, so `'A' + 1` is `66`\r\n  and `(char)('A' + 1)` is `'B'`.\r\n- Single quotes are a `char`, double quotes are a `String`. `char c = \"A\";` does\r\n  not compile.\r\n- Underscores are legal in numeric literals for readability: `1_000_000`.\r\n- Prefixes pick the base: `0x` hex, `0b` binary, a leading `0` octal — which is\r\n  why `010` is `8` and not `10`.\r\n",interview:"## Interview questions\n\n### Q1: How many primitive data types are there in Java, and what are they?\n\n**Answer:** Eight: `byte`, `short`, `int`, `long`, `float`, `double`, `char`, and `boolean`.\n\n### Q2: What is the difference between a primitive and a reference type?\n\n**Answer:** A primitive stores the actual value directly and is never `null`. A reference type stores the address of an object on the heap, can be `null`, and has methods. For example, `int` is primitive; `String` and arrays are reference types.\n\n### Q3: Why does a `long` literal need an `L` suffix?\n\n**Answer:** Without a suffix, a whole-number literal is treated as an `int`. If the value is larger than the `int` range, the compiler reports an error. The `L` suffix tells the compiler to treat the literal as a `long`.\n\n### Q4: Why is `char` considered a numeric type?\n\n**Answer:** A `char` stores the Unicode code point of a character, which is an integer (for example `'A'` is 65). Because of this, a `char` can be used in arithmetic and can be converted to and from `int`.\n\n![A char is a code point, so char plus int is an int](diagrams/04-data-types-interview-char-is-a-number.jpg)\n\n### Q5: What is the default type of `100` and `3.14` in Java?\n\n**Answer:** `100` is an `int` and `3.14` is a `double`. To make them `long` or `float` you must add the `L` or `f` suffix.\n\n![A literal has a type before the variable ever sees it](diagrams/04-data-types-interview-literal-default-types.jpg)\n\n### Q6: Why is `String` immutable?\n\n**Answer:** Once a `String` object is created its characters cannot be changed. Methods like `toUpperCase()` return a new `String` rather than modifying the original. Immutability makes Strings safe to share and allows the JVM to cache them in the String pool.\n\n### Q7: What is the difference between `==` and `.equals()` for Strings?\n\n**Answer:** `==` compares references (whether two variables point to the same object). `.equals()` compares the actual character contents. To compare text, always use `.equals()`.\n\n### Q8: What are the default values of primitive types?\n\n**Answer:**\n\n| Type      | Default     |\n|-----------|-------------|\n| `byte`/`short`/`int`/`long` | `0` |\n| `float`/`double`            | `0.0` |\n| `char`                      | `'\\u0000'` |\n| `boolean`                   | `false` |\n\n(Reference types default to `null`.)\n"}},{slug:`operators`,title:`Operators`,subject:`Java`,stage:`Fundamentals`,chapter:5,accent:`#facc15`,summary:`An operator is a symbol that performs an operation on one or more values. For example, in a + b, the + is an operator and a and b are operands. Operators are how you actually do something with the data you stored in Chapter 4.`,parts:{notes:'## Introduction\r\n\r\nAn **operator** is a symbol that performs an operation on one or more values. For example, in `a + b`, the `+` is an operator and `a` and `b` are operands. Operators are how you actually *do* something with the data you stored in Chapter 4.\r\n\r\nThis chapter is split into three small programs:\r\n\r\n| File | Topic |\r\n|------|-------|\r\n| `ArithmeticOperators.java`  | `+ - * / %` and `++ --` |\r\n| `AssignmentOperators.java`  | `=` and `+= -= *= /= %=` |\r\n| `LogicalOperators.java`     | `&& \\|\\| !` and relational operators |\r\n\r\n---\r\n\r\n## What is an Operator\r\n\r\nAn operator performs an action on **operands** (the values it works on).\r\n\r\n```java\r\nint sum = a + b;\r\n```\r\n\r\nOperators are grouped by how many operands they take:\r\n\r\n- **Unary** – one operand (e.g. `-x`, `!flag`, `count++`)\r\n- **Binary** – two operands (e.g. `a + b`, `a > b`)\r\n- **Ternary** – three operands (the `? :` operator, covered in Chapter 6)\r\n\r\n---\r\n\r\n## Categories of Operators\r\n\r\n| Category    | Operators                          | Purpose                              |\r\n|-------------|------------------------------------|--------------------------------------|\r\n| Arithmetic  | `+  -  *  /  %`                    | Math calculations                    |\r\n| Assignment  | `=  +=  -=  *=  /=  %=`            | Store / update values                |\r\n| Relational  | `==  !=  >  <  >=  <=`             | Compare values, produce a boolean    |\r\n| Logical     | `&&  \\|\\|  !`                    | Combine boolean conditions           |\r\n| Unary       | `++  --  -  !`                    | Act on a single operand              |\r\n\r\n---\r\n\r\n## Arithmetic Operators\r\n\r\n| Operator | Name           | Example  | Result (for `a=10`, `b=3`) |\r\n|----------|----------------|----------|----------------------------|\r\n| `+`      | Addition       | `a + b`  | `13`                       |\r\n| `-`      | Subtraction    | `a - b`  | `7`                        |\r\n| `*`      | Multiplication | `a * b`  | `30`                       |\r\n| `/`      | Division       | `a / b`  | `3` (integer division)     |\r\n| `%`      | Modulus        | `a % b`  | `1` (remainder)            |\r\n\r\n> **Integer division drops the decimal part.** `10 / 3` is `3`, not `3.33`. If you need the decimals, at least one operand must be a floating-point type: `10.0 / 3` gives `3.333...`.\r\n\r\nThe **modulus** operator `%` returns the remainder of a division. It is commonly used to test whether a number is even (`n % 2 == 0`) or to wrap values around a range.\r\n\r\n---\r\n\r\n## Assignment Operators\r\n\r\nThe `=` operator stores a value in a variable. **Compound** assignment operators combine arithmetic with assignment.\r\n\r\n| Operator | Example   | Equivalent to | \r\n|----------|-----------|---------------|\r\n| `=`      | `x = 5`   | store 5 in x  |\r\n| `+=`     | `x += 5`  | `x = x + 5`   |\r\n| `-=`     | `x -= 5`  | `x = x - 5`   |\r\n| `*=`     | `x *= 5`  | `x = x * 5`   |\r\n| `/=`     | `x /= 5`  | `x = x / 5`   |\r\n| `%=`     | `x %= 5`  | `x = x % 5`   |\r\n\r\n`+=` also works on Strings: `text += " more"` appends to `text`.\r\n\r\n---\r\n\r\n## Relational Operators\r\n\r\nRelational (comparison) operators compare two values and always produce a `boolean` (`true` or `false`).\r\n\r\n| Operator | Meaning                  | Example   | Result |\r\n|----------|--------------------------|-----------|--------|\r\n| `==`     | equal to                 | `5 == 5`  | `true` |\r\n| `!=`     | not equal to             | `5 != 3`  | `true` |\r\n| `>`      | greater than             | `5 > 3`   | `true` |\r\n| `<`      | less than                | `5 < 3`   | `false`|\r\n| `>=`     | greater than or equal to | `5 >= 5`  | `true` |\r\n| `<=`     | less than or equal to    | `5 <= 4`  | `false`|\r\n\r\n> For objects (like `String`), use `.equals()` to compare contents. `==` on objects compares references, not values.\r\n\r\n---\r\n\r\n## Logical Operators\r\n\r\nLogical operators combine boolean values.\r\n\r\n| Operator | Name | Result is `true` when…             |\r\n|----------|------|------------------------------------|\r\n| `&&`     | AND  | **both** sides are true            |\r\n| `\\|\\|`   | OR   | **at least one** side is true      |\r\n| `!`      | NOT  | the single operand is false        |\r\n\r\n**Truth table:**\r\n\r\n| A     | B     | `A && B` | `A \\|\\| B` |\r\n|-------|-------|----------|----------|\r\n| true  | true  | true     | true     |\r\n| true  | false | false    | true     |\r\n| false | true  | false    | true     |\r\n| false | false | false    | false    |\r\n\r\n**Short-circuit evaluation:** `&&` stops as soon as the left side is `false` (the result cannot be `true`), and `||` stops as soon as the left side is `true`. This is useful for safety:\r\n\r\n![The right side of a short-circuited && is never evaluated](diagrams/05-operators-notes-short-circuit.jpg)\r\n\r\n```java\r\nif (x != 0 && 10 / x > 1) { ... }\r\n```\r\n\r\n---\r\n\r\n## Increment and Decrement\r\n\r\n| Operator | Name      | Effect              |\r\n|----------|-----------|---------------------|\r\n| `++`     | increment | adds 1              |\r\n| `--`     | decrement | subtracts 1         |\r\n\r\nPosition matters:\r\n\r\n![Post-increment hands over the old value, pre-increment the new one](diagrams/05-operators-notes-post-vs-pre.jpg)\r\n\r\n| Form  | Name           | Behaviour                                  |\r\n|-------|----------------|--------------------------------------------|\r\n| `x++` | post-increment | use the current value, **then** add 1      |\r\n| `++x` | pre-increment  | add 1 **first**, then use the new value    |\r\n\r\n```java\r\nint p = 5;\r\nSystem.out.println(p++);\r\nSystem.out.println(++p);\r\n```\r\n\r\n---\r\n\r\n## Operator Precedence\r\n\r\nWhen several operators appear in one expression, Java evaluates them in a fixed order (highest first):\r\n\r\n1. `()` parentheses\r\n2. `++ -- !` (unary)\r\n3. `* / %`\r\n4. `+ -`\r\n5. `< <= > >=`\r\n6. `== !=`\r\n7. `&&`\r\n8. `||`\r\n9. `= += -= ...` (assignment)\r\n\r\n```java\r\nint result = 2 + 3 * 4;\r\nint result2 = (2 + 3) * 4;\r\n```\r\n\r\n> When in doubt, **use parentheses** to make the order explicit and the code readable.\r\n\r\n![Which operators bind tightest, and what parentheses change](diagrams/05-operators-notes-precedence.jpg)\r\n\r\n---\r\n\r\n## How to Compile and Run\r\n\r\n```bash\r\njavac ArithmeticOperators.java\r\njava ArithmeticOperators\r\n\r\njavac AssignmentOperators.java\r\njava AssignmentOperators\r\n\r\njavac LogicalOperators.java\r\njava LogicalOperators\r\n```\r\n\r\n---\r\n\r\n## Common Mistakes\r\n\r\n### Mistake 1: Expecting decimals from integer division\r\n\r\n```java\r\ndouble half = 1 / 2;\r\ndouble half2 = 1.0 / 2;\r\n```\r\n\r\n```expected\r\nhalf  is 0.0\r\nhalf2 is 0.5\r\n\r\n1 / 2 is worked out as int division first — the answer is 0 — and only then\r\nwidened to double. One operand has to be floating-point to keep the fraction.\r\n```\r\n\r\n### Mistake 2: Using `=` instead of `==`\r\n\r\n```java\r\nif (x = 5) { ... }   // ERROR: = assigns, it does not compare\r\nif (x == 5) { ... }  // correct\r\n```\r\n\r\n```expected\r\nerror: incompatible types: int cannot be converted to boolean\r\n\r\nJava will not accept a number where a boolean belongs, so the classic C bug\r\nof writing = for == is a build failure here rather than a bug that ships.\r\n```\r\n\r\n### Mistake 3: Comparing Strings with `==`\r\n\r\n```java\r\nif (name == "Java") { ... }\r\nif (name.equals("Java")) { ... }\r\n```\r\n\r\n```expected\r\nThe first is true only when name happens to be the pooled literal, so it\r\nappears to work in testing and fails on a String built at run time.\r\nThe second is true whenever the characters match, which is the question\r\nyou were asking.\r\n```\r\n\r\n### Mistake 4: Confusing `p++` and `++p`\r\n\r\n```java\r\nint p = 5;\r\nint a = p++;\r\nint b = ++p;\r\n```\r\n\r\n```expected\r\na is 5   (p++ hands over the old value, then increments)\r\np is now 6\r\nb is 7   (++p increments first, then hands over the new value)\r\np is now 7\r\n```\r\n\r\n---\r\n\r\n## The source files\r\n\r\nThe complete programs, renamed from the numbered file names the repository\r\nuses so they compile on their own. **Save** writes the file out of this page;\r\n**Run** replays the output it produced when this page was built.\r\n\r\n```bash\r\njavac Something.java\r\njava Something\r\n```\r\n\r\n### `ArithmeticOperators.java`\r\n\r\nThe five arithmetic operators, plus the two that catch people out.\r\n\r\n`10 / 3` is **`3`**, not `3.33`. With two `int` operands Java does integer\r\ndivision and throws the fraction away; to keep it, at least one side has to be a\r\nfloating-point type. `%` gives the remainder that division discarded.\r\n\r\nThe increment pair matters where it sits: **pre-increment** (`++x`) changes the\r\nvalue and then hands it over, while **post-increment** (`x++`) hands over the\r\nold value and changes it afterwards. Same variable, different result in the same\r\nexpression.\r\n\r\n```java ArithmeticOperators.java @run-operators-arithmetic-operators\r\npublic class ArithmeticOperators {\r\n\r\n    public static void main(String[] args) {\r\n\r\n        int a = 10;\r\n        int b = 3;\r\n\r\n        System.out.println("a + b = " + (a + b));\r\n        System.out.println("a - b = " + (a - b));\r\n        System.out.println("a * b = " + (a * b));\r\n        System.out.println("a / b = " + (a / b));\r\n        System.out.println("a % b = " + (a % b));\r\n\r\n        double x = 10;\r\n        double y = 3;\r\n        System.out.println("x / y = " + (x / y));\r\n\r\n        int count = 5;\r\n        count++;\r\n        count--;\r\n        System.out.println("count = " + count);\r\n\r\n        int p = 5;\r\n        System.out.println("p++ gives   : " + (p++));\r\n        System.out.println("now p is    : " + p);\r\n        System.out.println("++p gives   : " + (++p));\r\n    }\r\n}\r\n\r\n```\r\n\r\n```output @run-operators-arithmetic-operators\r\na + b = 13\r\na - b = 7\r\na * b = 30\r\na / b = 3\r\na % b = 1\r\nx / y = 3.3333333333333335\r\ncount = 5\r\np++ gives   : 5\r\nnow p is    : 6\r\n++p gives   : 7\r\n```\r\n\r\n### `AssignmentOperators.java`\r\n\r\n`=` stores a value. The compound forms â€” `+=`, `-=`, `*=`, `/=`, `%=` â€” are\r\nshorthand that reads the variable, applies the operation, and stores the result\r\nback. `num += 5` is exactly `num = num + 5`.\r\n\r\n`+=` works on `String` too, where it appends rather than adds.\r\n\r\n```java AssignmentOperators.java @run-operators-assignment-operators\r\npublic class AssignmentOperators {\r\n\r\n    public static void main(String[] args) {\r\n\r\n        int num = 10;\r\n        System.out.println("start       : " + num);\r\n\r\n        num += 5;\r\n        System.out.println("num += 5    : " + num);\r\n\r\n        num -= 3;\r\n        System.out.println("num -= 3    : " + num);\r\n\r\n        num *= 2;\r\n        System.out.println("num *= 2    : " + num);\r\n\r\n        num /= 4;\r\n        System.out.println("num /= 4    : " + num);\r\n\r\n        num %= 4;\r\n        System.out.println("num %= 4    : " + num);\r\n\r\n        String text = "Java";\r\n        text += " Programming";\r\n        System.out.println("text        : " + text);\r\n    }\r\n}\r\n\r\n```\r\n\r\n```output @run-operators-assignment-operators\r\nstart       : 10\r\nnum += 5    : 15\r\nnum -= 3    : 12\r\nnum *= 2    : 24\r\nnum /= 4    : 6\r\nnum %= 4    : 2\r\ntext        : Java Programming\r\n```\r\n\r\n### `LogicalOperators.java`\r\n\r\nTwo families that work together. **Relational** operators (`>`, `<`, `>=`, `<=`,\r\n`==`, `!=`) compare values and produce the booleans that **logical** operators\r\nthen combine.\r\n\r\n`&&` needs both sides true, `||` needs at least one, and `!` flips whichever it\r\nis given. The last section shows the detail worth knowing: `&&` and `||`\r\n**short-circuit**. If the left side of `&&` is already false the right side is\r\nnever evaluated at all â€” which is what makes `if (obj != null && obj.size() > 0)`\r\nsafe to write.\r\n\r\n```java LogicalOperators.java @run-operators-logical-operators\r\npublic class LogicalOperators {\r\n\r\n    public static void main(String[] args) {\r\n\r\n        int age = 25;\r\n        boolean hasLicense = true;\r\n\r\n        System.out.println("age > 18      : " + (age > 18));\r\n        System.out.println("age == 25     : " + (age == 25));\r\n        System.out.println("age != 30     : " + (age != 30));\r\n\r\n        boolean canDrive = (age >= 18) && hasLicense;\r\n        System.out.println("canDrive (AND): " + canDrive);\r\n\r\n        boolean weekend = false;\r\n        boolean holiday = true;\r\n        boolean dayOff = weekend || holiday;\r\n        System.out.println("dayOff (OR)   : " + dayOff);\r\n\r\n        System.out.println("!hasLicense   : " + (!hasLicense));\r\n\r\n        int x = 0;\r\n        boolean safe = (x != 0) && (10 / x > 1);\r\n        System.out.println("safe (short)  : " + safe);\r\n    }\r\n}\r\n\r\n```\r\n\r\n```output @run-operators-logical-operators\r\nage > 18      : true\r\nage == 25     : true\r\nage != 30     : true\r\ncanDrive (AND): true\r\ndayOff (OR)   : true\r\n!hasLicense   : false\r\nsafe (short)  : false\r\n```\r\n',revision:'The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## Arithmetic\r\n\r\n![Integer and floating-point division behave differently, including at zero](diagrams/05-operators-revision-division-by-type.jpg)\r\n\r\n- Integer division **truncates**: `7 / 2` is `3`, not `3.5`. One operand has to\r\n  be floating-point to get a floating-point answer.\r\n- `%` gives the remainder and keeps the **sign of the left operand**, so\r\n  `-7 % 2` is `-1`.\r\n- Division by zero throws `ArithmeticException` for integers, but gives\r\n  `Infinity` or `NaN` for `double` — the same expression, two behaviours.\r\n- `+` is overloaded: numeric addition, or String concatenation as soon as either\r\n  side is a String. `1 + 2 + "a"` is `"3a"`; `"a" + 1 + 2` is `"a12"`.\r\n\r\n![Plus adds until the first String, then concatenates the rest](diagrams/05-operators-revision-plus-is-overloaded.jpg)\r\n\r\n## Comparison and logic\r\n\r\n- `=` assigns, `==` compares. `if (x = 5)` does not compile in Java, because `if`\r\n  requires a `boolean` — a whole family of C bugs Java simply does not have.\r\n- `==` on objects compares references. Use `.equals()` for value equality.\r\n- `&&` and `||` **short-circuit**: the right side is not evaluated when the left\r\n  already decides the answer. That is what makes `if (s != null && s.isEmpty())`\r\n  safe.\r\n- `&` and `|` on booleans do the same logic **without** short-circuiting, so both\r\n  sides always run. Use them only when the right side has a side effect you want.\r\n\r\n## Increment, and precedence\r\n\r\n- `i++` evaluates to the **old** value, `++i` to the **new** one. As a statement\r\n  on its own the two are identical; inside a larger expression they are not.\r\n- `i = i++;` leaves `i` unchanged — the old value is saved, `i` is incremented,\r\n  then the saved value is written back over it.\r\n- Precedence, loosest last: unary → multiplicative → additive → relational →\r\n  equality → `&&` → `||` → ternary → assignment.\r\n- Precedence decides grouping, not evaluation order: Java always evaluates\r\n  operands left to right. Parentheses cost nothing and settle both.\r\n',interview:"## Interview questions\n\n### Q1: What is the difference between `/` and `%`?\n\n**Answer:** `/` returns the quotient of a division and `%` returns the remainder. For example, `17 / 5` is `3` and `17 % 5` is `2`.\n\n### Q2: Why does `5 / 2` give `2` instead of `2.5`?\n\n**Answer:** Both operands are `int`, so Java performs integer division and discards the decimal part. To get `2.5`, make at least one operand a floating-point value: `5.0 / 2`.\n\n### Q3: What is the difference between `x++` and `++x`?\n\n**Answer:** `x++` (post-increment) returns the current value of `x` and then increases it. `++x` (pre-increment) increases `x` first and then returns the new value.\n\n### Q4: What is short-circuit evaluation?\n\n**Answer:** With `&&`, if the left operand is `false`, the right operand is never evaluated because the result is already `false`. With `||`, if the left operand is `true`, the right operand is skipped. This avoids unnecessary work and can prevent errors such as division by zero.\n\n### Q5: What is the difference between `==` and `.equals()`?\n\n**Answer:** For primitives, `==` compares values. For objects, `==` compares references (whether they are the same object), while `.equals()` compares the actual contents. Use `.equals()` to compare String text.\n\n![What == compares for primitives and objects, against what .equals compares](diagrams/05-operators-interview-equals-vs-identity.jpg)\n\n### Q6: What does the `%` (modulus) operator do, and give a use case.\n\n**Answer:** It returns the remainder of a division. A common use is checking whether a number is even or odd: `n % 2 == 0` is true for even numbers.\n\n![The remainder cycles through a fixed range as the number grows](diagrams/05-operators-interview-modulus-wraps.jpg)\n\n### Q7: What is operator precedence?\n\n**Answer:** It is the order in which operators are evaluated in an expression. For example, `*` and `/` are evaluated before `+` and `-`. Parentheses have the highest precedence and can be used to override the default order.\n"}},{slug:`conditionals`,title:`Conditional statements`,subject:`Java`,stage:`Fundamentals`,chapter:6,accent:`#22d3ee`,summary:`So far our programs have run every line from top to bottom. Conditional statements let a program make decisions — run some code only when a condition is true, and other code otherwise. This is what makes a program behave differently for different inputs.`,parts:{notes:`## Introduction\r
\r
So far our programs have run every line from top to bottom. **Conditional statements** let a program make decisions — run some code only when a condition is true, and other code otherwise. This is what makes a program behave differently for different inputs.\r
\r
This chapter is split into three small programs:\r
\r
| File | Topic |\r
|------|-------|\r
| \`IfElse.java\`  | \`if\`, \`if-else\`, \`if-else-if\`, nested \`if\` |\r
| \`Ternary.java\`  | The \`? :\` ternary operator |\r
| \`Switch.java\`   | The \`switch\` statement |\r
\r
---\r
\r
## What is a Conditional Statement\r
\r
A conditional statement evaluates a **boolean condition** (something that is \`true\` or \`false\`, built using the relational and logical operators from Chapter 5) and chooses which block of code to run.\r
\r
![What is a Conditional Statement](diagrams/06-conditionals-notes-a2de992d.jpg)\r
\r
---\r
\r
## The \`if\` Statement\r
\r
Runs a block **only** when the condition is true.\r
\r
\`\`\`java\r
if (marks >= 33) {\r
    System.out.println("Pass");\r
}\r
\`\`\`\r
\r
If the condition is \`false\`, the block is skipped entirely.\r
\r
---\r
\r
## The \`if-else\` Statement\r
\r
Provides an alternative block for when the condition is false. Exactly one of the two blocks always runs.\r
\r
\`\`\`java\r
if (number % 2 == 0) {\r
    System.out.println("even");\r
} else {\r
    System.out.println("odd");\r
}\r
\`\`\`\r
\r
---\r
\r
## The \`if-else-if\` Ladder\r
\r
![An if-else-if ladder stops at the first true rung, so the order is the logic](diagrams/06-conditionals-notes-ladder-order.jpg)\r
\r
Tests several conditions **in order**. The first one that is true runs, and all the rest are skipped. The final \`else\` is optional and acts as a catch-all.\r
\r
\`\`\`java\r
if (marks >= 90) {\r
    grade = 'A';\r
} else if (marks >= 75) {\r
    grade = 'B';\r
} else if (marks >= 60) {\r
    grade = 'C';\r
} else {\r
    grade = 'F';\r
}\r
\`\`\`\r
\r
> Order matters. Because the ladder stops at the first true condition, you usually arrange conditions from the most specific/most restrictive to the least.\r
\r
---\r
\r
## Nested \`if\`\r
\r
An \`if\` placed inside another \`if\`. The inner condition is only checked when the outer one is true.\r
\r
\`\`\`java\r
if (age >= 18) {\r
    if (hasId) {\r
        System.out.println("Entry allowed");\r
    }\r
}\r
\`\`\`\r
\r
This is equivalent to \`if (age >= 18 && hasId)\`, but nesting is handy when each level needs its own \`else\`.\r
\r
---\r
\r
## The Ternary Operator\r
\r
The ternary operator is a compact \`if-else\` that **produces a value**:\r
\r
\`\`\`java\r
result = condition ? valueIfTrue : valueIfFalse;\r
\`\`\`\r
\r
Example:\r
\r
\`\`\`java\r
int max = (a > b) ? a : b;\r
String type = (n % 2 == 0) ? "even" : "odd";\r
\`\`\`\r
\r
| Part            | Meaning                              |\r
|-----------------|--------------------------------------|\r
| \`condition\`     | a boolean expression                 |\r
| \`valueIfTrue\`   | used when the condition is true      |\r
| \`valueIfFalse\`  | used when the condition is false     |\r
\r
Use it for short either/or choices. For anything complex, a normal \`if-else\` is more readable.\r
\r
---\r
\r
## The \`switch\` Statement\r
\r
A \`switch\` compares one variable against several constant \`case\` labels and runs the matching block. It is cleaner than a long ladder when you test a single value against fixed options.\r
\r
\`\`\`java\r
switch (day) {\r
    case 1:\r
        dayName = "Monday";\r
        break;\r
    case 2:\r
        dayName = "Tuesday";\r
        break;\r
    default:\r
        dayName = "Weekend";\r
}\r
\`\`\`\r
\r
**Key parts:**\r
\r
- **\`case\`** – a constant value to compare against.\r
- **\`break\`** – stops execution and exits the switch. Without it, execution "falls through" into the next case.\r
- **\`default\`** – runs when no case matches (like the final \`else\`). It is optional.\r
\r
**Fall-through** can be used on purpose by stacking cases that share a block:\r
\r
![A missing break lets a switch carry on into the next case](diagrams/06-conditionals-notes-fall-through.jpg)\r
\r
\`\`\`java\r
switch (d) {\r
    case 1: case 2: case 3: case 4: case 5:\r
        System.out.println("Weekday");\r
        break;\r
    case 6: case 7:\r
        System.out.println("Weekend");\r
        break;\r
}\r
\`\`\`\r
\r
A \`switch\` works with \`int\` (and smaller integer types), \`char\`, \`String\`, and \`enum\`.\r
\r
---\r
\r
## \`if-else\` vs \`switch\`\r
\r
![A ladder tests each rung in turn; a switch jumps straight to the case](diagrams/06-conditionals-notes-ladder-vs-jump.jpg)\r
\r
| Aspect            | \`if-else-if\`                          | \`switch\`                                |\r
|-------------------|---------------------------------------|-----------------------------------------|\r
| Condition type    | Any boolean expression (ranges, etc.) | Equality against constant values only   |\r
| Best for          | Ranges, complex/combined conditions   | One variable vs many fixed values       |\r
| Readability       | Can get long                          | Cleaner for many discrete cases         |\r
| Fall-through      | Not applicable                        | Possible (and sometimes useful)         |\r
\r
Use \`if-else\` when testing ranges or combined conditions (\`marks >= 75\`). Use \`switch\` when checking one value against a list of exact options (\`day == 1\`, \`2\`, \`3\`...).\r
\r
---\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac IfElse.java\r
java IfElse\r
\r
javac Ternary.java\r
java Ternary\r
\r
javac Switch.java\r
java Switch\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Using \`=\` instead of \`==\`\r
\r
\`\`\`java\r
if (x = 5) { ... }   // ERROR: = assigns; a condition needs ==\r
if (x == 5) { ... }  // correct\r
\`\`\`\r
\r
\`\`\`expected\r
error: incompatible types: int cannot be converted to boolean\r
if takes a boolean and nothing else.\r
\`\`\`\r
\r
### Mistake 2: Forgetting \`break\` in a switch\r
\r
\`\`\`java\r
switch (n) {\r
    case 1:\r
        System.out.println("one");\r
    case 2:\r
        System.out.println("two");\r
        break;\r
}\r
\`\`\`\r
\r
Add \`break\` unless you intentionally want fall-through.\r
\r
### Mistake 3: A misplaced semicolon after \`if\`\r
\r
\`\`\`java\r
if (x > 0);\r
{\r
    System.out.println("positive");\r
}\r
\`\`\`\r
\r
Do not put a semicolon directly after the \`if (...)\` condition.\r
\r
### Mistake 4: Overusing nested ternaries\r
\r
Deeply nested \`? :\` expressions are hard to read. Prefer an \`if-else-if\` ladder once there is more than one level.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`IfElse.java\`\r
\r
Three shapes of the same idea. A plain **\`if\`** runs its block only when the\r
condition holds. **\`if-else\`** adds the other path. An **\`if-else-if\` ladder**\r
tries each condition in order and stops at the first true one â€” the rest are\r
never even evaluated, which is why the order of the rungs is part of the logic.\r
\r
The last example nests an \`if\` inside another, for a decision that only makes\r
sense once an earlier one has passed.\r
\r
\`\`\`java IfElse.java @run-conditionals-if-else\r
public class IfElse {\r
\r
    public static void main(String[] args) {\r
\r
        int marks = 75;\r
\r
        if (marks >= 33) {\r
            System.out.println("Result: Pass");\r
        }\r
\r
        int number = 7;\r
        if (number % 2 == 0) {\r
            System.out.println(number + " is even");\r
        } else {\r
            System.out.println(number + " is odd");\r
        }\r
\r
        char grade;\r
        if (marks >= 90) {\r
            grade = 'A';\r
        } else if (marks >= 75) {\r
            grade = 'B';\r
        } else if (marks >= 60) {\r
            grade = 'C';\r
        } else if (marks >= 33) {\r
            grade = 'D';\r
        } else {\r
            grade = 'F';\r
        }\r
        System.out.println("Grade: " + grade);\r
\r
        int age = 20;\r
        boolean hasId = true;\r
        if (age >= 18) {\r
            if (hasId) {\r
                System.out.println("Entry allowed");\r
            } else {\r
                System.out.println("ID required");\r
            }\r
        }\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-conditionals-if-else\r
Result: Pass\r
7 is odd\r
Grade: B\r
Entry allowed\r
\`\`\`\r
\r
### \`Ternary.java\`\r
\r
The ternary is the only Java operator taking three operands, and the compact\r
form of \`if-else\` â€” with one crucial difference: it **produces a value**. A\r
plain \`if\` statement does not, which is why a ternary can sit on the right of an\r
assignment or straight inside a \`println\`.\r
\r
\`condition ? whenTrue : whenFalse\`. The file picks the larger of two numbers,\r
turns a number into the string \`"even"\` or \`"odd"\`, and shows the same logic\r
written out as \`if-else\` for comparison. Ternaries nest, as the last example\r
does â€” but that is usually where readability starts to lose.\r
\r
\`\`\`java Ternary.java @run-conditionals-ternary\r
public class Ternary {\r
\r
    public static void main(String[] args) {\r
\r
        int a = 10;\r
        int b = 20;\r
\r
        int max = (a > b) ? a : b;\r
        System.out.println("max         : " + max);\r
\r
        int number = 7;\r
        String type = (number % 2 == 0) ? "even" : "odd";\r
        System.out.println("7 is        : " + type);\r
\r
        // String type;\r
        // if (number % 2 == 0) type = "even"; else type = "odd";\r
\r
        int marks = 82;\r
        char grade = (marks >= 90) ? 'A'\r
                   : (marks >= 75) ? 'B'\r
                   : (marks >= 60) ? 'C'\r
                   : 'F';\r
        System.out.println("grade       : " + grade);\r
\r
        int age = 16;\r
        System.out.println("category    : " + (age >= 18 ? "Adult" : "Minor"));\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-conditionals-ternary\r
max         : 20\r
7 is        : odd\r
grade       : B\r
category    : Minor\r
\`\`\`\r
\r
### \`Switch.java\`\r
\r
A \`switch\` compares one value against a list of constant \`case\` labels and jumps\r
straight to the match, instead of walking down a ladder of conditions.\r
\r
\`break\` is what stops execution there. Leave it out and control **falls through**\r
into the next case and keeps going â€” occasionally what you want, and otherwise a\r
classic bug. \`default\` catches everything that matched nothing.\r
\r
\`\`\`java Switch.java @run-conditionals-switch\r
public class Switch {\r
\r
    public static void main(String[] args) {\r
\r
        int day = 3;\r
        String dayName;\r
\r
        switch (day) {\r
            case 1:\r
                dayName = "Monday";\r
                break;\r
            case 2:\r
                dayName = "Tuesday";\r
                break;\r
            case 3:\r
                dayName = "Wednesday";\r
                break;\r
            case 4:\r
                dayName = "Thursday";\r
                break;\r
            case 5:\r
                dayName = "Friday";\r
                break;\r
            default:\r
                dayName = "Weekend";\r
        }\r
        System.out.println("day " + day + " is " + dayName);\r
\r
        char grade = 'B';\r
        switch (grade) {\r
            case 'A':\r
                System.out.println("Excellent");\r
                break;\r
            case 'B':\r
                System.out.println("Good");\r
                break;\r
            case 'C':\r
                System.out.println("Average");\r
                break;\r
            default:\r
                System.out.println("Needs improvement");\r
        }\r
\r
        int d = 6;\r
        switch (d) {\r
            case 1:\r
            case 2:\r
            case 3:\r
            case 4:\r
            case 5:\r
                System.out.println("Weekday");\r
                break;\r
            case 6:\r
            case 7:\r
                System.out.println("Weekend");\r
                break;\r
        }\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-conditionals-switch\r
day 3 is Wednesday\r
Good\r
Weekend\r
\`\`\`\r
`,revision:"The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## `if` and its ladder\r\n\r\n![A semicolon after if makes the body empty and the block below unconditional](diagrams/06-conditionals-revision-stray-semicolon.jpg)\r\n\r\n- `if` takes a `boolean` and nothing else. `if (x = 5)` is a compile error rather\r\n  than a bug that ships.\r\n- An `if-else-if` ladder stops at the **first** condition that is true; the rest\r\n  are never evaluated even if they would also be true. Order the specific cases\r\n  before the general ones.\r\n- `else` binds to the nearest unmatched `if`. Braces make that explicit and are\r\n  worth writing even for one statement.\r\n- A stray semicolon — `if (x > 0);` — is a complete empty statement, so the block\r\n  after it runs unconditionally. It compiles silently.\r\n\r\n## `switch`\r\n\r\n![The types a switch can and cannot select on](diagrams/06-conditionals-revision-switch-types.jpg)\r\n\r\n- Works on `byte`, `short`, `char`, `int`, their wrappers, `String` and `enum`.\r\n  Not `long`, not `float` or `double`, not `boolean`.\r\n- Every case label must be a **compile-time constant**, which is why a `switch`\r\n  can be compiled to a jump table and an `if` ladder cannot.\r\n- Forget `break` and execution **falls through** into the next case. Deliberate\r\n  fall-through is occasionally useful; accidental fall-through is the classic\r\n  `switch` bug.\r\n- `default` catches everything unmatched. It can sit anywhere, but reads best\r\n  last, and without it an unmatched value simply does nothing.\r\n- Switching on a `String` uses `hashCode` then `equals` underneath, so a `null`\r\n  selector throws `NullPointerException` — check first.\r\n\r\n## The ternary\r\n\r\n- `condition ? a : b` is an **expression**: it produces a value and can be\r\n  assigned. `if` is a statement and cannot.\r\n- Both arms must produce compatible types, and only the arm that is chosen is\r\n  evaluated.\r\n- One level reads well. Nested ternaries are a line that has to be decoded rather\r\n  than read — use an `if` ladder.\r\n",interview:'## Interview questions\n\n### Q1: What is the difference between `if-else` and `switch`?\n\n**Answer:** `if-else` can test any boolean condition, including ranges and combined conditions. `switch` only tests equality of a single variable against constant values, but it is cleaner and often faster when checking one value against many fixed options.\n\n### Q2: What is the purpose of `break` in a `switch`?\n\n**Answer:** `break` exits the switch after a case runs. Without it, execution "falls through" and continues into the following cases until a `break` or the end of the switch is reached.\n\n### Q3: What is the `default` case?\n\n**Answer:** It is the block that runs when no `case` matches the switch value, similar to the final `else` in an if-else-if ladder. It is optional.\n\n### Q4: What is the ternary operator and when should you use it?\n\n![A ternary produces a value; an if statement produces nothing](diagrams/06-conditionals-interview-ternary-is-a-value.jpg)\n\n**Answer:** It is a three-operand conditional, `condition ? valueIfTrue : valueIfFalse`, that produces a value. Use it for short, simple either/or choices. For complex logic, a normal `if-else` is more readable.\n\n### Q5: Which data types can a `switch` work with?\n\n**Answer:** `byte`, `short`, `int`, `char`, their wrapper classes, `String`, and `enum` types. It cannot switch on `long`, `float`, `double`, or `boolean`.\n\n### Q6: In an if-else-if ladder, what happens if two conditions are true?\n\n**Answer:** Only the **first** true condition (from top to bottom) runs; the rest are skipped. This is why the order of conditions matters.\n\n### Q7: Can you switch on a `String` in Java?\n\n![A String switch uses equals, so case matters and null throws](diagrams/06-conditionals-interview-switch-on-string.jpg)\n\n**Answer:** Yes, since Java 7 a `switch` can use `String` values. The comparison uses `String.equals()` internally and is case-sensitive.\n'}},{slug:`loops`,title:`Loops`,subject:`Java`,stage:`Fundamentals`,chapter:7,accent:`#fb7185`,summary:`A loop repeats a block of code as long as a condition stays true. Instead of writing the same line five times, you write it once inside a loop and let it run five times. Loops are how programs process lists, repeat actions, and count.`,parts:{notes:`## Introduction\r
\r
A **loop** repeats a block of code as long as a condition stays true. Instead of writing the same line five times, you write it once inside a loop and let it run five times. Loops are how programs process lists, repeat actions, and count.\r
\r
This chapter uses one program, \`WhileDoWhileForLoops.java\`, that demonstrates all three loop types plus \`for-each\`, \`break\`, \`continue\`, and nesting.\r
\r
---\r
\r
## What is a Loop\r
\r
Every loop has three logical parts, no matter the syntax:\r
\r
1. **Initialisation** – a starting point (e.g. \`i = 1\`).\r
2. **Condition** – a boolean checked each round; the loop continues while it is true.\r
3. **Update** – a change that moves the loop toward ending (e.g. \`i++\`).\r
\r
> If the update never makes the condition false, you get an **infinite loop**. Always make sure the loop can end.\r
\r
---\r
\r
## The \`while\` Loop\r
\r
Checks the condition **before** each iteration. The body may run **zero** times if the condition starts false.\r
\r
\`\`\`java\r
int i = 1;\r
while (i <= 5) {\r
    System.out.print(i + " ");\r
    i++;\r
}\r
\`\`\`\r
\r
Use \`while\` when you do not know in advance how many times to loop (e.g. "keep reading until the input ends").\r
\r
---\r
\r
## The \`do-while\` Loop\r
\r
![while checks before the body, do-while checks after, so one may run zero times](diagrams/07-loops-notes-zero-or-once.jpg)\r
\r
Runs the body **first**, then checks the condition. So it always runs **at least once**, even if the condition is false.\r
\r
\`\`\`java\r
int j = 1;\r
do {\r
    System.out.print(j + " ");\r
    j++;\r
} while (j <= 5);\r
\`\`\`\r
\r
Use \`do-while\` when the body must execute at least once (e.g. show a menu, then ask whether to repeat).\r
\r
> Note the **semicolon** after the \`while(...)\` in a do-while.\r
\r
---\r
\r
## The \`for\` Loop\r
\r
Packs initialisation, condition, and update onto one line. Ideal when you know how many times to repeat.\r
\r
\`\`\`java\r
for (int k = 1; k <= 5; k++) {\r
    System.out.print(k + " ");\r
}\r
\`\`\`\r
\r
![The for Loop](diagrams/07-loops-notes-5fb6c61f.jpg)\r
\r
---\r
\r
## The Enhanced \`for-each\` Loop\r
\r
Iterates over **every element** of an array or collection without a counter or index. It is cleaner and avoids index errors when you simply need each value.\r
\r
\`\`\`java\r
int[] nums = {10, 20, 30};\r
for (int n : nums) {\r
    System.out.print(n + " ");\r
}\r
\`\`\`\r
\r
Read \`for (int n : nums)\` as "for each int \`n\` in \`nums\`". You cannot use it to change array elements by index or to know the current index — use a normal \`for\` loop for that.\r
\r
---\r
\r
## \`break\` and \`continue\`\r
\r
![break ends the loop, continue only ends the current pass](diagrams/07-loops-notes-early-exit.jpg)\r
\r
| Keyword    | Effect                                                        |\r
|------------|--------------------------------------------------------------|\r
| \`break\`    | Exits the **entire** loop immediately.                       |\r
| \`continue\` | Skips the rest of the **current** iteration and moves on.    |\r
\r
\`\`\`java\r
for (int b = 1; b <= 10; b++) {\r
    if (b == 4) break;\r
    System.out.print(b + " ");\r
}\r
\r
for (int c = 1; c <= 6; c++) {\r
    if (c % 2 == 0) continue;\r
    System.out.print(c + " ");\r
}\r
\`\`\`\r
\r
---\r
\r
## Nested Loops\r
\r
A loop placed inside another loop. For **each** step of the outer loop, the inner loop runs fully. Common for grids, tables, and patterns.\r
\r
\`\`\`java\r
for (int row = 1; row <= 3; row++) {\r
    for (int col = 1; col <= 3; col++) {\r
        System.out.print("* ");\r
    }\r
    System.out.println();\r
}\r
\`\`\`\r
\r
---\r
\r
## Which Loop Should I Use\r
\r
| Situation                                   | Best loop      |\r
|---------------------------------------------|----------------|\r
| You know the exact number of repetitions    | \`for\`          |\r
| You loop until some condition changes       | \`while\`        |\r
| The body must run at least once             | \`do-while\`     |\r
| You just need each element of an array       | \`for-each\`     |\r
\r
---\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac WhileDoWhileForLoops.java\r
java WhileDoWhileForLoops\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
while  : 1 2 3 4 5\r
do-while: 1 2 3 4 5\r
for    : 1 2 3 4 5\r
for-each: 10 20 30\r
break  : 1 2 3\r
continue: 1 3 5\r
nested :\r
* * *\r
* * *\r
* * *\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Forgetting the update (infinite loop)\r
\r
![Without an update the condition never becomes false](diagrams/07-loops-notes-never-ends.jpg)\r
\r
\`\`\`java\r
int i = 1;\r
while (i <= 5) {\r
    System.out.println(i);\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
1\r
1\r
1\r
1\r
… for ever.\r
\r
i is never changed, so the condition never becomes false. It compiles\r
perfectly — nothing here is a syntax error.\r
\`\`\`\r
\r
### Mistake 2: An off-by-one error\r
\r
\`\`\`java\r
for (int i = 0; i <= 5; i++)\r
for (int i = 0; i <  5; i++)\r
\`\`\`\r
\r
\`\`\`expected\r
The first runs 6 times: 0 1 2 3 4 5\r
The second runs 5 times: 0 1 2 3 4\r
\r
For an array of length 5 the second is the one you want — the valid indices\r
are 0 to 4, and i <= 5 walks off the end.\r
\`\`\`\r
\r
Decide carefully between \`<\` and \`<=\`.\r
\r
### Mistake 3: A stray semicolon after \`for\`/\`while\`\r
\r
\`\`\`java\r
for (int i = 0; i < 5; i++);\r
{\r
    System.out.println(i);\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
error: cannot find symbol — variable i\r
\r
The semicolon is a complete empty body, so the loop runs five times doing\r
nothing and the block below is a separate block that runs once. Here it does\r
not even compile, because i went out of scope with the loop.\r
\`\`\`\r
\r
### Mistake 4: Forgetting the semicolon in \`do-while\`\r
\r
\`\`\`java\r
do {\r
    ...\r
} while (i < 5)   // ERROR: needs a trailing semicolon\r
\`\`\`\r
\r
\`\`\`expected\r
error: ';' expected\r
\r
do-while is the only loop that ends in a semicolon.\r
\`\`\`\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`WhileDoWhileForLoops.java\`\r
\r
All four looping forms in one run, and the differences between them are exactly\r
where the condition sits:\r
\r
- **\`while\`** checks the condition **before** each pass. If it is already false,\r
  the body never runs at all.\r
- **\`do-while\`** runs the body **first** and checks afterwards, so it always runs\r
  at least once â€” even when the condition was false from the start.\r
- **\`for\`** is the same before-check as \`while\`, with the three parts â€”\r
  initialise, condition, update â€” gathered onto one line where you can see them\r
  together.\r
- **\`for-each\`** walks every element of an array or collection with no counter\r
  at all.\r
\r
\`\`\`java WhileDoWhileForLoops.java @run-loops-while-do-while-for-loops\r
public class WhileDoWhileForLoops {\r
\r
    public static void main(String[] args) {\r
\r
        System.out.print("while  : ");\r
        int i = 1;\r
        while (i <= 5) {\r
            System.out.print(i + " ");\r
            i++;\r
        }\r
        System.out.println();\r
\r
        System.out.print("do-while: ");\r
        int j = 1;\r
        do {\r
            System.out.print(j + " ");\r
            j++;\r
        } while (j <= 5);\r
        System.out.println();\r
\r
        System.out.print("for    : ");\r
        for (int k = 1; k <= 5; k++) {\r
            System.out.print(k + " ");\r
        }\r
        System.out.println();\r
\r
        System.out.print("for-each: ");\r
        int[] nums = { 10, 20, 30 };\r
        for (int n : nums) {\r
            System.out.print(n + " ");\r
        }\r
        System.out.println();\r
\r
        System.out.print("break  : ");\r
        for (int b = 1; b <= 10; b++) {\r
            if (b == 4) {\r
                break;\r
            }\r
            System.out.print(b + " ");\r
        }\r
        System.out.println();\r
\r
        System.out.print("continue: ");\r
        for (int c = 1; c <= 6; c++) {\r
            if (c % 2 == 0) {\r
                continue;\r
            }\r
            System.out.print(c + " ");\r
        }\r
        System.out.println();\r
\r
        System.out.println("nested :");\r
        for (int row = 1; row <= 3; row++) {\r
            for (int col = 1; col <= 3; col++) {\r
                System.out.print("* ");\r
            }\r
            System.out.println();\r
        }\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-loops-while-do-while-for-loops\r
while  : 1 2 3 4 5 \r
do-while: 1 2 3 4 5 \r
for    : 1 2 3 4 5 \r
for-each: 10 20 30 \r
break  : 1 2 3 \r
continue: 1 3 5 \r
nested :\r
* * * \r
* * * \r
* * *\r
\`\`\`\r
`,revision:"The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## Which loop\r\n\r\n- `while` checks first, so it may run **zero** times. `do-while` checks last, so\r\n  it always runs **at least once**.\r\n- `for` when the number of iterations is known or the counter is part of the\r\n  problem; `while` when the end condition is not a count; `for-each` to read\r\n  every element.\r\n- All three parts of a `for` header are optional. `for (;;)` is a deliberate\r\n  infinite loop.\r\n- `do-while` needs the semicolon after the closing `while (...)`. It is the only\r\n  loop that does.\r\n\r\n## `for-each` and its limits\r\n\r\n![The for-each variable is a copy, so writing to it changes nothing](diagrams/07-loops-revision-for-each-copy.jpg)\r\n\r\n- `for (int x : arr)` gives you a **copy** of each element, so assigning to the\r\n  loop variable changes nothing in the array.\r\n- It exposes no index, so you cannot know where you are, go backwards, or skip.\r\n- It cannot be used to remove from a collection while iterating — that throws\r\n  `ConcurrentModificationException`. Use an explicit `Iterator` or an index.\r\n\r\n## Leaving early\r\n\r\n![break leaves the innermost loop; a labelled break leaves both](diagrams/07-loops-revision-how-far-break-reaches.jpg)\r\n\r\n- `break` leaves the loop entirely. `continue` skips to the next iteration.\r\n- Both act on the **innermost** loop only. A labelled `break outer;` leaves an\r\n  outer loop from inside a nested one.\r\n- In a nested loop the inner body runs `outer × inner` times — the inner loop\r\n  restarts from the beginning on every pass of the outer.\r\n\r\n## Getting it wrong\r\n\r\n- Forgetting to update the variable the condition tests is an infinite loop, and\r\n  it compiles.\r\n- Off-by-one: `<` runs `n` times from zero, `<=` runs `n + 1`. Array indices go\r\n  from `0` to `length - 1`.\r\n- A stray semicolon — `for (...);` or `while (...);` — makes the loop body empty\r\n  and the block after it run once.\r\n",interview:"## Interview questions\n\n### Q1: What is the difference between `while` and `do-while`?\n\n**Answer:** A `while` loop checks the condition before the first iteration, so its body may run zero times. A `do-while` loop checks the condition after the body, so the body always runs at least once.\n\n### Q2: When would you use a `for` loop instead of a `while` loop?\n\n![One for header, five passes, and the condition failing on the sixth](diagrams/07-loops-interview-header-to-passes.svg)\n\n**Answer:** Use a `for` loop when the number of iterations is known or counter-based, because initialisation, condition, and update are grouped together. Use a `while` loop when you loop until some condition changes and the count is not known in advance.\n\n### Q3: What is the difference between `break` and `continue`?\n\n**Answer:** `break` exits the entire loop immediately. `continue` skips the remaining statements in the current iteration and proceeds to the next iteration.\n\n### Q4: What is an infinite loop and how does it happen?\n\n**Answer:** An infinite loop never ends because its condition never becomes false — usually because the update step is missing or wrong (e.g. forgetting `i++`). \n\n### Q5: What is the enhanced for-each loop, and what are its limits?\n\n**Answer:** It iterates over every element of an array or collection without an index: `for (int n : nums)`. It is concise but does not expose the index and cannot be used to replace elements by position or to iterate backward.\n\n### Q6: In a nested loop, how many times does the inner loop run?\n\n![One outer pass is a whole inner loop, so the body runs m times n](diagrams/07-loops-interview-nested-count.svg)\n\n**Answer:** The inner loop runs fully for each iteration of the outer loop. If the outer loop runs `m` times and the inner runs `n` times, the inner body executes `m × n` times in total.\n"}},{slug:`classes-and-objects`,title:`Classes and objects`,subject:`Java`,stage:`Classes and objects`,chapter:8,accent:`#c084fc`,summary:`Classes and objects are the two most fundamental building blocks of Object-Oriented Programming (OOP). Java is an object-oriented language, which means almost everything in Java revolves around classes and objects. If you do not understand classes and objects, you cannot progress further in Java.`,parts:{notes:`## Introduction\r
\r
Classes and objects are the two most fundamental building blocks of Object-Oriented Programming (OOP). Java is an object-oriented language, which means almost everything in Java revolves around classes and objects. If you do not understand classes and objects, you cannot progress further in Java.\r
\r
This program demonstrates how to create a simple \`Calculator\` class with a method, create an object of that class, and call the method using that object.\r
\r
---\r
\r
## What is a Class\r
\r
A class is a blueprint or template that defines the structure and behavior of objects. It describes what properties (variables) and actions (methods) an object will have, but it does not occupy any memory by itself.\r
\r
Think of a class like an architectural blueprint for a house. The blueprint describes the number of rooms, the size of windows, and the layout, but the blueprint itself is not a house. You build actual houses (objects) based on the blueprint (class).\r
\r
**Syntax:**\r
\r
\`\`\`java\r
class ClassName {\r
}\r
\`\`\`\r
\r
**Key Points:**\r
\r
- A class is declared using the \`class\` keyword.\r
- A class name should start with an uppercase letter (convention).\r
- A class can contain variables (also called fields or attributes) and methods.\r
- A class does not consume memory until an object is created from it.\r
\r
---\r
\r
## What is an Object\r
\r
An object is an instance of a class. When you create an object, Java allocates memory for it and initializes its properties. Each object has its own copy of the instance variables defined in the class.\r
\r
Using the house analogy: if the class is the blueprint, then each house built from that blueprint is an object. You can build multiple houses from the same blueprint, and each house can have different paint colors, furniture, and residents.\r
\r
**Syntax to create an object:**\r
\r
\`\`\`java\r
ClassName objectName = new ClassName();\r
\`\`\`\r
\r
**Breaking it down:**\r
\r
| Part                | Meaning                                                            |\r
|---------------------|--------------------------------------------------------------------|\r
| \`ClassName\`         | The type of the object (which class it belongs to)                |\r
| \`objectName\`        | The reference variable that points to the object in memory        |\r
| \`new\`               | A keyword that allocates memory for the object on the heap        |\r
| \`ClassName()\`       | The constructor call that initializes the object                  |\r
\r
---\r
\r
## Difference Between Class and Object\r
\r
![One class description, three objects, each with its own fields](diagrams/08-classes-and-objects-notes-one-class-many-objects.jpg)\r
\r
| Aspect            | Class                                    | Object                                    |\r
|-------------------|------------------------------------------|-------------------------------------------|\r
| Definition        | A blueprint or template                  | An instance of a class                    |\r
| Memory            | Does not occupy memory                   | Occupies memory when created              |\r
| Creation          | Defined using the \`class\` keyword        | Created using the \`new\` keyword           |\r
| Quantity          | Defined once                             | Can be created multiple times             |\r
| Contains          | Variable declarations and methods        | Actual values for those variables         |\r
| Real-world analogy| Recipe for a cake                        | The actual cake made from the recipe      |\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java Classes.java @run-classes-and-objects-classes\r
class Calculator {\r
\r
    int a;\r
\r
    public int add(int num1, int num2) {\r
        return num1 + num2;\r
    }\r
}\r
\r
public class Classes {\r
\r
    public static void main(String[] args) {\r
        int n1 = 2,\r
            n2 = 4;\r
\r
        Calculator calc = new Calculator();\r
\r
        int add = calc.add(n1, n2);\r
\r
        System.out.println(add);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-classes-and-objects-classes\r
6\r
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### \`class Calculator {\`\r
\r
- Declares a class named \`Calculator\`.\r
- This class is not \`public\`, which means it can only be accessed within the same file or package.\r
- This is the blueprint for creating Calculator objects.\r
\r
### \`int a;\`\r
\r
![Instance fields are given defaults, local variables are not](diagrams/08-classes-and-objects-notes-field-defaults.jpg)\r
\r
- This is an instance variable (also called a field or attribute).\r
- Every object of \`Calculator\` will have its own copy of \`a\`.\r
- Since it is an \`int\`, its default value is \`0\` if not explicitly assigned.\r
- In this program, \`a\` is declared but not used. It is there to show that a class can have properties.\r
\r
### \`public int add(int num1, int num2) {\`\r
\r
- **\`public\`**: This method can be accessed from outside the class.\r
- **\`int\`**: The return type. This method will return an integer value.\r
- **\`add\`**: The name of the method.\r
- **\`int num1, int num2\`**: These are parameters. When you call this method, you must pass two integer values.\r
\r
### \`return num1 + num2;\`\r
\r
- Adds \`num1\` and \`num2\` together and returns the result to the caller.\r
- The \`return\` keyword sends the value back and exits the method.\r
\r
### \`Calculator calc = new Calculator();\`\r
\r
- **\`Calculator calc\`**: Declares a reference variable \`calc\` of type \`Calculator\`.\r
- **\`new Calculator()\`**: Creates a new object of the \`Calculator\` class in heap memory.\r
- The reference variable \`calc\` now points to this newly created object.\r
\r
### \`int add = calc.add(n1, n2);\`\r
\r
- Calls the \`add\` method on the \`calc\` object, passing \`n1\` (2) and \`n2\` (4) as arguments.\r
- The method returns \`6\`, which is stored in the variable \`add\`.\r
- The dot operator (\`.\`) is used to access methods and properties of an object.\r
\r
### \`System.out.println(add);\`\r
\r
- Prints the value \`6\` to the console.\r
\r
---\r
\r
## How Object Creation Works in Memory\r
\r
When you write \`Calculator calc = new Calculator();\`, the following happens internally:\r
\r
1. **Stack Memory**: The reference variable \`calc\` is created on the stack. It holds the memory address (reference) of the actual object.\r
\r
2. **Heap Memory**: The \`new\` keyword allocates memory on the heap for the \`Calculator\` object. The instance variable \`a\` is initialized to its default value (\`0\` for \`int\`).\r
\r
3. **Reference Assignment**: The address of the newly created object on the heap is stored in the \`calc\` variable on the stack.\r
\r
![How Object Creation Works in Memory](diagrams/08-classes-and-objects-notes-f7a965b4.jpg)\r
\r
---\r
\r
## Methods in Java\r
\r
A method is a block of code that performs a specific task. Methods allow you to write reusable code.\r
\r
**Types of Methods:**\r
\r
| Type                     | Description                                                    | Example                           |\r
|--------------------------|----------------------------------------------------------------|-----------------------------------|\r
| Instance method          | Belongs to an object, requires object to call                  | \`calc.add(2, 4)\`                 |\r
| Static method            | Belongs to the class, does not require an object               | \`Math.max(2, 4)\`                 |\r
| Method with return value | Returns a value using the \`return\` keyword                     | \`public int add(int a, int b)\`   |\r
| Void method              | Does not return any value                                      | \`public void printHello()\`       |\r
\r
**Key Terminology:**\r
\r
- **Parameters**: Variables declared in the method definition. Example: \`int num1, int num2\` in \`add(int num1, int num2)\`.\r
- **Arguments**: Actual values passed when calling the method. Example: \`n1, n2\` in \`calc.add(n1, n2)\`.\r
- **Return Type**: The data type of the value the method sends back. If it returns nothing, use \`void\`.\r
\r
---\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac Classes.java\r
java Classes\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
6\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Trying to call an instance method without creating an object\r
\r
\`\`\`java\r
Calculator.add(2, 4);  // ERROR: add is not a static method\r
\`\`\`\r
\r
\`\`\`expected\r
error: non-static method add(int,int) cannot be referenced from a static context\r
\r
An instance method needs an object. Either construct one —\r
new Calculator().add(2, 4) — or declare add static.\r
\`\`\`\r
\r
You must create an object first and then call the method using that object.\r
\r
### Mistake 2: Forgetting the \`new\` keyword\r
\r
![Declaring a reference does not create an object](diagrams/08-classes-and-objects-notes-no-new-no-object.jpg)\r
\r
\`\`\`java\r
Calculator calc;\r
calc.add(2, 4);  // ERROR: calc is null, NullPointerException at runtime\r
\`\`\`\r
\r
\`\`\`expected\r
error: variable calc might not have been initialized\r
\r
A local has no default, so the compiler stops this one. Had calc been a\r
field it would have defaulted to null and thrown NullPointerException at\r
run time instead.\r
\`\`\`\r
\r
Declaring a reference variable does not create an object. You must use \`new\` to allocate memory.\r
\r
### Mistake 3: Mismatching return types\r
\r
\`\`\`java\r
public int add(int a, int b) {\r
    return;  // ERROR: must return an int value\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
error: incompatible types: missing return value\r
\r
A method with a return type must return a value on every path out of it.\r
\`\`\`\r
\r
If the return type is \`int\`, the method must return an integer value.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`Classes.java\`\r
\r
The smallest useful pair: a \`Calculator\` class that knows how to add, and a\r
\`main\` that creates one and asks it to.\r
\r
Two steps are worth separating in your head. \`new Calculator()\` builds an\r
**object** from the **class** â€” the class is the description, the object is the\r
thing. \`calc.add(2, 4)\` then calls a method on that object and gets \`6\` back.\r
\r
\`\`\`java Classes.java @run-classes-and-objects-classes\r
class Calculator {\r
\r
    int a;\r
\r
    public int add(int num1, int num2) {\r
        return num1 + num2;\r
    }\r
}\r
\r
public class Classes {\r
\r
    public static void main(String[] args) {\r
        int n1 = 2,\r
            n2 = 4;\r
\r
        Calculator calc = new Calculator();\r
\r
        int add = calc.add(n1, n2);\r
\r
        System.out.println(add);\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-classes-and-objects-classes\r
6\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## Class and object\r
\r
![Copying a reference copies the address, so two names reach one object](diagrams/08-classes-and-objects-revision-aliasing.jpg)\r
\r
- A **class** is the description; an **object** is one thing built from it. The\r
  class is written once and costs no memory; each \`new\` allocates on the heap.\r
- \`new\` does three things: allocates the object, runs the constructor, and hands\r
  back a reference to it.\r
- The reference lives on the stack, the object on the heap. Copying the reference\r
  copies the address, so two names can point at one object.\r
- One \`.java\` file may hold several classes but only **one public** class, and\r
  the file must be named after it.\r
\r
![A file may hold many classes but only one public class](diagrams/08-classes-and-objects-revision-one-public-class.jpg)\r
\r
## Fields, and what they start as\r
\r
- **Instance variables** get defaults: \`0\`, \`0.0\`, \`false\`, \`null\`. **Locals do\r
  not** — reading an uninitialised local is a compile error, not a surprise at\r
  run time.\r
- Each object has its own copy of every instance field. Change one object's and\r
  the others are untouched.\r
- The dot operator reaches a member through a reference. On a \`null\` reference it\r
  throws \`NullPointerException\` — the reference exists, the object does not.\r
\r
## Methods\r
\r
- A **parameter** is the name in the declaration; an **argument** is the value\r
  passed at the call.\r
- An instance method needs an object to call it on. Calling one from \`main\`\r
  without constructing anything is the commonest first error.\r
- Several methods may share a name if their **parameter lists** differ — that is\r
  overloading, and it is resolved at compile time.\r
- A method declaring a return type must return a value on **every** path,\r
  including the one after the last \`if\`.\r
`,interview:`## Interview questions

### Q1: What is the difference between a class and an object?

**Answer:** A class is a blueprint that defines properties and methods. An object is an instance of a class created at runtime. A class does not occupy memory, but an object does. You can create multiple objects from a single class, each with its own set of property values.

### Q2: What is the \`new\` keyword used for in Java?

![The four things new does, in order](diagrams/08-classes-and-objects-interview-what-new-does.jpg)

**Answer:** The \`new\` keyword is used to create a new object. It allocates memory on the heap for the object, initializes the instance variables to their default values, calls the constructor, and returns a reference to the newly created object.

### Q3: What are default values of instance variables in Java?

**Answer:**

| Data Type | Default Value |
|-----------|---------------|
| int       | 0             |
| float     | 0.0f          |
| double    | 0.0           |
| boolean   | false         |
| char      | '\\u0000'      |
| String    | null          |
| Object    | null          |

### Q4: What is the difference between a parameter and an argument?

**Answer:** A parameter is the variable declared in the method definition. An argument is the actual value passed to the method when it is called. For example, in \`public int add(int num1, int num2)\`, \`num1\` and \`num2\` are parameters. In \`calc.add(2, 4)\`, \`2\` and \`4\` are arguments.

### Q5: Can a class have multiple methods with the same name?

**Answer:** Yes, this is called method overloading. Multiple methods can have the same name as long as they have different parameter lists (different number of parameters, different types, or different order of types).

### Q6: What is the difference between stack and heap memory?

![What lives on the stack and what lives on the heap](diagrams/08-classes-and-objects-interview-stack-vs-heap.jpg)

**Answer:** Stack memory stores local variables and reference variables. It follows LIFO (Last In, First Out) order and is faster. Heap memory stores objects created with the \`new\` keyword. It is larger but slower. When a method finishes executing, its stack frame is removed, but objects on the heap persist until garbage collected.

### Q7: What happens if you do not initialize an instance variable?

**Answer:** Java automatically assigns default values to instance variables. For \`int\` it is \`0\`, for \`boolean\` it is \`false\`, and for object references it is \`null\`. However, local variables (declared inside a method) are NOT given default values and must be initialized before use.

### Q8: Can you have two public classes in the same Java file?

**Answer:** No. A Java source file can have only one public class, and the file name must match the name of that public class. You can have multiple non-public classes in the same file.
`}},{slug:`methods`,title:`Methods`,subject:`Java`,stage:`Classes and objects`,chapter:9,accent:`#60a5fa`,summary:`A method is a named block of code that performs a specific task. You define it once and call it whenever you need it. Methods make programs shorter, easier to read, and easier to fix, because the logic lives in one place.`,parts:{notes:`## Introduction

A **method** is a named block of code that performs a specific task. You define it once and call it whenever you need it. Methods make programs shorter, easier to read, and easier to fix, because the logic lives in one place.

This chapter has three programs:

| File | Topic |
|------|-------|
| \`MethodsBasics.java\`    | Declaring, calling, returning, \`void\` |
| \`MethodOverloading.java\`| Same name, different parameters (compile-time) |
| \`MethodOverriding.java\` | Child redefines a parent method (run-time) |

You already saw a method in Chapter 8 (\`Calculator.add\`). This chapter goes deeper.

---

## What is a Method

Instead of repeating the same code, you wrap it in a method and call it by name:

\`\`\`java
static int add(int a, int b) {
    return a + b;
}

int sum = add(5, 3);
\`\`\`

Benefits: **reuse** (write once, call many times), **readability** (a good name explains intent), and **maintainability** (fix a bug in one place).

---

## Anatomy of a Method

\`\`\`java
public static int add(int num1, int num2) {
    return num1 + num2;
}
\`\`\`

| Part            | Meaning                                                        |
|-----------------|----------------------------------------------------------------|
| Access modifier | Who can call it (\`public\`, \`private\`, …)                       |
| \`static\`        | Belongs to the class; callable without creating an object      |
| Return type     | The type of value returned, or \`void\` for nothing             |
| Method name     | What you call it (convention: starts lowercase, a verb)        |
| Parameters      | Inputs the method needs, in parentheses                        |
| Body            | The code that runs, in braces                                  |

---

## Parameters vs Arguments

- **Parameters** are the variables in the method *declaration*: \`add(int num1, int num2)\`.
- **Arguments** are the actual values you *pass* when calling: \`add(5, 3)\` — here \`5\` and \`3\` are arguments.

---

## Return Values and \`void\`

A method either returns a value or returns nothing:

\`\`\`java
static int square(int n) {
    return n * n;
}

static void greet(String name) {
    System.out.println("Hello, " + name);
}
\`\`\`

- If the return type is not \`void\`, the method **must** \`return\` a value of that type.
- \`return\` also immediately exits the method.
- A \`void\` method can still use a bare \`return;\` to exit early.

---

## Method Overloading

![The compiler picks an overload from the argument types alone](diagrams/09-methods-notes-overload-pick.jpg)

**Overloading** = multiple methods with the **same name** but **different parameter lists** (different number, types, or order). The compiler picks the right one based on the arguments. This is decided at **compile time** (also called *static* or *compile-time polymorphism*).

\`\`\`java
static int    add(int a, int b)        { return a + b; }
static int    add(int a, int b, int c) { return a + b + c; }
static double add(double a, double b)  { return a + b; }
static String add(int a, String b)     { return a + b; }
\`\`\`

\`\`\`java
add(2, 3);
add(2, 3, 4);
add(2.5, 3.5);
add(5, "th");
\`\`\`

> **The return type alone does not count.** Two methods that differ *only* by return type are a compile error. The **parameter lists** must differ.

---

## Method Overriding

![The reference type decides what compiles, the object decides what runs](diagrams/09-methods-notes-dynamic-dispatch.jpg)

**Overriding** = a **child class** provides its **own version** of a method already defined in its **parent**, with the **same name and same parameters**. Which version runs is decided at **run time** by the actual object — this is *runtime* (dynamic) polymorphism.

\`\`\`java
class Animal {
    void sound() { System.out.println("Some generic animal sound"); }
}

class Dog extends Animal {
    @Override
    void sound() { System.out.println("Woof"); }
}
\`\`\`

\`\`\`java
Animal a = new Dog();
a.sound();
\`\`\`

\`\`\`expected
Dog's version runs.

The reference type decides what you are allowed to call. The object decides
which version actually runs — that is dynamic dispatch.
\`\`\`

The \`@Override\` annotation is optional but recommended: the compiler will error if the method does not actually override anything (e.g. a typo in the name), catching bugs early.

> Overriding builds on **inheritance**, covered fully in Chapter 15, and underpins **polymorphism** in Chapter 17.

---

## Overloading vs Overriding

![Overloading is settled by javac, overriding by the JVM](diagrams/09-methods-notes-when-decided.jpg)

| Aspect            | Overloading                          | Overriding                          |
|-------------------|--------------------------------------|-------------------------------------|
| Method name       | Same                                 | Same                                |
| Parameter list    | **Different**                        | **Same**                            |
| Where             | Same class                           | Parent class and child class        |
| Inheritance       | Not required                         | Required (\`extends\`)                |
| Decided at        | Compile time (static)                | Run time (dynamic)                  |
| Also called       | Compile-time polymorphism            | Runtime polymorphism                |

---

## How to Compile and Run

\`\`\`bash
javac MethodsBasics.java
java MethodsBasics

javac MethodOverloading.java
java MethodOverloading

javac MethodOverriding.java
java MethodOverriding
\`\`\`

---

## Common Mistakes

### Mistake 1: Overloading by return type only

\`\`\`java
int  value() { return 1; }
double value() { return 1.0; } // ERROR: same name and parameters
\`\`\`

\`\`\`expected
error: method value() is already defined

The return type is not part of a method's signature, so these two are the
same method. Nothing at the call site could choose between them.
\`\`\`

The parameter lists must differ, not just the return type.

### Mistake 2: A non-void method that does not return on every path

\`\`\`java
static int max(int a, int b) {
    if (a > b) return a;
}
\`\`\`

\`\`\`expected
error: missing return statement

The compiler traces every path. When a is not greater than b, this one falls
off the end without returning.
\`\`\`

### Mistake 3: Thinking the reference type chooses the overridden method

\`\`\`java
Animal a = new Dog();
a.sound();
\`\`\`

\`\`\`expected
Dog's version runs.

The reference type decides what you are allowed to call. The object decides
which version actually runs — that is dynamic dispatch.
\`\`\`

### Mistake 4: Changing the parameter list when you meant to override

\`\`\`java
class Dog extends Animal {
    void sound(String s) { ... }
}
\`\`\`

\`\`\`expected
This is an overload, not an override.

The parameter list differs, so Animal.sound() is still what a.sound() calls
and the new method is never reached through an Animal reference.
@Override on it would have turned this into a compile error.
\`\`\`

Use \`@Override\` to catch this — the compiler will complain.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`MethodsBasics.java\`

A method is a named block of code you can call by name, which is what stops the
same lines being written out repeatedly. The file shows the three shapes you
will write most:

- one that **returns a value**, so its return type is \`int\` and the caller can
  store or print the result;
- one that returns **nothing**, declared \`void\` â€” it acts rather than answers;
- one with **no parameters** that still returns a value.

The last call shows a method used inside a larger expression, which works
because a call with a return type *is* a value.

\`\`\`java MethodsBasics.java @run-methods-methods-basics
public class MethodsBasics {

    static int add(int num1, int num2) {
        return num1 + num2;
    }

    static void greet(String name) {
        System.out.println("Hello, " + name + "!");
    }

    static double pi() {
        return 3.14159;
    }

    public static void main(String[] args) {

        int sum = add(5, 3);
        System.out.println("add(5, 3)  = " + sum);

        greet("Aditya");

        System.out.println("pi()       = " + pi());

        System.out.println("add nested = " + add(add(1, 2), 4));
    }
}

\`\`\`

\`\`\`output @run-methods-methods-basics
add(5, 3)  = 8
Hello, Aditya!
pi()       = 3.14159
add nested = 7
\`\`\`

### \`MethodOverloading.java\`

**Overloading** is several methods sharing one name but taking different
parameter lists. All four here are called \`add\`; they differ in how many
arguments they take and of what type.

That is enough for the compiler to pick the right one at the call site â€” the
choice is made from the arguments alone, before the program ever runs. Note what
is *not* enough: a different return type by itself does not overload anything.

\`\`\`java MethodOverloading.java @run-methods-method-overloading
public class MethodOverloading {

    static int add(int a, int b) {
        return a + b;
    }

    static int add(int a, int b, int c) {
        return a + b + c;
    }

    static double add(double a, double b) {
        return a + b;
    }

    static String add(int a, String b) {
        return a + b;
    }

    public static void main(String[] args) {
        System.out.println("add(2, 3)        = " + add(2, 3));
        System.out.println("add(2, 3, 4)     = " + add(2, 3, 4));
        System.out.println("add(2.5, 3.5)    = " + add(2.5, 3.5));
        System.out.println("add(5, \\"th\\")     = " + add(5, "th"));
    }
}

\`\`\`

\`\`\`output @run-methods-method-overloading
add(2, 3)        = 5
add(2, 3, 4)     = 9
add(2.5, 3.5)    = 6.0
add(5, "th")     = 5th
\`\`\`

### \`MethodOverriding.java\`

**Overriding** is a child class replacing a method it inherited from its parent.
Same name, same parameters â€” different body.

The three calls at the end are the point. The reference is typed \`Animal\` every
time, but the method that runs is the one belonging to the **object**, so a
\`Dog\` barks and a \`Cat\` meows. The reference type decides what you may call; the
object decides what actually happens.

\`\`\`java MethodOverriding.java @run-methods-method-overriding

class Animal {
    void sound() {
        System.out.println("Some generic animal sound");
    }
}

class Dog extends Animal {
    @Override
    void sound() {
        System.out.println("Woof");
    }
}

class Cat extends Animal {
    @Override
    void sound() {
        System.out.println("Meow");
    }
}

public class MethodOverriding {
    public static void main(String[] args) {

        Animal a1 = new Animal();
        Animal a2 = new Dog();
        Animal a3 = new Cat();

        a1.sound();
        a2.sound();
        a3.sound();
    }
}

\`\`\`

\`\`\`output @run-methods-method-overriding
Some generic animal sound
Woof
Meow
\`\`\`
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## Anatomy\r
\r
![A method can change what an object holds but cannot rebind the caller variable](diagrams/09-methods-revision-pass-by-value.jpg)\r
\r
- A method is a named block with a return type, a parameter list and a body. The\r
  name plus the parameter list is its **signature**; the return type is not part\r
  of it.\r
- \`void\` means it returns nothing. Any other return type must be returned on\r
  every path through the body — the compiler checks.\r
- Parameters are passed **by value**, always. For an object the value copied is\r
  the reference, so the method can change what the object contains but cannot\r
  rebind the caller's variable.\r
\r
## Overloading\r
\r
![An int argument prefers an exact match, then widening, then boxing](diagrams/09-methods-revision-most-specific.jpg)\r
\r
- Same name, **different parameter list** — different types, different count, or\r
  different order. Resolved by the compiler from the argument types.\r
- You cannot overload by return type alone. \`int f()\` and \`String f()\` do not\r
  compile together, because nothing at the call site would choose between them.\r
- The compiler picks the most specific applicable method, widening or boxing only\r
  if it has to. That is why an \`int\` argument prefers \`f(int)\` over \`f(long)\` and\r
  both over \`f(Integer)\`.\r
\r
## Overriding\r
\r
- Same name **and** same parameter list, in a subclass. Which one runs is decided\r
  at run time by the object's actual type, not by the type of the reference.\r
- \`@Override\` is optional and worth writing every time: it turns a typo that\r
  silently created a new overload into a compile error.\r
- Widening the return type to a subclass is allowed; narrowing the access\r
  modifier is not. An override cannot be less visible than what it overrides.\r
- Changing the parameter list when you meant to override gives you an overload\r
  instead, and the original still runs. This is the bug \`@Override\` exists for.\r
`,interview:`## Interview questions

### Q1: What is a method in Java?

**Answer:** A method is a named block of code that performs a task. It can take parameters as input and may return a value. Methods enable code reuse, readability, and easier maintenance.

### Q2: What is the difference between a parameter and an argument?

**Answer:** A parameter is the variable declared in the method definition; an argument is the actual value passed when the method is called. In \`add(int a, int b)\`, \`a\` and \`b\` are parameters; in \`add(5, 3)\`, \`5\` and \`3\` are arguments.

### Q3: What is method overloading?

**Answer:** Defining multiple methods with the same name but different parameter lists (number, type, or order). The compiler chooses which to call based on the arguments. It is resolved at compile time.

### Q4: What is method overriding?

**Answer:** A subclass provides its own implementation of a method already defined in its superclass, with the same name and parameter list. The version that runs is chosen at run time based on the actual object type.

### Q5: Can you overload a method by changing only the return type?

![The signature is the name and the parameter list, never the return type](diagrams/09-methods-interview-signature.jpg)

**Answer:** No. Overloaded methods must differ in their parameter lists. Two methods with the same name and parameters but different return types cause a compile error.

### Q6: What is the difference between overloading and overriding?

**Answer:** Overloading uses the same name with different parameters in the same class and is resolved at compile time (compile-time polymorphism). Overriding uses the same name and parameters across a parent and child class, requires inheritance, and is resolved at run time (runtime polymorphism).

### Q7: What does the \`@Override\` annotation do?

![A changed parameter list silently becomes an overload unless @Override is there](diagrams/09-methods-interview-override-annotation.jpg)

**Answer:** It tells the compiler the method is intended to override a superclass method. If it does not actually override one (for example due to a wrong name or parameter list), the compiler reports an error, which prevents subtle bugs.
`}},{slug:`arrays`,title:`Arrays`,subject:`Java`,stage:`Classes and objects`,chapter:10,accent:`#38bdf8`,summary:`Arrays are one of the most basic and widely used data structures in programming. In Java, an array is an object that stores a fixed number of values of the same data type in contiguous memory locations. Understanding arrays is essential because they form the foundation for more advanced data structures like ArrayLists, Stacks, and Queues.`,parts:{notes:`## Introduction\r
\r
Arrays are one of the most basic and widely used data structures in programming. In Java, an array is an object that stores a fixed number of values of the same data type in contiguous memory locations. Understanding arrays is essential because they form the foundation for more advanced data structures like ArrayLists, Stacks, and Queues.\r
\r
This chapter is split into four programs:\r
\r
| File | Topic |\r
|------|-------|\r
| \`Arrays.java\`            | Declaring, instantiating, and traversing a 1D array |\r
| \`EnhancedForLoop.java\` | The enhanced \`for-each\` loop over arrays |\r
| \`TwoDArray.java\`        | Two-dimensional (grid/matrix) arrays |\r
| \`JaggedArray.java\`      | Jagged arrays (rows of different lengths) |\r
\r
---\r
\r
## What is an Array\r
\r
An array is a container object that holds a fixed number of values of a single type. The length of an array is established when the array is created, and after creation, its length is fixed and cannot be changed.\r
\r
Think of an array as a row of numbered lockers in a school. Each locker (element) has a number (index) starting from 0, and each locker can hold exactly one item (value) of the same type.\r
\r
![What is an Array](diagrams/10-arrays-notes-de6dc4b3.jpg)\r
\r
**Key Characteristics:**\r
\r
- Arrays in Java are zero-indexed (the first element is at index 0).\r
- Arrays have a fixed size that cannot change after creation.\r
- All elements in an array must be of the same data type.\r
- Arrays in Java are objects, meaning they are stored on the heap.\r
- The \`length\` property tells you how many elements the array can hold.\r
\r
---\r
\r
## Why Do We Need Arrays\r
\r
Without arrays, if you wanted to store the marks of 100 students, you would need to declare 100 separate variables:\r
\r
\`\`\`java\r
int marks1 = 80;\r
int marks2 = 75;\r
int marks3 = 90;\r
\`\`\`\r
\r
This is impractical. With an array, you can store all 100 values in a single variable:\r
\r
\`\`\`java\r
int[] marks = new int[100];\r
\`\`\`\r
\r
Now you can access any student's marks using the index: \`marks[0]\`, \`marks[1]\`, \`marks[99]\`, and so on.\r
\r
---\r
\r
## Types of Arrays\r
\r
### 1. Single-Dimensional Array\r
\r
A simple list of elements.\r
\r
\`\`\`java\r
int[] numbers = {10, 20, 30, 40, 50};\r
\`\`\`\r
\r
### 2. Multi-Dimensional Array\r
\r
An array of arrays, commonly used to represent matrices or tables.\r
\r
\`\`\`java\r
int[][] matrix = {\r
    {1, 2, 3},\r
    {4, 5, 6},\r
    {7, 8, 9}\r
};\r
\`\`\`\r
\r
### 3. Jagged Array\r
\r
![A rectangular 2D array against a jagged one whose rows differ in length](diagrams/10-arrays-notes-jagged-shape.jpg)\r
\r
A multi-dimensional array where each row can have a different number of columns.\r
\r
\`\`\`java\r
int[][] jagged = new int[3][];\r
jagged[0] = new int[2];\r
jagged[1] = new int[4];\r
jagged[2] = new int[1];\r
\`\`\`\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java Arrays.java @run-arrays-arrays\r
public class Arrays {\r
\r
    public static void main(String[] args) {\r
        int nums[] = new int[5];\r
\r
        for (int i : nums) {\r
            System.out.println(i);\r
        }\r
\r
        System.out.println(nums + " Hi ");\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-arrays-arrays\r
0\r
0\r
0\r
0\r
0\r
[I@2b2fa4f7 Hi\r
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### \`int nums[] = new int[5];\`\r
\r
This single line does two things:\r
\r
1. **Declaration**: \`int nums[]\` declares a variable \`nums\` that can hold a reference to an integer array. Note: \`int[] nums\` and \`int nums[]\` are both valid syntaxes in Java, but \`int[] nums\` is the preferred Java style.\r
\r
2. **Instantiation**: \`new int[5]\` allocates memory on the heap for an array of 5 integers. All 5 elements are automatically initialized to the default value for \`int\`, which is \`0\`.\r
\r
After this line executes, the array looks like this:\r
\r
![int nums[] = new int[5];](diagrams/10-arrays-notes-814fab36.jpg)\r
\r
### \`for (int i : nums) {\`\r
\r
This is the **enhanced for-loop** (also called for-each loop), introduced in Java 5. It iterates over every element in the array without needing an index variable.\r
\r
- \`int i\` is a temporary variable that takes the value of each element in the array, one at a time.\r
- \`nums\` is the array being iterated over.\r
\r
In the first iteration, \`i = nums[0]\` which is \`0\`. In the second iteration, \`i = nums[1]\` which is \`0\`. And so on.\r
\r
Important: In the enhanced for-loop, \`i\` is NOT the index. It is the VALUE of the element at each position.\r
\r
### \`System.out.println(i);\`\r
\r
Prints the current value of \`i\` on each iteration. Since all elements are \`0\`, this prints \`0\` five times.\r
\r
### \`System.out.println(nums + " Hi ");\`\r
\r
This line is very instructive for understanding how arrays work in Java:\r
\r
- \`nums\` is a reference variable. When you try to print it directly, Java calls the \`toString()\` method inherited from the \`Object\` class.\r
- The default \`toString()\` method of an array returns a string in the format: \`[TypeCode@HashCode]\`. For example: \`[I@1b6d3586\`.\r
  - \`[I\` means it is an array of integers.\r
  - \`@1b6d3586\` is the hexadecimal representation of the object's hash code.\r
- \`+ " Hi "\` concatenates the string " Hi " to this representation.\r
- The output will be something like: \`[I@1b6d3586 Hi\`\r
\r
If you want to print the actual contents of the array, use \`Arrays.toString(nums)\`:\r
\r
\`\`\`java\r
import java.util.Arrays;\r
System.out.println(Arrays.toString(nums));\r
\`\`\`\r
\r
---\r
\r
## How Arrays Work in Memory\r
\r
![How Arrays Work in Memory](diagrams/10-arrays-notes-177013ae.jpg)\r
\r
- The reference variable \`nums\` is stored on the stack.\r
- The actual array data is stored on the heap.\r
- \`nums\` holds the memory address of the array on the heap.\r
- This is why printing \`nums\` directly shows a hash code instead of the array contents.\r
\r
---\r
\r
## Different Ways to Declare and Initialize Arrays\r
\r
\`\`\`java\r
int[] arr;\r
arr = new int[5];\r
\r
int[] arr = new int[5];\r
\r
int[] arr = {10, 20, 30, 40, 50};\r
\r
int[] arr = new int[]{10, 20, 30, 40, 50};\r
\r
int arr[] = new int[5];\r
\`\`\`\r
\r
---\r
\r
## Iterating Over Arrays\r
\r
### Method 1: Traditional for-loop\r
\r
\`\`\`java\r
for (int i = 0; i < nums.length; i++) {\r
    System.out.println(nums[i]);\r
}\r
\`\`\`\r
\r
Use this when you need the index value.\r
\r
### Method 2: Enhanced for-loop (for-each)\r
\r
\`\`\`java\r
for (int num : nums) {\r
    System.out.println(num);\r
}\r
\`\`\`\r
\r
Use this when you only need the values and do not care about the index.\r
\r
### Method 3: While loop\r
\r
\`\`\`java\r
int i = 0;\r
while (i < nums.length) {\r
    System.out.println(nums[i]);\r
    i++;\r
}\r
\`\`\`\r
\r
### Comparison\r
\r
| Feature              | Traditional for-loop | Enhanced for-loop |\r
|----------------------|---------------------|-------------------|\r
| Access to index      | Yes                 | No                |\r
| Can modify elements  | Yes                 | No (modifies local copy) |\r
| Readability          | Moderate            | High              |\r
| Risk of index errors | Yes                 | No                |\r
\r
---\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac Arrays.java\r
java Arrays\r
\r
javac EnhancedForLoop.java\r
java EnhancedForLoop\r
\r
javac TwoDArray.java\r
java TwoDArray\r
\r
javac JaggedArray.java\r
java JaggedArray\r
\`\`\`\r
\r
### Expected Output (\`Arrays\`)\r
\r
\`\`\`\r
0\r
0\r
0\r
0\r
0\r
[I@<some_hashcode> Hi\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: ArrayIndexOutOfBoundsException\r
\r
\`\`\`java\r
int[] arr = new int[5];\r
arr[5] = 10;  // ERROR: valid indices are 0 to 4\r
\`\`\`\r
\r
\`\`\`expected\r
Exception in thread "main" java.lang.ArrayIndexOutOfBoundsException:\r
    Index 5 out of bounds for length 5\r
\r
It compiles. Java checks every index at run time, so this is a crash rather\r
than silent memory corruption.\r
\`\`\`\r
\r
Arrays are zero-indexed, so an array of size 5 has indices 0 through 4.\r
\r
### Mistake 2: Confusing array size with last index\r
\r
\`\`\`java\r
int[] arr = new int[5];\r
\`\`\`\r
\r
### Mistake 3: Trying to resize an array\r
\r
![Growing an array means allocating a new one and copying](diagrams/10-arrays-notes-no-resize.jpg)\r
\r
\`\`\`java\r
int[] arr = new int[5];\r
\`\`\`\r
\r
If you need a resizable collection, use \`ArrayList\` instead.\r
\r
### Mistake 4: Comparing arrays with \`==\`\r
\r
![Two arrays with the same contents are still two different objects](diagrams/10-arrays-notes-identity-vs-value.jpg)\r
\r
\`\`\`java\r
int[] a = {1, 2, 3};\r
int[] b = {1, 2, 3};\r
System.out.println(a == b);\r
System.out.println(Arrays.equals(a, b));\r
\`\`\`\r
\r
\`\`\`expected\r
false\r
true\r
\r
== asks whether they are the same array. Arrays.equals asks whether they\r
hold the same values, which is nearly always the question.\r
\`\`\`\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`Arrays.java\`\r
\r
The smallest possible array program, and it makes two points. \`new int[5]\`\r
does not leave the slots empty â€” it fills them with the default for the type,\r
which for \`int\` is \`0\`, so the loop prints five zeros.\r
\r
The last line prints the array *reference* rather than its contents.\r
\`[I@1b6d3586\` is Java's type signature for "array of int", an \`@\`, and a hash\r
code that changes on every run. Use \`Arrays.toString(...)\` when you want the\r
values.\r
\r
\`\`\`java Arrays.java @run-arrays-arrays\r
public class Arrays {\r
\r
    public static void main(String[] args) {\r
        int nums[] = new int[5];\r
\r
        for (int i : nums) {\r
            System.out.println(i);\r
        }\r
\r
        System.out.println(nums + " Hi ");\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-arrays-arrays\r
0\r
0\r
0\r
0\r
0\r
[I@2b2fa4f7 Hi\r
\`\`\`\r
\r
### \`EnhancedForLoop.java\`\r
\r
The enhanced for loop â€” \`for (type variable : array)\` â€” reads each element in\r
order without an index variable. It is the right tool whenever the index itself\r
is not part of the job, and it works on arrays of objects (\`String[]\`) exactly\r
as it does on primitives.\r
\r
One limit worth knowing: the loop variable holds a **copy** of each value, so\r
assigning to it changes nothing in the array. To modify elements by position you\r
need an ordinary indexed \`for\` loop.\r
\r
\`\`\`java EnhancedForLoop.java @run-arrays-enhanced-for-loop\r
public class EnhancedForLoop {\r
\r
    public static void main(String[] args) {\r
\r
        int[] marks = { 85, 90, 75, 60, 95 };\r
\r
        int total = 0;\r
        System.out.print("marks: ");\r
        for (int mark : marks) {\r
            System.out.print(mark + " ");\r
            total += mark;\r
        }\r
        System.out.println();\r
        System.out.println("total: " + total);\r
        System.out.println("average: " + (total / marks.length));\r
\r
        String[] fruits = { "Apple", "Banana", "Cherry" };\r
        System.out.print("fruits: ");\r
        for (String fruit : fruits) {\r
            System.out.print(fruit + " ");\r
        }\r
        System.out.println();\r
\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-arrays-enhanced-for-loop\r
marks: 85 90 75 60 95 \r
total: 405\r
average: 81\r
fruits: Apple Banana Cherry\r
\`\`\`\r
\r
### \`TwoDArray.java\`\r
\r
A 2D array is an array of arrays â€” a grid, addressed with two indices as\r
\`matrix[row][col]\`. \`matrix.length\` gives the number of rows, and\r
\`matrix[i].length\` the number of columns in row \`i\`.\r
\r
The file builds one grid from literal values and prints it with nested loops,\r
then creates an empty \`new int[2][3]\` and fills two cells by hand â€” showing that\r
a 2D array can be declared with its contents or sized first and populated later.\r
\r
\`\`\`java TwoDArray.java @run-arrays-two-darray\r
public class TwoDArray {\r
\r
    public static void main(String[] args) {\r
\r
        int[][] matrix = {\r
            { 1, 2, 3 },\r
            { 4, 5, 6 },\r
            { 7, 8, 9 }\r
        };\r
\r
        System.out.println("rows: " + matrix.length + ", cols: " + matrix[0].length);\r
\r
        System.out.println("matrix[1][2] = " + matrix[1][2]);\r
\r
        System.out.println("grid:");\r
        for (int row = 0; row < matrix.length; row++) {\r
            for (int col = 0; col < matrix[row].length; col++) {\r
                System.out.print(matrix[row][col] + " ");\r
            }\r
            System.out.println();\r
        }\r
\r
        int[][] table = new int[2][3];\r
        table[0][0] = 10;\r
        table[1][2] = 99;\r
        System.out.println("table[0][0] = " + table[0][0] + ", table[1][2] = " + table[1][2]);\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-arrays-two-darray\r
rows: 3, cols: 3\r
matrix[1][2] = 6\r
grid:\r
1 2 3 \r
4 5 6 \r
7 8 9 \r
table[0][0] = 10, table[1][2] = 99\r
\`\`\`\r
\r
### \`JaggedArray.java\`\r
\r
A jagged array is a 2D array whose rows have **different** lengths. You create\r
the outer array with the column size left empty â€” \`new int[3][]\` â€” and then give\r
each row its own length.\r
\r
Because each row is its own array, \`jagged[row].length\` differs per row, and\r
that is what the printing loop uses. The \`direct\` array at the end shows the\r
same thing built from literals instead.\r
\r
\`\`\`java JaggedArray.java @run-arrays-jagged-array\r
public class JaggedArray {\r
\r
    public static void main(String[] args) {\r
\r
        int[][] jagged = new int[3][];\r
\r
        jagged[0] = new int[2];\r
        jagged[1] = new int[4];\r
        jagged[2] = new int[1];\r
\r
        for (int row = 0; row < jagged.length; row++) {\r
            for (int col = 0; col < jagged[row].length; col++) {\r
                jagged[row][col] = (row + 1) * (col + 1);\r
            }\r
        }\r
\r
        System.out.println("jagged array:");\r
        for (int row = 0; row < jagged.length; row++) {\r
            System.out.print("row " + row + " (len " + jagged[row].length + "): ");\r
            for (int value : jagged[row]) {\r
                System.out.print(value + " ");\r
            }\r
            System.out.println();\r
        }\r
\r
        int[][] direct = {\r
            { 1 },\r
            { 1, 2, 3 },\r
            { 1, 2 }\r
        };\r
        System.out.println("direct row 1 length: " + direct[1].length);\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-arrays-jagged-array\r
jagged array:\r
row 0 (len 2): 1 2 \r
row 1 (len 4): 2 4 6 8 \r
row 2 (len 1): 3 \r
direct row 1 length: 3\r
\`\`\`\r
`,revision:"The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## What an array is\r\n\r\n- A fixed-length, same-type container, allocated on the heap with `new`, and\r\n  **its length is decided once**. There is no resize — you allocate a new array\r\n  and copy.\r\n- `arr.length` is a field, not a method. `String.length()` is the method; the two\r\n  are easy to mix up and the compiler catches it.\r\n- Elements get the type's default on creation: `0`, `0.0`, `false`, `null`.\r\n- `int[] arr` and `int arr[]` mean the same thing. The first is the form to\r\n  write — the brackets belong to the type, not the name.\r\n\r\n## Indexing\r\n\r\n![Valid indices run from zero to length minus one; anything else throws](diagrams/10-arrays-revision-bounds.jpg)\r\n\r\n- Indices run `0` to `length - 1`. The size is not the last index, and confusing\r\n  the two is the classic off-by-one.\r\n- An index outside that range throws `ArrayIndexOutOfBoundsException` at run\r\n  time. Java checks every access; there is no reading past the end.\r\n- A negative index throws the same exception — there is no wrap-around.\r\n\r\n## Printing and comparing\r\n\r\n- `System.out.println(arr)` prints the type and hash, not the contents. Use\r\n  `Arrays.toString(arr)`, or `Arrays.deepToString` for nested arrays.\r\n- `==` on two arrays compares references. `Arrays.equals` compares contents, and\r\n  `Arrays.deepEquals` does it for nested arrays.\r\n- An **array** is fixed-length and can hold primitives. An **ArrayList** grows,\r\n  holds objects only, and costs boxing for numbers.\r\n- `for-each` reads every element and is the right default. Use an indexed `for`\r\n  when you need to write elements, go backwards, or know where you are.\r\n\r\n![A fixed primitive array against a growable ArrayList of objects](diagrams/10-arrays-revision-array-vs-arraylist.jpg)\r\n",interview:'## Interview questions\n\n### Q1: What is an array in Java?\n\n**Answer:** An array is a fixed-size, contiguous data structure that stores multiple values of the same data type. It is an object in Java, stored on the heap, and accessed via a reference variable on the stack. Elements are accessed using zero-based indices.\n\n### Q2: What is the default value of elements in an integer array?\n\n**Answer:** The default value is `0`. For `boolean` arrays, the default is `false`. For `double` and `float` arrays, the default is `0.0`. For object arrays (such as `String[]`), the default is `null`.\n\n### Q3: What is the difference between `int[] arr` and `int arr[]`?\n\n**Answer:** Both are valid syntaxes for declaring an array in Java. `int[] arr` is the preferred Java convention because it clearly indicates that `arr` is of type "integer array". `int arr[]` is the C/C++ style and is less commonly used in Java. Functionally, they are identical.\n\n### Q4: Can the size of an array be changed after creation?\n\n**Answer:** No. Once an array is created, its size is fixed and cannot be modified. If you need a dynamic-size collection, use `ArrayList` from the `java.util` package.\n\n### Q5: What is the difference between an array and an ArrayList?\n\n**Answer:**\n\n| Feature        | Array                    | ArrayList                 |\n|----------------|--------------------------|---------------------------|\n| Size           | Fixed                    | Dynamic (grows/shrinks)   |\n| Type           | Can hold primitives      | Can only hold objects     |\n| Performance    | Faster                   | Slightly slower           |\n| Methods        | Only `.length`           | Rich API (add, remove, etc.) |\n| Syntax         | `int[] arr = new int[5]` | `ArrayList<Integer> list = new ArrayList<>()` |\n\n### Q6: What happens when you print an array directly using `System.out.println(arr)`?\n\n![Reading the four parts of the string an array prints as](diagrams/10-arrays-interview-type-signature.jpg)\n\n**Answer:** It prints the class name followed by the `@` symbol and the hexadecimal hash code of the array object, such as `[I@1b6d3586`. The `[I` indicates it is an integer array. To print the actual contents, use `Arrays.toString(arr)`.\n\n### Q7: What is the enhanced for-loop and when should you use it?\n\n![What the enhanced for loop gives you and what it takes away](diagrams/10-arrays-interview-for-each-limits.jpg)\n\n**Answer:** The enhanced for-loop (for-each loop) is a simplified syntax introduced in Java 5 for iterating over arrays and collections. It should be used when you need to read all elements sequentially and do not need the index. It cannot be used to modify the original array elements or iterate in reverse.\n\n### Q8: What exception is thrown if you access an invalid array index?\n\n**Answer:** `ArrayIndexOutOfBoundsException`, which is a runtime exception. It is thrown when you try to access an index that is less than 0 or greater than or equal to the array\'s length.\n'}},{slug:`arrays-of-objects`,title:`Arrays of objects`,subject:`Java`,stage:`Classes and objects`,chapter:11,accent:`#34d399`,summary:`In the previous chapter, we worked with arrays that store primitive data types like int. But in real-world applications, we often need to store complex data -- for example, a student's roll number, name, and marks together as a single unit. This is where the combination of classes and arrays becomes powerful.`,parts:{notes:`## Introduction

In the previous chapter, we worked with arrays that store primitive data types like \`int\`. But in real-world applications, we often need to store complex data -- for example, a student's roll number, name, and marks together as a single unit. This is where the combination of classes and arrays becomes powerful.

This program demonstrates how to create a custom \`Student\` class with properties and then create multiple objects of that class, each with different values.

---

## What are Object Properties

Object properties (also called fields, attributes, or instance variables) are variables that belong to an object. They define the state or data that an object holds.

For example, a \`Student\` object might have the following properties:

- \`rollno\` -- the student's roll number
- \`name\` -- the student's name
- \`marks\` -- the student's marks

Each student object has its own independent copy of these properties. Changing the name of one student does not affect the name of another student.

---

## Understanding the Code

\`\`\`java ArraysProperties.java @run-arrays-of-objects-arrays-properties
class Student {

    int rollno;
    String name;
    float marks;

    public void printDetails() {
        System.out.println(rollno + ", " + name + ", " + marks);
    }
}

public class ArraysProperties {

    public static void main(String[] args) {
        Student s1 = new Student();
        s1.rollno = 1;
        s1.name = "Navin";
        s1.marks = 77;
        s1.printDetails();

        Student s2 = new Student();
        s2.rollno = 2;
        s2.name = "Aditya";
        s2.marks = 65;

        Student s3 = new Student();
        s3.rollno = 3;
        s3.name = "Tappu";
        s3.marks = 98;
    }
}
\`\`\`

\`\`\`output @run-arrays-of-objects-arrays-properties
1, Navin, 77.0
\`\`\`

---

## Line-by-Line Explanation

### The Student Class

#### \`class Student {\`

Declares a class named \`Student\`. This serves as the blueprint for creating student objects.

#### \`int rollno;\`

An instance variable of type \`int\` to store the student's roll number. Default value is \`0\`.

#### \`String name;\`

An instance variable of type \`String\` to store the student's name. Default value is \`null\` because \`String\` is a reference type.

#### \`float marks;\`

An instance variable of type \`float\` to store the student's marks. Default value is \`0.0f\`.

#### \`public void printDetails() {\`

A method that prints the student's details. It is \`void\` because it does not return any value. It simply prints output to the console.

#### \`System.out.println(rollno + ", " + name + ", " + marks);\`

This line uses string concatenation. The \`+\` operator, when used with a \`String\`, converts the other operands to their string representations and joins them together. So for a student with rollno=1, name="Navin", and marks=77, the output will be: \`1, Navin, 77.0\`.

Note that \`marks\` prints as \`77.0\` (not \`77\`) because it is a \`float\`.

### The Main Class

#### \`Student s1 = new Student();\`

Creates a new \`Student\` object and stores its reference in \`s1\`. At this point, all properties have their default values: \`rollno = 0\`, \`name = null\`, \`marks = 0.0\`.

#### \`s1.rollno = 1;\`

Uses the dot operator (\`.\`) to access the \`rollno\` property of the \`s1\` object and assigns it the value \`1\`. This is called direct field access.

#### \`s1.name = "Navin";\`

Assigns the string \`"Navin"\` to the \`name\` property of \`s1\`.

#### \`s1.marks = 77;\`

Assigns \`77\` to the \`marks\` property. Even though \`77\` is an integer literal, Java automatically converts it to \`77.0f\` because \`marks\` is of type \`float\` (widening conversion).

#### \`s1.printDetails();\`

Calls the \`printDetails()\` method on the \`s1\` object, which prints: \`1, Navin, 77.0\`.

#### Creating s2 and s3

The same process is repeated for \`s2\` and \`s3\`, but with different values. Notice that \`s2.printDetails()\` and \`s3.printDetails()\` are not called in this code, so their details will not be printed.

---

## How Multiple Objects Work in Memory

![How Multiple Objects Work in Memory](diagrams/11-arrays-of-objects-notes-9048ab8a.jpg)

Each object is completely independent. Changing \`s1.name\` has no effect on \`s2.name\` or \`s3.name\`.

---

## Creating an Array of Objects

![new Student[3] allocates three null slots, not three students](diagrams/11-arrays-of-objects-notes-array-of-nulls.jpg)

Instead of creating separate variables \`s1\`, \`s2\`, \`s3\`, you can create an array to hold all the student objects:

\`\`\`java
Student[] students = new Student[3];

students[0] = new Student();
students[0].rollno = 1;
students[0].name = "Navin";
students[0].marks = 77;

students[1] = new Student();
students[1].rollno = 2;
students[1].name = "Aditya";
students[1].marks = 65;

students[2] = new Student();
students[2].rollno = 3;
students[2].name = "Tappu";
students[2].marks = 98;
\`\`\`

Important: When you create \`new Student[3]\`, it creates an array of 3 references, all initialized to \`null\`. You must separately create each \`Student\` object using \`new Student()\` and assign it to the array element.

\`\`\`java
Student[] students = new Student[3];

students[0] = new Student();
\`\`\`

---

## Constructors vs Manual Assignment

In this code, properties are set manually one by one after creating the object. This is not the recommended approach for production code. A better approach is to use a constructor:

\`\`\`java
class Student {
    int rollno;
    String name;
    float marks;

    Student(int rollno, String name, float marks) {
        this.rollno = rollno;
        this.name = name;
        this.marks = marks;
    }
}

Student s1 = new Student(1, "Navin", 77);
\`\`\`

This is cleaner, less error-prone, and ensures that every student object is fully initialized upon creation.

![Manual field assignment leaves the object invalid for three statements](diagrams/11-arrays-of-objects-notes-half-built.jpg)

---

## How to Compile and Run

\`\`\`bash
javac ArraysProperties.java
java ArraysProperties
\`\`\`

### Expected Output

\`\`\`
1, Navin, 77.0
\`\`\`

Only \`s1.printDetails()\` is called, so only the first student's details are printed.

---

## Common Mistakes

### Mistake 1: Accessing properties on a null reference

\`\`\`java
Student s1;
s1.name = "Aditya";  // ERROR: NullPointerException (s1 is null)
\`\`\`

\`\`\`expected
error: variable s1 might not have been initialized

A local is never given a default, so this one is caught at compile time.
The same code with a field would compile and throw NullPointerException.
\`\`\`

You must create the object using \`new\` before accessing its properties.

### Mistake 2: Forgetting that object arrays contain null references

\`\`\`java
Student[] students = new Student[3];
students[0].name = "Aditya";  // ERROR: NullPointerException
\`\`\`

\`\`\`expected
Exception in thread "main" java.lang.NullPointerException:
    Cannot assign field "name" because "students[0]" is null

new Student[3] allocates the array, not the students. Every slot is null
until you put a new Student(...) in it.
\`\`\`

Creating the array only allocates space for references. You must create each object individually.

### Mistake 3: Confusing instance variables with local variables

Instance variables (declared in the class) have default values. Local variables (declared inside a method) do NOT have default values and must be initialized before use.

![Instance fields get defaults, locals get nothing at all](diagrams/11-arrays-of-objects-notes-defaults.jpg)

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`ArraysProperties.java\`

\`Student\` is an ordinary model class â€” three fields and a \`printDetails()\` that
prints them.

Read \`main\` carefully, because the output is smaller than it looks. Three
students are created and filled in, but only \`s1.printDetails()\` is called, so
only the first one prints. \`s2\` and \`s3\` exist in memory and say nothing â€”
creating an object and using it are two separate steps.

\`\`\`java ArraysProperties.java @run-arrays-of-objects-arrays-properties
class Student {

    int rollno;
    String name;
    float marks;

    public void printDetails() {
        System.out.println(rollno + ", " + name + ", " + marks);
    }
}

public class ArraysProperties {

    public static void main(String[] args) {
        Student s1 = new Student();
        s1.rollno = 1;
        s1.name = "Navin";
        s1.marks = 77;
        s1.printDetails();

        Student s2 = new Student();
        s2.rollno = 2;
        s2.name = "Aditya";
        s2.marks = 65;

        Student s3 = new Student();
        s3.rollno = 3;
        s3.name = "Tappu";
        s3.marks = 98;
    }
}

\`\`\`

\`\`\`output @run-arrays-of-objects-arrays-properties
1, Navin, 77.0
\`\`\`
`,revision:'The chapter, compressed. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## An array of objects holds references\n\n![Two array slots pointing at the same Student object](diagrams/11-arrays-of-objects-revision-two-slots-one-object.jpg)\n\n- `new Student[3]` allocates the **array**, not the students. All three slots are\n  `null` until each is assigned its own `new Student(...)`.\n- Forgetting that second step is the commonest cause of a\n  `NullPointerException` here: the array exists, the object in the slot does not.\n- Each element is a reference. Two slots may point at one object, and a change\n  through either is visible through both.\n\n## Instance versus local\n\n![Where a field lives versus where a local lives, and what each starts as](diagrams/11-arrays-of-objects-revision-field-versus-local.jpg)\n\n- **Instance variables** belong to the object, live on the heap, and get\n  defaults: `0`, `0.0`, `false`, `null`.\n- **Locals** belong to one call, live on the stack, and have no default at all —\n  reading one before assigning is a compile error.\n- A `String` field defaults to `null`, not to `""`. Calling a method on it throws.\n\n## Reaching in\n\n- The dot operator goes through a reference to a member. On `null` it throws\n  `NullPointerException` — the message names the field it tried to read.\n- `+` with a String on either side concatenates, and converts the other operand\n  by calling `toString` (or printing the primitive). A `double` field prints as\n  `77.0` rather than `77`, because that is what the value is.\n- Chained access — `arr[0].name.length()` — throws at the first `null` link, so\n  keep the chain short when any part of it may be absent.\n',interview:'## Interview questions\n\n### Q1: What is the difference between instance variables and local variables?\n\n**Answer:**\n\n| Feature           | Instance Variables                     | Local Variables                       |\n|-------------------|----------------------------------------|---------------------------------------|\n| Declared in       | Class body, outside methods            | Inside a method, constructor, or block|\n| Default value     | Yes (0, false, null, etc.)             | No (must be initialized)             |\n| Scope             | Entire class                           | Only within the declaring block       |\n| Stored in         | Heap (as part of the object)           | Stack                                 |\n| Access modifiers  | Can have (public, private, etc.)       | Cannot have access modifiers          |\n\n### Q2: What is NullPointerException and when does it occur?\n\n![A chained access throwing at the first null link](diagrams/11-arrays-of-objects-interview-where-npe-fires.jpg)\n\n**Answer:** `NullPointerException` is a runtime exception that occurs when you try to use a reference variable that points to `null` (no object). Common causes include calling a method on a null object, accessing a property of a null object, or trying to use an element of an object array that has not been initialized.\n\n### Q3: What is the default value of a String instance variable?\n\n![null is no object at all; the empty string is a real object](diagrams/11-arrays-of-objects-interview-null-versus-empty.jpg)\n\n**Answer:** The default value is `null`, not an empty string (`""`). `null` means the variable does not point to any `String` object in memory. An empty string is a valid `String` object that happens to have zero characters.\n\n### Q4: Can you create an array of objects in Java?\n\n**Answer:** Yes. You can create an array that holds references to objects. However, creating the array only creates space for references (initialized to `null`). You must separately create each object using the `new` keyword and assign it to the corresponding array index.\n\n### Q5: What is the dot operator (`.`) used for in Java?\n\n**Answer:** The dot operator is used to access members (properties and methods) of an object. For example, `s1.name` accesses the `name` property of the object referenced by `s1`, and `s1.printDetails()` calls the `printDetails()` method on that object.\n\n### Q6: What is string concatenation and how does the `+` operator work with Strings?\n\n**Answer:** String concatenation is the process of joining two or more strings together. When the `+` operator is used and at least one operand is a `String`, Java converts the other operand to its `String` representation and joins them. For example, `1 + ", " + "Navin"` produces `"1, Navin"`. Internally, Java uses `StringBuilder` for this conversion.\n\n### Q7: Why does `marks` print as `77.0` instead of `77`?\n\n**Answer:** Because `marks` is declared as `float`. When a `float` is converted to a `String` for printing, Java always includes the decimal point. Even though the value is a whole number, it is stored as a floating-point number (`77.0`), and that is how it is displayed.\n'}},{slug:`strings`,title:`Strings and StringBuilder`,subject:`Java`,stage:`Classes and objects`,chapter:12,accent:`#f97316`,summary:`Text is everywhere in programs — names, messages, file contents. In Java, text is handled by the String class. Strings are so important they get special syntax (double quotes) and their own memory area (the String Pool, from Chapter 2). This chapter covers Strings and their mutable companion, StringBuilder.`,parts:{notes:`## Introduction

Text is everywhere in programs — names, messages, file contents. In Java, text is handled by the **\`String\`** class. Strings are so important they get special syntax (double quotes) and their own memory area (the String Pool, from Chapter 2). This chapter covers Strings and their **mutable** companion, **\`StringBuilder\`**.

This chapter has two programs:

| File | Topic |
|------|-------|
| \`StringDemo.java\`         | The \`String\` class, methods, immutability |
| \`StringBuilderDemo.java\` | \`StringBuilder\` for efficient, mutable text |

---

## What is a String

A \`String\` is a sequence of characters. It is a **class** (a reference type), not a primitive, written between double quotes:

\`\`\`java
String name = "Java";
\`\`\`

Each character has an index starting at 0, just like an array:

![What is a String](diagrams/12-strings-notes-c5822e5d.jpg)

---

## Creating Strings

![Literals share one pooled object; new String always allocates another](diagrams/12-strings-notes-pool-versus-new.jpg)

\`\`\`java
String a = "Hello";
String b = new String("Hello");
\`\`\`

- A **literal** (\`"Hello"\`) is placed in the **String Pool** and reused if the same text already exists there.
- \`new String("Hello")\` **forces** a separate object on the heap, even if the same text is pooled. (See Chapter 2 for the String Pool.)

---

## String Immutability

![toUpperCase returns a new String; the variable only moves if you assign it](diagrams/12-strings-notes-immutable.jpg)

**Strings are immutable** — once created, their characters can never be changed. Any method that appears to modify a String actually returns a **new** String, leaving the original untouched.

\`\`\`java
String text = "Java Programming";
text.toUpperCase();
System.out.println(text);

String upper = text.toUpperCase();
System.out.println(upper);
\`\`\`

**Why immutable?** It makes Strings safe to share (including across threads), allows the String Pool to reuse them, and makes them reliable as keys in collections.

---

## Common String Methods

For \`String text = "Java Programming";\`

| Method                | Purpose                            | Result            |
|-----------------------|------------------------------------|-------------------|
| \`length()\`            | number of characters               | \`16\`              |
| \`toUpperCase()\`       | upper-case copy                    | \`"JAVA PROGRAMMING"\` |
| \`toLowerCase()\`       | lower-case copy                    | \`"java programming"\` |
| \`charAt(5)\`           | character at an index              | \`'P'\`             |
| \`indexOf("Prog")\`     | index of a substring (or -1)       | \`5\`               |
| \`substring(0, 4)\`     | part of the string \`[start, end)\`  | \`"Java"\`          |
| \`replace('a', 'A')\`   | replace characters                 | \`"JAvA ProgrAmming"\` |
| \`contains("Java")\`    | does it contain the text?          | \`true\`            |
| \`trim()\`              | remove leading/trailing spaces     | \`"spaced"\`        |

> Every one of these returns a **new** String. The original \`text\` is never changed.

---

## Comparing Strings

\`\`\`java
String a = "Hello";
String b = new String("Hello");

a == b
a.equals(b)
\`\`\`

- Use **\`.equals()\`** to compare text.
- Use **\`.equalsIgnoreCase()\`** to ignore upper/lower case.
- Avoid **\`==\`** for text; it compares object identity, not content.

---

## Why StringBuilder Exists

![Characters copied by += in a loop against StringBuilder](diagrams/12-strings-notes-quadratic-concat.jpg)

Because String is immutable, building text with \`+\` in a loop creates a **new** String on every step, which is wasteful:

\`\`\`java
String result = "";
for (int i = 0; i < 1000; i++) {
    result += i;
}
\`\`\`

\`StringBuilder\` is **mutable**: it changes its internal buffer in place, with no throwaway objects:

\`\`\`java
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 1000; i++) {
    sb.append(i);
}
String result = sb.toString();
\`\`\`

---

## StringBuilder Methods

Starting from \`new StringBuilder("Hello")\`:

| Method                | Effect                                  | Result after call |
|-----------------------|-----------------------------------------|-------------------|
| \`append(" World")\`    | add to the end                          | \`Hello World\`     |
| \`insert(5, ",")\`      | insert at an index                      | \`Hello, World\`    |
| \`replace(0, 5, "Hi")\` | replace chars in range \`[start, end)\`   | \`Hi, World\`       |
| \`deleteCharAt(2)\`     | remove one character                    | \`Hi World\`        |
| \`reverse()\`           | reverse the whole sequence              | \`dlroW iH\`        |
| \`length()\`            | current length                          | \`8\`               |
| \`toString()\`          | convert back to an immutable \`String\`   | \`"Hi World"\`      |

(The table follows the exact sequence of calls in \`StringBuilderDemo.java\`.)

---

## String vs StringBuilder vs StringBuffer

| Feature        | \`String\`              | \`StringBuilder\`         | \`StringBuffer\`            |
|----------------|-----------------------|-------------------------|---------------------------|
| Mutable?       | No (immutable)        | Yes                     | Yes                       |
| Thread-safe?   | Yes (because immutable)| No                     | Yes (synchronised)        |
| Speed          | Slow for many edits   | **Fastest** for edits   | Slower than StringBuilder |
| Use when       | Text rarely changes   | Building text in one thread | Building text across threads |

> Rule of thumb: use \`String\` for fixed text, \`StringBuilder\` when you build or modify text repeatedly, and \`StringBuffer\` only when multiple threads share the buffer.

---

## How to Compile and Run

\`\`\`bash
javac StringDemo.java
java StringDemo

javac StringBuilderDemo.java
java StringBuilderDemo
\`\`\`

---

## Common Mistakes

### Mistake 1: Comparing text with \`==\`

\`\`\`java
if (name == "Java") { ... }
if (name.equals("Java")) { ... }  // correct: compares characters
\`\`\`

\`\`\`expected
== is true only when name is the pooled literal itself, so it appears to
work in testing and fails on a String read from input or built at run time.
.equals is true whenever the characters match.
\`\`\`

### Mistake 2: Expecting a String method to change the original

\`\`\`java
String s = "hello";
s.toUpperCase();
s = s.toUpperCase();        // correct: reassign to keep the result
\`\`\`

\`\`\`expected
After s.toUpperCase(); on its own, s is still "hello".
After s = s.toUpperCase();      s is "HELLO".

A String never changes. Every method that looks like an edit returns a new
String and leaves the original exactly as it was.
\`\`\`

### Mistake 3: Building large text with \`+\` in a loop

\`\`\`java
String r = "";
for (...) r += x;
\`\`\`

\`\`\`expected
Correct, and quadratic.

Each += builds a whole new String from both sides, so appending n times
copies roughly n²/2 characters. Use a StringBuilder and one toString at
the end.
\`\`\`

### Mistake 4: \`substring\` end index confusion

\`\`\`java
"Java".substring(0, 4);
"Java".substring(1);
\`\`\`

\`\`\`expected
"Java".substring(0, 4)  ->  "Java"
"Java".substring(1)     ->  "ava"

The end index is exclusive, so the length is end - start and passing the
full length is legal rather than an error.
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`StringDemo.java\`

A \`String\` is a sequence of characters and, crucially, **immutable** â€” every
method that looks like it edits one actually returns a new \`String\` and leaves
the original untouched.

The file creates a string both ways, runs the common methods over it, and then
prints the original again. That last print is the proof: after all the calls,
the source string still reads exactly as it did.

Concatenation is no exception â€” joining two strings produces a third rather than
extending either.

\`\`\`java StringDemo.java @run-strings-string-demo
public class StringDemo {

    public static void main(String[] args) {

        String a = "Hello";
        String b = new String("Hello");

        System.out.println("a == b        : " + (a == b));
        System.out.println("a.equals(b)   : " + a.equals(b));

        String text = "Java Programming";

        System.out.println("length        : " + text.length());
        System.out.println("toUpperCase   : " + text.toUpperCase());
        System.out.println("toLowerCase   : " + text.toLowerCase());
        System.out.println("charAt(5)     : " + text.charAt(5));
        System.out.println("indexOf 'Prog': " + text.indexOf("Prog"));
        System.out.println("substring(0,4): " + text.substring(0, 4));
        System.out.println("replace a->A  : " + text.replace('a', 'A'));
        System.out.println("contains Java : " + text.contains("Java"));
        System.out.println("trim          : '" + "  spaced  ".trim() + "'");

        System.out.println("original text : " + text);

        String greet = "Hi, " + "there";
        System.out.println("concatenated  : " + greet);
    }
}

\`\`\`

\`\`\`output @run-strings-string-demo
a == b        : false
a.equals(b)   : true
length        : 16
toUpperCase   : JAVA PROGRAMMING
toLowerCase   : java programming
charAt(5)     : P
indexOf 'Prog': 5
substring(0,4): Java
replace a->A  : JAvA ProgrAmming
contains Java : true
trim          : 'spaced'
original text : Java Programming
concatenated  : Hi, there
\`\`\`

### \`StringBuilderDemo.java\`

\`StringBuilder\` is the mutable companion to \`String\`. \`append\`, \`insert\`,
\`replace\` and \`delete\` change the object **in place** rather than handing back a
new one, and \`toString()\` converts it to an ordinary \`String\` when you are done.

The reason it exists is the loop at the end. Because \`String\` is immutable,
building one with \`+\` inside a loop creates a fresh object on every pass and
throws the previous one away. A \`StringBuilder\` builds the same text in one
object.

\`\`\`java StringBuilderDemo.java @run-strings-string-builder-demo
public class StringBuilderDemo {

    public static void main(String[] args) {

        StringBuilder sb = new StringBuilder("Hello");

        sb.append(" World");
        System.out.println("after append : " + sb);

        sb.insert(5, ",");
        System.out.println("after insert : " + sb);

        sb.replace(0, 5, "Hi");
        System.out.println("after replace: " + sb);

        sb.deleteCharAt(2);
        System.out.println("after delete : " + sb);

        sb.reverse();
        System.out.println("after reverse: " + sb);

        sb.reverse();
        System.out.println("length       : " + sb.length());

        String result = sb.toString();
        System.out.println("toString     : " + result);

        StringBuilder csv = new StringBuilder();
        for (int i = 1; i <= 5; i++) {
            csv.append(i);
            if (i < 5) {
                csv.append(",");
            }
        }
        System.out.println("built in loop: " + csv);
    }
}

\`\`\`

\`\`\`output @run-strings-string-builder-demo
after append : Hello World
after insert : Hello, World
after replace: Hi, World
after delete : Hi World
after reverse: dlroW iH
length       : 8
toString     : Hi World
built in loop: 1,2,3,4,5
\`\`\`
`,revision:'The chapter, compressed. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## Immutable, and why it matters\n\n- A `String` never changes. Every method that looks like it edits one returns a\n  **new** String and leaves the original alone.\n- `s.toUpperCase();` on its own does nothing observable. You have to assign the\n  result.\n- Immutability is what makes literals safe to share in the pool, safe to use as\n  map keys, and safe to hand between threads without locking.\n\n## Comparing\n\n![Compile-time folding is pooled; run-time concatenation is not](diagrams/12-strings-revision-folded-or-built.jpg)\n\n- `==` compares references. `.equals()` compares characters. For text you want\n  `.equals()`, or `.equalsIgnoreCase()`.\n- Literals are pooled, so `"a" == "a"` is true. `new String("a") == "a"` is\n  false — `new` deliberately allocates outside the pool.\n- `"Ja" + "va" == "Java"` is **true**: two literals folded by the compiler.\n  `part + "va" == "Java"` is **false**: built at run time and never pooled.\n- `compareTo` returns the difference in the first differing character, negative,\n  zero or positive — it is for ordering, not for equality.\n\n## Building text\n\n- `+` in a loop allocates a new String every iteration, so the work is quadratic\n  in the length. Use `StringBuilder` and one `toString` at the end.\n- `StringBuilder` is mutable and **not** synchronised; `StringBuffer` is the\n  synchronised version and slower. Prefer `StringBuilder` unless several threads\n  really share one buffer.\n- The compiler already rewrites a single `a + b + c` into a `StringBuilder`. It\n  is the loop it cannot rewrite, because each iteration is its own expression.\n\n## Common methods\n\n![substring takes start inclusive and end exclusive](diagrams/12-strings-revision-substring-bounds.jpg)\n\n- `substring(a, b)` takes characters from `a` up to **but not including** `b`, so\n  the length is `b - a`. `substring(a)` runs to the end.\n- `charAt`, `indexOf` and `substring` all throw on an index outside the string;\n  `indexOf` returns `-1` when it simply does not find anything.\n- `trim()` removes ASCII whitespace only; `strip()` understands Unicode. Prefer\n  `strip()` in new code.\n- `split` takes a **regular expression**, not a literal — splitting on `"."`\n  matches every character unless you escape it.\n',interview:'## Interview questions\n\n### Q1: Why are Strings immutable in Java?\n\n**Answer:** Once created, a String\'s contents cannot change. Immutability allows safe sharing (including between threads), enables the String Pool to reuse literals, and makes Strings reliable as keys in hash-based collections. Methods that seem to modify a String return a new one instead.\n\n### Q2: What is the difference between `==` and `.equals()` for Strings?\n\n![Two distinct objects holding the same characters](diagrams/12-strings-interview-identity-versus-content.jpg)\n\n**Answer:** `==` compares references (whether two variables point to the same object), while `.equals()` compares the actual character contents. To compare text, always use `.equals()`.\n\n### Q3: What is the difference between `String` and `StringBuilder`?\n\n**Answer:** `String` is immutable, so each modification creates a new object. `StringBuilder` is mutable and modifies its internal buffer in place, which is far more efficient when building or repeatedly changing text.\n\n### Q4: What is the difference between `StringBuilder` and `StringBuffer`?\n\n**Answer:** Both are mutable. `StringBuffer` is synchronised (thread-safe) but slower; `StringBuilder` is not synchronised and is faster. Use `StringBuilder` for single-threaded code and `StringBuffer` only when a buffer is shared across threads.\n\n### Q5: What happens with `String s = "Hi"; s = s + " there";`?\n\n![Concatenation makes a second object and leaves the first as garbage](diagrams/12-strings-interview-reassign-not-edit.jpg)\n\n**Answer:** The original `"Hi"` is not changed. A new String `"Hi there"` is created and `s` is reassigned to point to it. The old object becomes eligible for garbage collection if nothing else references it.\n\n### Q6: Why is concatenating Strings with `+` in a loop discouraged?\n\n**Answer:** Because String is immutable, each `+=` creates a new String and copies the old contents, which is O(n²) work over a loop. A `StringBuilder` modifies one buffer in place, making it much faster.\n\n### Q7: What does `substring(a, b)` return?\n\n**Answer:** The portion of the String from index `a` (inclusive) to index `b` (exclusive). For example, `"Java".substring(0, 4)` returns `"Java"`, and `"Java".substring(1)` returns `"ava"`.\n'}},{slug:`encapsulation`,title:`Encapsulation`,subject:`Java`,stage:`Classes and objects`,chapter:13,accent:`#a78bfa`,summary:`Encapsulation is one of the four fundamental principles of Object-Oriented Programming (OOP), along with Inheritance, Polymorphism, and Abstraction. It is the mechanism of bundling data (variables) and the methods that operate on that data into a single unit (class), while restricting direct access to the internal data from outside the class.`,parts:{notes:`## Introduction

Encapsulation is one of the four fundamental principles of Object-Oriented Programming (OOP), along with Inheritance, Polymorphism, and Abstraction. It is the mechanism of bundling data (variables) and the methods that operate on that data into a single unit (class), while restricting direct access to the internal data from outside the class.

This program demonstrates how to make a variable private and provide controlled access to it through getter and setter methods.

---

## What is Encapsulation

Encapsulation means wrapping the data (variables) and code (methods) together as a single unit and controlling who can access what. The core idea is simple: hide the internal details and expose only what is necessary.

In Java, encapsulation is achieved by:

1. Declaring the instance variables of a class as \`private\`.
2. Providing \`public\` getter and setter methods to access and modify those variables.

Think of it like an ATM machine. You can withdraw money, check your balance, and deposit money (public methods). But you cannot directly access the vault, modify the database, or see the internal circuitry (private data). The ATM provides a controlled interface to interact with your account.

---

## Why Do We Need Encapsulation

![A valid value passes the setter; an invalid one is stopped at it](diagrams/13-encapsulation-notes-setter-is-a-gate.jpg)

### Problem Without Encapsulation

\`\`\`java
class Human {
    int age;
}

Human person = new Human();
person.age = -500;
\`\`\`

Without encapsulation, anyone can set \`age\` to an invalid value like \`-500\`. There is no validation, no control, and no way to enforce business rules.

### Solution With Encapsulation

\`\`\`java
class Human {
    private int age;

    public void setAge(int age) {
        if (age > 0 && age < 150) {
            this.age = age;
        } else {
            System.out.println("Invalid age");
        }
    }

    public int getAge() {
        return age;
    }
}
\`\`\`

Now the \`age\` variable is \`private\`, and the only way to set it is through the \`setAge\` method, which can include validation logic.

### Benefits of Encapsulation

| Benefit                | Description                                                      |
|------------------------|------------------------------------------------------------------|
| Data Hiding            | Internal data is hidden from outside classes                     |
| Data Validation        | Setter methods can validate data before assignment               |
| Flexibility            | Internal implementation can change without affecting external code|
| Read-Only/Write-Only   | You can create properties that are only readable or only writable|
| Maintainability        | Code is easier to maintain and debug                             |

---

## Access Modifiers in Java

![The four access modifiers as widening rings of visibility](diagrams/13-encapsulation-notes-access-rings.jpg)

Access modifiers control the visibility of classes, methods, and variables. Java has four access modifiers:

| Modifier    | Same Class | Same Package | Subclass (different package) | Everywhere |
|-------------|:----------:|:------------:|:----------------------------:|:----------:|
| \`private\`   | Yes        | No           | No                           | No         |
| (default)   | Yes        | Yes          | No                           | No         |
| \`protected\` | Yes        | Yes          | Yes                          | No         |
| \`public\`    | Yes        | Yes          | Yes                          | Yes        |

- **\`private\`**: Most restrictive. Accessible only within the same class. Used for encapsulation.
- **\`default\`** (no keyword): Accessible within the same package.
- **\`protected\`**: Accessible within the same package and by subclasses in other packages.
- **\`public\`**: Accessible from everywhere.

---

## Getters and Setters

### What is a Getter?

A getter (also called an accessor) is a public method that returns the value of a private variable. The naming convention is \`getVariableName()\`.

\`\`\`java
public int getAge() {
    return age;
}
\`\`\`

### What is a Setter?

A setter (also called a mutator) is a public method that sets or updates the value of a private variable. The naming convention is \`setVariableName(value)\`.

\`\`\`java
public void setAge(int age) {
    this.age = age;
}
\`\`\`

### Naming Convention

For a property named \`propertyName\`:

- Getter: \`getPropertyName()\` (for boolean: \`isPropertyName()\`)
- Setter: \`setPropertyName(type value)\`

This convention is called the JavaBeans naming convention and is used extensively in frameworks like Spring and Hibernate.

---

## Understanding the Code

\`\`\`java Encapsulation.java @run-encapsulation-encapsulation
class Human {

    private int age;

    public int getAge() {
        return age;
    }

    public void setAge(int age, Human obj) {
        Human obj1 = obj;
        obj1.age = age;
    }
}

public class Encapsulation {

    public static void main(String[] args) {

        Human obj = new Human();
        obj.setAge(4, obj);
        System.out.println(obj.getAge());
    }
}
\`\`\`

\`\`\`output @run-encapsulation-encapsulation
4
\`\`\`

Note: The \`setAge\` method in this code has an unconventional design for learning purposes. In real-world code, you would not pass the object as a parameter to its own setter.

---

## Line-by-Line Explanation

### \`private int age;\`

- Declares an instance variable \`age\` as \`private\`.
- This means \`age\` cannot be accessed directly from outside the \`Human\` class.
- Any attempt like \`obj.age = 5;\` from another class will result in a compilation error.

### \`public int getAge() { return age; }\`

- A public getter method that returns the value of the private \`age\` variable.
- This is the only way for outside code to read the value of \`age\`.
- The return type is \`int\` because \`age\` is an \`int\`.

### \`public void setAge(int age, Human obj) {\`

- A setter method that accepts two parameters: the new age value and a reference to a \`Human\` object.
- Note: This is an unusual setter design. The standard convention is \`public void setAge(int age)\`.

### \`Human obj1 = obj;\`

- Creates a new reference variable \`obj1\` that points to the same object as \`obj\`.
- No new object is created here. Both \`obj\` and \`obj1\` point to the same \`Human\` object in heap memory.

### \`obj1.age = age;\`

- Sets the \`age\` property of the object referenced by \`obj1\`.
- Since \`obj1\` and \`obj\` point to the same object, this also changes the \`age\` accessible through \`obj\`.
- Note: Even though \`age\` is \`private\`, it can be accessed from within the same class. The \`private\` restriction only applies to access from other classes.

### \`Human obj = new Human();\`

- Creates a new \`Human\` object with the default age value of \`0\`.

### \`obj.setAge(4, obj);\`

- Calls the setter method, passing \`4\` as the new age and \`obj\` itself as the \`Human\` reference.
- After this call, \`obj.age\` is \`4\`.

### \`System.out.println(obj.getAge());\`

- Calls the getter to retrieve the age and prints \`4\`.

---

## What Happens Without Encapsulation

If \`age\` were \`public\`:

\`\`\`java
class Human {
    public int age;
}

Human obj = new Human();
obj.age = -100;
obj.age = 999999;
System.out.println(obj.age);
\`\`\`

There is no way to enforce rules like "age must be between 0 and 150." With encapsulation, you can add validation inside the setter method.

---

## Best Practices for Encapsulation

1. **Always declare instance variables as \`private\`.**
2. **Provide getters only if external code needs to read the value.**
3. **Provide setters only if external code needs to modify the value.** If a property should be set only once (for example, during object creation), do not provide a setter -- use a constructor instead.
4. **Add validation logic inside setters.**
5. **Follow the JavaBeans naming convention** for getters and setters.
6. **Use the \`this\` keyword** to differentiate between the parameter and the instance variable when they have the same name.

Standard setter example:

\`\`\`java
public void setAge(int age) {
    if (age > 0 && age < 150) {
        this.age = age;
    }
}
\`\`\`

---

## How to Compile and Run

\`\`\`bash
javac Encapsulation.java
java Encapsulation
\`\`\`

### Expected Output

\`\`\`
4
\`\`\`

---

## Common Mistakes

### Mistake 1: Shadowing without using \`this\`

![age = age assigns the parameter to itself and never touches the field](diagrams/13-encapsulation-notes-shadowing.jpg)

\`\`\`java
public void setAge(int age) {
    age = age;
}
\`\`\`

\`\`\`expected
The field is never assigned — it stays at 0.

Both names refer to the parameter, so this assigns it to itself. It compiles,
it runs, and it silently does nothing. Write this.age = age.
\`\`\`

The correct way is:

\`\`\`java
public void setAge(int age) {
    this.age = age;
}
\`\`\`

### Mistake 2: Making getters and setters for every field blindly

Do not create getters and setters for every field just because it is a convention. Only expose what needs to be accessed or modified from outside. Creating unnecessary public methods defeats the purpose of encapsulation.

### Mistake 3: Trying to access private fields from outside the class

\`\`\`java
Human obj = new Human();
obj.age = 5;
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Encapsulation.java\`

\`Human\` keeps its \`age\` field **private** and exposes it through a public getter
and setter. That is encapsulation: the data is not reachable from outside the
class, and every read or write goes through a method you control â€” which is
where validation would live in real code.

\`main\` sets the age through the setter and reads it back through the getter.
Nothing outside \`Human\` ever touches the field directly.

\`\`\`java Encapsulation.java @run-encapsulation-encapsulation
class Human {

    private int age;

    public int getAge() {
        return age;
    }

    public void setAge(int age, Human obj) {
        Human obj1 = obj;
        obj1.age = age;
        // age = age;
    }
}

public class Encapsulation {

    public static void main(String[] args) {

        Human obj = new Human();
        obj.setAge(4, obj);
        System.out.println(obj.getAge());
    }
}

\`\`\`

\`\`\`output @run-encapsulation-encapsulation
4
\`\`\`
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it
comes from.

## The idea

![Encapsulation hides the data, abstraction hides the implementation](diagrams/13-encapsulation-revision-hides-what.jpg)

- Encapsulation is keeping an object's data private and exposing only the
  operations that are allowed on it. The object owns its own invariants.
- **Data hiding** is the mechanism — \`private\` fields. Encapsulation is the
  design: the class decides what may happen to its state.
- Encapsulation is about *how it is reached*; **abstraction** is about *what is
  shown*. One hides the data, the other hides the implementation.
- The payoff is that you can change a field's type, its representation, or
  validate on the way in, without touching a single caller.

## Getters, setters and \`this\`

- The JavaBeans convention: \`getX()\`/\`setX()\`, and \`isX()\` for a boolean.
- A setter is the place validation belongs. A setter that only assigns is a
  public field with extra steps — write one when it does something, not by reflex.
- \`this\` is the current object. Its everyday use is resolving **shadowing**:
  \`this.name = name\` when the parameter has the same name as the field.
- Without \`this\`, \`name = name\` assigns the parameter to itself and the field
  stays at its default. It compiles, and it is silent.

## Access

![One object reading another object private field, inside the same class](diagrams/13-encapsulation-revision-private-per-class.jpg)

- \`private\` is per **class**, not per object: one \`Account\` can read another
  \`Account\`'s private fields, because the code is inside the same class.
- Reaching a private field from outside its class is a compile error, not a
  run-time one.
- Encapsulation without accessors is possible and often better — expose
  behaviour (\`deposit\`, \`withdraw\`) rather than state (\`setBalance\`).
`,interview:`## Interview questions

### Q1: What is encapsulation in Java?

**Answer:** Encapsulation is the OOP principle of bundling data (variables) and methods that operate on that data into a single unit (class), while hiding the internal data from outside access. It is achieved by making variables \`private\` and providing \`public\` getter and setter methods.

### Q2: What is the difference between encapsulation and abstraction?

**Answer:** Encapsulation is about hiding the internal data by making variables private and controlling access through methods. Abstraction is about hiding the implementation details and showing only the essential features. Encapsulation is about "how" data is protected. Abstraction is about "what" is exposed to the user.

### Q3: What are the advantages of encapsulation?

![What each combination of getter and setter lets a caller do](diagrams/13-encapsulation-interview-which-accessors.jpg)

**Answer:** The advantages include: data hiding (internal state is protected from unauthorized access), data validation (setter methods can validate input before assignment), flexibility (internal implementation can change without affecting external code), read-only or write-only properties (by providing only a getter or only a setter), and improved maintainability.

### Q4: What is the \`this\` keyword in Java?

**Answer:** \`this\` is a reference variable that refers to the current object. It is commonly used to distinguish between instance variables and parameters when they have the same name. For example, in \`this.age = age;\`, \`this.age\` refers to the instance variable and \`age\` refers to the method parameter.

### Q5: Can a private variable be accessed from another object of the same class?

**Answer:** Yes. The \`private\` modifier restricts access at the class level, not the object level. Any method within the same class can access private members of any object of that class. This is why in the code, \`obj1.age = age;\` works inside the \`Human\` class even though \`age\` is private.

### Q6: What is data hiding?

![Data hiding is the mechanism inside the larger idea of encapsulation](diagrams/13-encapsulation-interview-hiding-inside-encapsulation.jpg)

**Answer:** Data hiding is a software development technique where the internal data of an object is hidden from outside access. In Java, it is implemented by declaring variables as \`private\`. Data hiding is a subset of encapsulation. Encapsulation includes both data hiding and providing controlled access through methods.

### Q7: What is the JavaBeans convention for getters and setters?

**Answer:** According to the JavaBeans specification, for a property named \`xyz\`, the getter should be named \`getXyz()\` (or \`isXyz()\` for boolean properties) and the setter should be named \`setXyz(Type value)\`. The first letter of the property name is capitalized. This convention is used by many Java frameworks for reflection-based access.

### Q8: Can you achieve encapsulation without getters and setters?

**Answer:** Partially. You can declare all variables as \`private\` and use constructors to set values and methods to operate on the data without explicitly providing getters and setters. However, getters and setters are the standard and most widely used approach to provide controlled access to private data.
`}},{slug:`static-keyword`,title:`The static keyword`,subject:`Java`,stage:`Classes and objects`,chapter:14,accent:`#f472b6`,summary:`The static keyword is one of the most important and frequently misunderstood concepts in Java. It fundamentally changes the behavior of a variable, method, or block by associating it with the class itself rather than with any specific object of that class.`,parts:{notes:`## Introduction

The \`static\` keyword is one of the most important and frequently misunderstood concepts in Java. It fundamentally changes the behavior of a variable, method, or block by associating it with the class itself rather than with any specific object of that class.

This program demonstrates how a static variable behaves differently from an instance variable. Specifically, it shows that changing a static variable affects all objects of the class because there is only one copy of a static variable shared among all instances.

---

## What is the Static Keyword

The \`static\` keyword in Java indicates that a member (variable, method, or block) belongs to the class itself, not to any specific instance (object) of the class. This means:

- A static variable has only one copy in memory, shared by all objects of the class.
- A static method can be called without creating an object of the class.
- A static block is executed once when the class is first loaded into memory.

Think of it like the Wi-Fi password in a hostel. Every room (object) has its own room number and furniture (instance variables), but the Wi-Fi password (static variable) is the same for every room. If the hostel changes the Wi-Fi password, it changes for everyone.

---

## Static Variables vs Instance Variables

| Feature                  | Static Variable                         | Instance Variable                      |
|--------------------------|----------------------------------------|----------------------------------------|
| Belongs to               | The class                              | The object                             |
| Number of copies         | One (shared by all objects)            | One per object                         |
| Memory allocation        | When class is loaded                   | When object is created                 |
| Accessed via             | ClassName.variableName (preferred)     | objectName.variableName                |
| Default value            | Same as instance variables (0, null, etc.) | Same (0, null, etc.)              |
| Also known as            | Class variable                         | Non-static variable                    |

---

## Understanding the Code

\`\`\`java StaticKeyword.java @run-static-keyword-static-keyword
class Phone{
    static String name;
    int price;

    public void show(){
        System.out.println(name + " : " + price);
    }
}

public class StaticKeyword {
    public static void main(String[] args) {
        Phone obj1 = new Phone();
        Phone.name = "Samsung";
        obj1.price = 1_45_000;

        Phone obj2 = new Phone();
        Phone.name = "Apple";
        obj2.price = 90_000;

        Phone.name = "Phone";

        obj1.show();
        obj2.show();
    }
}
\`\`\`

\`\`\`output @run-static-keyword-static-keyword
Phone : 145000
Phone : 90000
\`\`\`

---

## Line-by-Line Explanation

### \`static String name;\`

- Declares \`name\` as a static variable.
- There is only ONE copy of \`name\` in memory, regardless of how many \`Phone\` objects you create.
- It is accessed using \`Phone.name\`, not \`obj.name\` (though \`obj.name\` would also work, it is discouraged).

### \`int price;\`

- Declares \`price\` as an instance variable.
- Each \`Phone\` object gets its own separate copy of \`price\`.
- \`obj1.price\` and \`obj2.price\` are independent of each other.

### \`Phone.name = "Samsung";\`

- Sets the static variable \`name\` to \`"Samsung"\`.
- Since \`name\` is static, this change is visible through all objects.
- Accessed using the class name \`Phone.name\`, which is the correct way to access static members.

### \`obj1.price = 1_45_000;\`

- Sets the \`price\` of \`obj1\` to 145000.
- Note: The underscores (\`_\`) in \`1_45_000\` are numeric literals separators introduced in Java 7. They make large numbers easier to read but have no effect on the value.
- \`1_45_000\` is the same as \`145000\`.

### \`Phone.name = "Apple";\`

- Changes the static variable \`name\` to \`"Apple"\`.
- This overwrites the previous value \`"Samsung"\`.
- Since \`name\` is static, \`obj1\`'s \`name\` is also now \`"Apple"\` because there is only one shared copy.

### \`Phone.name = "Phone";\`

- Changes the static variable \`name\` again to \`"Phone"\`.
- Both \`obj1\` and \`obj2\` will now see \`name\` as \`"Phone"\`.

### \`obj1.show();\`

- Prints: \`Phone : 145000\`
- \`name\` is \`"Phone"\` (the latest static value), and \`obj1.price\` is \`145000\`.

### \`obj2.show();\`

- Prints: \`Phone : 90000\`
- \`name\` is still \`"Phone"\` (same static variable), and \`obj2.price\` is \`90000\`.

---

## Step-by-Step Execution Walkthrough

| Step | Code                          | Static \`name\` | \`obj1.price\` | \`obj2.price\` |
|------|-------------------------------|:-------------:|:------------:|:------------:|
| 1    | \`new Phone()\` (obj1)          | null          | 0            | -            |
| 2    | \`Phone.name = "Samsung"\`      | "Samsung"     | 0            | -            |
| 3    | \`obj1.price = 1_45_000\`       | "Samsung"     | 145000       | -            |
| 4    | \`new Phone()\` (obj2)          | "Samsung"     | 145000       | 0            |
| 5    | \`Phone.name = "Apple"\`        | "Apple"       | 145000       | 0            |
| 6    | \`obj2.price = 90_000\`         | "Apple"       | 145000       | 90000        |
| 7    | \`Phone.name = "Phone"\`        | "Phone"       | 145000       | 90000        |
| 8    | \`obj1.show()\` prints \`Phone : 145000\` | "Phone" | 145000       | 90000        |
| 9    | \`obj2.show()\` prints \`Phone : 90000\`  | "Phone" | 145000       | 90000        |

Notice that both objects print \`"Phone"\` as the name, even though \`obj1\` was created when \`name\` was \`"Samsung"\` and \`obj2\` was created when \`name\` was \`"Apple"\`. This is because \`name\` is static and there is only one copy.

---

## How Static Variables Work in Memory

![How Static Variables Work in Memory](diagrams/14-static-keyword-notes-c15424da.jpg)

- The static variable \`name\` is stored in the Method Area (or Class Data area), NOT inside any object.
- Each object only stores its own instance variables (\`price\`).
- When any code reads \`name\`, it goes to the one shared copy in the Method Area.

---

## Static Methods

A static method belongs to the class and can be called without creating an object.

\`\`\`java
class MathHelper {
    public static int square(int n) {
        return n * n;
    }
}

int result = MathHelper.square(5);
\`\`\`

### Rules for Static Methods

![Instance code may reach static members, but not the other way round](diagrams/14-static-keyword-notes-one-way-restriction.jpg)

1. A static method can access static variables and other static methods directly.
2. A static method CANNOT access instance variables or instance methods directly (because there is no object context).
3. A static method CANNOT use the \`this\` or \`super\` keyword.

\`\`\`java
class Example {
    static int x = 10;
    int y = 20;

    public static void staticMethod() {
        System.out.println(x);     // OK: accessing static variable
        // System.out.println(y);  // ERROR: cannot access instance variable
        // System.out.println(this.x); // ERROR: cannot use 'this' in static context
    }
}
\`\`\`

---

## Static Block

![Static initialisation happens once, before main and before any constructor](diagrams/14-static-keyword-notes-when-things-run.jpg)

A static block is a block of code that runs once when the class is loaded into memory, before any objects are created and before the \`main\` method executes.

\`\`\`java
class Demo {
    static {
        System.out.println("Static block executed");
    }

    Demo() {
        System.out.println("Constructor executed");
    }
}
\`\`\`

If you create two objects of \`Demo\`, the output will be:

\`\`\`
Static block executed
Constructor executed
Constructor executed
\`\`\`

The static block runs only once, while the constructor runs for every new object.

---

## When to Use Static

| Use Case                            | Example                              |
|--------------------------------------|--------------------------------------|
| Constants shared across all objects   | \`static final double PI = 3.14159\`  |
| Utility methods                       | \`Math.sqrt()\`, \`Arrays.sort()\`      |
| Counters or shared data              | Counting number of objects created   |
| Factory methods                       | \`Integer.parseInt("123")\`           |
| Main method                          | \`public static void main(String[])\` |

---

## How to Compile and Run

\`\`\`bash
javac StaticKeyword.java
java StaticKeyword
\`\`\`

### Expected Output

\`\`\`
Phone : 145000
Phone : 90000
\`\`\`

---

## Common Mistakes

### Mistake 1: Accessing instance variables from a static method

\`\`\`java
public static void main(String[] args) {
    System.out.println(price);  // ERROR: non-static variable cannot be referenced from a static context
}
\`\`\`

\`\`\`expected
error: non-static variable price cannot be referenced from a static context

A static method has no object, so there is no particular price to print.
Make price static, or construct an object and read it from that.
\`\`\`

### Mistake 2: Using \`this\` in a static method

\`\`\`java
public static void display() {
    System.out.println(this.name);  // ERROR: 'this' cannot be used in a static context
}
\`\`\`

\`\`\`expected
error: non-static variable this cannot be referenced from a static context

this means "the object this was called on", and a static method was not
called on one.
\`\`\`

### Mistake 3: Thinking static means constant

![static means one copy; final is what stops it changing](diagrams/14-static-keyword-notes-static-is-not-final.jpg)

\`static\` means class-level. It does NOT mean the value cannot change. For a constant, you need \`static final\`:

\`\`\`java
static final double PI = 3.14159;
static int count = 0;
\`\`\`

\`\`\`expected
PI cannot be reassigned — that is the final, not the static.
count can: count++ is perfectly legal, and every object sees the new value
because there is only one count.
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`StaticKeyword.java\`

\`Phone\` holds one of each kind of field: \`name\` is **static**, so it belongs to
the class and every object shares the single copy, while the price is an
**instance** field that each object owns separately.

The output is the demonstration. Two phones are created with different names,
then \`Phone.name\` is assigned once more â€” and because that one variable is
shared, *both* objects now report the new value. Their prices, being instance
fields, stay different.

\`\`\`java StaticKeyword.java @run-static-keyword-static-keyword
class Phone{
    static String name;
    int price;

    public void show(){
        System.out.println(name + " : " + price);
    }
}

public class StaticKeyword {
    public static void main(String[] args) {
        Phone obj1 = new Phone();
        Phone.name = "Samsung";
        obj1.price = 1_45_000;

        Phone obj2 = new Phone();
        Phone.name = "Apple";
        obj2.price = 90_000;

        Phone.name = "Phone";

        obj1.show();
        obj2.show();
    }
}

\`\`\`

\`\`\`output @run-static-keyword-static-keyword
Phone : 145000
Phone : 90000
\`\`\`
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it
comes from.

## What \`static\` means

- \`static\` binds a member to the **class**, not to any object. One copy exists,
  however many objects there are — or none at all.
- A static field is shared: change it through one object and every other object
  sees the change, because there was only ever one.
- \`static\` is not \`final\`. A static field is perfectly changeable; \`static final\`
  is the constant.
- Static members are reachable through an object reference, and it compiles — but
  it reads as instance state and it is not. Use \`ClassName.member\`.

## Static methods

![A static method is chosen by the reference type, an instance method by the object](diagrams/14-static-keyword-revision-hidden-not-overridden.jpg)

- A static method has no \`this\`, because there is no object it was called on. So
  it cannot touch instance fields or call instance methods directly.
- That is exactly why \`main\` is static: the JVM has to call something before any
  object of your class exists.
- An instance method can freely use static members. The restriction runs one way.
- Static methods are **hidden**, not overridden. Redeclaring one in a subclass
  shadows it, and which runs is decided by the reference type at compile time.

## Static blocks

![A class initialises on first use, not when the JVM starts](diagrams/14-static-keyword-revision-lazy-initialisation.jpg)

- A \`static { }\` block runs **once**, when the class is initialised — before the
  first object, and before \`main\` if it is in that class.
- Several static blocks run top to bottom, interleaved with static field
  initialisers in source order.
- Class initialisation is lazy: it happens on first use, not at JVM startup, so
  a class you never touch never runs its static block.
`,interview:`## Interview questions

### Q1: What is the static keyword in Java?

**Answer:** The \`static\` keyword is used to create class-level members. A static variable or method belongs to the class rather than to any specific object. Static variables have one shared copy across all instances. Static methods can be called without creating an object.

### Q2: What is the difference between a static variable and an instance variable?

**Answer:** A static variable belongs to the class and has only one copy shared by all objects. An instance variable belongs to individual objects, and each object has its own copy. Static variables are stored in the Method Area, while instance variables are stored on the heap as part of the object.

### Q3: Can a static method access non-static (instance) variables?

**Answer:** No. A static method cannot directly access instance variables or instance methods because there is no specific object context. To access instance members from a static method, you must first create an object and then use that object to access them.

### Q4: Why is the \`main\` method static in Java?

![A non-static main would need an object before any object could exist](diagrams/14-static-keyword-interview-why-main-is-static.jpg)

**Answer:** The \`main\` method is static because the JVM needs to call it without creating an object of the class. When the program starts, no objects exist yet. If \`main\` were not static, the JVM would need to create an object first, but it would not know which constructor to call or what arguments to pass.

### Q5: What is a static block and when is it executed?

**Answer:** A static block is a block of code that runs automatically when the class is first loaded into memory. It runs before the \`main\` method and before any constructors. It is used for static initialization, such as setting up complex static variables or loading configuration.

### Q6: Can we access static members using an object reference?

![Both spellings reach the one shared copy, but only one says so](diagrams/14-static-keyword-interview-through-an-object.jpg)

**Answer:** Yes, technically you can access static members using an object reference (e.g., \`obj1.name\`), but it is strongly discouraged. It creates confusion because it looks like an instance member access. The correct and recommended way is to use the class name (e.g., \`Phone.name\`). Most IDEs will warn you if you access static members through an object.

### Q7: Can you override a static method in Java?

**Answer:** No. Static methods cannot be overridden because they belong to the class, not to objects. If a subclass defines a static method with the same signature, it is called method hiding, not overriding. The method that gets called is determined by the reference type at compile time, not the object type at runtime.

### Q8: What are the numeric literal separators in Java?

**Answer:** Since Java 7, you can use underscores (\`_\`) as separators in numeric literals to improve readability. For example, \`1_00_000\` is the same as \`100000\`, and \`3.14_15_92\` is the same as \`3.141592\`. The underscores are ignored by the compiler and do not affect the value.
`}},{slug:`inheritance`,title:`Inheritance`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:15,accent:`#facc15`,summary:`Inheritance is one of the four pillars of Object-Oriented Programming (OOP). It allows a class to inherit properties and methods from another class. The class that inherits is called the child class (or subclass, or derived class), and the class being inherited from is called the parent class (or superclass, or base class).`,parts:{notes:`## Introduction

Inheritance is one of the four pillars of Object-Oriented Programming (OOP). It allows a class to inherit properties and methods from another class. The class that inherits is called the child class (or subclass, or derived class), and the class being inherited from is called the parent class (or superclass, or base class).

This program demonstrates multi-level inheritance (A -> B -> C), constructor chaining, and method overriding.

---

## What is Inheritance

Inheritance is a mechanism where a new class acquires the properties and behaviors of an existing class. The child class can use all the non-private members of the parent class without rewriting them.

Think of it like a family. A child inherits certain traits from their parents (eye color, height potential, etc.). The child has all the parent's traits plus potentially their own unique traits. In programming, the child class has all the parent's methods and variables, plus its own additions.

**Syntax:**

\`\`\`java
class ParentClass {
}

class ChildClass extends ParentClass {
}
\`\`\`

---

## Why Do We Need Inheritance

### Without Inheritance (Code Duplication)

\`\`\`java
class Car {
    String brand;
    int speed;
    public void start() { System.out.println("Starting..."); }
    public void stop() { System.out.println("Stopping..."); }
}

class ElectricCar {
    String brand;
    int speed;
    int batteryLevel;
    public void start() { System.out.println("Starting..."); }
    public void stop() { System.out.println("Stopping..."); }
    public void charge() { System.out.println("Charging..."); }
}
\`\`\`

### With Inheritance (Code Reuse)

\`\`\`java
class Car {
    String brand;
    int speed;
    public void start() { System.out.println("Starting..."); }
    public void stop() { System.out.println("Stopping..."); }
}

class ElectricCar extends Car {
    int batteryLevel;
    public void charge() { System.out.println("Charging..."); }
}
\`\`\`

### Benefits

| Benefit            | Description                                                   |
|--------------------|---------------------------------------------------------------|
| Code Reusability   | Write once in the parent, use in all children                 |
| Maintainability    | Fix a bug in one place, it is fixed for all subclasses        |
| Extensibility      | Add new features by creating subclasses without modifying parent |
| Polymorphism       | Treat objects of different subclasses uniformly through the parent type |

---

## Types of Inheritance in Java

### 1. Single Inheritance

One child class inherits from one parent class.

\`\`\`java
class A { }
class B extends A { }
\`\`\`

### 2. Multi-Level Inheritance

A child class inherits from a parent, which itself inherits from another parent.

\`\`\`java
class A { }
class B extends A { }
class C extends B { }
\`\`\`

### 3. Hierarchical Inheritance

Multiple child classes inherit from the same parent class.

\`\`\`java
class A { }
class B extends A { }
class C extends A { }
\`\`\`

### 4. Multiple Inheritance (NOT supported with classes in Java)

A class inheriting from more than one parent class. Java does NOT support this to avoid the Diamond Problem. However, multiple inheritance is possible through interfaces.

\`\`\`java
class C extends A, B { }

interface A { }
interface B { }
class C implements A, B { }  // OK
\`\`\`

---

## Understanding the Code

\`\`\`java Inheritance.java @run-inheritance-inheritance
class A {
    A() {
        System.out.println("A");
    }

    public void show() {
        System.out.println("Show Method : A");
    }
}

class C extends B {
    C() {
        System.out.println("C");
    }
}

class B extends A {
    B() {
        System.out.println("B");
    }

    public void show() {
        System.out.println("Show Method : B");
    }
}

public class Inheritance {
    public static void main(String[] args) {
        C obj = new C();
        obj.show();
    }
}
\`\`\`

\`\`\`output @run-inheritance-inheritance
A
B
C
Show Method : B
\`\`\`

### Inheritance Hierarchy

![Inheritance Hierarchy](diagrams/15-inheritance-notes-9bac1858.jpg)

---

## Line-by-Line Explanation

### Class A

\`\`\`java
class A {
    A() {
        System.out.println("A");
    }

    public void show() {
        System.out.println("Show Method : A");
    }
}
\`\`\`

- Class \`A\` is the base class (the topmost class in our hierarchy).
- It has a default constructor that prints \`"A"\`.
- It has a \`show()\` method that prints \`"Show Method : A"\`.

### Class B

\`\`\`java
class B extends A {
    B() {
        System.out.println("B");
    }

    public void show() {
        System.out.println("Show Method : B");
    }
}
\`\`\`

- Class \`B\` extends \`A\`, meaning \`B\` inherits everything from \`A\`.
- \`B\` has its own constructor that prints \`"B"\`.
- \`B\` overrides the \`show()\` method. When \`show()\` is called on a \`B\` object, it prints \`"Show Method : B"\` instead of \`"Show Method : A"\`.

### Class C

\`\`\`java
class C extends B {
    C() {
        System.out.println("C");
    }
}
\`\`\`

- Class \`C\` extends \`B\`, meaning \`C\` inherits from \`B\` (and transitively from \`A\`).
- \`C\` has its own constructor that prints \`"C"\`.
- \`C\` does NOT override the \`show()\` method. So when \`show()\` is called on a \`C\` object, it uses the version from \`B\` (the nearest parent that has the method).

### Main Method

\`\`\`java
C obj = new C();
obj.show();
\`\`\`

- \`new C()\` creates a new object of type \`C\`, which triggers constructor chaining.
- \`obj.show()\` calls the \`show()\` method. Since \`C\` does not have its own \`show()\`, Java looks up the hierarchy and finds it in \`B\`.

---

## Step-by-Step Execution Walkthrough

When \`new C()\` is executed:

| Step | What Happens                                           | Output |
|------|--------------------------------------------------------|--------|
| 1    | \`C()\` constructor is called                            | -      |
| 2    | Before executing \`C()\`'s body, Java calls \`B()\` (parent constructor) | - |
| 3    | Before executing \`B()\`'s body, Java calls \`A()\` (parent constructor) | - |
| 4    | Before executing \`A()\`'s body, Java calls \`Object()\` (every class implicitly extends Object) | - |
| 5    | \`Object()\` completes (does nothing visible)            | -      |
| 6    | \`A()\` body executes: \`System.out.println("A")\`         | A      |
| 7    | \`B()\` body executes: \`System.out.println("B")\`         | B      |
| 8    | \`C()\` body executes: \`System.out.println("C")\`         | C      |
| 9    | \`obj.show()\` -- \`C\` has no \`show()\`, looks at \`B\`      | Show Method : B |

**Key Insight:** Constructors are called from top to bottom (parent first, child last). This is called constructor chaining.

---

## Constructor Chaining in Inheritance

![The constructor call climbs to Object before any body runs](diagrams/15-inheritance-notes-constructor-chain.jpg)

When you create an object of a subclass, Java automatically calls the constructors of all parent classes in order, starting from the topmost parent (Object) down to the current class.

This happens because every constructor implicitly has a \`super()\` call as its first statement (unless you explicitly call another constructor).

\`\`\`java
class A {
    A() {
        // super();  <-- invisible call to Object()
        System.out.println("A");
    }
}

class B extends A {
    B() {
        // super();  <-- invisible call to A()
        System.out.println("B");
    }
}
\`\`\`

---

## Method Overriding

Method overriding occurs when a subclass provides its own implementation of a method that is already defined in its parent class.

### Rules for Method Overriding

1. The method in the child class must have the exact same name, return type, and parameter list as the method in the parent class.
2. The access modifier of the overriding method must be the same or less restrictive.
3. The overriding method cannot throw broader checked exceptions.
4. Static methods cannot be overridden (they can only be hidden).
5. Constructors cannot be overridden.
6. The \`@Override\` annotation is recommended (though optional) to catch errors at compile time.

### How Java Decides Which Method to Call

![The search for show() stops at the first class that declares it](diagrams/15-inheritance-notes-method-lookup.jpg)

When you call \`obj.show()\` on a \`C\` object:

1. Java first looks in class \`C\`. If \`show()\` exists in \`C\`, it calls that version.
2. If not found in \`C\`, Java looks in the parent class \`B\`. If \`show()\` exists in \`B\`, it calls that version.
3. If not found in \`B\`, Java looks in \`A\`, and so on up the hierarchy.

In our code, \`C\` does not have \`show()\`, so Java finds and calls \`B\`'s \`show()\`.

---

## The \`extends\` Keyword

![What extends gives the subclass and what it withholds](diagrams/15-inheritance-notes-inherited-or-not.jpg)

The \`extends\` keyword is used to create a child class that inherits from a parent class.

\`\`\`java
class Child extends Parent {
}
\`\`\`

What is inherited:

- All \`public\` and \`protected\` members (variables and methods)
- All \`default\` (package-private) members if the child is in the same package
- Constructors are NOT inherited, but they are called through constructor chaining

What is NOT inherited:

- \`private\` members (they exist in the parent but cannot be accessed directly from the child)
- Constructors

---

## How to Compile and Run

\`\`\`bash
javac Inheritance.java
java Inheritance
\`\`\`

### Expected Output

\`\`\`
A
B
C
Show Method : B
\`\`\`

---

## Common Mistakes

### Mistake 1: Thinking constructors are inherited

\`\`\`java
class A {
    A(int x) { }
}

class B extends A {
}
\`\`\`

\`\`\`expected
error: constructor A in class A cannot be applied to given types;
    required: int   found: no arguments

Constructors are not inherited. B gets a default constructor, which begins
with an implicit super() — and A has no no-argument constructor to call.
\`\`\`

You must explicitly define constructors in the child class and call the appropriate parent constructor using \`super()\`.

### Mistake 2: Thinking private members are not inherited

Private members ARE inherited (they exist in the child object's memory), but they cannot be accessed directly from the child class. They can only be accessed through public methods (getters/setters) defined in the parent class.

### Mistake 3: Forgetting that constructor chaining calls parent constructors first

The parent constructor always executes before the child constructor. You cannot skip it.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Inheritance.java\`

Three classes in a chain: \`A\`, then \`B extends A\`, then \`C extends B\`. That is
**multi-level** inheritance, and the output shows what it costs at construction
time.

Creating a single \`C\` prints \`A\`, \`B\` and \`C\` in that order, because every
constructor calls its parent's before running its own body. Construction always
starts at the top of the hierarchy and works down.

The final line is overriding at work: \`B\` supplies its own \`show()\`, and that is
the version that runs.

\`\`\`java Inheritance.java @run-inheritance-inheritance
class A {
    A() {
        System.out.println("A");
    }

    public void show() {
        System.out.println("Show Method : A");
    }
}

class C extends B {
    C() {
        System.out.println("C");
    }

    // public void show() {
    // System.out.println("Show Method : C");
    // }
}

class B extends A {
    B() {
        System.out.println("B");
    }

    public void show() {
        System.out.println("Show Method : B");
    }
}

public class Inheritance {
    public static void main(String[] args) {
        C obj = new C();
        obj.show();
    }
}

\`\`\`

\`\`\`output @run-inheritance-inheritance
A
B
C
Show Method : B
\`\`\`
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it
comes from.

## What is inherited

- \`extends\` gives a subclass every **field and method** of the parent, subject to
  access. It does not give it the parent's constructors.
- \`private\` members **are** inherited — they exist in the object — but they are
  not *accessible* from the subclass. That is a visibility rule, not an absence.
- Java has single inheritance for classes: one \`extends\`, any number of
  \`implements\`. That is how it sidesteps the **diamond problem** — two parents
  offering the same method with no rule to choose.
- Every class without an \`extends\` implicitly extends \`Object\`, so every object
  has \`toString\`, \`equals\`, \`hashCode\` and the rest.

## Constructors and chaining

![The compiler inserts a bare super() and it fails when there is none to call](diagrams/15-inheritance-revision-implicit-super.jpg)

- Constructors are not inherited. A subclass writes its own, and its first act is
  always a call to a parent constructor.
- If you write neither \`super(...)\` nor \`this(...)\`, the compiler inserts a bare
  \`super()\`. If the parent has no no-argument constructor, that is a compile
  error and you must call \`super(...)\` explicitly.
- Construction runs **parent first**, then child. So the parent's constructor
  output appears before the child's, and the child's fields are still at their
  defaults while the parent's constructor runs.

## Overriding

![The reference type decides what may be called; the object decides what runs](diagrams/15-inheritance-revision-reference-versus-object.jpg)

- Same name, same parameter list, in a subclass. Which one runs is decided at run
  time by the object's real type.
- An override may not reduce visibility, and may not throw broader checked
  exceptions. It may return a subclass of the original return type.
- \`super.method()\` calls the parent's version from inside the override, which is
  how you extend behaviour rather than replace it.
`,interview:`## Interview questions

### Q1: What is inheritance in Java?

**Answer:** Inheritance is an OOP mechanism where a child class acquires the properties and methods of a parent class using the \`extends\` keyword. It promotes code reusability, maintainability, and forms the basis for polymorphism.

### Q2: What types of inheritance does Java support?

**Answer:** Java supports single inheritance, multi-level inheritance, and hierarchical inheritance through classes. Java does NOT support multiple inheritance through classes (to avoid the Diamond Problem), but it supports multiple inheritance through interfaces.

### Q3: What is the Diamond Problem?

![Two parents offering the same method, and how interfaces avoid the ambiguity](diagrams/15-inheritance-interview-diamond.jpg)

**Answer:** The Diamond Problem occurs in multiple inheritance when a class inherits from two classes that both inherit from a common base class. If both parent classes have a method with the same signature, the compiler cannot determine which version to use. Java avoids this by not allowing multiple inheritance with classes.

### Q4: What is method overriding?

**Answer:** Method overriding is when a subclass provides a specific implementation for a method that is already defined in its parent class. The overriding method must have the same name, return type, and parameter list. At runtime, Java calls the overridden version based on the actual object type, not the reference type.

### Q5: What is the difference between method overloading and method overriding?

**Answer:**

| Feature          | Overloading                        | Overriding                          |
|------------------|------------------------------------|-------------------------------------|
| Definition       | Same name, different parameters    | Same name, same parameters          |
| Occurs in        | Same class                         | Parent and child classes            |
| Binding          | Compile-time (static binding)      | Runtime (dynamic binding)           |
| Return type      | Can be different                   | Must be same or covariant           |
| Access modifier  | Can be different                   | Cannot be more restrictive          |

### Q6: What is constructor chaining?

**Answer:** Constructor chaining is the process where constructors call other constructors in a chain. In inheritance, when a child object is created, the parent class constructor is called first (via implicit or explicit \`super()\` call), then the child class constructor executes. This ensures the parent part of the object is properly initialized before the child part.

### Q7: Can a child class access private members of the parent class?

**Answer:** No, a child class cannot directly access private members. However, private members do exist in the child object. They can be accessed indirectly through public or protected getter/setter methods provided by the parent class.

### Q8: What is the \`Object\` class in Java?

![Every class ends at Object, and what that already gives you](diagrams/15-inheritance-interview-object-root.jpg)

**Answer:** The \`Object\` class (in \`java.lang\` package) is the root of the class hierarchy. Every class in Java directly or indirectly extends \`Object\`. It provides fundamental methods like \`toString()\`, \`equals()\`, \`hashCode()\`, \`getClass()\`, \`clone()\`, \`wait()\`, \`notify()\`, and \`finalize()\`.

### Q9: Can you extend multiple classes in Java?

**Answer:** No. A class can extend only one class in Java. This is a design decision to avoid the Diamond Problem. If you need to inherit behavior from multiple sources, use interfaces.

### Q10: What happens if the parent class does not have a default (no-argument) constructor?

**Answer:** If the parent class only has parameterized constructors (no default constructor), the child class must explicitly call one of those parameterized constructors using \`super(arguments)\` in its constructor. If the child does not do this, the compiler will report an error because the implicit \`super()\` call cannot find a matching no-argument constructor in the parent.
`}},{slug:`super-and-this`,title:`super and this`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:16,accent:`#22d3ee`,summary:`The this and super keywords are two of the most important keywords in Java, especially when working with constructors and inheritance. They control how constructors call other constructors, how variables are resolved when there are naming conflicts, and how parent class members are accessed from a child class.`,parts:{notes:`## Introduction\r
\r
The \`this\` and \`super\` keywords are two of the most important keywords in Java, especially when working with constructors and inheritance. They control how constructors call other constructors, how variables are resolved when there are naming conflicts, and how parent class members are accessed from a child class.\r
\r
This program demonstrates constructor chaining using both \`this()\` and \`super()\`, showing the exact order in which constructors are called when an object is created.\r
\r
---\r
\r
## What is the \`this\` Keyword\r
\r
The \`this\` keyword is a reference to the current object -- the object on which a method or constructor is being invoked. It always points to the instance of the class in which it is used.\r
\r
### Simple Analogy\r
\r
Think of \`this\` as the word "I" in English. When a person says "I am going to the store," "I" refers to the person speaking. Similarly, when an object uses \`this\`, it refers to itself.\r
\r
### Basic Usage\r
\r
\`\`\`java\r
class Student {\r
    String name;\r
\r
    Student(String name) {\r
        this.name = name;\r
    }\r
}\r
\`\`\`\r
\r
Without \`this\`, both \`name\` references on the left and right would refer to the parameter, and the instance variable would never be set.\r
\r
---\r
\r
## What is the \`super\` Keyword\r
\r
The \`super\` keyword is a reference to the parent (superclass) object. It is used to access members of the parent class from the child class, especially when the child class has overridden or shadowed those members.\r
\r
### Simple Analogy\r
\r
Think of \`super\` as saying "my parent." When a child wants something from the parent's house, they say "let me get it from my parent." Similarly, when a child class wants to use a parent class member, it uses \`super\`.\r
\r
### Basic Usage\r
\r
\`\`\`java\r
class Animal {\r
    String type = "Animal";\r
}\r
\r
class Dog extends Animal {\r
    String type = "Dog";\r
\r
    void printTypes() {\r
        System.out.println(this.type);\r
        System.out.println(super.type);\r
    }\r
}\r
\`\`\`\r
\r
---\r
\r
## Difference Between \`this\` and \`super\`\r
\r
![One object holding both the parent field and the child field](diagrams/16-super-and-this-notes-shadowed-field.jpg)\r
\r
| Feature                   | \`this\`                                    | \`super\`                                    |\r
|---------------------------|-------------------------------------------|--------------------------------------------|\r
| Refers to                 | Current class object                      | Parent class object                        |\r
| Used for                  | Accessing current class members           | Accessing parent class members             |\r
| Constructor call          | \`this()\` calls another constructor of the same class | \`super()\` calls a constructor of the parent class |\r
| Can be used in            | Any non-static method or constructor      | Any non-static method or constructor       |\r
| Must be first statement   | Yes (when used as constructor call)       | Yes (when used as constructor call)        |\r
| Implicit behavior         | Not added automatically                   | \`super()\` is implicitly added to every constructor if no \`this()\` or \`super()\` is present |\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java SuperAndThis.java @run-super-and-this-super-and-this\r
\r
class A {\r
    public A() {\r
        super();\r
        System.out.println("A : Default Constructor");\r
    }\r
\r
    public A(int num) {\r
        this();\r
        System.out.println("A : Parameterized Constructor");\r
    }\r
}\r
\r
class B extends A {\r
    public B() {\r
        super(6);\r
        System.out.println("B : Default Constructor");\r
    }\r
\r
    public B(int num) {\r
        super();\r
        System.out.println("B : Parameterized Constructor");\r
    }\r
}\r
\r
public class SuperAndThis {\r
    public static void main(String[] args) {\r
        B obj = new B();\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-super-and-this-super-and-this\r
A : Default Constructor\r
A : Parameterized Constructor\r
B : Default Constructor\r
\`\`\`\r
\r
### Class Hierarchy\r
\r
![Class Hierarchy](diagrams/16-super-and-this-notes-66fbbbe7.jpg)\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### Class A - Default Constructor\r
\r
\`\`\`java\r
public A() {\r
    super();\r
    System.out.println("A : Default Constructor");\r
}\r
\`\`\`\r
\r
- \`super()\` calls the default constructor of the \`Object\` class (since \`A\` implicitly extends \`Object\`).\r
- Even if you do not write \`super()\`, the compiler adds it automatically.\r
- After the parent constructor completes, it prints \`"A : Default Constructor"\`.\r
\r
### Class A - Parameterized Constructor\r
\r
\`\`\`java\r
public A(int num) {\r
    this();\r
    System.out.println("A : Parameterized Constructor");\r
}\r
\`\`\`\r
\r
- \`this()\` calls the default constructor of class \`A\` (the one defined above).\r
- This is constructor chaining within the same class.\r
- After the default constructor completes (which first calls \`Object()\` then prints "A : Default Constructor"), this constructor then prints \`"A : Parameterized Constructor"\`.\r
\r
### Class B - Default Constructor\r
\r
\`\`\`java\r
public B() {\r
    super(6);\r
    System.out.println("B : Default Constructor");\r
}\r
\`\`\`\r
\r
- \`super(6)\` explicitly calls the parameterized constructor \`A(int num)\` of the parent class, passing \`6\` as the argument.\r
- This means \`A(int num)\` will execute, which internally calls \`this()\` (A's default constructor), creating a chain.\r
- After the parent's constructor chain completes, it prints \`"B : Default Constructor"\`.\r
\r
### Class B - Parameterized Constructor\r
\r
\`\`\`java\r
public B(int num) {\r
    super();\r
    System.out.println("B : Parameterized Constructor");\r
}\r
\`\`\`\r
\r
- \`super()\` calls the default constructor \`A()\` of the parent class.\r
- This constructor is not called in the current program because we only create \`new B()\` (the default constructor of B).\r
\r
---\r
\r
## Step-by-Step Execution Walkthrough\r
\r
When \`new B()\` is executed, the following chain of constructor calls occurs:\r
\r
| Step | Constructor Called         | Why                                       | Output                         |\r
|------|---------------------------|-------------------------------------------|--------------------------------|\r
| 1    | \`B()\`                     | We wrote \`new B()\`                       | (not yet, body runs after super) |\r
| 2    | \`A(int num)\` with num=6  | \`B()\` has \`super(6)\`                     | (not yet, body runs after this) |\r
| 3    | \`A()\`                     | \`A(int num)\` has \`this()\`                | (not yet, body runs after super) |\r
| 4    | \`Object()\`                | \`A()\` has \`super()\`                      | (nothing visible)              |\r
| 5    | \`A()\` body executes       | Back to step 3                           | A : Default Constructor        |\r
| 6    | \`A(int num)\` body executes| Back to step 2                           | A : Parameterized Constructor  |\r
| 7    | \`B()\` body executes       | Back to step 1                           | B : Default Constructor        |\r
\r
### Visual Call Stack\r
\r
![Visual Call Stack](diagrams/16-super-and-this-notes-1e6c91d9.jpg)\r
\r
---\r
\r
## Uses of \`this\` Keyword\r
\r
### 1. Distinguishing instance variables from parameters\r
\r
\`\`\`java\r
class Student {\r
    String name;\r
\r
    Student(String name) {\r
        this.name = name;\r
    }\r
}\r
\`\`\`\r
\r
### 2. Calling another constructor of the same class\r
\r
\`\`\`java\r
class Student {\r
    String name;\r
    int age;\r
\r
    Student() {\r
        this("Unknown", 0);\r
    }\r
\r
    Student(String name, int age) {\r
        this.name = name;\r
        this.age = age;\r
    }\r
}\r
\`\`\`\r
\r
### 3. Passing the current object as an argument\r
\r
\`\`\`java\r
class Printer {\r
    void print(Student s) {\r
        System.out.println(s.name);\r
    }\r
}\r
\r
class Student {\r
    String name = "Aditya";\r
\r
    void display(Printer p) {\r
        p.print(this);\r
    }\r
}\r
\`\`\`\r
\r
### 4. Returning the current object\r
\r
\`\`\`java\r
class Builder {\r
    int value;\r
\r
    Builder setValue(int value) {\r
        this.value = value;\r
        return this;\r
    }\r
}\r
\r
\`\`\`\r
\r
---\r
\r
## Uses of \`super\` Keyword\r
\r
### 1. Calling the parent class constructor\r
\r
\`\`\`java\r
class Animal {\r
    Animal(String type) {\r
        System.out.println("Animal: " + type);\r
    }\r
}\r
\r
class Dog extends Animal {\r
    Dog() {\r
        super("Dog");\r
    }\r
}\r
\`\`\`\r
\r
### 2. Accessing parent class methods (when overridden)\r
\r
\`\`\`java\r
class Animal {\r
    void sound() {\r
        System.out.println("Some sound");\r
    }\r
}\r
\r
class Dog extends Animal {\r
    void sound() {\r
        super.sound();\r
        System.out.println("Bark");\r
    }\r
}\r
\`\`\`\r
\r
### 3. Accessing parent class variables (when shadowed)\r
\r
\`\`\`java\r
class Animal {\r
    String type = "Animal";\r
}\r
\r
class Dog extends Animal {\r
    String type = "Dog";\r
\r
    void print() {\r
        System.out.println(this.type);\r
        System.out.println(super.type);\r
    }\r
}\r
\`\`\`\r
\r
---\r
\r
## Rules and Restrictions\r
\r
### Rule 1: \`this()\` and \`super()\` must be the first statement in a constructor\r
\r
![Only statement zero of a constructor may be this() or super()](diagrams/16-super-and-this-notes-first-statement.jpg)\r
\r
\`\`\`java\r
class B extends A {\r
    B() {\r
        System.out.println("Hello");  // ERROR if super() or this() follows\r
        super();\r
    }\r
}\r
\`\`\`\r
\r
### Rule 2: You cannot use both \`this()\` and \`super()\` in the same constructor\r
\r
Since both must be the first statement, you can only use one of them.\r
\r
\`\`\`java\r
class B extends A {\r
    B() {\r
        super();\r
        this();  // ERROR: call to this must be first statement\r
    }\r
}\r
\`\`\`\r
\r
### Rule 3: \`super()\` is added implicitly if no \`this()\` or \`super()\` is present\r
\r
If you do not write \`this()\` or \`super()\` in a constructor, the compiler automatically inserts \`super()\` (a call to the parent's no-argument constructor).\r
\r
![The compiler inserts super() as the first line of a constructor](diagrams/16-super-and-this-notes-implicit-super.jpg)\r
\r
### Rule 4: \`this\` and \`super\` cannot be used in static methods\r
\r
\`\`\`java\r
public static void main(String[] args) {\r
    // System.out.println(this);   // ERROR: 'this' in static context\r
    // System.out.println(super.x); // ERROR: 'super' in static context\r
}\r
\`\`\`\r
\r
### Rule 5: Recursive constructor calls are not allowed\r
\r
\`\`\`java\r
class A {\r
    A() {\r
        this();  // ERROR: recursive constructor invocation\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
error: recursive constructor invocation\r
\r
A() calls A(int), which calls A(). The compiler traces the cycle and stops\r
it — this one never gets as far as a StackOverflowError.\r
\`\`\`\r
\r
---\r
\r
## How to Compile and Run\r
\r
The file declares no package declaration, so compile and run from the\r
parent directory using the package path:\r
\r
\`\`\`bash\r
javac SuperAndThis.java\r
java SuperAndThis\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
A : Default Constructor\r
A : Parameterized Constructor\r
B : Default Constructor\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Forgetting that \`super()\` is added automatically\r
\r
\`\`\`java\r
class A {\r
    A(int x) { }\r
}\r
\r
class B extends A {\r
    B() {\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
error: constructor A in class A cannot be applied to given types;\r
    required: int   found: no arguments\r
\r
Every constructor that does not start with this(...) or super(...) gets a\r
bare super() inserted. A has no no-argument constructor, so you have to\r
write super(someInt) yourself.\r
\`\`\`\r
\r
Solution: Explicitly call \`super(someValue)\` or add a default constructor to the parent class.\r
\r
### Mistake 2: Using \`this()\` and \`super()\` in the same constructor\r
\r
This is not allowed. You can only use one of them.\r
\r
### Mistake 3: Creating infinite constructor recursion\r
\r
\`\`\`java\r
class A {\r
    A() {\r
        this(5);\r
    }\r
    A(int x) {\r
        this();  // ERROR: recursive constructor invocation\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
error: recursive constructor invocation\r
\r
A() calls A(int), which calls A(). The compiler traces the cycle and stops\r
it — this one never gets as far as a StackOverflowError.\r
\`\`\`\r
\r
Java detects direct circular constructor calls at compile time.\r
\r
### Mistake 4: Confusing \`this\` (reference) with \`this()\` (constructor call)\r
\r
- \`this\` (without parentheses) is a reference to the current object. It can be used anywhere in non-static methods.\r
- \`this()\` (with parentheses) is a constructor call. It can ONLY be used as the first statement inside a constructor.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`SuperAndThis.java\`\r
\r
Both keywords used as **constructor calls**, which is the part that confuses\r
people. \`super(...)\` calls a constructor of the parent class; \`this(...)\` calls\r
another constructor of the *same* class. Either one must be the first statement\r
in the constructor, so you can never use both.\r
\r
The thing to notice: \`super()\` is not optional. If you do not write a call\r
yourself, Java inserts a no-argument \`super()\` for you â€” which is why a parent\r
constructor always runs even when nothing in the child mentions it.\r
\r
Follow the chain the output traces: \`new B()\` calls \`super(6)\`, landing in\r
\`A(int)\`, which itself calls \`this()\` to reach \`A()\`. So \`A\`'s default\r
constructor finishes first, then \`A(int)\`, then \`B()\`.\r
\r
\`\`\`java SuperAndThis.java @run-super-and-this-super-and-this\r
\r
class A {\r
    public A() {\r
        super();\r
        System.out.println("A : Default Constructor");\r
    }\r
\r
    public A(int num) {\r
        // super();\r
        this();\r
        System.out.println("A : Parameterized Constructor");\r
    }\r
}\r
\r
class B extends A {\r
    public B() {\r
        super(6);\r
        System.out.println("B : Default Constructor");\r
    }\r
\r
    public B(int num) {\r
        super();\r
        System.out.println("B : Parameterized Constructor");\r
    }\r
}\r
\r
public class SuperAndThis {\r
    public static void main(String[] args) {\r
        B obj = new B();\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-super-and-this-super-and-this\r
A : Default Constructor\r
A : Parameterized Constructor\r
B : Default Constructor\r
\`\`\`\r
`,revision:"The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## `this` and `this()`\r\n\r\n![this and super exist in instance code and not in static code](diagrams/16-super-and-this-revision-static-context.jpg)\r\n\r\n- `this` is a **reference** to the current object. `this()` is a **call** to\r\n  another constructor in the same class. Same word, different things.\r\n- The everyday use of `this` is resolving shadowing: `this.name = name` when the\r\n  parameter and the field share a name.\r\n- Neither `this` nor `super` exists in a static context — there is no current\r\n  object to refer to.\r\n\r\n## `super` and `super()`\r\n\r\n![An override with and without a call to the parent version](diagrams/16-super-and-this-revision-super-call.jpg)\r\n\r\n- `super` reaches the parent's version of a member; `super()` calls a parent\r\n  constructor.\r\n- Every constructor begins with a call to `this(...)` or `super(...)`. Write\r\n  neither and the compiler inserts `super()`.\r\n- **They must be the first statement**, and you cannot have both: the parent has\r\n  to be fully constructed before the child touches anything, and two different\r\n  chains cannot both be first.\r\n- `super.method()` inside an override runs the parent's version — the way to add\r\n  to inherited behaviour rather than replace it.\r\n\r\n## The order things happen\r\n\r\n- A chain runs to the top before anything runs on the way down: `Object`'s\r\n  constructor completes first, then each subclass in turn.\r\n- So with `this()` delegating to a parameterised constructor, the *default*\r\n  constructor's output appears first — it is the one at the bottom of the chain\r\n  that reaches the parent.\r\n- A constructor calling `this()` on a path that leads back to itself is infinite\r\n  recursion, and the compiler rejects the direct cases.\r\n",interview:"## Interview questions\n\n### Q1: What is the `this` keyword in Java?\n\n**Answer:** `this` is a reference variable that refers to the current object. It is used to access instance variables when they are shadowed by parameters, to call other constructors of the same class using `this()`, to pass the current object as an argument, and to return the current object from a method.\n\n### Q2: What is the `super` keyword in Java?\n\n**Answer:** `super` is a reference variable that refers to the immediate parent class object. It is used to call the parent class constructor using `super()`, to access parent class methods that have been overridden, and to access parent class variables that have been shadowed by child class variables.\n\n### Q3: Why must `super()` or `this()` be the first statement in a constructor?\n\n**Answer:** Because the parent class must be fully initialized before the child class can use any inherited members. If the parent constructor were called in the middle or at the end, the child class might try to use uninitialized inherited members, leading to unpredictable behavior. Java enforces this rule at the compiler level.\n\n### Q4: What happens if you do not write `super()` in a constructor?\n\n**Answer:** The compiler automatically inserts `super()` (a call to the parent's no-argument constructor) as the first statement. If the parent class does not have a no-argument constructor, the code will not compile, and you must explicitly call a parameterized parent constructor using `super(arguments)`.\n\n![A child constructor failing because the parent has no no-argument constructor](diagrams/16-super-and-this-interview-missing-noarg.jpg)\n\n### Q5: Can `this()` and `super()` be used together in the same constructor?\n\n**Answer:** No. Both `this()` and `super()` must be the first statement in a constructor, and only one statement can be first. Therefore, you can use either `this()` or `super()`, but not both in the same constructor.\n\n### Q6: What is constructor chaining?\n\n**Answer:** Constructor chaining is the process of one constructor calling another constructor. It can happen within the same class using `this()` or across the inheritance hierarchy using `super()`. The purpose is to avoid code duplication and ensure proper initialization. Every object creation in Java ultimately chains up to the `Object` class constructor.\n\n### Q7: Can we use `this` and `super` inside a static method?\n\n**Answer:** No. `this` and `super` refer to object instances, but static methods belong to the class and do not have an object context. Using `this` or `super` in a static method will result in a compilation error: \"non-static variable this/super cannot be referenced from a static context.\"\n\n### Q8: What is the difference between `this()` and `this`?\n\n**Answer:** `this` (without parentheses) is a reference to the current object and can be used in any non-static context. `this()` (with parentheses) is a constructor call that invokes another constructor of the same class and can only be used as the first statement inside a constructor.\n\n![this as a reference compared with this() as a constructor call](diagrams/16-super-and-this-interview-this-vs-thiscall.jpg)\n\n### Q9: In the code example, why does `A : Default Constructor` appear before `A : Parameterized Constructor`?\n\n**Answer:** Because the parameterized constructor `A(int num)` uses `this()` to call the default constructor `A()` first. The call stack is: `B()` calls `super(6)` which invokes `A(int)`, then `A(int)` calls `this()` which invokes `A()`, then `A()` calls `super()` which invokes `Object()`. The execution then unwinds: `A()` prints first, then `A(int)` prints, then `B()` prints. Constructors always execute from parent to child.\n\n### Q10: What is the `Object` class and how is it related to `super()`?\n\n**Answer:** The `Object` class is the root superclass of every class in Java. Even if a class does not explicitly extend any class, it implicitly extends `Object`. When `super()` is called in the topmost user-defined class, it calls `Object()`'s constructor, which initializes the base object infrastructure (hash code, class metadata, etc.).\n"}},{slug:`polymorphism`,title:`Polymorphism`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:17,accent:`#fb7185`,summary:`Polymorphism is one of the four pillars of Object-Oriented Programming (OOP). The word "polymorphism" comes from Greek and means "many forms". In Java, polymorphism allows a single action to behave differently depending on the object that performs it.`,parts:{notes:`## Introduction\r
\r
Polymorphism is one of the four pillars of Object-Oriented Programming (OOP). The word "polymorphism" comes from Greek and means "many forms". In Java, polymorphism allows a single action to behave differently depending on the object that performs it.\r
\r
This program demonstrates runtime polymorphism, where a parent class reference (\`Animal\`) points to objects of different child classes (\`Dog\`, \`Fish\`, \`Bird\`), and the overridden \`move()\` method that runs is decided at runtime based on the actual object type.\r
\r
---\r
\r
## What is Polymorphism\r
\r
Polymorphism is the ability of an object, method, or reference to take on many forms. The same method call can produce different behavior depending on the actual object it is invoked on.\r
\r
Think of the word "draw". An artist draws a painting, a gunslinger draws a pistol, and a gambler draws a card. The same word means different actions depending on who performs it. Similarly, calling \`move()\` on different animals produces different movement.\r
\r
**Syntax:**\r
\r
\`\`\`java\r
class Parent {\r
    public void action() {\r
        System.out.println("Parent action");\r
    }\r
}\r
\r
class Child extends Parent {\r
    @Override\r
    public void action() {\r
        System.out.println("Child action");\r
    }\r
}\r
\r
Parent p = new Child();\r
p.action();\r
\`\`\`\r
\r
---\r
\r
## Why Do We Need Polymorphism\r
\r
![Adding a new subclass with and without polymorphism](diagrams/17-polymorphism-notes-adding-a-type.jpg)\r
\r
### Without Polymorphism (Rigid Code)\r
\r
\`\`\`java\r
void makeItMove(Dog dog)  { dog.move(); }\r
void makeItMove(Fish fish) { fish.move(); }\r
void makeItMove(Bird bird) { bird.move(); }\r
\`\`\`\r
\r
### With Polymorphism (Flexible Code)\r
\r
\`\`\`java\r
void makeItMove(Animal animal) {\r
    animal.move();  // Works for Dog, Fish, Bird, or any future Animal\r
}\r
\`\`\`\r
\r
### Benefits\r
\r
| Benefit            | Description                                                       |\r
|--------------------|-------------------------------------------------------------------|\r
| Code Reusability   | One method can handle objects of many related types               |\r
| Extensibility      | Add new subclasses without changing existing code                 |\r
| Maintainability    | Logic lives in one place instead of being duplicated per type     |\r
| Flexibility        | Program to the parent type, decide the actual behavior at runtime  |\r
\r
---\r
\r
## Types of Polymorphism in Java\r
\r
### 1. Compile-Time Polymorphism (Static)\r
\r
Resolved by the compiler. Achieved through **method overloading** (same method name, different parameter lists).\r
\r
![1. Compile-Time Polymorphism (Static)](diagrams/17-polymorphism-notes-27fb5b35.jpg)\r
\r
### 2. Runtime Polymorphism (Dynamic)\r
\r
Resolved during execution (the focus of this chapter's code). Achieved through **method overriding** combined with upcasting.\r
\r
![2. Runtime Polymorphism (Dynamic)](diagrams/17-polymorphism-notes-e7ff8e3d.jpg)\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java PolymorphismRuntime.java @run-polymorphism-polymorphism-runtime\r
class Animal {\r
    public void move() {\r
        System.out.println("Animal moves");\r
    }\r
}\r
\r
class Dog extends Animal {\r
    public void move() {\r
        System.out.println("Dog runs");\r
    }\r
}\r
\r
class Fish extends Animal {\r
    public void move() {\r
        System.out.println("Fish swims");\r
    }\r
}\r
\r
class Bird extends Animal {\r
    public void move() {\r
        System.out.println("Bird flies");\r
    }\r
}\r
\r
public class PolymorphismRuntime {\r
    public static void main(String[] args) {\r
        Animal animal = new Animal();\r
        Dog dog = new Dog();\r
        Fish fish = new Fish();\r
        Bird bird = new Bird();\r
\r
        Animal a1 = new Animal();\r
        Animal a2 = new Dog();\r
        Animal a3 = new Fish();\r
        Animal a4 = new Bird();\r
\r
        animal.move();\r
        dog.move();\r
        fish.move();\r
        bird.move();\r
\r
        a1.move();\r
        a2.move();\r
        a3.move();\r
        a4.move();\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-polymorphism-polymorphism-runtime\r
Animal moves\r
Dog runs\r
Fish swims\r
Bird flies\r
Animal moves\r
Dog runs\r
Fish swims\r
Bird flies\r
\`\`\`\r
\r
### Class Hierarchy\r
\r
![Class Hierarchy](diagrams/17-polymorphism-notes-37d7482f.jpg)\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### The Parent Class: Animal\r
\r
\`\`\`java\r
class Animal {\r
    public void move() {\r
        System.out.println("Animal moves");\r
    }\r
}\r
\`\`\`\r
\r
- \`Animal\` is the base class with a \`move()\` method that prints \`"Animal moves"\`.\r
- This is the method that the child classes will override.\r
\r
### The Child Classes: Dog, Fish, Bird\r
\r
\`\`\`java\r
class Dog extends Animal {\r
    public void move() {\r
        System.out.println("Dog runs");\r
    }\r
}\r
\`\`\`\r
\r
- \`Dog\`, \`Fish\`, and \`Bird\` each extend \`Animal\`.\r
- Each provides its own version of \`move()\`, replacing the parent's behavior. This is **method overriding**.\r
- \`Dog\` prints \`"Dog runs"\`, \`Fish\` prints \`"Fish swims"\`, and \`Bird\` prints \`"Bird flies"\`.\r
\r
### Direct Object References\r
\r
\`\`\`java\r
Animal animal = new Animal();\r
Dog dog = new Dog();\r
Fish fish = new Fish();\r
Bird bird = new Bird();\r
\`\`\`\r
\r
- Each variable's reference type matches its object type.\r
- Calling \`move()\` on each runs the method belonging to that exact class.\r
\r
### Parent References to Child Objects (Upcasting)\r
\r
\`\`\`java\r
Animal a1 = new Animal();\r
Animal a2 = new Dog();\r
Animal a3 = new Fish();\r
Animal a4 = new Bird();\r
\`\`\`\r
\r
- All four variables are of type \`Animal\`, but \`a2\`, \`a3\`, and \`a4\` point to child objects.\r
- This is called **upcasting** -- treating a subclass object as its superclass type.\r
- Even though the reference type is \`Animal\`, the actual object determines which \`move()\` runs.\r
\r
---\r
\r
## Step-by-Step Execution Walkthrough\r
\r
When \`main\` runs, the following output is produced:\r
\r
| Statement      | Reference Type | Actual Object | Method Called  | Output         |\r
|----------------|----------------|---------------|----------------|----------------|\r
| \`animal.move()\`| \`Animal\`       | \`Animal\`      | \`Animal.move()\`| Animal moves   |\r
| \`dog.move()\`   | \`Dog\`          | \`Dog\`         | \`Dog.move()\`   | Dog runs       |\r
| \`fish.move()\`  | \`Fish\`         | \`Fish\`        | \`Fish.move()\`  | Fish swims     |\r
| \`bird.move()\`  | \`Bird\`         | \`Bird\`        | \`Bird.move()\`  | Bird flies     |\r
| \`a1.move()\`    | \`Animal\`       | \`Animal\`      | \`Animal.move()\`| Animal moves   |\r
| \`a2.move()\`    | \`Animal\`       | \`Dog\`         | \`Dog.move()\`   | Dog runs       |\r
| \`a3.move()\`    | \`Animal\`       | \`Fish\`        | \`Fish.move()\`  | Fish swims     |\r
| \`a4.move()\`    | \`Animal\`       | \`Bird\`        | \`Bird.move()\`  | Bird flies     |\r
\r
**Key Insight:** \`a2\`, \`a3\`, and \`a4\` all have the reference type \`Animal\`, yet they print different results. The method that runs is chosen by the *actual object*, not the *reference type*. This is the essence of runtime polymorphism.\r
\r
![The reference type decides what compiles, the object type decides what runs](diagrams/17-polymorphism-notes-compile-vs-run.jpg)\r
\r
---\r
\r
## Upcasting and Dynamic Method Dispatch\r
\r
### Upcasting\r
\r
Upcasting is assigning a child object to a parent reference:\r
\r
\`\`\`java\r
Animal a2 = new Dog();\r
\`\`\`\r
\r
- This is always safe and happens automatically.\r
- The reference type (\`Animal\`) decides which methods are *accessible*.\r
- The object type (\`Dog\`) decides which version actually *runs*.\r
\r
### Dynamic Method Dispatch\r
\r
Dynamic method dispatch is the mechanism by which a call to an overridden method is resolved at runtime rather than compile time.\r
\r
\`\`\`java\r
Animal a2 = new Dog();\r
a2.move();\r
\`\`\`\r
\r
\`\`\`expected\r
Dog's move() runs.\r
\r
The reference type is Animal, so move() must exist on Animal for this to\r
compile. Which version actually runs is decided at run time by the object,\r
and the object is a Dog.\r
\`\`\`\r
\r
- At compile time, Java only checks that \`Animal\` has a \`move()\` method.\r
- At runtime, the JVM looks at the real object and dispatches to its overridden method.\r
\r
---\r
\r
## Compile-Time Polymorphism (Method Overloading)\r
\r
While this chapter's code demonstrates runtime polymorphism, Java also supports compile-time polymorphism through **method overloading** -- multiple methods with the same name but different parameter lists in the same class.\r
\r
\`\`\`java\r
class Calculator {\r
    int add(int a, int b) {\r
        return a + b;\r
    }\r
\r
    double add(double a, double b) {\r
        return a + b;\r
    }\r
\r
    int add(int a, int b, int c) {\r
        return a + b + c;\r
    }\r
}\r
\`\`\`\r
\r
### Rules for Method Overloading\r
\r
- The method name must be the same.\r
- The parameter list must differ in number, type, or order.\r
- The return type alone is NOT enough to overload a method.\r
- The compiler decides which version to call based on the arguments.\r
\r
---\r
\r
## Overloading vs Overriding\r
\r
![Overloading resolved by the compiler, overriding resolved by the JVM](diagrams/17-polymorphism-notes-when-decided.jpg)\r
\r
| Feature           | Overloading                     | Overriding                          |\r
|-------------------|---------------------------------|-------------------------------------|\r
| Polymorphism Type | Compile-time (static)           | Runtime (dynamic)                   |\r
| Parameters        | Must be different               | Must be the same                    |\r
| Inheritance       | Not required                    | Required (parent-child relationship)|\r
| Return Type       | Can be different                | Must be the same or covariant       |\r
| Resolved At       | Compile time                    | Runtime                             |\r
| Scope             | Same class                      | Across parent and child classes     |\r
\r
---\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac PolymorphismRuntime.java\r
java PolymorphismRuntime\r
\`\`\`\r
\r
> Note: The file declares no package declaration. If you compile from the parent directory, use:\r
>\r
> \`\`\`bash\r
> javac PolymorphismRuntime.java\r
> java PolymorphismRuntime\r
> \`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
Animal moves\r
Dog runs\r
Fish swims\r
Bird flies\r
Animal moves\r
Dog runs\r
Fish swims\r
Bird flies\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Confusing overloading and overriding\r
\r
Overloading is "same name, different parameters" in the same class (compile-time). Overriding is "same name, same parameters" across parent and child classes (runtime). They are different mechanisms.\r
\r
### Mistake 2: Thinking the reference type decides the method\r
\r
\`\`\`java\r
Animal a2 = new Dog();\r
a2.move();\r
\`\`\`\r
\r
\`\`\`expected\r
Dog's move() runs.\r
\r
The reference type is Animal, so move() must exist on Animal for this to\r
compile. Which version actually runs is decided at run time by the object,\r
and the object is a Dog.\r
\`\`\`\r
\r
The actual object type decides which overridden method runs, not the reference type.\r
\r
### Mistake 3: Trying to call child-specific methods through a parent reference\r
\r
\`\`\`java\r
class Dog extends Animal {\r
    public void bark() { System.out.println("Bark"); }\r
}\r
\r
Animal a2 = new Dog();\r
// a2.bark();  // ERROR: Animal reference does not know about bark()\r
\`\`\`\r
\r
\`\`\`expected\r
error: cannot find symbol — method bark()\r
    location: variable a2 of type Animal\r
\r
The object has bark(). The Animal reference does not know that, and the\r
compiler goes by the reference. Cast to Dog, or put the method on Animal.\r
\`\`\`\r
\r
The reference type (\`Animal\`) limits which methods are accessible at compile time. To call \`bark()\`, you must downcast: \`((Dog) a2).bark();\`.\r
\r
### Mistake 4: Forgetting the @Override annotation\r
\r
While optional, \`@Override\` lets the compiler verify that you are actually overriding a parent method. Without it, a typo in the method name silently creates a new method instead of overriding.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`PolymorphismRuntime.java\`\r
\r
Runtime polymorphism in its plainest form. \`a2\`, \`a3\` and \`a4\` are all declared\r
as \`Animal\` references, but each points at a \`Dog\`, a \`Fish\` and a \`Bird\`.\r
\r
Calling the same method on each one produces different behaviour, and it is the\r
**object** that decides which version runs â€” not the reference type, and not\r
anything the compiler worked out. That decision happens while the program is\r
running, which is why it is called runtime (or dynamic) dispatch.\r
\r
\`\`\`java PolymorphismRuntime.java @run-polymorphism-polymorphism-runtime\r
class Animal {\r
    public void move() {\r
        System.out.println("Animal moves");\r
    }\r
}\r
\r
class Dog extends Animal {\r
    public void move() {\r
        System.out.println("Dog runs");\r
    }\r
}\r
\r
class Fish extends Animal {\r
    public void move() {\r
        System.out.println("Fish swims");\r
    }\r
}\r
\r
class Bird extends Animal {\r
    public void move() {\r
        System.out.println("Bird flies");\r
    }\r
}\r
\r
public class PolymorphismRuntime {\r
    public static void main(String[] args) {\r
        Animal animal = new Animal();\r
        Dog dog = new Dog();\r
        Fish fish = new Fish();\r
        Bird bird = new Bird();\r
\r
        Animal a1 = new Animal();\r
        Animal a2 = new Dog();\r
        Animal a3 = new Fish();\r
        Animal a4 = new Bird();\r
\r
        animal.move();\r
        dog.move();\r
        fish.move();\r
        bird.move();\r
\r
        a1.move();\r
        a2.move();\r
        a3.move();\r
        a4.move();\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-polymorphism-polymorphism-runtime\r
Animal moves\r
Dog runs\r
Fish swims\r
Bird flies\r
Animal moves\r
Dog runs\r
Fish swims\r
Bird flies\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## Two kinds\r
\r
- **Compile-time** (overloading): same name, different parameter lists, chosen by\r
  the compiler from the argument types.\r
- **Run-time** (overriding): same signature in a subclass, chosen by the JVM from\r
  the object's actual type. This is **dynamic method dispatch**.\r
- Return type alone never distinguishes methods. Nothing at the call site could\r
  choose between them.\r
\r
## The reference does not decide\r
\r
- A parent-typed reference holding a child object calls the **child's** override.\r
  The reference type decides what you are *allowed* to call; the object decides\r
  *which version runs*.\r
- **Upcasting** — child to parent — is implicit and always safe. Downcasting is\r
  explicit and can throw \`ClassCastException\`; guard it with \`instanceof\`.\r
- Through a parent reference you cannot call methods that only the child\r
  declares, even though the object has them. Widen the reference or downcast.\r
- Fields are **not** polymorphic. A field access is resolved by the reference\r
  type at compile time, so shadowing a field and expecting override behaviour\r
  gives the parent's.\r
\r
## What it buys, and what does not participate\r
\r
- Programming to the parent type means one piece of code works for every subclass\r
  written afterwards, including ones that did not exist when it was written.\r
- \`static\` methods are hidden, not overridden — dispatch is by reference type.\r
- \`private\` and \`final\` methods cannot be overridden at all, so calls to them are\r
  bound at compile time.\r
- \`@Override\` is optional and worth writing every time: it turns a mistyped\r
  signature from a silent new overload into a compile error.\r
`,interview:`## Interview questions

### Q1: What is polymorphism in Java?

**Answer:** Polymorphism means "many forms". It is the ability of a single method call or reference to behave differently based on the actual object involved. Java supports compile-time polymorphism (method overloading) and runtime polymorphism (method overriding with dynamic method dispatch).

### Q2: What are the two types of polymorphism in Java?

**Answer:** Compile-time polymorphism (also called static polymorphism), achieved through method overloading and resolved by the compiler; and runtime polymorphism (also called dynamic polymorphism), achieved through method overriding and resolved by the JVM at runtime.

### Q3: What is the difference between method overloading and method overriding?

**Answer:**

| Feature       | Overloading                    | Overriding                          |
|---------------|--------------------------------|-------------------------------------|
| Definition    | Same name, different parameters| Same name, same parameters          |
| Occurs in     | Same class                     | Parent and child classes            |
| Binding       | Compile-time (static binding)  | Runtime (dynamic binding)           |
| Return type   | Can be different               | Must be same or covariant           |
| Inheritance   | Not required                   | Required                            |

### Q4: What is dynamic method dispatch?

**Answer:** Dynamic method dispatch is the mechanism by which a call to an overridden method is resolved at runtime rather than at compile time. When a parent reference points to a child object and an overridden method is called, the JVM determines the actual object type and invokes that object's version of the method.

### Q5: What is upcasting?

**Answer:** Upcasting is assigning a subclass object to a superclass reference, such as \`Animal a = new Dog();\`. It is done implicitly and safely. The reference type determines which methods are accessible at compile time, while the actual object type determines which overridden method runs at runtime.

### Q6: Can return type alone be used to overload a method?

**Answer:** No. Two methods that differ only in return type but have identical names and parameter lists will cause a compilation error. Overloading requires the parameter lists to differ in number, type, or order.

### Q7: Why does a parent reference to a child object call the child's method?

**Answer:** Because of dynamic method dispatch. Java uses late binding for overridden instance methods -- the method to call is chosen based on the actual object type at runtime, not the declared reference type. So \`Animal a = new Dog(); a.move();\` calls \`Dog\`'s \`move()\`.

### Q8: Can static methods be overridden?

**Answer:** No. Static methods belong to the class, not to instances, so they are not subject to dynamic dispatch. If a subclass declares a static method with the same signature, it is *method hiding*, not overriding. The version called depends on the reference type, not the object type.

### Q9: How is polymorphism related to inheritance?

**Answer:** Runtime polymorphism depends on inheritance. A child class must inherit from a parent and override its method for dynamic dispatch to occur. Inheritance establishes the "is-a" relationship that allows a parent reference to hold a child object.

### Q10: What is the benefit of programming to the parent type?

**Answer:** Programming to the parent type (for example, accepting an \`Animal\` parameter instead of \`Dog\`, \`Fish\`, and \`Bird\` separately) makes code reusable and extensible. New subclasses can be added without modifying existing methods, because the correct behavior is selected automatically at runtime.
`}},{slug:`final-keyword`,title:`The final keyword`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:18,accent:`#c084fc`,summary:`The final keyword in Java is a non-access modifier used to apply restrictions. It tells the compiler that something cannot be changed after it has been defined. Depending on where it is used, final restricts a variable, a method, or an entire class.`,parts:{notes:`## Introduction\r
\r
The \`final\` keyword in Java is a non-access modifier used to apply restrictions. It tells the compiler that something cannot be changed after it has been defined. Depending on where it is used, \`final\` restricts a variable, a method, or an entire class.\r
\r
This chapter is split across three files, one for each use:\r
\r
| File | Demonstrates |\r
|------|--------------|\r
| \`FinalVariable.java\` | \`final\` variables (constants and final references) |\r
| \`FinalClassDemo.java\`    | \`final\` classes (cannot be extended)               |\r
| \`FinalMethod.java\`   | \`final\` methods (cannot be overridden)             |\r
\r
---\r
\r
## What is the \`final\` Keyword\r
\r
\`final\` means "this cannot change". It is the keyword you use whenever you want to lock something down so that it cannot be reassigned, overridden, or extended.\r
\r
- A \`final\` **variable** can be assigned only once -- it becomes a constant.\r
- A \`final\` **method** cannot be overridden by a subclass.\r
- A \`final\` **class** cannot be extended (subclassed) at all.\r
\r
---\r
\r
## Three Uses of \`final\`\r
\r
![Three Uses of final](diagrams/18-final-keyword-notes-d6e5f79b.jpg)\r
\r
Each use applies the same idea -- "no further change" -- to a different building block of the language.\r
\r
---\r
\r
## Part 1: \`final\` Variables\r
\r
### Code\r
\r
\`\`\`java FinalVariable.java @run-final-keyword-final-variable
final class Base {\r
    public final void show() {\r
        System.out.println("Base show");\r
    }\r
}\r
\r
/*\r
 * class Derived extends Base {\r
 * // ERROR: Cannot override final method\r
 * public void show() {\r
 * System.out.println("Derived show");\r
 * }\r
 * }\r
 */\r
\r
public class FinalVariable {\r
    final int MAX = 100;\r
\r
    public static void main(String[] args) {\r
        final int AGE = 20;\r
\r
        // AGE = 30; // ERROR: final variable cannot be reassigned\r
        // MAX = 50; // ERROR: final variable cannot be reassigned\r
\r
        final double PI = 3.14159;\r
        System.out.println("Value of PI: " + PI);\r
\r
        double circumference = 2 * PI * 5;\r
        System.out.println("Circumference: " + circumference);\r
\r
        final StringBuilder sb = new StringBuilder("Hello");\r
        sb.append(" World"); // OK: modifying content\r
        System.out.println(sb);\r
\r
        // sb = new StringBuilder("Goodbye"); // ERROR: final reference cannot be reassigned\r
    }\r
}\r
\`\`\`

\`\`\`output @run-final-keyword-final-variable
Value of PI: 3.14159
Circumference: 31.4159
Hello World
\`\`\`\r
\r
### Explanation\r
\r
- \`final int MAX = 100;\` is a final **instance variable** (a constant tied to each object). It cannot be reassigned.\r
- \`final int AGE = 20;\` and \`final double PI = 3.14159;\` are final **local variables**. Once set, they cannot change. Attempting \`AGE = 30;\` is a compile-time error.\r
- Final variables can be freely **read** and used in expressions -- for example, \`2 * PI * 5\`.\r
- \`final StringBuilder sb = new StringBuilder("Hello");\` makes the **reference** \`sb\` final, not the object it points to:\r
  - \`sb.append(" World");\` is allowed -- you are modifying the object's contents.\r
  - \`sb = new StringBuilder("Goodbye");\` is NOT allowed -- you cannot point \`sb\` at a different object.\r
\r
### Key Insight\r
\r
For reference types, \`final\` locks the reference, not the object it points to. The object's internal state can still change.\r
\r
### Expected Output\r
\r
\`\`\`\r
Value of PI: 3.14159\r
Circumference: 31.4159\r
Hello World\r
\`\`\`\r
\r
---\r
\r
## Part 2: \`final\` Classes\r
\r
### Code\r
\r
\`\`\`java FinalClassDemo.java @run-final-keyword-final-class-demo
public class FinalClassDemo {\r
    public static void main(String[] args) {\r
        FinalClass finalClass = new FinalClass();\r
        finalClass.show();\r
    }\r
}\r
\r
final class FinalClass {\r
    public void show() {\r
        System.out.println("Final class method");\r
    }\r
}\r
\r
// class A extends FinalClass {\r
//     public void show() {\r
//         System.out.println("Derived class method");\r
//     }\r
// }\r
\r
/*\r
 * class Derived extends FinalClass {\r
 * // ERROR: Cannot extend final class\r
 * public void show() {\r
 * System.out.println("Derived class method");\r
 * }\r
 * }\r
 */\r
\`\`\`

\`\`\`output @run-final-keyword-final-class-demo
Final class method
\`\`\`\r
\r
### Explanation\r
\r
- \`FinalClass\` is declared \`final\`, so it cannot be extended. The commented-out \`class A extends FinalClass\` and \`class Derived extends FinalClass\` would both cause compile-time errors.\r
- You can still **create objects** of a final class and call its methods, as \`main\` does with \`new FinalClass()\` and \`finalClass.show()\`.\r
- This is exactly how Java's own \`String\` class is built -- it is \`final\` so no one can subclass and alter its behavior.\r
\r
### Why Make a Class \`final\`?\r
\r
- To prevent inheritance for security or design reasons.\r
- To guarantee that the class's behavior cannot be altered by a subclass.\r
- To make the class safely immutable.\r
\r
### Expected Output\r
\r
\`\`\`\r
Final class method\r
\`\`\`\r
\r
---\r
\r
## Part 3: \`final\` Methods\r
\r
### Code\r
\r
\`\`\`java FinalMethod.java @run-final-keyword-final-method
class A {\r
    public final void show() {\r
        System.out.println("A show");\r
    }\r
}\r
\r
class B extends A {\r
    // public void show() {\r
    //     System.out.println("B show");\r
    // }\r
}\r
\r
public class FinalMethod {\r
    public static void main(String[] args) {\r
        B b = new B();\r
        b.show();\r
    }\r
}\r
\`\`\`

\`\`\`output @run-final-keyword-final-method
A show
\`\`\`\r
\r
### Explanation\r
\r
- \`A\` declares \`show()\` as \`final\`. This means no subclass is allowed to override it.\r
- In \`B\`, the \`show()\` override is **commented out**, so \`B\` simply inherits \`A\`'s final \`show()\`. The program compiles and \`b.show()\` prints \`A show\`.\r
- If you **uncomment** \`B\`'s \`show()\`, the file fails to compile with: *"show() in B cannot override show() in A; overriding method is final"* -- which is exactly the rule this example teaches.\r
- A \`final\` method can still be **inherited** and **called** -- it just cannot be **redefined**.\r
\r
### Key Insight\r
\r
\`final\` on a method blocks overriding (changing behavior in a subclass), not inheritance.\r
\r
### Why Make a Method \`final\`?\r
\r
- To preserve critical behavior that subclasses must not change.\r
- To prevent accidental overriding of methods that the class relies on internally.\r
\r
---\r
\r
## Summary Table\r
\r
| Applied To | Meaning                                   | What is Blocked            | Still Allowed                        |\r
|------------|-------------------------------------------|----------------------------|--------------------------------------|\r
| Variable   | Value/reference assigned only once        | Reassignment               | Reading; modifying referenced object |\r
| Method     | Cannot be redefined in a subclass         | Overriding                 | Inheriting and calling the method    |\r
| Class      | Cannot be subclassed                      | Extending (\`extends\`)      | Creating objects of the class        |\r
\r
---\r
\r
## How to Compile and Run\r
\r
None of these files declares a package. Compile and run them from the parent directory using the package path:\r
\r
\`\`\`bash\r
# Final variable demo\r
javac FinalVariable.java\r
java FinalVariable\r
\r
# Final class demo\r
javac FinalClassDemo.java\r
java FinalClassDemo\r
\r
# Final method demo (override in B is commented out, so it prints "A show")\r
javac FinalMethod.java\r
java FinalMethod\r
\`\`\`\r
\r
> In \`FinalMethod.java\`, \`B\`'s \`show()\` override is commented out, so the file compiles and prints \`A show\`. Uncommenting that override produces the compile-time error "overriding method is final" -- which is the lesson the example teaches.\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Thinking \`final\` on a reference makes the object immutable\r
\r
\`\`\`java\r
final StringBuilder sb = new StringBuilder("Hello");\r
sb.append(" World");\r
sb = new StringBuilder("Hi");  // Error -- the reference IS final\r
\`\`\`

\`\`\`expected
sb.append(" World")  ->  works. sb now holds "Hello World".
sb = new StringBuilder("Hi")  ->  error: cannot assign a value to final
                                   variable sb

final freezes the reference, not the object. The name cannot be pointed
somewhere else; the thing it points at can change all it likes.
\`\`\`\r
\r
\`final\` locks the reference, not the contents of the object it points to.\r
\r
### Mistake 2: Confusing \`final\` with \`finally\` and \`finalize()\`\r
\r
- \`final\` -- a keyword that restricts variables, methods, and classes.\r
- \`finally\` -- a block in exception handling that always runs.\r
- \`finalize()\` -- a (deprecated) method called by the garbage collector before an object is destroyed.\r
\r
These three are unrelated despite the similar names.\r
\r
### Mistake 3: Trying to override a \`final\` method\r
\r
A subclass cannot override a \`final\` method (as \`FinalMethod.java\` shows). The compiler rejects it. To allow overriding, remove \`final\` from the parent method.\r
\r
### Mistake 4: Trying to extend a \`final\` class\r
\r
A \`final\` class cannot appear after \`extends\`. Use composition (holding an instance of the class) instead of inheritance.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`FinalVariable.java\`\r
\r
\`\`\`java FinalVariable.java @run-final-keyword-final-variable\r
final class Base {\r
    public final void show() {\r
        System.out.println("Base show");\r
    }\r
}\r
\r
/*\r
 * class Derived extends Base {\r
 * // ERROR: Cannot override final method\r
 * public void show() {\r
 * System.out.println("Derived show");\r
 * }\r
 * }\r
 */\r
\r
public class FinalVariable {\r
    final int MAX = 100;\r
\r
    public static void main(String[] args) {\r
        final int AGE = 20;\r
\r
        // AGE = 30; // ERROR: final variable cannot be reassigned\r
        // MAX = 50; // ERROR: final variable cannot be reassigned\r
\r
        final double PI = 3.14159;\r
\r
        System.out.println("Value of PI: " + PI);\r
\r
        double circumference = 2 * PI * 5;\r
        System.out.println("Circumference: " + circumference);\r
\r
        final StringBuilder sb = new StringBuilder("Hello");\r
        sb.append(" World"); // OK: modifying content\r
        System.out.println(sb);\r
\r
        // sb = new StringBuilder("Goodbye"); // ERROR: final reference cannot be\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-final-keyword-final-variable\r
Value of PI: 3.14159\r
Circumference: 31.4159\r
Hello World\r
\`\`\`\r
\r
### \`FinalClassDemo.java\`\r
\r
\`\`\`java FinalClassDemo.java @run-final-keyword-final-class-demo\r
public class FinalClassDemo {\r
    public static void main(String[] args) {\r
        FinalClass finalClass = new FinalClass();\r
        finalClass.show();\r
    }\r
}\r
\r
final class FinalClass {\r
    public void show() {\r
        System.out.println("Final class method");\r
    }   \r
}\r
\r
// class A extends FinalClass {\r
//     public void show() {\r
//         System.out.println("Derived class method");\r
//     }\r
// }\r
\r
/*\r
 * class Derived extends FinalClass {\r
 * // ERROR: Cannot extend final class\r
 * public void show() {\r
 * System.out.println("Derived class method");\r
 * }\r
 * }\r
 */\r
\`\`\`\r
\r
\`\`\`output @run-final-keyword-final-class-demo\r
Final class method\r
\`\`\`\r
\r
### \`FinalMethod.java\`\r
\r
\`\`\`java FinalMethod.java @run-final-keyword-final-method\r
class A {\r
    public final void show() {\r
        System.out.println("A show");\r
    }\r
}\r
\r
class B extends A {\r
    // public void show() {\r
    //     System.out.println("B show");\r
    // }\r
}\r
\r
public class FinalMethod {\r
    public static void main(String[] args) {\r
        B b = new B();\r
        b.show();\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-final-keyword-final-method\r
A show\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## Three uses\r
\r
- **\`final\` variable** — assign once. It may be assigned at the declaration, in\r
  an instance initialiser, or in every constructor, but not twice.\r
- **\`final\` method** — cannot be overridden. It is still inherited and still\r
  callable; only replacing it is forbidden.\r
- **\`final\` class** — cannot be extended. \`String\` is final so that nobody can\r
  subclass it and break the immutability the pool and the security model rely on.\r
\r
## What \`final\` does not do\r
\r
- \`final\` on a reference freezes the **reference**, not the object. A\r
  \`final List\` can still have things added to it; you just cannot point the name\r
  at a different list.\r
- Immutability is a property of the class, not of the keyword. \`final\` gives you\r
  one of the ingredients.\r
- A final class may have perfectly ordinary non-final methods — nothing can\r
  override them anyway, because nothing can extend the class.\r
- Constructors cannot be \`final\`. They are not inherited, so there is nothing the\r
  keyword could prevent.\r
\r
## The three lookalikes\r
\r
- **\`final\`** — a modifier: assign once, do not override, do not extend.\r
- **\`finally\`** — a block after \`try\`, which runs whether or not an exception was\r
  thrown.\r
- **\`finalize()\`** — a method the collector used to call before reclaiming an\r
  object. Deprecated, unpredictable, and not to be relied on.\r
\r
## When to reach for it\r
\r
- On fields that genuinely never change after construction — it documents the\r
  intent and the compiler enforces it.\r
- On a method whose behaviour a subclass must not alter for the class to stay\r
  correct.\r
- \`static final\` for constants, named in \`UPPER_SNAKE_CASE\`.\r
`,interview:'## Interview questions\n\n### Q1: What are the three uses of the `final` keyword in Java?\n\n**Answer:** `final` can be applied to a variable (it becomes a constant that can be assigned only once), a method (it cannot be overridden by a subclass), and a class (it cannot be extended/subclassed).\n\n### Q2: Does `final` make an object immutable?\n\n**Answer:** No. When applied to a reference variable, `final` only prevents the reference from being reassigned. The object\'s internal state can still be changed. For example, a `final StringBuilder` can still have text appended to it, but the variable cannot be pointed at a new `StringBuilder`.\n\n### Q3: Can a `final` method be inherited?\n\n**Answer:** Yes. A `final` method is inherited by subclasses and can be called normally. `final` only prevents the method from being *overridden*, not from being inherited or used.\n\n### Q4: Why is the `String` class declared `final`?\n\n**Answer:** To guarantee immutability and security. Because `String` cannot be subclassed, no one can create a malicious subclass that changes string behavior, and the JVM can safely cache and share string literals.\n\n### Q5: What is the difference between `final`, `finally`, and `finalize()`?\n\n**Answer:** `final` is a modifier that restricts variables, methods, and classes. `finally` is a block used with try-catch that always executes regardless of whether an exception occurs. `finalize()` is a method historically called by the garbage collector before reclaiming an object (now deprecated).\n\n### Q6: Can a `final` variable be initialized later (not at declaration)?\n\n**Answer:** Yes, a `final` variable can be a "blank final" -- declared without a value and assigned exactly once, either in a constructor (for instance variables) or before first use (for local variables). After that single assignment, it cannot be changed.\n\n### Q7: Can a constructor be declared `final`?\n\n**Answer:** No. Constructors are never inherited and therefore cannot be overridden, so marking a constructor `final` is meaningless and is a compile-time error.\n\n### Q8: What happens if you try to override a `final` method?\n\n**Answer:** The code fails to compile with an error such as "overridden method is final". The subclass is not permitted to provide a new implementation. This is exactly what `FinalMethod.java` demonstrates.\n\n### Q9: Can a `final` class have non-final methods?\n\n**Answer:** Yes. A `final` class can contain ordinary (non-final) methods, as `FinalClass` does with `show()`. Since the class itself cannot be subclassed, those methods can never be overridden anyway, so marking them `final` is unnecessary.\n\n### Q10: When should you use the `final` keyword?\n\n**Answer:** Use `final` for constants (`final` variables) to express values that must not change, for methods whose behavior is critical and must not be altered by subclasses, and for classes that are designed to be complete and not extended (often for immutability or security).\n'}},{slug:`object-methods`,title:`Object methods: toString, equals, hashCode`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:19,accent:`#60a5fa`,summary:`In Java, every class implicitly extends the Object class, inheriting a set of methods that every object has -- including toString(), equals(), and hashCode(). By default these methods work in terms of an object's identity (its memory address), which is rarely what we want. This chapter shows how to override all three so objects have a meaningful text form and are compared by their data instead of their identity.`,parts:{notes:`## Introduction\r
\r
In Java, every class implicitly extends the \`Object\` class, inheriting a set of methods that every object has -- including \`toString()\`, \`equals()\`, and \`hashCode()\`. By default these methods work in terms of an object's identity (its memory address), which is rarely what we want. This chapter shows how to **override all three** so objects have a meaningful text form and are compared by their data instead of their identity.\r
\r
This program defines a \`Laptop\` class that overrides \`toString()\`, \`equals()\`, and \`hashCode()\`, then demonstrates how each behaves and how they relate to the \`==\` operator.\r
\r
---\r
\r
## What is an Object\r
\r
An object is an instance of a class -- a concrete "thing" created from a class blueprint, holding its own copy of the fields defined by that class.\r
\r
\`\`\`java\r
Laptop obj1 = new Laptop();\r
obj1.brand = "Dell";\r
obj1.price = 50000;\r
\`\`\`\r
\r
- \`new Laptop()\` creates the object in memory.\r
- \`obj1\` is a **reference** -- it stores the address of that object, not the object itself.\r
- Two references can point to the same object, or to two different objects that happen to hold the same data. Telling these cases apart is what \`==\`, \`equals()\`, and \`hashCode()\` are about.\r
\r
---\r
\r
## The \`Object\` Class and Its Methods\r
\r
\`Object\` (in \`java.lang\`) is the root of every class hierarchy. The three methods we override most often are:\r
\r
| Method        | Default Behavior                                  | Why Override It                                  |\r
|---------------|---------------------------------------------------|--------------------------------------------------|\r
| \`toString()\`  | Returns \`ClassName@hexHashCode\`                   | To print a readable description of the object    |\r
| \`equals()\`    | Compares references (same as \`==\`)                | To compare objects by their data (field values)  |\r
| \`hashCode()\`  | A number based on the object's identity           | To keep equal objects consistent in hash collections |\r
\r
These three are commonly overridden together because they describe what an object *is* and when two objects are *the same*.\r
\r
---\r
\r
## The \`toString()\` Method\r
\r
The default \`toString()\` returns something like \`Laptop@1b6d3586\` (class name + \`@\` + hash code in hex). By overriding it we control what gets printed:\r
\r
\`\`\`java\r
@Override\r
public String toString() {\r
    return "Laptop{brand='" + brand + "', price=" + price + "}";\r
}\r
\`\`\`\r
\r
\`toString()\` is called **automatically** whenever an object is used where a \`String\` is expected:\r
\r
\`\`\`java\r
System.out.println("obj1.toString() : " + obj1);\r
\`\`\`\r
\r
---\r
\r
## Object Equality: \`==\` vs \`equals()\`\r
\r
There are two very different questions you can ask about two objects:\r
\r
| Comparison         | Question Asked                          | Compares       |\r
|--------------------|-----------------------------------------|----------------|\r
| \`obj1 == obj2\`     | Are these the *same object* in memory?  | References     |\r
| \`obj1.equals(obj2)\`| Do these objects *hold the same data*?  | Field values (when overridden) |\r
\r
\`\`\`java\r
obj1 == obj2;\r
obj1.equals(obj2);\r
\`\`\`\r
\r
A correct \`equals()\` override examines the fields:\r
\r
\`\`\`java\r
@Override\r
public boolean equals(Object o) {\r
    if (this == o) return true;\r
    if (o == null || getClass() != o.getClass())\r
        return false;\r
    Laptop other = (Laptop) o;\r
    return price == other.price && Objects.equals(brand, other.brand);\r
}\r
\`\`\`\r
\r
> Note the parameter type is \`Object\`, not \`Laptop\`. This is what makes it a genuine override of \`Object.equals\`. Using \`equals(Laptop)\` would create an *overload* that collections never call.\r
\r
---\r
\r
## The \`hashCode()\` Method\r
\r
A hash code is an \`int\` derived from an object's data. Hash-based collections (\`HashMap\`, \`HashSet\`) use it to decide which "bucket" an object goes into. The rule: **equal objects must have equal hash codes**. So whenever you override \`equals()\`, you must also override \`hashCode()\`:\r
\r
\`\`\`java\r
@Override\r
public int hashCode() {\r
    return Objects.hash(brand, price);\r
}\r
\`\`\`\r
\r
\`Objects.hash(...)\` builds a combined hash from the listed fields -- the same fields used in \`equals()\`.\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java ObjectsDemo.java @run-object-methods-objects-demo
import java.util.Objects;\r
\r
class Laptop {\r
    String brand;\r
    int price;\r
\r
    @Override\r
    public String toString() {\r
        return "Laptop{brand='" + brand + "', price=" + price + "}";\r
    }\r
\r
    @Override\r
    public boolean equals(Object o) {\r
        if (this == o)\r
            return true;\r
        if (o == null || getClass() != o.getClass())\r
            return false;\r
        Laptop other = (Laptop) o;\r
        return price == other.price && Objects.equals(brand, other.brand);\r
    }\r
\r
    @Override\r
    public int hashCode() {\r
        return Objects.hash(brand, price);\r
    }\r
}\r
\r
public class ObjectsDemo {\r
    public static void main(String[] args) {\r
        Laptop obj1 = new Laptop();\r
        obj1.brand = "Dell";\r
        obj1.price = 50000;\r
\r
        Laptop obj2 = new Laptop();\r
        obj2.brand = "Dell";\r
        obj2.price = 50000;\r
\r
        Laptop obj3 = new Laptop();\r
        obj3.brand = "HP";\r
        obj3.price = 60000;\r
\r
        System.out.println("obj1.toString() : " + obj1);\r
        System.out.println("obj3.toString() : " + obj3);\r
\r
        System.out.println("obj1 == obj2      : " + (obj1 == obj2));\r
        System.out.println("obj1.equals(obj2) : " + obj1.equals(obj2));\r
        System.out.println("obj1.equals(obj3) : " + obj1.equals(obj3));\r
\r
        System.out.println("obj1.hashCode()   : " + obj1.hashCode());\r
        System.out.println("obj2.hashCode()   : " + obj2.hashCode());\r
        System.out.println("obj3.hashCode()   : " + obj3.hashCode());\r
    }\r
}\r
\`\`\`

\`\`\`output @run-object-methods-objects-demo
obj1.toString() : Laptop{brand='Dell', price=50000}
obj3.toString() : Laptop{brand='HP', price=60000}
obj1 == obj2      : false
obj1.equals(obj2) : true
obj1.equals(obj3) : false
obj1.hashCode()   : 65966416
obj2.hashCode()   : 65966416
obj3.hashCode()   : 132633
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### \`import java.util.Objects;\`\r
\r
- Imports the \`Objects\` utility class, which provides the null-safe helpers \`Objects.equals(a, b)\` and \`Objects.hash(...)\` used inside our overrides.\r
\r
### The \`Laptop\` fields\r
\r
\`\`\`java\r
String brand;\r
int price;\r
\`\`\`\r
\r
- The two fields that define a laptop -- and that we use to decide equality and to build the hash code.\r
\r
### \`toString()\`\r
\r
\`\`\`java\r
@Override\r
public String toString() {\r
    return "Laptop{brand='" + brand + "', price=" + price + "}";\r
}\r
\`\`\`\r
\r
- Replaces the default \`Laptop@hashcode\` output with a readable description.\r
\r
### \`equals(Object o)\`\r
\r
\`\`\`java\r
if (this == o) return true;\r
if (o == null || getClass() != o.getClass()) return false;\r
Laptop other = (Laptop) o;\r
return price == other.price && Objects.equals(brand, other.brand);\r
\`\`\`\r
\r
- **Step 1:** if both references are the same object, they are equal.\r
- **Step 2:** if the other object is \`null\` or a different class, they are not equal.\r
- **Step 3:** cast to \`Laptop\` and compare the defining fields. \`price\` is a primitive, compared with \`==\`; \`brand\` is a \`String\`, compared null-safely with \`Objects.equals\`.\r
\r
### \`hashCode()\`\r
\r
\`\`\`java\r
@Override\r
public int hashCode() {\r
    return Objects.hash(brand, price);\r
}\r
\`\`\`\r
\r
- Produces a hash from the same fields used in \`equals()\`, so equal laptops always get the same hash code.\r
\r
### \`main\` -- creating objects\r
\r
- \`obj1\` and \`obj2\` hold identical data (\`Dell\`, \`50000\`); \`obj3\` is different (\`HP\`, \`60000\`).\r
\r
### \`main\` -- the comparisons\r
\r
- \`obj1 == obj2\` checks references -> \`false\` (different objects).\r
- \`obj1.equals(obj2)\` checks data -> \`true\`; \`obj1.equals(obj3)\` -> \`false\`.\r
- \`hashCode()\` is identical for the equal pair \`obj1\`/\`obj2\`, and different for \`obj3\`.\r
\r
---\r
\r
## Step-by-Step Execution Walkthrough\r
\r
| Statement                | What Happens                                      | Output                                 |\r
|--------------------------|---------------------------------------------------|----------------------------------------|\r
| \`"... " + obj1\`          | Calls \`obj1.toString()\`                            | Laptop{brand='Dell', price=50000}      |\r
| \`"... " + obj3\`          | Calls \`obj3.toString()\`                            | Laptop{brand='HP', price=60000}        |\r
| \`obj1 == obj2\`           | Reference comparison of two distinct objects       | false                                  |\r
| \`obj1.equals(obj2)\`      | Field comparison; same brand and price             | true                                   |\r
| \`obj1.equals(obj3)\`      | Field comparison; different brand and price        | false                                  |\r
| \`obj1.hashCode()\`        | Hash from ("Dell", 50000)                          | 65966416                               |\r
| \`obj2.hashCode()\`        | Same data as obj1 -> same hash                     | 65966416                               |\r
| \`obj3.hashCode()\`        | Hash from ("HP", 60000)                            | 132633                                 |\r
\r
**Key Insight:** \`obj1\` and \`obj2\` are different objects (\`==\` is false) yet are *equal* by data (\`equals()\` is true) and therefore share the **same hash code** -- exactly what hash-based collections require.\r
\r
---\r
\r
## The \`equals()\` / \`hashCode()\` Contract\r
\r
- If \`a.equals(b)\` is \`true\`, then \`a.hashCode()\` must equal \`b.hashCode()\`.\r
- The reverse is **not** required: two unequal objects *may* share a hash code (a "collision"); that is allowed and handled by the collection.\r
- Therefore, **whenever you override \`equals()\`, you must also override \`hashCode()\`** using the same fields. Overriding only one breaks \`HashMap\`/\`HashSet\`.\r
\r
A correct \`equals()\` must also be:\r
\r
- **Reflexive:** \`a.equals(a)\` is true.\r
- **Symmetric:** \`a.equals(b)\` implies \`b.equals(a)\`.\r
- **Transitive:** \`a.equals(b)\` and \`b.equals(c)\` imply \`a.equals(c)\`.\r
- **Consistent:** repeated calls return the same result.\r
- \`a.equals(null)\` is always false.\r
\r
> Most IDEs can generate correct \`equals()\`, \`hashCode()\`, and \`toString()\` for you via "Source Action" -- the same menu used to generate getters and setters.\r
\r
---\r
\r
## How to Compile and Run\r
\r
The file declares no package declaration, so compile and run from the parent directory using the package path:\r
\r
\`\`\`bash\r
javac ObjectsDemo.java\r
java ObjectsDemo\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
obj1.toString() : Laptop{brand='Dell', price=50000}\r
obj3.toString() : Laptop{brand='HP', price=60000}\r
obj1 == obj2      : false\r
obj1.equals(obj2) : true\r
obj1.equals(obj3) : false\r
obj1.hashCode()   : 65966416\r
obj2.hashCode()   : 65966416\r
obj3.hashCode()   : 132633\r
\`\`\`\r
\r
> The exact hash code numbers come from \`Objects.hash(brand, price)\` and depend only on the field values, so they are reproducible for the same data.\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Using \`==\` to compare object contents\r
\r
\`\`\`java\r
if (obj1 == obj2) { ... }\r
\`\`\`

\`\`\`expected
false, even when every field matches.

== on objects asks whether the two references point at one object. For
"do these hold the same values" you need equals(), and you have to write it.
\`\`\`\r
\r
\`==\` is \`true\` only when both references point to the exact same object. To compare data, use \`equals()\`.\r
\r
### Mistake 2: Overriding \`equals()\` but not \`hashCode()\`\r
\r
This breaks \`HashSet\` and \`HashMap\`: two "equal" objects can land in different buckets, so lookups and de-duplication fail. Always override both, using the same fields.\r
\r
### Mistake 3: Writing \`equals(Laptop)\` instead of \`equals(Object)\`\r
\r
\`\`\`java\r
public boolean equals(Laptop other) { ... }\r
\`\`\`

\`\`\`expected
This compiles and is never called by anything.

It is an overload, not an override — the collections all call
equals(Object). Adding @Override to it turns this into a compile error,
which is the whole reason to write @Override.
\`\`\`\r
\r
The real \`Object.equals\` takes an \`Object\`. With the wrong parameter type you create a new overloaded method, and collections still call the inherited \`equals(Object)\`. Use \`@Override\` so the compiler catches this.\r
\r
### Mistake 4: Using \`==\` to compare Strings inside \`equals()\`\r
\r
\`\`\`java\r
return brand == other.brand;\r
return Objects.equals(brand, other.brand);  // correct, null-safe content compare\r
\`\`\`

\`\`\`expected
The first is true only when both brands are the very same String object,
so it passes in testing on pooled literals and fails on anything read from
input. The second compares characters and survives either side being null.
\`\`\`\r
\r
### Mistake 5: Forgetting \`@Override\`\r
\r
Without it, a small signature mistake silently becomes a new method instead of an override, and the bug is hard to spot. The annotation makes the compiler verify the override.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`ObjectsDemo.java\`\r
\r
Every class inherits \`toString()\`, \`equals()\` and \`hashCode()\` from \`Object\`,\r
and by default all three work on **identity** â€” the object's address in memory.\r
\`Laptop\` overrides them so that two laptops with the same brand and price count\r
as equal:\r
\r
- **\`toString()\`** is called automatically whenever an object is printed or\r
  joined to a \`String\`. Without the override you would see \`Laptop@1b6d35\`.\r
- **\`equals(Object o)\`** takes an \`Object\`, not a \`Laptop\`. That matters: narrow\r
  the parameter type and you have written an *overload*, not an override, and\r
  nothing will call it.\r
- **\`hashCode()\`** must be overridden whenever \`equals()\` is. Equal objects are\r
  required to produce equal hash codes, and \`HashMap\` and \`HashSet\` depend on\r
  that contract holding.\r
\r
\`java.util.Objects\` supplies the null-safe helpers the overrides are built from â€”\r
\`Objects.equals(...)\` and \`Objects.hash(...)\`.\r
\r
In \`main\`, \`obj1\` and \`obj2\` carry identical data and \`obj3\` differs. Watch the\r
output: \`==\` is **false** for \`obj1\` and \`obj2\` because they are two separate\r
objects, while \`equals()\` is **true** because their data matches â€” and their hash\r
codes come out identical, exactly as the contract demands.\r
\r
\`\`\`java ObjectsDemo.java @run-object-methods-objects-demo\r
import java.util.Objects;\r
\r
class Laptop {\r
    String brand;\r
    int price;\r
\r
    @Override\r
    public String toString() {\r
        return "Laptop{brand='" + brand + "', price=" + price + "}";\r
    }\r
\r
    @Override\r
    public boolean equals(Object o) {\r
        if (this == o)\r
            return true;\r
\r
        if (o == null || getClass() != o.getClass())\r
            return false;\r
\r
        Laptop other = (Laptop) o;\r
        return price == other.price && Objects.equals(brand, other.brand);\r
    }\r
\r
    @Override\r
    public int hashCode() {\r
        return Objects.hash(brand, price);\r
    }\r
}\r
\r
public class ObjectsDemo {\r
    public static void main(String[] args) {\r
        Laptop obj1 = new Laptop();\r
        obj1.brand = "Dell";\r
        obj1.price = 50000;\r
\r
        Laptop obj2 = new Laptop();\r
        obj2.brand = "Dell";\r
        obj2.price = 50000;\r
\r
        Laptop obj3 = new Laptop();\r
        obj3.brand = "HP";\r
        obj3.price = 60000;\r
\r
        System.out.println("obj1.toString() : " + obj1);\r
        System.out.println("obj3.toString() : " + obj3);\r
\r
        System.out.println("obj1 == obj2      : " + (obj1 == obj2));\r
\r
        System.out.println("obj1.equals(obj2) : " + obj1.equals(obj2));\r
        System.out.println("obj1.equals(obj3) : " + obj1.equals(obj3));\r
\r
        System.out.println("obj1.hashCode()   : " + obj1.hashCode());\r
        System.out.println("obj2.hashCode()   : " + obj2.hashCode());\r
        System.out.println("obj3.hashCode()   : " + obj3.hashCode());\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-object-methods-objects-demo\r
obj1.toString() : Laptop{brand='Dell', price=50000}\r
obj3.toString() : Laptop{brand='HP', price=60000}\r
obj1 == obj2      : false\r
obj1.equals(obj2) : true\r
obj1.equals(obj3) : false\r
obj1.hashCode()   : 65966416\r
obj2.hashCode()   : 65966416\r
obj3.hashCode()   : 132633\r
\`\`\`\r
`,revision:"The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## What every object inherits\r\n\r\n- Every class extends `Object`, so every object has `toString`, `equals`,\r\n  `hashCode`, `getClass` and the rest — whether you wrote them or not.\r\n- The default `toString` prints `ClassName@hexHashCode`, which identifies the\r\n  object and says nothing about it. Override it and every log line and debugger\r\n  view improves at once.\r\n- The default `equals` is `==`: identity, not contents. Two objects with\r\n  identical fields are unequal until you say otherwise.\r\n\r\n## `equals` and `hashCode`\r\n\r\n- Override one and you must override the other. The contract is: equal objects\r\n  **must** have equal hash codes.\r\n- Break it and hash-based collections misbehave — an object put into a `HashSet`\r\n  is not found again, because the lookup went to a different bucket.\r\n- The converse is allowed: two unequal objects **may** share a hash code. That is\r\n  a collision, and it is normal.\r\n- The parameter must be `Object`. `equals(Laptop other)` is an **overload**, not\r\n  an override, so the collections that call `equals(Object)` never reach it —\r\n  which is what `@Override` catches.\r\n- Compare String fields inside `equals` with `.equals()`, not `==`. The `==`\r\n  works in testing because the literals are pooled, and fails on strings built at\r\n  run time.\r\n\r\n## The contract, and the helpers\r\n\r\n- Reflexive, symmetric, transitive, consistent, and `x.equals(null)` is false.\r\n- `java.util.Objects` has the null-safe versions: `Objects.equals(a, b)`,\r\n  `Objects.hash(a, b, c)`, `Objects.requireNonNull(x)`.\r\n- A `record` generates `equals`, `hashCode` and `toString` from its components,\r\n  correctly, and is usually the better answer for a plain data carrier.\r\n",interview:"## Interview questions\n\n### Q1: What is the `Object` class in Java?\n\n**Answer:** `Object` (in `java.lang`) is the root of the class hierarchy. Every class directly or indirectly extends it, inheriting methods such as `toString()`, `equals()`, `hashCode()`, `getClass()`, `clone()`, `wait()`, and `notify()`.\n\n### Q2: What does the default `toString()` return, and why override it?\n\n**Answer:** It returns the class name, `@`, and the object's hash code in hex (e.g. `Laptop@1b6d3586`). We override it to provide a meaningful, human-readable description of the object's state.\n\n### Q3: What is the difference between `==` and `equals()`?\n\n**Answer:** `==` compares references -- whether two variables point to the same object (for primitives, it compares values). `equals()` is a method that, when overridden, compares the logical content of two objects. By default (not overridden), `equals()` behaves like `==`.\n\n### Q4: Why must you override `hashCode()` when you override `equals()`?\n\n**Answer:** Because of the contract: equal objects must have equal hash codes. Hash-based collections use `hashCode()` to pick a bucket and `equals()` to confirm a match. If equal objects had different hash codes, they could be placed in different buckets and never be recognized as equal.\n\n### Q5: Can two unequal objects have the same hash code?\n\n**Answer:** Yes. That is called a hash collision and is perfectly legal. The contract only requires equal objects to share a hash code, not that unequal objects have different ones. Collections resolve collisions by also calling `equals()`.\n\n### Q6: What is the `java.util.Objects` class?\n\n**Answer:** A utility class of static helper methods for working with objects safely, including `Objects.equals(a, b)` (null-safe equality), `Objects.hash(...)` (combined hash code), `Objects.isNull`, `Objects.nonNull`, `Objects.requireNonNull`, and `Objects.toString`.\n\n### Q7: Why must the parameter of `equals()` be `Object`?\n\n**Answer:** Because `Object.equals(Object)` is the method being overridden. If you declare `equals(Laptop)`, you create an overload, not an override, and Java's collections -- which call `equals(Object)` -- will ignore it. `@Override` makes the compiler enforce the correct signature.\n\n### Q8: What does the `@Override` annotation do?\n\n**Answer:** It tells the compiler that a method is intended to override a superclass method. If it does not actually override anything (e.g. due to a wrong signature), the compiler reports an error, preventing subtle bugs.\n\n### Q9: What is the full contract of a correct `equals()` method?\n\n**Answer:** It must be reflexive (`a.equals(a)` is true), symmetric (`a.equals(b)` implies `b.equals(a)`), transitive (if `a.equals(b)` and `b.equals(c)` then `a.equals(c)`), consistent (repeated calls give the same result), and `a.equals(null)` must be false.\n\n### Q10: How do you compare two Strings correctly?\n\n**Answer:** Use `.equals()` (or `.equalsIgnoreCase()` for case-insensitive comparison), or `Objects.equals()` for a null-safe version. Avoid `==`, which compares references and may return `false` even for identical text when the strings are separate objects.\n"}},{slug:`upcasting-downcasting`,title:`Upcasting and downcasting`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:20,accent:`#38bdf8`,summary:`Casting means converting a value from one type to another. Java uses the word "casting" for two related but quite different ideas:`,parts:{notes:`## Introduction\r
\r
**Casting** means converting a value from one type to another. Java uses the word\r
"casting" for two related but quite different ideas:\r
\r
1. **Primitive type casting** -- converting between number types like \`int\`,\r
   \`double\`, \`byte\`, and \`char\`.\r
2. **Reference (object) casting** -- viewing an object through a different type\r
   in its inheritance hierarchy (**upcasting** and **downcasting**).\r
\r
This chapter covers all three across three programs:\r
\r
| File | Demonstrates |\r
|------|--------------|\r
| \`TypeCasting.java\` | Widening and narrowing of **primitive** types |\r
| \`UpCasting.java\`   | **Upcasting** a subclass object (\`B\`) to a superclass reference (\`A\`) |\r
| \`DownCasting.java\` | **Downcasting** a superclass reference (\`A\`) back to the subclass (\`B\`) |\r
\r
Both object-casting examples use the same simple hierarchy: a superclass \`A\` with\r
a method \`showA()\`, and a subclass \`B extends A\` that adds \`showB()\`. In each file\r
\`A\` and \`B\` are declared as **static nested classes** so that both files can keep\r
the same \`A\`/\`B\` names and still compile together in one package without clashing.\r
\r
---\r
\r
## Two Different Worlds of Casting\r
\r
| | Primitive casting | Reference casting |\r
|---|---|---|\r
| Works on | \`int\`, \`double\`, \`byte\`, \`char\`, ... | objects (class instances) |\r
| Changes | the actual value/representation | only the *view* (the reference type) |\r
| Risk | data loss (truncation, overflow) | \`ClassCastException\` at runtime |\r
| Directions | widening / narrowing | upcasting / downcasting |\r
\r
The key mental model: **primitive casting changes the data; reference casting\r
never changes the object -- it only changes which members you are allowed to\r
see.**\r
\r
---\r
\r
## Primitive Type Casting\r
\r
Java's numeric primitives can be ordered by size/capacity:\r
\r
![Primitive Type Casting](diagrams/20-upcasting-downcasting-notes-c5fe2384.jpg)\r
\r
### Widening (Implicit)\r
\r
Moving **up** this chain (small type into a bigger one) is always safe, so Java\r
does it automatically -- no cast needed, no data lost.\r
\r
\`\`\`java\r
int a = 10;\r
long b = a;\r
double c = b;\r
\`\`\`\r
\r
Both assignments happen without a cast. \`c\` prints as \`10.0\` — the value is\r
unchanged, only the type it is held in got wider.\r
\r
### Narrowing (Explicit)\r
\r
Moving **down** the chain (big type into a smaller one) may lose data, so Java\r
forces you to write an explicit \`(type)\` cast to show you accept the risk.\r
\r
\`\`\`java\r
double d = 9.99;\r
int e = (int) d;\r
\r
int big = 257;\r
byte sb = (byte) big;\r
\r
int code = 65;\r
char ch = (char) code;\r
\`\`\`\r
\r
Three different ways of losing something:\r
\r
- \`e\` is **\`9\`** — the fraction is dropped, not rounded.\r
- \`sb\` is **\`1\`** — a \`byte\` only holds \`-128..127\`, so \`257\` overflows and wraps\r
  around.\r
- \`ch\` is **\`'A'\`** — \`65\` is the character code for \`A\`, so nothing is lost\r
  here; the number is simply reinterpreted as a character.\r
\r
> Narrowing does **not** round. \`(int) 9.99\` is \`9\`, and \`(int) 9.0001\` is also \`9\`.\r
\r
---\r
\r
## Reference Casting: Upcasting\r
\r
**Upcasting** treats a subclass object through a superclass reference. It is\r
**implicit** (no cast needed) and **always safe**, because a subclass object\r
genuinely *is* a kind of its superclass.\r
\r
\`\`\`java\r
B b = new B();\r
A ref = b;\r
ref.showA();      // OK: showA() is declared in A\r
// ref.showB();   // ERROR: showB() is not declared in A\r
\`\`\`\r
\r
The rule at play: **the reference type decides what is visible.** Through an \`A\`\r
reference you can only call methods declared in \`A\`, even though the object is\r
really a \`B\`.\r
\r
---\r
\r
## Reference Casting: Downcasting\r
\r
**Downcasting** converts a superclass reference back to the subclass type, so the\r
subclass-specific members become available again. It must be written\r
**explicitly**, and it is only safe if the object *really is* that subclass --\r
otherwise Java throws a \`ClassCastException\` at runtime.\r
\r
\`\`\`java\r
A ref = new B();\r
ref.showA();\r
// ref.showB();   // ERROR: not visible through A\r
\r
if (ref instanceof B) {\r
    B b = (B) ref;\r
    b.showB();\r
}\r
\`\`\`\r
\r
Upcast first, so only \`A\`'s members are visible. \`instanceof\` then confirms the\r
object really is a \`B\`, and inside that block the \`(B)\` cast is guaranteed safe —\r
\`showB()\` becomes callable again.\r
\r
If you downcast an object that is **not** actually the target type, it compiles\r
but crashes at runtime with a \`ClassCastException\`:\r
\r
\`\`\`java\r
A onlyA = new A();\r
B bad = (B) onlyA;\r
\`\`\`\r
\r
---\r
\r
## The \`instanceof\` Operator\r
\r
\`instanceof\` tests whether an object is an instance of a given type (or subtype).\r
It returns a \`boolean\` and is the standard guard before a downcast:\r
\r
\`\`\`java\r
if (ref instanceof B) {\r
    B b = (B) ref;\r
    b.showB();\r
}\r
\`\`\`\r
\r
This prevents \`ClassCastException\` by confirming the real type first.\r
\r
> **Modern shortcut (Java 16+):** *pattern matching for \`instanceof\`* lets you\r
> test and cast in one step, binding the result to a new variable:\r
>\r
> \`\`\`java\r
> if (ref instanceof B b) { // checks the type AND casts into 'b'\r
>     b.showB();\r
> }\r
> \`\`\`\r
>\r
> This chapter keeps the explicit \`(B) ref\` cast on purpose, so the downcast step\r
> is visible while you are learning what casting actually does. The pattern form\r
> is the idiomatic choice in real code. (Your IDE may show an informational hint\r
> suggesting it -- that is a style tip, not an error.)\r
\r
---\r
\r
## Understanding the Code\r
\r
### \`TypeCasting.java\`\r
\r
\`\`\`java\r
int a = 10;\r
long b = a;\r
double c = b;\r
\r
double d = 9.99;\r
int e = (int) d;\r
int big = 257;\r
byte sb = (byte) big;\r
int code = 65;\r
char ch = (char) code;\r
\`\`\`\r
\r
### \`UpCasting.java\`\r
\r
\`\`\`java UpCasting.java @run-upcasting-downcasting-up-casting
public class UpCasting {\r
    static class A {\r
        public void showA() { System.out.println("In Class A"); }\r
    }\r
    static class B extends A {\r
        public void showB() { System.out.println("In Class B"); }\r
    }\r
\r
    public static void main(String[] args) {\r
        B b = new B();\r
        A ref = b;\r
        ref.showA();\r
    }\r
}\r
\`\`\`

\`\`\`output @run-upcasting-downcasting-up-casting
In Class A
\`\`\`\r
\r
### \`DownCasting.java\`\r
\r
\`\`\`java DownCasting.java @run-upcasting-downcasting-down-casting
public class DownCasting {\r
    static class A {\r
        public void showA() { System.out.println("In Class A"); }\r
    }\r
    static class B extends A {\r
        public void showB() { System.out.println("In Class B"); }\r
    }\r
\r
    public static void main(String[] args) {\r
        A ref = new B();\r
        if (ref instanceof B) {\r
            B b = (B) ref;\r
            b.showB();\r
        }\r
    }\r
}\r
\`\`\`

\`\`\`output @run-upcasting-downcasting-down-casting
In Class B
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### Widening\r
\r
- \`long b = a;\` -- an \`int\` always fits in a \`long\`, so Java converts it for you.\r
- \`double c = b;\` -- a \`long\` fits in a \`double\`; the value gains a \`.0\`.\r
\r
### Narrowing\r
\r
- \`(int) d\` -- discards everything after the decimal point (\`9.99 -> 9\`).\r
- \`(byte) big\` -- a \`byte\` holds only \`-128..127\`; \`257\` wraps around to \`1\`.\r
- \`(char) code\` -- interprets the integer \`65\` as a Unicode/ASCII code -> \`'A'\`.\r
\r
### Upcasting\r
\r
- \`A ref = b;\` -- implicit upcast; no \`(A)\` needed.\r
- \`ref.showA();\` -- works because \`showA()\` is declared in \`A\`.\r
- \`ref.showB();\` -- would not compile: \`showB()\` is not part of \`A\`.\r
\r
### Downcasting\r
\r
- \`A ref = new B();\` -- upcast; reference is \`A\`, object is \`B\`.\r
- \`ref instanceof B\` -- confirms the real type before casting.\r
- \`(B) ref\` -- explicit downcast; now \`showB()\` is reachable.\r
- Casting a plain \`A\` to \`B\` would throw \`ClassCastException\`.\r
\r
---\r
\r
## How to Compile and Run\r
\r
None of these files declares a package. Because each file keeps its \`A\`\r
and \`B\` as **static nested classes**, the files are fully self-contained -- you\r
can compile them individually or all at once. Compile and run from the parent\r
directory using the package path:\r
\r
\`\`\`bash\r
# Compile everything in the chapter at once...\r
javac *.java\r
\r
# ...or one file at a time, e.g.:\r
# javac TypeCasting.java\r
\r
# Then run each program:\r
java TypeCasting\r
java UpCasting\r
java DownCasting\r
\`\`\`\r
\r
### Expected Output\r
\r
the chapter folder:\r
\r
\`\`\`\r
Widening:\r
int a    = 10\r
long b   = 10\r
double c = 10.0\r
\r
Narrowing (double -> int):\r
double d = 9.99\r
int e    = 9\r
\r
Narrowing (int -> byte, overflow):\r
int big = 257\r
byte sb = 1\r
\r
Narrowing (int -> char):\r
int code = 65\r
char ch  = A\r
\`\`\`\r
\r
the chapter folder:\r
\r
\`\`\`\r
In Class A\r
In Class A\r
In Class B\r
\`\`\`\r
\r
the chapter folder:\r
\r
\`\`\`\r
In Class A\r
In Class B\r
onlyA is not a B - skipping downcast\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Expecting narrowing to round\r
\r
\`\`\`java\r
int e = (int) 9.99;\r
\`\`\`

\`\`\`expected
e is 9

A cast truncates towards zero. Math.round(9.99) is 10 when rounding is
what you meant.
\`\`\`\r
\r
Casting a floating-point value to an integer drops the decimal part. Use\r
\`Math.round()\` if you want rounding.\r
\r
### Mistake 2: Forgetting the explicit cast when narrowing\r
\r
\`\`\`java\r
int big = 257;\r
byte sb = big; // COMPILE ERROR: possible lossy conversion\r
byte ok = (byte) big; // correct -- you acknowledge the risk\r
\`\`\`

\`\`\`expected
error: incompatible types: possible lossy conversion from int to byte

With the cast it compiles — and gives 1, not 257. A byte keeps the low
eight bits and 257 is 100000001 in binary.
\`\`\`\r
\r
### Mistake 3: Calling subclass methods through a superclass reference\r
\r
\`\`\`java\r
A ref = new B();\r
ref.showB(); // COMPILE ERROR: showB() not visible via A\r
\`\`\`

\`\`\`expected
error: cannot find symbol — method showB()
    location: variable ref of type A

The object is a B and has the method. The compiler goes by the reference
type, which is A.
\`\`\`\r
\r
The reference type limits visibility. Downcast to \`B\` first (after an\r
\`instanceof\` check).\r
\r
### Mistake 4: Downcasting without checking the real type\r
\r
\`\`\`java\r
A onlyA = new A();\r
B b = (B) onlyA;\r
\`\`\`

\`\`\`expected
Exception in thread "main" java.lang.ClassCastException:
    class A cannot be cast to class B

It compiles, because A and B are related. It fails at run time, because
this particular object never was a B. Guard it with
if (onlyA instanceof B b) { ... }
\`\`\`\r
\r
Always guard a downcast with \`instanceof\` unless you are certain of the type.\r
\r
### Mistake 5: Confusing the reference type with the object type\r
\r
Upcasting/downcasting never changes the object -- only the *view* of it. A \`B\`\r
seen through an \`A\` reference is still a \`B\`, which is why a later downcast back\r
to \`B\` succeeds.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`TypeCasting.java\`\r
\r
Primitives only. It runs the three widening steps, then the three narrowing\r
ones, printing each value before and after so the truncation, the overflow and\r
the code-point conversion are all visible in the output rather than asserted.\r
\r
\`\`\`java TypeCasting.java @run-upcasting-downcasting-type-casting\r
public class TypeCasting {\r
    public static void main(String[] args) {\r
\r
        int a = 10;\r
        long b = a;\r
        double c = b;\r
\r
        System.out.println("Widening:");\r
        System.out.println("int a    = " + a);\r
        System.out.println("long b   = " + b);\r
        System.out.println("double c = " + c);\r
\r
        double d = 9.99;\r
        int e = (int) d;\r
        System.out.println("\\nNarrowing (double -> int):");\r
        System.out.println("double d = " + d);\r
        System.out.println("int e    = " + e);\r
\r
        int big = 257;\r
        byte sb = (byte) big;\r
        System.out.println("\\nNarrowing (int -> byte, overflow):");\r
        System.out.println("int big = " + big);\r
        System.out.println("byte sb = " + sb);\r
\r
        int code = 65;\r
        char ch = (char) code;\r
        System.out.println("\\nNarrowing (int -> char):");\r
        System.out.println("int code = " + code);\r
        System.out.println("char ch  = " + ch);\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-upcasting-downcasting-type-casting\r
Widening:\r
int a    = 10\r
long b   = 10\r
double c = 10.0\r
\r
Narrowing (double -> int):\r
double d = 9.99\r
int e    = 9\r
\r
Narrowing (int -> byte, overflow):\r
int big = 257\r
byte sb = 1\r
\r
Narrowing (int -> char):\r
int code = 65\r
char ch  = A\r
\`\`\`\r
\r
### \`DownCasting.java\`\r
\r
\`A\` and \`B\` are declared as static nested classes so the file is self-contained.\r
\`main\` upcasts a \`B\` into an \`A\` reference, guards with \`instanceof\`, downcasts,\r
and then repeats the attempt on a plain \`A\` — which fails the guard and is\r
skipped rather than crashing.\r
\r
\`\`\`java DownCasting.java @run-upcasting-downcasting-down-casting\r
public class DownCasting {\r
\r
    static class A {\r
        public void showA() {\r
            System.out.println("In Class A");\r
        }\r
    }\r
\r
    static class B extends A {\r
        public void showB() {\r
            System.out.println("In Class B");\r
        }\r
    }\r
\r
    public static void main(String[] args) {\r
        A ref = new B();\r
        ref.showA();\r
\r
        if (ref instanceof B) {\r
            B b = (B) ref;\r
            b.showB();\r
        }\r
\r
        A onlyA = new A();\r
        if (onlyA instanceof B) {\r
            B b2 = (B) onlyA;\r
            b2.showB();\r
        } else {\r
            System.out.println("onlyA is not a B - skipping downcast");\r
        }\r
\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-upcasting-downcasting-down-casting\r
In Class A\r
In Class B\r
onlyA is not a B - skipping downcast\r
\`\`\`\r
\r
### \`UpCasting.java\`\r
\r
The shorter half of the pair: assign a \`B\` to an \`A\` reference with no cast at\r
all, and call the one method that is visible through it.\r
\r
\`\`\`java UpCasting.java @run-upcasting-downcasting-up-casting\r
//      Superclass ref = new Subclass();\r
public class UpCasting {\r
\r
    static class A {\r
        public void showA() {\r
            System.out.println("In Class A");\r
        }\r
    }\r
\r
    static class B extends A {\r
        public void showB() {\r
            System.out.println("In Class B");\r
        }\r
    }\r
\r
    public static void main(String[] args) {\r
        B b = new B();\r
\r
        A ref = b;\r
\r
        ref.showA();\r
\r
        b.showA();\r
        b.showB();\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-upcasting-downcasting-up-casting\r
In Class A\r
In Class A\r
In Class B\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## Upcasting\r
\r
- Child reference to parent type. Implicit, always safe, and never fails —\r
  every child *is* a parent.\r
- The reference narrows what you may call; the object keeps everything it is. An\r
  overridden method still runs the child's version.\r
- This is what lets one method take a parent type and work with every subclass,\r
  including ones written later.\r
\r
## Downcasting\r
\r
- Parent reference back to a child type. **Explicit**, and checked at run time.\r
- It compiles whenever the types are related, and throws \`ClassCastException\` at\r
  run time when the object is not actually that type.\r
- Guard it with \`instanceof\`, which is false for \`null\` as well, so one check\r
  covers both failures.\r
- Pattern matching folds the two together:\r
  \`if (a instanceof Dog d) { d.bark(); }\` — test, cast and bind in one line.\r
\r
## Why you would\r
\r
- To reach a member the child declares and the parent does not. Through the\r
  parent reference the compiler will not let you, even though the object has it.\r
- Needing to downcast often is a sign the parent type is missing a method, or\r
  that the design wanted a \`switch\` over sealed types rather than a hierarchy.\r
- Fields are resolved by reference type and never overridden, so casting changes\r
  which field you see. Another reason to keep fields private and go through\r
  methods.\r
`,interview:`## Interview questions

### Q1: What is type casting in Java?

**Answer:** Converting a value or reference from one type to another. For
primitives it changes the numeric representation (widening/narrowing); for
objects it changes the reference type used to view an object (upcasting/downcasting).

### Q2: What is the difference between widening and narrowing?

**Answer:** Widening converts a smaller primitive type to a larger one
(e.g. \`int\` -> \`double\`); it is implicit and lossless. Narrowing converts a
larger type to a smaller one (e.g. \`double\` -> \`int\`); it requires an explicit
cast and may lose data through truncation or overflow.

### Q3: Does casting a \`double\` to an \`int\` round the value?

**Answer:** No. It truncates -- the fractional part is discarded. \`(int) 9.99\`
is \`9\`. Use \`Math.round()\` for rounding.

### Q4: What is upcasting? Is a cast required?

**Answer:** Upcasting is referring to a subclass object through a superclass
reference (e.g. \`A ref = new B();\`). No explicit cast is required because it is
always safe -- a subclass object *is a* superclass.

### Q5: What is downcasting and why is it risky?

**Answer:** Downcasting converts a superclass reference back to a subclass type
to access subclass-specific members. It is risky because if the object is not
actually that subclass, Java throws a \`ClassCastException\` at runtime. It must be
written explicitly and is usually guarded with \`instanceof\`.

### Q6: Can you access subclass-specific methods through a superclass reference?

**Answer:** No. The reference type determines visibility, so only members
declared in the superclass are accessible. To use subclass-specific methods you
must downcast to the subclass type first.

### Q7: What does the \`instanceof\` operator do?

**Answer:** It returns \`true\` if an object is an instance of the specified class
or one of its subclasses (and is not \`null\`). It is the standard way to verify a
type before performing a downcast safely.

### Q8: What happens if you downcast incorrectly?

**Answer:** The code compiles (the types are related), but at runtime the JVM
throws a \`java.lang.ClassCastException\` because the object is not really an
instance of the target subclass.

### Q9: Why is upcasting implicit but downcasting explicit?

**Answer:** Upcasting is always safe -- every subclass object is also an instance
of its superclass -- so Java does it automatically. Downcasting may be invalid
(the object might not be the target subclass), so Java requires an explicit cast
to make you take responsibility for the risk.

### Q10: Does casting an object change the object itself?

**Answer:** No. Reference casting only changes how the object is *viewed* (which
members are accessible); the underlying object and its actual type are unchanged.

---

*This is part of a Java learning series. It builds on the chapters on Inheritance
and Objects, and underpins polymorphism, interfaces, and the collections
framework, where upcasting and \`instanceof\` are used constantly.*
`}},{slug:`wrapper-classes`,title:`Wrapper classes`,subject:`Java`,stage:`Types and contracts`,chapter:21,accent:`#34d399`,summary:`A wrapper class wraps a primitive value (int, double, char, ...) inside an object. Each of Java's eight primitive types has a matching wrapper class in the java.lang package.`,parts:{notes:`## Introduction\r
\r
A **wrapper class** wraps a primitive value (\`int\`, \`double\`, \`char\`, ...) inside\r
an **object**. Each of Java's eight primitive types has a matching wrapper class\r
in the \`java.lang\` package.\r
\r
The wrapper lets a primitive behave like an object: it can be stored in\r
collections, can be \`null\`, and comes with a set of handy static helper methods.\r
\r
This chapter covers boxing/unboxing and the most common conversions in a single\r
program:\r
\r
| File | Demonstrates |\r
|------|--------------|\r
| \`WrapperClasses.java\` | Autoboxing, auto-unboxing, \`parseInt\`, \`valueOf\`, and \`toString\` |\r
\r
---\r
\r
## The Eight Wrapper Classes\r
\r
| Primitive | Wrapper class |\r
|-----------|---------------|\r
| \`byte\`    | \`Byte\`        |\r
| \`short\`   | \`Short\`       |\r
| \`int\`     | \`Integer\`     |\r
| \`long\`    | \`Long\`        |\r
| \`float\`   | \`Float\`       |\r
| \`double\`  | \`Double\`      |\r
| \`char\`    | \`Character\`   |\r
| \`boolean\` | \`Boolean\`     |\r
\r
> Note the two that don't simply capitalize the primitive name: \`int\` -> **\`Integer\`**\r
> and \`char\` -> **\`Character\`**.\r
\r
---\r
\r
## Why Wrapper Classes Exist\r
\r
- **Collections store objects, not primitives.** \`ArrayList<Integer>\`,\r
  \`HashMap<String, Double>\`, etc. can only hold objects, so numbers must be\r
  wrapped.\r
- **Nullability.** A primitive \`int\` is always a number; an \`Integer\` can be\r
  \`null\` to represent "no value".\r
- **Utility methods and constants.** Wrappers provide static helpers such as\r
  \`Integer.parseInt\`, \`Integer.valueOf\`, \`Integer.MAX_VALUE\`, and\r
  \`Integer.MIN_VALUE\`.\r
\r
---\r
\r
## Boxing and Autoboxing\r
\r
**Boxing** is wrapping a primitive into its wrapper object. **Autoboxing** is Java\r
doing that automatically (since Java 5):\r
\r
\`\`\`java\r
Integer num1 = 7;\r
\`\`\`\r
\r
You should **never** use \`new Integer(...)\`; it is deprecated. Let autoboxing (or\r
\`Integer.valueOf\`) do the work.\r
\r
---\r
\r
## Unboxing and Auto-unboxing\r
\r
**Unboxing** is extracting the primitive back out of the wrapper. **Auto-unboxing**\r
is Java doing it for you:\r
\r
\`\`\`java\r
Integer num1 = 7;\r
int num2 = num1;\r
// int manual = num1.intValue(); // the explicit, manual form\r
\`\`\`\r
\r
---\r
\r
## Converting Between Strings and Numbers\r
\r
A very common task is turning text (e.g. user input \`"10"\`) into a number, or a\r
number back into text.\r
\r
### \`parseInt\` vs \`valueOf\`\r
\r
Both read a numeric \`String\`, but they return **different types**:\r
\r
\`\`\`java\r
int num3      = Integer.parseInt("10");\r
Integer num5  = Integer.valueOf("10");\r
\`\`\`\r
\r
| Method | Returns | Use when you want... |\r
|--------|---------|----------------------|\r
| \`Integer.parseInt(s)\` | primitive \`int\` | a plain number |\r
| \`Integer.valueOf(s)\`  | \`Integer\` object | a wrapper object (e.g. for a collection) |\r
\r
### \`toString\`\r
\r
Going the other way, a wrapper object can produce its text form:\r
\r
\`\`\`java\r
Integer num4 = 10;\r
String s2 = num4.toString();\r
\`\`\`\r
\r
\`s2\` is the two-character string \`"10"\`, not the number.\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java\r
public class WrapperClasses {\r
    public static void main(String[] args) {\r
        int num = 7;\r
        Integer num1 = 7;\r
        int num2 = num1;\r
\r
        String str = "10";\r
        int num3 = Integer.parseInt(str);\r
\r
        Integer num4 = 10;\r
        String s2 = num4.toString();\r
\r
        String str3 = "10";\r
        Integer num5 = Integer.valueOf(str3);\r
    }\r
}\r
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
- \`int num = 7;\` -- an ordinary primitive \`int\`.\r
- \`Integer num1 = 7;\` -- **autoboxing**; the \`int\` is wrapped into an \`Integer\`.\r
- \`int num2 = num1;\` -- **auto-unboxing**; the \`Integer\` is unwrapped back to \`int\`.\r
- \`Integer.parseInt(str)\` -- parses the \`String\` \`"10"\` into a **primitive** \`int\`.\r
- \`num4.toString()\` -- converts the \`Integer\` object into the \`String\` \`"10"\`.\r
- \`Integer.valueOf(str3)\` -- parses the \`String\` \`"10"\` into an **\`Integer\` object**.\r
\r
---\r
\r
## How to Compile and Run\r
\r
The file declares no package declaration. Compile and run from the parent\r
directory using the package path:\r
\r
\`\`\`bash\r
# Compile\r
javac WrapperClasses.java\r
\r
# Run\r
java WrapperClasses\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
num  (primitive int)      = 7\r
num2 (auto-unboxed)       = 7\r
parseInt("10")            = 10\r
Integer(10).toString()    = 10\r
valueOf("10")             = 10\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Using \`new Integer(...)\`\r
\r
\`\`\`java\r
Integer x = new Integer(8);\r
Integer y = 8;              // correct -- autoboxing\r
Integer z = Integer.valueOf(8);\r
\`\`\`\r
\r
\`\`\`expected\r
All three give you an Integer holding 8.\r
new Integer(8) is deprecated for removal — it always allocates a fresh\r
object, defeats the valueOf cache, and there is no reason to want one.\r
\`\`\`\r
\r
The constructors were deprecated because they always create a new object and\r
waste memory; \`valueOf\`/autoboxing can reuse cached objects.\r
\r
### Mistake 2: Confusing \`parseInt\` with \`valueOf\`\r
\r
\`\`\`java\r
int a      = Integer.parseInt("10");\r
Integer b  = Integer.valueOf("10");\r
\`\`\`\r
\r
\`\`\`expected\r
a is the primitive int 10.\r
b is an Integer object holding 10.\r
\r
parseInt when you want a number, valueOf when you want the object.\r
\`\`\`\r
\r
\`parseInt\` returns a primitive; \`valueOf\` returns a wrapper object. Picking the\r
wrong one leads to needless boxing/unboxing.\r
\r
![Boxing in a loop allocates one object per iteration](diagrams/21-wrapper-classes-notes-boxing-cost.jpg)\r
\r
### Mistake 3: Parsing a non-numeric String\r
\r
\`\`\`java\r
int n = Integer.parseInt("abc");\r
\`\`\`\r
\r
\`\`\`expected\r
Exception in thread "main" java.lang.NumberFormatException:\r
    For input string: "abc"\r
\r
Unchecked, so nothing forces you to handle it. Validate first, or catch it.\r
\`\`\`\r
\r
\`parseInt\`/\`valueOf\` throw \`NumberFormatException\` if the text is not a valid\r
number. Validate or wrap the call in a \`try/catch\`.\r
\r
### Mistake 4: Unboxing a \`null\`\r
\r
\`\`\`java\r
Integer maybe = null;\r
int n = maybe;\r
\`\`\`\r
\r
\`\`\`expected\r
Exception in thread "main" java.lang.NullPointerException:\r
    Cannot invoke "java.lang.Integer.intValue()" because "maybe" is null\r
\r
Unboxing calls intValue() on the object. The line contains no visible method\r
call at all, which is what makes this one hard to spot.\r
\`\`\`\r
\r
A wrapper can be \`null\`; auto-unboxing \`null\` crashes. Check for \`null\` first.\r
\r
![Auto-unboxing a null wrapper calls intValue and throws](diagrams/21-wrapper-classes-notes-null-unboxing.jpg)\r
\r
### Mistake 5: Comparing wrappers with \`==\`\r
\r
![The Integer cache runs from -128 to 127 and == stops working above it](diagrams/21-wrapper-classes-notes-integer-cache.jpg)\r
\r
\`\`\`java\r
Integer a = 1000, b = 1000;\r
System.out.println(a == b);\r
System.out.println(a.equals(b));\r
\`\`\`\r
\r
\`\`\`expected\r
false\r
true\r
\r
valueOf caches -128 to 127, so the same code with 100 instead of 1000 prints\r
true — the behaviour depends on the value. Use .equals for wrappers.\r
\`\`\`\r
\r
Use \`.equals()\` (or unbox to primitives) to compare wrapper **values**. \`==\`\r
compares object identity, which is unreliable outside the small cached range\r
(-128..127).\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`WrapperClasses.java\`\r
\r
\`\`\`java WrapperClasses.java @run-wrapper-classes-wrapper-classes\r
public class WrapperClasses {\r
    public static void main(String[] args) {\r
\r
        int num = 7;\r
\r
        Integer num1 = 7;\r
\r
        // int num2 = num1.intValue(); // manual unboxing using intValue()\r
        int num2 = num1;\r
\r
        System.out.println("num  (primitive int)      = " + num);\r
        System.out.println("num2 (auto-unboxed)       = " + num2);\r
\r
        String str = "10";\r
        int num3 = Integer.parseInt(str);\r
        System.out.println("parseInt(\\"10\\")            = " + num3);\r
\r
        Integer num4 = 10;\r
        String s2 = num4.toString();\r
        System.out.println("Integer(10).toString()    = " + s2);\r
\r
        String str3 = "10";\r
        Integer num5 = Integer.valueOf(str3);\r
        System.out.println("valueOf(\\"10\\")             = " + num5);\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-wrapper-classes-wrapper-classes\r
num  (primitive int)      = 7\r
num2 (auto-unboxed)       = 7\r
parseInt("10")            = 10\r
Integer(10).toString()    = 10\r
valueOf("10")             = 10\r
\`\`\`\r
`,revision:'The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## What they are for\r\n\r\n![An int array holds numbers, a List of Integer holds references to objects](diagrams/21-wrapper-classes-revision-array-vs-list.jpg)\r\n\r\n- One class per primitive — `Integer`, `Double`, `Character`, `Boolean` and the\r\n  rest — so a value can be an **object** where an object is required.\r\n- Collections and generics hold objects only, so `List<int>` is illegal and\r\n  `List<Integer>` is what you write.\r\n- A wrapper can be `null`; a primitive cannot. That is sometimes the whole reason\r\n  to use one — "no value yet" is not `0`.\r\n- They also carry the utilities: `Integer.parseInt`, `MAX_VALUE`, `MIN_VALUE`,\r\n  `Integer.toBinaryString`.\r\n\r\n## Boxing\r\n\r\n- **Autoboxing** turns a primitive into its wrapper automatically; **unboxing**\r\n  goes back. The compiler inserts `Integer.valueOf` and `intValue`.\r\n- Unboxing a `null` throws `NullPointerException`, and the line that throws often\r\n  contains no visible method call at all — just `int x = map.get(k);`.\r\n- `parseInt` returns a **primitive** `int`; `valueOf` returns an `Integer`. When\r\n  you want a number, `parseInt` is the one that does not allocate.\r\n- `parseInt` on anything non-numeric throws `NumberFormatException`, which is\r\n  unchecked — nothing forces you to handle it.\r\n\r\n## Comparing, and `new`\r\n\r\n![new Integer always allocates and defeats the valueOf cache](diagrams/21-wrapper-classes-revision-what-new-defeats.jpg)\r\n\r\n- `==` on two wrappers compares **references**. Use `.equals()`, or unbox one\r\n  side deliberately.\r\n- `Integer.valueOf` caches −128 to 127, so `==` appears to work for small numbers\r\n  and fails above 127. Same code, two behaviours, decided by the value.\r\n- `new Integer(7)` is deprecated for removal: it always allocates, defeats the\r\n  cache, and there is no reason to want a distinct object. Use `valueOf` or just\r\n  the literal.\r\n',interview:'## Interview questions\n\n### Q1: What is a wrapper class?\n\n**Answer:** A class that wraps a primitive value inside an object. Each primitive\nhas a corresponding wrapper in `java.lang` (`int` -> `Integer`, `char` ->\n`Character`, etc.).\n\n### Q2: Why do we need wrapper classes?\n\n**Answer:** Because collections and generics work with objects, not primitives;\nbecause a wrapper can be `null`; and because wrappers provide useful static\nhelpers and constants.\n\n### Q3: What is autoboxing and auto-unboxing?\n\n**Answer:** Autoboxing is the automatic conversion of a primitive into its\nwrapper (`int` -> `Integer`); auto-unboxing is the reverse (`Integer` -> `int`).\nJava has done this automatically since Java 5.\n\n### Q4: What is the difference between `parseInt` and `valueOf`?\n\n**Answer:** `Integer.parseInt(s)` returns a primitive `int`; `Integer.valueOf(s)`\nreturns an `Integer` object. `valueOf` may also reuse cached objects for small\nvalues.\n\n### Q5: Why is `new Integer(7)` discouraged?\n\n**Answer:** The wrapper constructors are deprecated. They always allocate a new\nobject, whereas autoboxing or `valueOf` can reuse cached instances and is clearer.\n\n### Q6: What happens if you unbox a `null` wrapper?\n\n**Answer:** It throws a `NullPointerException` at runtime, because there is no\nprimitive value to extract.\n\n### Q7: Why can comparing two `Integer` objects with `==` give surprising results?\n\n![Two references share one cached Integer at 127 but point at two objects at 128](diagrams/21-wrapper-classes-interview-identity.jpg)\n\n**Answer:** `==` compares object references, not values. Java caches `Integer`\nobjects for values -128..127, so small equal values may share a reference and\ncompare `true`, while larger ones don\'t. Always use `.equals()` for value\ncomparison.\n\n### Q8: What exception does `parseInt` throw on bad input?\n\n![The two unchecked exceptions wrapper conversions introduce](diagrams/21-wrapper-classes-interview-two-exceptions.jpg)\n\n**Answer:** `NumberFormatException`, thrown when the string is not a valid number.\n\n---\n\n*This is part of a Java learning series. Wrapper classes underpin the collections\nframework and generics, where every "number" you store is really a boxed object.*\n'}},{slug:`abstract-keyword`,title:`The abstract keyword`,subject:`Java`,stage:`Types and contracts`,chapter:22,accent:`#f97316`,summary:`The abstract keyword marks something as incomplete -- a partial design that must be finished by a subclass. It is the foundation of abstraction: hiding implementation detail behind a contract.`,parts:{notes:`## Introduction\r
\r
The \`abstract\` keyword marks something as **incomplete** -- a partial design that\r
must be finished by a subclass. It is the foundation of **abstraction**: hiding\r
implementation detail behind a contract.\r
\r
There are two things you can declare \`abstract\`:\r
\r
- an **abstract class** -- a blueprint that cannot be instantiated on its own, and\r
- an **abstract method** -- a method with a signature but no body, which subclasses\r
  must implement.\r
\r
This chapter demonstrates both with a small \`Car\` hierarchy:\r
\r
| File | Demonstrates |\r
|------|--------------|\r
| \`AbstractKeyword.java\` | An abstract class \`Car\` with one abstract method (\`drive\`) and one concrete method (\`playMusic\`), implemented by the concrete class \`WagonR\` |\r
\r
---\r
\r
## Abstract Classes\r
\r
An **abstract class** is declared with the \`abstract\` keyword and **cannot be\r
instantiated** directly:\r
\r
\`\`\`java\r
abstract class Car {\r
    // Car c = new Car();   // ERROR: Car is abstract\r
    public abstract void drive();\r
    public void playMusic() {\r
        System.out.println("Playing Music");\r
    }\r
}\r
\`\`\`\r
\r
\`drive()\` has no body and ends in a semicolon — every concrete subclass must\r
supply one. \`playMusic()\` has a body, so subclasses inherit it as it stands and\r
never have to think about it. \`new Car()\` will not compile.\r
\r
![An abstract class mixes a method the subclass must write with one it gets free](diagrams/22-abstract-keyword-notes-two-method-kinds.jpg)\r
\r
It exists to be **extended**. It can freely mix abstract methods (which subclasses\r
must implement) with concrete methods (which subclasses inherit ready-made).\r
\r
---\r
\r
## Abstract Methods\r
\r
An **abstract method** declares only a signature and ends with a semicolon -- it\r
has **no body**:\r
\r
\`\`\`java\r
public abstract void drive();\r
\`\`\`\r
\r
Every **concrete** subclass is forced to **override** it and supply a real\r
implementation. This is the "contract": *any real Car must define how it drives.*\r
\r
---\r
\r
## The Rules of \`abstract\`\r
\r
![new is only legal on the subclass that implements every abstract method](diagrams/22-abstract-keyword-notes-where-new-becomes-legal.jpg)\r
\r
1. An abstract method **can only exist inside an abstract class**.\r
2. You **cannot create an object** of an abstract class (\`new Car()\` fails).\r
3. An abstract class **may have no abstract methods at all** -- it can still be\r
   abstract simply to prevent instantiation.\r
4. But an abstract method **must** live in an abstract class.\r
5. The first concrete subclass **must implement every** inherited abstract method\r
   (otherwise it, too, must be declared abstract).\r
\r
---\r
\r
## Concrete Classes\r
\r
A **concrete class** is a normal, fully-implemented class. When it extends an\r
abstract class, it must implement all the inherited abstract methods, which makes\r
it "complete" and therefore instantiable:\r
\r
\`\`\`java\r
class WagonR extends Car {\r
    public void drive() {\r
        System.out.println("Driving");\r
    }\r
}\r
\`\`\`\r
\r
---\r
\r
## Using an Abstract Type\r
\r
Although you can't instantiate an abstract class, you **can** use it as a\r
reference type and point it at a concrete subclass object (this is **upcasting**):\r
\r
\`\`\`java\r
Car obj = new WagonR();\r
obj.drive();\r
obj.playMusic();\r
\`\`\`\r
\r
The reference is the abstract type; the object is a real \`WagonR\`. \`drive()\`\r
prints \`Driving\` from \`WagonR\`'s implementation, and \`playMusic()\` prints\r
\`Playing Music\` from the concrete method \`Car\` provided.\r
\r
This is how abstraction is used in practice: code is written against the abstract\r
type, while the actual behaviour comes from whichever concrete subclass is plugged\r
in.\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java AbstractKeyword.java @run-abstract-keyword-abstract-keyword\r
abstract class Car {\r
    public abstract void drive();\r
    public void playMusic() {\r
        System.out.println("Playing Music");\r
    }\r
}\r
\r
class WagonR extends Car {\r
    public void drive() {\r
        System.out.println("Driving");\r
    }\r
}\r
\r
public class AbstractKeyword {\r
    public static void main(String[] args) {\r
        Car obj = new WagonR();\r
        obj.drive();\r
        obj.playMusic();\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-abstract-keyword-abstract-keyword\r
Driving\r
Playing Music\r
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
- \`abstract class Car\` -- declares an incomplete class that cannot be instantiated.\r
- \`public abstract void drive();\` -- an abstract method: no body; subclasses must\r
  implement it.\r
- \`public void playMusic() { ... }\` -- a concrete method inherited as-is by every\r
  subclass.\r
- \`class WagonR extends Car\` -- a concrete subclass.\r
- \`public void drive() { ... }\` -- implements (overrides) the abstract method.\r
- \`Car obj = new WagonR();\` -- upcasts the \`WagonR\` object to a \`Car\` reference.\r
- \`obj.drive();\` -- calls \`WagonR\`'s overridden version -> "Driving".\r
- \`obj.playMusic();\` -- calls \`Car\`'s inherited concrete method -> "Playing Music".\r
\r
---\r
\r
## How to Compile and Run\r
\r
All classes declare no package declaration. Compile and run from the\r
parent directory using the package path:\r
\r
\`\`\`bash\r
# Compile\r
javac AbstractKeyword.java\r
\r
# Run\r
java AbstractKeyword\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
Driving\r
Playing Music\r
\`\`\`\r
\r
---\r
\r
## Abstract Class vs Interface\r
\r
![Abstract class and interface compared on state, constructors and how many you get](diagrams/22-abstract-keyword-notes-class-vs-interface.jpg)\r
\r
| | Abstract class | Interface |\r
|---|----------------|-----------|\r
| Methods | abstract **and** concrete | abstract (plus \`default\`/\`static\` since Java 8) |\r
| Fields | instance fields allowed | only \`public static final\` constants |\r
| Inheritance | a class extends **one** | a class implements **many** |\r
| Constructors | yes | no |\r
| Use when | classes share state/behaviour and are closely related | you only need a contract many unrelated classes can fulfil |\r
\r
> A useful rule of thumb: an abstract class models an **"is-a"** relationship with\r
> shared code; an interface models a **"can-do"** capability.\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Trying to instantiate an abstract class\r
\r
\`\`\`java\r
Car c = new Car(); // COMPILE ERROR: Car is abstract; cannot be instantiated\r
\`\`\`\r
\r
\`\`\`expected\r
error: Car is abstract; cannot be instantiated\r
\r
Declare the reference as Car and point it at a concrete subclass:\r
Car c = new Sedan();\r
\`\`\`\r
\r
Instantiate a concrete subclass instead: \`Car c = new WagonR();\`.\r
\r
### Mistake 2: Forgetting to implement an abstract method\r
\r
\`\`\`java\r
class Sedan extends Car { } // COMPILE ERROR: must implement drive()\r
\`\`\`\r
\r
\`\`\`expected\r
error: Sedan is not abstract and does not override abstract method drive()\r
\r
Either implement drive(), or declare Sedan abstract too and pass the\r
obligation down to its subclasses.\r
\`\`\`\r
\r
A concrete subclass must implement **every** inherited abstract method, or be\r
declared \`abstract\` itself.\r
\r
### Mistake 3: Giving an abstract method a body\r
\r
\`\`\`java\r
public abstract void drive() { } // ERROR: abstract methods have NO body\r
\`\`\`\r
\r
\`\`\`expected\r
error: abstract methods cannot have a body\r
\r
An abstract method ends at the semicolon. Not even {} is allowed.\r
\`\`\`\r
\r
An abstract method ends with a semicolon and has no \`{ }\` block.\r
\r
### Mistake 4: Declaring an abstract method in a non-abstract class\r
\r
\`\`\`java\r
class Car {\r
    public abstract void drive(); // COMPILE ERROR\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
error: Car is not abstract and does not override abstract method drive()\r
\r
A class holding an abstract method must itself be abstract. The compiler\r
names the fix in the message.\r
\`\`\`\r
\r
If a class has even one abstract method, the class itself must be \`abstract\`.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`AbstractKeyword.java\`\r
\r
The whole idea in one file. \`Car\` is abstract and mixes the two method kinds:\r
\`drive()\` is a contract with no body, \`playMusic()\` is finished behaviour every\r
subclass gets free. \`WagonR\` is concrete — it implements \`drive()\`, so it is\r
complete and can be instantiated.\r
\r
Two rules the file quietly depends on: an abstract method can only live inside an\r
abstract class, and an abstract class is allowed to have no abstract methods at\r
all. The reverse is not allowed.\r
\r
\`\`\`java AbstractKeyword.java @run-abstract-keyword-abstract-keyword\r
abstract class Car {\r
    public abstract void drive();\r
\r
    public void playMusic() {\r
        System.out.println("Playing Music");\r
    }\r
}\r
\r
class WagonR extends Car {\r
    public void drive() {\r
        System.out.println("Driving");\r
    }\r
}\r
\r
public class AbstractKeyword {\r
    public static void main(String[] args) {\r
\r
        Car obj = new WagonR();\r
\r
        obj.drive();\r
        obj.playMusic();\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-abstract-keyword-abstract-keyword\r
Driving\r
Playing Music\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## Abstract classes\r
\r
![Which combinations of abstract compile and which do not](diagrams/22-abstract-keyword-revision-legal-or-not.jpg)\r
\r
- An \`abstract\` class cannot be instantiated. \`new Shape()\` is a compile error\r
  even when every method has a body.\r
- It can still have constructors, fields, and ordinary methods. The constructor\r
  runs when a subclass is constructed — that is what it is for.\r
- It may have **no** abstract methods at all. \`abstract\` alone is enough to say\r
  "this is not a whole thing yet".\r
- You can declare a reference of an abstract type and point it at any concrete\r
  subclass. That is the usual way to use one.\r
\r
## Abstract methods\r
\r
- An abstract method has a signature and **no body** — not even \`{}\`. Giving one\r
  a body is a compile error.\r
- An abstract method may only appear in an abstract class. Putting one in a\r
  concrete class does not compile, and the message tells you to mark the class.\r
- A concrete subclass must implement **every** inherited abstract method. If it\r
  does not, it must itself be declared abstract and pass the obligation on.\r
\r
## Against an interface\r
\r
![When to choose an abstract class and when to choose an interface](diagrams/22-abstract-keyword-revision-which-to-reach-for.jpg)\r
\r
- A class extends **one** abstract class and implements **any number** of\r
  interfaces. That is usually what decides it.\r
- An abstract class can hold state and a constructor; an interface holds\r
  constants and behaviour, not instance fields.\r
- Reach for an abstract class when subclasses share implementation as well as a\r
  contract; reach for an interface when you are only naming a capability.\r
`,interview:`## Interview questions

### Q1: What is an abstract class?

**Answer:** A class declared with the \`abstract\` keyword that cannot be
instantiated. It is meant to be extended and can contain both abstract and
concrete methods.

### Q2: What is an abstract method?

**Answer:** A method declared without a body (ending in a semicolon). It defines a
contract that every concrete subclass must implement.

### Q3: Can an abstract class have a constructor?

![The abstract class constructor runs while a concrete subclass is created](diagrams/22-abstract-keyword-interview-constructor-still-runs.jpg)

**Answer:** Yes. The constructor is not used to create an abstract object
directly, but it runs (via \`super(...)\`) when a concrete subclass is instantiated,
to initialize inherited fields.

### Q4: Can an abstract class have no abstract methods?

**Answer:** Yes. A class can be abstract purely to prevent direct instantiation,
even if all its methods have bodies.

### Q5: Can an abstract method exist in a non-abstract class?

![Both abstract compile errors are fixed by marking a class abstract](diagrams/22-abstract-keyword-interview-two-errors-one-cure.jpg)

**Answer:** No. Any class containing an abstract method must itself be declared
\`abstract\`.

### Q6: What happens if a subclass doesn't implement all abstract methods?

**Answer:** It is still incomplete, so it must be declared \`abstract\` too;
otherwise the code won't compile.

### Q7: Can you create a reference of an abstract type?

**Answer:** Yes. You can't instantiate the abstract class, but you can use it as a
reference pointing to a concrete subclass object (\`Car obj = new WagonR();\`).

### Q8: What is the difference between an abstract class and an interface?

**Answer:** An abstract class can have instance fields, constructors, and concrete
methods, and a class can extend only one. An interface mainly declares a contract
(plus \`default\`/\`static\` methods since Java 8), holds only constants, and a class
can implement many. Use an abstract class for closely related classes that share
state/behaviour; use an interface for a capability many unrelated classes can
provide.

---

*This is part of a Java learning series. It builds on Inheritance and
Polymorphism, and leads into Interfaces -- together they form the basis of
abstraction in Java.*
`}},{slug:`inner-classes`,title:`Inner classes`,subject:`Java`,stage:`Types and contracts`,chapter:23,accent:`#a78bfa`,summary:`An inner class is a class declared inside another class. Java uses them to group a helper type with the class it belongs to, to access the enclosing object's members, and to create small one-off types without cluttering the package with named files.`,parts:{notes:`## Introduction\r
\r
An **inner class** is a class declared **inside another class**. Java uses them to\r
group a helper type with the class it belongs to, to access the enclosing object's\r
members, and to create small one-off types without cluttering the package with\r
named files.\r
\r
This chapter covers the everyday flavours across three programs:\r
\r
| File | Demonstrates |\r
|------|--------------|\r
| \`InnerClass.java\` | A **member (non-static) inner class** \`B\` and a **static nested class** \`C\` inside an outer class \`A\` |\r
| \`AnonymousInnerClass.java\` | An **anonymous inner class** -- a one-off, unnamed subclass of \`A\` -- contrasted with a normal named subclass \`B\` |\r
| \`AbstractAnonymousInnerClass.java\` | An **anonymous inner class implementing an abstract class** \`A\` |\r
\r
> **Important:** all three files declare a top-level \`class A\` in the **same\r
> package** (the chapter folder). That means they **cannot be compiled together**\r
> (\`javac *.java\` fails with *"duplicate class: A"*). Compile and\r
> run them **one file at a time** -- see [How to Compile and Run](#how-to-compile-and-run).\r
\r
---\r
\r
## Kinds of Inner Class\r
\r
| Kind | Declared | Needs an outer instance? | Created with |\r
|------|----------|--------------------------|--------------|\r
| Member (non-static) inner | inside the class body | **yes** | \`outer.new Inner()\` |\r
| Static nested | inside the body, marked \`static\` | no | \`new Outer.Inner()\` |\r
| Anonymous | inline, no name | depends on context | \`new Type() { ... }\` |\r
\r
> The **outer (top-level) class itself can never be \`static\`** -- only a *nested*\r
> class may carry the \`static\` keyword.\r
\r
---\r
\r
## Member (Non-static) Inner Class\r
\r
A member inner class is tied to an **instance** of the outer class, so it can use\r
that object's fields and methods. To create one you need an outer object first:\r
\r
\`\`\`java\r
A obj = new A();\r
A.B obj2 = obj.new B();\r
obj2.show();\r
\`\`\`\r
\r
The type is written \`A.B\` because \`B\` is a member of \`A\`.\r
\r
---\r
\r
## Static Nested Class\r
\r
A static nested class is marked \`static\` and is **not** tied to any instance, so\r
you create it directly:\r
\r
\`\`\`java\r
A.C obj3 = new A.C();\r
obj3.show();\r
\`\`\`\r
\r
Use a static nested class when the helper type does not need access to a specific\r
outer object.\r
\r
![A non-static inner class carries a hidden reference to the outer object, a static nested class does not](diagrams/23-inner-classes-notes-hidden-reference.jpg)\r
\r
---\r
\r
## Anonymous Inner Class\r
\r
An **anonymous inner class** defines and instantiates an unnamed subclass in a\r
single expression. It is the shortcut for "I need a one-off subclass right here\r
and will never reuse it."\r
\r
\`\`\`java\r
A obj1 = new A() {\r
    public void show() {\r
        System.out.println("Showing in class A (Anonymous)");\r
    }\r
};\r
obj1.show();\r
\`\`\`\r
\r
Writing a whole separate named subclass (\`class B extends A { ... }\`) is the\r
right choice when the behaviour is reused. When it is needed **only once**, an\r
anonymous class avoids the overhead of naming and saving an extra class.\r
\r
---\r
\r
## Anonymous Subclass of an Abstract Class\r
\r
You cannot instantiate an abstract class with \`new A()\`. But \`new A() { ... }\`\r
does **not** create an \`A\`; it creates an instance of an **anonymous subclass**\r
that extends \`A\` and supplies the missing implementation:\r
\r
\`\`\`java\r
abstract class A {\r
    abstract public void show();\r
}\r
\r
// A obj = new A();   // ERROR: cannot instantiate the abstract class A\r
\r
A obj1 = new A() {\r
    public void show() {\r
        System.out.println("Showing in class A (Anonymous)");\r
    }\r
};\r
obj1.show();\r
\`\`\`\r
\r
This is the most common everyday use of anonymous classes -- providing a one-off\r
implementation of an abstract type or interface.\r
\r
![new A() with a body creates an anonymous subclass, never an A](diagrams/23-inner-classes-notes-anonymous-subclass.jpg)\r
\r
---\r
\r
## Understanding the Code\r
\r
### \`InnerClass.java\`\r
\r
\`\`\`java InnerClass.java @run-inner-classes-inner-class\r
class A {\r
    public void show() { System.out.println("Showing in class A"); }\r
    class B {\r
        public void show()    { System.out.println("Showing in class B"); }\r
        public void Display() { System.out.println("Displaying in class B"); }\r
    }\r
    static class C {\r
        public void show() { System.out.println("Showing in class C"); }\r
    }\r
}\r
\r
public class InnerClass {\r
    public static void main(String[] args) {\r
        A obj = new A();\r
        obj.show();\r
\r
        A.B obj2 = obj.new B();\r
        obj2.show();\r
        obj2.Display();\r
\r
        A.C obj3 = new A.C();\r
        obj3.show();\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-inner-classes-inner-class\r
Showing in class A\r
Showing in class B\r
Displaying in class B\r
Showing in class C\r
\`\`\`\r
\r
### \`AnonymousInnerClass.java\`\r
\r
\`\`\`java AnonymousInnerClass.java @run-inner-classes-anonymous-inner-class\r
class A { public void show() { System.out.println("Showing in class A"); } }\r
class B extends A { public void show() { System.out.println("Showing in class B"); } }\r
\r
public class AnonymousInnerClass {\r
    public static void main(String[] args) {\r
        A obj = new B();\r
        obj.show();\r
\r
        A obj1 = new A() {\r
            public void show() { System.out.println("Showing in class A (Anonymous)"); }\r
        };\r
        obj1.show();\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-inner-classes-anonymous-inner-class\r
Showing in class B\r
Showing in class A (Anonymous)\r
\`\`\`\r
\r
### \`AbstractAnonymousInnerClass.java\`\r
\r
\`\`\`java AbstractAnonymousInnerClass.java @run-inner-classes-abstract-anonymous-inner-class\r
abstract class A { abstract public void show(); }\r
\r
public class AbstractAnonymousInnerClass {\r
    public static void main(String[] args) {\r
        A obj1 = new A() {\r
            public void show() { System.out.println("Showing in class A (Anonymous)"); }\r
        };\r
        obj1.show();\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-inner-classes-abstract-anonymous-inner-class\r
Showing in class A (Anonymous)\r
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### File 1 -- member & static nested\r
\r
- \`class B\` inside \`A\` -- a non-static inner class, bound to an \`A\` instance.\r
- \`static class C\` -- a static nested class, independent of any \`A\` instance.\r
- \`A obj = new A();\` -- ordinary outer object.\r
- \`obj.new B();\` -- creates the inner \`B\`; requires the outer object \`obj\`.\r
- \`new A.C();\` -- creates the static \`C\` directly, no outer object needed.\r
\r
### File 2 -- anonymous vs named\r
\r
- \`class B extends A\` -- the traditional named subclass overriding \`show()\`.\r
- \`A obj = new B();\` -- uses that named subclass.\r
- \`new A() { public void show() {...} }\` -- an anonymous subclass created inline;\r
  \`obj1\` is typed \`A\` but refers to this one-off subclass.\r
\r
### File 3 -- anonymous + abstract\r
\r
- \`abstract class A\` with \`abstract public void show();\` -- cannot be instantiated.\r
- \`new A() { ... }\` -- builds an anonymous subclass that implements \`show()\` and\r
  instantiates **that** subclass (not \`A\` itself).\r
\r
---\r
\r
## How to Compile and Run\r
\r
![Each nested class becomes its own class file named with a dollar sign](diagrams/23-inner-classes-notes-class-files.jpg)\r
\r
Each file declares no package declaration. Because **all three files define a\r
top-level \`class A\`**, they conflict if compiled together -- compile and run them\r
**one at a time**:\r
\r
\`\`\`bash\r
# Member & static nested inner classes\r
javac InnerClass.java\r
java InnerClass\r
\r
# Anonymous inner class\r
javac AnonymousInnerClass.java\r
java AnonymousInnerClass\r
\r
# Anonymous subclass of an abstract class\r
javac AbstractAnonymousInnerClass.java\r
java AbstractAnonymousInnerClass\r
\`\`\`\r
\r
> Do **not** use \`javac *.java\` here -- it fails with\r
> *"duplicate class: \\_16\\_InnerClass.A"* because three files each declare \`class A\`.\r
\r
### Expected Output\r
\r
\`InnerClass\`:\r
\r
\`\`\`\r
Showing in class A\r
Showing in class B\r
Displaying in class B\r
Showing in class C\r
\`\`\`\r
\r
\`AnonymousInnerClass\`:\r
\r
\`\`\`\r
Showing in class B\r
Showing in class A (Anonymous)\r
\`\`\`\r
\r
\`AbstractAnonymousInnerClass\`:\r
\r
\`\`\`\r
Showing in class A (Anonymous)\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Creating a non-static inner class without an outer object\r
\r
\`\`\`java\r
A.B b = new A.B();   // COMPILE ERROR\r
A a = new A();\r
A.B b2 = a.new B();  // correct\r
\`\`\`\r
\r
\`\`\`expected\r
error: an enclosing instance that contains A.B is required\r
\r
A non-static inner class holds a hidden reference to an outer object, so\r
there has to be one: a.new B(). Mark B static and new A.B() is fine.\r
\`\`\`\r
\r
A non-static inner class needs an enclosing instance; use \`outerObject.new Inner()\`.\r
\r
### Mistake 2: Adding \`static\` to the outer class\r
\r
\`\`\`java\r
static class A { ... } // ERROR at top level\r
\`\`\`\r
\r
\`\`\`expected\r
error: modifier static not allowed here\r
\r
static means "not tied to an instance of the enclosing class", and a\r
top-level class has no enclosing class to be tied to.\r
\`\`\`\r
\r
Only *nested* classes can be \`static\`; a top-level class cannot.\r
\r
### Mistake 3: Forgetting the semicolon after an anonymous class\r
\r
\`\`\`java\r
A obj1 = new A() {\r
    public void show() { ... }\r
}   // ERROR: missing ';'\r
\`\`\`\r
\r
\`\`\`expected\r
error: ';' expected\r
\r
new A() { ... } is an expression, not a declaration, so the statement it is\r
part of still needs its semicolon after the closing brace.\r
\`\`\`\r
\r
\`new Type() { ... }\` is part of a statement, so it must end with \`;\`.\r
\r
### Mistake 4: Thinking \`new A() { ... }\` instantiates the abstract class\r
\r
It does not. It creates an **anonymous subclass** of \`A\` and instantiates that.\r
The abstract \`A\` is never directly instantiated.\r
\r
### Mistake 5: Compiling all files in this chapter together\r
\r
\`\`\`bash\r
javac *.java   # fails: duplicate class A\r
\`\`\`\r
\r
Compile each file individually, as shown above.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`InnerClass.java\`\r
\r
Both member kinds in one outer class. \`B\` is the non-static inner class — it is\r
tied to an *instance* of \`A\`, so it can reach that object's fields, and creating\r
one needs the \`obj.new B()\` form. \`C\` is the static nested class — it belongs to\r
the class rather than to any object, so \`new A.C()\` is enough.\r
\r
Note what \`A\` itself is **not**: a top-level class can never carry \`static\`. Only\r
nested classes can.\r
\r
\`\`\`java InnerClass.java @run-inner-classes-inner-class\r
class A {\r
    public void show() {\r
        System.out.println("Showing in class A");\r
    }\r
\r
    class B {\r
        public void show() {\r
            System.out.println("Showing in class B");\r
        }\r
\r
        public void Display() {\r
            System.out.println("Displaying in class B");\r
        }\r
    }\r
\r
    static class C {\r
        public void show() {\r
            System.out.println("Showing in class C");\r
        }\r
    }\r
}\r
\r
public class InnerClass {\r
    public static void main(String[] args) {\r
        A obj = new A();\r
        obj.show();\r
\r
        A.B obj2 = obj.new B();\r
        obj2.show();\r
        obj2.Display();\r
\r
        A.C obj3 = new A.C();\r
        obj3.show();\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-inner-classes-inner-class\r
Showing in class A\r
Showing in class B\r
Displaying in class B\r
Showing in class C\r
\`\`\`\r
\r
### \`AnonymousInnerClass.java\`\r
\r
The same override done twice, so the two routes sit side by side. \`class B\r
extends A\` is the traditional named subclass — worth writing when the behaviour\r
gets reused. \`new A() { ... }\` is the anonymous form: it declares an unnamed\r
subclass of \`A\` and instantiates it in a single expression.\r
\r
\`obj1\` is *declared* as an \`A\`, but the object behind it is that one-off\r
subclass. The trailing \`;\` after the closing brace is required, because the\r
whole construct is one statement.\r
\r
\`\`\`java AnonymousInnerClass.java @run-inner-classes-anonymous-inner-class\r
class A {\r
    public void show() {\r
        System.out.println("Showing in class A");\r
    }\r
}\r
\r
class B extends A {\r
    public void show() {\r
        System.out.println("Showing in class B");\r
    }\r
}\r
\r
public class AnonymousInnerClass {\r
    public static void main(String[] args) {\r
        A obj = new B();\r
        obj.show();\r
\r
        A obj1 = new A() {\r
            public void show() {\r
                System.out.println("Showing in class A (Anonymous)");\r
            }\r
        };\r
        obj1.show();\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-inner-classes-anonymous-inner-class\r
Showing in class B\r
Showing in class A (Anonymous)\r
\`\`\`\r
\r
### \`AbstractAnonymousInnerClass.java\`\r
\r
This is the two ideas combined: the \`abstract\` keyword from\r
[Chapter 22](#/study/abstract-keyword) and the anonymous class above.\r
\r
\`A\` is abstract, so \`new A()\` will not compile. \`new A() { ... }\` does compile —\r
and it is not creating an \`A\`. It creates an instance of an anonymous subclass\r
that extends \`A\` and fills in the missing \`show()\`. That distinction is the\r
whole file, and it is how abstract types get used everywhere in real Java without\r
a named subclass for each one.\r
\r
\`\`\`java AbstractAnonymousInnerClass.java @run-inner-classes-abstract-anonymous-inner-class\r
abstract class A {\r
    abstract public void show();\r
}\r
\r
public class AbstractAnonymousInnerClass {\r
    public static void main(String[] args) {\r
\r
        // obj.show();\r
\r
        A obj1 = new A() {\r
            public void show() {\r
                System.out.println("Showing in class A (Anonymous)");\r
            }\r
        };\r
        obj1.show();\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-inner-classes-abstract-anonymous-inner-class\r
Showing in class A (Anonymous)\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## Nested, static and not\r
\r
![An inner instance keeps its outer object alive, a static nested one does not](diagrams/23-inner-classes-revision-what-it-keeps-alive.jpg)\r
\r
- A **static nested** class is a class that happens to live inside another. It\r
  needs no outer object: \`new Outer.Nested()\`.\r
- A **non-static inner** class holds a hidden reference to an outer instance, so\r
  it must be created from one: \`outer.new Inner()\`.\r
- That hidden reference is why an inner class can read the outer object's private\r
  fields, and also why holding an inner instance keeps the outer one alive.\r
- A **top-level** class cannot be \`static\` — there is nothing for it to be static\r
  relative to. Only a nested one can.\r
\r
## Anonymous inner classes\r
\r
![When a lambda replaces an anonymous class and when it cannot](diagrams/23-inner-classes-revision-lambda-or-not.jpg)\r
\r
- \`new Runnable() { ... }\` declares a class and creates its only instance in one\r
  expression. It has no name and cannot be reused.\r
- \`new A() { ... }\` where \`A\` is abstract does **not** instantiate \`A\`. It\r
  creates an anonymous **subclass** of \`A\` and instantiates that — which is why\r
  it is legal.\r
- It works from an interface just as well, implementing it on the spot.\r
- It is an expression, so the statement needs a **semicolon** after the closing\r
  brace. Forgetting it is the usual first error.\r
- For a single-method interface a lambda says the same thing in one line. Reach\r
  for an anonymous class when you need fields, several methods, or a name in the\r
  stack trace.\r
`,interview:`## Interview questions

### Q1: What is an inner class?

**Answer:** A class declared inside another class. It groups a helper type with
its enclosing class and (for non-static inner classes) can access the outer
object's members.

### Q2: What is the difference between a static nested class and a non-static inner class?

**Answer:** A non-static (member) inner class is tied to an instance of the outer
class and is created with \`outerObject.new Inner()\`. A static nested class is not
tied to any instance and is created directly with \`new Outer.Inner()\`.

### Q3: How do you instantiate a non-static inner class?

![The two creation forms for a member inner class and a static nested class](diagrams/23-inner-classes-interview-creation-forms.jpg)

**Answer:** Through an outer-class object: \`Outer o = new Outer(); Outer.Inner i =
o.new Inner();\`.

### Q4: Can a top-level class be declared \`static\`?

![Static is meaningful on a nested class and meaningless on a top-level one](diagrams/23-inner-classes-interview-static-relative-to-what.jpg)

**Answer:** No. Only nested classes may be \`static\`; the \`static\` keyword is not
allowed on a top-level class.

### Q5: What is an anonymous inner class?

**Answer:** A class with no name that is defined and instantiated in one
expression (\`new Type() { ... }\`). It creates a one-off subclass of a class (or
implementation of an interface), typically for a single use.

### Q6: When would you use an anonymous inner class instead of a named subclass?

**Answer:** When the subclass/implementation is needed only once. Writing a
separate named class to use it a single time is unnecessary overhead; an anonymous
class keeps the one-off behaviour inline.

### Q7: Can you create an anonymous inner class from an abstract class or interface?

**Answer:** Yes. \`new AbstractType() { ... }\` creates an anonymous subclass that
implements the abstract methods and instantiates that subclass -- the abstract
type itself is never directly instantiated.

### Q8: Does \`new A() { ... }\` create an object of \`A\` when \`A\` is abstract?

**Answer:** No. It creates an instance of an anonymous subclass of \`A\` that
provides the missing implementation. \`A\` itself cannot be instantiated.

---

*This is part of a Java learning series. It builds directly on Inheritance
(Chapter 15), Polymorphism (Chapter 17), and the \`abstract\` keyword (Chapter 22),
and anonymous inner classes are the stepping stone to interfaces and lambda
expressions.*
`}},{slug:`interfaces`,title:`Interfaces`,subject:`Java`,stage:`Types and contracts`,chapter:24,accent:`#f472b6`,summary:`An interface is a fully abstract blueprint. It declares what a class can do (a set of method signatures) without saying how it does it. Any class that "signs the contract" by implementing the interface must provide the body for every method.`,parts:{notes:`## Introduction\r
\r
An **interface** is a fully abstract blueprint. It declares *what* a class can do (a set of method signatures) without saying *how* it does it. Any class that "signs the contract" by implementing the interface must provide the body for every method.\r
\r
Interfaces are how Java achieves **100% abstraction** and a safe form of **multiple inheritance**. This builds directly on the \`abstract\` keyword from Chapter 22.\r
\r
---\r
\r
## What is an Interface\r
\r
An interface is declared with the \`interface\` keyword instead of \`class\`:\r
\r
\`\`\`java\r
interface A {\r
    void show();\r
    void config();\r
}\r
\`\`\`\r
\r
Key facts:\r
\r
- Every method in an interface is **\`public\` and \`abstract\` by default** — you do not need to write those keywords.\r
- An interface cannot be instantiated with \`new\` (it has no method bodies to run).\r
- A class connects to an interface using the \`implements\` keyword and must override **every** method.\r
- Since Java 8, interfaces can also have \`default\` and \`static\` methods (with bodies), but the classic role is a pure contract.\r
\r
---\r
\r
## \`extends\` vs \`implements\`\r
\r
The relationship keyword depends on what is connecting to what:\r
\r
| From      | To        | Keyword      |\r
|-----------|-----------|--------------|\r
| class     | class     | \`extends\`    |\r
| class     | interface | \`implements\` |\r
| interface | class     | (not allowed) |\r
| interface | interface | \`extends\`    |\r
\r
A class can \`implement\` many interfaces at once, but can only \`extend\` one class.\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java Interface.java @run-interfaces-interface
interface A {\r
\r
    String area = "In Interfaces we need to initialize the fields by default";\r
\r
    void show();\r
    void config();\r
}\r
\r
interface X {\r
    void run();\r
}\r
\r
class B implements A, X {\r
    public void show() {\r
        System.out.println("Show");\r
    }\r
\r
    public void config() {\r
        System.out.println("Config");\r
    }\r
\r
    public void run() {\r
        System.out.println("Run");\r
    }\r
}\r
\r
public class Interface {\r
    public static void main(String[] args) {\r
        A obj;\r
        // obj = new A(); // ERROR: an interface cannot be instantiated\r
        obj = new B();\r
\r
        obj.show();\r
        obj.config();\r
    }\r
}\r
\`\`\`

\`\`\`output @run-interfaces-interface
Show
Config
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
### \`interface A { ... }\`\r
\r
- Declares an interface named \`A\`. It is a pure contract.\r
- \`void show();\` and \`void config();\` are abstract methods — only signatures, no bodies.\r
\r
### \`class B implements A, X { ... }\`\r
\r
- \`B\` promises to provide bodies for **all** methods of both \`A\` and \`X\`.\r
- The overriding methods must be declared \`public\`, because interface methods are \`public\` by default and an override cannot reduce visibility.\r
\r
### \`A obj; obj = new B();\`\r
\r
- \`obj\` is a reference of the **interface type** \`A\`.\r
- It points to an object of the implementing class \`B\`. This is upcasting (Chapter 20): you program to the interface, not the concrete class.\r
- \`obj.show()\` runs \`B\`'s version of \`show()\` at runtime (dynamic dispatch, Chapter 17).\r
\r
---\r
\r
## Fields in an Interface\r
\r
\`\`\`java\r
interface A {\r
    String area = "...";\r
}\r
\`\`\`\r
\r
- Every field in an interface is automatically **\`public static final\`** — a constant.\r
- Because it is \`final\`, it **must be initialised** at declaration. Leaving it uninitialised (\`int age;\`) is a compile error.\r
\r
---\r
\r
## Why Interface Fields Are \`public static final\`\r
\r
An interface is just a **blueprint**, so its fields behave like shared constants, not per-object state:\r
\r
- **Why \`static\`?** A blueprint has no objects of its own, so the field belongs to the interface itself, shared by all implementers — not duplicated per object.\r
- **Why \`final\`?** If implementers could change the value, the blueprint would no longer be a fixed contract. A constant keeps the contract reliable.\r
- **Why this matters:** if every object had its own changeable copy (say \`obj1.age = 20\`, \`obj2.age = 30\`), the "shared contract" value would be ambiguous. Making fields \`static final\` removes that ambiguity.\r
\r
---\r
\r
## Multiple Inheritance with Interfaces\r
\r
Java does **not** allow a class to extend more than one class (to avoid the "diamond problem" of conflicting inherited code). But a class **can implement many interfaces**:\r
\r
\`\`\`java\r
class B implements A, X { ... }\r
\`\`\`

\`\`\`expected
error: B is not abstract and does not override abstract method ...

Implementing an interface is a promise to provide every method in it — and
here there are two interfaces, so every method of both.
\`\`\`\r
\r
Because interfaces only declare method signatures (no conflicting bodies), combining several of them is safe. This is how Java gives you multiple inheritance of *type*.\r
\r
---\r
\r
## Interface vs Abstract Class\r
\r
| Aspect                | Interface                                  | Abstract Class                          |\r
|-----------------------|--------------------------------------------|-----------------------------------------|\r
| Keyword to connect    | \`implements\`                               | \`extends\`                               |\r
| Multiple inheritance  | Yes (implement many)                       | No (extend only one)                    |\r
| Methods               | Abstract by default (+ \`default\`/\`static\`) | Mix of abstract and concrete            |\r
| Fields                | \`public static final\` constants only       | Any kind of field, including state      |\r
| Constructor           | No                                         | Yes                                     |\r
| When to use           | A capability/contract many unrelated types share | A common base with shared code/state |\r
\r
---\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac Interface.java\r
java Interface\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
Show\r
Config\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Trying to instantiate an interface\r
\r
\`\`\`java\r
A obj = new A();  // ERROR: A is abstract; cannot be instantiated\r
A obj = new B();  // correct: use an implementing class\r
\`\`\`

\`\`\`expected
error: A is abstract; cannot be instantiated

An interface has no implementation to construct. Declare the reference as
the interface and construct a class that implements it.
\`\`\`\r
\r
### Mistake 2: Not overriding every method\r
\r
\`\`\`java\r
class B implements A, X {\r
    public void show() { ... }\r
}\r
\`\`\`

\`\`\`expected
error: B is not abstract and does not override abstract method ...

Implementing an interface is a promise to provide every method in it — and
here there are two interfaces, so every method of both.
\`\`\`\r
\r
A non-abstract class must implement **all** inherited interface methods.\r
\r
### Mistake 3: Reducing visibility of an overridden method\r
\r
\`\`\`java\r
class B implements A {\r
    void show() { ... }  // ERROR: must be public (interface methods are public)\r
}\r
\`\`\`

\`\`\`expected
error: attempting to assign weaker access privileges; was public

Interface methods are implicitly public, and an override may never be less
visible than what it overrides.
\`\`\`\r
\r
### Mistake 4: Forgetting to initialise an interface field\r
\r
\`\`\`java\r
interface A {\r
    int age;        // ERROR: must be initialised (it is final)\r
    int age = 0;    // correct\r
}\r
\`\`\`

\`\`\`expected
error: = expected

Interface fields are implicitly public static final. Being final, they must
be given a value where they are declared — there is no constructor to
assign them in later.
\`\`\`\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`Interface.java\`\r
\r
Which keyword you use depends on what is on each side:\r
\r
| From | To | Keyword |\r
|------|----|---------|\r
| class | class | \`extends\` |\r
| class | interface | \`implements\` |\r
| interface | class | \`implements\` |\r
| interface | interface | \`extends\` |\r
\r
\`B implements A, X\` takes both interfaces at once â€” something a class could\r
never do with \`extends\`, and the main reason interfaces exist. Every method\r
declared in either one has to be defined in \`B\`, or the file will not compile.\r
\r
Inside an interface you can drop \`public abstract\` from a method: that is what\r
methods are by default. Fields work the same way â€” they are implicitly\r
\`public static final\`, which is why \`area\` **must** be given a value at\r
declaration.\r
\r
That default is not arbitrary. An interface is a blueprint, not an object:\r
\r
- **\`static\`**, because a blueprint has no per-object state. If two classes\r
  implemented \`A\` and each got its own \`age\`, there would be no answer to "what\r
  is \`A.age\`?"\r
- **\`final\`**, because a blueprint that anything can rewrite is not a blueprint\r
  any more.\r
\r
\`\`\`java Interface.java @run-interfaces-interface\r
\r
// abstract class A {\r
//     public abstract void show();\r
//     public abstract void config();\r
// }\r
\r
interface A {\r
\r
    // int age; // in interfaces fields are final static by default\r
    String area = "In Interfaces we need to initialize the fields by default";\r
\r
    void show();\r
    void config();\r
}\r
\r
interface X{\r
    void run();\r
}\r
\r
class B implements A,X {\r
    public void show(){\r
        System.out.println("Show");\r
    }\r
\r
    public void config(){\r
        System.out.println("Config");\r
    }\r
\r
    public void run(){\r
        System.out.println("Run");\r
    }\r
}\r
\r
public class Interface {\r
    public static void main(String[] args) {\r
        A obj;\r
        // obj = new A(); // A is abstract; cannot be instantiated\r
        obj = new B();\r
\r
        obj.show();\r
        obj.config();\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-interfaces-interface\r
Show\r
Config\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## The contract\r
\r
- An interface is a set of method signatures a class promises to provide. You\r
  cannot instantiate one — only a class that implements it.\r
- Methods are implicitly \`public abstract\`; fields are implicitly\r
  \`public static final\`. Writing those keywords is legal and redundant.\r
- Because fields are \`final\`, an interface constant must be given a value at\r
  declaration — there is no constructor to assign it later.\r
- An implementing class must override **every** abstract method, and cannot\r
  reduce visibility: they are \`public\` in the interface, so they are \`public\` in\r
  the class.\r
\r
## Multiple inheritance, done safely\r
\r
- A class implements any number of interfaces. That is Java's answer to multiple\r
  inheritance — many contracts, one implementation hierarchy.\r
- The diamond problem does not arise for abstract methods, because there is no\r
  implementation to choose between. Where two interfaces provide the same\r
  \`default\` method, the class must override it and say which it wants.\r
- An interface may \`extend\` other interfaces, and several at once.\r
\r
## Since Java 8\r
\r
- \`default\` methods carry a body, so an interface can gain a method without\r
  breaking every existing implementer. That is why they exist.\r
- \`static\` methods on an interface are utilities that belong to the type but are\r
  not inherited.\r
- \`private\` methods (Java 9) let default methods share code without exposing it.\r
`,interview:`## Interview questions

### Q1: What is an interface in Java?

**Answer:** An interface is a reference type that declares a set of abstract methods (a contract). Classes implement the interface and provide the method bodies. By default its methods are \`public abstract\` and its fields are \`public static final\`.

### Q2: What is the difference between an interface and an abstract class?

**Answer:** A class can implement multiple interfaces but extend only one abstract class. Interfaces traditionally contain only abstract methods and constants and have no constructor, while abstract classes can have constructors, instance fields, and a mix of abstract and concrete methods. Use an interface for a capability shared by unrelated types; use an abstract class for a common base with shared code/state.

### Q3: Why are interface methods \`public\` and \`abstract\` by default?

**Answer:** An interface is a contract meant to be used by other code, so its methods are \`public\`. They are \`abstract\` because the interface only specifies *what* must be done, leaving *how* to the implementing class.

### Q4: Why are interface fields \`public static final\`?

**Answer:** An interface is a blueprint with no objects of its own, so fields are \`static\` (shared) and \`final\` (constant) to keep the contract fixed and unambiguous. Because they are \`final\`, they must be initialised when declared.

### Q5: How does Java achieve multiple inheritance through interfaces?

**Answer:** A class can implement several interfaces at once (\`class B implements A, X\`). Since interfaces declare only method signatures and no conflicting bodies, there is no diamond-problem ambiguity, so combining them is safe.

### Q6: Can an interface extend another interface?

**Answer:** Yes. An interface uses \`extends\` (not \`implements\`) to inherit from one or more other interfaces, combining their contracts.

### Q7: Can you create an object of an interface?

**Answer:** No. An interface cannot be instantiated because its methods have no bodies. You instantiate a class that implements it, and you may hold that object in a reference variable of the interface type.
`}},{slug:`enums`,title:`Enums`,subject:`Java`,stage:`Types and contracts`,chapter:25,accent:`#facc15`,summary:`An enum (short for enumeration) is a special type that represents a fixed set of named constants. When a variable can only take one value out of a small, known list — the status of a request, the days of the week, the suits in a deck of cards — an enum makes that list explicit and type-safe.`,parts:{notes:`## Introduction\r
\r
An **enum** (short for *enumeration*) is a special type that represents a fixed set of named constants. When a variable can only take one value out of a small, known list — the status of a request, the days of the week, the suits in a deck of cards — an enum makes that list explicit and type-safe.\r
\r
This chapter has two programs:\r
\r
| File | Topic |\r
|------|-------|\r
| \`Enums.java\` | A basic enum, its methods, comparison, and use in \`switch\` |\r
| \`Enums2.java\` | An enum with fields, constructors, and methods |\r
\r
---\r
\r
## What is an Enum\r
\r
An enum is declared with the \`enum\` keyword. Each named value listed inside is a constant.\r
\r
\`\`\`java\r
enum Status {\r
    Running, Failed, Pending, Success;\r
}\r
\`\`\`\r
\r
Here \`Status\` behaves like a class, and \`Running\`, \`Failed\`, \`Pending\`, and \`Success\` are its only objects. You can never create another \`Status\` value beyond these four.\r
\r
\`\`\`java\r
Status s = Status.Running;\r
\`\`\`\r
\r
---\r
\r
## Why Use Enums\r
\r
Imagine a server that returns a status. Without enums you might use raw numbers or strings:\r
\r
\`\`\`java\r
int status = 1;\r
String status = "Runing";\r
\`\`\`\r
\r
With an enum:\r
\r
\`\`\`java\r
Status status = Status.Running;\r
\`\`\`\r
\r
Benefits:\r
\r
- **Type safety** – only the defined values are allowed; a typo is a compile error.\r
- **Readability** – \`Status.Running\` is clearer than \`1\`.\r
- **Fixed set** – the compiler guarantees no other values exist.\r
- **Works with \`switch\`** – clean branching over the constants.\r
\r
> Enums **cannot be extended** — you cannot add new constants by subclassing. The set is final by design.\r
\r
---\r
\r
## Example 1: A Basic Enum\r
\r
\`\`\`java Enums.java @run-enums-enums
enum Status {\r
    Running, Failed, Pending, Success;\r
}\r
\r
public class Enums {\r
    public static void main(String[] args) {\r
        Status s = Status.Running;\r
\r
        System.out.println(s);\r
        System.out.println(s.ordinal());\r
\r
        Status[] all = Status.values();\r
        for (Status status : all) {\r
            System.out.println(status);\r
        }\r
\r
        if (s == Status.Running) {\r
            System.out.println("Running");\r
        }\r
\r
        switch (s) {\r
            case Running: System.out.println("Running"); break;\r
            case Failed:  System.out.println("Failed");  break;\r
            case Pending: System.out.println("Pending"); break;\r
            case Success: System.out.println("Success"); break;\r
            default:      System.out.println("Unknown");\r
        }\r
    }\r
}\r
\`\`\`

\`\`\`output @run-enums-enums
Running
0
Running
Failed
Pending
Success
Running
Running
\`\`\`\r
\r
---\r
\r
## Useful Enum Methods\r
\r
Every enum automatically gets these methods:\r
\r
| Method        | Returns                                              | Example                       |\r
|---------------|-----------------------------------------------------|-------------------------------|\r
| \`name()\`      | the exact name of the constant as a \`String\`        | \`Status.Running.name()\` -> \`"Running"\` |\r
| \`ordinal()\`   | the zero-based position in the declaration order    | \`Status.Running.ordinal()\` -> \`0\` |\r
| \`values()\`    | an array of all constants (static method)           | \`Status.values()\`            |\r
| \`valueOf("x")\`| the constant matching the given name                | \`Status.valueOf("Failed")\`   |\r
\r
> Do **not** use \`ordinal()\` as a stored/business value. If you reorder the constants, every ordinal changes. Use it only for display or iteration.\r
\r
---\r
\r
## Comparing Enums\r
\r
Enum constants are singletons (only one object per constant), so you can compare them with \`==\`:\r
\r
\`\`\`java\r
if (s == Status.Running) { ... }\r
\`\`\`\r
\r
\`==\` is safe here, is null-safe, and reads clearly. \`.equals()\` also works but \`==\` is the idiomatic choice for enums.\r
\r
---\r
\r
## Enums in a \`switch\`\r
\r
A \`switch\` pairs naturally with an enum. Inside the \`case\` labels you write the constant name **without** the enum prefix:\r
\r
\`\`\`java\r
switch (s) {\r
    case Running:\r
        ...\r
        break;\r
}\r
\`\`\`\r
\r
The compiler also helps you spot a \`switch\` that forgets one of the constants.\r
\r
---\r
\r
## Example 2: Enums with Fields and Constructors\r
\r
Enums are full classes, so each constant can carry data. Here each laptop constant stores a price.\r
\r
\`\`\`java Enums2.java @run-enums-enums2
enum Laptop {\r
    Alienware(120000),\r
    Lenovo(100000),\r
    HP,\r
    Dell(130000);\r
\r
    private int price;\r
\r
    Laptop() {\r
        this.price = 110000;\r
    }\r
\r
    Laptop(int price) {\r
        this.price = price;\r
    }\r
\r
    public int getPrice() {\r
        return price;\r
    }\r
}\r
\r
public class Enums2 {\r
    public static void main(String[] args) {\r
        Laptop lap = Laptop.Alienware;\r
        System.out.println(lap + " : " + lap.getPrice());\r
        System.out.println(lap.ordinal());\r
        System.out.println(lap.name());\r
    }\r
}\r
\`\`\`

\`\`\`output @run-enums-enums2
Alienware : 120000
0
Alienware
\`\`\`\r
\r
**What happens:**\r
\r
- The value in parentheses after a constant (\`Alienware(120000)\`) is passed to the matching enum **constructor**.\r
- \`HP\` has no parentheses, so the **no-argument constructor** runs and assigns the default price \`110000\`.\r
- An enum constructor is always **private** in effect — you can never call \`new Laptop()\` yourself. The constants are created once, when the enum is loaded.\r
\r
---\r
\r
## How Enums Work Internally\r
\r
Behind the scenes, an enum is compiled to a special \`final\` class that extends \`java.lang.Enum\`. Each constant becomes a \`public static final\` object of that class, created once. This is why:\r
\r
- The set of values is fixed and an enum **cannot be subclassed**.\r
- Constants are singletons, so \`==\` comparison is correct.\r
- They can have fields, constructors, and methods like any other class.\r
\r
---\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac Enums.java\r
java Enums\r
\r
javac Enums2.java\r
java Enums2\r
\`\`\`\r
\r
### Expected Output (Example 2)\r
\r
\`\`\`\r
Alienware : 120000\r
0\r
Alienware\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Trying to instantiate an enum\r
\r
\`\`\`java\r
Status s = new Status();  // ERROR: enum constructors are not accessible\r
Status s = Status.Running; // correct\r
\`\`\`

\`\`\`expected
error: enum types may not be instantiated

The constants are the only instances there will ever be. That is what makes
the set closed, and it is the point of an enum.
\`\`\`\r
\r
### Mistake 2: Prefixing the enum name inside a \`switch\` case\r
\r
\`\`\`java\r
switch (s) {\r
    case Status.Running:  // ERROR\r
    case Running:         // correct\r
}\r
\`\`\`

\`\`\`expected
error: an enum switch case label must be the unqualified name of an
    enumeration constant

The switch already knows the type from the selector, so the bare constant
name is both required and unambiguous.
\`\`\`\r
\r
### Mistake 3: Relying on \`ordinal()\` as a permanent value\r
\r
\`\`\`java\r
\`\`\`\r
\r
### Mistake 4: Forgetting the semicolon before members\r
\r
\`\`\`java\r
enum Laptop {\r
    HP, Dell   // ERROR: need a ';' here before the field/constructor\r
    private int price;\r
}\r
\`\`\`

\`\`\`expected
error: ';' expected

The constants must come first, and anything after them — a field, a
constructor, a method — has to be separated from the list by a semicolon.
\`\`\`\r
\r
When an enum has fields or methods, the list of constants must end with a semicolon.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`Enums.java\`\r
\r
An \`enum\` is a fixed set of named constants â€” here the four states a job can be\r
in. Because the set is closed, the compiler knows every possible value, which is\r
what makes an enum safer than passing strings or \`int\` codes around.\r
\r
The file prints one of the constants and shows it working with a \`switch\`, where\r
you write the bare constant name rather than qualifying it.\r
\r
\`\`\`java Enums.java @run-enums-enums\r
\r
enum Status {\r
    Running, Failed, Pending, Success;\r
}\r
\r
public class Enums {\r
    public static void main(String[] args) {\r
        int i = 5;\r
\r
        Status s = Status.Running;\r
\r
        System.out.println(s);\r
        System.out.println(s.ordinal());\r
\r
        Status[] ss = Status.values();\r
        for (Status status : ss) {\r
            System.out.println(status);\r
        }\r
\r
        if (s == Status.Running) {\r
            System.out.println("Running");\r
        }\r
\r
        switch (s) {\r
            case Running:\r
                System.out.println("Running");\r
                break;\r
            case Failed:\r
                System.out.println("Failed");\r
                break;\r
            case Pending:\r
                System.out.println("Pending");\r
                break;\r
            case Success:\r
                System.out.println("Success");\r
                break;\r
            default:\r
                System.out.println("Unknown");\r
        }\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-enums-enums\r
Running\r
0\r
Running\r
Failed\r
Pending\r
Success\r
Running\r
Running\r
\`\`\`\r
\r
### \`Enums2.java\`\r
\r
An enum is really a class, and this one proves it: each constant carries a\r
price, passed as a **constructor argument**. That means an enum can have fields,\r
a constructor and methods like any other type.\r
\r
Look at \`HP\` â€” it is declared with no argument, so it needs a constructor that\r
takes none. Add data to an enum and every constant must match some constructor\r
you have provided.\r
\r
\`\`\`java Enums2.java @run-enums-enums2\r
enum Laptop {\r
    Alienware(120000),\r
    Lenovo(100000),\r
    HP,\r
    Dell(130000);\r
\r
    private int price;\r
\r
    Laptop() {\r
        this.price = 110000;\r
    }\r
\r
    Laptop(int price) {\r
        this.price = price;\r
    }\r
\r
    public int getPrice() {\r
        return price;\r
    }\r
}\r
\r
public class Enums2 {\r
    public static void main(String[] args) {\r
        Laptop lap = Laptop.Alienware;\r
        System.out.println(lap + " : " + lap.getPrice());\r
        System.out.println(lap.ordinal());\r
        System.out.println(lap.name());\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-enums-enums2\r
Alienware : 120000\r
0\r
Alienware\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## What an enum is\r
\r
- A fixed set of named instances of a class. Each constant is a \`public static\r
  final\` object of the enum type, created once by the JVM.\r
- You cannot \`new\` one — the constants are the only instances there will ever be,\r
  which is what makes the set closed.\r
- Enums extend \`java.lang.Enum\` implicitly, so they cannot extend anything else.\r
  They can implement interfaces.\r
\r
## Fields, constructors and members\r
\r
- An enum may have fields, a constructor and methods. The constructor is\r
  implicitly private and runs once per constant.\r
- The constants must come **first**, and if anything follows them they need a\r
  **semicolon** after the list.\r
- Each constant may have its own body, overriding a method for just that value —\r
  a small state machine without a \`switch\`.\r
\r
## Using them\r
\r
- \`name()\` is the constant's identifier as written; \`ordinal()\` is its position.\r
  Do not persist \`ordinal()\` — reordering the constants silently changes it.\r
- \`values()\` returns all of them in declaration order; \`valueOf(String)\` looks one\r
  up and throws \`IllegalArgumentException\` if there is none.\r
- In a \`switch\`, case labels are the **bare** constant name. Prefixing the enum\r
  type does not compile, because the type is already known.\r
- Compare with \`==\`. Enum constants are singletons, so \`==\` is correct, faster\r
  than \`.equals()\`, and null-safe.\r
`,interview:`## Interview questions

### Q1: What is an enum in Java?

**Answer:** An enum is a special type that defines a fixed set of named constants. Each constant is a singleton object of the enum type. Enums are type-safe and are used when a variable should only hold one value from a known list.

### Q2: Can an enum have a constructor?

**Answer:** Yes. An enum can have constructors, fields, and methods. The constructor is effectively private and is called once for each constant when the enum is loaded. You provide arguments in parentheses after the constant name.

### Q3: Why can't you create an enum object with \`new\`?

**Answer:** Enum constructors are not accessible from outside the enum. The fixed set of constants is created automatically when the enum class is loaded, so creating more instances would break the guarantee that the value set is fixed.

### Q4: What is the difference between \`name()\` and \`ordinal()\`?

**Answer:** \`name()\` returns the constant's name as a String; \`ordinal()\` returns its zero-based position in the declaration order. \`ordinal()\` should not be used as a stored business value because it changes if the constants are reordered.

### Q5: Can you use an enum in a \`switch\` statement?

**Answer:** Yes. Enums work cleanly in a \`switch\`, and the case labels use the constant name without the enum prefix (e.g. \`case Running:\`).

### Q6: Can an enum implement an interface or extend a class?

**Answer:** An enum can implement interfaces, but it cannot extend a class because it already implicitly extends \`java.lang.Enum\`. For the same reason, an enum cannot be subclassed.

### Q7: How do you compare two enum values, and why?

**Answer:** Use \`==\`. Because each enum constant is a singleton, \`==\` compares the correct identity, is null-safe, and is the idiomatic approach. \`.equals()\` works too but is not needed.

---

*This is the final chapter of the Java learning series. Congratulations on completing it — revisit any chapter to reinforce the fundamentals.*
`}},{slug:`annotations`,title:`Annotations`,subject:`Java`,stage:`Types and contracts`,chapter:26,accent:`#22d3ee`,summary:`An annotation is extra information you attach to your code using the @ symbol. The most important idea to understand on day one:`,parts:{notes:`## Introduction

An **annotation** is **extra information you attach to your code** using the \`@\` symbol. The most important idea to understand on day one:

> An annotation by itself **does nothing**. It is just a label. Its power comes from **someone reading that label and reacting to it**.

Think of an annotation like a **sticky note** on a folder, or a **luggage tag** on a suitcase. The note does not move the suitcase — but the airport staff who *read* the tag know where to send it.

This whole chapter lives in **one runnable file**: [\`Annotations.java\`](Annotations.java). Run it once and you will watch all five topics print their results in order.

---

## What Is an Annotation

You attach an annotation by writing \`@Name\` directly above a class, method, field, or parameter:

\`\`\`java
@Override
public void shows() { ... }

@Deprecated
public void payOld() { ... }
\`\`\`

Some annotations can also carry **values** inside parentheses:

\`\`\`java
@Author(name = "Aditya", date = "2026-06-03")
class TaskRunner { }
\`\`\`

That is all the syntax there is. The interesting part is *who reads it*.

---

## Who Reads Annotations

There are exactly **three** audiences that read annotations. Knowing which audience an annotation is for explains everything about how it behaves.

| # | Reader | Example annotations | When it acts |
|---|--------|---------------------|--------------|
| 1 | **The compiler** | \`@Override\`, \`@Deprecated\`, \`@SuppressWarnings\` | While you compile |
| 2 | **Tools & frameworks** | JUnit \`@Test\`, Spring \`@Autowired\`, JPA \`@Entity\` | When the tool runs |
| 3 | **Your own code** (via Reflection) | any custom annotation you write | While the program runs |

This chapter shows all three: the compiler ones (Sections 1–2), how to *build* annotations for the framework/runtime audience (Sections 3–4), and how to *read them yourself* at runtime (Section 5).

---

## Section 1: \`@Override\`

\`@Override\` is the annotation you will use the most. It tells the compiler:

> "I am **on purpose** replacing a method from my parent class. Please **check** that a matching method really exists up there."

### The bug it catches

Our code has a parent \`A\` with a method named \`shows()\` (note the **'s'**):

\`\`\`java
class A {
    public void shows() {
        System.out.println("In A.shows()");
    }
}

class B extends A {
    @Override
    public void shows() {
        System.out.println("In B.shows() (correctly overrides A.shows())");
    }
}
\`\`\`

Now imagine you **mistype** the name in \`B\` as \`show()\` (missing the 's'):

| Without \`@Override\` | With \`@Override\` |
|---------------------|------------------|
| Compiler stays silent. It thinks \`show()\` is a brand-new method. \`A\`'s old \`shows()\` keeps running. **Silent bug** that you only notice much later. | Compiler **immediately errors**: *"method does not override a method from its superclass"*. You fix the typo in seconds. |

That is the whole point: **\`@Override\` turns a silent runtime bug into a loud compile-time error.**

![A mistyped override with and without the @Override annotation](diagrams/26-annotations-notes-override-typo.jpg)

> 💡 \`@Override\` is a *compile-time-only* check. It is thrown away by the compiler and never reaches the running program (this is called \`SOURCE\` retention — see Section 3).

---

## Section 2: The Other Built-in Annotations

Java ships four more everyday annotations in the \`java.lang\` package. All four are read by the **compiler**.

### \`@Deprecated\` — "old, don't use me"

Marks something as outdated so the compiler warns anyone who uses it.

\`\`\`java
@Deprecated(since = "2.0", forRemoval = true)
public void payOld() { ... }

public void pay() { ... }
\`\`\`

- \`since\` — the version it became outdated.
- \`forRemoval = true\` — it is scheduled to be **deleted** in a future version.

> ⚠️ **Advanced nuance (real Java behaviour):** when \`forRemoval = true\`, the warning belongs to the **\`"removal"\`** category, **not** \`"deprecation"\`. So to silence it you suppress \`"removal"\`, not \`"deprecation"\`. The code comments call this out.

### \`@SuppressWarnings\` — "mute these warnings here"

Tells the compiler to hide specific warning types for one element.

\`\`\`java
@SuppressWarnings({ "removal", "unused" })
public static void main(String[] args) { ... }
\`\`\`

Common values: \`"deprecation"\`, \`"removal"\`, \`"unchecked"\`, \`"unused"\`, \`"all"\`.
Use it **sparingly** — a hidden warning is a warning you chose to take responsibility for.

### \`@SafeVarargs\` — "my generic varargs is safe"

A generic varargs parameter \`(T...)\` makes Java nervous (it warns about "unchecked / heap pollution"). If you know your method only **reads** the values, this annotation promises that and silences the warning.

\`\`\`java
@SafeVarargs
private static <T> List<T> listOf(T... items) {
    List<T> list = new ArrayList<>();
    for (T item : items) list.add(item);
    return list;
}
\`\`\`

It is only allowed on methods that **can't be overridden** (\`static\`, \`final\`, or \`private\`), so the promise can't be broken by a subclass.

### \`@FunctionalInterface\` — "exactly one abstract method"

Declares that an interface is meant to have **a single abstract method** so it can be used with a lambda. The compiler then **enforces** that rule. (Full treatment in [Chapter 27](#/study/interface-types).)

\`\`\`java
@FunctionalInterface
interface Calculator {
    int operate(int a, int b);
    default void banner() { ... }
}

Calculator add = (x, y) -> x + y;
\`\`\`

---

## Section 3: Meta-Annotations

A **meta-annotation** is **an annotation that you put on *another annotation*** to configure how it behaves. Think of them as the **settings** for the custom labels you create. There are five.

### \`@Retention\` — how long the annotation lives

This is the most important meta-annotation. It has three settings:

| Policy | Kept in \`.class\` file? | Visible at runtime? | Example / use |
|--------|------------------------|---------------------|---------------|
| \`SOURCE\`  | ❌ no  | ❌ no | \`@Override\` — compiler check only, then discarded |
| \`CLASS\`   | ✅ yes | ❌ no | the **default** if you don't specify |
| \`RUNTIME\` | ✅ yes | ✅ **yes** | needed so **frameworks/reflection can read it** |

> 🔑 **Rule of thumb:** if you want to read an annotation with reflection (Section 5), it **must** be \`@Retention(RUNTIME)\`. This single fact explains 90% of "why is my annotation not being found?" problems.

![Where each retention policy drops the annotation on the way to the JVM](diagrams/26-annotations-notes-retention-lifecycle.jpg)

### \`@Target\` — where the annotation may be used

Restricts an annotation to certain places. If you use it elsewhere, it's a compile error.

\`\`\`java
@Target({ ElementType.TYPE, ElementType.METHOD })
@interface Author { ... }
\`\`\`

Common targets: \`TYPE\` (class/interface/enum), \`METHOD\`, \`FIELD\`, \`PARAMETER\`, \`CONSTRUCTOR\`.

### \`@Documented\` — show it in Javadoc

Asks the Javadoc tool to display the annotation in the generated API documentation.

### \`@Inherited\` — let subclasses inherit it

Normally a subclass does **not** see an annotation placed on its parent. \`@Inherited\` flips that:

\`\`\`java
@Inherited
@interface Framework { String value(); }

@Framework("Spring")
class BaseEntity { }

class ChildEntity extends BaseEntity { }
\`\`\`

Yet asking \`ChildEntity\` for \`@Framework\` returns **"Spring"**, because the parent's annotation is \`@Inherited\`. (Works for class annotations only.)

### \`@Repeatable\` — apply the same annotation many times

By default you can use an annotation on something only once. \`@Repeatable\` allows repeats, but you must also create a **container** annotation to hold them:

\`\`\`java
@Repeatable(Schedules.class)
@interface Schedule { String day(); }

@interface Schedules { Schedule[] value(); }

@Schedule(day = "Monday")
@Schedule(day = "Friday")
class TaskRunner { }
\`\`\`

You read all repeats at once with \`getAnnotationsByType(Schedule.class)\`.

---

## Section 4: Writing Your Own Annotation

You declare an annotation with **\`@interface\`** (the \`@\` is part of it). Inside, each "method" is an **element** — a named attribute you can set.

\`\`\`java
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.METHOD)
@interface TestCase {
    String description();
    Priority priority() default Priority.MEDIUM;
    int timeoutMillis() default 1000;
    String[] tags() default {};
}
\`\`\`

\`description()\` has no \`default\`, so it is **required** — every use must supply
it. The other three all have defaults, so they are **optional**: an enum
default, a primitive default, and an empty-array default.

### Element rules (the things people forget)

- An element's type must be a **primitive, \`String\`, \`Class\`, an enum, another annotation, or an array** of those.
- Elements have **no parameters and no body**.
- \`default <value>\` makes an element **optional**. With no default, it is **required**.
- An element named exactly **\`value\`** can be set without writing \`value=\`:

\`\`\`java
@interface Role { String value(); }

@Role("ADMIN")
\`\`\`

### Using them

\`\`\`java
@TestCase(
    description = "login succeeds with valid credentials",
    priority = Priority.HIGH,
    tags = { "auth", "smoke" })
public void testLogin() { }

@TestCase(description = "cart total adds up")
public void testCart() { }
\`\`\`

---

## Section 5: Reading Annotations by Reflection

This is **why annotations exist**. Frameworks like JUnit, Spring, and Jackson read *your* annotations at runtime using **Reflection** (the ability of a running program to inspect its own classes, methods, and fields) and then act on them.

In the file we build **two tiny frameworks ourselves** to prove the idea.

### Mini-framework 1: a JSON serializer driven by \`@JsonField\`

\`\`\`java
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
@interface JsonField { String name() default ""; }

class Account {
    @JsonField(name = "account_id") int id;
    @JsonField                      String owner;
    @JsonField(name = "balance")    int balance;
    String internalNote;
}
\`\`\`

\`owner\` is tagged with no \`name\`, so its default \`""\` applies and the key falls
back to the real field name. \`internalNote\` is tagged with nothing at all.

The serializer walks every field, and **only fields tagged with \`@JsonField\`** appear in the JSON:

\`\`\`text
{"account_id": 1, "owner": "Ada", "balance": 500}
\`\`\`

Notice \`internalNote\` is **missing** — proving the output is driven by the annotation, not by the raw field list.

![Only the fields carrying @JsonField reach the JSON output](diagrams/26-annotations-notes-jsonfield-filter.jpg)

### Mini-framework 2: a validator driven by \`@Range\`

\`\`\`java
@interface Range { int min(); int max(); }

@Range(min = 0, max = 1_000_000)
int balance;
\`\`\`

The validator reads each \`@Range\` and reports any value outside the bounds:

\`\`\`text
   OK     : balance = 500
   INVALID: balance = -50 (allowed 0..1000000)
\`\`\`

### The key reflection calls

| Call | What it does |
|------|--------------|
| \`SomeClass.class.getAnnotation(X.class)\` | get one annotation on a class (or \`null\`) |
| \`method.getAnnotation(X.class)\` | get an annotation on a method |
| \`field.getAnnotation(X.class)\` | get an annotation on a field |
| \`SomeClass.class.getAnnotationsByType(X.class)\` | get **all** repeats of a \`@Repeatable\` annotation |
| \`field.setAccessible(true)\` then \`field.get(obj)\` | read the field's value |

> 🔑 Remember: **none** of this works unless the annotation is \`@Retention(RUNTIME)\`.

---

## How to Compile and Run

From the repository root (\`d:\\Repos\\java\`):

\`\`\`bash
javac Annotations.java
java Annotations
\`\`\`

### Expected Output

\`\`\`text
===== SECTION 1 : @Override =====
In B.shows() (correctly overrides A.shows())
In B.shows() (correctly overrides A.shows())

===== SECTION 2 : other built-in annotations =====
Old payment flow (DEPRECATED - will be removed)
New payment flow
listOf(...) = [Ada, Linus, Grace]
[Calculator] combines two ints into one
add(2, 3) = 5
mul(2, 3) = 6

===== SECTION 3 : meta-annotations =====
@Author name = Aditya, date = 2026-06-03
BaseEntity  @Framework = Spring
ChildEntity @Framework = Spring   <-- inherited via @Inherited
TaskRunner runs on 2 day(s):
   - Monday
   - Friday

===== SECTION 4 : custom annotations =====
Class-level @Role value = ADMIN
Scanning methods for @TestCase ...
  testLogin() -> desc='login succeeds with valid credentials', priority=HIGH, timeout=1000, tags=[auth, smoke]
  testCart() -> desc='cart total adds up', priority=MEDIUM, timeout=1000, tags=[]

===== SECTION 5 : reflection mini-framework =====
JSON (driven by @JsonField):
  {"account_id": 1, "owner": "Ada", "balance": 500}
  {"account_id": 2, "owner": "Linus", "balance": -50}
Validation (driven by @Range):
 good account:
   OK     : balance = 500
 bad account:
   INVALID: balance = -50 (allowed 0..1000000)
\`\`\`

> The order of \`testLogin\` / \`testCart\` in Section 4 may swap — \`getDeclaredMethods()\` does not guarantee an order.

---

## Quick Reference Tables

### All annotations used in this chapter

| Annotation | Kind | One-line meaning |
|-----------|------|------------------|
| \`@Override\` | built-in | "I really override a parent method" (compiler checks) |
| \`@Deprecated\` | built-in | "old, don't use me" |
| \`@SuppressWarnings\` | built-in | "hide these warnings here" |
| \`@SafeVarargs\` | built-in | "my generic varargs is type-safe" |
| \`@FunctionalInterface\` | built-in | "this interface has exactly one abstract method" |
| \`@Retention\` | meta | how long the annotation lives |
| \`@Target\` | meta | where the annotation may be used |
| \`@Documented\` | meta | show it in Javadoc |
| \`@Inherited\` | meta | subclasses inherit it |
| \`@Repeatable\` | meta | may be applied more than once |
| \`@Author\`, \`@TestCase\`, \`@Role\`, \`@JsonField\`, \`@Range\` | custom | annotations we built ourselves |

### \`@Retention\` cheat-sheet

| Policy | Survives to runtime? | Use when |
|--------|----------------------|----------|
| \`SOURCE\` | No | Pure compiler checks (\`@Override\`) |
| \`CLASS\` (default) | No | You rarely need this explicitly |
| \`RUNTIME\` | **Yes** | **Anything read by reflection / frameworks** |

---

## Common Mistakes

### Mistake 1: Expecting an annotation to "do" something on its own

\`\`\`java
@Range(min = 0, max = 100)
int balance = -50;
\`\`\`

\`\`\`expected
Compiles, runs, and balance is -50.

An annotation is metadata. Nothing checks it unless something goes looking:
a compiler plugin, a framework, or your own reflection code.
\`\`\`

An annotation is just a label. **Something must read it** (our \`validate()\` method) for it to have any effect.

### Mistake 2: Forgetting \`@Retention(RUNTIME)\`

\`\`\`java
@interface JsonField { }
field.getAnnotation(JsonField.class);
\`\`\`

\`\`\`expected
null

The default retention is CLASS, which means the annotation is written into
the .class file and then discarded by the class loader. Reflection can only
see @Retention(RetentionPolicy.RUNTIME).
\`\`\`

If reflection should see it, you **must** write \`@Retention(RetentionPolicy.RUNTIME)\`.

### Mistake 3: Suppressing the wrong warning category

\`\`\`java
@Deprecated(forRemoval = true)
@SuppressWarnings("deprecation")          // does NOT silence it!
@SuppressWarnings("removal")              // correct
\`\`\`

\`\`\`expected
The "deprecation" category does not cover forRemoval = true.
Use @SuppressWarnings("removal") for that one — and the narrowest scope you
can, so the next deprecation is not silenced with it.
\`\`\`

### Mistake 4: Giving a required element no value

\`\`\`java
@interface TestCase { String description(); }

@TestCase                                       // ERROR: description is missing
@TestCase(description = "ok")                    // correct
\`\`\`

### Mistake 5: Using an annotation where its \`@Target\` forbids it

\`\`\`java
@Target(ElementType.METHOD)
@interface TestCase { ... }

@TestCase                       // ERROR if placed on a field/class
int x;
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Annotations.java\`

Everything above, in one runnable file. It is laid out the way Java requires —
every supporting type first, then the single \`public\` class whose \`main\` runs
the five demos in order and prints a banner for each.

A few things in it exist only to be demonstrated, and are easy to misread as
mistakes:

- **\`A.shows()\` has a trailing \`s\`.** That is the typo trap from Section 1. Drop
  the \`s\` from \`B\`'s copy and \`@Override\` turns a silent bug into a compile
  error.
- **\`int neverUsed = 42;\`** is never used on purpose — it is what the \`"unused"\`
  half of \`@SuppressWarnings\` is hiding.
- **\`@SuppressWarnings({ "removal", "unused" })\`** says \`removal\`, not
  \`deprecation\`. \`payOld()\` is marked \`forRemoval = true\`, and that puts its
  warning in a different lint category.
- **\`Account.internalNote\` carries no \`@JsonField\`.** It is left bare so the JSON
  output proves the serializer is driven by the annotation rather than by the
  field list.
- **\`ChildEntity\` declares no \`@Framework\`** and still reports one, because
  \`@Framework\` is \`@Inherited\`.


\`\`\`java Annotations.java @run-annotations-annotations
import java.lang.annotation.Documented;
import java.lang.annotation.ElementType;
import java.lang.annotation.Inherited;
import java.lang.annotation.Repeatable;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;
import java.lang.reflect.Field;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

class A {
    public void shows() {
        System.out.println("In A.shows()");
    }
}

class B extends A {

    @Override
    public void shows() {
        System.out.println("In B.shows() (correctly overrides A.shows())");
    }
}

class LegacyPaymentApi {

    @Deprecated(since = "2.0", forRemoval = true)
    public void payOld() {
        System.out.println("Old payment flow (DEPRECATED - will be removed)");
    }

    public void pay() {
        System.out.println("New payment flow");
    }
}

@FunctionalInterface
interface Calculator {
    int operate(int a, int b);

    default void banner() {
        System.out.println("[Calculator] combines two ints into one");
    }
}

@Documented
@Retention(RetentionPolicy.RUNTIME)
@Target({ ElementType.TYPE, ElementType.METHOD })
@interface Author {
    String name();
    String date() default "unknown";
}

@Inherited
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.TYPE)
@interface Framework {
    String value();
}

@Framework("Spring")
class BaseEntity {
}

class ChildEntity extends BaseEntity {
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.TYPE)
@Repeatable(Schedules.class)
@interface Schedule {
    String day();
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.TYPE)
@interface Schedules {
    Schedule[] value();
}

@Author(name = "Aditya", date = "2026-06-03")
@Schedule(day = "Monday")
@Schedule(day = "Friday")
class TaskRunner {
}

enum Priority {
    LOW, MEDIUM, HIGH
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.METHOD)
@interface TestCase {
    String description();
    Priority priority() default Priority.MEDIUM;
    int timeoutMillis() default 1000;
    String[] tags() default {};
}

@Retention(RetentionPolicy.RUNTIME)
@Target({ ElementType.TYPE, ElementType.METHOD })
@interface Role {
    String value();
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
@interface JsonField {
    String name() default "";
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
@interface Range {
    int min();
    int max();
}

class Account {

    @JsonField(name = "account_id")
    int id;

    @JsonField
    String owner;

    @JsonField(name = "balance")
    @Range(min = 0, max = 1_000_000)
    int balance;

    String internalNote;

    Account(int id, String owner, int balance, String internalNote) {
        this.id = id;
        this.owner = owner;
        this.balance = balance;
        this.internalNote = internalNote;
    }
}

@Role("ADMIN")
public class Annotations {

    @TestCase(
            description = "login succeeds with valid credentials",
            priority = Priority.HIGH,
            tags = { "auth", "smoke" })
    public void testLogin() {
    }

    @TestCase(description = "cart total adds up")
    public void testCart() {
    }

    @SafeVarargs
    private static <T> List<T> listOf(T... items) {
        List<T> list = new ArrayList<>();
        for (T item : items) {
            list.add(item);
        }
        return list;
    }

    static String toJson(Object obj) throws IllegalAccessException {
        StringBuilder json = new StringBuilder("{");
        boolean first = true;

        for (Field field : obj.getClass().getDeclaredFields()) {
            JsonField jf = field.getAnnotation(JsonField.class);
            if (jf == null) {
                continue;
            }
            field.setAccessible(true);
            String key = jf.name().isEmpty() ? field.getName() : jf.name();
            Object value = field.get(obj);

            if (!first) {
                json.append(", ");
            }
            first = false;

            if (value instanceof String) {
                json.append('"').append(key).append("\\": \\"").append(value).append('"');
            } else {
                json.append('"').append(key).append("\\": ").append(value);
            }
        }
        return json.append("}").toString();
    }

    static void validate(Object obj) throws IllegalAccessException {
        for (Field field : obj.getClass().getDeclaredFields()) {
            Range range = field.getAnnotation(Range.class);
            if (range == null) {
                continue;
            }
            field.setAccessible(true);
            int value = field.getInt(obj);
            if (value < range.min() || value > range.max()) {
                System.out.println("   INVALID: " + field.getName() + " = " + value
                        + " (allowed " + range.min() + ".." + range.max() + ")");
            } else {
                System.out.println("   OK     : " + field.getName() + " = " + value);
            }
        }
    }

    @SuppressWarnings({ "removal", "unused" })
    public static void main(String[] args) throws IllegalAccessException {

        System.out.println("===== SECTION 1 : @Override =====");
        B b = new B();
        b.shows();
        A upcast = new B();
        upcast.shows();

        System.out.println("\\n===== SECTION 2 : other built-in annotations =====");

        LegacyPaymentApi api = new LegacyPaymentApi();
        api.payOld();
        api.pay();

        int neverUsed = 42;

        List<String> names = listOf("Ada", "Linus", "Grace");
        System.out.println("listOf(...) = " + names);

        Calculator add = (x, y) -> x + y;
        Calculator mul = (x, y) -> x * y;
        add.banner();
        System.out.println("add(2, 3) = " + add.operate(2, 3));
        System.out.println("mul(2, 3) = " + mul.operate(2, 3));

        System.out.println("\\n===== SECTION 3 : meta-annotations =====");

        Author author = TaskRunner.class.getAnnotation(Author.class);
        System.out.println("@Author name = " + author.name() + ", date = " + author.date());

        Framework base = BaseEntity.class.getAnnotation(Framework.class);
        Framework child = ChildEntity.class.getAnnotation(Framework.class);
        System.out.println("BaseEntity  @Framework = " + (base != null ? base.value() : "none"));
        System.out.println("ChildEntity @Framework = " + (child != null ? child.value() : "none")
                + "   <-- inherited via @Inherited");

        Schedule[] schedules = TaskRunner.class.getAnnotationsByType(Schedule.class);
        System.out.println("TaskRunner runs on " + schedules.length + " day(s):");
        for (Schedule s : schedules) {
            System.out.println("   - " + s.day());
        }

        System.out.println("\\n===== SECTION 4 : custom annotations =====");

        Role role = Annotations.class.getAnnotation(Role.class);
        System.out.println("Class-level @Role value = " + role.value());

        System.out.println("Scanning methods for @TestCase ...");
        for (var method : Annotations.class.getDeclaredMethods()) {
            TestCase tc = method.getAnnotation(TestCase.class);
            if (tc == null) {
                continue;
            }
            System.out.println("  " + method.getName() + "() -> "
                    + "desc='" + tc.description() + "'"
                    + ", priority=" + tc.priority()
                    + ", timeout=" + tc.timeoutMillis()
                    + ", tags=" + Arrays.toString(tc.tags()));
        }

        System.out.println("\\n===== SECTION 5 : reflection mini-framework =====");

        Account good = new Account(1, "Ada", 500, "secret memo");
        Account bad = new Account(2, "Linus", -50, "another memo");

        System.out.println("JSON (driven by @JsonField):");
        System.out.println("  " + toJson(good));
        System.out.println("  " + toJson(bad));

        System.out.println("Validation (driven by @Range):");
        System.out.println(" good account:");
        validate(good);
        System.out.println(" bad account:");
        validate(bad);
    }
}
\`\`\`

\`\`\`output @run-annotations-annotations
===== SECTION 1 : @Override =====
In B.shows() (correctly overrides A.shows())
In B.shows() (correctly overrides A.shows())

===== SECTION 2 : other built-in annotations =====
Old payment flow (DEPRECATED - will be removed)
New payment flow
listOf(...) = [Ada, Linus, Grace]
[Calculator] combines two ints into one
add(2, 3) = 5
mul(2, 3) = 6

===== SECTION 3 : meta-annotations =====
@Author name = Aditya, date = 2026-06-03
BaseEntity  @Framework = Spring
ChildEntity @Framework = Spring   <-- inherited via @Inherited
TaskRunner runs on 2 day(s):
   - Monday
   - Friday

===== SECTION 4 : custom annotations =====
Class-level @Role value = ADMIN
Scanning methods for @TestCase ...
  testLogin() -> desc='login succeeds with valid credentials', priority=HIGH, timeout=1000, tags=[auth, smoke]
  testCart() -> desc='cart total adds up', priority=MEDIUM, timeout=1000, tags=[]

===== SECTION 5 : reflection mini-framework =====
JSON (driven by @JsonField):
  {"account_id": 1, "owner": "Ada", "balance": 500}
  {"account_id": 2, "owner": "Linus", "balance": -50}
Validation (driven by @Range):
 good account:
   OK     : balance = 500
 bad account:
   INVALID: balance = -50 (allowed 0..1000000)
\`\`\`
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## What they are\r
\r
- Metadata attached to code. An annotation does nothing on its own — something\r
  has to read it: the compiler, a build tool, or your code by reflection.\r
- \`@Override\` is checked by the compiler and turns a mistyped signature from a\r
  silent new overload into an error.\r
- \`@Deprecated\` marks something as still working but not to be used; the compiler\r
  warns at each use. \`@SuppressWarnings\` does the opposite for one specific\r
  category — pass the right one, and keep the scope as small as possible.\r
- \`@FunctionalInterface\` asserts exactly one abstract method, and fails the build\r
  if a second is added.\r
\r
## The meta-annotations\r
\r
- \`@Retention\` — how long it survives: \`SOURCE\` (dropped at compile),\r
  \`CLASS\` (in the file, not loaded), \`RUNTIME\` (readable by reflection).\r
- \`@Target\` — where it may be written: type, method, field, parameter and so on.\r
  Using it elsewhere is a compile error.\r
- \`@Documented\` puts it in the Javadoc; \`@Inherited\` lets a subclass pick it up.\r
- \`@Repeatable\` allows the same annotation more than once on one element, and\r
  requires a container annotation holding an array of them.\r
\r
![Two repeated annotations wrapped into a container annotation](diagrams/26-annotations-revision-repeatable-container.jpg)\r
\r
## Writing your own\r
\r
- \`public @interface Name { }\`. Elements are declared like methods, with an\r
  optional \`default\`.\r
- Element types are limited: primitives, \`String\`, \`Class\`, enums, other\r
  annotations, and arrays of those.\r
- An element with no \`default\` is required, and omitting it is a compile error.\r
\r
![Which annotation elements are required and which have defaults](diagrams/26-annotations-revision-required-elements.jpg)\r
- Reading one at run time needs \`@Retention(RUNTIME)\` and reflection —\r
  \`getAnnotation\`. Without that retention it is simply not there to find.\r
`,interview:"## Interview questions\n\n### Q1: What is an annotation in Java?\n\n**Answer:** It is metadata attached to code using `@`. By itself it changes nothing; it is read by the compiler, by tools/frameworks, or by your own code via reflection, which then act on it.\n\n![The same annotation does nothing until something reads it](diagrams/26-annotations-interview-label-needs-a-reader.jpg)\n\n### Q2: What does `@Override` do, and why is it useful?\n\n**Answer:** It tells the compiler that a method is intended to override a superclass method, and the compiler verifies a matching method exists. This catches typos and signature mismatches at compile time instead of letting them become silent runtime bugs.\n\n### Q3: What is the difference between `@Deprecated` and `@SuppressWarnings`?\n\n**Answer:** `@Deprecated` marks an element as outdated so the compiler *produces* a warning at every use. `@SuppressWarnings` does the opposite — it *hides* named categories of warnings for the element it is placed on.\n\n### Q4: What is a meta-annotation? Name them.\n\n**Answer:** A meta-annotation is an annotation applied to another annotation to configure it. The five built-in ones are `@Retention`, `@Target`, `@Documented`, `@Inherited`, and `@Repeatable`.\n\n![Meta-annotations configure the annotation, which configures the code](diagrams/26-annotations-interview-meta-layer.jpg)\n\n### Q5: Explain the three retention policies.\n\n**Answer:** `SOURCE` is discarded by the compiler (e.g. `@Override`). `CLASS` is stored in the `.class` file but not loaded into the JVM (the default). `RUNTIME` is stored and loaded, so it is readable via reflection — required for frameworks.\n\n### Q6: How do you read an annotation at runtime?\n\n**Answer:** The annotation must use `@Retention(RUNTIME)`. Then you use reflection — e.g. `clazz.getAnnotation(MyAnno.class)`, `method.getAnnotation(...)`, `field.getAnnotation(...)`, or `getAnnotationsByType(...)` for repeatable ones — and read its elements.\n\n### Q7: How do you create a custom annotation, and what types can its elements have?\n\n**Answer:** Declare it with `@interface`. Each element looks like a method with no body; its type must be a primitive, `String`, `Class`, enum, another annotation, or an array of those. `default` makes an element optional, and an element named `value` can be set without naming it.\n\n### Q8: What is `@Repeatable` and what does it require?\n\n**Answer:** It lets the same annotation be applied multiple times on one element. It requires a **container** annotation whose single `value` element is an array of the repeatable type, and you read the repeats with `getAnnotationsByType(...)`.\n"}},{slug:`interface-types`,title:`Types of interface`,subject:`Java`,stage:`Types and contracts`,chapter:27,accent:`#fb7185`,summary:`In Chapter 24 you learned what an interface is. This chapter is about the three flavours an interface can take, named by how many abstract methods they contain:`,parts:{notes:`## Introduction\r
\r
In [Chapter 24](#/study/interfaces) you learned **what** an interface is. This chapter is about the **three flavours** an interface can take, named by **how many abstract methods they contain**:\r
\r
| Abstract methods | Name of the interface |\r
|------------------|-----------------------|\r
| **2 or more** | **Normal** (regular) interface |\r
| **Exactly 1** | **Functional** (SAM) interface |\r
| **Zero** | **Marker** (tagging) interface |\r
\r
That single number — how many abstract methods — is the entire idea behind this chapter. Everything else follows from it.\r
\r
---\r
\r
## Quick Recap: What Is an Interface\r
\r
An interface is a **contract**: a list of methods a class promises to provide. A class signs the contract with \`implements\` and must supply a body for every abstract method.\r
\r
\`\`\`java\r
interface Animal { void sound(); }\r
\r
class Dog implements Animal {\r
    public void sound() { System.out.println("Woof"); }\r
}\r
\`\`\`\r
\r
- Interface methods are \`public abstract\` **by default**.\r
- Interface fields are \`public static final\` **by default** (constants).\r
- You cannot create an interface with \`new\`; you create a class that implements it.\r
\r
---\r
\r
## The Three Types at a Glance\r
\r
![The Three Types at a Glance](diagrams/27-interface-types-notes-b414e6b1.jpg)\r
\r
---\r
\r
## File Map\r
\r
| File | Type | Topic |\r
|------|------|-------|\r
| [\`NormalInterface.java\`](NormalInterface.java) | Normal | many methods + constant, \`default\`, \`static\`, \`private\` members |\r
| [\`FunctionalORSAMInterface.java\`](FunctionalORSAMInterface.java) | Functional | one method, lambdas, method references, built-ins |\r
| [\`MarkerInterface.java\`](MarkerInterface.java) | Marker | empty interface used as a tag with \`instanceof\` |\r
\r
---\r
\r
## Type 1: Normal (Regular) Interface\r
\r
A **normal** interface has **two or more abstract methods**. It is the everyday contract from Chapter 24. Since Java 8 and 9, an interface can hold a lot more than abstract methods, and this file shows **all five** kinds of members in one place.\r
\r
![The five kinds of interface member and who supplies the body](diagrams/27-interface-types-notes-member-kinds.jpg)\r
\r
### The five member kinds\r
\r
| Member | Has a body? | Who provides it | Since |\r
|--------|-------------|-----------------|-------|\r
| **constant** (\`int MAX_VOLUME = 100;\`) | n/a | the interface (a shared \`public static final\` value) | always |\r
| **abstract method** | ❌ no | the **implementing class must** | always |\r
| **\`default\` method** | ✅ yes | the interface (class may override) | Java 8 |\r
| **\`static\` method** | ✅ yes | the interface (called on the interface) | Java 8 |\r
| **\`private\` method** | ✅ yes | the interface (hidden helper) | Java 9 |\r
\r
\`\`\`java\r
interface RemoteControl {\r
\r
    int MAX_VOLUME = 100;\r
\r
    void turnOn();\r
    void turnOff();\r
    void setVolume(int level);\r
\r
    default void mute() {\r
        setVolume(0);\r
        System.out.println("Muted (via default method)");\r
    }\r
\r
    static boolean isValidVolume(int level) {\r
        return level >= 0 && level <= MAX_VOLUME;\r
    }\r
\r
    private void log(String action) {\r
        System.out.println("[RemoteControl] " + action);\r
    }\r
\r
    default void reset() {\r
        log("reset requested");\r
        turnOff();\r
        turnOn();\r
    }\r
}\r
\`\`\`\r
\r
### Why \`default\` methods exist\r
\r
Before Java 8, adding a method to an interface **broke every class** that implemented it (they suddenly had an unimplemented method). \`default\` methods solve this: the interface ships a ready-made body, so old classes keep working without changes — and any class is still free to **override** it. In the code, \`AcRemote\` overrides \`mute()\` to refuse muting:\r
\r
\`\`\`java\r
class AcRemote implements RemoteControl {\r
    @Override\r
    public void mute() {\r
        System.out.println("AC cannot be muted (overrode the default mute())");\r
    }\r
}\r
\`\`\`\r
\r
### Why \`static\` and \`private\` methods exist\r
\r
- **\`static\`** methods are utilities that belong to the interface itself, called as \`RemoteControl.isValidVolume(80)\`. They are **not** inherited by implementing classes.\r
- **\`private\`** methods (Java 9+) let two \`default\` methods share code **without** exposing that helper as part of the public contract.\r
\r
---\r
\r
## Type 2: Functional (SAM) Interface\r
\r
A **functional** interface has **exactly one abstract method**. **SAM = Single Abstract Method.** Because there is only one method to fill in, the compiler lets you replace a whole class with a tiny **lambda**.\r
\r
\`\`\`java\r
@FunctionalInterface\r
interface A {\r
    void show(int i);\r
}\r
\`\`\`\r
\r
> \`@FunctionalInterface\` (from [Chapter 26](#/study/annotations)) is optional, but adding it means the compiler will **error** if anyone later adds a second abstract method — protecting every lambda that depends on this interface.\r
\r
### From verbose class to one-line lambda\r
\r
The file shows the same logic getting shorter and shorter:\r
\r
\`\`\`java\r
A oldWay = new A() {\r
    @Override public void show(int i) { System.out.println("Anonymous class show: " + i); }\r
};\r
\r
A lambda   = (int i) -> System.out.println("Lambda show: " + i);\r
\r
A inferred = (i)     -> System.out.println("Inferred-type show: " + i);\r
\r
A shortest = i       -> System.out.println("Shortest show: " + i);\r
\r
A methodRef = FunctionalORSAMInterface::printLoudly;\r
\`\`\`\r
\r
All five do the **same thing** — they just get progressively shorter. Each step\r
drops one more piece of ceremony: the anonymous class becomes a lambda, the\r
lambda drops its parameter **type** (it is inferred), then its **parentheses**\r
(one parameter needs none), and finally the body itself becomes a **method\r
reference** with \`::\`.\r
\r
![Each step from anonymous class to method reference drops one piece of ceremony](diagrams/27-interface-types-notes-lambda-ladder.jpg)\r
\r
### Lambdas can return values too\r
\r
\`\`\`java\r
@FunctionalInterface\r
interface Calculator { int operate(int a, int b); }\r
\r
Calculator add = (a, b) -> a + b;\r
Calculator max = (a, b) -> a > b ? a : b;\r
add.operate(7, 8);\r
\`\`\`\r
\r
The lambda body *is* the return value here — no \`return\` keyword needed for a\r
single expression. \`add.operate(7, 8)\` gives \`15\`.\r
\r
### You rarely need to write your own\r
\r
The \`java.util.function\` package already provides ready-made functional interfaces:\r
\r
| Interface | Shape | Meaning |\r
|-----------|-------|---------|\r
| \`Runnable\` | \`() -> void\` | do something, return nothing |\r
| \`Supplier<T>\` | \`() -> T\` | supply a value |\r
| \`Consumer<T>\` | \`(T) -> void\` | consume a value |\r
| \`Function<T,R>\` | \`(T) -> R\` | transform T into R |\r
| \`Predicate<T>\` | \`(T) -> boolean\` | test a condition |\r
| \`BiFunction<T,U,R>\` | \`(T,U) -> R\` | combine two inputs into one output |\r
\r
\`\`\`java\r
Predicate<Integer> isEven = n -> n % 2 == 0;\r
isEven.test(10);\r
\`\`\`\r
\r
---\r
\r
## Type 3: Marker (Tagging) Interface\r
\r
A **marker** interface is **completely empty** — no methods, no constants. It does not add behaviour; it adds a **type tag**. Code can then ask *"is this object an instance of the marker?"* with \`instanceof\` and decide what to do.\r
\r
\`\`\`java\r
interface Deletable { }\r
interface Downloadable { }\r
\r
class Document implements Deletable, Downloadable { ... }\r
class Movie    implements Downloadable { ... }\r
\`\`\`\r
\r
### How the tag is used\r
\r
A service checks the tag before acting. Forget the tag, and the object simply cannot be deleted:\r
\r
\`\`\`java\r
static void delete(Object item) {\r
    if (item instanceof Deletable) {\r
        System.out.println("Deleting: " + item.getClass().getSimpleName());\r
    } else {\r
        System.out.println("REFUSED to delete " + item.getClass().getSimpleName()\r
                + " - it is not marked Deletable");\r
    }\r
}\r
\`\`\`\r
\r
### Why an *empty* interface is useful\r
\r
- It attaches metadata at the **type level**, checkable by both the compiler and the runtime, with no fields or methods.\r
- The JDK uses several markers you already rely on:\r
  - \`java.io.Serializable\` → "this object may be turned into bytes"\r
  - \`java.lang.Cloneable\` → "\`Object.clone()\` is allowed here"\r
  - \`java.util.RandomAccess\` → "indexed \`get(i)\` is fast on this list"\r
\r
\`\`\`java\r
"hello" instanceof java.io.Serializable\r
movie   instanceof java.io.Serializable\r
\`\`\`\r
\r
The first is \`true\` — \`String\` implements \`Serializable\`. The second is \`false\`,\r
because our \`Movie\` never claimed the tag.\r
\r
![Which markers Document and Movie claim, and what delete refuses](diagrams/27-interface-types-notes-marker-tag.jpg)\r
\r
> **Marker vs annotation:** Today, annotations (Chapter 26) often replace markers. But a marker is a real **type**, so it gives **compile-time** checks an annotation can't — for example, a method can demand a \`Deletable\` parameter and the compiler will reject anything untagged.\r
\r
---\r
\r
## How to Compile and Run\r
\r
From the repository root (\`d:\\Repos\\java\`):\r
\r
\`\`\`bash\r
javac NormalInterface.java\r
javac FunctionalORSAMInterface.java\r
javac MarkerInterface.java\r
\r
java NormalInterface\r
java FunctionalORSAMInterface\r
java MarkerInterface\r
\`\`\`\r
\r
### Expected Output — Normal interface (the chapter folder)\r
\r
\`\`\`text\r
TV: ON\r
TV volume set to 45\r
Rejected volume 250 (must be 0..100)\r
TV volume set to 0\r
Muted (via default method)\r
[RemoteControl] reset requested\r
TV: OFF\r
TV: ON\r
TV: OFF\r
\r
AC: ON\r
AC cannot be muted (overrode the default mute())\r
AC: OFF\r
\r
Is 80 a valid volume?  true\r
Is 120 a valid volume? false\r
MAX_VOLUME = 100\r
\`\`\`\r
\r
### Expected Output — Functional interface (the chapter folder)\r
\r
\`\`\`text\r
Anonymous class show: 1\r
Lambda show: 2\r
Inferred-type show: 3\r
Shortest show: 4\r
>>> 5 <<<\r
\r
[Calculator] I combine two ints into one\r
add(7, 8) = 15\r
max(7, 8) = 8\r
\r
isEven(10) = true\r
isEven(7)  = false\r
\`\`\`\r
\r
### Expected Output — Marker interface (the chapter folder)\r
\r
\`\`\`text\r
=== delete() respects the Deletable marker ===\r
Deleting: Document\r
REFUSED to delete Movie - it is not marked Deletable\r
\r
=== download() respects the Downloadable marker ===\r
Downloading: Document\r
Downloading: Movie\r
\r
=== a built-in marker: java.io.Serializable ===\r
Is "hello" Serializable? true\r
Is a Movie Serializable?  false\r
\`\`\`\r
\r
---\r
\r
## Side-by-Side Comparison\r
\r
| Aspect | Normal | Functional (SAM) | Marker |\r
|--------|--------|------------------|--------|\r
| Abstract methods | 2 or more | exactly 1 | 0 |\r
| Usable as a lambda? | No | **Yes** | No |\r
| Typical use | a full capability contract | a single action passed as a value | a type tag checked with \`instanceof\` |\r
| Special annotation | — | \`@FunctionalInterface\` | — |\r
| JDK example | \`List\`, \`Map\` | \`Runnable\`, \`Comparator\` | \`Serializable\`, \`Cloneable\` |\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Putting two abstract methods in a \`@FunctionalInterface\`\r
\r
\`\`\`java\r
@FunctionalInterface\r
interface A {\r
    void show(int i);\r
    void hide();        // ERROR: a functional interface allows only ONE\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
error: Unexpected @FunctionalInterface annotation\r
    A is not a functional interface: multiple non-overriding abstract methods\r
\r
The annotation is optional, and this is exactly what it buys you: without\r
it the interface compiles and quietly stops working with lambdas.\r
\`\`\`\r
\r
### Mistake 2: Thinking \`default\`/\`static\` methods break "functional"\r
\r
They don't. Only **abstract** methods count toward the "exactly one" rule. A functional interface may have many \`default\`/\`static\`/\`private\` methods.\r
\r
### Mistake 3: Forgetting to implement every abstract method of a normal interface\r
\r
\`\`\`java\r
class TvRemote implements RemoteControl {\r
    public void turnOn() { ... }\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
error: TvRemote is not abstract and does not override abstract method ...\r
\r
Every abstract method, not just the interesting one. A default method would\r
not need overriding — that is what default methods are for.\r
\`\`\`\r
\r
### Mistake 4: Reducing visibility when implementing\r
\r
\`\`\`java\r
class TvRemote implements RemoteControl {\r
    void turnOn() { ... }    // ERROR: must be public (interface methods are public)\r
}\r
\`\`\`\r
\r
\`\`\`expected\r
error: attempting to assign weaker access privileges; was public\r
\r
Interface methods are implicitly public, and an override may never be less\r
visible than what it overrides.\r
\`\`\`\r
\r
### Mistake 5: Putting methods inside a marker interface\r
\r
A marker interface must stay **empty**. The moment you add a method, it stops being a marker and becomes a normal/functional interface.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`NormalInterface.java\`\r
\r
A **normal** interface is one with two or more abstract methods, and this file\r
puts all five member kinds into one of them. \`RemoteControl\` declares the\r
constant \`MAX_VOLUME\`, three abstract methods that every implementer must\r
provide, a \`default mute()\` that ships a body so adding it broke nobody, a\r
\`static isValidVolume()\` that belongs to the interface itself and is never\r
inherited, and a \`private log()\` that exists only so the two default methods can\r
share code without exposing it in the contract.\r
\r
\`TvRemote\` implements the three abstract methods and takes the rest as they\r
come. \`AcRemote\` implements them too but **overrides \`mute()\`**, because an air\r
conditioner has nothing to mute — proof that a default is a default, not a fixed\r
behaviour.\r
\r
\`\`\`java NormalInterface.java @run-interface-types-normal-interface\r
\r
interface RemoteControl {\r
\r
    int MAX_VOLUME = 100;\r
\r
    void turnOn();\r
    void turnOff();\r
    void setVolume(int level);\r
\r
    default void mute() {\r
        setVolume(0);\r
        System.out.println("Muted (via default method)");\r
    }\r
\r
    static boolean isValidVolume(int level) {\r
        return level >= 0 && level <= MAX_VOLUME;\r
    }\r
\r
    private void log(String action) {\r
        System.out.println("[RemoteControl] " + action);\r
    }\r
\r
    default void reset() {\r
        log("reset requested");\r
        turnOff();\r
        turnOn();\r
    }\r
}\r
\r
class TvRemote implements RemoteControl {\r
\r
    private int volume = 10;\r
\r
    @Override\r
    public void turnOn() {\r
        System.out.println("TV: ON");\r
    }\r
\r
    @Override\r
    public void turnOff() {\r
        System.out.println("TV: OFF");\r
    }\r
\r
    @Override\r
    public void setVolume(int level) {\r
        if (RemoteControl.isValidVolume(level)) {\r
            this.volume = level;\r
            System.out.println("TV volume set to " + volume);\r
        } else {\r
            System.out.println("Rejected volume " + level + " (must be 0.." + MAX_VOLUME + ")");\r
        }\r
    }\r
}\r
\r
class AcRemote implements RemoteControl {\r
\r
    @Override\r
    public void turnOn() {\r
        System.out.println("AC: ON");\r
    }\r
\r
    @Override\r
    public void turnOff() {\r
        System.out.println("AC: OFF");\r
    }\r
\r
    @Override\r
    public void setVolume(int level) {\r
        System.out.println("AC has no volume control; ignoring " + level);\r
    }\r
\r
    @Override\r
    public void mute() {\r
        System.out.println("AC cannot be muted (overrode the default mute())");\r
    }\r
}\r
\r
public class NormalInterface {\r
    public static void main(String[] args) {\r
\r
        RemoteControl tv = new TvRemote();\r
        tv.turnOn();\r
        tv.setVolume(45);\r
        tv.setVolume(250);\r
        tv.mute();\r
        tv.reset();\r
        tv.turnOff();\r
\r
        System.out.println();\r
\r
        RemoteControl ac = new AcRemote();\r
        ac.turnOn();\r
        ac.mute();\r
        ac.turnOff();\r
\r
        System.out.println();\r
\r
        System.out.println("Is 80 a valid volume?  " + RemoteControl.isValidVolume(80));\r
        System.out.println("Is 120 a valid volume? " + RemoteControl.isValidVolume(120));\r
\r
        System.out.println("MAX_VOLUME = " + RemoteControl.MAX_VOLUME);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-interface-types-normal-interface\r
TV: ON\r
TV volume set to 45\r
Rejected volume 250 (must be 0..100)\r
TV volume set to 0\r
Muted (via default method)\r
[RemoteControl] reset requested\r
TV: OFF\r
TV: ON\r
TV: OFF\r
\r
AC: ON\r
AC cannot be muted (overrode the default mute())\r
AC: OFF\r
\r
Is 80 a valid volume?  true\r
Is 120 a valid volume? false\r
MAX_VOLUME = 100\r
\`\`\`\r
\r
### \`FunctionalORSAMInterface.java\`\r
\r
Two functional interfaces and five ways to fill the first one in. \`A.show()\` is\r
implemented as an anonymous class, then as a lambda with an explicit type, then\r
with the type inferred, then without parentheses, and finally as a method\r
reference — and the output shows all five doing the same job.\r
\r
\`Calculator\` proves two further points: a \`default\` method does **not** break\r
the single-abstract-method rule, and a lambda whose body is one expression\r
returns that expression without a \`return\`. The \`Predicate<Integer>\` at the end\r
is the reminder that \`java.util.function\` already ships most of the interfaces\r
you would otherwise write.\r
\r
\`\`\`java FunctionalORSAMInterface.java @run-interface-types-functional-orsaminterface\r
\r
@FunctionalInterface\r
interface A {\r
    void show(int i);\r
}\r
\r
@FunctionalInterface\r
interface Calculator {\r
    int operate(int a, int b);\r
\r
    default void describe() {\r
        System.out.println("[Calculator] I combine two ints into one");\r
    }\r
}\r
\r
public class FunctionalORSAMInterface {\r
\r
    static void printLoudly(int i) {\r
        System.out.println(">>> " + i + " <<<");\r
    }\r
\r
    public static void main(String[] args) {\r
\r
        A oldWay = new A() {\r
            @Override\r
            public void show(int i) {\r
                System.out.println("Anonymous class show: " + i);\r
            }\r
        };\r
        oldWay.show(1);\r
\r
        A lambda = (int i) -> System.out.println("Lambda show: " + i);\r
        lambda.show(2);\r
\r
        A inferred = (i) -> System.out.println("Inferred-type show: " + i);\r
        inferred.show(3);\r
\r
        A shortest = i -> System.out.println("Shortest show: " + i);\r
        shortest.show(4);\r
\r
        A methodRef = FunctionalORSAMInterface::printLoudly;\r
        methodRef.show(5);\r
\r
        System.out.println();\r
\r
        Calculator add = (a, b) -> a + b;\r
        Calculator max = (a, b) -> a > b ? a : b;\r
        add.describe();\r
        System.out.println("add(7, 8) = " + add.operate(7, 8));\r
        System.out.println("max(7, 8) = " + max.operate(7, 8));\r
\r
        System.out.println();\r
\r
        java.util.function.Predicate<Integer> isEven = n -> n % 2 == 0;\r
        System.out.println("isEven(10) = " + isEven.test(10));\r
        System.out.println("isEven(7)  = " + isEven.test(7));\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-interface-types-functional-orsaminterface\r
Anonymous class show: 1\r
Lambda show: 2\r
Inferred-type show: 3\r
Shortest show: 4\r
>>> 5 <<<\r
\r
[Calculator] I combine two ints into one\r
add(7, 8) = 15\r
max(7, 8) = 8\r
\r
isEven(10) = true\r
isEven(7)  = false\r
\`\`\`\r
\r
### \`MarkerInterface.java\`\r
\r
\`Deletable\` and \`Downloadable\` are empty on purpose — that emptiness *is* the\r
interface. They carry no methods and no fields; the only thing they do is let\r
\`instanceof\` answer a question about a type.\r
\r
\`Document\` claims both tags, \`Movie\` claims only one, and \`delete()\` refuses\r
anything that is not \`Deletable\`. Nothing enforces this but the check itself,\r
which is exactly how \`Serializable\` and \`Cloneable\` work in the JDK.\r
\r
\`\`\`java MarkerInterface.java @run-interface-types-marker-interface\r
\r
interface Deletable {\r
}\r
\r
interface Downloadable {\r
}\r
\r
class Document implements Deletable, Downloadable {\r
    String name;\r
\r
    Document(String name) {\r
        this.name = name;\r
    }\r
}\r
\r
class Movie implements Downloadable {\r
    String title;\r
\r
    Movie(String title) {\r
        this.title = title;\r
    }\r
}\r
\r
public class MarkerInterface {\r
\r
    static void delete(Object item) {\r
        if (item instanceof Deletable) {\r
            System.out.println("Deleting: " + item.getClass().getSimpleName());\r
        } else {\r
            System.out.println("REFUSED to delete " + item.getClass().getSimpleName()\r
                    + " - it is not marked Deletable");\r
        }\r
    }\r
\r
    static void download(Object item) {\r
        if (item instanceof Downloadable) {\r
            System.out.println("Downloading: " + item.getClass().getSimpleName());\r
        } else {\r
            System.out.println("Cannot download " + item.getClass().getSimpleName());\r
        }\r
    }\r
\r
    public static void main(String[] args) {\r
        Document doc = new Document("resume.pdf");\r
        Movie movie = new Movie("Inception");\r
\r
        System.out.println("=== delete() respects the Deletable marker ===");\r
        delete(doc);\r
        delete(movie);\r
\r
        System.out.println("\\n=== download() respects the Downloadable marker ===");\r
        download(doc);\r
        download(movie);\r
\r
        System.out.println("\\n=== a built-in marker: java.io.Serializable ===");\r
        System.out.println("Is \\"hello\\" Serializable? " + ("hello" instanceof java.io.Serializable));\r
        System.out.println("Is a Movie Serializable?  " + (movie instanceof java.io.Serializable));\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-interface-types-marker-interface\r
=== delete() respects the Deletable marker ===\r
Deleting: Document\r
REFUSED to delete Movie - it is not marked Deletable\r
\r
=== download() respects the Downloadable marker ===\r
Downloading: Document\r
Downloading: Movie\r
\r
=== a built-in marker: java.io.Serializable ===\r
Is "hello" Serializable? true\r
Is a Movie Serializable?  false\r
\`\`\`\r
`,revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## Three kinds\r
\r
- **Normal** — two or more abstract methods. An implementing class overrides all\r
  of them.\r
- **Functional** — exactly one abstract method, so a lambda or a method reference\r
  can stand in for an implementation.\r
- **Marker** — no methods at all. It carries meaning by its presence:\r
  \`Serializable\`, \`Cloneable\`, \`RandomAccess\`.\r
\r
## Functional interfaces\r
\r
![Only abstract methods count toward the single-abstract-method rule](diagrams/27-interface-types-revision-what-counts.jpg)\r
\r
- One abstract method is the whole rule. \`default\` and \`static\` methods do not\r
  count against it, however many there are.\r
- Methods that override \`Object\`'s public methods — \`equals\`, \`hashCode\`,\r
  \`toString\` — do not count either.\r
- \`@FunctionalInterface\` is optional. It is worth adding because it makes the\r
  rule enforced rather than assumed.\r
- \`Runnable\`, \`Comparator\`, \`Function\`, \`Predicate\` and \`Supplier\` are the ones\r
  you meet first.\r
\r
## Marker interfaces, and the rules that apply to all three\r
\r
- A marker has no methods by definition. Adding one stops it being a marker and\r
  breaks every class that implemented it for the label alone.\r
- An annotation carries data and can be read reflectively; a marker interface is\r
  a **type**, so it can be checked with \`instanceof\` and used in a signature.\r
  Choose the interface when you want the compiler involved.\r
\r
![A marker interface is a type, an annotation is data](diagrams/27-interface-types-revision-marker-vs-annotation.jpg)\r
- A class may implement any number of interfaces of any of the three kinds.\r
- Implementing methods are \`public\` because interface methods are, and an\r
  override may never reduce visibility.\r
`,interview:`## Interview questions

### Q1: What are the three types of interfaces in Java?

**Answer:** Normal (two or more abstract methods), Functional/SAM (exactly one abstract method), and Marker (zero methods). They are distinguished by how many abstract methods they declare.

### Q2: What is a functional interface and why does it matter?

**Answer:** An interface with exactly one abstract method (a SAM). It matters because it can be implemented with a lambda expression or method reference, making code much shorter. \`@FunctionalInterface\` makes the compiler enforce the single-method rule.

### Q3: Do \`default\` and \`static\` methods stop an interface from being functional?

**Answer:** No. Only abstract methods are counted. A functional interface can have any number of \`default\`, \`static\`, or \`private\` methods and still qualify.

### Q4: Why were \`default\` methods added in Java 8?

**Answer:** To let new methods be added to existing interfaces without breaking the classes that already implement them. The interface supplies a body, so old implementers keep compiling, and they may override it if needed.

![Adding an abstract method breaks implementers; a default method does not](diagrams/27-interface-types-interview-why-default.jpg)

### Q5: What is a marker interface? Give examples.

**Answer:** An empty interface used purely as a type tag, checked with \`instanceof\`. Examples include \`java.io.Serializable\`, \`java.lang.Cloneable\`, and \`java.util.RandomAccess\`.

### Q6: Marker interface vs annotation — when would you choose each?

**Answer:** Both attach metadata. A marker interface is a real type, so it enables compile-time checks (a method can require the marker type as a parameter). An annotation is more flexible and can carry data (elements) and target many places, but is usually checked at runtime via reflection.

### Q7: Can a class implement more than one interface?

**Answer:** Yes. A class can implement many interfaces (e.g. \`class Document implements Deletable, Downloadable\`), which is how Java provides multiple inheritance of type. It can still only \`extend\` one class.

![A class extends one class but implements any number of interfaces](diagrams/27-interface-types-interview-one-class-many-interfaces.jpg)
`}},{slug:`exceptions`,title:`Exceptions`,subject:`Java`,stage:`Errors and input`,chapter:28,accent:`#c084fc`,summary:`An exception is an event that disrupts the normal flow of a program while it is running. Java represents every such event as an object — and gives you a structured way to catch it and recover, instead of letting the whole program crash.`,parts:{notes:`## Introduction\r
\r
An **exception** is an event that disrupts the normal flow of a program **while it is running**. Java represents every such event as an **object** — and gives you a structured way to **catch** it and recover, instead of letting the whole program crash.\r
\r
But before we talk about handling, we need to be precise about **what can go wrong** in a Java program. Not every "error" is an exception, and confusing the categories is the single most common beginner mistake. This chapter sorts them out with three tiny, runnable files.\r
\r
> The big idea: there are **three kinds of failure** — *compile-time*, *logical*, and *runtime* — and **only the third kind** (runtime) involves exceptions and \`try\`/\`catch\`.\r
\r
---\r
\r
## The Three Kinds of Errors\r
\r
| Kind | When it happens | Does it compile? | Does it run? | Who detects it |\r
|------|-----------------|------------------|--------------|----------------|\r
| **Compile-time** | Before the program starts | ❌ **No** \`.class\` produced | never starts | the **compiler** (\`javac\`) |\r
| **Logical** | While running | ✅ yes | ✅ yes, to the end | **a human** (Java can't) |\r
| **Runtime** | While running | ✅ yes | 💥 crashes partway | the **JVM**, by throwing an exception |\r
\r
![The Three Kinds of Errors](diagrams/28-exceptions-notes-e33ac54f.jpg)\r
\r
---\r
\r
## File Map\r
\r
| File | Demonstrates | Key idea |\r
|------|--------------|----------|\r
| [\`CompileTimeError.java\`](CompileTimeError.java) | Compile-time errors (and the \`1/0\` myth) | the compiler refuses to build; \`1/0\` is **not** one of these |\r
| [\`LogicalError.java\`](LogicalError.java) | A logical error | compiles and runs, but the answer is wrong |\r
| [\`RuntimeError.java\`](RuntimeError.java) | A runtime error + \`try\`/\`catch\` | catch the exception so the program keeps running |\r
\r
---\r
\r
## File 1: Compile-Time Error (and a Famous Misconception)\r
\r
A **compile-time error** is one the compiler (\`javac\`) catches **before the program ever runs**. No \`.class\` file is produced, so there is nothing to launch. These are almost always **syntax** or **type** mistakes:\r
\r
\`\`\`java\r
int x = "hello";\r
int y = 5\r
System.out.println(z);\r
notAMethod();\r
\`\`\`\r
\r
Four lines, four different refusals: a **type mismatch** (a \`String\` cannot\r
become an \`int\`), a **missing \`;\`**, an **undeclared variable** \`z\`, and a call\r
to a **method that does not exist**. Put any one of them in a file and \`javac\`\r
stops with an error pointing at the exact line — which is why none of them are\r
in the runnable file below.\r
\r
### The famous misconception: \`1 / 0\`\r
\r
It is tempting to think this line is a compile-time error:\r
\r
\`\`\`java\r
int result = 1 / 0;\r
\`\`\`\r
\r
**It is not.** This file proves it:\r
\r
1. \`javac CompileTimeError.java\` — **compiles successfully**, a \`.class\` file appears.\r
2. \`java CompileTimeError\` — **crashes at runtime**:\r
\r
\`\`\`text\r
Exception in thread "main" java.lang.ArithmeticException: / by zero\r
\`\`\`\r
\r
So integer **divide-by-zero is a *runtime* error** (an \`ArithmeticException\`) — exactly the family we learn to handle in File 3 — **not** a compile-time error. The file keeps this line on purpose so you can compile it (no error) and then run it (it throws) and see the difference with your own eyes.\r
\r
> 💡 **Why does this trip people up?** Some IDEs underline \`1/0\` with a *warning*, which looks like a compile error. But the **language** only forbids it at runtime, because in general a divisor's value isn't known until the program runs.\r
\r
---\r
\r
## File 2: Logical Error\r
\r
A **logical error** is the sneakiest bug of all, because **nothing complains**:\r
\r
- The code is legal, so it **compiles**.\r
- It **runs** all the way through with no crash.\r
- But the **result is wrong**, because your logic didn't match your intent.\r
\r
\`\`\`java\r
int result = 1 + 0;\r
System.out.println(result);\r
\`\`\`\r
\r
The programmer meant \`10\`; the expression legally evaluates to \`1\`, and \`1\` is\r
what prints. There is no error message anywhere — Java cannot read your mind. The only way to catch a logical error is to **test the program and notice the output is wrong**. This is exactly why we write unit tests.\r
\r
> The compiler checks **grammar and types**, never **correctness of intent**. \`1 + 0\` is grammatically perfect; it's just not what you wanted.\r
\r
---\r
\r
## File 3: Runtime Error and \`try\`/\`catch\`\r
\r
A **runtime error** happens **while the program is running**: it compiled, it started, and then an illegal operation threw an **exception**. If nobody handles it, the JVM prints a stack trace and stops at that line.\r
\r
**Exception handling** is how we deal with this gracefully. We wrap risky code in a \`try\` block; if it throws, control jumps to a matching \`catch\` block instead of crashing:\r
\r
\`\`\`java\r
int i = 9, j = 0, result = 0;\r
\r
try {\r
    result = i / j;\r
} catch (Exception e) {\r
    System.out.println("Exception: " + e.getMessage());\r
}\r
\r
System.out.println("Result is : " + result);\r
System.out.println("Worked normally");\r
\`\`\`\r
\r
\`i / j\` is \`9 / 0\`, so it throws right there and control jumps into the \`catch\`\r
instead of crashing. Both \`println\`s after the block still run.\r
\r
### The key insight: a *caught* exception does NOT stop the program\r
\r
![Which lines run with a try/catch and which never run without one](diagrams/28-exceptions-notes-caught-vs-uncaught.jpg)\r
\r
Running the file prints:\r
\r
\`\`\`text\r
Exception: / by zero\r
Result is : 0\r
Worked normally\r
\`\`\`\r
\r
Notice that **\`Worked normally\` prints**. Because the exception was **caught**, the program did **not** crash — it simply continued after the \`try\`/\`catch\`. Contrast this with the situation if there were **no** \`try\`/\`catch\`: the throw would crash the program at \`result = i / j\`, and **neither** of the final two lines would ever run.\r
\r
| | With \`try\`/\`catch\` | Without \`try\`/\`catch\` |\r
|---|---|---|\r
| At \`i / j\` | exception caught | exception thrown, **uncaught** |\r
| Lines after | **run normally** | **never run** (program dies) |\r
| Exit | normal | stack trace, exit code 1 |\r
\r
### \`e.getMessage()\` vs printing the whole exception\r
\r
\`\`\`java\r
System.out.println("Exception: " + e);\r
System.out.println("Exception: " + e.getMessage());\r
\`\`\`\r
\r
The first prints the **type and the message** — \`ArithmeticException: / by zero\`.\r
The second prints the **message only** — \`/ by zero\`.\r
\r
### Ordering multiple \`catch\` blocks (most specific first)\r
\r
![Specific catch blocks first, the general one last](diagrams/28-exceptions-notes-catch-order.jpg)\r
\r
When you catch several types, you must list them **most specific first, most general last**:\r
\r
\`\`\`java\r
try {\r
    ...\r
} catch (ArithmeticException e) {\r
    ...\r
} catch (ArrayIndexOutOfBoundsException e) {\r
    ...\r
} catch (Exception e) {\r
    ...\r
}\r
\`\`\`\r
\r
Two specific types, then the general one last.\r
\r
\`Exception\` is the parent of all the others, so a single \`catch (Exception e)\` can catch everything. Because it is the most general, placing it **before** a more specific catch makes that specific block **unreachable** — which is a **compile error**.\r
\r
---\r
\r
## The Exception Hierarchy\r
\r
Everything you can throw or catch descends from **\`Throwable\`**:\r
\r
![The Exception Hierarchy](diagrams/28-exceptions-notes-bce116fa.jpg)\r
\r
| Branch | What it means | Should you catch it? |\r
|--------|---------------|----------------------|\r
| **\`Error\`** | Something went badly wrong inside the JVM (out of memory, stack overflow). | **No** — usually unrecoverable; let it crash. |\r
| **\`RuntimeException\`** | Usually a **bug in your code** (bad index, null, divide by zero). | Fix the bug; catch only when sensible. |\r
| **Other \`Exception\`s** | External/expected failures (file missing, DB down). | **Yes** — must be handled or declared. |\r
\r
---\r
\r
## Checked vs Unchecked Exceptions\r
\r
This single distinction explains *why the compiler forces \`try\`/\`catch\` in some cases but not others*:\r
\r
| | **Unchecked** | **Checked** |\r
|---|---|---|\r
| Base type | \`RuntimeException\` (and \`Error\`) | \`Exception\` (but **not** \`RuntimeException\`) |\r
| Compiler forces handling? | ❌ No | ✅ **Yes** — \`try/catch\` **or** \`throws\` |\r
| Typical cause | a **bug** in your logic | an **external** condition you can't control |\r
| Examples | \`ArithmeticException\`, \`NullPointerException\`, \`ArrayIndexOutOfBoundsException\` | \`IOException\`, \`FileNotFoundException\`, \`SQLException\` |\r
\r
> \`ArithmeticException\` from \`1/0\` is **unchecked**, which is why File 1 compiled without any \`try\`/\`catch\` — the compiler never demanded it. You only found out at runtime.\r
\r
![A checked exception is stopped at compile time, an unchecked one at runtime](diagrams/28-exceptions-notes-checked-gate.jpg)\r
\r
---\r
\r
## How to Compile and Run\r
\r
From the repository root (\`d:\\Repos\\java\`):\r
\r
\`\`\`bash\r
javac CompileTimeError.java\r
javac LogicalError.java\r
javac RuntimeError.java\r
\r
java CompileTimeError\r
java LogicalError\r
java RuntimeError\r
\`\`\`\r
\r
### Expected Output — File 1 (the chapter folder)\r
\r
It **compiles** with no error, but **running** it throws:\r
\r
\`\`\`text\r
Exception in thread "main" java.lang.ArithmeticException: / by zero\r
	at CompileTimeError.main(CompileTimeError.java:6)\r
\`\`\`\r
\r
### Expected Output — File 2 (the chapter folder)\r
\r
\`\`\`text\r
1\r
\`\`\`\r
\r
(No error — that's the whole point of a *logical* error. You only know \`1\` is "wrong" if you expected \`10\`.)\r
\r
### Expected Output — File 3 (the chapter folder)\r
\r
\`\`\`text\r
Exception: / by zero\r
Result is : 0\r
Worked normally\r
\`\`\`\r
\r
---\r
\r
## Side-by-Side Comparison\r
\r
| Aspect | Compile-time | Logical | Runtime |\r
|--------|--------------|---------|---------|\r
| Detected by | \`javac\` | a human / a test | the JVM (throws an exception) |\r
| Program builds? | No | Yes | Yes |\r
| Program runs? | Never starts | Runs fully | Crashes (unless caught) |\r
| Example in this chapter | a missing \`;\` (commented) | \`1 + 0\` | \`1 / 0\`, \`i / j\` |\r
| How you fix it | follow the compiler message | check your logic | \`try\`/\`catch\`, or fix the cause |\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Thinking \`1 / 0\` is a compile-time error\r
\r
It compiles fine and throws \`ArithmeticException\` **at runtime**. Divide-by-zero is an unchecked runtime exception, not a syntax error.\r
\r
### Mistake 2: Expecting Java to catch logical errors\r
\r
\`\`\`java\r
int total = price - tax;\r
\`\`\`\r
\r
\`\`\`expected\r
Compiles. Runs. Prints a number.\r
\r
It is simply the wrong number if you meant price + tax. Java checks types\r
and not intent — this is the class of error only a test will find.\r
\`\`\`\r
\r
That was meant to be \`price + tax\`. It compiles, it runs, and it is silently\r
wrong. Only **testing** reveals this — the compiler and JVM see nothing wrong.\r
\r
### Mistake 3: Ordering \`catch\` blocks general-first\r
\r
\`\`\`java\r
} catch (Exception e) { ... }\r
} catch (ArithmeticException e) { ... }  // ERROR: unreachable code\r
\`\`\`\r
\r
\`\`\`expected\r
error: exception ArithmeticException has already been caught\r
\r
catch blocks are tried in order, so a general one first makes every block\r
after it unreachable. Specific first, general last.\r
\`\`\`\r
\r
Put the **specific** types first, \`Exception\` **last**.\r
\r
### Mistake 4: Assuming a caught exception stops the program\r
\r
A caught exception lets the program **continue** after the \`try\`/\`catch\`. Only an **uncaught** exception terminates it.\r
\r
### Mistake 5: Catching \`Error\` to "be safe"\r
\r
\`\`\`java\r
catch (Throwable t) { }\r
\`\`\`\r
\r
\`\`\`expected\r
This also catches OutOfMemoryError and StackOverflowError.\r
\r
There is nothing useful to do about either, and swallowing them turns a\r
clear crash into a program that carries on in an unknown state. Catch the\r
exceptions you can actually handle.\r
\`\`\`\r
\r
\`Error\`s signal an unrecoverable JVM state. Catch \`Exception\`, not \`Throwable\`/\`Error\`.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
> Two of these end in a stack trace on purpose — that is the thing the chapter\r
> is demonstrating. The recorded output is what the program really printed,\r
> exit code and all.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`CompileTimeError.java\`\r
\r
Despite the name, this file has no compile-time error in it at all — that is the\r
joke, and the lesson. It compiles cleanly and produces a \`.class\` file; the\r
failure only arrives when you run it. The \`println\` never executes, because the\r
program has already stopped on the line above it.\r
\r
Real compile-time errors would look like \`int x = "hello";\` (type mismatch),\r
\`int y = 5\` (missing semicolon), or a call to a method that does not exist —\r
none of which are in the file, because a file containing one could not be built\r
at all.\r
\r
\`\`\`java CompileTimeError.java @run-exceptions-compile-time-error\r
// no package\r
\r
public class CompileTimeError {\r
    public static void main(String[] args) {\r
\r
        int result = 1 / 0;\r
        System.out.println(result);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-exceptions-compile-time-error\r
Exception in thread "main" java.lang.ArithmeticException: / by zero\r
	at CompileTimeError.main(CompileTimeError.java:6)\r
\`\`\`\r
\r
### \`LogicalError.java\`\r
\r
The sneakiest of the three. This file compiles, runs to the end, crashes\r
nowhere, and is still wrong. The programmer meant to compute \`10\` and wrote\r
\`1 + 0\`, which legally evaluates to \`1\`.\r
\r
Nothing warns you, because there is nothing to warn about — the compiler checks\r
grammar and types, not whether your arithmetic is what you intended. Only a\r
human reading the output, or a failing test, catches this one.\r
\r
\`\`\`java LogicalError.java @run-exceptions-logical-error\r
// no package\r
\r
public class LogicalError {\r
    public static void main(String[] args) {\r
\r
        int result = 1 + 0;\r
        System.out.println(result);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-exceptions-logical-error\r
1\r
\`\`\`\r
\r
### \`RuntimeError.java\`\r
\r
The third file throws the same \`ArithmeticException\` as the first one, but wraps\r
it. That single difference is the whole chapter:\r
\r
- **\`result = i / j\`** is \`9 / 0\`, so it throws. The assignment never completes,\r
  which is why \`result\` is still \`0\` at the end rather than anything sensible.\r
- **\`catch (Exception e)\`** is the parent of every exception type, so it catches\r
  it. \`ArithmeticException\` would work here too and would be the better choice\r
  in real code — catch the narrowest type that covers what can actually go\r
  wrong.\r
- With several \`catch\` blocks you order them **most specific first**. \`Exception\`\r
  must come last: a more specific block placed after it is unreachable and will\r
  not compile.\r
- **Both \`println\`s at the bottom run.** The exception was caught, so it stopped\r
  the \`try\` block, not the program. Remove the \`try\`/\`catch\` and neither line\r
  would ever print — that contrast is the point of the file.\r
\r
For where \`ArithmeticException\` sits among the rest, see\r
[the hierarchy above](#the-exception-hierarchy).\r
\r
\`\`\`java RuntimeError.java @run-exceptions-runtime-error\r
// no package\r
\r
public class RuntimeError {\r
    public static void main(String[] args) {\r
\r
        int i = 9;\r
        int j = 0;\r
        int result = 0;\r
\r
        try {\r
            result = i / j;\r
        } catch (Exception e) {\r
            System.out.println("Exception: " + e.getMessage());\r
        }\r
\r
        System.out.println("Result is : " + result);\r
        System.out.println("Worked normally");\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-exceptions-runtime-error\r
Exception: / by zero\r
Result is : 0\r
Worked normally\r
\`\`\`\r
`,revision:"The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## Three kinds of wrong\r\n\r\n- **Compile-time** — the code does not compile. Syntax, types, a missing return.\r\n- **Runtime** — it compiles and then fails while running. This is what exceptions\r\n  are about.\r\n- **Logical** — it compiles, runs, finishes, and the answer is wrong. Java cannot\r\n  help; only tests can.\r\n- `int x = 1 / 0;` is a **runtime** error, not a compile-time one — the compiler\r\n  does not evaluate arithmetic to check it.\r\n\r\n## The hierarchy\r\n\r\n- `Throwable` splits into `Error` and `Exception`. `Exception` splits again into\r\n  `RuntimeException` and everything else.\r\n- **Checked** — anything under `Exception` that is not a `RuntimeException`. The\r\n  compiler insists you catch it or declare it.\r\n- **Unchecked** — `RuntimeException` and its subclasses. Programming mistakes:\r\n  `NullPointerException`, `ArrayIndexOutOfBoundsException`,\r\n  `ArithmeticException`.\r\n- **`Error`** — the JVM in trouble: `OutOfMemoryError`, `StackOverflowError`.\r\n  Do not catch these; there is nothing useful to do and you hide the cause.\r\n\r\n## Catching\r\n\r\n- `catch` blocks are tried **in order**, so the specific ones must come first.\r\n  Putting `catch (Exception e)` first makes every block after it unreachable, and\r\n  that is a compile error.\r\n- A caught exception does **not** stop the program. Execution continues after the\r\n  `try`/`catch`, which is the point of catching it.\r\n- `finally` runs whether or not anything was thrown, and whether or not it was\r\n  caught — for releasing what you acquired.\r\n- try-with-resources closes anything `AutoCloseable` for you, in reverse order,\r\n  even on the exception path. Prefer it to a `finally` that calls `close`.\r\n",interview:"## Interview questions\n\n### Q1: What is the difference between a compile-time error, a logical error, and a runtime error?\n\n**Answer:** A compile-time error is caught by the compiler before the program runs (e.g. a syntax or type mistake) and produces no `.class` file. A logical error compiles and runs to completion but yields the wrong result because the logic is incorrect — Java cannot detect it. A runtime error occurs while running, when an illegal operation throws an exception; uncaught, it crashes the program.\n\n### Q2: Is `int x = 1 / 0;` a compile-time or runtime error?\n\n**Answer:** Runtime. It compiles successfully and throws `java.lang.ArithmeticException: / by zero` when executed, because it is an unchecked runtime exception, not a syntax error.\n\n### Q3: What is an exception?\n\n**Answer:** An object representing an event that disrupts normal program flow at runtime. Every exception descends from `Throwable`. You handle exceptions with `try`/`catch`/`finally` or declare them with `throws`.\n\n### Q4: Explain the exception hierarchy.\n\n**Answer:** `Throwable` is the root, with two branches: `Error` (serious JVM problems like `OutOfMemoryError`, normally not caught) and `Exception` (recoverable problems). Under `Exception`, `RuntimeException` and its subclasses are unchecked; all other `Exception` subclasses (like `IOException`, `SQLException`) are checked.\n\n### Q5: What is the difference between checked and unchecked exceptions?\n\n**Answer:** Checked exceptions (subclasses of `Exception` but not `RuntimeException`) must be handled with `try`/`catch` or declared with `throws`, or the code won't compile. Unchecked exceptions (`RuntimeException` and `Error` subclasses) are not enforced by the compiler — they usually indicate bugs.\n\n### Q6: Why must specific `catch` blocks come before general ones?\n\n**Answer:** Because `catch` blocks are checked top-to-bottom and `Exception` is the parent of all the others. A general `catch (Exception e)` placed first would catch everything, making any specific `catch` after it unreachable — which is a compile error.\n\n### Q7: Does catching an exception stop the program?\n\n**Answer:** No. A caught exception transfers control to the `catch` block and the program continues afterward. Only an uncaught exception propagates up and terminates the program with a stack trace.\n"}},{slug:`throw-and-throws`,title:`throw, throws and custom exceptions`,subject:`Java`,stage:`Errors and input`,chapter:29,accent:`#60a5fa`,summary:`In Chapter 28 we caught exceptions that Java threw for us — like ArithmeticException from 1 / 0. This chapter is the other side of the coin, and it teaches the two distinct verbs of exception production and the two ways to deal with one:`,parts:{notes:'## Introduction\r\n\r\nIn [Chapter 28](#/study/exceptions) we **caught** exceptions that Java threw for us — like `ArithmeticException` from `1 / 0`. This chapter is the **other side of the coin**, and it teaches the two distinct verbs of exception *production* and the two ways to deal with one:\r\n\r\n1. **File 1** — how **you** raise an exception on purpose with the **`throw`** keyword, using your **own** exception type (a custom exception class), and **handle** it on the spot with `try`/`catch`.\r\n2. **File 2** — how a method **declares** an exception with the **`throws`** keyword and **propagates** it up the call stack instead of handling it.\r\n\r\nTogether these let you signal *your own* error conditions ("balance can\'t be negative", "age must be positive") and choose **where** in the program they get dealt with.\r\n\r\n> **One-line summary:** `throw` *raises* an exception; `try`/`catch` *handles* it here (File 1); `throws` *declares* it and *passes it up* to the caller (File 2).\r\n\r\n---\r\n\r\n## `throw` vs `throws` (Don\'t Confuse Them)\r\n\r\nThese look almost identical but do **opposite jobs**. This is the most common confusion in the whole topic:\r\n\r\n| | `throw` | `throws` |\r\n|---|---------|----------|\r\n| What it is | a **statement** that raises an exception **now** | a **clause** in a method signature |\r\n| Where it goes | **inside** a method body | after the **parameter list**, before `{` |\r\n| How many | one exception object at a time | a comma-separated **list of types** |\r\n| Meaning | "raise this exception right here" | "this method *might* throw these; caller beware" |\r\n| Example | `throw new CustomException("bad");` | `void load() throws IOException { ... }` |\r\n\r\nBoth appear in the same four lines below. The clause on the signature is\r\n`throws` — a warning to callers. The statement in the body is `throw` — the\r\nthing that actually happens.\r\n\r\n```java\r\nvoid readFile(String path) throws IOException {\r\n    if (path == null)\r\n        throw new IOException("path is null");\r\n}\r\n```\r\n\r\n**File 1** uses **`throw`** (it catches everything inside the same method, so it needs no `throws`). **File 2** uses **`throws`** (it declares the exception and lets it travel up instead of catching it).\r\n\r\n---\r\n\r\n## File Map\r\n\r\n| File | Keyword | Strategy | Demonstrates |\r\n|------|---------|----------|--------------|\r\n| [`Throw.java`](Throw.java) | `throw` | **handle here** | a custom exception class, throwing it with `throw`, and catching it among several `catch` blocks |\r\n| [`Throws.java`](Throws.java) | `throws` | **pass it up** | declaring a checked exception with `throws`, propagating it through `show()` → `main()` → JVM, and a `static{}` block |\r\n\r\nFile 1 contains **two classes**:\r\n\r\n- `CustomException` — our own exception type.\r\n- `Throw` — the `public` class with `main`, which throws and catches it.\r\n\r\nFile 2 also contains **two classes**:\r\n\r\n- `A` — a helper whose `show()` method declares `throws` and does **not** catch.\r\n- `Throws` — the `public` class with `main` (and a `static{}` block) that also declares `throws`.\r\n\r\n---\r\n\r\n## Building a Custom Exception: the `CustomException` Class\r\n\r\nA custom exception is just **a class that extends an existing exception class**. Here we extend `Exception`:\r\n\r\n```java\r\nclass CustomException extends Exception {\r\n    CustomException(String message) {\r\n        super(message);\r\n    }\r\n}\r\n```\r\n\r\n- **`extends Exception`** makes `CustomException` a real exception — it can be `throw`n and `catch`ed exactly like a built-in one ("is-a" relationship).\r\n- The **constructor** takes a message and passes it to the parent with **`super(message)`**. That stored message is what **`e.getMessage()`** returns later.\r\n- Giving it a meaningful name (`CustomException`, or better `InsufficientBalanceException`) makes both the **`catch` blocks** and the **error messages** self-documenting.\r\n\r\n> Because it extends `Exception` (and not `RuntimeException`), `CustomException` is a **checked** exception — see [the section below](#exception-vs-runtimeexception-as-a-base-class) for what that implies.\r\n\r\n---\r\n\r\n## Throwing It with `throw`\r\n\r\nThe `throw` keyword raises an exception object immediately. As soon as it runs, normal execution stops and Java starts hunting for a matching `catch`:\r\n\r\n```java\r\nif (j == 0) {\r\n    // throw new ArithmeticException();\r\n    // throw new ArithmeticException("...j = 0");\r\n    throw new CustomException("This is arithmetic exception where j = 0");\r\n}\r\n```\r\n\r\nThe file shows **three things you *could* throw**, two of them commented out. Each one would be caught by a **different** `catch` block below, which is a neat way to see how the type you throw decides where control lands:\r\n\r\n- The third line is the live one. It throws `CustomException`, so **{Block 2}** catches it.\r\n- Uncomment either `ArithmeticException` line instead and **{Block 1}** catches it.\r\n\r\n> **Syntax:** `throw` needs an **object** (`throw new X(...)`), not a class. `throw CustomException;` is a compile error; it must be `throw new CustomException(...);`.\r\n\r\n---\r\n\r\n## Catching It: Ordering the `catch` Blocks\r\n\r\nReading down: **{Block 2}** is the exact type we threw, **{Block 1}** would take\r\nan `ArithmeticException`, and the last one is the safety net — the parent type,\r\ncatching anything the other two missed.\r\n\r\n```java\r\n} catch (CustomException e) {\r\n    j = 18 / 1;\r\n    System.out.println("Exception: " + e.getMessage());\r\n} catch (ArithmeticException e) {\r\n    j = 18 / 1;\r\n    System.out.println("Exception: " + e.getMessage());\r\n} catch (Exception e) {\r\n    System.out.println(e);\r\n}\r\n```\r\n\r\nThree rules at work here:\r\n\r\n1. **The thrown type picks the block.** We threw a `CustomException`, so **{Block 2}** runs and the others are skipped.\r\n2. **Most specific first, most general last.** `CustomException` and `ArithmeticException` are specific; `Exception` (their ancestor) comes **last** as a catch-all. Reverse the order and the specific blocks become **unreachable** — a compile error.\r\n3. **Recovery.** Each catch sets `j = 18 / 1` (i.e. `18`), so after handling, the program has a sensible value to print instead of crashing.\r\n\r\n`e.getMessage()` prints **only** the message text; `e` (or `System.out.println(e)`) prints the **type and** the message.\r\n\r\n---\r\n\r\n## Walkthrough: What File 1 Does\r\n\r\n```java\r\nint i = 20;\r\nint j = 0;\r\n\r\ntry {\r\n    j = 18 / i;\r\n    if (j == 0) {\r\n        throw new CustomException("This is arithmetic exception where j = 0");\r\n    }\r\n} catch (CustomException e) {\r\n    j = 18 / 1;\r\n    System.out.println("Exception: " + e.getMessage());\r\n}\r\n\r\nSystem.out.println("Result is : " + j);\r\n```\r\n\r\nStep by step:\r\n\r\n1. `j = 18 / i` → `18 / 20`. Because both are `int`, this is **integer division**, which truncates `0.9` to **`0`**.\r\n2. `if (j == 0)` is **true**, so we `throw new CustomException(...)`.\r\n3. The throw transfers control to the **`CustomException`** catch block (**{Block 2}**).\r\n4. Inside, `j = 18 / 1` sets `j` to **`18`**, and we print the message.\r\n5. Execution continues past the try/catch and prints the recovered value.\r\n\r\n### Expected Output\r\n\r\n```text\r\nException: This is arithmetic exception where j = 0\r\nResult is : 18\r\n```\r\n\r\n> Note the message text says *"arithmetic exception"*, but the object thrown is actually a **`CustomException`** — the wording is just the human message we chose; the **type** is what controls which `catch` runs.\r\n\r\n---\r\n\r\n## Declaring with `throws`: Propagating Up the Call Stack\r\n\r\nFile 1 **handled** the exception where it happened. File 2 ([`Throws.java`](Throws.java)) does the opposite: it **refuses to handle** the exception and instead **declares** it with `throws`, pushing the responsibility onto the caller.\r\n\r\n```java\r\nclass A {\r\n    public void show() throws ClassNotFoundException {\r\n        Class.forName("Calc");\r\n    }\r\n}\r\n```\r\n\r\n`Class.forName("Calc")` asks the JVM to load a class **by name at runtime**. There is no class called `Calc`, so it throws **`ClassNotFoundException`** — a **checked** exception. *Checked* means the compiler will **not** let the code build unless every method in the path either:\r\n\r\n- **catches** it with `try`/`catch`, **or**\r\n- **declares** it with `throws ClassNotFoundException`.\r\n\r\nHere `show()` chooses to **declare**, so the exception escapes the method untouched.\r\n\r\n> **Why pass it up instead of catching it?** Often the method where an error *occurs* doesn\'t know how to *respond* to it. A low-level `show()` can\'t decide whether to retry, log, or warn the user — so it reports the problem upward and lets a higher-level method (which has that context) decide. `throws` is how it reports upward.\r\n\r\n---\r\n\r\n## Combined Methods: How Propagation Chains Together\r\n\r\nThe power of `throws` is that it **chains**. Each method in the call stack that declares (rather than catches) the exception passes it one level higher, until something catches it — or the JVM ends the program:\r\n\r\n```java\r\npublic static void main(String[] args) throws ClassNotFoundException {\r\n    A a = new A();\r\n    a.show();\r\n}\r\n```\r\n\r\n![Combined Methods: How Propagation Chains Together](diagrams/29-throw-and-throws-notes-ee068e24.jpg)\r\n\r\nThe real stack trace from running the file proves this exact chain (read it **bottom-up** — that is the order the calls were made):\r\n\r\n```text\r\n	at A.show(Throws.java:5)\r\n	at Throws.main(Throws.java:18)\r\n```\r\n\r\n> This is what **"combined methods and class throw"** means: a chain of methods that each declare `throws`, so one exception travels through several methods before anything stops it.\r\n\r\n---\r\n\r\n## The `static` Block and Class Loading\r\n\r\nFile 2 also shows a **`static{}` block** — a chunk of code that runs **once, when the class is first loaded/initialized**, which is just **before `main()` starts**:\r\n\r\n```java\r\npublic class Throws {\r\n    static {\r\n        System.out.println("Class Loaded");\r\n    }\r\n\r\n    public static void main(String[] args) throws ClassNotFoundException { ... }\r\n}\r\n```\r\n\r\nThat is why the program\'s output begins with `Class Loaded` **before** the exception appears: class initialization happens first, then `main` runs and triggers the failing `Class.forName`.\r\n\r\n| Runs... | When |\r\n|---------|------|\r\n| `static {}` block | once, at class load — **before** `main` |\r\n| instance initializer / constructor | every time an object is created with `new` |\r\n| `main()` body | after the class is fully initialized |\r\n\r\n---\r\n\r\n## Walkthrough: What File 2 Does\r\n\r\n```java Throws.java @run-throw-and-throws-throws\nclass A {\r\n    public void show() throws ClassNotFoundException {\r\n        Class.forName("Calc");\r\n    }\r\n}\r\n\r\npublic class Throws {\r\n    static { System.out.println("Class Loaded"); }\r\n\r\n    public static void main(String[] args) throws ClassNotFoundException {\r\n        A a = new A();\r\n        a.show();\r\n    }\r\n}\r\n```\n\n```output @run-throw-and-throws-throws\nClass Loaded\nException in thread "main" java.lang.ClassNotFoundException: Calc\n	at java.base/jdk.internal.loader.BuiltinClassLoader.loadClass(BuiltinClassLoader.java:580)\n	at java.base/java.lang.ClassLoader.loadClass(ClassLoader.java:502)\n	at java.base/java.lang.Class.forName0(Native Method)\n	at java.base/java.lang.Class.forName(Class.java:478)\n	at java.base/java.lang.Class.forName(Class.java:468)\n	at A.show(Throws.java:3)\n	at Throws.main(Throws.java:12)\n```\r\n\r\nStep by step:\r\n\r\n1. The JVM loads `Throws`; its **`static{}`** block runs and prints `Class Loaded`.\r\n2. `main()` runs, creates an `A`, and calls `a.show()`.\r\n3. Inside `show()`, `Class.forName("Calc")` finds no such class and **throws `ClassNotFoundException`**.\r\n4. `show()` declared `throws`, so it does **not** catch — the exception goes back to `main()`.\r\n5. `main()` also declared `throws`, so it does **not** catch — the exception goes to the **JVM**.\r\n6. The JVM prints a stack trace and ends the program. The "finished normally" lines never run.\r\n\r\n### Expected Output\r\n\r\n```text\r\nClass Loaded\r\nException in thread "main" java.lang.ClassNotFoundException: Calc\r\n	at java.base/jdk.internal.loader.BuiltinClassLoader.loadClass(BuiltinClassLoader.java:580)\r\n	at java.base/java.lang.ClassLoader.loadClass(ClassLoader.java:502)\r\n	at java.base/java.lang.Class.forName0(Native Method)\r\n	at java.base/java.lang.Class.forName(Class.java:478)\r\n	at java.base/java.lang.Class.forName(Class.java:468)\r\n	at A.show(Throws.java:5)\r\n	at Throws.main(Throws.java:18)\r\n```\r\n\r\n> **File 1 vs File 2 in one line:** File 1 **caught** the exception, so it printed a tidy message and continued (`Result is : 18`). File 2 **declared** the exception and let it escape, so the program **crashed** with a stack trace. Same machinery, opposite strategy.\r\n\r\n---\r\n\r\n## `Exception` vs `RuntimeException` as a Base Class\r\n\r\nWhen you create a custom exception, **what you extend decides whether it is checked or unchecked**:\r\n\r\n| You extend... | Your exception is... | Compiler forces handling? |\r\n|---------------|----------------------|---------------------------|\r\n| `Exception` | **checked** | ✅ Yes — `try/catch` **or** `throws` required |\r\n| `RuntimeException` | **unchecked** | ❌ No — handling is optional |\r\n\r\n```java\r\nclass CheckedProblem   extends Exception        { }\r\nclass UncheckedProblem extends RuntimeException  { }\r\n```\r\n\r\nOur `CustomException extends Exception`, so it is **checked**. In the file this is invisible because the `throw` and the `catch` are in the **same method** — but if a *method* threw it and let it escape, that method would need a `throws CustomException` clause.\r\n\r\n**Rule of thumb:** extend `Exception` for *recoverable, expected* conditions you want callers to deal with deliberately; extend `RuntimeException` for *programming bugs* that shouldn\'t normally happen.\r\n\r\n---\r\n\r\n## How to Compile and Run\r\n\r\nFrom the repository root (`d:\\Repos\\java`):\r\n\r\n```bash\r\n# File 1 — throw + catch\r\njavac Throw.java\r\njava Throw\r\n\r\n# File 2 — throws + propagation\r\njavac Throws.java\r\njava Throws\r\n```\r\n\r\nEach command compiles **two classes** into the folder, because each source file holds two (`CustomException` + `Throw` for File 1; `A` + `Throws` for File 2).\r\n\r\n### Expected Output — File 1 (`Throw`)\r\n\r\n```text\r\nException: This is arithmetic exception where j = 0\r\nResult is : 18\r\n```\r\n\r\n### Expected Output — File 2 (`Throws`)\r\n\r\n```text\r\nClass Loaded\r\nException in thread "main" java.lang.ClassNotFoundException: Calc\r\n	...\r\n	at A.show(Throws.java:5)\r\n	at Throws.main(Throws.java:18)\r\n```\r\n\r\nFile 2 **crashes on purpose** (exit code 1) — that is the point: nobody caught the propagated exception, so the JVM reports it.\r\n\r\n### Try it yourself\r\n\r\n**File 1:** swap the active `throw` for one of the commented ones to watch a **different** `catch` block fire:\r\n\r\n```java\r\nthrow new ArithmeticException("now I land in Block 1");\r\n```\r\n\r\nOutput becomes `Exception: now I land in Block 1` — handled by the `ArithmeticException` block instead.\r\n\r\n**File 2:** stop the crash by *handling* the exception instead of declaring it — wrap the call in `main` and drop the `throws`:\r\n\r\n```java\r\npublic static void main(String[] args) {\r\n    try {\r\n        new A().show();\r\n    } catch (ClassNotFoundException e) {\r\n        System.out.println("Handled: " + e.getMessage());\r\n    }\r\n}\r\n```\r\n\r\nNow the program prints `Class Loaded` then `Handled: Calc` and exits normally — propagation stopped because *you* caught it.\r\n\r\n---\r\n\r\n## Common Mistakes\r\n\r\n### Mistake 1: Confusing `throw` and `throws`\r\n\r\n`throw` **does** the throwing (a statement, inside the method). `throws` **declares** that a method may throw (a clause in the signature). See [the table above](#throw-vs-throws-dont-confuse-them).\r\n\r\n### Mistake 2: Throwing a class instead of an object\r\n\r\n```java\r\nthrow CustomException;            // ERROR\r\nthrow new CustomException("x");   // correct - throw an INSTANCE\r\n```\n\n```expected\nerror: cannot find symbol — variable CustomException\n\nthrow takes an object, not a type. You have to construct one, and that is\nwhere the message goes.\n```\r\n\r\n### Mistake 3: Catching the parent before the child\r\n\r\n```java\r\n} catch (Exception e) { ... }\r\n} catch (CustomException e) { ... }    // ERROR: unreachable code\r\n```\n\n```expected\nerror: exception CustomException has already been caught\n\nCustomException is an Exception, so the first block already covers it and\nthe second can never run. The compiler refuses rather than warning.\n```\r\n\r\nList `CustomException` (specific) **before** `Exception` (general).\r\n\r\n### Mistake 4: Forgetting `super(message)` in the constructor\r\n\r\n```java\r\nclass CustomException extends Exception {\r\n    CustomException(String message) { }\r\n}\r\n```\n\n```expected\nCompiles, and getMessage() returns null.\n\nThe message was accepted and thrown away. Pass it on with super(message),\nand take a cause as well so the original stack trace survives.\n```\r\n\r\nCall `super(message)` so `getMessage()` can return it.\r\n\r\n### Mistake 5: Extending `Exception` but never handling it\r\n\r\nBecause `Exception` is **checked**, a method that throws it and lets it escape **must** declare `throws CustomException`, or the code won\'t compile. (It works in File 1 only because the throw is caught in the same method.)\r\n\r\n### Mistake 6: Calling a `throws` method without handling or re-declaring\r\n\r\n```java\r\nvoid caller() {\r\n    new A().show();   // ERROR: unhandled ClassNotFoundException\r\n}\r\n```\n\n```expected\nerror: unreported exception ClassNotFoundException;\n    must be caught or declared to be thrown\n\nThat is a checked exception doing its job: the compiler makes every caller\ndecide, rather than letting it travel silently to the top.\n```\r\n\r\nIf `show()` declares `throws ClassNotFoundException`, then `caller()` must **either** wrap it in `try`/`catch` **or** add `throws ClassNotFoundException` to its own signature (as File 2\'s `main` does). You cannot silently ignore a checked exception.\r\n\r\n### Mistake 7: Thinking `throws` *handles* the exception\r\n\r\n`throws` does **not** handle anything — it only **declares** and **passes the problem upward**. If every method (including `main`) just declares `throws`, the exception reaches the JVM and the program **still crashes**, as File 2 demonstrates. To actually stop it, some method must `catch` it.\r\n\r\n---\r\n\r\n## The source files\r\n\r\nThe complete programs, renamed from the numbered file names the repository\r\nuses so they compile on their own. **Save** writes the file out of this page;\r\n**Run** replays the output it produced when this page was built.\r\n\r\n> Two of these end in a stack trace on purpose — that is the thing the chapter\r\n> is demonstrating. The recorded output is what the program really printed,\r\n> exit code and all.\r\n\r\n```bash\r\njavac Something.java\r\njava Something\r\n```\r\n\r\n### `Throw.java`\r\n\r\nThe first file raises an exception on purpose and deals with it in the same\r\nmethod. Reading it top to bottom:\r\n\r\n- **`class CustomException extends Exception`** is the whole of what a custom\r\n  exception type is. The constructor forwards its message to `Exception` with\r\n  `super(message)`, and that stored message is what `e.getMessage()` returns\r\n  further down.\r\n- **`j = 18 / i`** is integer division — `18 / 20` truncates to `0` rather than\r\n  `0.9`, which is what makes the `if (j == 0)` branch run at all.\r\n- The **two commented-out `throw`s** are alternatives, left in deliberately.\r\n  Uncomment either and the `ArithmeticException` block catches it instead; the\r\n  live line throws `CustomException`, so the first `catch` wins. The type you\r\n  throw is what decides where control lands.\r\n- The **`catch` order** is specific → general. `Exception` has to come last: it\r\n  is the ancestor of the other two, so putting it first would make them\r\n  unreachable and the file would not compile.\r\n- Each `catch` sets **`j = 18 / 1`**, so the program recovers instead of dying\r\n  and the final `println` has something sensible to report.\r\n\r\n```java Throw.java @run-throw-and-throws-throw\r\n// no package\r\n\r\nclass CustomException extends Exception {\r\n    CustomException(String message) {\r\n        super(message);\r\n    }\r\n}\r\n\r\npublic class Throw {\r\n    public static void main(String[] args) {\r\n        int i = 20;\r\n        int j = 0;\r\n\r\n        try {\r\n            j = 18 / i;\r\n            if (j == 0) {\r\n                // throw new ArithmeticException();\r\n                // throw new ArithmeticException("This is arithmetic exception where j = 0");\r\n                throw new CustomException("This is arithmetic exception where j = 0");\r\n            }\r\n        } catch (CustomException e) {\r\n            j = 18 / 1;\r\n            System.out.println("Exception: " + e.getMessage());\r\n        } catch (ArithmeticException e) {\r\n            j = 18 / 1;\r\n            System.out.println("Exception: " + e.getMessage());\r\n        } catch (Exception e) {\r\n            System.out.println(e);\r\n        }\r\n\r\n        System.out.println("Result is : " + j);\r\n    }\r\n}\r\n```\r\n\r\n```output @run-throw-and-throws-throw\r\nException: This is arithmetic exception where j = 0\r\nResult is : 18\r\n```\r\n\r\n### `Throws.java`\r\n\r\nThe second file never catches anything. It declares instead, and the exception\r\nwalks out of the program:\r\n\r\n- **`Class.forName("Calc")`** looks a class up by name at runtime. There is no\r\n  class called `Calc`, so it throws `ClassNotFoundException` — a *checked*\r\n  exception, which means the compiler forces every method in the chain to either\r\n  catch it or declare it.\r\n- **`show()` declares `throws ClassNotFoundException`** rather than catching, so\r\n  the exception escapes to whoever called it.\r\n- **`main()` declares it too**, so it escapes again — this time to the JVM,\r\n  which prints a stack trace and ends the program with a non-zero exit code.\r\n  That chain, `show()` → `main()` → JVM, is the whole point of the file.\r\n- The two **`println` calls after the throwing lines never run.** Once an\r\n  exception is raised, control leaves the method immediately.\r\n- The **`static {}` block** runs once, when the class is loaded — which is\r\n  *before* `main()` starts. That is why `Class Loaded` appears above the stack\r\n  trace rather than below it.\r\n\r\n```java Throws.java @run-throw-and-throws-throws\r\n// no package\r\n\r\nclass A {\r\n    public void show() throws ClassNotFoundException {\r\n        Class.forName("Calc");\r\n        System.out.println("show() finished normally");\r\n    }\r\n}\r\n\r\npublic class Throws {\r\n\r\n    static {\r\n        System.out.println("Class Loaded");\r\n    }\r\n\r\n    public static void main(String[] args) throws ClassNotFoundException {\r\n        A a = new A();\r\n        a.show();\r\n\r\n        System.out.println("main() finished normally");\r\n    }\r\n}\r\n```\r\n\r\n```output @run-throw-and-throws-throws\r\nClass Loaded\r\nException in thread "main" java.lang.ClassNotFoundException: Calc\r\n	at java.base/jdk.internal.loader.BuiltinClassLoader.loadClass(BuiltinClassLoader.java:580)\r\n	at java.base/java.lang.ClassLoader.loadClass(ClassLoader.java:502)\r\n	at java.base/java.lang.Class.forName0(Native Method)\r\n	at java.base/java.lang.Class.forName(Class.java:478)\r\n	at java.base/java.lang.Class.forName(Class.java:468)\r\n	at A.show(Throws.java:5)\r\n	at Throws.main(Throws.java:18)\r\n```\r\n',revision:`The chapter, compressed. If a line here is news, the **Notes** part is where it\r
comes from.\r
\r
## \`throw\` and \`throws\`\r
\r
- **\`throw\`** is a statement: it raises one exception object, now.\r
  \`throw new IllegalArgumentException("n must be positive");\`\r
- **\`throws\`** is part of a method signature: it declares what this method may\r
  raise and does not handle. One is an action, the other a warning label.\r
- You can only \`throw\` something assignable to \`Throwable\`, and only one at a\r
  time.\r
- Once a \`throw\` executes, the rest of the method does not. Control leaves for\r
  the nearest matching \`catch\`, unwinding frames as it goes.\r
\r
## Checked and unchecked, again\r
\r
- A **checked** exception must be caught or declared. That is the compiler\r
  forcing a decision at every call site.\r
- An **unchecked** one needs neither, so \`throws RuntimeException\` is legal and\r
  usually just documentation.\r
- An overriding method may declare **fewer or narrower** checked exceptions than\r
  the method it overrides, never broader — callers of the parent type must stay\r
  correct.\r
\r
## Custom exceptions\r
\r
- Extend \`Exception\` for a checked one, \`RuntimeException\` for unchecked. Nothing\r
  else is required.\r
- Provide the constructor that takes a \`cause\` and pass it on. Losing the\r
  original stack trace is the usual reason a production failure is unexplainable.\r
- Throw the most specific type that fits, and put the values in the message. A\r
  bare \`throw new Exception("error")\` tells the person reading the log nothing.\r
\r
## Class initialisation order\r
\r
- A \`static\` block runs when the class is initialised — before \`main\`, and before\r
  any exception \`main\` goes on to throw.\r
- So output from a static initialiser appears **before** the stack trace, even\r
  though the trace is about the line that ran later.\r
`,interview:"## Interview questions\n\n### Q1: What is the difference between `throw` and `throws`?\n\n**Answer:** `throw` is a statement used inside a method to actually raise an exception object (`throw new X(...)`). `throws` is a clause in a method's signature that declares the checked exceptions the method might propagate to its caller. One performs the action; the other documents the possibility.\n\n### Q2: How do you create a custom exception?\n\n**Answer:** Define a class that extends an existing exception type — `Exception` for a checked exception or `RuntimeException` for an unchecked one — and provide a constructor that passes a message to `super(message)` so `getMessage()` works.\n\n### Q3: Why call `super(message)` in a custom exception's constructor?\n\n**Answer:** The message is stored by the `Throwable` base class. Passing it up with `super(message)` lets `getMessage()` (and the printed stack trace) report it. Omitting it leaves the message `null`.\n\n### Q4: Should a custom exception extend `Exception` or `RuntimeException`?\n\n**Answer:** Extend `Exception` for checked exceptions — recoverable, expected conditions that the compiler forces callers to handle or declare. Extend `RuntimeException` for unchecked exceptions — typically programming bugs that callers shouldn't be forced to catch.\n\n### Q5: Can you throw a built-in exception manually?\n\n**Answer:** Yes. `throw new IllegalArgumentException(\"...\")` or `throw new ArithmeticException(\"...\")` is common for validating inputs. You are not limited to custom types.\n\n### Q6: When you `throw` an exception, which `catch` block handles it?\n\n**Answer:** The first `catch` whose declared type matches the thrown object's type (or a supertype of it), checked top to bottom. That's why specific types must be listed before general ones — otherwise the general block would catch it first and the specific block would be unreachable.\n\n### Q7: What happens to code after a `throw` statement?\n\n**Answer:** It is skipped. `throw` immediately transfers control to the nearest matching `catch` (or propagates out of the method if none matches), so any statements after it in the same block do not execute.\n\n### Q8: What does it mean to *propagate* an exception, and how does `throws` do it?\n\n**Answer:** Propagating means letting an exception travel up the call stack instead of catching it where it occurs. A method declares `throws SomeException` to say \"I won't handle this — my caller must.\" If every method in the chain declares it (e.g. `show()` → `main()`), the exception reaches the JVM, which prints a stack trace and ends the program. Some method must `catch` it to stop the propagation.\n\n### Q9: Why must `main` (or some caller) declare `throws` for a checked exception?\n\n**Answer:** Checked exceptions are enforced by the compiler: any method that calls code which may throw a checked exception must either catch it or re-declare it with `throws`. If a method like `show()` declares `throws ClassNotFoundException`, then `main` must do the same (or use `try`/`catch`), or the program won't compile.\n\n### Q10: When does a `static{}` block run, relative to `main`?\n\n**Answer:** A `static{}` block runs **once**, when the class is first loaded and initialized — which happens **before** `main` executes. That is why File 2 prints `Class Loaded` before the exception. Static blocks are typically used for one-time setup of static state.\n"}},{slug:`reading-input`,title:`Reading input`,subject:`Java`,stage:`Errors and input`,chapter:30,accent:`#38bdf8`,summary:`Every program so far has had its data written into the source. Real programs get it from somewhere else, and the simplest somewhere else is the keyboard.`,parts:{notes:`Every program so far has had its data written into the source. Real programs get\r
it from somewhere else, and the simplest somewhere else is the keyboard.\r
\r
Java gives you \`System.in\` for that. It is the counterpart to \`System.out\`, and\r
it is deliberately primitive: it hands you one **byte** at a time and leaves the\r
rest to you. This chapter covers the three ways up from there.\r
\r
## What \`System.in\` actually is\r
\r
\`System.in\` is an \`InputStream\` — a source of raw bytes, not text. That single\r
fact explains everything that surprises people about input in Java.\r
\r
![What System.in actually is](diagrams/30-reading-input-notes-5300cde9.jpg)\r
\r
Each layer adds convenience. You will use the top of the stack almost always,\r
but knowing the bottom exists is what makes the errors readable.\r
\r
## Reading a single byte\r
\r
\`\`\`java Input.java\r
import java.io.IOException;\r
\r
public class Input {\r
    public static void main(String[] args) throws IOException {\r
\r
        System.out.println();\r
\r
        int n = System.in.read();\r
\r
        System.out.println(n);\r
\r
        System.out.println((char) n);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-input-1\r
javac Input.java\r
java Input\r
\`\`\`\r
\r
Type \`A\` and press Enter:\r
\r
\`\`\`output @run-input-1\r
65\r
A\r
\`\`\`\r
\r
Two things worth pulling out.\r
\r
**\`read()\` returns an \`int\`, not a \`char\`.** It returns the byte value, which is\r
65 for \`A\`, and \`-1\` when the input has ended. That is why it cannot be a\r
\`char\` — there would be no value left over to mean "nothing more".\r
\r
**It reads one byte, not one keypress.** Pressing Enter puts a newline into the\r
stream as well, so the next \`read()\` would return 10 rather than waiting for you.\r
This is the source of most "my program skipped a question" bugs.\r
\r
**\`throws IOException\` is not optional.** Reading can fail, so \`read()\` declares a\r
checked exception. Either declare it on \`main\` as above, or wrap the call in\r
\`try\`/\`catch\`.\r
\r
## Reading a whole line with \`BufferedReader\`\r
\r
One byte at a time is unusable for real input. \`InputStreamReader\` turns the byte\r
stream into characters, and \`BufferedReader\` turns those into lines.\r
\r
\`\`\`java BufferReader.java\r
import java.io.BufferedReader;\r
import java.io.InputStreamReader;\r
\r
public class BufferReader {\r
    public static void main(String[] args) throws Exception {\r
\r
        System.out.println("Enter a number");\r
\r
        InputStreamReader S_Reader = new InputStreamReader(System.in);\r
\r
        BufferedReader BR = new BufferedReader(S_Reader);\r
\r
        int n = Integer.parseInt(BR.readLine());\r
\r
        System.out.println(n);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-input-2\r
javac BufferReader.java\r
java BufferReader\r
\`\`\`\r
\r
Type \`42\` and press Enter:\r
\r
\`\`\`output @run-input-2\r
Enter a number\r
42\r
\`\`\`\r
\r
\`readLine()\` returns a **\`String\`** — always. There is no \`readInt()\`. Converting\r
is your job, and \`Integer.parseInt\` is how:\r
\r
| Want | Use |\r
|---|---|\r
| \`int\` | \`Integer.parseInt(line)\` |\r
| \`long\` | \`Long.parseLong(line)\` |\r
| \`double\` | \`Double.parseDouble(line)\` |\r
| \`boolean\` | \`Boolean.parseBoolean(line)\` |\r
\r
\`readLine()\` returns \`null\` at end of input, and \`parseInt\` throws\r
\`NumberFormatException\` if the text is not a number. Both are worth handling in\r
anything that will meet a real user.\r
\r
The two wrappers are usually written as one expression:\r
\r
\`\`\`java\r
BufferedReader br = new BufferedReader(new InputStreamReader(System.in));\r
\`\`\`\r
\r
## Reading with \`Scanner\`\r
\r
\`Scanner\` does the parsing for you, which makes it the usual choice while\r
learning:\r
\r
\`\`\`java\r
import java.util.Scanner;\r
\r
Scanner sc = new Scanner(System.in);\r
\r
System.out.print("Name: ");\r
String name = sc.nextLine();\r
\r
System.out.print("Age: ");\r
int age = sc.nextInt();\r
\r
System.out.println(name + " is " + age);\r
\r
sc.close();\r
\`\`\`

\`\`\`expected
name is "" — the empty String, without waiting for you to type anything.

nextInt() takes the number and leaves the newline you pressed after it, so
the following nextLine() reads that leftover newline and returns at once.
Add a bare sc.nextLine() to consume it.
\`\`\`\r
\r
| Method | Reads |\r
|---|---|\r
| \`nextLine()\` | the rest of the line, as a \`String\` |\r
| \`next()\` | one word, up to the next space |\r
| \`nextInt()\` | an \`int\` |\r
| \`nextDouble()\` | a \`double\` |\r
| \`nextBoolean()\` | a \`boolean\` |\r
| \`hasNextInt()\` | checks before reading, so you can validate |\r
\r
## \`Scanner\` or \`BufferedReader\`?\r
\r
| | \`Scanner\` | \`BufferedReader\` |\r
|---|---|---|\r
| Parsing | built in — \`nextInt()\` | you call \`Integer.parseInt\` |\r
| Speed | slower | considerably faster |\r
| Buffer | small | large |\r
| Errors | \`InputMismatchException\` | \`NumberFormatException\` |\r
| Thread safety | not synchronised | synchronised |\r
\r
Use \`Scanner\` while learning and for anything interactive. Use \`BufferedReader\`\r
when input volume matters — competitive programming problems that read a hundred\r
thousand lines are the standard example, and the difference there is seconds, not\r
milliseconds.\r
\r
## How to Compile and Run\r
\r
\`\`\`bash\r
javac Input.java\r
java Input\r
\r
javac BufferReader.java\r
java BufferReader\r
\`\`\`\r
\r
Both wait for you to type. Enter a value and press Enter.\r
\r
## Common Mistakes\r
\r
### Mistake 1: \`nextInt()\` followed by \`nextLine()\` reading nothing\r
\r
\`\`\`java\r
int age = sc.nextInt();\r
String name = sc.nextLine();\r
\`\`\`

\`\`\`expected
name is "" — the empty String, without waiting for you to type anything.

nextInt() takes the number and leaves the newline you pressed after it, so
the following nextLine() reads that leftover newline and returns at once.
Add a bare sc.nextLine() to consume it.
\`\`\`\r
\r
\`nextInt()\` consumes the digits and stops. The Enter key is still sitting in the\r
stream, so the next \`nextLine()\` finds it immediately and returns \`""\`. Fix it by\r
adding a throwaway \`sc.nextLine()\` after the number, or by reading everything\r
with \`nextLine()\` and parsing yourself.\r
\r
This is the single most common input bug in Java, and it looks like the program\r
skipped a question.\r
\r
### Mistake 2: Expecting \`read()\` to give you a character\r
\r
\`System.in.read()\` returns an \`int\` — the byte value. Cast it with \`(char) n\`\r
when you want the character. And remember \`-1\` means the input has ended.\r
\r
### Mistake 3: Forgetting that \`readLine()\` returns text\r
\r
\`\`\`java\r
int n = br.readLine();              // does not compile\r
int n = Integer.parseInt(br.readLine());  // correct\r
\`\`\`

\`\`\`expected
error: incompatible types: String cannot be converted to int

readLine gives you text and nothing else. Everything numeric goes through
Integer.parseInt — which is also where bad input throws
NumberFormatException rather than being silently accepted.
\`\`\`\r
\r
### Mistake 4: Not handling \`IOException\`\r
\r
\`read()\` and \`readLine()\` both throw a checked exception. \`throws IOException\` on\r
\`main\` is fine for a learning program; real code catches it.\r
\r
### Mistake 5: Closing \`System.in\`\r
\r
\`sc.close()\` closes the underlying \`System.in\` as well. Open a second \`Scanner\`\r
afterwards and it will find the stream already closed. Create one \`Scanner\` for\r
the whole program.\r
\r
### Mistake 6: Assuming input is valid\r
\r
\`Integer.parseInt("abc")\` throws \`NumberFormatException\`, and \`sc.nextInt()\` on\r
the same input throws \`InputMismatchException\`. Check with \`hasNextInt()\`, or\r
catch the exception.\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`Input.java\`\r
\r
The rawest way to read input there is. \`System.in.read()\` returns **one byte**,\r
as an \`int\` — so typing \`A\` gives \`65\`, and casting that to \`char\` gives \`A\`\r
back. It reads a single character, not a line, and it hands you a number rather\r
than text.\r
\r
\`main\` declares \`throws IOException\` because \`read()\` is a checked-exception\r
method and nothing here catches it.\r
\r
\`\`\`java Input.java @run-reading-input-input\r
import java.io.IOException;\r
\r
public class Input {\r
    public static void main(String[] args) throws IOException {\r
        \r
        System.out.println();\r
        \r
        int n = System.in.read();\r
\r
        System.out.println(n);\r
\r
        System.out.println((char)n);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-reading-input-input\r
\r
65\r
A\r
\`\`\`\r
\r
### \`BufferReader.java\`\r
\r
The practical version. \`BufferedReader\` wrapped around an\r
\`InputStreamReader\` reads a whole **line** at a time as a \`String\`, which is\r
what you almost always want.\r
\r
The cost is that a line of text is not a number: \`Integer.parseInt(...)\` has to\r
convert it, and it throws \`NumberFormatException\` if what arrived was not\r
numeric.\r
\r
\`\`\`java BufferReader.java @run-reading-input-buffer-reader\r
import java.io.BufferedReader;\r
import java.io.InputStreamReader;\r
\r
public class BufferReader {\r
    public static void main(String[] args) throws Exception {\r
\r
        System.out.println("Enter a number");\r
\r
        InputStreamReader S_Reader = new InputStreamReader(System.in);\r
\r
        BufferedReader BR = new BufferedReader(S_Reader);\r
\r
        int n = Integer.parseInt(BR.readLine());\r
\r
        System.out.println(n);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`output @run-reading-input-buffer-reader\r
Enter a number\r
42\r
\`\`\`\r
`,revision:"The chapter, compressed. If a line here is news, the **Notes** part is where it\r\ncomes from.\r\n\r\n## `Scanner`\r\n\r\n- `new Scanner(System.in)` and then `nextInt`, `nextLine`, `nextDouble`. Easy to\r\n  read, and the right default for a small program.\r\n- `nextInt` consumes the number and **leaves the newline**. The next `nextLine`\r\n  then returns an empty String. Call an extra `nextLine` to clear it, or read\r\n  everything with `nextLine` and parse.\r\n- `nextInt` on non-numeric input throws `InputMismatchException` and does **not**\r\n  consume the bad token, so an unguarded retry loops forever.\r\n- `hasNextInt()` tests before reading, which is how you validate without\r\n  exceptions.\r\n\r\n## `BufferedReader`\r\n\r\n- `new BufferedReader(new InputStreamReader(System.in))`, then `readLine`.\r\n  Faster, buffered, and it only gives you Strings.\r\n- Everything numeric goes through `Integer.parseInt` or `Double.parseDouble`, so\r\n  bad input is a `NumberFormatException` you handle yourself.\r\n- `readLine` returns `null` at end of input rather than throwing — the loop\r\n  condition, not an error.\r\n- It throws `IOException`, which is **checked**, so the method must catch it or\r\n  declare `throws IOException`.\r\n\r\n## Which, and the shared traps\r\n\r\n- `Scanner` for convenience and mixed types; `BufferedReader` for volume, or\r\n  when you want to parse the line yourself.\r\n- Do not wrap `System.in` in two readers at once — the buffered one will have\r\n  swallowed input the other expects.\r\n- Closing a `Scanner` over `System.in` closes `System.in` for the whole program.\r\n  In a short program, leave it open.\r\n",interview:'## Interview questions\n\n### Q1: What is `System.in`, and what does it give you?\n\n**Answer:** `System.in` is an `InputStream` — a source of raw **bytes**, not\ntext. Its `read()` method returns one byte at a time as an `int`, and `-1` when\nthe input has ended. Everything else — `InputStreamReader`, `BufferedReader`,\n`Scanner` — is a layer on top that turns those bytes into characters, lines or\nparsed values.\n\n### Q2: Why does `System.in.read()` return an `int` rather than a `char`?\n\n**Answer:** Because it needs a value that no real byte can take, to signal that\nthe input has ended. A byte is 0 to 255; `read()` returns those values plus `-1`\nfor end of stream. A `char` has no spare value to mean "nothing more", so the\nreturn type has to be wider.\n\n### Q3: What is the difference between `Scanner` and `BufferedReader`?\n\n**Answer:** `Scanner` parses for you — `nextInt()`, `nextDouble()`, `next()` —\nwhich makes it convenient but slower, and it uses a small buffer.\n`BufferedReader` only gives you `readLine()`, returning a `String` you convert\nyourself with `Integer.parseInt`, but it is considerably faster and uses a large\nbuffer. `Scanner` for interactive programs, `BufferedReader` when the input is\nlarge.\n\n### Q4: Why do you need `InputStreamReader` between `System.in` and `BufferedReader`?\n\n**Answer:** Because they speak different things. `System.in` produces bytes;\n`BufferedReader` reads characters. `InputStreamReader` is the bridge that decodes\nbytes into characters using a character set. That is why the two are almost\nalways written together as\n`new BufferedReader(new InputStreamReader(System.in))`.\n\n### Q5: Why does `nextLine()` return an empty String straight after `nextInt()`?\n\n**Answer:** `nextInt()` reads the digits and stops, leaving the newline from the\nEnter key in the stream. The following `nextLine()` finds that newline\nimmediately and returns everything before it, which is nothing. The fix is either\nan extra throwaway `sc.nextLine()` after the number, or reading every line with\n`nextLine()` and parsing the values yourself. It is the most common input bug in\nJava, and it looks like the program skipped a question.\n\n### Q6: How do you convert the `String` from `readLine()` into a number?\n\n**Answer:** With the wrapper class parse methods: `Integer.parseInt`,\n`Long.parseLong`, `Double.parseDouble`, `Boolean.parseBoolean`. All of them throw\n`NumberFormatException` if the text is not a valid number, so anything meeting a\nreal user should catch it or validate first.\n\n### Q7: Why do input methods throw `IOException`, and what must you do about it?\n\n**Answer:** Reading from a stream can fail — the source can be closed or\nunavailable — so `read()` and `readLine()` declare `IOException`, which is a\n**checked** exception. The compiler forces you to either catch it or declare\n`throws IOException` on the enclosing method. Declaring it on `main` is\nacceptable in a small program; real code handles it.\n\n### Q8: What happens if you close a `Scanner` wrapping `System.in`?\n\n**Answer:** It closes `System.in` itself, because closing a wrapper closes what\nit wraps. Any `Scanner` created afterwards will find the stream already closed\nand fail. Create one `Scanner` for the whole program rather than one per method.\n'}}],ee={notes:{label:`Notes`,blurb:`The explanation itself.`},advanced:{label:`Advanced`,blurb:`The mechanisms underneath.`},revision:{label:`Quick revision`,blurb:`The chapter, compressed.`},interview:{label:`Interview`,blurb:`Questions, with full answers.`}},E=[`notes`,`advanced`,`revision`,`interview`],D=[`Fundamentals`,`Classes and objects`,`Inheritance and polymorphism`,`Types and contracts`,`Errors and input`],te=e=>{let t=D.indexOf(e);return t===-1?D.length:t},O=T.map(e=>{let t=E.filter(t=>e.parts[t]).map((t,n)=>({id:t,...ee[t],content:C(e.parts[t]),route:n===0?`study/${e.slug}`:`study/${e.slug}/${t}`}));return{...e,parts:t,route:`study/${e.slug}`}}).sort((e,t)=>te(e.stage)-te(t.stage)||(e.chapter??99)-(t.chapter??99)),ne=e=>[e.title,e.tagline??``,e.summary,e.subject,e.stage].concat(e.parts.map(e=>e.content)).join(` `).toLowerCase(),k=[...new Set(O.map(e=>e.subject))].sort(),re=e=>{let t=O.filter(t=>t.subject===e);return D.map(e=>({stage:e,topics:t.filter(t=>t.stage===e)})).filter(e=>e.topics.length>0)},A=e=>O.find(t=>t.slug===e)??null,ie=(e,t)=>e?.parts.find(e=>e.id===t)??e?.parts[0]??null,j=`Every Java tutorial says the same sentence and then moves on: *"Java has a garbage
collector, so you don't have to free memory."* True, and useless. It tells you
nothing about why your service pauses for 400 ms every few minutes, or why a
program with a garbage collector can still die of \`OutOfMemoryError\`.

This walks through what the collector actually does — the one rule it follows,
the shape of the heap it works on, and the flags that make it narrate its own
work. Two small programs come with it. Both run on any JDK 8 or newer, no
libraries, no build tool.

> Everything here is observation, not configuration advice. Do not tune a
> collector because a guide told you to — measure first, and change one flag at a
> time.

## The one rule: reachability

The collector does not care how old an object is, how big it is, or whether you
are finished with it. It asks exactly one question:

**Can this object still be reached, by following references, from a GC root?**

GC roots are the places the JVM trusts as definitely alive:

- local variables and parameters in every method currently running
- \`static\` fields of loaded classes
- live threads
- references held by native code

It starts at those roots and follows every reference it can. What it reaches is
live. Everything else is garbage, whatever you intended.

![The one rule: reachability](diagrams/garbage-collection-notes-1792a608.jpg)

Two consequences fall straight out of this, and both matter:

- **Reference cycles are not a problem.** Two objects pointing at each other are
  collected happily, because no root reaches either. This is the thing Python's
  old reference-counting collector had to add a whole extra cycle detector for.
- **A reference you forgot about keeps an object alive forever.** That is the
  entire mechanism behind a Java memory leak, and there is a section on it below.

## Watching an object die

\`WeakReference\` lets you point at an object *without* keeping it alive. It is the
only clean way to observe collection happening, so it makes a good first demo.

\`\`\`java Reachability.java @run-garbage-collection-reachability
import java.lang.ref.WeakReference;

public class Reachability {

    public static void main(String[] args) {
        Object note = new Object();

        // A weak reference watches the object without keeping it alive.
        WeakReference<Object> watcher = new WeakReference<>(note);

        System.gc();
        System.out.println("while a variable points at it : " + (watcher.get() != null));

        note = null;    // the last way to reach it is gone
        System.gc();
        System.out.println("after that variable is gone   : " + (watcher.get() != null));
    }
}
\`\`\`

\`\`\`output @run-garbage-collection-reachability
while a variable points at it : true
after that variable is gone   : false
\`\`\`

\`\`\`bash @run-reach
javac Reachability.java
java Reachability
\`\`\`

\`\`\`output @run-reach
while a variable points at it : true
after that variable is gone   : false
\`\`\`

That is the whole rule, in two lines of output. While something can reach the
object it stays; the moment nothing can, it goes.

Now the version that catches people out. Nothing changes except that a list also
holds the object:

\`\`\`java StillHeld.java @run-garbage-collection-still-held
import java.lang.ref.WeakReference;
import java.util.ArrayList;
import java.util.List;

public class StillHeld {

    public static void main(String[] args) {
        List<Object> cache = new ArrayList<>();

        Object note = new Object();
        WeakReference<Object> watcher = new WeakReference<>(note);

        cache.add(note);   // the list is now a second way to reach it
        note = null;       // so dropping our own variable is not enough
        System.gc();
        System.out.println("while the list holds it : " + (watcher.get() != null));

        cache.clear();
        System.gc();
        System.out.println("after the list is empty : " + (watcher.get() != null));
    }
}
\`\`\`

\`\`\`output @run-garbage-collection-still-held
while the list holds it : true
after the list is empty : false
\`\`\`

The second program is the bug that will page you at 3 a.m. The object is
unreachable *from your code* and perfectly reachable *from the heap*, so it
stays — and a list that is only ever added to is how every Java memory leak
begins.

> \`System.gc()\` is a request, not a command. It works here because these objects
> are plainly unreachable and the JVM has nothing better to do. Never put it in
> real code — see the mistakes section.

## The heap, drawn

Decades of measurement produced one dominant observation, usually called the
**weak generational hypothesis**:

> Most objects die very young. The ones that survive a while tend to keep
> surviving.

So the JVM sorts the heap by age and collects the young part often and cheaply:

![The heap, drawn](diagrams/garbage-collection-notes-67c3484b.jpg)

**Eden** is where \`new\` puts almost everything. Allocation there is a pointer
bump, which is why creating objects in Java is so much cheaper than people
assume. The two **survivor** spaces are a holding area, and only one is in use at
a time. The **old generation** is where objects go once they have proved they are
sticking around.

Metaspace is worth naming because it trips people up: it holds class metadata,
lives outside the heap in native memory, and has its own \`OutOfMemoryError\`.

## The life of one object

Follow a single object through the machine.

**It is allocated in Eden**, alongside everything else.

![The life of one object](diagrams/garbage-collection-notes-e355e58e.jpg)

**Eden fills up, so a minor GC runs.** Say only \`B\` and \`D\` are still reachable.

![The life of one object](diagrams/garbage-collection-notes-99a84503.jpg)

Look closely at what happened to the dead objects: **nothing**. They were not
deleted one at a time. The survivors were copied out and the whole of Eden was
declared empty in a single step. Garbage costs nothing to collect. Only surviving
costs anything — which is why a young collection where almost everything died is
nearly free.

![A young collection costs what survived it, not what died](diagrams/garbage-collection-notes-cost-of-surviving.jpg)

**More allocation, another minor GC.** Survivors move to the *other* survivor
space and their age goes up.

![The life of one object](diagrams/garbage-collection-notes-2854bda8.jpg)

**\`B\` keeps surviving.** Once its age crosses the tenuring threshold — 15 by
default — it is promoted out of the young generation for good.

![The life of one object](diagrams/garbage-collection-notes-c915c2a4.jpg)

**The old generation fills up** over minutes or hours, and now the expensive
collection runs, because it has to walk the whole heap.

| | Cleans | Frequency | Typical pause |
|---|---|---|---|
| **Minor / young GC** | Young generation | Very often | Under a millisecond to a few ms |
| **Major / old GC** | Old generation | Rarely | Noticeably longer |
| **Full GC** | Whole heap **+** metaspace | Rarest | The spike on your latency graph |

**Stop-the-world** means the JVM suspends every application thread while the
collector works. Every collector does this at some point. The difference between
them is how long those pauses last and how much work happens concurrently, next
to your running code.

## Watching the heap move

The second program prints the three numbers \`Runtime\` exposes, so you can watch
memory get used and handed back.

\`\`\`java HeapWatch.java
public class HeapWatch {

    /** Used memory, in megabytes. */
    static long usedMb() {
        Runtime rt = Runtime.getRuntime();
        return (rt.totalMemory() - rt.freeMemory()) / (1024 * 1024);
    }

    public static void main(String[] args) {
        System.out.println("at startup        : " + usedMb() + " MB");

        // Two million Strings, each one abandoned by the next loop.
        String last = null;
        for (int i = 0; i < 2_000_000; i++) {
            last = "object number " + i;
        }
        System.out.println("after allocating  : " + usedMb() + " MB   (kept: " + last + ")");

        System.gc();
        System.out.println("after collecting  : " + usedMb() + " MB");

        // This one stays reachable, so collecting cannot reclaim it.
        byte[] kept = new byte[50 * 1024 * 1024];
        System.gc();
        System.out.println("holding 50 MB     : " + usedMb() + " MB   (kept " + kept.length / (1024 * 1024) + " MB)");
    }
}
\`\`\`

\`\`\`bash @run-heap
javac HeapWatch.java
java HeapWatch
\`\`\`

Your numbers will differ — the default heap size depends on how much RAM the
machine has. The pattern is the point:

\`\`\`output @run-heap
at startup        : 7 MB
after allocating  : 20 MB   (kept: object number 1999999)
after collecting  : 3 MB
holding 50 MB     : 55 MB   (kept 50 MB)
\`\`\`

Two million Strings were created, and usage never got near the hundred-odd
megabytes that would imply — the collector was already clearing up in the
background while the loop ran, which is why that middle number is different
every run. After the explicit collection it drops to almost nothing.

The last line is the important one. It stays high because \`kept\` still points at
that memory, and **reachable memory does not come back**, however often you
collect.

## Making the JVM narrate

You do not need a profiler for the first pass. The JVM will log its own
collections:

\`\`\`bash
java -Xlog:gc HeapWatch
\`\`\`

Real output from the program above:

\`\`\`text
[0.007s][info][gc] Using G1
[0.058s][info][gc] GC(0) Pause Young (Normal) (G1 Evacuation Pause) 5M->1M(10M) 1.333ms
[0.070s][info][gc] GC(3) Pause Young (Normal) (G1 Evacuation Pause) 17M->1M(30M) 0.701ms
[0.080s][info][gc] GC(6) Pause Young (Normal) (G1 Evacuation Pause) 17M->1M(90M) 0.972ms
[0.102s][info][gc] GC(7) Pause Full (System.gc()) 53M->1M(8M) 3.163ms
\`\`\`

One line, taken apart:

![Making the JVM narrate](diagrams/garbage-collection-notes-8b9b4556.jpg)

That third line is the whole article in one sentence: **17 MB of objects existed,
1 MB was still reachable, and reclaiming the other 16 MB took 0.7 milliseconds.**

\`-Xlog:gc*\` prints every phase if you want the detail.
\`-Xlog:gc:file=gc.log\` writes to a file instead of the console. On Java 8 the
older spelling is \`-XX:+PrintGCDetails\`.

## Which collector you are running

The JVM ships several. You pick one with a flag; the code never changes.

| Collector | Flag | Suits | Trade-off |
|---|---|---|---|
| **Serial** | \`-XX:+UseSerialGC\` | Small heaps, one CPU, containers | Single-threaded, simple, predictable |
| **Parallel** | \`-XX:+UseParallelGC\` | Batch jobs | Best throughput, longest pauses |
| **G1** *(default)* | \`-XX:+UseG1GC\` | General server work | Balances throughput against pause time |
| **ZGC** | \`-XX:+UseZGC\` | Large heaps, low latency | Sub-millisecond pauses even on huge heaps |

**G1** — Garbage-First — has been the default since Java 9. It does not use three
fixed blocks. It splits the heap into a couple of thousand equal regions, labels
each one Eden, survivor or old, and collects the regions holding the most garbage
first. Hence the name. You can ask it for a pause target with
\`-XX:MaxGCPauseMillis=200\`, and it will try.

![Typical worst-case pause for each collector on the same heap](diagrams/garbage-collection-notes-collectors.jpg)

Check what you are actually getting:

\`\`\`powershell @flags
java -XX:+PrintFlagsFinal -version | findstr "UseG1GC UseZGC UseSerialGC UseParallelGC"
\`\`\`

\`\`\`output @flags
bool UseG1GC                = true    {product} {ergonomic}
bool UseParallelGC          = false   {product} {default}
bool UseSerialGC            = false   {product} {default}
bool UseZGC                 = false   {product} {default}
\`\`\`

\`{ergonomic}\` means the JVM chose it for you based on the machine it found.

## Leaks, in a language that cannot leak

You never forget to \`free\`, so the C-style leak is gone. You can still exhaust
the heap, and it always looks like this:

> A Java memory leak is an object that is still **reachable** but no longer
> **useful**.

The collector sees a live reference and correctly refuses to touch it. The usual
suspects:

- **A collection that only grows.** A \`static Map\` used as a cache with no
  eviction policy. Every entry stays reachable for the life of the process.
- **Listeners registered and never removed.** The publisher holds a reference to
  your object forever, and your object holds whatever it was holding.
- **Long-lived objects holding short-lived ones.** A session that keeps every
  request it has handled.
- **\`ThreadLocal\` in a thread pool.** The thread outlives the request, so the
  value stays attached until something removes it explicitly.

When the heap fills you get:

\`\`\`text
Exception in thread "main" java.lang.OutOfMemoryError: Java heap space
\`\`\`

That is not the collector failing. It means it ran, found everything still
reachable, and had nothing to reclaim. Look at your own object graph.

## When it goes wrong

- **\`OutOfMemoryError: Java heap space\`** — either the workload genuinely needs
  more than \`-Xmx\` allows, or something is holding references it should have
  dropped. Run with \`-XX:+HeapDumpOnOutOfMemoryError\` and open the resulting
  \`.hprof\` in Eclipse MAT or VisualVM; the dominator tree points at the culprit
  in about a minute.
- **\`OutOfMemoryError: Metaspace\`** — the heap is fine and class metadata is not.
  Usually a container or framework loading classes dynamically without bound.
  \`-XX:MaxMetaspaceSize\` caps it so it fails early instead of eating the machine.
- **\`OutOfMemoryError: GC overhead limit exceeded\`** — the JVM is spending more
  than 98% of its time collecting and recovering less than 2%. The heap is
  effectively full; this is the polite version of the error above.
- **\`StackOverflowError\`** — nothing to do with the heap. That is a runaway
  recursion filling a thread's stack. \`-Xss\` sizes it, but the recursion is
  usually the bug.
- **Regular latency spikes in production** — turn on GC logging before guessing.
  If the spikes line up with \`Pause Full\` lines, you have a promotion problem, not
  a slow database.
- **The program runs fine, then dies on a smaller machine** — the default heap is
  a fraction of physical RAM, so it silently changes between your laptop and the
  server. Set \`-Xmx\` explicitly for anything you deploy.

## Things worth unlearning

**\`System.gc()\` does not force a collection.** It is a hint, it can be disabled
outright with \`-XX:+DisableExplicitGC\`, and when it is honoured it usually
triggers a full GC — the expensive one. It belongs in demos like the ones above,
not in application code.

**Setting a local variable to \`null\` does not help.** The JVM already knows a
local is dead after its last use. It only matters for a long-lived *field* that
holds something large well past its usefulness.

**\`finalize()\` is not cleanup.** It may run late, on any thread, or never. It has
been deprecated for removal since Java 9. Use try-with-resources:

\`\`\`java
try (var reader = new BufferedReader(new FileReader("data.txt"))) {
    // close() is guaranteed, collector or no collector
}
\`\`\`

**\`-Xmx\` is not memory used.** It is a ceiling. \`maxMemory()\` reporting 4 GB
means the JVM is allowed 4 GB, not that it has taken it.

**The heap is not all the memory Java uses.** Metaspace, thread stacks,
JIT-compiled code and direct \`ByteBuffer\`s all live outside it. A Java process
can comfortably use more OS memory than \`-Xmx\` — which is exactly how a container
with a memory limit ends up killed while the heap looks healthy.

![What -Xmx bounds, and everything it does not](diagrams/garbage-collection-notes-beyond-xmx.jpg)

## Worth trying

Run \`HeapWatch\` with a small heap and watch the behaviour change:

\`\`\`bash
java -Xmx64m -Xlog:gc HeapWatch
\`\`\`

It still finishes, because the collector keeps clearing Eden — it just has to
work much harder, and the log shows it. Drop to \`-Xmx32m\` and it fails, because
the retained 50 MB no longer fits anywhere. That failure is the clearest possible
demonstration of the difference between garbage and live data.

## Further reading

- [HotSpot garbage collection tuning guide](https://docs.oracle.com/en/java/javase/21/gctuning/index.html)
- [JEP 248: G1 as the default collector](https://openjdk.org/jeps/248)
- [Unified JVM logging (\`-Xlog\`)](https://docs.oracle.com/en/java/javase/21/docs/specs/man/java.html#enable-logging-with-the-jvm-unified-logging-framework)
- [Eclipse Memory Analyzer](https://eclipse.dev/mat/) — for when a heap dump is the only way forward
`,M=`The notes end with a working mental model: reachability decides what dies,
generations make young collections cheap, and the log tells you what happened.
That is enough to reason about a program. It is not enough to diagnose a
production pause or choose a collector on purpose.

This is the layer underneath: how G1 actually works, what a safepoint costs, how
the low-latency collectors avoid stopping the world at all, and what to do when
the heap fills anyway.

## G1, properly

G1 does not have three fixed blocks. It splits the heap into **1024 to 2048
equal-sized regions**, between 1 MB and 32 MB each depending on heap size, and
labels each region by role:

![G1, properly](diagrams/garbage-collection-advanced-b6628eba.jpg)

The roles are **not contiguous**. Eden is wherever the Eden-labelled regions
happen to be, and a region changes role over its life. That is the design that
lets G1 target a pause time: it estimates how long collecting each region will
take, sorts by how much garbage each holds, and collects as many as fit in the
budget you asked for. Garbage first.

\`\`\`bash
java -XX:+UseG1GC -XX:MaxGCPauseMillis=100 -XX:G1HeapRegionSize=8m MyApp
\`\`\`

### Humongous objects

An object larger than **half a region** cannot be allocated normally. It becomes
a **humongous** allocation: a run of contiguous regions reserved for one object,
rounded up to a whole number of regions.

This is a real operational problem and it looks like a mystery. With 1 MB
regions, an array of 600 KB is humongous and occupies a full 1 MB region.
Allocate a lot of them and you fragment the heap into unusable gaps, then get
**humongous allocation failures** that trigger full GCs while the heap looks
half empty.

\`\`\`text
[gc] GC(41) Pause Young (Concurrent Start) (G1 Humongous Allocation) 812M->790M(1024M)
\`\`\`

Seeing \`G1 Humongous Allocation\` as a pause *cause* is the signal. The fixes, in
order of preference: allocate smaller buffers, or raise
\`-XX:G1HeapRegionSize\` so the objects stop being humongous.

![Humongous objects fragment the heap into unusable gaps](diagrams/garbage-collection-advanced-humongous.jpg)

### Remembered sets and the write barrier

To collect one region without scanning the whole heap, G1 must know which
references point **into** it from elsewhere. It keeps that in a per-region
**remembered set**.

Keeping remembered sets accurate means intercepting reference writes. Every
\`obj.field = other\` in your program compiles to the store **plus a write
barrier** — a few instructions that check whether the write crosses regions and,
if so, queue it for the remembered-set machinery.

That is the hidden cost of G1: a small tax on every reference write in the whole
application, plus the memory the remembered sets occupy, which on a
reference-dense heap can be several percent. It buys the ability to collect a
subset of regions, which is what makes pause targets possible.

### Concurrent marking, and mixed collections

Old regions are not collected by young pauses. They are dealt with by a
concurrent cycle:

1. **Initial mark** — piggybacked on a young pause, marks the roots.
2. **Concurrent mark** — walks the object graph *while the application runs*.
3. **Remark** — a short stop-the-world pause to finish up.
4. **Cleanup** — reclaims regions that turned out to be entirely garbage, for
   free, without evacuating anything.
5. **Mixed collections** — subsequent young pauses also collect the old regions
   with the most garbage.

The cycle starts when heap occupancy crosses **IHOP**, adaptive by default and
pinnable with \`-XX:InitiatingHeapOccupancyPercent=45\`.

![Which phases of a G1 concurrent cycle stop the world](diagrams/garbage-collection-advanced-concurrent-cycle.jpg)

Marking concurrently while the graph mutates needs an algorithm that cannot lose
a live object. G1 uses **SATB** — snapshot-at-the-beginning: the write barrier
records the *old* value of any overwritten reference, so anything reachable when
marking started stays marked. The cost is **floating garbage**: objects that
died during the cycle are not collected until the next one. That is a deliberate
trade — a little wasted space in exchange for never pausing to mark.

> If the concurrent cycle cannot finish before the heap fills, G1 falls back to a
> **full, single-threaded, stop-the-world** collection. \`Pause Full\` in the log
> of a G1 application is a failure, not a routine event. It means the collector
> lost the race, and the answer is usually a bigger heap or a lower IHOP.

## Safepoints

Almost every GC operation needs the application to stop at a point where the JVM
can walk the stacks and know what is a reference. Those points are
**safepoints**, and a stop-the-world pause has two parts:

![Safepoints](diagrams/garbage-collection-advanced-793305aa.jpg)

The GC log reports the second part. The first can be worse and does not show up
in naive monitoring. A thread only polls for safepoints at method returns and
loop back-edges — and a **counted loop over an \`int\`** historically had its poll
optimised away, so a long-running tight loop could hold every other thread
waiting for milliseconds.

\`\`\`bash
java -Xlog:safepoint MyApp
java -XX:+UnlockDiagnosticVMOptions -XX:+PrintSafepointStatistics MyApp
\`\`\`

Long time-to-safepoint is worth suspecting when pause *duration* looks fine but
latency does not. And note that GC is not the only cause: biased-lock revocation,
\`Thread.getAllStackTraces\`, class redefinition by an agent and heap dumps all run
at safepoints. A profiler that samples via \`getStackTrace\` is itself a safepoint
storm, which is why async profilers exist.

## The low-latency collectors

**ZGC** and **Shenandoah** take a different approach: do essentially everything
concurrently, including moving objects, so pause times stop scaling with heap
size at all. Sub-millisecond pauses on a terabyte heap are the claim, and it
holds.

Moving an object while the application is reading it requires the reader to be
intercepted. ZGC uses **coloured pointers** — spare bits in a 64-bit reference
carry marking and relocation state — plus a **load barrier** on every reference
read. If a load hits a reference to a moved object, the barrier forwards it and
fixes the reference in place. Shenandoah reaches the same end with a forwarding
pointer in each object header.

![G1 taxes every reference write, ZGC taxes every reference read](diagrams/garbage-collection-advanced-the-barrier.jpg)

| | Pause behaviour | Cost |
|---|---|---|
| **Serial** | Scales with heap | None; one thread; smallest footprint |
| **Parallel** | Scales with heap | Best raw throughput |
| **G1** | Targeted, tens of ms | Write barrier, remembered sets |
| **ZGC / Shenandoah** | Sub-millisecond, flat | Load/read barrier, ~10-15% throughput |

Generational ZGC — the default ZGC mode since Java 23 — recovers much of that
throughput by applying the same weak generational hypothesis G1 relies on.

**Choose deliberately.** A batch job that runs for four hours and nobody watches
should use Parallel: pauses are irrelevant and throughput is everything. A
request-serving API with a p99 latency target should use G1, or ZGC if the heap
is large. A 200 MB container running one small service is often best on Serial,
where the other collectors' bookkeeping costs more than it saves.

## The three references that are not strong

Reachability is not binary. Four strengths exist, and they behave differently
under memory pressure:

\`\`\`java References.java @run-garbage-collection-references
import java.lang.ref.PhantomReference;
import java.lang.ref.ReferenceQueue;
import java.lang.ref.SoftReference;
import java.lang.ref.WeakReference;
import java.util.ArrayList;
import java.util.List;

public class References {

    public static void main(String[] args) {
        byte[] one = new byte[1024 * 1024];
        byte[] two = new byte[1024 * 1024];
        byte[] three = new byte[1024 * 1024];

        ReferenceQueue<byte[]> queue = new ReferenceQueue<>();
        WeakReference<byte[]> weak = new WeakReference<>(one);
        SoftReference<byte[]> soft = new SoftReference<>(two);
        PhantomReference<byte[]> phantom = new PhantomReference<>(three, queue);

        System.out.println("phantom.get() is always null : " + (phantom.get() == null));

        one = null;     // drop every strong reference
        two = null;
        three = null;
        System.gc();

        System.out.println("weak survived a collection   : " + (weak.get() != null));
        System.out.println("soft survived a collection   : " + (soft.get() != null));
        System.out.println("phantom was queued           : " + (queue.poll() == phantom));

        // Now run the JVM genuinely short of memory. Catching OutOfMemoryError is
        // acceptable here only because that is the thing being demonstrated.
        try {
            List<byte[]> hog = new ArrayList<>();
            while (true) hog.add(new byte[8 * 1024 * 1024]);
        } catch (OutOfMemoryError expected) {
            System.out.println("soft cleared under pressure  : " + (soft.get() == null));
        }
    }
}
\`\`\`

\`\`\`output @run-garbage-collection-references
phantom.get() is always null : true
weak survived a collection    : false
soft survived a collection    : true
phantom was enqueued          : true
soft cleared under pressure   : true
\`\`\`

\`\`\`bash @run-refs
javac References.java
java -Xmx128m References
\`\`\`

\`\`\`output @run-refs
phantom.get() is always null : true
weak survived a collection    : false
soft survived a collection    : true
phantom was enqueued          : true
soft cleared under pressure   : true
\`\`\`

That is the whole hierarchy in five lines:

- **Strong** — an ordinary variable. Always prevents collection.
- **Weak** — cleared as soon as no strong reference remains. \`WeakHashMap\` is
  built on it. Use it to attach metadata to objects you do not own, so the entry
  disappears when the key does.
- **Soft** — cleared only when the JVM is short of memory. Intended for caches,
  though in practice a bounded cache with an explicit policy behaves far more
  predictably, because "short of memory" is decided by the collector and not by
  you.
- **Phantom** — never returns the object; it is enqueued *after* the object has
  been finalised and is unreachable. It exists for cleanup that must happen after
  collection, and \`Cleaner\` is the supported way to use it.

\`finalize()\` is deprecated for removal and should never be used: it can run late,
on any thread, or never, it can resurrect the object, and its mere presence adds
a whole extra collection cycle before the object can be reclaimed. Deterministic
cleanup belongs in \`AutoCloseable\` and try-with-resources; \`Cleaner\` is the
GC-triggered fallback.

## Diagnosing a real problem

Reach for these in order. Do not start by changing flags.

**1. Turn on logging, always.** In production, permanently:

\`\`\`bash
java -Xlog:gc*:file=gc.log:time,uptime,level,tags:filecount=5,filesize=20m MyApp
\`\`\`

Then read it with [GCEasy](https://gceasy.io/) or
[GCViewer](https://github.com/chewiebug/GCViewer) rather than by eye. What you
are looking for: is the *live* heap after each full collection trending upward?
That, not pause count, is the signature of a leak.

**2. Get a heap dump.** Either on failure or on demand:

\`\`\`bash
java -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/dumps MyApp

jcmd <pid> GC.heap_dump /dumps/live.hprof
jcmd <pid> GC.class_histogram
\`\`\`

**3. Open it in [Eclipse MAT](https://eclipse.dev/mat/).** Go straight to the
**dominator tree** — it answers "if this object were collected, how much memory
would come back?", which is the question you actually have. *Leak Suspects*
produces a usable answer in under a minute for most real leaks.

The distinction MAT teaches is **shallow versus retained** size. A \`HashMap\` with
a million entries has a small shallow size and an enormous retained size. Sorting
by retained size finds the cache nobody bounded.

**4. Only now, tune.** And tune three things at most:

| Flag | What it does |
|---|---|
| \`-Xmx\` | The ceiling. Set it explicitly for anything you deploy. |
| \`-XX:MaxGCPauseMillis\` | G1's pause target. It is a goal, not a guarantee. |
| \`-XX:+UseZGC\` etc. | Change collector, when the workload genuinely calls for it. |

Everything else — survivor ratios, tenuring thresholds, region sizes — is
adaptive by default and is usually made worse by hand. If a flag fixes something,
you should be able to say which log line proved it.

### In a container

Modern JVMs are container-aware and read the cgroup limit rather than the host's
memory. But the default heap is a *fraction* of that limit, and the rest of the
process — metaspace, thread stacks, JIT code cache, direct buffers — lives
outside \`-Xmx\`. A container killed by the OOM killer while the heap looks healthy
is almost always this.

\`\`\`bash
java -XX:MaxRAMPercentage=75 -XX:+ExitOnOutOfMemoryError -jar app.jar
\`\`\`

\`MaxRAMPercentage\` scales with the limit instead of hardcoding a number, and
\`ExitOnOutOfMemoryError\` makes the container die honestly rather than limping
along with a poisoned heap.

Use \`-XX:NativeMemoryTracking=summary\` plus \`jcmd <pid> VM.native_memory\` when
the heap is fine and the process still grows.

## When it goes wrong

- **\`Pause Full\` in a G1 log** — the concurrent cycle lost the race. Raise the
  heap or lower \`InitiatingHeapOccupancyPercent\`. Do not treat it as normal.
- **\`G1 Humongous Allocation\` as a pause cause** — objects larger than half a
  region. Allocate smaller buffers or raise \`G1HeapRegionSize\`.
- **\`OutOfMemoryError: GC overhead limit exceeded\`** — more than 98% of time in
  GC recovering under 2% of the heap. Effectively full; find the leak.
- **\`OutOfMemoryError: Metaspace\` with a near-empty heap** — unbounded dynamic
  class loading. Cap it with \`-XX:MaxMetaspaceSize\` so it fails early and
  visibly.
- **\`OutOfMemoryError: unable to create native thread\`** — nothing to do with the
  heap. Thread stacks are native memory; you have hit an OS or cgroup limit.
- **Pause times fine, latency terrible** — long time-to-safepoint. \`-Xlog:safepoint\`.
- **Container killed, heap healthy** — off-heap growth. Native memory tracking.
- **A performance change after a JDK upgrade** — the default collector or its
  ergonomics changed. \`-XX:+PrintFlagsFinal\` on both versions and diff them.

## Further reading

- [HotSpot GC Tuning Guide, JDK 21](https://docs.oracle.com/en/java/javase/21/gctuning/index.html)
- [JEP 333: ZGC](https://openjdk.org/jeps/333) and [JEP 439: Generational ZGC](https://openjdk.org/jeps/439)
- [Eclipse Memory Analyzer](https://eclipse.dev/mat/)
- [Unified JVM logging](https://docs.oracle.com/en/java/javase/21/docs/specs/man/java.html#enable-logging-with-the-jvm-unified-logging-framework)
`,ae=`Fourteen questions on the collector. The Easy set is asked of everyone; the
Medium set separates people who have read about GC from people who have watched a
log; the Hard set is asked when the role involves keeping something running.

The **Notes** cover Easy and most of Medium. The **Advanced** page covers the
rest.

## Easy

### 1. What does the garbage collector do, and what do you give up for it?

It reclaims heap memory occupied by objects your program can no longer reach, so
you never free memory by hand. That removes the whole family of manual
memory-management bugs — leaks, dangling pointers, double frees. What you give up
is control over *when* memory is released, and you accept pauses while the
collector works.

### 2. How does the collector decide an object is garbage?

By **reachability**, not reference counting. Starting from **GC roots** — local
variables and parameters on thread stacks, static fields, live threads, JNI
references — it follows every reference it can. Whatever it reaches is live;
everything else is garbage. A useful consequence: two objects referring to each
other are still collected, because no root reaches either. Reference-counting
collectors need a separate cycle detector for exactly this case.

### 3. Does \`System.gc()\` force a collection?

No. It is a request. It can be disabled outright with \`-XX:+DisableExplicitGC\`,
and when it is honoured it usually triggers a **full** GC, the most expensive
kind. It is legitimate in a demo that must prove an object was collected. It does
not belong in application code.

### 4. Does setting a variable to \`null\` help the garbage collector?

Almost never. The JVM already knows a local variable is dead after its last use,
and the compiled code will not keep the reference alive. It only matters for a
**long-lived field** holding something large well past its usefulness — a cache
entry, a field on a session object. Nulling a local before returning from a
method achieves nothing.

### 5. Why should you not use \`finalize()\`?

There is no guarantee it runs promptly, on a particular thread, or at all. It can
resurrect the object. Its mere presence forces an extra collection cycle before
the object can be reclaimed. It has been deprecated for removal since Java 9.
Deterministic cleanup belongs in \`AutoCloseable\` with try-with-resources;
\`Cleaner\` is the supported GC-triggered fallback.

## Medium

### 6. Why is the heap generational?

Because of the **weak generational hypothesis**: most objects die very young, and
those that survive tend to keep surviving. Splitting the heap lets the JVM
collect the small young generation frequently and cheaply rather than scanning
everything each time. The key detail is that a collection copies out the
**survivors** rather than deleting the dead — so a young collection where almost
everything died costs almost nothing. Garbage is free to collect; only surviving
costs.

### 7. Walk me through the life of an object.

It is allocated in **Eden**. When Eden fills, a minor GC copies whatever is still
reachable into a **survivor** space and declares all of Eden empty in one step.
Each subsequent young collection moves survivors to the *other* survivor space
and increments their age. Once the age crosses the tenuring threshold —
15 by default — the object is **promoted** to the old generation, which is
collected far more rarely and far more expensively.

### 8. What is stop-the-world, and does every collector do it?

It is a phase where the JVM suspends all application threads so the collector can
work on a stable object graph. Every collector has stop-the-world phases; the
difference is how long they last and how much work is moved into concurrent
phases running alongside the application. ZGC and Shenandoah do essentially
everything concurrently, including moving objects, so their pauses stay
sub-millisecond regardless of heap size.

### 9. Can a Java program leak memory? What does one look like?

Yes — not by forgetting to free, but by holding a reference to something no
longer useful. The canonical shapes: a \`static\` collection used as a cache with
no eviction, a listener registered and never removed, a \`ThreadLocal\` in a pooled
thread that outlives the request, and a long-lived object retaining every
short-lived one it has seen. The collector correctly refuses to reclaim reachable
objects, the heap fills, and you get \`OutOfMemoryError: Java heap space\`. The bug
is in your object graph.

### 10. \`OutOfMemoryError\` has several messages. What do they mean?

**\`Java heap space\`** — the heap is genuinely full of reachable objects.
**\`GC overhead limit exceeded\`** — over 98% of time spent collecting, recovering
under 2%; the polite version of the same thing. **\`Metaspace\`** — class metadata
exhausted, which lives in native memory outside the heap, so \`-Xmx\` will not
help. **\`unable to create native thread\`** — an OS or cgroup thread limit;
nothing to do with the heap at all. **\`Direct buffer memory\`** — off-heap
\`ByteBuffer\`s, bounded by \`-XX:MaxDirectMemorySize\`.

![Which OutOfMemoryError is the heap and which is not](diagrams/garbage-collection-interview-oom-boundary.jpg)

### 11. What is the default collector, and why that one?

**G1**, since Java 9. It divides the heap into a couple of thousand equal regions
rather than fixed generational blocks, labels each Eden, survivor or old, and
collects the regions holding the most garbage first — hence Garbage-First. That
gives a reasonable balance of throughput and predictable pauses for typical
server work, and it lets you state a pause target with \`-XX:MaxGCPauseMillis\`
that the collector will try to meet by choosing how many regions to include.

## Hard

### 12. How can G1 collect one region without scanning the whole heap?

Each region keeps a **remembered set** of references pointing into it from
elsewhere. Keeping those accurate requires a **write barrier**: every reference
store in your program is followed by a few instructions that check whether the
write crosses regions and queue it if so. That is G1's hidden cost — a tax on
every reference write, plus the memory the remembered sets occupy. What it buys
is the ability to collect a chosen subset of regions, which is what makes a pause
target possible at all.

### 13. What is a humongous allocation, and why does it cause trouble?

An object larger than **half a G1 region** cannot be allocated normally, so it is
given a run of contiguous regions rounded up to a whole number. With 1 MB
regions, a 600 KB array occupies a full region. Allocate many and the heap
fragments into unusable gaps, producing humongous allocation failures and full
GCs while the heap looks half empty. It shows in the log as
\`G1 Humongous Allocation\` as the *cause* of a pause. Fix by allocating smaller
buffers or raising \`-XX:G1HeapRegionSize\`.

### 14. What is a safepoint, and why can pause time look fine while latency is not?

A safepoint is a point in compiled code where the JVM can stop a thread and
reliably walk its stack. A stop-the-world pause has two parts: getting every
thread *to* a safepoint, and then doing the work. GC logs report the second.
Threads only poll at method returns and loop back-edges, so a long counted loop
can hold everything else waiting — time-to-safepoint that naive monitoring never
sees. \`-Xlog:safepoint\` shows it. GC is not the only trigger: heap dumps, class
redefinition, biased-lock revocation and \`Thread.getAllStackTraces\` all need
safepoints, which is why safepoint-based profilers distort what they measure.

### 15. Weak, soft, phantom — what is each for?

A **weak** reference is cleared as soon as no strong reference remains;
\`WeakHashMap\` is built on it, and it is right for attaching metadata to objects
you do not own. A **soft** reference is cleared only when the JVM is short of
memory, which is intended for caches — though a bounded cache with an explicit
policy behaves far more predictably, because "short of memory" is the collector's
judgement, not yours. A **phantom** reference never returns the object; it is
enqueued after collection as a hook for cleanup, and \`Cleaner\` is the supported
way to use it.

### 16. A service pauses for 400 ms every few minutes. How do you find out why?

Do not change flags first. Turn on \`-Xlog:gc*\` to a rotating file and read it
with a tool rather than by eye. Ask whether the *live* heap after each full
collection is trending upward — that, not pause count, is the signature of a
leak. If it is, take a heap dump with \`-XX:+HeapDumpOnOutOfMemoryError\` or
\`jcmd GC.heap_dump\`, open it in Eclipse MAT, and go to the **dominator tree**,
which answers "how much would come back if this went away". Sort by **retained**
size, not shallow. Only once the log or the dump has named a cause should a flag
change — and you should be able to point at the line that proved it.

![The order that finds a pause: log, then dump, then one flag](diagrams/garbage-collection-interview-find-the-cause.jpg)

## What to revise next

Question 12 is the one that most often exposes a memorised answer, because it
requires you to explain a mechanism rather than name a thing. Question 16 is the
one that gets asked as "tell me about a time" — have a real log or dump you can
describe.

If a role mentions latency or scale, the **Advanced** page's sections on
safepoints and the low-latency collectors are where the follow-ups come from.
`,oe=`The chapter in a page. If a line here is news, the **Notes** or **Advanced**
part is where it comes from.

## The one rule

- **Reachability.** Can the object still be reached, by following references,
  from a GC root? If not, it is garbage.
- GC roots: locals and parameters of running methods, \`static\` fields, live
  threads, native references.
- Age, size and whether you are "finished with it" are all irrelevant.
- **Reference cycles are fine.** Two objects pointing at each other are still
  collected, because no root reaches either.

## The heap

- **Weak generational hypothesis**: most objects die very young; survivors keep
  surviving.
- **Eden** → **survivor spaces** → **old generation**, promoted after ~15
  collections.
- **Metaspace** is not the heap. Class metadata, native memory, its own
  \`OutOfMemoryError\`.
- A collection copies out the **survivors** and declares the rest empty in one
  step. Garbage is free to collect; only surviving costs.

## The three kinds of pause

| | Cleans | Frequency | Pause |
|---|---|---|---|
| Minor / young | Young generation | Very often | Under a ms to a few ms |
| Major / old | Old generation | Rarely | Noticeably longer |
| Full | Whole heap + metaspace | Rarest | The spike on your graph |

**Stop-the-world** means every application thread is suspended. Every collector
does it; they differ in how long and how much runs concurrently.

## The collectors

![Throughput and pause time are the two ends of one choice](diagrams/garbage-collection-revision-the-trade.jpg)

- **Serial** — one thread, small heaps, containers.
- **Parallel** — best throughput, longest pauses. Right for batch jobs.
- **G1** *(default since Java 9)* — heap split into 1024–2048 equal **regions**,
  collects the ones with most garbage first. \`-XX:MaxGCPauseMillis\` is a goal.
- **ZGC / Shenandoah** — sub-millisecond pauses at any heap size, for ~10–15%
  throughput.

## G1 specifics

- Roles are per-region and change over time. Eden is wherever the Eden regions
  are.
- **Humongous** = larger than half a region, given whole regions. Too many
  fragment the heap — look for \`G1 Humongous Allocation\` as a pause *cause*.
- **Remembered sets** track references pointing into each region, maintained by a
  **write barrier** on every reference store. That is G1's hidden cost.
- **SATB** marking means objects that die mid-cycle survive to the next one —
  floating garbage, traded for never pausing to mark.
- \`Pause Full\` in a G1 log is a **failure**, not routine.

## Safepoints

- A pause is: get every thread to a safepoint, *then* do the work. Logs report
  only the second half.
- Threads poll at method returns and loop back-edges, so a long counted loop
  holds everything else waiting.
- \`-Xlog:safepoint\`. GC is not the only trigger — heap dumps, class redefinition
  and \`Thread.getAllStackTraces\` all need one.

## Reference strengths

| | Cleared when |
|---|---|
| **Strong** | Never — it is what keeps things alive |
| **Weak** | No strong reference remains. \`WeakHashMap\` |
| **Soft** | The JVM is short of memory. Intended for caches |
| **Phantom** | Never returns the object; enqueued after collection. Use \`Cleaner\` |

\`finalize()\` is deprecated for removal: may run late, on any thread, or never,
and adds a whole extra collection cycle. Use \`AutoCloseable\`.

## Leaks and errors

- A **Java memory leak** is an object still *reachable* but no longer *useful*:
  an unbounded static cache, an unremoved listener, a \`ThreadLocal\` in a pool.
- \`Java heap space\` — genuinely full of reachable objects.
- \`GC overhead limit exceeded\` — >98% of time collecting, <2% recovered.
- \`Metaspace\` — class metadata, outside the heap. \`-Xmx\` will not help.
- \`unable to create native thread\` — an OS or cgroup limit. Not the heap at all.

## Diagnosing, in order

![Live heap after each full GC: flat is healthy, climbing is a leak](diagrams/garbage-collection-revision-leak-signature.jpg)

1. \`-Xlog:gc*\` to a rotating file, always, in production.
2. Ask whether the **live heap after each full GC** is trending upward. That, not
   pause count, is the leak signature.
3. Heap dump → **Eclipse MAT** → **dominator tree**. Sort by **retained** size,
   not shallow.
4. Only then change a flag, and only \`-Xmx\`, \`-XX:MaxGCPauseMillis\`, or the
   collector. Everything else is adaptive and usually made worse by hand.

## Things worth unlearning

- \`System.gc()\` does not force anything, and usually triggers a full GC.
- Setting a **local** to \`null\` achieves nothing. A long-lived **field** is
  different.
- \`-Xmx\` is a ceiling, not a reservation.
- The heap is not all the memory Java uses — metaspace, thread stacks, JIT code
  cache and direct buffers all sit outside it, which is how a container gets
  killed while the heap looks healthy.
`,se=`Three initials, one letter apart, and every Java course throws them at you in the
first ten minutes without saying what they are for. Then you install "Java" from
a download page, type \`javac\`, and the terminal says it does not know that
command.

So start here. This is the piece everything else in this section stands on: what
happens between the file you type and the program that runs, and which of those
three things you actually need on your machine.

Nothing here assumes you know Java. If you have never written a line of it, this
is the right place to start.

## The promise, and how it is kept

Java's old slogan was **write once, run anywhere**. Most languages, before it,
compiled straight to machine code — instructions for one specific processor on
one specific operating system. Build on Windows, and the result would not run on
a Mac.

Java splits that in two:

![The promise, and how it is kept](diagrams/jdk-jre-jvm-notes-3ca1f715.jpg)

\`javac\` does **not** produce machine code. It produces **bytecode** — an
instruction set for a computer that does not exist, a made-up processor that
Java's designers invented. Then a real program, the **JVM**, reads that bytecode
and executes it on whatever machine it happens to be running on.

Every platform gets its own JVM. The bytecode never changes. That is the whole
trick: the portable part is the \`.class\` file, and the platform-specific part is
somebody else's problem.

## The three names

They are not three alternatives. They are three boxes, one inside the next:

![The three names](diagrams/jdk-jre-jvm-notes-cbd54c44.jpg)

| | Stands for | What is in it | You need it to |
|---|---|---|---|
| **JDK** | Java Development Kit | The JRE **plus** \`javac\`, \`javap\`, \`jar\`, the debugger | **write and compile** Java |
| **JRE** | Java Runtime Environment | The JVM **plus** the standard library | **run** Java that someone else compiled |
| **JVM** | Java Virtual Machine | The engine itself | **execute** bytecode |

One sentence to keep: **the JDK is for people who write Java, the JRE is for
people who only run it, and the JVM is the engine inside both.**

> Install the **JDK**. Always, as a learner. You need \`javac\`, and only the JDK
> has it. Since Java 11 there is no separate JRE download anyway — the JDK is the
> thing you get.

## Which JDK, and from where

"Java" as a download is a small mess of names. What you want:

- **[Eclipse Temurin](https://adoptium.net/)** — free, no account, no licence
  question to think about. This is the sensible default.
- Oracle's own JDK is fine for personal use but has had licence changes people
  remember badly. Amazon Corretto and Microsoft's build are equally fine.

Pick a **long-term support** version: **21** or **17**. LTS releases get years of
updates; the versions in between are fine to play with but are superseded every
six months.

Check what you have:

\`\`\`bash @check
java -version
javac -version
\`\`\`

Both should print a version, and the two should agree. On the machine these
guides were written on:

\`\`\`output @check
java version "26" 2026-03-17
Java(TM) SE Runtime Environment (build 26+35-2893)
Java HotSpot(TM) 64-Bit Server VM (build 26+35-2893, mixed mode, sharing)
javac 26
\`\`\`

If \`java\` works but \`javac\` does not, you have a runtime and not a kit — install
the JDK. If neither works, the installer did not put Java on your \`PATH\`; on
Windows the easiest fix is to reinstall and tick that box, and a **new** terminal
is required afterwards because \`PATH\` changes only reach shells opened later.

![PATH decides which java runs, JAVA_HOME decides what build tools use](diagrams/jdk-jre-jvm-notes-two-javas.jpg)

## Compile it, run it, look inside it

The smallest program that proves the whole chain works:

\`\`\`java Hello.java @run-jdk-jre-jvm-hello
public class Hello {
    public static void main(String[] args) {
        System.out.println("It compiled, and it ran.");
    }
}
\`\`\`

\`\`\`output @run-jdk-jre-jvm-hello
It compiled, and it ran.
\`\`\`

\`\`\`bash @run-hello
javac Hello.java
java Hello
\`\`\`

\`\`\`output @run-hello
It compiled, and it ran.
\`\`\`

Two things trip people up here, both worth saying plainly:

- **\`javac Hello.java\`** takes the file name, with the extension. It writes
  \`Hello.class\` next to it.
- **\`java Hello\`** takes the **class** name, with *no* extension. \`java
  Hello.class\` is a common first error and does not work.

The file name must match the public class inside it. \`public class Hello\` has to
live in \`Hello.java\`, or the compiler refuses.

![javac takes a file name, java takes a class name](diagrams/jdk-jre-jvm-notes-file-or-class.jpg)

Now look at what \`javac\` actually produced. \`javap\` ships with the JDK and prints
bytecode back as text:

\`\`\`bash @javap
javap -c Hello
\`\`\`

\`\`\`output @javap
Compiled from "Hello.java"
public class Hello {
  public Hello();
    Code:
         0: aload_0
         1: invokespecial #1                  // Method java/lang/Object."<init>":()V
         4: return

  public static void main(java.lang.String[]);
    Code:
         0: getstatic     #7                  // Field java/lang/System.out:Ljava/io/PrintStream;
         3: ldc           #13                 // String It compiled, and it ran.
         5: invokevirtual #15                 // Method java/io/PrintStream.println:(Ljava/lang/String;)V
         8: return
}
\`\`\`

That is the bytecode. \`getstatic\` fetches \`System.out\`, \`ldc\` loads the string
constant, \`invokevirtual\` calls \`println\`. It is not machine code and no
processor can run it — it is instructions for the imaginary machine the JVM
pretends to be.

You will never write this. It is worth seeing once, because it makes "the JVM
executes bytecode" stop being a sentence you repeat and start being a thing you
have looked at.

## What the JVM does while your program runs

The JVM is not a simple interpreter. Four jobs run the whole time:

**Class loading.** Classes are found and loaded *when first used*, not up front.
This is why a missing library gives you \`NoClassDefFoundError\` halfway through a
run rather than at startup.

**Verification.** Before running anything, the JVM checks the bytecode is
structurally valid and cannot corrupt memory. A hand-edited \`.class\` file gets
rejected here.

**Execution, and then compilation.** The JVM starts by *interpreting* bytecode,
one instruction at a time — quick to start, slow to run. While that happens it
counts how often each method is called. Methods that get hot are handed to the
**JIT** — the Just-In-Time compiler — which compiles them to real machine code
for the processor it is actually on.

**Memory management.** Objects are allocated on the heap and reclaimed by the
**garbage collector** when nothing refers to them any more. You never free
memory by hand. That is [its own subject](#/advanced/deep-gc), and
there is a whole piece on it here.

The JIT is the interesting one, because you can watch it happen.

## Watching the JIT warm up

Call the same method ten times and time each call. Nothing about the method
changes — but the machine underneath it does.

\`\`\`java HowJavaRuns.java
public class HowJavaRuns {

    /** Deliberately simple arithmetic, called a great many times. */
    static long work(int rounds) {
        long total = 0;
        for (int i = 1; i <= rounds; i++) {
            total += (i % 7) * (i % 13);
        }
        return total;
    }

    public static void main(String[] args) {
        System.out.println("running on : " + System.getProperty("java.vm.name"));
        System.out.println("version    : " + System.getProperty("java.version"));
        System.out.println("vendor     : " + System.getProperty("java.vendor"));
        System.out.println();
        System.out.println("same method, ten times over:");

        for (int round = 1; round <= 10; round++) {
            long start = System.nanoTime();
            work(3_000_000);
            long micros = (System.nanoTime() - start) / 1000;
            System.out.printf("  run %2d : %6d microseconds%n", round, micros);
        }
    }
}
\`\`\`

\`\`\`bash @run-jit
javac HowJavaRuns.java
java HowJavaRuns
\`\`\`

\`\`\`output @run-jit
running on : Java HotSpot(TM) 64-Bit Server VM
version    : 26
vendor     : Oracle Corporation

same method, ten times over:
  run  1 :   5763 microseconds
  run  2 :   4744 microseconds
  run  3 :   3388 microseconds
  run  4 :   3362 microseconds
  run  5 :   3401 microseconds
  run  6 :   3820 microseconds
  run  7 :   3596 microseconds
  run  8 :   3555 microseconds
  run  9 :   3579 microseconds
  run 10 :   3517 microseconds
\`\`\`

The first run is interpreted. By the third the JIT has compiled the method and it
is running roughly 40% faster, and it stays there. Your numbers will be
different — the shape will not be.

![The same method, interpreted first and compiled afterwards](diagrams/jdk-jre-jvm-notes-jit-warmup.jpg)

This is also the single biggest reason naive Java benchmarks lie. Timing
something once, cold, measures the interpreter. Real benchmarks warm the code up
first and then measure, which is what [JMH](https://github.com/openjdk/jmh)
exists to do properly.

> Try \`java -Xint HowJavaRuns\` to turn the JIT off entirely and force pure
> interpretation. The numbers get dramatically worse and stay flat — no warm-up,
> because there is nothing warming up. That contrast is the JIT's whole
> contribution, in one flag.

## Packages, and the folder problem

The moment you write more than one file, you will meet packages. A package is a
namespace, and it must match the folder layout:

\`\`\`java
package tools.text;      // must live in tools/text/Formatter.java

public class Formatter { ... }
\`\`\`

Compile and run from the folder **above** the package root:

\`\`\`bash
javac tools/text/Formatter.java
java tools.text.Formatter
\`\`\`

The run command uses dots, not slashes, and no \`.class\`. Getting
\`ClassNotFoundException\` here almost always means you ran it from the wrong
directory.

## When it goes wrong

- **\`'javac' is not recognized\` / \`command not found\`** — you have a runtime, not
  a JDK, or \`PATH\` was not updated. Install the JDK and open a **new** terminal.
- **\`error: class Hello is public, should be declared in a file named Hello.java\`** —
  the file name and the public class name must match exactly, capitals included.
- **\`Could not find or load main class Hello\`** — you probably typed
  \`java Hello.class\`, or you are in the wrong directory, or the class declares a
  \`package\` and you did not run it by its full dotted name.
- **\`UnsupportedClassVersionError: ... has been compiled by a more recent version\`** —
  the \`.class\` was built by a newer JDK than the \`java\` running it. Either
  upgrade the runtime or compile with \`javac --release 17 Hello.java\`.
- **Two Javas installed and the wrong one wins** — on Windows, \`where java\` shows
  which one \`PATH\` finds first; \`JAVA_HOME\` is what build tools read, and the two
  disagreeing is a classic afternoon lost.
- **\`Exception in thread "main" java.lang.NoClassDefFoundError\`** — the class
  compiled but is not on the classpath at run time. Check \`-cp\`.

## What to read next

The two things the JVM does that shape how you write code both have their own
pieces here:

- **[Stack, heap and the String Pool](#/advanced/deep-memory)** — where
  your variables actually live, and why \`==\` lies about Strings.
- **[How the garbage collector works](#/advanced/deep-gc)** — what
  happens to objects when you stop using them.

## Further reading

- [Eclipse Temurin downloads](https://adoptium.net/)
- [The Java Virtual Machine Specification](https://docs.oracle.com/javase/specs/jvms/se21/html/index.html)
- [\`javap\` documentation](https://docs.oracle.com/en/java/javase/21/docs/specs/man/javap.html)
`,ce=`The notes stop at "the JVM loads your class and runs it". That sentence hides
four mechanisms that decide how your program starts, how fast it gets, and what
happens when a dependency is missing. This is the layer underneath.

You do not need any of this to write Java. You need most of it to debug Java that
is behaving strangely, and interviewers past the junior level ask about all of
it.

## Class loading, and the delegation model

Classes are not loaded at startup. They are loaded **the first time they are
actively used** — the first \`new\`, the first static field read, the first static
method call. That laziness is why a missing library often surfaces twenty minutes
into a run rather than at launch.

Loading is done by class loaders arranged in a chain:

![Class loading, and the delegation model](diagrams/jdk-jre-jvm-advanced-5d798fcd.jpg)

The rule is **parent-first delegation**: before a loader looks for a class
itself, it asks its parent. So \`java.lang.String\` always resolves to the real one
in the JDK, no matter what a jar on your classpath calls itself. That is a
security property, not a convenience — without it, a jar could ship its own
\`java.lang.String\` and every program that loaded it would be compromised.

Two consequences worth knowing:

- **A class's identity is \`(name, class loader)\`, not just the name.** Two loaders
  can load the same bytes and produce two incompatible types. This is why an
  application server can run two versions of the same library, and also why you
  occasionally get the deeply confusing \`ClassCastException: com.acme.Thing
  cannot be cast to com.acme.Thing\`.
- **Web containers deliberately break parent-first.** Servlet containers use
  child-first for the web application's own classes so a war can override a
  library the container also ships. That inversion is the source of most
  "works standalone, fails in Tomcat" reports.

### The three phases

Loading a class runs **load → link → initialise**:

1. **Load** — find the bytes, produce a \`Class\` object.
2. **Link** — *verify* the bytecode is structurally safe, *prepare* static fields
   with default values (\`0\`, \`null\`, \`false\`), and *resolve* symbolic references,
   which the JVM is allowed to do lazily.
3. **Initialise** — run static initialisers and static field assignments, in
   source order, exactly once, thread-safely.

That last guarantee is doing real work. The JVM holds a lock per class during
initialisation, which is why the **static holder idiom** is a correct lazy
singleton with no synchronisation of your own:

\`\`\`java
public class Config {
    private Config() {}

    private static class Holder {
        static final Config INSTANCE = new Config();
    }

    public static Config get() {
        return Holder.INSTANCE;   // Holder loads on first call, once, safely
    }
}
\`\`\`

\`Holder\` is not initialised until \`Holder.INSTANCE\` is first read. The class
loader provides the thread safety. No \`volatile\`, no double-checked locking, no
way to get it wrong.

## Inside a \`.class\` file

A class file is a well-defined binary format, and it is smaller than people
expect:

\`\`\`text
magic          0xCAFEBABE          always, since 1995
minor/major    e.g. 65 = Java 21   what UnsupportedClassVersionError compares
constant pool  every string, class name, method signature, literal
access flags   public, final, abstract, interface...
this / super   indexes into the constant pool
interfaces     indexes
fields         name, descriptor, flags, attributes
methods        name, descriptor, flags, Code attribute (the bytecode)
attributes     SourceFile, LineNumberTable, Signature, annotations...
\`\`\`

The **constant pool** is the interesting part. Every method call in your code is
stored as a *symbolic* reference — a string naming the class, the method and its
descriptor — not an address. Resolution to something concrete happens at run
time, on first use. That indirection is what makes it possible to recompile one
class and not the ones that call it.

Look at any of it:

\`\`\`bash @javap-v
javap -v -p Hello
\`\`\`

\`\`\`output @javap-v
Classfile /C:/scratch/Hello.class
  Last modified 26 Jul 2026; size 428 bytes
  SHA-256 checksum 853d1229ba6ac2f0accdd8901bcc6448d4aac8c241cb1690a8954fd2bff5a4fc
  Compiled from "Hello.java"
public class Hello
  minor version: 0
  major version: 70
  flags: (0x0021) ACC_PUBLIC, ACC_SUPER
  this_class: #21                         // Hello
  super_class: #2                         // java/lang/Object
  interfaces: 0, fields: 0, methods: 2, attributes: 1
Constant pool:
   #1 = Methodref          #2.#3          // java/lang/Object."<init>":()V
   #2 = Class              #4             // java/lang/Object
   #3 = NameAndType        #5:#6          // "<init>":()V
   #4 = Utf8               java/lang/Object
   #5 = Utf8               <init>
   #6 = Utf8               ()V
   #7 = Fieldref           #8.#9          // java/lang/System.out:Ljava/io/PrintStream;
   #8 = Class              #10            // java/lang/System
   #9 = NameAndType        #11:#12        // out:Ljava/io/PrintStream;
  #10 = Utf8               java/lang/System
  #11 = Utf8               out
  #12 = Utf8               Ljava/io/PrintStream;
  #13 = String             #14            // It compiled, and it ran.
  #14 = Utf8               It compiled, and it ran.
  #15 = Methodref          #16.#17        // java/io/PrintStream.println:(Ljava/lang/String;)V
\`\`\`

Major version 70 is Java 26; 65 is Java 21, 61 is 17, 52 is Java 8. When you get
\`UnsupportedClassVersionError\`, the number in the message is this field, and
subtracting 44 gives you the Java version.

![A JVM runs its own class-file version and every older one, never a newer one](diagrams/jdk-jre-jvm-advanced-class-version.jpg)

### Method descriptors

The odd strings in \`javap\` output are **descriptors**, and reading them is a
five-minute skill worth having:

| Descriptor | Type |
|---|---|
| \`I\` \`J\` \`D\` \`F\` \`Z\` \`B\` \`C\` \`S\` | int, long, double, float, boolean, byte, char, short |
| \`V\` | void |
| \`Ljava/lang/String;\` | a reference type |
| \`[I\` | \`int[]\` |
| \`[[Ljava/lang/String;\` | \`String[][]\` |

So \`(Ljava/lang/String;I)V\` is \`void something(String, int)\`. Descriptors are why
**erasure** is visible: \`List<String>\` and \`List<Integer>\` both compile to
\`Ljava/util/List;\`, which is exactly why you cannot overload on them.

### The five invoke instructions

| Instruction | Used for |
|---|---|
| \`invokestatic\` | static methods |
| \`invokespecial\` | constructors, \`private\` methods, \`super.x()\` |
| \`invokevirtual\` | ordinary instance methods — dispatched on the runtime type |
| \`invokeinterface\` | calls through an interface reference |
| \`invokedynamic\` | lambdas, method references, string concatenation, records |

\`invokedynamic\` is the one worth knowing about. A lambda does **not** compile to
an anonymous inner class. It compiles to an \`invokedynamic\` whose first execution
calls a *bootstrap method* — \`LambdaMetafactory\` — which spins the implementing
class at run time and caches it. That is why lambdas do not produce a
\`MyClass$1.class\` file on disk, and why the first lambda in a process is
measurably slower than the rest.

Since Java 9, string concatenation uses \`invokedynamic\` too, via
\`StringConcatFactory\`, rather than the \`StringBuilder\` chain older compilers
emitted.

## The JIT, in detail

The notes showed the method getting faster. Here is what is happening.

HotSpot uses **tiered compilation** — two compilers and five levels:

\`\`\`text
level 0   interpreter                      counting invocations and loop backedges
level 1   C1, no profiling                 trivial methods, compile once, done
level 2   C1, limited profiling
level 3   C1, full profiling               the usual warm path
level 4   C2, fully optimising             the hot path
\`\`\`

A method starts interpreted. Cross a threshold and C1 compiles it quickly, with
instrumentation. Stay hot and C2 recompiles it using everything the
instrumentation learned. C2 is slow to compile and produces excellent code, which
is exactly the trade you want for the 1% of methods that run 99% of the time.

What C2 does with the profile is the part that has no ahead-of-time equivalent:

- **Inlining.** The single most valuable optimisation, because it enables all the
  others. Small hot methods disappear into their callers.
- **Monomorphic dispatch.** If a call site has only ever seen one receiver type,
  C2 compiles a direct call plus a type guard, rather than a virtual dispatch.
  This is why \`interface\`-heavy Java is not slow in practice — and why a call
  site that sees three or more types (*megamorphic*) genuinely is slower.
- **Escape analysis.** If an object provably never leaves the method, it need not
  be heap-allocated at all; its fields become registers. See the
  [memory notes](#/advanced/deep-memory/advanced) for what that does to allocation costs.
- **Loop unrolling, dead code elimination, constant folding**, and range-check
  elimination on array accesses it can prove are in bounds.

### Deoptimisation

Every one of those is **speculative**. C2 compiles code that is correct *given
what has happened so far*. When the assumption breaks — a second implementation
of that interface finally shows up, a branch never taken is taken — the JVM
**deoptimises**: it discards the compiled code, falls back to the interpreter
mid-method, and recompiles later with the wider profile.

That is why a long-running service can get *slower* after a deployment that
introduced a second implementation of a hot interface, and why benchmark numbers
from a five-second run are worthless.

Watch it happen:

\`\`\`bash
java -XX:+PrintCompilation HowJavaRuns
java -XX:+UnlockDiagnosticVMOptions -XX:+PrintInlining HowJavaRuns
\`\`\`

\`PrintCompilation\` marks deoptimisation with \`made not entrant\`. Compare against
\`-Xint\` (interpret only) and \`-XX:TieredStopAtLevel=1\` (C1 only) to see the
contribution of each tier.

> Never benchmark by timing a loop. Use [JMH](https://github.com/openjdk/jmh),
> which handles warm-up, dead-code elimination and constant folding — all three
> of which will otherwise make your microbenchmark measure nothing at all.

## Classpath, jars and the module path

A **jar** is a zip with a \`META-INF/MANIFEST.MF\`. The classpath is an ordered
list of places to look; **first match wins**, which is what "jar hell" means —
two jars containing the same class, and the winner decided by ordering you did
not choose.

\`\`\`bash
javac -d out $(find src -name '*.java')
jar --create --file app.jar --main-class Hello -C out .
java -jar app.jar
java -cp "app.jar;lib/*" Hello        # Windows uses ; — Unix uses :
\`\`\`

Note that \`-jar\` **ignores \`-cp\` entirely**. The manifest's \`Class-Path\` is the
only classpath a \`-jar\` launch respects, which surprises people regularly.

![The classpath is an ordered list and the first match wins](diagrams/jdk-jre-jvm-advanced-first-match-wins.jpg)

### Modules, briefly

Java 9 added a second, stricter mechanism. A module declares what it needs and
what it exposes:

\`\`\`java module-info.java
module com.acme.app {
    requires java.net.http;
    requires transitive com.acme.model;

    exports com.acme.app.api;
    opens com.acme.app.entity;      // reflection allowed, e.g. for a framework
}
\`\`\`

On the **module path**, anything not \`exports\`ed is genuinely inaccessible — not
merely discouraged. That is where \`InaccessibleObjectException\` comes from when a
framework reflects into a package that was never opened, and why \`--add-opens\`
exists as an escape hatch.

![exports, opens and neither — the three states of a package](diagrams/jdk-jre-jvm-advanced-module-access.jpg)

Most applications still run on the classpath, where everything lands in the
"unnamed module" and the old rules apply. The JDK itself is fully modularised
regardless, which is what makes \`jlink\` possible:

\`\`\`bash
jlink --add-modules java.base,java.net.http --output runtime --strip-debug
\`\`\`

That produces a runtime containing only the modules you named — tens of
megabytes instead of hundreds, which is why it matters for containers.

## Startup, and the two ways to shorten it

Class loading and JIT warm-up are both startup costs. Two mechanisms attack them:

**Class Data Sharing (CDS)** memory-maps a pre-parsed archive of class metadata,
shared between processes. The default archive covers the JDK's own classes; you
can add your application's:

\`\`\`bash
java -XX:ArchiveClassesAtExit=app.jsa -jar app.jar
java -XX:SharedArchiveFile=app.jsa -jar app.jar
\`\`\`

**Ahead-of-time compilation** sidesteps the JVM entirely — GraalVM Native Image
produces a native binary that starts in milliseconds. The cost is a closed world:
reflection, dynamic proxies and runtime class loading must all be declared up
front, which is why frameworks need explicit native-image support.

## When it goes wrong

- **\`ClassCastException: X cannot be cast to X\`** — two class loaders, two
  identities. Look for a duplicated jar on both the container's and the
  application's classpath.
- **\`NoSuchMethodError\`** — the class was found, but not the method signature the
  caller was compiled against. Classic version skew: compiled against v2, running
  with v1 on the classpath.
- **\`NoClassDefFoundError\` versus \`ClassNotFoundException\`** — the second means
  nobody could find the class. The first often means the class *was* found but
  its **static initialiser threw**, so the class is permanently marked
  unusable — the real exception is in the first occurrence, further up the log.
- **\`UnsupportedClassVersionError\`** — compiled by a newer JDK than the one
  running it. Compile with \`--release\`, and note that \`-source\`/\`-target\` without
  \`--release\` will happily let you link against APIs that do not exist in the
  older runtime.
- **\`InaccessibleObjectException\`** — a framework reflected into a package that
  was not \`opens\`ed. \`--add-opens java.base/java.lang=ALL-UNNAMED\` is the
  sledgehammer.
- **\`-jar\` ignoring your \`-cp\`** — as above. Use \`-cp app.jar:libs/*\` with an
  explicit main class instead.
- **A performance cliff after a deploy** — a new implementation of a hot
  interface turned a monomorphic call site megamorphic. \`-XX:+PrintInlining\`
  shows it.

## Further reading

- [The Java Virtual Machine Specification, SE 21](https://docs.oracle.com/javase/specs/jvms/se21/html/index.html)
- [JEP 341: Default CDS archives](https://openjdk.org/jeps/341)
- [JMH — the only correct way to microbenchmark Java](https://github.com/openjdk/jmh)
- [The Java Module System](https://openjdk.org/projects/jigsaw/spec/)
`,le=`Fourteen questions on how Java runs, in the order an interview tends to walk
them. Answer each one out loud before reading on — the gap between "I know this"
and "I can say this in two clean sentences" is exactly what the interview
measures.

The **Notes** cover everything in the Easy set. The **Advanced** page covers
everything in Medium and Hard.

## Easy

### 1. What is the difference between the JDK, the JRE and the JVM?

They are nested, not alternatives. The **JVM** is the engine that executes
bytecode and manages memory. The **JRE** is the JVM plus the standard library —
everything needed to *run* a Java program. The **JDK** is the JRE plus
development tools such as \`javac\` and \`javap\`, so it is what you need to *write*
Java. Install the JDK; it contains the other two. Since Java 11 there is no
separate JRE download at all.

### 2. What is bytecode, and why does it make Java portable?

\`javac\` does not produce machine code. It produces **bytecode** — instructions
for a specified virtual machine, stored in \`.class\` files. Every platform ships
its own JVM that understands the same bytecode, so one compiled artefact runs
anywhere without recompiling. The portable part is the \`.class\` file; the
platform-specific part is the JVM, which is somebody else's problem.

### 3. Why is \`main\` declared \`public static void main(String[] args)\`?

**\`static\`** because the JVM must call it before any object exists, so there is
no instance to call it on. **\`public\`** so the launcher can reach it from outside
the class. **\`void\`** because the exit status comes from \`System.exit\`, not from
a return value. **\`String[]\`** carries the command-line arguments. Since Java 25,
instance \`main\` methods in compact source files relax most of this for small
programs, but the classic form is what you will be asked about.

### 4. What is the difference between \`javac Hello.java\` and \`java Hello\`?

\`javac\` takes a **file name**, with the \`.java\` extension, and writes
\`Hello.class\`. \`java\` takes a **class name** — no extension, and dots rather than
slashes if the class is in a package. \`java Hello.class\` is the standard first
mistake and does not work.

### 5. What does \`PATH\` have to do with \`JAVA_HOME\`?

\`PATH\` decides which \`java\` and \`javac\` your shell finds. \`JAVA_HOME\` is a
convention read by build tools — Maven, Gradle, application servers — to locate a
JDK. They are set independently, so they can disagree, and a machine where
\`java -version\` says one thing and Gradle says another is almost always this.

## Medium

### 6. What does the JIT compiler actually do?

The JVM starts by **interpreting** bytecode, which is fast to start and slow to
run, while counting invocations and loop back-edges. Methods that cross a
threshold are compiled to native machine code — first by **C1**, quickly and with
profiling instrumentation, then by **C2** for the genuinely hot ones, using
everything that profiling learned. Because it compiles with runtime knowledge, it
can do things an ahead-of-time compiler cannot: inline aggressively, turn a call
site that has only ever seen one type into a direct call, and eliminate bounds
checks it can prove are unnecessary.

### 7. What is deoptimisation, and why does it exist?

Most JIT optimisations are **speculative** — correct given the behaviour observed
so far. When an assumption breaks, such as a second implementation of a hot
interface finally appearing, the JVM discards the compiled code, resumes in the
interpreter mid-method, and recompiles later with the wider profile. It exists
because speculation is what makes the optimisations possible; without a way to
back out, the JIT could only apply what it could prove statically.

![Speculative compilation, and the fall back to the interpreter](diagrams/jdk-jre-jvm-interview-deoptimise.jpg)

### 8. Why is timing a method in a loop a bad benchmark?

Because you are usually measuring the interpreter, the JIT compiling, or nothing
at all. A cold first run measures interpretation. A hot loop whose result is
unused gets deleted by dead-code elimination. A constant input gets folded away
entirely. Real benchmarks warm the code up, consume the result, and randomise
inputs — which is what JMH does, and why hand-rolled timing loops routinely
report impossible numbers.

### 9. Explain the parent-first delegation model, and why it matters.

Before a class loader searches for a class, it asks its parent, all the way up to
the bootstrap loader. So \`java.lang.String\` always resolves to the JDK's version
regardless of what is on the classpath. It is a **security** property: without
it, a malicious jar could supply its own \`java.lang.String\`. Servlet containers
deliberately invert it for web application classes so a war can override a
library the container also ships, which is the source of most "works standalone,
fails in Tomcat" bugs.

### 10. When is a class actually loaded and initialised?

**Loaded** lazily, on first active use. **Initialised** — static initialisers and
static field assignments run — on first \`new\`, first static method call, or first
non-constant static field access. Reading a \`static final\` compile-time constant
does *not* trigger initialisation, because the compiler inlined the value. The
JVM guarantees initialisation happens exactly once and is thread-safe, which is
what makes the static holder idiom a correct lazy singleton with no
synchronisation of your own.

![What triggers class initialisation and what quietly does not](diagrams/jdk-jre-jvm-interview-when-it-initialises.jpg)

### 11. What is the difference between \`ClassNotFoundException\` and \`NoClassDefFoundError\`?

\`ClassNotFoundException\` is checked and comes from an explicit lookup —
\`Class.forName\`, or a loader that genuinely could not find the bytes.
\`NoClassDefFoundError\` is an \`Error\` thrown by the JVM: the class was present at
compile time but is missing or unusable now. The trap is that it is also thrown
when a class's **static initialiser threw** — the class is marked permanently
unusable, and the real exception is the *first* occurrence, further up the log.

## Hard

### 12. How does a lambda compile, and why is it not an anonymous inner class?

It compiles to an **\`invokedynamic\`** instruction. On first execution the JVM
calls a bootstrap method — \`LambdaMetafactory\` — which generates the implementing
class at run time and caches the call site for subsequent calls. That is why
lambdas produce no \`MyClass$1.class\` on disk, why the first lambda in a process
is measurably slower than the rest, and why the strategy can change between JDK
versions without recompiling anything. Since Java 9, string concatenation uses
the same mechanism via \`StringConcatFactory\`.

### 13. Why does a class's identity include its class loader?

Because the runtime type is the pair \`(fully-qualified name, defining loader)\`.
Two loaders can load identical bytes and produce two types that are not
assignment-compatible. This is what lets an application server run two versions
of the same library in isolation — and what produces the notorious
\`ClassCastException: com.acme.Thing cannot be cast to com.acme.Thing\`, which
means exactly one thing: the same jar is visible through two loaders.

### 14. What does the module system change, and what breaks because of it?

On the **module path**, a package that is not \`exports\`ed is genuinely
inaccessible rather than merely discouraged, and reflection into a package
requires \`opens\`. That gives real encapsulation of internals and makes \`jlink\`
possible — a runtime image containing only the modules you declared. What breaks
is any framework that reflects into JDK internals: hence
\`InaccessibleObjectException\` and the \`--add-opens\` escape hatch. Applications on
the classpath land in the unnamed module and mostly keep the old behaviour, which
is why most code never noticed.

## What to revise next

If questions 6 to 8 were shaky, the JIT section of the **Advanced** page is
where they come from. If 9 to 11 were, read the class loading section — those
three are the same mechanism asked three ways, and interviewers know it.
`,N=`The chapter in a page. If a line here is news, the **Notes** or **Advanced**
part is where it comes from.

## The three names

- **JDK ⊃ JRE ⊃ JVM.** JDK to write, JRE to run, JVM to execute. Install the JDK.
- \`javac\` takes a **file** name; \`java\` takes a **class** name, dots not slashes,
  no extension.
- Bytecode is not machine code. It is instructions for a specified virtual
  machine, which is why one \`.class\` file runs everywhere.

## What the JVM does

- **Load** lazily, on first active use → **link** (verify, prepare, resolve) →
  **initialise** (static blocks, once, thread-safely).
- That per-class initialisation lock is why the **static holder idiom** is a
  correct lazy singleton with no synchronisation of your own.
- **Verify** rejects bytecode that could corrupt memory.
- **Execute** — interpret first, then JIT-compile what gets hot.
- **Collect** — reclaim unreachable objects.

## Class loading

- Chain: bootstrap → platform → application → yours. **Parent-first**: ask the
  parent before looking yourself.
- That is a security property. Without it a jar could ship its own
  \`java.lang.String\`.
- A class's identity is **\`(name, class loader)\`**. Two loaders, two
  incompatible types — which is what
  \`ClassCastException: com.acme.Thing cannot be cast to com.acme.Thing\` means.
- Servlet containers deliberately invert it to child-first, which is why things
  work standalone and fail in Tomcat.

![The same class loaded by two loaders is two incompatible types](diagrams/jdk-jre-jvm-revision-class-identity.jpg)

## Inside a \`.class\`

- \`0xCAFEBABE\`, then the version. **Major 65 = Java 21**, 61 = 17, 52 = 8;
  subtract 44 from the number in \`UnsupportedClassVersionError\`.
- The **constant pool** holds every name symbolically, resolved at run time. That
  indirection is why you can recompile one class and not its callers.
- Descriptors: \`I\` int, \`J\` long, \`Z\` boolean, \`V\` void, \`Ljava/lang/String;\` a
  reference, \`[I\` an int array. So \`(Ljava/lang/String;I)V\` is
  \`void f(String, int)\`.
- Five invokes: \`static\`, \`special\` (constructors, private, \`super\`), \`virtual\`,
  \`interface\`, **\`dynamic\`** — which is how lambdas and string concatenation
  work, and why lambdas produce no extra \`.class\` file.

## The JIT

- **Tiered**: interpreter → C1 with profiling → C2 fully optimising. Slow to
  compile, excellent output, applied only to the small hot fraction.
- **Inlining** is the enabler for everything else.
- A call site that has seen one type compiles to a direct call; one that has seen
  three or more is **megamorphic** and genuinely slower.
- Optimisations are **speculative**, so the JVM can **deoptimise** — throw the
  compiled code away and fall back — when an assumption breaks.
- Which is why timing a loop is not a benchmark. Use JMH.

![A call site gets slower as it sees more receiver types](diagrams/jdk-jre-jvm-revision-call-site-memory.jpg)

## Classpath and modules

- Classpath is ordered; **first match wins**. That is jar hell.
- \`-jar\` **ignores \`-cp\`**. Only the manifest's \`Class-Path\` applies.
- On the module path, a package that is not \`exports\`ed is genuinely
  inaccessible, and reflection needs \`opens\` — hence
  \`InaccessibleObjectException\` and \`--add-opens\`.
- \`jlink\` builds a runtime with only the modules you named.

## The errors, decoded

| Message | What it means |
|---|---|
| \`ClassNotFoundException\` | Nobody could find the class |
| \`NoClassDefFoundError\` | Found at compile time, missing now — **or its static initialiser threw** |
| \`NoSuchMethodError\` | Class found, signature not. Version skew |
| \`UnsupportedClassVersionError\` | Compiled by a newer JDK than is running it |
| \`ClassCastException: X to X\` | Same class, two loaders |
`,ue=`Two questions come up in almost every Java interview, and they are the same
question wearing different hats. *Is Java pass by value or pass by reference?*
And *why does \`==\` sometimes work on Strings and sometimes not?*

Both answers fall out of one picture: where your variables actually live while
the program runs. Get that picture right and a whole category of confusing
behaviour stops being confusing.

This follows on from [JDK, JRE and the JVM](#/advanced/deep-jvm), but it
stands on its own. You need to be able to compile and run a file; that is all.

## Two places, two rules

When the JVM runs your program it splits memory into areas. Two of them matter
now.

![Two places, two rules](diagrams/memory-notes-d5a15e37.jpg)

**The stack** holds local variables, method parameters, and *references* to
objects. It is organised as frames — one per method call, pushed when the method
is entered and thrown away when it returns. That is why a local variable
disappears when the method ends, and why runaway recursion gives you
\`StackOverflowError\`. It is fast, it is per-thread, and it is small.

**The heap** holds every object: everything made with \`new\`, every array, every
String. It is shared across the whole program, several references can point at
one object, and it is cleaned up by the garbage collector rather than by you.

The rule that follows:

> A **primitive** (\`int\`, \`double\`, \`boolean\`, \`char\`…) stores its value
> directly. An **object** variable stores a reference — the address of something
> on the heap.

Everything below is a consequence of that one line.

## Copying a primitive, copying a reference

\`\`\`java
int a = 10;
int b = a;      // copies the VALUE
b = 20;         // a is still 10

int[] x = { 1, 2, 3 };
int[] y = x;    // copies the REFERENCE
y[0] = 99;      // x[0] is now 99 as well
\`\`\`

Nothing was cloned in the second case. \`x\` and \`y\` are two names for one array.
This is not a special rule about arrays — it is true of every object. Assigning
an object never copies it.

## Pass by value, always

Here is the sentence that settles the interview question:

> **Java is always pass by value.** When you pass an object, the value being
> copied is the reference.

Which means a method can change what an object *contains*, but can never change
which object the caller's variable points at:

\`\`\`java
static void tryToChange(int number, int[] array, String text) {
    number = 999;      // changes the local copy — caller unaffected
    array[0] = 999;    // changes the shared object — caller sees it
    text = "changed";  // rebinds the local copy — caller unaffected
}
\`\`\`

\`array[0] = 999\` reaches through the reference and edits the object. \`array =
new int[]{999}\` would not: it would point the *local* copy of the reference at a
new array and leave the caller's alone.

![What a method can and cannot change in the caller](diagrams/memory-notes-pass-by-value.jpg)

## The String Pool

Strings get a memory area of their own, and it exists for one reason: programs
use the same short strings over and over, and storing a thousand copies of
\`"active"\` would be a waste.

So the JVM keeps a **String Pool**. When the compiler sees a literal like
\`"Java"\`, it checks the pool. If that text is already there, it reuses the
existing object rather than making a new one.

![The String Pool](diagrams/memory-notes-59bdc7c6.jpg)

\`new String("Java")\` deliberately opts out: it *always* creates a fresh object on
the heap, outside the pool. Which is exactly why you should not write it.
\`intern()\` goes the other way — it returns the pooled instance for any String.

## \`==\` versus \`.equals()\`

- **\`==\`** asks *are these the same object?* It compares references.
- **\`.equals()\`** asks *do these say the same thing?* For String it compares
  characters.

You want \`.equals()\` essentially always. \`==\` on Strings appears to work,
because the pool makes literals identical — and then quietly fails the day the
text arrives from a file, a database, or \`Scanner\`, because that String was built
at run time and was never pooled.

This is the most common real bug in beginner Java, and it is not a syntax error.
It compiles, it runs, and it returns the wrong answer.

## See all of it at once

\`\`\`java Memory.java @run-memory-memory
public class Memory {

    static void tryToChange(int number, int[] array, String text) {
        number = 999;      // changes the local copy only
        array[0] = 999;    // changes the object both names point at
        text = "changed";  // rebinds the local copy only
    }

    public static void main(String[] args) {

        int a = 10;
        int b = a;
        b = 20;
        System.out.println("a = " + a + ", b = " + b);

        int[] x = { 1, 2, 3 };
        int[] y = x;
        y[0] = 99;
        System.out.println("x[0] = " + x[0] + ", y[0] = " + y[0]);

        int number = 1;
        int[] array = { 1 };
        String text = "original";
        tryToChange(number, array, text);
        System.out.println("number = " + number + ", array[0] = " + array[0] + ", text = " + text);

        System.out.println();

        String s1 = "Java";
        String s2 = "Java";
        String s3 = new String("Java");
        String s4 = s3.intern();

        System.out.println("s1 == s2          : " + (s1 == s2));
        System.out.println("s1 == s3          : " + (s1 == s3));
        System.out.println("s1.equals(s3)     : " + s1.equals(s3));
        System.out.println("s1 == s4          : " + (s1 == s4));

        // Compile-time constants are folded and pooled; runtime work is not.
        String part = "Ja";
        String built = part + "va";
        final String constPart = "Ja";
        String folded = constPart + "va";

        System.out.println("s1 == built       : " + (s1 == built));
        System.out.println("s1 == folded      : " + (s1 == folded));
        System.out.println("s1 == \\"Ja\\" + \\"va\\" : " + (s1 == "Ja" + "va"));

        System.out.println();

        String greeting = "hello";
        String shouted = greeting.toUpperCase();
        System.out.println("greeting = " + greeting + ", shouted = " + shouted);

        StringBuilder sb = new StringBuilder("hello");
        sb.append(" world").insert(0, ">> ");
        System.out.println("builder  = " + sb);
    }
}
\`\`\`

\`\`\`output @run-memory-memory
a = 10, b = 20
x[0] = 99, y[0] = 99
number = 1, array[0] = 999, text = original

s1 == s2          : true
s1 == s3          : false
s1.equals(s3)     : true
s1 == s4          : true
s1 == built       : false
s1 == folded      : true
s1 == "Ja" + "va" : true

greeting = hello, shouted = HELLO
builder  = >> hello world
\`\`\`

\`\`\`bash @run-memory
javac Memory.java
java Memory
\`\`\`

\`\`\`output @run-memory
a = 10, b = 20
x[0] = 99, y[0] = 99
number = 1, array[0] = 999, text = original

s1 == s2          : true
s1 == s3          : false
s1.equals(s3)     : true
s1 == s4          : true
s1 == built       : false
s1 == folded      : true
s1 == "Ja" + "va" : true

greeting = hello, shouted = HELLO
builder  = >> hello world
\`\`\`

Every line is one of the rules above, made visible. Three deserve a note.

**\`s1 == built\` is false.** \`part + "va"\` is concatenation performed at *run*
time. The result is a brand-new object that never entered the pool, even though
the characters are identical. This is the trap in a nutshell.

**\`s1 == folded\` is true.** \`constPart\` is \`final\` and initialised from a
literal, so the compiler can work the answer out while compiling and fold it into
a pooled literal. Same for \`"Ja" + "va"\`, which is two literals and no variables
at all. Whether \`==\` succeeds depends on what the *compiler* could figure out —
which is a fine reason never to rely on it.

![Whether a concatenation is pooled is decided at compile time](diagrams/memory-notes-constant-folding.jpg)

**\`text\` is still \`"original"\`.** The method reassigned its own copy of the
reference. The caller never saw it.

## Immutability, and why it is a feature

A String cannot be changed after it is created. \`toUpperCase()\` does not shout at
the original; it returns a new String and leaves the old one alone. So does
\`trim()\`, \`replace()\`, \`substring()\` and the rest.

That is what makes the pool safe in the first place — if \`s1\` could be edited in
place, and \`s2\` shared the same object, editing one would silently change the
other. Immutability also makes Strings safe to use as \`HashMap\` keys and safe to
share between threads without locking.

The cost shows up in loops:

\`\`\`java
// O(n²): every += builds a whole new String
String result = "";
for (String word : words) result += word;

// O(n): one buffer, appended to
StringBuilder sb = new StringBuilder();
for (String word : words) sb.append(word);
String result = sb.toString();
\`\`\`

For a handful of items nobody cares. For ten thousand, the first version is the
difference between instant and noticeably slow. A single \`a + b + c\` outside a
loop is fine — the compiler turns it into a builder for you.

![The quadratic cost of += inside a loop](diagrams/memory-notes-concat-cost.jpg)

## Where this leads

The heap is also where the garbage collector does its work — once nothing on the
stack refers to an object any more, it becomes eligible for collection. That is
the subject of the next piece:
**[how the garbage collector works](#/advanced/deep-gc)**.

## When it goes wrong

- **Two Strings look identical but \`==\` is false** — one of them was built at run
  time. Use \`.equals()\`. This is not a bug to work around with \`intern()\`.
- **\`NullPointerException\` comparing Strings** — \`input.equals("yes")\` explodes
  when \`input\` is null. Write \`"yes".equals(input)\`, or use
  \`Objects.equals(a, b)\`.
- **\`StackOverflowError\`** — recursion with no base case, or one that is never
  reached. The heap is not involved; \`-Xss\` resizes the stack but rarely fixes
  the actual problem.
- **\`equals()\` works but the object vanishes from a \`HashSet\`** — you overrode
  \`equals\` and not \`hashCode\`. The two must always be overridden together.
- **String comparison ignoring case fails on some inputs** —
  \`equalsIgnoreCase\` handles the common cases; locale-sensitive text (Turkish
  dotted \`i\` is the classic) needs \`toLowerCase(Locale.ROOT)\` first.
- **A huge String is not being collected** — before Java 7u6, \`substring()\`
  shared the parent's character array, so a small substring pinned a large
  String. Modern versions copy, so this is history, but it still turns up in
  interview questions.

## Further reading

- [\`String\` API documentation](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/String.html)
- [JLS §3.10.5: String literals and interning](https://docs.oracle.com/javase/specs/jls/se21/html/jls-3.html#jls-3.10.5)
- [\`StringBuilder\` versus \`StringBuffer\`](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/StringBuilder.html)
`,de=`The notes gave you the two-box picture: primitives on the stack, objects on the\r
heap, Strings pooled. Every one of those is a simplification, and the places\r
where it leaks are exactly where the confusing bugs live.\r
\r
This is the layer underneath: what an object actually costs, why allocation is\r
nearly free, why \`==\` on boxed integers works up to 127 and then stops, and what\r
a String really looks like in memory.\r
\r
## What an object costs\r
\r
An object is not just its fields. On a 64-bit HotSpot JVM:\r
\r
![What an object costs](diagrams/memory-advanced-09624b6a.jpg)\r
\r
So \`new Object()\` is **16 bytes**, not zero. An \`Integer\` holding one \`int\` is\r
16 bytes plus a 4-byte reference to reach it — five times the cost of the \`int\`\r
it wraps. A \`Long\` is 24. This is the entire argument for primitive arrays over\r
\`List<Integer>\` in anything numeric, and the reason Project Valhalla exists.\r

![A million ints against a million boxed Integers](diagrams/memory-advanced-million-ints.jpg)
\r
The **mark word** is doing several jobs at once: it stores the identity hash code\r
once one has been requested, the GC age used for promotion, and the object's lock\r
state. That overloading is why calling \`hashCode()\` on an object can interfere\r
with biased locking, and why \`System.identityHashCode\` is not free the first\r
time.\r
\r
**Compressed oops** are why a 4-byte class pointer works: below a 32 GB heap the\r
JVM stores references as 32-bit offsets scaled by 8. Cross 32 GB and every\r
reference doubles in width, so a 33 GB heap can hold *less* live data than a 31 GB\r
one. If you are sizing a heap near that line, stay under it.\r
\r
## Why allocation is nearly free\r
\r
Allocating on a shared heap should require a lock. It does not, because each\r
thread gets a **Thread-Local Allocation Buffer** — a private slab of Eden. A\r
\`new\` inside it is a pointer bump and a bounds check: a handful of instructions,\r
no synchronisation.\r
\r
![Why allocation is nearly free](diagrams/memory-advanced-26171445.jpg)\r
\r
A thread that fills its TLAB gets another. Only that hand-out is synchronised.\r
The practical consequence: **short-lived objects are cheap**, and the "don't\r
allocate in a loop" advice inherited from C++ is mostly wrong in Java. What is\r
expensive is objects that *survive* — those get copied by the collector, possibly\r
several times, and eventually promoted.\r
\r
\`-XX:+PrintTLAB\` shows the sizes if you are curious.\r
\r
### Escape analysis, and objects that are never allocated\r
\r
C2 goes further. If it can prove an object never escapes the method that created\r
it, it does not allocate it at all — a transformation called **scalar\r
replacement**, where the object's fields become local variables in registers.\r
\r
\`\`\`java\r
// The Point never escapes. C2 may compile this to plain arithmetic\r
// on two ints, with no allocation and nothing for the GC to collect.\r
static double distance(int x1, int y1, int x2, int y2) {\r
    Point a = new Point(x1, y1);\r
    Point b = new Point(x2, y2);\r
    return Math.hypot(a.x() - b.x(), a.y() - b.y());\r
}\r
\`\`\`\r
\r
Escape analysis only runs on **inlined** code, so it depends on the whole call\r
chain being small enough to inline. It is not something to rely on — but it is\r
why "wrapping values in a small object" is often free in practice, and why\r
measuring beats assuming.\r

![Escape analysis deciding between the heap and no allocation at all](diagrams/memory-advanced-escape-analysis.jpg)
\r
\`\`\`bash\r
java -XX:+UnlockDiagnosticVMOptions -XX:+PrintEscapeAnalysis MyApp\r
java -XX:-DoEscapeAnalysis MyApp        # turn it off and compare\r
\`\`\`\r
\r
## The boxing cache\r
\r
\`Integer.valueOf\` does not always create an object. Values from **-128 to 127**\r
come from a cache built at class initialisation, so identical small values are\r
the same object — and \`==\` appears to work.\r
\r
\`\`\`java Boxing.java @run-memory-boxing
public class Boxing {\r
    public static void main(String[] args) {\r
\r
        Integer a = 127, b = 127;\r
        Integer c = 128, d = 128;\r
\r
        System.out.println("127 == 127 : " + (a == b));\r
        System.out.println("128 == 128 : " + (c == d));\r
        System.out.println("128 equals : " + c.equals(d));\r
\r
        Integer present = 42;\r
        int primitive = 42;\r
        System.out.println("mixed  ==  : " + (present == primitive));\r
\r
        System.out.println("Long  100  : " + (Long.valueOf(100) == Long.valueOf(100)));\r
        System.out.println("Long  1000 : " + (Long.valueOf(1000) == Long.valueOf(1000)));\r
\r
        String s = "a fairly long string to hash";\r
        long first = time(s);\r
        long second = time(s);\r
        System.out.println("hash cached: " + (second <= first));\r
\r
        System.out.println("latin1 len : " + "hello".length());\r
        System.out.println("utf16 len  : " + "hello 世界".length());\r
    }\r
\r
    static long time(String s) {\r
        long start = System.nanoTime();\r
        for (int i = 0; i < 1000; i++) s.hashCode();\r
        return System.nanoTime() - start;\r
    }\r
}\r
\`\`\`

\`\`\`output @run-memory-boxing
127 == 127 : true
128 == 128 : false
128 equals : true
mixed  ==  : true
Long  100  : true
Long  1000 : false
hash cached: true
latin1 len : 5
utf16 len  : 8
\`\`\`\r
\r
\`\`\`bash @run-boxing\r
javac Boxing.java\r
java Boxing\r
\`\`\`\r
\r
\`\`\`output @run-boxing\r
127 == 127 : true\r
128 == 128 : false\r
128 equals : true\r
mixed  ==  : true\r
Long  100  : true\r
Long  1000 : false\r
hash cached: true\r
latin1 len : 5\r
utf16 len  : 8\r
\`\`\`\r
\r
Three things in there are worth pulling out.\r
\r
**Lines one and two are the same code with a different number.** A comparison\r
that passes every test and fails in production the day an id exceeds 127 is a\r
genuinely nasty bug, and it is entirely created by \`==\` on boxed types. Use\r
\`.equals()\`, or compare \`int\` values.\r
\r
**\`mixed ==\` is true** because comparing an \`Integer\` with an \`int\` **unboxes**\r
the object rather than boxing the primitive. Convenient — until the \`Integer\` is\r
\`null\`, at which point the same line throws \`NullPointerException\` with no \`null\`\r
visible anywhere in the source. This is the single most common surprise in\r
Java's autoboxing.\r
\r
**The upper bound is configurable.** \`-XX:AutoBoxCacheMax=1000\` raises it, which\r
means code that "works" because of the cache can behave differently on a\r
differently-configured JVM. Another reason not to depend on it.\r
\r
## What a String really is\r
\r
Since Java 9, \`String\` does not hold a \`char[]\`. It holds a \`byte[]\` plus a\r
one-byte coder flag:\r
\r
\`\`\`java\r
private final byte[] value;\r
private final byte coder;      // 0 = LATIN1, 1 = UTF16\r
private int hash;              // cached, computed lazily\r
\`\`\`\r
\r
That is **compact strings**, and for typical Latin-1 text it halves the memory a\r
String occupies. The output above shows the flip side: \`"hello 世界"\` reports\r
length 8 because \`length()\` counts UTF-16 code units, not characters. Anything\r
outside the Basic Multilingual Plane — emoji, most notably — occupies **two** code\r
units, so \`"👍".length()\` is 2 and \`charAt(0)\` returns half a character. Use\r
\`codePointCount\` and \`codePoints()\` when correctness matters.\r

![One emoji taking two UTF-16 code units](diagrams/memory-advanced-code-units.jpg)
\r
\`hash\` being a non-final cached field is a deliberate benign data race: two\r
threads computing it concurrently produce the same value, so no synchronisation\r
is needed. It also means the first \`hashCode()\` on a long String is O(n) and\r
every subsequent one is O(1) — visible in the demo above.\r
\r
### The pool is a native hash table\r
\r
The String Pool is not a normal Java collection. It is the **StringTable**, a\r
fixed-bucket hash table in native memory holding references to String objects on\r
the heap. Two flags matter if you use \`intern()\` heavily:\r
\r
\`\`\`bash\r
java -XX:+PrintStringTableStatistics MyApp\r
java -XX:StringTableSize=1000003 MyApp     # a prime, default is 65536\r
\`\`\`\r
\r
An overloaded StringTable degrades to linked-list scans in each bucket, and\r
\`intern()\` — which is a native call — gets progressively slower. Before Java 7 the\r
pool lived in PermGen and interning too much would exhaust it outright; it moved\r
to the heap in 7, which is why \`intern()\` stopped being actively dangerous and\r
became merely a bad default.\r
\r
**String deduplication** is the modern alternative and it needs no code change.\r
G1 can spot Strings with identical contents during collection and point them at\r
one shared \`byte[]\`:\r
\r
\`\`\`bash\r
java -XX:+UseStringDeduplication -Xlog:stringdedup MyApp\r
\`\`\`\r
\r
Applications that read a lot of repeated text — parsers, log processors — often\r
find several percent of the heap here.\r
\r
## Concatenation, and what the compiler emits\r
\r
\`a + b\` on Strings has not compiled to a \`StringBuilder\` chain since Java 9. It\r
compiles to an **\`invokedynamic\`** that calls \`StringConcatFactory\`, which\r
generates a specialised concatenation method at run time — usually one that sizes\r
the byte array exactly once and fills it, beating what a \`StringBuilder\` chain\r
would do.\r
\r
Which means:\r
\r
\`\`\`java\r
// Fine. One invokedynamic, one allocation.\r
String message = "user " + id + " logged in at " + time;\r
\r
// Not fine. n invokedynamics, n intermediate Strings, O(n²) copying.\r
String all = "";\r
for (String line : lines) all += line;\r
\r
// Correct. One buffer, grown geometrically.\r
StringBuilder sb = new StringBuilder();\r
for (String line : lines) sb.append(line);\r
\`\`\`\r
\r
The rule is not "never use \`+\`". It is "never use \`+\` **in a loop**", and the\r
reason is that the loop makes the intermediate results survive.\r
\r
\`StringBuilder\` itself starts at 16 characters and doubles, copying on each\r
growth. If you know roughly how big the result is, \`new StringBuilder(4096)\`\r
removes those copies. \`StringBuffer\` is the synchronised version and is almost\r
never the right answer — a buffer shared between threads has bigger design\r
problems than its own locking.\r
\r
## Immutability, defensive copies and \`final\`\r
\r
\`final\` on a field prevents **reassignment**, not mutation:\r
\r
\`\`\`java\r
final List<String> names = new ArrayList<>();\r
names.add("ada");           // perfectly legal\r
names = new ArrayList<>();  // compile error\r
\`\`\`\r
\r
Real immutability needs four things: final fields, immutable field *types*, no\r
setters, and defensive copies of anything mutable that crosses the boundary in\r
either direction.\r
\r
\`\`\`java\r
public final class Order {\r
    private final List<Item> items;\r
\r
    public Order(List<Item> items) {\r
        this.items = List.copyOf(items);          // copy in\r
    }\r
\r
    public List<Item> items() {\r
        return items;                             // already unmodifiable\r
    }\r
}\r
\`\`\`\r
\r
A \`record\` gets you final fields and no setters for free, but **not** deep\r
immutability — \`record Order(List<Item> items)\` still hands out a reference to a\r
mutable list. If that matters, add a compact constructor that copies:\r
\r
\`\`\`java\r
record Order(List<Item> items) {\r
    Order {\r
        items = List.copyOf(items);\r
    }\r
}\r
\`\`\`\r
\r
There is one more guarantee worth knowing: **final fields are safely published**.\r
The JVM's memory model promises that a thread which sees a reference to a\r
correctly-constructed object also sees its final fields fully initialised, with\r
no synchronisation. Non-final fields carry no such promise — which is precisely\r
why the broken double-checked locking singleton is broken.\r
\r
## When it goes wrong\r
\r
- **\`NullPointerException\` on a line with no \`null\` in it** — autoboxing. A\r
  \`Integer\` compared to an \`int\`, or assigned to an \`int\`, unboxes and throws.\r
  Very common with \`Map.get\` returning \`null\` for a missing key.\r
- **A comparison that works in tests and fails in production** — \`==\` on boxed\r
  values, and the production id went past 127.\r
- **\`OutOfMemoryError\` from a \`HashMap\` that should be small** — keys that\r
  override \`equals\` but not \`hashCode\`, so every put lands in its own entry and\r
  nothing is ever found or replaced.\r
- **\`String.length()\` disagreeing with what the user typed** — emoji and other\r
  supplementary characters take two UTF-16 code units. Use \`codePointCount\`.\r
- **\`substring\` retaining a huge parent String** — true before Java 7u6, where\r
  substrings shared the parent's array. Fixed since, but still asked about.\r
- **Heap dominated by \`char[]\`/\`byte[]\`** — duplicated Strings. Turn on\r
  \`-XX:+UseStringDeduplication\` before reaching for \`intern()\`.\r
- **A 33 GB heap performing worse than 31 GB** — compressed oops switched off.\r
  Every reference is now 8 bytes.\r
\r
## Further reading\r
\r
- [JEP 254: Compact Strings](https://openjdk.org/jeps/254)\r
- [JEP 280: Indified String Concatenation](https://openjdk.org/jeps/280)\r
- [JOL — Java Object Layout, for measuring any of this](https://openjdk.org/projects/code-tools/jol/)\r
- [\`Integer.valueOf\` and the cache, JLS §5.1.7](https://docs.oracle.com/javase/specs/jls/se21/html/jls-5.html#jls-5.1.7)\r
`,fe=`Fourteen questions on where values live and what they cost. This topic produces
more interview questions per page than any other in Java, because almost every
confusing behaviour in the language traces back to one of them.

The **Notes** cover the Easy set. The **Advanced** page covers Medium and Hard.

## Easy

### 1. What is the difference between the stack and the heap?

The **stack** holds local variables, method parameters and references, organised
as one frame per method call and discarded when the method returns. It is
per-thread, fast, and small — overflow it and you get \`StackOverflowError\`. The
**heap** holds every object created with \`new\`, is shared across the whole
program, and is reclaimed by the garbage collector. Primitives are stored by
value; objects are stored on the heap and reached through references.

![Frames piling up until the stack runs out](diagrams/memory-interview-stack-depth.jpg)

### 2. What is the difference between a primitive and a reference type?

A primitive stores its value directly. A reference type stores the address of an
object on the heap. Copying a primitive copies the value, so the copies are
independent. Copying a reference copies the address, so both names refer to one
object — which is why \`int[] y = x; y[0] = 99;\` changes \`x[0]\` too.

### 3. Is Java pass by value or pass by reference?

**Always pass by value.** When you pass an object, the value copied is the
reference. So a method can change what an object *contains* — \`list.add(...)\`,
\`array[0] = 9\` — and the caller sees it. But reassigning the parameter itself
only rebinds the method's own copy, and the caller sees nothing.

### 4. What is the String Pool, and why does it exist?

A region where the JVM stores String literals so identical text is stored once.
When the compiler sees \`"Java"\` it reuses the pooled object if it already exists.
It exists because programs use the same short strings constantly and storing a
thousand copies of \`"active"\` would be waste. It is why two identical literals
are \`==\`, and why \`new String("Java")\` — which deliberately allocates outside the
pool — is not.

### 5. What is the difference between \`==\` and \`.equals()\`?

\`==\` compares references: *is this the same object?* \`.equals()\` compares content
as the class defines it. For Strings, two variables can hold identical text in
different objects, so \`==\` returns false while \`.equals()\` returns true. Use
\`.equals()\` for value comparison; \`==\` is correct for primitives and for
deliberate identity checks.

### 6. Why are Strings immutable?

Because immutability is what makes the pool safe — if one variable could edit a
shared String in place, every other variable pointing at it would change too. It
also makes Strings safe as \`HashMap\` keys, because the hash cannot go stale, and
safe to share across threads without locking, and safe to pass to a method that
might keep a reference.

## Medium

### 7. Why does \`Integer a = 127, b = 127; a == b\` return true, but 128 return false?

\`Integer.valueOf\` returns cached instances for **-128 to 127**, so small
identical values really are the same object and \`==\` succeeds. Above the cache,
each boxing allocates, so \`==\` compares two different objects and fails. The
upper bound is even tunable with \`-XX:AutoBoxCacheMax\`. This is why \`==\` on boxed
types is a bug that passes every test and fails the day an id exceeds 127.

### 8. How can a line with no \`null\` in it throw \`NullPointerException\`?

Autoboxing. Comparing an \`Integer\` to an \`int\`, or assigning one to an \`int\`,
**unboxes** the object by calling \`intValue()\` — and that call throws if the
reference is null. \`if (map.get(key) == someInt)\` is the classic: a missing key
returns \`null\`, the unboxing throws, and nothing in the source looks like a
dereference.

### 9. Why is \`"Ja" + "va" == "Java"\` true but \`part + "va" == "Java"\` false?

Constant folding. Two literals, or a \`final\` variable initialised from a literal,
can be evaluated by the **compiler** and folded into a single pooled literal, so
\`==\` succeeds. Concatenation involving a non-constant variable happens at **run**
time and produces a fresh object that never entered the pool, so \`==\` fails. That
the answer depends on what the compiler could work out is a fine reason never to
rely on it.

### 10. What is the \`equals\` / \`hashCode\` contract, and what breaks if you ignore it?

If two objects are \`equals\`, they must return the same \`hashCode\`. Unequal
objects *may* collide. Override \`equals\` without \`hashCode\` and hash-based
collections stop working: you \`put\` an object into a \`HashMap\`, look it up with
an equal object, land in a different bucket and get \`null\` — the entry is not
lost, it is unreachable. The same object also appears twice in a \`HashSet\`.
Records generate both correctly.

![An equal key landing in a different bucket and finding nothing](diagrams/memory-interview-hashcode-contract.jpg)

### 11. When would you use \`StringBuilder\` over \`+\`?

**In a loop.** Outside a loop, \`a + b + c\` compiles to a single \`invokedynamic\`
that sizes the result once — since Java 9 it often beats a hand-written
\`StringBuilder\`. Inside a loop, \`result += x\` allocates a new String every
iteration and copies everything so far, turning O(n) work into O(n²).
\`StringBuilder\` keeps one buffer and grows it geometrically.

### 12. Does \`final\` make an object immutable?

No. \`final\` prevents **reassignment** of the variable, not mutation of what it
points at. \`final List<String> names = new ArrayList<>()\` still allows
\`names.add(...)\`. Real immutability needs final fields, immutable field types, no
setters, and defensive copies in both directions. \`final\` does buy one real
guarantee: **safe publication** — a thread that sees a reference to a properly
constructed object also sees its final fields fully initialised, with no
synchronisation.

## Hard

### 13. What does an object actually cost in memory?

On 64-bit HotSpot: a 8-byte **mark word** (identity hash, GC age, lock state), a
4-byte **class pointer** with compressed oops, an extra 4-byte length for arrays,
then fields, then padding to an 8-byte boundary. So \`new Object()\` is 16 bytes,
and an \`Integer\` wrapping a 4-byte \`int\` costs 16 plus a 4-byte reference to
reach it. Compressed oops stop working above a 32 GB heap, at which point every
reference doubles — so a 33 GB heap can hold *less* live data than a 31 GB one.

### 14. If allocation goes to a shared heap, why does it not need a lock?

Because each thread gets a **Thread-Local Allocation Buffer**, a private slab of
Eden. A \`new\` inside it is a pointer bump and a bounds check — no
synchronisation. Only handing out a fresh TLAB is synchronised. On top of that,
C2's **escape analysis** can prove an object never leaves its method and apply
**scalar replacement**, turning its fields into registers so nothing is allocated
at all. Together they are why "avoid allocation" advice inherited from C++ is
mostly wrong in Java: what costs is objects that *survive*, not objects that are
created.

## What to revise next

Questions 7 and 8 are the same mechanism — the boxing cache and implicit
unboxing — and interviewers ask them back to back. Question 13 tends to be
followed by "so how would you store a million integers efficiently?", and the
answer is \`int[]\`, for exactly the reasons in that answer.

Where objects go when nothing refers to them any more is the next topic:
**[garbage collection](#/advanced/deep-gc)**.
`,pe=`The chapter in a page. If a line here is news, the **Notes** or **Advanced**
part is where it comes from.

## Where things live

- **Stack** — locals, parameters, references. One frame per call, discarded on
  return. Per-thread, fast, small.
- **Heap** — every object made with \`new\`. Shared, collected automatically.
- A **primitive** holds its value. A **reference** holds the address of something
  on the heap.
- Copying a primitive copies the value. Copying a reference copies the address,
  so both names point at one object.

## Pass by value, always

- When you pass an object, the value copied is the **reference**.
- So a method can change what the object *contains* and the caller sees it.
- Reassigning the parameter itself rebinds only the method's copy. The caller
  sees nothing.

## The String Pool

- Literals are stored once and shared, so \`"a" == "a"\` is true.
- \`new String("a")\` deliberately allocates outside the pool, so \`==\` fails.
- \`intern()\` returns the pooled instance for any String.
- \`"Ja" + "va" == "Java"\` is **true** — two literals, folded by the compiler.
  \`part + "va" == "Java"\` is **false** — built at run time, never pooled.
- Use \`.equals()\`. Every time.

## Strings, underneath

- Since Java 9 a String holds a \`byte[]\` plus a coder flag — **compact strings**,
  one byte per character for Latin-1.
- \`length()\` counts **UTF-16 code units**, not characters. Emoji count as 2, so
  \`"👍".length()\` is 2.
- The hash is cached in a non-final field after the first \`hashCode()\`. A benign
  data race — both threads compute the same number.
- The pool is the native **StringTable**. \`-XX:+UseStringDeduplication\` is the
  modern answer to duplicate text, not \`intern()\`.

## Concatenation

- \`a + b + c\` outside a loop is fine — one \`invokedynamic\`, one allocation.
- \`result += x\` **inside a loop** is O(n²). Use \`StringBuilder\`.
- \`StringBuilder\` starts at 16 chars and doubles. Pre-size it if you know
  roughly how big the result is.
- \`StringBuffer\` is the synchronised version and almost never the right answer.

## What an object costs

- 8-byte **mark word** (identity hash, GC age, lock state) + 4-byte class
  pointer + fields + padding to 8.
- \`new Object()\` = **16 bytes**. An \`Integer\` wrapping a 4-byte \`int\` = 16, plus
  a 4-byte reference to reach it.
- **Compressed oops** stop above a 32 GB heap, so a 33 GB heap holds *less* live
  data than a 31 GB one.

## Why allocation is cheap

- Each thread gets a **TLAB** — a private slab of Eden. \`new\` is a pointer bump,
  no synchronisation.
- **Escape analysis** plus **scalar replacement** can mean an object is never
  allocated at all.
- So the C++ instinct to avoid allocation is mostly wrong here. What costs is
  objects that **survive**.

## The boxing cache

- \`Integer.valueOf\` caches **-128 to 127**. So \`==\` works on small values and
  fails above 127 — a bug that passes every test.
- Comparing an \`Integer\` to an \`int\` **unboxes**, which throws
  \`NullPointerException\` when the reference is null. No \`null\` visible in the
  source.
- Same cache on \`Long\`, \`Short\`, \`Byte\` and \`Character\`.

![The Integer cache runs to 127 and == stops working after it](diagrams/memory-revision-boxing-cache.jpg)

## \`final\` and immutability

- \`final\` stops **reassignment**, not mutation. \`final List\` still accepts \`add\`.
- Real immutability: final fields, immutable field types, no setters, defensive
  copies both ways.
- A \`record\` gives you final fields, not deep immutability — copy in the compact
  constructor if it holds a mutable collection.
- \`final\` fields are **safely published**: see the reference, see the fields
  fully built, without synchronisation.

![final freezes the variable binding, not the object](diagrams/memory-revision-final-freezes.jpg)
`,me=`Open a page, and your browser sends a few hundred bytes of text to a machine it
has never met. That machine sends a few hundred bytes back, and the conversation
is over. Everything else — the images, the fonts, the API call the page makes
three seconds later — is the same exchange again, from the start.

That is the whole protocol. Two messages, both of them text you can read, both
of them the same shape: a first line, some headers, a blank line, and a body.
The rest of this page is what goes in those fields and why each one is there.

## One exchange, in full

![A request and a response, field by field](diagrams/http-notes-two-messages.jpg)

Here is a complete request and the complete response it got, with nothing
hidden. This is a real exchange with a small web server, printed as the text
that crossed the connection:

\`\`\`text
GET /greeting?name=ada HTTP/1.1
Host: localhost
Accept: text/plain
Connection: close

\`\`\`

\`\`\`text
HTTP/1.1 200 OK
Date: Sat, 01 Aug 2026 06:11:39 GMT
Content-type: text/plain; charset=utf-8
Content-length: 12
Cache-control: max-age=60

Hello, ada.
\`\`\`

Four things in that transcript are worth stopping on before anything else.

**The blank line is structural.** It is the only thing separating the headers
from the body, in both directions. Miss it and the server waits for headers that
never end.

**Every line ends \`\\r\\n\`**, carriage return and line feed, not the bare newline
your editor writes. Write one by hand with plain newlines and a strict server
will reject it.

**\`Host\` is not decoration.** One IP address serves thousands of sites, and the
path alone does not say which one you want. That header is what makes shared
hosting and every CDN possible, and it is why it is the one header HTTP/1.1
makes compulsory.

**Header names are case-insensitive.** That server answered \`Content-type\` where
most clients write \`Content-Type\`, and no receiver anywhere is allowed to care
about the difference.

## The shape of a request

Every request is a method, a target and a version on the first line, then
headers, then an optional body.

\`\`\`text
POST /orders?draft=true HTTP/1.1        <- method, target, version
Host: shop.example.com                  <- which site, on a shared address
Content-Type: application/json          <- what the body is
Content-Length: 27                      <- how long the body is
Authorization: Bearer eyJhbGciOi…       <- who is asking
Accept: application/json                <- what you would like back
                                        <- the blank line
{"sku":"A-11","quantity":2}             <- the body
\`\`\`

The target is a path and a query string, not a whole URL — the scheme and the
host were used to open the connection and travel in \`Host\`. The exception is a
request to a proxy, which is handed the absolute URL because it has to decide
where to forward it.

## The methods, and the two words that decide everything

![The methods sorted by whether they are safe and whether they are idempotent](diagrams/http-notes-safe-idempotent.jpg)

There are nine, and most work uses four. What matters is not the list but the
two properties, because every retry, every cache and every "are you sure?"
dialogue in the world is derived from them.

**Safe** means the method is a read: it is not *supposed* to change anything, so
a crawler may follow it without asking permission.

**Idempotent** means doing it twice has the same effect as doing it once. Not
the same *response* — deleting twice gives you \`204\` then \`404\` — the same
resulting state. This is the property that decides whether a client may safely
retry after a timeout, and a timeout is exactly the case where the client has no
idea whether the first attempt arrived.

| Method | Safe | Idempotent | Body | What it is for |
|---|---|---|---|---|
| \`GET\` | yes | yes | no | Fetch a representation |
| \`HEAD\` | yes | yes | no | The headers a \`GET\` would give, without the body |
| \`OPTIONS\` | yes | yes | no | What may be done here — used by CORS preflight |
| \`PUT\` | no | **yes** | yes | Replace the resource at this URL with what I send |
| \`DELETE\` | no | **yes** | no | Remove it |
| \`POST\` | no | no | yes | Hand this to the resource and let it decide |
| \`PATCH\` | no | no | yes | Apply this partial change |
| \`TRACE\` | yes | yes | no | Echo the request back — usually disabled |
| \`CONNECT\` | no | no | — | Open a tunnel through a proxy, which is how HTTPS gets through one |

\`PUT\` is idempotent because it carries the whole new state: send it five times
and the resource ends up the same as after one. \`POST\` is not, because the
server decides what it means — five identical \`POST /orders\` calls are five
orders, which is why the browser warns you before re-submitting a form.

> The everyday mistake is \`POST\` for everything, or \`GET\` for something that
> changes state. The second is worse: it means a link preview, a prefetch or an
> overenthusiastic crawler can delete your data by reading it.

## Status codes

![The five status code families and what each means](diagrams/http-notes-status-families.jpg)

Three digits, and the first one is the whole summary: **1** wait, **2** it
worked, **3** it is elsewhere, **4** you got it wrong, **5** we got it wrong.
That split matters more than the individual numbers — a client that handles the
families sensibly and knows six specific codes is in better shape than one with
a switch of forty.

| Code | Name | When you actually see it |
|---|---|---|
| \`200\` | OK | The normal answer, with a body |
| \`201\` | Created | A \`POST\` made something; \`Location\` says where |
| \`204\` | No Content | It worked and there is deliberately nothing to send |
| \`206\` | Partial Content | A range request — video seeking, resumed downloads |
| \`301\` \`308\` | Moved Permanently | The URL changed for good; update your links |
| \`302\` \`307\` | Found / Temporary Redirect | Go here for now, keep using this URL |
| \`304\` | Not Modified | Your cached copy is still good; no body follows |
| \`400\` | Bad Request | The message itself is malformed |
| \`401\` | Unauthorized | You are not authenticated. Badly named — it means unauthenticated |
| \`403\` | Forbidden | We know who you are and you still may not |
| \`404\` | Not Found | Nothing here. Also the polite way to hide a \`403\` |
| \`405\` | Method Not Allowed | The URL exists; that verb does not apply to it |
| \`409\` | Conflict | The state moved under you — a failed optimistic update |
| \`415\` | Unsupported Media Type | Your \`Content-Type\` is not one we accept |
| \`422\` | Unprocessable Content | Well-formed, but the values are wrong |
| \`429\` | Too Many Requests | Rate limited; \`Retry-After\` says when to come back |
| \`500\` | Internal Server Error | An unhandled failure on our side |
| \`502\` | Bad Gateway | A proxy asked something upstream and got nonsense |
| \`503\` | Service Unavailable | Temporarily down or overloaded; retry later |
| \`504\` | Gateway Timeout | A proxy asked something upstream and got silence |

The \`301\`/\`308\` and \`302\`/\`307\` pairs exist because of a historical accident:
browsers turned a redirected \`POST\` into a \`GET\`, in defiance of the
specification, and enough of the web came to depend on that behaviour that it
could not be fixed. So \`307\` and \`308\` were added to mean "the same thing, and
this time really do keep the method". Use \`308\` and \`307\` for anything that is
not a plain page move.

The three gateway codes are worth telling apart when you are on call. \`502\` is
"the thing behind me answered, and it was rubbish". \`504\` is "the thing behind
me did not answer in time". \`503\` is "I am the thing, and I am not well". They
point at three different machines.

## Headers

Headers are metadata, in two rough groups: those describing the **body** you are
looking at, and those controlling the **exchange** itself.

| Header | Direction | What it decides |
|---|---|---|
| \`Host\` | request | Which site on this address; compulsory in HTTP/1.1 |
| \`Content-Type\` | both | How to interpret the bytes — \`application/json\`, \`text/html; charset=utf-8\` |
| \`Content-Length\` | both | Exactly how many bytes of body follow |
| \`Transfer-Encoding: chunked\` | both | The length is not known yet; the body arrives in sized pieces |
| \`Content-Encoding\` | response | The body has been compressed — \`gzip\`, \`br\`, \`zstd\` |
| \`Accept\` | request | What formats you can handle, with weights |
| \`Accept-Encoding\` | request | Which compressions you can decode |
| \`Accept-Language\` | request | Which languages you would prefer |
| \`Authorization\` | request | Credentials — \`Bearer <token>\`, \`Basic <base64>\` |
| \`Cookie\` / \`Set-Cookie\` | request / response | The state a stateless protocol keeps for you |
| \`Cache-Control\` | both | Who may store this, and for how long |
| \`ETag\` / \`If-None-Match\` | response / request | A version tag, and the conditional that uses it |
| \`Location\` | response | Where a redirect goes, or where a \`201\` created something |
| \`User-Agent\` | request | Which client this is. Mostly a pile of historical lies |

Two rules about them. Names are case-insensitive, and a header may be repeated —
in which case it means the same thing as one header with the values joined by
commas. \`Set-Cookie\` is the exception that ruins every naive parser: it may
appear many times and may **not** be folded together, because a cookie value can
itself contain a comma.

## The body, and knowing where it ends

Sending bytes is easy. Knowing when to stop reading is the part with the rules.

**\`Content-Length\`** is the simple answer: this many bytes, then the message is
over, and the connection can be used for the next request.

**\`Transfer-Encoding: chunked\`** is for when the sender does not know the length
in advance — a page assembled as it is generated, or a stream. The body arrives
as a series of chunks, each preceded by its size in hexadecimal, ending with a
zero-sized chunk:

\`\`\`text
HTTP/1.1 200 OK
Transfer-Encoding: chunked

7\\r\\n
Mozilla\\r\\n
9\\r\\n
Developer\\r\\n
0\\r\\n
\\r\\n
\`\`\`

**Closing the connection** is the third way, and the reason \`Connection: close\`
appears in the transcript above. It works, and it costs you the connection.

A message must not use both \`Content-Length\` and \`Transfer-Encoding\`. When one
arrives with both, servers and proxies have historically disagreed about which
to believe, and *that disagreement* is the whole of request smuggling — the
front end reads one request where the back end reads two. Modern servers reject
such a message outright, which is the only safe reading.

## Compression, and negotiation in general

The client says what it can take, the server picks:

\`\`\`text
Accept-Encoding: gzip, br, zstd
    ->  Content-Encoding: br
        Vary: Accept-Encoding
\`\`\`

\`Vary\` is the half everybody forgets. It tells caches that the response depends
on that request header, so a shared cache must not hand the Brotli copy to a
client that only understands gzip. Miss it and the failure is somebody else's
browser showing binary rubbish.

The same three-step shape — an \`Accept-*\` header, a chosen representation, and a
\`Vary\` naming what the choice depended on — is all of content negotiation, and
it works identically for media type, language and encoding.

## Cookies, and how a stateless protocol remembers you

HTTP has no memory. Two requests from the same person are two unrelated
messages, and every login on the web is built on working around that.

The server sends a cookie; the browser sends it back on every subsequent request
to that site until it expires.

\`\`\`text
Set-Cookie: session=8f2b…; Max-Age=1209600; Path=/; Secure; HttpOnly; SameSite=Lax
\`\`\`

The attributes are the security-relevant part, and each one closes a specific

![Each cookie attribute and the hole it closes](diagrams/http-notes-cookie-attributes.jpg)
hole:

- **\`Secure\`** — never send this over plain HTTP. Without it, one request to
  \`http://\` hands the session to anyone on the network.
- **\`HttpOnly\`** — JavaScript cannot read it. This is what stops a cross-site
  scripting bug from turning into stolen sessions.
- **\`SameSite=Lax\`** (the modern default) — do not send it on cross-site
  requests other than top-level navigations, which is most of cross-site request
  forgery gone. \`Strict\` is stricter and logs people out when they arrive from a
  link; \`None\` requires \`Secure\` and is for deliberate third-party use.
- **\`Max-Age\` / \`Expires\`** — how long it lives. Neither means "until the
  browser closes", which is what you get if you omit both.
- **\`Domain\` / \`Path\`** — how widely it is sent. Widening \`Domain\` to the parent
  hands the cookie to every subdomain, including the one you let a contractor
  host something on.

Tokens in \`Authorization\` headers are the alternative, and they trade one
problem for another: no automatic sending means no cross-site request forgery,
but it also means you have to store the token somewhere, and \`localStorage\` is
readable by any script on the page.

## Caching

![What a cache does with a stored response](diagrams/http-notes-cache-decision.jpg)

The fastest request is the one never sent. HTTP has two mechanisms and they
stack.

**Freshness** — the response says how long it may be reused, and until then no
request happens at all:

\`\`\`text
Cache-Control: max-age=3600, public
\`\`\`

\`public\` means a shared cache may keep it; \`private\` means only the browser;
\`no-store\` means keep it nowhere, which is what you want for a bank statement.
\`no-cache\` is the badly named one: it means *store it, but revalidate before
reusing it* — not "do not cache".

**Validation** — when the copy is stale, the client asks whether it is still
good rather than downloading it again:

\`\`\`text
ETag: "v3-9f2c"                    (response)
If-None-Match: "v3-9f2c"           (the next request)
    ->  HTTP/1.1 304 Not Modified  (no body at all)
\`\`\`

A \`304\` carries headers and nothing else, so a stale-but-unchanged megabyte
costs a few hundred bytes to confirm. \`Last-Modified\` and \`If-Modified-Since\`
are the same idea with a timestamp instead of a tag, and are weaker because a
timestamp has one-second resolution and files change twice a second.

The pattern the whole modern web uses: name build artefacts with a content hash,
serve them \`max-age=31536000, immutable\`, and never cache the HTML that points
at them. Then a deploy changes the file names, the HTML is always fresh, and
nothing has to be invalidated.

## One connection, or many

![How the four HTTP versions carry the same messages](diagrams/http-notes-versions.jpg)

The original protocol opened a TCP connection per file, and a page with forty
images paid forty handshakes. Everything since has been about that.

- **HTTP/1.0** — one request, one connection. Slow, and the reason \`Connection:
  keep-alive\` was bolted on.
- **HTTP/1.1** — persistent connections by default, so several requests reuse
  one socket. But they must be answered in order, so one slow response blocks
  everything queued behind it. Browsers worked around it by opening six
  connections per host and sharding assets across subdomains.
- **HTTP/2** — one connection, many interleaved streams, headers compressed
  rather than repeated in full on every request. The application-layer queue is
  gone.
- **HTTP/3** — the same model over QUIC, which is built on UDP, so a lost packet
  stalls only its own stream instead of the whole connection. It also folds the
  TLS handshake into the transport handshake, which is a round trip saved.

Two things stay true across all three: **the messages above do not change**.
Methods, status codes, headers and bodies mean exactly the same thing in HTTP/3
as in the plain text at the top of this page. What changes is how they are
framed and multiplexed on the way. The [advanced page](#/advanced/deep-http/advanced)
takes each of those framings apart.

## When it goes wrong

- **A request hangs with no response** — usually a missing blank line after the
  headers, or a \`Content-Length\` larger than the body you actually sent. The
  server is still waiting for the rest.
- **\`411 Length Required\`** — you sent a body with neither a length nor chunked
  encoding.
- **\`415\`, and the body looked fine** — the \`Content-Type\` did not match what
  the endpoint accepts. \`application/json\` and \`text/plain\` are not
  interchangeable to a strict server.
- **The API works in curl and fails in the browser** — that is CORS, not HTTP.
  The request never reached your code, or its response was withheld from the
  page. Look for the \`OPTIONS\` preflight in the network tab.
- **Everything cached forever after one bad deploy** — you served \`max-age\` on
  the HTML. There is no way to reach into a browser and clear it; only a URL
  change or a wait fixes it. Cache the hashed assets, never the document.
- **The response is gibberish** — a \`Content-Encoding\` your client did not
  decode, or a charset mismatch. \`charset=utf-8\` on \`Content-Type\` is not
  optional for text.

## What to read next

- **[HTTP, underneath](#/advanced/deep-http/advanced)** — connection pooling,
  HTTP/2 frames and HPACK, QUIC, conditional requests in full, proxies, retries
  and the timeouts that go with them.
- **[HTTPS, TLS and the certificate](#/advanced/deep-https)** — the other half.
  Everything on this page travels in clear text until you put it inside TLS.

## Further reading

- [RFC 9110 — HTTP Semantics](https://www.rfc-editor.org/rfc/rfc9110.html)
- [RFC 9111 — HTTP Caching](https://www.rfc-editor.org/rfc/rfc9111.html)
- [MDN's HTTP reference](https://developer.mozilla.org/en-US/docs/Web/HTTP)
`,he=`The [notes](#/advanced/deep-http) are about the message. This is about what
happens to it: the connection it travels on, the framing that carries it, the
caches and proxies between the two ends, and what to do when one of them
answers late or not at all.

Everything here assumes the message shape from that page is familiar, because
none of it changes. HTTP/2 and HTTP/3 are new ways of carrying the same request.

## What a request actually costs

![How many round trips are spent before the request is sent](diagrams/http-advanced-round-trips.jpg)

Before a single byte of your \`GET\` moves, the two machines have to agree that
they are talking. Over a plain connection that is one round trip; over TLS it
was three, and is now two.

\`\`\`text
TCP        SYN  ->            1 round trip
           <-  SYN, ACK
           ACK  ->
TLS 1.2    ClientHello ->     2 more round trips
           <-  ServerHello, Certificate
           key exchange ->
           <-  Finished
HTTP       GET /  ->          the request finally leaves
\`\`\`

A round trip between London and Virginia is about 75 milliseconds. Three of them
is a quarter of a second before your server has even seen the request, and no
amount of application tuning touches it. This is why every optimisation below is
really about the same thing: **do not pay for a connection you have already
paid for.**

TLS 1.3 cut the handshake to one round trip and QUIC folds it into the transport
handshake, so the modern floor is one. The full sequence is on the
[HTTPS page](#/advanced/deep-https/advanced).

## Keep-alive, and the pool that manages it

HTTP/1.1 keeps the connection open after a response, so the next request skips
everything above. A client library holds those open sockets in a pool, keyed by
scheme, host and port, and that is what "connection pooling" means — there is no
pool of anything else.

Java's \`HttpClient\` does this for you, as long as you keep the client. Build one
per request and every call pays a fresh handshake; under load you run out of
ephemeral ports rather than out of CPU.

\`\`\`java
// once, for the life of the application
HttpClient client = HttpClient.newHttpClient();

// every call after the first reuses an open connection to that host
client.send(HttpRequest.newBuilder(URI.create(url)).build(),
            HttpResponse.BodyHandlers.ofString());
\`\`\`

That is the single most common performance mistake in Java HTTP code, and it is
invisible until the connection count is what falls over.

## What a validated cache entry costs

The other half of not paying for work twice. The client keeps the \`ETag\` it was
given and offers it back; if nothing changed, the answer has no body at all.
This runs locally and needs nothing installed:

\`\`\`java Revalidate.java @run-http-revalidate
import com.sun.net.httpserver.HttpServer;
import java.net.InetSocketAddress;
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

/** Asking twice for the same thing: once plainly, once with the tag we were given. */
public class Revalidate {

    static final String TAG = "\\"v3\\"";
    static final String TEXT = "the quick brown fox\\n";

    public static void main(String[] args) throws Exception {
        // A small server that answers "you already have it" when the tag matches.
        HttpServer server = HttpServer.create(new InetSocketAddress("127.0.0.1", 0), 0);
        server.createContext("/fox", exchange -> {
            String tagTheClientHas = exchange.getRequestHeaders().getFirst("If-None-Match");
            exchange.getResponseHeaders().add("ETag", TAG);

            if (TAG.equals(tagTheClientHas)) {
                exchange.sendResponseHeaders(304, -1);   // -1 means "no body at all"
            } else {
                exchange.sendResponseHeaders(200, TEXT.length());
                exchange.getResponseBody().write(TEXT.getBytes());
            }
            exchange.close();
        });
        server.start();

        String url = "http://127.0.0.1:" + server.getAddress().getPort() + "/fox";
        HttpClient client = HttpClient.newHttpClient();

        HttpResponse<String> first = client.send(
                HttpRequest.newBuilder(URI.create(url)).build(),
                HttpResponse.BodyHandlers.ofString());

        System.out.println("plain request");
        System.out.println("  status : " + first.statusCode());
        System.out.println("  etag   : " + first.headers().firstValue("etag").orElseThrow());
        System.out.println("  bytes  : " + first.body().length());

        HttpResponse<String> second = client.send(
                HttpRequest.newBuilder(URI.create(url))
                        .header("If-None-Match", TAG)
                        .build(),
                HttpResponse.BodyHandlers.ofString());

        System.out.println("same request, with the tag we were given");
        System.out.println("  status : " + second.statusCode());
        System.out.println("  bytes  : " + second.body().length());

        server.stop(0);
    }
}
\`\`\`

\`\`\`output @run-http-revalidate
plain request
  status : 200
  etag   : "v3"
  bytes  : 20
same request, with the tag we were given
  status : 304
  bytes  : 0
\`\`\`

Two things in that transcript are the whole point.

**The \`304\` carried no body.** The client got a status line, an \`ETag\` and
nothing else — twenty bytes saved here, and a megabyte saved on anything real.
That is what makes revalidating on every page load affordable.

**The server decided by comparing tags, not by looking at a clock.** No
expiry had to be guessed and no cache had to be cleared: the client offered what
it had, and the server said whether it was still current.

## Head-of-line blocking, three times over

![Head-of-line blocking in HTTP/1.1, HTTP/2 and HTTP/3](diagrams/http-advanced-head-of-line.jpg)

One connection carrying several requests has an ordering problem, and each
protocol version solves a different layer of it.

**HTTP/1.1.** Responses must come back in request order. *Pipelining* — sending
several requests without waiting — was specified and is effectively dead,
because one slow response holds up every response behind it and buggy proxies
made it unsafe. Browsers instead opened six connections per host and authors
sharded assets across \`img1.\`, \`img2.\`, \`img3.\` subdomains to get more.

**HTTP/2.** Requests become independent streams multiplexed over one connection,
so the application-level queue is gone. But TCP still delivers bytes strictly in
order: lose one packet and the kernel holds back every stream's data behind it,
however unrelated. On a clean network HTTP/2 is a large win; on a lossy mobile
link, a single-connection protocol can be worse than six connections were.

**HTTP/3.** QUIC gives each stream its own delivery order, so a lost packet
stalls only the stream whose bytes were in it. That is the actual reason it
exists — not UDP for its own sake.

## HTTP/2 on the wire

![Three requests interleaved as frames on one HTTP/2 connection](diagrams/http-advanced-h2-frames.jpg)

The text is gone. HTTP/2 sends **frames**: a nine-byte header giving length,
type, flags and stream identifier, then a payload.

| Frame | What it carries |
|---|---|
| \`HEADERS\` | A request or response's headers, HPACK-compressed |
| \`DATA\` | Body bytes |
| \`SETTINGS\` | Connection parameters, exchanged at the start and acknowledged |
| \`WINDOW_UPDATE\` | Flow control credit — permission to send more |
| \`RST_STREAM\` | Cancel one stream without touching the others |
| \`GOAWAY\` | I am closing; streams above this id were not processed |
| \`PING\` | Liveness, and round-trip measurement |

A **stream** is a request and its response, identified by an odd number from the
client. Streams are interleaved frame by frame, which is why one connection can
carry a hundred requests without any of them waiting for another to finish.

**HPACK** is the header compression, and it exists because headers are enormously
repetitive: the same \`User-Agent\`, \`Cookie\` and \`Accept\` on every one of a
hundred requests. It keeps a table of previously sent headers on both ends and
sends an index instead of the text. A first request costs the full headers; the
next costs a handful of bytes. It also has a static table of the sixty-one most
common header/value pairs, so \`:method: GET\` is a single byte.

The pseudo-headers are the other visible change. The request line is gone,
replaced by \`:method\`, \`:scheme\`, \`:authority\` and \`:path\` — and \`:authority\`
is what \`Host\` became.

**Flow control** is per-stream and per-connection, and it is a real source of
production stalls: a client that never sends \`WINDOW_UPDATE\` — because it is not
reading the body it asked for — stops the server sending, and the symptom is a
request that hangs with the server insisting it responded.

**Server push existed and is gone.** Browsers removed it because pushing what the
client already has in cache wastes more than it saves. \`103 Early Hints\` with
\`Link: rel=preload\` replaced it: tell the client what to fetch, let it decide.

## HTTP/3 and QUIC

QUIC is a transport built on UDP, in user space, with TLS 1.3 built in rather
than layered on.

- **Streams are independent**, so packet loss is contained (above).
- **The handshake is one round trip**, because transport and TLS negotiate
  together. A resumed connection can be zero — see the 0-RTT caveats on the
  [HTTPS page](#/advanced/deep-https/advanced).
- **A connection has an identifier**, not just an address pair. Walk out of the
  house and your phone moves from wi-fi to mobile data; the addresses change and
  the connection survives, because it is identified by that id rather than by
  the four-tuple. TCP cannot do this at all.
- **It is in user space**, so it can be updated with the browser rather than
  with the kernel. That is why congestion control changes now ship in months.

The cost is CPU — encryption and packet handling that a kernel used to do — and
that some corporate networks block UDP outright, which is why every HTTP/3 client
keeps a TCP path to fall back to.

## Conditional requests, in full

![The conditional headers and what each one is for](diagrams/http-advanced-conditionals.jpg)

The notes cover \`If-None-Match\` for caching. The same mechanism, pointed the
other way, is how you avoid the lost update.

| Header | Means | Fails with |
|---|---|---|
| \`If-None-Match: "tag"\` | Only send it if the tag is different | \`304\` |
| \`If-Match: "tag"\` | Only write it if the tag still matches | \`412\` |
| \`If-Modified-Since: <date>\` | Only send it if it changed after this | \`304\` |
| \`If-Unmodified-Since: <date>\` | Only write it if nothing changed since | \`412\` |
| \`If-Range: "tag"\` | Continue this download only if it has not changed | full \`200\` |

\`If-Match\` is optimistic concurrency, and it is the correct answer to two people
editing the same record. Read gives you \`ETag: "7"\`; your \`PUT\` carries
\`If-Match: "7"\`; if somebody saved first the tag is now \`"8"\` and you get \`412
Precondition Failed\` instead of silently overwriting their work.

An \`ETag\` is **strong** by default and **weak** when prefixed \`W/\`. Strong means
byte-for-byte identical, which is what ranges require. Weak means semantically
equivalent — a page whose rendering timestamp changed but whose content did not.
Serve weak tags rather than a strong tag you cannot honour.

**Ranges** ride on the same machinery. \`Range: bytes=1000-1999\` gets \`206
Partial Content\` with a \`Content-Range\`, which is how a video player seeks and
how a download resumes; an unsatisfiable range gets \`416\`. \`Accept-Ranges:
bytes\` on a normal response is the advertisement that this is possible.

**\`Expect: 100-continue\`** is the one nobody meets until it bites: send the
headers of a large upload, wait for \`100 Continue\`, then send the body — so a
rejected upload costs the headers rather than the gigabyte. Java's \`HttpClient\`
supports it with \`.expectContinue(true)\`.

## Caching, the parts that bite

The directives that matter beyond \`max-age\`:

- **\`s-maxage\`** overrides \`max-age\` for shared caches only, so a CDN can hold
  something for an hour that a browser holds for a minute.
- **\`must-revalidate\`** forbids serving stale after expiry, even if the origin
  is unreachable. Without it, a cache may serve stale on error, which is often
  what you want and occasionally catastrophic.
- **\`stale-while-revalidate=60\`** allows the cache to answer instantly with the
  stale copy and refresh in the background. This is the single best directive
  for perceived speed on a busy site.
- **\`stale-if-error=86400\`** keeps the site up when the origin is down.
- **\`immutable\`** promises the body will never change at this URL, so the browser
  will not even revalidate on a reload. It belongs on hashed assets and nowhere
  else.
- **\`Age\`** is added by shared caches: how many seconds this copy has been
  sitting there. A response that arrives with a large \`Age\` and a small
  \`max-age\` is about to expire, and \`Age\` is what tells you a CDN answered.

**Heuristic freshness** is what happens when you send no caching headers at all.
Caches are permitted to guess, typically ten per cent of the time since
\`Last-Modified\`. So "I did not set a cache header" does not mean "not cached" —
it means "cached for a length of time you did not choose".

**\`Vary\`** determines what a cache keys on beyond the URL. Every value you add
multiplies the number of stored copies, so \`Vary: Accept-Encoding\` is routine,
\`Vary: Origin\` is required if you reflect origins in CORS headers, and \`Vary:
User-Agent\` fragments the cache into near-uselessness.

## Proxies, gateways, and the headers that appear

![Headers added by each hop between the browser and the application](diagrams/http-advanced-proxy-chain.jpg)

Between the two ends there is usually a load balancer, a CDN, a corporate proxy,
or all three. Each rewrites something.

- **\`X-Forwarded-For\`** — the chain of client addresses, appended left to right.
  \`Forwarded\` is the standardised replacement and is used less.
- **\`X-Forwarded-Proto\`** — the scheme the *client* used, which is how an app
  behind a TLS-terminating balancer knows it is really on HTTPS. Get this wrong
  and you generate \`http://\` links from an HTTPS site, or redirect in a loop.
- **\`Via\`** — which proxies handled it, by protocol and name.

**None of these can be trusted from the outside.** A client may send its own
\`X-Forwarded-For\`, and if your framework reads the leftmost value you have just
let anyone claim any IP address — for rate limiting, for audit logs, for
allow-lists. Trust only what your own trusted hop appended, and configure the
number of hops explicitly.

**Hop-by-hop headers** belong to a single connection and must not be forwarded:
\`Connection\`, \`Keep-Alive\`, \`Transfer-Encoding\`, \`Upgrade\`, \`TE\`, \`Trailer\`,
\`Proxy-Authorization\`. The \`Connection\` header names any others that should be
dropped. HTTP/2 forbids most of them outright, which is why a proxy translating
between versions has to strip rather than copy.

## Retries, timeouts and idempotency

A network failure tells you a request did not *complete*. It never tells you

![Deciding whether a failed request may be retried](diagrams/http-advanced-retry-decision.jpg)
whether it *arrived*. That distinction is the whole of retry design.

**Which failures may be retried.** Connection refused and DNS failure are safe —
nothing was sent. A timeout after the request left is safe only if the method is
idempotent. \`429\` and \`503\` are retryable and often carry \`Retry-After\`. \`500\`
generally is not, because it usually means your request will fail the same way
again.

**Exponential backoff with jitter**, not a fixed delay. Ten thousand clients
retrying at exactly two seconds is a second outage on top of the first — the
thundering herd — and the jitter is the part that prevents it, not the backoff.

**Idempotency keys** make a non-idempotent operation safe to retry. Send a
unique key with the \`POST\`; the server records the key with its result and
returns the same result for a repeat rather than doing the work twice. This is
how every payment API you have used avoids double-charging.

**Timeouts come in layers**, and setting only one is the usual mistake:

- *connect* — how long to wait for the TCP handshake.
- *request or read* — how long to wait for a response once sent.
- *total* — the deadline for the whole operation including retries.

Java's \`HttpClient\` exposes the first as \`connectTimeout\` on the builder and the
second as \`timeout\` on the request; if you set only the second, a host that
blackholes packets hangs you for the connect duration too. And a retry inside a
call whose caller has its own timeout is how a five-second budget becomes
thirty.

## Streaming: three shapes

HTTP is request and response, and three things bend it into a stream.

- **Server-sent events** — one \`GET\`, \`Content-Type: text/event-stream\`, and the
  response body never ends: \`data: …\` lines pushed as they occur. One direction
  only, reconnects automatically, plain HTTP all the way through. This is the
  right answer far more often than it is chosen.
- **WebSocket** — a \`GET\` with \`Upgrade: websocket\`, answered \`101 Switching
  Protocols\`, after which the connection is no longer HTTP at all. Full duplex,
  and you own the framing, the heartbeats and the reconnection.
- **Long polling** — a request the server holds open until it has something to
  say. The fallback that works everywhere and costs a connection per waiting
  client.

## CORS, precisely

Same-origin policy is a browser rule and applies to nothing else, which is why a
call works in curl and fails in a page. The server does not block anything; the
browser withholds the response from the script unless the server has said it may
be shared.

**Simple requests** — \`GET\`, \`HEAD\`, or \`POST\` with a form-ish content type and
no unusual headers — are sent, and the *response* is checked for
\`Access-Control-Allow-Origin\`. The request happened either way, which is why
CORS is not a defence against anything.

**Everything else is preflighted.** The browser sends \`OPTIONS\` first, with
\`Access-Control-Request-Method\` and \`Access-Control-Request-Headers\`; the server
answers with the allowed methods, headers and a \`Access-Control-Max-Age\` saying
how long that permission may be cached.

Two rules people meet the hard way. With \`Access-Control-Allow-Credentials:
true\` you may **not** use \`*\` for the origin — you must echo the actual origin,
and then you must add \`Vary: Origin\` or a cache will serve one site's permission
to another. And a header your JavaScript wants to *read* off the response has to
be listed in \`Access-Control-Expose-Headers\`; it is not enough that the server
sent it.

## Two failures that are protocol, not code

**Request smuggling.** A front-end proxy and a back-end server disagree about
where one request ends — because one honours \`Content-Length\` and the other
\`Transfer-Encoding\`, or because one accepts an odd chunk header the other does
not. The remainder of the attacker's message is then read by the back end as the
*start of the next person's request*. The defences are structural: reject any
message carrying both framing headers, use the same server implementation for
both hops where possible, and prefer HTTP/2 end to end, which has one framing
layer and no ambiguity.

**Response splitting and header injection.** Any header value built from user
input must have carriage returns and newlines stripped, or the attacker writes
their own headers and body into your response. Modern server libraries reject
control characters in header values; string concatenation into a raw response
does not.

Both are worth knowing as a reader of logs even if you never write a proxy: they
are why "why is this request in that user's session" is a question with a
protocol answer.

## Reading it on the wire

\`\`\`bash
curl -v https://example.com                # request and response headers
curl -sS -o /dev/null -w '%{http_version} %{http_code} %{time_total}\\n' URL
curl --http1.1 URL                         # force a version, to compare
curl -H 'Accept-Encoding: gzip' -I URL     # what negotiation actually chose
\`\`\`

For Java, the client has its own logging, and it is the fastest way to see
frames rather than guess at them:

\`\`\`bash
java -Djdk.httpclient.HttpClient.log=requests,headers,frames Program.java
\`\`\`

In a browser, the network panel's protocol column tells you whether you got
\`h2\`, \`h3\` or \`http/1.1\` — and it is regularly not what you assumed, because a
single misconfigured hop downgrades the whole path.

## Further reading

- [RFC 9112 — HTTP/1.1](https://www.rfc-editor.org/rfc/rfc9112.html)
- [RFC 9113 — HTTP/2](https://www.rfc-editor.org/rfc/rfc9113.html)
- [RFC 9114 — HTTP/3](https://www.rfc-editor.org/rfc/rfc9114.html)
- [RFC 9111 — HTTP Caching](https://www.rfc-editor.org/rfc/rfc9111.html)
`,ge=`Fourteen questions on HTTP. The easy ones are asked to check you have read a
message rather than only used a library; the hard ones are asked because the
answer decides whether your service loses data under a retry.

The **Notes** cover the message and the semantics. The **Advanced** page covers
connections, framing, caching and the failures.

## Easy

### 1. What is in an HTTP request, exactly?

A first line carrying the method, the request target and the version; then any
number of header lines; then a blank line; then an optional body. The response
is the same shape with a status line instead — version, three-digit code, reason
phrase. Every line ends with a carriage return and a line feed, and the blank
line is the only thing that marks where the headers stop and the body starts.

### 2. Why is \`Host\` compulsory?

Because one IP address serves many sites. The connection is opened to an
address, and the path alone does not say which of the thousand sites on that
address you meant. \`Host\` carries it, which is what makes shared hosting, name
based virtual hosts and every CDN possible. In HTTP/2 it survives as the
\`:authority\` pseudo-header.

### 3. What is the difference between \`PUT\` and \`POST\`?

\`PUT\` says "make the resource at this URL be exactly this", so sending it five
times leaves the same state as sending it once — it is idempotent. \`POST\` hands
data to a resource and lets the server decide what it means, usually creating
something new, so five identical posts are five orders. That is why the browser
warns you before re-submitting a form and why a \`POST\` should answer \`201\` with
a \`Location\` header rather than the created object alone.

### 4. What do \`401\` and \`403\` actually mean?

\`401\` means you have not proved who you are — it is badly named, since it means
unauthenticated, and a correct \`401\` carries a \`WWW-Authenticate\` header saying
how to authenticate. \`403\` means the server knows exactly who you are and you
still may not have it, so retrying with the same credentials is pointless. Some
services deliberately answer \`404\` where \`403\` is true, to avoid confirming that
a resource exists.

### 5. What is a \`304 Not Modified\`?

The answer to a conditional request. The client says \`If-None-Match: "v3"\` with
the tag from its cached copy; if the resource still has that tag, the server
replies \`304\` with headers and no body at all, and the client reuses what it
already has. It turns re-fetching a megabyte into confirming a few hundred
bytes.

## Medium

### 6. Which failures may a client safely retry?

Retry when nothing can have been done: DNS failure, connection refused, or a
\`429\`/\`503\` — the last two often carrying \`Retry-After\`. After a timeout the
request may or may not have arrived, so retrying is safe only if the method is
idempotent. \`POST\` is not, and the fix is not "never retry" but an **idempotency
key**: a unique value the server records with the result, so a repeat returns
the first result instead of doing the work twice. Back off exponentially and add
jitter — ten thousand clients retrying at exactly two seconds is a second
outage.

### 7. How does HTTP caching actually work?

Two mechanisms that stack. **Freshness**: the response carries \`Cache-Control:
max-age=…\`, and until it expires no request is made at all. **Validation**: once
stale, the client asks with \`If-None-Match\` or \`If-Modified-Since\`, and a \`304\`
revives the stored copy. \`no-cache\` means store it but always revalidate;
\`no-store\` means keep nothing. If you send no directives, caches are permitted
to guess a lifetime from \`Last-Modified\`, so omitting headers does not mean
uncached.

### 8. What is \`Vary\` for?

It names the request headers the stored response depends on, so a shared cache
knows a copy is not interchangeable. Without \`Vary: Accept-Encoding\`, a cache
can hand a Brotli-compressed body to a client that only understands gzip. If you
reflect the request origin into \`Access-Control-Allow-Origin\`, you must add
\`Vary: Origin\` or one site's permission gets served to another. Every value
added multiplies the number of stored copies, which is why \`Vary: User-Agent\`
effectively disables caching.

### 9. What does HTTP/2 change, and what does it not?

It changes the framing: binary frames instead of text, many interleaved streams
over one connection, and HPACK compression so repeated headers cost an index
rather than their full text. It does not change the semantics — the same
methods, status codes, headers and bodies mean the same things. It also does not
remove head-of-line blocking entirely: streams no longer queue at the
application layer, but TCP still delivers in order, so one lost packet stalls
every stream until it is retransmitted.

### 10. Why does an API call work in curl and fail in the browser?

Because CORS is a browser rule, not a server one. The request usually reached
the server and was answered; the browser then withheld the response from the
script because the server did not say it could be shared. Anything that is not a
simple \`GET\`, \`HEAD\` or form-ish \`POST\` is preflighted with an \`OPTIONS\` request
first. Note what this means: CORS protects nobody's data on the server — the
call happened — it protects the *reading* of the response by another origin.

### 11. What is the difference between \`Transfer-Encoding: chunked\` and \`Content-Encoding: gzip\`?

\`Transfer-Encoding\` is about framing: it says how the message is cut into pieces
on this hop when the total length is not known in advance, and each chunk is
prefixed by its size in hexadecimal until a zero-size chunk ends it.
\`Content-Encoding\` is about the representation itself: the body has been
compressed and the receiver must decompress it to get the resource. One is a
property of the connection, the other of the content, and a response can carry
both.

## Hard

### 12. How would you prevent two users overwriting each other's edits?

Optimistic concurrency with conditional requests. A read returns \`ETag: "7"\`;
the update sends \`If-Match: "7"\`; if somebody saved in between, the tag is now
\`"8"\` and the server answers \`412 Precondition Failed\` instead of silently
replacing their work. The client then re-reads and either merges or asks the
user. The alternative — a lock held across a think-time — does not survive a
user closing the tab.

### 13. What is request smuggling, in one paragraph?

Two servers in a chain disagree about where one request ends. A front-end proxy
honours \`Content-Length\` while the back end honours \`Transfer-Encoding\`, or one
accepts a malformed chunk header the other rejects, so the tail of an attacker's
message is read by the back end as the beginning of the *next* client's request.
The defences are structural rather than clever: reject any message carrying both
framing headers, keep the same implementation on both hops where you can, and
prefer HTTP/2 end to end, which has one unambiguous framing layer.

### 14. Why does HTTP/3 use UDP, and what does it cost?

Not for UDP's sake — to escape TCP's single ordered byte stream. QUIC gives each
stream its own delivery order, so a lost packet stalls only the stream whose
bytes it carried instead of every stream on the connection. It also folds the
TLS handshake into the transport handshake, so a new connection costs one round
trip instead of two or three, and identifies connections by a connection id
rather than by an address pair, so a phone moving from wi-fi to mobile data
keeps its connection. The costs are CPU — work the kernel used to do now happens
in user space with encryption on every packet — and networks that block UDP,
which is why clients keep a TCP path to fall back to.

## What to revise next

- **[HTTPS, TLS and the certificate](#/advanced/deep-https)** — everything above
  travels in clear text until you wrap it, and the wrapping has its own
  handshake, its own failures and its own interview questions.
`,_e='The chapter in a page. If a line here is news, the **Notes** or **Advanced**\npart is where it comes from.\n\n## The message\n\n- Request: method, target, version, then headers, a blank line, then an optional\n  body. Response: version, status, reason, then the same three.\n- Lines end `\\r\\n`. The blank line is what ends the headers, in both directions.\n- `Host` is compulsory in HTTP/1.1 — one address serves many sites.\n- Header names are case-insensitive. Repeated headers mean one header joined by\n  commas, and `Set-Cookie` is the exception that must never be folded.\n\n## Methods\n\n- **Safe** = a read, changes nothing. **Idempotent** = doing it twice leaves the\n  same state as doing it once.\n- `GET` `HEAD` `OPTIONS` are safe and idempotent. `PUT` and `DELETE` are\n  idempotent but not safe. `POST` and `PATCH` are neither.\n- Idempotency is what decides whether a client may retry after a timeout — and a\n  timeout never tells you whether the request arrived.\n- `PUT` replaces the whole resource; `PATCH` applies a partial change; `POST`\n  hands it over and lets the server decide.\n\n## Status codes\n\n- 1 wait · 2 worked · 3 elsewhere · 4 your fault · 5 our fault.\n- `401` means unauthenticated; `403` means authenticated and still refused.\n- `301`/`308` permanent, `302`/`307` temporary. The `307`/`308` pair preserves\n  the method; the older pair does not, because browsers changed `POST` to `GET`.\n- `502` upstream answered rubbish · `504` upstream did not answer · `503` this\n  server is unwell. Three different machines.\n- `409` conflict, `412` precondition failed, `422` well-formed but wrong,\n  `429` rate limited with `Retry-After`.\n\n## Body framing\n\n- `Content-Length`, or `Transfer-Encoding: chunked`, or close the connection.\n  Never the first two together — that ambiguity is request smuggling.\n- Chunked sends each piece with its size in hex, ending with a zero-size chunk.\n- `Content-Encoding` is compression of the body; `Transfer-Encoding` is framing\n  of the message. They are not alternatives.\n\n## Caching\n\n- **Freshness** — `max-age`, and no request is made at all. **Validation** —\n  `ETag` plus `If-None-Match`, answered `304` with no body.\n- `no-cache` means revalidate before reuse. `no-store` means keep nothing.\n  `private` browser only, `public`/`s-maxage` for shared caches.\n- `Vary` names the request headers a cached copy depends on. Missing `Vary:\n  Accept-Encoding` hands Brotli to a client that cannot read it.\n- Send nothing and caches guess — heuristic freshness. "No header" is not "not\n  cached".\n- The pattern: hash the asset names, `max-age=31536000, immutable`, never cache\n  the HTML that points at them.\n- `stale-while-revalidate` serves instantly and refreshes behind you;\n  `stale-if-error` keeps the site up when the origin is down.\n\n## Conditionals\n\n- `If-None-Match` → `304`, for caching. `If-Match` → `412`, for optimistic\n  concurrency and the lost update.\n- Strong `ETag` means byte-identical; `W/` weak means semantically the same.\n  Ranges need a strong tag.\n- `Range` → `206` with `Content-Range`; unsatisfiable → `416`.\n\n## Cookies\n\n- `Secure` (HTTPS only), `HttpOnly` (no JavaScript), `SameSite=Lax` (no\n  cross-site sending, which is most of CSRF gone).\n- No `Max-Age` and no `Expires` means a session cookie, not a permanent one.\n- Widening `Domain` hands the cookie to every subdomain.\n\n## Connections and versions\n\n- HTTP/1.1 keeps connections alive; responses come back in order, so one slow\n  response blocks the queue. Browsers open six per host to compensate.\n- HTTP/2 multiplexes independent streams over one connection, with binary frames\n  and HPACK header compression. TCP still blocks all streams on packet loss.\n- HTTP/3 runs on QUIC over UDP: per-stream delivery, one-round-trip handshake,\n  and connection ids that survive a change of network.\n- The messages do not change between versions. Only the framing does.\n- Build one `HttpClient` and keep it. One per request means a handshake per\n  request and, under load, no ephemeral ports left.\n\n## Proxies and CORS\n\n- `X-Forwarded-For` and `X-Forwarded-Proto` are what a client claims until your\n  own trusted hop appends them. Never trust the leftmost value from outside.\n- Hop-by-hop headers — `Connection`, `Keep-Alive`, `Transfer-Encoding`,\n  `Upgrade`, `TE` — belong to one connection and are not forwarded.\n- CORS is a browser rule. The request still reached the server; the browser\n  withheld the response. It defends nobody\'s data.\n- `Allow-Credentials: true` forbids `*`, so echo the origin and add\n  `Vary: Origin`. Headers your script reads must be in `Expose-Headers`.\n',ve=`The S is a wrapper. Underneath it, every message on the
[HTTP page](#/advanced/deep-http) is unchanged — same methods, same status
codes, same headers — but the bytes that leave your machine are now unreadable
to everyone the connection passes through, and there are rather a lot of them:
your router, your internet provider, whoever runs the café wi-fi, every network
between there and the server.

Without it, all of that is plain text. A password in a form body is readable by
anyone on the path, and — worse, and less obvious — it is *editable* by them.

## Three promises, not one

![The three promises TLS makes and what each one stops](diagrams/https-notes-three-promises.jpg)

HTTPS is HTTP carried inside TLS, and TLS makes three separate promises. Mixing
them up is where most misunderstanding starts.

**Confidentiality.** Nobody in the middle can read the traffic. This is the one
everybody knows and the easiest of the three.

**Integrity.** Nobody in the middle can change it. Every record carries an
authentication tag, so a flipped bit is not a corrupted page — it is a broken
connection. Before HTTPS was universal, providers routinely injected adverts
into pages in transit, and this is what stopped it.

**Authentication.** The far end is really who it claims to be. This is the hard
one, the one certificates exist for, and the only one that requires anybody to
trust anybody.

The first two are worth nothing without the third. An encrypted connection to an
attacker is a perfectly private conversation with the wrong person.

> The padlock means the connection is private and the server proved it owns that
> domain name. It does not mean the site is honest, the company real, or your
> data safe once it arrives. Phishing sites have valid certificates — getting
> one is free and takes a minute.

## The handshake, in outline

![The TLS 1.3 handshake in one round trip](diagrams/https-notes-handshake.jpg)

Before any HTTP is sent, the two ends agree on how they will talk. In TLS 1.3
that is one round trip:

\`\`\`text
client                                                server
  |                                                      |
  |-- ClientHello ------------------------------------->|   versions I support,
  |   + my half of a key exchange                        |   cipher suites, the
  |                                                      |   name I am asking for
  |<------------------------------- ServerHello --------|   the version and
  |   + its half of the key exchange                     |   suite it picked
  |   + certificate, + signature over the handshake      |
  |                                                      |
  |   both sides now derive the same secret key          |
  |                                                      |
  |-- Finished ----------------------------------------->|
  |<-------------------------------------- Finished ----|
  |                                                      |
  |=== GET / HTTP/1.1 — encrypted from here on =========>|
\`\`\`

Four things happen in that exchange, and they map onto the three promises:

1. **Agreement.** Which TLS version and which cipher suite — the set of
   algorithms — both ends will use.
2. **Key exchange.** Each side sends half of a calculation; each combines the
   other's half with its own secret; both arrive at the same key without that
   key ever crossing the network.
3. **Identity.** The server sends its certificate and a signature proving it
   holds the private key that certificate names.
4. **Confirmation.** Both send a \`Finished\` message that is a checksum of the
   whole conversation so far, so a tamperer who altered anything earlier is
   caught here.

Only then does the first HTTP byte move.

## How two strangers agree a secret in public

The part that sounds impossible: everything above is sent in the open, and yet
both ends end up with a key that the observer does not have.

Each side picks a private number and computes a public value from it. They swap

![Two sides deriving the same secret from public values](diagrams/https-notes-key-agreement.jpg)
public values. Each then combines its own private number with the other's public
value, and the mathematics is arranged so that both combinations produce the
same answer — while an observer with only the two public values cannot compute
it in any practical time.

This is Diffie–Hellman, and modern TLS uses the elliptic-curve version, ECDHE.
The final E is the important letter: **ephemeral**. A fresh private number is
picked for every connection and discarded afterwards. That gives **forward
secrecy** — an attacker who records today's traffic and steals the server's
private key next year still cannot read it, because that key was not what
encrypted it.

That was not always true. Older TLS allowed the client to pick the secret and
send it encrypted with the server's public key, so the private key *was* the
key to everything ever recorded. TLS 1.3 removed that option entirely.

## What a certificate actually is

A certificate is a public key, some names, some dates, and somebody else's

![What is inside a certificate](diagrams/https-notes-certificate.jpg)
signature over all of it.

| Field | What it holds |
|---|---|
| Subject | The name the certificate is issued to |
| **Subject Alternative Names** | The domain names it is actually valid for |
| Public key | The key half that anyone may have |
| Issuer | Who signed it |
| Not before / not after | The validity window |
| Key usage | What it may be used for — signing, key agreement |
| Signature | The issuer's signature over everything above |

The Subject Alternative Names field is the one that matters and the one people
still get wrong: browsers stopped looking at the common name years ago. A
certificate for \`example.com\` that does not list \`www.example.com\` in its SANs
will fail on \`www\`, however sensible that looks to a human.

The signature is what makes it worth anything. Anyone can generate a certificate
claiming to be your bank — that takes one command. What they cannot do is get a
certificate authority your browser already trusts to sign it, because the
authority will only do that after checking you control the domain.

## The chain of trust

![The chain from a root certificate down to the one a server sends](diagrams/https-notes-chain.jpg)

Your browser does not know your bank. It knows about a hundred or so root
authorities, shipped with the operating system or the browser, and the bank's
certificate leads back to one of them:

\`\`\`text
  ISRG Root X1                 in your machine's trust store already
        signs
  R11 (intermediate)           short-lived, lives with the authority
        signs
  bank.example.com             the certificate the server sends you
\`\`\`

The server sends its own certificate **and** the intermediates, but not the
root — you already have that, and sending it would prove nothing. Verification
walks the chain: each certificate's signature is checked against the public key
of the one above it, until it reaches a certificate already in the trust store.

A missing intermediate is the classic deployment fault. It works in your browser
— which quietly fetched the intermediate earlier from another site — and fails
in curl, in Java, and on a colleague's phone.

## What the client checks, and in what order

When a certificate arrives, roughly this happens:

1. **Does the chain lead to a trusted root**, with every signature valid?
2. **Is every certificate in the chain currently within its dates?** An expired
   certificate is the single most common HTTPS outage, and it happens at a time
   nobody chose.
3. **Does the name match?** The domain you asked for against the SANs — with
   wildcards matching one label only, so \`*.example.com\` covers \`a.example.com\`
   and not \`a.b.example.com\`.
4. **Is it allowed to be used this way**, per its key usage and extensions?
5. **Has it been revoked?** In practice the weakest check of the five — see the
   [advanced page](#/advanced/deep-https/advanced).

Any failure is fatal by design. The browser's "proceed anyway" button is a
deliberate inconvenience, and it does not appear at all on a site that has sent
an HSTS header before.

## What HTTPS does not hide

![What TLS hides and what it leaves visible](diagrams/https-notes-not-hidden.jpg)

Encryption covers the request and the response. It does not cover the fact that
you made one.

- **The domain name is visible.** The DNS lookup usually goes out in clear text,
  and the name is sent in the handshake so that one address can serve many sites
  — that is the SNI field, and it is not encrypted by default.
- **The address, timing and size are visible.** An observer sees that you talked
  to a machine, when, and roughly how much. Traffic analysis can identify which
  page of a site you loaded from the size pattern alone.
- **Nothing is protected at the ends.** TLS protects the wire. Once decrypted,
  the data is as safe as the server it lands on, and your own machine's
  antivirus, browser extensions and corporate proxy are all inside the tunnel.
- **A valid certificate is not a character reference.** It proves control of a
  domain, nothing more.

## Getting everybody onto it

Serving HTTPS is not enough if the first request still goes to \`http://\`. Three
mechanisms close that gap:

**The redirect.** \`http://\` answers \`301\` to the \`https://\` URL. Necessary, and
by itself insufficient — that first plain request can be intercepted before the
redirect arrives.

**HSTS.** A header that tells the browser to never use plain HTTP for this site
again:

\`\`\`text
Strict-Transport-Security: max-age=31536000; includeSubDomains; preload
\`\`\`

After it is seen once, the browser rewrites \`http://\` to \`https://\` before
sending anything, and refuses to let a user click through a certificate warning.
It is remembered for the whole \`max-age\`, which is why testing with a large
value on a domain you have not finished migrating is a way to lose a weekend.

**Preloading.** Being on the list that ships inside browsers, so the protection
applies even on the very first visit. It is a request form and a slow removal
process — think before submitting.

**Mixed content.** An HTTPS page that pulls a script over HTTP is not secure —
the script can be replaced in transit and it can rewrite the page. Browsers
block active mixed content outright and upgrade or block passive content. The
\`upgrade-insecure-requests\` policy fixes an old codebase without editing every
URL.

## Certificates in practice

- **They are free and automatic now.** Let's Encrypt issues them over the ACME
  protocol; a client such as certbot proves control of the domain, fetches the
  certificate and renews it. Ninety-day lifetimes are deliberate: they force the
  renewal to be automated, because anything done by hand annually is eventually
  forgotten.
- **Wildcards versus SANs.** One \`*.example.com\` covers every subdomain and
  requires a DNS-based proof; a multi-SAN certificate lists exactly the names
  you have. Prefer explicit names unless the subdomains are dynamic.
- **Expiry is an outage.** Monitor the *not after* date as you would disk space.
  The famous ones — payment networks, government sites, telephone networks —
  were all somebody assuming the renewal cron job was still running.
- **Self-signed is fine for local development** and useless for anything a
  browser will visit. An internal certificate authority whose root you install
  on company machines is the right answer inside an organisation.

## When it goes wrong

- **\`NET::ERR_CERT_DATE_INVALID\`** — expired, or the machine's own clock is
  wrong. Check the clock before blaming the site.
- **\`NET::ERR_CERT_COMMON_NAME_INVALID\`** — the name you asked for is not in the
  SANs. Usually \`www\` versus the bare domain.
- **\`ERR_CERT_AUTHORITY_INVALID\`, or Java's \`PKIX path building failed\`** — the
  chain does not reach a trusted root. Either the server is not sending its
  intermediates, or the client's trust store does not contain the root, which is
  what happens with a corporate proxy that re-signs everything.
- **\`SSLHandshakeException: No appropriate protocol\`** — the two ends share no
  version or cipher suite. Modern client, ancient server, and the correct fix is
  never to re-enable TLS 1.0 permanently.
- **The site works in the browser and fails in curl or Java** — almost always
  the missing intermediate above. Browsers hide the problem; nothing else does.
- **Mixed content warnings after a migration** — a hard-coded \`http://\` in a
  template, a CDN URL, or a third-party widget.

## What to read next

- **[HTTPS, underneath](#/advanced/deep-https/advanced)** — the handshake
  message by message, cipher suites decoded, revocation and why it mostly does
  not work, resumption and 0-RTT, and the Java side: keystores, \`cacerts\`, and
  reading a handshake log.
- **[HTTP, request and response](#/advanced/deep-http)** — what is travelling
  inside the tunnel.

## Further reading

- [RFC 8446 — TLS 1.3](https://www.rfc-editor.org/rfc/rfc8446.html)
- [Let's Encrypt: how it works](https://letsencrypt.org/how-it-works/)
- [Mozilla's server-side TLS configuration guide](https://wiki.mozilla.org/Security/Server_Side_TLS)
`,ye=`The [notes](#/advanced/deep-https) are about what TLS promises and what a
certificate is. This is the machinery: the handshake message by message, what
1.3 threw away and why, how the chain is really validated, and what the errors
mean when Java refuses to connect.

## The 1.2 handshake, message by message

Worth seeing once, because every diagnostic tool still speaks in these names and
because 1.3 is best understood as this with things removed.

\`\`\`text
ClientHello        versions, cipher suites, random, extensions (SNI, ALPN)
    ServerHello    the chosen version and suite, its own random
    Certificate    the server certificate, plus intermediates
    ServerKeyExchange   its ECDHE public value, signed with the certificate key
    ServerHelloDone
ClientKeyExchange  the client ECDHE public value
ChangeCipherSpec   everything after this point is encrypted
Finished           a hash of the whole handshake so far
    ChangeCipherSpec
    Finished
\`\`\`

Two round trips before any application data, on top of TCP's one. The signature
on \`ServerKeyExchange\` is the join between the two halves: it proves that
whoever chose that key exchange value also holds the private key belonging to
the certificate. Without it, an attacker in the middle could substitute their
own value and the certificate would be decoration.

## What 1.3 removed

![TLS 1.2 and TLS 1.3 handshakes compared](diagrams/https-advanced-twelve-thirteen.jpg)

TLS 1.3 is mostly a deletion, and each deletion closed a hole:

- **RSA key transport is gone.** The client can no longer encrypt the secret to
  the server's public key, so every connection now has forward secrecy. That
  alone makes recorded traffic worthless to a future key theft.
- **Renegotiation, compression and custom groups are gone**, each having been
  the root of a named attack — CRIME used TLS compression to read cookies a byte
  at a time.
- **The cipher suite list shrank from hundreds to five.** No RC4, no 3DES, no
  CBC modes, no MD5 or SHA-1 signatures. A suite you can choose is now a suite
  that is safe.
- **One round trip instead of two**, because the client guesses the key exchange
  group and sends its share in the first message. A wrong guess costs a
  \`HelloRetryRequest\` and one extra trip.
- **The certificate is encrypted.** In 1.2 a passive observer could read which
  certificate a server served; in 1.3 everything after the first exchange is
  under encryption.

## A cipher suite, decoded

![A cipher suite name broken into its parts](diagrams/https-advanced-suite.jpg)

\`\`\`text
TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256
     |     |         |        |    |
     |     |         |        |    +-- hash, used to derive the keys
     |     |         |        +------- mode: encrypts and authenticates at once
     |     |         +---------------- bulk cipher and key size
     |     +-------------------------- how the certificate is signed
     +-------------------------------- key exchange, ephemeral
\`\`\`

A 1.3 suite drops the first two fields, because the key exchange is always
ephemeral and the signature algorithm is negotiated separately:
\`TLS_AES_256_GCM_SHA384\` names only the cipher and the hash.

**GCM matters more than the key size.** It encrypts and authenticates in one
pass, so there is no "decrypt now, check the tag later" mistake available to
make. The older CBC modes needed a separate authentication step and got the
order wrong often enough to produce a decade of padding-oracle attacks.

## What your runtime already offers

Nothing here connects anywhere. Every answer comes from the JDK's own defaults,
which is the point: most of the decisions were taken before you wrote a line.

\`\`\`java TlsFacts.java @run-https-tls-facts
import java.security.KeyStore;
import java.security.cert.X509Certificate;
import java.util.Arrays;
import javax.net.ssl.SSLContext;
import javax.net.ssl.SSLParameters;
import javax.net.ssl.TrustManagerFactory;
import javax.net.ssl.X509TrustManager;

/** What this machine will agree to, and who it already believes. */
public class TlsFacts {

    public static void main(String[] args) throws Exception {
        SSLParameters defaults = SSLContext.getDefault().getDefaultSSLParameters();

        System.out.println("protocols this JDK will offer:");
        for (String protocol : defaults.getProtocols()) {
            System.out.println("  " + protocol);
        }

        System.out.println();
        System.out.println("first four cipher suites it would propose, in order:");
        Arrays.stream(defaults.getCipherSuites()).limit(4)
                .forEach(suite -> System.out.println("  " + suite));

        // Passing null asks for the default trust store — the \`cacerts\` file
        // inside the JDK — without naming a path that differs on every machine.
        TrustManagerFactory factory =
                TrustManagerFactory.getInstance(TrustManagerFactory.getDefaultAlgorithm());
        factory.init((KeyStore) null);
        X509TrustManager trust = (X509TrustManager) factory.getTrustManagers()[0];
        X509Certificate[] anchors = trust.getAcceptedIssuers();

        System.out.println();
        System.out.println("certificate authorities trusted out of the box: " + anchors.length);
        Arrays.stream(anchors)
                .map(certificate -> certificate.getSubjectX500Principal().getName())
                .filter(name -> name.contains("ISRG") || name.contains("DigiCert Global Root CA"))
                .sorted()
                .forEach(name -> System.out.println("  " + name));
    }
}
\`\`\`

\`\`\`output @run-https-tls-facts
protocols this JDK will offer:
  TLSv1.3
  TLSv1.2

first four cipher suites it would propose, in order:
  TLS_AES_256_GCM_SHA384
  TLS_AES_128_GCM_SHA256
  TLS_CHACHA20_POLY1305_SHA256
  TLS_ECDHE_ECDSA_WITH_AES_256_GCM_SHA384

certificate authorities trusted out of the box: 109
  CN=DigiCert Global Root CA,OU=www.digicert.com,O=DigiCert Inc,C=US
  CN=ISRG Root X1,O=Internet Security Research Group,C=US
  CN=ISRG Root X2,O=Internet Security Research Group,C=US
\`\`\`

Three things to take from it. The offered protocols are 1.3 and 1.2 and nothing
older, because everything before them is broken and was removed. The preferred
suites are the 1.3 ones, strongest first. And **109 organisations are trusted by
default** — any one of which can issue a certificate for any domain, which is
the structural weakness of the whole system and the reason Certificate
Transparency exists.

## A handshake you can run

One command makes a key pair and a self-signed certificate for \`localhost\`:

\`\`\`bash @tls-keystore
keytool -genkeypair -alias localhost -keyalg EC -groupname secp256r1 -sigalg SHA256withECDSA -dname "CN=localhost, O=After Hours, C=GB" -validity 365 -ext "SAN=DNS:localhost,IP:127.0.0.1" -keystore local.p12 -storetype PKCS12 -storepass changeit
\`\`\`

\`\`\`output @tls-keystore
Generating 256-bit EC (secp256r1) key pair and self-signed certificate (SHA256withECDSA) with a validity of 365 days
	for: CN=localhost, O=After Hours, C=GB
\`\`\`

Then a small server that uses it, and two clients — one told to trust that
certificate, one not:

\`\`\`java TlsHandshake.java
import com.sun.net.httpserver.HttpsConfigurator;
import com.sun.net.httpserver.HttpsServer;
import java.net.InetSocketAddress;
import java.nio.file.Files;
import java.nio.file.Path;
import java.security.KeyStore;
import java.security.cert.X509Certificate;
import javax.net.ssl.KeyManagerFactory;
import javax.net.ssl.SSLContext;
import javax.net.ssl.SSLSession;
import javax.net.ssl.SSLSocket;
import javax.net.ssl.TrustManagerFactory;

/** One TLS handshake, done twice: by a client that trusts the certificate, and by one that does not. */
public class TlsHandshake {

    static final char[] PASSWORD = "changeit".toCharArray();

    public static void main(String[] args) throws Exception {
        // The file keytool just made. It holds one key pair and one certificate.
        KeyStore store = KeyStore.getInstance("PKCS12");
        try (var in = Files.newInputStream(Path.of("local.p12"))) {
            store.load(in, PASSWORD);
        }

        // The server proves who it is with it; the client decides who to believe from it.
        KeyManagerFactory identity = KeyManagerFactory.getInstance("PKIX");
        identity.init(store, PASSWORD);
        TrustManagerFactory whoToBelieve = TrustManagerFactory.getInstance("PKIX");
        whoToBelieve.init(store);

        SSLContext context = SSLContext.getInstance("TLS");
        context.init(identity.getKeyManagers(), whoToBelieve.getTrustManagers(), null);

        HttpsServer server = HttpsServer.create(new InetSocketAddress("127.0.0.1", 0), 0);
        server.setHttpsConfigurator(new HttpsConfigurator(context));
        server.createContext("/", exchange -> exchange.sendResponseHeaders(204, -1));
        server.start();
        int port = server.getAddress().getPort();

        try (SSLSocket socket = (SSLSocket) context.getSocketFactory()
                .createSocket("127.0.0.1", port)) {

            // Nothing is agreed until this returns. No HTTP has been sent yet.
            socket.startHandshake();
            SSLSession session = socket.getSession();

            System.out.println("a client that trusts the certificate");
            System.out.println("  protocol : " + session.getProtocol());
            System.out.println("  cipher   : " + session.getCipherSuite());

            X509Certificate served = (X509Certificate) session.getPeerCertificates()[0];
            System.out.println("  subject  : " + served.getSubjectX500Principal());
            System.out.println("  issued by: " + served.getIssuerX500Principal());
        }

        // The same server, reached with the JDK's own list of authorities. It has
        // never heard of this certificate, and says so.
        System.out.println("a client that does not");
        try (SSLSocket stranger = (SSLSocket) SSLContext.getDefault().getSocketFactory()
                .createSocket("127.0.0.1", port)) {
            stranger.startHandshake();
        } catch (Exception refused) {
            for (Throwable cause = refused; cause != null; cause = cause.getCause()) {
                System.out.println("  " + cause.getClass().getSimpleName() + ": " + cause.getMessage());
            }
        }
        server.stop(0);
    }
}
\`\`\`

\`\`\`bash @tls-handshake
java TlsHandshake.java
\`\`\`

\`\`\`output @tls-handshake
a client that trusts the certificate
  protocol : TLSv1.3
  cipher   : TLS_AES_256_GCM_SHA384
  subject  : CN=localhost, O=After Hours, C=GB
  issued by: CN=localhost, O=After Hours, C=GB
a client that does not
  SSLHandshakeException: (certificate_unknown) PKIX path building failed: sun.security.provider.certpath.SunCertPathBuilderException: unable to find valid certification path to requested target
  ValidatorException: PKIX path building failed: sun.security.provider.certpath.SunCertPathBuilderException: unable to find valid certification path to requested target
  SunCertPathBuilderException: unable to find valid certification path to requested target
\`\`\`

\`subject\` and \`issued by\` being the same string is what self-signed means: it
vouches for itself, and a signature you write for yourself convinces nobody. The
second half is the error every Java developer meets eventually, and reading it
backwards gives the diagnosis — *no certification path*, so the chain never
reached anything in the trust store.

**Do not fix it by trusting everything.** The \`TrustManager\` that accepts any
certificate, pasted from an answer site, deletes the authentication promise and
leaves the padlock in place. The real fixes are to install the missing
intermediate on the server, or to add the internal root to the client's trust
store.

## Keystore and truststore

![Keystore against truststore in Java](diagrams/https-advanced-stores.jpg)

Java's two files are constantly confused, and the distinction is exactly the two
sides of the handshake:

| | Holds | Used when |
|---|---|---|
| **Keystore** | Your private key and your certificate | You are proving who *you* are — a server, or a client doing mTLS |
| **Truststore** | Other people's root certificates | You are deciding whether to believe *them* |

The default truststore is \`$JAVA_HOME/lib/security/cacerts\` and its default
password is \`changeit\`. Either file can be replaced with system properties:

\`\`\`bash
java -Djavax.net.ssl.trustStore=corp.p12 -Djavax.net.ssl.trustStorePassword=secret App.java
\`\`\`

To see what is inside one, or what a server is really sending:

\`\`\`bash
keytool -list -v -keystore local.p12 -storepass changeit
openssl s_client -connect example.com:443 -servername example.com
\`\`\`

The \`openssl\` line is the fastest way to answer "is the server sending its
intermediates", which is a different question from what your browser managed to
build for itself.

## Reading a handshake

\`\`\`bash
java -Djavax.net.debug=ssl:handshake App.java
\`\`\`

That prints every message with its extensions, the certificates offered, and the
point of failure. It is far too verbose for production and is the only sane
first step when a connection fails on one machine and works on another.
\`ssl:handshake:verbose\` adds the raw bytes; \`all\` adds the record layer and is
rarely what you want.

## The chain, and why intermediates exist

A root authority's private key is the most valuable object in the system, so it
lives offline and signs almost nothing — only intermediate certificates. Those
intermediates do the day-to-day issuing.

The arrangement means a compromised intermediate can be revoked and replaced
without touching the roots that ship inside every browser and operating system,
which cannot be updated quickly because they live in devices that will never be
patched again.

For whoever runs the server, this becomes a duty: **send the chain** — leaf,
then intermediates, in order, no root. Authorities give you a \`fullchain.pem\`
for exactly this reason, and a configuration pointing at \`cert.pem\` instead is
the most common cause of "it works in Chrome and nowhere else".

## Revocation, and why it barely works

![The four revocation mechanisms and how well each works](diagrams/https-advanced-revocation.jpg)

A certificate stolen on day one stays valid for its whole lifetime unless
something says otherwise. Four attempts at that something:

- **CRL** — a list of revoked serial numbers published by the authority. The
  lists grew to megabytes and are checked by almost nobody.
- **OCSP** — ask the authority about one certificate. Better, except that it
  tells the authority what you are browsing, adds a round trip, and leaves the
  browser choosing between failing every connection when the responder is down
  and ignoring the answer. Browsers chose to ignore it, which makes it theatre.
- **OCSP stapling** — the *server* fetches its own signed status periodically
  and includes it in the handshake. No privacy leak, no extra round trip. This
  is the one that works, and it has to be turned on.
- **Must-staple** — a flag in the certificate meaning "reject me if no staple is
  attached", which turns stapling from advisory into enforced.

The honest summary is that revocation is unreliable and the industry's real
answer is **short lifetimes**. A 90-day certificate that expires by itself is
worth more than a three-year one that could theoretically be revoked.

## Certificate Transparency

Since any of those hundred-odd authorities can issue for any domain, the
question is how you would ever find out that one had. The answer is public
append-only logs: an authority submits every certificate it issues and receives
a signed timestamp proving it did, and browsers reject certificates that carry
no such proof.

Anyone can watch the logs for their own domains, which is how mis-issuance is
now caught in hours rather than never. It turns an unfalsifiable trust
assumption into an auditable record.

## Resumption, tickets and 0-RTT

![Zero round trip resumption and the replay it allows](diagrams/https-advanced-zero-rtt.jpg)

A full handshake is expensive, so TLS lets a returning client skip most of it.
In 1.3 the server hands out a **session ticket**: the session state, encrypted
with a key only the server holds. The client presents it later as a pre-shared
key and the handshake gets shorter.

**0-RTT** goes further — the client sends application data *with* its first
message, using the resumed key. Zero round trips, and one serious catch: that
data is **replayable**. Anyone who captured it can send it again and the server
cannot tell the difference. So 0-RTT is for idempotent requests only: a \`GET\` of
a public page, never a \`POST\` that moves money. Enable it knowing that, or leave
it off.

Ticket keys are the other subtlety. They must be rotated, and every server
behind a load balancer needs the same ones, or resumption silently never
happens and you pay full handshakes while believing you do not.

## SNI, ALPN and ECH

Three extensions in the first message, all doing work you have relied on today.

- **SNI** names the host you want, so one address can serve thousands of
  certificates. It is sent before encryption starts, so it is visible — which is
  what makes domain-level blocking possible.
- **ALPN** negotiates the protocol inside the tunnel. This is how HTTP/2 is
  chosen: not by a redirect or an upgrade, but as a string in the handshake —
  \`h2\`, \`http/1.1\`, \`h3\`.
- **ECH**, Encrypted Client Hello, encrypts that first message including SNI,
  using a key published in DNS. It closes the last obvious leak, and its
  deployment depends on encrypted DNS arriving first.

## Mutual TLS

Ordinary TLS authenticates the server. mTLS authenticates both: the server sends
a \`CertificateRequest\`, and the client presents a certificate and proves it
holds the matching key.

It is the standard for service-to-service traffic inside a cluster — a service
mesh is largely automated mTLS — because it takes shared secrets out of the
picture entirely. The cost is a certificate lifecycle for every client, which is
why it is rare on the public web and ordinary inside infrastructure.

## Termination, and where the trust boundary really is

Almost no production application terminates TLS itself. A load balancer or CDN
does, and what runs behind it is plain HTTP on a private network — or a second
TLS connection, when that network is not trusted either.

Two consequences worth keeping straight. The application no longer knows the
connection was secure, so it reads \`X-Forwarded-Proto\` — and must be configured
to believe that header only from the balancer. And a CDN that terminates TLS can
read everything, which is a deliberate trade: you get caching and denial-of-
service protection, and you have added a party to the conversation.

## Pinning, and why browsers withdrew it

Pinning means refusing any certificate but a named one, so a compromised
authority cannot help an attacker. \`HPKP\` did this over HTTP headers and was
removed from browsers, because a wrong pin or a lost key bricks the domain for
the whole pin lifetime and the failure was worse than the attack. It remains
right in mobile applications, where the pins ship with the binary and both are
updated together.

## Further reading

- [RFC 8446 — TLS 1.3](https://www.rfc-editor.org/rfc/rfc8446.html)
- [The Illustrated TLS 1.3 Connection](https://tls13.xargs.org/)
- [Mozilla's server-side TLS configuration guide](https://wiki.mozilla.org/Security/Server_Side_TLS)
- [Certificate Transparency](https://certificate.transparency.dev/)
`,be=`Thirteen questions on HTTPS and TLS. Interviewers use this subject to find out
whether you understand *why* the mechanism exists — nearly every follow-up is
some form of "and what stops an attacker doing that?"

The **Notes** cover the promises, the handshake and the certificate. The
**Advanced** page covers the messages, revocation, resumption and the Java side.

## Easy

### 1. What does HTTPS actually add to HTTP?

Three things, and they are separate. **Confidentiality** — nobody on the network
path can read the traffic. **Integrity** — nobody can change it undetected,
because every record carries an authentication tag. **Authentication** — the
server proves it is the one that owns the domain you asked for. The HTTP inside
is completely unchanged: same methods, same status codes, same headers.

### 2. What does the padlock actually prove?

That the connection is encrypted and that the server proved control of that
domain name to a certificate authority. It does not mean the site is honest, the
company is real, or your data is safe once it arrives. Phishing sites routinely
have perfectly valid certificates, because a domain-validated certificate is
free and takes about a minute to obtain.

### 3. What is in a certificate?

A public key, the names it is valid for, a validity window, what the key may be
used for, and a signature from the issuer over all of it. The names that matter
are in the **Subject Alternative Names** extension — browsers stopped consulting
the common name years ago — and a wildcard there matches exactly one label, so
\`*.example.com\` does not cover \`a.b.example.com\`.

### 4. Why is a self-signed certificate rejected?

Because its issuer is itself, and there is no reason for a stranger to believe
it. Validation walks a chain of signatures from the certificate you were handed
up to a root already installed on your machine; a self-signed certificate is a
chain of one that never reaches such a root. It is fine for local development,
where you can add it to your own trust store deliberately.

## Medium

### 5. How do two strangers agree on a key over an open network?

Each side picks a private number and sends the public value derived from it.
Each then combines its own private number with the other's public value, and the
arithmetic is arranged so both arrive at the same answer while an observer with
only the two public values cannot compute it. That is Diffie–Hellman; TLS uses
the elliptic-curve form, ECDHE. The certificate is not what encrypts anything —
it is what signs the exchange, proving the party choosing that key share is the
one the certificate names.

### 6. What is forward secrecy, and how do you get it?

Recorded traffic stays unreadable even if the server's private key is stolen
later. You get it by generating a fresh key pair for every connection and
throwing it away afterwards — the E for ephemeral in ECDHE. The alternative, old
RSA key transport, had the client encrypt the secret with the server's long-term
public key, which made that one key sufficient to decrypt every conversation
ever recorded. TLS 1.3 removed the option entirely.

### 7. What did TLS 1.3 change?

Mostly deletions. RSA key transport gone, so every connection has forward
secrecy. Renegotiation and compression gone, each having been the root of a
named attack. The cipher suite list cut from hundreds to five, with no CBC
modes, no RC4 and no SHA-1. The handshake reduced to one round trip, by having
the client send a key share with its first message. And the certificate is now
sent encrypted, so a passive observer cannot see which one was used.

### 8. Why does a site work in the browser but fail in curl or Java?

Almost always a missing intermediate certificate. The server must send its leaf
plus the intermediates that link it to a root; if it sends only the leaf, a
browser often still succeeds because it cached that intermediate from another
site or fetched it from the URL in the certificate. Nothing else does that, so
curl and Java report a broken chain — in Java's words, \`PKIX path building
failed\`. The fix is on the server: serve the full chain.

### 9. What is HSTS and what problem does it solve?

\`Strict-Transport-Security\` tells the browser to use HTTPS for this host for the
next \`max-age\` seconds, and to refuse any click-through on a certificate
warning. It closes the gap left by a plain redirect: the very first \`http://\`
request can be intercepted before the redirect ever arrives. The catch is that
it is remembered — setting a year on a domain you have not finished migrating
locks visitors out of the parts that are still plain HTTP, and preloading makes
that worse by shipping the rule inside the browser.

### 10. What does HTTPS not protect?

The fact that you connected at all. The DNS lookup is usually in clear text, and
SNI — the host name, sent so one address can serve many certificates — is
unencrypted in the handshake, so the domain is visible. Timing and message sizes
are visible too, and are enough to identify which page of a site was loaded. And
nothing at either end is protected: once the tunnel terminates, at a CDN or a
load balancer, the traffic is plain again to whoever runs it.

## Hard

### 11. Why is certificate revocation considered broken, and what replaced it?

Because there is no reliable way to tell a client that a valid-looking
certificate has been withdrawn. Revocation lists grew to megabytes and are
largely unread; OCSP asks the authority about a single certificate, which leaks
browsing history, adds a round trip, and forces browsers to choose between
failing when the responder is down and ignoring the answer — they chose to
ignore it. OCSP stapling fixes the privacy and latency problems by having the
server include its own recently signed status in the handshake, and must-staple
makes it mandatory. But the real answer the industry settled on is short
lifetimes: a certificate that expires in 90 days and renews automatically limits
the damage without needing revocation to work.

### 12. What is 0-RTT, and why is it dangerous?

On a resumed connection, TLS 1.3 lets the client send application data along
with its very first message, using a key derived from the previous session — so
the request costs no round trips at all. The danger is that this early data is
not tied to a fresh exchange, so anyone who captured it can send it again and
the server has no way to tell the copy from the original. It is therefore safe
only for idempotent requests: a \`GET\` of a public resource, never anything that
creates an order or moves money. Servers that accept 0-RTT should restrict it by
method and path rather than trusting clients to be careful.

### 13. Any of a hundred authorities can issue a certificate for your domain. What stops that being fatal?

Nothing prevents it — that is the structural weakness of the system, and it has
happened. What changed is that it can no longer happen quietly. **Certificate
Transparency** requires every issued certificate to be submitted to public
append-only logs, and browsers reject certificates that arrive without proof of
submission. Anyone can monitor the logs for their own domains, so mis-issuance
is detected in hours rather than never. Pinning was the other answer — refusing
any certificate but a named one — and browsers withdrew it because a wrong pin
bricks a domain for the whole pin lifetime; it survives in mobile applications,
where pins and binary ship together.

## What to revise next

- **[HTTP, request and response](#/advanced/deep-http)** — what travels inside
  the tunnel, and the caching, retry and framing rules that decide how much of
  it there is.
`,xe=`The chapter in a page. If a line here is news, the **Notes** or **Advanced**
part is where it comes from.

## What TLS promises

- **Confidentiality** — nobody on the path can read it. **Integrity** — nobody
  can change it without breaking the connection. **Authentication** — the far
  end is who it says it is.
- The first two are worthless without the third: an encrypted connection to an
  attacker is a private conversation with the wrong person.
- The padlock means the connection is private and the server proved it controls
  that domain. It says nothing about whether the site is honest.
- HTTP inside the tunnel is unchanged. Same methods, same status codes, same
  headers.

## The handshake

- TLS 1.3 is one round trip: \`ClientHello\` with a key share, \`ServerHello\` with
  its share plus the certificate and a signature, then \`Finished\` both ways.
- Four jobs: agree a version and suite, agree a key, prove identity, confirm
  nothing was tampered with.
- **ECDHE** — both sides swap public values and derive the same secret; the
  secret never crosses the wire. The final E is *ephemeral*, which is what gives
  **forward secrecy**.
- TLS 1.3 removed RSA key transport, renegotiation, compression, CBC modes and
  every weak suite. Five suites remain.
- 1.3 also encrypts the certificate, so a passive observer no longer sees it.

## The certificate

- A public key, some names, some dates, and an issuer's signature over all of
  it.
- **Subject Alternative Names** decide which hosts it is valid for. The common
  name has not been consulted for years.
- Wildcards match one label: \`*.example.com\` covers \`a.example.com\`, not
  \`a.b.example.com\`.
- The chain runs leaf → intermediate(s) → root. The server sends everything
  except the root; you already have that.
- A missing intermediate works in a browser and fails everywhere else. Serve
  \`fullchain.pem\`.

## What the client checks

- Chain to a trusted root, every signature valid.
- Dates — expiry is the most common HTTPS outage there is.
- Hostname against the SANs.
- Key usage, then revocation, which is the weakest check of the five.

## Revocation

- **CRL** — huge lists nobody reads. **OCSP** — leaks browsing history, adds a
  round trip, and browsers ignore failures, so it is theatre.
- **OCSP stapling** is the one that works: the server fetches its own signed
  status and includes it in the handshake. **Must-staple** enforces it.
- The industry's real answer is short lifetimes — 90 days, renewed
  automatically over ACME.

## What HTTPS does not hide

- The domain name: DNS is usually clear text, and SNI is sent unencrypted so one
  address can serve many certificates. ECH is the fix, and needs encrypted DNS
  first.
- Timing, sizes and who you talked to. Page identity can be recovered from size
  patterns alone.
- Anything at the ends. Once decrypted it is as safe as the server, and a CDN
  that terminates TLS reads everything.

## Getting everyone onto it

- Redirect \`http://\` to \`https://\`, then send **HSTS** so the browser stops
  using plain HTTP at all — and refuses to let anyone click through a warning.
- \`max-age\` is remembered. Test with a small one before committing a year.
- **Mixed content**: an HTTPS page loading an HTTP script is not secure, and
  browsers block it.

## Java

- **Keystore** holds your key and certificate — proving who you are.
  **Truststore** holds other people's roots — deciding who to believe. The
  default is \`cacerts\`, password \`changeit\`.
- \`PKIX path building failed\` means the chain reached nothing trusted: a missing
  intermediate, or a corporate root you have not installed.
- Never "fix" it with a trust-everything \`TrustManager\`. That deletes
  authentication and keeps the padlock.
- \`-Djavax.net.debug=ssl:handshake\` prints the whole negotiation.
  \`openssl s_client -connect host:443 -servername host\` shows what the server
  actually sends.

## Resumption

- Session tickets let a returning client skip most of the handshake.
- **0-RTT** sends data in the first message and is **replayable** — idempotent
  requests only, never a \`POST\` that moves money.
- Ticket keys must be rotated and shared across every server behind a load
  balancer, or resumption quietly never happens.
`,Se=`The [HTTP pages](#/advanced/deep-http) end where the response arrives. Nothing
is on screen yet — what came back is a stream of bytes, and there is a
surprising amount of work between those bytes and a pixel.

Knowing that work is not optional trivia. Every argument about where a page
should be rendered — on your server or in the reader's browser — is really an
argument about which of the steps below happens when, and which of them one
missing file can stop dead.

## Bytes to pixels, in five steps

![The five steps from bytes to pixels](diagrams/browser-notes-pipeline.jpg)

\`\`\`text
   bytes      HTML  ->  DOM      a tree of elements
              CSS   ->  CSSOM    a tree of styles
                        |
              render tree        the visible elements, with their styles
                        |
              layout             where each box goes, and how big
                        |
              paint              what colour every pixel is
                        |
              composite          the layers, stacked, on screen
\`\`\`

Each step needs the one before it, and that dependency is where all the delay
comes from. Two facts follow immediately, and they explain most of what a
performance tool will ever tell you:

**Nothing paints without the CSSOM.** The browser will not show you a half
styled page, because a flash of unstyled content is worse than a moment of
blank. So a stylesheet blocks the first paint of the whole page.

**Layout is expensive and cascades.** Changing the width of one element can move
everything after it, so layout is per-frame work the browser tries very hard to
avoid repeating.

## The DOM is not the HTML

The HTML you sent is text. The DOM is the object tree the browser built from it,
and after that point the text is irrelevant — every script, every style rule and
every measurement works on the tree.

The distinction matters more than it sounds. A page whose HTML is one empty
\`<div>\` has a DOM of one empty div, and if JavaScript later builds ten thousand
nodes, those nodes were never in the HTML at all. Anything reading the HTML
rather than the DOM — a crawler that does not run scripts, a "view source", a
link preview — sees the empty div. That single fact is most of what the
[rendering page](#/interview/deep-rendering) is about.

The browser is also very forgiving here. Unclosed tags, misnested elements and
missing \`<tbody>\` are all repaired as the tree is built, which is why the DOM
frequently does not match what you typed.

## What blocks what

![What each tag in the head blocks](diagrams/browser-notes-blocking.jpg)

A parser reading top to bottom hits three kinds of thing, and they behave
differently.

| In the head | Blocks parsing | Blocks first paint | Notes |
|---|---|---|---|
| \`<link rel="stylesheet">\` | no | **yes** | The parser continues; nothing paints until it lands |
| \`<script src>\` | **yes** | yes | Parsing stops dead — the script might write to the document |
| \`<script src defer>\` | no | no | Runs after parsing, in order |
| \`<script src async>\` | no | no | Runs the moment it arrives, in any order |
| \`<script type="module">\` | no | no | Deferred by default |
| \`<img>\` | no | no | Laid out around; may shift the page when it lands |
| Web font | no | no | But text using it may be invisible until it arrives |

The classic ordinary script is the one to be careful about. \`<script src="…">\`
in the head with no attribute stops the parser, waits for the network, executes,
and only then does parsing resume — which is why "put scripts at the end of the
body" was advice for a decade, and why \`defer\` is the better version of that
advice now.

**\`defer\` is what you usually want**: it downloads in parallel, waits for the
DOM, and runs scripts in the order you wrote them. **\`async\`** is for things
that depend on nothing and that nothing depends on — analytics, and little else.

## One thread does nearly all of it

Parsing, style, layout, paint, and every line of your JavaScript run on the
**main thread**, one at a time. There is no second thread quietly running the
page while a script works.

That is the whole reason "JavaScript is blocking the page" is a sentence people
say. A function that takes 300 milliseconds is 300 milliseconds in which nothing
scrolls, no button responds, and no animation moves. The browser is not stuck —
it is waiting for its turn.

The loop the thread runs is worth carrying in your head:

![One task, its microtasks, and then a frame](diagrams/browser-notes-event-loop.jpg)

\`\`\`text
  take one task           (a script, an event handler, a timer)
    run it to completion  (nothing interrupts it)
  drain the microtasks    (promise callbacks — before anything is drawn)
  maybe render a frame    (style, layout, paint — about every 16ms if it can)
\`\`\`

Two consequences of "run it to completion": a \`while\` loop that never ends
freezes the tab, and a promise \`.then()\` runs *before* the next frame is drawn
rather than after the current one. Work has to be broken up voluntarily; nothing
will do it for you.

## Reflow and repaint

When something changes, the browser redoes as little as it can.

![What each kind of change costs](diagrams/browser-notes-change-cost.jpg)

- Change a colour, and it **repaints** — no geometry moved.
- Change a width, a font size, or add an element, and it **reflows**: layout
  runs again for everything affected, then paints.
- Change only \`transform\` or \`opacity\`, and it can often skip both, handing the
  work to the compositor — which is why animations should use those two
  properties and nothing else.

Animating \`left\` moves an element by making the browser lay out the page sixty
times a second. Animating \`transform\` moves an already-painted layer. The
difference on a phone is the difference between smooth and unusable.

## What the numbers mean

![The five metrics and what each one blames](diagrams/browser-notes-metrics.jpg)

Every performance report uses the same handful of measurements, and they answer
different questions.

| Metric | Measures | Good |
|---|---|---|
| **TTFB** | Time to the first byte of the response | under 800ms |
| **FCP** | First Contentful Paint — anything at all appears | under 1.8s |
| **LCP** | Largest Contentful Paint — the main thing appears | under 2.5s |
| **CLS** | Cumulative Layout Shift — how much the page jumped | under 0.1 |
| **INP** | Interaction to Next Paint — how fast it answers a tap | under 200ms |

\`TTFB\` is the network and your server. \`FCP\` and \`LCP\` are the pipeline above.
\`CLS\` is images and adverts without reserved space, and fonts that swap. \`INP\` is
main-thread work — the metric that punishes a heavy page even after it has
loaded, and the one a single long task ruins.

Note what is *not* in the list: the old \`load\` event, and \`DOMContentLoaded\`.
They measure the browser's bookkeeping rather than anything a reader can see.

## The order things actually happen

For a plain page with one stylesheet and one deferred script:

\`\`\`text
  TTFB      response starts arriving
            HTML parsed, DOM grows as bytes land
            stylesheet requested (found by the parser)
            image requested
  FCP       stylesheet arrives -> CSSOM -> first paint
  LCP       the hero image lands and paints
            DOMContentLoaded -> deferred scripts run
            load -> everything including images is done
\`\`\`

Two things are worth noticing. The first paint waits for CSS but not for the
script, which is why \`defer\` is free. And \`LCP\` is usually an image or a heading
— finding out which one is the first move in fixing it, and the browser will
tell you in the performance panel rather than making you guess.

## Where to look

The three tools, in the order you would reach for them:

- **The network panel** — waterfall, sizes, and which requests blocked the
  others. Sort by time and look for the long bar with everything queued behind
  it.
- **The performance panel** — a recording of the main thread. Long tasks appear
  as wide blocks with a red corner; that is where INP goes to die.
- **Lighthouse** — one synthetic run with advice attached. Useful as a checklist
  and misleading as a measurement, because it is one machine on one connection.
  What real readers experienced is *field data*, and it is a different number.

## When it goes wrong

- **A blank page for two seconds, then everything at once** — something in the
  head is blocking. A stylesheet from a slow third party, or a synchronous
  script.
- **Text appears, then jumps** — a web font swapped in, or an image without
  \`width\` and \`height\` pushed the page down. Both are CLS, and both are fixed by
  reserving the space.
- **The page loads fast and feels slow** — main-thread work after load. A tap
  waits behind a long task; the metric is INP.
- **It is fast on your machine** — of course it is. You have a fast processor, a
  warm cache and a local server. Throttle to a mid-range phone and 3G in the
  performance panel before believing anything.

## What to read next

- **[Underneath the pipeline](#/advanced/deep-browser/advanced)** — the preload
  scanner, forced synchronous layout, font loading, long tasks, the back/forward
  cache and the resource hints.
- **[Server rendering and client rendering](#/interview/deep-rendering)** — which
  of these steps happens on your server, and which in the reader's browser.

## Further reading

- [How browsers work](https://web.dev/articles/howbrowserswork)
- [Core Web Vitals](https://web.dev/articles/vitals)
- [Rendering performance](https://web.dev/articles/rendering-performance)
`,Ce=`The [notes](#/advanced/deep-browser) are the pipeline: parse, style, layout,
paint, composite, and the one thread that runs nearly all of it. This is what
sits underneath — the optimisations the browser already applies, the ones you
have to ask for, and the three or four ways an ordinary line of code makes the
whole thing worse.

## The preload scanner

![What the preload scanner can and cannot see](diagrams/browser-advanced-preload-scanner.jpg)

The parser stops at a synchronous script. If that were the whole story, a page
with a script in the head would discover nothing else until that script had run.

So every browser runs a second, simpler parser ahead of the real one, doing
nothing but looking for URLs to start fetching: stylesheets, scripts, images. It
is why a blocking script costs you execution order but not, usually, download
parallelism.

It also explains a class of self-inflicted wound. **The scanner only sees markup**
— anything requested by JavaScript is invisible to it:

\`\`\`html
<!-- the scanner finds this while the parser is still busy -->
<img src="/hero.jpg">

<!-- the scanner cannot see this at all; the request starts much later -->
<script>document.write('<img src="/hero.jpg">')<\/script>
\`\`\`

A hero image inserted by a framework after hydration is discovered several
hundred milliseconds later than the same image in the HTML. That single fact is
one of the strongest arguments for server rendering, and it is on the
[rendering page](#/interview/deep-rendering/advanced).

## Resource hints, and which to reach for

![The four resource hints and what each is for](diagrams/browser-advanced-hints.jpg)

Four of them, and they are not interchangeable:

| Hint | Does | Use for |
|---|---|---|
| \`dns-prefetch\` | Resolves the name early | A third party you will use later |
| \`preconnect\` | Name, TCP and TLS, early | The origin your fonts or API live on |
| \`preload\` | Fetches now, at high priority | The LCP image, a critical font |
| \`prefetch\` | Fetches at idle, for a *future* navigation | The next page in a flow |

\`\`\`html
<link rel="preconnect" href="https://fonts.example.com" crossorigin>
<link rel="preload" as="image" href="/hero.avif" fetchpriority="high">
\`\`\`

Two rules. \`preload\` without \`as\` fetches the thing twice, because the browser
cannot match the preload to the later request. And a preconnect to more than two
or three origins costs more than it saves — each one is a handshake you are
paying for on the chance it is needed.

\`fetchpriority="high"\` on the LCP image and \`loading="lazy"\` on everything below
the fold are the two highest-value attributes on the whole list, and both are one
word long.

## Forced synchronous layout

![Interleaved reads and writes against batched ones](diagrams/browser-advanced-thrash.jpg)

The expensive mistake that looks like ordinary code.

The browser batches style and layout work, and would rather do it once per
frame. But reading certain properties requires an up-to-date answer, so the read
forces layout to run *now* — and if you are in a loop that writes and then
reads, you force it on every iteration.

\`\`\`javascript
// bad: read, write, read, write — layout runs once per element
for (const card of cards) {
  card.style.height = card.offsetHeight + 20 + 'px'   // read then write
}

// good: all the reads, then all the writes — layout runs once
const heights = cards.map((card) => card.offsetHeight)
cards.forEach((card, i) => {
  card.style.height = heights[i] + 20 + 'px'
})
\`\`\`

The properties that force it are the geometric ones: \`offsetTop\`, \`offsetHeight\`,
\`clientWidth\`, \`scrollTop\`, \`getBoundingClientRect()\`, and \`getComputedStyle()\`
for anything layout-dependent. The pattern to hold on to is **read everything,
then write everything** — batching the phases rather than the elements.

In the performance panel this appears as a purple *Layout* block with a warning
triangle, usually inside a task you thought was cheap.

## Compositing, and the properties that are free

The compositor runs on its own thread with the layers it was given. Changes it
can make alone never touch the main thread at all — which is why they keep
working while JavaScript is busy.

- **Free**: \`transform\`, \`opacity\`, and \`filter\` on a promoted layer.
- **Costs paint**: \`background-color\`, \`box-shadow\`, \`border-radius\`.
- **Costs layout and paint**: \`width\`, \`height\`, \`top\`, \`left\`, \`margin\`,
  \`font-size\`.

\`will-change: transform\` promotes an element to its own layer ahead of time, and
is a request, not a hint to sprinkle everywhere: every layer costs memory, and a
page that promotes fifty elements is slower, not faster. Promote the thing that
is about to animate, and remove it afterwards.

## Long tasks, and giving the thread back

A task longer than 50ms is a **long task**, and anything the reader does during
it waits. That is INP, and it is the metric most sites now fail.

Splitting work is manual. Three tools, in increasing order of modernity:

![Which ways of yielding actually let the browser render](diagrams/browser-advanced-yield.jpg)

\`\`\`javascript
// 1. yield with a macrotask, so the browser can render between chunks
await new Promise((resolve) => setTimeout(resolve, 0))

// 2. do it when nothing else is happening — never for anything urgent
requestIdleCallback(() => buildTheOffscreenThing())

// 3. the explicit version, where supported
await scheduler.yield()
\`\`\`

Note what does **not** help. \`await\` on a promise that is already resolved yields
nothing — microtasks drain before the frame, so the thread is still yours.
Neither does moving the work into \`Promise.resolve().then()\`. Rendering only gets
a chance between *tasks*.

Genuinely heavy computation belongs in a **Web Worker**, which has its own
thread and no DOM. The cost is that everything crossing the boundary is copied
(or transferred), so a worker pays for itself when the work is large and the
messages are small.

## Fonts, and the invisible text problem

A web font is not render-blocking, but text that needs it cannot be drawn until
it arrives. The browser's choice is spelled by one property:

\`\`\`css
@font-face {
  font-family: Inter;
  src: url(/inter.woff2) format('woff2');
  font-display: swap;   /* draw in a fallback now, swap when it lands */
}
\`\`\`

- \`block\` — invisible text for up to three seconds. This is the default in
  effect, and it is why headings sometimes appear late.
- \`swap\` — fallback immediately, swap when ready. Costs a layout shift.
- \`optional\` — fallback, and only use the web font if it arrived almost
  instantly. The best answer for body text on a slow connection.

\`size-adjust\` and the metric-override descriptors let a fallback occupy the same
space as the real font, which turns the swap from a visible jump into nothing at
all.

## The back/forward cache

Going back should be instant, and it is — if the page qualifies. The bfcache
freezes the whole page, including the JavaScript heap, and restores it.

Things that disqualify a page: an open connection the browser cannot pause, a
\`Cache-Control: no-store\` header on the document, and — historically the common
one — an \`unload\` handler. Use \`pagehide\` instead, and listen for
\`pageshow\` with \`event.persisted\` to notice a restore.

This is worth more than most optimisations, because it turns a whole navigation
into zero requests, and it is lost by accident rather than won by effort.

## Speculative and priority hints

The browser assigns every request a priority, and mostly guesses well. Where it
guesses badly, it is usually the LCP image: an \`<img>\` far down the markup gets
a low priority even though it is the thing being measured.

\`\`\`html
<img src="/hero.avif" fetchpriority="high" width="1200" height="630" alt="">
<img src="/below.avif" loading="lazy" width="800" height="400" alt="">
\`\`\`

\`width\` and \`height\` on every image is the other half. They let the browser
reserve the box before the bytes arrive, which is CLS eliminated rather than
reduced — and they work even when the image is styled to a different size,
because the browser only needs the ratio.

## Measuring it honestly

![Lab data against field data](diagrams/browser-advanced-lab-field.jpg)

Two kinds of data, and confusing them is the most common mistake in performance
work.

**Lab data** — Lighthouse, WebPageTest, your own machine. Reproducible,
debuggable, and not what anyone experienced. Use it to compare two versions of
your own page.

**Field data** — what real visits recorded, gathered with the browser's own APIs
or read from the Chrome UX Report. Noisy, un-debuggable, and the truth. Use it
to decide whether there is a problem at all.

\`\`\`javascript
// the field version: report what this visit actually experienced
new PerformanceObserver((list) => {
  for (const entry of list.getEntries()) {
    send({ metric: entry.name, value: entry.startTime })
  }
}).observe({ type: 'largest-contentful-paint', buffered: true })
\`\`\`

And when using lab tools, throttle. A mid-range Android phone is roughly four to
six times slower than a development laptop, and every conclusion about
JavaScript cost changes at that ratio.

## A short list of what actually moves the numbers

In rough order of return, on a typical site:

1. **Send less JavaScript.** Nothing else on this list compares. Every kilobyte
   is downloaded, parsed, compiled and executed on the main thread.
2. **Fix the LCP element's request chain.** It should be in the HTML, high
   priority, and not waiting behind a script or a font.
3. **Reserve space** for images, adverts and anything that arrives late.
4. **Cache properly** — hashed asset names with a long \`max-age\`, and never on
   the document. The [HTTP page](#/advanced/deep-http/advanced) has the
   directives.
5. **Break up long tasks**, especially the one that runs on load.
6. **Then** micro-optimise the CSS.

## Further reading

- [Optimize Largest Contentful Paint](https://web.dev/articles/optimize-lcp)
- [Optimize Interaction to Next Paint](https://web.dev/articles/optimize-inp)
- [Back/forward cache](https://web.dev/articles/bfcache)
- [Avoid large, complex layouts and layout thrashing](https://web.dev/articles/avoid-large-complex-layouts-and-layout-thrashing)
`,we=`Twelve questions on what a browser does with a response. They come up in
front-end interviews directly, and in back-end ones disguised as "why is the
page slow when the API is fast".

The **Notes** cover the pipeline and the metrics. The **Advanced** page covers
the preload scanner, layout thrash, long tasks and the bfcache.

## Easy

### 1. What happens between the response arriving and something appearing on screen?

The HTML is parsed into the DOM and the CSS into the CSSOM; the two are combined
into a render tree of what is visible with its styles; layout works out where
every box goes and how big; paint decides the colour of each pixel; and the
compositor stacks the layers on screen. Each step depends on the one before it,
which is why a single late file can hold up the lot.

### 2. Why does a stylesheet block rendering but an image does not?

Because nothing can be painted correctly without knowing its styles, and a flash
of unstyled content is worse than a moment of blank — so the browser waits for
the CSSOM before the first paint. An image affects only its own box, and the
browser lays the page out around it; the cost of a late image is a layout shift
rather than a delayed page, and that is what \`width\` and \`height\` attributes
prevent.

### 3. What is the difference between \`defer\` and \`async\`?

Both download without blocking the parser. \`defer\` waits until parsing is
finished and runs scripts in the order they appear, so it is safe for code with
dependencies — that is the sane default. \`async\` runs each script the moment it
arrives, in whatever order they land, so it suits things that depend on nothing
and that nothing depends on, such as analytics. A plain \`<script src>\` with
neither attribute stops the parser until it has downloaded and executed.

### 4. What are LCP, CLS and INP?

Largest Contentful Paint is when the main thing on the page appeared — usually a
hero image or a heading — and under 2.5 seconds is the target. Cumulative Layout
Shift measures how much the page jumped about while loading, mostly from images
without reserved space and fonts swapping in. Interaction to Next Paint measures
how long the page takes to respond visibly to a tap or a click, which is a
main-thread problem rather than a network one.

## Medium

### 5. Why is animating \`left\` worse than animating \`transform\`?

\`left\` is a layout property, so changing it makes the browser re-run layout and
paint for every frame — sixty times a second, for a change that cascades to
everything after the element. \`transform\` and \`opacity\` can be handled by the
compositor with the layer it already painted, so they need neither layout nor
paint and keep working even while the main thread is busy. On a mid-range phone
that is the difference between smooth and unusable.

### 6. What is forced synchronous layout?

The browser batches layout work and would rather do it once per frame, but
reading a geometric property — \`offsetHeight\`, \`getBoundingClientRect()\`,
\`scrollTop\` — needs an up-to-date answer, so it forces layout to run
immediately. Interleaving writes and reads in a loop therefore triggers layout on
every iteration; the fix is to read everything first, then write everything,
batching the phases rather than the elements. It shows up in the performance
panel as a layout block with a warning triangle inside a task you assumed was
cheap.

### 7. Why does \`await Promise.resolve()\` not let the page render?

Because promise callbacks are microtasks, and the browser drains the entire
microtask queue at the end of the current task, before it considers rendering a
frame. Yielding to the browser needs a new *task* — \`setTimeout(fn, 0)\`, or
\`scheduler.yield()\` where it exists. This is the usual reason a "chunked" loop
that awaits promises still freezes the page.

### 8. What is the preload scanner, and how do you defeat it?

A second, simpler parser that runs ahead of the real one doing nothing but
looking for URLs to start downloading, which is why a blocking script does not
stop the rest of the page being fetched. You defeat it by hiding URLs from
markup: images injected by JavaScript, CSS \`background-image\` on an element that
does not exist yet, fonts referenced only from a stylesheet that itself arrives
late. Each of those is discovered hundreds of milliseconds later than the same
resource written in HTML.

### 9. How would you improve a page whose LCP is 4 seconds?

Find the LCP element first — the performance panel names it — because the answer
differs completely between an image and a block of text. For an image: get it
into the HTML rather than injected by script, add \`fetchpriority="high"\`, remove
anything blocking its discovery, and serve it in a modern format at the right
size. For text: the blocker is usually a font or a render-blocking stylesheet,
so inline the critical CSS and use \`font-display: swap\` or \`optional\`. Then check
TTFB — if the server took two seconds, nothing on the front end will fix it.

## Hard

### 10. A page scores well in Lighthouse and users say it is slow. What is going on?

Lighthouse is a single synthetic run on one machine, at one connection speed,
with a cold cache and no third-party consent dialogue — it measures load, and it
measures it under conditions nobody has. Real users' complaints are usually
about *interaction*: long tasks after load, an expensive event handler, or a
third-party script that only executes for real visitors. Look at field data —
the browser's own performance observers reporting from real sessions, or the
Chrome UX Report — and specifically at INP, which lab load metrics do not
capture at all.

### 11. What is the back/forward cache, and how is it lost?

When you navigate away, the browser can freeze the whole page — DOM, JavaScript
heap and all — and restore it instantly when you come back, so the back button
costs zero requests. A page is disqualified by things that cannot be frozen
safely: an \`unload\` handler, \`Cache-Control: no-store\` on the document, and some
open connections. The fix is to use \`pagehide\` instead of \`unload\` and to listen
for \`pageshow\` with \`event.persisted\` so you know a restore happened and can
refresh anything stale — this is worth more than most optimisations and is
usually lost by accident.

### 12. When is a Web Worker the right answer, and when is it not?

When the work is genuinely CPU-bound and large — parsing a big file, image
processing, a heavy computation — and the data crossing the boundary is small,
because everything sent to a worker is structurally cloned unless it is
explicitly transferred. It is the wrong answer for anything that needs the DOM,
which workers cannot touch, and for small chunks of work where the messaging
overhead exceeds the saving. For most sites the bigger win is simply doing less
on the main thread: shipping less JavaScript and breaking the remaining work
into tasks.

## What to revise next

- **[Server rendering and client rendering](#/interview/deep-rendering)** — which
  of these steps happens on your server, which in the browser, and what each
  choice costs.
`,Te=`The chapter in a page. If a line here is news, the **Notes** or **Advanced**
part is where it comes from.

## The pipeline

- HTML → DOM, CSS → CSSOM, the two combined into the render tree, then layout,
  paint, composite. Each step needs the one before it.
- Nothing paints without the CSSOM, so a stylesheet blocks the first paint of
  the whole page.
- The DOM is not the HTML. It is the tree built from it, and anything that reads
  HTML instead of running scripts sees only what was in the bytes.

## What blocks what

- \`<script src>\` stops the parser dead. \`defer\` waits for the DOM and keeps
  order; \`async\` runs whenever it lands, in any order; modules are deferred by
  default.
- \`defer\` is the sane default. \`async\` is for things nothing depends on.
- Stylesheets do not block parsing but do block painting.
- The **preload scanner** runs ahead of the parser looking for URLs, so a
  blocking script costs execution order rather than download parallelism — but
  it only sees markup, never a request made by JavaScript.

## One thread

- Parsing, style, layout, paint and all your JavaScript share the main thread.
  A 300ms function is 300ms of nothing responding.
- Task → microtasks → maybe a frame. Promises drain *before* rendering, so
  awaiting a resolved promise yields nothing to the browser.
- Yield with \`setTimeout(…, 0)\` or \`scheduler.yield()\`. Move genuinely heavy
  work to a Web Worker.

## Layout and paint

- Change colour → repaint. Change geometry → reflow, then repaint. Change
  \`transform\` or \`opacity\` → the compositor does it alone.
- Animate \`transform\` and \`opacity\`, never \`left\` or \`width\`.
- **Forced synchronous layout**: reading \`offsetHeight\`,
  \`getBoundingClientRect()\` and friends after a write forces layout immediately.
  Read everything, then write everything.
- \`will-change\` promotes a layer and costs memory. Promote what is about to
  animate, not everything.

## The numbers

- **TTFB** the server · **FCP** anything appears · **LCP** the main thing
  appears (under 2.5s) · **CLS** how much it jumped (under 0.1) · **INP** how
  fast it answers a tap (under 200ms).
- \`load\` and \`DOMContentLoaded\` measure bookkeeping, not experience.
- CLS is fixed by \`width\` and \`height\` on images and reserved space for anything
  that arrives late.
- INP is fixed by breaking up long tasks — anything over 50ms.

## Loading

- \`preconnect\` for an origin you will use, \`preload\` for the LCP image or a
  critical font, \`prefetch\` for the *next* page, \`dns-prefetch\` for a maybe.
- \`preload\` without \`as\` fetches twice.
- \`fetchpriority="high"\` on the LCP image, \`loading="lazy"\` below the fold.
- \`font-display: swap\` or \`optional\`; the default hides text for up to three
  seconds.
- The bfcache makes a back navigation free — and \`Cache-Control: no-store\` or an
  \`unload\` handler disqualifies the page. Use \`pagehide\`.

## Measuring

- **Lab** (Lighthouse, your machine) is reproducible and is not what anyone
  experienced. **Field** (real visits, CrUX) is noisy and is the truth.
- Throttle before believing anything: a mid-range phone is four to six times
  slower than a laptop.
- In order of return: send less JavaScript, fix the LCP request chain, reserve
  space, cache properly, break up long tasks.
`,Ee=`# Client, server, and what a request actually is

Before any of this is about Spring, it is about two machines and one message.

You type \`www.amazon.com\` and press enter. Your browser sends a message to a
machine sitting in a data centre somewhere, that machine works out what you
asked for, and it sends a message back. Everything else in this section —
servlets, Tomcat, Spring, Spring Boot — exists to make the middle of that
sentence, *works out what you asked for*, something you can write without
writing the rest of it.

## Who is the client and who is the server

A **client** is whoever asks. It is not a kind of machine and it is not
necessarily a browser:

- A browser loading a page
- A mobile app on Android or iOS
- A React frontend running in somebody's tab
- Postman, when you are testing
- Another one of your own services

A **server** is whoever answers. It is a program that does not exit — it stays
running, waits, and responds. Amazon has one. YouTube has one. Your bank has
one. And the thing you are about to build is one.

![Browsers, mobile apps, Postman and other services all sending requests to one Spring Boot application, which checks login details, reads a database, applies business rules and returns a response](diagrams/spring-client-server-notes-client-server.jpg)

**The client asks. The server responds.** That is the whole arrangement, and
the reason it is worth stating so plainly is that every layer above it is a
convenience over exactly this.

What a server actually does with a request is unglamorous and always roughly
the same four things: check who is asking, fetch or write something in a
database, apply whatever the business rules are, and send an answer back.

## The message they agree on

Two programs written by different people, in different languages, on different
continents, have to understand each other. So they agree on a format in advance.
That agreement is **HTTP** — HyperText Transfer Protocol — and it settles six
things:

- What a request looks like
- What a response looks like
- Which **method** is being used
- Which **URL** is being asked for
- What data is being sent
- Which **status code** comes back

HTTP is not code and it is not a library. It is a shape that text has to be in.
You could type one by hand, and for years people learning this did.

## One round trip, step by step

![A client sending a GET request for a courses page, the server fetching data and preparing a response, and a 200 OK coming back with the body](diagrams/spring-client-server-notes-round-trip.jpg)

Four steps, and they never vary: the client sends a request, the server
processes it, the server sends a response, the client does something with it.

## Inside a request

A request has four parts, and you will be reading and writing all four for the
rest of your career.

\`\`\`expected
POST /login HTTP/1.1
Host: www.coderarmy.in
Content-Type: application/json
Accept: application/json
Authorization: Bearer token

{
  "email": "abc@gmail.com",
  "password": "12345"
}
\`\`\`

| Part | What it is | In the example |
| --- | --- | --- |
| **Method** | What kind of thing you are doing | \`POST\` — I am sending data |
| **Path** | Which functionality you want | \`/login\` — the login endpoint |
| **Headers** | Information *about* the request | who you are, what format you are sending, what you can accept |
| **Body** | The actual data | the email and password |

### The methods

Five of them carry almost all real traffic, and the difference between them is
intent rather than capability — nothing physically stops you sending data with a
\`GET\`.

| Method | What it means |
| --- | --- |
| **GET** | Give me something. Changes nothing. |
| **POST** | Here is something new. |
| **PUT** | Replace this thing with what I am sending. |
| **PATCH** | Change part of this thing. |
| **DELETE** | Remove it. |

\`GET\` and \`DELETE\` normally carry no body. \`POST\`, \`PUT\` and \`PATCH\` normally do.

### The headers worth knowing on day one

| Header | The question it answers |
| --- | --- |
| \`Accept: application/json\` | What format can the client understand? |
| \`Content-Type: application/json\` | What format is this body in? |
| \`Authorization: Bearer …\` | Who is the client? |
| \`Host: www.coderarmy.in\` | Which host was the client trying to reach? |

\`Accept\` and \`Content-Type\` get confused constantly, and the distinction is
worth fixing now: **\`Content-Type\` describes the body that is in this message.
\`Accept\` describes the body you would like back.**

## Inside a response

A response has three parts — a status line, headers, and a body.

\`\`\`expected
HTTP/1.1 200 OK
Content-Type: application/json

{
  "message": "Login successful"
}
\`\`\`

The **status code** is the server's one-glance summary of what happened, and
the first digit is the part that matters:

| Code | Means |
| --- | --- |
| **200** | It worked |
| **404** | Not found — what you asked for is not there |
| **500** | The server broke while trying |

\`404\` and \`500\` are the pair worth telling apart: a \`404\` means you asked for
something that does not exist, and a \`500\` means you asked correctly and
something on the other side fell over.

## And the s in HTTPS

Everything above is HTTP, which is plain text on the wire. Anybody positioned
between the two machines — a router, a café's wifi, an internet provider — can
read it, and can change it before it arrives.

![The same request sent over HTTP, which anybody on the path can read, and over HTTPS, where the same message passes as unreadable ciphertext](diagrams/spring-client-server-notes-http-vs-https.jpg)

**HTTPS is the same HTTP, sent through a pipe that has three properties:**

| Property | What it stops |
| --- | --- |
| **Encryption** | Nobody in the middle can read the message |
| **Integrity** | Nobody in the middle can change it without being noticed |
| **Authentication** | Nobody in the middle can pretend to *be* the far end |

The third is the one people forget, and it is the hardest of the three. Scrambling
a message is no use if you scrambled it for an impostor — so before any data
moves, the server presents a **certificate**, which is a file saying "this key
belongs to \`www.coderarmy.in\`", signed by an organisation your browser already
trusts. That exchange is the **TLS handshake**, and it is what the padlock in
the address bar is reporting on.

![A TLS handshake in which the client says hello, the server presents a certificate, the client checks who signed it, a key is agreed, and only then does the ordinary HTTP request travel encrypted](diagrams/spring-client-server-notes-tls-handshake.jpg)

The important part of that picture is the order. **The certificate is checked
before a single byte of your request moves**, which is why an expired or
mismatched certificate stops the page dead rather than showing it with a
warning.

Two things follow that are worth knowing on day one:

- **Your code does not change.** The same \`@GetMapping("/hello")\` serves HTTP or
  HTTPS. Encryption happens below your method and above the socket; the request
  object you are handed is identical either way.
- **In production it is not optional.** Browsers mark plain HTTP as insecure, and
  a login form sent over it is a password sent in readable text to everybody on
  the path.

That is enough HTTPS to write a Spring application and to answer the question in
an interview. If you want the whole of it one day — every field of the message,
what changed when one connection per file stopped being enough, and the
handshake step by step with who vouches for whom — there are two long pages on
this site about exactly that: **[HTTP, request and
response](#/advanced/deep-http)** and **[HTTPS, TLS and the
certificate](#/advanced/deep-https)**.

## Why this page comes first

Everything after this is machinery for turning that text message into a Java
method call, and turning your return value back into a text message. Servlets
do a part of it, Tomcat does a part of it, Spring MVC does a part of it, and
Spring Boot arranges for all three to be there without you asking.

If the message itself is clear, none of those layers are mysterious — they are
just labour you are no longer doing.
`,De=`# From a Java program to a server

You already know how to write Java. Classes, objects, inheritance, collections,
threads, files — all of it. And none of it, on its own, gets you a server.

This page is about the gap, because the gap is smaller and stranger than people
expect.

## A normal program ends. A server does not.

Here is the first Java program anybody writes:

\`\`\`java
public class Main {
    public static void main(String[] args) {
        System.out.println("Hello World");
    }
}
\`\`\`

You compile it, you run it, and the shape of its life is: **start → run the
instructions → finish → exit.** It does its work and it stops. That is what a
program is.

A server has a different shape. It starts, and then it *keeps running* — waiting
for requests, processing them, sending responses, and going back to waiting. It
does not finish. Finishing would be the failure.

![A normal program starting, running instructions and exiting, against a server that starts and then loops forever between waiting for a request, processing it and responding](diagrams/spring-java-server-notes-two-shapes.jpg)

You can get the second shape with nothing but what you already know:

\`\`\`java
while (true) {
    // keep the program alive
}
\`\`\`

That is a server in the sense that it does not exit. It is useless in every
other sense, because nothing can talk to it.

## What has to be true before anything can talk to it

To be reachable, a program has to be listening on a **port** — a numbered door
on the machine. And the language already has this. \`java.net\` has been in the
JDK since 1.0:

\`\`\`java
ServerSocket server = new ServerSocket(8080);
\`\`\`

That opens port 8080 and waits. When a browser connects, you are holding a
connection with bytes coming down it, and those bytes look like this:

\`\`\`expected
GET /users HTTP/1.1
Host: localhost:8080
Accept: application/json
\`\`\`

Which is text. You could read it a line at a time, and if you did you would
have to work out, by hand:

- that \`GET\` is the method, and means fetch
- that \`/users\` is the path being asked for
- that \`Host\` is a header, and so is \`Accept\`
- where the headers stop and the body starts (a blank line)
- how long the body is, from \`Content-Length\`
- what to write back so the browser understands it

None of that is hard. All of it is exacting, and all of it is the same every
single time.

## Then you write the router, and it starts to hurt

Having parsed the request, you have to decide what to run:

\`\`\`java
if (url.equals("/users")) {
    // call user logic
} else if (url.equals("/orders")) {
    // call order logic
} else if (url.equals("/products")) {
    // call product logic
}
\`\`\`

One \`else if\` per endpoint. That chain is the routing table of your
application, written by hand, and it grows a branch every time anybody adds a
feature — while being the least interesting code in the project.

## The two lists

This is the honest summary of the gap, and it is why the rest of this section
exists.

| What core Java gives you | What a web application also needs |
| --- | --- |
| Classes and objects | HTTP requests and responses |
| Inheritance | URLs and routing |
| Collections | Headers |
| Threads | Cookies and sessions |
| Files | REST APIs |
| Sockets — the raw bytes | Everything above, built on top of them |

The left column is a language. The right column is a job. Nothing on the right
is impossible with what is on the left — it is all just *work*, and it is the
same work in every project anybody has ever written.

**So somebody standardised it.** That is the next page.
`,Oe=`# Servlets and the container

A browser sends an HTTP request. Your Java code has a method that should run.
Between those two facts there is a gap, and until now this section has been
describing filling it by hand.

**A servlet is the standard answer to that gap**, and a servlet container is
the program that does the parts you were doing by hand.

![An HTTP request arriving at a servlet container, which parses it and calls a servlet, which calls your Java code](diagrams/spring-servlets-notes-the-gap.jpg)

## What the container actually does for you

Tomcat is the one you will meet; Jetty and Undertow are the same idea by other
people. Whichever it is, this is the list of jobs it takes off you — and every
line of it is something the previous page had you writing yourself:

- Opening a port, usually 8080
- Listening for connections
- Reading the raw TCP bytes
- Parsing them into an HTTP request
- Building a request object and a response object
- Managing a pool of threads, so requests are served concurrently
- Working out which servlet should handle this request, and calling it
- Turning what you wrote into a valid HTTP response and sending it
- Handling keep-alive, timeouts and connection lifecycle

That is the whole of the boring half of a web server, done once, correctly, by
people who have been fixing edge cases in it for twenty years.

## What a servlet is

A servlet is a class that the container calls when a request arrives. It has one
method per HTTP method:

\`\`\`java
doGet()
doPost()
\`\`\`

One method per HTTP method. \`GET /hello\` arrives; the container parses it,
decides this servlet is the one registered for \`/hello\`, and calls \`doGet()\`.
You are handed a request object you can ask questions of and a response object
you can write into. You never see a byte.

**This is a genuine step up.** The bytes, the parsing, the threads and the
socket are gone, and what is left is a method with your logic in it.

## And then it stops being enough

Servlets solved the plumbing. They did not solve the application, and by the
time a real project has a few dozen endpoints the complaints are always the
same six:

| Problem | What it looks like |
| --- | --- |
| **Boilerplate** | Every servlet is the same twenty lines around three lines that matter |
| **Configuration** | XML for every mapping, before anything runs at all |
| **Tight coupling** | A class that needs a database creates one with \`new\`, so it can never be given a different one |
| **Hard to test** | You cannot test that class without a real database, because it built its own |
| **Hard to maintain** | Changing one dependency means editing every class that constructs it |
| **Repeated across projects** | The same wiring code, retyped, in every application anybody starts |

The third and fourth are a pair, and they are the ones Spring was written for.
A class that creates the things it depends on has decided, permanently, which
ones they are — so it cannot be handed a different implementation, and it cannot
be tested without the real one. **Tight coupling and difficult testing are the
same problem said twice.**

Loosen it and the work moves rather than disappearing: if a class no longer
builds what it needs, something else has to build it, hand it over, and do that
for every class in the application in the right order.

**That something is the Spring Framework.** That is the next page.
`,ke=`# The Spring Framework

The previous page ended with a job nobody wants: something has to build every
object in the application, hand each one what it needs, and do it in the right
order.

Spring is that something. Everything else it does is built on it.

## Inversion of control, in one sentence

Normally your code is in charge. It decides when to create an object, so it
calls \`new\`, and it holds the object it made.

**Inversion of control** turns that around: a container creates the objects and
hands them to you. You never call \`new\` on anything the application is made of,
and you never decide when it is created.

![Without inversion of control a class builds its own dependency with new; with it, the container builds both and hands the dependency in](diagrams/spring-framework-notes-ioc.jpg)

**Dependency injection** is the technique that implements it: the dependency is
*injected* — passed in — rather than constructed inside the class that uses it.
Which means the class using it never learns which implementation arrived, and
that is the whole point. A test can hand it one thing and production another,
and the class itself does not change either way.

## Beans and the ApplicationContext

An object that Spring creates and manages is a **bean**. It is not a special
kind of object — it is an ordinary Java object whose lifetime somebody else
owns.

The **ApplicationContext** is the container that owns them. On startup it scans
your classes, works out what each one needs, builds them in dependency order,
and keeps them. When something asks for an \`OrderRepository\`, the context is
what answers.

**Configuration** is the fourth word on the slide, and it is what the context is
reading to do all of the above: which classes to manage, how to build the ones
it cannot work out for itself, and what differs between one environment and the
next.

## What you actually get from it

**Loose coupling** — the thing all four of those exist for. Classes depend on
what they need rather than on which implementation of it they got, and the
decision is made in one place instead of in every constructor in the project.

And with it, the two consequences from the previous page, reversed: a class that
is handed its dependencies can be handed different ones, so it can be tested,
and changing how something is built no longer means editing everything that
uses it.

## The modules

Spring is not one library. It is a family, and Spring Core — the container
described above — is the thing the rest of them are built on.

| Module | What it is for |
| --- | --- |
| **Spring Core** | IoC, dependency injection, beans, configuration |
| **Spring MVC** | Turning HTTP requests into method calls |
| **Spring Data** | Talking to a database without writing the plumbing |
| **Spring Security** | Login, tokens, roles, permissions |
| **Spring AOP** | Behaviour applied around methods without editing them |
| **Spring Boot** | Making all of the above start without configuration |
| **Spring AI** | Talking to language models and vector databases |

You do not choose between them. A normal application uses four or five at once,
and Spring Boot is the one that arranges for them to fit together.

## Where the servlet went

Spring MVC sits on top of the servlet layer rather than replacing it. There is
still a servlet container, still a servlet — one of them, called the
\`DispatcherServlet\` — and it receives every request and works out which of your
methods should handle it.

Which is why, after all of that, this is the entire endpoint:

\`\`\`java
@GetMapping("/hello")
public String sayHello() {
    return "Hello World";
}
\`\`\`

Every layer in this section is still there underneath those four lines. The port
is open because the container opened it. The bytes were parsed because the
container parsed them. The method was found because the \`DispatcherServlet\`
looked it up. The object was created because the ApplicationContext built it.

**None of it went away. You simply stopped writing it.**
`,Ae=`# MVC, Data, Security, AOP

Spring Core gives you a container. On its own that is an object factory. What
makes Spring a way to build applications is the modules stacked on it, and there
are four you will use in almost every project.

## Spring MVC — HTTP into method calls

MVC is the layer that makes an HTTP request look like a Java method. It owns one
servlet, the \`DispatcherServlet\`, which receives every request and works out
which of your methods should answer it — which is the \`if\`/\`else\` router from
two pages ago, written once and driven by annotations.

\`\`\`java
@RestController
@GetMapping("/hello")
public String sayHello() {
    return "Hello World";
}
\`\`\`

Everything the servlet chapter had you doing by hand is in the gap between the
request arriving and that method being called: the path matched, the parameters
converted, and what you returned turned back into a response.

## Spring Data — the database, without the plumbing

Talking to a database in plain JDBC means writing SQL, opening a connection,
executing, walking a \`ResultSet\`, closing everything in the right order, and
handling the exceptions. Every query. Every time.

![The database stack from Spring Data JPA down through Hibernate and JDBC to the database, with JPA as the specification and Hibernate as the implementation](diagrams/spring-modules-notes-data-stack.jpg)

Two names in that stack get mixed up constantly, and the distinction is simple:

> **JPA says what should be done. Hibernate actually does it.**

JPA is a specification — a set of interfaces and rules, with no code that runs.
Hibernate is an implementation of it. You write against JPA, Hibernate performs
it, and in principle you could swap in a different implementation.

Spring Data sits one layer above again. Its job is the list at the top of this
section — the connections, the result sets, the closing, the repetition — so
that the only thing left in your code is the question you wanted to ask.

## Spring Security — who is asking, and what they may do

Security is the module people put off and then bolt on badly. It covers:

- **Authentication** — proving who you are. Login, JWT, OAuth.
- **Authorisation** — what you are allowed to do. Roles and permissions.
- **Password encoding**, so what is stored is a hash and not a password.
- **CSRF protection**, so another site cannot make requests as you.
- **Access control** on endpoints and on methods.

The reason it belongs to the framework rather than to your code is that it works
as a **filter chain** in front of everything: a request is checked before it
reaches any controller, so an endpoint cannot forget to be protected.

## Spring AOP — behaviour without editing the method

Some things need to happen around a lot of methods and belong to none of them:

- Logging that a method was called, and how long it took
- Checking a permission before it runs
- Starting a transaction, and committing or rolling it back
- Recording a performance metric
- Turning an exception into a sensible response

Writing those into every method means the same five lines in a hundred places,
and the actual logic buried in the middle of them. **Aspect-oriented
programming** lets you write the behaviour once and say *where* it applies, and
Spring weaves it in around the method at run time.

Transaction management is on that list, and it is the one you will meet first.
The method that moves money says only that it moves money; beginning the
transaction, committing it, and rolling it back if something threw are wrapped
around it rather than written into it.

## Spring AI — the newest one

The most recent addition, and it is the same idea applied to language models:
one set of interfaces over OpenAI, Gemini, Anthropic and the rest, plus the
pieces a real application needs around them — embeddings, vector databases, RAG
pipelines and chat models.

The value is the same value Spring Data offers over JDBC. You write against one
abstraction, and swapping the provider underneath is configuration rather than a
rewrite.

## The point of all four

None of these modules do anything you could not do yourself. Every one of them
is a thing that every application needs, done once, so that the code you write
is about the thing you are actually building.
`,je=`# Spring Boot

Spring solved the wiring. It did not solve *starting*.

Spring gave you a container, a web layer and a database layer — and then asked
you to configure all three, and to install a server to deploy the result into,
before a single request could be answered. **Too many configurations** was the
second item on the list of what developers were fighting, and Spring solved
every item on it except that one.

**Spring Boot is Spring with the configuration already decided.**

## The five things it gives you

| | What it means |
| --- | --- |
| **Auto-configuration** | It looks at what is on the classpath and configures it. A JDBC driver is present, so it builds a data source. Spring MVC is present, so it sets up the web layer. |
| **Starter dependencies** | One dependency pulls in a coherent set. \`spring-boot-starter-web\` brings MVC, JSON, validation and an embedded server, at versions known to work together. |
| **Embedded server** | Tomcat runs *inside* your application. There is no server to install and no WAR to deploy — the output is a jar you run. |
| **Sensible defaults** | Every setting has a value that is reasonable, and you override only what you disagree with. |
| **Production-ready features** | Health checks, metrics and monitoring endpoints, available by adding one dependency. |

So by the end of all of it, what a web application asks you to write is this:

\`\`\`java
@RestController
@GetMapping("/hello")
\`\`\`

## What just happened

Adding one starter dependency set up an embedded Tomcat, Spring MVC, a default
project structure, JSON support and basic error handling — none of which appear
anywhere in your code. That is the list of what auto-configuration did, and it
is the whole of what "Spring Boot is easier" means.

## The sentence that clears up the most confusion

> **Spring Boot uses Spring. Spring Boot does not replace Spring.**

Everything from the previous pages is still there and still running:

![A request passing down through the servlet container, servlet technology, Spring MVC, Spring Core and Spring Boot's auto-configuration to reach the business logic the developer wrote](diagrams/spring-boot-notes-the-stack.jpg)

Which is also the honest answer to "should I learn Spring or Spring Boot?"
Boot is how you start a Spring application. What you are actually learning is
still dependency injection, beans, MVC, and the database layer — the same things
this section has been building up to.

## What you still have to know

Auto-configuration removes the typing, not the understanding. When something
goes wrong you are debugging Spring, not Boot, and the list of what Boot does
*not* teach you is short and non-negotiable:

- **Spring Core** — beans, IoC, dependency injection, scopes
- **Spring MVC** — how a request becomes a method call
- **Servlets and the container** — what is underneath the whole web layer
- **HTTP** — methods, headers, status codes
- **Database concepts** — transactions, connections, what a query costs
- **Security concepts** — authentication and authorisation

Every one of those is a page earlier in this section, and every one of them is
what an interviewer is actually asking about when they ask about Spring Boot.

## Microservices, while we are here

The two get mentioned in the same breath so often that people assume one means
the other. They are different kinds of thing:

> **Microservices are an architecture. Spring Boot is a tool people commonly
> use to build them.**

Instead of one application doing everything, you run several small ones — a user
service, an order service, a payment service, a notification service, a product
service — each with its own job and often its own database, talking over HTTP or
a message queue.

Spring Boot suits that shape because a Boot application is a self-contained jar
with its own server, which is exactly what you want when you are running twenty
of them. But you can build a perfectly good single application with Boot, and
most projects should start that way. The architecture is a decision about how to
split a system; the framework is a decision about what to write it in.

## Where to go next

The four scenarios under [System Design](#/design) are the other half of this.
Everything on this page is about getting a request into your code cleanly.
Those are about what happens when the code is fine and the system falls over
anyway — the connection pool, the migration, the cache, the leader. That is the
half a Spring Boot tutorial never covers and an interview always does.
`,Me=`# The Spring interview

Fifteen questions across the whole of this section, easy to hard. The early
ones check you know the words. The later ones are the ones that separate a
candidate who has followed a tutorial from one who understands what the tutorial
was doing.

## Easy

### 1. What is HTTP, and what does HTTPS add to it?

HTTP is an agreement about the shape of two text messages, so that two programs
written by different people in different languages can understand each other. It
defines what a request looks like, what a response looks like, which method is
used, which URL is being called, what data is sent, and which status code comes
back.

**HTTPS is the same HTTP through a pipe with three properties**: nobody in the
middle can read it, nobody in the middle can change it unnoticed, and nobody in
the middle can pretend to be the far end. The third is the hard one, and it is
what the certificate and the TLS handshake are for — encrypting a message for an
impostor buys you nothing.

Worth adding unprompted: **your code is identical either way.** The same
controller method serves both, because the encryption happens below your method
and above the socket.

### 2. What is the difference between \`Content-Type\` and \`Accept\`?

\`Content-Type\` describes the body that is in *this* message. \`Accept\` describes
the format the sender would like *back*. A client posting JSON and wanting JSON
returned sends both, and they happen to have the same value, which is why they
get confused.

### 3. What does a status code's first digit tell you?

Who has a problem. \`2xx\` means it worked, \`3xx\` means look elsewhere, \`4xx\`
means the request was wrong and that is the client's problem, \`5xx\` means the
request was fine and the server broke. Returning \`500\` for bad input is the
common mistake — it turns the caller's bug into your incident.

### 4. What is a servlet container, and name three.

The program that opens the port, reads the bytes, parses them into an HTTP
request, manages a thread pool, decides which servlet handles the request, calls
it, and writes the response back. Tomcat, Jetty and Undertow.

### 5. What is a bean?

An object whose creation and lifetime Spring owns, rather than your code. It is
an ordinary Java object; the only thing that makes it a bean is who built it.

## Medium

### 6. Explain inversion of control and dependency injection, and how they differ.

**Inversion of control** is the principle: your code does not create the objects
it uses; a container creates them and hands them over. **Dependency injection**
is the technique that implements it — the dependency is passed in, usually as a
constructor parameter, rather than constructed inside the class.

One is the rule and the other is the mechanism. You can invert control other
ways — a service locator, for instance — and it would still be IoC and not DI.

### 7. Why is \`new\` inside a class a problem?

Because it makes a decision permanent that should not be. A class that writes
\`new MySqlRepository()\` has chosen its database in a file whose job is business
logic. It can never be given a different implementation, which means it can
never be unit tested without a real database, and changing that choice means
editing every class that made it.

Taking the dependency as a constructor parameter moves the decision to one place
and leaves the class saying only what it needs.

### 8. Why does Spring MVC still need a servlet?

Because it did not replace the servlet layer; it sits on top of it. There is
still a container and still a servlet — one, the \`DispatcherServlet\` — which
receives every request and dispatches it to the right handler method. Spring MVC
is a better programming model over servlets, not an alternative to them.

### 9. What is the difference between \`@Controller\` and \`@RestController\`?

\`@RestController\` is \`@Controller\` plus \`@ResponseBody\`. With \`@Controller\`, a
returned \`String\` is the *name of a view* to render; with \`@RestController\` it is
the response body. Leaving \`@RestController\` off an API controller produces a
template-not-found error for a value you meant to return as JSON, which is the
most common first-hour confusion in Spring.

### 10. What is the difference between JPA and Hibernate?

JPA is a specification: interfaces and rules, with no code that executes.
Hibernate is an implementation of that specification. **JPA says what should be
done; Hibernate does it.** You write against JPA so the implementation can in
principle be swapped.

Spring Data JPA is a further layer above both, which generates the repository
implementation from the interface you declare.

### 11. What does \`@Transactional\` actually do, and how?

It wraps the method: begin a transaction before it, commit after it returns,
roll back if it throws. The mechanism is **AOP** — Spring creates a proxy around
the bean, and the proxy runs the transaction logic around your method.

Which explains its two famous failure modes. Calling a \`@Transactional\` method
from *within the same class* goes straight to the method and not through the
proxy, so no transaction starts. And by default only unchecked exceptions
trigger a rollback — a checked exception commits unless you say otherwise.

## Hard

### 12. "Should I learn Spring or Spring Boot?"

The question has a false alternative in it. **Spring Boot uses Spring; it does
not replace it.** Boot removes configuration — it does not remove beans, IoC,
dependency injection, MVC or the database layer, which are what you are actually
learning either way.

Auto-configuration removes the typing, not the understanding. When something
breaks, you are debugging Spring.

### 13. Are microservices and Spring Boot the same thing?

No. **Microservices are an architecture; Spring Boot is a tool commonly used to
build them.** The architecture is a decision about splitting a system into
separately deployed services with their own data; the framework is a decision
about what to write each one in. Boot suits it because a Boot application is a
self-contained jar with its own embedded server, which is what you want when
running twenty of them — but a single application built with Boot is perfectly
normal, and is how most projects should start.

### 14. Walk a request all the way from the browser to your method.

1. The browser opens a connection and sends an HTTP request as text.
2. The **servlet container** — embedded Tomcat — accepts it, reads the bytes,
   parses them, and takes a thread from its pool.
3. It hands a request and response object to the servlet registered for that
   path: the **DispatcherServlet**.
4. **Spring MVC** matches the path and method against your handler mappings,
   converts path variables and the body into your parameter types, and calls
   your method.
5. Your controller depends on a service, which depends on a repository, all of
   which the **ApplicationContext** built at startup and injected.
6. You return an object. MVC serialises it to JSON, the container writes a valid
   HTTP response, and the connection is either reused or closed.

Every layer in that list is still doing its job. Spring Boot's contribution is
that none of it appeared in your code.

### 15. What is the one thing you would tell somebody starting Spring Boot today?

That the framework is a pile of work you are no longer doing, and it is worth
knowing what that work was. Everything Boot removed — the socket, the parsing,
the thread pool, the routing table, the object graph, the transaction handling —
is still happening, and every one of those is a thing an interview can ask about
by name.

Learn what each layer removed, in the order it was removed, and the whole
framework stops being magic and becomes a list of decisions somebody made for
good reasons.
`,Ne=[`notes`,`advanced`,`revision`,`interview`],Pe=[{id:`intro`,number:1,kind:`Lecture 1`,title:`Introduction to Spring & Spring Boot`,tagline:`From one HTTP message to auto-configuration.`,accent:`#6db33f`,slugs:[`spring-client-server`,`spring-java-server`,`spring-servlets`,`spring-framework`,`spring-modules`,`spring-boot`,`spring-interview`],summary:`The first lecture in order: client–server architecture, why servlets were introduced and what they fixed, why Spring mattered, how its modules differ — and why the internals still matter when Boot does so much for you.`}],Fe=e=>({...e,topics:e.slugs.map(e=>Le.find(t=>t.slug===e)).filter(Boolean)}),Ie=()=>Pe.map(Fe),Le=[{slug:`deep-jvm`,title:`How the JVM runs your code`,tagline:`Class loading, bytecode and the JIT.`,summary:`Chapter 2 stops at "the JVM executes bytecode". This goes underneath it: how classes are found and loaded, what is actually inside a .class file, and how the JIT decides which methods are worth compiling to machine code.`,subject:`Java Advanced`,stage:`How it runs`,accent:`#38bdf8`,parts:{notes:se,advanced:ce,revision:N,interview:le}},{slug:`deep-memory`,title:`Memory, in detail`,tagline:`What an object costs, and the boxing cache.`,summary:`Chapters 2, 12 and 21 give you stack, heap, the String Pool and autoboxing. This adds the numbers: what an object weighs in bytes, why allocation is nearly free, and why == on boxed integers works up to 127 and then stops.`,subject:`Java Advanced`,stage:`How it runs`,accent:`#34d399`,parts:{notes:ue,advanced:de,revision:pe,interview:fe}},{slug:`deep-gc`,title:`Garbage collection`,tagline:`The one rule, and the log that proves it.`,summary:`Reachability is the only thing the collector cares about. Two runnable programs show an object being collected and the heap filling and emptying, and the advanced part covers how G1 really works and how to read a heap dump after the thing has fallen over.`,subject:`Java Advanced`,stage:`How it runs`,accent:`#f97316`,parts:{notes:j,advanced:M,revision:oe,interview:ae}},{slug:`deep-http`,title:`HTTP, request and response`,tagline:`Two messages, and everything built on them.`,summary:`Every web page, every API call and every image on this site is one text message asking for something and one text message answering. This is that message field by field — the methods, the status codes, the headers that decide caching and identity — and then what happened to it when one connection per file stopped being enough: keep-alive, HTTP/2 streams, and QUIC.`,subject:`The web`,stage:`On the wire`,accent:`#60a5fa`,parts:{notes:me,advanced:he,revision:_e,interview:ge}},{slug:`deep-https`,title:`HTTPS, TLS and the certificate`,tagline:`What the padlock actually proves.`,summary:`HTTPS is HTTP through a pipe that nobody else can read, tamper with, or pretend to be the far end of — and the third of those is the hard one. This is the handshake message by message, what a certificate is and who vouches for it, why 1.3 dropped a round trip, and how to read the failure when Java says PKIX path building failed.`,subject:`The web`,stage:`On the wire`,accent:`#6baf8d`,parts:{notes:ve,advanced:ye,revision:xe,interview:be}},{slug:`deep-browser`,title:`What the browser does with the response`,tagline:`From bytes to pixels, and what blocks what.`,summary:`The response arrives; nothing is on screen yet. This is what happens in between — HTML parsed into a tree, CSS into another one, the two combined, laid out, painted and composited — and which of those steps a stylesheet, a script or a font can stop dead. It is the prerequisite for every argument about where a page should be rendered.`,subject:`The web`,stage:`In the browser`,accent:`#f472b6`,parts:{notes:Se,advanced:Ce,revision:Te,interview:we}},{slug:`spring-client-server`,title:`Client, server, and what a request is`,tagline:`One message out, one message back.`,summary:`Before any of it is about Spring it is about two machines and one text message. Who the client is and who the server is, what HTTP actually settles between them, and a request taken apart field by field — method, path, headers, body — then the response and what the first digit of a status code is telling you.`,subject:`Spring`,stage:`Before Spring`,accent:`#38bdf8`,parts:{notes:Ee}},{slug:`spring-java-server`,title:`From a Java program to a server`,tagline:`main() exits. A server must not.`,summary:`You know Java and that does not give you a server, and the gap is stranger than it looks: a program starts, runs and exits, while a server starts and never finishes. This is that gap closed by hand — ServerSocket, parsing the request text yourself, and the if/else router that grows until it is the least interesting thousand lines you own.`,subject:`Spring`,stage:`Before Spring`,accent:`#34d399`,parts:{notes:De}},{slug:`spring-servlets`,title:`Servlets and the container`,tagline:`What Tomcat is actually doing for you.`,summary:`A servlet is the standard answer to "a request arrived, run my method". This is the list of jobs the container takes off you — port, bytes, parsing, threads, lifecycle — what a servlet looks like, and then the six reasons servlets alone stopped being enough, ending at the one that Spring was written to fix.`,subject:`Spring`,stage:`Before Spring`,accent:`#fb923c`,parts:{notes:Oe}},{slug:`spring-framework`,title:`The Spring Framework`,tagline:`IoC, dependency injection, and beans.`,summary:`Inversion of control in one sentence and dependency injection as the technique that implements it — what a bean is, what the ApplicationContext does at startup, why a class that calls new can never be tested, and the module map that everything else in Spring hangs off. Ends at the six-line endpoint, with every layer underneath it still running.`,subject:`Spring`,stage:`The framework`,accent:`#8b6cff`,parts:{notes:ke}},{slug:`spring-modules`,title:`MVC, Data, Security, AOP`,tagline:`The four you will use in every project.`,summary:`Spring MVC turning a request into a method call, and why @RestController is not @Controller. The database stack from Spring Data down through JPA and Hibernate to JDBC — and which of those is the specification and which does the work. Then Security as a filter chain in front of everything, and AOP as the reason @Transactional has no transaction code in it.`,subject:`Spring`,stage:`The framework`,accent:`#f472b6`,parts:{notes:Ae}},{slug:`spring-boot`,title:`Spring Boot`,tagline:`Spring, with the configuration already decided.`,summary:`Auto-configuration, starters, the embedded server, and the sentence that clears up the most confusion: Spring Boot uses Spring, it does not replace it. What @SpringBootApplication actually expands to and the component-scan trap inside it, what you still have to understand once the typing is gone, and why microservices are an architecture rather than a framework.`,subject:`Spring`,stage:`The framework`,accent:`#22d3ee`,parts:{notes:je}},{slug:`spring-interview`,title:`The Spring interview`,tagline:`Fifteen questions, with full answers.`,summary:`The whole section as questions, graded easy to hard. What HTTP is and what HTTPS adds, status codes, what a servlet container does, IoC against DI, why new inside a class is a problem, JPA against Hibernate, what @Transactional does — and a request walked end to end from the browser to your method.`,subject:`Spring`,stage:`The interview`,accent:`#facc15`,parts:{interview:Me}}].map(e=>{let t=Ne.filter(t=>e.parts[t]).map((t,n)=>({id:t,...ee[t],content:C(e.parts[t]),route:n===0?`advanced/${e.slug}`:`advanced/${e.slug}/${t}`}));return{...e,parts:t,route:`advanced/${e.slug}`}}),Re=e=>[e.title,e.tagline,e.summary,e.subject,e.stage].concat(e.parts.map(e=>e.content)).join(` `).toLowerCase(),ze=e=>Le.find(t=>t.slug===e)??null,Be=(e,t)=>e?.parts.find(e=>e.id===t)??e?.parts[0]??null,Ve=o((e=>{var t=Symbol.for(`react.transitional.element`),n=Symbol.for(`react.fragment`);function r(e,n,r){var i=null;if(r!==void 0&&(i=``+r),n.key!==void 0&&(i=``+n.key),`key`in n)for(var a in r={},n)a!==`key`&&(r[a]=n[a]);else r=n;return n=r.ref,{$$typeof:t,type:e,key:i,ref:n===void 0?null:n,props:r}}e.Fragment=n,e.jsx=r,e.jsxs=r})),P=o(((e,t)=>{t.exports=Ve()}))(),He={search:`M11 4a7 7 0 1 0 4.2 12.6l3.6 3.6 1.4-1.4-3.6-3.6A7 7 0 0 0 11 4Zm0 2a5 5 0 1 1 0 10 5 5 0 0 1 0-10Z`,download:`M12 3v10.2l3.6-3.6 1.4 1.4-6 6-6-6 1.4-1.4L10 13.2V3h2ZM4 19h16v2H4v-2Z`,github:`M12 2a10 10 0 0 0-3.2 19.5c.5.1.7-.2.7-.5v-1.7c-2.8.6-3.4-1.3-3.4-1.3-.4-1.2-1.1-1.5-1.1-1.5-.9-.6.1-.6.1-.6 1 .1 1.5 1 1.5 1 .9 1.5 2.3 1.1 2.9.8.1-.7.4-1.1.6-1.4-2.2-.2-4.6-1.1-4.6-5 0-1.1.4-2 1-2.7-.1-.3-.4-1.3.1-2.6 0 0 .8-.3 2.7 1a9.4 9.4 0 0 1 5 0c1.9-1.3 2.7-1 2.7-1 .5 1.3.2 2.3.1 2.6.6.7 1 1.6 1 2.7 0 3.9-2.4 4.8-4.6 5 .4.3.7.9.7 1.9v2.8c0 .3.2.6.7.5A10 10 0 0 0 12 2Z`,close:`M6.4 5 5 6.4 10.6 12 5 17.6 6.4 19 12 13.4 17.6 19 19 17.6 13.4 12 19 6.4 17.6 5 12 10.6 6.4 5Z`,star:`M12 3.5l2.6 5.3 5.9.9-4.2 4.1 1 5.8-5.3-2.8-5.3 2.8 1-5.8-4.2-4.1 5.9-.9L12 3.5Z`,copy:`M8 2h10a2 2 0 0 1 2 2v12h-2V4H8V2ZM4 6h10a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2Zm0 2v12h10V8H4Z`,check:`M9.5 16.2 5.3 12l-1.4 1.4 5.6 5.6L20.1 8.4l-1.4-1.4L9.5 16.2Z`,grid:`M3 3h8v8H3V3Zm10 0h8v8h-8V3ZM3 13h8v8H3v-8Zm10 0h8v8h-8v-8Z`,list:`M3 5h18v3H3V5Zm0 5.5h18v3H3v-3ZM3 16h18v3H3v-3Z`,sun:`M12 7a5 5 0 1 0 0 10 5 5 0 0 0 0-10Zm0-5h0v3m0 14v3M2 12h3m14 0h3M4.9 4.9l2.1 2.1m10 10 2.1 2.1m0-14.2-2.1 2.1m-10 10-2.1 2.1`,moon:`M20 14.5A8.5 8.5 0 0 1 9.5 4 8.5 8.5 0 1 0 20 14.5Z`,monitor:`M3 4h18v12H3V4Zm2 2v8h14V6H5Zm3 12h8v2H8v-2Z`,eye:`M2.6 12S6.1 6.2 12 6.2 21.4 12 21.4 12 17.9 17.8 12 17.8 2.6 12 2.6 12Zm9.4-2.6a2.6 2.6 0 1 0 0 5.2 2.6 2.6 0 0 0 0-5.2Z`,link:`M10.6 13.4a4 4 0 0 1 0-5.7l3-3a4 4 0 0 1 5.7 5.7l-1.3 1.3-1.4-1.4 1.3-1.3a2 2 0 0 0-2.8-2.9l-3 3a2 2 0 0 0 0 2.9l-1.5 1.4Zm2.8-2.8a4 4 0 0 1 0 5.7l-3 3a4 4 0 0 1-5.7-5.7l1.3-1.3 1.4 1.4-1.3 1.3a2 2 0 0 0 2.8 2.9l3-3a2 2 0 0 0 0-2.9l1.5-1.4Z`,shield:`M12 2 4 5v6.5c0 4.7 3.2 9 8 10.5 4.8-1.5 8-5.8 8-10.5V5l-8-3Zm0 2.2 6 2.2v5.1c0 3.6-2.4 7-6 8.3-3.6-1.3-6-4.7-6-8.3V6.4l6-2.2Z`,bolt:`M13 2 4 14h6l-1 8 9-12h-6l1-8Z`,arrow:`M13.2 5 11.8 6.4l4.6 4.6H3v2h13.4l-4.6 4.6 1.4 1.4 7-7-7-7Z`,terminal:`M3 4h18v16H3V4Zm2 2v12h14V6H5Zm2 2.5L8.4 7l3.6 3.6L8.4 14 7 12.6l2.2-2L7 8.5ZM13 13h4v1.6h-4V13Z`,book:`M5 3h9l5 5v13H5V3Zm2 2v14h10V9h-4V5H7Zm2 6h6v1.6H9V11Zm0 3.4h6V16H9v-1.6Z`,coffee:`M4 3h12v8a6 6 0 0 1-12 0V3Zm13 2h1.5a3.5 3.5 0 0 1 0 7H17v-2h1.5a1.5 1.5 0 0 0 0-3H17V5ZM3 19h14v2H3v-2Z`},Ue=new Set([`sun`,`eye`]);function F({name:e,size:t=18,className:n=``}){let r=He[e];if(!r)return null;let i=Ue.has(e);return(0,P.jsx)(`svg`,{className:`icon ${n}`,width:t,height:t,viewBox:`0 0 24 24`,fill:i?`none`:`currentColor`,stroke:i?`currentColor`:`none`,strokeWidth:i?2:0,strokeLinecap:`round`,strokeLinejoin:`round`,"aria-hidden":`true`,focusable:`false`,children:(0,P.jsx)(`path`,{d:r})})}var We=`(prefers-reduced-motion: reduce)`,Ge=e=>Math.min(e,8)*55;function Ke(e){let[t,n]=(0,_.useState)(()=>window.matchMedia?.(e).matches??!1);return(0,_.useEffect)(()=>{let t=window.matchMedia?.(e);if(!t)return;let r=()=>n(t.matches);return r(),t.addEventListener(`change`,r),()=>t.removeEventListener(`change`,r)},[e]),t}function qe(){return Ke(We)}function Je({threshold:e=0,rootMargin:t=`0px 0px 20% 0px`}={}){let n=(0,_.useRef)(null),[r,i]=(0,_.useState)(!1);return(0,_.useEffect)(()=>{let r=n.current;if(!r)return;if(typeof IntersectionObserver>`u`){i(!0);return}let a=new IntersectionObserver(([e])=>{e.isIntersecting&&(i(!0),a.disconnect())},{threshold:e,rootMargin:t});return a.observe(r),()=>a.disconnect()},[e,t]),[n,r]}function Ye(e){let[t,n]=(0,_.useState)(!1);return(0,_.useEffect)(()=>{let t=e.current;if(!t||typeof IntersectionObserver>`u`)return;let r=null,i=()=>{r?.disconnect();let e=document.querySelector(`.site-header`)?.offsetHeight??64;r=new IntersectionObserver(([e])=>n(!e.isIntersecting),{rootMargin:`-${Math.round(e)}px 0px 0px 0px`}),r.observe(t)};return i(),window.addEventListener(`resize`,i),()=>{r?.disconnect(),window.removeEventListener(`resize`,i)}},[e]),t}function Xe(e,{offset:t=130}={}){let[n,r]=(0,_.useState)(null);return(0,_.useEffect)(()=>{if(!e.length){r(null);return}let n=0,i=()=>{n=0;let i=document.documentElement,a=window.innerHeight+window.scrollY>=i.scrollHeight-8,o=e[0];if(a)o=e[e.length-1];else for(let n of e){let e=document.getElementById(n);if(e)if(e.getBoundingClientRect().top<=t)o=n;else break}r(e=>e===o?e:o)},a=()=>{n||=requestAnimationFrame(i)};return i(),window.addEventListener(`scroll`,a,{passive:!0}),window.addEventListener(`resize`,a),()=>{cancelAnimationFrame(n),window.removeEventListener(`scroll`,a),window.removeEventListener(`resize`,a)}},[e,t]),n}var Ze=70,Qe=1.8,$e=`.notch`,et=`pre, table, .problem-table, .code-output, [data-swipe-through]`;function tt({onLeft:e,onRight:t,enabled:n=!0}={}){(0,_.useEffect)(()=>{if(!n)return;let r=null,i=e=>{if(e.pointerType!==`touch`)return;if(e.isPrimary===!1){r=null;return}if(e.target.closest?.($e)){r=null;return}let t=e.target.closest?.(et);if(t&&t.scrollWidth>t.clientWidth){r=null;return}r={x:e.clientX,y:e.clientY}},a=n=>{if(!r||n.pointerType!==`touch`)return;let i=n.clientX-r.x,a=n.clientY-r.y;r=null,!(Math.abs(i)<Ze||Math.abs(i)<Math.abs(a)*Qe)&&(i<0?e?.():t?.())},o=()=>{r=null};return window.addEventListener(`pointerdown`,i,{passive:!0}),window.addEventListener(`pointerup`,a,{passive:!0}),window.addEventListener(`pointercancel`,o,{passive:!0}),()=>{window.removeEventListener(`pointerdown`,i),window.removeEventListener(`pointerup`,a),window.removeEventListener(`pointercancel`,o)}},[e,t,n])}function nt(){let e=(0,_.useRef)(null);return{ref:e,onPointerMove:(0,_.useCallback)(t=>{let n=e.current;if(!n||t.pointerType===`touch`)return;let r=n.getBoundingClientRect();n.style.setProperty(`--mx`,`${(t.clientX-r.left)/r.width*100}%`),n.style.setProperty(`--my`,`${(t.clientY-r.top)/r.height*100}%`)},[]),onPointerLeave:(0,_.useCallback)(()=>{let t=e.current;t&&(t.style.removeProperty(`--mx`),t.style.removeProperty(`--my`))},[])}}function rt(){(0,_.useEffect)(()=>{let e=0,t=()=>{e=0;let t=document.documentElement.scrollHeight-window.innerHeight,n=t>0?window.scrollY/t:0;document.documentElement.style.setProperty(`--scroll-progress`,String(Math.min(1,Math.max(0,n))))},n=()=>{e||=requestAnimationFrame(t)};return t(),window.addEventListener(`scroll`,n,{passive:!0}),window.addEventListener(`resize`,n,{passive:!0}),()=>{cancelAnimationFrame(e),window.removeEventListener(`scroll`,n),window.removeEventListener(`resize`,n)}},[])}function it({threshold:e=6,hideAfter:t=220}={}){let[n,r]=(0,_.useState)({scrolled:!1,hidden:!1});return(0,_.useEffect)(()=>{let n=window.scrollY,i=0,a=()=>{i=0;let a=window.scrollY,o=a-n;if(Math.abs(o)<e)return;n=a;let s={scrolled:a>8,hidden:o>0&&a>t};r(e=>e.scrolled===s.scrolled&&e.hidden===s.hidden?e:s)},o=()=>{i||=requestAnimationFrame(a)};return window.addEventListener(`scroll`,o,{passive:!0}),()=>{cancelAnimationFrame(i),window.removeEventListener(`scroll`,o)}},[e,t]),n}var at={codework:{light:{"#0c73f0":`#6d4aff`},relax:{"#0c73f0":`#2f6a5a`},dark:{"#0c73f0":`#2c2559`}},design:{light:{"#e0f3ff":`#eae5ff`},relax:{"#e0f3ff":`#e5e9d9`},dark:{"#e0f3ff":`#2b2545`}}},ot=`afterhours:`,st=(e,t)=>{try{let n=localStorage.getItem(ot+e);return n===null?t:JSON.parse(n)}catch{return t}};function I(e,t){let[n,r]=(0,_.useState)(()=>st(e,t));return(0,_.useEffect)(()=>{try{localStorage.setItem(ot+e,JSON.stringify(n))}catch{}},[e,n]),[n,r]}var ct=[`light`,`relax`,`dark`,`system`],lt=new Set([`light`,`relax`,`dark`]),ut=()=>window.matchMedia?.(`(prefers-color-scheme: dark)`).matches??!1;function dt(){let[e,t]=I(`theme`,`system`);return(0,_.useEffect)(()=>{let t=()=>{let t=lt.has(e)?e:ut()?`dark`:`light`;document.documentElement.dataset.theme=t;let n=document.querySelector(`meta[name="theme-color"]`);n&&(n.content=getComputedStyle(document.documentElement).getPropertyValue(`--bg`).trim())};if(t(),e!==`system`)return;let n=window.matchMedia(`(prefers-color-scheme: dark)`);return n.addEventListener(`change`,t),()=>n.removeEventListener(`change`,t)},[e]),{theme:e,setTheme:t,cycle:()=>t(e=>ct[(ct.indexOf(e)+1)%ct.length])}}function ft(){let[e,t]=(0,_.useState)(()=>document.documentElement.dataset.theme??`light`);return(0,_.useEffect)(()=>{let e=()=>t(document.documentElement.dataset.theme??`light`);e();let n=new MutationObserver(e);return n.observe(document.documentElement,{attributes:!0,attributeFilter:[`data-theme`]}),()=>n.disconnect()},[]),e}var pt=null,mt=()=>pt??=new Promise((e,t)=>{let n=()=>window.lottie?e(window.lottie):t(Error(`no lottie player`));window.lottie?e(window.lottie):document.readyState===`loading`?document.addEventListener(`DOMContentLoaded`,n,{once:!0}):n()}),ht=new Map,gt=e=>(ht.has(e)||ht.set(e,fetch(`./animations/${e}.json`).then(t=>{if(!t.ok)throw Error(`animation ${e}: ${t.status}`);return t.json()})),ht.get(e)),_t=e=>[parseInt(e.slice(1,3),16)/255,parseInt(e.slice(3,5),16)/255,parseInt(e.slice(5,7),16)/255],vt=e=>`#`+e.slice(0,3).map(e=>Math.round(e*255).toString(16).padStart(2,`0`)).join(``);function yt(e,t){let n=new Map(Object.entries(t).map(([e,t])=>[e.toLowerCase(),_t(t)])),r=structuredClone(e),i=e=>{if(Array.isArray(e))return e.forEach(i);if(!(!e||typeof e!=`object`))for(let[t,r]of Object.entries(e)){if(t===`c`&&r?.a===0&&Array.isArray(r.k)&&typeof r.k[0]==`number`){let e=n.get(vt(r.k));e&&(r.k=[...e,...r.k.slice(3)]);continue}i(r)}};return i(r.layers),i(r.assets),r}function bt({name:e,size:t=24,height:n=t,loop:r=!0,speed:i=1,className:a=``,onDone:o,children:s=null}){let c=(0,_.useRef)(null),[l,u]=(0,_.useState)(!1),d=qe(),f=ft(),p=at[e]?.[f],m=(0,_.useRef)(o);return m.current=o,(0,_.useEffect)(()=>{let t=null,n=!1,a=0;return Promise.all([mt(),gt(e)]).then(([e,o])=>{n||!c.current||(t=e.loadAnimation({container:c.current,renderer:`svg`,loop:r,autoplay:!d,animationData:p?yt(o,p):o}),t.setSpeed(i),u(!0),d?(t.goToAndStop(Math.floor(t.totalFrames/2),!0),r||(a=setTimeout(()=>m.current?.(),1400))):r||t.addEventListener(`complete`,()=>m.current?.()))}).catch(()=>{}),()=>{n=!0,clearTimeout(a),t?.destroy()}},[e,r,i,d,p]),(0,P.jsxs)(`span`,{className:`lottie ${l?`is-ready`:``} ${a}`,style:{width:t,height:n},"aria-hidden":`true`,children:[(0,P.jsx)(`span`,{className:`lottie-stage`,ref:c}),s&&(0,P.jsx)(`span`,{className:`lottie-fallback`,children:s})]})}var xt=`(min-width: 1081px)`;function St({name:e=`codework`,width:t=420,height:n=350,className:r=`page-head`,id:i,children:a}){let o=Ke(xt);return(0,P.jsxs)(`section`,{id:i,className:`${r} ${o?`has-art`:``}`,children:[(0,P.jsx)(`div`,{className:`page-head-copy`,children:a}),o&&(0,P.jsx)(`div`,{className:`page-head-art`,"aria-hidden":`true`,children:(0,P.jsx)(bt,{name:e,size:t,height:n})})]})}function Ct({as:e=`div`,variant:t=`up`,delay:n=0,className:r=``,style:i,children:a,...o}){let[s,c]=Je();return(0,P.jsx)(e,{ref:s,className:`reveal-on-scroll reveal-${t} ${c?`is-in`:``} ${r}`,style:{"--delay":`${n}ms`,...i},...o,children:a})}var wt=(e,t,n)=>{if(!t.length)return!0;let r=n.get(e.slug);return t.every(e=>r.includes(e))};function Tt({deck:e,onOpen:t}){let[n]=e.topics,r=e.topics.reduce((e,t)=>e+t.parts.length,0);return(0,P.jsx)(Ct,{variant:`blur`,children:(0,P.jsxs)(`article`,{className:`card deck-card`,style:{"--accent":e.accent},onClick:()=>t(n.route),children:[(0,P.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,P.jsx)(`span`,{className:`dsa-rule`,"aria-hidden":`true`}),(0,P.jsxs)(`header`,{className:`dsa-card-head`,children:[(0,P.jsx)(`span`,{className:`dsa-order`,"aria-hidden":`true`,children:String(e.number).padStart(2,`0`)}),(0,P.jsx)(`span`,{className:`badge badge-article`,children:e.kind})]}),(0,P.jsx)(`h3`,{children:(0,P.jsx)(`button`,{type:`button`,className:`card-link`,onClick:e=>{e.stopPropagation(),t(n.route)},children:e.title})}),(0,P.jsx)(`p`,{className:`tagline`,children:e.tagline}),(0,P.jsx)(`p`,{className:`card-summary`,children:e.summary}),(0,P.jsx)(`ol`,{className:`deck-topics`,children:e.topics.map((e,n)=>(0,P.jsx)(`li`,{style:{"--accent":e.accent},children:(0,P.jsxs)(`button`,{type:`button`,className:`deck-chip`,title:e.tagline,onClick:n=>{n.stopPropagation(),t(e.route)},children:[(0,P.jsx)(`span`,{className:`deck-chip-n`,"aria-hidden":`true`,children:String(n+1).padStart(2,`0`)}),e.title]})},e.slug))}),(0,P.jsx)(`footer`,{className:`card-foot`,children:(0,P.jsxs)(`span`,{className:`dim`,children:[e.topics.length,` topics · `,r,` pages`]})})]})})}function Et({query:e,onQuery:t,onOpen:n}){let r=(0,_.useMemo)(()=>e.trim().toLowerCase().split(/\s+/).filter(Boolean),[e]),i=(0,_.useMemo)(()=>new Map(Le.map(e=>[e.slug,Re(e)])),[]),a=(0,_.useMemo)(()=>Ie().map(e=>({...e,topics:e.topics.filter(e=>wt(e,r,i))})).filter(e=>e.topics.length>0),[r,i]),o=a.reduce((e,t)=>e+t.topics.length,0);return(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(St,{children:[(0,P.jsxs)(`p`,{className:`eyebrow`,children:[(0,P.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`Spring, from the request up`]}),(0,P.jsxs)(`h1`,{children:[`Nothing here is magic.`,(0,P.jsx)(`br`,{}),(0,P.jsx)(`span`,{className:`grad`,children:`It is work you stopped doing.`})]}),(0,P.jsxs)(`p`,{className:`lede`,children:[`One card per lecture, written up in the order the thing was actually built. A request as two text messages; the server you could write with`,` `,(0,P.jsx)(`code`,{children:`ServerSocket`}),` and why nobody does; what a servlet container takes off you and where servlets alone run out; then inversion of control, beans, MVC, Data, Security and AOP — and finally Spring Boot, which is all of that with the configuration already decided. Read a card in order: every page is a layer, and each one only makes sense as the answer to the one before it.`]})]}),(0,P.jsxs)(`div`,{className:`toolbar`,children:[(0,P.jsx)(`div`,{className:`toolbar-row`,children:(0,P.jsxs)(`div`,{className:`search`,children:[(0,P.jsx)(F,{name:`search`,size:17,className:`search-icon`}),(0,P.jsx)(`input`,{type:`search`,value:e,placeholder:`Search Spring, questions, code…`,onChange:e=>t(e.target.value),"aria-label":`Search Spring notes`})]})}),(0,P.jsx)(`div`,{className:`chips`,children:(0,P.jsxs)(`span`,{className:`chips-count`,children:[o,` `,o===1?`topic`:`topics`]})})]}),o===0?(0,P.jsxs)(`div`,{className:`empty empty-in`,children:[(0,P.jsx)(F,{name:`search`,size:28}),(0,P.jsx)(`h3`,{children:`Nothing matches that`}),(0,P.jsx)(`p`,{children:`Try a different word, or clear the search.`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>t(``),children:`Clear search`})]}):(0,P.jsx)(`div`,{className:`deck-grid`,"aria-label":`Spring contents`,children:a.map(e=>(0,P.jsx)(Tt,{deck:e,onOpen:n},e.id))})]})}var Dt=`itsadityakr`,Ot=`Aditya Kumar`,kt=e=>`https://github.com/${Dt}/${e}`,At=e=>`./apps/${e}`,jt=[{id:`me-not-afk`,name:`Me Not AFK`,repo:`me_not_afk`,tagline:`Stay awake & show as Available.`,summary:`Keeps your screen on and your computer looking busy — so presence indicators in Teams or Slack stay green during short breaks.`,version:`0.1.0`,status:`stable`,updated:`2026-05-18`,icon:At(`me-not-afk.png`),download:`https://www.mediafire.com/file/59jq4n7vgt8jgy6/Me_Not_AFK-win32-x64.zip/file`,accent:`#34d399`,tags:[`Productivity`,`Utility`],platforms:[`Windows`],size:`141 MB`,license:`MIT`,highlights:[`One-tap toggle with a live session timer and progress ring.`,`Choose how often it checks in — 15s, 30s, 1m, 2m or 5m.`,`Auto-off timer (30m, 1h or custom up to 12h), and it can quit when done.`,`Light / Dark / System themes that follow your OS live.`,`Switches off cleanly — nothing left running behind your back.`],requirements:[`Windows 10 (1809+) or 11, x64 for full functionality`,`macOS and Linux keep the screen awake only`],privacy:`No internet connection and no tracking. Your settings stay on your machine.`},{id:`shut-me-down`,name:`Shut Me Down`,repo:`shut_me_down`,tagline:`Set a timer, walk away.`,summary:`Schedules a power action — shut down, restart, sleep, hibernate, lock or sign out — after a delay or at a wall-clock time, with a live countdown dial and one-tap cancel.`,version:`0.1.0`,status:`stable`,updated:`2026-06-02`,icon:At(`shut-me-down.png`),download:`https://www.mediafire.com/file/m921qt2losiqfac/Shut_Me_Down-win32-x64.zip/file`,accent:`#fb923c`,tags:[`Utility`,`Power`],platforms:[`Windows`],size:`141 MB`,license:`MIT`,highlights:[`Six actions — shut down, restart, sleep, hibernate, lock, sign out.`,`Schedule by delay (15m to 7 days) or by clock time, rolling to tomorrow if past.`,`Countdown dial that turns red in the final minute.`,`Shut down and restart still go ahead even if you close the app.`,`Force-close toggle, a confirmation before arming, and cancel any time.`],requirements:[`Windows 10 (1809+) or 11, x64`,`Scheduling works on Windows only`],privacy:`No internet connection and no tracking. Your preferences stay on your machine.`},{id:`context-menu-tool`,name:`Context Menu Tool`,repo:`reg_e`,tagline:`Own your right-click menu.`,summary:`Add, edit and remove your own Windows right-click entries from a small desktop app — for files, folders, folder background and the desktop.`,version:`1.0.0`,status:`stable`,updated:`2026-04-27`,icon:At(`context-menu-tool.png`),download:`https://www.mediafire.com/file/8wfj0yg04fb3k7j/Context_Menu_Tool-win32-x64.zip/file`,accent:`#60a5fa`,tags:[`Utility`,`Registry`],platforms:[`Windows`],size:`111 MB`,license:`MIT`,highlights:[`Four targets — files, folders, folder background and desktop background.`,`Give each entry a name, an icon and whatever you want it to open.`,`List, edit and remove the entries you created.`,`No administrator rights needed.`],requirements:[`Windows 10 or 11, x64`],privacy:`Changes apply to your account only, and nothing is sent anywhere.`},{id:`control-deck`,name:`Control Deck`,repo:`control_deck`,tagline:`Your PC, from your phone.`,summary:`Scan a QR code on your PC and your phone becomes a remote — volume, displays, media keys and system actions, all over your own WiFi.`,version:`0.1.0`,status:`beta`,updated:`2026-07-11`,icon:null,download:`https://www.mediafire.com/file/7b0q3mvi48g3fcb/Control_Deck-win32-x64.zip/file`,accent:`#a78bfa`,tags:[`Remote`,`Productivity`],platforms:[`Windows`,`Phone browser`],size:`110 MB`,license:`MIT`,highlights:[`Pair by scanning a QR code — nothing to install on the phone.`,`Volume, display, media and system controls, straight from your pocket.`,`Runs entirely on your own WiFi — nothing goes out to the internet.`,`Lives quietly in the system tray.`],requirements:[`Windows 10 or 11, x64`,`PC and phone on the same WiFi network`],privacy:`Traffic never leaves your local network, and pairing lasts only for the session.`}].map(e=>({...e,url:kt(e.repo),downloadUrl:e.download,cloneCommand:`git clone ${kt(e.repo)}.git`,issuesUrl:`${kt(e.repo)}/issues`})),Mt=[...new Set(jt.flatMap(e=>e.tags))].sort(),Nt={stable:`Stable`,beta:`Beta`,wip:`In progress`},Pt=`The Microsoft Store has a particular way of failing. The download does not error
and it does not stop — it sits at \`Pending\`, or crawls at a fraction of the speed
the same connection gives everything else. Nothing in the interface explains why,
so the fix is a short list of usual suspects, worked through cheapest first.

![What throttles Store downloads, and the three methods in cheapest-first order](diagrams/microsoft-store-slow-downloads-escalation.jpg)

> Start at Method 1 and stop as soon as the download moves. Method 3 re-registers
> the Store app itself and is the heaviest of the three — there is no reason to
> reach for it if a metered-connection toggle was the whole problem.

## Why it happens

Three things throttle Store traffic more often than a slow connection does:

- **Delivery Optimization**, which caps how much bandwidth background downloads
  may use and is set conservatively out of the box.
- **A metered connection**, which Windows sets automatically on some Wi-Fi and
  mobile networks and which pauses Store downloads almost entirely.
- **A stale Store cache**, where a half-finished download blocks the queue behind
  it and never times out on its own.

## Method 1: Give the Store its bandwidth back

### 1. Find out what else is using the connection

Press \`Ctrl + Shift + Esc\` for Task Manager, open the **Performance** tab and
watch the network graph, or sort the **Processes** tab by **Network**. A sync
client or a game launcher updating in the background is the single most common
cause. End what you do not need right now.

### 2. Turn off the metered connection

Press \`Win + I\` for Settings, then **Network & internet**, then your active
adapter — **Wi-Fi** or **Ethernet** — and open its properties. Turn
**Metered connection** off.

> On a metered connection Windows deliberately holds back Store downloads and
> most updates. If this toggle was on, it is very likely the answer.

### 3. Raise the Delivery Optimization limit

Go to **Settings → Windows Update → Advanced options → Delivery Optimization →
Advanced options**. Switch the download limits from **Absolute bandwidth** to
**Percentage of measured bandwidth**, and set both the foreground and background
sliders to \`100%\`.

### 4. Repair and reset the Store app

Search the Start menu for **Microsoft Store**, right-click it and choose
**App settings**. Set **Let this app run in the background** to **Always**, then
click **Terminate**, then **Repair**. If the download still sticks, come back and
click **Reset**.

> **Repair** keeps your data and preferences. **Reset** clears the app's local
> state, which includes the stuck download queue — that is exactly what you want
> here, and it does not touch your installed apps or your Microsoft account.

### 5. Clear the network and Store caches

Open **Command Prompt** as administrator — \`Win + X\`, then **Terminal (Admin)** —
and run these in order:

\`\`\`shell
ipconfig /flushdns
netsh winsock reset
wsreset
\`\`\`

\`wsreset\` opens an empty Store window when it finishes, which is how you know it
worked. \`netsh winsock reset\` needs a restart to fully take effect.

> A \`netsh winsock reset\` drops custom socket layers some VPN clients install. If
> a VPN stops connecting afterwards, reinstall its client — it will put its own
> layer back.

## Method 2: Point DNS somewhere faster

The Store resolves a lot of hostnames per download, and a slow resolver shows up
as a long stall before the transfer ever starts.

### 1. Open the adapter properties

Press \`Win + R\`, type \`ncpa.cpl\` and press Enter. Right-click your active
connection and choose **Properties**.

### 2. Set the DNS servers by hand

Select **Internet Protocol Version 4 (TCP/IPv4)**, click **Properties**, choose
**Use the following DNS server addresses** and fill in a public pair:

| Provider   | Preferred | Alternate |
| ---------- | --------- | --------- |
| Google     | \`8.8.8.8\` | \`8.8.4.4\` |
| Cloudflare | \`1.1.1.1\` | \`1.0.0.1\` |

Click **OK**, then flush the cache so nothing stale survives the change:

\`\`\`shell
ipconfig /flushdns
\`\`\`

### 3. Check which one is actually closer to you

\`\`\`shell
ping 8.8.8.8
ping 1.1.1.1
\`\`\`

Whichever returns the lower average time is the one to keep as **Preferred**.
The difference is usually small; if it is large, that alone was probably the
bottleneck.

## Method 3: Re-register the Store

If the Store still refuses, rebuild its registration. Open **PowerShell as
administrator** and run:

\`\`\`powershell
Get-AppxPackage -AllUsers Microsoft.WindowsStore* | ForEach-Object { Add-AppxPackage -DisableDevelopmentMode -Register "$($_.InstallLocation)\\AppXManifest.xml" }
\`\`\`

Red text scrolling past is normal here — the command touches packages it cannot
always re-register, and the Store itself usually succeeds anyway. Restart the PC
afterwards; the registration is not fully live until you do.

## If none of it works

- Check for pending Windows updates in **Settings → Windows Update**. The Store
  depends on components that ship with them.
- Try a different network — a phone hotspot is enough to prove whether the
  problem is the machine or the connection.
- Sign out of the Store and back in. A stale token presents as a permanent
  \`Pending\`, with no error to explain it.
- Run \`sfc /scannow\`, then \`DISM /Online /Cleanup-Image /RestoreHealth\`, if the
  machine has other unexplained app problems as well.
`,Ft=`Office LTSC is the perpetual, offline-first build of Office: Word, Excel,
PowerPoint and Outlook installed once, with security updates but no feature
churn and no cloud requirement. It installs through the **Office Deployment
Tool** — \`setup.exe\` plus an XML file that says which apps you want — rather than
through a wizard, which is what makes it worth writing down.

> This covers a licensed install: an account that carries an Office licence, a
> product key you hold, or a volume-licence deployment against your
> organisation's KMS host. If none of those apply, Microsoft 365 has a personal
> subscription and there is a free web version.

## Before you start

- **Administrator rights** — the installer writes to \`Program Files\`.
- **About 4 GB free**, plus room for the download.
- **The Office Deployment Tool**, from Microsoft's Download Center. It is a
  self-extracting executable that gives you \`setup.exe\` and a sample XML.
- **No other Office installed.** Mixing an existing Click-to-Run Office with a
  new LTSC install is the most common way this goes wrong — uninstall the old one
  from **Settings → Apps** first.

Extract the deployment tool somewhere short and easy to type. \`C:\\office2024\`
works and is what the commands below assume.

## Step 1: Write the configuration file

This is the whole install, described in one file — which apps, which language,
32- or 64-bit, and how quietly it runs:

\`\`\`xml configuration.xml
<Configuration>
  <Add OfficeClientEdition="64" Channel="PerpetualVL2024">
    <Product ID="ProPlus2024Volume" PIDKEY="">
      <Language ID="en-us" />
      <ExcludeApp ID="Groove" />
      <ExcludeApp ID="Lync" />
      <ExcludeApp ID="OneDrive" />
      <ExcludeApp ID="Bing" />
    </Product>
  </Add>

  <Property Name="SharedComputerLicensing" Value="0" />
  <Property Name="DeviceBasedLicensing" Value="0" />
  <Property Name="AUTOACTIVATE" Value="0" />

  <Updates Enabled="TRUE" />
  <Display Level="Full" AcceptEULA="TRUE" />
  <Logging Level="Standard" Path="%temp%" />
</Configuration>
\`\`\`

Save it next to \`setup.exe\`. The parts worth changing:

- **\`OfficeClientEdition\`** — \`64\` unless you have a specific reason for \`32\`.
- **\`Channel\`** — \`PerpetualVL2024\` for Office LTSC 2024, \`PerpetualVL2021\` for
  the 2021 release.
- **\`Product ID\`** — \`ProPlus2024Volume\` for the full suite,
  \`Standard2024Volume\` for the Standard edition.
- **\`PIDKEY\`** — leave it empty to activate later, or put your key in it.
- **\`ExcludeApp\`** — one line per app you do not want. \`Access\`, \`Publisher\`,
  \`Outlook\` and \`Teams\` are the other names people usually reach for.
- **\`Display Level\`** — \`Full\` shows the progress window; \`None\` installs
  silently.

> Use \`Display Level="None"\` only in a script. Run interactively it looks like
> nothing is happening for ten minutes, and people reboot halfway through.

## Step 2: Install from an elevated prompt

Press \`Win + S\`, type \`cmd\`, right-click **Command Prompt** and choose
**Run as administrator**. Then:

\`\`\`cmd
cd /d C:\\office2024
setup.exe /configure configuration.xml
\`\`\`

Downloading and installing takes anywhere from a few minutes to half an hour
depending on the connection. The window closes when it is done.

To download the payload once and install several machines from it offline:

\`\`\`cmd
setup.exe /download configuration.xml
\`\`\`

That pulls the files into an \`Office\` folder next to \`setup.exe\`; copy the whole
folder and run the \`/configure\` line on each machine.

![Installing straight from setup.exe, or downloading the payload once for many machines](diagrams/office-365-ltsc-setup-setup-modes.jpg)

> If setup exits immediately, look in \`%temp%\` for the log the \`Logging\` element
> writes. A wrong \`Channel\` / \`Product ID\` pairing and a leftover Office install
> account for most instant failures.

## Step 3: Activate

![The three routes an Office LTSC install can activate through](diagrams/office-365-ltsc-setup-activation.jpg)

**With a Microsoft account that holds a licence** — open Word, go to
**File → Account → Sign in**, and it activates itself.

**With a product key you hold** — **File → Account → Change Product Key**, type
it in, restart the app.

**On a volume licence, against your KMS host** — Office ships with a public
generic volume-licence key already applied, and activates against the KMS server
your network points it at. Set the host explicitly and trigger activation from
the Office install folder:

\`\`\`cmd
cd /d "C:\\Program Files\\Microsoft Office\\Office16"
cscript ospp.vbs /sethst:kms.your-domain.internal
cscript ospp.vbs /act
\`\`\`

Check where it stands at any time:

\`\`\`cmd
cscript ospp.vbs /dstatus
\`\`\`

\`LICENSE STATUS: ---LICENSED---\` is what you are looking for. Without a KMS host
to talk to, this path simply will not activate — that is the design, not a fault.

## Step 4: Confirm the install

- Open Word. **File → Account** shows the edition, the channel and the
  activation state on one screen.
- **Settings → Apps → Installed apps** lists a single Office entry. Two entries
  means an older install survived and should be removed.
- **File → Account → Update Options → Update Now** confirms updates work.

## When it goes wrong

- **"We're sorry, we had a problem installing"** — an existing Office is in the
  way. Uninstall it, or use Microsoft's Support and Recovery Assistant to remove
  the remnants, then run setup again.
- **Setup runs but nothing appears** — check the log in \`%temp%\`; a typo in
  \`configuration.xml\` fails silently at the parse stage.
- **Activation cannot reach the server** — from the machine, confirm the KMS host
  resolves and that TCP 1688 is open to it.
- **Outlook missing** — it is excluded in the sample above. Delete its
  \`ExcludeApp\` line and re-run \`/configure\`; only the missing app is added.
`,It=`A USB webcam is the cheapest camera you can put on a Pi, and it works without any
of the ribbon-cable ceremony the official camera modules need. The catch is that
"it appears as \`/dev/video0\`" is not the same as "it works" — permissions, formats
and frame rates all have their own ways of failing quietly.

This gets a Logitech C270 — or any UVC webcam — from plugged in to streaming
frames into OpenCV, checking at each step that it actually did something.

## What you need

- Raspberry Pi 5 running Raspberry Pi OS (64-bit)
- A USB webcam. The C270 is what this was written against; anything UVC-class
  behaves the same
- An internet connection for the packages

> Plug the camera into a **USB 2.0** port (the black ones) rather than USB 3.0 if
> you have interference problems on 2.4 GHz Wi-Fi. USB 3.0 is a well-documented
> source of 2.4 GHz noise, and a camera streaming continuously is exactly the
> load that shows it up.

## Step 1: Plug it in and update

\`\`\`bash
sudo apt update && sudo apt full-upgrade -y
\`\`\`

## Step 2: Confirm the Pi sees it

\`\`\`bash
lsusb
\`\`\`

Look for a line naming the camera:

\`\`\`text
Bus 001 Device 005: ID 046d:0825 Logitech, Inc. Webcam C270
\`\`\`

More detail, including the USB speed it negotiated:

\`\`\`bash
usb-devices
\`\`\`

And the device node it was given:

\`\`\`bash
ls -l /dev/video*
\`\`\`

\`\`\`text
crw-rw----+ 1 root video 81, 0 Sep 10 12:34 /dev/video0
\`\`\`

> Most webcams claim **two** nodes — \`/dev/video0\` for frames and \`/dev/video1\`
> for metadata. Opening the wrong one gives you a camera that reports success and
> returns nothing. If index \`0\` yields no frames, try \`1\`.

![A UVC webcam claims two device nodes and only one of them gives frames](diagrams/raspberry-pi-5-external-webcam-video-nodes.jpg)

## Step 3: Install the tools

\`\`\`bash
sudo apt install -y v4l-utils fswebcam ffmpeg
\`\`\`

- **\`v4l-utils\`** — \`v4l2-ctl\`, which asks the camera what it can do
- **\`fswebcam\`** — grabs a still from the command line
- **\`ffmpeg\`** — brings \`ffplay\`, for watching the live feed

## Step 4: Fix the permissions

Your user needs to be in the \`video\` group:

\`\`\`bash
groups
sudo usermod -aG video "$USER"
\`\`\`

Log out and back in — group membership is only picked up by new sessions. Then
confirm:

\`\`\`bash
groups | grep video
\`\`\`

If the device node still is not owned by the \`video\` group, add a udev rule:

\`\`\`bash
sudo nano /etc/udev/rules.d/99-webcam.rules
\`\`\`

\`\`\`text 99-webcam.rules
SUBSYSTEM=="video4linux", ATTR{name}=="C270 HD WEBCAM", GROUP="video", MODE="0660"
\`\`\`

Save with \`Ctrl + X\`, \`Y\`, \`Enter\`, then reload and replug the camera:

\`\`\`bash
sudo udevadm control --reload-rules && sudo udevadm trigger
\`\`\`

## Step 5: Ask the camera what it supports

This is the step that saves the most time later:

\`\`\`bash
v4l2-ctl --list-devices
v4l2-ctl -d /dev/video0 --list-formats-ext
\`\`\`

You get every format with its resolutions and frame rates. A C270 will show
\`YUYV\` at up to 1280x960 and \`MJPG\` at 1280x720.

> **Ask for MJPG.** On \`YUYV\` the camera sends raw frames and a C270 manages
> about 5 fps at 720p over USB 2.0. In \`MJPG\` the camera compresses first and
> gives you 30. Nearly every "my webcam is so slow" problem is this.

![YUYV against MJPG on a C270 at 720p over USB 2.0](diagrams/raspberry-pi-5-external-webcam-mjpg.jpg)

Check the controls it exposes — brightness, autofocus, exposure:

\`\`\`bash
v4l2-ctl -d /dev/video0 --list-ctrls
\`\`\`

## Step 6: Take a picture

\`\`\`bash
fswebcam -d /dev/video0 -r 1280x720 --no-banner test.jpg
\`\`\`

Open \`test.jpg\`. If it is dark or green, the camera needed a moment to settle its
auto-exposure — tell \`fswebcam\` to throw the first frames away:

\`\`\`bash
fswebcam -d /dev/video0 -r 1280x720 -S 10 --no-banner test.jpg
\`\`\`

## Step 7: Watch it live

With a desktop, over VNC or with X forwarding:

\`\`\`bash
ffplay -f v4l2 -input_format mjpeg -video_size 1280x720 /dev/video0
\`\`\`

There is also \`cheese\`, if you want a graphical app with a shutter button:

\`\`\`bash
sudo apt install -y cheese
cheese
\`\`\`

## Step 8: Read frames in OpenCV

Inside a virtual environment with \`opencv-python\` installed:

\`\`\`python test_webcam.py
import cv2

# Ask V4L2 directly — the default backend probes several APIs and is slower
# to open on Linux.
cap = cv2.VideoCapture(0, cv2.CAP_V4L2)

# MJPG before size: the camera switches format, then applies the resolution.
cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter_fourcc(*"MJPG"))
cap.set(cv2.CAP_PROP_FRAME_WIDTH, 1280)
cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 720)
cap.set(cv2.CAP_PROP_FPS, 30)

if not cap.isOpened():
    raise SystemExit("Could not open the camera — check /dev/video* and groups")

print("Capturing at {}x{} @ {} fps".format(
    int(cap.get(cv2.CAP_PROP_FRAME_WIDTH)),
    int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT)),
    cap.get(cv2.CAP_PROP_FPS),
))

try:
    while True:
        ok, frame = cap.read()
        if not ok:
            print("Dropped a frame")
            continue

        cv2.imshow("Webcam", frame)

        key = cv2.waitKey(1) & 0xFF
        if key == ord("q"):
            break
        if key == ord("s"):
            cv2.imwrite("snapshot.jpg", frame)
            print("Saved snapshot.jpg")
finally:
    cap.release()
    cv2.destroyAllWindows()
\`\`\`

\`\`\`bash
python test_webcam.py
\`\`\`

\`q\` quits, \`s\` saves a still. The \`finally\` block matters: leave without
releasing the device and the next run finds the camera busy.

Running headless? Drop \`cv2.imshow\` and write frames instead:

\`\`\`python capture_headless.py
import cv2

cap = cv2.VideoCapture(0, cv2.CAP_V4L2)
cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter_fourcc(*"MJPG"))

for i in range(5):
    ok, frame = cap.read()
    if ok:
        cv2.imwrite(f"frame_{i:02d}.jpg", frame)

cap.release()
print("Wrote 5 frames")
\`\`\`

## When it does not work

- **\`Could not open the camera\`** — something else holds it. \`sudo fuser -v
  /dev/video0\` names the process; \`cheese\` and \`ffplay\` are the usual suspects.
- **Permission denied** — you are not in \`video\` yet, or you have not logged out
  since being added.
- **Black or green frames** — auto-exposure has not settled. Discard the first
  few frames, or fix exposure with \`v4l2-ctl -d /dev/video0 -c
  auto_exposure=1 -c exposure_time_absolute=200\`.
- **Very low frame rate** — you are on \`YUYV\`. Set \`MJPG\` before the resolution,
  as above.
- **The camera vanishes mid-session** — power. A camera on a long unpowered
  extension browns out; use a powered hub or a shorter cable.
- **\`select() timeout\`** — a marginal USB cable, almost every time.

## Further reading

- [fswebcam manual](http://manpages.ubuntu.com/manpages/bionic/man1/fswebcam.1.html)
- [Video4Linux (v4l-utils)](https://www.linuxtv.org/wiki/index.php/Video4Linux)
`,Lt=`A Raspberry Pi does not need a monitor, a keyboard or an Ethernet cable to be set
up. Raspberry Pi Imager can write the Wi-Fi credentials, the hostname, your user
account and an SSH key into the card before it ever boots — so the first time the
board powers on, it joins your network and waits for you.

This is the whole route from a blank microSD card to a desktop on your screen:
write the card, find the board, SSH in, turn on VNC. Plus the trick that makes it
survive a change of network — a phone hotspot with the same SSID.

![From a written microSD card to a VNC desktop, with no monitor attached](diagrams/raspberry-pi-5-headless-setup-card-to-desktop.jpg)

## What you need

**Hardware**

- Raspberry Pi 5 and its 27 W USB-C power supply — an underpowered phone charger
  causes reboots that look like software faults
- microSD card, 16 GB or larger, and a reader
- A Wi-Fi network, or a phone that can make a hotspot

**Software on the Windows machine**

- [Raspberry Pi Imager](https://www.raspberrypi.com/software/) — writes the card
- [Angry IP Scanner](https://angryip.org/) — finds the board on the network
- [PuTTY](https://www.putty.org/) — SSH, if you would rather not use the built-in
  \`ssh\` command
- [RealVNC Viewer](https://www.realvnc.com/en/connect/download/viewer/) — the
  desktop

> Windows 10 and 11 both ship with OpenSSH, so \`ssh pi@raspberrypi.local\` in
> Terminal does everything PuTTY does. PuTTY is listed because its saved sessions
> are convenient when you have several boards.

## Step 1: Write the card

Download the image first if your connection is unreliable — Imager can fetch it
for you, but a failed download halfway through a 3 GB image wastes more time than
it saves. Grab **Raspberry Pi OS (64-bit) with desktop** from the
[OS downloads page](https://www.raspberrypi.com/software/operating-systems/).

1. Open Imager.
2. **Choose Device** → Raspberry Pi 5.
3. **Choose OS** → the downloaded file via **Use custom**, or pick
   **Raspberry Pi OS (64-bit)** to let Imager download it.
4. **Choose Storage** → your card. Check the size; it is about to be erased.
5. Click **Next**, then **Edit Settings** when it offers OS customisation. This
   is the part that makes the setup headless:

   - **Set hostname**: \`raspberrypi5\` — you will reach it as
     \`raspberrypi5.local\`
   - **Set username and password**: \`pi\` and something you will remember
   - **Configure wireless LAN**: your SSID and password, exactly as typed
     elsewhere — both are case-sensitive
   - **Wireless LAN country**: required, and the radio stays off without it
   - **Locale settings**: timezone and keyboard layout
   - **Services tab → Enable SSH**: password authentication, or paste a public
     key if you have one

6. **Save**, then **Yes** to apply the settings, then **Yes** to erase and write.

> On a 2.4 GHz-only network, make sure the phone or router is not broadcasting
> the same SSID on 5 GHz with different credentials. Mismatched bands under one
> name are a common reason a board silently never appears.

## Step 2: First boot

Put the card in, connect power, and leave it alone for **four or five minutes**.
The first boot resizes the filesystem and reboots itself once. Later boots take
two or three.

The green LED flickering is disk activity — steady green with nothing happening
usually means the card was written badly. Write it again before assuming worse.

## Step 3: Find it on the network

Try the name first, from Windows Terminal:

\`\`\`powershell
ping raspberrypi5.local
\`\`\`

If a reply comes back, you have the address and can skip the scanner. mDNS is not
always allowed on managed or guest networks, in which case:

1. Find your own subnet:

   \`\`\`powershell
   ipconfig
   \`\`\`

   Look at the **IPv4 Address** of your active adapter — say \`192.168.1.24\`.

2. Open Angry IP Scanner and set the range to that subnet: \`192.168.1.0\` to
   \`192.168.1.255\`.
3. **Start**, and watch for the hostname you set. Note the address, for example
   \`192.168.1.50\`.

> Give the board a reserved address in your router's DHCP settings once you find
> it. Then the address stops changing and none of this has to be repeated.

## Step 4: SSH in

From Windows Terminal:

\`\`\`powershell
ssh pi@raspberrypi5.local
\`\`\`

or with PuTTY: host \`raspberrypi5.local\` (or the IP), port \`22\`, connection type
**SSH**, then **Open**.

The first connection asks you to trust the host key — that is expected exactly
once per board. Then log in with the username and password from Step 1.

Update everything before you build anything on top of it:

\`\`\`bash
sudo apt update && sudo apt full-upgrade -y
\`\`\`

> Host key warnings on a later connection usually mean the address was reused by
> a different machine, not that anything is wrong. Remove the stale entry with
> \`ssh-keygen -R raspberrypi5.local\` and connect again.

## Step 5: Turn on VNC

\`\`\`bash
sudo raspi-config
\`\`\`

- **Interface Options → VNC → Yes.** Wait for the confirmation that the server is
  enabled.
- **Display Options → VNC Resolution**, and pick something real like
  \`1920x1080\`. Headless boards have no monitor to negotiate with, and the default
  is a small window.
- **Advanced Options → Wayland**, and choose **X11** if VNC refuses to connect
  later. RealVNC's viewer is happier with X11 on Bookworm.

Finish, then:

\`\`\`bash
sudo reboot
\`\`\`

The SSH session drops, which is expected.

## Step 6: Open the desktop

Wait two or three minutes for the reboot, then open **VNC Viewer**, enter
\`raspberrypi5.local\` or the IP address, and sign in with the same credentials.
The full desktop appears.

Check what you are running once you are in:

\`\`\`bash
hostnamectl
vcgencmd measure_temp
\`\`\`

## Keeping access when the network changes

![A phone hotspot named after the home network, with the Pi and laptop on it](diagrams/raspberry-pi-5-headless-setup-hotspot.jpg)

A board configured for one Wi-Fi network is unreachable anywhere else. The fix is
a hotspot that pretends to be that network:

1. On your phone, open **Settings → Hotspot**.
2. Set the hotspot **name and password to exactly the SSID and password you gave
   Imager**.
3. Turn it on before powering the board, and connect your laptop to the same
   hotspot.

The Pi joins what it thinks is home, your laptop is on the same network, and
everything above works unchanged — useful on a desk that is not yours.

To add a second network properly rather than by impersonation:

\`\`\`bash
sudo nmcli device wifi connect "OtherNetwork" password "secret"
sudo nmcli connection show
\`\`\`

Bookworm uses NetworkManager, so both networks are remembered and it joins
whichever it can see.

## When it does not work

- **Nothing on the scan.** Confirm the Wi-Fi country was set, the SSID has no
  stray space, and the network is 2.4 GHz-capable. Give it a full five minutes.
- **\`ssh: connect to host ... port 22: Connection refused\`.** SSH was not enabled
  in Imager. Put the card back in the reader and create an empty file named \`ssh\`
  in the boot partition — the Pi enables it on the next boot.
- **VNC connects to a black screen.** The desktop session is not running: switch
  to X11 in \`raspi-config\`, set a real resolution, reboot.
- **\`Cannot currently show the desktop\`.** Same cause — the Wayland session is
  not shareable. X11 fixes it.
- **Random reboots under load.** Power. A Pi 5 wants the 27 W supply, especially
  with an SSD or a camera attached.
`,Rt=`You already own a decent camera: the one in your laptop lid. This points it at\r
the Pi — FFmpeg encodes on the laptop and throws H.264 over UDP, and a short\r
Python script on the Pi displays the feed and lets you grab stills or record\r
clips from it.\r
\r
It is useful when the Pi is the machine doing the vision work but sits where a\r
camera cannot, and it costs nothing but a Wi-Fi network both machines are on.\r
\r
> UDP is chosen deliberately. It drops frames rather than queueing them, which is\r
> exactly what you want for a live view — TCP would buy reliability at the price\r
> of latency that climbs the whole time it runs.\r
\r
## The shape of it\r
\r
![The shape of it](diagrams/raspberry-pi-5-laptop-webcam-stream-91f24c1d.jpg)\r
\r
## What you need\r
\r
- A Windows laptop with a working camera\r
- A Raspberry Pi 5 on the **same network**, and its IP address\r
- FFmpeg on both\r
\r
## Step 1: FFmpeg on the laptop\r
\r
\`\`\`powershell\r
winget install --id Gyan.FFmpeg -e\r
\`\`\`\r
\r
Close and reopen the terminal so \`PATH\` picks it up, then:\r
\r
\`\`\`powershell\r
ffmpeg -version\r
\`\`\`\r
\r
## Step 2: Find the camera's exact name\r
\r
\`\`\`powershell\r
ffmpeg -list_devices true -f dshow -i dummy\r
\`\`\`\r
\r
The command ends with an error — that is normal, \`dummy\` is not a real input. The\r
part you want is above it:\r
\r
\`\`\`text\r
[dshow @ ...] "HP TrueVision HD Camera" (video)\r
[dshow @ ...]   Alternative name "@device_pnp_\\\\?\\usb#vid_..."\r
\`\`\`\r
\r
Copy the quoted name **character for character**, including spaces.\r
\r
Then ask what that camera can actually do:\r
\r
\`\`\`powershell\r
ffmpeg -f dshow -list_options true -i video="HP TrueVision HD Camera"\r
\`\`\`\r
\r
> If the name has awkward characters, use the **Alternative name** string\r
> instead. It is ugly, but it never gets misquoted.\r
\r
## Step 3: Prepare the Pi\r
\r
\`\`\`bash\r
sudo apt update && sudo apt full-upgrade -y\r
sudo apt install -y ffmpeg python3-opencv\r
\`\`\`\r
\r
Get its address — you need it for the laptop command:\r
\r
\`\`\`bash\r
hostname -I\r
\`\`\`\r
\r
## Step 4: Start streaming\r
\r
On the laptop, with the Pi's address in place of \`192.168.1.50\`:\r
\r
\`\`\`powershell\r
ffmpeg -f dshow -i video="HP TrueVision HD Camera" -vcodec libx264 -preset ultrafast -tune zerolatency -pix_fmt yuv420p -b:v 1500k -f mpegts udp://192.168.1.50:1234\r
\`\`\`\r
\r
What each part is doing:\r
\r
| Flag                  | Why it is there                                        |\r
| --------------------- | ------------------------------------------------------ |\r
| \`-f dshow\`            | DirectShow — the Windows capture API                   |\r
| \`-vcodec libx264\`     | H.264, which the Pi decodes cheaply                    |\r
| \`-preset ultrafast\`   | Encode speed over file size; latency is the priority   |\r
| \`-tune zerolatency\`   | No B-frames, no lookahead                              |\r
| \`-pix_fmt yuv420p\`    | The pixel format every decoder accepts                 |\r
| \`-b:v 1500k\`          | 1.5 Mbit/s — fine for 720p over Wi-Fi                  |\r
| \`-f mpegts\`           | A container that survives packet loss                  |\r
| \`udp://IP:1234\`       | Where it goes                                          |\r
\r
Confirm packets are arriving before writing any Python. On the Pi:\r
\r
\`\`\`bash\r
ffplay -fflags nobuffer -flags low_delay -i udp://@:1234\r
\`\`\`\r
\r
A window with your face in it means the network half is done.\r
\r
## Step 5: Receive it in Python\r
\r
\`\`\`python receive_stream.py\r
import datetime\r
\r
import cv2\r
\r
# @ means "listen on every interface" rather than "connect to a host".\r
STREAM_URL = "udp://@0.0.0.0:1234"\r
\r
cap = cv2.VideoCapture(STREAM_URL, cv2.CAP_FFMPEG)\r
if not cap.isOpened():\r
    raise SystemExit("No stream. Is ffmpeg running on the laptop?")\r
\r
# One frame of buffer: show the newest picture, drop whatever fell behind.\r
cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)\r
\r
fourcc = cv2.VideoWriter_fourcc(*"XVID")\r
writer = None\r
\r
\r
def stamp():\r
    return datetime.datetime.now().strftime("%Y%m%d_%H%M%S")\r
\r
\r
print("s = save a still   v = start/stop recording   q = quit")\r
\r
try:\r
    while True:\r
        ok, frame = cap.read()\r
        if not ok:\r
            print("No frame — waiting for the stream")\r
            continue\r
\r
        cv2.imshow("Video Stream", frame)\r
        key = cv2.waitKey(1) & 0xFF\r
\r
        if key == ord("s"):\r
            name = f"image_{stamp()}.jpg"\r
            cv2.imwrite(name, frame)\r
            print("Saved", name)\r
\r
        elif key == ord("v"):\r
            if writer is None:\r
                name = f"video_{stamp()}.avi"\r
                height, width = frame.shape[:2]\r
                writer = cv2.VideoWriter(name, fourcc, 20.0, (width, height))\r
                print("Recording to", name)\r
            else:\r
                writer.release()\r
                writer = None\r
                print("Recording stopped")\r
\r
        elif key == ord("q"):\r
            break\r
\r
        if writer is not None:\r
            writer.write(frame)\r
finally:\r
    cap.release()\r
    if writer is not None:\r
        writer.release()\r
    cv2.destroyAllWindows()\r
\`\`\`\r
\r
Run it on the Pi, with the laptop already streaming:\r
\r
\`\`\`bash\r
python3 receive_stream.py\r
\`\`\`\r
\r
> \`writer\` doubles as the recording flag — if it is not \`None\`, recording is in\r
> progress. One variable instead of two cannot fall out of sync with itself.\r
\r
## Checking the port\r
\r
If nothing arrives, prove the Pi is listening before blaming the code:\r
\r
\`\`\`bash\r
sudo apt install -y netcat-openbsd\r
nc -ul 1234\r
\`\`\`\r
\r
Running with no output means the port is open and quiet. Bytes scrolling past\r
mean the stream is reaching the Pi and the problem is downstream, in OpenCV.\r
\r
Check the firewall too — Windows blocks outbound UDP from new applications on\r
first use, and the prompt is easy to dismiss without reading:\r
\r
\`\`\`powershell\r
Get-NetFirewallRule -DisplayName "*ffmpeg*" | Select-Object DisplayName, Action, Enabled\r
\`\`\`\r
\r
## Tuning it\r
\r
- **Too much latency** — lower the bitrate, drop the resolution, and add\r
  \`-g 30\` so keyframes are more frequent. Wi-Fi is nearly always the limit.\r
- **Torn or smeared picture** — packet loss. Reduce the bitrate; move closer to\r
  the access point; prefer 5 GHz.\r
- **Better quality** — raise \`-b:v\` to \`3000k\` and set the size explicitly with\r
  \`-video_size 1280x720\`. Watch latency as you go up.\r
- **Sound as well** — add the microphone as a second dshow input:\r
  \`-f dshow -i video="Camera":audio="Microphone (Realtek)"\`.\r
\r
## When it does not work\r
\r
- **\`Could not run graph\`** — the camera name is wrong, or another app has the\r
  camera. Close Teams, Zoom and the Camera app.\r
- **\`Error: Could not open video stream\`** — start FFmpeg first. OpenCV gives up\r
  quickly if nothing is arriving when it opens the socket.\r
- **Works with \`ffplay\`, not with Python** — you are opening \`udp://@0.0.0.0:1234\`\r
  rather than a host address, and only one process can hold that port. Stop\r
  \`ffplay\` before running the script.\r
- **Different subnets** — a laptop on guest Wi-Fi cannot reach a Pi on the main\r
  network. Compare \`ipconfig\` and \`hostname -I\` and put them on the same one.\r
`,zt=`An 8x8 matrix on a MAX7219 board is about the cheapest thing you can wire to a Pi
that visibly does something. Five jumper wires, one SPI bus, and a library that
gives you a drawing surface — after which lighting a pixel is one line of Python.

This wires it up, gets SPI running on a Pi 5, lights a single LED at coordinates
you choose, and then scrolls text across it.

## What you need

- Raspberry Pi 5
- MAX7219 8x8 LED matrix module — the common \`FC-16\` or \`MAX7219 dot matrix\`
  board
- Five female-to-female jumper wires
- A breadboard, optionally

## Wiring

![The five MAX7219 pins and the Pi 5 header pins they go to](diagrams/raspberry-pi-5-max7219-led-matrix-wiring.jpg)

Power off the Pi before touching any of this.

| MAX7219 | Pi 5 pin  | GPIO           |
| ------- | --------- | -------------- |
| VCC     | Pin 2     | 5 V            |
| GND     | Pin 6     | Ground         |
| DIN     | Pin 19    | GPIO 10 (MOSI) |
| CS      | Pin 24    | GPIO 8 (CE0)   |
| CLK     | Pin 23    | GPIO 11 (SCLK) |

> **5 V, not 3.3 V.** The matrix is dim and unstable on 3.3 V. The data lines are
> fine at the Pi's 3.3 V logic levels — the MAX7219 reads them happily — so no
> level shifter is needed for a single module.
>
> One 8x8 module at full brightness draws a few hundred milliamps. Several
> chained modules want their own 5 V supply with its ground tied to the Pi's,
> rather than the Pi's 5 V rail.

## Step 1: Turn on SPI

\`\`\`bash
sudo raspi-config
\`\`\`

**Interface Options → SPI → Yes**, then reboot. Check afterwards:

\`\`\`bash
ls /dev/spi*
\`\`\`

\`\`\`text
/dev/spidev0.0  /dev/spidev0.1
\`\`\`

No devices means SPI is still off, or the overlay was disabled in
\`/boot/firmware/config.txt\` — \`dtparam=spi=on\` should be there and uncommented.

## Step 2: Install the library

Bookworm will not let pip write to the system Python, so this goes in a virtual
environment:

\`\`\`bash
mkdir -p ~/projects/matrix && cd ~/projects/matrix
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install luma.led_matrix
\`\`\`

\`luma.led_matrix\` pulls in \`luma.core\`, \`spidev\` and \`Pillow\`. You do not need
\`rm-hull/max7219\` — that is the older library this one replaced.

Your user needs access to the SPI device:

\`\`\`bash
sudo usermod -aG spi,gpio "$USER"
\`\`\`

Log out and back in. With that in place none of this needs \`sudo\`.

## Step 3: Light one LED

\`\`\`python single_led.py
from time import sleep

from luma.core.interface.serial import noop, spi
from luma.core.render import canvas
from luma.led_matrix.device import max7219

# port=0, device=0 is /dev/spidev0.0 — the CE0 pin you wired CS to.
serial = spi(port=0, device=0, gpio=noop())
device = max7219(serial, width=8, height=8, block_orientation=0, rotate=0)

# 0-255. Anything above about 32 is uncomfortable to look at indoors.
device.contrast(16)

# canvas draws into a buffer and flushes it to the matrix on exit, so the
# display never shows a half-drawn frame.
with canvas(device) as draw:
    draw.point((2, 3), fill="white")

sleep(5)
\`\`\`

\`\`\`bash
python single_led.py
\`\`\`

One LED, third column, fourth row — \`(x, y)\` counted from the top-left corner,
starting at zero. \`fill="white"\` is simply "on"; the matrix has no colour.

> Nothing lights up? Check \`block_orientation\`. Modules from different batches
> mount the chip rotated, and \`0\`, \`90\` or \`-90\` is the difference between a
> picture and nothing. Try each.

## Step 4: Draw a bit more

The \`draw\` object is a Pillow drawing surface — anything Pillow can do onto 8x8
pixels works:

\`\`\`python shapes.py
from time import sleep

from luma.core.interface.serial import noop, spi
from luma.core.render import canvas
from luma.led_matrix.device import max7219

serial = spi(port=0, device=0, gpio=noop())
device = max7219(serial, width=8, height=8, block_orientation=0)
device.contrast(16)

# A border
with canvas(device) as draw:
    draw.rectangle((0, 0, 7, 7), outline="white", fill="black")
sleep(2)

# A diagonal
with canvas(device) as draw:
    draw.line((0, 0, 7, 7), fill="white")
sleep(2)

# A dot walking the top row
for x in range(8):
    with canvas(device) as draw:
        draw.point((x, 0), fill="white")
    sleep(0.1)

device.clear()
\`\`\`

## Step 5: Scroll a message

\`\`\`python scroll_text.py
from luma.core.interface.serial import noop, spi
from luma.core.legacy import show_message
from luma.core.legacy.font import LCD_FONT, proportional
from luma.led_matrix.device import max7219

serial = spi(port=0, device=0, gpio=noop())
device = max7219(serial, width=8, height=8, block_orientation=0)
device.contrast(16)

show_message(
    device,
    "HELLO",
    fill="white",
    font=proportional(LCD_FONT),
    scroll_delay=0.06,
)
\`\`\`

\`scroll_delay\` is seconds per column — smaller is faster. \`proportional()\` trims
the blank columns either side of each glyph, which matters a great deal when the
display is eight pixels wide.

## Chaining modules

![Four MAX7219 modules chained DOUT to DIN as one 32x8 display](diagrams/raspberry-pi-5-max7219-led-matrix-chaining.jpg)

Four modules soldered in a row are one 32x8 display as far as the library is
concerned. Wire \`DOUT\` of each board to \`DIN\` of the next, share power and
ground, and tell it how many there are:

\`\`\`python
device = max7219(serial, cascaded=4, block_orientation=-90, rotate=0)
\`\`\`

\`cascaded\` is the module count; \`block_orientation\` is per-module and usually
needs \`-90\` on the common FC-16 strips.

## When it does not work

- **Nothing at all.** Recheck the wiring, then confirm \`/dev/spidev0.0\` exists.
  DIN and CLK swapped is the most common mistake.
- **All LEDs on, permanently.** No data is reaching the chip — that is its
  power-on state. Suspect CS, and confirm you are on \`device=0\` if CS is on CE0.
- **Garbled or shifted output.** \`block_orientation\` again — try \`90\` and \`-90\`.
- **Flickering.** Power. A single module is fine off the Pi; several are not.
- **\`No such file or directory: '/dev/spidev0.0'\`.** SPI is not enabled, or the
  reboot after enabling it has not happened.
- **\`PermissionError\`.** Not in the \`spi\` group yet, or not logged out since
  being added.

## Where to take it

- A clock, using \`show_message\` on a timer
- A scrolling notifier fed by a sensor reading
- Chained modules for text that fits more than five letters
- \`luma.core.sprite_system\` for frame-by-frame animation
`,Bt=`Raspberry Pi OS Bookworm refuses \`pip install\` outside a virtual environment. It
is not being difficult: the system Python is managed by \`apt\`, and pip writing
into it is how a Pi ends up with a broken desktop after a routine upgrade. The
error even tells you the answer — \`externally-managed-environment\`.

![Where pip may write on Bookworm: not the system Python, only the venv](diagrams/raspberry-pi-5-opencv-venv-why-venv.jpg)

So OpenCV goes in a virtual environment. This sets one up at a known path,
installs OpenCV and NumPy into it, proves the camera stack works, and gives you a
requirements file so the whole thing can be rebuilt from scratch.

## What you need

- Raspberry Pi 5 running **Raspberry Pi OS (64-bit)**, Bookworm or newer
- An internet connection
- A terminal — locally, over SSH, or through VNC

## Step 1: Update first

\`\`\`bash
sudo apt update && sudo apt full-upgrade -y
\`\`\`

\`update\` refreshes the package lists; \`full-upgrade\` installs the newer versions,
including kernel and firmware. Reboot afterwards if the firmware changed.

## Step 2: Create the environment

Install the \`venv\` module if it is not already there:

\`\`\`bash
sudo apt install -y python3-venv python3-pip
\`\`\`

Then make a project folder and an environment inside it:

\`\`\`bash
mkdir -p ~/projects/vision
cd ~/projects/vision
python3 -m venv venv
\`\`\`

> Add \`--system-site-packages\` if you also want the \`apt\`-installed system
> packages visible inside the environment — \`picamera2\` and \`gpiozero\` ship that
> way and are painful to install with pip:
>
> \`\`\`bash
> python3 -m venv --system-site-packages venv
> \`\`\`

## Step 3: Activate it

\`\`\`bash
source ~/projects/vision/venv/bin/activate
\`\`\`

The prompt gains a \`(venv)\` prefix. That prefix is the only reliable sign you are
inside it — check for it before every \`pip install\`.

Save yourself the typing with a shell alias:

\`\`\`bash
echo "alias vision='source ~/projects/vision/venv/bin/activate'" >> ~/.bashrc
source ~/.bashrc
\`\`\`

## Step 4: Install OpenCV

Upgrade pip first, then install:

\`\`\`bash
pip install --upgrade pip
pip install numpy opencv-python
\`\`\`

On a Pi 5 with 64-bit Bookworm this pulls a prebuilt wheel and takes a couple of
minutes. If pip starts compiling from source instead, you are on a 32-bit OS or
an older release — stop it, and install the system build instead:

\`\`\`bash
sudo apt install -y python3-opencv
\`\`\`

Building OpenCV from source on the Pi works but takes hours; do it only when you
need a module the wheels leave out.

> Want the extra algorithms — SIFT, tracking, the \`ximgproc\` filters? Install
> \`opencv-contrib-python\` **instead of** \`opencv-python\`, never both. Having both
> in one environment produces import errors that make no sense.

## Step 5: Check it works

\`\`\`python check_opencv.py
import cv2
import numpy as np

print("OpenCV:", cv2.__version__)
print("NumPy :", np.__version__)

# A gradient, written out as a file — proves the imaging path end to end
# without needing a camera or a display attached.
image = np.zeros((240, 320, 3), dtype=np.uint8)
image[:, :, 1] = np.linspace(0, 255, 320, dtype=np.uint8)
cv2.imwrite("check.png", image)

print("Wrote check.png")
\`\`\`

Run it:

\`\`\`bash
python check_opencv.py
\`\`\`

Three lines of output and a \`check.png\` next to the script means the install is
sound.

## Step 6: Leave the environment

\`\`\`bash
deactivate
\`\`\`

The prompt loses its prefix and \`python\` means the system interpreter again.
Nothing is uninstalled — activate it again whenever you come back.

## Recording what you installed

Write the exact versions down once things work:

\`\`\`bash
pip freeze > requirements.txt
\`\`\`

Then the environment can be rebuilt anywhere:

\`\`\`bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
\`\`\`

Keep \`requirements.txt\` in the project folder and out of the environment folder —
\`venv/\` is disposable and should not be copied around or committed.

![requirements.txt lives in the project folder, not inside venv](diagrams/raspberry-pi-5-opencv-venv-project-layout.jpg)

For a vision project that also does machine learning, this is the set worth
starting from:

\`\`\`bash
pip install numpy opencv-python matplotlib pillow tqdm
pip install mediapipe          # face, hand and pose landmarks
pip install scikit-learn       # classical models, no GPU needed
\`\`\`

> TensorFlow on a Pi 5 is possible and slow. If inference is the goal rather than
> training, reach for **TensorFlow Lite** or MediaPipe's bundled models instead —
> both run several times faster on this hardware.

## When it does not work

- **\`error: externally-managed-environment\`** — you are outside the virtual
  environment. Activate it. Do not pass \`--break-system-packages\`; the name is a
  fair description of what it does.
- **\`ImportError: libGL.so.1: cannot open shared object file\`** — the headless
  OS image lacks the GL libraries OpenCV's GUI functions want:

  \`\`\`bash
  sudo apt install -y libgl1 libglib2.0-0
  \`\`\`

  Or use \`opencv-python-headless\` if you never call \`cv2.imshow\`.
- **\`cv2.imshow\` fails over SSH** — there is no display to draw on. Connect with
  \`ssh -X\`, work through VNC, or write frames to disk with \`cv2.imwrite\`.
- **\`pip\` is killed part-way through** — it ran out of memory. Add swap
  temporarily, or install one package at a time.
- **The camera is not found** — the modern camera stack is \`picamera2\`, not
  OpenCV's \`VideoCapture\`. Install it with \`apt\`, and create the environment with
  \`--system-site-packages\` so it is importable.

## Further reading

- [OpenCV documentation](https://docs.opencv.org/)
- [Python virtual environments](https://docs.python.org/3/tutorial/venv.html)
`,Vt=`The hosts file is the last word on name resolution: whatever it says wins, before
DNS is ever consulted. That makes it useful for pointing a domain at a test
server and equally useful for blocking things — which is why installers, ad
blockers and licence-patching scripts all like to write to it, and why a site
that "works everywhere except this PC" usually traces back here.

![The hosts file is consulted before the DNS cache and before any DNS server](diagrams/reset-windows-hosts-file-resolution-order.jpg)

Resetting it puts the file back to the contents Windows ships with, which is to
say a header comment and nothing else.

> Everything in the file is lost when you reset it. If you put entries there on
> purpose — a staging domain, a local service, a block list — copy them out
> first. Step 1 does exactly that.

## Before you start

- **Administrator rights.** The file lives under \`System32\` and is not writable
  by a standard user.
- **Windows PowerShell**, which is already installed. Open it with \`Win + X\` and
  **Terminal (Admin)** or **Windows PowerShell (Admin)**, and accept the UAC
  prompt.
- **Know what a reset costs you.** If a program you rely on wrote entries here,
  it will need to write them again.

The file itself is always at:

\`\`\`text
C:\\Windows\\System32\\drivers\\etc\\hosts
\`\`\`

## Step 1: Read it, then back it up

Look at what is actually in there before you throw it away:

\`\`\`powershell
Get-Content "$env:SystemRoot\\System32\\drivers\\etc\\hosts"
\`\`\`

Then keep a timestamped copy next to the original:

\`\`\`powershell
$hosts = "$env:SystemRoot\\System32\\drivers\\etc\\hosts"
Copy-Item $hosts "$hosts.backup-$(Get-Date -Format 'yyyyMMdd-HHmmss')"
\`\`\`

The backup sits in the same folder, so restoring is a rename away.

> Anything mapping a licensing or activation domain to \`127.0.0.1\` or \`0.0.0.0\`
> was put there to block that domain. Removing it is the point of the reset —
> just know that whatever depended on the block will start reaching the internet
> again.

## Step 2: Write the default file back

This is the file exactly as Windows ships it — every line a comment, no active
mappings:

\`\`\`powershell reset-hosts.ps1
$hosts = "$env:SystemRoot\\System32\\drivers\\etc\\hosts"

Set-Content -Path $hosts -Encoding ASCII -Value @"
# Copyright (c) 1993-2009 Microsoft Corp.
#
# This is a sample HOSTS file used by Microsoft TCP/IP for Windows.
#
# This file contains the mappings of IP addresses to host names. Each
# entry should be kept on an individual line. The IP address should
# be placed in the first column followed by the corresponding host name.
# The IP address and the host name should be separated by at least one
# space.
#
# Additionally, comments (such as these) may be inserted on individual
# lines or following the machine name denoted by a '#' symbol.
#
# For example:
#
#      102.54.94.97     rhino.acme.com          # source server
#       38.25.63.10     x.acme.com              # x client host

# localhost name resolution is handled within DNS itself.
#	127.0.0.1       localhost
#	::1             localhost
"@
\`\`\`

\`-Encoding ASCII\` matters. Windows reads the hosts file happily as ASCII or
UTF-8 without a BOM, and PowerShell 5.1 will otherwise write UTF-16 — which the
resolver treats as garbage and ignores wholesale.

> \`127.0.0.1 localhost\` is commented out on purpose. Modern Windows resolves
> \`localhost\` internally and does not need the line; uncommenting it is harmless
> but pointless.

## Step 3: Flush the cache

Entries already resolved stay in the cache until it is cleared, so the reset can
look like it did nothing:

\`\`\`powershell
ipconfig /flushdns
\`\`\`

No restart is needed — the hosts file is read on demand.

## Step 4: Check that it took

Print the file back:

\`\`\`powershell
Get-Content "$env:SystemRoot\\System32\\drivers\\etc\\hosts"
\`\`\`

Then confirm a domain that was previously redirected now resolves to a real
address:

\`\`\`powershell
Resolve-DnsName example.com | Select-Object Name, IPAddress
\`\`\`

If the address still points at \`127.0.0.1\` or \`0.0.0.0\`, the cache did not clear
or something rewrote the file — see below.

## When it does not work

- **"Access to the path is denied."** PowerShell is not elevated. Close it and
  reopen with **Run as administrator**.
- **The file reverts on its own.** Something is putting the entries back —
  usually security software or an activation tool with a scheduled task. Check
  \`Task Scheduler\` and your antivirus quarantine.
- **The file is read-only.** Clear the attribute and try again:

  \`\`\`powershell
  Set-ItemProperty "$env:SystemRoot\\System32\\drivers\\etc\\hosts" -Name IsReadOnly -Value $false
  \`\`\`

- **Windows Defender blocks the write.** Controlled folder access counts
  \`System32\` as protected. Allow the app under **Windows Security → Virus &
  threat protection → Ransomware protection**, or turn the feature off long
  enough to make the change.
- **You want the old entries back.** Rename the backup from Step 1 over the top
  of \`hosts\`, or copy the lines you actually wanted out of it.

## Further reading

- [Microsoft on the hosts file](https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/hosts)
- [PowerShell scripting overview](https://docs.microsoft.com/en-us/powershell/scripting/overview?view=powershell-7.1)
`,Ht=`Per-project virtual environments are the right default. They are also tedious
when the "project" is a scratch file you will delete in an hour — and on Windows,
\`pip install\` into the system Python is how a machine ends up with three Pythons
and a \`PATH\` nobody can explain.

The middle ground is one environment at a fixed path, \`C:\\global_env\`, driven by
four small batch files: install Python, create the environment, open a shell
inside it, list what is in it. VS Code points at it once and forgets about it.

![One global environment for scratch work against a per-project environment](diagrams/vscode-global-python-venv-global-vs-project.jpg)

> Every script below is printed in full. Use **Save** on a block to write the
> \`.bat\` straight out of this page, or copy it into Notepad yourself — nothing is
> hosted anywhere, and you should read a script before you run it either way.

## What you end up with

\`\`\`text
C:\\Python\\Python-3.10.10\\   the interpreter
C:\\global_env\\              the virtual environment
  Scripts\\activate.bat      what the shells call
  Scripts\\python.exe        what VS Code points at
  Lib\\site-packages\\        everything you pip install
\`\`\`

Keep the four \`.bat\` files together in a folder you can find again — \`C:\\pyenv\`
or a **Tools** folder on the desktop. They are pinnable to the taskbar, which is
most of the convenience.

## Step 1: Install Python

If Python is already installed and \`python --version\` prints something you are
happy with, skip to Step 2 and point \`PYTHON_PATH\` at it instead.

This downloads a known version and installs it silently at a known path — no
wizard, no per-user surprise location:

\`\`\`batch 1_install_python.bat
@echo off
REM Downloads and installs a specific Python at a fixed path.
set "PYTHON_VERSION=3.10.10"
set "PYTHON_URL=https://www.python.org/ftp/python/%PYTHON_VERSION%/python-%PYTHON_VERSION%-amd64.exe"
set "INSTALL_PATH=C:\\Python\\Python-%PYTHON_VERSION%"

echo Downloading Python %PYTHON_VERSION%...
curl -o python_installer.exe %PYTHON_URL%
if not exist python_installer.exe (
    echo Download failed. Check your connection and try again.
    pause
    exit /b 1
)

echo Installing Python %PYTHON_VERSION% to %INSTALL_PATH%...
python_installer.exe /quiet InstallAllUsers=1 PrependPath=1 TargetDir="%INSTALL_PATH%"

del python_installer.exe
echo Done. Python %PYTHON_VERSION% is at %INSTALL_PATH%.
pause
\`\`\`

Run it **as administrator** — \`InstallAllUsers=1\` writes outside your profile.

What the installer flags mean:

- **\`/quiet\`** — no wizard.
- **\`InstallAllUsers=1\`** — machine-wide rather than per-user.
- **\`PrependPath=1\`** — puts Python on \`PATH\`, so \`python\` works in a new shell.
- **\`TargetDir\`** — the fixed path the next script depends on.

> Pinning the version is deliberate. \`3.10.10\` is old enough that every library
> in the list at the bottom has a wheel for it — TensorFlow and MediaPipe in
> particular lag behind the newest Python by a year or more. Change the version
> only after checking what you actually install still ships for it.

## Step 2: Create the environment

\`\`\`batch 2_create_venv.bat
@echo off
set "PYTHON_PATH=C:\\Python\\Python-3.10.10\\python.exe"
set "VENV_PATH=C:\\global_env"

if not exist "%PYTHON_PATH%" (
    echo Python not found at %PYTHON_PATH%. Run 1_install_python.bat first.
    pause
    exit /b 1
)

if not exist "%VENV_PATH%" (
    echo Creating directory at %VENV_PATH%...
    mkdir "%VENV_PATH%"
) else (
    echo Directory already exists: %VENV_PATH%
)

echo Creating virtual environment...
"%PYTHON_PATH%" -m venv "%VENV_PATH%"

echo Virtual environment created in %VENV_PATH%.
pause
\`\`\`

It calls the interpreter by full path rather than whatever \`python\` happens to
mean in that shell, which is the difference between this working every time and
working on the machine you wrote it on.

> Creating a folder directly under \`C:\\\` needs administrator rights on most
> setups. If it fails, either run the file as administrator or move \`VENV_PATH\`
> somewhere inside your profile, such as \`%USERPROFILE%\\global_env\` — update the
> other scripts to match.

## Step 3: Open a shell inside it

\`\`\`batch 3_activate_venv.bat
@echo off
set "VENV_PATH=C:\\global_env"
set "VENV_ACTIVATE=%VENV_PATH%\\Scripts\\activate.bat"

if exist "%VENV_ACTIVATE%" (
    echo Activating the virtual environment in a new terminal...
    start cmd /k "%VENV_ACTIVATE%"
) else (
    echo Virtual environment not found. Run 2_create_venv.bat first.
    pause
)
\`\`\`

\`start cmd /k\` opens a new Command Prompt and leaves it open — activating in the
window that runs the batch file would be pointless, because the shell dies with
the script. The new prompt is prefixed \`(global_env)\`.

## Step 4: See what is installed

\`\`\`batch 4_list_packages.bat
@echo off
set "VENV_PATH=C:\\global_env"
set "VENV_ACTIVATE=%VENV_PATH%\\Scripts\\activate.bat"

if exist "%VENV_ACTIVATE%" (
    echo Listing installed packages...
    start cmd /k "call %VENV_ACTIVATE% && pip list"
) else (
    echo Virtual environment not found. Run 2_create_venv.bat first.
    pause
)
\`\`\`

## Step 5: Point VS Code at it

![The interpreter, the environment it creates, and the path VS Code is given](diagrams/vscode-global-python-venv-interpreter-path.jpg)

1. Install the **Python** extension — \`Ctrl + Shift + X\`, search \`Python\`, the
   Microsoft one.
2. \`Ctrl + Shift + P\` → **Python: Select Interpreter** → **Enter interpreter
   path**.
3. Give it \`C:\\global_env\\Scripts\\python.exe\`.

The interpreter shows in the status bar, new terminals open already activated,
and **Run Python File** uses it without further ceremony.

To make it the default for every folder you open, put this in your user
\`settings.json\` — \`Ctrl + Shift + P\` → **Preferences: Open User Settings (JSON)**:

\`\`\`json settings.json
{
  "python.defaultInterpreterPath": "C:\\\\global_env\\\\Scripts\\\\python.exe",
  "python.terminal.activateEnvironment": true
}
\`\`\`

## Step 6: Install what you need

From an activated prompt — Step 3 gives you one:

\`\`\`batch
python.exe -m pip install --upgrade pip
pip install numpy pandas matplotlib
\`\`\`

The set this environment was originally built for, if you want the whole thing
in one go:

\`\`\`batch install_libs.bat
@echo off
call C:\\global_env\\Scripts\\activate.bat

python.exe -m pip install --upgrade pip
pip install flask pymongo flask_pymongo python-dotenv ^
    pandas numpy tqdm nltk ^
    matplotlib seaborn scikit-learn ^
    opencv-python mediapipe Pillow ^
    tensorflow keras

pip list
pause
\`\`\`

That is a large download — TensorFlow alone is several hundred megabytes. Install
what you need and come back for the rest.

Write down what ended up in there, so the environment can be rebuilt:

\`\`\`batch
pip freeze > C:\\global_env\\requirements.txt
\`\`\`

## When it goes wrong

- **\`'python' is not recognized\`** — the \`PATH\` change from the installer only
  applies to shells opened afterwards. Open a new terminal. If it still fails,
  call the interpreter by full path.
- **Activation does nothing** — check \`C:\\global_env\\Scripts\\activate.bat\`
  exists. If it does not, Step 2 did not complete; delete the folder and run it
  again.
- **VS Code ignores the interpreter** — reload the window (\`Ctrl + Shift + P\` →
  **Developer: Reload Window**). The Python extension caches its list.
- **The file saved as \`create_env.bat.txt\`** — Explorer is hiding known
  extensions. Turn on **View → Show → File name extensions** and rename it.
- **PowerShell refuses \`activate.ps1\`** — that is the execution policy, and it is
  why these scripts use \`cmd\`. Either keep using Command Prompt or allow local
  scripts for your user:

  \`\`\`powershell
  Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
  \`\`\`

- **One project needs a different version of something** — that is the moment to
  give that project its own environment. The global one is for scratch work, not
  for pinning a dependency tree.

## Further reading

- [Python virtual environments](https://docs.python.org/3/tutorial/venv.html)
- [Python in VS Code](https://code.visualstudio.com/docs/python/python-tutorial)
`,Ut=`LTSC is the version of Windows 11 built for machines that are supposed to do one
job for years: no Microsoft Store, no Copilot, no Teams, no widgets, no
preinstalled games, and feature updates that arrive every few years instead of
twice a year. On an older laptop that is not a compromise, it is the point — the
same hardware boots faster and idles with noticeably less running behind it.

This walks the whole thing: getting an ISO, writing it with Rufus, installing
clean, and the handful of things worth doing before you call the machine done.

![The route from an ISO download to a machine booted from the USB drive](diagrams/windows-11-ltsc-setup-iso-to-machine.jpg)

> LTSC is a volume-licensing edition. Microsoft's Evaluation Center offers a
> 90-day evaluation ISO of **Windows 11 Enterprise LTSC** that anyone can
> download, which is fine for trying it on a spare machine; running it long term
> needs a licence that covers it.

## What the hardware needs

| Component | Minimum                                                     |
| --------- | ----------------------------------------------------------- |
| CPU       | 1 GHz, 2+ cores, 64-bit                                     |
| RAM       | 4 GB                                                        |
| Storage   | 64 GB                                                       |
| Firmware  | UEFI, Secure Boot capable                                   |
| TPM       | 2.0                                                         |
| Graphics  | DirectX 12 / WDDM 2.x                                       |
| Display   | 9" or larger, 720p                                          |

You also need a USB drive of **8 GB or more** — it gets wiped — and a second
working machine to write it from.

> Older hardware often fails on TPM 2.0 or Secure Boot alone. Rufus can write an
> installer that skips those checks (see the Rufus step); Microsoft does not
> support the result, and such machines may not receive updates. Decide before
> you wipe the disk, not after.

## Step 1: Get the ISO

1. Go to the **Microsoft Evaluation Center** and find **Windows 11 Enterprise
   LTSC**, or download the ISO from your volume-licensing portal if you have one.
2. Pick your language and architecture (**x64**).
3. Save the ISO somewhere with room to spare — it is 4–5 GB.

Check the download before you trust it. Compare the hash against the one the
download page lists:

\`\`\`powershell
Get-FileHash .\\Windows11_LTSC.iso -Algorithm SHA256
\`\`\`

> Download the ISO from Microsoft. The "free Windows ISO" mirrors that turn up
> first in search results are the single most common way a machine gets
> compromised before it has even finished installing.

## Step 2: Install Rufus

Rufus writes the ISO to the USB drive and handles the partitioning for you.

1. Download it from [rufus.ie](https://rufus.ie) — take the portable build if
   you would rather not install anything.
2. Run it. It asks for administrator rights, because writing a raw disk needs
   them.

## Step 3: Write the USB drive

1. Plug in the USB drive. **Everything on it will be erased.**
2. In Rufus, pick it under **Device** — check the size and letter twice; Rufus
   will happily overwrite an external hard disk if you point it at one.
3. Click **SELECT** and choose the ISO.
4. Set the options:
   - **Image option**: \`Standard Windows installation\`
   - **Partition scheme**: \`GPT\` for UEFI, which is nearly everything from about
     2013 onwards; \`MBR\` only for genuinely old BIOS machines
   - **Target system**: fills itself in from the partition scheme
   - **File system**: \`NTFS\`
   - **Volume label**: anything — \`WIN11LTSC\` does the job
5. Click **START**. Rufus may offer to **remove the requirement for 4 GB+ RAM,
   Secure Boot and TPM 2.0**, and to skip the Microsoft-account requirement. Tick
   those only if your machine actually needs them.
6. Wait for **READY**, then eject the drive properly rather than pulling it out.

## Step 4: Boot the target PC from it

1. Plug the drive into the machine you are installing on.
2. Restart and hold the boot-menu key. It differs by make:

   | Make               | Boot menu   | Firmware setup |
   | ------------------ | ----------- | -------------- |
   | Dell               | \`F12\`       | \`F2\`           |
   | HP                 | \`F9\`        | \`F10\`          |
   | Lenovo             | \`F12\`       | \`F1\` / \`F2\`    |
   | Acer               | \`F12\`       | \`F2\`           |
   | ASUS               | \`Esc\`/\`F8\`  | \`F2\` / \`Del\`   |
   | Most desktop boards| \`F11\`/\`F8\`  | \`Del\`          |

3. Choose the USB entry. Prefer the one prefixed **UEFI:** if both appear.

> No USB entry in the list? Turn **Fast Boot** off in firmware setup, and if the
> drive was written as GPT/UEFI make sure **CSM / Legacy boot** is off too. A
> mismatch between how the stick was written and how the firmware boots is the
> usual reason it does not show up.

## Step 5: Install

1. Confirm language, time and keyboard, then **Next → Install now**.
2. At the product key prompt, choose **I don't have a product key** if you plan
   to activate later.
3. Choose **Custom: Install Windows only (advanced)**. Upgrade is not what you
   want on a machine you are rebuilding.
4. Partition the disk. For a clean install, delete every partition on the target
   disk until it is a single block of unallocated space, select it and click
   **Next** — Windows creates the EFI, MSR and recovery partitions itself.

   > This erases the disk. If anything on it matters, stop here and copy it off
   > first. Double-check you are looking at the right disk if the machine has
   > more than one.

5. Let it run. It restarts a few times; pull the USB drive out after the first
   restart so it does not boot the installer again.
6. Work through the out-of-box setup — region, keyboard, network, account name.

## Step 6: Activate

**Settings → System → Activation.** Enter the key you hold, or connect the
machine to the network that handles activation for you. Evaluation installs run
for 90 days without one.

## Step 7: Drivers and the first hour

1. **Windows Update** first: **Settings → Windows Update**, then
   **Advanced options → Optional updates**, which is where most driver updates
   hide.
2. **The manufacturer's own drivers** next, from their support page for your
   exact model — chipset, graphics, Wi-Fi, audio. LTSC ships a thin driver set on
   purpose.
3. Check nothing is left unrecognised:

   \`\`\`powershell
   Get-PnpDevice -Status Error, Unknown | Select-Object FriendlyName, Class
   \`\`\`

4. Install what you actually use — a browser, an archiver, your editor. There is
   no Store here to fall back on, so \`winget\` is the fastest route if you want
   one:

   \`\`\`powershell
   winget install --id Mozilla.Firefox -e
   \`\`\`

5. Confirm the machine is protected: **Windows Security → Virus & threat
   protection** should report Defender as active.

## Worth knowing afterwards

![What Windows 11 LTSC leaves out compared with the standard edition](diagrams/windows-11-ltsc-setup-ltsc-vs-standard.jpg)

- **No Microsoft Store.** Some apps distribute only there. Check that the ones
  you depend on have a standalone installer before you commit a machine to LTSC.
- **Feature updates are rare by design.** Security updates still arrive monthly;
  the interface simply stops changing under you.
- **It is genuinely lighter.** On 4 GB machines the difference in idle memory
  against a standard Windows 11 install is the whole reason to do this.
`,Wt=`awesome-windows`,Gt=e=>`https://github.com/${Dt}/${Wt}/tree/main/${encodeURIComponent(e)}`,Kt=[{slug:`windows-11-ltsc-setup`,title:`Install Windows 11 LTSC on a low-spec PC`,tagline:`The lean Windows, from ISO to first boot.`,summary:`LTSC drops the Store, Copilot and the preinstalled clutter, which is exactly what an old machine wants. Download the ISO, write it with Rufus, install, and clean up afterwards.`,category:`Windows`,tags:[`Windows`,`Install`,`LTSC`],updated:`2026-07-26`,accent:`#60a5fa`,folder:`Windows - Windows 11 LTSC Setup`,markdown:Ut},{slug:`office-365-ltsc-setup`,title:`Install and activate Office 365 LTSC`,tagline:`One XML file, one command.`,summary:`Office LTSC installs through the Office Deployment Tool rather than a wizard. Write the configuration file, run setup from an elevated prompt, then activate — with the licence you hold or against your KMS host.`,category:`Windows`,tags:[`Windows`,`Office`,`LTSC`],updated:`2026-07-26`,accent:`#f97316`,folder:`Windows - Office 365 LTSC Setup`,markdown:Ft},{slug:`microsoft-store-slow-downloads`,title:`Fix slow or stuck Microsoft Store downloads`,tagline:`When the progress bar refuses to move.`,summary:`Four methods, cheapest first — free the bandwidth, reset the Store cache, clear the delivery-optimisation files, then re-register the app if it is still sulking.`,category:`Windows`,tags:[`Windows`,`Troubleshooting`,`Store`],updated:`2026-07-26`,accent:`#34d399`,folder:`Windows - Microsoft Store Slow Downloading Fix`,markdown:Pt},{slug:`reset-windows-hosts-file`,title:`Reset the Windows hosts file`,tagline:`Put DNS back the way Windows shipped it.`,summary:`A blocked site or a stale override usually traces back to the hosts file. Back up what is there, write the default contents back with PowerShell, and flush the DNS cache.`,category:`Windows`,tags:[`Windows`,`Networking`,`PowerShell`],updated:`2026-07-26`,accent:`#a78bfa`,folder:`Windows - Reset Windows Host`,markdown:Vt},{slug:`vscode-global-python-venv`,title:`A global Python virtual environment for VS Code`,tagline:`One environment, four batch files.`,summary:`Keeps a single virtual environment at C:\\global_env and drives it with batch files you save straight off the page — install Python, create it, open a shell inside it, list what is in it — then points VS Code at the interpreter.`,category:`Developer setup`,tags:[`Windows`,`Python`,`VS Code`],updated:`2026-07-26`,accent:`#38bdf8`,folder:`VS Code (Windows) - Setting Up a Global Python Virtual Environment`,markdown:Ht},{slug:`raspberry-pi-5-headless-setup`,title:`Set up a Raspberry Pi 5 headless, from Windows`,tagline:`No monitor, no keyboard — just WiFi.`,summary:`Flash Raspberry Pi OS with Imager and preconfigure SSH and WiFi, find the board with Angry IP Scanner, connect over PuTTY and VNC, and keep a phone hotspot as the fallback route in.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`SSH`,`Setup`],updated:`2026-07-26`,accent:`#f472b6`,folder:`Raspberry Pi 5 - Setup Your Pi on Windows`,markdown:Lt},{slug:`raspberry-pi-5-opencv-venv`,title:`OpenCV in a virtual environment on the Pi 5`,tagline:`Install it once, keep it contained.`,summary:`Bookworm blocks pip at system level, so OpenCV goes into a virtual environment. Create it, install, run a script against it and deactivate when you are done.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`Python`,`OpenCV`],updated:`2026-07-26`,accent:`#22d3ee`,folder:`Raspberry Pi 5 - Initializing Virtual Environment and OpenCV`,markdown:Bt},{slug:`raspberry-pi-5-external-webcam`,title:`Use a USB webcam with the Raspberry Pi 5`,tagline:`Plug it in, prove it works.`,summary:`Confirm the Pi sees a Logitech C270, check what resolutions it actually supports with v4l-utils, grab a still with fswebcam and then read frames from OpenCV.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`Camera`,`OpenCV`],updated:`2026-07-26`,accent:`#facc15`,folder:`Raspberry Pi 5 - External Webcam Setup`,markdown:It},{slug:`raspberry-pi-5-laptop-webcam-stream`,title:`Stream a laptop webcam to the Raspberry Pi 5`,tagline:`Borrow the camera you already own.`,summary:`FFmpeg pushes the laptop camera over UDP, the Pi displays the feed with OpenCV, and a small Python script lets you snap stills and record clips from the stream.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`Camera`,`FFmpeg`],updated:`2026-07-26`,accent:`#fb7185`,folder:`Raspberry Pi 5 - Laptop Webcam Setup Wirelessly`,markdown:Rt},{slug:`raspberry-pi-5-max7219-led-matrix`,title:`Drive a MAX7219 8x8 LED matrix from the Pi 5`,tagline:`Eight by eight, one pixel at a time.`,summary:`Wiring table for the five pins, the libraries to install, and a first Python script that lights a single LED at the coordinates you ask for.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`Electronics`,`Python`],updated:`2026-07-26`,accent:`#c084fc`,folder:`Raspberry Pi 5 - Integration of MAX7219 (8x8) LED Matrix Display`,markdown:zt}].map(w(`articles`)).map(e=>({...e,sourceUrl:Gt(e.folder)})),qt=[...new Set(Kt.map(e=>e.category))].sort(),Jt=e=>Kt.find(t=>t.slug===e)??null,Yt=e=>new Date(e).toLocaleDateString(void 0,{year:`numeric`,month:`short`,day:`numeric`});function Xt({article:e,onOpen:t}){let n=nt();return(0,P.jsxs)(`article`,{className:`card article-card`,style:{"--accent":e.accent},onClick:()=>t(e.route),...n,children:[(0,P.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,P.jsx)(`header`,{className:`article-card-head`,children:(0,P.jsx)(`span`,{className:`badge badge-article`,children:e.category})}),(0,P.jsx)(`h3`,{children:(0,P.jsx)(`button`,{type:`button`,className:`card-link`,onClick:n=>{n.stopPropagation(),t(e.route)},children:e.title})}),(0,P.jsx)(`p`,{className:`tagline`,children:e.tagline}),(0,P.jsx)(`p`,{className:`card-summary`,children:e.summary}),(0,P.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,P.jsx)(`li`,{children:e},e))}),(0,P.jsxs)(`footer`,{className:`card-foot`,children:[(0,P.jsxs)(`span`,{className:`dim`,children:[`Updated `,Yt(e.updated)]}),(0,P.jsxs)(`span`,{className:`article-more`,children:[`Read `,(0,P.jsx)(F,{name:`arrow`,size:15})]})]})]})}var Zt=e=>[e.title,e.tagline,e.summary,e.category,...e.tags,e.content].join(` `).toLowerCase();function Qt({query:e,onQuery:t,category:n,onCategory:r,onOpen:i}){let a=(0,_.useMemo)(()=>{let t=e.trim().toLowerCase().split(/\s+/).filter(Boolean);return Kt.filter(e=>{if(n&&e.category!==n)return!1;if(!t.length)return!0;let r=Zt(e);return t.every(e=>r.includes(e))}).sort((e,t)=>t.updated.localeCompare(e.updated))},[e,n]);return(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(St,{name:`design`,width:380,height:380,children:[(0,P.jsxs)(`p`,{className:`eyebrow`,children:[(0,P.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`Written down once, in full`]}),(0,P.jsxs)(`h1`,{children:[`Things worth`,(0,P.jsx)(`br`,{}),(0,P.jsx)(`span`,{className:`grad`,children:`not looking up twice.`})]}),(0,P.jsx)(`p`,{className:`lede`,children:`Windows fixes and Raspberry Pi setups I had to work out the hard way. Every step is here on the page — nothing to download, nothing behind a drive link. Where a guide needs a script, the script is printed in full so you can read it, save it yourself and know what it does before you run it.`})]}),(0,P.jsxs)(`div`,{className:`toolbar`,children:[(0,P.jsx)(`div`,{className:`toolbar-row`,children:(0,P.jsxs)(`div`,{className:`search`,children:[(0,P.jsx)(F,{name:`search`,size:17,className:`search-icon`}),(0,P.jsx)(`input`,{type:`search`,value:e,placeholder:`Search guides, commands, steps…`,onChange:e=>t(e.target.value),"aria-label":`Search guides`})]})}),(0,P.jsxs)(`div`,{className:`chips`,children:[(0,P.jsx)(`button`,{type:`button`,className:`chip ${n?``:`is-active`}`,onClick:()=>r(null),children:`All`}),qt.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`chip ${n===e?`is-active`:``}`,onClick:()=>r(n===e?null:e),"aria-pressed":n===e,children:e},e)),(0,P.jsxs)(`span`,{className:`chips-count`,children:[a.length,` `,a.length===1?`guide`:`guides`]})]})]}),a.length>0?(0,P.jsx)(`div`,{className:`catalog catalog-grid`,children:a.map((e,t)=>(0,P.jsx)(Ct,{className:`reveal-cell`,delay:Ge(t),children:(0,P.jsx)(Xt,{article:e,onOpen:i})},e.slug))}):(0,P.jsxs)(`div`,{className:`empty empty-in`,children:[(0,P.jsx)(F,{name:`search`,size:28}),(0,P.jsx)(`h3`,{children:`No guide matches that`}),(0,P.jsx)(`p`,{children:`Try a different word, or clear the filter.`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>{t(``),r(null)},children:`Clear filters`})]})]})}function $t({raised:e=!1}){let[t,n]=(0,_.useState)(!1),r=qe();return(0,_.useEffect)(()=>{let e=0,t=()=>{e=0,n(window.scrollY>640)},r=()=>{e||=requestAnimationFrame(t)};return t(),window.addEventListener(`scroll`,r,{passive:!0}),()=>{cancelAnimationFrame(e),window.removeEventListener(`scroll`,r)}},[]),(0,P.jsx)(`button`,{type:`button`,className:`back-to-top ${t?`is-shown`:``} ${e?`is-raised`:``}`,onClick:()=>window.scrollTo({top:0,behavior:r?`auto`:`smooth`}),"aria-label":`Back to top`,tabIndex:t?0:-1,"aria-hidden":!t||void 0,children:(0,P.jsx)(F,{name:`arrow`,size:17})})}var en=c(o(((e,t)=>{function n(e){return e instanceof Map?e.clear=e.delete=e.set=function(){throw Error(`map is read-only`)}:e instanceof Set&&(e.add=e.clear=e.delete=function(){throw Error(`set is read-only`)}),Object.freeze(e),Object.getOwnPropertyNames(e).forEach(t=>{let r=e[t],i=typeof r;(i===`object`||i===`function`)&&!Object.isFrozen(r)&&n(r)}),e}var r=class{constructor(e){e.data===void 0&&(e.data={}),this.data=e.data,this.isMatchIgnored=!1}ignoreMatch(){this.isMatchIgnored=!0}};function i(e){return e.replace(/&/g,`&amp;`).replace(/</g,`&lt;`).replace(/>/g,`&gt;`).replace(/"/g,`&quot;`).replace(/'/g,`&#x27;`)}function a(e,...t){let n=Object.create(null);for(let t in e)n[t]=e[t];return t.forEach(function(e){for(let t in e)n[t]=e[t]}),n}var o=`</span>`,s=e=>!!e.scope,c=(e,{prefix:t})=>{if(e.startsWith(`language:`))return e.replace(`language:`,`language-`);if(e.includes(`.`)){let n=e.split(`.`);return[`${t}${n.shift()}`,...n.map((e,t)=>`${e}${`_`.repeat(t+1)}`)].join(` `)}return`${t}${e}`},l=class{constructor(e,t){this.buffer=``,this.classPrefix=t.classPrefix,e.walk(this)}addText(e){this.buffer+=i(e)}openNode(e){if(!s(e))return;let t=c(e.scope,{prefix:this.classPrefix});this.span(t)}closeNode(e){s(e)&&(this.buffer+=o)}value(){return this.buffer}span(e){this.buffer+=`<span class="${e}">`}},u=(e={})=>{let t={children:[]};return Object.assign(t,e),t},d=class e{constructor(){this.rootNode=u(),this.stack=[this.rootNode]}get top(){return this.stack[this.stack.length-1]}get root(){return this.rootNode}add(e){this.top.children.push(e)}openNode(e){let t=u({scope:e});this.add(t),this.stack.push(t)}closeNode(){if(this.stack.length>1)return this.stack.pop()}closeAllNodes(){for(;this.closeNode(););}toJSON(){return JSON.stringify(this.rootNode,null,4)}walk(e){return this.constructor._walk(e,this.rootNode)}static _walk(e,t){return typeof t==`string`?e.addText(t):t.children&&(e.openNode(t),t.children.forEach(t=>this._walk(e,t)),e.closeNode(t)),e}static _collapse(t){typeof t!=`string`&&t.children&&(t.children.every(e=>typeof e==`string`)?t.children=[t.children.join(``)]:t.children.forEach(t=>{e._collapse(t)}))}},f=class extends d{constructor(e){super(),this.options=e}addText(e){e!==``&&this.add(e)}startScope(e){this.openNode(e)}endScope(){this.closeNode()}__addSublanguage(e,t){let n=e.root;t&&(n.scope=`language:${t}`),this.add(n)}toHTML(){return new l(this,this.options).value()}finalize(){return this.closeAllNodes(),!0}};function p(e){return e?typeof e==`string`?e:e.source:null}function m(e){return _(`(?=`,e,`)`)}function h(e){return _(`(?:`,e,`)*`)}function g(e){return _(`(?:`,e,`)?`)}function _(...e){return e.map(e=>p(e)).join(``)}function v(e){let t=e[e.length-1];return typeof t==`object`&&t.constructor===Object?(e.splice(e.length-1,1),t):{}}function y(...e){return`(`+(v(e).capture?``:`?:`)+e.map(e=>p(e)).join(`|`)+`)`}function b(e){return RegExp(e.toString()+`|`).exec(``).length-1}function x(e,t){let n=e&&e.exec(t);return n&&n.index===0}var S=/\[(?:[^\\\]]|\\.)*\]|\(\??|\\([1-9][0-9]*)|\\./;function C(e,{joinWith:t}){let n=0;return e.map(e=>{n+=1;let t=n,r=p(e),i=``;for(;r.length>0;){let e=S.exec(r);if(!e){i+=r;break}i+=r.substring(0,e.index),r=r.substring(e.index+e[0].length),e[0][0]===`\\`&&e[1]?i+=`\\`+String(Number(e[1])+t):(i+=e[0],e[0]===`(`&&n++)}return i}).map(e=>`(${e})`).join(t)}var w=/\b\B/,T=`[a-zA-Z]\\w*`,ee=`[a-zA-Z_]\\w*`,E=`\\b\\d+(\\.\\d+)?`,D=`(-?)(\\b0[xX][a-fA-F0-9]+|(\\b\\d+(\\.\\d*)?|\\.\\d+)([eE][-+]?\\d+)?)`,te=`\\b(0b[01]+)`,O=`!|!=|!==|%|%=|&|&&|&=|\\*|\\*=|\\+|\\+=|,|-|-=|/=|/|:|;|<<|<<=|<=|<|===|==|=|>>>=|>>=|>=|>>>|>>|>|\\?|\\[|\\{|\\(|\\^|\\^=|\\||\\|=|\\|\\||~`,ne=(e={})=>{let t=/^#![ ]*\//;return e.binary&&(e.begin=_(t,/.*\b/,e.binary,/\b.*/)),a({scope:`meta`,begin:t,end:/$/,relevance:0,"on:begin":(e,t)=>{e.index!==0&&t.ignoreMatch()}},e)},k={begin:`\\\\[\\s\\S]`,relevance:0},re={scope:`string`,begin:`'`,end:`'`,illegal:`\\n`,contains:[k]},A={scope:`string`,begin:`"`,end:`"`,illegal:`\\n`,contains:[k]},ie={begin:/\b(a|an|the|are|I'm|isn't|don't|doesn't|won't|but|just|should|pretty|simply|enough|gonna|going|wtf|so|such|will|you|your|they|like|more)\b/},j=function(e,t,n={}){let r=a({scope:`comment`,begin:e,end:t,contains:[]},n);r.contains.push({scope:`doctag`,begin:`[ ]*(?=(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):)`,end:/(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):/,excludeBegin:!0,relevance:0});let i=y(`I`,`a`,`is`,`so`,`us`,`to`,`at`,`if`,`in`,`it`,`on`,/[A-Za-z]+['](d|ve|re|ll|t|s|n)/,/[A-Za-z]+[-][a-z]+/,/[A-Za-z][a-z]{2,}/);return r.contains.push({begin:_(/[ ]+/,`(`,i,/[.]?[:]?([.][ ]|[ ])/,`){3}`)}),r},M=j(`//`,`$`),ae=j(`/\\*`,`\\*/`),oe=j(`#`,`$`),se=Object.freeze({__proto__:null,APOS_STRING_MODE:re,BACKSLASH_ESCAPE:k,BINARY_NUMBER_MODE:{scope:`number`,begin:te,relevance:0},BINARY_NUMBER_RE:te,COMMENT:j,C_BLOCK_COMMENT_MODE:ae,C_LINE_COMMENT_MODE:M,C_NUMBER_MODE:{scope:`number`,begin:D,relevance:0},C_NUMBER_RE:D,END_SAME_AS_BEGIN:function(e){return Object.assign(e,{"on:begin":(e,t)=>{t.data._beginMatch=e[1]},"on:end":(e,t)=>{t.data._beginMatch!==e[1]&&t.ignoreMatch()}})},HASH_COMMENT_MODE:oe,IDENT_RE:T,MATCH_NOTHING_RE:w,METHOD_GUARD:{begin:`\\.\\s*[a-zA-Z_]\\w*`,relevance:0},NUMBER_MODE:{scope:`number`,begin:E,relevance:0},NUMBER_RE:E,PHRASAL_WORDS_MODE:ie,QUOTE_STRING_MODE:A,REGEXP_MODE:{scope:`regexp`,begin:/\/(?=[^/\n]*\/)/,end:/\/[gimuy]*/,contains:[k,{begin:/\[/,end:/\]/,relevance:0,contains:[k]}]},RE_STARTERS_RE:O,SHEBANG:ne,TITLE_MODE:{scope:`title`,begin:T,relevance:0},UNDERSCORE_IDENT_RE:ee,UNDERSCORE_TITLE_MODE:{scope:`title`,begin:ee,relevance:0}});function ce(e,t){e.input[e.index-1]===`.`&&t.ignoreMatch()}function le(e,t){e.className!==void 0&&(e.scope=e.className,delete e.className)}function N(e,t){t&&e.beginKeywords&&(e.begin=`\\b(`+e.beginKeywords.split(` `).join(`|`)+`)(?!\\.)(?=\\b|\\s)`,e.__beforeBegin=ce,e.keywords=e.keywords||e.beginKeywords,delete e.beginKeywords,e.relevance===void 0&&(e.relevance=0))}function ue(e,t){Array.isArray(e.illegal)&&(e.illegal=y(...e.illegal))}function de(e,t){if(e.match){if(e.begin||e.end)throw Error(`begin & end are not supported with match`);e.begin=e.match,delete e.match}}function fe(e,t){e.relevance===void 0&&(e.relevance=1)}var pe=(e,t)=>{if(!e.beforeMatch)return;if(e.starts)throw Error(`beforeMatch cannot be used with starts`);let n=Object.assign({},e);Object.keys(e).forEach(t=>{delete e[t]}),e.keywords=n.keywords,e.begin=_(n.beforeMatch,m(n.begin)),e.starts={relevance:0,contains:[Object.assign(n,{endsParent:!0})]},e.relevance=0,delete n.beforeMatch},me=[`of`,`and`,`for`,`in`,`not`,`or`,`if`,`then`,`parent`,`list`,`value`],he=`keyword`;function ge(e,t,n=he){let r=Object.create(null);return typeof e==`string`?i(n,e.split(` `)):Array.isArray(e)?i(n,e):Object.keys(e).forEach(function(n){Object.assign(r,ge(e[n],t,n))}),r;function i(e,n){t&&(n=n.map(e=>e.toLowerCase())),n.forEach(function(t){let n=t.split(`|`);r[n[0]]=[e,_e(n[0],n[1])]})}}function _e(e,t){return t?Number(t):+!ve(e)}function ve(e){return me.includes(e.toLowerCase())}var ye={},be=e=>{console.error(e)},xe=(e,...t)=>{console.log(`WARN: ${e}`,...t)},Se=(e,t)=>{ye[`${e}/${t}`]||(console.log(`Deprecated as of ${e}. ${t}`),ye[`${e}/${t}`]=!0)},Ce=Error();function we(e,t,{key:n}){let r=0,i=e[n],a={},o={};for(let e=1;e<=t.length;e++)o[e+r]=i[e],a[e+r]=!0,r+=b(t[e-1]);e[n]=o,e[n]._emit=a,e[n]._multi=!0}function Te(e){if(Array.isArray(e.begin)){if(e.skip||e.excludeBegin||e.returnBegin)throw be(`skip, excludeBegin, returnBegin not compatible with beginScope: {}`),Ce;if(typeof e.beginScope!=`object`||e.beginScope===null)throw be(`beginScope must be object`),Ce;we(e,e.begin,{key:`beginScope`}),e.begin=C(e.begin,{joinWith:``})}}function Ee(e){if(Array.isArray(e.end)){if(e.skip||e.excludeEnd||e.returnEnd)throw be(`skip, excludeEnd, returnEnd not compatible with endScope: {}`),Ce;if(typeof e.endScope!=`object`||e.endScope===null)throw be(`endScope must be object`),Ce;we(e,e.end,{key:`endScope`}),e.end=C(e.end,{joinWith:``})}}function De(e){e.scope&&typeof e.scope==`object`&&e.scope!==null&&(e.beginScope=e.scope,delete e.scope)}function Oe(e){De(e),typeof e.beginScope==`string`&&(e.beginScope={_wrap:e.beginScope}),typeof e.endScope==`string`&&(e.endScope={_wrap:e.endScope}),Te(e),Ee(e)}function ke(e){function t(t,n){return new RegExp(p(t),`m`+(e.case_insensitive?`i`:``)+(e.unicodeRegex?`u`:``)+(n?`g`:``))}class n{constructor(){this.matchIndexes={},this.regexes=[],this.matchAt=1,this.position=0}addRule(e,t){t.position=this.position++,this.matchIndexes[this.matchAt]=t,this.regexes.push([t,e]),this.matchAt+=b(e)+1}compile(){this.regexes.length===0&&(this.exec=()=>null);let e=this.regexes.map(e=>e[1]);this.matcherRe=t(C(e,{joinWith:`|`}),!0),this.lastIndex=0}exec(e){this.matcherRe.lastIndex=this.lastIndex;let t=this.matcherRe.exec(e);if(!t)return null;let n=t.findIndex((e,t)=>t>0&&e!==void 0),r=this.matchIndexes[n];return t.splice(0,n),Object.assign(t,r)}}class r{constructor(){this.rules=[],this.multiRegexes=[],this.count=0,this.lastIndex=0,this.regexIndex=0}getMatcher(e){if(this.multiRegexes[e])return this.multiRegexes[e];let t=new n;return this.rules.slice(e).forEach(([e,n])=>t.addRule(e,n)),t.compile(),this.multiRegexes[e]=t,t}resumingScanAtSamePosition(){return this.regexIndex!==0}considerAll(){this.regexIndex=0}addRule(e,t){this.rules.push([e,t]),t.type===`begin`&&this.count++}exec(e){let t=this.getMatcher(this.regexIndex);t.lastIndex=this.lastIndex;let n=t.exec(e);if(this.resumingScanAtSamePosition()&&!(n&&n.index===this.lastIndex)){let t=this.getMatcher(0);t.lastIndex=this.lastIndex+1,n=t.exec(e)}return n&&(this.regexIndex+=n.position+1,this.regexIndex===this.count&&this.considerAll()),n}}function i(e){let t=new r;return e.contains.forEach(e=>t.addRule(e.begin,{rule:e,type:`begin`})),e.terminatorEnd&&t.addRule(e.terminatorEnd,{type:`end`}),e.illegal&&t.addRule(e.illegal,{type:`illegal`}),t}function o(n,r){let a=n;if(n.isCompiled)return a;[le,de,Oe,pe].forEach(e=>e(n,r)),e.compilerExtensions.forEach(e=>e(n,r)),n.__beforeBegin=null,[N,ue,fe].forEach(e=>e(n,r)),n.isCompiled=!0;let s=null;return typeof n.keywords==`object`&&n.keywords.$pattern&&(n.keywords=Object.assign({},n.keywords),s=n.keywords.$pattern,delete n.keywords.$pattern),s||=/\w+/,n.keywords&&=ge(n.keywords,e.case_insensitive),a.keywordPatternRe=t(s,!0),r&&(n.begin||=/\B|\b/,a.beginRe=t(a.begin),!n.end&&!n.endsWithParent&&(n.end=/\B|\b/),n.end&&(a.endRe=t(a.end)),a.terminatorEnd=p(a.end)||``,n.endsWithParent&&r.terminatorEnd&&(a.terminatorEnd+=(n.end?`|`:``)+r.terminatorEnd)),n.illegal&&(a.illegalRe=t(n.illegal)),n.contains||=[],n.contains=[].concat(...n.contains.map(function(e){return je(e===`self`?n:e)})),n.contains.forEach(function(e){o(e,a)}),n.starts&&o(n.starts,r),a.matcher=i(a),a}if(e.compilerExtensions||=[],e.contains&&e.contains.includes(`self`))throw Error("ERR: contains `self` is not supported at the top-level of a language.  See documentation.");return e.classNameAliases=a(e.classNameAliases||{}),o(e)}function Ae(e){return e?e.endsWithParent||Ae(e.starts):!1}function je(e){return e.variants&&!e.cachedVariants&&(e.cachedVariants=e.variants.map(function(t){return a(e,{variants:null},t)})),e.cachedVariants?e.cachedVariants:Ae(e)?a(e,{starts:e.starts?a(e.starts):null}):Object.isFrozen(e)?a(e):e}var Me=`11.11.1`,Ne=class extends Error{constructor(e,t){super(e),this.name=`HTMLInjectionError`,this.html=t}},Pe=i,Fe=a,Ie=Symbol(`nomatch`),Le=7,Re=function(e){let t=Object.create(null),i=Object.create(null),a=[],o=!0,s=`Could not find the language '{}', did you forget to load/include a language module?`,c={disableAutodetect:!0,name:`Plain text`,contains:[]},l={ignoreUnescapedHTML:!1,throwUnescapedHTML:!1,noHighlightRe:/^(no-?highlight)$/i,languageDetectRe:/\blang(?:uage)?-([\w-]+)\b/i,classPrefix:`hljs-`,cssSelector:`pre code`,languages:null,__emitter:f};function u(e){return l.noHighlightRe.test(e)}function d(e){let t=e.className+` `;t+=e.parentNode?e.parentNode.className:``;let n=l.languageDetectRe.exec(t);if(n){let t=re(n[1]);return t||(xe(s.replace(`{}`,n[1])),xe(`Falling back to no-highlight mode for this block.`,e)),t?n[1]:`no-highlight`}return t.split(/\s+/).find(e=>u(e)||re(e))}function p(e,t,n){let r=``,i=``;typeof t==`object`?(r=e,n=t.ignoreIllegals,i=t.language):(Se(`10.7.0`,`highlight(lang, code, ...args) has been deprecated.`),Se(`10.7.0`,`Please use highlight(code, options) instead.
https://github.com/highlightjs/highlight.js/issues/2277`),i=e,r=t),n===void 0&&(n=!0);let a={code:r,language:i};oe(`before:highlight`,a);let o=a.result?a.result:v(a.language,a.code,n);return o.code=a.code,oe(`after:highlight`,o),o}function v(e,n,i,a){let c=Object.create(null);function u(e,t){return e.keywords[t]}function d(){if(!O.keywords){k.addText(A);return}let e=0;O.keywordPatternRe.lastIndex=0;let t=O.keywordPatternRe.exec(A),n=``;for(;t;){n+=A.substring(e,t.index);let r=E.case_insensitive?t[0].toLowerCase():t[0],i=u(O,r);if(i){let[e,a]=i;if(k.addText(n),n=``,c[r]=(c[r]||0)+1,c[r]<=Le&&(ie+=a),e.startsWith(`_`))n+=t[0];else{let n=E.classNameAliases[e]||e;m(t[0],n)}}else n+=t[0];e=O.keywordPatternRe.lastIndex,t=O.keywordPatternRe.exec(A)}n+=A.substring(e),k.addText(n)}function f(){if(A===``)return;let e=null;if(typeof O.subLanguage==`string`){if(!t[O.subLanguage]){k.addText(A);return}e=v(O.subLanguage,A,!0,ne[O.subLanguage]),ne[O.subLanguage]=e._top}else e=S(A,O.subLanguage.length?O.subLanguage:null);O.relevance>0&&(ie+=e.relevance),k.__addSublanguage(e._emitter,e.language)}function p(){O.subLanguage==null?d():f(),A=``}function m(e,t){e!==``&&(k.startScope(t),k.addText(e),k.endScope())}function h(e,t){let n=1,r=t.length-1;for(;n<=r;){if(!e._emit[n]){n++;continue}let r=E.classNameAliases[e[n]]||e[n],i=t[n];r?m(i,r):(A=i,d(),A=``),n++}}function g(e,t){return e.scope&&typeof e.scope==`string`&&k.openNode(E.classNameAliases[e.scope]||e.scope),e.beginScope&&(e.beginScope._wrap?(m(A,E.classNameAliases[e.beginScope._wrap]||e.beginScope._wrap),A=``):e.beginScope._multi&&(h(e.beginScope,t),A=``)),O=Object.create(e,{parent:{value:O}}),O}function _(e,t,n){let i=x(e.endRe,n);if(i){if(e[`on:end`]){let n=new r(e);e[`on:end`](t,n),n.isMatchIgnored&&(i=!1)}if(i){for(;e.endsParent&&e.parent;)e=e.parent;return e}}if(e.endsWithParent)return _(e.parent,t,n)}function y(e){return O.matcher.regexIndex===0?(A+=e[0],1):(ae=!0,0)}function b(e){let t=e[0],n=e.rule,i=new r(n),a=[n.__beforeBegin,n[`on:begin`]];for(let n of a)if(n&&(n(e,i),i.isMatchIgnored))return y(t);return n.skip?A+=t:(n.excludeBegin&&(A+=t),p(),!n.returnBegin&&!n.excludeBegin&&(A=t)),g(n,e),n.returnBegin?0:t.length}function C(e){let t=e[0],r=n.substring(e.index),i=_(O,e,r);if(!i)return Ie;let a=O;O.endScope&&O.endScope._wrap?(p(),m(t,O.endScope._wrap)):O.endScope&&O.endScope._multi?(p(),h(O.endScope,e)):a.skip?A+=t:(a.returnEnd||a.excludeEnd||(A+=t),p(),a.excludeEnd&&(A=t));do O.scope&&k.closeNode(),!O.skip&&!O.subLanguage&&(ie+=O.relevance),O=O.parent;while(O!==i.parent);return i.starts&&g(i.starts,e),a.returnEnd?0:t.length}function w(){let e=[];for(let t=O;t!==E;t=t.parent)t.scope&&e.unshift(t.scope);e.forEach(e=>k.openNode(e))}let T={};function ee(t,r){let a=r&&r[0];if(A+=t,a==null)return p(),0;if(T.type===`begin`&&r.type===`end`&&T.index===r.index&&a===``){if(A+=n.slice(r.index,r.index+1),!o){let t=Error(`0 width match regex (${e})`);throw t.languageName=e,t.badRule=T.rule,t}return 1}if(T=r,r.type===`begin`)return b(r);if(r.type===`illegal`&&!i){let e=Error(`Illegal lexeme "`+a+`" for mode "`+(O.scope||`<unnamed>`)+`"`);throw e.mode=O,e}else if(r.type===`end`){let e=C(r);if(e!==Ie)return e}if(r.type===`illegal`&&a===``)return A+=`
`,1;if(M>1e5&&M>r.index*3)throw Error(`potential infinite loop, way more iterations than matches`);return A+=a,a.length}let E=re(e);if(!E)throw be(s.replace(`{}`,e)),Error(`Unknown language: "`+e+`"`);let D=ke(E),te=``,O=a||D,ne={},k=new l.__emitter(l);w();let A=``,ie=0,j=0,M=0,ae=!1;try{if(E.__emitTokens)E.__emitTokens(n,k);else{for(O.matcher.considerAll();;){M++,ae?ae=!1:O.matcher.considerAll(),O.matcher.lastIndex=j;let e=O.matcher.exec(n);if(!e)break;let t=ee(n.substring(j,e.index),e);j=e.index+t}ee(n.substring(j))}return k.finalize(),te=k.toHTML(),{language:e,value:te,relevance:ie,illegal:!1,_emitter:k,_top:O}}catch(t){if(t.message&&t.message.includes(`Illegal`))return{language:e,value:Pe(n),illegal:!0,relevance:0,_illegalBy:{message:t.message,index:j,context:n.slice(j-100,j+100),mode:t.mode,resultSoFar:te},_emitter:k};if(o)return{language:e,value:Pe(n),illegal:!1,relevance:0,errorRaised:t,_emitter:k,_top:O};throw t}}function b(e){let t={value:Pe(e),illegal:!1,relevance:0,_top:c,_emitter:new l.__emitter(l)};return t._emitter.addText(e),t}function S(e,n){n=n||l.languages||Object.keys(t);let r=b(e),i=n.filter(re).filter(ie).map(t=>v(t,e,!1));i.unshift(r);let[a,o]=i.sort((e,t)=>{if(e.relevance!==t.relevance)return t.relevance-e.relevance;if(e.language&&t.language){if(re(e.language).supersetOf===t.language)return 1;if(re(t.language).supersetOf===e.language)return-1}return 0}),s=a;return s.secondBest=o,s}function C(e,t,n){let r=t&&i[t]||n;e.classList.add(`hljs`),e.classList.add(`language-${r}`)}function w(e){let t=null,n=d(e);if(u(n))return;if(oe(`before:highlightElement`,{el:e,language:n}),e.dataset.highlighted){console.log("Element previously highlighted. To highlight again, first unset `dataset.highlighted`.",e);return}if(e.children.length>0&&(l.ignoreUnescapedHTML||(console.warn(`One of your code blocks includes unescaped HTML. This is a potentially serious security risk.`),console.warn(`https://github.com/highlightjs/highlight.js/wiki/security`),console.warn(`The element with unescaped HTML:`),console.warn(e)),l.throwUnescapedHTML))throw new Ne(`One of your code blocks includes unescaped HTML.`,e.innerHTML);t=e;let r=t.textContent,i=n?p(r,{language:n,ignoreIllegals:!0}):S(r);e.innerHTML=i.value,e.dataset.highlighted=`yes`,C(e,n,i.language),e.result={language:i.language,re:i.relevance,relevance:i.relevance},i.secondBest&&(e.secondBest={language:i.secondBest.language,relevance:i.secondBest.relevance}),oe(`after:highlightElement`,{el:e,result:i,text:r})}function T(e){l=Fe(l,e)}let ee=()=>{te(),Se(`10.6.0`,`initHighlighting() deprecated.  Use highlightAll() now.`)};function E(){te(),Se(`10.6.0`,`initHighlightingOnLoad() deprecated.  Use highlightAll() now.`)}let D=!1;function te(){function e(){te()}if(document.readyState===`loading`){D||window.addEventListener(`DOMContentLoaded`,e,!1),D=!0;return}document.querySelectorAll(l.cssSelector).forEach(w)}function O(n,r){let i=null;try{i=r(e)}catch(e){if(be(`Language definition for '{}' could not be registered.`.replace(`{}`,n)),o)be(e);else throw e;i=c}i.name||=n,t[n]=i,i.rawDefinition=r.bind(null,e),i.aliases&&A(i.aliases,{languageName:n})}function ne(e){delete t[e];for(let t of Object.keys(i))i[t]===e&&delete i[t]}function k(){return Object.keys(t)}function re(e){return e=(e||``).toLowerCase(),t[e]||t[i[e]]}function A(e,{languageName:t}){typeof e==`string`&&(e=[e]),e.forEach(e=>{i[e.toLowerCase()]=t})}function ie(e){let t=re(e);return t&&!t.disableAutodetect}function j(e){e[`before:highlightBlock`]&&!e[`before:highlightElement`]&&(e[`before:highlightElement`]=t=>{e[`before:highlightBlock`](Object.assign({block:t.el},t))}),e[`after:highlightBlock`]&&!e[`after:highlightElement`]&&(e[`after:highlightElement`]=t=>{e[`after:highlightBlock`](Object.assign({block:t.el},t))})}function M(e){j(e),a.push(e)}function ae(e){let t=a.indexOf(e);t!==-1&&a.splice(t,1)}function oe(e,t){let n=e;a.forEach(function(e){e[n]&&e[n](t)})}function ce(e){return Se(`10.7.0`,`highlightBlock will be removed entirely in v12.0`),Se(`10.7.0`,`Please use highlightElement now.`),w(e)}Object.assign(e,{highlight:p,highlightAuto:S,highlightAll:te,highlightElement:w,highlightBlock:ce,configure:T,initHighlighting:ee,initHighlightingOnLoad:E,registerLanguage:O,unregisterLanguage:ne,listLanguages:k,getLanguage:re,registerAliases:A,autoDetection:ie,inherit:Fe,addPlugin:M,removePlugin:ae}),e.debugMode=function(){o=!1},e.safeMode=function(){o=!0},e.versionString=Me,e.regex={concat:_,lookahead:m,either:y,optional:g,anyNumberOfTimes:h};for(let e in se)typeof se[e]==`object`&&n(se[e]);return Object.assign(e,se),e},ze=Re({});ze.newInstance=()=>Re({}),t.exports=ze,ze.HighlightJS=ze,ze.default=ze}))()).default;function tn(e){let t=e.regex,n={},r={begin:/\$\{/,end:/\}/,contains:[`self`,{begin:/:-/,contains:[n]}]};Object.assign(n,{className:`variable`,variants:[{begin:t.concat(/\$[\w\d#@][\w\d_]*/,`(?![\\w\\d])(?![$])`)},r]});let i={className:`subst`,begin:/\$\(/,end:/\)/,contains:[e.BACKSLASH_ESCAPE]},a=e.inherit(e.COMMENT(),{match:[/(^|\s)/,/#.*$/],scope:{2:`comment`}}),o={begin:/<<-?\s*(?=\w+)/,starts:{contains:[e.END_SAME_AS_BEGIN({begin:/(\w+)/,end:/(\w+)/,className:`string`})]}},s={className:`string`,begin:/"/,end:/"/,contains:[e.BACKSLASH_ESCAPE,n,i]};i.contains.push(s);let c={match:/\\"/},l={className:`string`,begin:/'/,end:/'/},u={match:/\\'/},d={begin:/\$?\(\(/,end:/\)\)/,contains:[{begin:/\d+#[0-9a-f]+/,className:`number`},e.NUMBER_MODE,n]},f=e.SHEBANG({binary:`(${[`fish`,`bash`,`zsh`,`sh`,`csh`,`ksh`,`tcsh`,`dash`,`scsh`].join(`|`)})`,relevance:10}),p={className:`function`,begin:/\w[\w\d_]*\s*\(\s*\)\s*\{/,returnBegin:!0,contains:[e.inherit(e.TITLE_MODE,{begin:/\w[\w\d_]*/})],relevance:0},m=[`if`,`then`,`else`,`elif`,`fi`,`time`,`for`,`while`,`until`,`in`,`do`,`done`,`case`,`esac`,`coproc`,`function`,`select`],h=[`true`,`false`],g={match:/(\/[a-z._-]+)+/},_=[`break`,`cd`,`continue`,`eval`,`exec`,`exit`,`export`,`getopts`,`hash`,`pwd`,`readonly`,`return`,`shift`,`test`,`times`,`trap`,`umask`,`unset`],v=[`alias`,`bind`,`builtin`,`caller`,`command`,`declare`,`echo`,`enable`,`help`,`let`,`local`,`logout`,`mapfile`,`printf`,`read`,`readarray`,`source`,`sudo`,`type`,`typeset`,`ulimit`,`unalias`],y=`autoload.bg.bindkey.bye.cap.chdir.clone.comparguments.compcall.compctl.compdescribe.compfiles.compgroups.compquote.comptags.comptry.compvalues.dirs.disable.disown.echotc.echoti.emulate.fc.fg.float.functions.getcap.getln.history.integer.jobs.kill.limit.log.noglob.popd.print.pushd.pushln.rehash.sched.setcap.setopt.stat.suspend.ttyctl.unfunction.unhash.unlimit.unsetopt.vared.wait.whence.where.which.zcompile.zformat.zftp.zle.zmodload.zparseopts.zprof.zpty.zregexparse.zsocket.zstyle.ztcp`.split(`.`),b=`chcon.chgrp.chown.chmod.cp.dd.df.dir.dircolors.ln.ls.mkdir.mkfifo.mknod.mktemp.mv.realpath.rm.rmdir.shred.sync.touch.truncate.vdir.b2sum.base32.base64.cat.cksum.comm.csplit.cut.expand.fmt.fold.head.join.md5sum.nl.numfmt.od.paste.ptx.pr.sha1sum.sha224sum.sha256sum.sha384sum.sha512sum.shuf.sort.split.sum.tac.tail.tr.tsort.unexpand.uniq.wc.arch.basename.chroot.date.dirname.du.echo.env.expr.factor.groups.hostid.id.link.logname.nice.nohup.nproc.pathchk.pinky.printenv.printf.pwd.readlink.runcon.seq.sleep.stat.stdbuf.stty.tee.test.timeout.tty.uname.unlink.uptime.users.who.whoami.yes`.split(`.`);return{name:`Bash`,aliases:[`sh`,`zsh`],keywords:{$pattern:/\b[a-z][a-z0-9._-]+\b/,keyword:m,literal:h,built_in:[..._,...v,`set`,`shopt`,...y,...b]},contains:[f,e.SHEBANG(),p,d,a,o,g,s,c,l,u,n]}}var nn=e=>({IMPORTANT:{scope:`meta`,begin:`!important`},BLOCK_COMMENT:e.C_BLOCK_COMMENT_MODE,HEXCOLOR:{scope:`number`,begin:/#(([0-9a-fA-F]{3,4})|(([0-9a-fA-F]{2}){3,4}))\b/},FUNCTION_DISPATCH:{className:`built_in`,begin:/[\w-]+(?=\()/},ATTRIBUTE_SELECTOR_MODE:{scope:`selector-attr`,begin:/\[/,end:/\]/,illegal:`$`,contains:[e.APOS_STRING_MODE,e.QUOTE_STRING_MODE]},CSS_NUMBER_MODE:{scope:`number`,begin:e.NUMBER_RE+`(%|em|ex|ch|rem|vw|vh|vmin|vmax|cm|mm|in|pt|pc|px|deg|grad|rad|turn|s|ms|Hz|kHz|dpi|dpcm|dppx)?`,relevance:0},CSS_VARIABLE:{className:`attr`,begin:/--[A-Za-z_][A-Za-z0-9_-]*/}}),rn=`a.abbr.address.article.aside.audio.b.blockquote.body.button.canvas.caption.cite.code.dd.del.details.dfn.div.dl.dt.em.fieldset.figcaption.figure.footer.form.h1.h2.h3.h4.h5.h6.header.hgroup.html.i.iframe.img.input.ins.kbd.label.legend.li.main.mark.menu.nav.object.ol.optgroup.option.p.picture.q.quote.samp.section.select.source.span.strong.summary.sup.table.tbody.td.textarea.tfoot.th.thead.time.tr.ul.var.video`.split(`.`),an=`defs.g.marker.mask.pattern.svg.switch.symbol.feBlend.feColorMatrix.feComponentTransfer.feComposite.feConvolveMatrix.feDiffuseLighting.feDisplacementMap.feFlood.feGaussianBlur.feImage.feMerge.feMorphology.feOffset.feSpecularLighting.feTile.feTurbulence.linearGradient.radialGradient.stop.circle.ellipse.image.line.path.polygon.polyline.rect.text.use.textPath.tspan.foreignObject.clipPath`.split(`.`),on=[...rn,...an],sn=`any-hover.any-pointer.aspect-ratio.color.color-gamut.color-index.device-aspect-ratio.device-height.device-width.display-mode.forced-colors.grid.height.hover.inverted-colors.monochrome.orientation.overflow-block.overflow-inline.pointer.prefers-color-scheme.prefers-contrast.prefers-reduced-motion.prefers-reduced-transparency.resolution.scan.scripting.update.width.min-width.max-width.min-height.max-height`.split(`.`).sort().reverse(),cn=`active.any-link.blank.checked.current.default.defined.dir.disabled.drop.empty.enabled.first.first-child.first-of-type.fullscreen.future.focus.focus-visible.focus-within.has.host.host-context.hover.indeterminate.in-range.invalid.is.lang.last-child.last-of-type.left.link.local-link.not.nth-child.nth-col.nth-last-child.nth-last-col.nth-last-of-type.nth-of-type.only-child.only-of-type.optional.out-of-range.past.placeholder-shown.read-only.read-write.required.right.root.scope.target.target-within.user-invalid.valid.visited.where`.split(`.`).sort().reverse(),ln=[`after`,`backdrop`,`before`,`cue`,`cue-region`,`first-letter`,`first-line`,`grammar-error`,`marker`,`part`,`placeholder`,`selection`,`slotted`,`spelling-error`].sort().reverse(),un=`accent-color.align-content.align-items.align-self.alignment-baseline.all.anchor-name.animation.animation-composition.animation-delay.animation-direction.animation-duration.animation-fill-mode.animation-iteration-count.animation-name.animation-play-state.animation-range.animation-range-end.animation-range-start.animation-timeline.animation-timing-function.appearance.aspect-ratio.backdrop-filter.backface-visibility.background.background-attachment.background-blend-mode.background-clip.background-color.background-image.background-origin.background-position.background-position-x.background-position-y.background-repeat.background-size.baseline-shift.block-size.border.border-block.border-block-color.border-block-end.border-block-end-color.border-block-end-style.border-block-end-width.border-block-start.border-block-start-color.border-block-start-style.border-block-start-width.border-block-style.border-block-width.border-bottom.border-bottom-color.border-bottom-left-radius.border-bottom-right-radius.border-bottom-style.border-bottom-width.border-collapse.border-color.border-end-end-radius.border-end-start-radius.border-image.border-image-outset.border-image-repeat.border-image-slice.border-image-source.border-image-width.border-inline.border-inline-color.border-inline-end.border-inline-end-color.border-inline-end-style.border-inline-end-width.border-inline-start.border-inline-start-color.border-inline-start-style.border-inline-start-width.border-inline-style.border-inline-width.border-left.border-left-color.border-left-style.border-left-width.border-radius.border-right.border-right-color.border-right-style.border-right-width.border-spacing.border-start-end-radius.border-start-start-radius.border-style.border-top.border-top-color.border-top-left-radius.border-top-right-radius.border-top-style.border-top-width.border-width.bottom.box-align.box-decoration-break.box-direction.box-flex.box-flex-group.box-lines.box-ordinal-group.box-orient.box-pack.box-shadow.box-sizing.break-after.break-before.break-inside.caption-side.caret-color.clear.clip.clip-path.clip-rule.color.color-interpolation.color-interpolation-filters.color-profile.color-rendering.color-scheme.column-count.column-fill.column-gap.column-rule.column-rule-color.column-rule-style.column-rule-width.column-span.column-width.columns.contain.contain-intrinsic-block-size.contain-intrinsic-height.contain-intrinsic-inline-size.contain-intrinsic-size.contain-intrinsic-width.container.container-name.container-type.content.content-visibility.counter-increment.counter-reset.counter-set.cue.cue-after.cue-before.cursor.cx.cy.direction.display.dominant-baseline.empty-cells.enable-background.field-sizing.fill.fill-opacity.fill-rule.filter.flex.flex-basis.flex-direction.flex-flow.flex-grow.flex-shrink.flex-wrap.float.flood-color.flood-opacity.flow.font.font-display.font-family.font-feature-settings.font-kerning.font-language-override.font-optical-sizing.font-palette.font-size.font-size-adjust.font-smooth.font-smoothing.font-stretch.font-style.font-synthesis.font-synthesis-position.font-synthesis-small-caps.font-synthesis-style.font-synthesis-weight.font-variant.font-variant-alternates.font-variant-caps.font-variant-east-asian.font-variant-emoji.font-variant-ligatures.font-variant-numeric.font-variant-position.font-variation-settings.font-weight.forced-color-adjust.gap.glyph-orientation-horizontal.glyph-orientation-vertical.grid.grid-area.grid-auto-columns.grid-auto-flow.grid-auto-rows.grid-column.grid-column-end.grid-column-start.grid-gap.grid-row.grid-row-end.grid-row-start.grid-template.grid-template-areas.grid-template-columns.grid-template-rows.hanging-punctuation.height.hyphenate-character.hyphenate-limit-chars.hyphens.icon.image-orientation.image-rendering.image-resolution.ime-mode.initial-letter.initial-letter-align.inline-size.inset.inset-area.inset-block.inset-block-end.inset-block-start.inset-inline.inset-inline-end.inset-inline-start.isolation.justify-content.justify-items.justify-self.kerning.left.letter-spacing.lighting-color.line-break.line-height.line-height-step.list-style.list-style-image.list-style-position.list-style-type.margin.margin-block.margin-block-end.margin-block-start.margin-bottom.margin-inline.margin-inline-end.margin-inline-start.margin-left.margin-right.margin-top.margin-trim.marker.marker-end.marker-mid.marker-start.marks.mask.mask-border.mask-border-mode.mask-border-outset.mask-border-repeat.mask-border-slice.mask-border-source.mask-border-width.mask-clip.mask-composite.mask-image.mask-mode.mask-origin.mask-position.mask-repeat.mask-size.mask-type.masonry-auto-flow.math-depth.math-shift.math-style.max-block-size.max-height.max-inline-size.max-width.min-block-size.min-height.min-inline-size.min-width.mix-blend-mode.nav-down.nav-index.nav-left.nav-right.nav-up.none.normal.object-fit.object-position.offset.offset-anchor.offset-distance.offset-path.offset-position.offset-rotate.opacity.order.orphans.outline.outline-color.outline-offset.outline-style.outline-width.overflow.overflow-anchor.overflow-block.overflow-clip-margin.overflow-inline.overflow-wrap.overflow-x.overflow-y.overlay.overscroll-behavior.overscroll-behavior-block.overscroll-behavior-inline.overscroll-behavior-x.overscroll-behavior-y.padding.padding-block.padding-block-end.padding-block-start.padding-bottom.padding-inline.padding-inline-end.padding-inline-start.padding-left.padding-right.padding-top.page.page-break-after.page-break-before.page-break-inside.paint-order.pause.pause-after.pause-before.perspective.perspective-origin.place-content.place-items.place-self.pointer-events.position.position-anchor.position-visibility.print-color-adjust.quotes.r.resize.rest.rest-after.rest-before.right.rotate.row-gap.ruby-align.ruby-position.scale.scroll-behavior.scroll-margin.scroll-margin-block.scroll-margin-block-end.scroll-margin-block-start.scroll-margin-bottom.scroll-margin-inline.scroll-margin-inline-end.scroll-margin-inline-start.scroll-margin-left.scroll-margin-right.scroll-margin-top.scroll-padding.scroll-padding-block.scroll-padding-block-end.scroll-padding-block-start.scroll-padding-bottom.scroll-padding-inline.scroll-padding-inline-end.scroll-padding-inline-start.scroll-padding-left.scroll-padding-right.scroll-padding-top.scroll-snap-align.scroll-snap-stop.scroll-snap-type.scroll-timeline.scroll-timeline-axis.scroll-timeline-name.scrollbar-color.scrollbar-gutter.scrollbar-width.shape-image-threshold.shape-margin.shape-outside.shape-rendering.speak.speak-as.src.stop-color.stop-opacity.stroke.stroke-dasharray.stroke-dashoffset.stroke-linecap.stroke-linejoin.stroke-miterlimit.stroke-opacity.stroke-width.tab-size.table-layout.text-align.text-align-all.text-align-last.text-anchor.text-combine-upright.text-decoration.text-decoration-color.text-decoration-line.text-decoration-skip.text-decoration-skip-ink.text-decoration-style.text-decoration-thickness.text-emphasis.text-emphasis-color.text-emphasis-position.text-emphasis-style.text-indent.text-justify.text-orientation.text-overflow.text-rendering.text-shadow.text-size-adjust.text-transform.text-underline-offset.text-underline-position.text-wrap.text-wrap-mode.text-wrap-style.timeline-scope.top.touch-action.transform.transform-box.transform-origin.transform-style.transition.transition-behavior.transition-delay.transition-duration.transition-property.transition-timing-function.translate.unicode-bidi.user-modify.user-select.vector-effect.vertical-align.view-timeline.view-timeline-axis.view-timeline-inset.view-timeline-name.view-transition-name.visibility.voice-balance.voice-duration.voice-family.voice-pitch.voice-range.voice-rate.voice-stress.voice-volume.white-space.white-space-collapse.widows.width.will-change.word-break.word-spacing.word-wrap.writing-mode.x.y.z-index.zoom`.split(`.`).sort().reverse();function dn(e){let t=e.regex,n=nn(e),r={begin:/-(webkit|moz|ms|o)-(?=[a-z])/},i=/@-?\w[\w]*(-\w+)*/,a=[e.APOS_STRING_MODE,e.QUOTE_STRING_MODE];return{name:`CSS`,case_insensitive:!0,illegal:/[=|'\$]/,keywords:{keyframePosition:`from to`},classNameAliases:{keyframePosition:`selector-tag`},contains:[n.BLOCK_COMMENT,r,n.CSS_NUMBER_MODE,{className:`selector-id`,begin:/#[A-Za-z0-9_-]+/,relevance:0},{className:`selector-class`,begin:`\\.[a-zA-Z-][a-zA-Z0-9_-]*`,relevance:0},n.ATTRIBUTE_SELECTOR_MODE,{className:`selector-pseudo`,variants:[{begin:`:(`+cn.join(`|`)+`)`},{begin:`:(:)?(`+ln.join(`|`)+`)`}]},n.CSS_VARIABLE,{className:`attribute`,begin:`\\b(`+un.join(`|`)+`)\\b`},{begin:/:/,end:/[;}{]/,contains:[n.BLOCK_COMMENT,n.HEXCOLOR,n.IMPORTANT,n.CSS_NUMBER_MODE,...a,{begin:/(url|data-uri)\(/,end:/\)/,relevance:0,keywords:{built_in:`url data-uri`},contains:[...a,{className:`string`,begin:/[^)]/,endsWithParent:!0,excludeEnd:!0}]},n.FUNCTION_DISPATCH]},{begin:t.lookahead(/@/),end:`[{;]`,relevance:0,illegal:/:/,contains:[{className:`keyword`,begin:i},{begin:/\s/,endsWithParent:!0,excludeEnd:!0,relevance:0,keywords:{$pattern:/[a-z-]+/,keyword:`and or not only`,attribute:sn.join(` `)},contains:[{begin:/[a-z-]+(?=:)/,className:`attribute`},...a,n.CSS_NUMBER_MODE]}]},{className:`selector-tag`,begin:`\\b(`+on.join(`|`)+`)\\b`}]}}function fn(e){let t=e.COMMENT(/^\s*@?rem\b/,/$/,{relevance:10});return{name:`Batch file (DOS)`,aliases:[`bat`,`cmd`],case_insensitive:!0,illegal:/\/\*/,keywords:{keyword:[`if`,`else`,`goto`,`for`,`in`,`do`,`call`,`exit`,`not`,`exist`,`errorlevel`,`defined`,`equ`,`neq`,`lss`,`leq`,`gtr`,`geq`],built_in:`prn.nul.lpt3.lpt2.lpt1.con.com4.com3.com2.com1.aux.shift.cd.dir.echo.setlocal.endlocal.set.pause.copy.append.assoc.at.attrib.break.cacls.cd.chcp.chdir.chkdsk.chkntfs.cls.cmd.color.comp.compact.convert.date.dir.diskcomp.diskcopy.doskey.erase.fs.find.findstr.format.ftype.graftabl.help.keyb.label.md.mkdir.mode.more.move.path.pause.print.popd.pushd.promt.rd.recover.rem.rename.replace.restore.rmdir.shift.sort.start.subst.time.title.tree.type.ver.verify.vol.ping.net.ipconfig.taskkill.xcopy.ren.del`.split(`.`)},contains:[{className:`variable`,begin:/%%[^ ]|%[^ ]+?%|![^ ]+?!/},{className:`function`,begin:{className:`symbol`,begin:`^\\s*[A-Za-z._?][A-Za-z0-9_$#@~.?]*(:|\\s+label)`,relevance:0}.begin,end:`goto:eof`,contains:[e.inherit(e.TITLE_MODE,{begin:`([_a-zA-Z]\\w*\\.)*([_a-zA-Z]\\w*:)?[_a-zA-Z]\\w*`}),t]},{className:`number`,begin:`\\b\\d+`,relevance:0},t]}}var pn=`[0-9](_*[0-9])*`,mn=`\\.(${pn})`,hn=`[0-9a-fA-F](_*[0-9a-fA-F])*`,gn={className:`number`,variants:[{begin:`(\\b(${pn})((${mn})|\\.)?|(${mn}))[eE][+-]?(${pn})[fFdD]?\\b`},{begin:`\\b(${pn})((${mn})[fFdD]?\\b|\\.([fFdD]\\b)?)`},{begin:`(${mn})[fFdD]?\\b`},{begin:`\\b(${pn})[fFdD]\\b`},{begin:`\\b0[xX]((${hn})\\.?|(${hn})?\\.(${hn}))[pP][+-]?(${pn})[fFdD]?\\b`},{begin:`\\b(0|[1-9](_*[0-9])*)[lL]?\\b`},{begin:`\\b0[xX](${hn})[lL]?\\b`},{begin:`\\b0(_*[0-7])*[lL]?\\b`},{begin:`\\b0[bB][01](_*[01])*[lL]?\\b`}],relevance:0};function _n(e,t,n){return n===-1?``:e.replace(t,r=>_n(e,t,n-1))}function vn(e){let t=e.regex,n=`[À-ʸa-zA-Z_$][À-ʸa-zA-Z_$0-9]*`,r=n+_n(`(?:<[À-ʸa-zA-Z_$][À-ʸa-zA-Z_$0-9]*~~~(?:\\s*,\\s*[À-ʸa-zA-Z_$][À-ʸa-zA-Z_$0-9]*~~~)*>)?`,/~~~/g,2),i={keyword:`synchronized.abstract.private.var.static.if.const .for.while.strictfp.finally.protected.import.native.final.void.enum.else.break.transient.catch.instanceof.volatile.case.assert.package.default.public.try.switch.continue.throws.protected.public.private.module.requires.exports.do.sealed.yield.permits.goto.when`.split(`.`),literal:[`false`,`true`,`null`],type:[`char`,`boolean`,`long`,`float`,`int`,`byte`,`short`,`double`],built_in:[`super`,`this`]},a={className:`meta`,begin:`@[À-ʸa-zA-Z_$][À-ʸa-zA-Z_$0-9]*`,contains:[{begin:/\(/,end:/\)/,contains:[`self`]}]},o={className:`params`,begin:/\(/,end:/\)/,keywords:i,relevance:0,contains:[e.C_BLOCK_COMMENT_MODE],endsParent:!0};return{name:`Java`,aliases:[`jsp`],keywords:i,illegal:/<\/|#/,contains:[e.COMMENT(`/\\*\\*`,`\\*/`,{relevance:0,contains:[{begin:/\w+@/,relevance:0},{className:`doctag`,begin:`@[A-Za-z]+`}]}),{begin:/import java\.[a-z]+\./,keywords:`import`,relevance:2},e.C_LINE_COMMENT_MODE,e.C_BLOCK_COMMENT_MODE,{begin:/"""/,end:/"""/,className:`string`,contains:[e.BACKSLASH_ESCAPE]},e.APOS_STRING_MODE,e.QUOTE_STRING_MODE,{match:[/\b(?:class|interface|enum|extends|implements|new)/,/\s+/,n],className:{1:`keyword`,3:`title.class`}},{match:/non-sealed/,scope:`keyword`},{begin:[t.concat(/(?!else)/,n),/\s+/,n,/\s+/,/=(?!=)/],className:{1:`type`,3:`variable`,5:`operator`}},{begin:[/record/,/\s+/,n],className:{1:`keyword`,3:`title.class`},contains:[o,e.C_LINE_COMMENT_MODE,e.C_BLOCK_COMMENT_MODE]},{beginKeywords:`new throw return else`,relevance:0},{begin:[`(?:`+r+`\\s+)`,e.UNDERSCORE_IDENT_RE,/\s*(?=\()/],className:{2:`title.function`},keywords:i,contains:[{className:`params`,begin:/\(/,end:/\)/,keywords:i,relevance:0,contains:[a,e.APOS_STRING_MODE,e.QUOTE_STRING_MODE,gn,e.C_BLOCK_COMMENT_MODE]},e.C_LINE_COMMENT_MODE,e.C_BLOCK_COMMENT_MODE]},gn,a]}}var yn=`[A-Za-z$_][0-9A-Za-z$_]*`,bn=`as.in.of.if.for.while.finally.var.new.function.do.return.void.else.break.catch.instanceof.with.throw.case.default.try.switch.continue.typeof.delete.let.yield.const.class.debugger.async.await.static.import.from.export.extends.using`.split(`.`),xn=[`true`,`false`,`null`,`undefined`,`NaN`,`Infinity`],Sn=`Object.Function.Boolean.Symbol.Math.Date.Number.BigInt.String.RegExp.Array.Float32Array.Float64Array.Int8Array.Uint8Array.Uint8ClampedArray.Int16Array.Int32Array.Uint16Array.Uint32Array.BigInt64Array.BigUint64Array.Set.Map.WeakSet.WeakMap.ArrayBuffer.SharedArrayBuffer.Atomics.DataView.JSON.Promise.Generator.GeneratorFunction.AsyncFunction.Reflect.Proxy.Intl.WebAssembly`.split(`.`),Cn=[`Error`,`EvalError`,`InternalError`,`RangeError`,`ReferenceError`,`SyntaxError`,`TypeError`,`URIError`],wn=[`setInterval`,`setTimeout`,`clearInterval`,`clearTimeout`,`require`,`exports`,`eval`,`isFinite`,`isNaN`,`parseFloat`,`parseInt`,`decodeURI`,`decodeURIComponent`,`encodeURI`,`encodeURIComponent`,`escape`,`unescape`],Tn=[`arguments`,`this`,`super`,`console`,`window`,`document`,`localStorage`,`sessionStorage`,`module`,`global`],En=[].concat(wn,Sn,Cn);function Dn(e){let t=e.regex,n=(e,{after:t})=>{let n=`</`+e[0].slice(1);return e.input.indexOf(n,t)!==-1},r=yn,i={begin:`<>`,end:`</>`},a=/<[A-Za-z0-9\\._:-]+\s*\/>/,o={begin:/<[A-Za-z0-9\\._:-]+/,end:/\/[A-Za-z0-9\\._:-]+>|\/>/,isTrulyOpeningTag:(e,t)=>{let r=e[0].length+e.index,i=e.input[r];if(i===`<`||i===`,`){t.ignoreMatch();return}i===`>`&&(n(e,{after:r})||t.ignoreMatch());let a,o=e.input.substring(r);if(a=o.match(/^\s*=/)){t.ignoreMatch();return}if((a=o.match(/^\s+extends\s+/))&&a.index===0){t.ignoreMatch();return}}},s={$pattern:yn,keyword:bn,literal:xn,built_in:En,"variable.language":Tn},c=`[0-9](_?[0-9])*`,l=`\\.(${c})`,u=`0|[1-9](_?[0-9])*|0[0-7]*[89][0-9]*`,d={className:`number`,variants:[{begin:`(\\b(${u})((${l})|\\.)?|(${l}))[eE][+-]?(${c})\\b`},{begin:`\\b(${u})\\b((${l})\\b|\\.)?|(${l})\\b`},{begin:`\\b(0|[1-9](_?[0-9])*)n\\b`},{begin:`\\b0[xX][0-9a-fA-F](_?[0-9a-fA-F])*n?\\b`},{begin:`\\b0[bB][0-1](_?[0-1])*n?\\b`},{begin:`\\b0[oO][0-7](_?[0-7])*n?\\b`},{begin:`\\b0[0-7]+n?\\b`}],relevance:0},f={className:`subst`,begin:`\\$\\{`,end:`\\}`,keywords:s,contains:[]},p={begin:".?html`",end:``,starts:{end:"`",returnEnd:!1,contains:[e.BACKSLASH_ESCAPE,f],subLanguage:`xml`}},m={begin:".?css`",end:``,starts:{end:"`",returnEnd:!1,contains:[e.BACKSLASH_ESCAPE,f],subLanguage:`css`}},h={begin:".?gql`",end:``,starts:{end:"`",returnEnd:!1,contains:[e.BACKSLASH_ESCAPE,f],subLanguage:`graphql`}},g={className:`string`,begin:"`",end:"`",contains:[e.BACKSLASH_ESCAPE,f]},_={className:`comment`,variants:[e.COMMENT(/\/\*\*(?!\/)/,`\\*/`,{relevance:0,contains:[{begin:`(?=@[A-Za-z]+)`,relevance:0,contains:[{className:`doctag`,begin:`@[A-Za-z]+`},{className:`type`,begin:`\\{`,end:`\\}`,excludeEnd:!0,excludeBegin:!0,relevance:0},{className:`variable`,begin:`[A-Za-z$_][0-9A-Za-z$_]*(?=\\s*(-)|$)`,endsParent:!0,relevance:0},{begin:/(?=[^\n])\s/,relevance:0}]}]}),e.C_BLOCK_COMMENT_MODE,e.C_LINE_COMMENT_MODE]},v=[e.APOS_STRING_MODE,e.QUOTE_STRING_MODE,p,m,h,g,{match:/\$\d+/},d];f.contains=v.concat({begin:/\{/,end:/\}/,keywords:s,contains:[`self`].concat(v)});let y=[].concat(_,f.contains),b=y.concat([{begin:/(\s*)\(/,end:/\)/,keywords:s,contains:[`self`].concat(y)}]),x={className:`params`,begin:/(\s*)\(/,end:/\)/,excludeBegin:!0,excludeEnd:!0,keywords:s,contains:b},S={variants:[{match:[/class/,/\s+/,r,/\s+/,/extends/,/\s+/,t.concat(r,`(`,t.concat(/\./,r),`)*`)],scope:{1:`keyword`,3:`title.class`,5:`keyword`,7:`title.class.inherited`}},{match:[/class/,/\s+/,r],scope:{1:`keyword`,3:`title.class`}}]},C={relevance:0,match:t.either(/\bJSON/,/\b[A-Z][a-z]+([A-Z][a-z]*|\d)*/,/\b[A-Z]{2,}([A-Z][a-z]+|\d)+([A-Z][a-z]*)*/,/\b[A-Z]{2,}[a-z]+([A-Z][a-z]+|\d)*([A-Z][a-z]*)*/),className:`title.class`,keywords:{_:[...Sn,...Cn]}},w={label:`use_strict`,className:`meta`,relevance:10,begin:/^\s*['"]use (strict|asm)['"]/},T={variants:[{match:[/function/,/\s+/,r,/(?=\s*\()/]},{match:[/function/,/\s*(?=\()/]}],className:{1:`keyword`,3:`title.function`},label:`func.def`,contains:[x],illegal:/%/},ee={relevance:0,match:/\b[A-Z][A-Z_0-9]+\b/,className:`variable.constant`};function E(e){return t.concat(`(?!`,e.join(`|`),`)`)}let D={match:t.concat(/\b/,E([...wn,`super`,`import`].map(e=>`${e}\\s*\\(`)),r,t.lookahead(/\s*\(/)),className:`title.function`,relevance:0},te={begin:t.concat(/\./,t.lookahead(t.concat(r,/(?![0-9A-Za-z$_(])/))),end:r,excludeBegin:!0,keywords:`prototype`,className:`property`,relevance:0},O={match:[/get|set/,/\s+/,r,/(?=\()/],className:{1:`keyword`,3:`title.function`},contains:[{begin:/\(\)/},x]},ne=`(\\([^()]*(\\([^()]*(\\([^()]*\\)[^()]*)*\\)[^()]*)*\\)|`+e.UNDERSCORE_IDENT_RE+`)\\s*=>`,k={match:[/const|var|let/,/\s+/,r,/\s*/,/=\s*/,/(async\s*)?/,t.lookahead(ne)],keywords:`async`,className:{1:`keyword`,3:`title.function`},contains:[x]};return{name:`JavaScript`,aliases:[`js`,`jsx`,`mjs`,`cjs`],keywords:s,exports:{PARAMS_CONTAINS:b,CLASS_REFERENCE:C},illegal:/#(?![$_A-z])/,contains:[e.SHEBANG({label:`shebang`,binary:`node`,relevance:5}),w,e.APOS_STRING_MODE,e.QUOTE_STRING_MODE,p,m,h,g,_,{match:/\$\d+/},d,C,{scope:`attr`,match:r+t.lookahead(`:`),relevance:0},k,{begin:`(`+e.RE_STARTERS_RE+`|\\b(case|return|throw)\\b)\\s*`,keywords:`return throw case`,relevance:0,contains:[_,e.REGEXP_MODE,{className:`function`,begin:ne,returnBegin:!0,end:`\\s*=>`,contains:[{className:`params`,variants:[{begin:e.UNDERSCORE_IDENT_RE,relevance:0},{className:null,begin:/\(\s*\)/,skip:!0},{begin:/(\s*)\(/,end:/\)/,excludeBegin:!0,excludeEnd:!0,keywords:s,contains:b}]}]},{begin:/,/,relevance:0},{match:/\s+/,relevance:0},{variants:[{begin:i.begin,end:i.end},{match:a},{begin:o.begin,"on:begin":o.isTrulyOpeningTag,end:o.end}],subLanguage:`xml`,contains:[{begin:o.begin,end:o.end,skip:!0,contains:[`self`]}]}]},T,{beginKeywords:`while if switch catch for`},{begin:`\\b(?!function)`+e.UNDERSCORE_IDENT_RE+`\\([^()]*(\\([^()]*(\\([^()]*\\)[^()]*)*\\)[^()]*)*\\)\\s*\\{`,returnBegin:!0,label:`func.def`,contains:[x,e.inherit(e.TITLE_MODE,{begin:r,className:`title.function`})]},{match:/\.\.\./,relevance:0},te,{match:`\\$[A-Za-z$_][0-9A-Za-z$_]*`,relevance:0},{match:[/\bconstructor(?=\s*\()/],className:{1:`title.function`},contains:[x]},D,ee,S,O,{match:/\$[(.]/}]}}function On(e){let t={className:`attr`,begin:/"(\\.|[^\\"\r\n])*"(?=\s*:)/,relevance:1.01},n={match:/[{}[\],:]/,className:`punctuation`,relevance:0},r=[`true`,`false`,`null`],i={scope:`literal`,beginKeywords:r.join(` `)};return{name:`JSON`,aliases:[`jsonc`],keywords:{literal:r},contains:[t,n,e.QUOTE_STRING_MODE,i,e.C_NUMBER_MODE,e.C_LINE_COMMENT_MODE,e.C_BLOCK_COMMENT_MODE],illegal:`\\S`}}function kn(e){let t=[`string`,`char`,`byte`,`int`,`long`,`bool`,`decimal`,`single`,`double`,`DateTime`,`xml`,`array`,`hashtable`,`void`],n={$pattern:/-?[A-z\.\-]+\b/,keyword:`if else foreach return do while until elseif begin for trap data dynamicparam end break throw param continue finally in switch exit filter try process catch hidden static parameter`,built_in:`ac asnp cat cd CFS chdir clc clear clhy cli clp cls clv cnsn compare copy cp cpi cpp curl cvpa dbp del diff dir dnsn ebp echo|0 epal epcsv epsn erase etsn exsn fc fhx fl ft fw gal gbp gc gcb gci gcm gcs gdr gerr ghy gi gin gjb gl gm gmo gp gps gpv group gsn gsnp gsv gtz gu gv gwmi h history icm iex ihy ii ipal ipcsv ipmo ipsn irm ise iwmi iwr kill lp ls man md measure mi mount move mp mv nal ndr ni nmo npssc nsn nv ogv oh popd ps pushd pwd r rbp rcjb rcsn rd rdr ren ri rjb rm rmdir rmo rni rnp rp rsn rsnp rujb rv rvpa rwmi sajb sal saps sasv sbp sc scb select set shcm si sl sleep sls sort sp spjb spps spsv start stz sujb sv swmi tee trcm type wget where wjb write`},r=/\w[\w\d]*((-)[\w\d]+)*/,i={begin:"`[\\s\\S]",relevance:0},a={className:`variable`,variants:[{begin:/\$\B/},{className:`keyword`,begin:/\$this/},{begin:/\$[\w\d][\w\d_:]*/}]},o={className:`literal`,begin:/\$(null|true|false)\b/},s={className:`string`,variants:[{begin:/"/,end:/"/},{begin:/@"/,end:/^"@/}],contains:[i,a,{className:`variable`,begin:/\$[A-z]/,end:/[^A-z]/}]},c={className:`string`,variants:[{begin:/'/,end:/'/},{begin:/@'/,end:/^'@/}]},l=e.inherit(e.COMMENT(null,null),{variants:[{begin:/#/,end:/$/},{begin:/<#/,end:/#>/}],contains:[{className:`doctag`,variants:[{begin:/\.(synopsis|description|example|inputs|outputs|notes|link|component|role|functionality)/},{begin:/\.(parameter|forwardhelptargetname|forwardhelpcategory|remotehelprunspace|externalhelp)\s+\S+/}]}]}),u={className:`built_in`,variants:[{begin:`(Add|Clear|Close|Copy|Enter|Exit|Find|Format|Get|Hide|Join|Lock|Move|New|Open|Optimize|Pop|Push|Redo|Remove|Rename|Reset|Resize|Search|Select|Set|Show|Skip|Split|Step|Switch|Undo|Unlock|Watch|Backup|Checkpoint|Compare|Compress|Convert|ConvertFrom|ConvertTo|Dismount|Edit|Expand|Export|Group|Import|Initialize|Limit|Merge|Mount|Out|Publish|Restore|Save|Sync|Unpublish|Update|Approve|Assert|Build|Complete|Confirm|Deny|Deploy|Disable|Enable|Install|Invoke|Register|Request|Restart|Resume|Start|Stop|Submit|Suspend|Uninstall|Unregister|Wait|Debug|Measure|Ping|Repair|Resolve|Test|Trace|Connect|Disconnect|Read|Receive|Send|Write|Block|Grant|Protect|Revoke|Unblock|Unprotect|Use|ForEach|Sort|Tee|Where)+(-)[\\w\\d]+`}]},d={className:`class`,beginKeywords:`class enum`,end:/\s*[{]/,excludeEnd:!0,relevance:0,contains:[e.TITLE_MODE]},f={className:`function`,begin:/function\s+/,end:/\s*\{|$/,excludeEnd:!0,returnBegin:!0,relevance:0,contains:[{begin:`function`,relevance:0,className:`keyword`},{className:`title`,begin:r,relevance:0},{begin:/\(/,end:/\)/,className:`params`,relevance:0,contains:[a]}]},p={begin:/using\s/,end:/$/,returnBegin:!0,contains:[s,c,{className:`keyword`,begin:/(using|assembly|command|module|namespace|type)/}]},m={variants:[{className:`operator`,begin:`(-and|-as|-band|-bnot|-bor|-bxor|-casesensitive|-ccontains|-ceq|-cge|-cgt|-cle|-clike|-clt|-cmatch|-cne|-cnotcontains|-cnotlike|-cnotmatch|-contains|-creplace|-csplit|-eq|-exact|-f|-file|-ge|-gt|-icontains|-ieq|-ige|-igt|-ile|-ilike|-ilt|-imatch|-in|-ine|-inotcontains|-inotlike|-inotmatch|-ireplace|-is|-isnot|-isplit|-join|-le|-like|-lt|-match|-ne|-not|-notcontains|-notin|-notlike|-notmatch|-or|-regex|-replace|-shl|-shr|-split|-wildcard|-xor)\\b`},{className:`literal`,begin:/(-){1,2}[\w\d-]+/,relevance:0}]},h={className:`selector-tag`,begin:/@\B/,relevance:0},g={className:`function`,begin:/\[.*\]\s*[\w]+[ ]??\(/,end:/$/,returnBegin:!0,relevance:0,contains:[{className:`keyword`,begin:`(${n.keyword.toString().replace(/\s/g,`|`)})\\b`,endsParent:!0,relevance:0},e.inherit(e.TITLE_MODE,{endsParent:!0})]},_=[g,l,i,e.NUMBER_MODE,s,c,u,a,o,h],v={begin:/\[/,end:/\]/,excludeBegin:!0,excludeEnd:!0,relevance:0,contains:[`self`].concat(_,{begin:`(`+t.join(`|`)+`)`,className:`built_in`,relevance:0},{className:`type`,begin:/[\.\w\d]+/,relevance:0})};return g.contains.unshift(v),{name:`PowerShell`,aliases:[`pwsh`,`ps`,`ps1`],case_insensitive:!0,keywords:n,contains:_.concat(d,f,p,m,v)}}function An(e){let t=e.regex,n=/[\p{XID_Start}_]\p{XID_Continue}*/u,r=`and.as.assert.async.await.break.case.class.continue.def.del.elif.else.except.finally.for.from.global.if.import.in.is.lambda.match.nonlocal|10.not.or.pass.raise.return.try.while.with.yield`.split(`.`),i={$pattern:/[A-Za-z]\w+|__\w+__/,keyword:r,built_in:`__import__.abs.all.any.ascii.bin.bool.breakpoint.bytearray.bytes.callable.chr.classmethod.compile.complex.delattr.dict.dir.divmod.enumerate.eval.exec.filter.float.format.frozenset.getattr.globals.hasattr.hash.help.hex.id.input.int.isinstance.issubclass.iter.len.list.locals.map.max.memoryview.min.next.object.oct.open.ord.pow.print.property.range.repr.reversed.round.set.setattr.slice.sorted.staticmethod.str.sum.super.tuple.type.vars.zip`.split(`.`),literal:[`__debug__`,`Ellipsis`,`False`,`None`,`NotImplemented`,`True`],type:[`Any`,`Callable`,`Coroutine`,`Dict`,`List`,`Literal`,`Generic`,`Optional`,`Sequence`,`Set`,`Tuple`,`Type`,`Union`]},a={className:`meta`,begin:/^(>>>|\.\.\.) /},o={className:`subst`,begin:/\{/,end:/\}/,keywords:i,illegal:/#/},s={begin:/\{\{/,relevance:0},c={className:`string`,contains:[e.BACKSLASH_ESCAPE],variants:[{begin:/([uU]|[bB]|[rR]|[bB][rR]|[rR][bB])?'''/,end:/'''/,contains:[e.BACKSLASH_ESCAPE,a],relevance:10},{begin:/([uU]|[bB]|[rR]|[bB][rR]|[rR][bB])?"""/,end:/"""/,contains:[e.BACKSLASH_ESCAPE,a],relevance:10},{begin:/([fF][rR]|[rR][fF]|[fF])'''/,end:/'''/,contains:[e.BACKSLASH_ESCAPE,a,s,o]},{begin:/([fF][rR]|[rR][fF]|[fF])"""/,end:/"""/,contains:[e.BACKSLASH_ESCAPE,a,s,o]},{begin:/([uU]|[rR])'/,end:/'/,relevance:10},{begin:/([uU]|[rR])"/,end:/"/,relevance:10},{begin:/([bB]|[bB][rR]|[rR][bB])'/,end:/'/},{begin:/([bB]|[bB][rR]|[rR][bB])"/,end:/"/},{begin:/([fF][rR]|[rR][fF]|[fF])'/,end:/'/,contains:[e.BACKSLASH_ESCAPE,s,o]},{begin:/([fF][rR]|[rR][fF]|[fF])"/,end:/"/,contains:[e.BACKSLASH_ESCAPE,s,o]},e.APOS_STRING_MODE,e.QUOTE_STRING_MODE]},l=`[0-9](_?[0-9])*`,u=`(\\b(${l}))?\\.(${l})|\\b(${l})\\.`,d=`\\b|${r.join(`|`)}`,f={className:`number`,relevance:0,variants:[{begin:`(\\b(${l})|(${u}))[eE][+-]?(${l})[jJ]?(?=${d})`},{begin:`(${u})[jJ]?`},{begin:`\\b([1-9](_?[0-9])*|0+(_?0)*)[lLjJ]?(?=${d})`},{begin:`\\b0[bB](_?[01])+[lL]?(?=${d})`},{begin:`\\b0[oO](_?[0-7])+[lL]?(?=${d})`},{begin:`\\b0[xX](_?[0-9a-fA-F])+[lL]?(?=${d})`},{begin:`\\b(${l})[jJ](?=${d})`}]},p={className:`comment`,begin:t.lookahead(/# type:/),end:/$/,keywords:i,contains:[{begin:/# type:/},{begin:/#/,end:/\b\B/,endsWithParent:!0}]},m={className:`params`,variants:[{className:``,begin:/\(\s*\)/,skip:!0},{begin:/\(/,end:/\)/,excludeBegin:!0,excludeEnd:!0,keywords:i,contains:[`self`,a,f,c,e.HASH_COMMENT_MODE]}]};return o.contains=[c,f,a],{name:`Python`,aliases:[`py`,`gyp`,`ipython`],unicodeRegex:!0,keywords:i,illegal:/(<\/|\?)|=>/,contains:[a,f,{scope:`variable.language`,match:/\bself\b/},{beginKeywords:`if`,relevance:0},{match:/\bor\b/,scope:`keyword`},c,p,e.HASH_COMMENT_MODE,{match:[/\bdef/,/\s+/,n],scope:{1:`keyword`,3:`title.function`},contains:[m]},{variants:[{match:[/\bclass/,/\s+/,n,/\s*/,/\(\s*/,n,/\s*\)/]},{match:[/\bclass/,/\s+/,n]}],scope:{1:`keyword`,3:`title.class`,6:`title.class.inherited`}},{className:`meta`,begin:/^[\t ]*@/,end:/(?=#)|$/,contains:[f,m,c]}]}}function jn(e){return{name:`Shell Session`,aliases:[`console`,`shellsession`],contains:[{className:`meta.prompt`,begin:/^\s{0,3}[/~\w\d[\]()@-]*[>%$#][ ]?/,starts:{end:/[^\\](?=\s*$)/,subLanguage:`bash`}}]}}function Mn(e){let t=e.regex,n=t.concat(/[\p{L}_]/u,t.optional(/[\p{L}0-9_.-]*:/u),/[\p{L}0-9_.-]*/u),r=/[\p{L}0-9._:-]+/u,i={className:`symbol`,begin:/&[a-z]+;|&#[0-9]+;|&#x[a-f0-9]+;/},a={begin:/\s/,contains:[{className:`keyword`,begin:/#?[a-z_][a-z1-9_-]+/,illegal:/\n/}]},o=e.inherit(a,{begin:/\(/,end:/\)/}),s=e.inherit(e.APOS_STRING_MODE,{className:`string`}),c=e.inherit(e.QUOTE_STRING_MODE,{className:`string`}),l={endsWithParent:!0,illegal:/</,relevance:0,contains:[{className:`attr`,begin:r,relevance:0},{begin:/=\s*/,relevance:0,contains:[{className:`string`,endsParent:!0,variants:[{begin:/"/,end:/"/,contains:[i]},{begin:/'/,end:/'/,contains:[i]},{begin:/[^\s"'=<>`]+/}]}]}]};return{name:`HTML, XML`,aliases:[`html`,`xhtml`,`rss`,`atom`,`xjb`,`xsd`,`xsl`,`plist`,`wsf`,`svg`],case_insensitive:!0,unicodeRegex:!0,contains:[{className:`meta`,begin:/<![a-z]/,end:/>/,relevance:10,contains:[a,c,s,o,{begin:/\[/,end:/\]/,contains:[{className:`meta`,begin:/<![a-z]/,end:/>/,contains:[a,o,c,s]}]}]},e.COMMENT(/<!--/,/-->/,{relevance:10}),{begin:/<!\[CDATA\[/,end:/\]\]>/,relevance:10},i,{className:`meta`,end:/\?>/,variants:[{begin:/<\?xml/,relevance:10,contains:[c]},{begin:/<\?[a-z][a-z0-9]+/}]},{className:`tag`,begin:/<style(?=\s|>)/,end:/>/,keywords:{name:`style`},contains:[l],starts:{end:/<\/style>/,returnEnd:!0,subLanguage:[`css`,`xml`]}},{className:`tag`,begin:/<script(?=\s|>)/,end:/>/,keywords:{name:`script`},contains:[l],starts:{end:/<\/script>/,returnEnd:!0,subLanguage:[`javascript`,`handlebars`,`xml`]}},{className:`tag`,begin:/<>|<\/>/},{className:`tag`,begin:t.concat(/</,t.lookahead(t.concat(n,t.either(/\/>/,/>/,/\s/)))),end:/\/?>/,contains:[{className:`name`,begin:n,relevance:0,starts:l}]},{className:`tag`,begin:t.concat(/<\//,t.lookahead(t.concat(n,/>/))),contains:[{className:`name`,begin:n,relevance:0},{begin:/>/,relevance:0,endsParent:!0}]}]}}function Nn(){return{async:!1,breaks:!1,extensions:null,gfm:!0,hooks:null,pedantic:!1,renderer:null,silent:!1,tokenizer:null,walkTokens:null}}var Pn=Nn();function Fn(e){Pn=e}var In={exec:()=>null};function Ln(e){let t=[];return n=>{let r=Math.max(0,Math.min(3,n-1)),i=t[r];return i||(i=e(r),t[r]=i),i}}function L(e,t=``){let n=typeof e==`string`?e:e.source,r={replace:(e,t)=>{let i=typeof t==`string`?t:t.source;return i=i.replace(zn.caret,`$1`),n=n.replace(e,i),r},getRegex:()=>new RegExp(n,t)};return r}var Rn=((e=``)=>{try{return!!RegExp(`(?<=1)(?<!1)`+e)}catch{return!1}})(),zn={codeRemoveIndent:/^(?: {1,4}| {0,3}\t)/gm,outputLinkReplace:/\\([\[\]])/g,indentCodeCompensation:/^(\s+)(?:```)/,beginningSpace:/^\s+/,endingHash:/#$/,startingSpaceChar:/^ /,endingSpaceChar:/ $/,nonSpaceChar:/[^ ]/,newLineCharGlobal:/\n/g,tabCharGlobal:/\t/g,multipleSpaceGlobal:/\s+/g,blankLine:/^[ \t]*$/,doubleBlankLine:/\n[ \t]*\n[ \t]*$/,blockquoteStart:/^ {0,3}>/,blockquoteSetextReplace:/\n {0,3}((?:=+|-+) *)(?=\n|$)/g,blockquoteSetextReplace2:/^ {0,3}>[ \t]?/gm,listReplaceNesting:/^ {1,4}(?=( {4})*[^ ])/g,listIsTask:/^\[[ xX]\] +\S/,listReplaceTask:/^\[[ xX]\] +/,listTaskCheckbox:/\[[ xX]\]/,anyLine:/\n.*\n/,hrefBrackets:/^<(.*)>$/,tableDelimiter:/[:|]/,tableAlignChars:/^\||\| *$/g,tableRowBlankLine:/\n[ \t]*$/,tableAlignRight:/^ *-+: *$/,tableAlignCenter:/^ *:-+: *$/,tableAlignLeft:/^ *:-+ *$/,startATag:/^<a /i,endATag:/^<\/a>/i,startPreScriptTag:/^<(pre|code|kbd|script)(\s|>)/i,endPreScriptTag:/^<\/(pre|code|kbd|script)(\s|>)/i,startAngleBracket:/^</,endAngleBracket:/>$/,pedanticHrefTitle:/^([^'"]*[^\s])\s+(['"])(.*)\2/,unicodeAlphaNumeric:/[\p{L}\p{N}]/u,escapeTest:/[&<>"']/,escapeReplace:/[&<>"']/g,escapeTestNoEncode:/[<>"']|&(?!(#\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\w+);)/,escapeReplaceNoEncode:/[<>"']|&(?!(#\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\w+);)/g,caret:/(^|[^\[])\^/g,percentDecode:/%25/g,findPipe:/\|/g,splitPipe:/ \|/,slashPipe:/\\\|/g,carriageReturn:/\r\n|\r/g,spaceLine:/^ +$/gm,notSpaceStart:/^\S*/,endingNewline:/\n$/,listItemRegex:e=>RegExp(`^( {0,3}${e})((?:[	 ][^\\n]*)?(?:\\n|$))`),nextBulletRegex:Ln(e=>RegExp(`^ {0,${e}}(?:[*+-]|\\d{1,9}[.)])((?:[ 	][^\\n]*)?(?:\\n|$))`)),hrRegex:Ln(e=>RegExp(`^ {0,${e}}((?:- *){3,}|(?:_ *){3,}|(?:\\* *){3,})(?:\\n+|$)`)),fencesBeginRegex:Ln(e=>RegExp(`^ {0,${e}}(?:\`\`\`|~~~)`)),headingBeginRegex:Ln(e=>RegExp(`^ {0,${e}}#`)),htmlBeginRegex:Ln(e=>RegExp(`^ {0,${e}}<(?:[a-z].*>|!--)`,`i`)),blockquoteBeginRegex:Ln(e=>RegExp(`^ {0,${e}}>`))},Bn=/^(?:[ \t]*(?:\n|$))+/,Vn=/^((?: {4}| {0,3}\t)[^\n]+(?:\n(?:[ \t]*(?:\n|$))*)?)+/,Hn=/^ {0,3}(`{3,}(?=[^`\n]*(?:\n|$))|~{3,})([^\n]*)(?:\n|$)(?:|([\s\S]*?)(?:\n|$))(?: {0,3}\1[~`]* *(?=\n|$)|$)/,Un=/^ {0,3}((?:-[\t ]*){3,}|(?:_[ \t]*){3,}|(?:\*[ \t]*){3,})(?:\n+|$)/,Wn=/^ {0,3}(#{1,6})(?=\s|$)(.*)(?:\n+|$)/,Gn=/ {0,3}(?:[*+-]|\d{1,9}[.)])/,Kn=/^(?!bull |blockCode|fences|blockquote|heading|html|table)((?:.|\n(?!\s*?\n|bull |blockCode|fences|blockquote|heading|html|table))+?)\n {0,3}(=+|-+) *(?:\n+|$)/,qn=L(Kn).replace(/bull/g,Gn).replace(/blockCode/g,/(?: {4}| {0,3}\t)/).replace(/fences/g,/ {0,3}(?:`{3,}|~{3,})/).replace(/blockquote/g,/ {0,3}>/).replace(/heading/g,/ {0,3}#{1,6}(?:\s|$)/).replace(/html/g,/ {0,3}<[^\n>]+>\n/).replace(/\|table/g,``).getRegex(),Jn=L(Kn).replace(/bull/g,Gn).replace(/blockCode/g,/(?: {4}| {0,3}\t)/).replace(/fences/g,/ {0,3}(?:`{3,}|~{3,})/).replace(/blockquote/g,/ {0,3}>/).replace(/heading/g,/ {0,3}#{1,6}(?:\s|$)/).replace(/html/g,/ {0,3}<[^\n>]+>\n/).replace(/table/g,/ {0,3}\|?(?:[:\- ]*\|)+[\:\- ]*\n/).getRegex(),Yn=/^([^\n]+(?:\n(?!hr|heading|lheading|blockquote|fences|list|html|table|[ \t]+\n)[^\n]+)*)/,Xn=/^[^\n]+/,Zn=/(?!\s*\])(?:\\[\s\S]|[^\[\]\\])+/,Qn=L(/^ {0,3}\[(label)\]: *(?:\n[ \t]*)?([^<\s][^\s]*|<.*?>)(?:(?: +(?:\n[ \t]*)?| *\n[ \t]*)(title))? *(?:\n+|$)/).replace(`label`,Zn).replace(`title`,/(?:"(?:\\"?|[^"\\])*"|'[^'\n]*(?:\n[^'\n]+)*\n?'|\([^()]*\))/).getRegex(),$n=L(/^(bull)([ \t][^\n]*?)?(?:\n|$)/).replace(/bull/g,Gn).getRegex(),er=`address|article|aside|base|basefont|blockquote|body|caption|center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|footer|form|frame|frameset|h[1-6]|head|header|hr|html|iframe|legend|li|link|main|menu|menuitem|meta|nav|noframes|ol|optgroup|option|p|param|search|section|summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul`,tr=/<!--(?:-?>|[\s\S]*?(?:-->|$))/,nr=L(`^ {0,3}(?:<(script|pre|style|textarea)[\\s>][\\s\\S]*?(?:</\\1>[^\\n]*\\n*|$)|comment[^\\n]*(\\n+|$)|<\\?[\\s\\S]*?(?:\\?>[^\\n]*\\n*|$)|<![A-Z][\\s\\S]*?(?:>[^\\n]*\\n*|$)|<!\\[CDATA\\[[\\s\\S]*?(?:\\]\\]>[^\\n]*\\n*|$)|</?(tag)(?: +|\\n|/?>)[\\s\\S]*?(?:(?:\\n[ 	]*)+\\n|$)|<(?!script|pre|style|textarea)([a-z][\\w-]*)(?:attribute)*? */?>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n[ 	]*)+\\n|$)|</(?!script|pre|style|textarea)[a-z][\\w-]*\\s*>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n[ 	]*)+\\n|$))`,`i`).replace(`comment`,tr).replace(`tag`,er).replace(`attribute`,/ +[a-zA-Z:_][\w.:-]*(?: *= *"[^"\n]*"| *= *'[^'\n]*'| *= *[^\s"'=<>`]+)?/).getRegex(),rr=e=>L(Yn).replace(`hr`,Un).replace(`heading`,` {0,3}#{1,6}(?:\\s|$)`).replace(`|lheading`,``).replace(`|table`,``).replace(`blockquote`,` {0,3}>`).replace(`fences`," {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~~~)[^\\n]*\\n").replace(`list`,e).replace(`html`,`</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)`).replace(`tag`,er).getRegex(),ir=rr(/ {0,3}(?:[*+-]|1[.)])[ \t]+[^ \t\n]/),ar=rr(/ {0,3}(?:[*+-]|\d{1,9}[.)])(?:[ \t]|\n|$)/),or={blockquote:L(/^( {0,3}> ?(paragraph|[^\n]*)(?:\n|$))+/).replace(`paragraph`,ar).getRegex(),code:Vn,def:Qn,fences:Hn,heading:Wn,hr:Un,html:nr,lheading:qn,list:$n,newline:Bn,paragraph:ir,table:In,text:Xn},sr=L(`^ *([^\\n ].*)\\n {0,3}((?:\\| *)?:?-+:? *(?:\\| *:?-+:? *)*(?:\\| *)?)(?:\\n((?:(?! *\\n|hr|heading|blockquote|code|fences|list|html).*(?:\\n|$))*)\\n*|$)`).replace(`hr`,Un).replace(`heading`,` {0,3}#{1,6}(?:\\s|$)`).replace(`blockquote`,` {0,3}>`).replace(`code`,`(?: {4}| {0,3}	)[^\\n]`).replace(`fences`," {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~~~)[^\\n]*\\n").replace(`list`,` {0,3}(?:[*+-]|1[.)])[ \\t]`).replace(`html`,`</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)`).replace(`tag`,er).getRegex(),cr={...or,lheading:Jn,table:sr,paragraph:L(Yn).replace(`hr`,Un).replace(`heading`,` {0,3}#{1,6}(?:\\s|$)`).replace(`|lheading`,``).replace(`table`,sr).replace(`blockquote`,` {0,3}>`).replace(`fences`," {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~~~)[^\\n]*\\n").replace(`list`,` {0,3}(?:[*+-]|1[.)])[ \\t]+[^ \\t\\n]`).replace(`html`,`</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)`).replace(`tag`,er).getRegex()},lr={...or,html:L(`^ *(?:comment *(?:\\n|\\s*$)|<(tag)[\\s\\S]+?</\\1> *(?:\\n{2,}|\\s*$)|<tag(?:"[^"]*"|'[^']*'|\\s[^'"/>\\s]*)*?/?> *(?:\\n{2,}|\\s*$))`).replace(`comment`,tr).replace(/tag/g,`(?!(?:a|em|strong|small|s|cite|q|dfn|abbr|data|time|code|var|samp|kbd|sub|sup|i|b|u|mark|ruby|rt|rp|bdi|bdo|span|br|wbr|ins|del|img)\\b)\\w+(?!:|[^\\w\\s@]*@)\\b`).getRegex(),def:/^ *\[([^\]]+)\]: *<?([^\s>]+)>?(?: +(["(][^\n]+[")]))? *(?:\n+|$)/,heading:/^(#{1,6})(.*)(?:\n+|$)/,fences:In,lheading:/^(.+?)\n {0,3}(=+|-+) *(?:\n+|$)/,paragraph:L(Yn).replace(`hr`,Un).replace(`heading`,` *#{1,6} *[^
]`).replace(`lheading`,qn).replace(`|table`,``).replace(`blockquote`,` {0,3}>`).replace(`|fences`,``).replace(`|list`,``).replace(`|html`,``).replace(`|tag`,``).getRegex()},ur=/^\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/,dr=/^(`+)([^`]|[^`][\s\S]*?[^`])\1(?!`)/,fr=/^( {2,}|\\)\n(?!\s*$)/,pr=/^(`+|[^`])(?:(?= {2,}\n)|[\s\S]*?(?:(?=[\\<!\[`*_]|\b_|$)|[^ ](?= {2,}\n)))/,mr=/[\p{P}\p{S}]/u,hr=/[\s\p{P}\p{S}]/u,gr=/[^\s\p{P}\p{S}]/u,_r=L(/^((?![*_])punctSpace)/,`u`).replace(/punctSpace/g,hr).getRegex(),vr=/(?!~)[\p{P}\p{S}]/u,yr=/(?!~)[\s\p{P}\p{S}]/u,br=/(?:[^\s\p{P}\p{S}]|~)/u,xr=L(/link|precode-code|html/,`g`).replace(`link`,/\[(?:[^\[\]`]|(?<a>`+)[^`]+\k<a>(?!`))*?\]\((?:\\[\s\S]|[^\\\(\)]|\((?:\\[\s\S]|[^\\\(\)])*\))*\)/).replace(`precode-`,Rn?"(?<!`)()":"(^^|[^`])").replace(`code`,/(?<b>`+)[^`]+\k<b>(?!`)/).replace(`html`,/<(?! )[^<>]*?>/).getRegex(),Sr=/^(?:\*+(?:((?!\*)punct)|([^\s*]))?)|^_+(?:((?!_)punct)|([^\s_]))?/,Cr=L(Sr,`u`).replace(/punct/g,mr).getRegex(),wr=L(Sr,`u`).replace(/punct/g,vr).getRegex(),Tr=`^[^_*]*?__[^_*]*?\\*[^_*]*?(?=__)|[^*]+(?=[^*])|(?!\\*)punct(\\*+)(?=[\\s]|$)|notPunctSpace(\\*+)(?!\\*)(?=punctSpace|$)|(?!\\*)punctSpace(\\*+)(?=notPunctSpace)|[\\s](\\*+)(?!\\*)(?=punct)|(?!\\*)punct(\\*+)(?!\\*)(?=punct)|notPunctSpace(\\*+)(?=notPunctSpace)`,Er=L(Tr,`gu`).replace(/notPunctSpace/g,gr).replace(/punctSpace/g,hr).replace(/punct/g,mr).getRegex(),Dr=L(Tr,`gu`).replace(/notPunctSpace/g,br).replace(/punctSpace/g,yr).replace(/punct/g,vr).getRegex(),Or=L(`^[^_*]*?\\*\\*[^_*]*?_[^_*]*?(?=\\*\\*)|[^_]+(?=[^_])|(?!_)punct(_+)(?=[\\s]|$)|notPunctSpace(_+)(?!_)(?=punctSpace|$)|(?!_)punctSpace(_+)(?=notPunctSpace)|[\\s](_+)(?!_)(?=punct)|(?!_)punct(_+)(?!_)(?=punct)`,`gu`).replace(/notPunctSpace/g,gr).replace(/punctSpace/g,hr).replace(/punct/g,mr).getRegex(),kr=L(/^~~?(?:((?!~)punct)|[^\s~])/,`u`).replace(/punct/g,mr).getRegex(),Ar=L(`^[^~]+(?=[^~])|(?!~)punct(~~?)(?=[\\s]|$)|notPunctSpace(~~?)(?!~)(?=punctSpace|$)|(?!~)punctSpace(~~?)(?=notPunctSpace)|[\\s](~~?)(?!~)(?=punct)|(?!~)punct(~~?)(?!~)(?=punct)|notPunctSpace(~~?)(?=notPunctSpace)`,`gu`).replace(/notPunctSpace/g,gr).replace(/punctSpace/g,hr).replace(/punct/g,mr).getRegex(),jr=L(/\\(punct)/,`gu`).replace(/punct/g,mr).getRegex(),Mr=L(/^<(scheme:[^\s\x00-\x1f<>]*|email)>/).replace(`scheme`,/[a-zA-Z][a-zA-Z0-9+.-]{1,31}/).replace(`email`,/[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+(@)[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+(?![-_])/).getRegex(),Nr=L(tr).replace(`(?:-->|$)`,`-->`).getRegex(),Pr=L(`^comment|^</[a-zA-Z][\\w:-]*\\s*>|^<[a-zA-Z][\\w-]*(?:attribute)*?\\s*/?>|^<\\?[\\s\\S]*?\\?>|^<![a-zA-Z]+\\s[\\s\\S]*?>|^<!\\[CDATA\\[[\\s\\S]*?\\]\\]>`).replace(`comment`,Nr).replace(`attribute`,/\s+[a-zA-Z:_][\w.:-]*(?:\s*=\s*"[^"]*"|\s*=\s*'[^']*'|\s*=\s*[^\s"'=<>`]+)?/).getRegex(),Fr=/(?:\[(?:\\[\s\S]|[^\[\]\\])*\]|\\[\s\S]|`+(?!`)[^`]*?`+(?!`)|``+(?=\])|[^\[\]\\`])*?/,Ir=L(/^!?\[(label)\]\(\s*(href)(?:(?:[ \t]+(?:\n[ \t]*)?|\n[ \t]*)(title))?\s*\)/).replace(`label`,Fr).replace(`href`,/<(?:\\.|[^\n<>\\])+>|[^ \t\n\x00-\x1f]+|(?=\))/).replace(`title`,/"(?:\\"?|[^"\\])*"|'(?:\\'?|[^'\\])*'|\((?:\\\)?|[^)\\])*\)/).getRegex(),Lr=L(/^!?\[(label)\]\[(ref)\]/).replace(`label`,Fr).replace(`ref`,Zn).getRegex(),Rr=L(/^!?\[(ref)\](?:\[\])?/).replace(`ref`,Zn).getRegex(),zr=L(`reflink|nolink(?!\\()`,`g`).replace(`reflink`,Lr).replace(`nolink`,Rr).getRegex(),Br=/[hH][tT][tT][pP][sS]?|[fF][tT][pP]/,Vr={_backpedal:In,anyPunctuation:jr,autolink:Mr,blockSkip:xr,br:fr,code:dr,del:In,delLDelim:In,delRDelim:In,emStrongLDelim:Cr,emStrongRDelimAst:Er,emStrongRDelimUnd:Or,escape:ur,link:Ir,nolink:Rr,punctuation:_r,reflink:Lr,reflinkSearch:zr,tag:Pr,text:pr,url:In},Hr={...Vr,link:L(/^!?\[(label)\]\((.*?)\)/).replace(`label`,Fr).getRegex(),reflink:L(/^!?\[(label)\]\s*\[([^\]]*)\]/).replace(`label`,Fr).getRegex()},Ur={...Vr,emStrongRDelimAst:Dr,emStrongLDelim:wr,delLDelim:kr,delRDelim:Ar,url:L(/^((?:protocol):\/\/|www\.)(?:[a-zA-Z0-9\-]+\.?)+[^\s<]*|^email/).replace(`protocol`,Br).replace(`email`,/[A-Za-z0-9._+-]+(@)[a-zA-Z0-9-_]+(?:\.[a-zA-Z0-9-_]*[a-zA-Z0-9])+(?![-_])/).getRegex(),_backpedal:/(?:[^?!.,:;*_'"~()&]+|\([^)]*\)|&(?![a-zA-Z0-9]+;$)|[?!.,:;*_'"~)]+(?!$))+/,del:/^(~~?)(?=[^\s~])((?:\\[\s\S]|[^\\])*?(?:\\[\s\S]|[^\s~\\]))\1(?=[^~]|$)/,text:L(/^(`+|~+|[^`~])(?:(?=[`~])|(?= {2,}\n)|(?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)|[\s\S]*?(?:(?=[\\<!\[`*~_]|\b_|protocol:\/\/|www\.|$)|[^ ](?= {2,}\n)|[^a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-](?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)))/).replace(`protocol`,Br).getRegex()},Wr={...Ur,br:L(fr).replace(`{2,}`,`*`).getRegex(),text:L(Ur.text).replace(`\\b_`,`\\b_| {2,}\\n`).replace(/\{2,\}/g,`*`).getRegex()},Gr={normal:or,gfm:cr,pedantic:lr},Kr={normal:Vr,gfm:Ur,breaks:Wr,pedantic:Hr},qr={"&":`&amp;`,"<":`&lt;`,">":`&gt;`,'"':`&quot;`,"'":`&#39;`},Jr=e=>qr[e];function Yr(e,t){if(t){if(zn.escapeTest.test(e))return e.replace(zn.escapeReplace,Jr)}else if(zn.escapeTestNoEncode.test(e))return e.replace(zn.escapeReplaceNoEncode,Jr);return e}function Xr(e){try{e=encodeURI(e).replace(zn.percentDecode,`%`)}catch{return null}return e}function Zr(e,t){let n=e.replace(zn.findPipe,(e,t,n)=>{let r=!1,i=t;for(;--i>=0&&n[i]===`\\`;)r=!r;return r?`|`:` |`}).split(zn.splitPipe),r=0;if(n[0].trim()||n.shift(),n.length>0&&!n.at(-1)?.trim()&&n.pop(),t)if(n.length>t)n.splice(t);else for(;n.length<t;)n.push(``);for(;r<n.length;r++)n[r]=n[r].trim().replace(zn.slashPipe,`|`);return n}function Qr(e,t,n){let r=e.length;if(r===0)return``;let i=0;for(;i<r;){let a=e.charAt(r-i-1);if(a===t&&!n)i++;else if(a!==t&&n)i++;else break}return e.slice(0,r-i)}function $r(e){let t=e.split(`
`),n=t.length-1;for(;n>=0&&zn.blankLine.test(t[n]);)n--;return t.length-n<=2?e:t.slice(0,n+1).join(`
`)}function ei(e,t){if(e.indexOf(t[1])===-1)return-1;let n=0;for(let r=0;r<e.length;r++)if(e[r]===`\\`)r++;else if(e[r]===t[0])n++;else if(e[r]===t[1]&&(n--,n<0))return r;return n>0?-2:-1}function ti(e,t=0){let n=t,r=``;for(let t of e)if(t===`	`){let e=4-n%4;r+=` `.repeat(e),n+=e}else r+=t,n++;return r}function ni(e,t,n,r,i){let a=t.href,o=t.title||null,s=e[1].replace(i.other.outputLinkReplace,`$1`);r.state.inLink=!0;let c={type:e[0].charAt(0)===`!`?`image`:`link`,raw:n,href:a,title:o,text:s,tokens:r.inlineTokens(s)};return r.state.inLink=!1,c}function ri(e,t,n){let r=e.match(n.other.indentCodeCompensation);if(r===null)return t;let i=r[1];return t.split(`
`).map(e=>{let t=e.match(n.other.beginningSpace);if(t===null)return e;let[r]=t;return r.length>=i.length?e.slice(i.length):e}).join(`
`)}var ii=class{options;rules;lexer;constructor(e){this.options=e||Pn}space(e){let t=this.rules.block.newline.exec(e);if(t&&t[0].length>0)return{type:`space`,raw:t[0]}}code(e){let t=this.rules.block.code.exec(e);if(t){let e=this.options.pedantic?t[0]:$r(t[0]);return{type:`code`,raw:e,codeBlockStyle:`indented`,text:e.replace(this.rules.other.codeRemoveIndent,``)}}}fences(e){let t=this.rules.block.fences.exec(e);if(t){let e=t[0],n=ri(e,t[3]||``,this.rules);return{type:`code`,raw:e,lang:t[2]?t[2].trim().replace(this.rules.inline.anyPunctuation,`$1`):t[2],text:n}}}heading(e){let t=this.rules.block.heading.exec(e);if(t){let e=t[2].trim();if(this.rules.other.endingHash.test(e)){let t=Qr(e,`#`);(this.options.pedantic||!t||this.rules.other.endingSpaceChar.test(t))&&(e=t.trim())}return{type:`heading`,raw:Qr(t[0],`
`),depth:t[1].length,text:e,tokens:this.lexer.inline(e)}}}hr(e){let t=this.rules.block.hr.exec(e);if(t)return{type:`hr`,raw:Qr(t[0],`
`)}}blockquote(e){let t=this.rules.block.blockquote.exec(e);if(t){let e=Qr(t[0],`
`).split(`
`),n=``,r=``,i=[];for(;e.length>0;){let t=!1,a=[],o;for(o=0;o<e.length;o++)if(this.rules.other.blockquoteStart.test(e[o]))a.push(e[o]),t=!0;else if(!t)a.push(e[o]);else break;e=e.slice(o);let s=a.join(`
`),c=s.replace(this.rules.other.blockquoteSetextReplace,`
    $1`).replace(this.rules.other.blockquoteSetextReplace2,``);n=n?`${n}
${s}`:s,r=r?`${r}
${c}`:c;let l=this.lexer.state.top;if(this.lexer.state.top=!0,this.lexer.blockTokens(c,i,!0),this.lexer.state.top=l,e.length===0)break;let u=i.at(-1);if(u?.type===`code`)break;if(u?.type===`blockquote`){let t=u,a=t.raw+`
`+e.join(`
`),o=this.blockquote(a);i[i.length-1]=o,n=n.substring(0,n.length-t.raw.length)+o.raw,r=r.substring(0,r.length-t.text.length)+o.text;break}else if(u?.type===`list`){let t=u,a=t.raw+`
`+e.join(`
`),o=this.list(a);i[i.length-1]=o,n=n.substring(0,n.length-u.raw.length)+o.raw,r=r.substring(0,r.length-t.raw.length)+o.raw,e=a.substring(i.at(-1).raw.length).split(`
`);continue}}return{type:`blockquote`,raw:n,tokens:i,text:r}}}list(e){let t=this.rules.block.list.exec(e);if(t){let n=t[1].trim(),r=n.length>1,i={type:`list`,raw:``,ordered:r,start:r?+n.slice(0,-1):``,loose:!1,items:[]};n=r?`\\d{1,9}\\${n.slice(-1)}`:`\\${n}`,this.options.pedantic&&(n=r?n:`[*+-]`);let a=this.rules.other.listItemRegex(n),o=!1;for(;e;){let n=!1,r=``,s=``;if(!(t=a.exec(e))||this.rules.block.hr.test(e))break;r=t[0],e=e.substring(r.length);let c=ti(t[2].split(`
`,1)[0],t[1].length),l=e.split(`
`,1)[0],u=!c.trim(),d=0;if(this.options.pedantic?(d=2,s=c.trimStart()):u?d=t[1].length+1:(d=c.search(this.rules.other.nonSpaceChar),d=d>4?1:d,s=c.slice(d),d+=t[1].length),u&&this.rules.other.blankLine.test(l)&&(r+=l+`
`,e=e.substring(l.length+1),n=!0),!n){let t=this.rules.other.nextBulletRegex(d),n=this.rules.other.hrRegex(d),i=this.rules.other.fencesBeginRegex(d),a=this.rules.other.headingBeginRegex(d),o=this.rules.other.htmlBeginRegex(d),f=this.rules.other.blockquoteBeginRegex(d);for(;e;){let p=e.split(`
`,1)[0],m;if(l=p,this.options.pedantic?(l=l.replace(this.rules.other.listReplaceNesting,`  `),m=l):m=l.replace(this.rules.other.tabCharGlobal,`    `),i.test(l)||a.test(l)||o.test(l)||f.test(l)||t.test(l)||n.test(l))break;if(m.search(this.rules.other.nonSpaceChar)>=d||!l.trim())s+=`
`+m.slice(d);else{if(u||c.replace(this.rules.other.tabCharGlobal,`    `).search(this.rules.other.nonSpaceChar)>=4||i.test(c)||a.test(c)||n.test(c))break;s+=`
`+l}u=!l.trim(),r+=p+`
`,e=e.substring(p.length+1),c=m.slice(d)}}i.loose||(o?i.loose=!0:this.rules.other.doubleBlankLine.test(r)&&(o=!0)),i.items.push({type:`list_item`,raw:r,task:!!this.options.gfm&&this.rules.other.listIsTask.test(s),loose:!1,text:s,tokens:[]}),i.raw+=r}let s=i.items.at(-1);if(s)s.raw=s.raw.trimEnd(),s.text=s.text.trimEnd();else return;i.raw=i.raw.trimEnd();for(let e of i.items){this.lexer.state.top=!1,e.tokens=this.lexer.blockTokens(e.text,[]);let t=e.tokens[0];if(e.task&&(t?.type===`text`||t?.type===`paragraph`)){e.text=e.text.replace(this.rules.other.listReplaceTask,``),t.raw=t.raw.replace(this.rules.other.listReplaceTask,``),t.text=t.text.replace(this.rules.other.listReplaceTask,``);for(let e=this.lexer.inlineQueue.length-1;e>=0;e--)if(this.rules.other.listIsTask.test(this.lexer.inlineQueue[e].src)){this.lexer.inlineQueue[e].src=this.lexer.inlineQueue[e].src.replace(this.rules.other.listReplaceTask,``);break}let n=this.rules.other.listTaskCheckbox.exec(e.raw);if(n){let t={type:`checkbox`,raw:n[0]+` `,checked:n[0]!==`[ ]`};e.checked=t.checked,i.loose?e.tokens[0]&&[`paragraph`,`text`].includes(e.tokens[0].type)&&`tokens`in e.tokens[0]&&e.tokens[0].tokens?(e.tokens[0].raw=t.raw+e.tokens[0].raw,e.tokens[0].text=t.raw+e.tokens[0].text,e.tokens[0].tokens.unshift(t)):e.tokens.unshift({type:`paragraph`,raw:t.raw,text:t.raw,tokens:[t]}):e.tokens.unshift(t)}}else e.task&&=!1;if(!i.loose){let t=e.tokens.filter(e=>e.type===`space`);i.loose=t.length>0&&t.some(e=>this.rules.other.anyLine.test(e.raw))}}if(i.loose)for(let e of i.items){e.loose=!0;for(let t of e.tokens)t.type===`text`&&(t.type=`paragraph`)}return i}}html(e){let t=this.rules.block.html.exec(e);if(t){let e=$r(t[0]);return{type:`html`,block:!0,raw:e,pre:t[1]===`pre`||t[1]===`script`||t[1]===`style`,text:e}}}def(e){let t=this.rules.block.def.exec(e);if(t){let e=t[1].toLowerCase().replace(this.rules.other.multipleSpaceGlobal,` `),n=t[2]?t[2].replace(this.rules.other.hrefBrackets,`$1`).replace(this.rules.inline.anyPunctuation,`$1`):``,r=t[3]?t[3].substring(1,t[3].length-1).replace(this.rules.inline.anyPunctuation,`$1`):t[3];return{type:`def`,tag:e,raw:Qr(t[0],`
`),href:n,title:r}}}table(e){let t=this.rules.block.table.exec(e);if(!t||!this.rules.other.tableDelimiter.test(t[2]))return;let n=Zr(t[1]),r=t[2].replace(this.rules.other.tableAlignChars,``).split(`|`),i=t[3]?.trim()?t[3].replace(this.rules.other.tableRowBlankLine,``).split(`
`):[],a={type:`table`,raw:Qr(t[0],`
`),header:[],align:[],rows:[]};if(n.length===r.length){for(let e of r)this.rules.other.tableAlignRight.test(e)?a.align.push(`right`):this.rules.other.tableAlignCenter.test(e)?a.align.push(`center`):this.rules.other.tableAlignLeft.test(e)?a.align.push(`left`):a.align.push(null);for(let e=0;e<n.length;e++)a.header.push({text:n[e],tokens:this.lexer.inline(n[e]),header:!0,align:a.align[e]});for(let e of i)a.rows.push(Zr(e,a.header.length).map((e,t)=>({text:e,tokens:this.lexer.inline(e),header:!1,align:a.align[t]})));return a}}lheading(e){let t=this.rules.block.lheading.exec(e);if(t){let e=t[1].trim();return{type:`heading`,raw:Qr(t[0],`
`),depth:t[2].charAt(0)===`=`?1:2,text:e,tokens:this.lexer.inline(e)}}}paragraph(e){let t=this.rules.block.paragraph.exec(e);if(t){let e=t[1].charAt(t[1].length-1)===`
`?t[1].slice(0,-1):t[1];return{type:`paragraph`,raw:t[0],text:e,tokens:this.lexer.inline(e)}}}text(e){let t=this.rules.block.text.exec(e);if(t)return{type:`text`,raw:t[0],text:t[0],tokens:this.lexer.inline(t[0])}}escape(e){let t=this.rules.inline.escape.exec(e);if(t)return{type:`escape`,raw:t[0],text:t[1]}}tag(e){let t=this.rules.inline.tag.exec(e);if(t)return!this.lexer.state.inLink&&this.rules.other.startATag.test(t[0])?this.lexer.state.inLink=!0:this.lexer.state.inLink&&this.rules.other.endATag.test(t[0])&&(this.lexer.state.inLink=!1),!this.lexer.state.inRawBlock&&this.rules.other.startPreScriptTag.test(t[0])?this.lexer.state.inRawBlock=!0:this.lexer.state.inRawBlock&&this.rules.other.endPreScriptTag.test(t[0])&&(this.lexer.state.inRawBlock=!1),{type:`html`,raw:t[0],inLink:this.lexer.state.inLink,inRawBlock:this.lexer.state.inRawBlock,block:!1,text:t[0]}}link(e){let t=this.rules.inline.link.exec(e);if(t){let e=t[2].trim();if(!this.options.pedantic&&this.rules.other.startAngleBracket.test(e)){if(!this.rules.other.endAngleBracket.test(e))return;let t=Qr(e.slice(0,-1),`\\`);if((e.length-t.length)%2==0)return}else{let e=ei(t[2],`()`);if(e===-2)return;if(e>-1){let n=(t[0].indexOf(`!`)===0?5:4)+t[1].length+e;t[2]=t[2].substring(0,e),t[0]=t[0].substring(0,n).trim(),t[3]=``}}let n=t[2],r=``;if(this.options.pedantic){let e=this.rules.other.pedanticHrefTitle.exec(n);e&&(n=e[1],r=e[3])}else r=t[3]?t[3].slice(1,-1):``;return n=n.trim(),this.rules.other.startAngleBracket.test(n)&&(n=this.options.pedantic&&!this.rules.other.endAngleBracket.test(e)?n.slice(1):n.slice(1,-1)),ni(t,{href:n&&n.replace(this.rules.inline.anyPunctuation,`$1`),title:r&&r.replace(this.rules.inline.anyPunctuation,`$1`)},t[0],this.lexer,this.rules)}}reflink(e,t){let n;if((n=this.rules.inline.reflink.exec(e))||(n=this.rules.inline.nolink.exec(e))){let e=t[(n[2]||n[1]).replace(this.rules.other.multipleSpaceGlobal,` `).toLowerCase()];if(!e){let e=n[0].charAt(0);return{type:`text`,raw:e,text:e}}return ni(n,e,n[0],this.lexer,this.rules)}}emStrong(e,t,n=``){let r=this.rules.inline.emStrongLDelim.exec(e);if(!(!r||!r[1]&&!r[2]&&!r[3]&&!r[4]||r[4]&&n.match(this.rules.other.unicodeAlphaNumeric))&&(!(r[1]||r[3])||!n||this.rules.inline.punctuation.exec(n))){let n=[...r[0]].length-1,i,a,o=n,s=0,c=r[0][0]===`*`?this.rules.inline.emStrongRDelimAst:this.rules.inline.emStrongRDelimUnd;for(c.lastIndex=0,t=t.slice(-1*e.length+n);(r=c.exec(t))!==null;){if(i=r[1]||r[2]||r[3]||r[4]||r[5]||r[6],!i)continue;if(a=[...i].length,r[3]||r[4]){o+=a;continue}else if((r[5]||r[6])&&n%3&&!((n+a)%3)){s+=a;continue}if(o-=a,o>0)continue;a=Math.min(a,a+o+s);let t=[...r[0]][0].length,c=e.slice(0,n+r.index+t+a);if(Math.min(n,a)%2){let e=c.slice(1,-1);return{type:`em`,raw:c,text:e,tokens:this.lexer.inlineTokens(e)}}let l=c.slice(2,-2);return{type:`strong`,raw:c,text:l,tokens:this.lexer.inlineTokens(l)}}}}codespan(e){let t=this.rules.inline.code.exec(e);if(t){let e=t[2].replace(this.rules.other.newLineCharGlobal,` `),n=this.rules.other.nonSpaceChar.test(e),r=this.rules.other.startingSpaceChar.test(e)&&this.rules.other.endingSpaceChar.test(e);return n&&r&&(e=e.substring(1,e.length-1)),{type:`codespan`,raw:t[0],text:e}}}br(e){let t=this.rules.inline.br.exec(e);if(t)return{type:`br`,raw:t[0]}}del(e,t,n=``){let r=this.rules.inline.delLDelim.exec(e);if(r&&(!r[1]||!n||this.rules.inline.punctuation.exec(n))){let n=[...r[0]].length-1,i,a,o=n,s=this.rules.inline.delRDelim;for(s.lastIndex=0,t=t.slice(-1*e.length+n);(r=s.exec(t))!==null;){if(i=r[1]||r[2]||r[3]||r[4]||r[5]||r[6],!i||(a=[...i].length,a!==n))continue;if(r[3]||r[4]){o+=a;continue}if(o-=a,o>0)continue;a=Math.min(a,a+o);let t=[...r[0]][0].length,s=e.slice(0,n+r.index+t+a),c=s.slice(n,-n);return{type:`del`,raw:s,text:c,tokens:this.lexer.inlineTokens(c)}}}}autolink(e){let t=this.rules.inline.autolink.exec(e);if(t){let e,n;return t[2]===`@`?(e=t[1],n=`mailto:`+e):(e=t[1],n=e),{type:`link`,raw:t[0],text:e,href:n,tokens:[{type:`text`,raw:e,text:e}]}}}url(e){let t;if(t=this.rules.inline.url.exec(e)){let e,n;if(t[2]===`@`)e=t[0],n=`mailto:`+e;else{let r;do r=t[0],t[0]=this.rules.inline._backpedal.exec(t[0])?.[0]??``;while(r!==t[0]);e=t[0],n=t[1]===`www.`?`http://`+t[0]:t[0]}return{type:`link`,raw:t[0],text:e,href:n,tokens:[{type:`text`,raw:e,text:e}]}}}inlineText(e){let t=this.rules.inline.text.exec(e);if(t){let e=this.lexer.state.inRawBlock;return{type:`text`,raw:t[0],text:t[0],escaped:e}}}},ai=class e{tokens;options;state;inlineQueue;tokenizer;constructor(e){this.tokens=[],this.tokens.links=Object.create(null),this.options=e||Pn,this.options.tokenizer=this.options.tokenizer||new ii,this.tokenizer=this.options.tokenizer,this.tokenizer.options=this.options,this.tokenizer.lexer=this,this.inlineQueue=[],this.state={inLink:!1,inRawBlock:!1,top:!0};let t={other:zn,block:Gr.normal,inline:Kr.normal};this.options.pedantic?(t.block=Gr.pedantic,t.inline=Kr.pedantic):this.options.gfm&&(t.block=Gr.gfm,this.options.breaks?t.inline=Kr.breaks:t.inline=Kr.gfm),this.tokenizer.rules=t}static get rules(){return{block:Gr,inline:Kr}}static lex(t,n){return new e(n).lex(t)}static lexInline(t,n){return new e(n).inlineTokens(t)}lex(e){e=e.replace(zn.carriageReturn,`
`),this.blockTokens(e,this.tokens);for(let e=0;e<this.inlineQueue.length;e++){let t=this.inlineQueue[e];this.inlineTokens(t.src,t.tokens)}return this.inlineQueue=[],this.tokens}blockTokens(e,t=[],n=!1){this.tokenizer.lexer=this,this.options.pedantic&&(e=e.replace(zn.tabCharGlobal,`    `).replace(zn.spaceLine,``));let r=1/0;for(;e;){if(e.length<r)r=e.length;else{this.infiniteLoopError(e.charCodeAt(0));break}let i;if(this.options.extensions?.block?.some(n=>(i=n.call({lexer:this},e,t))?(e=e.substring(i.raw.length),t.push(i),!0):!1))continue;if(i=this.tokenizer.space(e)){e=e.substring(i.raw.length);let n=t.at(-1);i.raw.length===1&&n!==void 0?n.raw+=`
`:t.push(i);continue}if(i=this.tokenizer.code(e)){e=e.substring(i.raw.length);let n=t.at(-1);n?.type===`paragraph`||n?.type===`text`?(n.raw+=(n.raw.endsWith(`
`)?``:`
`)+i.raw,n.text+=`
`+i.text,this.inlineQueue.at(-1).src=n.text):t.push(i);continue}if(i=this.tokenizer.fences(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.heading(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.hr(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.blockquote(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.list(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.html(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.def(e)){e=e.substring(i.raw.length);let n=t.at(-1);n?.type===`paragraph`||n?.type===`text`?(n.raw+=(n.raw.endsWith(`
`)?``:`
`)+i.raw,n.text+=`
`+i.raw,this.inlineQueue.at(-1).src=n.text):this.tokens.links[i.tag]||(this.tokens.links[i.tag]={href:i.href,title:i.title},t.push(i));continue}if(i=this.tokenizer.table(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.lheading(e)){e=e.substring(i.raw.length),t.push(i);continue}let a=e;if(this.options.extensions?.startBlock){let t=1/0,n=e.slice(1),r;this.options.extensions.startBlock.forEach(e=>{r=e.call({lexer:this},n),typeof r==`number`&&r>=0&&(t=Math.min(t,r))}),t<1/0&&t>=0&&(a=e.substring(0,t+1))}if(this.state.top&&(i=this.tokenizer.paragraph(a))){let r=t.at(-1);n&&r?.type===`paragraph`?(r.raw+=(r.raw.endsWith(`
`)?``:`
`)+i.raw,r.text+=`
`+i.text,this.inlineQueue.pop(),this.inlineQueue.at(-1).src=r.text):t.push(i),n=a.length!==e.length,e=e.substring(i.raw.length);continue}if(i=this.tokenizer.text(e)){e=e.substring(i.raw.length);let n=t.at(-1);n?.type===`text`?(n.raw+=(n.raw.endsWith(`
`)?``:`
`)+i.raw,n.text+=`
`+i.text,this.inlineQueue.pop(),this.inlineQueue.at(-1).src=n.text):t.push(i);continue}if(e){this.infiniteLoopError(e.charCodeAt(0));break}}return this.state.top=!0,t}inline(e,t=[]){return this.inlineQueue.push({src:e,tokens:t}),t}inlineTokens(e,t=[]){this.tokenizer.lexer=this;let n=e;if(this.tokens.links){let e=Object.keys(this.tokens.links);e.length>0&&(n=n.replace(this.tokenizer.rules.inline.reflinkSearch,t=>e.includes(t.slice(t.lastIndexOf(`[`)+1,-1))?`[`+`a`.repeat(t.length-2)+`]`:t))}n=n.replace(this.tokenizer.rules.inline.anyPunctuation,`++`),n=n.replace(this.tokenizer.rules.inline.blockSkip,(e,t,n)=>{let r=n?n.length:0;return e.slice(0,r)+`[`+`a`.repeat(e.length-r-2)+`]`}),n=this.options.hooks?.emStrongMask?.call({lexer:this},n)??n;let r=!1,i=``,a=1/0;for(;e;){if(e.length<a)a=e.length;else{this.infiniteLoopError(e.charCodeAt(0));break}r||(i=``),r=!1;let o;if(this.options.extensions?.inline?.some(n=>(o=n.call({lexer:this},e,t))?(e=e.substring(o.raw.length),t.push(o),!0):!1))continue;if(o=this.tokenizer.escape(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.tag(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.link(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.reflink(e,this.tokens.links)){e=e.substring(o.raw.length);let n=t.at(-1);o.type===`text`&&n?.type===`text`?(n.raw+=o.raw,n.text+=o.text):t.push(o);continue}if(o=this.tokenizer.emStrong(e,n,i)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.codespan(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.br(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.del(e,n,i)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.autolink(e)){e=e.substring(o.raw.length),t.push(o);continue}if(!this.state.inLink&&(o=this.tokenizer.url(e))){e=e.substring(o.raw.length),t.push(o);continue}let s=e;if(this.options.extensions?.startInline){let t=1/0,n=e.slice(1),r;this.options.extensions.startInline.forEach(e=>{r=e.call({lexer:this},n),typeof r==`number`&&r>=0&&(t=Math.min(t,r))}),t<1/0&&t>=0&&(s=e.substring(0,t+1))}if(o=this.tokenizer.inlineText(s)){e=e.substring(o.raw.length),o.raw.slice(-1)!==`_`&&(i=o.raw.slice(-1)),r=!0;let n=t.at(-1);n?.type===`text`?(n.raw+=o.raw,n.text+=o.text):t.push(o);continue}if(e){this.infiniteLoopError(e.charCodeAt(0));break}}return t}infiniteLoopError(e){let t=`Infinite loop on byte: `+e;if(this.options.silent)console.error(t);else throw Error(t)}},oi=class{options;parser;constructor(e){this.options=e||Pn}space(e){return``}code({text:e,lang:t,escaped:n}){let r=(t||``).match(zn.notSpaceStart)?.[0],i=e.replace(zn.endingNewline,``)+`
`;return r?`<pre><code class="language-`+Yr(r)+`">`+(n?i:Yr(i,!0))+`</code></pre>
`:`<pre><code>`+(n?i:Yr(i,!0))+`</code></pre>
`}blockquote({tokens:e}){return`<blockquote>
${this.parser.parse(e)}</blockquote>
`}html({text:e}){return e}def(e){return``}heading({tokens:e,depth:t}){return`<h${t}>${this.parser.parseInline(e)}</h${t}>
`}hr(e){return`<hr>
`}list(e){let t=e.ordered,n=e.start,r=``;for(let t=0;t<e.items.length;t++){let n=e.items[t];r+=this.listitem(n)}let i=t?`ol`:`ul`,a=t&&n!==1?` start="`+n+`"`:``;return`<`+i+a+`>
`+r+`</`+i+`>
`}listitem(e){return`<li>${this.parser.parse(e.tokens)}</li>
`}checkbox({checked:e}){return`<input `+(e?`checked="" `:``)+`disabled="" type="checkbox"> `}paragraph({tokens:e}){return`<p>${this.parser.parseInline(e)}</p>
`}table(e){let t=``,n=``;for(let t=0;t<e.header.length;t++)n+=this.tablecell(e.header[t]);t+=this.tablerow({text:n});let r=``;for(let t=0;t<e.rows.length;t++){let i=e.rows[t];n=``;for(let e=0;e<i.length;e++)n+=this.tablecell(i[e]);r+=this.tablerow({text:n})}return r&&=`<tbody>${r}</tbody>`,`<table>
<thead>
`+t+`</thead>
`+r+`</table>
`}tablerow({text:e}){return`<tr>
${e}</tr>
`}tablecell(e){let t=this.parser.parseInline(e.tokens),n=e.header?`th`:`td`;return(e.align?`<${n} align="${e.align}">`:`<${n}>`)+t+`</${n}>
`}strong({tokens:e}){return`<strong>${this.parser.parseInline(e)}</strong>`}em({tokens:e}){return`<em>${this.parser.parseInline(e)}</em>`}codespan({text:e}){return`<code>${Yr(e,!0)}</code>`}br(e){return`<br>`}del({tokens:e}){return`<del>${this.parser.parseInline(e)}</del>`}link({href:e,title:t,tokens:n}){let r=this.parser.parseInline(n),i=Xr(e);if(i===null)return r;e=i;let a=`<a href="`+e+`"`;return t&&(a+=` title="`+Yr(t)+`"`),a+=`>`+r+`</a>`,a}image({href:e,title:t,text:n,tokens:r}){r&&(n=this.parser.parseInline(r,this.parser.textRenderer));let i=Xr(e);if(i===null)return Yr(n);e=i;let a=`<img src="${e}" alt="${Yr(n)}"`;return t&&(a+=` title="${Yr(t)}"`),a+=`>`,a}text(e){return`tokens`in e&&e.tokens?this.parser.parseInline(e.tokens):`escaped`in e&&e.escaped?e.text:Yr(e.text)}},si=class{strong({text:e}){return e}em({text:e}){return e}codespan({text:e}){return e}del({text:e}){return e}html({text:e}){return e}text({text:e}){return e}link({text:e}){return``+e}image({text:e}){return``+e}br(){return``}checkbox({raw:e}){return e}},ci=class e{options;renderer;textRenderer;constructor(e){this.options=e||Pn,this.options.renderer=this.options.renderer||new oi,this.renderer=this.options.renderer,this.renderer.options=this.options,this.renderer.parser=this,this.textRenderer=new si}static parse(t,n){return new e(n).parse(t)}static parseInline(t,n){return new e(n).parseInline(t)}parse(e){this.renderer.parser=this;let t=``;for(let n=0;n<e.length;n++){let r=e[n];if(this.options.extensions?.renderers?.[r.type]){let e=r,n=this.options.extensions.renderers[e.type].call({parser:this},e);if(n!==!1||![`space`,`hr`,`heading`,`code`,`table`,`blockquote`,`list`,`html`,`def`,`paragraph`,`text`].includes(e.type)){t+=n||``;continue}}let i=r;switch(i.type){case`space`:t+=this.renderer.space(i);break;case`hr`:t+=this.renderer.hr(i);break;case`heading`:t+=this.renderer.heading(i);break;case`code`:t+=this.renderer.code(i);break;case`table`:t+=this.renderer.table(i);break;case`blockquote`:t+=this.renderer.blockquote(i);break;case`list`:t+=this.renderer.list(i);break;case`checkbox`:t+=this.renderer.checkbox(i);break;case`html`:t+=this.renderer.html(i);break;case`def`:t+=this.renderer.def(i);break;case`paragraph`:t+=this.renderer.paragraph(i);break;case`text`:t+=this.renderer.text(i);break;default:{let e=`Token with "`+i.type+`" type was not found.`;if(this.options.silent)return console.error(e),``;throw Error(e)}}}return t}parseInline(e,t=this.renderer){this.renderer.parser=this;let n=``;for(let r=0;r<e.length;r++){let i=e[r];if(this.options.extensions?.renderers?.[i.type]){let e=this.options.extensions.renderers[i.type].call({parser:this},i);if(e!==!1||![`escape`,`html`,`link`,`image`,`strong`,`em`,`codespan`,`br`,`del`,`text`].includes(i.type)){n+=e||``;continue}}let a=i;switch(a.type){case`escape`:n+=t.text(a);break;case`html`:n+=t.html(a);break;case`link`:n+=t.link(a);break;case`image`:n+=t.image(a);break;case`checkbox`:n+=t.checkbox(a);break;case`strong`:n+=t.strong(a);break;case`em`:n+=t.em(a);break;case`codespan`:n+=t.codespan(a);break;case`br`:n+=t.br(a);break;case`del`:n+=t.del(a);break;case`text`:n+=t.text(a);break;default:{let e=`Token with "`+a.type+`" type was not found.`;if(this.options.silent)return console.error(e),``;throw Error(e)}}}return n}},li=class{options;block;constructor(e){this.options=e||Pn}static passThroughHooks=new Set([`preprocess`,`postprocess`,`processAllTokens`,`emStrongMask`]);static passThroughHooksRespectAsync=new Set([`preprocess`,`postprocess`,`processAllTokens`]);preprocess(e){return e}postprocess(e){return e}processAllTokens(e){return e}emStrongMask(e){return e}provideLexer(e=this.block){return e?ai.lex:ai.lexInline}provideParser(e=this.block){return e?ci.parse:ci.parseInline}},ui=class{defaults=Nn();options=this.setOptions;parse=this.parseMarkdown(!0);parseInline=this.parseMarkdown(!1);Parser=ci;Renderer=oi;TextRenderer=si;Lexer=ai;Tokenizer=ii;Hooks=li;constructor(...e){this.use(...e)}walkTokens(e,t){let n=[];for(let r of e)switch(n=n.concat(t.call(this,r)),r.type){case`table`:{let e=r;for(let r of e.header)n=n.concat(this.walkTokens(r.tokens,t));for(let r of e.rows)for(let e of r)n=n.concat(this.walkTokens(e.tokens,t));break}case`list`:{let e=r;n=n.concat(this.walkTokens(e.items,t));break}default:{let e=r;this.defaults.extensions?.childTokens?.[e.type]?this.defaults.extensions.childTokens[e.type].forEach(r=>{let i=e[r].flat(1/0);n=n.concat(this.walkTokens(i,t))}):e.tokens&&(n=n.concat(this.walkTokens(e.tokens,t)))}}return n}use(...e){let t=this.defaults.extensions||{renderers:{},childTokens:{}};return e.forEach(e=>{let n={...e};if(n.async=this.defaults.async||n.async||!1,e.extensions&&(e.extensions.forEach(e=>{if(!e.name)throw Error(`extension name required`);if(`renderer`in e){let n=t.renderers[e.name];n?t.renderers[e.name]=function(...t){let r=e.renderer.apply(this,t);return r===!1&&(r=n.apply(this,t)),r}:t.renderers[e.name]=e.renderer}if(`tokenizer`in e){if(!e.level||e.level!==`block`&&e.level!==`inline`)throw Error(`extension level must be 'block' or 'inline'`);let n=t[e.level];n?n.unshift(e.tokenizer):t[e.level]=[e.tokenizer],e.start&&(e.level===`block`?t.startBlock?t.startBlock.push(e.start):t.startBlock=[e.start]:e.level===`inline`&&(t.startInline?t.startInline.push(e.start):t.startInline=[e.start]))}`childTokens`in e&&e.childTokens&&(t.childTokens[e.name]=e.childTokens)}),n.extensions=t),e.renderer){let t=this.defaults.renderer||new oi(this.defaults);for(let n in e.renderer){if(!(n in t))throw Error(`renderer '${n}' does not exist`);if([`options`,`parser`].includes(n))continue;let r=n,i=e.renderer[r],a=t[r];t[r]=(...e)=>{let n=i.apply(t,e);return n===!1&&(n=a.apply(t,e)),n||``}}n.renderer=t}if(e.tokenizer){let t=this.defaults.tokenizer||new ii(this.defaults);for(let n in e.tokenizer){if(!(n in t))throw Error(`tokenizer '${n}' does not exist`);if([`options`,`rules`,`lexer`].includes(n))continue;let r=n,i=e.tokenizer[r],a=t[r];t[r]=(...e)=>{let n=i.apply(t,e);return n===!1&&(n=a.apply(t,e)),n}}n.tokenizer=t}if(e.hooks){let t=this.defaults.hooks||new li;for(let n in e.hooks){if(!(n in t))throw Error(`hook '${n}' does not exist`);if([`options`,`block`].includes(n))continue;let r=n,i=e.hooks[r],a=t[r];li.passThroughHooks.has(n)?t[r]=e=>{if(this.defaults.async&&li.passThroughHooksRespectAsync.has(n))return(async()=>{let n=await i.call(t,e);return a.call(t,n)})();let r=i.call(t,e);return a.call(t,r)}:t[r]=(...e)=>{if(this.defaults.async)return(async()=>{let n=await i.apply(t,e);return n===!1&&(n=await a.apply(t,e)),n})();let n=i.apply(t,e);return n===!1&&(n=a.apply(t,e)),n}}n.hooks=t}if(e.walkTokens){let t=this.defaults.walkTokens,r=e.walkTokens;n.walkTokens=function(e){let n=[];return n.push(r.call(this,e)),t&&(n=n.concat(t.call(this,e))),n}}this.defaults={...this.defaults,...n}}),this}setOptions(e){return this.defaults={...this.defaults,...e},this}lexer(e,t){return ai.lex(e,t??this.defaults)}parser(e,t){return ci.parse(e,t??this.defaults)}parseMarkdown(e){return(t,n)=>{let r={...n},i={...this.defaults,...r},a=this.onError(!!i.silent,!!i.async);if(this.defaults.async===!0&&r.async===!1)return a(Error(`marked(): The async option was set to true by an extension. Remove async: false from the parse options object to return a Promise.`));if(typeof t>`u`||t===null)return a(Error(`marked(): input parameter is undefined or null`));if(typeof t!=`string`)return a(Error(`marked(): input parameter is of type `+Object.prototype.toString.call(t)+`, string expected`));if(i.hooks&&(i.hooks.options=i,i.hooks.block=e),i.async)return(async()=>{let n=i.hooks?await i.hooks.preprocess(t):t,r=await(i.hooks?await i.hooks.provideLexer(e):e?ai.lex:ai.lexInline)(n,i),a=i.hooks?await i.hooks.processAllTokens(r):r;i.walkTokens&&await Promise.all(this.walkTokens(a,i.walkTokens));let o=await(i.hooks?await i.hooks.provideParser(e):e?ci.parse:ci.parseInline)(a,i);return i.hooks?await i.hooks.postprocess(o):o})().catch(a);try{i.hooks&&(t=i.hooks.preprocess(t));let n=(i.hooks?i.hooks.provideLexer(e):e?ai.lex:ai.lexInline)(t,i);i.hooks&&(n=i.hooks.processAllTokens(n)),i.walkTokens&&this.walkTokens(n,i.walkTokens);let r=(i.hooks?i.hooks.provideParser(e):e?ci.parse:ci.parseInline)(n,i);return i.hooks&&(r=i.hooks.postprocess(r)),r}catch(e){return a(e)}}}onError(e,t){return n=>{if(n.message+=`
Please report this to https://github.com/markedjs/marked.`,e){let e=`<p>An error occurred:</p><pre>`+Yr(n.message+``,!0)+`</pre>`;return t?Promise.resolve(e):e}if(t)return Promise.reject(n);throw n}}},di=new ui;function R(e,t){return di.parse(e,t)}R.options=R.setOptions=function(e){return di.setOptions(e),R.defaults=di.defaults,Fn(R.defaults),R},R.getDefaults=Nn,R.defaults=Pn,R.use=function(...e){return di.use(...e),R.defaults=di.defaults,Fn(R.defaults),R},R.walkTokens=function(e,t){return di.walkTokens(e,t)},R.parseInline=di.parseInline,R.Parser=ci,R.parser=ci.parse,R.Renderer=oi,R.TextRenderer=si,R.Lexer=ai,R.lexer=ai.lex,R.Tokenizer=ii,R.Hooks=li,R.parse=R,R.options,R.setOptions,R.use,R.walkTokens,R.parseInline,ci.parse,ai.lex,en.registerLanguage(`bash`,tn),en.registerLanguage(`css`,dn),en.registerLanguage(`dos`,fn),en.registerLanguage(`java`,vn),en.registerLanguage(`javascript`,Dn),en.registerLanguage(`json`,On),en.registerLanguage(`powershell`,kn),en.registerLanguage(`python`,An),en.registerLanguage(`shell`,jn),en.registerLanguage(`xml`,Mn);var fi=e=>e.toLowerCase().replace(/[`*_~]/g,``).replace(/[^\p{L}\p{N}]+/gu,`-`).replace(/^-+|-+$/g,``),pi=new ui({gfm:!0}),mi=e=>e.replace(/[&<>"]/g,e=>({"&":`&amp;`,"<":`&lt;`,">":`&gt;`,'"':`&quot;`})[e]),hi={bash:`Bash`,sh:`Shell`,shell:`Shell`,batch:`Batch`,bat:`Batch`,cmd:`Command Prompt`,powershell:`PowerShell`,ps1:`PowerShell`,python:`Python`,py:`Python`,java:`Java`,javascript:`JavaScript`,js:`JavaScript`,jsx:`JSX`,html:`HTML`,json:`JSON`,css:`CSS`,yaml:`YAML`,ini:`INI`,text:`Text`,plaintext:`Text`},gi=(e,t)=>en.getLanguage(t)?en.highlight(e,{language:t,ignoreIllegals:!0}).value:e,_i={bash:`bash`,sh:`shell`,shell:`shell`,batch:`dos`,bat:`dos`,cmd:`dos`,powershell:`powershell`,ps1:`powershell`,python:`python`,py:`python`,java:`java`,javascript:`javascript`,js:`javascript`,jsx:`javascript`,html:`xml`,json:`json`},vi=(e,t)=>{if(!t||!en.getLanguage(t))return mi(e);try{return en.highlight(e,{language:t,ignoreIllegals:!0}).value}catch{return mi(e)}},yi=/^```output[ \t]+@?([^\n]+?)[ \t]*\n([\s\S]*?)\n?^```[ \t]*$/gm,bi=/^```demo[ \t]+@?([^\n]+?)[ \t]*\n([\s\S]*?)\n?^```[ \t]*$/gm,xi=new Map,Si=new Map;pi.use({renderer:{heading({tokens:e,depth:t,text:n}){let r=this.parser.parseInline(e);return`<h${t} id="${fi(n)}">${r}</h${t}>\n`},code({text:e,lang:t}){let[n=``,...r]=(t??``).trim().split(/\s+/),i=n.toLowerCase();if(i===`expected`)return`<pre class="code-expected">${mi(e)}</pre>\n`;if(i===`pseudo`)return`<pre class="code-pseudo" data-trace="Pseudocode">${mi(e)}</pre>\n`;let a=hi[i]??i,o=i?` class="language-${mi(i)} hljs"`:` class="hljs"`,s=r.find(e=>!e.startsWith(`@`))??``,c=r.find(e=>e.startsWith(`@`)),l=c?c.slice(1):s,u=l?xi.get(l):void 0,d=s?Si.get(s):void 0;return`<pre${[a?` data-lang="${mi(a)}"`:``,s?` data-file="${mi(s)}"`:``,u===void 0?``:` data-output="${mi(u)}"`,d===void 0?``:` data-demo="${mi(d)}"`].join(``)}><code${o}>${vi(e,_i[i])}</code></pre>\n`},link({href:e,title:t,tokens:n}){let r=this.parser.parseInline(n),i=t?` title="${mi(t)}"`:``,a=/^[a-z]+:/i.test(e)?` target="_blank" rel="noreferrer"`:``;return`<a href="${mi(e)}"${i}${a}>${r}</a>`}}});var Ci=e=>{xi=new Map,Si=new Map;let t=e.replace(yi,(e,t,n)=>(xi.set(t.trim(),n),``)).replace(bi,(e,t,n)=>(Si.set(t.trim(),n),``));return pi.parse(t)};function wi(e){let t=[];for(let n of pi.lexer(e))n.type===`heading`&&n.depth===2&&t.push({id:fi(n.text),text:n.text.replace(/[`*_~]/g,``).trim()});return t}function Ti({headings:e}){let t=Xe((0,_.useMemo)(()=>e.map(e=>e.id),[e])),n=(0,_.useRef)(null);(0,_.useEffect)(()=>{let e=n.current,t=e?.querySelector(`[data-active="true"]`);if(!t||e.scrollWidth<=e.clientWidth)return;let r=e.getBoundingClientRect(),i=t.getBoundingClientRect(),a=e.scrollLeft;if(i.left<r.left+24)a-=r.left+24-i.left;else if(i.right>r.right-24)a+=i.right-(r.right-24);else return;e.scrollTo({left:a,behavior:`smooth`})},[t]);let r=(e,t)=>{e.preventDefault(),document.getElementById(t)?.scrollIntoView({behavior:`smooth`,block:`start`})};return(0,P.jsxs)(`aside`,{className:`article-toc`,"aria-label":`On this page`,children:[(0,P.jsx)(`h2`,{children:`On this page`}),(0,P.jsx)(`ol`,{ref:n,children:e.map(e=>(0,P.jsx)(`li`,{children:(0,P.jsx)(`a`,{href:`#${e.id}`,onClick:t=>r(t,e.id),"data-active":e.id===t,"aria-current":e.id===t?`true`:void 0,children:e.text})},e.id))})]})}var Ei={JVM:`The Java Virtual Machine — the program that actually runs your compiled Java. It reads bytecode, manages memory for you, and exists in a version for every operating system.`,JDK:`Java Development Kit — what you install to write Java. It contains everything needed to run Java, plus the compiler and other tools.`,JRE:`Java Runtime Environment — enough to run a Java program, but not to compile one. It has no compiler.`,bytecode:`What the Java compiler produces instead of machine code: instructions for an imagined computer that the JVM understands. The same bytecode file runs on any machine that has a JVM.`,"machine code":`Instructions a real processor executes directly. Specific to one kind of chip, which is why a program compiled to machine code on one platform will not run on another.`,compiler:"A program that turns source code you wrote into something a machine can run. In Java that is `javac`, and what comes out is bytecode.",interpreter:`A program that reads instructions one at a time and performs them, rather than translating them all up front. Quick to start, slower to run.`,JIT:`Just-In-Time compiler. Part of the JVM that watches which methods run most often and translates those into fast machine code while the program is running.`,"class loader":`The part of the JVM that finds a class and reads it into memory. There is a chain of them, and each asks its parent before looking itself.`,classpath:`The list of folders and jar files the JVM searches when it needs to find a class. First match wins.`,jar:`A zip file containing compiled Java classes, plus a small description of what is inside. The normal way Java code is shipped.`,"constant pool":`A table inside every compiled class file holding its strings, numbers, and the names of everything it refers to. Method calls are stored as names, not addresses, and resolved when first used.`,bootstrap:`The very first step of a process — here, the built-in class loader that loads the JDK itself, before anything else exists to do it.`,deoptimisation:`When the JVM throws away fast machine code it had produced because an assumption behind it turned out to be wrong, and falls back to interpreting until it can try again.`,inlining:`Replacing a call to a small method with the body of that method, so there is no call at all. The single most valuable thing an optimiser does, because it makes other optimisations possible.`,invokedynamic:`A bytecode instruction that says "work out what to call the first time this runs, then remember it". Lambdas and string joining both use it.`,lambda:"A short function written inline, without giving it a name or a class — `x -> x * 2`. Java compiles it into a class at run time rather than at compile time.",reflection:`Code inspecting or calling other code by name at run time, rather than being wired up at compile time. Frameworks use it heavily; it is also why some of them need extra permissions on newer Java.`,module:`A named group of packages that declares what it needs and what it lets others see. Stricter than the classpath, and what makes it possible to ship a trimmed-down Java runtime.`,heap:`The shared pool of memory where every object lives. The garbage collector works here.`,stack:`A per-thread scratch area holding local variables and the trail of method calls currently in progress. Each call pushes a frame; returning pops it.`,frame:`One slot on the stack, created for a single method call, holding its local variables. It disappears when the method returns.`,reference:`A value that says where an object is, rather than holding the object itself. Copying a reference does not copy the object.`,primitive:"One of Java’s built-in simple values — `int`, `double`, `boolean` and friends. Stored directly rather than through a reference.",object:"A piece of data created with `new`, living on the heap, reached through a reference.",immutable:`Cannot be changed after it is created. Any method that looks like it changes the value actually returns a new one.`,"String Pool":`A store the JVM keeps of text written directly in your source, so the same words are stored once rather than many times.`,intern:`To swap a piece of text for the shared copy in the String Pool, so identical text becomes literally the same object.`,autoboxing:"Java quietly converting between a simple value like `int` and its object wrapper `Integer`. Convenient, and the source of several classic bugs.",metaspace:`Memory holding the JVM’s description of your classes. It sits outside the heap, so it can run out while the heap is nearly empty.`,TLAB:`Thread-Local Allocation Buffer — a private slice of memory each thread gets, so creating an object needs no coordination with other threads.`,"escape analysis":`The optimiser working out that an object never leaves the method that made it, which sometimes means it need not be created at all.`,"compressed oops":`Storing references as 4 bytes instead of 8 by treating them as offsets. Works only below a 32 GB heap.`,"native memory":"Memory the operating system gives the process directly, outside the Java heap. Thread stacks and some buffers live here, and `-Xmx` does not limit it.","garbage collection":`The JVM automatically reclaiming memory used by objects your program can no longer reach, so you never free memory by hand.`,"garbage collector":`The part of the JVM that finds objects nothing can reach any more and takes their memory back.`,reachability:`Whether an object can still be found by following references from somewhere the program is definitely using. If it cannot, it is garbage.`,"GC root":`A starting point the collector trusts as definitely in use: local variables of running methods, static fields, live threads.`,generation:`An age group in the heap. New objects go in the young generation; ones that survive long enough are moved to the old generation.`,Eden:`The part of the heap where brand-new objects are placed. Named for the garden — everything starts here.`,"survivor space":`A holding area for objects that made it through one collection but are not old enough to be moved to the old generation.`,promotion:`Moving an object from the young part of the heap to the old part, because it has survived enough collections to look long-lived.`,"stop-the-world":`A moment when the JVM pauses every thread in your program so the collector can work on memory that is not changing underneath it.`,safepoint:`A place in running code where a thread can be safely stopped. Pauses cannot begin until every thread has reached one.`,"write barrier":`A few extra instructions the JVM runs after your program stores a reference, so the collector can keep track of what points where.`,region:`A fixed-size block the heap is divided into by the G1 collector. Each one is used for a purpose, and that purpose can change over its life.`,humongous:`An object too big to fit in the normal way — over half a region — so it gets a run of regions to itself. Too many of them fragment the heap.`,"heap dump":`A snapshot file of everything in the heap at one moment, which you open in a tool to find out what was holding memory.`,"memory leak":`In Java, an object still reachable but no longer needed. The collector correctly refuses to reclaim it, and the heap slowly fills.`,thread:`One sequence of work running on its own, so a program can do several things at once. Each thread has its own stack.`,"platform thread":`An ordinary Java thread, which is a thin wrapper over a thread the operating system provides. Expensive enough that programs pool them.`,"virtual thread":`A thread managed by the JVM rather than the operating system. Cheap enough to create millions, so waiting no longer wastes an expensive resource.`,"carrier thread":`A real operating-system thread that virtual threads take turns running on. There are only a handful, usually one per processor core.`,blocking:`Waiting for something to finish — a network reply, a file, a lock — while doing nothing.`,concurrency:`Several pieces of work in progress at the same time. Not the same as doing them simultaneously; mostly it means interleaving them.`,pool:`A set of expensive things kept and reused instead of created and thrown away — threads, database connections.`,pinning:`When a virtual thread cannot be moved off the real thread it is running on, so waiting occupies that real thread anyway and the benefit is lost.`,continuation:`A saved copy of where a piece of work had got to, so it can be paused and picked up again later — possibly somewhere else.`,"back-pressure":`Slowing down work coming in when the thing it depends on cannot keep up, instead of piling requests up until something breaks.`,Semaphore:`A counter used to limit how many things may do something at once. Take a permit before, give it back after.`,ThreadLocal:`A value attached to one thread, so each thread sees its own. Awkward when there are a million threads instead of a pooled few.`,"race condition":`A bug where the result depends on which thread happened to get there first.`,"structured concurrency":`Treating a group of tasks started together as one unit that succeeds, fails and is cancelled together, matching the shape of the code that started them.`,class:`A definition: what fields something has and what it can do. Nothing exists until you make an object from it.`,constructor:`A special block that runs when an object is created, to set its starting values. Same name as the class, and no return type.`,method:`A named block of code you can call, optionally taking values in and giving one back.`,field:`A variable that belongs to an object or to a class, rather than to one method.`,parameter:`A variable in a method’s declaration, standing for whatever the caller passes in. The value actually passed is the argument.`,variable:`A name for a value you can read, and usually change.`,literal:'A value written directly in the source — `42`, `"hello"`, `true` — rather than computed.',syntax:`The rules for how code must be written down. Break them and the compiler stops you before anything runs.`,"compile-time":`While the compiler is turning your source into bytecode, before the program runs. Errors caught here are the cheap ones.`,runtime:`While the program is actually running.`,instance:`One particular object made from a class.`,static:`Belonging to the class rather than to any object made from it. One copy, shared by everything.`,encapsulation:`Keeping fields private and letting the outside world touch them only through methods, so you can check what is being set and change the internals later.`,inheritance:"A class taking on the fields and methods of another, so shared behaviour is written once. `extends` is the keyword.",superclass:`The class being extended — the parent.`,subclass:`The class doing the extending — the child.`,overriding:`A subclass replacing an inherited method with its own version. Which one runs is decided from the actual object, while the program is running.`,overloading:`Several methods in one class sharing a name but taking different parameters. Which one runs is decided by the compiler.`,interface:`A list of things a type must be able to do, with no instance data of its own. A class can implement many of them.`,abstract:`Declared but not finished. An abstract class cannot be made into an object, and an abstract method has no body — a subclass must supply it.`,enum:`A type whose values are a fixed, named list — the days of the week, the states of an order.`,annotation:`A note attached to code that does nothing by itself. Something else — the compiler, a framework, your own code — has to read it.`,constant:`A value that is fixed once set and never changes afterwards.`,scope:`The region of code where a name means something. Outside it, the name does not exist.`,argument:`The actual value handed to a method when you call it.`,"return type":"The kind of value a method hands back. `void` means it hands back nothing.",exception:`An object describing something that went wrong, thrown at the point of failure and caught somewhere that can deal with it.`,"stack trace":`The list of method calls that were in progress when something failed, innermost first. It tells you how the program got there.`,cast:`Telling the compiler to treat a value as a different type. Some casts are safe; some can fail at run time.`,API:`The set of things a library or class offers you to call. The surface you use without needing to read the inside.`,record:`A short way of declaring a class whose only job is to carry a few values. Java writes the constructor, the accessors and the equality checks for you.`,sealed:`A type that names exactly which other types are allowed to extend or implement it, so the compiler knows the full list.`,exhaustive:"Covering every possible case. An exhaustive `switch` needs no fallback branch, because nothing can be left over.","pattern matching":`Checking what shape a value has and pulling its pieces out in one step, rather than testing the type and then casting.`,erasure:"Generic type information being discarded at compile time, so `List<String>` and `List<Integer>` are the same type at run time.","checked exception":`An error the compiler forces you to either handle or declare. Meant for things a caller could reasonably recover from.`,polymorphism:`Calling the same method on different types and getting each type’s own behaviour, decided by the actual object rather than the declared type.`,"preview feature":`A finished-looking Java feature that is deliberately not final yet. It needs a flag to use, and its shape can still change between releases.`,LTS:`Long-Term Support — a Java release that keeps getting security and bug fixes for years, rather than being superseded in six months.`,volatile:`A marker on a field promising that a value written by one thread becomes visible to others, and stopping the compiler reordering around it.`,"hash code":`A number derived from an object’s contents, used to decide which bucket it goes in inside a map or set. Equal objects must produce equal numbers.`,cgroup:`The Linux mechanism a container uses to cap how much memory and CPU a process may use. Modern JVMs read it instead of the machine’s totals.`,idempotent:`Doing something twice leaves things exactly as doing it once did. It is what decides whether a failed attempt can safely be tried again.`,"round trip":`One message out and its answer back. The time it takes is set by distance and cannot be reduced by making either machine faster.`,ETag:`A short label a server attaches to a version of something, so a client can later ask "is it still this one?" instead of downloading it again.`,"keep-alive":`Leaving a connection open after an answer arrives, so the next question can skip the cost of setting one up.`,QUIC:`A newer way of carrying web traffic in which each request is delivered separately, so one lost packet does not hold up the others.`,handshake:`The opening exchange in which two machines agree how they will talk, and prove they are who they claim, before any real data moves.`,certificate:`A file holding a public key, the names it may be used for, and a signature from someone else vouching that the pairing is genuine.`,"certificate authority":`An organisation whose signature machines are willing to believe. It signs only after checking that the applicant controls the name involved.`,"forward secrecy":`Traffic recorded today stays unreadable even if the long-term key is stolen tomorrow, because what encrypted it was thrown away afterwards.`,"cipher suite":`The named set of algorithms two ends settle on — how they agree a key, how they scramble the bytes, and how they detect tampering.`,truststore:`The list of signers a program is willing to believe. Anything presented to it that does not lead back to one of them is refused.`,client:`Whoever is doing the asking — a browser, a phone app, another program. It holds no authority; it only sends questions and waits.`,server:`A machine that stays running and answers whoever asks. Nothing about it is special except that it is always there.`,request:`One question sent to a server. It travels alone and carries everything needed to answer it.`,response:`The answer sent back, and the only thing the asker ever sees of what happened.`,backend:`The part nobody sees: the code on the server that decides what the answer is.`,frontend:`The part in front of the user — what is drawn, tapped and typed into.`,database:`The program that keeps data on disk safely, so it survives a restart, a crash, and the machine being switched off.`,SQL:`The language database questions are written in. It describes what you want rather than how to go and get it.`,transaction:`A group of changes that must all happen or none of them happen. Money leaving one account and arriving in another is one transaction, never two.`,rollback:`Undoing a transaction entirely, as though it had never started.`,queue:`A waiting line: things join at one end and are taken from the other, in the order they arrived.`,deploy:`Putting new code onto the machines that are serving real traffic.`,production:`The live system real people are using right now. Everything else is a rehearsal.`,staging:`A practice copy of production, kept for finding out what a change does before it matters.`,downtime:`Time during which the system is not answering. The number everything else is measured against.`,monitoring:`Watching numbers coming out of a live system continuously, rather than looking after something breaks.`,metric:`One such number — error rate, latency, how full something is.`,alert:`A message sent automatically when a metric crosses a line somebody drew in advance.`,"vertical scaling":`Making one machine bigger. Simple, and it runs out.`,"horizontal scaling":`Adding more machines. It does not run out, and it makes every shared thing behind them a new problem.`,"cache hit":`The answer was already in the cache, so the slow original was never asked.`,"cache miss":`It was not there, so the original had to be asked after all — and the answer kept for next time.`,latency:`How long one request takes from asking to answered. It is a duration, not a rate — a system can be fast and still handle very little.`,throughput:`How much work gets finished per second. Independent of latency: a slow queue that is very wide still moves a lot.`,RPS:`Requests per second — the rate traffic arrives at. The number every capacity calculation starts from.`,p99:`The figure 99 out of 100 requests came in under. It describes your worst-served users, which is exactly what an average hides.`,saturation:`How full a limited resource currently is. The one health signal that rises before anything breaks, rather than after.`,timeout:`A limit set in advance: if there is no answer within it, give up rather than wait indefinitely.`,TCP:`The rulebook two machines use to send data reliably across a network — nothing lost, nothing out of order. Setting one up costs a round trip before any real data moves.`,"connection pool":`A stock of already-open database connections kept for reuse, because opening one costs a handshake and an authentication every time. Requests borrow one, use it, and return it.`,"connection leak":`A connection borrowed from the pool and never given back. Nothing looks wrong until, one day, there are none left.`,"acquire timeout":`The longest a request will wait purely to be handed a connection before it gives up. Without one, a request blocks a thread forever.`,"query timeout":`The longest a single query is allowed to run before it is cut off.`,"connection pooler":`A separate middleman holding one shared pool for every app server — PgBouncer, ProxySQL, RDS Proxy. Without it, twenty servers each bring their own pool to the same database.`,"exponential backoff":`Waiting longer before each retry — one second, then two, then four — instead of hammering something that is already struggling.`,jitter:`A small random offset added to a timer, so that thousands of clients holding the same timer do not all fire in the same instant.`,"head-of-line blocking":`One slow item at the front holding up everything behind it, however fast those would have been on their own.`,"retry storm":`Failed requests all coming back at once, so load peaks at exactly the moment there is least capacity to carry it.`,"cascading failure":`One small thing breaking, and everything that depended on it breaking in turn, so the system keeps getting worse without further help.`,"blast radius":`How much of a system one failure is able to damage. Design work is largely making this number smaller.`,bulkhead:`A wall between parts of a system, so a flood in one compartment cannot sink the whole ship. In practice: separate pools, separate queues, separate quotas.`,"circuit breaker":`A guard that stops calling something once it has failed enough times, failing instantly instead — which gives the struggling thing room to recover.`,backpressure:`Pushing work away when overloaded rather than quietly accepting it. Refusing quickly is kinder than accepting and never answering.`,"graceful degradation":`Working partially instead of dying completely. Recommendations gone but films still playing.`,schema:`The shape of a database — which tables exist, which columns they hold, what type each is. Not the data, the structure around it.`,DDL:`The SQL commands that change structure rather than contents: create a table, add a column, drop one.`,migration:`Moving something already in use to a new shape while it stays in use — most often a database’s structure, and always the code that reads it too. The hard part is never the end state, it is the in-between.`,backfill:`Filling a newly added column with values for all the rows that existed before it. Done in small batches, or it locks the table.`,"rolling deploy":`Updating servers a few at a time so the site never goes fully down — which guarantees a window where old and new code are running together.`,canary:`Releasing to one server, or one percent of users, first, and watching before continuing.`,"feature flag":`A switch in configuration rather than in code, so a feature can be turned on and off without deploying again.`,"blue-green":`Two complete environments with traffic switched from one to the other. It does not help with schema changes, because both share the same database.`,"roll forward":`Fixing a bad change by shipping a new one on top, rather than trying to reverse it. Usually the only option once new data exists.`,"backward compatible":`The new version still works with everything old. Nothing that has not been updated yet breaks.`,"forward compatible":`Old code tolerates things it does not recognise rather than crashing on them.`,resumable:`Able to carry on from where it stopped. A job that dies at 60% costs you the remaining 40%, not the whole run.`,"dual write":`Writing to both the old and the new place during a switch, so either side can be read from while the move is in progress.`,"online schema change":`Changing a huge table with almost no downtime by building a synced copy in the new shape and swapping it in at the end.`,PITR:`Point-in-time recovery — restoring a database to an exact moment in the past. The last line of defence, and worthless until a restore has actually been tested.`,"replication lag":`How far behind the copies are. Three seconds of lag means the replica is answering with three-second-old data.`,replica:`A copy of a database kept in step with the original, usually so reads can be served without touching it.`,cache:`A small fast copy of data kept close by, so the slow original does not have to be asked again.`,RAM:`A computer’s short-term memory. Around a thousand times faster than disk, and emptied on restart — which is why caches live here.`,Redis:`The usual product used as a cache: a separate server holding keys and values in memory.`,TTL:`An expiry timer on a cached entry. It is also the backstop that heals every invalidation you missed.`,stale:`Out of date. The stored copy still answers, and what it answers is no longer true.`,eviction:`The cache throwing out old entries because it is full, rather than because they expired.`,invalidate:`Deleting a cached entry because it is no longer correct. Deleting rather than overwriting, because a delete cannot arrive out of order and win.`,"eventual consistency":`The copies will agree in the end, just not at this instant. The gap is the thing you have to design around.`,"thundering herd":`A popular entry expiring and every request that wanted it rushing at the original together.`,"single-flight":`Letting exactly one request fetch a missing value while the rest wait for its answer, instead of all fetching it.`,"stale-while-revalidate":`Handing back the slightly old value immediately and refreshing it in the background.`,"negative caching":`Remembering that something does not exist, so repeated lookups for it stop reaching the original.`,"bloom filter":`A very small structure that can say "definitely not here" cheaply, and "possibly here" the rest of the time.`,CDC:`Change data capture — reading a database’s own log of what changed, so other systems can react to every write without being told.`,"pub-sub":`One message published once and delivered to everybody who subscribed. How you reach twenty servers that each hold their own private copy.`,"source of truth":`The one place that is authoritative. Everything else is a copy, and a copy is allowed to be wrong.`,"distributed system":`One product running across many machines that have to cooperate over a network they cannot rely on.`,"network partition":`The network breaking between two groups of machines. Both sides are alive and healthy; they simply cannot reach each other.`,"load balancer":`The traffic director in front of a group of servers, deciding which one answers each request.`,sharding:`Splitting data across machines by some key — these users here, those users there — so no single machine holds all of it.`,"leader election":`The process a cluster runs to choose which of its machines is allowed to make decisions, after the previous one stops answering.`,heartbeat:`A small "still here" message sent on a fixed interval. Its absence is the only evidence anyone ever has that a machine has died.`,quorum:`More than half. Requiring it is what makes two rival leaders arithmetically impossible, because two groups cannot both be a majority of the same set.`,consensus:`Getting many machines to agree on one value, reliably, while some of them are failing.`,Raft:`A consensus algorithm written to be understandable, and the one most systems now use. It guarantees at most one leader per term.`,"split-brain":`Two machines both believing they are in charge and both accepting writes. The worst failure in distributed systems, because it is silent.`,"fencing token":`An ever-increasing number stamped on every write, so storage can refuse one that carries an out-of-date stamp — which is how a frozen leader that wakes up is stopped.`,lease:`Authority granted for a fixed period and lost automatically unless renewed. It cleans up after a machine that froze without anyone having to notice.`,"GC pause":`The runtime stopping a program briefly to reclaim memory. From outside, the machine is indistinguishable from one that has died.`,"gray failure":`A machine that is alive but degraded rather than cleanly dead. The hardest kind to detect, and the usual cause of leadership flapping.`,"failure detector":`The logic that decides a machine is dead. It is always a guess, because a missing message and a slow message look identical.`,"FLP impossibility":`The proof that on a network with no timing guarantees, agreement cannot be guaranteed within any bounded time. Why a perfect failure detector cannot exist.`,"election storm":`Leaders repeatedly elected and unseated, so a cluster spends its time voting instead of working. Almost always a timeout set too aggressively.`,"CAP theorem":`When the network splits, a system can stay consistent or stay available, and not both. Which one it picks is a design decision, not an accident.`,PACELC:`CAP with the ordinary case added: during a partition, choose consistency or availability — and the rest of the time, choose latency or consistency. Caching is that second choice.`,cluster:`A group of machines run as one system. What makes it a cluster rather than a pile of servers is that they have to agree with each other about something.`,follower:`A machine that copies whatever the one in charge decided, and does not decide anything itself.`,replication:`Keeping a second copy in step with the first, so it can serve reads or take over.`,Paxos:`The original consensus algorithm. Correct, famously hard to follow, and the reason Raft was written.`,ZAB:`The consensus protocol ZooKeeper runs on. Same job as Raft, different lineage.`,ZooKeeper:`A separate service other systems use to hold the facts they must all agree on — who leads, what is locked, what the configuration is. Built so you do not write consensus yourself.`,etcd:`The same idea as ZooKeeper, built on Raft, and what Kubernetes keeps its state in.`,epoch:`A counter that only ever goes up, one step per election. Anything stamped with an old number is refused, which is how a system ignores messages from a leader that has already been replaced.`,"pre-vote":`A round before the real election in which a machine asks whether anyone would vote for it. It stops a node that was cut off from unseating a perfectly healthy leader the moment it comes back.`,"witness node":`A machine that votes and holds no data, added only so an even-sized cluster can still reach a majority.`,"distributed lock":`A claim on something held across machines. It looks like leader election and is not: without a majority and without a fencing token, two holders can genuinely believe they have it.`,lock:`Exclusive use of something while you hold it. Everyone else waits, which is fine when the hold is short and is an outage when it is not.`,backoff:`Waiting before trying again, and waiting longer each time, so a struggling thing is not hit hardest exactly when it is weakest.`,idempotency:`The property of being safe to repeat. It is what makes a retry harmless, and it is why it matters whether an operation sets a value or adds to one.`,checkpoint:`A saved marker of how far a long job got, so a failure costs you the rest of it rather than the whole of it.`,"kill switch":`A way to stop something that is already running, without a deploy. Worth building before you need it, because the moment you need it is the worst moment to write one.`,observability:`Being able to tell what a live system is doing from outside it. Monitoring is the watching; this is whether there was anything worth watching.`,"golden signals":`The four numbers worth alerting on before any others: latency, traffic, errors, and how full things are. The fourth is the one that warns; the first three report.`,"Little’s Law":`How many things are in progress equals how fast they arrive multiplied by how long each takes. It turns "we need a bigger pool" into an actual number.`,"cache-aside":`The application checks the cache itself, and on a miss reads the original and stores the answer. The common pattern, and the one with the race in it.`,"read-through":`The same as cache-aside with the logic moved into the cache layer, so the application only ever asks the cache.`,"write-through":`Every write goes to the cache and the original together. They stay in step, and every write pays for both.`,"write-behind":`Write to the cache now and to the original shortly after. The fastest writes, and the ones you lose if the cache dies first.`,"cache stampede":`One popular entry expiring and every request that wanted it arriving at the original in the same instant.`,"hot key":`One entry taking a large share of all traffic, so whichever single machine holds it carries the load alone.`,"cache penetration":`Repeated requests for something that does not exist. Nothing is ever cached, so every one reaches the original.`,"read-your-own-writes":`A guarantee that whoever made a change sees it immediately afterwards, even if nobody else does yet.`,"monotonic reads":`A guarantee that you never see newer data and then older data again. Without it, moving between two servers looks like a bug.`,outbox:`A table written inside the same transaction as the change itself, and read afterwards by whoever needs to be told. It exists because a write to your database and a message to something else cannot be made one step.`,"key-value store":`A store that is nothing but labels and values. No tables, no joins, and very fast because of it.`,PgBouncer:`A middleman that sits in front of a database holding one shared pool, so twenty application servers do not each bring a hundred connections of their own.`,"gh-ost":`A tool for changing a huge table without a long lock: it builds a copy in the new shape, keeps it in step in the background, and swaps it in at the end.`,Flyway:`A tool that records which migrations a database has already had applied, so "what state is this one in" has an answer.`,binlog:`A database’s own ordered record of everything that changed. Reading it is how other systems find out about a write without being told.`},Di=Object.keys(Ei).sort((e,t)=>t.length-e.length),Oi={JVM:[`advanced/deep-jvm`,`study/jvm-and-memory`],JDK:[`advanced/deep-jvm`],JRE:[`advanced/deep-jvm`],bytecode:[`study/jvm-and-memory`,`advanced/deep-jvm`],JIT:[`advanced/deep-jvm`],"class loader":[`advanced/deep-jvm`],classpath:[`advanced/deep-jvm`],"constant pool":[`advanced/deep-jvm`],heap:[`advanced/deep-memory`,`study/jvm-and-memory`],stack:[`advanced/deep-memory`,`study/jvm-and-memory`],frame:[`advanced/deep-memory`],reference:[`advanced/deep-memory`],metaspace:[`advanced/deep-memory`],TLAB:[`advanced/deep-memory`],"escape analysis":[`advanced/deep-memory`],"compressed oops":[`advanced/deep-memory`],"native memory":[`advanced/deep-memory`],"garbage collection":[`advanced/deep-gc`],"garbage collector":[`advanced/deep-gc`],reachability:[`advanced/deep-gc`],"GC root":[`advanced/deep-gc`],generation:[`advanced/deep-gc`],Eden:[`advanced/deep-gc`],"survivor space":[`advanced/deep-gc`],promotion:[`advanced/deep-gc`],"stop-the-world":[`advanced/deep-gc`],safepoint:[`advanced/deep-gc`],"write barrier":[`advanced/deep-gc`],humongous:[`advanced/deep-gc`],"heap dump":[`advanced/deep-gc`],"memory leak":[`advanced/deep-gc`],primitive:[`study/data-types`,`study/wrapper-classes`],variable:[`study/data-types`],literal:[`study/data-types`],cast:[`study/type-casting`,`study/upcasting-downcasting`],object:[`study/classes-and-objects`,`study/object-methods`,`study/arrays-of-objects`],class:[`study/classes-and-objects`,`study/inner-classes`],constructor:[`study/classes-and-objects`,`study/super-and-this`],instance:[`study/classes-and-objects`,`study/static-keyword`],field:[`study/classes-and-objects`,`study/encapsulation`],method:[`study/methods`,`study/object-methods`],parameter:[`study/methods`],"return type":[`study/methods`],immutable:[`study/strings`],"String Pool":[`study/strings`,`advanced/deep-memory`],intern:[`study/strings`],encapsulation:[`study/encapsulation`],static:[`study/static-keyword`],inheritance:[`study/inheritance`,`study/super-and-this`],superclass:[`study/inheritance`],subclass:[`study/inheritance`],polymorphism:[`study/polymorphism`,`study/upcasting-downcasting`],overriding:[`study/polymorphism`,`study/inheritance`],overloading:[`study/polymorphism`,`study/methods`],constant:[`study/final-keyword`],"hash code":[`study/object-methods`],autoboxing:[`study/wrapper-classes`],abstract:[`study/abstract-keyword`,`study/interface-types`],interface:[`study/interfaces`,`study/interface-types`],enum:[`study/enums`],annotation:[`study/annotations`],reflection:[`study/annotations`],exception:[`study/exceptions`,`study/throw-and-throws`],"checked exception":[`study/exceptions`],"stack trace":[`study/exceptions`],idempotent:[`advanced/deep-http`],ETag:[`advanced/deep-http`],"keep-alive":[`advanced/deep-http/advanced`],QUIC:[`advanced/deep-http/advanced`],"round trip":[`advanced/deep-http/advanced`],handshake:[`advanced/deep-https`],certificate:[`advanced/deep-https`],"certificate authority":[`advanced/deep-https`],"forward secrecy":[`advanced/deep-https`],"cipher suite":[`advanced/deep-https/advanced`],truststore:[`advanced/deep-https/advanced`],"connection pool":[`design/connection-pool-exhaustion`],"connection pooler":[`design/connection-pool-exhaustion`],"connection leak":[`design/connection-pool-exhaustion`],"acquire timeout":[`design/connection-pool-exhaustion`],"query timeout":[`design/connection-pool-exhaustion`],"head-of-line blocking":[`design/connection-pool-exhaustion`],"retry storm":[`design/connection-pool-exhaustion`],"cascading failure":[`design/connection-pool-exhaustion`],bulkhead:[`design/connection-pool-exhaustion`],"circuit breaker":[`design/connection-pool-exhaustion`],backpressure:[`design/connection-pool-exhaustion`],saturation:[`design/connection-pool-exhaustion`],migration:[`design/schema-migration`],backfill:[`design/schema-migration`],"dual write":[`design/schema-migration`],"online schema change":[`design/schema-migration`],"roll forward":[`design/schema-migration`],"backward compatible":[`design/schema-migration`],"forward compatible":[`design/schema-migration`],"blue-green":[`design/schema-migration`],"rolling deploy":[`design/schema-migration`],resumable:[`design/schema-migration`],"replication lag":[`design/schema-migration`],cache:[`design/cache-inconsistency`],invalidate:[`design/cache-inconsistency`],TTL:[`design/cache-inconsistency`],"eventual consistency":[`design/cache-inconsistency`],"thundering herd":[`design/cache-inconsistency`],"single-flight":[`design/cache-inconsistency`],"stale-while-revalidate":[`design/cache-inconsistency`],"negative caching":[`design/cache-inconsistency`],CDC:[`design/cache-inconsistency`],"source of truth":[`design/cache-inconsistency`],"leader election":[`design/leader-election`],quorum:[`design/leader-election`],consensus:[`design/leader-election`],Raft:[`design/leader-election`],"split-brain":[`design/leader-election`],"fencing token":[`design/leader-election`],heartbeat:[`design/leader-election`],"failure detector":[`design/leader-election`],"FLP impossibility":[`design/leader-election`],"election storm":[`design/leader-election`],"gray failure":[`design/leader-election`],"GC pause":[`design/leader-election`],"network partition":[`design/leader-election`],"CAP theorem":[`design/leader-election`],cluster:[`design/leader-election`],follower:[`design/leader-election`],Paxos:[`design/leader-election`],ZAB:[`design/leader-election`],ZooKeeper:[`design/leader-election`],etcd:[`design/leader-election`],epoch:[`design/leader-election`],"pre-vote":[`design/leader-election`],"witness node":[`design/leader-election`],"distributed lock":[`design/leader-election`],"cache-aside":[`design/cache-inconsistency`],"read-through":[`design/cache-inconsistency`],"write-through":[`design/cache-inconsistency`],"write-behind":[`design/cache-inconsistency`],"cache stampede":[`design/cache-inconsistency`],"hot key":[`design/cache-inconsistency`],"cache penetration":[`design/cache-inconsistency`],"read-your-own-writes":[`design/cache-inconsistency`],"monotonic reads":[`design/cache-inconsistency`],outbox:[`design/cache-inconsistency`],PACELC:[`design/cache-inconsistency`],PgBouncer:[`design/connection-pool-exhaustion`],"golden signals":[`design/connection-pool-exhaustion`],"gh-ost":[`design/schema-migration`],Flyway:[`design/schema-migration`],binlog:[`design/schema-migration`]},ki=`# Why SOLID exists

Nobody has ever opened a codebase and thought *"this fails the Interface
Segregation Principle."* What they think is: **why is this taking so long?** A
change that was described in one sentence has turned into eleven files, three of
which have nothing to do with the feature, and a test suite that now fails in a
module nobody touched.

SOLID is five answers to that. They were not derived from theory — Robert
Martin collected them in the late nineties out of things that were already known
to hurt, and the acronym was arranged afterwards by Michael Feathers because
five unrelated names are hard to remember. That order is a mnemonic, not a
sequence. You do not apply S before O.

This page is the problem the five are answers to. Read it first, or the
principles will read like arbitrary rules that some people are very confident
about.

## The only thing that costs money

A program is written once and changed for years. Every serious cost in software
— the estimate that triples, the release that slips, the rewrite that gets
proposed every eighteen months — is the cost of changing something that already
works.

So the useful question about a design is never *is this elegant?* It is:

**When a requirement changes, how much of this has to change with it?**

A good design has a small answer. A bad design has an answer that keeps
surprising you. Everything below is a way of naming that surprise.

![A one-sentence requirement spreading into unrelated files and a failing test](diagrams/solid-intro-notes-blast-radius.jpg)

### The four symptoms

Martin's own list of what bad design feels like from the inside is still the
clearest, and it is worth being able to name these in an interview because they
are what the principles are diagnosing:

| Symptom | What it feels like |
| --- | --- |
| **Rigidity** | One change forces a cascade of other changes. The estimate is never the change itself; it is everything that has to move with it. |
| **Fragility** | A change breaks things in places that have no conceptual relationship to it. Fixing the invoice PDF breaks login. |
| **Immobility** | You cannot lift a useful piece out and use it elsewhere, because it drags half the system behind it. |
| **Viscosity** | Doing the right thing is harder than doing the wrong thing, so under pressure everyone does the wrong thing, and the design decays. |

Notice that all four are about **change**, and none of them are about whether
the code currently works. A rigid system passes all its tests.

## Coupling and cohesion

Every one of the five principles is coupling or cohesion wearing a different
hat. If you only remember two words from this page, remember these.

**Coupling** is how much one piece of code has to know about another. Two
classes are tightly coupled when changing one forces you to change the other.

**Cohesion** is how much the things inside one piece of code belong together. A
class is cohesive when everything in it is there for the same reason.

The goal is **low coupling, high cohesion** — and the two are in tension, which
is why this is a skill rather than a rule. Cram everything into one class and
coupling between classes drops to zero, because there is only one class; you
have simply moved the mess inside it and destroyed cohesion. Split everything
into single-method classes and each is perfectly cohesive, while the coupling
between them becomes a spider's web nobody can follow.

Here is coupling you can see:

\`\`\`java
// Tight. OrderService knows there is a MySQL database, knows the driver
// class name, knows the SQL dialect, knows the table layout.
class OrderService {
    void place(Order order) {
        Connection c = DriverManager.getConnection("jdbc:mysql://localhost/shop");
        PreparedStatement ps = c.prepareStatement(
            "INSERT INTO orders (customer_id, total) VALUES (?, ?)");
        ps.setLong(1, order.customerId());
        ps.setBigDecimal(2, order.total());
        ps.executeUpdate();
    }
}
\`\`\`

Every one of those facts is a reason this class might have to change. Move to
Postgres, rename a column, add a caching layer, write a test that does not need
a database — all of them open this file, and none of them are about placing an
order.

\`\`\`java
// Loose. OrderService knows there is somewhere to put an order. That is all.
class OrderService {
    private final OrderRepository orders;

    OrderService(OrderRepository orders) { this.orders = orders; }

    void place(Order order) { orders.save(order); }
}
\`\`\`

The database did not disappear — something still has to hold that JDBC code.
What changed is **who has to know**. \`OrderService\` now has one reason to
change: the rules of placing an order.

That single move is most of what the five principles are about. The rest is
detail about when to make it and how far.

![What the tightly coupled class must know against what the loose one must know](diagrams/solid-intro-notes-coupling.jpg)

## The five, in one line each

| | Name | What it says |
| --- | --- | --- |
| **S** | Single Responsibility | A class should have one reason to change — one *actor* it answers to. |
| **O** | Open/Closed | You should be able to add behaviour without editing existing code. |
| **L** | Liskov Substitution | Anywhere the base type works, a subtype must work too, with no caller aware of the difference. |
| **I** | Interface Segregation | No client should be forced to depend on methods it does not use. |
| **D** | Dependency Inversion | Both high-level and low-level code should depend on abstractions, and the abstraction should belong to the high-level side. |

Read them together and a shape appears. **S** and **I** are about cohesion —
keeping things that change together in one place, and things that change apart
in different places. **O**, **L** and **D** are about coupling — making sure the
thing you depend on is stable, and that depending on it cannot betray you.

![The five principles sorted into cohesion and coupling](diagrams/solid-intro-notes-two-groups.jpg)

## What SOLID is not

Three corrections worth having before you start, because all three come up in
interviews and all three are how the principles get misused.

**It is not a checklist.** There is no state of a codebase where SOLID is
"done", and no measurement that says a class passes. They are smell detectors:
they tell you where to look, not what to do.

**It is not free.** Every application of every principle adds an indirection —
an interface, another class, another file. Indirection has a cost you pay on
every read of that code forever, in exchange for a change you might make later.
When the change never comes, you paid for nothing. This is the failure mode of
programmers who have just learned SOLID, and it is worse than the disease: a
system with forty interfaces that each have exactly one implementation is not
flexible, it is fog.

**It is not object-oriented-only, and it is not universal either.** The ideas
translate to functions and modules directly. But SOLID was written for
1990s-style OO with deep inheritance hierarchies, and some of its emphasis shows
that age — a Java codebase leaning on records, sealed types and small functions
already gets several of these for free.

The honest summary is Martin's own defence of it: *these are not laws of
physics, they are observations about pain.* Learn what the pain feels like, and
the rules follow.

## How to read the rest

Each principle gets three passes:

- **Notes** — the rule, the code that breaks it, the code that does not, and
  where the rule itself goes wrong.
- **Quick revision** — the same ground compressed to what fits on a phone screen
  twenty minutes before a call.
- **Interview** — questions graded easy to hard, with answers written the length
  you would actually say them.

The last chapter, [SOLID in practice](#/design/solid-in-practice), takes one
ordinary service and refactors it through all five with the diff at each step,
which is the only way to see how they interact.
`,Ai=`# Why SOLID exists — quick revision

The chapter in a page. If a line here is news, the **Notes** part is where it
comes from.

## The premise

- Software is written once and **changed for years**. Every real cost is the cost
  of change.
- The only useful question about a design: **when a requirement changes, how much
  has to change with it?**
- SOLID = five observations about that pain. Collected by **Robert C. Martin**,
  acronym arranged later by **Michael Feathers**. The order is a mnemonic, not a
  sequence.

## The four symptoms

- **Rigidity** — one change forces a cascade of others.
- **Fragility** — a change breaks something with no conceptual link to it.
- **Immobility** — you cannot reuse a piece without dragging the system behind it.
- **Viscosity** — the wrong thing is easier than the right thing, so the design
  decays under deadline pressure.

All four are about change. **A rigid system still passes all its tests.**

## The two words underneath everything

- **Coupling** — how much one piece must know about another. Changing A forces a
  change in B.
- **Cohesion** — how much the contents of one piece belong together.
- Goal: **low coupling, high cohesion**. They pull against each other, which is
  why this is judgement rather than a rule.
- One giant class = zero coupling, no cohesion. One class per method = perfect
  cohesion, unfollowable coupling.

## The five

| | Name | One line |
| --- | --- | --- |
| **S** | Single Responsibility | One reason to change — one **actor** it answers to. |
| **O** | Open/Closed | Add behaviour without editing existing code. |
| **L** | Liskov Substitution | A subtype must work anywhere the base type works. |
| **I** | Interface Segregation | No client forced to depend on what it does not use. |
| **D** | Dependency Inversion | Depend on abstractions; the abstraction belongs to the high-level side. |

- **S** and **I** are **cohesion** — keep what changes together together.
- **O**, **L** and **D** are **coupling** — make what you depend on stable and
  trustworthy.

## The three corrections

- **Not a checklist.** No codebase is ever "SOLID-complete". They are smell
  detectors — they say where to look, not what to do.
- **Not free.** Every application adds an indirection you pay for on every read,
  forever, in exchange for a change that may never come. Forty interfaces with
  one implementation each is fog, not flexibility.
- **Not universal.** Written for 1990s deep-inheritance OO. Records, sealed types
  and small functions already give you some of it.

## The line to say in an interview

*"They are five ways of saying the same thing: put the things that change for
the same reason together, and keep everything else at arm's length behind an
abstraction that does not move."*
`,ji=`# Why SOLID exists — interview questions

Ten questions on the framing. These are the openers — the ones asked before the
interviewer picks a letter and goes deep. Getting them right buys you the
benefit of the doubt on everything after.

## Easy

### 1. What does SOLID stand for?

Single Responsibility, Open/Closed, Liskov Substitution, Interface Segregation,
Dependency Inversion. Five design principles collected by **Robert C. Martin**;
the acronym was arranged later by **Michael Feathers**, so the order is a
memory aid rather than an order of application.

### 2. What problem do they solve?

The cost of change. A program is written once and modified for years, so the
expensive question is not whether the code works today but how much has to move
when a requirement moves. SOLID is five ways of keeping that blast radius small.

### 3. What is coupling, and what is cohesion?

**Coupling** is how much one piece of code must know about another — two classes
are tightly coupled when changing one forces a change in the other. **Cohesion**
is how much the things inside one piece belong together — a class is cohesive
when everything in it is there for the same reason. The aim is low coupling and
high cohesion, and they are in tension: you can drive coupling to zero by
putting everything in one class, which destroys cohesion.

### 4. Are these rules you must always follow?

No. They are smell detectors. They tell you where a design is likely to hurt
when it changes; they do not tell you that every class must be split or every
dependency must have an interface. Applying them without a change in mind buys
indirection you pay for on every read and never cash in.

## Medium

### 5. Name the symptoms of a bad design.

Martin's four: **rigidity** — one change forces a cascade; **fragility** — a
change breaks something unrelated; **immobility** — you cannot reuse a part
without dragging the system with it; and **viscosity** — doing it right is
harder than doing it wrong, so the design decays under pressure. All four are
about change, which is why a rigid system still passes its tests.

### 6. Which principles are about coupling and which about cohesion?

**S** and **I** are cohesion principles: they are about what belongs in one unit
and what should be separated, from the class's side and from the interface's
side respectively. **O**, **L** and **D** are coupling principles: they are
about depending on something in a way that will not betray you — O says depend
on an abstraction so new cases do not reopen your file, L says the abstraction
must be honest about its behaviour, D says the abstraction must be owned by the
side that consumes it.

### 7. What is the cost of applying SOLID?

Indirection. Every split class, every extracted interface, every injected
dependency adds a hop the reader has to follow, and moves behaviour from
"visible in this file" to "somewhere behind this type". You pay that on every
read of the code, forever. You get paid back only when the change you designed
for actually arrives. That is why the right time to apply a principle is when
you have seen the change happen once — not when you first imagine it might.

### 8. Is SOLID still relevant in a functional or modern style?

Yes, but the emphasis shifts. The principles were written against 1990s OO with
deep inheritance, and the letter most tied to that — Liskov — matters less in a
codebase that prefers composition and sealed types to subclassing. The
underlying ideas translate directly: SRP is a cohesive module, OCP is dispatch
on a closed set or a passed-in function, ISP is a narrow function signature, DIP
is a function taking its effects as parameters. In Java specifically, records
and sealed interfaces give you several of these by construction.

## Hard

### 9. Can following SOLID make a design worse? Give a concrete example.

Yes, and the most common way is applying Open/Closed to an axis of change that
never varies. You anticipate that payment providers will multiply, so you build
a \`PaymentStrategy\` interface, a factory, a registry and a configuration key —
and three years later there is still exactly one implementation. The system is
now harder to read, harder to debug (the stack trace goes through a factory),
and no more flexible, because when the second provider finally arrives it
differs along an axis you did not predict and the abstraction does not fit it.

The general failure is that OCP requires you to guess **which** axis will vary,
and a wrong guess costs more than no guess. The mitigation is Martin's own
advice: take the first bullet. Write the concrete thing, wait for the second
case, and let the shape of the real variation tell you where the seam is.

### 10. How do SOLID, DRY and YAGNI fit together — and where do they conflict?

They answer different questions and two of them regularly disagree.

**DRY** is about knowledge, not text: every piece of knowledge should have one
authoritative representation. It supports SRP — duplicated logic is usually a
responsibility that has not been named — but it fights it when people
de-duplicate code that merely looks alike. Two identical validation rules owned
by two different teams are not duplication; merging them couples those teams and
breaks SRP, and the next divergence turns the shared method into a flag
parameter.

**YAGNI** is the direct counterweight to Open/Closed. OCP says build the seam;
YAGNI says you will not need it. The resolution is timing rather than
compromise: YAGNI governs the first occurrence, OCP governs the second. Write it
concretely, and when a second case arrives, refactor to the abstraction the two
real cases share. That way the abstraction is derived from evidence instead of
prediction, which is also the only way to get it right.

The order of priority when they conflict: **make it work, make it clear, then
make it open.** A principle that is applied before you understand the domain is
a guess wearing a rule's uniform.
`,Mi=`# S — Single Responsibility

The most quoted of the five, and the most misquoted. The version everyone
repeats is *"a class should do one thing."* That version is useless, because
"one thing" has no fixed size. Is placing an order one thing? Is validating,
pricing, saving and emailing four things, or four steps of one thing? Any
argument you make, someone can make the opposite with equal confidence.

Martin's actual wording is sharper:

> **A class should have one, and only one, reason to change.**

And the reason he later gave for that wording is sharper still:

> **A module should be responsible to one, and only one, actor.**

That second sentence is the one to remember. It converts an aesthetic argument
into a factual question you can answer: *who asks for changes to this code?* If
the answer is more than one person or department, the class has more than one
responsibility — regardless of how tidy it looks.

## The classic violation

\`\`\`java
class Employee {
    private final String name;
    private final int hoursWorked;
    private final BigDecimal hourlyRate;

    // Asked for by Accounting
    BigDecimal calculatePay() {
        return hourlyRate.multiply(BigDecimal.valueOf(hoursWorked));
    }

    // Asked for by HR
    int reportHours() {
        return hoursWorked;
    }

    // Asked for by the DBA
    void save() {
        // JDBC, transactions, the employees table
    }
}
\`\`\`

This is Martin's own example and it is worth walking through, because the damage
is not obvious from the code. Three methods, all about employees, all short. It
looks fine.

Now the accident. \`calculatePay\` and \`reportHours\` both need "hours worked", so
a developer does the obvious thing and extracts \`regularHours()\` to share
between them. Six months later Accounting asks for a change to overtime rules.
The developer changes \`regularHours()\`, tests \`calculatePay\`, ships.

**HR's report is now wrong, and nobody finds out until the quarterly numbers do
not match.** Two departments' rules were sharing an implementation because they
happened to use the same words, and neither department knew.

![A change from one department travelling through a shared method into another department report](diagrams/single-responsibility-notes-the-accident.jpg)

That is the failure SRP is describing. Not "the class is big". Not "it does
three things". It is that **two actors are coupled through code neither of them
knows about.**

### The fix

Split by actor. The data stays in one place; the policies that different people
own move apart.

\`\`\`java
record EmployeeData(String name, int hoursWorked, BigDecimal hourlyRate) {}

class PayCalculator {          // Accounting owns this
    BigDecimal calculate(EmployeeData e) {
        return e.hourlyRate().multiply(BigDecimal.valueOf(e.hoursWorked()));
    }
}

class HourReporter {           // HR owns this
    int report(EmployeeData e) { return e.hoursWorked(); }
}

class EmployeeRepository {     // The DBA owns this
    void save(EmployeeData e) { /* JDBC */ }
}
\`\`\`

Now the overtime change opens exactly one file, and HR's report cannot be
collateral damage. If the two calculations *should* stay in step, that is now a
decision somebody makes on purpose rather than an accident of a shared private
method.

![The employee class split into three, one per department that owns it](diagrams/single-responsibility-notes-split-by-actor.jpg)

## "One reason to change" in practice

The rule is easier to apply backwards. Rather than asking *does this class have
one responsibility* — which invites you to answer yes — ask:

1. **Who requests changes here?** Names of people or departments, not
   abstractions. Two names, two responsibilities.
2. **What is the list of reasons this file has been edited?** Run
   \`git log -- ThisFile.java\` and read the commit messages. If they fall into
   clearly unrelated groups, the file has clearly unrelated jobs. This is the
   most reliable signal available and almost nobody uses it.
3. **Can you describe the class without "and" or "or"?** A weak test, but a fast
   one. "Parses the config file **and** validates the values **and** applies
   defaults" is three sentences pretending to be one.

### The layers that are almost always separate actors

Some seams recur in nearly every application, because the people behind them
really are different:

| Concern | Changes when |
| --- | --- |
| **Business rules** | The business changes its mind |
| **Persistence** | The storage technology or schema changes |
| **Presentation** | The UI or the API contract changes |
| **Notification** | Someone changes the wording of an email |
| **Validation** | The rules about acceptable input change |

A class that touches three of those rows is not a design decision; it is usually
just what happens when nobody stops.

## Where this goes wrong

SRP is the principle most often taken too far, and the result has a name:
**anaemic classes with a service for every verb.** You end up with
\`OrderCreator\`, \`OrderValidator\`, \`OrderPricer\`, \`OrderPersister\`,
\`OrderNotifier\` and \`OrderOrchestrator\`, all one method long, none of which can
be understood without the other five open.

That design does not have high cohesion — it has **no** cohesion, spread thin.
The test is whether the split follows a real seam. Splitting \`Employee\` by
department follows one: those groups genuinely change independently. Splitting
\`placeOrder\` into five classes because it has five steps follows nothing —
those five steps change together, for one reason, requested by one person.

Two more caveats worth knowing:

**A responsibility is not a method.** A class with fifteen methods that all serve
one actor is fine. \`String\` has dozens of methods and one responsibility.

**Small is not the goal.** SRP happens to produce smaller classes, but you can
have a five-line class that violates it and a two-hundred-line class that does
not. Optimising for line count instead of for reasons-to-change is how the
service-per-verb design happens.

## The Facade, and where the steps go

When you split a class by actor, something still has to run the whole sequence.
That something is a **facade** — a thin class whose one responsibility is
ordering the others.

\`\`\`java
class Payroll {
    private final PayCalculator pay;
    private final EmployeeRepository repository;
    private final PaySlipMailer mailer;

    Payroll(PayCalculator pay, EmployeeRepository repository, PaySlipMailer mailer) {
        this.pay = pay;
        this.repository = repository;
        this.mailer = mailer;
    }

    void runFor(EmployeeData employee) {
        BigDecimal amount = pay.calculate(employee);
        repository.recordPayment(employee, amount);
        mailer.send(employee, amount);
    }
}
\`\`\`

\`Payroll\` knows the sequence and nothing else — no tax rules, no SQL, no SMTP.
Its one reason to change is that the *order of the steps* changes. That is a
legitimate responsibility, and it is the answer to the objection that SRP leaves
you with a bag of parts and no assembly.

![A facade holding the sequence, with the three split classes inside it](diagrams/single-responsibility-notes-facade.jpg)

Note also what this class is doing to its dependencies: taking them in the
constructor rather than constructing them. That is [Dependency
Inversion](#/design/dependency-inversion) arriving on its own, which is the usual
way the five turn up — you apply one and find you have most of another.
`,Ni=`# S — Single Responsibility — quick revision

The chapter in a page. If a line here is news, the **Notes** part is where it
comes from.

## The statement

- Popular version: *"a class should do one thing."* **Useless** — "one thing" has
  no fixed size.
- Martin's version: **one, and only one, reason to change.**
- Martin's later, sharper version: **a module should be responsible to one, and
  only one, actor.**
- Actor = the person or department who asks for the change. Two actors, two
  responsibilities. This is the version to quote.

## The canonical example

\`Employee\` with \`calculatePay()\` (Accounting), \`reportHours()\` (HR) and \`save()\`
(the DBA).

- Both calculations need "hours worked", so someone extracts a shared
  \`regularHours()\`.
- Accounting changes overtime rules → **HR's report silently goes wrong.**
- The damage is not size. It is **two actors coupled through code neither knows
  about.**

Fix: split by actor — \`PayCalculator\`, \`HourReporter\`, \`EmployeeRepository\`,
with the data in a plain \`record\`.

## How to find the seam

1. **Who requests changes here?** Real names, not abstractions.
2. **\`git log -- File.java\`** — do the commit messages fall into unrelated
   groups? Most reliable signal, almost never used.
3. **Describe it without "and"/"or".** Weak but fast.

Seams that recur in almost every app, because the people really are different:
**business rules · persistence · presentation · notification · validation**.

## The traps

- **A responsibility is not a method.** Fifteen methods serving one actor is
  fine. \`String\` has dozens.
- **Small is not the goal.** A 5-line class can violate it; a 200-line class can
  satisfy it.
- **Over-application:** \`OrderCreator\`, \`OrderValidator\`, \`OrderPricer\`,
  \`OrderPersister\`, \`OrderNotifier\` — one method each, none understandable
  alone. That is not high cohesion, it is **no cohesion spread thin**.
- The test: does the split follow a seam that **actually changes independently**?
  Five steps that always change together are one responsibility, not five.

## The facade

- After splitting, something must run the sequence — a thin class whose one
  responsibility is **ordering the others**.
- Its reason to change: the order of the steps changed. Legitimate.
- It takes its collaborators in the constructor → you have just done
  [Dependency Inversion](#/design/dependency-inversion) as a side effect. The
  five arrive together.

## The line to say in an interview

*"Not one thing — one actor. Ask who signs off on changes to this file; if
that's two different people, the file is coupling them together and neither
knows."*
`,z=`# S — Single Responsibility — interview questions

Ten questions. The first three are asked to check you know the words; the rest
are asked to find out whether you have ever actually used it.

## Easy

### 1. State the Single Responsibility Principle.

A class should have **one, and only one, reason to change**. Martin's later
phrasing makes it concrete: a module should be **responsible to one, and only
one, actor** — one person or group who asks for changes to it.

### 2. Why is "a class should do one thing" a bad way to state it?

Because "one thing" has no fixed size, so the rule cannot be applied or
disputed. Is placing an order one thing, or is it validating, pricing, saving
and notifying? Both answers are defensible, which means the rule decides nothing.
"One reason to change" replaces the aesthetic question with a factual one you can
answer by asking who requests the change.

### 3. Give an example of a class that violates it.

An \`Employee\` class with \`calculatePay()\`, \`reportHours()\` and \`save()\`. Three
different actors own those three methods — Accounting, HR and the DBA — so
three unrelated groups can each force this file to change.

## Medium

### 4. Walk through the actual harm in that example. It is only three short methods.

The harm arrives through sharing. \`calculatePay\` and \`reportHours\` both need
hours worked, so a developer extracts a shared private \`regularHours()\` — a
perfectly sensible-looking removal of duplication. Later, Accounting changes the
overtime rules; the developer edits \`regularHours()\`, tests the pay calculation
and ships. HR's report is now wrong, and nobody notices until the quarterly
numbers disagree.

Two departments were coupled through an implementation detail neither of them
knew existed. That is the point of SRP, and it explains why size is not the
symptom: the class was small and the bug was still structural.

### 5. How do you decide where to split a class?

Follow the actors, not the verbs. In practice:

- Ask who requests changes to it — real people or teams. Two names is two
  responsibilities.
- Read \`git log\` for the file. If the commit messages fall into unrelated
  groups, so does the file. This is the strongest signal available because it is
  evidence rather than prediction.
- Look for the seams that recur everywhere: business rules, persistence,
  presentation, notification, validation. Those change for genuinely different
  reasons in almost every system.

### 6. Does SRP mean a class should have only one method?

No, and this is the most common over-correction. A responsibility is not a
method. \`String\` has dozens of methods and one responsibility, because everything
in it serves the same actor for the same reason. The failure mode of getting this
wrong is a codebase of \`OrderCreator\`, \`OrderValidator\`, \`OrderPricer\`,
\`OrderPersister\` — one method each, none of which can be understood without the
other three open. That is not high cohesion; it is no cohesion spread thin.

### 7. If you split a class into four, what runs the sequence?

A facade — a thin class whose single responsibility is **ordering the others**.
It holds no rules of its own: it calls the calculator, hands the result to the
repository, then to the mailer. Its one reason to change is that the sequence of
steps changed, which is a real and separate responsibility. It also naturally
takes its collaborators through the constructor, which is Dependency Inversion
arriving as a side effect.

### 8. How does SRP relate to DRY? Can they conflict?

They usually agree — duplicated logic is often a responsibility that has not
been given a name — but they conflict when code merely looks alike. If two teams
have validation rules that are currently identical, DRY says extract one method
and SRP says do not: those rules answer to two actors, and merging them means the
next divergence gets handled with a boolean flag. The resolution is that DRY is
about duplicated **knowledge**, not duplicated **text**. Same code, two owners,
two reasons to change — leave it duplicated.

## Hard

### 9. Where does SRP stop being useful? Give a case where you would deliberately not apply it.

When the split has no seam behind it. SRP pays only when the pieces genuinely
change independently; when they always change together, splitting them buys
indirection and costs coherence, because a change now touches four files instead
of one and each file is meaningless alone.

Concretely, I would not split a five-step domain operation into five classes just
because it has five steps — those steps are one policy, requested by one person,
and they will be edited together forever. Similarly I would not extract a
repository interface for a table that is only ever read by one aggregate in one
way.

There is also a scale caveat: SRP applied at class level in a small service can
produce more files than the service has behaviour. The principle scales — the
same reasoning applies to modules and services — and choosing the level to apply
it at is part of the judgement. Splitting a *service* by actor is often right
where splitting every *class* by actor is not.

### 10. How would you argue SRP to someone who says it just produces more files?

By agreeing with the observation and disputing the conclusion. It does produce
more files, and that is a real cost — more navigation, more names, more
constructor wiring. The question is what you get for it.

What you get is that the blast radius of a change becomes visible in the file
tree rather than hidden in a call graph. When Accounting asks for a change, the
files that can be affected are the ones with Accounting's rules in them, and
that fact is checkable before you start rather than discovered in production.
Rigidity and fragility — the cascade of forced edits and the unrelated
breakage — are exactly what a mixed-actor class produces, and they are far more
expensive than an extra file.

But I would also hold the line the other way: the cost is only worth paying when
the actors are real. If I cannot name two different people who would ask for two
different changes, I would leave the class alone and revisit when the second
actor shows up. Applying SRP on speculation gives you the file count without the
benefit.
`,B=`# O — Open/Closed

> **Software entities should be open for extension, but closed for
> modification.** — Bertrand Meyer, 1988

Read literally that is a contradiction, which is why it confuses people. How can
something accept new behaviour without anyone editing it?

The resolution is that **open** and **closed** are talking about two different
files.

- **Open for extension** — the system's behaviour can grow. There is a way to add
  a new case.
- **Closed for modification** — adding that case does not require you to open and
  edit the code that already works and is already tested.

You still write new code. What you do not do is go back into a working,
regression-tested class and add another branch to it. The unit of "closed" is a
file, not the codebase.

## The smell it names

If you want one heuristic for spotting an OCP violation, it is this: **a
conditional that grows a branch every time the business adds a case.**

\`\`\`java
class ShippingCalculator {
    BigDecimal cost(Order order, String method) {
        if (method.equals("STANDARD")) {
            return BigDecimal.valueOf(5);
        } else if (method.equals("EXPRESS")) {
            return BigDecimal.valueOf(15);
        } else if (method.equals("OVERNIGHT")) {
            return BigDecimal.valueOf(30);
        }
        throw new IllegalArgumentException("Unknown method: " + method);
    }
}
\`\`\`

Every new shipping method opens this file. That sounds survivable until you
notice the same \`if\` chain has been copied into the label printer, the
returns service, the customer-facing estimate and the CSV export. Now adding
**Drone** is a five-file change where four of the files will be found by
grepping, and the fifth will be found by a customer.

That fifth file is the actual argument for OCP. It is not about the effort of
editing one method; it is that a decision expressed as a conditional gets
**duplicated**, and duplicated conditionals go out of sync.

![The same shipping conditional copied into five files, four findable and one not](diagrams/open-closed-notes-copied-conditional.jpg)

### Closing it

Put each case behind a common abstraction and let the language do the dispatch.

\`\`\`java
interface ShippingMethod {
    String code();
    BigDecimal cost(Order order);
}

class Standard implements ShippingMethod {
    public String code() { return "STANDARD"; }
    public BigDecimal cost(Order order) { return BigDecimal.valueOf(5); }
}

class Express implements ShippingMethod {
    public String code() { return "EXPRESS"; }
    public BigDecimal cost(Order order) { return BigDecimal.valueOf(15); }
}

class ShippingCalculator {
    private final Map<String, ShippingMethod> methods;

    ShippingCalculator(List<ShippingMethod> available) {
        this.methods = available.stream()
            .collect(Collectors.toMap(ShippingMethod::code, m -> m));
    }

    BigDecimal cost(Order order, String code) {
        ShippingMethod method = methods.get(code);
        if (method == null) throw new IllegalArgumentException("Unknown method: " + code);
        return method.cost(order);
    }
}
\`\`\`

Adding **Drone** is now one new file. \`ShippingCalculator\` is closed: it has no
knowledge of any particular method, and the test suite that covers it does not
need to run again to prove Drone did not break Express.

![Adding a shipping method as one new file while the tested classes stay shut](diagrams/open-closed-notes-add-a-file.jpg)

This is the **Strategy** pattern, and that is not a coincidence — most of the
Gang of Four patterns are OCP wearing a specific shape. Strategy, Template
Method, Decorator, Visitor and Observer are all mechanisms for adding a case
without editing the dispatcher.

## The abstraction is a guess

Here is the part that the tutorials leave out, and the part that separates
someone who has read about OCP from someone who has been burned by it.

**Closing a module against one kind of change opens it to another.**

The design above is closed against *new shipping methods*. It is wide open to a
change like *"cost now depends on the destination country and the day of the
week"*, which alters the \`cost\` signature and therefore edits the interface and
**every implementation of it** — strictly worse than the \`if\` chain, where it
would have been one file.

So OCP requires you to predict **which axis will vary**. Get that right and it is
the highest-leverage of the five. Get it wrong and you have paid for an
abstraction that does not fit the change when it comes.

Martin's own guidance is the honest one: you cannot close against everything, so
close strategically against the change you have **already seen happen**. The
practical rule:

> **Take the first bullet.** Write the concrete thing. When the second case
> arrives, look at how it differs from the first, and put the seam *there* — on
> the axis the two real cases actually vary along, not the one you imagined.

The second case is worth waiting for because it is evidence. One case tells you
nothing about the shape of variation; two cases tell you almost everything.

## What "closed" is worth

There is a second argument for OCP that has nothing to do with tidiness and is
often the more persuasive one in a real team:

**Code you do not edit does not need re-testing, re-reviewing or re-deploying.**

In a system where \`ShippingCalculator\` is shared, a change to it means every team
depending on it takes the risk. In the closed version, adding Drone touches a
file that nothing else has ever loaded. That is the difference between "we can
ship this Tuesday" and "this needs a regression cycle".

It matters most at boundaries you do not control: a public library, a plugin
API, a module consumed by another team. Inside a single class that one person
owns, the argument is much weaker, which is a legitimate reason to leave the
\`if\` chain alone.

## In modern Java

Two things change the calculus, and both are worth mentioning if you are asked
about this in a Java interview.

**Enums with behaviour** close the simple cases without any of the ceremony:

\`\`\`java
enum ShippingMethod {
    STANDARD  { BigDecimal cost(Order o) { return BigDecimal.valueOf(5); } },
    EXPRESS   { BigDecimal cost(Order o) { return BigDecimal.valueOf(15); } },
    OVERNIGHT { BigDecimal cost(Order o) { return BigDecimal.valueOf(30); } };

    abstract BigDecimal cost(Order order);
}
\`\`\`

Adding a case still edits this file, so it is not strictly closed — but the
compiler now guarantees there is exactly one place to edit, which removes the
real danger (the fifth copy of the \`if\` chain). For a set that lives in one
module and is not extended by outsiders, this is usually the right trade.

**Sealed interfaces plus exhaustive \`switch\`** deliberately invert the principle:

\`\`\`java
sealed interface Shape permits Circle, Square {}

double area(Shape shape) {
    return switch (shape) {
        case Circle c -> Math.PI * c.radius() * c.radius();
        case Square s -> s.side() * s.side();
    };
}
\`\`\`

Here adding a \`Triangle\` is a **compile error at every switch**, and that is the
feature. When the set of cases is fixed and the set of operations grows — shapes
in a geometry library, node types in an AST — being told about every place that
needs updating beats silently missing one. This is the **expression problem**:
you can be open to new types or open to new operations, and choosing which is a
design decision rather than a rule.

![Interface and polymorphism against sealed types and switch, and which axis each closes](diagrams/open-closed-notes-expression-problem.jpg)

Interviewers who know Java well ask this one, because "always use polymorphism
over switch" is exactly the kind of answer that shows the principle was
memorised rather than understood.
`,Pi=`# O — Open/Closed — quick revision

The chapter in a page. If a line here is news, the **Notes** part is where it
comes from.

## The statement

- **Open for extension, closed for modification** — Bertrand Meyer, 1988.
- Not a contradiction: they refer to **different files**. Behaviour grows by
  **adding** a file, not by **editing** a tested one.
- The unit of "closed" is a file/class, not the codebase. You always write new
  code.

## The smell

- **A conditional that grows a branch every time the business adds a case.**
- The real damage is not the edit — it is that the same \`if\` chain gets **copied**
  into the label printer, the returns service, the estimate and the CSV export,
  and then drifts out of sync.
- The one you find by grepping is fine. The one you find from a customer is the
  argument for OCP.

## The fix

- Common interface + one class per case + dispatch by lookup (\`Map<String, T>\`)
  or by polymorphism.
- Adding a case = **one new file**, and the existing test suite does not need to
  re-run to prove nothing broke.
- This is **Strategy**. Most GoF patterns are OCP in a specific shape —
  Strategy, Template Method, Decorator, Visitor, Observer.

## The catch nobody mentions

- **Closing against one axis opens you to another.**
- Closed against *new shipping methods* → wide open to *"cost now depends on
  country and day of week"*, which edits the interface **and every
  implementation** — worse than the \`if\` chain, which would have been one file.
- OCP requires **predicting which axis varies**. A wrong guess costs more than no
  guess.
- **Take the first bullet.** Write it concretely; when the **second** case
  arrives, put the seam where the two real cases actually differ. One case tells
  you nothing about the shape of variation; two tell you almost everything.

## Why "closed" is worth money

- **Code you do not edit needs no re-test, re-review or re-deploy.**
- Strongest at boundaries you do not control: a public library, a plugin API, a
  module another team consumes.
- Weakest inside one class one person owns — a legitimate reason to leave the
  \`if\` chain alone.

## In modern Java

- **Enum with abstract method** — adding a case still edits the file, so not
  strictly closed, but the compiler guarantees **exactly one place to edit**.
  Removes the real danger (the copied chain). Usually the right trade for a
  closed set inside one module.
- **Sealed interface + exhaustive \`switch\`** — deliberately the opposite: adding
  a type is a **compile error at every switch**, and that is the feature.
- This is the **expression problem**: open to new *types* or open to new
  *operations* — pick one. Fixed types, growing operations → sealed + switch.
  Growing types, fixed operations → interface + polymorphism.

## The line to say in an interview

*"Add a case by adding a file, not by editing a tested one. But closing one axis
opens another, so I close against variation I've already seen happen — the
second case, not the first."*
`,Fi=`# O — Open/Closed — interview questions

Ten questions. The hard ones here are where Java-heavy interviewers like to
live, because the modern language features make the naive answer wrong.

## Easy

### 1. State the Open/Closed Principle.

Software entities should be **open for extension but closed for modification** —
you should be able to add new behaviour to a system without editing the code
that already works. Bertrand Meyer, 1988.

### 2. That sounds like a contradiction. How can both be true?

Because the two halves are about different files. The **system** is open: its
behaviour can grow. The **existing class** is closed: growing the system does not
mean opening and editing it. You still write new code — you write it in a new
file that plugs into a seam the old one already provides.

### 3. What does a violation look like?

A conditional that gains a branch every time the business adds a case — an
\`if/else\` or \`switch\` on a type code, inside a class that has to be reopened for
every new type. \`if (method.equals("STANDARD")) … else if (method.equals("EXPRESS")) …\`
is the canonical shape.

### 4. How do you fix it?

Replace the conditional with polymorphism: an interface for the varying
behaviour, one implementation per case, and dispatch by lookup or by the
language's own virtual dispatch. Adding a case becomes adding a file. That is
the Strategy pattern, and most of the Gang of Four patterns are OCP in a
specific shape.

## Medium

### 5. What is the real cost of the \`if\` chain? It is only one method to edit.

That it does not stay one method. The same decision — "which shipping method is
this?" — gets re-expressed in the label printer, the returns flow, the customer
estimate and the export job, because each of them needs the same fact. Now
adding a case is a five-file change, four of which you find by grepping and the
fifth of which you find from a customer. OCP's value is less about the effort of
one edit and more about preventing a business decision from being duplicated
into places that will drift apart.

### 6. Give the argument for OCP that a manager would care about.

Code you do not edit does not need re-testing, re-reviewing or re-deploying.
When a shared calculator has to be opened for every new case, every team
depending on it inherits the regression risk, and a one-day feature acquires a
release cycle. When the case is a new file that nothing else has ever loaded,
the risk is contained. That argument is strongest at boundaries you do not
control — a library, a plugin API, another team's dependency — and weakest
inside a class one person owns.

### 7. Does OCP mean you should never modify existing code?

No. It means you should not have to modify *working, tested* code to add a
routine new case. Bug fixes modify existing code. Changing a business rule
modifies existing code. And when the abstraction itself turns out to be wrong,
the right move is to change it rather than to bend the new case around it —
preserving a bad seam because "the class is closed" is how OCP turns into
cargo cult.

## Hard

### 8. When does applying OCP make the design worse?

When you close against the wrong axis, which is a coin flip if you do it before
you have seen the change. A \`ShippingMethod\` interface is closed against new
methods. Now the requirement arrives that cost depends on destination country
and day of week: that changes the \`cost\` signature, so it edits the interface
**and every implementation** — a worse outcome than the \`if\` chain, where it
would have been one file.

The general statement is that **closing a module against one kind of change
opens it to another**, and you cannot close against all of them. So I apply it
against variation I have evidence for: write the first case concretely, and when
the second arrives, put the seam along the axis those two actually differ on.
One case tells you nothing about the shape of the variation; two tell you
almost everything.

The failure mode of ignoring this is a codebase of interfaces with one
implementation each — indirection with no extension behind it, which is harder
to read and no more flexible.

### 9. In Java, is an \`enum\` with an abstract method a violation of OCP?

Strictly, yes — adding a constant edits the enum file, so it is not closed. In
practice it is often the better design for a closed set that lives in one module.

The reason is that OCP's real payoff is preventing the *scattered duplicate*
conditional, and an enum with behaviour eliminates that completely: the compiler
guarantees every case's behaviour lives in exactly one place, and there is no
way to add a constant without supplying the implementation. You give up
third-party extensibility, which you did not want for a fixed set like currencies
or order states, and you get a design that is far easier to read than five
one-method classes plus a registry.

I would use the interface when the set is genuinely open — plugins, tenant-
specific rules, anything another module contributes — and the enum when the set
is closed and mine.

### 10. Explain the expression problem and how it relates to Open/Closed.

The expression problem is that a design can be open to new **types** or open to
new **operations**, but not both without extra machinery — and OCP as usually
taught only mentions the first half.

Interface plus polymorphism is open to new types: adding \`Drone\` is one new file,
and every existing operation keeps working. But adding a new *operation* means
adding a method to the interface, which edits every implementation.

A sealed interface with exhaustive \`switch\` is the mirror image. Adding an
operation is one new method somewhere, touching nothing. Adding a type is a
**compile error at every switch** — and when the type set is genuinely fixed,
that is the feature, not the flaw: being told about all twelve places that need
updating is strictly better than silently missing one and finding out at runtime.

So the choice is a design decision driven by which side actually varies. Node
types in an AST or shapes in a geometry library are stable while the operations
multiply — seal them. Payment providers or tenant rules multiply while the
operations are stable — use an interface. Answering "always prefer polymorphism
over switch" is the giveaway that the principle was memorised rather than
understood, because Java 21 gives you a first-class tool for the other half.
`,Ii=`# L — Liskov Substitution

The odd one out. Four of the five principles are advice; this one is a
**theorem**, stated by Barbara Liskov in 1987 and formalised with Jeannette Wing
in 1994. It is also the only one you can break without the compiler saying a
word, because it constrains **behaviour**, and Java's type system only checks
**shape**.

The formal version:

> Let *q(x)* be a property provable about objects *x* of type T. Then *q(y)*
> should be true for objects *y* of type S where S is a subtype of T.

The usable version:

> **If your code takes a \`Bird\`, handing it a \`Penguin\` must not break it — and
> the code must not have to know it got a penguin.**

That last clause is the whole principle. The moment a caller writes
\`if (bird instanceof Penguin)\`, substitutability has already failed; the
\`instanceof\` is the diagnosis, not the cure.

## What "must not break" actually means

The rule is not vague. Substitutability decomposes into four checkable
conditions, and knowing them by name is what separates a good answer from a
vague one.

| Condition | Rule for the subtype |
| --- | --- |
| **Preconditions** | Cannot be **strengthened**. A subtype may not demand more of the caller. |
| **Postconditions** | Cannot be **weakened**. A subtype must deliver at least what the base promised. |
| **Invariants** | Must be **preserved**. Anything true of the base type must stay true. |
| **History** | A subtype must not allow state changes the base type forbids. |

The first two are worth staring at, because the direction is counter-intuitive
and interviewers know it. A subtype may accept **more** input and promise
**more** about its output — never the reverse.

![What a subtype is allowed to change about the contract and what it is not](diagrams/liskov-substitution-notes-which-way.jpg)

### Strengthening a precondition

\`\`\`java
class Rectangle {
    void setWidth(int width) { /* any positive width */ }
}

class SmallRectangle extends Rectangle {
    @Override
    void setWidth(int width) {
        if (width > 100) throw new IllegalArgumentException("too wide");  // stricter
    }
}
\`\`\`

Code written against \`Rectangle\` that calls \`setWidth(500)\` is correct. Hand it a
\`SmallRectangle\` and it throws. The caller did nothing wrong and has no way to
know. **Precondition strengthened — LSP violated.**

### Weakening a postcondition

\`\`\`java
class Account {
    /** @return the balance, never negative */
    BigDecimal balance() { ... }
}

class OverdraftAccount extends Account {
    @Override
    BigDecimal balance() { ... }   // can return a negative number
}
\`\`\`

Every caller that assumed a non-negative balance — a report, a sum, a UI
formatter — is now wrong, silently, in a way that compiles.

Note that Java enforces the *shape* of return types (covariant returns are
allowed) and nothing at all about their *values*. The compiler cannot help you
here. This is why LSP violations are found in production.

## The square and the rectangle

The famous example, and worth doing properly because most retellings stop before
the interesting part.

Mathematically a square **is a** rectangle. So:

\`\`\`java
class Rectangle {
    protected int width, height;

    void setWidth(int width)   { this.width = width; }
    void setHeight(int height) { this.height = height; }
    int area()                 { return width * height; }
}

class Square extends Rectangle {
    @Override
    void setWidth(int width) { this.width = width; this.height = width; }

    @Override
    void setHeight(int height) { this.width = height; this.height = height; }
}
\`\`\`

The overrides are necessary — a square with different sides is not a square, so
the invariant must be maintained. And they are exactly what breaks the caller:

\`\`\`java
void resize(Rectangle r) {
    r.setWidth(5);
    r.setHeight(4);
    assert r.area() == 20;   // holds for Rectangle, fails for Square (16)
}
\`\`\`

\`resize\` is correct code. It was written against \`Rectangle\`'s contract, which
includes the implicit promise that **width and height are independent**. \`Square\`
cannot keep that promise, so \`Square\` is not a subtype of \`Rectangle\` in the
behavioural sense — whatever geometry says.

![Three calls on a square, each one moving the side the caller did not touch](diagrams/liskov-substitution-notes-square-trace.jpg)

**The lesson is not "squares are not rectangles."** It is that the "is-a"
relationship of everyday language is not the "is-substitutable-for" relationship
that inheritance requires. The word "is" is a trap, and it is the single most
common source of bad hierarchies.

Two things fix this example, and both are worth mentioning if asked:

- **Make them immutable.** With no setters, there is no way to break the
  invariant — \`withWidth()\` returns a new object, and \`Square\` extending
  \`Rectangle\` becomes harmless. Mutability is what created the problem.
- **Do not relate them by inheritance at all.** Have both implement a \`Shape\`
  interface with \`area()\`. They share an operation, not a substitutable
  contract.

## The \`UnsupportedOperationException\` tell

The clearest smell in Java, and it is sitting in the standard library:

\`\`\`java
List<String> names = List.of("Ada", "Grace");
names.add("Alan");   // UnsupportedOperationException at runtime
\`\`\`

\`List.of\` returns something that satisfies the \`List\` interface in shape and
refuses half of it in behaviour. Any method taking a \`List\` and calling \`add\` is
correct against the declared contract and broken by this argument. That is an
LSP violation shipped in the JDK, and the designers knew it — \`Collections\`
has carried \`UnsupportedOperationException\` since 1.2 as a deliberate trade
between a clean hierarchy and a practical one.

![A list that satisfies the interface and refuses half of it at runtime](diagrams/liskov-substitution-notes-too-wide.jpg)

So the rule of thumb: **a method that throws "you cannot call this" is an
admission that the type is too wide.** The type should have been split so that
callers who need mutation ask for a mutable type. That is [Interface
Segregation](#/design/interface-segregation) — the two principles meet here,
because a fat interface is exactly what forces implementers into
\`UnsupportedOperationException\`.

## How to avoid violating it

**Prefer composition to inheritance.** Most LSP violations are inheritance used
for code reuse rather than for substitutability. If you only want the parent's
code, hold an instance of it instead of extending it — then you expose exactly
the operations that make sense and promise nothing you cannot keep.

**Write the contract down.** Preconditions, postconditions and invariants that
live only in someone's head cannot be preserved by a subclass author who has
never met them. Javadoc, or better, a test suite written against the *interface*
and run against every implementation — a "contract test". That is the only
mechanism that actually catches these.

**Watch for these four tells:**

- An override that throws where the parent did not
- An override that is empty, or that ignores its arguments
- A caller doing \`instanceof\` or \`getClass()\` to decide what to do
- An override that strengthens validation

**Use \`final\` and sealed types.** A class that was not designed for extension
should say so. \`sealed\` lets you permit the subtypes you have actually reasoned
about, which turns "please respect my invariants" into something the compiler
enforces.

## Where the principle is weaker than it sounds

LSP is stated about subclasses, but its real subject is **any** substitution
behind an abstraction, and that matters more in modern code than inheritance
does. Two implementations of a \`PaymentGateway\` interface must be substitutable
for the same reason — if one of them silently succeeds on a duplicate charge and
the other throws, every caller is now provider-specific and the interface is
decorative.

It is also the principle with the most tolerated violations in real systems. The
JDK ships them; so does almost every framework. That is not permission, but it
is context: the practical goal is that violations are **documented and
contained**, not that they never occur.
`,Li=`# L — Liskov Substitution — quick revision

The chapter in a page. If a line here is news, the **Notes** part is where it
comes from.

## The statement

- **Barbara Liskov, 1987**; formalised with **Jeannette Wing**, 1994. The only
  one of the five with a formal definition.
- Usable form: **anywhere the base type works, a subtype must work — and the
  caller must not have to know which it got.**
- The moment a caller writes \`if (x instanceof Penguin)\`, substitutability has
  already failed. The \`instanceof\` is the diagnosis, not the cure.
- The compiler checks **shape**, LSP constrains **behaviour**. Nothing catches
  this at compile time, which is why it is found in production.

## The four conditions

| Condition | Rule for the subtype |
| --- | --- |
| **Preconditions** | Cannot be **strengthened** — may not demand more of the caller. |
| **Postconditions** | Cannot be **weakened** — must deliver at least what the base promised. |
| **Invariants** | Must be **preserved**. |
| **History** | Must not allow state changes the base forbids. |

Direction to memorise: a subtype may accept **more** and promise **more**. Never
the reverse.

- Strengthened precondition: override throws on input the parent accepted.
- Weakened postcondition: \`balance()\` promised "never negative", override returns
  a negative.

## Square / Rectangle

- \`Square\` overrides \`setWidth\`/\`setHeight\` to keep sides equal — **it has to**,
  or it is not a square.
- Caller: \`setWidth(5); setHeight(4); assert area() == 20\` — holds for
  \`Rectangle\`, gives **16** for \`Square\`.
- \`Rectangle\`'s contract includes an implicit promise: **width and height are
  independent.** \`Square\` cannot keep it.
- **The lesson is not "squares aren't rectangles."** It is that everyday **is-a**
  is not **is-substitutable-for**. "Is" is the trap.
- Two fixes: make them **immutable** (no setters, no invariant to break), or
  relate them by a \`Shape\` interface instead of inheritance.

## The Java tell

- \`List.of("a").add("b")\` → **\`UnsupportedOperationException\`**. An LSP violation
  shipped in the JDK, knowingly, since 1.2.
- Rule of thumb: **a method that throws "you cannot call this" means the type is
  too wide.** Split it → that is
  [Interface Segregation](#/design/interface-segregation). The two principles
  meet exactly here.

## How to avoid it

- **Prefer composition to inheritance.** Most violations are inheritance used for
  code reuse rather than substitutability.
- **Write the contract down** — Javadoc, or better a **contract test** run against
  every implementation. Invariants in someone's head cannot be preserved.
- **\`final\` and \`sealed\`** — a class not designed for extension should say so;
  \`sealed\` makes "respect my invariants" compiler-enforced.

Four tells: an override that **throws** where the parent did not · an override
that is **empty** or ignores its arguments · a caller doing **\`instanceof\`** ·
an override that **strengthens validation**.

## Wider than inheritance

- Applies to **any** substitution behind an abstraction. Two \`PaymentGateway\`
  implementations where one throws on a duplicate charge and the other silently
  succeeds is the same violation, and the interface is now decorative.

## The line to say in an interview

*"It's about behaviour, not shape — the compiler can't check it. A subtype may
accept more and promise more, never less. If callers need \`instanceof\` to use it
safely, it isn't a subtype."*
`,Ri=`# L — Liskov Substitution — interview questions

Ten questions. This is the letter interviewers use to find out whether you have
memorised an acronym or understood a contract, because the naive answer —
"a subclass should behave like its parent" — is correct and says nothing.

## Easy

### 1. State the Liskov Substitution Principle.

Objects of a subtype must be usable anywhere objects of the base type are
expected, without the calling code needing to know or behaving differently.
Formally, any property provable about the base type must hold for the subtype.
Barbara Liskov, 1987.

### 2. Why is it different from the other four principles?

It is the only one with a formal definition, and the only one that constrains
behaviour rather than structure. That matters practically: Java's type system
checks that a subclass has the right method signatures and nothing about what
those methods do, so an LSP violation compiles cleanly and shows up at runtime.

### 3. Give the classic example.

\`Square extends Rectangle\`. \`Square\` must override \`setWidth\` and \`setHeight\` to
keep its sides equal, so a caller that sets width to 5 and height to 4 and
expects an area of 20 gets 16. The caller is correct code written against
\`Rectangle\`'s contract; the subclass broke it.

## Medium

### 4. What are the specific conditions a subtype must satisfy?

Four. **Preconditions cannot be strengthened** — the subtype may not demand more
of the caller than the base did. **Postconditions cannot be weakened** — it must
deliver at least what the base promised. **Invariants must be preserved.** And
the **history constraint**: it must not permit state changes the base type
forbids, for instance by adding a setter to something the base guaranteed was
immutable after construction.

The direction is the part worth getting right: a subtype may accept more and
promise more. Never less.

### 5. Give a concrete example of strengthening a precondition.

A base \`Rectangle.setWidth(int)\` that accepts any positive width, and a
\`SmallRectangle\` override that throws if the width exceeds 100. Code written
against \`Rectangle\` calling \`setWidth(500)\` is correct and now throws. The caller
did nothing wrong and has no way to know which object it was handed. Adding
validation in an override feels responsible and is usually a violation.

### 6. What is the real lesson of the square-rectangle problem?

Not that squares are not rectangles — geometrically they are. The lesson is that
the everyday **is-a** relationship is not the **is-substitutable-for**
relationship that inheritance actually requires. \`Rectangle\`'s contract carries
an implicit promise that width and height are independent, and \`Square\` cannot
keep that promise while remaining a square.

It also points at the cause: mutability. Make both immutable, so that \`withWidth\`
returns a new object rather than mutating, and the problem disappears entirely —
there is no invariant left to violate. The other fix is to stop relating them by
inheritance and have both implement a \`Shape\` interface with \`area()\`. They
share an operation, not a contract.

### 7. Where does the JDK violate this?

\`Collections.unmodifiableList\` and \`List.of\` return objects that satisfy the
\`List\` interface in shape and throw \`UnsupportedOperationException\` for \`add\`,
\`remove\` and \`set\`. Any method that takes a \`List\` and mutates it is correct
against the declared contract and broken by these arguments.

It is a deliberate trade — a clean immutable/mutable split in the collections
hierarchy would have doubled the interface count in 1998 — but it is a
violation, and the general rule stands: **a method that throws "you cannot call
this" is an admission that the type is too wide.**

### 8. How do you prevent LSP violations in a codebase?

Four things, in order of how much they actually help.

Prefer **composition over inheritance** — most violations are inheritance used
for code reuse rather than substitutability, and holding an instance instead of
extending one lets you expose only what you can honour. Write the contract
**down**, because preconditions that live in someone's head cannot be preserved
by a subclass author who never met them. Enforce it with **contract tests** — a
test suite written against the interface and run against every implementation,
which is the only mechanism that reliably catches this. And mark classes not
designed for extension \`final\` or \`sealed\`, which turns a request into something
the compiler enforces.

## Hard

### 9. Does LSP apply outside inheritance?

Yes, and in modern code that is where it matters most. Its real subject is any
substitution behind an abstraction. Two implementations of a \`PaymentGateway\`
interface must be substitutable for exactly the same reason a subclass must be:
if one throws on a duplicate charge and the other silently succeeds, then every
caller has to know which provider it has, the interface stops being an
abstraction, and you have paid for indirection while keeping the coupling.

The same applies to two \`Repository\` implementations where one is transactional
and one is not, or two \`Cache\` implementations where one can silently evict and
the other cannot. Those differences are postconditions, and callers written
against the interface will assume the stronger one.

### 10. Are covariant return types and Java's exception rules enough to enforce LSP?

They cover a fraction of it, and knowing which fraction is the point of the
question.

Java does enforce two of the conditions structurally. **Return types** may be
covariant — an override may return a narrower type, which is postcondition
strengthening and is allowed. **Checked exceptions** may not be broadened — an
override cannot declare a checked exception the base did not, which is a
precondition/postcondition rule enforced by the compiler.

Everything else is unenforced. **Parameter types are invariant**, not
contravariant — Java will not let an override widen its parameter type at all,
so it forbids something LSP permits rather than catching something LSP forbids.
Unchecked exceptions are completely free: an override can throw
\`IllegalStateException\` or \`UnsupportedOperationException\` wherever it likes,
which is how the JDK's own violation works. And nothing at all is checked about
values, invariants or state transitions — that a \`balance()\` override may now
return a negative number is invisible to the compiler.

So the honest answer is that the type system checks the signatures and you are
on your own for the semantics. Which is why the practical defences are the ones
above — contract tests, documented invariants, \`sealed\`, and preferring
composition when you only wanted the code rather than the contract.
`,zi=`# I — Interface Segregation

> **No client should be forced to depend on methods it does not use.**

The shortest of the five and the easiest to state. It came out of a real job:
Martin was working on a Xerox printer system where one enormous \`Job\` class
served every function of the machine, and a change to the stapling code meant
recompiling and redeploying everything, including firmware that had no idea what
a staple was.

That is the principle in one sentence: **a dependency you do not use can still
hurt you.**

## What "fat" means

An interface is too fat when its implementers cannot honestly implement all of
it, or its callers only ever want a slice of it. Both symptoms are visible.

\`\`\`java
interface Worker {
    void work();
    void eat();
    void sleep();
    void attendStandup();
}

class Human implements Worker {
    public void work() { ... }
    public void eat() { ... }
    public void sleep() { ... }
    public void attendStandup() { ... }
}

class Robot implements Worker {
    public void work() { ... }
    public void eat()  { throw new UnsupportedOperationException(); }
    public void sleep() { throw new UnsupportedOperationException(); }
    public void attendStandup() { throw new UnsupportedOperationException(); }
}
\`\`\`

\`Robot\` is carrying three methods it has no answer for. Notice what that costs:

- Every caller holding a \`Worker\` can call \`eat()\`, and the compiler will let
  them, so the failure is at runtime.
- Anyone reading \`Worker\` cannot tell which methods are actually safe to call.
- Those three \`UnsupportedOperationException\`s are also [Liskov
  violations](#/design/liskov-substitution) — a \`Robot\` cannot stand in for a
  \`Worker\` where the caller eats.

That overlap is not a coincidence. **A fat interface is what forces implementers
into LSP violations**, which is why the two letters are so often discussed
together: ISP is the structural cause, LSP is the runtime symptom.

![Two implementers of one interface, one of which can only answer a quarter of it](diagrams/interface-segregation-notes-who-can-answer.jpg)

### Segregated

\`\`\`java
interface Workable  { void work(); }
interface Feedable  { void eat(); }
interface Restable  { void sleep(); }

class Human implements Workable, Feedable, Restable { ... }
class Robot implements Workable { ... }
\`\`\`

Now \`Robot\` implements exactly what it can do, and a method that only needs work
done takes a \`Workable\` — so it is impossible for it to call \`eat()\`, rather than
merely unwise.

## The compile-time cost that started it

The Xerox story is worth keeping because it names the cost that a small example
hides. When \`Job\` had forty methods and every module depended on \`Job\`:

- A change to any one method **recompiled every module**, in a build that took
  hours.
- Every module had to be **redeployed and retested**, including ones that could
  not possibly be affected.

Java's incremental compilation makes this less dramatic than it was in C++, but
the same effect is alive and expensive in modern systems, just at a different
granularity:

| Where | The cost of a fat interface |
| --- | --- |
| **Modules** | A change to a shared interface rebuilds and retests every consumer |
| **Services** | A shared client library forces every service to upgrade in step |
| **APIs** | A published interface cannot lose a method without breaking somebody |
| **Tests** | A mock of a 20-method interface needs 20 stubs to test one call |

That last row is the one you feel every day. If setting up a test requires
stubbing methods the test never calls, the interface is too fat — a
**mock that needs more lines than the assertion** is ISP telling you something.

![Three callers of a six-method interface, each using a different pair](diagrams/interface-segregation-notes-disjoint-callers.jpg)

## Role interfaces

The good version of this principle is not "make interfaces small". It is:

> **Define interfaces from the client's side, named for the role the client
> needs.**

The difference matters. "Small" is arbitrary; "one role" is a criterion. An
interface should describe what a *caller* wants done, not enumerate what an
*implementation* can do.

\`\`\`java
// Header interfaces — one per implementation, named after the class.
// The class decides what the interface says.
interface OrderServiceInterface {
    Order create(Cart cart);
    void cancel(OrderId id);
    List<Order> history(CustomerId id);
    Invoice invoice(OrderId id);
    void refund(OrderId id, BigDecimal amount);
}

// Role interfaces — one per use, named after the need.
// The caller decides what the interface says.
interface OrderPlacer   { Order create(Cart cart); }
interface OrderHistory  { List<Order> history(CustomerId id); }
interface Refunder      { void refund(OrderId id, BigDecimal amount); }
\`\`\`

One class can implement all three role interfaces — segregating the *interface*
does not mean splitting the *class*. That is the point most often missed. The
checkout page depends on \`OrderPlacer\` and knows nothing about refunds; the
admin console depends on \`Refunder\`; neither recompiles or retests when the other
one's method signature changes.

This also tends to produce better names. \`OrderPlacer\` says what it is for.
\`OrderServiceInterface\` says only that somebody needed an interface.

## Where it goes wrong

**One method per interface is not the goal.** Taken literally, "no client should
depend on what it does not use" argues for splitting until every interface has
exactly one method, at which point you have a codebase where every dependency is
a \`Consumer<T>\` with a domain name and nobody can find anything. Cohesion is
still a constraint: methods that clients always use *together* belong together.
\`Iterator\` has three methods and is perfectly segregated, because no caller has
ever wanted \`next()\` without \`hasNext()\`.

**It is not an excuse to skip interfaces.** ISP says how to shape an abstraction
you need. It does not say every class needs one.

**Java's \`default\` methods are not a fix.** It is tempting to widen an interface
and give the new method a default implementation so nobody breaks. That solves
the compile error and keeps the design problem: implementers now inherit
behaviour they never agreed to, and callers still see a method that may do
nothing. Defaults are for evolving a published interface without breaking the
world — a migration tool, not a design.

## The smell list

You have an ISP problem when:

- An implementation throws \`UnsupportedOperationException\` or does nothing in
  some methods
- A test needs stubs for methods it never calls
- An interface's name ends in \`Manager\`, \`Service\`, \`Handler\` or \`Helper\` and it
  has more than about five methods
- Different callers use disjoint subsets of the same interface — the clearest
  signal of all, and the one that tells you exactly where to cut
- A change to a method causes a rebuild in modules that never call it

The last two are the ones to look for on a real codebase, because they are
observable without an opinion.

![How much a fat interface costs at each scale, from a published API down to one caller](diagrams/interface-segregation-notes-by-scale.jpg)
`,Bi=`# I — Interface Segregation — quick revision

The chapter in a page. If a line here is news, the **Notes** part is where it
comes from.

## The statement

- **No client should be forced to depend on methods it does not use.**
- Origin: a Xerox printer system with one giant \`Job\` class. Changing the
  stapling code recompiled and redeployed **everything**, including firmware that
  knew nothing about staples.
- In one sentence: **a dependency you do not use can still hurt you.**

## The example

\`Worker { work(); eat(); sleep(); attendStandup(); }\` — \`Robot\` implements it and
throws \`UnsupportedOperationException\` for three of them.

- Callers can call \`eat()\` on a robot; the compiler allows it, so it fails at
  **runtime**.
- Those exceptions are also **LSP violations**.
- **A fat interface is what forces implementers into LSP violations.** ISP is the
  structural cause; LSP is the runtime symptom. That is why the two are always
  discussed together.

Fix: \`Workable\`, \`Feedable\`, \`Restable\`. \`Robot implements Workable\` only, and a
method needing work done takes a \`Workable\` — so calling \`eat()\` becomes
**impossible**, not merely unwise.

## What the fat costs, by scale

| Where | Cost |
| --- | --- |
| **Modules** | A change to the interface rebuilds and retests every consumer |
| **Services** | A shared client library forces everyone to upgrade in step |
| **APIs** | A published interface cannot drop a method without breaking somebody |
| **Tests** | Mocking a 20-method interface needs 20 stubs to test one call |

The test row is the one you feel daily: **a mock longer than the assertion** is
ISP telling you something.

## Role interfaces — the actual technique

- Not "make interfaces small". **Define the interface from the client's side,
  named for the role the client needs.**
- "Small" is arbitrary; "one role" is a criterion.
- \`OrderPlacer\`, \`OrderHistory\`, \`Refunder\` — **not** \`OrderServiceInterface\`
  with five methods.
- **One class can implement all three.** Segregating the *interface* is not
  splitting the *class* — the most commonly missed point.
- Bonus: role names say what they are for. \`OrderServiceInterface\` only says
  somebody needed an interface.

## The traps

- **One method per interface is not the goal.** Cohesion still applies —
  \`Iterator\` has three methods and is perfectly segregated, because nobody has
  ever wanted \`next()\` without \`hasNext()\`.
- **Not an excuse to skip interfaces.** ISP shapes an abstraction you need; it
  does not say every class needs one.
- **\`default\` methods are not a fix.** They silence the compile error and keep
  the design problem — implementers inherit behaviour they never agreed to. A
  migration tool for published interfaces, not a design.

## The smell list

- An implementation that **throws** or **does nothing** in some methods
- A test needing stubs for methods it never calls
- \`…Manager\` / \`…Service\` / \`…Handler\` with more than ~5 methods
- **Different callers using disjoint subsets** — the clearest signal, and it
  tells you exactly where to cut
- A change causing rebuilds in modules that never call the method

## The line to say in an interview

*"The interface belongs to the caller, not to the class implementing it. Name it
for the role the caller needs — one class can implement several. Segregating the
interface isn't splitting the class."*
`,Vi=`# I — Interface Segregation — interview questions

Nine questions. The reliable follow-up on this one is "how is it different from
Single Responsibility?", so have that answer ready before you need it.

## Easy

### 1. State the Interface Segregation Principle.

No client should be forced to depend on methods it does not use. Prefer several
small, role-specific interfaces to one general-purpose one.

### 2. Where did it come from?

A Xerox printer system Robert Martin consulted on. One \`Job\` class served every
function of the machine, so a change to the stapling code recompiled and
redeployed every module — including firmware with no concept of stapling. The
principle is the generalisation of that: a dependency you never use can still
cost you.

### 3. Give an example of a violation.

A \`Worker\` interface with \`work()\`, \`eat()\`, \`sleep()\` and \`attendStandup()\`,
implemented by a \`Robot\` that throws \`UnsupportedOperationException\` for three
of them. The robot is carrying methods it has no answer for, and any caller
holding a \`Worker\` can call them and find out at runtime.

## Medium

### 4. How is ISP different from SRP? They sound like the same idea.

They are the same instinct applied to different things. SRP is about the
**class** — how many actors can force this implementation to change. ISP is
about the **interface** — how many different clients depend on this contract,
and whether they use disjoint parts of it.

The distinction is sharpest in the case where they disagree: one class can
legitimately implement three role interfaces. That is ISP satisfied — each
caller depends only on what it uses — while the class remains one cohesive unit
with one reason to change. Segregating the interface is not splitting the class,
and treating them as the same rule is how people end up splitting classes that
should not be split.

### 5. What is the relationship between ISP and LSP?

ISP is the cause, LSP is the symptom. When an interface is too wide, some
implementer cannot honestly satisfy all of it, so it throws
\`UnsupportedOperationException\` or silently does nothing — and that is a Liskov
violation, because a caller written against the interface now breaks depending
on which implementation it was handed. Narrowing the interface removes the
pressure that produced the violation. If you find yourself writing a stub method
that throws, the fix is usually at the interface, not at the class.

### 6. Does ISP mean every interface should have one method?

No. Cohesion is still a constraint, and taken literally the rule would push you
towards a codebase of single-method interfaces where nothing can be found.
\`Iterator\` has three methods and is perfectly segregated, because no caller has
ever wanted \`next()\` without \`hasNext()\`. The criterion is not size but
**role**: methods that clients always use together belong in the same interface;
methods that different clients use in disjoint subsets do not.

### 7. How do you decide where to cut a fat interface?

By looking at the callers, not the implementation. List every place the interface
is used and record which methods each one actually calls. If those sets are
disjoint, they are your role interfaces and the boundaries are already drawn for
you — no judgement required. If they all overlap heavily, the interface is
cohesive and should be left alone.

The other useful signal is the test suite: if setting up a test requires stubbing
methods that the test never calls, the thing under test is depending on more than
it uses. A mock that is longer than the assertion is ISP telling you where the
cut goes.

### 8. Are Java's \`default\` methods a solution to ISP problems?

No, and I would push back on that suggestion. Defaults exist so a published
interface can gain a method without breaking every existing implementer — an
evolution tool, and a good one; \`Collection.stream()\` could not have shipped
without them.

But using them to widen an interface hides a design problem rather than solving
it. Implementers inherit behaviour they never agreed to and may not want, callers
still see a method that might do nothing useful, and the interface is still fat —
you have only removed the compile error that was telling you so. Fixing the
design means splitting by role; defaults are what you reach for when you cannot,
because the interface is public and the world depends on it.

## Hard

### 9. Where does ISP matter most, and where does it barely matter at all?

It scales with how expensive a rebuild-and-redeploy is for the people on the
other side of the interface, which means it matters most where you do not control
the consumers.

At the extreme: a published library or a service's shared client library. There,
a method you add is a method every consumer inherits, and a method you remove
breaks them. Every consumer takes the risk of a change none of them asked for,
and the coordination cost of an upgrade is real money. Segregating there pays for
itself immediately.

In the middle: modules within one build, where a fat interface causes wider
rebuilds, wider test runs and mocks nobody wants to write. Worth doing, and the
test-setup pain is usually what makes the case.

At the low end: an interface with one implementation, used by one caller, inside
one class's package. There, segregating buys nothing — there is no second client
to protect and no independent deploy to decouple — and the extra types cost more
in reading than they save. I would leave that alone and revisit when a second
caller appears with a different slice of the methods, because that second caller
is the evidence that tells you where the cut actually goes.
`,Hi=`# D — Dependency Inversion

Two sentences, and the second one is the one people forget:

> **A. High-level modules should not depend on low-level modules. Both should
> depend on abstractions.**
>
> **B. Abstractions should not depend on details. Details should depend on
> abstractions.**

Almost everyone can quote sentence A. Sentence B is what makes it *inversion*
rather than just "use interfaces", and it is the difference between a design
that actually decoupled something and one that added a file.

## What is being inverted

In an unmanaged design, dependencies point the same way as the calls. The
business logic calls the database, so the business logic depends on the
database:

\`\`\`
OrderService  ──────►  MySqlOrderRepository
  (policy)               (detail)
\`\`\`

That is the wrong way round, because **policy is the valuable, stable part and
detail is the replaceable part**. The thing that matters is depending on the
thing that does not.

Inverting it means introducing an interface — but the crucial part is **where
the interface lives**:

\`\`\`
OrderService  ──────►  OrderRepository  ◄──────  MySqlOrderRepository
  (policy)              (abstraction,              (detail)
                        owned by policy)
\`\`\`

Now the arrow from the detail points *up*, against the direction of the call.
The database module depends on the business module's interface; the business
module knows nothing about databases. At run time \`OrderService\` still ends up
executing SQL. At **compile** time it has no idea SQL exists.

That last distinction is the whole idea. Dependency inversion is about the
direction of **source-code dependencies**, not the direction of control flow.

![Policy and detail both depending on an abstraction that the policy owns](diagrams/dependency-inversion-notes-inversion.jpg)

## The ownership test

Here is how to tell whether you have inverted anything, and it is the question
that catches people who think Spring gave them DIP for free:

> **Who owns the interface — the caller, or the implementer?**

\`\`\`java
// NOT inverted. The interface is defined next to the JDBC code, in terms
// the database chose, and the business module imports the persistence package.
package com.shop.persistence;

public interface OrderDao {
    void insert(OrderRow row);
    OrderRow selectById(long id);
    List<OrderRow> selectByCustomer(long customerId, int limit, int offset);
}
\`\`\`

\`\`\`java
// Inverted. The interface is defined in the domain, in the domain's own
// language, because the domain is the thing that needs it.
package com.shop.domain;

public interface Orders {
    void save(Order order);
    Optional<Order> byId(OrderId id);
    List<Order> placedBy(CustomerId customer);
}
\`\`\`

The first version has an interface and no inversion. \`OrderRow\` is a database
shape; \`limit\` and \`offset\` are a database concern; the package name says who is
in charge. Swapping to a document store means rewriting the interface and every
caller.

The second is inverted. It speaks in \`Order\` and \`OrderId\`, it is declared by
the module that consumes it, and the JDBC implementation lives in another package
that depends *inward* on this one. That implementation can be replaced without
the domain noticing.

![The same repository interface written in database language and in domain language](diagrams/dependency-inversion-notes-ownership.jpg)

**The interface belongs to the client.** If you remember one line from this
chapter, that is the one — and note that it is the same sentence [Interface
Segregation](#/design/interface-segregation) arrives at from the other
direction.

## DIP is not dependency injection

They are constantly conflated, including by people who should know better, and
distinguishing them is a reliable interview point.

| | What it is |
| --- | --- |
| **Dependency Inversion** | A **principle** about which way source dependencies point and who owns the abstraction |
| **Dependency Injection** | A **technique** for giving an object its collaborators from outside instead of constructing them |
| **IoC container** | A **tool** — Spring, Guice — that performs injection for you |

You can do injection and violate inversion completely:

\`\`\`java
@Service
class OrderService {
    private final MySqlOrderRepository repository;   // injected, and concrete

    OrderService(MySqlOrderRepository repository) { this.repository = repository; }
}
\`\`\`

Spring is wiring that. It is still a high-level module depending on a
low-level detail. And you can invert without any container at all — a constructor
taking an interface, called from \`main\`, is complete dependency inversion with no
framework involved.

The relationship is that injection is the usual *mechanism* by which inversion is
achieved, because something still has to choose the implementation, and that
something should be neither the caller nor the callee. It should be the entry
point: \`main\`, a configuration class, a container. That component is the only
one allowed to know every concrete type, and it exists precisely so that nothing
else has to.

## Why \`new\` is the tell

The other quick diagnostic:

\`\`\`java
class OrderService {
    private final EmailSender email = new SmtpEmailSender("smtp.shop.com", 587);
}
\`\`\`

\`new\` on a concrete implementation inside a class that has policy in it means the
dependency is welded in. You cannot test it without a mail server, you cannot
swap it, and the port number is now part of the business logic's source.

This does **not** mean \`new\` is forbidden. Creating value objects, records,
collections, \`StringBuilder\`, exceptions — all fine, because those are not
dependencies you would ever want to replace. The rule is narrower:

> **Do not construct things that reach outside the process, hold policy, or that
> you would want to replace in a test.**

## What it buys you

**Testing** is the benefit you feel first and the one worth citing. A service
depending on an interface can be tested with a two-line fake and no Docker
container. A service depending on \`MySqlOrderRepository\` needs a database to
prove that a discount is calculated correctly, which is why suites like that
take forty minutes and get skipped.

**Deferred decisions** is the benefit architects cite. If the domain does not
know what the database is, the choice of database is not blocking the domain
being written — and it stays changeable for longer.

![A domain core surrounded by adapters that depend inward on it](diagrams/dependency-inversion-notes-ports-adapters.jpg)

**Boundaries you can see.** Inverted dependencies all point inward, which means
the layer diagram is a fact about the source code rather than a picture in a
wiki. That is the whole content of hexagonal architecture, ports and adapters,
and Clean Architecture: they are DIP applied at the module level with a naming
convention.

\`\`\`
        ┌──────────────────────────────────┐
        │            Domain                │   knows nothing outside itself
        │   Order, OrderId, Orders (port)  │
        └──────────────▲───────────────────┘
                       │ implements
        ┌──────────────┴───────────────────┐
        │           Adapters               │   knows the domain
        │  JdbcOrders, SmtpMailer, HttpApi │
        └──────────────────────────────────┘
\`\`\`

## Where it goes wrong

**An interface with one implementation, forever.** The most common
over-application. If there is exactly one implementation and there will only ever
be one, the interface is not decoupling anything — it is a redirect the reader
has to follow, and "Go to definition" now lands on a file with no code in it. The
honest exceptions are testing (a fake is a second implementation) and module
boundaries you genuinely want to keep clean.

**Inverting inside a layer.** DIP pays at boundaries — between policy and
mechanism, between your code and the outside world. Applying it between two
classes that live in the same package and change together buys nothing and
costs a hop.

**Abstractions that leak the detail anyway.** An \`OrderRepository\` whose methods
take a \`Specification\` object that compiles to SQL has not inverted anything; the
database's model has just been re-exported through an interface. The test is
whether you could implement the interface against something completely
different — an in-memory map, a file, an HTTP call — without contorting it. If
you could not, sentence B is being violated: the abstraction depends on the
detail.
`,Ui=`# D — Dependency Inversion — quick revision

The chapter in a page. If a line here is news, the **Notes** part is where it
comes from.

## The statement — both halves

- **A.** High-level modules should not depend on low-level modules. **Both should
  depend on abstractions.**
- **B.** Abstractions should not depend on details. **Details should depend on
  abstractions.**

Everyone quotes A. **B is what makes it *inversion*** rather than "use
interfaces".

## What is inverted

- Unmanaged: \`OrderService ──► MySqlOrderRepository\`. Dependency follows the
  call. Wrong way round — **policy is the stable, valuable part**.
- Inverted: \`OrderService ──► OrderRepository ◄── MySqlOrderRepository\`. The
  detail's arrow points **up**, against the call.
- At **run time** the service still executes SQL. At **compile time** it does not
  know SQL exists.
- **DIP is about the direction of source-code dependencies, not control flow.**

## The ownership test

> **Who owns the interface — the caller or the implementer?**

- Not inverted: \`package com.shop.persistence; interface OrderDao { insert(OrderRow), selectById(long), … }\`
  — database shapes, database vocabulary, database package.
- Inverted: \`package com.shop.domain; interface Orders { save(Order), byId(OrderId), … }\`
  — domain vocabulary, declared by the module that **needs** it.
- **The interface belongs to the client.** Same sentence
  [ISP](#/design/interface-segregation) arrives at from the other side.

## DIP ≠ DI ≠ Spring

| | |
| --- | --- |
| **Dependency Inversion** | A **principle** — which way dependencies point, who owns the abstraction |
| **Dependency Injection** | A **technique** — collaborators handed in from outside |
| **IoC container** | A **tool** — Spring, Guice |

- You can inject and still violate inversion: \`OrderService(MySqlOrderRepository repo)\`
  is injected **and concrete**.
- You can invert with **no framework**: a constructor taking an interface, called
  from \`main\`.
- Something must pick the implementation — that job belongs to the **entry
  point** (\`main\`, a config class, the container). It is the only place allowed
  to know every concrete type.

## The \`new\` tell

- \`new SmtpEmailSender("smtp.shop.com", 587)\` inside a policy class = welded in,
  untestable, port number now part of the business logic.
- **\`new\` is not forbidden.** Records, value objects, collections, exceptions —
  all fine.
- Rule: **do not construct things that reach outside the process, hold policy, or
  that you'd want to replace in a test.**

## What it buys

- **Testing** — a two-line fake instead of a Docker container. The benefit you
  feel first.
- **Deferred decisions** — the domain can be written before the database is
  chosen, and stays changeable longer.
- **Visible boundaries** — all arrows point inward, so the layer diagram is a
  fact about the source rather than a wiki picture. **Hexagonal / ports and
  adapters / Clean Architecture are all DIP at module scale.**

## Where it goes wrong

- **One implementation, forever.** Not decoupling — just a redirect where "Go to
  definition" lands on an empty file. Honest exceptions: a test fake counts as a
  second implementation; real module boundaries.
- **Inverting inside a layer.** DIP pays at boundaries. Between two classes in
  the same package that change together, it buys nothing.
- **Leaky abstraction.** A repository taking a \`Specification\` that compiles to
  SQL has re-exported the database model. Test: **could you implement it against
  an in-memory map without contorting it?** If not, sentence B is broken.

## The line to say in an interview

*"It's not 'use interfaces' — it's that the interface is declared by the module
that consumes it, in that module's language. Spring injecting a concrete class is
DI without DIP."*
`,Wi=`# D — Dependency Inversion — interview questions

Ten questions. Question 4 is the one that decides the conversation: most
candidates cannot separate the principle from the framework, and the ones who
can get asked better questions afterwards.

## Easy

### 1. State the Dependency Inversion Principle.

Two parts. High-level modules should not depend on low-level modules — both
should depend on abstractions. And abstractions should not depend on details;
details should depend on abstractions. The second half is the one that makes it
an *inversion* rather than just advice to use interfaces.

### 2. What is being inverted?

The direction of the source-code dependency relative to the direction of the
call. Normally the business logic calls the database, so it also depends on the
database. After inversion the business module declares an interface, the database
module implements it, and the dependency arrow from the detail points back up
towards the policy — against the flow of control.

### 3. Give an example of a violation.

A service that constructs its own collaborator:
\`private final EmailSender email = new SmtpEmailSender("smtp.shop.com", 587);\`.
The policy class now depends on a concrete implementation and on its
configuration; it cannot be tested without a mail server and cannot be pointed at
anything else.

## Medium

### 4. What is the difference between dependency inversion and dependency injection?

Inversion is a **principle** about which way dependencies point and who owns the
abstraction. Injection is a **technique** for handing an object its collaborators
from outside rather than letting it construct them. A container like Spring is a
**tool** that performs injection.

They are independent. You can inject and still violate inversion — \`OrderService\`
taking a \`MySqlOrderRepository\` in its constructor is injected and still a
high-level module depending on a low-level detail. And you can invert with no
framework at all: a constructor taking an interface, wired up in \`main\`, is
complete DIP.

The relationship is that injection is the usual mechanism for achieving
inversion, because something has to choose the implementation and it should be
neither the caller nor the callee. That job belongs to the entry point, which is
the only component allowed to know every concrete type.

### 5. How do you tell whether you have actually inverted anything?

Ask who owns the interface. If it lives in the persistence package, is named
after the implementation, and speaks in rows, columns, limits and offsets, then
nothing was inverted — the database's model was re-exported through an interface
and the business module still imports downwards.

If it lives in the domain, is named for what the domain needs, and speaks in
domain types, then it is inverted: the implementation package depends on the
domain package and not the other way round. The one-line version is **the
interface belongs to the client**.

### 6. Does every class need an interface?

No, and this is the most common over-application. An interface with exactly one
implementation that will never have another is not decoupling anything — it is a
hop the reader must follow, and "Go to definition" lands on a file with no
behaviour in it. Two honest exceptions: a test fake is a real second
implementation, and a genuine module boundary is worth keeping clean even at one
implementation. Everything else, I would leave concrete until there is a second
case.

### 7. Does DIP mean you should never use \`new\`?

No. \`new\` on records, value objects, collections, \`StringBuilder\` and exceptions
is fine — those are not things you would ever want to replace. The narrower rule
is: do not construct things that reach outside the process, that carry policy, or
that you would need to fake in a test. A database client, an HTTP client, a mail
sender, a clock and a random source are the usual list.

### 8. How does DIP relate to hexagonal or Clean Architecture?

Those architectures are DIP applied at module scale with a naming convention. The
domain declares **ports** — interfaces in its own language — and the outside
world supplies **adapters** that implement them. Because every adapter depends
inward on the domain and the domain depends on nothing, the layer diagram becomes
a fact you can verify from the import statements rather than a picture in a wiki.
Everything else about those architectures is vocabulary on top of that one rule.

## Hard

### 9. Give an example of an abstraction that satisfies the letter of DIP and violates its spirit.

A repository interface whose methods take a \`Specification\` object that is really
a SQL builder, or one that returns a \`Page\` with \`limit\` and \`offset\`, or one
that exposes \`flush()\` and \`detach()\` because JPA has them.

All of those have the interface in the right package and the arrows pointing the
right way, so the diagram passes. But the abstraction is describing the detail's
model, which is exactly what the second half of the principle forbids —
abstractions should not depend on details. The practical consequence is that the
substitution the interface was supposed to enable is impossible: you cannot
implement it against an in-memory map or an HTTP service without contorting it,
because it has assumed a relational database throughout.

The test I would apply is exactly that. Write a second implementation backed by a
\`HashMap\`. If it comes out naturally, the abstraction is real. If it needs
absurd stubs and no-ops, the abstraction is the database wearing a different
name, and the extra file bought nothing.

### 10. Where would you deliberately not apply DIP, and how do you argue that in review?

Inside a layer, and around anything stable enough that replacement is not a real
scenario.

Two classes in the same package that were written together and change together
gain nothing from an interface between them — there is no independent deploy to
decouple, no second implementation to enable, and no test seam worth having,
because a test of the pair is more valuable than a test of each with the other
mocked. The same goes for depending on the standard library: nobody extracts an
interface over \`String\` or \`List\`, and the reason is not that they are special
but that the probability of replacement is zero.

The argument I would make in review is about evidence rather than taste. The cost
of an abstraction is paid on every read, forever, and by everyone. The benefit is
paid out only if the substitution actually happens. So the question is not "could
this ever change?" — everything could — but "have I seen it change, or is there a
concrete plan?" A test fake counts as evidence. An architecture diagram from
someone who has left does not.

The counter-argument worth conceding is boundaries you do not control: anything
crossing the process edge — database, queue, third-party API, clock — should be
inverted from day one, because those *do* get replaced, they are the hardest
things to test against, and retrofitting the seam later means touching every
caller.
`,Gi=`# SOLID in practice

Five principles explained one at a time is the easy half. The hard half is that
they interact — applying one usually creates the conditions for another, and
applying all five to everything produces a codebase nobody can read.

This chapter is one ordinary service, refactored the whole way through with the
diff at each step, and then the other side of the argument: what
over-application looks like for each letter, and how SOLID fits with the rest of
the advice you have been given.

## The starting point

A real shape, the kind that exists in every codebase that grew rather than being
designed.

\`\`\`java
class OrderService {

    void placeOrder(Cart cart, String customerEmail, String shippingMethod) {

        // 1. Validation
        if (cart.items().isEmpty()) throw new IllegalArgumentException("empty cart");
        if (!customerEmail.contains("@")) throw new IllegalArgumentException("bad email");

        // 2. Pricing
        BigDecimal total = BigDecimal.ZERO;
        for (CartItem item : cart.items()) {
            total = total.add(item.price().multiply(BigDecimal.valueOf(item.quantity())));
        }

        // 3. Shipping — a branch per method
        if (shippingMethod.equals("STANDARD")) {
            total = total.add(BigDecimal.valueOf(5));
        } else if (shippingMethod.equals("EXPRESS")) {
            total = total.add(BigDecimal.valueOf(15));
        } else if (shippingMethod.equals("OVERNIGHT")) {
            total = total.add(BigDecimal.valueOf(30));
        }

        // 4. Persistence
        try (Connection c = DriverManager.getConnection("jdbc:mysql://localhost/shop")) {
            PreparedStatement ps = c.prepareStatement(
                "INSERT INTO orders (email, total) VALUES (?, ?)");
            ps.setString(1, customerEmail);
            ps.setBigDecimal(2, total);
            ps.executeUpdate();
        } catch (SQLException e) {
            throw new RuntimeException(e);
        }

        // 5. Notification
        Properties props = new Properties();
        props.put("mail.smtp.host", "smtp.shop.com");
        Session session = Session.getDefaultInstance(props);
        Message message = new MimeMessage(session);
        // ... build and send
    }
}
\`\`\`

Before touching it, name what is actually wrong — not "it's long":

- **Five actors** own this method: the product owner (validation), finance
  (pricing), logistics (shipping), the DBA (schema), and marketing (email
  wording). Any of the five can force a change here, and a change for one can
  break another. **SRP.**
- **A new shipping method reopens this file** — and the same \`if\` chain is
  already copied into the estimate page. **OCP.**
- **The class depends on MySQL and SMTP by name**, so it cannot be tested without
  both. **DIP.**

That is the diagnosis. Now the treatment, one principle at a time.

![One method holding five concerns, each owned by a different department](diagrams/solid-in-practice-notes-five-actors.jpg)

## Step 1 — SRP: split by actor

\`\`\`java
class OrderValidator {
    void validate(Cart cart, String email) {
        if (cart.items().isEmpty()) throw new IllegalArgumentException("empty cart");
        if (!email.contains("@")) throw new IllegalArgumentException("bad email");
    }
}

class Pricer {
    BigDecimal subtotal(Cart cart) {
        return cart.items().stream()
            .map(i -> i.price().multiply(BigDecimal.valueOf(i.quantity())))
            .reduce(BigDecimal.ZERO, BigDecimal::add);
    }
}

class OrderService {
    void placeOrder(Cart cart, String email, String shippingMethod) {
        new OrderValidator().validate(cart, email);
        BigDecimal total = new Pricer().subtotal(cart);
        // ... shipping, persistence, notification still inline
    }
}
\`\`\`

**What improved:** finance can change the pricing rule without reading the email
code, and the pricing logic is now testable on its own.

**What has not:** those \`new\` calls. \`OrderService\` is still welded to specific
implementations — it has less code and the same coupling. This is the halfway
state most refactors stop at, and it is worth noticing that it has bought less
than it looks like.

## Step 2 — OCP: close the shipping branch

\`\`\`java
interface ShippingMethod {
    String code();
    BigDecimal surcharge(Cart cart);
}

class Standard implements ShippingMethod {
    public String code() { return "STANDARD"; }
    public BigDecimal surcharge(Cart cart) { return BigDecimal.valueOf(5); }
}

class Overnight implements ShippingMethod {
    public String code() { return "OVERNIGHT"; }
    public BigDecimal surcharge(Cart cart) { return BigDecimal.valueOf(30); }
}
\`\`\`

**What improved:** adding *Drone* is one new file. Nothing that currently works
gets edited, so nothing that currently works needs re-testing.

**The judgement call:** this is only worth it because shipping methods have
already changed twice and the same conditional exists in two other places. Had
there been one shipping method and no history of change, the \`if\` would have
stayed — see the warning about closing against the wrong axis in the
[Open/Closed](#/design/open-closed) chapter.

## Step 3 — DIP: invert persistence and mail

The domain declares what it needs, in its own words:

\`\`\`java
// package com.shop.domain — owned by the business logic
interface Orders {
    void save(Order order);
}

interface CustomerNotifier {
    void orderPlaced(Order order);
}
\`\`\`

\`\`\`java
// package com.shop.adapters — depends inward on the domain
class JdbcOrders implements Orders {
    private final DataSource dataSource;

    JdbcOrders(DataSource dataSource) { this.dataSource = dataSource; }

    public void save(Order order) { /* SQL, using the injected DataSource */ }
}

class EmailNotifier implements CustomerNotifier {
    public void orderPlaced(Order order) { /* SMTP */ }
}
\`\`\`

**What improved:** \`OrderService\` can now be tested with an in-memory \`Orders\`
and a recording \`CustomerNotifier\` — no database, no mail server, milliseconds
instead of minutes. That is the benefit that actually changes how a team works.

Note the direction: \`Orders\` is declared beside \`OrderService\`, not beside
\`JdbcOrders\`. That is what makes it inversion rather than an interface.

## Step 4 — ISP: only what each caller uses

\`Orders\` above has one method, so it is already segregated. It is worth showing
what pressure it would come under:

\`\`\`java
// The version that grows over a year
interface Orders {
    void save(Order order);
    Optional<Order> byId(OrderId id);
    List<Order> placedBy(CustomerId customer);
    List<Order> awaitingDispatch();
    void markDispatched(OrderId id, TrackingNumber tracking);
    Report monthlyTotals(YearMonth month);
}
\`\`\`

Now the checkout flow, the warehouse console and the finance report all depend on
all six methods and use two each. Split by role, and one class can still
implement all three:

\`\`\`java
interface OrderWriter    { void save(Order order); }
interface DispatchQueue  { List<Order> awaitingDispatch(); void markDispatched(OrderId id, TrackingNumber t); }
interface OrderReporting { Report monthlyTotals(YearMonth month); }

class JdbcOrders implements OrderWriter, DispatchQueue, OrderReporting { ... }
\`\`\`

**What improved:** the checkout tests stub one method instead of six, and a
change to the reporting query cannot force the checkout module to rebuild.

## Step 5 — LSP: make sure the implementations agree

The interfaces exist; the remaining question is whether every implementation
means the same thing by them. This is the step people skip, and it is the one
that produces production incidents.

\`\`\`java
class JdbcOrders implements OrderWriter {
    public void save(Order order) { /* throws on duplicate order id */ }
}

class InMemoryOrders implements OrderWriter {
    public void save(Order order) { map.put(order.id(), order); }  // silently overwrites
}
\`\`\`

Both compile. Both satisfy the interface. And the test suite now proves a
behaviour that production does not have — the retry path that looks safe in tests
will double-charge against MySQL.

The fix is a **contract test**: one abstract test class written against
\`OrderWriter\`, extended once per implementation.

\`\`\`java
abstract class OrderWriterContract {
    abstract OrderWriter subject();

    @Test
    void rejects_a_duplicate_id() {
        OrderWriter orders = subject();
        orders.save(anOrder(ID));
        assertThrows(DuplicateOrderException.class, () -> orders.save(anOrder(ID)));
    }
}

class JdbcOrdersTest     extends OrderWriterContract { OrderWriter subject() { return new JdbcOrders(ds); } }
class InMemoryOrdersTest extends OrderWriterContract { OrderWriter subject() { return new InMemoryOrders(); } }
\`\`\`

The fake now has to be honest, or the build fails. This is the only mechanism
that reliably enforces Liskov, and almost nobody writes it.

![One contract test extended by the real implementation and the fake](diagrams/solid-in-practice-notes-contract-test.jpg)

## The assembled result

\`\`\`java
class OrderService {
    private final OrderValidator validator;
    private final Pricer pricer;
    private final Map<String, ShippingMethod> shipping;
    private final OrderWriter orders;
    private final CustomerNotifier notifier;

    OrderService(OrderValidator validator, Pricer pricer, List<ShippingMethod> shipping,
                 OrderWriter orders, CustomerNotifier notifier) {
        this.validator = validator;
        this.pricer = pricer;
        this.shipping = shipping.stream().collect(toMap(ShippingMethod::code, m -> m));
        this.orders = orders;
        this.notifier = notifier;
    }

    Order placeOrder(Cart cart, String email, String method) {
        validator.validate(cart, email);

        ShippingMethod chosen = shipping.get(method);
        if (chosen == null) throw new IllegalArgumentException("Unknown method: " + method);

        BigDecimal total = pricer.subtotal(cart).add(chosen.surcharge(cart));

        Order order = Order.newOrder(email, total);
        orders.save(order);
        notifier.orderPlaced(order);
        return order;
    }
}
\`\`\`

Read what this class now is: **the sequence of steps in placing an order, and
nothing else.** No SQL, no SMTP, no shipping rates, no validation rules. One
reason to change — the order of the steps.

The honest accounting: five files became about twelve. That is a real cost. What
was bought is that the twelve each have one owner, the test suite runs without
infrastructure, and adding a shipping method touches nothing that already works.

## Over-application: what each letter looks like taken too far

The other half of the job, and the half that interviews probe when they want to
know whether you have actually maintained a large codebase.

| Letter | Taken too far | What it costs |
| --- | --- | --- |
| **S** | A class per verb: \`OrderCreator\`, \`OrderValidator\`, \`OrderPricer\`, \`OrderPersister\`, \`OrderNotifier\` — one method each | No cohesion spread thin; nothing readable alone |
| **O** | An interface, a factory and a registry for a set that will always have one member | Indirection with no extension; the stack trace goes through three files |
| **L** | Refusing all inheritance and duplicating instead | Real duplication traded for an imagined violation |
| **I** | One method per interface, \`Consumer<T>\` with domain names | Nothing findable; the type system stops telling you what belongs together |
| **D** | An interface over every class, including value objects and pure functions | "Go to definition" always lands on an empty file |

The common thread: every one of these is the principle applied **without a
change in mind**. The principles are about the cost of change, so applying them
where nothing changes is pure cost.

## SOLID next to everything else

**DRY** — one authoritative representation of each piece of *knowledge*. Agrees
with SRP most of the time; conflicts when people de-duplicate code that merely
looks alike. Two identical rules owned by two teams are not duplication, and
merging them couples the teams. Same text, different owners, leave it.

**YAGNI** — the direct counterweight to OCP. YAGNI governs the first occurrence,
OCP governs the second. Write it concretely; when a second real case arrives,
build the seam along the axis those two actually differ on. That way the
abstraction comes from evidence rather than prediction.

**KISS** — the tiebreaker. When a principle and simplicity disagree and you
cannot name the change you are designing for, simplicity wins.

**Design patterns** — mostly SOLID with names. Strategy, Decorator, Template
Method and Observer are OCP mechanisms. Adapter and Repository are DIP
mechanisms. Facade is SRP's answer to "who runs the steps". Knowing the
principle is more useful than knowing the pattern, because the principle tells
you *when*.

**Law of Demeter** — do not reach through objects: \`a.getB().getC().doThing()\`.
Not part of SOLID but the same family, and it catches coupling the five miss.

## The order to apply them

If you are looking at a class like the one at the top of this page and want a
procedure rather than five principles:

1. **SRP first.** Find the actors and split. Everything else is easier once the
   pieces are the right size, and this step needs no guesses about the future.
2. **DIP next**, at the boundaries only — database, network, clock, mail. This
   is where the testability arrives, which is what makes every later step safe.
3. **OCP where you have evidence** — a conditional that has already grown twice,
   or one that has been copied.
4. **ISP when a second caller appears** wanting a different slice. The second
   caller tells you where the cut goes; before that you would be guessing.
5. **LSP as a check, continuously.** Not a refactoring step — a thing you verify
   with contract tests whenever there is more than one implementation.

![The order to apply the five principles, and what each step needs before it](diagrams/solid-in-practice-notes-the-order.jpg)

And the sixth rule, which is not in the acronym: **stop when the next change
would be cheaper than the abstraction that prevents it.**
`,Ki=`# SOLID in practice — quick revision

The chapter in a page. If a line here is news, the **Notes** part is where it
comes from.

## Diagnosing the god method

A \`placeOrder\` doing validation, pricing, a shipping \`if\` chain, JDBC and SMTP:

- **Five actors** own it — product, finance, logistics, DBA, marketing. **SRP.**
- **New shipping method reopens the file**, and the chain is already copied
  elsewhere. **OCP.**
- **MySQL and SMTP by name** → untestable without both. **DIP.**

Say the diagnosis in those terms, not "it's long".

## The refactor, step by step

1. **SRP** — split by actor: \`OrderValidator\`, \`Pricer\`. *Watch out:* if the
   caller still does \`new OrderValidator()\`, you have less code and **the same
   coupling**. This is where most refactors stop.
2. **OCP** — \`ShippingMethod\` interface, one class per method, \`Map<String, …>\`
   dispatch. Only worth it because it had **already changed twice** and was
   **already copied**.
3. **DIP** — domain declares \`Orders\` and \`CustomerNotifier\`; adapters implement
   them. Interface lives **beside the caller**, not beside the JDBC. Payoff:
   tests run in milliseconds with no infrastructure.
4. **ISP** — when \`Orders\` grows to six methods used two at a time by three
   callers, split into \`OrderWriter\` / \`DispatchQueue\` / \`OrderReporting\`. **One
   class still implements all three.**
5. **LSP** — the step everyone skips. \`JdbcOrders.save\` throws on a duplicate id;
   \`InMemoryOrders.save\` silently overwrites. Both compile, and the retry path
   that passes in tests **double-charges in production**.

## The contract test

- One abstract test class written against the **interface**, extended once per
  implementation.
- Forces the fake to be honest, or the build fails.
- **The only mechanism that reliably enforces Liskov**, and almost nobody writes
  it. Worth saying out loud in an interview.

## The end state

\`OrderService\` is **the sequence of steps and nothing else** — no SQL, no SMTP,
no rates, no rules. One reason to change: the order of the steps.

Honest accounting: **5 files → ~12**. Bought: one owner each, tests with no
infrastructure, new shipping method touches nothing that works.

## Over-application, per letter

| Letter | Too far | Cost |
| --- | --- | --- |
| **S** | A class per verb, one method each | No cohesion, spread thin |
| **O** | Interface + factory + registry for a set of one | Indirection with no extension |
| **L** | Refusing all inheritance | Real duplication for an imagined violation |
| **I** | One method per interface | Nothing findable |
| **D** | An interface over every class | "Go to definition" lands on an empty file |

Common thread: **the principle applied with no change in mind.** SOLID is about
the cost of change, so applying it where nothing changes is pure cost.

## Next to the other advice

- **DRY** — one representation of each piece of **knowledge**, not each piece of
  text. Same code, two owners → leave it duplicated.
- **YAGNI** — governs the **first** occurrence; OCP governs the **second**.
- **KISS** — the tiebreaker. If you cannot name the change you are designing for,
  simplicity wins.
- **Patterns** — Strategy/Decorator/Template Method/Observer = OCP.
  Adapter/Repository = DIP. Facade = SRP's answer to "who runs the steps".
- **Law of Demeter** — \`a.getB().getC().doThing()\`. Not in SOLID, same family.

## The order to apply them

1. **SRP** — split by actor. Needs no guess about the future.
2. **DIP** — at boundaries only (database, network, clock, mail). Testability
   arrives here, which makes every later step safe.
3. **OCP** — only where there is evidence: a conditional that grew twice or got
   copied.
4. **ISP** — when a **second caller** wants a different slice.
5. **LSP** — not a step, a continuous check via contract tests.

**Rule six:** stop when the next change would be cheaper than the abstraction
that prevents it.
`,qi=`# SOLID in practice — interview questions

Ten questions of the kind asked once the definitions are out of the way. These
are the ones that decide the level you get offered, because they are all really
asking the same thing: **do you know when not to?**

## Easy

### 1. Here is a 100-line \`placeOrder\` method. What is wrong with it?

Not "it is long" — that is the symptom. Concretely: five different actors can
force it to change, so a change requested by finance can break the email
marketing depends on. A new shipping method reopens a file that already works,
and the same conditional has been copied into the estimate page, so the two will
drift. And it names MySQL and SMTP directly, so it cannot be tested without both.

That is SRP, OCP and DIP in one method, and naming them that way is what turns
"clean it up" into a plan.

### 2. Which principle would you apply first?

SRP, because it needs no prediction. Finding the actors and splitting by them is
based on who owns the code today, not on a guess about what will change. Then
DIP at the boundaries — database, network, clock, mail — because that is where
testability comes from, and once the tests run in milliseconds every later
refactor is safe.

### 3. Which patterns implement which principles?

Strategy, Template Method, Decorator, Visitor and Observer are all Open/Closed
mechanisms — ways to add a case without editing the dispatcher. Adapter and
Repository are Dependency Inversion mechanisms. Facade is SRP's answer to "who
runs the sequence after I have split the class". The principle is the more useful
thing to know, because the pattern tells you *how* and the principle tells you
*when*.

## Medium

### 4. You split a class into five and the caller now does \`new Validator()\`, \`new Pricer()\`. What did you actually achieve?

Less code per file and exactly the same coupling. The caller still names concrete
implementations, still cannot be tested without them, and still has to be edited
if any of them changes shape. This is the halfway state most refactors stop at,
and it is worth recognising because it looks like progress in a diff. The
missing step is to take the collaborators through the constructor, which is what
turns the split into something you can substitute.

### 5. Two implementations of your repository — the JDBC one throws on a duplicate ID and the in-memory fake silently overwrites. Why does that matter, and how do you prevent it?

It matters because the test suite now proves a behaviour production does not
have. A retry path that looks idempotent against the fake will double-charge
against MySQL, and no test will ever catch it — the tests are the thing that is
wrong.

The prevention is a contract test: one abstract test class written against the
interface, extended once per implementation, so every implementation runs the
same assertions. The fake either behaves like the real thing or the build fails.
It is the only mechanism that reliably enforces Liskov, and it is rare enough
that saying it out loud usually changes the conversation.

### 6. How do you decide when an abstraction is worth its cost?

By whether the change it is designed for has evidence behind it. The cost of an
abstraction is paid on every read of the code, by everyone, forever. The benefit
is paid out only if the substitution actually happens.

So I look for: has this varied before? Is the same decision already duplicated
somewhere? Is there a second caller with a different need? Does testing require
it? Any of those is evidence. "It might change one day" is not — everything might
change, and that reasoning justifies abstracting everything, which is how you get
a codebase where every interface has one implementation.

### 7. What is the difference between DRY and SRP, and when do they conflict?

DRY is about knowledge having one authoritative representation; SRP is about a
module answering to one actor. They usually agree, because duplicated logic is
often a responsibility nobody named.

They conflict when two pieces of code look identical but are owned by different
people. Two teams with currently-identical validation rules are not duplication —
merging them couples the teams, violates SRP, and guarantees that the first
divergence gets handled by adding a boolean parameter to the shared method. The
rule I use is that DRY applies to duplicated *knowledge*, not duplicated *text*.
Same code, two owners, two reasons to change: leave it.

### 8. How do YAGNI and Open/Closed coexist? They seem to contradict each other.

They do contradict, and the resolution is timing rather than compromise. YAGNI
governs the first occurrence: write the concrete thing, do not build a seam for a
variation you have imagined. OCP governs the second: when a real second case
arrives, look at how it differs from the first and put the abstraction on that
axis.

The reason this ordering is right rather than merely diplomatic is that OCP
requires you to predict which axis will vary, and closing against one axis opens
you to another. One case gives you no information about the shape of the
variation; two cases give you almost all of it. Waiting is not laziness, it is
how you get the abstraction right.

## Hard

### 9. Describe a time SOLID made a codebase worse. What would you do differently?

The recognisable version is the anticipatory abstraction. A team expects payment
providers to multiply, so on day one they build a \`PaymentStrategy\` interface, a
factory, a registry and a configuration key. Three years later there is still one
implementation, every stack trace goes through two extra frames, and when the
second provider finally arrives it differs along an axis nobody predicted — it
needs a redirect and a webhook, not a synchronous charge — so the interface is
rewritten anyway and every mock in the test suite with it.

The lesson is not "do not abstract". It is that the abstraction was derived from
a prediction instead of from evidence. What I would do differently is write the
first provider concretely, keep it behind nothing but a well-named class, and let
the second provider's real requirements draw the interface. The refactor at that
point is smaller than the rewrite, and the resulting abstraction fits both cases
because both cases existed when it was designed.

The second recognisable version is over-splitting under SRP: a service per verb,
each one method long. That one is worse in a way that is harder to reverse,
because the cohesion has been thrown away rather than merely deferred, and
putting it back means understanding six files to reconstruct one workflow.

### 10. Are these principles still the right advice in 2026 — microservices, functional style, records and sealed types?

Mostly yes, with a shift in emphasis and one genuine demotion.

The two that got stronger are SRP and DIP, because they scaled up. SRP applied to
services is the service boundary question, and getting it wrong there produces a
distributed monolith — the same coupling as before, now with network calls
between the pieces. DIP at module scale is exactly what hexagonal architecture
and Clean Architecture are; the language is different but the rule is the same
one.

The one that got demoted is LSP, at least in its classic form. It was written for
deep inheritance hierarchies, and a codebase built on records, sealed interfaces
and composition has far fewer places to violate it by subclassing. But the
underlying idea survived the move and matters more than ever, because it applies
to any substitution behind an abstraction — two implementations of a gateway
interface that disagree about duplicate handling is the same violation, and in a
microservice world you have many more such interfaces.

The one that needs the most care is OCP. Sealed types and exhaustive \`switch\`
deliberately invert it: adding a case becomes a compile error at every use site,
and when the type set is fixed that is a feature. So the modern answer is not
"prefer polymorphism to switch" but "decide which side varies" — the expression
problem — and Java now gives you a first-class tool for either choice.

What has not changed at all is the framing. These are observations about the cost
of change, not laws. The correct amount of SOLID in a system is however much
makes the changes you actually get cheaper than they would otherwise be, and no
more than that.
`,Ji=`Eighty-eight questions, with the answers written out. Low-level design first —
objects, patterns, code quality — then high-level design: architecture, data,
scale, failure and the network underneath.

Two rounds hide behind one name. **LLD** hands you one feature and asks for
classes: a lift, a parking lot, a rate limiter. **HLD** hands you a product and
asks for boxes: a shortener, a chat app, a feed. Read the invitation — "design a
chess game" is one and "design WhatsApp" is the other — and remember what is
actually scored, which is requirements gathered before anything is drawn,
trade-offs said out loud, and knowing where your own design breaks.

## LLD — the ground rules

### 1. What is low-level design, and how is it different from high-level design?

High-level design decides the boxes — which services exist, what stores the
data, where the queue goes. Low-level design takes one of those boxes and
decides the classes inside it: their responsibilities, their interfaces and how
they collaborate. HLD is answerable with a diagram and no code; LLD is
answerable with class names, method signatures and a pattern or two. They are
usually separate interview rounds because they test separate skills.

### 2. What is the purpose of low-level design in software development?

To turn an architecture into something a team can actually build without each
member inventing their own version of it. It fixes the class structure, the
relationships, the interfaces and the error behaviour, so that the code that
gets written is the code that was agreed. Done well it is also where
extensibility is bought — most of the "we cannot add that feature" conversations
trace back to an LLD decision nobody wrote down.

### 3. How would you approach an LLD question you have never seen?

Requirements first, exactly as in the other round: what must the system do, what
is explicitly out of scope. Then name the nouns — they become your classes — and
the verbs, which become methods. Assign one responsibility per class, define the
interfaces between them, and only then reach for a pattern, if one fits. Say
what you are trading away as you go, and leave the last five minutes for
extension: "if they asked for X next, here is where it goes."

### 4. What factors decide which data structure you use?

The operations you need and how often you need them. A structure is a bet on
access pattern: a hash map buys constant lookup by key and gives up order; a
balanced tree keeps order and costs a logarithm; an array is unbeatable for
iteration and awful for insertion in the middle. After that, memory, thread
safety and whether the size is known in advance. Say the operation counts out
loud — "reads are far more common than writes here" — because that is the
sentence the choice actually follows from.

### 5. What factors decide which algorithm you use?

Input size and shape first: a quadratic sort is fine for ten items and unusable
for ten million, and nearly-sorted data changes the answer again. Then stability,
whether the data fits in memory, whether the machine can parallelise it, and
whether a predictable time matters more than a good average — a real-time system
often prefers a slower algorithm with less variance. Practicality counts too:
a library implementation that is correct beats a hand-rolled one that is
theoretically better.

### 6. What are UML behavioural diagrams, and when do you use each?

They describe what a system *does* rather than what it contains. A **sequence
diagram** shows messages between participants in time order — the right tool for
one flow, such as a login. A **state diagram** shows one object's states and the
events that move it between them — the right tool for anything with a lifecycle,
such as an order or a connection. An **activity diagram** is a flowchart of a
process, and a **use case diagram** shows actors and goals. In an interview,
sequence and state are the two worth being able to draw quickly.

### 7. How would you model a login flow as a sequence diagram?

Participants across the top — user, UI, authentication service, user store,
token service — and time running down. The user submits credentials; the UI
calls authenticate; the service fetches the stored record; it verifies the hash;
on success it asks the token service to issue a token, which is returned to the
UI and stored. Draw the failure path too: invalid credentials return an error
without disclosing which half was wrong, and repeated failures trigger a lockout.
An answer that only draws the happy path is the one that gets the follow-up.

### 8. How would you model a system with a state diagram?

Name the states an object can genuinely be in, the events that cause a change,
and the guard conditions on those transitions. For an order: *created → paid →
shipped → delivered*, with *cancelled* reachable from the first two and not the
last. The value is in what the diagram makes impossible — it says out loud that
you cannot cancel a delivered order — and that is exactly the rule that gets
lost when the same logic lives in scattered \`if\` statements.

### 9. Why is concurrency control important, and how do you handle it in LLD?

Because two threads touching the same state without coordination produce results
that no sequential execution could produce, and those bugs do not reproduce.
The tools, roughly in order of preference: do not share the state at all; make
it immutable; use the concurrent collections rather than wrapping ordinary ones;
and only then reach for explicit locks. When you do lock, keep the critical
section small, always acquire locks in the same order to avoid deadlock, and
consider an atomic compare-and-set instead of a lock for a single value.

### 10. How would you design logging and monitoring for a complex application?

Log in structured form — key-value or JSON, not prose — so it can be queried
rather than grepped, and carry a correlation id through every hop so one
request's journey can be reassembled. Use levels honestly, and never log secrets
or personal data. Then separate the three things people conflate: **logs** for
what happened in one request, **metrics** for aggregate behaviour over time, and
**traces** for where the time went across services. Alert on the metrics, not on
individual log lines, or the alerts get muted within a fortnight.

## LLD — the patterns

![The three families of design pattern as a tree](diagrams/design-interview-interview-pattern-families.jpg)


### 11. What are design patterns, and why do they matter?

Named solutions to problems that recur in object-oriented design. Their value is
not that they are clever — most are five classes and an interface — but that they
are *named*: "put a factory here" carries a page of meaning to anyone who knows
the vocabulary, and that shared vocabulary is most of what a design discussion
runs on. They also encode consequences somebody else has already discovered,
which is cheaper than discovering them yourself in production.

### 12. What are the categories of design patterns?

Three. **Creational** patterns are about how objects are made — singleton,
factory method, abstract factory, builder, prototype. **Structural** patterns are
about how objects are composed — adapter, decorator, proxy, facade, composite,
bridge, flyweight. **Behavioural** patterns are about how objects communicate —
strategy, observer, command, state, template method, chain of responsibility,
iterator, mediator, visitor. If you can place a pattern in its category and say
what varies in it, you know it well enough for an interview.

### 13. Explain the Singleton pattern and its use cases.

One instance for the whole process, with a global point of access. It suits
things that genuinely are single — a configuration registry, a connection pool,
a logger — and in Java the sane implementations are an \`enum\` or a static holder
class, both of which are thread-safe without a lock. Be ready for the follow-up,
because it is always asked: singletons are global state, they make testing hard
because you cannot substitute them, and they hide dependencies. Injecting one
instance is usually better than enforcing one instance.

### 14. What is the Factory pattern, and when would you use it?

It moves the decision of *which* concrete class to create out of the code that
uses it, so callers depend on an interface and not on a constructor. Use it when
the concrete type depends on configuration or input — a parser chosen by file
extension, a payment provider chosen by country — or when construction is
complicated enough to be worth naming. The benefit is that adding a new type
touches the factory and nothing else, which is the open/closed principle doing
its job.

### 15. What is the difference between a static factory method and the Factory pattern?

A static factory method is just a named constructor on the same class —
\`Optional.of\`, \`List.of\`, \`Integer.valueOf\`. It buys a meaningful name, the
freedom to return a cached instance or a subtype, and nothing more. The Factory
*pattern* is a separate abstraction whose whole purpose is that the choice of
concrete class can vary at run time and can be substituted. One is a convenience;
the other is a seam.

### 16. What is the Observer pattern, and where would you use it?

One object keeps a list of dependents and notifies them when its state changes,
so the subject knows nothing about them beyond an interface. It is the shape of
every event system: a UI updating when a model changes, an order service telling
email, analytics and inventory that an order was placed. The things to say next
are the failure modes — a slow observer blocks the subject unless notification is
asynchronous, an observer that forgets to unsubscribe is a memory leak, and
notification order should not be relied upon.

### 17. What is the Strategy pattern?

A family of interchangeable algorithms behind one interface, chosen at run time
by the caller. Sorting with a comparator, pricing with a discount rule, routing
with a different cost function — each is a strategy. It is the standard cure for
a \`switch\` statement that grows a branch per case, and it is worth naming what it
costs: more classes, and the choice has to be made somewhere, which is often a
factory.

### 18. How do the Proxy and Decorator patterns differ?

![A proxy and a decorator wrapping the same service](diagrams/design-interview-interview-proxy-decorator.jpg)

Structurally they are twins — both wrap an object and implement its interface.
The difference is intent. A **proxy** controls access: it may be lazy, remote,
or check permissions, and the caller believes it is talking to the real thing.
A **decorator** adds behaviour: it does the same job plus something extra, and
decorators are designed to stack. If you are asking "may this call proceed?" it
is a proxy; if you are asking "and also do this," it is a decorator.

### 19. What problem does the Chain of Responsibility pattern solve?

It decouples a request from whichever handler ends up dealing with it. Each
handler decides to process the request or pass it along, so senders do not know
the chain's shape and handlers can be reordered or inserted freely. Every
middleware stack, servlet filter chain and approval workflow is this pattern.
The risk is that a request falls off the end unhandled, so decide explicitly
whether the chain has a default terminal handler.

### 20. Explain how the Command pattern supports undo and redo.

The pattern turns a request into an object carrying everything needed to perform
it. Give that object an \`undo\` alongside its \`execute\`, keep executed commands on
a stack, and undo is popping one and calling \`undo\`; redo is a second stack fed
by what undo pops. It also buys queuing, logging and retrying for free, since a
command is a value you can store — which is why it appears wherever operations
need to be replayed.

### 21. What is the Null Object pattern?

Instead of returning \`null\` for "nothing here", return an instance that
implements the same interface and does nothing harmlessly — a logger that
discards, a discount that subtracts zero. Callers stop being littered with null
checks and stop throwing when one is forgotten. Use it where a do-nothing
behaviour is genuinely correct; where absence is an error the caller must handle,
\`Optional\` or an exception says so more honestly.

### 22. Can several patterns be combined? Give an example.

Routinely, and most real designs do. A factory returns strategies; the strategies
are decorated to add caching and metrics; the whole thing sits behind a facade
so callers see one method. Or: a composite tree of components, each visited by a
visitor, with an iterator walking it. The warning to voice is that each pattern
adds indirection, and three stacked on a simple problem is harder to read than
the \`if\` statement they replaced.

### 23. What should you consider before choosing a pattern?

Whether the problem it solves is one you actually have. A pattern is a trade —
flexibility along one axis, bought with indirection and more classes — so name
the axis you expect to vary. Then check the cost against the team, the lifetime
of the code and whether a simpler construct would do. "I do not need a pattern
here" is a valid and frequently correct answer, and saying it deliberately reads
as judgement rather than ignorance.

### 24. When should you avoid patterns, and how do you prevent over-engineering?

When the variation you are designing for is imagined rather than observed. The
guards are **YAGNI** — build it when it is needed, not when it is foreseen — and
**KISS** — the simplest thing that satisfies today's requirement. Two concrete
tests: if a pattern's abstraction has exactly one implementation and no plausible
second one, delete it; if a new joiner cannot trace one request through the code
in ten minutes, the indirection is costing more than it earns.

## LLD — objects and code quality

### 25. What are the SOLID principles, in one line each?

**S** — a class should have one reason to change. **O** — open for extension,
closed for modification, so new behaviour means new code rather than edited code.
**L** — a subtype must be usable anywhere its parent is, with no surprises.
**I** — no client should be forced to depend on methods it does not use.
**D** — depend on abstractions, and let the high-level module own the interface.
Each has a page of its own in this section, starting with
[why SOLID exists](#/design/solid-intro).

### 26. What is tight coupling, and why avoid it?

Two components are tightly coupled when changing one forces a change in the
other — usually because one knows the other's concrete type, its internals or
its construction. It makes change expensive, testing hard, and reuse impossible,
and it is what turns a one-line feature into a three-day one. The cures are the
usual ones: depend on interfaces, inject dependencies rather than constructing
them, and keep data private. The goal is not zero coupling, which is impossible,
but coupling to things that do not change.

### 27. What is dependency injection, and why is it useful?

A class is handed what it needs instead of creating it. That inverts who decides
the concrete type — the caller or a container, rather than the class itself —
which makes substitution possible: a fake repository in a test, a different
payment provider in another region. It is the practical technique that serves
[dependency inversion](#/design/dependency-inversion), and constructor injection
is the version worth preferring because it makes the dependency impossible to
forget.

### 28. What is the difference between abstraction and encapsulation?

Abstraction is about the *interface*: deciding what a thing does and hiding how
it does it, so callers deal with a simplified model. Encapsulation is about the
*data*: keeping state private and only allowing changes through methods that
maintain the invariants. One is a design act, the other an implementation
mechanism, and they support each other — you abstract to decide what to expose,
and encapsulate to make sure nothing else gets out.

### 29. What is polymorphism, and why does it matter in design?

Calling the same operation on different types and getting each type's own
behaviour, decided by the actual object rather than the declared one. It is what
lets a caller hold a list of \`Shape\` and draw all of them without knowing which
are circles. In design terms it is the mechanism behind almost every pattern
here — strategy, state, visitor and the rest are all polymorphism with a name
attached.

### 30. Interface or abstract class — how do you choose?

An interface is a contract with no state and no constructor, and a class may
implement many; use it when unrelated types need to be callable the same way, or
when you want to name a role. An abstract class may hold state and shared
implementation, and a class has only one parent; use it when subtypes genuinely
share substance rather than merely a shape. Default methods have blurred the
distinction, so the honest modern rule is: prefer an interface unless you need
shared state.

### 31. What is the difference between inheritance and composition?

Inheritance says a subtype *is* a kind of its parent and takes its
implementation; composition says an object *has* a collaborator and delegates to
it. Inheritance is the tighter coupling of the two — it exposes the parent's
internals, is fixed at compile time and breaks under
[Liskov](#/design/liskov-substitution) when the subtype cannot honour the
contract. Composition can be changed at run time and keeps the surface small,
which is why the standing advice is to prefer it and to use inheritance only for
genuine substitutability.

### 32. What is the difference between association, aggregation and composition?

![Aggregation and composition drawn as ownership of lifetime](diagrams/design-interview-interview-composition.jpg)

**Association** is any relationship at all — a student and a course know about
each other. **Aggregation** is a whole-part relationship where the part outlives
the whole: a department has employees, and deleting the department does not
delete the people. **Composition** is a whole-part relationship where the part
does not outlive the whole: an order and its line items, a house and its rooms.
The practical question is always ownership of lifetime, and that is what the
interviewer is checking you can articulate.

### 33. Why is modular design important?

Because it bounds how much of the system you have to understand to change one
thing. A module with a narrow, explicit interface can be tested, replaced,
reasoned about and worked on by a different person in parallel. The measure of a
good module boundary is not size — it is whether changes tend to stop at it, and
a module that is edited every time any feature ships is not a module.

### 34. What are code smells, and how do you deal with them?

Surface signs that the design underneath is wrong: long methods, large classes,
long parameter lists, duplicated code, feature envy, shotgun surgery, primitive
obsession. None is a bug and each is a hint. The response is refactoring with
tests in place — extract a method or a class, replace a conditional with
polymorphism, introduce a parameter object — and the discipline is to do it while
you are already in the code, not as a project of its own.

### 35. What is the KISS principle?

Keep it simple: prefer the least complicated solution that satisfies the
requirement, because complexity is paid for on every future read, not once at
the moment of writing. Practically it means fewer layers, plainer names, and
resisting the clever version. It is not an argument against structure — it is an
argument against structure that is not carrying its weight.

### 36. What is YAGNI, and how does it interact with extensibility?

"You aren't gonna need it" — do not build for requirements that have not
arrived. It conflicts, on the surface, with designing for extension, and the
resolution is that YAGNI targets *speculative features* while open/closed targets
*the axis you already know varies*. If you have added a third payment provider,
generality along that axis is evidence-based; if you are imagining a second
database vendor nobody has mentioned, it is not.

### 37. How do you handle versioning and backward compatibility?

Never change the meaning of an existing field or the shape of an existing
response; add, do not repurpose. Version at the boundary — a URI segment or a
media type for an API, a schema version for stored data — and support the old one
for a stated period. For persisted data, prefer additive migrations with
defaults so old and new code can run side by side during a deploy, which is what
a rolling release requires.

### 38. How would you design authentication and authorization for a distributed system?

Authenticate once at the edge and carry a signed, short-lived token — a JWT or
similar — so downstream services verify a signature instead of calling an
identity service on every request. Authorize on the resource side, with roles or
attributes checked where the data is, since only that service knows what the
data is. Then the practical parts: hash passwords with a slow algorithm such as
bcrypt or argon2, support token revocation through short lifetimes plus a refresh
flow, and use mutual TLS between services if the network is not trusted — see
[HTTPS](#/advanced/deep-https/advanced).

### 39. What does database normalization buy you, and when do you denormalise?

Normalising removes redundancy by splitting data into tables keyed by their
dependencies, so one fact is stored in one place; the payoff is that updates
cannot leave contradictory copies, and the cost is joins at read time. You
denormalise when reads dominate and the joins are the bottleneck — duplicating a
user's display name onto their posts, for example — and you accept in exchange
that you now have two copies to keep honest. The decision is a read/write ratio
question, not a purity question.

### 40. What is database indexing, and what does it cost?

An index is a separate ordered structure — usually a B-tree — mapping column
values to row locations, so a lookup becomes a logarithmic descent instead of a
full scan. It costs disk space and slows every write, because each insert,
update and delete must maintain every affected index. So index the columns you
filter and join on, watch for indexes nothing uses, and remember that an index
on a low-cardinality column such as a boolean rarely helps.

## HLD — architecture

### 41. What are the components of a high-level design?

![The request path from browser to database](diagrams/design-interview-interview-architecture.jpg)

Clients, an entry layer — DNS, CDN, load balancer, gateway — the services
themselves, the data stores, the caches, the asynchronous machinery of queues
and workers, and the cross-cutting concerns of monitoring, logging and security.
A good HLD answer names each box, says what it is responsible for, and draws the
arrows in the direction data actually flows. What separates a strong answer is
the second pass over the same diagram, marking where each box fails and what
happens then.

### 42. Monolith or microservices — how do you decide?

![One deployable against several services behind a gateway](diagrams/design-interview-interview-monolith-services.jpg)

By team and change patterns rather than by fashion. A monolith is one deployable
with in-process calls: simpler to build, test, debug and transact across, and it
is the right first answer for almost every new product. Microservices buy
independent deployment and scaling, and independent failure, at the price of
network calls, distributed transactions, versioned contracts and a great deal of
operational machinery. The honest answer in an interview is usually "start with a
modular monolith and split along the seams that hurt" — and then to name the
seams.

### 43. What is a service-oriented seam? Where would you split a system?

Along the boundaries where data and change are already separate: the parts that
scale differently, are owned by different teams, or have different availability
requirements. Splitting by technical layer — a "database service", a "logic
service" — is the classic mistake, because every feature then touches every
service. If two proposed services would need a distributed transaction between
them for an ordinary operation, they are one service.

### 44. Explain event-driven architecture and when it fits.

Services publish events describing what happened and other services react,
rather than one service calling the next. It decouples producers from consumers,
absorbs load spikes in the broker, and lets new consumers be added without
touching the producer. The costs are real: no end-to-end transaction, eventual
consistency by construction, harder debugging, and the need to handle duplicate
and out-of-order delivery — so consumers must be idempotent. It fits
notification, analytics and workflows; it fits a synchronous read badly.

### 45. What is the role of a message broker, and how do Kafka and RabbitMQ differ?

![A queue and a log, and how consumers read each](diagrams/design-interview-interview-broker.jpg)

A broker sits between producers and consumers so neither has to be available at
the same moment, and so a burst can be absorbed rather than dropped. **RabbitMQ**
is a traditional queue: it routes messages to consumers and deletes them once
acknowledged, which suits task distribution and per-message routing rules.
**Kafka** is an append-only log: messages are retained for a period and consumers
track their own offset, which suits event streaming, replay and multiple
independent consumers reading the same data.

### 46. Synchronous or asynchronous communication — which and why?

Synchronous when the caller genuinely cannot proceed without the answer, and can
tolerate the callee's latency and failure being its own. Asynchronous when the
work can happen later — sending email, generating a report, updating a search
index — because it decouples availability and smooths load. The rule worth
stating is that every synchronous call adds its callee's downtime to your own, so
a chain of five synchronous services cannot be more available than the product of
their availabilities.

### 47. How would you design an API gateway, and what belongs in it?

One entry point in front of many services, responsible for the things every
request needs and no service should repeat: TLS termination, authentication,
rate limiting, routing, request and response shaping, and metrics. It also lets
clients make one call where they would otherwise make five. The risks are that it
becomes a single point of failure — so it must be replicated and stateless — and
that business logic creeps into it, at which point every team is blocked on one
component.

### 48. What is the Circuit Breaker pattern?

A wrapper around a remote call that counts failures and, past a threshold, stops
making the call at all for a period — failing immediately instead. It exists
because retrying a dead dependency turns one service's outage into everyone's:
threads pile up waiting on timeouts until the caller dies too. After the cool-off
it allows a trial request and closes again if that succeeds. Pair it with a
sensible timeout and a fallback, since failing fast is only useful if you have
decided what to do instead.

### 49. What is service discovery, and why is it needed?

In a dynamic environment, instances come and go and their addresses are not
known in advance, so services find each other through a registry rather than
configuration. Instances register themselves and are health-checked;
clients either query the registry directly or a proxy does it for them, which is
what a load balancer or a service mesh sidecar is doing. DNS-based discovery is
the simplest workable version, with the caveat that DNS caching can outlive the
instance.

### 50. What is a reverse proxy, and how does it differ from a load balancer?

A reverse proxy sits in front of servers and answers on their behalf, adding TLS
termination, caching, compression, request routing and a security boundary. A
load balancer's specific job is distributing traffic across several equivalent
servers. Every load balancer is a reverse proxy; not every reverse proxy is
balancing anything — one in front of a single server is still useful for
everything else it does.

### 51. What role does containerization play in a system design?

It packages an application with its dependencies so that the thing tested is the
thing that runs, which removes the largest class of environment bugs. In
architecture terms it makes instances interchangeable and disposable, which is
what auto-scaling and rolling deployment need, and it gives an orchestrator the
means to place, restart and health-check them. It is not virtualisation of the
machine — containers share the host kernel, which is why they start in
milliseconds.

### 52. What is the difference between stateful and stateless services?

A stateless service keeps nothing between requests that it needs again, so any
instance can serve any request and scaling is a matter of adding instances. A
stateful service holds session data, connections or in-memory results, so
requests must reach the right instance and losing one loses something. The
standard move is to push state out into a shared store — a session in Redis, for
example — so the service itself becomes stateless and the hard problem is
isolated in one place designed for it.

## HLD — data

### 53. Relational or NoSQL — how do you choose?

Relational when the data is naturally related and you need joins, multi-row
transactions and a schema the database enforces — orders, ledgers, anything
where correctness is the point. NoSQL when the access pattern is simple and
known, the volume is large, and the shape varies: key-value for caches and
sessions, document for records read as a whole, wide-column for very large
write-heavy datasets, graph for relationship traversal. State the access pattern
first; the store follows from it.

### 54. What is the CAP theorem, and how does it shape a design?

![Two nodes either side of a network partition](diagrams/design-interview-interview-cap.jpg)


When a network partition splits a distributed store, you must choose between
answering with possibly stale data (availability) and refusing to answer
(consistency). Partition tolerance is not optional on a real network, so the
choice is really CP or AP. A bank ledger picks consistency and rejects writes
during a partition; a social feed picks availability and reconciles afterwards.
The subtlety worth adding is that the trade only bites *during* a partition —
the rest of the time you can have both, and PACELC is the extension that says so.

### 55. What is the difference between ACID and BASE?

ACID — atomicity, consistency, isolation, durability — is the relational
guarantee: a transaction happens entirely or not at all, and the database is
never left in an invalid state. BASE — basically available, soft state,
eventually consistent — is what large distributed stores offer instead: the
system stays up, replicas converge over time, and the application deals with
temporary disagreement. The design question is which data deserves which:
payments ACID, view counts BASE, in the same product.

### 56. How is sharding different from partitioning, and how do you choose a key?

![A router sending keys to three shards, each with a replica](diagrams/design-interview-interview-sharding.jpg)

Partitioning is splitting a table into pieces; sharding is partitioning across
*different machines*, so it adds scale but also cross-shard queries and
rebalancing. The shard key decides everything: it should spread load evenly, keep
data that is read together on one shard, and have high cardinality. A key such as
country creates hotspots; a hash of the user id spreads well but makes range
queries expensive. Say which queries you are optimising for, and admit which ones
you have made hard.

### 57. What is consistent hashing, and what problem does it solve?

![The key space before and after a node leaves](diagrams/design-interview-interview-consistent-hashing.jpg)


Hashing a key modulo the number of servers remaps almost every key when a server
is added or removed, which empties every cache at once. Consistent hashing places
servers and keys on a ring and assigns each key to the next server clockwise, so
adding or removing one only moves the keys in its arc — roughly \`1/n\` of them.
Virtual nodes are the necessary refinement: several ring positions per physical
server, so load stays even when servers differ in capacity or a neighbour leaves.

### 58. What is database replication, and what do the models cost?

Copying data to more than one machine, for availability, read scaling and
disaster recovery. **Synchronous** replication acknowledges only when a replica
has the write — no data loss, higher write latency. **Asynchronous** acknowledges
immediately — fast, with a window in which a failure loses recent writes.
Single-leader is the common arrangement, with reads served by followers, and the
consequence to mention is read-your-writes: a user who just wrote may read a
follower that has not caught up.

### 59. How do you keep data consistent across services?

Not with a distributed transaction, in most cases — two-phase commit couples
availability across every participant. The usual answer is the **saga**: a
sequence of local transactions, each publishing an event, with a compensating
action to undo each step if a later one fails. Pair it with the **outbox
pattern** so the state change and the event are written in one local transaction,
and make consumers idempotent, since at-least-once delivery is the realistic
guarantee.

### 60. How do you ensure data integrity?

At several layers, deliberately. Constraints in the database — keys, uniqueness,
foreign keys, checks — because that is the one place nothing can bypass.
Validation at the boundary for clear errors. Transactions so partial updates
cannot be observed. Optimistic concurrency with a version column or an
[ETag](#/advanced/deep-http/advanced) so two writers cannot silently overwrite
each other. Then checksums for anything transferred, and audit records for
anything you would need to reconstruct.

### 61. Explain caching and the update strategies.

![The cache-aside read path, hit and miss](diagrams/design-interview-interview-cache-aside.jpg)


A cache trades freshness for speed by keeping a copy nearer the reader.
**Cache-aside** — the application checks the cache, and on a miss loads from the
store and populates it — is the common default. **Read-through** hides that
inside the cache client. **Write-through** writes to cache and store together,
keeping them consistent at the cost of write latency. **Write-behind** writes to
the cache and flushes later, which is fastest and can lose data. **Write-around**
writes only to the store, which suits data that is rarely re-read.

### 62. What happens on a cache miss, and what is a cache stampede?

A miss means the value is not there, so the caller fetches it from the source and
usually populates the cache. The problem is what happens when a popular key
expires and a thousand requests miss at once: they all hit the database
simultaneously — a stampede, or thundering herd. The defences are a lock or
single-flight so only one request refills, staggered expiry times so keys do not
all die together, and serving stale content while a background refresh runs.

### 63. How does a cache decide what to remove when it is full?

By an eviction policy. **LRU** discards the least recently used and suits most
workloads; **LFU** discards the least frequently used and suits stable
popularity; **FIFO** is simple and rarely best; **TTL** removes by age regardless
of use. Real caches such as Redis combine them and approximate LRU rather than
maintaining exact order, because exactness costs more than it is worth.

### 64. What is cache invalidation, and why is it hard?

Making sure a cached copy does not outlive the truth. It is hard because the
cache does not know when the source changed, so you are choosing between
expiring by time — simple, and either too slow or too eager — and invalidating on
write, which is precise and requires every writer to know every cache key
affected. Add multiple caches at several layers and one missed invalidation
serves stale data indefinitely. Tag-based purging and short TTLs are the
practical compromise.

### 65. Explain distributed caching and its advantages.

A cache spread across several nodes, with keys assigned by consistent hashing, so
the cached set is larger than one machine's memory and survives a node loss. It
also gives every application instance the same view, which a local in-process
cache cannot. The costs are a network hop per lookup, serialisation, and the need
to think about what happens during a rebalance — plus the reminder that a cache
holding personal data is a store that needs the same protections as any other.

### 66. Explain a CDN and what belongs on it.

A network of edge servers that hold copies of your content close to readers, so
the request never crosses an ocean. Static assets — images, video, CSS,
JavaScript — are the obvious content, cached by URL, which is why hashed file
names and long \`max-age\` matter; many CDNs will also cache whole HTML responses
and run logic at the edge. It cuts latency and origin load, and it absorbs
traffic spikes and some denial-of-service attacks. What does not belong there is
anything personalised, unless you have thought hard about the cache key.

## HLD — scale and reliability

### 67. Horizontal or vertical scaling?

Vertical is a bigger machine: no code changes, no distribution problems, and a
hard ceiling plus a single point of failure. Horizontal is more machines: no
ceiling in principle, and redundancy for free, at the price of load balancing,
state management, data partitioning and everything else on this page. The usual
answer is to scale vertically until it is uncomfortable and horizontally after
that — and to note that horizontal scaling only works if the service is
stateless.

### 68. Explain load balancing and the algorithms worth knowing.

![A balancer spreading traffic and removing a failed instance](diagrams/design-interview-interview-load-balancing.jpg)

Distributing requests across equivalent servers, so no one of them is
overwhelmed and any of them may fail. **Round robin** is the simple default;
**least connections** suits requests of varying length; **weighted** variants
handle machines of different sizes; **hashing** on a client attribute gives
stickiness. Layer 4 balancing routes by address and port and is fast; layer 7
reads the request and can route by path or header. The balancer itself must be
redundant, or you have moved the single point of failure rather than removed it.

### 69. What is a sticky session, and what is the cost?

The balancer sends a given client to the same server every time, usually by
cookie or by hashing the address, so in-memory session state stays valid. It is
the quick fix for a stateful service, and it costs you even load — one server can
end up with all the busy users — and correctness on failure, because when that
instance dies its sessions die with it. Externalising session state is the
answer that removes the need.

### 70. How does a load balancer know a server has failed?

Health checks. A **passive** check watches real traffic for errors and timeouts;
an **active** check polls an endpoint on an interval. A good health endpoint
reports whether this instance can actually serve — including its critical
dependencies — but not so deeply that one slow database marks the entire fleet
unhealthy at once. Failing instances are removed from rotation and re-added after
consecutive successes, and the interval and threshold decide how long users see
errors.

### 71. How do you achieve high availability?

Remove single points of failure and make failure survivable: multiple instances
behind a balancer, replicas of every data store, spread across availability zones
and, if the requirement justifies it, regions. Then add health checks and
automatic failover, so recovery does not wait for a human. Say the number: three
nines is about nine hours of downtime a year, four nines is under an hour, and
each nine costs considerably more than the last — so ask what the business
actually needs.

### 72. What is fault tolerance, and how is it different from high availability?

![The tools that stop one failure spreading](diagrams/design-interview-interview-failure-kit.jpg)


High availability is about staying reachable, usually through redundancy. Fault
tolerance is about continuing to work correctly while something is broken —
degrading rather than collapsing. In practice it is timeouts on every remote
call, retries with exponential backoff and jitter, circuit breakers, bulkheads so
one pool of threads cannot exhaust the process, graceful degradation such as
serving a cached or reduced response, and idempotent operations so a retry is
safe.

### 73. How do you design for disaster recovery?

Start from two numbers the business must give you: **RPO**, how much data you can
afford to lose, and **RTO**, how long you can afford to be down. Those decide
everything else — backups with tested restores at one end, a warm standby in
another region in the middle, active-active at the expensive end. The two things
interviewers listen for are that backups are worthless until a restore has been
rehearsed, and that failover must be practised, because an untested failover is a
plan rather than a capability.

### 74. What is rate limiting, and how would you implement it?

Capping how many requests a client may make, to protect the service from abuse
and from accidental overload, and to enforce quotas. **Token bucket** allows
bursts up to a bucket size and refills at a steady rate — the usual choice.
**Leaky bucket** smooths output to a fixed rate. **Fixed window** is simplest and
allows a double burst at the boundary, which **sliding window** fixes. In a
distributed setting the counter has to be shared — Redis, typically — and the
response should be \`429\` with a \`Retry-After\`.

### 75. What is leader election, and what happens when the leader fails?

![A leader with two followers, and the election after it fails](diagrams/design-interview-interview-leader.jpg)

Several nodes agree on one to coordinate work — accepting writes, assigning
partitions, running a scheduled job once rather than n times. It is implemented
with a consensus algorithm such as Raft or Paxos, or delegated to a coordination
service such as ZooKeeper or etcd. When the leader fails the others detect the
missed heartbeats and elect a new one; writes pause during that window, and the
danger to name is **split brain** — two nodes both believing they lead — which is
why elections require a quorum.

### 76. How does auto-scaling work, and what are its limits?

A controller watches a metric — CPU, queue depth, requests per instance — and
adds or removes instances against a target, within a minimum and maximum. It
needs stateless instances, a warm-up period so new instances are not sent traffic
before they are ready, and a cool-down so it does not oscillate. Its limits are
worth saying: it cannot help if the bottleneck is a shared database, and it is
too slow for a sudden spike, which is what pre-warming and queues are for.

### 77. Explain latency, throughput and availability, and how they interact.

Latency is how long one operation takes; throughput is how many complete per
second; availability is the fraction of time the system answers at all. They are
not independent: pushing throughput towards capacity makes queues form and
latency rise sharply, which is why utilisation above about 70% feels unstable.
Measure latency at percentiles — p50 hides everything, p99 is what your worst
users experience — and remember that averages of latency are close to
meaningless.

### 78. How do you find and fix a bottleneck?

Measure before guessing: metrics for the system, tracing to see where the time
goes across services, profiling within one. The bottleneck is nearly always one
of four things — the database, a chatty network path, a lock, or a full queue.
Then fix in the cheap order: cache what is read repeatedly, index what is
filtered, batch what is chatty, and only then add machines. Adding machines in
front of a saturated database makes the problem arrive faster.

## HLD — the network

### 79. What is the OSI model, and how does TCP/IP map onto it?

OSI is a seven-layer teaching model — physical, data link, network, transport,
session, presentation, application. TCP/IP is the four-layer model the internet
actually runs on — link, internet, transport, application — and it collapses
OSI's top three into one. The value in an interview is shared vocabulary: "layer
4 load balancing" means routing by address and port, and "layer 7" means reading
the HTTP request itself.

### 80. TCP or UDP?

TCP is connection-oriented and reliable: it establishes a connection, orders
bytes, retransmits what is lost and controls congestion, at the cost of a
handshake and head-of-line blocking. UDP sends packets with no guarantees and
almost no overhead, which suits real-time audio and video, gaming and DNS, where
a late packet is worth less than a lost one. Note that HTTP/3 runs on UDP and
rebuilds reliability per stream on top — see
[HTTP](#/advanced/deep-http/advanced).

### 81. What is DNS, and why does it matter to a design?

The system that turns a name into an address, resolved by a chain of servers with
answers cached at every level for the length of their TTL. It matters
architecturally because it is the first request of any interaction, because TTL
is what decides how quickly a failover can take effect, and because DNS itself
can do simple load distribution and geographic routing. The failure mode worth
naming: a long TTL that outlives an instance sends users to a machine that is no
longer there.

### 82. What is the difference between HTTP and HTTPS?

HTTPS is the same protocol carried inside TLS, which adds three things: nobody on
the path can read the traffic, nobody can change it undetected, and the server
proves it controls the domain. Certificates and a chain of trust provide the
third, which is the one that makes the other two worth having. It is not
optional now — browsers mark plain HTTP insecure and HTTP/2 and /3 effectively
require TLS. There is a whole page on it: [HTTPS](#/advanced/deep-https).

### 83. What are the principles of RESTful API design?

Resources named by nouns in the URL, with HTTP methods as the verbs and status
codes as the outcome. It should be stateless — each request carries what it
needs — cacheable where possible, and layered so a proxy can sit in between.
Practically: plural nouns, \`GET\` never changing state, \`PUT\` idempotent, \`POST\`
for creation answering \`201\` with a \`Location\`, filtering and pagination in the
query string, and versioning at the boundary. The
[HTTP notes](#/advanced/deep-http) cover the semantics each of those relies on.

### 84. What are the steps in designing an API?

Start from the consumer's use cases, not from your tables. Name the resources
and the operations on them; fix the request and response shapes with explicit
types; decide the error model — one shape, machine-readable codes, no leaking of
internals; decide authentication, rate limits and pagination up front, because
each is painful to retrofit. Then document it and version it before the first
external consumer arrives, and treat every published field as a promise.

### 85. How would you approach security in a high-level design?

In layers, and in the assumption that any one of them will fail. TLS everywhere,
including between internal services; authentication at the edge with
authorization at the resource; least privilege for every credential; secrets in a
manager rather than in configuration; input validated at the boundary and output
encoded on the way out; rate limiting and audit logging. Then the data itself:
encrypted at rest, personal data minimised and retained on a schedule, and
backups protected exactly as carefully as the live store.

### 86. How do you design for data privacy and protection?

Collect the minimum that the product actually needs and keep it for a stated
period rather than forever. Classify what you hold, encrypt it in transit and at
rest, and restrict access by role with audit records of who read what. Support
the operations regulation requires — export and deletion — which is a design
constraint, because a user's data scattered across seven services and three
backups is very hard to delete on request. Pseudonymise where analytics does not
need identity.

### 87. What is the difference between HLD and LLD, in one answer?

HLD is the system: services, data stores, communication, deployment topology,
and the non-functional properties — scale, availability, latency. LLD is one
component: classes, interfaces, algorithms, data structures and error handling.
HLD is what you agree with other teams; LLD is what you agree within one. Both
are design; they differ in the size of the thing being decided and in who has to
live with the decision.

### 88. You are asked to design a URL shortener. What does a good answer look like?

![A complete URL shortener architecture](diagrams/design-interview-interview-shortener.jpg)

Requirements first: shorten, redirect, optional analytics; a hundred million
new URLs a month, reads a thousand times writes, redirect in under 50ms.
Estimate: about forty writes a second, forty thousand reads, a few hundred
gigabytes a year. Interface: two operations. The key decision is id generation —
a counter encoded in base 62, or a hash with collision handling — and then the
read path is a key-value store with a cache in front and a CDN doing the
redirect. Finish with what breaks: hot keys, the counter as a single point,
custom aliases, expiry, and the fact that a 301 cannot be un-cached.

## What to revise next

- **[SOLID in practice](#/design/solid-in-practice)** — the low-level half,
  worked through on one service.
- **[HTTP](#/advanced/deep-http)** and **[HTTPS](#/advanced/deep-https)** — the
  protocol questions above, several levels deeper.
`,Yi=`# The words first

Every scenario in this section is a story about one part of a running system
breaking. None of them are hard to follow, but all of them assume you already
know what a replica is, what a pool is, what p99 means. That assumption is where
most people fall off — not at the idea, at the vocabulary the idea is written in.

So this page is the vocabulary, once. It is written as prose rather than as a
table on purpose: **every word with a dotted underline can be tapped, and it will
tell you what it means without taking you anywhere.** Read it, then never read it
again — the scenarios each add the four or five words they need on top and
otherwise assume these.

## The shape everything else sits inside

Almost every system in this section is the same picture with a different part of
it on fire. It is worth having that picture in your head before the first one,
because every scenario is a sentence about one arrow in it.

![Clients through a load balancer to three app servers, a shared cache and a connection pool, then the primary database and its two read replicas](diagrams/hld-vocabulary-notes-stack.jpg)

Four scenarios, four arrows:

- **Connection pool exhaustion** is the box marked *connection pool* running out.
- **Schema migration failure** is the three app servers disagreeing about what
  shape the database has.
- **Cache inconsistency** is the cache and the primary holding different answers.
- **Leader election failure** is the primary dying and the copies failing to
  agree on which of them is next.

## The basic setup

At the left of that picture is a client — a browser, a phone app, another
program — and it does exactly one thing: it sends a request and waits for a
response. On the right is a server, which is not a special kind of machine, only
one that stays running and answers whoever asks.

Between them is an API: the list of questions the server has agreed to answer,
the way a menu is the list of dishes a kitchen has agreed to cook. One line on
that menu is an endpoint. And the two halves of the thing you are building are
the frontend, which is what the user sees and taps, and the backend, which is
the code on the server deciding what the answer actually is.

## The database

The database is the program that keeps your data on disk safely, so that it
survives a restart, a crash, and somebody switching the machine off. You ask it
questions in SQL — a language that describes *what* you want rather than how to
go and fetch it.

Two words about it matter more than the rest in these scenarios. The **schema**
is the shape of the database — which tables exist, which columns they hold, what
type each one is. Not the contents; the structure around the contents. Changing
it is what the migration scenario is about, and the commands that change it are
called DDL.

The other is the transaction: a group of changes that must all happen or none of
them happen. Money leaving one account and arriving in another is one
transaction, never two. Making it real is a commit; abandoning it is a rollback.
A transaction held open too long is one of the recurring villains here, because
whatever it is holding, nobody else can have.

## The network

Two machines talk over TCP, which guarantees that nothing is lost and nothing
arrives out of order — and which costs a handshake before a single useful byte
moves. That cost is small, and it is charged every time a connection is opened,
which is the entire reason pools exist.

Then four numbers, and you will meet all of them again:

- **Latency** is how long one request takes. Lower is better.
- **Throughput** is how much work finishes per second. Higher is better. It is
  not the same thing as latency, and a system can be excellent at one and hopeless
  at the other.
- **RPS** is the rate requests arrive at, and it is what every capacity
  calculation starts from.
- **p99** is the figure 99 requests out of 100 came in under.

The last one deserves a second read, because it is the number these scenarios are
diagnosed with. An average of 40ms sounds healthy right up until you learn that
one request in a hundred is taking nine seconds — and it is that one in a hundred
that retries, holds a connection, and starts the cascade. A timeout is the other
half of the same conversation: a limit fixed in advance, past which you give up
rather than wait forever.

## Inside the server

Inside one server, work is done by threads. Two hundred threads means two hundred
requests can be in progress at once — that is concurrency. A thread that is
stuck waiting on something is blocking, and a blocking thread is a worker being
paid to do nothing. Requests that cannot be served yet stand in a queue.

A pool is a stock of something expensive kept ready for reuse — an office
keeping a hundred charged power banks in a drawer, rather than building a new one
each time somebody asks. Almost everything that goes wrong in the first scenario
is about a drawer that emptied.

And one property worth having a name for: an operation is idempotent if doing it
twice leaves things exactly as doing it once did. "Set status to paid" is
idempotent. "Add ₹100" is not. It is what decides whether a client can safely
retry.

## Many machines together

A distributed system is one product running across many machines that have to
cooperate over a network they cannot rely on. A load balancer sits in front and
decides which machine answers each request. Sharding splits the data across them
— these users here, those users there — so no single machine holds all of it.

Underneath, one machine is allowed to accept writes and the rest copy whatever it
did. Each copy is a replica, and how far behind it is running is the replication
lag: three seconds of lag means the copy is answering with three-second-old data.

And the thing that breaks all of it: a network partition, where the network
fails between two groups of machines. Both groups are alive and healthy. They
simply cannot reach each other, and neither can tell whether the other has died
or is merely unreachable. That single ambiguity is the whole of the fourth
scenario.

## Caching

A cache is a small fast copy of data kept close by, so the slow original does not
have to be asked again — today's file on your desk instead of a walk to the
cupboard. In practice it is Redis, holding keys and values in RAM, which is
roughly a thousand times faster than disk and emptied on every restart.

Asking it either produces a cache hit or a cache miss. Every entry carries a
TTL, an expiry timer, which is also the backstop that quietly heals every
invalidation you failed to send. When the value in the cache is no longer true it
is stale; when the cache is full and throws entries out to make room, that is
eviction.

## Shipping changes

Putting new code onto the machines serving real traffic is a deploy. The live
system is production; the practice copy is staging. Nobody deploys everything at
once, so:

- A **rolling deploy** updates a few servers at a time, so the site never goes
  fully down — which guarantees a window where old and new code are running
  together.
- A **canary** sends the new version to one server, or one percent of users,
  first, and watches.
- A **feature flag** is a switch in configuration rather than in code, so a
  feature can be turned on without deploying again.
- **Rollback** is going back because the new version broke something.

And downtime is time during which the system is not answering, which is the
number all of the above exists to keep small.

## Scaling and health

There are two ways to make a system carry more. Vertical scaling makes one
machine bigger; it is simple and it runs out. Horizontal scaling adds more
machines; it does not run out, and it turns every shared thing behind those
machines into a new problem — which is a sentence the first scenario spends a
page on.

Keeping it alive is monitoring: watching numbers out of the live system
continuously, rather than looking only after something breaks. One such number is
a metric, and an alert is a message sent automatically when a metric crosses a
line somebody drew in advance.

Three more names, and they are the ones these pages end on. A cascading failure
is one small thing breaking and everything depending on it breaking in turn, so
the system keeps getting worse on its own. Blast radius is how much of the
system a single failure is able to damage — design work is largely making that
number smaller. And graceful degradation is working partially instead of dying
completely: recommendations gone, but the films still play.

## Where to go from here

The four scenarios are independent — read them in any order. If you want the one
that teaches the most transferable lesson first, start with **connection pool
exhaustion**, because *bounded resource, held too long, queue grows, retries
amplify* is the same story the other three tell in different clothes.
`,Xi=`# Connection pool exhaustion

The database is idle. CPU at 8%, disk quiet, not one slow query in the log. And
the site is down.

This is the most common production failure that nothing in the database will
explain, because the thing that ran out is not in the database. It is a drawer
of open phone lines sitting inside your application, and it has exactly as many
lines in it as you told it to have.

## Why a pool exists at all

To talk to the database, your app needs an open **connection**. Opening one is
slow, because three things have to happen before a single byte of SQL moves:

1. **TCP handshake** — the two machines say hello and agree to talk
2. **Authentication** — the app proves its username and password
3. **Session setup** — the DB prepares a workspace for this connection

That is somewhere between 20 and 100 milliseconds. Doing it on *every* request
would be like ringing the bank and re-verifying your identity every time you
want to read one number off your statement.

So at startup the app opens, say, a hundred connections and keeps them in a
drawer. That drawer is the **pool**.

![A request borrowing a connection from the pool, running its query, and returning the connection](diagrams/connection-pool-exhaustion-notes-borrow.jpg)

Borrow, use, return. That is the whole idea, and **the trouble starts when the
return step is late — or never happens at all.**

## What "exhausted" means

Exhausted means empty. Every connection is out on loan and there are none left
in the drawer.

Say the pool holds 100. Normally a request borrows one for 10 milliseconds and
gives it back, so a hundred is plenty — that is ten thousand requests a second
of headroom. Now queries get slower, or traffic doubles. Connections stay out
longer. Slowly, all hundred are busy.

The 101st request asks for a connection and **gets nothing**. It has to stand
and wait. Then the 102nd. Then the 103rd. The line grows, everyone in it waits
longer, and requests start hitting their timeout and failing.

![Requests queueing because all hundred pool connections are in use, while the database itself sits idle at eight per cent CPU](diagrams/connection-pool-exhaustion-notes-exhausted.jpg)

**The database itself is perfectly fine**, and the diagram is drawn that way on
purpose. Think of a restaurant with a hundred waiters. Every waiter is stuck at
a table. New customers walk in, but nobody is free to take an order. The kitchen
is standing idle — and yet service has stopped. The kitchen is your database;
the waiters are your connections.

## Why it happens

| Reason | What is going on |
| --- | --- |
| **Slow query** | One query takes 5 seconds instead of 10ms, so it holds a connection 500 times longer |
| **Traffic spike** | A sale starts, ten times the users arrive, and you need far more connections than usual |
| **Connection leak** | The code borrowed a connection and never gave it back — that one is gone for good |
| **Long transaction** | Code opens a transaction, calls a slow external API in the middle, then commits. The connection is held for the whole of it |

**A leak is the worst of the four**, because nothing looks wrong. Traffic is
normal, queries are fast, dashboards are green. But every few hours one more
connection is lost from the drawer, until one day there are none left. Fine
today, slow tomorrow, down the day after.

There is a formula behind the first two rows, and it is worth knowing by name
because it turns "we need a bigger pool" into an actual number. **Little's Law**
says the number of things in progress equals the arrival rate multiplied by how
long each one takes:

\`\`\`expected
connections needed  =  RPS  ×  query time

  200 RPS × 500ms  =  100 connections
  200 RPS × 1.0s   =  200 connections   ← query got 2× slower,
                                          pool requirement doubled
\`\`\`

That second line is the entire scenario in one arithmetic step. Nothing about
your traffic changed. The query got slower, and the pool you sized for it is now
half the size it needs to be.

## Why one small thing takes down everything

Almost every part of your app needs the database. So when connections run out:

1. Requests cannot proceed → threads sit **blocked**, waiting, doing no useful work
2. Requests hit their timeout and fail
3. The client sees a failure and **retries** — which is *more* traffic, not less
4. More traffic → connections run out faster → more failures → more retries

![One slow query holding connections longer, exhausting the pool, timing out requests and provoking retries that feed straight back into more load](diagrams/connection-pool-exhaustion-notes-cascade.jpg)

The arrow that closes the loop is the whole problem. This is a **cascading
failure**: one small jam brings down everything, and it gets worse on its own,
without any further help from the outside.

Two things make the loop turn faster. **Head-of-line blocking** — one five
second query holds a connection while fifty queries that each need two
milliseconds queue behind it. And a **retry storm** — every failed request comes
back immediately, so the load peaks at exactly the moment the system is least
able to carry it.

## How to fix it

**Find the root cause first. Do not just raise the pool size.** Going from 100
to 500 does not create capacity out of thin air; it sends five hundred
connections' worth of load at the database, and now the *database* falls over
instead. You moved the problem, you did not fix it.

| Problem | Fix | Why it works |
| --- | --- | --- |
| Slow query | Optimise it; add an **index** | The connection is held for 10ms instead of 5s, so the same hundred connections serve far more traffic |
| High traffic | Add **caching** | Requests answered from cache never touch the DB, so they never take a connection |
| High read traffic | Use **read replicas** | Reads go to copies of the DB, freeing the primary |
| Leak | Release in \`finally\` / \`try-with-resources\` / \`using\` | Guarantees the connection returns even if the code threw |
| Bad tuning | Set an **acquire timeout** and a **query timeout** | A request waits two seconds for a connection and then fails fast, instead of blocking a thread forever |

The overall goal is one sentence: **reduce unnecessary pressure on the
database.** Do not let every request reach it, and whatever does reach it should
finish fast.

### The architecture that survives it

Put those fixes on the picture and the shape changes. Every box below exists to
stop traffic before it needs a connection, or to stop one part of the app
starving another.

![Clients passing through a cache, a circuit breaker and separate bulkhead pools to a shared pooler, and from there to the primary and read replicas](diagrams/connection-pool-exhaustion-notes-architecture.jpg)

Three of those boxes are worth naming, because they are what an interviewer is
listening for:

- **Bulkhead** — the ship's wall. Reports and analytics get their own small
  pool, so a heavy report cannot starve the checkout API. One flooded
  compartment does not sink the ship.
- **Circuit breaker** — when the failure rate is high, stop calling the database
  at all for a while. Fail instantly, and give it room to recover instead of
  piling on.
- **The shared pooler** is the one people miss, and it is the trap in horizontal
  scaling: twenty app servers each carrying a pool of a hundred is **two
  thousand connections** arriving at a database sized for four hundred. Adding
  servers made it worse. A single pooler in front — PgBouncer, ProxySQL, RDS
  Proxy — holds one real pool for all of them.

And whatever else you do, keep **backpressure**: when overloaded, push work away
rather than quietly accepting it. Rejecting a request in two seconds is kinder
than letting it wait sixty and holding a thread the whole time.

## What to watch

- **Active connections against the maximum** — how full the drawer is
- **Connection wait time** — how long a request waits *just to get* a
  connection. This is the best early warning there is; it starts climbing well
  before anything fails
- **Query execution time** — slow queries are the usual root cause
- **Timeout and error rate** — the damage that is already happening

> The red flag is active connections sitting near the maximum all the time.
> Nothing is broken *yet*. You are one traffic spike away from an outage, and
> the graph has been telling you so for a fortnight.

Connection wait time belongs to **saturation**, the fourth of the four golden
signals — latency, traffic, errors, saturation. Saturation is the one that
warns; the other three report.

## Takeaway

Database connections are a **limited and expensive resource**. Hold them too
long and the whole application slows down, whatever the database is doing.

**Fast query → release immediately → cache what you can → monitor saturation.**
`,Zi=`# Connection pool exhaustion — interview questions

Ten questions, easy to hard, and one table of the concepts an interviewer is
listening for by name. The last question is the whole scenario in one paragraph
— if you can only keep one thing from this page, keep that.

## Easy

### 1. Why does a connection pool exist?

Because opening a database connection is expensive: a TCP handshake, then
authentication, then session setup — 20 to 100 milliseconds before a single byte
of SQL can move. Doing that per request would dominate the response time. So the
application opens a fixed number of connections at startup and hands them out on
loan: borrow, run the query, return.

### 2. What does "the pool is exhausted" actually mean?

Every connection in it is currently on loan and none is free. A request that
asks for one gets nothing and joins a waiting queue. If it waits past its
acquire timeout, it fails.

### 3. The pool is exhausted and the database is at 8% CPU. Is the database the problem?

No, and this is the telling detail. The resource that ran out lives in the
application, not the database. It is a restaurant where every waiter is stuck at
a table — the kitchen is idle, and service has still stopped.

## Medium

### 4. Traffic has not changed, but the pool is now exhausting daily. What changed?

Query time. Little's Law says concurrency equals arrival rate multiplied by
service time — \`connections = RPS × query time\` — so 200 RPS at 500ms needs 100
connections, and the same 200 RPS at one second needs 200. A query that got
twice as slow doubled the pool requirement without a single extra user. Look for
a dropped index, a table that outgrew its plan, or a new join.

The other candidate with the same signature is a **leak**: a connection borrowed
and never returned. That one shows up as a slow drift over hours or days rather
than a step change, and it correlates with uptime rather than with traffic.

### 5. Why is raising the pool size from 100 to 500 usually the wrong first move?

Because the pool is not where the capacity is. Five hundred connections is five
hundred concurrent sessions arriving at a database that was sized for a hundred
— so the contention moves from your queue to its buffer pool, its locks and its
scheduler, and now the database falls over instead of the app. You have moved
the failure, not removed it, and you have moved it somewhere with a worse blast
radius.

Raise it deliberately, after you know the query time and have done the
arithmetic, and only up to what the database can actually serve.

### 6. Explain why this becomes a cascading failure rather than staying a slowdown.

Four steps, and the fourth closes a loop:

1. Connections stay out longer, so the pool empties.
2. Requests queue, threads block, and requests start hitting their timeout.
3. The client sees a failure and retries — immediately.
4. That retry is additional load, arriving exactly when the system has the least
   capacity to absorb it.

The system now degrades without any further input from outside. Two mechanisms
make it turn faster: **head-of-line blocking**, where one five-second query
holds a connection while fifty two-millisecond queries wait behind it, and a
**retry storm**, where every client retries in lockstep. Exponential backoff
plus jitter is the fix for the second.

### 7. What single metric would have warned you first, and why that one?

**Connection wait time** — how long a request waits purely to acquire a
connection. It starts rising as soon as utilisation approaches the limit, which
is well before any request fails, so it is a leading indicator rather than a
report of damage. Error rate and p99 latency both only move once users are
already affected.

In golden-signal terms it is **saturation**. Latency, traffic and errors tell you
what has happened; saturation tells you what is about to.

## Hard

### 8. You scale from 4 app servers to 20 to handle load, and the database immediately gets worse. Why?

Because each server carries its own pool. Twenty servers with a pool of a
hundred is two thousand connections presented to a database provisioned for a
few hundred, and every one of those is a real server-side session with its own
memory. Horizontal scaling multiplied the pressure on the shared bottleneck.

The fix is a **shared external pooler** — PgBouncer, ProxySQL, RDS Proxy —
sitting between the app tier and the database, holding one real pool that all
twenty servers multiplex through.

### 9. A nightly analytics report takes down checkout. What is the structural fix?

A **bulkhead**: give reports their own small, separate pool rather than letting
them draw from the same one checkout uses. The ship's-wall analogy is exact — one
compartment floods, the ship stays up. Ten connections for reporting means the
worst a runaway report can do is exhaust reporting.

Pair it with a **circuit breaker** on the database call, so that once the
failure rate crosses a threshold the app stops calling and fails instantly
instead of piling more waiting threads onto a struggling database. And pair both
with **graceful degradation**: if the pool is full, serve a slightly stale
cached value rather than an error, where the read path tolerates it.

### 10. Give the whole scenario as one interview answer.

> A connection pool is a **bounded resource**. Slow queries raise the
> concurrency you need — Little's Law, \`connections = RPS × query time\` — so the
> pool saturates, **head-of-line blocking** kicks in, and client retries amplify
> the load exactly when there is least capacity, which turns it into a
> **cascading failure**. Prevent it by cutting the work: optimise and index the
> slow query, cache reads so they never take a connection, and send reads to
> replicas. Bound it: acquire and query timeouts at every layer, bulkheads and a
> circuit breaker to shrink the blast radius, and a shared pooler so horizontal
> scaling does not multiply connections. And watch **pool saturation and
> connection wait time**, because those move before anything fails.

## The concepts, by name

An interviewer is scoring on whether you can name the mechanism, not on whether
you can describe the symptom. These are the names.

| Concept | In one line | How it applies here |
| --- | --- | --- |
| **Resource pooling** | Keep expensive things ready and reuse them | The pool itself — same idea as thread pools and HTTP connection pools |
| **Bounded resource** | Something with a hard maximum that can run out | The pool holds exactly N. Design assuming it *will* hit zero |
| **Little's Law** | \`in progress = arrival rate × time each takes\` | \`connections = RPS × query time\`. Why a slow query alone exhausts the pool |
| **Head-of-line blocking** | The one in front blocks everyone behind, however fast they are | One 5s query holds a connection while fifty 2ms queries queue |
| **Backpressure** | When overloaded, push work away instead of quietly accepting it | Rejecting in 2s beats waiting 60s and holding a thread |
| **Layered timeouts** | A time limit at every stage | Acquire timeout + query timeout + request timeout |
| **Retry storm** | Failures retried in lockstep, doubling load at the worst moment | Fix with exponential backoff plus jitter |
| **Cascading failure** | One failure spreads outward like dominoes | Pool empty → APIs fail → retries → more load → outage |
| **Bulkhead** | Sealed compartments, so one flood does not sink the ship | A separate small pool for reports, so they cannot starve checkout |
| **Circuit breaker** | If it keeps failing, stop calling it for a while | Fail instantly and let the database recover |
| **Caching** | Keep a fast copy of frequently-read data | The most effective fix — those requests never take a connection at all |
| **Read replicas** | Copies that serve reads only | Splits the load; writes to the primary, reads to the copies |
| **Horizontal scaling trap** | More servers can hurt when each brings its own pool | 20 × 100 = 2000 connections. Fix with a shared pooler |
| **Saturation** | How full a limited resource is — the fourth golden signal | Pool utilisation *is* saturation, and it is the metric that warns first |
| **Resource lifecycle** | Whatever you take, you must guarantee you give back | Connections, files, sockets, locks — one rule, four resources |
| **Graceful degradation** | Work partially rather than die fully | Pool full → serve slightly old cached data instead of an error |
`,Qi=`# Schema migration fails midway

A migration looks like a database task. It is not. It is **two changes that have
to line up** — the database changes shape, and the code that reads and writes
that shape changes with it — and between the two there is a window, minutes or
days long, where both the old code and the new code are running against the same
database at the same time.

That window is not a mistake. It is what a rolling deploy *is*. Every safe
migration technique in this page exists because of it.

## What "failed midway" means

The script started, did part of its work, then stopped — a timeout, an error, a
failed deploy, somebody pressing Ctrl+C. The system is now **half old and half
new**:

- Some servers run new code and expect the new shape
- Some servers still run old code and expect the old shape
- The database is partly changed — some of it done, some not

APIs fail, queries error, background workers crash, and data can end up
inconsistent. Here is the state, drawn:

![Two app servers on new code and two on old, reading different column names from a database whose rename is only half applied](diagrams/schema-migration-notes-mixed-version.jpg)

Both arrows into the database are wrong at once, and that is the whole scenario.

## The classic example: renaming a column

You want to rename \`phone_number\` to \`mobile_number\`. In production, this is
what happens:

1. The migration starts
2. Some servers pick up the new code — they now ask for \`mobile_number\`
3. The migration fails before every database and replica is updated

Now **new code** asks for \`mobile_number\`, which on some databases does not
exist yet — error. And **old code** asks for \`phone_number\`, which where the
rename did succeed is gone — error. Both sides are broken.

> A rename is the single most dangerous migration, because a rename is really a
> **delete plus a create in disguise**. There is no instant at which both names
> exist — unless you deliberately create that overlap yourself. Creating the
> overlap is the whole trick below.

## Why many servers make it hard

A deploy is never instant, and the pieces update at different times:

- App servers update in batches — that is what "rolling" means
- Another team's service releases on its own schedule, possibly next week
- Read replicas lag behind the primary by seconds
- Background workers and cron jobs restart only when they finish current work

So for some window — minutes, sometimes days — old code and new code run
simultaneously against the same database. Which gives the golden rule:

> **The migration must be safe for old code AND new code at the same time.**

## The safe way: expand → migrate → contract

Never change anything in place. Break one dangerous change into several boring
safe ones, each its own deploy, each separately reversible.

![The six steps of a safe rename: expand, dual write, backfill, switch reads, stop dual write, and contract weeks later](diagrams/schema-migration-notes-expand-contract.jpg)

| Step | What you do | Why it is safe |
| --- | --- | --- |
| **1. Expand** | Add a new column \`mobile_number\`. Do not touch the old one | Old code does not know it exists, so nothing breaks |
| **2. Dual write** | Deploy code that **writes to both** columns, but still reads the old one | Both old and new code work fine |
| **3. Backfill** | Copy old data into the new column, in small batches | Just filling data; nobody is reading it yet |
| **4. Switch reads** | Deploy code that reads \`mobile_number\` | Both columns still exist, so this is reversible |
| **5. Stop dual write** | Deploy code that only uses the new column | The old column is now unused, but still there |
| **6. Contract** | Drop \`phone_number\` — days or weeks later | Nothing depends on it any more |

Yes, it is slower. But if step 3 goes wrong you simply stop, and stopping is not
an outage. The one step drawn in a different colour is the only irreversible
one, and it is deliberately last and deliberately weeks later.

## Can we just roll back?

Usually not, and this surprises people:

- New data has already been written in the new format, so undoing loses it
- The change is irreversible — a dropped column, a narrowed type, a deleted table
- Other services already depend on the new structure
- The migration was only partly applied, so there is no clean point to return to

That is why experienced teams **roll forward** instead: write a *new* migration
that repairs the broken state, rather than trying to reverse the old one. The
real safety comes from planning before the deploy, not from hoping rollback
works.

## Migrations also hurt performance

Changing a big table on a live database can lock it, so every other query queues
behind the \`ALTER\` — and that pile-up *is* the outage. It can also rewrite the
whole table on disk, blow up replication lag so copies serve very old data, and
hold one giant transaction open for an hour.

**Fix one — online schema change tools.** gh-ost and pt-online-schema-change
build a shadow copy of the table in the new shape, keep it in sync in the
background, and swap it in instantly at the end. Users barely notice.

![A live table copied in chunks into a shadow table in the new shape, kept in step from the binlog, then swapped in by an atomic rename](diagrams/schema-migration-notes-shadow-table.jpg)

**Fix two — batch your backfills.** Never \`UPDATE\` four hundred million rows in
one statement. Do a thousand rows, commit, pause, repeat. Short locks, low
replication lag. It is the same lesson as connection pool exhaustion: **long
transactions are poison.**

## How teams reduce the risk

- **Test on a production-sized copy** — a ten-row table on your laptop tells you
  nothing about a four-hundred-million-row table
- **Take a backup first**, and know how long a restore actually takes
- **Canary, then staged rollout** — one server, then 10%, then everything
- **Feature flags** — ship the code switched off, turn it on separately
- **Monitor while it runs** — error rate, latency, replication lag, lock waits
- **Make it idempotent and resumable** — \`ADD COLUMN IF NOT EXISTS\`, and a saved
  checkpoint of the last processed row, so a death at 60% does not mean starting
  over
- **Keep a kill switch** — be able to stop the rollout before damage spreads

One database detail changes the whole strategy and is worth knowing: **DDL
atomicity**. PostgreSQL can run DDL inside a transaction and roll it back
cleanly. MySQL **auto-commits** it — a failure halfway leaves it half applied,
with nothing to undo. Which engine you are on decides how much you can rely on
the migration tool and how much you must build the overlap yourself.

## Takeaway

A schema migration is **not a database change — it is a distributed system
change.** The database, every service, every replica and every background worker
has to survive the in-between state, because that in-between state is
guaranteed to happen.

**Backward compatible → expand before contract → gradual rollout → monitor →
recovery plan.**
`,$i=`# Schema migration fails midway — interview questions

Ten questions, easy to hard, then the concepts by name. This is the scenario
where a candidate who has only ever run migrations locally is easiest to spot —
the tell is treating it as a database question.

## Easy

### 1. What is a schema migration?

A script that changes the *shape* of the database rather than its contents — add
a column, rename one, create or drop a table, change a type. Tools like Flyway,
Liquibase and Alembic keep a ledger table recording which migrations a given
database has already had applied, so "what state is this database actually in"
has an answer.

### 2. Why is a migration not just a database change?

Because the code that reads and writes that shape has to change with it, and the
two changes cannot happen at the same instant across a fleet of servers. The
migration is one half of a distributed change; the deploy is the other.

### 3. What state does a half-finished migration leave you in?

Mixed. Some app servers on new code, some on old, the database partly altered,
and replicas still lagging on the previous shape. Queries fail from both
directions at once.

## Medium

### 4. Why is renaming a column the most dangerous single migration?

Because a rename is a delete and a create wearing one name. There is no moment
at which both \`phone_number\` and \`mobile_number\` exist, so during any rolling
deploy one half of your fleet is guaranteed to be asking for a column that is
not there. New code fails where the rename has not landed; old code fails where
it has.

The safe version manufactures the overlap deliberately — add the new column
first, run both together for a while, and remove the old one much later.

### 5. Walk through expand–migrate–contract for that rename.

Six separate deploys, each individually reversible:

1. **Expand** — \`ADD COLUMN mobile_number\`. Old code does not know it exists.
2. **Dual write** — write to both columns, still read the old one. Both versions
   of the code work.
3. **Backfill** — copy the old values into the new column in batches. Nobody
   reads it yet, so a failure here costs nothing.
4. **Switch reads** — read from \`mobile_number\`. Both columns still exist, so
   this step can be undone by redeploying.
5. **Stop dual write** — new column only. The old one is now dead weight.
6. **Contract** — \`DROP phone_number\`, days or weeks later, once you are sure.

At no point is anything half-broken, and only the last step is irreversible.

### 6. Why do experienced teams plan to roll forward rather than roll back?

Because rollback is frequently not available. New rows have already been written
in the new format, so reversing loses them. Some changes are irreversible by
construction — a dropped column, a narrowed type. Other services may already
depend on the new structure. And a migration that failed midway has no clean
point to return to in the first place.

So the plan is a *new* migration that repairs the state, and the real safety
comes from the design of the rollout rather than from an undo button.

### 7. How does a migration on a large table turn into an outage even when it succeeds?

Through locking. A long \`ALTER TABLE\` holds the table, every other query queues
behind it, and the queue is the outage — the migration itself may finish
perfectly. It can also rewrite the whole table on disk, push replication lag to
minutes so replicas serve very stale data, and hold a single transaction open
for an hour.

Two fixes. **Online schema change tools** (gh-ost, pt-online-schema-change)
build a shadow copy in the new shape, keep it synced from the binlog, and do an
atomic rename at the end. And **batched backfills** — a thousand rows, commit,
pause, repeat — which keep locks short and lag low.

## Hard

### 8. Does blue-green deployment protect you here?

No, and it is a good trap question. Blue-green gives you two complete
application environments and an instant traffic switch, but **both share one
database**. The schema is the thing you are changing, and it is the thing the
two environments have in common. What blue-green actually adds is a fast way to
move all traffic back to old code — which does not help when the old code is the
half that the applied migration broke.

Canary plus feature flags is the useful pairing here, because it limits the
blast radius of the *code* change while expand–contract limits the blast radius
of the *schema* change.

### 9. How would you make a migration safe to re-run after it died at 60%?

Two properties, and they are separate.

**Idempotent** — running it twice has the same effect as running it once.
\`ADD COLUMN IF NOT EXISTS\`, \`CREATE INDEX IF NOT EXISTS\`, and backfills written
as "set X where X is null" rather than "set X for everything".

**Resumable** — it can continue from where it stopped. That means checkpointing:
record the last processed primary key after each batch, and start from that
checkpoint on the next run. Without it, a four-hour backfill that dies at 60%
costs you four hours, not one and a half.

One engine detail decides how much of this you have to build yourself.
PostgreSQL can run DDL inside a transaction and roll it back cleanly; MySQL
auto-commits DDL, so a mid-way failure leaves it half applied with nothing to
undo. On MySQL, the overlap and the checkpoints are entirely your responsibility.

### 10. Give the whole scenario as one interview answer.

> A schema migration is a distributed change, not a database change — during a
> rolling deploy old and new code run together, so a failed migration leaves a
> **mixed-version** fleet on a partly-migrated schema. So never change in place:
> use **expand–migrate–contract** so every step is backward compatible,
> dual-write and backfill in **batches**, and use **online schema change** tools
> to avoid long locks and replication lag. Keep migrations **idempotent and
> resumable**, gate the read switch behind a **feature flag**, roll out
> gradually with monitoring on errors, p99, lock waits and lag — and plan to
> **roll forward** rather than depend on rollback, with a tested backup and PITR
> as the last resort.

## The concepts, by name

| Concept | In one line | How it applies here |
| --- | --- | --- |
| **Backward compatibility** | New stuff still works with old stuff | The new schema must not break servers still on old code |
| **Forward compatibility** | Old stuff tolerates new stuff it does not understand | Old code should ignore an unknown column, not crash on it |
| **Expand–migrate–contract** | Add the new, run both, remove the old — as separate deploys | The six-step table. The core technique of the whole topic |
| **Mixed-version operation** | Two versions of your app live at the same moment | Guaranteed during any rolling deploy. Design for the *overlap* |
| **Rolling / canary / blue-green** | Ways of releasing gradually | Blue-green does **not** protect you — both environments share one database |
| **Deploy ≠ release** | Shipping the code and turning it on are separate events | Feature flags make "turn it off" a config change, not a redeploy |
| **Idempotency** | Twice has the same effect as once | \`ADD COLUMN IF NOT EXISTS\`. Lets you safely rerun a half-failed migration |
| **Resumability** | Able to continue from where it stopped | Checkpoint the last processed row id |
| **DDL atomicity** | Whether a structure change can be cleanly undone | Postgres: DDL in a transaction. MySQL: auto-commits. Changes your whole strategy |
| **Dual write / shadow read** | Write both during the switch; optionally read both and compare | Lets you verify the new column before trusting it |
| **Batched backfill** | Small chunks with pauses | Short locks, low lag. Long transactions are poison |
| **Locking and blocking** | One operation holds a table, others queue behind it | The pile-up behind a long \`ALTER\` is the actual outage |
| **Online schema change** | Build a synced shadow copy, swap instantly | gh-ost, pt-online-schema-change |
| **Replication lag** | How far behind the copies are | A heavy migration pushes replicas minutes behind |
| **Roll forward vs rollback** | Fix by moving ahead rather than reversing | Reversal is often impossible once new data exists |
| **Blast radius control** | Limit how much one mistake can damage | Canary + feature flag + kill switch |
| **Backup and PITR** | Restore to an exact moment in the past | The last line of defence. An untested restore is not a backup |
| **Schema versioning ledger** | A table recording which migrations are applied | Flyway / Liquibase / Alembic — the source of truth for DB state |
| **Observability during change** | Watch the metrics *while* it rolls out | Errors, p99, lock waits, replication lag |
`,ea=`# Cache drifts from the database

The price changed from ₹1000 to ₹800. The database says ₹800. The cache says
₹1000. Some users see one number, some see the other, and a few of them buy at
the wrong one.

Nothing failed. No exception was thrown, no alert fired, every dashboard is
green. This is the failure mode of caching, and it is not an accident you can
engineer away — it is the price of the speed, and the job is to bound it rather
than to eliminate it.

## Why a cache exists

Reading from a database means going to disk, and disk is slow. If ten thousand
people load the same product page, asking the database ten thousand times for
the same answer is waste. So you keep the answer in RAM, right next to the app.
Ask once, serve ten thousand times.

![A read checking the cache, returning instantly on a hit, and falling through to the database and back into the cache on a miss](diagrams/cache-inconsistency-notes-read-path.jpg)

That is **cache-aside**, also called lazy loading, and it is what almost
everybody runs. It is also the pattern with the race condition further down this
page.

## Why it goes wrong

The cache and the database are **two entirely separate systems**. A write has to
land in both, and there is no way to make those two changes one indivisible
step. There is no transaction spanning them.

![A write updating the database while the cache invalidation fails, leaving users reading the old price](diagrams/cache-inconsistency-notes-write-path.jpg)

| Reason | What is going on |
| --- | --- |
| **Invalidation failed** | The "delete this key" command errored, or the cache server was momentarily down |
| **Delayed refresh** | The update was sent as a background message that has not been processed yet |
| **Network issue** | The DB write succeeded, but the call to the cache timed out |
| **Concurrent writes** | Two services update the same key; they arrive out of order and the older value wins |
| **Long TTL** | Nothing failed at all — the old value is simply still inside its expiry window |

That last row is worth sitting with. Most staleness is not a bug. It is the TTL
doing exactly what you configured it to do.

## The race that everybody forgets

Even when your code is entirely correct, this happens:

![A reader writing the old value into the cache after the writer has already updated the database and deleted the key](diagrams/cache-inconsistency-notes-race.jpg)

The reader looked, missed, and read the old value. The writer then updated the
database and cleared the key. And the reader — still holding ₹1000 in its hand
from a moment ago — writes it into the cache *after* the delete.

The cache now holds the wrong value **permanently**, and nothing failed. Only
the TTL will eventually save you.

> Mitigation, in order of how much they buy you: always set a **TTL** as a
> safety net; **delete** the key rather than writing a value into it; and for
> very hot keys either delete a second time a moment later, or attach a version
> number to the value and reject writes carrying an older one.

## Why stale data is dangerous

The application does not merely *display* cached data. It makes **decisions**
with it:

- A user sees an outdated account balance
- Inventory shows five in stock when there are zero → you oversell
- Old prices appear at checkout
- An "already processed?" check reads stale data → the same payment goes through
  twice

For a product listing, sixty seconds of staleness is harmless. For a payment
balance it is a correctness bug, not a UX bug. That difference — **per-path
consistency** — is the judgement the whole topic comes down to.

## How to handle it

**1. Invalidate, do not update.** When the DB changes, *delete* the cache key
rather than writing the new value into it. A delete is **idempotent** — deleting
twice is the same as deleting once — and it is safe whatever order things arrive
in. A blind write is not: a slow, older write can land last and win.

**2. Order matters — DB first, then cache.** Update the database, *then* delete
the key. Never the reverse; if you delete first, another request can read the
old DB value and put it straight back before your write commits.

**3. TTL as a backstop, with jitter.** Give every key an expiry, so any missed
invalidation heals itself. Short TTL means fresher data and more DB load; long
TTL means less load and more staleness. Add **jitter** — a random ±10% — so a
million keys do not all expire in the same second and stampede the database
together.

**4. Do not trust the cache for critical reads.** Payments, balances, the final
checkout price, decrementing stock: read those from the database directly and
enforce the rule with a constraint or a transaction. Here correctness beats
speed, and it is not close.

**5. Event-driven invalidation at scale.** Rather than hoping every service
remembers to invalidate, have the database broadcast its own changes — via
**CDC**, an outbox table, or pub-sub. One listener then clears the relevant keys
everywhere, and there is a single source of truth for "this thing changed".

![Database changes flowing through a change-data-capture listener and a pub-sub topic to the shared cache and to every app server’s local cache](diagrams/cache-inconsistency-notes-invalidation.jpg)

## Why many servers make it worse

More moving parts means more windows in which copies disagree: several cache
servers, several DB replicas, asynchronous replication, concurrent writers.

The hardest case is a **local in-process cache** — data held inside each app
server's own memory. There is no single shared key to delete, so a plain "delete
key" does not reach twenty servers. That is exactly what the pub-sub broadcast
above is for, and even then you want a short TTL underneath it.

Two user-visible guarantees are worth naming while you are here.
**Read-your-own-writes**: after somebody edits their own profile they must see
the change immediately, so bypass the cache for that user or update it inline.
And **monotonic reads**: never show newer data and then older data again, which
is what happens when two servers hold different cache states and the load
balancer moves you between them. It feels like a bug because it is one.

## What to watch

Hit rate · miss rate · eviction rate · **invalidation failures** · staleness age,
meaning how old the oldest value you are still serving is · and DB load spikes
immediately after a mass expiry, which is a stampede you can see.

## Takeaway

Caching buys speed by accepting **eventual consistency**. Assume stale data
*will* happen — then decide, per read path, how much staleness is acceptable,
and send the paths that can tolerate none straight to the database.
`,ta=`# Cache drifts from the database — interview questions

Ten questions, easy to hard, then the concepts by name. The dividing line
between a junior and a senior answer here is whether you treat staleness as a
bug to fix or a budget to spend.

## Easy

### 1. Why is there a cache at all?

Because a database read goes to disk and RAM is roughly a thousand times faster.
If ten thousand people load the same product page, asking the database ten
thousand times for the same answer is pure waste. Ask once, serve ten thousand
times.

### 2. Describe cache-aside.

The application checks the cache. On a **hit** it returns immediately. On a
**miss** it reads the database, writes the answer into the cache, and returns
it. The application owns the logic — which is why it is also the pattern that
carries the race condition.

Read-through is the same shape with the logic moved into the cache library
instead of your code.

### 3. Why can the cache and the database disagree at all?

Because they are two separate systems and there is no transaction that spans
both. A write has to land in two places, and there is no way to make those two
landings one indivisible step. Whatever you do, there is a window.

## Medium

### 4. Should you update the cache with the new value, or delete the key?

**Delete.** A delete is idempotent — doing it twice is the same as doing it once
— and it is safe regardless of what order concurrent operations arrive in. A
blind write is not: two writers can have their updates arrive out of order, and
the older value lands last and wins, and it stays wrong until the TTL expires.

Deleting turns "get the ordering right" into "the next reader will fetch the
truth", which is a much weaker thing to have to guarantee.

### 5. Update the DB then delete the key, or delete then update? Why?

Database first, then delete. If you delete first, there is a window between the
delete and the commit in which another request reads the *old* value from the
database — because your write has not committed yet — and puts it straight back
into the cache. You have invalidated nothing.

### 6. Your code is correct and the cache still ends up permanently wrong. How?

The classic cache-aside race:

1. A reader misses and reads the old value, 1000, from the database
2. A writer updates the database to 800 and deletes the key
3. The reader — still holding 1000 from step 1 — writes it into the cache

The cache now holds 1000 with no expiry pressure and nothing having failed. This
is why a TTL is not optional even when your invalidation is perfect: it is the
only thing that heals this. For hot keys you can also do a delayed second delete
a few hundred milliseconds later, or attach a version to the value so a write
carrying an older version is rejected.

### 7. What does a TTL cost, and why add jitter to it?

A TTL trades freshness against database load: short means fresher data and more
misses, long means fewer misses and more staleness. Jitter — a random ±10% on
each key's expiry — exists because keys created together expire together. A
million keys written during a deploy will all expire in the same second, miss in
the same second, and hit the database in the same second. That is a stampede you
scheduled yourself.

## Hard

### 8. Twenty app servers each keep a local in-process cache. How do you invalidate?

You cannot, with a plain delete — there is no shared key to delete, only twenty
private copies in twenty heaps. You need a **broadcast**: publish "product:42
changed" to a pub-sub topic that every app server subscribes to, and have each
one evict its own copy on receipt.

At scale the publisher should not be your application code. Drive it from the
database's own change log via **CDC**, or from an **outbox** table written in
the same transaction as the change. That way a service that forgets to invalidate
cannot exist, because no service is doing the invalidating.

And keep a short TTL underneath it anyway, because a broadcast is a best-effort
delivery and a missed message otherwise never heals.

### 9. Which reads should never be served from cache, and how do you decide?

Decide per read path, by asking what a wrong answer costs — not by asking how
hot the path is. The application does not merely display cached data, it makes
decisions with it.

- Product listing, sixty seconds stale: harmless.
- Search results, seconds stale: fine.
- Account balance, stock decrement, final checkout price, an "already
  processed?" idempotency check: **zero staleness**. Read from the database and
  enforce the invariant with a constraint or a transaction, because stale stock
  oversells and a stale idempotency check charges twice.

Two guarantees are worth offering unprompted here. **Read-your-own-writes** —
after a user edits their profile they must see it immediately, so bypass the
cache for that user. And **monotonic reads** — never show newer data and then
older data again, which is what a load balancer moving a user between two
servers with different cache states will do.

### 10. Give the whole scenario as one interview answer.

> Cache and DB are separate systems with no shared transaction, so caching is an
> explicit trade of **consistency for latency** — staleness is guaranteed, and
> the job is bounding it. Update the **DB first, then invalidate** — delete,
> don't write, because delete is idempotent and order-safe — and always keep a
> **TTL with jitter** as a self-healing backstop. Drive invalidation from
> **CDC or pub-sub** so every node hears about the change, especially local
> in-process caches that have no shared key to delete. Protect against
> **stampedes** with single-flight and stale-while-revalidate. And for
> correctness-critical reads like balances or stock, bypass the cache entirely
> and enforce it in the database, because being right matters more than being
> fast.

## The concepts, by name

| Concept | In one line | How it applies here |
| --- | --- | --- |
| **Cache-aside (lazy loading)** | App checks cache → miss → reads DB → fills cache itself | The most common pattern, and the one with the race |
| **Read-through** | The cache library fetches from the DB on a miss | Same idea, logic in the cache layer instead of your code |
| **Write-through** | Every write goes to cache and DB together | Keeps them in sync, makes every write slower |
| **Write-behind** | Write to cache now, flush to the DB later | Fastest writes; if the cache dies before flushing, that data is gone |
| **Invalidation vs update** | Delete the key vs overwrite it | Delete is idempotent and order-safe; overwriting loses to a slower older write |
| **TTL and jitter** | Expiry timer plus a small random offset | Auto-heals missed invalidations; jitter prevents synchronised mass expiry |
| **Eventual consistency** | Copies converge, but not instantly | That gap *is* the scenario |
| **CAP / PACELC** | CAP: during a partition pick C or A. PACELC adds: *else*, pick latency or consistency | Caching is that second trade — speed bought with staleness |
| **Read-your-own-writes** | A user must at least see their own update immediately | Bypass or inline-update the cache for the writer |
| **Monotonic reads** | Never show newer data and then older again | Two servers with different cache states; feels like a bug to users |
| **Cache stampede** | A hot key expires, thousands miss at once | **Single-flight** and **stale-while-revalidate** |
| **Hot key** | One key takes a huge share of traffic | Overloads whichever single cache node holds it |
| **Cache penetration** | Repeated lookups for something that does not exist | **Negative caching**, or a bloom filter |
| **CDC / outbox / pub-sub** | Broadcast "this row changed" to everyone | Essential when caches are spread across many servers |
| **Versioned keys** | Put a version in the key: \`user:42:v7\` | Bump the version to invalidate a whole family at once |
| **Source of truth** | The one authoritative place | The DB. The cache is a disposable copy — never let data live only there |
| **Per-path consistency** | Different endpoints deserve different guarantees | Listing: 60s fine. Balance: 0s |
| **Graceful degradation** | Keep working partially when something breaks | Cache down → serve from the DB, rate-limited, rather than failing |
`,na=`# Leader election fails

Some jobs must be done by **exactly one** machine or they are wrong: accepting
writes, so there is one clear order of changes; running the nightly invoice job,
so it is emailed once rather than twenty times; deciding which node owns which
shard.

So the cluster picks a **leader**, and everyone else follows it. The leader is a
deliberate single point of coordination, because coordinating is precisely the
job. This page is about what happens when the leader dies and the cluster cannot
cleanly agree on the next one.

## The healthy picture

![One leader accepting all writes, sending heartbeats to four followers, and appending to the replicated log](diagrams/leader-election-notes-healthy.jpg)

One leader, four followers, a heartbeat every few hundred milliseconds. A
follower that stops hearing the heartbeat for long enough assumes the leader is
dead and starts an election.

## What "election fails" means

The leader crashes. The rest start an election. But it does not finish cleanly —
the network is broken, messages are slow, nobody gets a majority. There are two
possible outcomes, and they are opposites:

| Outcome | What happens | How bad |
| --- | --- | --- |
| **No leader** | Nobody accepts writes. Requests fail or time out | An **outage**. Annoying — but your data is safe |
| **Two leaders (split-brain)** | Both accept writes, independently | **Data corruption.** Much worse |

> Every serious system deliberately chooses the outage. A few seconds of
> downtime is recoverable. Corrupted data usually is not.

## Split-brain, drawn

The network splits the cluster in two. Both halves are alive and healthy; they
simply cannot reach each other. Without quorum, both halves elect a leader:

![A partitioned cluster in which both sides have elected a leader and both are accepting writes](diagrams/leader-election-notes-split-brain.jpg)

Now the same row has two different values on two machines, the copies drift
apart with no correct way to merge them, and whichever version you eventually
keep, **somebody's saved data is silently lost**.

It is the worst failure mode in distributed systems because it is **quiet**. No
errors are thrown. Both halves look perfectly healthy on every dashboard. You
find out days later, when the numbers do not add up.

## Why elections fail

Network partitions, slow links, node crashes, badly tuned timeouts, delayed
heartbeats, and a GC pause that freezes a node long enough for everyone else to
conclude it died.

Underneath all of those is one fact, and it is worth saying out loud in an
interview:

> **You cannot tell the difference between "that node is dead" and "that node is
> slow."**

All you know is that no heartbeat arrived. Maybe it crashed. Maybe the message
is stuck in a router. Every failure detector in the world is a guess based on a
timeout. This is not sloppy engineering — it is a proven limit called **FLP
impossibility**.

## How systems do it safely

**1. Consensus algorithms** — Raft, Paxos, ZAB, and the products built on them
(ZooKeeper, etcd). They do not prevent failures. They guarantee something
narrower and far more useful: **at most one leader per term**, whatever fails.

**2. Quorum.** A node becomes leader only if more than half the cluster votes
for it. In a five-node cluster it needs three. This is what makes split-brain
mathematically impossible: you cannot have two disjoint groups that each contain
more than half of the same cluster. Split 3 against 2, and only the three can
elect anyone — the two know they are the minority and step down.

![A partitioned cluster where the three-node majority elects a leader and the two-node minority steps down](diagrams/leader-election-notes-quorum.jpg)

> **Corollary: always use an odd number of nodes.** A four-node cluster needs
> three to agree, so it survives exactly one failure — the same as a three-node
> cluster. You bought a machine and gained nothing.

**3. Terms and epochs.** Every election increments a counter that only ever goes
up. Any message stamped with an old term is rejected on arrival. This is how the
cluster ignores ghosts from the past.

**4. Fencing tokens — the part most people miss.** Quorum protects the
*election*. It does nothing about this:

![A garbage-collection-paused old leader waking up and having its write rejected because its term is lower than the highest storage has seen](diagrams/leader-election-notes-fencing.jpg)

Every write carries the leader's term number, and **the storage layer refuses
any write whose term is lower than the highest it has already seen**. That
number is the fencing token. Without it you can still get split-brain with a
perfectly correct election.

**5. Randomised election timeouts.** If every follower gives up at the same
instant, they all become candidates at once, split the votes evenly, and nobody
wins — repeatedly. A random delay of 150–300ms means somebody always goes first
and wins. Raft's *pre-vote* step goes further and stops a node that was cut off
from disrupting a perfectly healthy leader when it rejoins.

## What happens while there is no leader

Most systems **stop accepting writes** during an election. Writes fail, some
requests time out, availability dips for a few seconds. That is the correct
trade — it is CAP with **consistency** chosen over availability, deliberately.

On the application side it means: **retry with backoff, and make your writes
idempotent**, so a client can safely re-send the same request once the new
leader is up without double-charging anybody.

## Tuning is the real-world problem

In practice most incidents are not the algorithm. They are the timeouts.

- **Too short** → a healthy leader is voted out over a small network hiccup →
  **election storms**, endless churn, and a cluster that spends all its time
  voting instead of working
- **Too long** → a genuinely dead leader goes unnoticed for a minute → a long
  outage

Rule of thumb: election timeout ≫ heartbeat interval ≫ realistic network round
trip **and** your worst GC pause. That last clause is the one people forget, and
it is why a JVM cluster with a two-second stop-the-world pause and a
one-second election timeout will flap forever.

## What to watch

**Leader changes per hour** is the single most useful signal. Then: time spent
with no leader, heartbeat latency, healthy nodes against quorum size, and how
fast the term number is climbing — a term rising quickly means churn even if a
leader is technically always present.

## Takeaway

**One leader at a time, enforced by quorum plus terms plus fencing.** When in
doubt, stop writing: a few seconds of lost availability is recoverable, and lost
consistency usually is not.
`,ra=`# Leader election fails — interview questions

Ten questions, easy to hard, then the concepts by name. This is the scenario
where the interviewer is checking one specific thing: whether you know that
quorum is not the whole answer.

## Easy

### 1. Why does a cluster need a leader?

Because some jobs must be done by exactly one machine or they are wrong.
Accepting writes needs a single ordering point. A scheduled job needs to run
once, not once per node. Shard ownership needs one decider. The leader is a
deliberate single point of coordination, because coordinating is the job.

### 2. What is a heartbeat, and what does a missing one mean?

A small "I'm alive" message the leader sends every few hundred milliseconds. A
follower that stops receiving them for longer than its election timeout assumes
the leader is dead and starts an election. What a missing heartbeat *means* is
weaker than it sounds — see question 5.

### 3. What is split-brain?

Two nodes both believing they are the leader at the same time, and both
accepting writes. The same record ends up with two different values on two
machines, the copies diverge, and whichever one you eventually keep, somebody's
saved data is silently lost.

## Medium

### 4. An election fails. Which outcome do you want, and why?

No leader. Nobody accepts writes, requests fail or time out, and you have an
outage — but the data is intact and the situation is recoverable in seconds.
The alternative is split-brain, which is silent data corruption you may not
notice for days.

Every serious system makes this choice deliberately. It is CAP with consistency
picked over availability.

### 5. Why is split-brain considered worse than downtime when downtime is what users see?

Because downtime is loud and bounded, and corruption is quiet and unbounded.
During an outage every dashboard is red, every client gets an error, and the
moment the new leader is up the system is correct again. During split-brain no
errors are thrown, both halves look perfectly healthy, and the divergence keeps
growing. You discover it a week later when a reconciliation report disagrees
with itself, and by then there is no correct merge — you are choosing whose
writes to throw away.

### 6. How does quorum make split-brain impossible?

A node becomes leader only with votes from more than half the cluster. Two
disjoint groups cannot both contain more than half of the same set, so two
leaders cannot both be elected. If a five-node cluster splits three against two,
only the three can form a majority; the two know they are the minority and step
down rather than serving writes.

The corollary is the sizing rule: **always an odd number of nodes**. A four-node
cluster needs three to agree, so it tolerates exactly one failure — identical to
a three-node cluster. The fourth machine bought nothing.

### 7. Why do elections use randomised timeouts?

Because if every follower gives up at the same instant, they all become
candidates simultaneously, split the vote evenly, and nobody reaches a majority
— and then they all time out together and do it again. A random delay in the
150–300ms range means one node always goes first and wins.

Raft adds a *pre-vote* phase for the related problem: a node that was
partitioned off, and has been incrementing its term while alone, would otherwise
force a perfectly healthy leader to step down the moment it rejoins.

## Hard

### 8. Your election is provably correct and you still get two writers. How?

A GC pause, or any freeze. The leader stops for thirty seconds; the cluster
correctly concludes it is dead and elects a new one at term 6. Then the old
leader wakes up. It has no idea time has passed — from its point of view it is
still the leader at term 5 — and it confidently sends a write.

Quorum protected the election, but nothing protected the *write*. The fix is a
**fencing token**: every write carries the leader's term, and the storage layer
refuses any write whose term is lower than the highest it has already accepted.
The old leader's term-5 write is rejected at the door.

This is the answer the question is really fishing for. Quorum plus terms is the
common answer; quorum plus terms plus fencing at the storage layer is the
correct one.

### 9. Most leader-election incidents are not algorithm bugs. What are they?

Timeout tuning, and it fails in both directions.

Too short, and a healthy leader is voted out over a momentary network hiccup.
The cluster churns — an **election storm** — and spends all its time voting
instead of doing work. Too long, and a genuinely dead leader goes unnoticed for
a minute, which is a long outage for something that should recover in seconds.

The rule is: election timeout much greater than heartbeat interval, which is
much greater than both the realistic network round trip **and your worst
stop-the-world GC pause**. That last clause is the one people leave out, and a
JVM cluster with a two-second pause and a one-second election timeout will flap
forever.

The hardest variant is **gray failure** — a node that is alive but degraded
rather than cleanly dead. It answers heartbeats slowly enough to be doubted and
fast enough to be re-elected, so leadership oscillates.

### 10. Give the whole scenario as one interview answer.

> Leader election exists so exactly one node orders writes, and it fails in two
> directions: **no leader**, a brief availability loss, or **split-brain** — two
> leaders both accepting writes — which is silent data corruption. So every real
> system deliberately chooses the outage. Safety comes from **quorum**, since
> two disjoint majorities cannot exist, **monotonic terms**, and crucially
> **fencing tokens** at the storage layer, because quorum alone does not stop a
> GC-paused leader waking up and writing. In practice most incidents are
> **tuning**, not algorithm — you fundamentally cannot distinguish a slow node
> from a dead one, so size the cluster odd, keep the election timeout well above
> the heartbeat interval and your worst GC pause, make client writes idempotent
> and retried with backoff, and alert on leader-change rate.

## The concepts, by name

| Concept | In one line | How it applies here |
| --- | --- | --- |
| **Consensus (Raft / Paxos / ZAB)** | Making many machines agree on one value despite failures | Guarantees at most one leader per term |
| **Quorum** | More than half must agree | 3 of 5. Two groups cannot both be a majority |
| **Odd cluster sizing** | 3, 5 or 7 — never 4 or 6 | 4 survives the same single failure as 3; the extra machine is wasted |
| **Split-brain** | Two leaders both accepting writes | The exact disaster everything else exists to prevent |
| **Fencing token / epoch** | An ever-increasing stamp on every write; storage rejects old stamps | Stops a GC-paused old leader writing after it wakes |
| **Heartbeat and failure detection** | Periodic "I'm alive" pings with a timeout | How a dead leader is noticed — and it is always a guess |
| **FLP impossibility** | In an async network you cannot guarantee agreement in bounded time | You genuinely cannot tell dead from slow |
| **Randomised timeouts** | Each node waits a slightly different duration | Prevents split votes where everyone campaigns at once |
| **CAP — choosing CP** | During a partition, pick consistency or availability | These systems pick consistency: stop writing rather than fork the data |
| **Lease** | Leadership granted for a fixed time, renewed or expired | A frozen leader loses its claim with nobody doing anything |
| **Network partition** | The cluster splits into groups that cannot reach each other | The trigger for most elections. The minority must step down |
| **Gray failure** | Alive but slow or degraded, not cleanly dead | The hardest case, and the usual cause of flapping |
| **Election storm** | Leaders repeatedly elected and unseated | Timeouts set too aggressively; the cluster only votes |
| **Idempotency + retry** | Safe to re-send the same request | Clients must retry writes that failed during the leaderless window |
| **Distributed lock ≠ leader election** | A Redis or DB lock is not the same thing | No quorum, no fencing — it can hand the lock to two holders |
| **Witness / tiebreaker node** | A cheap voting-only node with no data | Lets a two-datacentre setup still form an odd majority |
| **Observability of coordination** | Monitor the cluster's *agreement* health, not just CPU | Leader-change rate and leaderless time catch this early |
`,ia=[`notes`,`revision`,`interview`],aa=`Scenario-based HLD`,oa=[{slug:`solid-intro`,title:`Why SOLID exists`,tagline:`Coupling, cohesion, and the cost of change.`,summary:`SOLID is five answers to one question: why does a change that sounds small take a week? This is the question first — what coupling and cohesion actually are, what rigidity and fragility look like in a real codebase — and then the five acronyms as the answers they were written to be.`,subject:`System Design`,stage:`Foundations`,accent:`#38bdf8`,parts:{notes:ki,revision:Ai,interview:ji}},{slug:`single-responsibility`,title:`S — Single Responsibility`,tagline:`One reason to change.`,summary:`The most quoted and the most misquoted of the five. It does not say a class should do one thing; it says a class should answer to one person. This is what that distinction means in code, why "one thing" is unfalsifiable, and how to find the seams without splitting everything into fifty classes.`,subject:`System Design`,stage:`The five principles`,accent:`#34d399`,parts:{notes:Mi,revision:Ni,interview:z}},{slug:`open-closed`,title:`O — Open/Closed`,tagline:`Extend without editing.`,summary:`Open for extension, closed for modification — a sentence that sounds like it forbids editing code. It does not. It is about which file you have to open to add the next case, why the switch statement that grows a case per feature is the smell it names, and why guessing the axis of change in advance is how it goes wrong.`,subject:`System Design`,stage:`The five principles`,accent:`#f97316`,parts:{notes:B,revision:Pi,interview:Fi}},{slug:`liskov-substitution`,title:`L — Liskov Substitution`,tagline:`A subtype must keep the promise.`,summary:`The only one of the five with a formal definition, and the only one you can break without the compiler noticing. Preconditions, postconditions and invariants, the square-rectangle problem done properly, and why every collection in the JDK that throws UnsupportedOperationException is an admission.`,subject:`System Design`,stage:`The five principles`,accent:`#a78bfa`,parts:{notes:Ii,revision:Li,interview:Ri}},{slug:`interface-segregation`,title:`I — Interface Segregation`,tagline:`No client should depend on what it does not use.`,summary:`A fat interface makes every implementer carry methods it has no answer for, and every caller recompile when a method it never calls changes. This is what "fat" means measurably, role interfaces as the fix, and why the interface belongs to the caller rather than to the class implementing it.`,subject:`System Design`,stage:`The five principles`,accent:`#f472b6`,parts:{notes:zi,revision:Bi,interview:Vi}},{slug:`dependency-inversion`,title:`D — Dependency Inversion`,tagline:`Both sides depend on the abstraction.`,summary:`The one people think they already do because they use Spring. Inversion is about which module owns the interface, not about who calls new — dependency injection is a technique, this is the rule it serves. Includes the ports and adapters layout that falls straight out of it.`,subject:`System Design`,stage:`The five principles`,accent:`#facc15`,parts:{notes:Hi,revision:Ui,interview:Wi}},{slug:`solid-in-practice`,title:`SOLID in practice`,tagline:`Where it pays, and where it costs.`,summary:`One CRUD service refactored the whole way through, principle by principle, with the diff at each step. Then the other half of the job: what over-applying each one looks like, why a principle is a smell detector rather than a checklist, and how SOLID sits next to DRY, YAGNI and the design patterns.`,subject:`System Design`,stage:`Putting it together`,accent:`#22d3ee`,parts:{notes:Gi,revision:Ki,interview:qi}},{slug:`design-interview`,title:`The system design interview`,tagline:`What is actually being scored.`,summary:`Two rounds hide behind one name: low-level design, where you are asked for classes and a pattern, and high-level design, where you are asked for a box diagram that survives ten million users. This is what each one is really testing, the shape of an answer that scores, and eighty-eight questions with the answers written out.`,subject:`System Design`,stage:`The interview`,accent:`#8b6cff`,parts:{interview:Ji}},{slug:`hld-vocabulary`,title:`The words first`,tagline:`The vocabulary every scenario assumes.`,summary:`Client, replica, pool, p99, replication lag, TTL — the words the four scenarios are written in, gathered once so none of them has to stop and explain. Written as prose rather than a table, with every term tappable for what it means. Also the one diagram every scenario is a sentence about: clients, load balancer, app tier, cache, pool, primary and replicas.`,subject:`System Design`,stage:aa,accent:`#94a3b8`,parts:{notes:Yi}},{slug:`connection-pool-exhaustion`,title:`Connection pool exhaustion`,tagline:`The database is idle and the site is down.`,summary:`The bounded resource nobody watches. Why a pool exists, what Little’s Law says about the size you actually need, and how one slow query becomes a cascading failure through head-of-line blocking and a retry storm — then the architecture that survives it: caching, replicas, bulkheads, circuit breakers and a shared pooler.`,subject:`System Design`,stage:aa,accent:`#f97316`,parts:{notes:Xi,interview:Zi}},{slug:`schema-migration`,title:`Schema migration fails midway`,tagline:`Half the fleet on old code, half on new.`,summary:`A migration is not a database change, it is a distributed change — during any rolling deploy old and new code run against one database at once. Why a rename is the most dangerous thing you can ship, expand–migrate–contract step by step, online schema change with a shadow table, and why rollback is usually not available.`,subject:`System Design`,stage:aa,accent:`#38bdf8`,parts:{notes:Qi,interview:$i}},{slug:`cache-inconsistency`,title:`Cache drifts from the database`,tagline:`Nothing failed, and the price is still wrong.`,summary:`Cache and database are two systems with no shared transaction, so staleness is the price of the speed rather than a bug. The cache-aside race that leaves a permanently wrong value with nothing having failed, why you delete instead of update, TTL with jitter, and CDC-driven invalidation for caches spread across twenty servers.`,subject:`System Design`,stage:aa,accent:`#34d399`,parts:{notes:ea,interview:ta}},{slug:`leader-election`,title:`Leader election fails`,tagline:`No leader is an outage. Two is corruption.`,summary:`Why a cluster deliberately chooses downtime over split-brain, how quorum makes two leaders arithmetically impossible, and the part most answers miss — fencing tokens, because quorum protects the election and nothing else stops a GC-paused old leader waking up and writing. Plus why most real incidents are timeout tuning.`,subject:`System Design`,stage:aa,accent:`#a78bfa`,parts:{notes:na,interview:ra}}].map(e=>{let t=ia.filter(t=>e.parts[t]).map((t,n)=>({id:t,...ee[t],content:C(e.parts[t]),route:n===0?`design/${e.slug}`:`design/${e.slug}/${t}`}));return{...e,parts:t,route:`design/${e.slug}`}}),sa=e=>[e.title,e.tagline,e.summary,e.subject,e.stage].concat(e.parts.map(e=>e.content)).join(` `).toLowerCase(),ca=()=>oa.filter(e=>e.stage===aa),la=e=>oa.find(t=>t.slug===e)??null,ua=(e,t)=>e?.parts.find(e=>e.id===t)??e?.parts[0]??null,da=`Open a REPL — any REPL, in any language — and type the simplest sum a child
could check:

\`\`\`text
0.1 + 0.2
0.30000000000000004
\`\`\`

That is not a typing mistake and it is not a bug in the language you happened to
pick. It is the same answer in Node, in Python, in C, and in \`jshell\`:

\`\`\`text
jshell> 0.1 + 0.2
$1 ==> 0.30000000000000004
\`\`\`

Two different runtimes, written by different people, decades apart, agreeing to
the last digit. When that happens the answer is never a bug — it is a shared
standard underneath both of them, and the standard is
[IEEE 754](https://en.wikipedia.org/wiki/IEEE_754), which is what a \`double\` is.

This piece is about why the 4 is there, why it is exactly a 4, and what to write
instead when you need money to add up. You need to be able to run a Java file;
that is all.

## The machine only has two digits

Everything below follows from one constraint. You write numbers with ten digits
available. The hardware has two.

![Ten digits going in, two coming out](diagrams/floating-point-notes-base-two.jpg)

So every number you type has to be re-spelled in base two before anything can be
done with it. For whole numbers that is a clean, finite job. For fractions it is
not, and the difference between those two sentences is the whole chapter.

## Whole numbers always fit

To write a whole number in binary you halve it repeatedly and keep the
remainders:

![Ten, halved down to nothing](diagrams/floating-point-notes-halving.jpg)

Read the remainders bottom to top and you have the answer:

![Ten in base ten is 1010 in base two](diagrams/floating-point-notes-ten-in-binary.jpg)

This process **always** ends. Each step halves the number, so a number the size
of ten takes four steps and a number the size of a billion takes thirty. There
is no whole number you can write down that binary cannot spell exactly, as long
as you have enough bits for it. \`int\` gives you 32 of them, \`long\` gives you 64,
and inside that range arithmetic is exact.

## Fractions are the problem

The mirror image of halving is doubling. To write a fraction in binary you
double it repeatedly and keep the whole part that falls out:

![0.1 doubled, over and over, never reaching zero](diagrams/floating-point-notes-doubling.jpg)

Look at the fifth line. \`0.6 × 2 = 1.2\` leaves \`0.2\` behind — which is where the
second line started. The process has arrived back where it was, so from here it
repeats \`0011\` forever and never reaches zero:

\`\`\`text
0.1  in base two  =  0.0001100110011001100110011…
\`\`\`

This is not a strange property of one tenth. It is the same thing that happens
to one third in base ten: \`1/3 = 0.3333…\` and no amount of paper finishes it.
Which fractions terminate depends entirely on the base. In base ten a fraction
terminates when its denominator is built only from 2s and 5s — the factors of
ten. In base two the only factor available is 2, so **only fractions whose
denominator is a power of two terminate**.

That is a short list, and it is the list of decimals a \`double\` holds exactly:

- \`0.5\` is 1/2 — exact.
- \`0.25\` is 1/4 — exact.
- \`0.75\` is 1/2 + 1/4 — exact.
- \`0.1\` is 1/10, and 10 is 2 × 5. The 5 is fatal. Not exact.

\`0.1\`, \`0.2\` and \`0.3\` are all fatal for the same reason. Nothing about them is
exact before the arithmetic has even started.

## Sixty-four bits, and then it stops

A \`double\` has to fit in 64 bits, laid out like this:

![The three fields of an IEEE 754 double](diagrams/floating-point-notes-bit-layout.jpg)

Fifty-two bits of fraction is the entire budget. The expansion of \`0.1\` is
infinite, so it gets cut off and rounded to the nearest value those 52 bits can
represent — and that value is not one tenth. It is very slightly more:

![What the three literals actually hold](diagrams/floating-point-notes-stored-value.jpg)

Those are not approximations of what is stored. Those *are* what is stored,
printed to the last digit. The moment you write \`double x = 0.1;\` you have
already lost, quietly, before any operator has run.

## So where does the 4 come from

Now the sum. Two numbers that are each slightly wrong get added, and the exact
answer to *that* addition does not fit in 52 bits either, so it is rounded a
third time:

![Two rounded inputs, one rounded result, and a gap that survives](diagrams/floating-point-notes-the-sum.jpg)

The stored \`0.3\` and the computed \`0.1 + 0.2\` are not the same number. They
differ by one bit in the last place — \`3fd3333333333333\` against
\`3fd3333333333334\` — which is about 5.5 × 10⁻¹⁷. That single bit is the 4 on the
end.

And that is the honest summary of the whole thing: **three roundings, one of
which lands one bit away from the other side.**

## Why \`0.3\` on its own still prints as \`0.3\`

Here is the part that makes it feel like a conspiracy. If \`0.3\` is not really
0.3, why does printing it show \`0.3\`?

Because \`Double.toString\` does not print the stored value. It prints **the
shortest decimal that reads back as that exact same double** — a rule called
shortest round-trip printing. For the stored 0.3 the shortest such decimal is
\`0.3\`, so that is what you see. For the sum of 0.1 and 0.2 the shortest one that
round-trips is \`0.30000000000000004\`, because \`0.3\` would read back as the
*other* double.

So printing is not lying to you. It is telling you the shortest true thing, and
the extra digits appear precisely when the shortest true thing is long.

## See all of it at once

\`\`\`java FloatingPoint.java @run-floating-point-floating-point
import java.math.BigDecimal;

public class FloatingPoint {

    public static void main(String[] args) {

        System.out.println("0.1 + 0.2         = " + (0.1 + 0.2));
        System.out.println("0.1 + 0.2 == 0.3  : " + (0.1 + 0.2 == 0.3));
        System.out.println("0.1 * 3           = " + (0.1 * 3));
        System.out.println("1.03 - 0.42       = " + (1.03 - 0.42));

        double total = 0;
        for (int i = 0; i < 10; i++) total += 0.1;
        System.out.println("0.1 added ten times = " + total);

        System.out.println();

        // Not approximations of the stored values — the stored values.
        System.out.println("0.1   is really " + new BigDecimal(0.1));
        System.out.println("0.2   is really " + new BigDecimal(0.2));
        System.out.println("0.3   is really " + new BigDecimal(0.3));
        System.out.println("their sum is    " + new BigDecimal(0.1 + 0.2));

        System.out.println();

        // One bit apart, which is the entire story.
        System.out.println("bits of 0.3       : " + Long.toHexString(Double.doubleToLongBits(0.3)));
        System.out.println("bits of 0.1 + 0.2 : " + Long.toHexString(Double.doubleToLongBits(0.1 + 0.2)));
        System.out.println("the gap           : " + ((0.1 + 0.2) - 0.3));

        System.out.println();

        // Powers of two are exact, so nothing surprising happens here.
        System.out.println("0.5 + 0.25 == 0.75 : " + (0.5 + 0.25 == 0.75));
        // And the exact way to add two decimals is not to use double at all.
        System.out.println("BigDecimal sum     : "
                + new BigDecimal("0.1").add(new BigDecimal("0.2")));
    }
}
\`\`\`

\`\`\`output @run-floating-point-floating-point
0.1 + 0.2         = 0.30000000000000004
0.1 + 0.2 == 0.3  : false
0.1 * 3           = 0.30000000000000004
1.03 - 0.42       = 0.6100000000000001
0.1 added ten times = 0.9999999999999999

0.1   is really 0.1000000000000000055511151231257827021181583404541015625
0.2   is really 0.200000000000000011102230246251565404236316680908203125
0.3   is really 0.299999999999999988897769753748434595763683319091796875
their sum is    0.3000000000000000444089209850062616169452667236328125

bits of 0.3       : 3fd3333333333333
bits of 0.1 + 0.2 : 3fd3333333333334
the gap           : 5.551115123125783E-17

0.5 + 0.25 == 0.75 : true
BigDecimal sum     : 0.3
\`\`\`

Two lines deserve a note.

**\`0.1 added ten times\` is \`0.9999999999999999\`, not \`1.0\`.** Each \`+=\` rounds,
and nine roundings in a row drift the other way from the single rounding in
\`0.1 + 0.2\`. There is no rule of thumb about which direction the error goes;
that is why "just round at the end" is not a strategy.

**\`0.5 + 0.25 == 0.75\` is \`true\`.** Halves and quarters are powers of two, so
nothing was ever approximated and \`==\` behaves exactly as your instinct says it
should. Floating point is not vague — it is exact arithmetic on a set of values
that happens not to include one tenth.

## What to write instead

Three answers, and which one is right depends entirely on what the number *is*.

**Money is not a \`double\`.** Ever. Hold it as a \`long\` of minor units — pence,
cents, paise — or as \`BigDecimal\`. A \`long\` of cents is exact, fast, and the
arithmetic is ordinary integer arithmetic:

\`\`\`java
long price = 1099;      // £10.99
long tax   = price * 20 / 100;
\`\`\`

**Exact decimal arithmetic is \`BigDecimal\`**, and it must be built from a
\`String\`:

\`\`\`java
new BigDecimal("0.1").add(new BigDecimal("0.2"))   // 0.3, exactly
new BigDecimal(0.1)                                // 0.1000000000000000055511…
\`\`\`

\`new BigDecimal(0.1)\` takes the \`double\` — already wrong — and records its error
in full precision. \`BigDecimal.valueOf(0.1)\` goes via \`Double.toString\` and gives
you \`0.1\`, which is usually what you meant, but the \`String\` constructor is the
one with no surprises in it.

**Measurements can stay \`double\`** — a temperature, a distance, a weight, a
probability. Those come from instruments with error bars far wider than 10⁻¹⁷,
and \`double\` is fast and has plenty of range. What changes is how you *compare*
them:

\`\`\`java
if (a == b)                      // wrong for computed doubles
if (Math.abs(a - b) < 1e-9)      // a tolerance you chose deliberately
\`\`\`

The tolerance has to be yours. There is no universally correct epsilon, because
the size of the gap between adjacent doubles grows with the magnitude of the
number — near 1.0 it is about 2 × 10⁻¹⁶, and near a billion it is about
10⁻⁷.

## When it goes wrong

- **A total is off by a penny** — money went through a \`double\` somewhere.
  Find it and make it a \`long\` of minor units. Rounding at the end does not fix
  an error that accumulated on the way.
- **A loop never terminates** — \`for (double d = 0; d != 1.0; d += 0.1)\`
  steps straight past \`1.0\` without ever equalling it. Count with an \`int\` and
  derive the \`double\` from it.
- **A test passes locally and fails in CI** — usually a different rounding in
  the print path or a different JDK's \`Math\` intrinsic, not a different answer.
  Compare with a tolerance rather than a string.
- **\`assertEquals(0.3, a + b)\` fails** — use the three-argument overload with a
  delta, which exists for exactly this reason.
- **A value sorts strangely or vanishes from a \`HashSet\`** — \`NaN\` is not
  equal to itself under \`==\`, and \`0.0 == -0.0\` is true while their
  \`Double.equals\` is false. Use \`Double.compare\` when the ordering matters.
- **Summing a large array loses accuracy** — the running total grows until
  small addends fall off the end of its precision. Sum smallest first, or use
  \`DoubleStream.sum\`, which does the compensation for you.

## Where this leads

The rest of it — the exact bit layout, subnormals, \`ulp\`, why \`0.1f + 0.2f\`
*does* equal \`0.3f\`, and how to make a sum of a million doubles come out right —
is in the **Interview** part, where the Medium and Hard questions work through
each one with the numbers attached.

## Further reading

- [What Every Computer Scientist Should Know About Floating-Point Arithmetic](https://docs.oracle.com/cd/E19957-01/806-3568/ncg_goldberg.html)
- [JLS §4.2.3: floating-point types and values](https://docs.oracle.com/javase/specs/jls/se21/html/jls-4.html#jls-4.2.3)
- [\`BigDecimal\` API documentation](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/math/BigDecimal.html)
- [floating-point-gui.de](https://floating-point-gui.de/)
`,fa='Fourteen questions on floating point. The first six are asked constantly — the\n`0.1 + 0.2` question is one of the most common opening questions in any language\n— and the rest are what separates knowing the answer from understanding it.\n\nThe **Notes** cover the Easy set. Medium and Hard go past them, and every answer\nhere is self-contained.\n\n## Easy\n\n### 1. Why is `0.1 + 0.2` not `0.3`?\n\nBecause neither operand is exactly what you wrote. `0.1` and `0.2` have infinite\nrepeating expansions in binary, so each is rounded to the nearest `double` on the\nway in — both slightly high. Adding those two rounded values and rounding the\nresult once more lands on the double **one bit above** the one that `0.3`\nrounds to. `Double.toString` then prints the shortest decimal that identifies\nthat particular double, and that decimal is `0.30000000000000004`.\n\n### 2. Is this a Java problem?\n\nNo. `double` is IEEE 754 binary64, which is the same in JavaScript, Python, C,\nC#, Go and the hardware itself, so all of them print the same digits. Languages\nthat appear not to have the problem are either rounding on the way out or using\na decimal type by default.\n\n### 3. Which decimal values *are* exact in a `double`?\n\nThose whose fractional part is a sum of powers of two — so `0.5`, `0.25`,\n`0.75`, `0.125` and so on. Written as a fraction in lowest terms, a value is\nexact only when its denominator is a power of two. `1/10` has a factor of 5 in\nits denominator, and 5 is not available in base two, so it repeats forever.\n\n### 4. Why does printing `0.3` show `0.3` if it is not stored exactly?\n\n`Double.toString` does not print the stored value. It prints the **shortest\ndecimal string that reads back as that same double** — shortest round-trip\nprinting. For the stored 0.3 that string is `"0.3"`. For `0.1 + 0.2` it is\n`"0.30000000000000004"`, because `"0.3"` would read back as a different double.\n`new BigDecimal(0.3)` prints the real stored value if you want to see it.\n\n### 5. How should two doubles be compared?\n\nWith a tolerance you chose deliberately — `Math.abs(a - b) < epsilon` — not with\n`==`. `==` is correct only for values you know were never computed, such as a\nliteral you assigned and never touched. The epsilon has to suit the magnitudes\ninvolved, because the gap between adjacent doubles grows with the size of the\nnumber.\n\n### 6. What should be used for money?\n\nA `long` of minor units — pence, cents — or `BigDecimal` built from a `String`.\n`long` is exact, fast, and uses ordinary integer arithmetic; `BigDecimal` is\nright when you need arbitrary scale and explicit rounding modes. `double` is\nwrong for money at any scale, and rounding at the end does not repair an error\nthat accumulated on the way.\n\n## Medium\n\n### 7. Walk through how `0.1` is stored, bit by bit.\n\nConvert by repeated doubling: `0.1 × 2 = 0.2` gives bit 0, then 0.4 gives 0, 0.8\ngives 0, `0.8 × 2 = 1.6` gives 1 with 0.6 carried, `0.6 × 2 = 1.2` gives 1 with\n0.2 carried — and 0.2 is where the second step started, so `0011` repeats\nforever. Normalising `0.0001100110011…` gives `1.100110011… × 2⁻⁴`. The sign bit\nis 0, the exponent field holds `-4 + 1023 = 1019`, and the fraction field takes\nthe first 52 bits after the leading 1, rounded. `Double.toHexString(0.1)` shows\nthe result as `0x1.999999999999ap-4` — the final `a` is the rounding.\n\n### 8. What is a "unit in the last place", and why does it matter?\n\nThe distance between one representable double and the next one. Because the\nexponent scales the whole ladder, that distance is not constant: `Math.ulp(1.0)`\nis about 2.2 × 10⁻¹⁶, and `Math.ulp(1e16)` is 2.0. It matters because it is what\nmakes a fixed epsilon wrong — `1e-9` allows millions of ulps of slack near 1.0\nand less than one ulp near a billion — and because above 2⁵³ consecutive\nintegers no longer exist, so `1e16 + 1 == 1e16`.\n\n### 9. Why is `0.1f + 0.2f == 0.3f` true when the `double` version is false?\n\nA `float` has 24 bits of precision rather than 53, so its representable values\nare much further apart. The errors in `0.1f` and `0.2f` are individually far\nlarger than in the `double` case, but the sum and `0.3f` both round into the same\n24-bit slot, so they compare equal. `float` is less accurate here, not more —\nthe coarser grid hid the discrepancy rather than avoiding it.\n\n### 10. Why is `NaN == NaN` false but `Double.valueOf(NaN).equals(NaN)` true?\n\n`==` follows IEEE 754, where NaN means "not a number" and compares false against\neverything including itself. `Double.equals` deliberately breaks from that so\nthat `equals`/`hashCode` stay reflexive — otherwise a `Double` used as a map key\ncould be stored and never retrieved, and a `List.contains` on it would always be\nfalse. The same split shows up with zero: `0.0 == -0.0` is true, but\n`Double.compare(0.0, -0.0)` returns 1.\n\n### 11. `new BigDecimal(0.1)`, `BigDecimal.valueOf(0.1)`, `new BigDecimal("0.1")` — what is the difference?\n\n`new BigDecimal(0.1)` takes the `double`, which is already wrong, and records its\nexact value — `0.1000000000000000055511…`. `BigDecimal.valueOf(0.1)` routes\nthrough `Double.toString`, so it gets `0.1`, which is usually what was meant.\n`new BigDecimal("0.1")` never involves a `double` at all and is the one to reach\nfor by default. Note also that `BigDecimal.equals` compares scale as well as\nvalue, so `2.0` and `2.00` are unequal — use `compareTo(...) == 0`.\n\n### 12. Why can a parallel sum of doubles give a different answer each run?\n\nFloating-point addition is not associative: `(a + b) + c` and `a + (b + c)` can\nround differently. A parallel stream splits the data into chunks, sums them\nindependently and combines them, and the split points depend on the fork-join\npool, so the grouping — and therefore the rounding — can change between runs.\nIt is also the reason the JIT is not permitted to reorder a summation loop for\nyou.\n\n## Hard\n\n### 13. How would you sum a million doubles accurately?\n\nNaive accumulation loses the low bits of each addend once the running total is\nlarge. **Compensated (Kahan) summation** keeps the discarded part in a second\nvariable and adds it back on the next iteration, which reduces the error to\nroughly a constant instead of growing with n. In practice you do not write it:\n`DoubleStream.sum` carries a compensation term internally, so\n`Arrays.stream(values).sum()` returns exactly `100000.0` for a million tenths\nwhere a hand-written loop returns `100000.00000133288`. Alternatives are sorting\nsmallest-magnitude first, or dropping to `BigDecimal` when exactness matters more\nthan speed.\n\n### 14. What are subnormal numbers, and what is `strictfp`?\n\n**Subnormals** use the reserved all-zeros exponent to switch off the implicit\nleading 1, filling the gap between the smallest normal number and zero with a\ngradual slope rather than a cliff. They guarantee that `a - b == 0` implies\n`a == b`, and on some hardware they execute far more slowly — which is where\noccasional reports of a loop mysteriously slowing near zero come from.\n\n**`strictfp`** is history. Before Java 17, intermediate results were allowed to\nbe computed with the x87\'s extra precision, so identical code could give\ndifferent answers on different hardware unless the method was marked `strictfp`.\n[JEP 306](https://openjdk.org/jeps/306) made strict evaluation mandatory\neverywhere, so since Java 17 the keyword is a no-op and can be deleted.\n',pa='The chapter in a page. If a line here is news, the **Notes** or the **Interview**\nanswers are where it comes from.\n\n## The one-sentence answer\n\n- `0.1` and `0.2` cannot be stored exactly in binary, so each is rounded on the\n  way in. Their sum is rounded again, and it lands **one bit** above the stored\n  `0.3`. That bit is the `4` on the end of `0.30000000000000004`.\n- Not a Java bug. IEEE 754, so Node, Python and C all give the same digits.\n\n## Why fractions break and integers do not\n\n- Whole numbers convert by **repeated halving**, which always terminates.\n- Fractions convert by **repeated doubling**, which terminates only when the\n  denominator is a power of two.\n- `1/10` in binary is `0.0001100110011…` with `0011` repeating forever — the\n  same way `1/3` never finishes in base ten.\n- Exact: `0.5`, `0.25`, `0.75`, `0.125`. Not exact: `0.1`, `0.2`, `0.3`.\n\n## The 64 bits\n\n- **1** sign · **11** exponent (biased by 1023) · **52** fraction.\n- The leading `1.` is implicit, so precision is **53 bits**, about 15–17\n  decimal digits.\n- `Double.toHexString(0.1)` is `0x1.999999999999ap-4` — the `a` on the end *is*\n  the rounding.\n- `0.1 + 0.2` is `3fd3333333333334`; `0.3` is `3fd3333333333333`. One bit.\n  `Math.nextUp(0.3)` returns the sum.\n\n## Why `0.3` still prints as `0.3`\n\n- `Double.toString` prints the **shortest decimal that reads back as the same\n  double**, not the stored value.\n- So the extra digits appear exactly when the shortest true answer is long.\n- `new BigDecimal(0.3)` shows the real thing:\n  `0.29999999999999998889776975374843…`.\n\n## `ulp` — the gap between neighbours\n\n- Doubles are dense near zero, sparse near the top. `Math.ulp` gives the gap.\n- `ulp(1.0)` ≈ 2.2e-16 · `ulp(1e9)` ≈ 1.2e-7 · `ulp(1e16)` = **2.0**.\n- So `1e16 + 1 == 1e16`. Above 2⁵³ consecutive integers do not exist.\n- There is **no universal epsilon**: `1e-9` is generous near 1.0 and stricter\n  than bitwise-equal near a billion.\n\n## What to use instead\n\n- **Money** — `long` of minor units, or `BigDecimal`. Never `double`.\n- **Exact decimals** — `new BigDecimal("0.1")`. The `String` constructor.\n  `new BigDecimal(0.1)` records the error in full; `BigDecimal.valueOf(0.1)`\n  goes via `Double.toString` and gives `0.1`.\n- **Measurements** — `double` is fine. Compare with a tolerance you chose:\n  `Math.abs(a - b) < 1e-9`.\n- **Loop counters** — count with an `int` and derive the double. `d != 1.0`\n  with `d += 0.1` never terminates.\n\n## NaN, infinity and signed zero\n\n- `1 / 0.0` is `Infinity`. Integer division by zero throws; this does not.\n- `0.0 / 0.0` is `NaN`, and `NaN == NaN` is **false**.\n- But `Double.valueOf(NaN).equals(NaN)` is **true** — otherwise a map key could\n  never be found again.\n- `0.0 == -0.0` is true; `Double.compare(0.0, -0.0)` is `1`.\n- **`==` for arithmetic, `Double.compare` for ordering and collections.** A\n  comparator using `<` breaks its contract the first time a NaN arrives.\n\n## The traps\n\n- `0.1f + 0.2f == 0.3f` is **true** — `float` is *less* precise, so both errors\n  round into the same slot. Not evidence that float is safer.\n- `String.format("%.2f", 2.675)` is `2.68`; `new BigDecimal(2.675)` rounded\n  half-up is `2.67`. Formatter rounds the shortest representation, `BigDecimal`\n  rounds the stored value. Both correct.\n- `BigDecimal.equals` compares scale too, so `2.0` is not equal to `2.00`. Use\n  `compareTo(...) == 0`.\n- Floating-point addition is **not associative**, so a parallel sum is not\n  reproducible and the JIT may not reorder your loop.\n- `DoubleStream.sum` compensates internally and beats a hand-written loop:\n  a million tenths give `100000.0` against the loop\'s `100000.00000133288`.\n- Since **Java 17** all arithmetic is strict and `strictfp` does nothing.\n',ma=`Somebody has to turn your data into HTML. The only question is who, and when —
your server before the response is sent, or the reader's browser afterwards.

That is the whole of the argument, and every name in it — SSR, CSR, SSG, ISR,
islands, streaming — is a different answer to those two words. It helps a great
deal to have read [what the browser does with a response](#/advanced/deep-browser)
first, because every trade-off below is a step of that pipeline being moved.

## The two shapes

![The same page rendered on the client and on the server](diagrams/rendering-notes-who-and-when.jpg)

**Client-side rendering.** The server sends an almost empty document and a
bundle of JavaScript. The browser runs it, the code fetches the data, builds the
DOM and paints. Everything after the first paint is fast, because navigating is
just more JavaScript.

**Server-side rendering.** The server fetches the data, builds the HTML and
sends a finished page. The browser paints it as it arrives. Then the same
JavaScript loads anyway, to make the page interactive — that second step is
called hydration and it is where most of the surprises live.

\`\`\`text
CLIENT                            SERVER
  request                           request
  |  empty shell + JS               |  server fetches data
  |  parse, execute                 |  server builds HTML
  |  fetch data      <- a second    |  full page
  |  build DOM          round trip  |  paint            <- reader sees it here
  |  paint  <- reader sees it here  |  JS loads
                                    |  hydrate          <- interactive here
\`\`\`

The reader sees something much earlier on the right. They can *use* it at
roughly the same time on both, because both are waiting for the same bundle.
Holding those two sentences at once is most of understanding this subject.

## What is actually sent

![The bytes each approach puts in the response](diagrams/rendering-notes-what-is-sent.jpg)

And now the strings themselves. The client version sends 75 bytes of HTML and a
promise; the server version sends the page:

\`\`\`text
--- what the client version sends ---
<!doctype html>
<div id="root"></div>
<script src="/app.js" defer><\/script>

--- what the server version sends ---
<!doctype html>
<div id="root">
  <h1>Latest</h1>
  <ul>
    <li>Why the parser stops — 6 min</li>
    <li>Hydration, and what it costs — 9 min</li>
  </ul>
</div>
<script>window.__DATA__ = [{"title":"…","minutes":6}]<\/script>
<script src="/app.js" defer><\/script>
\`\`\`

Three things to notice, and they cause most of the differences below.

**The server version contains the content.** Anything that reads HTML rather
than running it — a crawler, a link preview, a text browser, a reader with
JavaScript disabled by a corporate policy — sees a page in one case and an empty
div in the other.

**The data is sent twice.** Once as markup, once as \`__DATA__\` for the
JavaScript to pick up so it does not have to fetch it again. That duplication is
the price of not making a second round trip.

**Both send the same bundle.** Server rendering does not remove any JavaScript.
It changes when the reader sees something, not how much code arrives.

## Hydration

![The gap between a page appearing and answering](diagrams/rendering-notes-hydration.jpg)

The server sent HTML. The browser painted it. But none of the buttons work yet,
because a button is only a button once the framework has attached its handlers.

Hydration is the framework walking the server's markup, building its own
internal tree beside it, and adopting the existing DOM nodes rather than
recreating them. It costs roughly what rendering on the client would have cost —
the same components run — which is why an SSR page can look ready and ignore
your clicks for a second.

Between paint and hydration the page is in the **uncanny valley**: readable,
scrollable, and unresponsive. It is the single most common complaint about
server rendering and it is not a bug — it is the shape.

Two rules follow. The server and client must produce the **same** markup — a
mismatch means the framework throws the server's work away and re-renders,
turning your optimisation into a slowdown. And anything that differs between the
two by nature — \`Date.now()\`, \`Math.random()\`, \`window.innerWidth\`, the reader's
timezone — has to be moved out of the first render.

## The third option: build it in advance

If the page is the same for everybody, neither answer to "when" is right. Build
it at **deploy time** and serve a file.

**Static site generation.** Every page is HTML on disk, served by a CDN, with no
server work per request at all. The fastest thing there is, and the site you are
reading is one.

**Incremental static regeneration** is the same thing with an expiry: serve the
built page, and when it is older than N seconds, rebuild it in the background
for the next visitor. It is \`stale-while-revalidate\` from the
[HTTP page](#/advanced/deep-http/advanced), applied to whole pages.

The limit is personalisation. A page that says "Hello, Ada" cannot be a shared
static file — but it can be a static file with one small piece fetched
afterwards, which is usually the right compromise.

## Choosing

![Client, server and static compared across what matters](diagrams/rendering-notes-choosing.jpg)

| | Client | Server | Static |
|---|---|---|---|
| First paint | Late | Early | Immediate |
| Interactive | Late | Late | Late |
| Server cost per request | None | Real | None |
| Works for crawlers | Poorly | Yes | Yes |
| Personalised content | Easy | Easy | Hard |
| Live data | Easy | Easy | Stale by design |
| Complexity | Low | High | Low |

Some honest rules of thumb:

- **Content that strangers arrive at from a search** — server or static. The
  first paint and the crawler both matter, and neither is negotiable.
- **An application behind a login** — client rendering is fine. Nobody indexes
  it, the first visit is a load screen either way, and every subsequent
  navigation is instant.
- **A marketing site** — static. It changes when somebody edits it, not when
  somebody visits it.
- **A dashboard** — client, with the shell static.

And one that people resist: **the choice is per route, not per site.** A product
page and its checkout have nothing in common; modern frameworks let each pick,
and picking one for everything is how you get a slow product page or an
overengineered dashboard.

## What SEO really needs

The scare story is that crawlers cannot run JavaScript. Google's can, and does.
The reality is more mundane and more annoying:

- Rendering is a **second pass**, queued after the initial crawl, and the queue
  can be hours or days behind.
- Other crawlers — social previews, link unfurlers, smaller search engines,
  most AI crawlers — genuinely do not run scripts at all. A page whose title and
  description are set by JavaScript has no title on any of them.
- The metadata in \`<head>\` is read from the HTML. If your framework writes
  \`<title>\` on the client, the preview card in a chat app will be blank.

So the practical rule is not "server render for SEO". It is: **whatever a
machine needs to read — title, description, canonical link, the main text —
should be in the HTML.** Everything else can be built in the browser.

## When it goes wrong

- **"Text content did not match"** in the console — a hydration mismatch. Look
  for a date, a random value, or something read from \`window\` during the first
  render.
- **The page flashes empty after appearing** — the mismatch above, at its worst:
  the framework discarded the server's markup.
- **Fast first paint, terrible interaction score** — hydration is doing too much
  at once. This is what islands and progressive hydration exist for; see the
  [advanced page](#/interview/deep-rendering/advanced).
- **Two identical API calls per page load** — the server fetched the data and
  the client fetched it again, because it was not handed the result.
- **It works locally and 500s in production** — server-rendering code touching
  \`window\`, \`document\` or \`localStorage\`, which do not exist on a server.
- **Every request is slow under load** — rendering is CPU work, and you are now
  doing it per visitor. Caching is not optional at that point.

## What to read next

- **[Rendering, underneath](#/interview/deep-rendering/advanced)** — streaming,
  islands, server components, caching a personalised page, and the data-fetching
  waterfalls that undo all of it.
- **[What the browser does with the response](#/advanced/deep-browser)** — the
  pipeline every one of these strategies is rearranging.

## Further reading

- [Rendering on the web](https://web.dev/articles/rendering-on-the-web)
- [Google: JavaScript SEO basics](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics)
`,ha=`The [notes](#/interview/deep-rendering) are the two shapes and the third option.
This is everything between them: streaming a page before it is finished, sending
less JavaScript rather than sending it earlier, caching something personalised,
and the mistakes that quietly cancel the whole exercise.

## What leaves the server, exactly

Neither strategy is a framework feature. Both are a string, and it is worth
seeing them built with no library involved at all:

\`\`\`javascript render.mjs
// The same page, prepared two ways. Nothing here is a framework: the point is
// what leaves the server, and that is just a string either way.

const posts = [
  { title: 'Why the parser stops', minutes: 6 },
  { title: 'Hydration, and what it costs', minutes: 9 },
]

/** Client-side rendering: an empty shell, and the work is sent as code. */
function clientShell() {
  return \`<!doctype html>
<div id="root"></div>
<script src="/app.js" defer><\/script>\`
}

/** Server-side rendering: the same page, already built. */
function serverHtml() {
  const items = posts
    .map((post) => \`    <li>\${post.title} — \${post.minutes} min</li>\`)
    .join('\\n')

  return \`<!doctype html>
<div id="root">
  <h1>Latest</h1>
  <ul>
\${items}
  </ul>
</div>
<script>window.__DATA__ = \${JSON.stringify(posts)}<\/script>
<script src="/app.js" defer><\/script>\`
}

for (const [name, html] of [['client', clientShell()], ['server', serverHtml()]]) {
  console.log(\`--- what the \${name} version sends ---\`)
  console.log(html)
  console.log(\`(\${html.length} bytes of HTML)\\n\`)
}
\`\`\`

\`\`\`bash @run-rendering-two-ways
node render.mjs
\`\`\`

\`\`\`output @run-rendering-two-ways
--- what the client version sends ---
<!doctype html>
<div id="root"></div>
<script src="/app.js" defer><\/script>
(75 bytes of HTML)

--- what the server version sends ---
<!doctype html>
<div id="root">
  <h1>Latest</h1>
  <ul>
    <li>Why the parser stops — 6 min</li>
    <li>Hydration, and what it costs — 9 min</li>
  </ul>
</div>
<script>window.__DATA__ = [{"title":"Why the parser stops","minutes":6},{"title":"Hydration, and what it costs","minutes":9}]<\/script>
<script src="/app.js" defer><\/script>
(336 bytes of HTML)
\`\`\`

A real framework does the same job with components instead of template strings,
and adds the part that matters: the client bundle runs *the same components*
against \`window.__DATA__\` and adopts the markup rather than rebuilding it.
Everything below is a variation on those forty lines.

## Streaming, and why TTFB stops mattering

![A streamed response arriving in three pieces](diagrams/rendering-advanced-streaming.jpg)

Plain server rendering has a flaw the notes glossed over: nothing is sent until
everything is ready. One slow database query and the reader stares at a white
screen for its entire duration — worse than client rendering, which at least
showed a shell.

Streaming fixes it by sending the response in pieces as they are produced:

\`\`\`text
  0ms    <head>, the shell, the header        -> browser starts painting
         (the slow query is still running)
 40ms    the navigation and the article       -> more paint
900ms    the recommendations panel            -> arrives last
\`\`\`

The mechanism is chunked transfer encoding from the
[HTTP page](#/advanced/deep-http) — the server simply does not know the length
in advance. What frameworks add is a way to say "this part may be late": React's
\`Suspense\` boundaries, and the equivalents elsewhere. The slow part is sent as a
placeholder, then the real markup follows later in the stream with a small
inline script that swaps it in.

Two things fall out of it. The head and the critical CSS reach the browser
almost immediately, so the [preload scanner](#/advanced/deep-browser/advanced)
starts fetching everything while your database is still thinking. And a slow
component now degrades one region of the page instead of the whole response.

## Hydration is the bill, and there are four ways to reduce it

![Four ways of reducing hydration cost](diagrams/rendering-advanced-less-javascript.jpg)

Server rendering moved the *first paint* earlier. It did nothing about
interactivity, because the same bundle still has to download and execute. The
strategies below are all attempts at that second problem.

**Progressive hydration.** Hydrate the page in pieces, in priority order, rather
than in one long task. The tree is the same; the scheduling is not.

**Partial hydration and islands.** Most of a page is not interactive. An article
is text; the search box, the menu and the comment form are not. So ship
JavaScript for those three and none for the rest:

\`\`\`html
<article>… all static, no JavaScript at all …</article>

<!-- one island, hydrated on its own -->
<search-box data-hydrate="idle"></search-box>
\`\`\`

Astro's islands and similar approaches make this the default rather than an
optimisation. The result is pages that ship a few kilobytes of script instead of
a few hundred, and the cost is that interactive parts cannot share state with
each other for free.

**Server components.** The framework splits components into those that run only
on the server — they may query a database, and their code is *never* sent — and
those that run in both. The output is not HTML but a serialised description of
the tree, so a navigation can update part of a page without a full reload and
without shipping the components that produced it. React Server Components are
the well-known implementation; the important idea is that some code has no
client half at all.

**Resumability.** The most radical: instead of re-running components to rebuild
state, serialise the state into the HTML so the client can pick up exactly where
the server stopped, attaching handlers lazily on first interaction. Qwik is the
example. Hydration cost approaches zero; the complexity moves into
serialisation.

## Static, incremental, and on-demand

![When the page is built, against how fresh it is](diagrams/rendering-advanced-freshness.jpg)

Once you accept that the answer to "when" can be *earlier than the request*,
there is a spectrum:

| Built | Freshness | Cost per request |
|---|---|---|
| At deploy | Whatever it was at deploy | A file from a CDN |
| At deploy, revalidated after N seconds | N seconds stale at worst | A file, plus a background rebuild |
| On first request, then cached | Stale until purged | One render per cache miss |
| Every request | Always current | One render, always |

The middle two are where most real sites live. The mechanics are the same
\`stale-while-revalidate\` idea from HTTP caching: serve the stored copy
immediately, refresh behind the reader's back, and let the *next* visitor have
the new one.

The operational question is invalidation. Time-based expiry is simple and always
either too slow or too eager; **tag-based purging** — the CMS tells the cache
"anything tagged \`post-91\` is now wrong" — is what you want the moment editors
are involved, and it is worth choosing a platform that supports it.

## Caching something personalised

![Four ways to cache a page that is different for each reader](diagrams/rendering-advanced-personal-cache.jpg)

The apparent contradiction: a page that says "Hello, Ada" cannot be shared, and
an uncacheable page is a render per visitor.

Four ways out, in increasing order of effort:

1. **Split the page.** Cache the shell; fetch the personal fragment separately
   after paint. The header flickers in a moment later, which is usually
   acceptable and occasionally not.
2. **Vary on a small key.** Cache per *segment* rather than per person —
   logged-in versus not, country, currency. Two hundred copies is fine; two
   million is not. This is \`Vary\`, and the rules are on the
   [HTTP page](#/advanced/deep-http/advanced).
3. **Edge-side personalisation.** The CDN holds the shared page and injects the
   personal bits at the edge, from a cookie. Fast, and a second system to debug.
4. **Do not cache that route.** Checkout is not a caching problem.

The mistake to avoid is caching a personalised page by accident — a shared cache
that keeps one user's page and serves it to another. That is not a performance
bug, it is a data breach, and it is why \`Cache-Control: private\` exists.

## The waterfall that undoes everything

![Serial data fetching against fetching everything at once](diagrams/rendering-advanced-waterfall.jpg)

Server rendering removes the client's data round trip. It is easy to reintroduce
it, and worse.

\`\`\`text
  bad:   render page -> discover it needs the user -> fetch user
                     -> discover it needs orders   -> fetch orders
                     -> render again                            (three trips, serial)

  good:  know what the route needs -> fetch user and orders together -> render
\`\`\`

Nested components each fetching their own data produce exactly the bad case,
because a child cannot start until its parent has rendered. The fixes have
names in every framework — route loaders, \`Promise.all\` at the top, deferred
data with a placeholder — and they are all the same idea: **hoist the fetching
to where the whole set is known.**

The other duplicate is fetching on the server and then again on the client,
because the client had no way to know it was already done. Serialising the
result into the HTML — the \`__DATA__\` above, or a framework's cache — is what
prevents it, and it is a per-request cost worth measuring: a large payload
embedded in the page can undo the paint you gained.

## What server rendering costs you

Honest accounting, because most write-ups on this subject only list the wins:

- **CPU per request.** Rendering is string building and component execution, and
  it is now yours, per visitor, at peak. A static page costs a file read.
- **A server that must exist.** Static sites survive their host; a rendered site
  has an origin to keep running, scale and patch.
- **Two environments for one codebase.** No \`window\`, no \`localStorage\`, a
  different fetch, different environment variables — and a whole class of bug
  that only appears in one of them.
- **Memory and leaks.** A module-level cache in a component file is per-process
  on a server and per-user in a browser. Request-scoped state stored globally is
  the classic way to serve one user's data to another.
- **Slower deploys and cold starts**, particularly on serverless platforms,
  where a cold start lands on a real reader.

None of these argue against it. They argue for doing it where it pays — the
routes strangers land on — and not on the admin panel.

## Measuring the right thing

The metrics that answer "did this help":

- **TTFB** gets *worse* with server rendering and better with streaming. On its
  own it is not a goal.
- **LCP** is what server rendering is for. If it did not move, the LCP element
  is probably waiting on something else — a font, a script, a lazily discovered
  image.
- **INP** is what hydration ruins. This is the number islands and server
  components are trying to fix.
- **Total blocking time** in the lab tracks the same problem before you have
  field data.

And the one that is not a metric: **bytes of JavaScript**. Every strategy above
is ultimately a way of shipping less of it, and it is the number to watch in
review, because it only ever goes up on its own.

## A decision, in five questions

1. Does a stranger arrive here from a link? → the HTML needs content.
2. Is it the same for everybody? → build it in advance.
3. Is it the same for a *group*? → cache per segment.
4. Is the interactive part small? → islands, not full hydration.
5. Is it behind a login and nobody indexes it? → client rendering is fine, and
   simplest.

Route by route. A site whose every page uses the same answer has almost
certainly got some of them wrong.

## Further reading

- [Rendering on the web](https://web.dev/articles/rendering-on-the-web)
- [React: Server Components](https://react.dev/reference/rsc/server-components)
- [Astro: islands architecture](https://docs.astro.build/en/concepts/islands/)
- [The cost of JavaScript](https://web.dev/articles/bootup-time)
`,ga=`Twelve questions on where a page should be rendered. This subject is asked about
badly — a lot of interviewers want the word "SEO" and stop — so the answers below
are written to survive the follow-up, which is nearly always "and what does that
cost?"

The **Notes** cover the two shapes and hydration. The **Advanced** page covers
streaming, islands, server components and caching.

## Easy

### 1. What is the difference between client-side and server-side rendering?

In client rendering the server sends a nearly empty document and a JavaScript
bundle; the browser runs it, fetches the data and builds the page. In server
rendering the server fetches the data, builds the HTML and sends a finished
page, which the browser paints as it arrives — and then loads the same bundle
anyway to make it interactive. The visible difference is when something appears;
the invisible one is that the HTML contains the content in the second case and
does not in the first.

### 2. What is hydration?

The step after a server-rendered page has painted, in which the framework runs
the same components in the browser, builds its own internal tree, and attaches
event handlers to the DOM nodes the server's markup already produced instead of
recreating them. Until it finishes, the page is readable but does not respond to
clicks. It costs roughly what client rendering would have cost, because the same
component code runs either way.

### 3. Does server rendering make a page faster?

It makes the first paint earlier, which is what LCP measures and what a reader
notices first. It does not make the page interactive any sooner, because the
same JavaScript still has to download and execute, and it makes TTFB worse
because the server now does work before it can answer. So "faster" is only true
for one specific measurement — and if the page is behind a login and nobody
indexes it, that measurement may not be the one that matters.

### 4. What is static site generation, and when is it right?

Building every page to HTML at deploy time and serving files from a CDN, with no
server work per request at all. It is right whenever the page is the same for
everybody and changes when somebody edits it rather than when somebody visits
it — documentation, marketing pages, blogs. It is the fastest option available,
and the limit is personalisation and live data, which is what incremental
regeneration and split pages exist to work around.

## Medium

### 5. What causes a hydration mismatch, and why does it matter?

The markup the server produced differs from what the client's first render
produces — typically because the code used \`Date.now()\`, \`Math.random()\`, a
timezone, \`window.innerWidth\`, or data that changed between the two moments. It
matters because the framework cannot reconcile the difference safely, so it
discards the server's markup and re-renders from scratch: you paid for the
server render, the transfer and the second render, and the reader sees a flash.
The fix is to move anything environment-dependent into an effect that runs after
the first render.

### 6. What does streaming server rendering solve?

Plain server rendering sends nothing until everything is ready, so one slow query
means a white screen for its entire duration — worse than client rendering,
which at least showed a shell. Streaming sends the response in chunks as they are
produced: the head and shell go immediately, so the browser starts painting and
the preload scanner starts fetching assets while the database is still working,
and the slow region arrives later with a placeholder swapped out. The transport
is just chunked transfer encoding; what the framework adds is a way to mark which
parts may be late.

### 7. What are islands, and what problem do they solve?

The observation that most of a page is not interactive: an article is text, and
only the search box, the menu and a form need JavaScript. Islands architecture
ships script for those components alone and leaves the rest as static HTML, so a
page sends a few kilobytes instead of a few hundred and hydration stops being a
long task. The trade-off is that separate islands cannot share client state for
free, so a design with many interdependent interactive regions fits it badly.

### 8. How do you cache a page that says "Hello, Ada" at the top?

Do not cache that page as a unit. Either split it — cache the shared shell and
fetch the personal fragment after paint — or cache per *segment* rather than per
person, keying on something with few values such as logged-in state, country or
currency, and declaring it with \`Vary\`. Larger sites push the personalisation to
the edge, injecting the personal bits at the CDN from a cookie. And the thing to
be careful about is the opposite failure: a shared cache that stores one user's
personalised page and serves it to another, which is a data breach rather than a
performance bug, and is what \`Cache-Control: private\` prevents.

### 9. What does SEO actually require here?

Less than the folklore says and more than nothing. Google's crawler runs
JavaScript, but rendering is a second pass on a queue that can lag by days, and
most other consumers — social previews, link unfurlers, smaller engines, many AI
crawlers — do not run scripts at all. So the practical rule is that anything a
machine needs to read must be in the HTML: the title, the description, the
canonical link and the main text. A page whose \`<title>\` is set by JavaScript
has no title in a chat app's preview card, whatever Google eventually indexes.

## Hard

### 10. What is a data-fetching waterfall, and how do you remove one?

Nested components each fetching their own data, so a child's request cannot start
until its parent has rendered — three levels deep means three serial round trips
before anything is complete. Server rendering makes it worse rather than better,
because those trips now happen before the response is sent at all. The fix is to
hoist fetching to where the full set of requirements is known: a route-level
loader that starts every request together, \`Promise.all\` at the top of the tree,
or deferred data that streams in behind a placeholder. The related duplicate is
fetching on the server and again on the client, which is prevented by
serialising the result into the HTML for the client to pick up.

### 11. What are React Server Components, and how are they different from SSR?

Server-side rendering runs components on the server to produce HTML, and then
ships those same components to the browser so they can be hydrated. Server
components run only on the server and are *never* sent: their output is a
serialised description of the tree rather than HTML, so a navigation can update
part of a page without a full document request and without the code that
produced it ever reaching the client. The practical consequence is that a
component can query a database directly and cost zero client bytes; the cost is
a sharper boundary in the codebase, because anything interactive has to be
explicitly marked as running on the client.

### 12. Your team wants to server-render everything. What would you push back on?

That the choice is per route, not per site. The routes strangers arrive at from
links — landing pages, product pages, articles — need content in the HTML and an
early paint; the admin panel behind a login needs neither and is simpler and
cheaper as a client-rendered application. Rendering on the server means CPU per
visitor, an origin that must scale and stay up, two runtime environments for one
codebase, and a class of bug that appears only in one of them. The honest version
of the decision is: does a machine need to read this page, is it the same for
everybody or for a group, and is the interactive part small — and those four
answers differ from one route to the next.

## What to revise next

- **[What the browser does with the response](#/advanced/deep-browser)** — the
  pipeline every one of these strategies is rearranging, and the metrics they
  are judged on.
`,_a=`The chapter in a page. If a line here is news, the **Notes** or **Advanced**
part is where it comes from.

## The question

- Somebody has to turn data into HTML. The only question is **who** and
  **when** — your server before the response, or the browser after it.
- Client rendering: empty shell plus a bundle; the browser fetches data and
  builds the DOM. Server rendering: finished HTML, painted on arrival, then the
  same bundle loads anyway to make it interactive.
- Server rendering moves the **first paint** earlier. It does not move
  interactivity, because the same JavaScript still has to arrive and run.

## What is actually sent

- The client version sends a \`<div id="root">\` and a promise. The server version
  sends the content, plus the data again as JSON so the client need not refetch.
- Anything reading HTML rather than running it — link previews, most crawlers,
  a text browser — sees only what was in the bytes.
- Server rendering removes no JavaScript at all.

## Hydration

- The framework re-runs the components, builds its tree, and adopts the existing
  DOM nodes instead of recreating them. It costs about what client rendering
  would have cost.
- Between paint and hydration the page is readable and unresponsive. That is the
  shape, not a bug.
- Server and client markup must match. A mismatch means the work is thrown away
  and rendered again — slower than not having bothered.
- So keep \`Date.now()\`, \`Math.random()\`, \`window\` and timezone out of the first
  render.

## Build it in advance

- **Static generation** — HTML on disk, served by a CDN, no per-request work.
  The fastest thing there is.
- **Incremental regeneration** — serve the built page, rebuild in the background
  once it is older than N seconds. It is \`stale-while-revalidate\` for pages.
- Tag-based purging beats time-based expiry the moment editors are involved.

## Sending less, not sooner

- **Progressive hydration** — same tree, hydrated in priority order instead of
  one long task.
- **Islands / partial hydration** — ship script only for the interactive parts;
  the article is text.
- **Server components** — some components have no client half at all, so their
  code is never sent.
- **Resumability** — serialise state instead of re-running components; attach
  handlers on first interaction.

## Streaming

- Plain SSR sends nothing until everything is ready, so one slow query is a
  white screen.
- Streaming sends the shell first and the slow region later, using chunked
  encoding and a placeholder that is swapped in.
- It gets the head to the browser immediately, so the preload scanner starts
  work while the database is still thinking.

## Personalisation and caching

- Split the page and fetch the personal fragment after paint; or cache per
  *segment* with \`Vary\`; or personalise at the edge; or do not cache that route.
- Caching a personalised page by accident is a data breach, not a performance
  bug. That is what \`Cache-Control: private\` is for.

## The traps

- **Waterfalls** — nested components each fetching their own data serialise the
  requests. Hoist fetching to where the whole set is known.
- **Double fetch** — the server fetched it and the client fetched it again
  because nothing handed over the result.
- **Server cost** — rendering is CPU per visitor. A static page is a file read.
- **Two environments** — no \`window\` on a server, and module-level state is
  per-process there and per-user in a browser.

## Choosing

- Landed on from a link → the HTML needs content. Same for everybody → build it
  in advance. Same for a group → cache per segment. Small interactive part →
  islands. Behind a login → client rendering is fine.
- The choice is **per route**, not per site.
- Watch TTFB (worse with SSR, better with streaming), LCP (what SSR is for), INP
  (what hydration ruins) — and the kilobytes of JavaScript, which only ever grow
  on their own.
`,va=[`notes`,`advanced`,`revision`,`interview`],ya=[{slug:`deep-floating-point`,title:`Floating point, and 0.1 + 0.2`,tagline:`Where the 4 on the end comes from.`,summary:`Type 0.1 + 0.2 into jshell and you get 0.30000000000000004 — the same digits Node and Python give, because all three are the same 64 bits underneath. This is the conversion that never terminates, the three roundings that follow from it, and what to write instead when the number is money.`,subject:`Interview`,stage:`Asked constantly`,accent:`#fbbf24`,parts:{notes:da,revision:pa,interview:fa}},{slug:`deep-rendering`,title:`Server rendering and client rendering`,tagline:`Who builds the HTML, and when.`,summary:`The same page can arrive as finished HTML, as an empty div and a megabyte of JavaScript, or as something in between built at deploy time. This is what each one actually sends, what the reader sees while waiting, what hydration costs, and how streaming, islands and static generation fit between the two extremes.`,subject:`Interview`,stage:`Asked constantly`,accent:`#facc15`,parts:{notes:ma,advanced:ha,revision:_a,interview:ga}}].map(e=>{let t=va.filter(t=>e.parts[t]).map((t,n)=>({id:t,...ee[t],content:C(e.parts[t]),route:n===0?`interview/${e.slug}`:`interview/${e.slug}/${t}`}));return{...e,parts:t,route:`interview/${e.slug}`}}),ba=e=>[e.title,e.tagline,e.summary,e.subject,e.stage].concat(e.parts.map(e=>e.content)).join(` `).toLowerCase(),xa=e=>ya.find(t=>t.slug===e)??null,Sa=(e,t)=>e?.parts.find(e=>e.id===t)??e?.parts[0]??null,Ca={tracks:[`Foundations`,`Arrays and strings`,`Linear structures`,`Recursion and search`,`Trees and ranges`,`Ordering and design`,`Graphs, DP and the rest`],sheets:[{slug:`basic`,title:`Pattern printing`,tagline:`Nested loops, drawn on the screen.`,track:`Foundations`,order:1,accent:`#34d399`,note:`These are not LeetCode problems and there is nothing to link to — they are printed here as names because the sheet counts them, and because a nested loop with the right bounds is the only thing any of them is.`,summary:`Pyramids, diamonds, hourglasses, and the hollow version of each. None of them are hard. They are here because every one is the same nested loop with different bounds, and that loop is what the rest of the sheet assumes you can write without thinking about it.`,pattern:{spot:[`The output is a shape rather than data.`,`Rows and columns, drawn on the screen.`,`Every one is the same nested loop with different bounds.`],shape:`for (int row = 1; row <= n; row++) {
    for (int col = 1; col <= n; col++)
        System.out.print(cond(row, col) ? "*" : " ");
    System.out.println();
}`,cost:`O(n²) — every cell printed`,watch:[`Work out the condition for one cell before writing either loop.`,"`println` inside the inner loop prints one character per line.",`Test n = 1, an even n and an odd n. Symmetry hides bugs.`]},bands:{easy:[{key:`square-star-pattern`,name:`Square Star Pattern`,frequency:53},{key:`right-half-pyramid`,name:`Right Half Pyramid`,frequency:10},{key:`left-half-pyramid`,name:`Left Half Pyramid`,frequency:44},{key:`inverted-right-half-pyramid`,name:`Inverted Right Half Pyramid`,frequency:46},{key:`inverted-left-half-pyramid`,name:`Inverted Left Half Pyramid`,frequency:3},{key:`full-pyramid`,name:`Full Pyramid`,frequency:43},{key:`inverted-full-pyramid`,name:`Inverted Full Pyramid`,frequency:41}],medium:[{key:`hollow-square-pattern`,name:`Hollow Square Pattern`,frequency:33},{key:`hollow-pyramid-pattern`,name:`Hollow Pyramid Pattern`,frequency:57},{key:`diamond-star-pattern`,name:`Diamond Star Pattern`,frequency:45},{key:`hollow-diamond-pattern`,name:`Hollow Diamond Pattern`,frequency:10},{key:`butterfly-pattern`,name:`Butterfly Pattern`,frequency:24},{key:`hourglass-pattern`,name:`Hourglass Pattern`,frequency:52}],hard:[{key:`cross-x-star-pattern`,name:`Cross (X) Star Pattern`,frequency:9},{key:`plus-star-pattern`,name:`Plus (+) Star Pattern`,frequency:43},{key:`hollow-butterfly-pattern`,name:`Hollow Butterfly Pattern`,frequency:16},{key:`hollow-hourglass-pattern`,name:`Hollow Hourglass Pattern`,frequency:11},{key:`right-pascal-s-triangle-pattern`,name:`Right Pascal's Triangle Pattern`,frequency:8},{key:`zigzag-star-pattern`,name:`Zigzag Star Pattern`,frequency:48}]}},{slug:`coding-foundations`,title:`Coding foundations`,tagline:`Numbers, digits, and the loop around them.`,track:`Foundations`,order:2,accent:`#f97316`,summary:`Before any data structure there is the plain problem: read a number, take it apart, put it back together. Fizz Buzz, primes, powers and square roots — the questions that check you can turn a rule into a loop without reaching for anything clever.`,pattern:{spot:[`A rule stated in words, with no data structure in it.`,`Digits, divisors, powers — a number taken apart and put back.`,`The answer is arithmetic and a loop, and nothing else.`],shape:`while (n > 0) {
    int digit = n % 10;   // take the last digit
    n /= 10;              // and drop it
}`,cost:`O(log n) digits · O(√n) divisors`,watch:["Zero produces no iterations at all: `while (n > 0)` skips it.","Negatives — Java's `%` takes the sign of the left operand.",`Reversing an int need not fit in an int. Accumulate in a long.`]},bands:{easy:[{key:`add-two-integers`,name:`Add Two Integers`,frequency:33,url:`https://leetcode.com/problems/add-two-integers/`},{key:`fizz-buzz`,name:`Fizz Buzz`,frequency:4,url:`https://leetcode.com/problems/fizz-buzz/`},{key:`determine-color-of-a-chessboard-square`,name:`Determine Color of a Chessboard Square`,frequency:50,url:`https://leetcode.com/problems/determine-color-of-a-chessboard-square/`},{key:`maximum-69-number`,name:`Maximum 69 Number`,frequency:4,url:`https://leetcode.com/problems/maximum-69-number/`},{key:`fibonacci-number`,name:`Fibonacci Number`,frequency:39,url:`https://leetcode.com/problems/fibonacci-number/`},{key:`number-of-steps-to-reduce-a-number-to-zero`,name:`Number of Steps to Reduce a Number to Zero`,frequency:35,url:`https://leetcode.com/problems/number-of-steps-to-reduce-a-number-to-zero/`},{key:`power-of-two`,name:`Power of Two`,frequency:32,url:`https://leetcode.com/problems/power-of-two/`},{key:`add-digits`,name:`Add Digits`,frequency:26,url:`https://leetcode.com/problems/add-digits/`},{key:`palindrome-number`,name:`Palindrome Number`,frequency:44,url:`https://leetcode.com/problems/palindrome-number/`},{key:`happy-number`,name:`Happy Number`,frequency:33,url:`https://leetcode.com/problems/happy-number/`},{key:`power-of-three`,name:`Power of Three`,frequency:7,url:`https://leetcode.com/problems/power-of-three/`},{key:`ugly-number`,name:`Ugly Number`,frequency:16,url:`https://leetcode.com/problems/ugly-number/`},{key:`pascals-triangle`,name:`Pascal's Triangle`,frequency:30,url:`https://leetcode.com/problems/pascals-triangle/`},{key:`pascals-triangle-ii`,name:`Pascal's Triangle II`,frequency:12,url:`https://leetcode.com/problems/pascals-triangle-ii/`},{key:`sqrtx`,name:`Sqrt(x)`,frequency:46,url:`https://leetcode.com/problems/sqrtx/`}],medium:[{key:`count-primes`,name:`Count Primes`,frequency:58,url:`https://leetcode.com/problems/count-primes/`},{key:`spiral-matrix-ii`,name:`Spiral Matrix II`,frequency:45,url:`https://leetcode.com/problems/spiral-matrix-ii/`},{key:`powx-n`,name:`Pow(x, n)`,frequency:56,url:`https://leetcode.com/problems/powx-n/`},{key:`factorial-trailing-zeroes`,name:`Factorial Trailing Zeroes`,frequency:44,url:`https://leetcode.com/problems/factorial-trailing-zeroes/`},{key:`perfect-number`,name:`Perfect Number`,frequency:30,url:`https://leetcode.com/problems/perfect-number/`},{key:`self-dividing-numbers`,name:`Self Dividing Numbers`,frequency:6,url:`https://leetcode.com/problems/self-dividing-numbers/`}],hard:[{key:`divide-two-integers`,name:`Divide Two Integers`,frequency:13,url:`https://leetcode.com/problems/divide-two-integers/`},{key:`super-pow`,name:`Super Pow`,frequency:42,url:`https://leetcode.com/problems/super-pow/`},{key:`number-of-digit-one`,name:`Number of Digit One`,frequency:55,url:`https://leetcode.com/problems/number-of-digit-one/`},{key:`max-points-on-a-line`,name:`Max Points on a Line`,frequency:36,url:`https://leetcode.com/problems/max-points-on-a-line/`},{key:`integer-to-roman`,name:`Integer to Roman`,frequency:23,url:`https://leetcode.com/problems/integer-to-roman/`},{key:`valid-number`,name:`Valid Number`,frequency:33,url:`https://leetcode.com/problems/valid-number/`}]}},{slug:`dsa-foundations`,title:`DSA foundations`,tagline:`What a pattern is, before the patterns.`,track:`Foundations`,order:3,accent:`#a78bfa`,summary:`The vocabulary the rest of the sheet is written in — what a structure costs, what a pattern is, and how to tell which one a question is asking for. Short, and worth going through before the categories that assume it.`,pattern:{spot:[`Before the patterns: what each structure costs to use.`,`The limit on n, and which complexity it leaves room for.`,`A solution that is correct and was never going to finish.`],shape:`// read the limit, then pick the shape it allows
if (n <= 10)      bruteForce();      // even O(n!) fits
else if (n <= 5_000)   quadratic();  // O(n^2)
else if (n <= 200_000) sortOrHeap(); // O(n log n)
else                   onePass();    // O(n), and mind the constant`,cost:`O(n!) down to O(n) — the limit picks`,watch:[`Choosing the structure before reading the limits.`,"A library call that hides a loop — `contains`, `remove(0)`, `+` on strings.",`Counting constants instead of shape. O(2n) and O(n) are one thing.`]},bands:{easy:[{key:`palindrome-number`,name:`Palindrome Number`,frequency:20,url:`https://leetcode.com/problems/palindrome-number/`},{key:`valid-palindrome`,name:`Valid Palindrome`,frequency:29,url:`https://leetcode.com/problems/valid-palindrome/`},{key:`reverse-string`,name:`Reverse a String`,frequency:8,url:`https://leetcode.com/problems/reverse-string/`},{key:`fizz-buzz`,name:`FizzBuzz`,frequency:58,url:`https://leetcode.com/problems/fizz-buzz/`},{key:`fibonacci-number`,name:`Fibonacci Number`,frequency:10,url:`https://leetcode.com/problems/fibonacci-number/`},{key:`climbing-stairs`,name:`Climbing Stairs`,frequency:26,url:`https://leetcode.com/problems/climbing-stairs/`}],medium:[{key:`reverse-integer`,name:`Reverse Integer`,frequency:49,url:`https://leetcode.com/problems/reverse-integer/`},{key:`string-to-integer-atoi`,name:`String to Integer (atoi)`,frequency:17,url:`https://leetcode.com/problems/string-to-integer-atoi/`},{key:`excel-sheet-column-number`,name:`Excel Sheet Column Number`,frequency:12,url:`https://leetcode.com/problems/excel-sheet-column-number/`},{key:`happy-number`,name:`Happy Number`,frequency:8,url:`https://leetcode.com/problems/happy-number/`},{key:`power-of-three`,name:`Power of Three`,frequency:55,url:`https://leetcode.com/problems/power-of-three/`},{key:`zigzag-conversion`,name:`Zigzag Conversion`,frequency:32,url:`https://leetcode.com/problems/zigzag-conversion/`}],hard:[{key:`add-binary`,name:`Add Binary`,frequency:9,url:`https://leetcode.com/problems/add-binary/`},{key:`multiply-strings`,name:`Multiply Strings`,frequency:10,url:`https://leetcode.com/problems/multiply-strings/`},{key:`basic-calculator`,name:`Basic Calculator`,frequency:5,url:`https://leetcode.com/problems/basic-calculator/`},{key:`fraction-to-recurring-decimal`,name:`Fraction to Recurring Decimal`,frequency:14,url:`https://leetcode.com/problems/fraction-to-recurring-decimal/`},{key:`integer-to-english-words`,name:`Integer to English Words`,frequency:11,url:`https://leetcode.com/problems/integer-to-english-words/`},{key:`count-and-say`,name:`Count and Say`,frequency:47,url:`https://leetcode.com/problems/count-and-say/`}]}},{slug:`arrays`,title:`Arrays`,tagline:`The index, and what one pass can do.`,track:`Arrays and strings`,order:4,accent:`#f472b6`,summary:`The structure everything else is built on top of. These problems are about doing the work in a single pass instead of two nested loops — rotating in place, merging sorted halves, and finding the one element that does not belong.`,pattern:{spot:[`A sequence, and a nested loop that feels wasteful.`,`One pass and one carried value would answer it.`,`In place, with no second array allowed.`],shape:`int write = 0;
for (int read = 0; read < a.length; read++)
    if (keep(a[read])) a[write++] = a[read];
// a[0..write) is the answer, done in place`,cost:`O(n) time · O(1) space`,watch:[`Two indices moving at different rates. Name them for what they do.`,`Compare against the last one kept, not the last one seen.`,"`i <= a.length` reads one past the end."]},bands:{easy:[{key:`two-sum`,name:`Two Sum`,frequency:5,url:`https://leetcode.com/problems/two-sum/`},{key:`contains-duplicate`,name:`Contains Duplicate`,frequency:49,url:`https://leetcode.com/problems/contains-duplicate/`},{key:`move-zeroes`,name:`Move Zeroes`,frequency:57,url:`https://leetcode.com/problems/move-zeroes/`},{key:`missing-number`,name:`Missing Number`,frequency:55,url:`https://leetcode.com/problems/missing-number/`},{key:`best-time-to-buy-and-sell-stock`,name:`Best Time to Buy and Sell Stock`,frequency:12,url:`https://leetcode.com/problems/best-time-to-buy-and-sell-stock/`},{key:`majority-element`,name:`Majority Element`,frequency:23,url:`https://leetcode.com/problems/majority-element/`},{key:`pascals-triangle`,name:`Pascal's Triangle`,frequency:21,url:`https://leetcode.com/problems/pascals-triangle/`}],medium:[{key:`product-of-array-except-self`,name:`Product of Array Except Self`,frequency:9,url:`https://leetcode.com/problems/product-of-array-except-self/`},{key:`rotate-array`,name:`Rotate Array`,frequency:48,url:`https://leetcode.com/problems/rotate-array/`},{key:`3sum`,name:`3Sum`,frequency:24,url:`https://leetcode.com/problems/3sum/`},{key:`find-minimum-in-rotated-sorted-array`,name:`Find Minimum in Rotated Sorted Array`,frequency:34,url:`https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/`},{key:`maximum-product-subarray`,name:`Maximum Product Subarray`,frequency:53,url:`https://leetcode.com/problems/maximum-product-subarray/`},{key:`subarray-sum-equals-k`,name:`Subarray Sum Equals K`,frequency:34,url:`https://leetcode.com/problems/subarray-sum-equals-k/`},{key:`spiral-matrix`,name:`Spiral Matrix`,frequency:30,url:`https://leetcode.com/problems/spiral-matrix/`}],hard:[{key:`first-missing-positive`,name:`First Missing Positive`,frequency:55,url:`https://leetcode.com/problems/first-missing-positive/`},{key:`trapping-rain-water`,name:`Trapping Rain Water`,frequency:11,url:`https://leetcode.com/problems/trapping-rain-water/`},{key:`jump-game-ii`,name:`Jump Game II`,frequency:3,url:`https://leetcode.com/problems/jump-game-ii/`},{key:`sliding-window-maximum`,name:`Sliding Window Maximum`,frequency:3,url:`https://leetcode.com/problems/sliding-window-maximum/`},{key:`max-chunks-to-make-sorted-ii`,name:`Max Chunks To Make Sorted II`,frequency:17,url:`https://leetcode.com/problems/max-chunks-to-make-sorted-ii/`},{key:`next-permutation`,name:`Next Permutation`,frequency:12,url:`https://leetcode.com/problems/next-permutation/`}]}},{slug:`strings`,title:`Strings`,tagline:`Characters, with the awkward parts left in.`,track:`Arrays and strings`,order:5,accent:`#facc15`,summary:`A string is an array you are not allowed to modify. Palindromes, anagrams, parsing and comparison — the category where the solution is usually short and the edge case you did not consider is what fails it.`,pattern:{spot:[`Characters, and a question about order, counts or a substring.`,`Anagrams, palindromes, or a fixed alphabet.`,`A 26-slot count table turns out to be the whole answer.`],shape:`int[] count = new int[26];
for (char c : s.toCharArray()) count[c - 'a']++;`,cost:`O(n) with a 26-slot count table`,watch:["Building with `+` in a loop is quadratic. Use `StringBuilder`.","`==` compares references. Use `.equals`, every time.","`c - 'a'` on an uppercase letter is a negative index."]},bands:{easy:[{key:`valid-anagram`,name:`Valid Anagram`,frequency:19,url:`https://leetcode.com/problems/valid-anagram/`},{key:`is-subsequence`,name:`Is Subsequence`,frequency:25,url:`https://leetcode.com/problems/is-subsequence/`},{key:`longest-common-prefix`,name:`Longest Common Prefix`,frequency:13,url:`https://leetcode.com/problems/longest-common-prefix/`},{key:`roman-to-integer`,name:`Roman to Integer`,frequency:4,url:`https://leetcode.com/problems/roman-to-integer/`},{key:`first-unique-character-in-a-string`,name:`First Unique Character in a String`,frequency:51,url:`https://leetcode.com/problems/first-unique-character-in-a-string/`},{key:`reverse-vowels-of-a-string`,name:`Reverse Vowels of a String`,frequency:35,url:`https://leetcode.com/problems/reverse-vowels-of-a-string/`},{key:`find-the-index-of-the-first-occurrence-in-a-string`,name:`Find the Index of the First Occurrence in a String`,frequency:15,url:`https://leetcode.com/problems/find-the-index-of-the-first-occurrence-in-a-string/`},{key:`repeated-substring-pattern`,name:`Repeated Substring Pattern`,frequency:37,url:`https://leetcode.com/problems/repeated-substring-pattern/`}],medium:[{key:`longest-palindromic-substring`,name:`Longest Palindromic Substring`,frequency:58,url:`https://leetcode.com/problems/longest-palindromic-substring/`},{key:`group-anagrams`,name:`Group Anagrams`,frequency:37,url:`https://leetcode.com/problems/group-anagrams/`},{key:`find-all-anagrams-in-a-string`,name:`Find All Anagrams in a String`,frequency:9,url:`https://leetcode.com/problems/find-all-anagrams-in-a-string/`},{key:`decode-string`,name:`Decode String`,frequency:50,url:`https://leetcode.com/problems/decode-string/`},{key:`reverse-words-in-a-string`,name:`Reverse Words in a String`,frequency:24,url:`https://leetcode.com/problems/reverse-words-in-a-string/`},{key:`valid-palindrome-ii`,name:`Valid Palindrome II`,frequency:17,url:`https://leetcode.com/problems/valid-palindrome-ii/`},{key:`string-compression`,name:`String Compression`,frequency:29,url:`https://leetcode.com/problems/string-compression/`},{key:`repeated-string-match`,name:`Repeated String Match`,frequency:49,url:`https://leetcode.com/problems/repeated-string-match/`},{key:`longest-happy-prefix`,name:`Longest Happy Prefix`,frequency:33,url:`https://leetcode.com/problems/longest-happy-prefix/`},{key:`string-matching-in-an-array`,name:`String Matching in an Array`,frequency:23,url:`https://leetcode.com/problems/string-matching-in-an-array/`}],hard:[{key:`minimum-window-substring`,name:`Minimum Window Substring`,frequency:38,url:`https://leetcode.com/problems/minimum-window-substring/`},{key:`wildcard-matching`,name:`Wildcard Matching`,frequency:33,url:`https://leetcode.com/problems/wildcard-matching/`},{key:`interleaving-string`,name:`Interleaving String`,frequency:15,url:`https://leetcode.com/problems/interleaving-string/`},{key:`palindrome-partitioning`,name:`Palindrome Partitioning`,frequency:43,url:`https://leetcode.com/problems/palindrome-partitioning/`},{key:`shortest-palindrome`,name:`Shortest Palindrome`,frequency:17,url:`https://leetcode.com/problems/shortest-palindrome/`},{key:`text-justification`,name:`Text Justification`,frequency:50,url:`https://leetcode.com/problems/text-justification/`},{key:`word-break-ii`,name:`Word Break II`,frequency:33,url:`https://leetcode.com/problems/word-break-ii/`},{key:`distinct-echo-substrings`,name:`Distinct Echo Substrings`,frequency:56,url:`https://leetcode.com/problems/distinct-echo-substrings/`},{key:`find-substring-with-given-hash-value`,name:`Find Substring with Given Hash Value`,frequency:22,url:`https://leetcode.com/problems/find-substring-with-given-hash-value/`},{key:`longest-common-subpath`,name:`Longest Common Subpath`,frequency:51,url:`https://leetcode.com/problems/longest-common-subpath/`}]}},{slug:`bit-manipulation`,title:`Bit manipulation`,tagline:`XOR, masks, and the shift trick.`,track:`Arrays and strings`,order:6,accent:`#38bdf8`,summary:`Once you can see an integer as thirty-two switches, a whole class of problem stops needing extra memory. XOR to cancel the pairs, a mask to select, and the counting tricks that collapse a loop into one expression.`,pattern:{spot:[`Pairs that cancel out, or a set of flags.`,`"Without extra memory" in the problem statement.`,`A subset of at most thirty-two things.`],shape:`x & 1        // lowest bit
x >> 1       // drop it
x & (x - 1)  // clear the lowest set bit
a ^ a == 0   // XOR cancels a pair`,cost:`O(1) per op · O(32) per scan`,watch:["`>>` keeps the sign and `>>>` does not. Use `>>>` for bits.","`&`, `|` and `^` bind looser than `==`. Bracket everything.","`1 << 31` is negative. Past 30, write `1L <<`."]},bands:{easy:[{key:`single-number`,name:`Single Number`,frequency:10,url:`https://leetcode.com/problems/single-number/`},{key:`number-of-1-bits`,name:`Number of 1 Bits`,frequency:8,url:`https://leetcode.com/problems/number-of-1-bits/`},{key:`counting-bits`,name:`Counting Bits`,frequency:18,url:`https://leetcode.com/problems/counting-bits/`},{key:`reverse-bits`,name:`Reverse Bits`,frequency:44,url:`https://leetcode.com/problems/reverse-bits/`},{key:`hamming-distance`,name:`Hamming Distance`,frequency:45,url:`https://leetcode.com/problems/hamming-distance/`},{key:`power-of-two`,name:`Power of Two`,frequency:37,url:`https://leetcode.com/problems/power-of-two/`}],medium:[{key:`sum-of-two-integers`,name:`Sum of Two Integers`,frequency:43,url:`https://leetcode.com/problems/sum-of-two-integers/`},{key:`single-number-iii`,name:`Single Number III`,frequency:50,url:`https://leetcode.com/problems/single-number-iii/`},{key:`subsets`,name:`Subsets`,frequency:5,url:`https://leetcode.com/problems/subsets/`},{key:`repeated-dna-sequences`,name:`Repeated DNA Sequences`,frequency:32,url:`https://leetcode.com/problems/repeated-dna-sequences/`},{key:`total-hamming-distance`,name:`Total Hamming Distance`,frequency:55,url:`https://leetcode.com/problems/total-hamming-distance/`},{key:`single-number-ii`,name:`Single Number II`,frequency:17,url:`https://leetcode.com/problems/single-number-ii/`}],hard:[{key:`maximum-xor-of-two-numbers-in-an-array`,name:`Maximum XOR of Two Numbers in an Array`,frequency:16,url:`https://leetcode.com/problems/maximum-xor-of-two-numbers-in-an-array/`},{key:`utf-8-validation`,name:`UTF-8 Validation`,frequency:59,url:`https://leetcode.com/problems/utf-8-validation/`},{key:`maximum-product-of-word-lengths`,name:`Maximum Product of Word Lengths`,frequency:34,url:`https://leetcode.com/problems/maximum-product-of-word-lengths/`},{key:`find-longest-awesome-substring`,name:`Find Longest Awesome Substring`,frequency:15,url:`https://leetcode.com/problems/find-longest-awesome-substring/`},{key:`minimum-flips-to-make-a-or-b-equal-to-c`,name:`Minimum Flips to Make a OR b Equal to c`,frequency:57,url:`https://leetcode.com/problems/minimum-flips-to-make-a-or-b-equal-to-c/`},{key:`maximum-xor-with-an-element-from-array`,name:`Maximum XOR With an Element From Array`,frequency:11,url:`https://leetcode.com/problems/maximum-xor-with-an-element-from-array/`}]}},{slug:`hash-tables`,title:`Hash tables`,tagline:`Memory spent to buy back a second pass.`,track:`Arrays and strings`,order:7,accent:`#60a5fa`,summary:`The answer to almost every "have I seen this before" question, and the reason the nested loop is rarely the intended solution. The problems are easy to spot and the skill is choosing what to key on.`,pattern:{spot:[`"Have I seen this before", or a count per key.`,`A nested loop that is really searching for a complement.`,`Grouping things that share some computed property.`],shape:`Map<Integer, Integer> seen = new HashMap<>();
for (int i = 0; i < a.length; i++) {
    if (seen.containsKey(target - a[i])) return new int[]{seen.get(target - a[i]), i};
    seen.put(a[i], i);
}`,cost:`O(n) time · O(n) space`,watch:[`Check before you insert, or an element pairs with itself.`,`Choosing what to key on. The key is the design.`,"`int[]` as a key hashes by identity — nothing is ever found."]},bands:{easy:[{key:`two-sum`,name:`Two Sum`,frequency:56,url:`https://leetcode.com/problems/two-sum/`},{key:`valid-anagram`,name:`Valid Anagram`,frequency:47,url:`https://leetcode.com/problems/valid-anagram/`},{key:`ransom-note`,name:`Ransom Note`,frequency:43,url:`https://leetcode.com/problems/ransom-note/`},{key:`word-pattern`,name:`Word Pattern`,frequency:10,url:`https://leetcode.com/problems/word-pattern/`},{key:`design-hashmap`,name:`Design HashMap`,frequency:16,url:`https://leetcode.com/problems/design-hashmap/`},{key:`contains-duplicate-ii`,name:`Contains Duplicate II`,frequency:36,url:`https://leetcode.com/problems/contains-duplicate-ii/`}],medium:[{key:`group-anagrams`,name:`Group Anagrams`,frequency:16,url:`https://leetcode.com/problems/group-anagrams/`},{key:`longest-consecutive-sequence`,name:`Longest Consecutive Sequence`,frequency:27,url:`https://leetcode.com/problems/longest-consecutive-sequence/`},{key:`top-k-frequent-elements`,name:`Top K Frequent Elements`,frequency:19,url:`https://leetcode.com/problems/top-k-frequent-elements/`},{key:`subarray-sum-equals-k`,name:`Subarray Sum Equals K`,frequency:35,url:`https://leetcode.com/problems/subarray-sum-equals-k/`},{key:`4sum-ii`,name:`4Sum II`,frequency:43,url:`https://leetcode.com/problems/4sum-ii/`},{key:`sort-characters-by-frequency`,name:`Sort Characters By Frequency`,frequency:16,url:`https://leetcode.com/problems/sort-characters-by-frequency/`}],hard:[{key:`minimum-window-substring`,name:`Minimum Window Substring`,frequency:51,url:`https://leetcode.com/problems/minimum-window-substring/`},{key:`maximum-frequency-stack`,name:`Maximum Frequency Stack`,frequency:60,url:`https://leetcode.com/problems/maximum-frequency-stack/`},{key:`split-array-into-consecutive-subsequences`,name:`Split Array into Consecutive Subsequences`,frequency:44,url:`https://leetcode.com/problems/split-array-into-consecutive-subsequences/`},{key:`all-oone-data-structure`,name:"All O`one Data Structure",frequency:15,url:`https://leetcode.com/problems/all-oone-data-structure/`},{key:`lfu-cache`,name:`LFU Cache`,frequency:21,url:`https://leetcode.com/problems/lfu-cache/`},{key:`number-of-matching-subsequences`,name:`Number of Matching Subsequences`,frequency:44,url:`https://leetcode.com/problems/number-of-matching-subsequences/`}]}},{slug:`two-pointers`,title:`Two pointers`,tagline:`Two indices, one pass, no extra space.`,track:`Arrays and strings`,order:8,accent:`#8b6cff`,summary:`When the input is sorted, or can be, a pair of indices walking towards each other replaces the nested loop. Knowing which of the two to move, and being able to say why, is the whole of it.`,pattern:{spot:[`Sorted input, and a pair or triple that has to satisfy something.`,`Two ends walking towards each other.`,`A fast index reading and a slow one writing.`],shape:`int lo = 0, hi = a.length - 1;
while (lo < hi) {
    int sum = a[lo] + a[hi];
    if (sum == target) return true;
    if (sum < target) lo++; else hi--;
}`,cost:`O(n), after an O(n log n) sort`,watch:[`Be able to say why moving that pointer cannot skip the answer.`,`Skip duplicates in all three places, not only the first.`,"`lo < hi`, never `lo != hi`, or an odd length crosses over."]},bands:{easy:[{key:`merge-sorted-array`,name:`Merge Sorted Array`,frequency:36,url:`https://leetcode.com/problems/merge-sorted-array/`},{key:`valid-palindrome`,name:`Valid Palindrome`,frequency:17,url:`https://leetcode.com/problems/valid-palindrome/`},{key:`move-zeroes`,name:`Move Zeroes`,frequency:4,url:`https://leetcode.com/problems/move-zeroes/`},{key:`squares-of-a-sorted-array`,name:`Squares of a Sorted Array`,frequency:33,url:`https://leetcode.com/problems/squares-of-a-sorted-array/`},{key:`remove-element`,name:`Remove Element`,frequency:8,url:`https://leetcode.com/problems/remove-element/`},{key:`reverse-string`,name:`Reverse String`,frequency:52,url:`https://leetcode.com/problems/reverse-string/`}],medium:[{key:`two-sum-ii-input-array-is-sorted`,name:`Two Sum II - Input Array Is Sorted`,frequency:39,url:`https://leetcode.com/problems/two-sum-ii-input-array-is-sorted/`},{key:`container-with-most-water`,name:`Container With Most Water`,frequency:23,url:`https://leetcode.com/problems/container-with-most-water/`},{key:`3sum`,name:`3Sum`,frequency:5,url:`https://leetcode.com/problems/3sum/`},{key:`boats-to-save-people`,name:`Boats to Save People`,frequency:10,url:`https://leetcode.com/problems/boats-to-save-people/`},{key:`4sum`,name:`4Sum`,frequency:35,url:`https://leetcode.com/problems/4sum/`},{key:`3sum-closest`,name:`3Sum Closest`,frequency:56,url:`https://leetcode.com/problems/3sum-closest/`}],hard:[{key:`trapping-rain-water`,name:`Trapping Rain Water`,frequency:18,url:`https://leetcode.com/problems/trapping-rain-water/`},{key:`minimum-window-substring`,name:`Minimum Window Substring`,frequency:49,url:`https://leetcode.com/problems/minimum-window-substring/`},{key:`subarrays-with-k-different-integers`,name:`Subarrays with K Different Integers`,frequency:13,url:`https://leetcode.com/problems/subarrays-with-k-different-integers/`},{key:`minimum-operations-to-reduce-x-to-zero`,name:`Minimum Operations to Reduce X to Zero`,frequency:16,url:`https://leetcode.com/problems/minimum-operations-to-reduce-x-to-zero/`},{key:`number-of-subsequences-that-satisfy-the-given-sum-condition`,name:`Number of Subsequences That Satisfy Given Sum Condition`,frequency:15,url:`https://leetcode.com/problems/number-of-subsequences-that-satisfy-the-given-sum-condition/`},{key:`3sum-with-multiplicity`,name:`3Sum With Multiplicity`,frequency:19,url:`https://leetcode.com/problems/3sum-with-multiplicity/`}]}},{slug:`prefix-sum`,title:`Prefix sum`,tagline:`Any range, answered by one subtraction.`,track:`Arrays and strings`,order:9,accent:`#6baf8d`,summary:`Build the running totals once and every range query afterwards is a subtraction. It generalises to counts, to two dimensions, and to the difference array for when the updates arrive as ranges as well.`,pattern:{spot:[`Many range queries over data that does not change.`,"A sum, a count or an XOR over `a[l..r]`.",`Negative numbers, so a sliding window is not available.`],shape:`int[] pre = new int[a.length + 1];
for (int i = 0; i < a.length; i++) pre[i + 1] = pre[i] + a[i];
// sum of a[l..r] == pre[r + 1] - pre[l]`,cost:`O(n) build · O(1) per query`,watch:["Size it `n + 1`. The leading zero removes every case at l = 0.","Seed the map with `put(0, 1)` before the loop starts.",`Sums over a long array overflow an int.`]},bands:{easy:[{key:`range-sum-query-immutable`,name:`Range Sum Query - Immutable`,frequency:50,url:`https://leetcode.com/problems/range-sum-query-immutable/`},{key:`running-sum-of-1d-array`,name:`Running Sum of 1d Array`,frequency:48,url:`https://leetcode.com/problems/running-sum-of-1d-array/`},{key:`find-pivot-index`,name:`Find Pivot Index`,frequency:11,url:`https://leetcode.com/problems/find-pivot-index/`},{key:`find-the-highest-altitude`,name:`Find the Highest Altitude`,frequency:37,url:`https://leetcode.com/problems/find-the-highest-altitude/`},{key:`number-of-ways-to-split-array`,name:`Number of Ways to Split Array`,frequency:23,url:`https://leetcode.com/problems/number-of-ways-to-split-array/`},{key:`sum-of-all-odd-length-subarrays`,name:`Sum of All Odd Length Subarrays`,frequency:4,url:`https://leetcode.com/problems/sum-of-all-odd-length-subarrays/`}],medium:[{key:`subarray-sum-equals-k`,name:`Subarray Sum Equals K`,frequency:14,url:`https://leetcode.com/problems/subarray-sum-equals-k/`},{key:`contiguous-array`,name:`Contiguous Array`,frequency:43,url:`https://leetcode.com/problems/contiguous-array/`},{key:`product-of-array-except-self`,name:`Product of Array Except Self`,frequency:9,url:`https://leetcode.com/problems/product-of-array-except-self/`},{key:`range-sum-query-2d-immutable`,name:`Range Sum Query 2D - Immutable`,frequency:36,url:`https://leetcode.com/problems/range-sum-query-2d-immutable/`},{key:`maximum-size-subarray-sum-equals-k`,name:`Maximum Size Subarray Sum Equals k`,frequency:16,url:`https://leetcode.com/problems/maximum-size-subarray-sum-equals-k/`},{key:`minimum-value-to-get-positive-step-by-step-sum`,name:`Minimum Value to Get Positive Step by Step Sum`,frequency:6,url:`https://leetcode.com/problems/minimum-value-to-get-positive-step-by-step-sum/`}],hard:[{key:`count-number-of-nice-subarrays`,name:`Count Number of Nice Subarrays`,frequency:28,url:`https://leetcode.com/problems/count-number-of-nice-subarrays/`},{key:`number-of-submatrices-that-sum-to-target`,name:`Number of Submatrices That Sum to Target`,frequency:4,url:`https://leetcode.com/problems/number-of-submatrices-that-sum-to-target/`},{key:`count-of-range-sum`,name:`Count of Range Sum`,frequency:41,url:`https://leetcode.com/problems/count-of-range-sum/`},{key:`max-sum-of-rectangle-no-larger-than-k`,name:`Max Sum of Rectangle No Larger Than K`,frequency:48,url:`https://leetcode.com/problems/max-sum-of-rectangle-no-larger-than-k/`},{key:`maximum-sum-of-two-non-overlapping-subarrays`,name:`Maximum Sum of Two Non-Overlapping Subarrays`,frequency:59,url:`https://leetcode.com/problems/maximum-sum-of-two-non-overlapping-subarrays/`},{key:`maximum-subarray-min-product`,name:`Maximum Subarray Min-Product`,frequency:26,url:`https://leetcode.com/problems/maximum-subarray-min-product/`}]}},{slug:`sliding-window`,title:`Sliding window`,tagline:`Grow it, shrink it, never restart it.`,track:`Arrays and strings`,order:10,accent:`#34d399`,summary:`A window that only ever moves forwards, so the whole scan stays linear. Fixed size is the easy half. The half worth practising is knowing when the window is allowed to grow and when it has to give ground.`,pattern:{spot:[`"Longest", "shortest" or "count of" a contiguous run.`,`A window of fixed size k.`,`A condition that stays broken until the window shrinks.`],shape:`int lo = 0;
for (int hi = 0; hi < a.length; hi++) {
    add(a[hi]);
    while (invalid()) remove(a[lo++]);
    best = Math.max(best, hi - lo + 1);
}`,cost:`O(n) — in once, out once`,watch:["The window only moves forwards. Resetting `lo` makes it quadratic.",`It assumes the sum grows as the window widens. Negatives break that.`,'"Exactly k" is `atMost(k) - atMost(k - 1)`.']},bands:{easy:[{key:`best-time-to-buy-and-sell-stock`,name:`Best Time to Buy and Sell Stock`,frequency:55,url:`https://leetcode.com/problems/best-time-to-buy-and-sell-stock/`},{key:`maximum-average-subarray-i`,name:`Maximum Average Subarray I`,frequency:35,url:`https://leetcode.com/problems/maximum-average-subarray-i/`},{key:`minimum-size-subarray-sum`,name:`Minimum Size Subarray Sum`,frequency:40,url:`https://leetcode.com/problems/minimum-size-subarray-sum/`},{key:`maximum-number-of-vowels-in-a-substring-of-given-length`,name:`Maximum Number of Vowels in a Substring`,frequency:14,url:`https://leetcode.com/problems/maximum-number-of-vowels-in-a-substring-of-given-length/`},{key:`defuse-the-bomb`,name:`Defuse the Bomb`,frequency:15,url:`https://leetcode.com/problems/defuse-the-bomb/`},{key:`maximum-average-subarray-i`,name:`Find Max Average Subarray I`,frequency:35,url:`https://leetcode.com/problems/maximum-average-subarray-i/`},{key:`maximum-sum-of-distinct-subarrays-with-length-k`,name:`Maximum Sum of Distinct Subarrays With Length K`,frequency:19,url:`https://leetcode.com/problems/maximum-sum-of-distinct-subarrays-with-length-k/`},{key:`substrings-of-size-three-with-distinct-characters`,name:`Substrings of Size Three with Distinct Characters`,frequency:22,url:`https://leetcode.com/problems/substrings-of-size-three-with-distinct-characters/`},{key:`diet-plan-performance`,name:`Diet Plan Performance`,frequency:10,url:`https://leetcode.com/problems/diet-plan-performance/`},{key:`number-of-sub-arrays-of-size-k-and-average-greater-than-or-equal-to-threshold`,name:`Number of Sub-arrays of Size K and Average Greater than or Equal to Threshold`,frequency:40,url:`https://leetcode.com/problems/number-of-sub-arrays-of-size-k-and-average-greater-than-or-equal-to-threshold/`}],medium:[{key:`longest-repeating-character-replacement`,name:`Longest Repeating Character Replacement`,frequency:49,url:`https://leetcode.com/problems/longest-repeating-character-replacement/`},{key:`permutation-in-string`,name:`Permutation in String`,frequency:53,url:`https://leetcode.com/problems/permutation-in-string/`},{key:`fruit-into-baskets`,name:`Fruit Into Baskets`,frequency:35,url:`https://leetcode.com/problems/fruit-into-baskets/`},{key:`max-consecutive-ones-iii`,name:`Max Consecutive Ones III`,frequency:8,url:`https://leetcode.com/problems/max-consecutive-ones-iii/`},{key:`frequency-of-the-most-frequent-element`,name:`Frequency of the Most Frequent Element`,frequency:19,url:`https://leetcode.com/problems/frequency-of-the-most-frequent-element/`},{key:`grumpy-bookstore-owner`,name:`Grumpy Bookstore Owner`,frequency:11,url:`https://leetcode.com/problems/grumpy-bookstore-owner/`},{key:`maximum-points-you-can-obtain-from-cards`,name:`Maximum Points You Can Obtain from Cards`,frequency:41,url:`https://leetcode.com/problems/maximum-points-you-can-obtain-from-cards/`},{key:`minimum-swaps-to-group-all-1s-together`,name:`Minimum Swaps to Group All 1's Together`,frequency:26,url:`https://leetcode.com/problems/minimum-swaps-to-group-all-1s-together/`},{key:`check-if-a-string-contains-all-binary-codes-of-size-k`,name:`Check If a String Contains All Binary Codes of Size K`,frequency:30,url:`https://leetcode.com/problems/check-if-a-string-contains-all-binary-codes-of-size-k/`},{key:`longest-subarray-of-1s-after-deleting-one-element`,name:`Longest Subarray of 1's After Deleting One Element`,frequency:55,url:`https://leetcode.com/problems/longest-subarray-of-1s-after-deleting-one-element/`}],hard:[{key:`longest-substring-without-repeating-characters`,name:`Longest Substring Without Repeating Characters`,frequency:19,url:`https://leetcode.com/problems/longest-substring-without-repeating-characters/`},{key:`minimum-window-substring`,name:`Minimum Window Substring`,frequency:41,url:`https://leetcode.com/problems/minimum-window-substring/`},{key:`sliding-window-maximum`,name:`Sliding Window Maximum`,frequency:33,url:`https://leetcode.com/problems/sliding-window-maximum/`},{key:`subarrays-with-k-different-integers`,name:`Subarrays with K Different Integers`,frequency:17,url:`https://leetcode.com/problems/subarrays-with-k-different-integers/`},{key:`substring-with-concatenation-of-all-words`,name:`Substring with Concatenation of All Words`,frequency:41,url:`https://leetcode.com/problems/substring-with-concatenation-of-all-words/`},{key:`binary-subarrays-with-sum`,name:`Binary Subarrays with Sum`,frequency:47,url:`https://leetcode.com/problems/binary-subarrays-with-sum/`},{key:`minimum-number-of-flips-to-make-the-binary-string-alternating`,name:`Minimum Number of Flips to Make the Binary String Alternating`,frequency:41,url:`https://leetcode.com/problems/minimum-number-of-flips-to-make-the-binary-string-alternating/`},{key:`minimum-swaps-to-group-all-1s-together-ii`,name:`Minimum Swaps to Group All 1's Together II`,frequency:22,url:`https://leetcode.com/problems/minimum-swaps-to-group-all-1s-together-ii/`},{key:`maximum-number-of-occurrences-of-a-substring`,name:`Maximum Number of Occurrences of a Substring`,frequency:31,url:`https://leetcode.com/problems/maximum-number-of-occurrences-of-a-substring/`},{key:`minimum-window-subsequence`,name:`Minimum Window Subsequence`,frequency:58,url:`https://leetcode.com/problems/minimum-window-subsequence/`}]}},{slug:`kadanes-algorithm`,title:`Kadane's algorithm`,tagline:`The best subarray, in one pass.`,track:`Arrays and strings`,order:11,accent:`#f97316`,summary:`One decision, taken at every element: extend the run, or start a new one. A small algorithm with a lot of variants — circular arrays, products in place of sums, and the ones that want to know where the subarray was rather than what it came to.`,pattern:{spot:[`Best contiguous subarray, by sum or by product.`,`One decision per index: extend the run, or start again here.`,`A running answer carried forward instead of recomputed.`],shape:`int best = a[0], here = a[0];
for (int i = 1; i < a.length; i++) {
    here = Math.max(a[i], here + a[i]);   // extend, or start again
    best = Math.max(best, here);
}`,cost:`O(n) time · O(1) space`,watch:["All-negative input. Starting `best` at 0 returns a wrong 0.",`Products need the running minimum too — a negative flips them.`,`Recovering the indices means recording the start as it moves.`]},bands:{easy:[{key:`maximum-subarray`,name:`Maximum Subarray`,frequency:4,url:`https://leetcode.com/problems/maximum-subarray/`},{key:`maximum-average-subarray-i`,name:`Maximum Average Subarray I`,frequency:56,url:`https://leetcode.com/problems/maximum-average-subarray-i/`},{key:`best-time-to-buy-and-sell-stock`,name:`Best Time to Buy and Sell Stock`,frequency:59,url:`https://leetcode.com/problems/best-time-to-buy-and-sell-stock/`},{key:`maximum-absolute-sum-of-any-subarray`,name:`Maximum Absolute Sum of Any Subarray`,frequency:25,url:`https://leetcode.com/problems/maximum-absolute-sum-of-any-subarray/`},{key:`longest-continuous-increasing-subsequence`,name:`Longest Continuous Increasing Subsequence`,frequency:42,url:`https://leetcode.com/problems/longest-continuous-increasing-subsequence/`},{key:`degree-of-an-array`,name:`Degree of an Array`,frequency:6,url:`https://leetcode.com/problems/degree-of-an-array/`}],medium:[{key:`maximum-product-subarray`,name:`Maximum Product Subarray`,frequency:52,url:`https://leetcode.com/problems/maximum-product-subarray/`},{key:`maximum-length-of-repeated-subarray`,name:`Maximum Length of Repeated Subarray`,frequency:10,url:`https://leetcode.com/problems/maximum-length-of-repeated-subarray/`},{key:`bitwise-ors-of-subarrays`,name:`Bitwise ORs of Subarrays`,frequency:39,url:`https://leetcode.com/problems/bitwise-ors-of-subarrays/`},{key:`maximum-sum-of-two-non-overlapping-subarrays`,name:`Maximum Sum of Two Non-Overlapping Subarrays`,frequency:14,url:`https://leetcode.com/problems/maximum-sum-of-two-non-overlapping-subarrays/`},{key:`longest-turbulent-subarray`,name:`Longest Turbulent Subarray`,frequency:34,url:`https://leetcode.com/problems/longest-turbulent-subarray/`},{key:`find-two-non-overlapping-sub-arrays-each-with-target-sum`,name:`Find Two Non-overlapping Sub-arrays Each With Target Sum`,frequency:19,url:`https://leetcode.com/problems/find-two-non-overlapping-sub-arrays-each-with-target-sum/`}],hard:[{key:`maximum-sum-circular-subarray`,name:`Maximum Sum Circular Subarray`,frequency:44,url:`https://leetcode.com/problems/maximum-sum-circular-subarray/`},{key:`maximum-sum-of-3-non-overlapping-subarrays`,name:`Maximum Sum of 3 Non-Overlapping Subarrays`,frequency:49,url:`https://leetcode.com/problems/maximum-sum-of-3-non-overlapping-subarrays/`},{key:`k-concatenation-maximum-sum`,name:`K-Concatenation Maximum Sum`,frequency:50,url:`https://leetcode.com/problems/k-concatenation-maximum-sum/`},{key:`max-dot-product-of-two-subsequences`,name:`Max Dot Product of Two Subsequences`,frequency:60,url:`https://leetcode.com/problems/max-dot-product-of-two-subsequences/`},{key:`number-of-subarrays-with-bounded-maximum`,name:`Number of Subarrays with Bounded Maximum`,frequency:40,url:`https://leetcode.com/problems/number-of-subarrays-with-bounded-maximum/`},{key:`maximum-number-of-non-overlapping-subarrays-with-sum-equals-target`,name:`Maximum Number of Non-Overlapping Subarrays With Sum Equals Target`,frequency:42,url:`https://leetcode.com/problems/maximum-number-of-non-overlapping-subarrays-with-sum-equals-target/`}]}},{slug:`matrix`,title:`Matrix`,tagline:`Two indices and a great many off-by-ones.`,track:`Arrays and strings`,order:12,accent:`#a78bfa`,summary:`Grids, spirals, rotations, and the traversals that are easy to describe and awkward to write. Most of these ask for it in place, which is where the index arithmetic starts to matter.`,pattern:{spot:[`A grid, and a traversal or a rotation.`,`Four or eight directions out of every cell.`,`Row and column indices moving together.`],shape:`// rotate 90° in place: transpose, then reverse each row
for (int i = 0; i < n; i++)
    for (int j = i + 1; j < n; j++) swap(m, i, j, j, i);
for (int[] row : m) reverse(row);`,cost:`O(rows × cols)`,watch:["`j` starts at `i + 1` in a transpose, or every swap undoes itself.","`m[0].length` is the width of row 0 only.",`Check all four bounds before every step.`]},bands:{easy:[{key:`transpose-matrix`,name:`Transpose Matrix`,frequency:34,url:`https://leetcode.com/problems/transpose-matrix/`},{key:`valid-sudoku`,name:`Valid Sudoku`,frequency:25,url:`https://leetcode.com/problems/valid-sudoku/`},{key:`count-negative-numbers-in-a-sorted-matrix`,name:`Count Negative Numbers in a Sorted Matrix`,frequency:55,url:`https://leetcode.com/problems/count-negative-numbers-in-a-sorted-matrix/`},{key:`flipping-an-image`,name:`Flipping an Image`,frequency:60,url:`https://leetcode.com/problems/flipping-an-image/`},{key:`matrix-diagonal-sum`,name:`Matrix Diagonal Sum`,frequency:39,url:`https://leetcode.com/problems/matrix-diagonal-sum/`},{key:`toeplitz-matrix`,name:`Toeplitz Matrix`,frequency:59,url:`https://leetcode.com/problems/toeplitz-matrix/`}],medium:[{key:`spiral-matrix`,name:`Spiral Matrix`,frequency:8,url:`https://leetcode.com/problems/spiral-matrix/`},{key:`rotate-image`,name:`Rotate Image`,frequency:16,url:`https://leetcode.com/problems/rotate-image/`},{key:`set-matrix-zeroes`,name:`Set Matrix Zeroes`,frequency:14,url:`https://leetcode.com/problems/set-matrix-zeroes/`},{key:`word-search`,name:`Word Search`,frequency:22,url:`https://leetcode.com/problems/word-search/`},{key:`game-of-life`,name:`Game of Life`,frequency:53,url:`https://leetcode.com/problems/game-of-life/`},{key:`search-a-2d-matrix-ii`,name:`Search a 2D Matrix II`,frequency:40,url:`https://leetcode.com/problems/search-a-2d-matrix-ii/`}],hard:[{key:`maximal-rectangle`,name:`Maximal Rectangle`,frequency:10,url:`https://leetcode.com/problems/maximal-rectangle/`},{key:`maximal-square`,name:`Maximal Square`,frequency:17,url:`https://leetcode.com/problems/maximal-square/`},{key:`shortest-path-in-binary-matrix`,name:`Shortest Path in Binary Matrix`,frequency:19,url:`https://leetcode.com/problems/shortest-path-in-binary-matrix/`},{key:`max-sum-of-rectangle-no-larger-than-k`,name:`Max Sum of Rectangle No Larger Than K`,frequency:19,url:`https://leetcode.com/problems/max-sum-of-rectangle-no-larger-than-k/`},{key:`number-of-laser-beams-in-a-bank`,name:`Number of Laser Beams in a Bank`,frequency:41,url:`https://leetcode.com/problems/number-of-laser-beams-in-a-bank/`},{key:`trapping-rain-water-ii`,name:`Trapping Rain Water II`,frequency:6,url:`https://leetcode.com/problems/trapping-rain-water-ii/`}]}},{slug:`linked-list`,title:`Linked list`,tagline:`Reversal, and the fast runner.`,track:`Linear structures`,order:13,accent:`#f472b6`,summary:`No indices, so everything turns on what you are still holding on to. Reversal in three variables, cycle detection with two speeds, and the merge and reorder problems that want both at the same time.`,pattern:{spot:[`No indices, and the answer is about position or order.`,`Reversing, merging, or finding a cycle.`,`A node has to go, and there is no way back to the one before it.`],shape:`ListNode prev = null, cur = head;
while (cur != null) {
    ListNode next = cur.next;
    cur.next = prev;
    prev = cur; cur = next;
}
return prev;`,cost:`O(n) time · O(1) space`,watch:["Save `next` before you overwrite `cur.next`. That is the bug.",`A dummy head removes every "what if it is the first node".`,"Check `fast` and `fast.next` before stepping twice."]},bands:{easy:[{key:`linked-list-cycle`,name:`Linked List Cycle`,frequency:27,url:`https://leetcode.com/problems/linked-list-cycle/`},{key:`merge-two-sorted-lists`,name:`Merge Two Sorted Lists`,frequency:41,url:`https://leetcode.com/problems/merge-two-sorted-lists/`},{key:`reverse-linked-list`,name:`Reverse Linked List`,frequency:14,url:`https://leetcode.com/problems/reverse-linked-list/`},{key:`middle-of-the-linked-list`,name:`Middle of the Linked List`,frequency:51,url:`https://leetcode.com/problems/middle-of-the-linked-list/`},{key:`palindrome-linked-list`,name:`Palindrome Linked List`,frequency:45,url:`https://leetcode.com/problems/palindrome-linked-list/`},{key:`intersection-of-two-linked-lists`,name:`Intersection of Two Linked Lists`,frequency:46,url:`https://leetcode.com/problems/intersection-of-two-linked-lists/`},{key:`remove-duplicates-from-sorted-list`,name:`Remove Duplicates from Sorted List`,frequency:32,url:`https://leetcode.com/problems/remove-duplicates-from-sorted-list/`},{key:`convert-binary-number-in-a-linked-list-to-integer`,name:`Convert Binary Number in a Linked List to Integer`,frequency:20,url:`https://leetcode.com/problems/convert-binary-number-in-a-linked-list-to-integer/`},{key:`delete-node-in-a-linked-list`,name:`Delete Node in a Linked List`,frequency:59,url:`https://leetcode.com/problems/delete-node-in-a-linked-list/`},{key:`remove-linked-list-elements`,name:`Remove Linked List Elements`,frequency:39,url:`https://leetcode.com/problems/remove-linked-list-elements/`}],medium:[{key:`add-two-numbers`,name:`Add Two Numbers`,frequency:16,url:`https://leetcode.com/problems/add-two-numbers/`},{key:`remove-nth-node-from-end-of-list`,name:`Remove Nth Node From End of List`,frequency:6,url:`https://leetcode.com/problems/remove-nth-node-from-end-of-list/`},{key:`reverse-linked-list-ii`,name:`Reverse Linked List II`,frequency:60,url:`https://leetcode.com/problems/reverse-linked-list-ii/`},{key:`linked-list-cycle-ii`,name:`Linked List Cycle II`,frequency:42,url:`https://leetcode.com/problems/linked-list-cycle-ii/`},{key:`copy-list-with-random-pointer`,name:`Copy List with Random Pointer`,frequency:24,url:`https://leetcode.com/problems/copy-list-with-random-pointer/`},{key:`odd-even-linked-list`,name:`Odd Even Linked List`,frequency:47,url:`https://leetcode.com/problems/odd-even-linked-list/`},{key:`sort-list`,name:`Sort List`,frequency:56,url:`https://leetcode.com/problems/sort-list/`},{key:`remove-duplicates-from-sorted-list-ii`,name:`Remove Duplicates from Sorted List II`,frequency:12,url:`https://leetcode.com/problems/remove-duplicates-from-sorted-list-ii/`},{key:`partition-list`,name:`Partition List`,frequency:32,url:`https://leetcode.com/problems/partition-list/`},{key:`remove-zero-sum-consecutive-nodes-from-linked-list`,name:`Remove Zero Sum Consecutive Nodes from Linked List`,frequency:41,url:`https://leetcode.com/problems/remove-zero-sum-consecutive-nodes-from-linked-list/`}],hard:[{key:`merge-k-sorted-lists`,name:`Merge k Sorted Lists`,frequency:33,url:`https://leetcode.com/problems/merge-k-sorted-lists/`},{key:`reverse-nodes-in-k-group`,name:`Reverse Nodes in k-Group`,frequency:18,url:`https://leetcode.com/problems/reverse-nodes-in-k-group/`},{key:`reorder-list`,name:`Reorder List`,frequency:60,url:`https://leetcode.com/problems/reorder-list/`},{key:`find-the-duplicate-number`,name:`Find the Duplicate Number`,frequency:50,url:`https://leetcode.com/problems/find-the-duplicate-number/`},{key:`lfu-cache`,name:`LFU Cache`,frequency:51,url:`https://leetcode.com/problems/lfu-cache/`},{key:`flatten-a-multilevel-doubly-linked-list`,name:`Flatten a Multilevel Doubly Linked List`,frequency:56,url:`https://leetcode.com/problems/flatten-a-multilevel-doubly-linked-list/`},{key:`design-linked-list`,name:`Design Linked List`,frequency:23,url:`https://leetcode.com/problems/design-linked-list/`},{key:`insertion-sort-list`,name:`Insertion Sort List`,frequency:32,url:`https://leetcode.com/problems/insertion-sort-list/`},{key:`convert-binary-search-tree-to-sorted-doubly-linked-list`,name:`Convert Binary Search Tree to Sorted Doubly Linked List`,frequency:44,url:`https://leetcode.com/problems/convert-binary-search-tree-to-sorted-doubly-linked-list/`},{key:`insert-into-a-sorted-circular-linked-list`,name:`Insert into a Sorted Circular Linked List`,frequency:12,url:`https://leetcode.com/problems/insert-into-a-sorted-circular-linked-list/`}]}},{slug:`stacks`,title:`Stacks`,tagline:`Last in, first out — and the monotonic trick.`,track:`Linear structures`,order:14,accent:`#facc15`,summary:`Matching brackets is the introduction. The real subject is the monotonic stack: keeping the elements that could still matter and dropping the ones that cannot, which is what turns several quadratic problems linear.`,pattern:{spot:[`Matching pairs, or "the next greater element".`,`The most recent unfinished thing is the one you need.`,`A recursion you have been asked to write as a loop.`],shape:`Deque<Integer> st = new ArrayDeque<>();
for (int i = 0; i < a.length; i++) {
    while (!st.isEmpty() && a[st.peek()] < a[i]) ans[st.pop()] = a[i];
    st.push(i);
}`,cost:`O(n) — each index pushed once`,watch:[`Push indices, not values, when the answer needs a position.`,"Use `ArrayDeque`. `Stack` is synchronised and iterates backwards.",`Drain the stack after the loop, or the last run is lost.`]},bands:{easy:[{key:`valid-parentheses`,name:`Valid Parentheses`,frequency:57,url:`https://leetcode.com/problems/valid-parentheses/`},{key:`next-greater-element-i`,name:`Next Greater Element I`,frequency:28,url:`https://leetcode.com/problems/next-greater-element-i/`},{key:`baseball-game`,name:`Baseball Game`,frequency:53,url:`https://leetcode.com/problems/baseball-game/`},{key:`backspace-string-compare`,name:`Backspace String Compare`,frequency:39,url:`https://leetcode.com/problems/backspace-string-compare/`},{key:`remove-all-adjacent-duplicates-in-string`,name:`Remove All Adjacent Duplicates In String`,frequency:3,url:`https://leetcode.com/problems/remove-all-adjacent-duplicates-in-string/`},{key:`remove-outermost-parentheses`,name:`Remove Outermost Parentheses`,frequency:30,url:`https://leetcode.com/problems/remove-outermost-parentheses/`},{key:`crawler-log-folder`,name:`Crawler Log Folder`,frequency:40,url:`https://leetcode.com/problems/crawler-log-folder/`},{key:`final-prices-with-a-special-discount-in-a-shop`,name:`Final Prices With a Special Discount in a Shop`,frequency:20,url:`https://leetcode.com/problems/final-prices-with-a-special-discount-in-a-shop/`},{key:`next-greater-node-in-linked-list`,name:`Next Greater Node In Linked List`,frequency:25,url:`https://leetcode.com/problems/next-greater-node-in-linked-list/`},{key:`online-stock-span`,name:`Online Stock Span`,frequency:56,url:`https://leetcode.com/problems/online-stock-span/`}],medium:[{key:`min-stack`,name:`Min Stack`,frequency:41,url:`https://leetcode.com/problems/min-stack/`},{key:`evaluate-reverse-polish-notation`,name:`Evaluate Reverse Polish Notation`,frequency:58,url:`https://leetcode.com/problems/evaluate-reverse-polish-notation/`},{key:`daily-temperatures`,name:`Daily Temperatures`,frequency:55,url:`https://leetcode.com/problems/daily-temperatures/`},{key:`remove-k-digits`,name:`Remove K Digits`,frequency:26,url:`https://leetcode.com/problems/remove-k-digits/`},{key:`132-pattern`,name:`132 Pattern`,frequency:50,url:`https://leetcode.com/problems/132-pattern/`},{key:`next-greater-element-ii`,name:`Next Greater Element II`,frequency:14,url:`https://leetcode.com/problems/next-greater-element-ii/`},{key:`design-a-stack-with-increment-operation`,name:`Design a Stack With Increment Operation`,frequency:3,url:`https://leetcode.com/problems/design-a-stack-with-increment-operation/`},{key:`asteroid-collision`,name:`Asteroid Collision`,frequency:46,url:`https://leetcode.com/problems/asteroid-collision/`},{key:`odd-even-jump`,name:`Odd Even Jump`,frequency:20,url:`https://leetcode.com/problems/odd-even-jump/`},{key:`sum-of-subarray-ranges`,name:`Sum of Subarray Ranges`,frequency:43,url:`https://leetcode.com/problems/sum-of-subarray-ranges/`}],hard:[{key:`longest-valid-parentheses`,name:`Longest Valid Parentheses`,frequency:40,url:`https://leetcode.com/problems/longest-valid-parentheses/`},{key:`largest-rectangle-in-histogram`,name:`Largest Rectangle in Histogram`,frequency:40,url:`https://leetcode.com/problems/largest-rectangle-in-histogram/`},{key:`maximum-frequency-stack`,name:`Maximum Frequency Stack`,frequency:44,url:`https://leetcode.com/problems/maximum-frequency-stack/`},{key:`sum-of-subarray-minimums`,name:`Sum of Subarray Minimums`,frequency:22,url:`https://leetcode.com/problems/sum-of-subarray-minimums/`},{key:`trapping-rain-water`,name:`Trapping Rain Water`,frequency:16,url:`https://leetcode.com/problems/trapping-rain-water/`},{key:`basic-calculator-ii`,name:`Basic Calculator II`,frequency:3,url:`https://leetcode.com/problems/basic-calculator-ii/`},{key:`simplify-path`,name:`Simplify Path`,frequency:17,url:`https://leetcode.com/problems/simplify-path/`},{key:`exclusive-time-of-functions`,name:`Exclusive Time of Functions`,frequency:7,url:`https://leetcode.com/problems/exclusive-time-of-functions/`},{key:`score-of-parentheses`,name:`Score of Parentheses`,frequency:60,url:`https://leetcode.com/problems/score-of-parentheses/`},{key:`sum-of-total-strength-of-wizards`,name:`Sum of Total Strength of Wizards`,frequency:5,url:`https://leetcode.com/problems/sum-of-total-strength-of-wizards/`}]}},{slug:`queues`,title:`Queues`,tagline:`First in, first out — and the sliding maximum.`,track:`Linear structures`,order:15,accent:`#38bdf8`,summary:`The other end of the same idea. Level-order traversal, task scheduling, and the monotonic queue that answers "the maximum of every window" without ever looking at a window twice.`,pattern:{spot:[`Level by level, or the shortest path with unweighted edges.`,`First in, first out.`,`Things that arrive and expire in the same order.`],shape:`Queue<Node> q = new ArrayDeque<>(List.of(root));
while (!q.isEmpty()) {
    int level = q.size();          // fix the size first
    for (int i = 0; i < level; i++) { ... }
}`,cost:`O(n) — in once, out once`,watch:["Read `q.size()` into a variable before the level loop.",`Mark as seen when you enqueue, not when you dequeue.`,"`poll` returns null on empty; `remove` throws."]},bands:{easy:[{key:`number-of-recent-calls`,name:`Number of Recent Calls`,frequency:3,url:`https://leetcode.com/problems/number-of-recent-calls/`},{key:`implement-queue-using-stacks`,name:`Implement Queue using Stacks`,frequency:52,url:`https://leetcode.com/problems/implement-queue-using-stacks/`},{key:`moving-average-from-data-stream`,name:`Moving Average from Data Stream`,frequency:33,url:`https://leetcode.com/problems/moving-average-from-data-stream/`},{key:`time-needed-to-buy-tickets`,name:`Time Needed to Buy Tickets`,frequency:33,url:`https://leetcode.com/problems/time-needed-to-buy-tickets/`},{key:`first-unique-character-in-a-string`,name:`First Unique Character in a String`,frequency:37,url:`https://leetcode.com/problems/first-unique-character-in-a-string/`},{key:`design-a-number-container-system`,name:`Design a Number Container System`,frequency:28,url:`https://leetcode.com/problems/design-a-number-container-system/`},{key:`reveal-cards-in-increasing-order`,name:`Reveal Cards In Increasing Order`,frequency:4,url:`https://leetcode.com/problems/reveal-cards-in-increasing-order/`},{key:`design-bounded-blocking-queue`,name:`Design Bounded Blocking Queue`,frequency:56,url:`https://leetcode.com/problems/design-bounded-blocking-queue/`},{key:`longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit`,name:`Longest Continuous Subarray With Absolute Diff Less Than or Equal to Limit`,frequency:43,url:`https://leetcode.com/problems/longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit/`},{key:`maximum-number-of-robots-within-budget`,name:`Maximum Number of Robots Within Budget`,frequency:23,url:`https://leetcode.com/problems/maximum-number-of-robots-within-budget/`}],medium:[{key:`design-circular-queue`,name:`Design Circular Queue`,frequency:7,url:`https://leetcode.com/problems/design-circular-queue/`},{key:`open-the-lock`,name:`Open the Lock`,frequency:33,url:`https://leetcode.com/problems/open-the-lock/`},{key:`jump-game-vi`,name:`Jump Game VI`,frequency:44,url:`https://leetcode.com/problems/jump-game-vi/`},{key:`dota2-senate`,name:`Dota2 Senate`,frequency:32,url:`https://leetcode.com/problems/dota2-senate/`},{key:`shortest-path-in-binary-matrix`,name:`Shortest Path in Binary Matrix`,frequency:54,url:`https://leetcode.com/problems/shortest-path-in-binary-matrix/`},{key:`design-hit-counter`,name:`Design Hit Counter`,frequency:24,url:`https://leetcode.com/problems/design-hit-counter/`},{key:`number-of-students-unable-to-eat-lunch`,name:`Number of Students Unable to Eat Lunch`,frequency:27,url:`https://leetcode.com/problems/number-of-students-unable-to-eat-lunch/`},{key:`shortest-path-in-a-grid-with-obstacles-elimination`,name:`Shortest Path in a Grid with Obstacles Elimination`,frequency:7,url:`https://leetcode.com/problems/shortest-path-in-a-grid-with-obstacles-elimination/`},{key:`shortest-path-with-alternating-colors`,name:`Shortest Path with Alternating Colors`,frequency:7,url:`https://leetcode.com/problems/shortest-path-with-alternating-colors/`},{key:`shortest-path-to-get-all-keys`,name:`Shortest Path to Get All Keys`,frequency:24,url:`https://leetcode.com/problems/shortest-path-to-get-all-keys/`}],hard:[{key:`task-scheduler`,name:`Task Scheduler`,frequency:8,url:`https://leetcode.com/problems/task-scheduler/`},{key:`sliding-window-maximum`,name:`Sliding Window Maximum`,frequency:48,url:`https://leetcode.com/problems/sliding-window-maximum/`},{key:`constrained-subsequence-sum`,name:`Constrained Subsequence Sum`,frequency:25,url:`https://leetcode.com/problems/constrained-subsequence-sum/`},{key:`maximum-performance-of-a-team`,name:`Maximum Performance of a Team`,frequency:56,url:`https://leetcode.com/problems/maximum-performance-of-a-team/`},{key:`jump-game-vii`,name:`Jump Game VII`,frequency:60,url:`https://leetcode.com/problems/jump-game-vii/`},{key:`design-food-rating-system`,name:`Design Food Rating System`,frequency:17,url:`https://leetcode.com/problems/design-food-rating-system/`},{key:`design-snake-game`,name:`Design Snake Game`,frequency:54,url:`https://leetcode.com/problems/design-snake-game/`},{key:`shortest-bridge`,name:`Shortest Bridge`,frequency:40,url:`https://leetcode.com/problems/shortest-bridge/`},{key:`jump-game-iv`,name:`Jump Game IV`,frequency:32,url:`https://leetcode.com/problems/jump-game-iv/`},{key:`minimum-moves-to-reach-target-with-rotations`,name:`Minimum Moves to Reach Target with Rotations`,frequency:52,url:`https://leetcode.com/problems/minimum-moves-to-reach-target-with-rotations/`}]}},{slug:`deque`,title:`Deque`,tagline:`Both ends, for when one is not enough.`,track:`Linear structures`,order:16,accent:`#60a5fa`,summary:`A queue you can push to and pop from either side. It is the structure underneath the sliding-window maximum and underneath 0-1 BFS, and it is worth meeting before the problem that needs it.`,pattern:{spot:[`You need to add or remove at both ends.`,`The maximum or minimum of every window.`,`A zero-cost step that should jump the queue.`],shape:`Deque<Integer> dq = new ArrayDeque<>();
dq.addFirst(x); dq.addLast(y);
dq.pollFirst();  dq.pollLast();`,cost:`O(1) at either end`,watch:["Use `ArrayDeque` — the one good stack, queue and deque.",`Store indices, so the front can be tested for expiry.`,"`push` and `offer` add at opposite ends. Check the alias table."]},bands:{easy:[{key:`moving-average-from-data-stream`,name:`Moving Average from Data Stream`,frequency:7,url:`https://leetcode.com/problems/moving-average-from-data-stream/`},{key:`number-of-recent-calls`,name:`Number of Recent Calls`,frequency:44,url:`https://leetcode.com/problems/number-of-recent-calls/`},{key:`design-circular-queue`,name:`Design Circular Queue`,frequency:41,url:`https://leetcode.com/problems/design-circular-queue/`},{key:`design-a-stack-with-increment-operation`,name:`Design a Stack With Increment Operation`,frequency:41,url:`https://leetcode.com/problems/design-a-stack-with-increment-operation/`},{key:`reveal-cards-in-increasing-order`,name:`Reveal Cards In Increasing Order`,frequency:37,url:`https://leetcode.com/problems/reveal-cards-in-increasing-order/`},{key:`first-unique-number`,name:`First Unique Number`,frequency:3,url:`https://leetcode.com/problems/first-unique-number/`}],medium:[{key:`design-circular-deque`,name:`Design Circular Deque`,frequency:50,url:`https://leetcode.com/problems/design-circular-deque/`},{key:`design-front-middle-back-queue`,name:`Design Front Middle Back Queue`,frequency:43,url:`https://leetcode.com/problems/design-front-middle-back-queue/`},{key:`jump-game-vi`,name:`Jump Game VI`,frequency:32,url:`https://leetcode.com/problems/jump-game-vi/`},{key:`dota2-senate`,name:`Dota2 Senate`,frequency:41,url:`https://leetcode.com/problems/dota2-senate/`},{key:`find-the-most-competitive-subsequence`,name:`Find the Most Competitive Subsequence`,frequency:12,url:`https://leetcode.com/problems/find-the-most-competitive-subsequence/`},{key:`longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit`,name:`Longest Continuous Subarray With Absolute Diff Less Than or Equal to Limit`,frequency:26,url:`https://leetcode.com/problems/longest-continuous-subarray-with-absolute-diff-less-than-or-equal-to-limit/`}],hard:[{key:`sliding-window-maximum`,name:`Sliding Window Maximum`,frequency:29,url:`https://leetcode.com/problems/sliding-window-maximum/`},{key:`shortest-subarray-with-sum-at-least-k`,name:`Shortest Subarray with Sum at Least K`,frequency:29,url:`https://leetcode.com/problems/shortest-subarray-with-sum-at-least-k/`},{key:`constrained-subsequence-sum`,name:`Constrained Subsequence Sum`,frequency:23,url:`https://leetcode.com/problems/constrained-subsequence-sum/`},{key:`sliding-window-median`,name:`Sliding Window Median`,frequency:23,url:`https://leetcode.com/problems/sliding-window-median/`},{key:`maximum-number-of-robots-within-budget`,name:`Maximum Number of Robots Within Budget`,frequency:7,url:`https://leetcode.com/problems/maximum-number-of-robots-within-budget/`},{key:`odd-even-jump`,name:`Odd Even Jump`,frequency:3,url:`https://leetcode.com/problems/odd-even-jump/`}]}},{slug:`sorting`,title:`Sorting`,tagline:`Not the algorithms — what to sort by.`,track:`Linear structures`,order:17,accent:`#8b6cff`,summary:`Very few interviews ask you to implement a sort. A great many are solved the moment the input is sorted by the right key, so these are about choosing the comparator — and about the counting sorts that beat n log n when the range is small.`,pattern:{spot:[`The problem becomes easy once the input is in order.`,`A comparison rule stated in the question itself.`,`Counts over a small, fixed range of values.`],shape:`Arrays.sort(people, Comparator
    .comparingInt((int[] p) -> p[0])
    .thenComparing(p -> p[1], Comparator.reverseOrder()));`,cost:`O(n log n) · O(n + k) counting`,watch:["Never subtract in a comparator — `a - b` overflows. `Integer.compare`.","`Arrays.sort` on primitives is unstable, with an O(n²) worst case.",`Sorting destroys the original indices.`]},bands:{easy:[{key:`sort-array-by-parity`,name:`Sort Array by Parity`,frequency:16,url:`https://leetcode.com/problems/sort-array-by-parity/`},{key:`merge-sorted-array`,name:`Merge Sorted Array`,frequency:29,url:`https://leetcode.com/problems/merge-sorted-array/`},{key:`intersection-of-two-arrays`,name:`Intersection of Two Arrays`,frequency:52,url:`https://leetcode.com/problems/intersection-of-two-arrays/`},{key:`height-checker`,name:`Height Checker`,frequency:10,url:`https://leetcode.com/problems/height-checker/`},{key:`sort-array-by-increasing-frequency`,name:`Sort Array By Increasing Frequency`,frequency:15,url:`https://leetcode.com/problems/sort-array-by-increasing-frequency/`},{key:`minimum-absolute-difference`,name:`Minimum Absolute Difference`,frequency:5,url:`https://leetcode.com/problems/minimum-absolute-difference/`},{key:`sort-the-matrix-diagonally`,name:`Sort the Matrix Diagonally`,frequency:16,url:`https://leetcode.com/problems/sort-the-matrix-diagonally/`},{key:`sort-integers-by-the-power-value`,name:`Sort Integers by The Power Value`,frequency:25,url:`https://leetcode.com/problems/sort-integers-by-the-power-value/`},{key:`sort-the-people`,name:`Sort the People`,frequency:26,url:`https://leetcode.com/problems/sort-the-people/`},{key:`pancake-sorting`,name:`Pancake Sorting`,frequency:15,url:`https://leetcode.com/problems/pancake-sorting/`}],medium:[{key:`sort-colors`,name:`Sort Colors`,frequency:5,url:`https://leetcode.com/problems/sort-colors/`},{key:`sort-list`,name:`Sort List`,frequency:57,url:`https://leetcode.com/problems/sort-list/`},{key:`kth-largest-element-in-an-array`,name:`Kth Largest Element in an Array`,frequency:54,url:`https://leetcode.com/problems/kth-largest-element-in-an-array/`},{key:`meeting-rooms-ii`,name:`Meeting Rooms II`,frequency:25,url:`https://leetcode.com/problems/meeting-rooms-ii/`},{key:`minimum-number-of-arrows-to-burst-balloons`,name:`Minimum Number of Arrows to Burst Balloons`,frequency:12,url:`https://leetcode.com/problems/minimum-number-of-arrows-to-burst-balloons/`},{key:`largest-number`,name:`Largest Number`,frequency:17,url:`https://leetcode.com/problems/largest-number/`},{key:`relative-sort-array`,name:`Relative Sort Array`,frequency:42,url:`https://leetcode.com/problems/relative-sort-array/`},{key:`course-schedule-iii`,name:`Course Schedule III`,frequency:3,url:`https://leetcode.com/problems/course-schedule-iii/`},{key:`maximize-sum-of-array-after-k-negations`,name:`Maximize Sum Of Array After K Negations`,frequency:22,url:`https://leetcode.com/problems/maximize-sum-of-array-after-k-negations/`},{key:`most-profit-assigning-work`,name:`Most Profit Assigning Work`,frequency:13,url:`https://leetcode.com/problems/most-profit-assigning-work/`}],hard:[{key:`maximum-gap`,name:`Maximum Gap`,frequency:60,url:`https://leetcode.com/problems/maximum-gap/`},{key:`reverse-pairs`,name:`Reverse Pairs`,frequency:7,url:`https://leetcode.com/problems/reverse-pairs/`},{key:`find-median-from-data-stream`,name:`Find Median from Data Stream`,frequency:7,url:`https://leetcode.com/problems/find-median-from-data-stream/`},{key:`count-of-smaller-numbers-after-self`,name:`Count of Smaller Numbers After Self`,frequency:11,url:`https://leetcode.com/problems/count-of-smaller-numbers-after-self/`},{key:`wiggle-sort-ii`,name:`Wiggle Sort II`,frequency:7,url:`https://leetcode.com/problems/wiggle-sort-ii/`},{key:`couples-holding-hands`,name:`Couples Holding Hands`,frequency:44,url:`https://leetcode.com/problems/couples-holding-hands/`},{key:`find-smallest-common-element-in-all-rows`,name:`Find Smallest Common Element in All Rows`,frequency:47,url:`https://leetcode.com/problems/find-smallest-common-element-in-all-rows/`},{key:`least-number-of-unique-integers-after-k-removals`,name:`Least Number of Unique Integers after K Removals`,frequency:19,url:`https://leetcode.com/problems/least-number-of-unique-integers-after-k-removals/`},{key:`smallest-range-covering-elements-from-k-lists`,name:`Smallest Range Covering Elements from K Lists`,frequency:25,url:`https://leetcode.com/problems/smallest-range-covering-elements-from-k-lists/`},{key:`queue-reconstruction-by-height`,name:`Queue Reconstruction by Height`,frequency:40,url:`https://leetcode.com/problems/queue-reconstruction-by-height/`}]}},{slug:`recursion-and-backtracking`,title:`Recursion and backtracking`,tagline:`Try it, undo it, try the next one.`,track:`Recursion and search`,order:18,accent:`#6baf8d`,summary:`Permutations, subsets, combinations, and the board problems. The recursion is the easy part. The discipline is undoing the choice on the way back out, so the next branch starts from a clean state.`,pattern:{spot:[`All permutations, all subsets, or a board to fill.`,`A choice at each step, and the rest depends on it.`,`n is 12 or 20, because the answer is exponential.`],shape:`void go(int i, List<Integer> path) {
    if (i == n) { out.add(new ArrayList<>(path)); return; }
    path.add(a[i]); go(i + 1, path); path.remove(path.size() - 1);
    go(i + 1, path);
}`,cost:`O(2ⁿ) subsets · O(n!) permutations`,watch:[`Undo the choice on the way out.`,`Copy the path when you store it, or every answer is the same list.`,"Duplicates: sort, then `if (i > start && a[i] == a[i-1]) continue`."]},bands:{easy:[{key:`fibonacci-number`,name:`Fibonacci Number`,frequency:22,url:`https://leetcode.com/problems/fibonacci-number/`},{key:`subsets`,name:`Subsets`,frequency:38,url:`https://leetcode.com/problems/subsets/`},{key:`path-sum`,name:`Path Sum`,frequency:26,url:`https://leetcode.com/problems/path-sum/`},{key:`merge-two-sorted-lists`,name:`Merge Two Sorted Lists`,frequency:29,url:`https://leetcode.com/problems/merge-two-sorted-lists/`},{key:`letter-case-permutation`,name:`Letter Case Permutation`,frequency:45,url:`https://leetcode.com/problems/letter-case-permutation/`},{key:`binary-tree-paths`,name:`Binary Tree Paths`,frequency:46,url:`https://leetcode.com/problems/binary-tree-paths/`}],medium:[{key:`generate-parentheses`,name:`Generate Parentheses`,frequency:18,url:`https://leetcode.com/problems/generate-parentheses/`},{key:`combination-sum`,name:`Combination Sum`,frequency:42,url:`https://leetcode.com/problems/combination-sum/`},{key:`letter-combinations-of-a-phone-number`,name:`Letter Combinations of a Phone Number`,frequency:42,url:`https://leetcode.com/problems/letter-combinations-of-a-phone-number/`},{key:`permutations`,name:`Permutations`,frequency:33,url:`https://leetcode.com/problems/permutations/`},{key:`palindrome-partitioning`,name:`Palindrome Partitioning`,frequency:16,url:`https://leetcode.com/problems/palindrome-partitioning/`},{key:`subsets-ii`,name:`Subsets II`,frequency:41,url:`https://leetcode.com/problems/subsets-ii/`}],hard:[{key:`n-queens`,name:`N-Queens`,frequency:38,url:`https://leetcode.com/problems/n-queens/`},{key:`word-search-ii`,name:`Word Search II`,frequency:6,url:`https://leetcode.com/problems/word-search-ii/`},{key:`sudoku-solver`,name:`Sudoku Solver`,frequency:8,url:`https://leetcode.com/problems/sudoku-solver/`},{key:`remove-invalid-parentheses`,name:`Remove Invalid Parentheses`,frequency:27,url:`https://leetcode.com/problems/remove-invalid-parentheses/`},{key:`expression-add-operators`,name:`Expression Add Operators`,frequency:4,url:`https://leetcode.com/problems/expression-add-operators/`},{key:`partition-to-k-equal-sum-subsets`,name:`Partition to K Equal Sum Subsets`,frequency:4,url:`https://leetcode.com/problems/partition-to-k-equal-sum-subsets/`}]}},{slug:`divide-and-conquer`,title:`Divide and conquer`,tagline:`Halve it until the answer is obvious.`,track:`Recursion and search`,order:19,accent:`#34d399`,summary:`Split it, solve each side, and spend the work in the merge. Merge sort is the model — the problems here are the ones where the merge step counts something neither half could see on its own.`,pattern:{spot:[`The halves can be solved independently of each other.`,`The real work is in the merge, not the split.`,`Each step halves what is left.`],shape:`int solve(int lo, int hi) {
    if (lo >= hi) return base;
    int mid = lo + (hi - lo) / 2;
    return merge(solve(lo, mid), solve(mid + 1, hi));
}`,cost:`O(n log n) with a linear merge`,watch:["`lo + (hi - lo) / 2`, never `(lo + hi) / 2`.","One side must take `mid + 1`, or the recursion never shrinks.",`A base case for one element, and one for none.`]},bands:{easy:[{key:`search-insert-position`,name:`Search Insert Position`,frequency:6,url:`https://leetcode.com/problems/search-insert-position/`},{key:`merge-sorted-array`,name:`Merge Sorted Array`,frequency:23,url:`https://leetcode.com/problems/merge-sorted-array/`},{key:`maximum-subarray`,name:`Maximum Subarray`,frequency:43,url:`https://leetcode.com/problems/maximum-subarray/`},{key:`majority-element`,name:`Majority Element`,frequency:38,url:`https://leetcode.com/problems/majority-element/`},{key:`peak-index-in-a-mountain-array`,name:`Peak Index in a Mountain Array`,frequency:6,url:`https://leetcode.com/problems/peak-index-in-a-mountain-array/`},{key:`valid-perfect-square`,name:`Valid Perfect Square`,frequency:33,url:`https://leetcode.com/problems/valid-perfect-square/`}],medium:[{key:`convert-sorted-list-to-binary-search-tree`,name:`Convert Sorted List to Binary Search Tree`,frequency:43,url:`https://leetcode.com/problems/convert-sorted-list-to-binary-search-tree/`},{key:`maximum-binary-tree`,name:`Maximum Binary Tree`,frequency:19,url:`https://leetcode.com/problems/maximum-binary-tree/`},{key:`find-k-closest-elements`,name:`Find K Closest Elements`,frequency:45,url:`https://leetcode.com/problems/find-k-closest-elements/`},{key:`different-ways-to-add-parentheses`,name:`Different Ways to Add Parentheses`,frequency:9,url:`https://leetcode.com/problems/different-ways-to-add-parentheses/`},{key:`sort-an-array`,name:`Sort an Array (Merge Sort)`,frequency:4,url:`https://leetcode.com/problems/sort-an-array/`},{key:`beautiful-array`,name:`Beautiful Array`,frequency:52,url:`https://leetcode.com/problems/beautiful-array/`}],hard:[{key:`the-skyline-problem`,name:`The Skyline Problem`,frequency:19,url:`https://leetcode.com/problems/the-skyline-problem/`},{key:`reverse-pairs`,name:`Reverse Pairs`,frequency:30,url:`https://leetcode.com/problems/reverse-pairs/`},{key:`median-of-two-sorted-arrays`,name:`Median of Two Sorted Arrays`,frequency:46,url:`https://leetcode.com/problems/median-of-two-sorted-arrays/`},{key:`count-of-smaller-numbers-after-self`,name:`Count of Smaller Numbers After Self`,frequency:15,url:`https://leetcode.com/problems/count-of-smaller-numbers-after-self/`},{key:`russian-doll-envelopes`,name:`Russian Doll Envelopes`,frequency:20,url:`https://leetcode.com/problems/russian-doll-envelopes/`},{key:`k-th-smallest-prime-fraction`,name:`K-th Smallest Prime Fraction`,frequency:31,url:`https://leetcode.com/problems/k-th-smallest-prime-fraction/`}]}},{slug:`binary-search`,title:`Binary search`,tagline:`Not only on arrays — on the answer.`,track:`Recursion and search`,order:20,accent:`#f97316`,summary:`The array version is ten lines and one off-by-one. The version worth the practice is binary search on the answer: guess a value, ask whether it is feasible, and halve the range of values it could have been.`,pattern:{spot:[`Sorted input — or a monotonic yes/no about the answer itself.`,`n is a billion, so the input cannot even be read once.`,`"The first index such that", or "the smallest value that works".`],shape:`int lo = 0, hi = n;
while (lo < hi) {
    int mid = lo + (hi - lo) / 2;
    if (feasible(mid)) hi = mid; else lo = mid + 1;
}
return lo;   // the smallest feasible value`,cost:`O(log n) probes × one check`,watch:["Pick one form and keep it. `lo <= hi` with `hi = mid` never ends.",`The predicate must be monotone: false, false, true, true.`,`The midpoint overflows on large bounds.`]},bands:{easy:[{key:`binary-search`,name:`Binary Search`,frequency:58,url:`https://leetcode.com/problems/binary-search/`},{key:`first-bad-version`,name:`First Bad Version`,frequency:57,url:`https://leetcode.com/problems/first-bad-version/`},{key:`search-insert-position`,name:`Search Insert Position`,frequency:29,url:`https://leetcode.com/problems/search-insert-position/`},{key:`guess-number-higher-or-lower`,name:`Guess Number Higher or Lower`,frequency:19,url:`https://leetcode.com/problems/guess-number-higher-or-lower/`},{key:`sqrtx`,name:`Sqrt(x)`,frequency:37,url:`https://leetcode.com/problems/sqrtx/`},{key:`arranging-coins`,name:`Arranging Coins`,frequency:59,url:`https://leetcode.com/problems/arranging-coins/`}],medium:[{key:`search-in-rotated-sorted-array`,name:`Search in Rotated Sorted Array`,frequency:13,url:`https://leetcode.com/problems/search-in-rotated-sorted-array/`},{key:`find-peak-element`,name:`Find Peak Element`,frequency:59,url:`https://leetcode.com/problems/find-peak-element/`},{key:`koko-eating-bananas`,name:`Koko Eating Bananas`,frequency:31,url:`https://leetcode.com/problems/koko-eating-bananas/`},{key:`capacity-to-ship-packages-within-d-days`,name:`Capacity to Ship Packages Within D Days`,frequency:8,url:`https://leetcode.com/problems/capacity-to-ship-packages-within-d-days/`},{key:`time-based-key-value-store`,name:`Time Based Key-Value Store`,frequency:24,url:`https://leetcode.com/problems/time-based-key-value-store/`},{key:`search-a-2d-matrix`,name:`Search a 2D Matrix`,frequency:24,url:`https://leetcode.com/problems/search-a-2d-matrix/`}],hard:[{key:`find-in-mountain-array`,name:`Find in Mountain Array`,frequency:19,url:`https://leetcode.com/problems/find-in-mountain-array/`},{key:`median-of-two-sorted-arrays`,name:`Median of Two Sorted Arrays`,frequency:44,url:`https://leetcode.com/problems/median-of-two-sorted-arrays/`},{key:`split-array-largest-sum`,name:`Split Array Largest Sum`,frequency:38,url:`https://leetcode.com/problems/split-array-largest-sum/`},{key:`find-the-duplicate-number`,name:`Find the Duplicate Number`,frequency:26,url:`https://leetcode.com/problems/find-the-duplicate-number/`},{key:`kth-smallest-number-in-multiplication-table`,name:`Kth Smallest Number in Multiplication Table`,frequency:47,url:`https://leetcode.com/problems/kth-smallest-number-in-multiplication-table/`},{key:`divide-chocolate`,name:`Divide Chocolate`,frequency:50,url:`https://leetcode.com/problems/divide-chocolate/`}]}},{slug:`binary-tree`,title:`Binary tree`,tagline:`Three traversals and a lot of recursion.`,track:`Trees and ranges`,order:21,accent:`#a78bfa`,summary:`Depth, diameter, paths and views. Nearly all of these are one traversal with something carried down and something returned back up, and the difficulty is deciding which of the two each piece of state is.`,pattern:{spot:[`Nodes with children, and a question about depth, paths or shape.`,`A node's answer is built from its children's.`,`Level by level, or one specific traversal order.`],shape:`int depth(Node n) {
    if (n == null) return 0;
    return 1 + Math.max(depth(n.left), depth(n.right));
}`,cost:`O(n) time · O(h) stack`,watch:[`Decide what is carried down and what is returned up.`,`A null node is a base case, not a crash.`,`Depth costs stack. A degenerate tree is n frames.`]},bands:{easy:[{key:`maximum-depth-of-binary-tree`,name:`Maximum Depth of Binary Tree`,frequency:6,url:`https://leetcode.com/problems/maximum-depth-of-binary-tree/`},{key:`invert-binary-tree`,name:`Invert Binary Tree`,frequency:29,url:`https://leetcode.com/problems/invert-binary-tree/`},{key:`symmetric-tree`,name:`Symmetric Tree`,frequency:59,url:`https://leetcode.com/problems/symmetric-tree/`},{key:`diameter-of-binary-tree`,name:`Diameter of Binary Tree`,frequency:8,url:`https://leetcode.com/problems/diameter-of-binary-tree/`},{key:`subtree-of-another-tree`,name:`Subtree of Another Tree`,frequency:43,url:`https://leetcode.com/problems/subtree-of-another-tree/`},{key:`path-sum`,name:`Path Sum`,frequency:21,url:`https://leetcode.com/problems/path-sum/`}],medium:[{key:`binary-tree-level-order-traversal`,name:`Binary Tree Level Order Traversal`,frequency:31,url:`https://leetcode.com/problems/binary-tree-level-order-traversal/`},{key:`binary-tree-right-side-view`,name:`Binary Tree Right Side View`,frequency:24,url:`https://leetcode.com/problems/binary-tree-right-side-view/`},{key:`validate-binary-search-tree`,name:`Validate Binary Search Tree`,frequency:49,url:`https://leetcode.com/problems/validate-binary-search-tree/`},{key:`lowest-common-ancestor-of-a-binary-tree`,name:`Lowest Common Ancestor of a Binary Tree`,frequency:27,url:`https://leetcode.com/problems/lowest-common-ancestor-of-a-binary-tree/`},{key:`construct-binary-tree-from-preorder-and-inorder-traversal`,name:`Construct Binary Tree from Preorder and Inorder`,frequency:4,url:`https://leetcode.com/problems/construct-binary-tree-from-preorder-and-inorder-traversal/`},{key:`path-sum-ii`,name:`Path Sum II`,frequency:20,url:`https://leetcode.com/problems/path-sum-ii/`},{key:`kth-smallest-element-in-a-bst`,name:`Kth Smallest Element in a BST`,frequency:42,url:`https://leetcode.com/problems/kth-smallest-element-in-a-bst/`}],hard:[{key:`binary-tree-maximum-path-sum`,name:`Binary Tree Maximum Path Sum`,frequency:43,url:`https://leetcode.com/problems/binary-tree-maximum-path-sum/`},{key:`binary-tree-cameras`,name:`Binary Tree Cameras`,frequency:31,url:`https://leetcode.com/problems/binary-tree-cameras/`},{key:`serialize-and-deserialize-binary-tree`,name:`Serialize and Deserialize Binary Tree`,frequency:29,url:`https://leetcode.com/problems/serialize-and-deserialize-binary-tree/`},{key:`vertical-order-traversal-of-a-binary-tree`,name:`Vertical Order Traversal of a Binary Tree`,frequency:55,url:`https://leetcode.com/problems/vertical-order-traversal-of-a-binary-tree/`},{key:`maximum-sum-bst-in-binary-tree`,name:`Maximum Sum BST in Binary Tree`,frequency:57,url:`https://leetcode.com/problems/maximum-sum-bst-in-binary-tree/`},{key:`recover-binary-search-tree`,name:`Recover Binary Search Tree`,frequency:51,url:`https://leetcode.com/problems/recover-binary-search-tree/`}]}},{slug:`bst-ordered-set`,title:`BST and ordered set`,tagline:`Sorted order, for free, as you insert.`,track:`Trees and ranges`,order:22,accent:`#f472b6`,summary:`A binary search tree is an in-order traversal waiting to happen, which is what makes validation, k-th element and range queries short to write. The ordered set is the same guarantee handed to you by the library.`,pattern:{spot:[`A tree that carries an ordering guarantee.`,`"The k-th smallest", or the next value above a given one.`,`Ranges, neighbours, or output that must come out sorted.`],shape:`// in-order traversal of a BST is sorted
void inorder(Node n) {
    if (n == null) return;
    inorder(n.left); visit(n); inorder(n.right);
}`,cost:`O(log n) balanced · O(n) skewed`,watch:[`Validate with a min/max window, not against the parent alone.`,`Inorder of a BST is sorted. Most questions reduce to that.`,`Sorted input degenerates a plain BST into a list.`]},bands:{easy:[{key:`search-in-a-binary-search-tree`,name:`Search in a Binary Search Tree`,frequency:50,url:`https://leetcode.com/problems/search-in-a-binary-search-tree/`},{key:`minimum-absolute-difference-in-bst`,name:`Minimum Absolute Difference in BST`,frequency:28,url:`https://leetcode.com/problems/minimum-absolute-difference-in-bst/`},{key:`range-sum-of-bst`,name:`Range Sum of BST`,frequency:23,url:`https://leetcode.com/problems/range-sum-of-bst/`},{key:`convert-sorted-array-to-binary-search-tree`,name:`Convert Sorted Array to Binary Search Tree`,frequency:56,url:`https://leetcode.com/problems/convert-sorted-array-to-binary-search-tree/`},{key:`increasing-order-search-tree`,name:`Increasing Order Search Tree`,frequency:5,url:`https://leetcode.com/problems/increasing-order-search-tree/`},{key:`find-mode-in-binary-search-tree`,name:`Find Mode in Binary Search Tree`,frequency:4,url:`https://leetcode.com/problems/find-mode-in-binary-search-tree/`}],medium:[{key:`trim-a-binary-search-tree`,name:`Trim a Binary Search Tree`,frequency:3,url:`https://leetcode.com/problems/trim-a-binary-search-tree/`},{key:`delete-node-in-a-bst`,name:`Delete Node in a BST`,frequency:8,url:`https://leetcode.com/problems/delete-node-in-a-bst/`},{key:`kth-smallest-element-in-a-bst`,name:`Kth Smallest Element in a BST`,frequency:45,url:`https://leetcode.com/problems/kth-smallest-element-in-a-bst/`},{key:`my-calendar-i`,name:`My Calendar I`,frequency:27,url:`https://leetcode.com/problems/my-calendar-i/`},{key:`balance-a-binary-search-tree`,name:`Balance a Binary Search Tree`,frequency:17,url:`https://leetcode.com/problems/balance-a-binary-search-tree/`},{key:`binary-search-tree-iterator`,name:`Binary Search Tree Iterator`,frequency:21,url:`https://leetcode.com/problems/binary-search-tree-iterator/`}],hard:[{key:`contains-duplicate-iii`,name:`Contains Duplicate III`,frequency:29,url:`https://leetcode.com/problems/contains-duplicate-iii/`},{key:`range-module`,name:`Range Module`,frequency:16,url:`https://leetcode.com/problems/range-module/`},{key:`my-calendar-ii`,name:`My Calendar II`,frequency:59,url:`https://leetcode.com/problems/my-calendar-ii/`},{key:`count-of-smaller-numbers-after-self`,name:`Count of Smaller Numbers After Self`,frequency:36,url:`https://leetcode.com/problems/count-of-smaller-numbers-after-self/`},{key:`closest-binary-search-tree-value-ii`,name:`Closest Binary Search Tree Value II`,frequency:11,url:`https://leetcode.com/problems/closest-binary-search-tree-value-ii/`},{key:`design-skiplist`,name:`Design Skiplist`,frequency:10,url:`https://leetcode.com/problems/design-skiplist/`}]}},{slug:`tries`,title:`Tries`,tagline:`One node per character, prefixes shared.`,track:`Trees and ranges`,order:23,accent:`#facc15`,summary:`Prefix search, autocomplete, and the word-search problems that would otherwise rescan the dictionary for every candidate. The structure is simple; knowing when the shared prefixes pay for the node overhead is the part to practise.`,pattern:{spot:[`Prefixes, autocomplete, or one dictionary queried many times.`,`Many lookups against a word list that does not change.`,`The bits of an integer, walked from the top down.`],shape:`class Node { Node[] next = new Node[26]; boolean end; }
for (char c : word.toCharArray()) {
    int i = c - 'a';
    if (cur.next[i] == null) cur.next[i] = new Node();
    cur = cur.next[i];
}
cur.end = true;`,cost:`O(length) per insert or search`,watch:[`The end flag, or "car" is found inside "cartoon".`,`Cost is the word length, not the size of the dictionary.`,`Twenty-six children per node is a great deal of memory.`]},bands:{easy:[{key:`implement-trie-prefix-tree`,name:`Implement Trie (Prefix Tree)`,frequency:25,url:`https://leetcode.com/problems/implement-trie-prefix-tree/`},{key:`longest-word-in-dictionary`,name:`Longest Word in Dictionary`,frequency:17,url:`https://leetcode.com/problems/longest-word-in-dictionary/`},{key:`replace-words`,name:`Replace Words`,frequency:47,url:`https://leetcode.com/problems/replace-words/`},{key:`map-sum-pairs`,name:`Map Sum Pairs`,frequency:42,url:`https://leetcode.com/problems/map-sum-pairs/`},{key:`index-pairs-of-a-string`,name:`Index Pairs of a String`,frequency:35,url:`https://leetcode.com/problems/index-pairs-of-a-string/`},{key:`short-encoding-of-words`,name:`Short Encoding of Words`,frequency:5,url:`https://leetcode.com/problems/short-encoding-of-words/`}],medium:[{key:`design-add-and-search-words-data-structure`,name:`Design Add and Search Words Data Structure`,frequency:52,url:`https://leetcode.com/problems/design-add-and-search-words-data-structure/`},{key:`search-suggestions-system`,name:`Search Suggestions System`,frequency:44,url:`https://leetcode.com/problems/search-suggestions-system/`},{key:`camelcase-matching`,name:`Camelcase Matching`,frequency:6,url:`https://leetcode.com/problems/camelcase-matching/`},{key:`stream-of-characters`,name:`Stream of Characters`,frequency:5,url:`https://leetcode.com/problems/stream-of-characters/`},{key:`implement-magic-dictionary`,name:`Implement Magic Dictionary`,frequency:13,url:`https://leetcode.com/problems/implement-magic-dictionary/`},{key:`longest-word-in-dictionary-through-deleting`,name:`Longest Word in Dictionary through Deleting`,frequency:50,url:`https://leetcode.com/problems/longest-word-in-dictionary-through-deleting/`}],hard:[{key:`word-search-ii`,name:`Word Search II`,frequency:46,url:`https://leetcode.com/problems/word-search-ii/`},{key:`maximum-xor-of-two-numbers-in-an-array`,name:`Maximum XOR of Two Numbers in an Array`,frequency:59,url:`https://leetcode.com/problems/maximum-xor-of-two-numbers-in-an-array/`},{key:`palindrome-pairs`,name:`Palindrome Pairs`,frequency:17,url:`https://leetcode.com/problems/palindrome-pairs/`},{key:`count-pairs-with-xor-in-a-range`,name:`Count Pairs With XOR in a Range`,frequency:32,url:`https://leetcode.com/problems/count-pairs-with-xor-in-a-range/`},{key:`concatenated-words`,name:`Concatenated Words`,frequency:10,url:`https://leetcode.com/problems/concatenated-words/`},{key:`prefix-and-suffix-search`,name:`Prefix and Suffix Search`,frequency:40,url:`https://leetcode.com/problems/prefix-and-suffix-search/`}]}},{slug:`fenwick-tree-binary-indexed-tree`,title:`Fenwick tree`,tagline:`Prefix sums that survive an update.`,track:`Trees and ranges`,order:24,accent:`#38bdf8`,summary:`A prefix-sum array answers ranges instantly right up until something changes, and then it costs a rebuild. A Fenwick tree gives back a little of that speed to make the query and the update both logarithmic.`,pattern:{spot:[`Prefix sums, and the values keep changing.`,`Counting how many earlier values were smaller.`,`Both the query and the update have to be fast.`],shape:`void add(int i, int v) { for (; i <= n; i += i & -i) t[i] += v; }
int sum(int i)          { int s = 0; for (; i > 0; i -= i & -i) s += t[i]; return s; }`,cost:`O(log n) query and update`,watch:["It is one-indexed. `i & -i` is zero at index 0 and never moves.",`Only invertible operations. Sums yes, minima no.`,`Compress the coordinates first when the values are large.`]},bands:{easy:[{key:`range-sum-query-immutable`,name:`Range Sum Query - Immutable`,frequency:45,url:`https://leetcode.com/problems/range-sum-query-immutable/`},{key:`height-checker`,name:`Height Checker`,frequency:22,url:`https://leetcode.com/problems/height-checker/`},{key:`relative-sort-array`,name:`Relative Sort Array`,frequency:14,url:`https://leetcode.com/problems/relative-sort-array/`},{key:`count-good-triplets`,name:`Count Good Triplets`,frequency:40,url:`https://leetcode.com/problems/count-good-triplets/`},{key:`number-of-good-pairs`,name:`Number of Good Pairs`,frequency:59,url:`https://leetcode.com/problems/number-of-good-pairs/`},{key:`find-the-distance-value-between-two-arrays`,name:`Find the Distance Value Between Two Arrays`,frequency:15,url:`https://leetcode.com/problems/find-the-distance-value-between-two-arrays/`}],medium:[{key:`range-sum-query-mutable`,name:`Range Sum Query - Mutable`,frequency:22,url:`https://leetcode.com/problems/range-sum-query-mutable/`},{key:`range-sum-query-2d-mutable`,name:`Range Sum Query 2D - Mutable`,frequency:48,url:`https://leetcode.com/problems/range-sum-query-2d-mutable/`},{key:`corporate-flight-bookings`,name:`Corporate Flight Bookings`,frequency:20,url:`https://leetcode.com/problems/corporate-flight-bookings/`},{key:`count-number-of-teams`,name:`Count Number of Teams`,frequency:40,url:`https://leetcode.com/problems/count-number-of-teams/`},{key:`queries-on-a-permutation-with-key`,name:`Queries on a Permutation With Key`,frequency:5,url:`https://leetcode.com/problems/queries-on-a-permutation-with-key/`},{key:`my-calendar-iii`,name:`My Calendar III`,frequency:27,url:`https://leetcode.com/problems/my-calendar-iii/`}],hard:[{key:`count-of-smaller-numbers-after-self`,name:`Count of Smaller Numbers After Self`,frequency:31,url:`https://leetcode.com/problems/count-of-smaller-numbers-after-self/`},{key:`count-of-range-sum`,name:`Count of Range Sum`,frequency:52,url:`https://leetcode.com/problems/count-of-range-sum/`},{key:`create-sorted-array-through-instructions`,name:`Create Sorted Array through Instructions`,frequency:32,url:`https://leetcode.com/problems/create-sorted-array-through-instructions/`},{key:`number-of-pairs-satisfying-inequality`,name:`Number of Pairs Satisfying Inequality`,frequency:39,url:`https://leetcode.com/problems/number-of-pairs-satisfying-inequality/`},{key:`booking-concert-tickets-in-groups`,name:`Booking Concert Tickets in Groups`,frequency:44,url:`https://leetcode.com/problems/booking-concert-tickets-in-groups/`},{key:`maximum-sum-queries`,name:`Maximum Sum Queries`,frequency:53,url:`https://leetcode.com/problems/maximum-sum-queries/`}]}},{slug:`segment-tree`,title:`Segment tree`,tagline:`Any range, any operation, both directions.`,track:`Trees and ranges`,order:25,accent:`#60a5fa`,summary:`The general form of the same problem: a tree over ranges that answers a query and takes an update in logarithmic time, for any operation that combines. Lazy propagation is what makes updating a whole range affordable.`,pattern:{spot:[`Range queries and range updates on the same array.`,`Any operation that combines associatively.`,`A Fenwick tree cannot do it — min, max, gcd.`],shape:`int query(int node, int lo, int hi, int l, int r) {
    if (r < lo || hi < l) return IDENTITY;
    if (l <= lo && hi <= r) return t[node];
    int mid = (lo + hi) / 2;
    return combine(query(2*node, lo, mid, l, r),
                   query(2*node+1, mid+1, hi, l, r));
}`,cost:`O(log n) per op · 4n space`,watch:[`The identity must be neutral: 0 for sum, MAX_VALUE for min.`,`Size it 4n, not 2n.`,`Push the lazy value down before you descend.`]},bands:{easy:[{key:`range-sum-query-immutable`,name:`Range Sum Query - Immutable`,frequency:54,url:`https://leetcode.com/problems/range-sum-query-immutable/`},{key:`range-sum-query-2d-immutable`,name:`Range Sum Query 2D - Immutable`,frequency:19,url:`https://leetcode.com/problems/range-sum-query-2d-immutable/`},{key:`kth-largest-element-in-a-stream`,name:`Kth Largest Element in a Stream`,frequency:29,url:`https://leetcode.com/problems/kth-largest-element-in-a-stream/`},{key:`range-addition`,name:`Range Addition`,frequency:36,url:`https://leetcode.com/problems/range-addition/`},{key:`product-of-the-last-k-numbers`,name:`Product of the Last K Numbers`,frequency:49,url:`https://leetcode.com/problems/product-of-the-last-k-numbers/`},{key:`shortest-word-distance-ii`,name:`Shortest Word Distance II`,frequency:3,url:`https://leetcode.com/problems/shortest-word-distance-ii/`}],medium:[{key:`range-sum-query-mutable`,name:`Range Sum Query - Mutable`,frequency:15,url:`https://leetcode.com/problems/range-sum-query-mutable/`},{key:`range-sum-query-2d-mutable`,name:`Range Sum Query 2D - Mutable`,frequency:46,url:`https://leetcode.com/problems/range-sum-query-2d-mutable/`},{key:`describe-the-painting`,name:`Describe the Painting`,frequency:23,url:`https://leetcode.com/problems/describe-the-painting/`},{key:`count-number-of-rectangles-containing-each-point`,name:`Count Number of Rectangles Containing Each Point`,frequency:42,url:`https://leetcode.com/problems/count-number-of-rectangles-containing-each-point/`},{key:`corporate-flight-bookings`,name:`Corporate Flight Bookings`,frequency:12,url:`https://leetcode.com/problems/corporate-flight-bookings/`},{key:`car-pooling`,name:`Car Pooling`,frequency:31,url:`https://leetcode.com/problems/car-pooling/`}],hard:[{key:`longest-increasing-subsequence-ii`,name:`Longest Increasing Subsequence II`,frequency:41,url:`https://leetcode.com/problems/longest-increasing-subsequence-ii/`},{key:`falling-squares`,name:`Falling Squares`,frequency:36,url:`https://leetcode.com/problems/falling-squares/`},{key:`range-module`,name:`Range Module`,frequency:13,url:`https://leetcode.com/problems/range-module/`},{key:`my-calendar-iii`,name:`My Calendar III`,frequency:58,url:`https://leetcode.com/problems/my-calendar-iii/`},{key:`count-integers-in-intervals`,name:`Count Integers in Intervals`,frequency:33,url:`https://leetcode.com/problems/count-integers-in-intervals/`},{key:`handling-sum-queries-after-update`,name:`Handling Sum Queries After Update`,frequency:43,url:`https://leetcode.com/problems/handling-sum-queries-after-update/`}]}},{slug:`sparse-table`,title:`Sparse table`,tagline:`Immutable ranges, answered instantly.`,track:`Trees and ranges`,order:26,accent:`#8b6cff`,summary:`When the array never changes you can precompute overlapping power-of-two ranges and answer a minimum or a maximum in constant time. It is the right structure exactly when a segment tree would be more machinery than the question needs.`,pattern:{spot:[`Range minimum or maximum, and the array never changes.`,`Very many queries, each of which has to be O(1).`,`An idempotent operation — min, max, gcd, and, or.`],shape:`for (int k = 1; (1 << k) <= n; k++)
    for (int i = 0; i + (1 << k) <= n; i++)
        st[k][i] = Math.min(st[k-1][i], st[k-1][i + (1 << (k-1))]);`,cost:`O(n log n) build · O(1) query`,watch:[`Overlapping blocks double-count. Never for sums.`,`It cannot be updated. Use a segment tree if the data moves.`,`The build costs O(n log n) in both time and space.`]},bands:{easy:[{key:`range-sum-query-immutable`,name:`Range Sum Query - Immutable`,frequency:46,url:`https://leetcode.com/problems/range-sum-query-immutable/`},{key:`range-sum-query-2d-immutable`,name:`Range Sum Query 2D - Immutable`,frequency:44,url:`https://leetcode.com/problems/range-sum-query-2d-immutable/`},{key:`find-peak-element`,name:`Find Peak Element`,frequency:60,url:`https://leetcode.com/problems/find-peak-element/`},{key:`find-minimum-in-rotated-sorted-array`,name:`Find Minimum in Rotated Sorted Array`,frequency:22,url:`https://leetcode.com/problems/find-minimum-in-rotated-sorted-array/`},{key:`kth-largest-element-in-a-stream`,name:`Kth Largest Element in a Stream`,frequency:41,url:`https://leetcode.com/problems/kth-largest-element-in-a-stream/`},{key:`height-checker`,name:`Height Checker`,frequency:16,url:`https://leetcode.com/problems/height-checker/`}],medium:[{key:`trapping-rain-water`,name:`Trapping Rain Water`,frequency:27,url:`https://leetcode.com/problems/trapping-rain-water/`},{key:`product-of-array-except-self`,name:`Product of Array Except Self`,frequency:34,url:`https://leetcode.com/problems/product-of-array-except-self/`},{key:`range-addition`,name:`Range Addition`,frequency:49,url:`https://leetcode.com/problems/range-addition/`},{key:`range-sum-query-mutable`,name:`Range Sum Query - Mutable`,frequency:49,url:`https://leetcode.com/problems/range-sum-query-mutable/`},{key:`corporate-flight-bookings`,name:`Corporate Flight Bookings`,frequency:13,url:`https://leetcode.com/problems/corporate-flight-bookings/`},{key:`my-calendar-iii`,name:`My Calendar III`,frequency:3,url:`https://leetcode.com/problems/my-calendar-iii/`}],hard:[{key:`sliding-window-maximum`,name:`Sliding Window Maximum`,frequency:23,url:`https://leetcode.com/problems/sliding-window-maximum/`},{key:`sum-of-subarray-minimums`,name:`Sum of Subarray Minimums`,frequency:7,url:`https://leetcode.com/problems/sum-of-subarray-minimums/`},{key:`number-of-subarrays-with-bounded-maximum`,name:`Number of Subarrays with Bounded Maximum`,frequency:24,url:`https://leetcode.com/problems/number-of-subarrays-with-bounded-maximum/`},{key:`shortest-subarray-with-sum-at-least-k`,name:`Shortest Subarray with Sum at Least K`,frequency:5,url:`https://leetcode.com/problems/shortest-subarray-with-sum-at-least-k/`},{key:`count-of-range-sum`,name:`Count of Range Sum`,frequency:12,url:`https://leetcode.com/problems/count-of-range-sum/`},{key:`constrained-subsequence-sum`,name:`Constrained Subsequence Sum`,frequency:51,url:`https://leetcode.com/problems/constrained-subsequence-sum/`}]}},{slug:`heaps`,title:`Heaps`,tagline:`The next smallest, again and again.`,track:`Ordering and design`,order:27,accent:`#6baf8d`,summary:`A priority queue is the answer to top-k, to the k-way merge, and to the running median with two heaps facing each other. Sorting the whole input in order to take three of it is the mistake this replaces.`,pattern:{spot:[`"Top k", "the k-th largest", or a running median.`,`Merging k sorted things into one.`,`You need the best item again and again as things arrive.`],shape:`PriorityQueue<Integer> pq = new PriorityQueue<>();   // min-heap
for (int x : a) {
    pq.offer(x);
    if (pq.size() > k) pq.poll();    // keep only the k largest
}`,cost:`O(n log k) for top-k`,watch:[`A min-heap keeps the k largest. It reads like the opposite.`,"`PriorityQueue` is a min-heap, and iterating it is not sorted.","`remove(Object)` is O(n), not O(log n)."]},bands:{easy:[{key:`kth-largest-element-in-a-stream`,name:`Kth Largest Element in a Stream`,frequency:21,url:`https://leetcode.com/problems/kth-largest-element-in-a-stream/`},{key:`last-stone-weight`,name:`Last Stone Weight`,frequency:59,url:`https://leetcode.com/problems/last-stone-weight/`},{key:`sort-array-by-increasing-frequency`,name:`Sort Array by Increasing Frequency`,frequency:16,url:`https://leetcode.com/problems/sort-array-by-increasing-frequency/`},{key:`minimum-cost-to-connect-sticks`,name:`Minimum Cost to Connect Sticks`,frequency:37,url:`https://leetcode.com/problems/minimum-cost-to-connect-sticks/`},{key:`relative-ranks`,name:`Relative Ranks`,frequency:50,url:`https://leetcode.com/problems/relative-ranks/`},{key:`third-maximum-number`,name:`Third Maximum Number`,frequency:15,url:`https://leetcode.com/problems/third-maximum-number/`}],medium:[{key:`top-k-frequent-elements`,name:`Top K Frequent Elements`,frequency:6,url:`https://leetcode.com/problems/top-k-frequent-elements/`},{key:`k-closest-points-to-origin`,name:`K Closest Points to Origin`,frequency:33,url:`https://leetcode.com/problems/k-closest-points-to-origin/`},{key:`furthest-building-you-can-reach`,name:`Furthest Building You Can Reach`,frequency:57,url:`https://leetcode.com/problems/furthest-building-you-can-reach/`},{key:`task-scheduler`,name:`Task Scheduler`,frequency:19,url:`https://leetcode.com/problems/task-scheduler/`},{key:`reorganize-string`,name:`Reorganize String`,frequency:56,url:`https://leetcode.com/problems/reorganize-string/`},{key:`car-pooling`,name:`Car Pooling`,frequency:30,url:`https://leetcode.com/problems/car-pooling/`}],hard:[{key:`find-median-from-data-stream`,name:`Find Median from Data Stream`,frequency:44,url:`https://leetcode.com/problems/find-median-from-data-stream/`},{key:`sliding-window-median`,name:`Sliding Window Median`,frequency:4,url:`https://leetcode.com/problems/sliding-window-median/`},{key:`merge-k-sorted-lists`,name:`Merge k Sorted Lists`,frequency:3,url:`https://leetcode.com/problems/merge-k-sorted-lists/`},{key:`minimum-cost-to-hire-k-workers`,name:`Minimum Cost to Hire K Workers`,frequency:24,url:`https://leetcode.com/problems/minimum-cost-to-hire-k-workers/`},{key:`find-k-th-smallest-pair-distance`,name:`Find K-th Smallest Pair Distance`,frequency:15,url:`https://leetcode.com/problems/find-k-th-smallest-pair-distance/`},{key:`single-threaded-cpu`,name:`Single-Threaded CPU`,frequency:56,url:`https://leetcode.com/problems/single-threaded-cpu/`}]}},{slug:`intervals`,title:`Intervals`,tagline:`Sort by start, then look at the overlap.`,track:`Ordering and design`,order:28,accent:`#34d399`,summary:`Merging, inserting, counting the rooms, finding the gap. Nearly all of them open with a sort and turn on a single comparison — and the sweep-line versions are what the same problem becomes when the intervals arrive as events.`,pattern:{spot:[`Pairs of start and end, and a question about overlap.`,`Merging, inserting, or counting how many are on at once.`,`Fitting as many as possible in without a clash.`],shape:`Arrays.sort(iv, (x, y) -> Integer.compare(x[0], y[0]));
for (int[] cur : iv)
    if (!out.isEmpty() && cur[0] <= out.getLast()[1])
        out.getLast()[1] = Math.max(out.getLast()[1], cur[1]);
    else out.add(cur);`,cost:`O(n log n) — the sort`,watch:["Decide whether touching ends overlap — `<=` or `<` — first.",`Sort by start to merge; sort by end to fit the most in.`,"Take the `max` of the ends, or a nested interval is truncated."]},bands:{easy:[{key:`summary-ranges`,name:`Summary Ranges`,frequency:14,url:`https://leetcode.com/problems/summary-ranges/`},{key:`meeting-rooms`,name:`Meeting Rooms`,frequency:60,url:`https://leetcode.com/problems/meeting-rooms/`},{key:`find-right-interval`,name:`Find Right Interval`,frequency:16,url:`https://leetcode.com/problems/find-right-interval/`},{key:`remove-covered-intervals`,name:`Remove Covered Intervals`,frequency:22,url:`https://leetcode.com/problems/remove-covered-intervals/`},{key:`teemo-attacking`,name:`Teemo Attacking`,frequency:48,url:`https://leetcode.com/problems/teemo-attacking/`},{key:`partition-labels`,name:`Partition Labels`,frequency:40,url:`https://leetcode.com/problems/partition-labels/`}],medium:[{key:`merge-intervals`,name:`Merge Intervals`,frequency:29,url:`https://leetcode.com/problems/merge-intervals/`},{key:`insert-interval`,name:`Insert Interval`,frequency:13,url:`https://leetcode.com/problems/insert-interval/`},{key:`non-overlapping-intervals`,name:`Non-overlapping Intervals`,frequency:30,url:`https://leetcode.com/problems/non-overlapping-intervals/`},{key:`meeting-rooms-ii`,name:`Meeting Rooms II`,frequency:25,url:`https://leetcode.com/problems/meeting-rooms-ii/`},{key:`interval-list-intersections`,name:`Interval List Intersections`,frequency:30,url:`https://leetcode.com/problems/interval-list-intersections/`},{key:`car-pooling`,name:`Car Pooling`,frequency:28,url:`https://leetcode.com/problems/car-pooling/`}],hard:[{key:`employee-free-time`,name:`Employee Free Time`,frequency:39,url:`https://leetcode.com/problems/employee-free-time/`},{key:`minimum-interval-to-include-each-query`,name:`Minimum Interval to Include Each Query`,frequency:45,url:`https://leetcode.com/problems/minimum-interval-to-include-each-query/`},{key:`my-calendar-iii`,name:`My Calendar III`,frequency:53,url:`https://leetcode.com/problems/my-calendar-iii/`},{key:`minimum-number-of-arrows-to-burst-balloons`,name:`Minimum Number of Arrows to Burst Balloons`,frequency:55,url:`https://leetcode.com/problems/minimum-number-of-arrows-to-burst-balloons/`},{key:`minimum-number-of-taps-to-open-to-water-a-garden`,name:`Minimum Number of Taps to Open to Water a Garden`,frequency:27,url:`https://leetcode.com/problems/minimum-number-of-taps-to-open-to-water-a-garden/`},{key:`maximum-number-of-events-that-can-be-attended-ii`,name:`Maximum Number of Events That Can Be Attended II`,frequency:10,url:`https://leetcode.com/problems/maximum-number-of-events-that-can-be-attended-ii/`}]}},{slug:`data-structure-design`,title:`Data structure design`,tagline:`Build the thing, and meet the time limit.`,track:`Ordering and design`,order:29,accent:`#f97316`,summary:`LRU caches, randomised sets, iterators and rate limiters. The question is never one algorithm — it is which two structures to hold at once so that every operation the specification lists comes out constant time.`,pattern:{spot:[`The question names a class and lists its operations.`,`Every operation comes with a required cost.`,`No single container gives you all of them.`],shape:`class LRU {                       // one lookup + one order
    Map<Integer, Node> index = new HashMap<>();
    Node head, tail;                 // most recent .. least recent
    int get(int k) { Node n = index.get(k); moveToFront(n); return n.val; }
}`,cost:`O(1) per listed operation`,watch:[`The answer is two structures kept in step, not one clever one.`,`Write the cost of each method down before you write the method.`,`Removal in O(1) almost always means swap with the last.`]},bands:{easy:[{key:`implement-queue-using-stacks`,name:`Implement Queue using Stacks`,frequency:12,url:`https://leetcode.com/problems/implement-queue-using-stacks/`},{key:`design-hashmap`,name:`Design HashMap`,frequency:59,url:`https://leetcode.com/problems/design-hashmap/`},{key:`implement-stack-using-queues`,name:`Implement Stack using Queues`,frequency:14,url:`https://leetcode.com/problems/implement-stack-using-queues/`},{key:`design-parking-system`,name:`Design Parking System`,frequency:3,url:`https://leetcode.com/problems/design-parking-system/`},{key:`design-hashset`,name:`Design HashSet`,frequency:16,url:`https://leetcode.com/problems/design-hashset/`},{key:`peeking-iterator`,name:`Peeking Iterator`,frequency:7,url:`https://leetcode.com/problems/peeking-iterator/`}],medium:[{key:`lru-cache`,name:`LRU Cache`,frequency:48,url:`https://leetcode.com/problems/lru-cache/`},{key:`min-stack`,name:`Min Stack`,frequency:24,url:`https://leetcode.com/problems/min-stack/`},{key:`time-based-key-value-store`,name:`Time Based Key-Value Store`,frequency:48,url:`https://leetcode.com/problems/time-based-key-value-store/`},{key:`insert-delete-getrandom-o1`,name:`Insert Delete GetRandom O(1)`,frequency:22,url:`https://leetcode.com/problems/insert-delete-getrandom-o1/`},{key:`design-underground-system`,name:`Design Underground System`,frequency:5,url:`https://leetcode.com/problems/design-underground-system/`},{key:`design-circular-queue`,name:`Design Circular Queue`,frequency:29,url:`https://leetcode.com/problems/design-circular-queue/`}],hard:[{key:`maximum-frequency-stack`,name:`Maximum Frequency Stack`,frequency:49,url:`https://leetcode.com/problems/maximum-frequency-stack/`},{key:`lfu-cache`,name:`LFU Cache`,frequency:35,url:`https://leetcode.com/problems/lfu-cache/`},{key:`design-search-autocomplete-system`,name:`Design Search Autocomplete System`,frequency:46,url:`https://leetcode.com/problems/design-search-autocomplete-system/`},{key:`design-twitter`,name:`Design Twitter`,frequency:55,url:`https://leetcode.com/problems/design-twitter/`},{key:`snapshot-array`,name:`Snapshot Array`,frequency:29,url:`https://leetcode.com/problems/snapshot-array/`},{key:`design-a-text-editor`,name:`Design a Text Editor`,frequency:34,url:`https://leetcode.com/problems/design-a-text-editor/`}]}},{slug:`greedy`,title:`Greedy`,tagline:`Take the best now, then prove it was safe.`,track:`Ordering and design`,order:30,accent:`#a78bfa`,summary:`The shortest solutions on the sheet and the easiest to get quietly wrong. The code is a sort and a loop. The work is the argument that the locally best choice cannot have cost you the global answer.`,pattern:{spot:[`Maximise or minimise, and one local choice looks obviously right.`,`Sorting first makes the choice obvious.`,`Repeatedly take the best thing available.`],shape:`Arrays.sort(items, byBestFirst);
for (T item : items) if (fits(item)) take(item);`,cost:`O(n log n) — a sort and a pass`,watch:[`Prove the choice is safe, or find the counter-example.`,`Try to break your own rule on a small input before trusting it.`,`When it does fail, the answer is dynamic programming.`]},bands:{easy:[{key:`assign-cookies`,name:`Assign Cookies`,frequency:25,url:`https://leetcode.com/problems/assign-cookies/`},{key:`lemonade-change`,name:`Lemonade Change`,frequency:31,url:`https://leetcode.com/problems/lemonade-change/`},{key:`jump-game`,name:`Jump Game`,frequency:42,url:`https://leetcode.com/problems/jump-game/`},{key:`maximum-units-on-a-truck`,name:`Maximum Units on a Truck`,frequency:11,url:`https://leetcode.com/problems/maximum-units-on-a-truck/`},{key:`largest-perimeter-triangle`,name:`Largest Perimeter Triangle`,frequency:42,url:`https://leetcode.com/problems/largest-perimeter-triangle/`},{key:`best-time-to-buy-and-sell-stock-ii`,name:`Best Time to Buy and Sell Stock II`,frequency:25,url:`https://leetcode.com/problems/best-time-to-buy-and-sell-stock-ii/`}],medium:[{key:`jump-game-ii`,name:`Jump Game II`,frequency:38,url:`https://leetcode.com/problems/jump-game-ii/`},{key:`gas-station`,name:`Gas Station`,frequency:37,url:`https://leetcode.com/problems/gas-station/`},{key:`non-overlapping-intervals`,name:`Non-overlapping Intervals`,frequency:20,url:`https://leetcode.com/problems/non-overlapping-intervals/`},{key:`hand-of-straights`,name:`Hand of Straights`,frequency:41,url:`https://leetcode.com/problems/hand-of-straights/`},{key:`wiggle-subsequence`,name:`Wiggle Subsequence`,frequency:35,url:`https://leetcode.com/problems/wiggle-subsequence/`},{key:`task-scheduler`,name:`Task Scheduler`,frequency:3,url:`https://leetcode.com/problems/task-scheduler/`}],hard:[{key:`candy`,name:`Candy`,frequency:18,url:`https://leetcode.com/problems/candy/`},{key:`ipo`,name:`IPO`,frequency:39,url:`https://leetcode.com/problems/ipo/`},{key:`remove-duplicate-letters`,name:`Remove Duplicate Letters`,frequency:37,url:`https://leetcode.com/problems/remove-duplicate-letters/`},{key:`patching-array`,name:`Patching Array`,frequency:23,url:`https://leetcode.com/problems/patching-array/`},{key:`minimum-cost-to-hire-k-workers`,name:`Minimum Cost to Hire K Workers`,frequency:17,url:`https://leetcode.com/problems/minimum-cost-to-hire-k-workers/`},{key:`create-maximum-number`,name:`Create Maximum Number`,frequency:25,url:`https://leetcode.com/problems/create-maximum-number/`}]}},{slug:`graphs`,title:`Graphs`,tagline:`DFS, BFS, and the shortest path.`,track:`Graphs, DP and the rest`,order:31,accent:`#f472b6`,summary:`Islands, course schedules, and the shortest route across a grid. Once you can see the input as nodes and edges, most of these are one of three traversals with the right thing recorded on the way through.`,pattern:{spot:[`Things connected to things — grids, courses, routes, islands.`,`Shortest path, reachability, or ordering under dependencies.`,`A grid where you may step to a neighbour.`],shape:`Queue<Integer> q = new ArrayDeque<>(List.of(src));
seen[src] = true;
while (!q.isEmpty()) {
    int u = q.poll();
    for (int v : adj[u]) if (!seen[v]) { seen[v] = true; q.add(v); }
}`,cost:`O(V + E) · O(E log V) Dijkstra`,watch:[`Mark as seen when you enqueue, not when you dequeue.`,`BFS is the shortest path only while the edges are unweighted.`,`Cycle detection is a different check for directed and undirected.`]},bands:{easy:[{key:`flood-fill`,name:`Flood Fill`,frequency:57,url:`https://leetcode.com/problems/flood-fill/`},{key:`course-schedule`,name:`Course Schedule`,frequency:48,url:`https://leetcode.com/problems/course-schedule/`},{key:`find-if-path-exists-in-graph`,name:`Find if Path Exists in Graph`,frequency:55,url:`https://leetcode.com/problems/find-if-path-exists-in-graph/`},{key:`number-of-provinces`,name:`Number of Provinces`,frequency:56,url:`https://leetcode.com/problems/number-of-provinces/`},{key:`find-the-town-judge`,name:`Find the Town Judge`,frequency:50,url:`https://leetcode.com/problems/find-the-town-judge/`},{key:`island-perimeter`,name:`Island Perimeter`,frequency:4,url:`https://leetcode.com/problems/island-perimeter/`}],medium:[{key:`number-of-islands`,name:`Number of Islands`,frequency:4,url:`https://leetcode.com/problems/number-of-islands/`},{key:`clone-graph`,name:`Clone Graph`,frequency:35,url:`https://leetcode.com/problems/clone-graph/`},{key:`rotting-oranges`,name:`Rotting Oranges`,frequency:52,url:`https://leetcode.com/problems/rotting-oranges/`},{key:`course-schedule-ii`,name:`Course Schedule II`,frequency:16,url:`https://leetcode.com/problems/course-schedule-ii/`},{key:`accounts-merge`,name:`Accounts Merge`,frequency:6,url:`https://leetcode.com/problems/accounts-merge/`},{key:`network-delay-time`,name:`Network Delay Time`,frequency:36,url:`https://leetcode.com/problems/network-delay-time/`},{key:`pacific-atlantic-water-flow`,name:`Pacific Atlantic Water Flow`,frequency:13,url:`https://leetcode.com/problems/pacific-atlantic-water-flow/`}],hard:[{key:`critical-connections-in-a-network`,name:`Critical Connections in a Network`,frequency:59,url:`https://leetcode.com/problems/critical-connections-in-a-network/`},{key:`word-ladder`,name:`Word Ladder`,frequency:55,url:`https://leetcode.com/problems/word-ladder/`},{key:`alien-dictionary`,name:`Alien Dictionary`,frequency:30,url:`https://leetcode.com/problems/alien-dictionary/`},{key:`swim-in-rising-water`,name:`Swim in Rising Water`,frequency:4,url:`https://leetcode.com/problems/swim-in-rising-water/`},{key:`remove-max-number-of-edges-to-keep-graph-fully-traversable`,name:`Remove Max Number of Edges to Keep Graph Fully Traversable`,frequency:40,url:`https://leetcode.com/problems/remove-max-number-of-edges-to-keep-graph-fully-traversable/`},{key:`bus-routes`,name:`Bus Routes`,frequency:25,url:`https://leetcode.com/problems/bus-routes/`}]}},{slug:`dynamic-programming`,title:`Dynamic programming`,tagline:`The recursion, with the repeats remembered.`,track:`Graphs, DP and the rest`,order:32,accent:`#facc15`,summary:`One dimension, then grids, then strings. Every one of these is a recursion you already know plus a table that stops it solving the same subproblem a thousand times. Naming the state is the difficulty; the table is bookkeeping.`,pattern:{spot:[`Overlapping subproblems — a recursion that recomputes the same thing.`,`Count the ways, or the best over all the choices.`,`A choice at each step, and no greedy rule that is safe.`],shape:`int[] dp = new int[n + 1];
dp[0] = base;
for (int i = 1; i <= n; i++)
    dp[i] = combine(dp[i - 1], dp[i - 2]);`,cost:`states × work per state`,watch:[`Name the state in one sentence before you write anything.`,`The 0/1 knapsack's inner loop runs backwards.`,`Coins outside gives combinations; coins inside gives permutations.`]},bands:{easy:[{key:`climbing-stairs`,name:`Climbing Stairs`,frequency:33,url:`https://leetcode.com/problems/climbing-stairs/`},{key:`min-cost-climbing-stairs`,name:`Min Cost Climbing Stairs`,frequency:33,url:`https://leetcode.com/problems/min-cost-climbing-stairs/`},{key:`unique-paths`,name:`Unique Paths`,frequency:30,url:`https://leetcode.com/problems/unique-paths/`},{key:`best-time-to-buy-and-sell-stock`,name:`Best Time to Buy and Sell Stock`,frequency:41,url:`https://leetcode.com/problems/best-time-to-buy-and-sell-stock/`},{key:`n-th-tribonacci-number`,name:`N-th Tribonacci Number`,frequency:45,url:`https://leetcode.com/problems/n-th-tribonacci-number/`},{key:`divisor-game`,name:`Divisor Game`,frequency:33,url:`https://leetcode.com/problems/divisor-game/`}],medium:[{key:`house-robber`,name:`House Robber`,frequency:15,url:`https://leetcode.com/problems/house-robber/`},{key:`coin-change`,name:`Coin Change`,frequency:60,url:`https://leetcode.com/problems/coin-change/`},{key:`longest-increasing-subsequence`,name:`Longest Increasing Subsequence`,frequency:26,url:`https://leetcode.com/problems/longest-increasing-subsequence/`},{key:`longest-common-subsequence`,name:`Longest Common Subsequence`,frequency:36,url:`https://leetcode.com/problems/longest-common-subsequence/`},{key:`minimum-path-sum`,name:`Minimum Path Sum`,frequency:24,url:`https://leetcode.com/problems/minimum-path-sum/`},{key:`best-time-to-buy-and-sell-stock-with-cooldown`,name:`Best Time to Buy and Sell Stock with Cooldown`,frequency:28,url:`https://leetcode.com/problems/best-time-to-buy-and-sell-stock-with-cooldown/`},{key:`house-robber-ii`,name:`House Robber II`,frequency:44,url:`https://leetcode.com/problems/house-robber-ii/`}],hard:[{key:`edit-distance`,name:`Edit Distance`,frequency:46,url:`https://leetcode.com/problems/edit-distance/`},{key:`burst-balloons`,name:`Burst Balloons`,frequency:56,url:`https://leetcode.com/problems/burst-balloons/`},{key:`regular-expression-matching`,name:`Regular Expression Matching`,frequency:51,url:`https://leetcode.com/problems/regular-expression-matching/`},{key:`best-time-to-buy-and-sell-stock-iii`,name:`Best Time to Buy and Sell Stock III`,frequency:21,url:`https://leetcode.com/problems/best-time-to-buy-and-sell-stock-iii/`},{key:`shortest-path-visiting-all-nodes`,name:`Shortest Path Visiting All Nodes`,frequency:33,url:`https://leetcode.com/problems/shortest-path-visiting-all-nodes/`},{key:`dungeon-game`,name:`Dungeon Game`,frequency:55,url:`https://leetcode.com/problems/dungeon-game/`}]}},{slug:`maths-geometry`,title:`Maths and geometry`,tagline:`GCD, modular arithmetic, and points.`,track:`Graphs, DP and the rest`,order:33,accent:`#38bdf8`,summary:`Number theory, a little combinatorics, and the geometry that turns up in coordinate problems. Small, self-contained, and the category most likely to come down to remembering one identity.`,pattern:{spot:[`Divisibility, primes, or combinations.`,`Points, lines and areas on a plane.`,`An answer asked for modulo 1e9+7.`],shape:`long gcd(long a, long b) { return b == 0 ? a : gcd(b, a % b); }
// lcm(a, b) == a / gcd(a, b) * b   — divide first, or it overflows`,cost:`O(log n) gcd · O(n log log n) sieve`,watch:["Overflow. Multiply in `long` and take the modulus at every step.",`Avoid floating point wherever the answer is exact.`,`Use the cross product, not slopes — slopes divide by zero.`]},bands:{easy:[{key:`palindrome-number`,name:`Palindrome Number`,frequency:49,url:`https://leetcode.com/problems/palindrome-number/`},{key:`power-of-two`,name:`Power of Two`,frequency:49,url:`https://leetcode.com/problems/power-of-two/`},{key:`count-the-number-of-prime-numbers-less-than-n`,name:`Count Primes`,frequency:32,url:`https://leetcode.com/problems/count-the-number-of-prime-numbers-less-than-n/`},{key:`ugly-number`,name:`Ugly Number`,frequency:53,url:`https://leetcode.com/problems/ugly-number/`},{key:`excel-sheet-column-number`,name:`Excel Sheet Column Number`,frequency:47,url:`https://leetcode.com/problems/excel-sheet-column-number/`},{key:`strobogrammatic-number`,name:`Strobogrammatic Number`,frequency:47,url:`https://leetcode.com/problems/strobogrammatic-number/`}],medium:[{key:`reverse-integer`,name:`Reverse Integer`,frequency:22,url:`https://leetcode.com/problems/reverse-integer/`},{key:`perfect-squares`,name:`Perfect Squares`,frequency:49,url:`https://leetcode.com/problems/perfect-squares/`},{key:`factorial-trailing-zeroes`,name:`Factorial Trailing Zeroes`,frequency:23,url:`https://leetcode.com/problems/factorial-trailing-zeroes/`},{key:`integer-break`,name:`Integer Break`,frequency:44,url:`https://leetcode.com/problems/integer-break/`},{key:`nth-digit`,name:`Nth Digit`,frequency:46,url:`https://leetcode.com/problems/nth-digit/`},{key:`rectangle-area`,name:`Rectangle Area`,frequency:51,url:`https://leetcode.com/problems/rectangle-area/`}],hard:[{key:`max-points-on-a-line`,name:`Max Points on a Line`,frequency:4,url:`https://leetcode.com/problems/max-points-on-a-line/`},{key:`largest-rectangle-in-histogram`,name:`Largest Rectangle in Histogram`,frequency:24,url:`https://leetcode.com/problems/largest-rectangle-in-histogram/`},{key:`minimum-area-rectangle`,name:`Minimum Area Rectangle`,frequency:54,url:`https://leetcode.com/problems/minimum-area-rectangle/`},{key:`integer-to-english-words`,name:`Integer to English Words`,frequency:4,url:`https://leetcode.com/problems/integer-to-english-words/`},{key:`numbers-with-repeated-digits`,name:`Numbers With Repeated Digits`,frequency:59,url:`https://leetcode.com/problems/numbers-with-repeated-digits/`},{key:`k-th-smallest-in-lexicographical-order`,name:`K-th Smallest in Lexicographical Order`,frequency:54,url:`https://leetcode.com/problems/k-th-smallest-in-lexicographical-order/`}]}},{slug:`advanced-topics`,title:`Advanced topics`,tagline:`The ones that need an algorithm by name.`,track:`Graphs, DP and the rest`,order:34,accent:`#60a5fa`,summary:`String matching, union-find, topological order, and the rest of the algorithms with somebody’s name attached. Rarely the first question you are asked, and often the one that decides whether there is another round.`,pattern:{spot:[`A named algorithm is the intended answer.`,`Union-find, KMP, a rolling hash, or a topological order.`,`The constraints have ruled out everything simpler.`],shape:`int find(int x) { return p[x] == x ? x : (p[x] = find(p[x])); }
void union(int a, int b) { p[find(a)] = find(b); }`,cost:`union-find ~O(1) · KMP O(n + m)`,watch:[`Recognising which one is wanted is the whole difficulty.`,`The implementations are short once you know the name.`,`Most have a library equivalent that you are not allowed to use.`]},bands:{easy:[{key:`find-pivot-index`,name:`Find Pivot Index`,frequency:29,url:`https://leetcode.com/problems/find-pivot-index/`},{key:`running-sum-of-1d-array`,name:`Running Sum of 1d Array`,frequency:45,url:`https://leetcode.com/problems/running-sum-of-1d-array/`},{key:`power-of-two`,name:`Power of Two`,frequency:24,url:`https://leetcode.com/problems/power-of-two/`},{key:`number-of-1-bits`,name:`Number of 1 Bits`,frequency:34,url:`https://leetcode.com/problems/number-of-1-bits/`},{key:`contains-duplicate-ii`,name:`Contains Duplicate II`,frequency:5,url:`https://leetcode.com/problems/contains-duplicate-ii/`},{key:`sqrtx`,name:`Sqrt(x)`,frequency:15,url:`https://leetcode.com/problems/sqrtx/`}],medium:[{key:`range-sum-query-mutable`,name:`Range Sum Query - Mutable`,frequency:26,url:`https://leetcode.com/problems/range-sum-query-mutable/`},{key:`my-calendar-i`,name:`My Calendar I`,frequency:3,url:`https://leetcode.com/problems/my-calendar-i/`},{key:`subarrays-with-k-different-integers`,name:`Subarrays with K Different Integers`,frequency:25,url:`https://leetcode.com/problems/subarrays-with-k-different-integers/`},{key:`bitwise-ors-of-subarrays`,name:`Bitwise ORs of Subarrays`,frequency:28,url:`https://leetcode.com/problems/bitwise-ors-of-subarrays/`},{key:`find-all-anagrams-in-a-string`,name:`Find All Anagrams in a String`,frequency:30,url:`https://leetcode.com/problems/find-all-anagrams-in-a-string/`},{key:`continuous-subarray-sum`,name:`Continuous Subarray Sum`,frequency:3,url:`https://leetcode.com/problems/continuous-subarray-sum/`}],hard:[{key:`count-of-smaller-numbers-after-self`,name:`Count of Smaller Numbers After Self`,frequency:15,url:`https://leetcode.com/problems/count-of-smaller-numbers-after-self/`},{key:`the-skyline-problem`,name:`The Skyline Problem`,frequency:58,url:`https://leetcode.com/problems/the-skyline-problem/`},{key:`longest-duplicate-substring`,name:`Longest Duplicate Substring`,frequency:21,url:`https://leetcode.com/problems/longest-duplicate-substring/`},{key:`count-of-range-sum`,name:`Count of Range Sum`,frequency:4,url:`https://leetcode.com/problems/count-of-range-sum/`},{key:`sliding-window-maximum`,name:`Sliding Window Maximum`,frequency:30,url:`https://leetcode.com/problems/sliding-window-maximum/`},{key:`median-of-two-sorted-arrays`,name:`Median of Two Sorted Arrays`,frequency:40,url:`https://leetcode.com/problems/median-of-two-sorted-arrays/`}]}}]},wa={all:{label:`All`,blurb:`Every problem in the topic.`},easy:{label:`Easy`,blurb:`Where the pattern is introduced.`},medium:{label:`Medium`,blurb:`The shape an interview usually asks for.`},hard:{label:`Hard`,blurb:`The pattern, and the twist on top of it.`}},Ta=[`all`,`easy`,`medium`,`hard`],Ea=[`easy`,`medium`,`hard`],Da=e=>{let t=new Set;return e.filter(e=>t.has(e.key)?!1:(t.add(e.key),!0))},V=Ca.sheets.map(e=>{let t=Ea.map(t=>({id:t,problems:Da((e.bands[t]??[]).map(e=>({...e,band:t})))})).filter(e=>e.problems.length>0),n=(t,n)=>({id:t,...wa[t],problems:n,count:n.length,keys:[...new Set(n.map(e=>e.key))],headline:[...n].sort((e,t)=>t.frequency-e.frequency).slice(0,3).map(e=>e.name),route:t===`all`?`dsa/${e.slug}`:`dsa/${e.slug}/${t}`}),r=[n(`all`,Da(t.flatMap(e=>e.problems))),...t.map(e=>n(e.id,e.problems))],i=Object.fromEntries(r.map(e=>[e.id,e.count]));return{...e,bands:r,counts:i,route:`dsa/${e.slug}`}}).sort((e,t)=>e.order-t.order),Oa=Ca.tracks,ka=V.reduce((e,t)=>e+t.counts.all,0),Aa=V.flatMap(e=>e.bands[0].problems.map(e=>e.frequency)).sort((e,t)=>e-t),ja=e=>Aa[Math.floor(Aa.length*e)]??0,Ma=[ja(.25),ja(.5),ja(.75)],Na=[{id:1,label:`Low`,blurb:`The bottom quarter of the sheet.`},{id:2,label:`Medium`,blurb:`Below the middle.`},{id:3,label:`High`,blurb:`Above the middle.`},{id:4,label:`Top`,blurb:`The top quarter — the ones that keep coming up.`}],Pa=e=>e<Ma[0]?1:e<Ma[1]?2:e<Ma[2]?3:4,Fa=[40,30,20,10,5,1],Ia=e=>{let t=Fa.find(t=>e>=t);return t===void 0?String(e):`${t}+`},La={0:`Any number of companies`,2:`Medium and up`,3:`High and up`,4:`Top quarter only`},Ra={any:{label:`Any`,blurb:`Every topic, however far in you are.`},todo:{label:`Not started`,blurb:`Nothing ticked yet.`},doing:{label:`In progress`,blurb:`Started, not finished.`},done:{label:`Finished`,blurb:`Every problem in the band ticked.`}},za=({done:e,total:t})=>e===0?`todo`:e>=t?`done`:`doing`,Ba=e=>e?.size?V.reduce((t,n)=>t+Ga(n.bands[0],e),0):0,Va=new Map(V.flatMap(e=>e.bands[0].problems).map(e=>[e.key,e])),Ha=(e,t=`all`)=>{let n=Ya(e,t);return[e.title,e.tagline,e.summary,e.track,...n.problems.map(e=>e.name)].join(` `).toLowerCase()},Ua=(e=V)=>Ca.tracks.map(t=>({track:t,topics:e.filter(e=>e.track===t)})).filter(e=>e.topics.length>0),Wa=e=>V.reduce((t,n)=>t+(n.counts[e]??0),0),Ga=(e,t)=>!e||!t?.size?0:e.keys.reduce((e,n)=>e+ +!!t.has(n),0),Ka=(e,t,n)=>{let r=Ya(e,t),i=Ga(r,n);return{done:i,total:r.count,ratio:r.count?i/r.count:0}},qa={order:{label:`Sheet order`,compare:(e,t)=>e.order-t.order},size:{label:`Most problems`,compare:(e,t,n)=>t.counts[n]-e.counts[n]},least:{label:`Least done`,compare:(e,t)=>e.ratio-t.ratio||e.order-t.order},most:{label:`Furthest along`,compare:(e,t)=>t.ratio-e.ratio||e.order-t.order},name:{label:`A to Z`,compare:(e,t)=>e.title.localeCompare(t.title)}},Ja=e=>V.find(t=>t.slug===e)??null,Ya=(e,t)=>e?.bands.find(e=>e.id===t)??e?.bands[0]??null,Xa=[{slug:`scrollbar`,name:`Scrollbar`,tagline:`Design a scrollbar, take the CSS.`,summary:`Pick the colours and sizes, watch a real scrollbar change as you do, and copy the CSS out. Includes the standards-based fallback for browsers that do not implement the WebKit pseudo-elements.`,tags:[`CSS`,`Generator`],status:`live`,accent:`#6baf8d`},{slug:`colour`,name:`Colour`,tagline:`Pick it, convert it, build a scale from it.`,summary:`A real picker with hue and alpha, input in any format you can paste, and output in HEX, RGB, HSL, HSB and OKLCH. Plus contrast ratios against black and white with the WCAG grades, a tint scale stepped through OKLCH, and the five standard harmonies.`,tags:[`Colour`,`CSS`,`Accessibility`],status:`live`,accent:`#8b6cff`},{slug:`gradient`,name:`Gradient`,tagline:`Linear, radial and conic, with real stops.`,summary:`Drag stops along the bar, add one anywhere by double-clicking, and choose the colour space the browser interpolates through — OKLCH avoids the grey dip sRGB puts halfway between two saturated colours. Copies as two declarations so older browsers still get something.`,tags:[`Colour`,`CSS`,`Generator`],status:`live`,accent:`#f472b6`},{slug:`mesh`,name:`Mesh gradient`,tagline:`Drag the colours around until it looks right.`,summary:`Layered radial gradients you position by dragging, with per-point spread and a base colour underneath. Copies as plain CSS, and exports a PNG painted from the same numbers on a canvas — so the image is as sharp as the size you pick rather than as sharp as your screen.`,tags:[`Colour`,`CSS`,`Export`],status:`live`,accent:`#ff0080`},{slug:`pdf-to-image`,name:`PDF to image`,tagline:`Pages out as PNGs, nothing uploaded.`,summary:`Drop a PDF in and get each page as a PNG at the resolution you choose. It runs in the tab, so the file never leaves the machine — which is the whole reason to have it here rather than using one of the sites that wants your document.`,tags:[`Files`,`Offline`],status:`upcoming`,accent:`#f97316`},{slug:`box-shadow`,name:`Box shadow`,tagline:`Layered shadows that look like light.`,summary:`Stack several shadows with independent offset, blur, spread and opacity, preview them on a surface you can recolour, and copy the result.`,tags:[`CSS`,`Generator`],status:`upcoming`,accent:`#38bdf8`},{slug:`base64`,name:`Base64 and data URIs`,tagline:`Encode a file into a stylesheet.`,summary:`Convert text or a small file to Base64 and back, and wrap it as a data URI with the right MIME type — for the times an icon is easier inlined than hosted.`,tags:[`Encoding`,`Offline`],status:`upcoming`,accent:`#34d399`},{slug:`cron`,name:`Cron reader`,tagline:`What that schedule actually means.`,summary:`Type a cron expression and read it back in English, with the next several run times listed so you can check it does what you meant before it does not.`,tags:[`Scheduling`],status:`upcoming`,accent:`#f472b6`}].map(e=>({...e,route:`tools/${e.slug}`})),Za=e=>Xa.find(t=>t.slug===e&&t.status===`live`)??null,Qa={"":`the study notes`,study:`the study notes`,dsa:`the DSA sheet`,design:`the design principles`,advanced:`the Spring notes`,interview:`the interview questions`,articles:`the guides`,tools:`the tools`,apps:`the apps`};function $a(e){if(e.startsWith(`study/`)){let t=A(e.slice(6).split(`/`)[0]);if(t)return t.title}if(e.startsWith(`articles/`)){let t=Jt(e.slice(9));if(t)return t.title}if(e.startsWith(`design/`)){let t=la(e.slice(7).split(`/`)[0]);if(t)return t.title}if(e.startsWith(`interview/`)){let t=xa(e.slice(10).split(`/`)[0]);if(t)return t.title}if(e.startsWith(`advanced/`)){let t=ze(e.slice(9).split(`/`)[0]);if(t)return t.title}if(e.startsWith(`dsa/`)){let t=Ja(e.slice(4).split(`/`)[0]);if(t)return t.title}if(e.startsWith(`tools/`)){let t=Za(e.slice(6));if(t)return t.name}return Qa[e]??`where you were`}async function eo(e){try{return await navigator.clipboard.writeText(e),!0}catch{let t=document.createElement(`textarea`);t.value=e,t.setAttribute(`readonly`,``),t.style.position=`fixed`,t.style.opacity=`0`,document.body.appendChild(t),t.select();let n=!1;try{n=document.execCommand(`copy`)}catch{n=!1}return t.remove(),n}}var to=e=>e.replace(/[.*+?^${}()|[\]\\]/g,`\\$&`),no=e=>to(e).replace(/ +/g,`\\s+`).replace(/['’]/g,`['’]`),ro=e=>String(e).replace(/[&<>"]/g,e=>({"&":`&amp;`,"<":`&lt;`,">":`&gt;`,'"':`&quot;`})[e]),io=`pre, code, a, h1, h2, h3, h4, h5, h6, .term, .problem-link`;function ao(e){for(let t of e.querySelectorAll(`h2`))t.classList.add(`qa-level`),t.dataset.level=t.textContent.trim().toLowerCase();for(let t of[...e.querySelectorAll(`h3`)]){let e=document.createElement(`section`);e.className=`qa`;let n=document.createElement(`div`);n.className=`qa-answer`,t.replaceWith(e),t.classList.add(`qa-question`),e.append(t);let r=e.nextSibling;for(;r&&!(r.nodeType===1&&/^H[1-3]$/.test(r.tagName));){let e=r.nextSibling;n.append(r),r=e}e.append(n)}}function oo(e){let t=new Map(Di.map(e=>[e.toLowerCase(),e])),n=document.createTreeWalker(e,NodeFilter.SHOW_TEXT,{acceptNode(e){if(!e.nodeValue.trim())return NodeFilter.FILTER_REJECT;let t=e.parentElement;return!t||t.closest(io)?NodeFilter.FILTER_REJECT:NodeFilter.FILTER_ACCEPT}}),r=[];for(;n.nextNode();)r.push(n.currentNode);for(let e of r)for(;e&&t.size;){let n=null;for(let[r,i]of t){let t=RegExp(`\\b${no(i)}\\b`,`i`).exec(e.nodeValue);t&&(n===null||t.index<n.index)&&(n={index:t.index,text:t[0],key:r,term:i})}if(!n)break;let r=e.splitText(n.index),i=r.splitText(n.text.length),a=document.createElement(`span`);a.className=`term`,a.tabIndex=0,a.setAttribute(`role`,`button`),a.dataset.term=n.term,a.setAttribute(`aria-label`,`${n.term}: ${Ei[n.term]}`),a.textContent=n.text,r.replaceWith(a),t.delete(n.key),e=i}}function so(e){for(let t of e.querySelectorAll(`pre`)){if(t.classList.contains(`code-expected`))continue;let e=document.createElement(`div`);e.className=`code-actions`;let n=t.dataset.file;n&&e.insertAdjacentHTML(`beforeend`,`<button type="button" class="code-action code-save" data-action="save">Save ${ro(n)}</button>`),t.dataset.output!==void 0&&(e.insertAdjacentHTML(`beforeend`,`<button type="button" class="code-action code-run" data-action="run" title="Replay the output this program produced"><span class="play" aria-hidden="true"></span>Run</button>`),t.insertAdjacentHTML(`afterend`,`<pre class="code-output" hidden aria-live="polite"></pre>`)),e.insertAdjacentHTML(`beforeend`,`<button type="button" class="code-action code-copy" data-action="copy">Copy</button>`),t.appendChild(e)}}function co(e){for(let t of e.querySelectorAll(`img`)){if(t.closest(`figure`))continue;let e=document.createElement(`figure`);e.className=`figure`,(t.parentElement?.tagName===`P`&&t.parentElement.childNodes.length===1?t.parentElement:t).replaceWith(e),e.append(t),t.classList.add(`figure-img`),t.loading=`lazy`,t.decoding=`async`,t.dataset.action=`preview`,e.insertAdjacentHTML(`beforeend`,`<div class="figure-actions"><button type="button" class="code-action figure-preview" data-action="preview" title="Open ${ro(t.alt||`this`)} larger">Preview</button></div>`)}}function lo(e){for(let t of e.querySelectorAll(`a[href^="problem:"]`)){let e=Va.get(t.getAttribute(`href`).slice(8)),n=t.textContent.trim()||e?.name;if(!e?.url){let e=document.createElement(`span`);e.className=`problem-link is-plain`,e.textContent=n??t.textContent,t.replaceWith(e);continue}t.href=e.url,t.target=`_blank`,t.rel=`noreferrer`,t.className=`problem-link`,t.title=`${e.name} — opens the problem`,t.textContent=n}}function uo(e,t=``){let n=document.createElement(`div`);return n.innerHTML=e,t===`interview`&&ao(n),lo(n),oo(n),so(n),co(n),n.innerHTML}var H=e=>String(e).replace(/\.java$/,``),U=/\b(Arrays|List|ArrayList|LinkedList|Map|HashMap|TreeMap|Set|HashSet|TreeSet|Deque|ArrayDeque|Queue|PriorityQueue|Collections|Comparator|Iterator|Optional|Random|Scanner|StringJoiner|IntStream|Stream)\b/,fo=e=>String(e).split(/\r?\n/).map(e=>e.trim()).filter(e=>e&&!e.startsWith(`//`)),po=e=>`"${e.replace(/\\/g,`\\\\`).replace(/"/g,`\\"`)}"`,mo=(e,t)=>e.endsWith(`;`)?`        ${e}`:`        System.out.println(${po(`${e.padEnd(t)} -> `)} + (${e}));`;function ho(e,t,n){let r=H(e),i=Array.isArray(n)?n:fo(n),a=Math.max(0,...i.filter(e=>!e.endsWith(`;`)).map(e=>e.length)),o=`${t}\n${i.join(`
`)}`;return`${U.test(o)?`import java.util.*;

`:``}public class ${r} {\n\n${t.replace(/\s+$/,``).split(`
`).map(e=>e.trim()?`    ${e}`:``).join(`
`)}\n\n    public static void main(String[] args) {\n${i.map(e=>mo(e,a)).join(`
`)}\n    }\n}\n`}function go({count:e=3}){return Array.from({length:e},(t,n)=>(0,P.jsx)(`div`,{className:`skeleton skeleton-line ${n===e-1?`is-medium`:``}`},n))}function _o(){return(0,P.jsxs)(`div`,{className:`prose skeleton-prose`,role:`status`,"aria-label":`Loading the page`,children:[(0,P.jsx)(`div`,{className:`skeleton-cat`,"aria-hidden":`true`,children:(0,P.jsx)(bt,{name:`cat-loading`,size:128})}),(0,P.jsx)(`div`,{className:`skeleton skeleton-title`}),(0,P.jsx)(go,{count:4}),(0,P.jsx)(`div`,{className:`skeleton skeleton-block`}),(0,P.jsx)(go,{count:3}),(0,P.jsx)(`div`,{className:`skeleton skeleton-block`}),(0,P.jsx)(go,{count:2})]})}var vo=`a[href], button:not([disabled]), input, select, [tabindex]:not([tabindex="-1"])`,yo=240,bo=96,xo=.5;function So({onClose:e,labelledBy:t,className:n=``,style:r,children:i}){let a=(0,_.useRef)(null),o=(0,_.useRef)(null),s=(0,_.useRef)(!1),[c,l]=(0,_.useState)(`out`),[u,d]=(0,_.useState)(0),f=(0,_.useRef)(null),p=(0,_.useCallback)(()=>{s.current||(s.current=!0,l(`out`),setTimeout(e,yo))},[e]);(0,_.useEffect)(()=>{o.current??=document.activeElement,document.body.classList.add(`is-locked`),a.current?.focus();let e=requestAnimationFrame(()=>l(`in`)),t=e=>{if(e.key===`Escape`){p();return}if(e.key!==`Tab`)return;let t=a.current?.querySelectorAll(vo);if(!t?.length)return;let n=t[0],r=t[t.length-1];e.shiftKey&&document.activeElement===n?(e.preventDefault(),r.focus()):!e.shiftKey&&document.activeElement===r&&(e.preventDefault(),n.focus())};return window.addEventListener(`keydown`,t),()=>{cancelAnimationFrame(e),window.removeEventListener(`keydown`,t),document.body.classList.remove(`is-locked`),o.current?.focus?.()}},[p]);let m=e=>{f.current={id:e.pointerId,y:e.clientY,at:performance.now()},e.currentTarget.setPointerCapture?.(e.pointerId)},h=e=>{f.current?.id===e.pointerId&&d(Math.max(0,e.clientY-f.current.y))},g=e=>{let t=f.current;if(t?.id!==e.pointerId)return;f.current=null;let n=Math.max(0,e.clientY-t.y),r=n/Math.max(1,performance.now()-t.at);d(0),(n>bo||r>xo)&&p()};return(0,P.jsx)(`div`,{className:`overlay is-${c}`,onMouseDown:e=>e.target===e.currentTarget&&p(),children:(0,P.jsxs)(`div`,{className:`sheet ${n}`,style:u?{...r,transform:`translate3d(0, ${u}px, 0)`,transition:`none`}:r,role:`dialog`,"aria-modal":`true`,"aria-labelledby":t,tabIndex:-1,ref:a,children:[(0,P.jsx)(`div`,{className:`sheet-grab`,"aria-hidden":`true`,onPointerDown:m,onPointerMove:h,onPointerUp:g,onPointerCancel:g}),(0,P.jsx)(`button`,{type:`button`,className:`sheet-close btn btn-icon`,onClick:p,"aria-label":`Close`,children:(0,P.jsx)(F,{name:`close`,size:18})}),i]})})}var Co=20;function wo({host:e,html:t}){let[n,r]=(0,_.useState)([]),[i,a]=(0,_.useState)(null),[o,s]=(0,_.useState)(``);(0,_.useEffect)(()=>{let n=e.current;if(!n||!t)return r([]);let i=[];for(let e of n.querySelectorAll(`h2.qa-level`)){let t=[];for(let n=e.nextElementSibling;n&&n.tagName!==`H2`;n=n.nextElementSibling)n.classList.contains(`qa`)&&t.push(n);t.length&&i.push({id:e.dataset.level,heading:e,questions:t})}r(i),a(null)},[e,t]);let c=n.reduce((e,t)=>e+t.questions.length,0),l=c>=Co,u=c;if((0,_.useEffect)(()=>{if(!l)return;let e=o.trim().toLowerCase().split(/\s+/).filter(Boolean);for(let t of n){let n=0;for(let r of t.questions){let a=r.dataset.text??=r.textContent.toLowerCase(),o=(!i||t.id===i)&&e.every(e=>a.includes(e));r.classList.toggle(`is-filtered`,!o),o&&(n+=1)}t.heading.classList.toggle(`is-filtered`,n===0)}return()=>{for(let e of n){e.heading.classList.remove(`is-filtered`);for(let t of e.questions)t.classList.remove(`is-filtered`)}}},[n,i,o,l]),!l)return null;let d=o.trim().toLowerCase().split(/\s+/).filter(Boolean);return(i||d.length)&&(u=n.reduce((e,t)=>e+(i&&t.id!==i?0:t.questions.filter(e=>d.every(t=>(e.dataset.text??e.textContent.toLowerCase()).includes(t))).length),0)),(0,P.jsxs)(`div`,{className:`qa-filter`,children:[(0,P.jsxs)(`div`,{className:`search`,children:[(0,P.jsx)(F,{name:`search`,size:17,className:`search-icon`}),(0,P.jsx)(`input`,{type:`search`,value:o,placeholder:`Search these questions…`,onChange:e=>s(e.target.value),"aria-label":`Search the questions on this page`})]}),(0,P.jsxs)(`div`,{className:`chips`,children:[(0,P.jsx)(`button`,{type:`button`,className:`chip ${i?``:`is-active`}`,onClick:()=>a(null),"aria-pressed":!i,children:`All`}),n.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`chip ${i===e.id?`is-active`:``}`,onClick:()=>a(i===e.id?null:e.id),"aria-pressed":i===e.id,children:[e.heading.textContent,(0,P.jsx)(`span`,{className:`chip-count`,children:e.questions.length})]},e.id)),(0,P.jsx)(`span`,{className:`chips-count`,children:u===c?`${c} questions`:`${u} of ${c}`})]}),u===0&&(0,P.jsxs)(`p`,{className:`qa-filter-empty`,children:[`Nothing matches that.`,` `,(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:()=>{s(``),a(null)},children:`Clear`})]})]})}function To({term:e,anchor:t,pinned:n,onGo:r,onHold:i,onRelease:a}){let o=(0,_.useRef)(null),[s,c]=(0,_.useState)(null),l=Oi[e]??[];return(0,_.useLayoutEffect)(()=>{let e=o.current;if(!e||!t)return;let n=e.getBoundingClientRect(),r=t.left+t.width/2-n.width/2;r=Math.max(12,Math.min(r,window.innerWidth-n.width-12));let i=t.top>n.height+12;c({left:r,top:i?t.top-n.height-9:t.bottom+9,above:i})},[e,t]),(0,P.jsxs)(`div`,{ref:o,className:`term-tip ${s?.above===!1?`is-below`:``} ${n?`is-pinned`:``}`,role:`tooltip`,style:{left:s?.left??0,top:s?.top??0,visibility:s?`visible`:`hidden`},onMouseEnter:i,onMouseLeave:a,children:[(0,P.jsx)(`p`,{className:`term-tip-text`,children:Ei[e]}),l.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`term-go`,onClick:()=>r(e),children:[(0,P.jsx)(`span`,{className:`term-go-label`,children:`Go to the chapter`}),(0,P.jsxs)(`span`,{className:`term-go-name`,children:[$a(e),(0,P.jsx)(F,{name:`arrow`,size:13})]})]},e))]})}function Eo(e,t){let n=URL.createObjectURL(new Blob([t],{type:`text/plain;charset=utf-8`})),r=document.createElement(`a`);r.href=n,r.download=e,document.body.appendChild(r),r.click(),r.remove(),setTimeout(()=>URL.revokeObjectURL(n),1e3)}function Do(e,t,n,r,i){let a=n.split(`
`),o=()=>e.querySelectorAll(`.code-output`)[t],s=``,c=0,l=()=>{let e=o();if(e){if(c>=a.length){e.classList.remove(`is-running`);return}s+=(c?`
`:``)+a[c],c+=1,e.textContent=s,e.hidden=!1,e.classList.add(`is-running`),i(s),r.push(setTimeout(l,45))}};l()}var Oo=1,ko=5,Ao=.5;function jo({src:e,alt:t,onClose:n}){let[r,i]=(0,_.useState)(Oo),a=(0,_.useRef)(null),o=e=>Math.min(ko,Math.max(Oo,Math.round(e*100)/100)),s=r>Oo,c=()=>{i(Oo),a.current?.scrollTo({top:0,left:0})};return(0,P.jsxs)(So,{onClose:n,labelledBy:t?`preview-caption`:void 0,className:`sheet-image`,children:[(0,P.jsxs)(`div`,{className:`preview-tools`,role:`group`,"aria-label":`Zoom`,children:[(0,P.jsx)(`button`,{type:`button`,className:`btn btn-icon`,onClick:()=>i(e=>o(e-Ao)),disabled:r<=Oo,"aria-label":`Zoom out`,children:(0,P.jsx)(`span`,{"aria-hidden":`true`,children:`−`})}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost btn-sm preview-fit`,onClick:c,disabled:!s,children:s?`${Math.round(r*100)}%`:`Fit`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-icon`,onClick:()=>i(e=>o(e+Ao)),disabled:r>=ko,"aria-label":`Zoom in`,children:(0,P.jsx)(`span`,{"aria-hidden":`true`,children:`+`})})]}),(0,P.jsxs)(`figure`,{className:`preview ${s?`is-zoomed`:``}`,children:[(0,P.jsx)(`div`,{className:`preview-stage`,ref:a,children:(0,P.jsx)(`img`,{src:e,alt:t,style:s?{width:`${r*100}%`}:void 0,onClick:()=>r>=ko?c():i(e=>o(e+Ao))})}),t?(0,P.jsx)(`figcaption`,{id:`preview-caption`,className:`muted`,children:t}):null]})]})}function Mo({content:e,variant:t=``,onFollowTerm:n}){let r=(0,_.useRef)(null),[i,a]=(0,_.useState)(null),[o,s]=(0,_.useState)(null),[c,l]=(0,_.useState)(null),u=(0,_.useRef)(0),d=(0,_.useRef)(new Map);(0,_.useEffect)(()=>{let n=!0;a(null),d.current.clear();let r=requestAnimationFrame(()=>{let r=uo(Ci(e),t);n&&a(r)});return()=>{n=!1,cancelAnimationFrame(r)}},[e,t]),(0,_.useEffect)(()=>{let e=r.current;if(!e||i===null)return;let t=[],n=()=>{clearTimeout(u.current),u.current=setTimeout(()=>l(e=>e?.pinned?e:null),160)},a=()=>clearTimeout(u.current),o=(e,t)=>{a(),l({term:e.dataset.term,el:e,anchor:e.getBoundingClientRect(),pinned:t})},c={until:0},f=()=>Date.now()<c.until,p=t=>{if(t.pointerType===`mouse`)return;c.until=Date.now()+900;let n=t.target.closest?.(`.term`);!n||!e.contains(n)||(a(),l(e=>e?.el===n?null:{term:n.dataset.term,el:n,anchor:n.getBoundingClientRect(),pinned:!0}))},m=t=>{if(f())return;let n=t.target.closest?.(`.term`);!n||!e.contains(n)||(a(),l(e=>e?.pinned?e:{term:n.dataset.term,el:n,anchor:n.getBoundingClientRect()}))},h=e=>{f()||e.target.closest?.(`.term`)&&n()},g=async n=>{let r=n.target.closest(`.term`);if(r&&e.contains(r)){f()||o(r,!0);return}let i=n.target.closest(`[data-action="preview"]`);if(i&&e.contains(i)){let e=i.tagName===`IMG`?i:i.closest(`.figure`)?.querySelector(`img`);e&&s({src:e.currentSrc||e.src,alt:e.alt});return}let a=n.target.closest(`.code-action`);if(a&&e.contains(a)){let n=a.closest(`.code-actions`)?.parentElement;if(!n)return;let r=n.querySelector(`code`),i=r?r.textContent:n.textContent;if(a.dataset.action===`save`)Eo(n.dataset.file,n.dataset.demo===void 0?i:ho(n.dataset.file,i,n.dataset.demo));else if(a.dataset.action===`run`){let r=n.nextElementSibling;if(r?.classList.contains(`code-output`)){let i=[...e.querySelectorAll(`.code-output`)].indexOf(r);i!==-1&&Do(e,i,n.dataset.output,t,e=>d.current.set(i,e))}}else await eo(i),a.textContent=`Copied`,a.classList.add(`is-copied`),t.push(setTimeout(()=>{a.textContent=`Copy`,a.classList.remove(`is-copied`)},2e3));return}let c=n.target.closest(`a[href^="#"]`);if(!c||!e.contains(c))return;let l=decodeURIComponent(c.getAttribute(`href`).slice(1)),u=e.querySelector(`[id="${CSS.escape(l)}"]`);u&&(n.preventDefault(),u.scrollIntoView({behavior:`smooth`,block:`start`}))},_=0,v=()=>{_||=requestAnimationFrame(()=>{_=0,l(e=>e&&(!e.pinned||!e.el?.isConnected?null:{...e,anchor:e.el.getBoundingClientRect()}))})},y=e=>{e.target.closest?.(`.term, .term-tip`)||l(null)},b=e=>{e.key===`Escape`&&l(null)};return e.addEventListener(`click`,g),e.addEventListener(`pointerdown`,p),e.addEventListener(`mouseover`,m),e.addEventListener(`mouseout`,h),e.addEventListener(`focusin`,m),e.addEventListener(`focusout`,h),window.addEventListener(`scroll`,v,{passive:!0}),document.addEventListener(`click`,y),window.addEventListener(`keydown`,b),()=>{e.removeEventListener(`click`,g),e.removeEventListener(`pointerdown`,p),e.removeEventListener(`mouseover`,m),e.removeEventListener(`mouseout`,h),e.removeEventListener(`focusin`,m),e.removeEventListener(`focusout`,h),window.removeEventListener(`scroll`,v),document.removeEventListener(`click`,y),window.removeEventListener(`keydown`,b),cancelAnimationFrame(_),clearTimeout(u.current),t.forEach(clearTimeout)}},[i]),(0,_.useEffect)(()=>{if(!d.current.size)return;let e=r.current;if(!e)return;let t=e.querySelectorAll(`.code-output`);for(let[e,n]of d.current){let r=t[e];!r||r.textContent===n||(r.textContent=n,r.hidden=!1)}});let f=(0,_.useMemo)(()=>({__html:i}),[i]);return i===null?(0,P.jsx)(_o,{}):(0,P.jsxs)(P.Fragment,{children:[t===`interview`&&(0,P.jsx)(wo,{host:r,html:i}),(0,P.jsx)(`div`,{className:`prose${t?` prose-${t}`:``}`,ref:r,dangerouslySetInnerHTML:f}),c?(0,y.createPortal)((0,P.jsx)(To,{term:c.term,anchor:c.anchor,pinned:c.pinned,onHold:()=>clearTimeout(u.current),onRelease:()=>{clearTimeout(u.current),u.current=setTimeout(()=>l(e=>e?.pinned?e:null),160)},onGo:e=>{l(null),n?.(e)}}),document.body):null,o?(0,y.createPortal)((0,P.jsx)(jo,{...o,onClose:()=>s(null)}),document.body):null]})}function No({article:e,onNavigate:t,onFollowTerm:n}){let r=(0,_.useMemo)(()=>wi(e.content),[e]),i=Kt.findIndex(t=>t.slug===e.slug),a=Kt[i-1]??null,o=Kt[i+1]??null;return(0,_.useEffect)(()=>{window.scrollTo({top:0})},[e.slug]),tt({onLeft:(0,_.useCallback)(()=>o&&t(o.route),[o,t]),onRight:(0,_.useCallback)(()=>a&&t(a.route),[a,t])}),(0,P.jsxs)(`article`,{className:`article`,style:{"--accent":e.accent},children:[(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm back`,onClick:()=>t(`articles`),children:[(0,P.jsx)(F,{name:`arrow`,size:15,className:`flip`}),(0,P.jsx)(`span`,{children:`All articles`})]}),(0,P.jsxs)(`header`,{className:`article-head`,children:[(0,P.jsxs)(`div`,{className:`article-meta`,children:[(0,P.jsx)(`span`,{className:`badge badge-article`,children:e.category}),(0,P.jsxs)(`span`,{className:`dim`,children:[`Updated `,Yt(e.updated)]})]}),(0,P.jsx)(`h1`,{children:e.title}),(0,P.jsx)(`p`,{className:`lede`,children:e.summary}),(0,P.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,P.jsx)(`li`,{children:e},e))})]}),(0,P.jsxs)(`div`,{className:`article-body ${r.length>2?`has-toc`:``}`,children:[r.length>2&&(0,P.jsx)(Ti,{headings:r}),(0,P.jsx)(Mo,{content:e.content,onFollowTerm:n})]}),(0,P.jsxs)(`footer`,{className:`article-foot`,children:[(0,P.jsxs)(`p`,{className:`hint`,children:[`Written for my own machine and kept here because it worked. Commands that touch the registry, the hosts file or a bootable drive deserve a read before a run.`,` `,(0,P.jsx)(`a`,{href:e.sourceUrl,target:`_blank`,rel:`noreferrer`,children:`The original lives on GitHub`}),` `,`if you would rather clone it.`]}),(0,P.jsxs)(`nav`,{className:`article-nav`,children:[a?(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link`,onClick:()=>t(a.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Previous`}),(0,P.jsx)(`strong`,{children:a.title})]}):(0,P.jsx)(`span`,{}),o&&(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link is-next`,onClick:()=>t(o.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Next`}),(0,P.jsx)(`strong`,{children:o.title})]})]})]})]})}var Po=(e,t,n)=>{if(!t.length)return!0;let r=n.get(e.slug);return t.every(e=>r.includes(e))};function Fo({topic:e,onOpen:t}){return(0,P.jsx)(`span`,{className:`part-links`,children:e.parts.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`part-link is-${e.id}`,onClick:n=>{n.stopPropagation(),t(e.route)},title:e.blurb,children:e.label},e.id))})}function Io({topics:e,onOpen:t}){let n=e.reduce((e,t)=>e+t.parts.length,0),[r]=e;return(0,P.jsx)(Ct,{className:`scenario-wrap`,variant:`blur`,children:(0,P.jsxs)(`article`,{className:`card scenario-card`,style:{"--accent":`#f97316`},onClick:()=>t(r.route),children:[(0,P.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,P.jsx)(`header`,{className:`card-head`,children:(0,P.jsxs)(`div`,{className:`card-title`,children:[(0,P.jsx)(`h3`,{children:(0,P.jsx)(`button`,{type:`button`,className:`card-link`,onClick:e=>{e.stopPropagation(),t(r.route)},children:`Scenario-based HLD`})}),(0,P.jsx)(`p`,{className:`tagline`,children:`The failures, and why each one spreads.`})]})}),(0,P.jsx)(`p`,{className:`card-summary`,children:`One production failure per topic, taken apart the way a design round asks you to: what the mechanism is, why it spreads instead of staying contained, and the architecture that survives it.`}),(0,P.jsx)(`ol`,{className:`scenario-list`,children:e.map((e,n)=>(0,P.jsxs)(`li`,{style:{"--accent":e.accent},children:[(0,P.jsx)(`span`,{className:`scenario-index`,"aria-hidden":`true`,children:String(n+1).padStart(2,`0`)}),(0,P.jsx)(`button`,{type:`button`,className:`scenario-link`,onClick:n=>{n.stopPropagation(),t(e.route)},children:e.title}),(0,P.jsx)(`span`,{className:`dim`,children:e.tagline}),(0,P.jsx)(Fo,{topic:e,onOpen:t})]},e.slug))}),(0,P.jsx)(`footer`,{className:`card-foot`,children:(0,P.jsxs)(`span`,{className:`dim`,children:[e.length,` scenarios · `,n,` pages`]})})]})})}function Lo({query:e,onQuery:t,onOpen:n}){let r=(0,_.useMemo)(()=>e.trim().toLowerCase().split(/\s+/).filter(Boolean),[e]),i=(0,_.useMemo)(()=>new Map(oa.map(e=>[e.slug,sa(e)])),[]),a=(0,_.useMemo)(()=>ca().filter(e=>Po(e,r,i)),[r,i]),o=a.length;return(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(St,{children:[(0,P.jsxs)(`p`,{className:`eyebrow`,children:[(0,P.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`System design, from what actually breaks`]}),(0,P.jsxs)(`h1`,{children:[`Four things that break,`,(0,P.jsx)(`br`,{}),(0,P.jsx)(`span`,{className:`grad`,children:`and why each one spreads.`})]}),(0,P.jsxs)(`p`,{className:`lede`,children:[`Scenario-based high-level design: a connection pool that empties while the database sits idle, a migration that leaves half the fleet on the old schema, a cache that stays wrong with nothing having failed, and a cluster that cannot agree who leads. Each one is the mechanism first, then the architecture that survives it — drawn as a diagram rather than described. `,(0,P.jsx)(`strong`,{children:`Notes`}),` teaches it; `,(0,P.jsx)(`strong`,{children:`Interview`}),` `,`is the questions with full answers, graded easy to hard.`]})]}),(0,P.jsxs)(`div`,{className:`toolbar`,children:[(0,P.jsx)(`div`,{className:`toolbar-row`,children:(0,P.jsxs)(`div`,{className:`search`,children:[(0,P.jsx)(F,{name:`search`,size:17,className:`search-icon`}),(0,P.jsx)(`input`,{type:`search`,value:e,placeholder:`Search scenarios, questions, terms…`,onChange:e=>t(e.target.value),"aria-label":`Search system design notes`})]})}),(0,P.jsx)(`div`,{className:`chips`,children:(0,P.jsxs)(`span`,{className:`chips-count`,children:[o,` `,o===1?`scenario`:`scenarios`]})})]}),o===0?(0,P.jsxs)(`div`,{className:`empty empty-in`,children:[(0,P.jsx)(F,{name:`search`,size:28}),(0,P.jsx)(`h3`,{children:`Nothing matches that`}),(0,P.jsx)(`p`,{children:`Try a different word, or clear the search.`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>t(``),children:`Clear search`})]}):(0,P.jsx)(`section`,{className:`syllabus`,"aria-label":`System design contents`,children:(0,P.jsx)(Io,{topics:a,onOpen:n})})]})}var Ro=(e,t,n)=>{if(!t.length)return!0;let r=n.get(e.slug);return t.every(e=>r.includes(e))};function zo({topic:e,onOpen:t}){return(0,P.jsx)(`span`,{className:`part-links`,children:e.parts.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`part-link is-${e.id}`,onClick:n=>{n.stopPropagation(),t(e.route)},title:e.blurb,children:e.label},e.id))})}function Bo({topic:e,index:t,onOpen:n}){let r=nt();return(0,P.jsxs)(`article`,{className:`card dsa-card`,style:{"--accent":e.accent},onClick:()=>n(e.route),...r,children:[(0,P.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,P.jsx)(`span`,{className:`dsa-rule`,"aria-hidden":`true`}),(0,P.jsxs)(`header`,{className:`dsa-card-head`,children:[(0,P.jsx)(`span`,{className:`dsa-order`,"aria-hidden":`true`,children:String(t).padStart(2,`0`)}),(0,P.jsx)(`span`,{className:`badge badge-article`,children:e.stage})]}),(0,P.jsx)(`h3`,{children:(0,P.jsx)(`button`,{type:`button`,className:`card-link`,onClick:t=>{t.stopPropagation(),n(e.route)},children:e.title})}),(0,P.jsx)(`p`,{className:`tagline`,children:e.tagline}),(0,P.jsx)(`p`,{className:`card-summary`,children:e.summary}),(0,P.jsx)(`footer`,{className:`card-foot`,children:(0,P.jsx)(zo,{topic:e,onOpen:n})})]})}function Vo({query:e,onQuery:t,onOpen:n}){let r=(0,_.useMemo)(()=>e.trim().toLowerCase().split(/\s+/).filter(Boolean),[e]),i=(0,_.useMemo)(()=>new Map(ya.map(e=>[e.slug,ba(e)])),[]),a=(0,_.useMemo)(()=>ya.filter(e=>Ro(e,r,i)),[r,i]),o=a.length;return(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(St,{children:[(0,P.jsxs)(`p`,{className:`eyebrow`,children:[(0,P.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`The questions that come up`]}),(0,P.jsxs)(`h1`,{children:[`Asked constantly.`,(0,P.jsx)(`br`,{}),(0,P.jsx)(`span`,{className:`grad`,children:`Answered well about a third of the time.`})]}),(0,P.jsxs)(`p`,{className:`lede`,children:[`Two questions, taken all the way down. `,(0,P.jsx)(`strong`,{children:`Why 0.1 + 0.2 is not 0.3`}),` — the conversion that never terminates, the roundings that follow, and what to write when the number is money. And`,` `,(0,P.jsx)(`strong`,{children:`server rendering against client rendering`}),` — what each one actually sends, what the reader stares at while waiting, and what hydration costs. Each comes in`,` `,(0,P.jsx)(`strong`,{children:`Notes`}),`, `,(0,P.jsx)(`strong`,{children:`Quick revision`}),` and`,` `,(0,P.jsx)(`strong`,{children:`Interview`}),`.`]})]}),(0,P.jsxs)(`div`,{className:`toolbar`,children:[(0,P.jsx)(`div`,{className:`toolbar-row`,children:(0,P.jsxs)(`div`,{className:`search`,children:[(0,P.jsx)(F,{name:`search`,size:17,className:`search-icon`}),(0,P.jsx)(`input`,{type:`search`,value:e,placeholder:`Search questions, answers, code…`,onChange:e=>t(e.target.value),"aria-label":`Search interview notes`})]})}),(0,P.jsx)(`div`,{className:`chips`,children:(0,P.jsxs)(`span`,{className:`chips-count`,children:[o,` `,o===1?`topic`:`topics`]})})]}),o===0?(0,P.jsxs)(`div`,{className:`empty empty-in`,children:[(0,P.jsx)(F,{name:`search`,size:28}),(0,P.jsx)(`h3`,{children:`Nothing matches that`}),(0,P.jsx)(`p`,{children:`Try a different word, or clear the search.`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>t(``),children:`Clear search`})]}):(0,P.jsx)(`div`,{className:`catalog catalog-grid`,"aria-label":`Interview contents`,children:a.map((e,t)=>(0,P.jsx)(Ct,{className:`reveal-cell`,delay:Ge(t%8),children:(0,P.jsx)(Bo,{topic:e,index:t+1,onOpen:n})},e.slug))})]})}var Ho=2*Math.PI*15.5;function Uo({done:e,total:t,size:n=38,label:r}){let i=t?e/t:0,a=t>0&&e===t;return(0,P.jsxs)(`span`,{className:`ring ${a?`is-complete`:``} ${e?``:`is-empty`}`,style:{"--size":`${n}px`},title:r??`${e} of ${t} done`,children:[(0,P.jsxs)(`svg`,{viewBox:`0 0 36 36`,"aria-hidden":`true`,children:[(0,P.jsx)(`circle`,{className:`ring-track`,cx:`18`,cy:`18`,r:`15.5`}),(0,P.jsx)(`circle`,{className:`ring-fill`,cx:`18`,cy:`18`,r:`15.5`,style:{strokeDasharray:Ho,strokeDashoffset:Ho*(1-i)}})]}),(0,P.jsx)(`span`,{className:`ring-label`,children:a?(0,P.jsx)(F,{name:`check`,size:n*.4}):(0,P.jsxs)(P.Fragment,{children:[Math.round(i*100),(0,P.jsx)(`span`,{className:`ring-unit`,children:`%`})]})}),(0,P.jsxs)(`span`,{className:`sr-only`,children:[e,` of `,t,` problems done`]})]})}function Wo({topic:e,solved:t,compact:n=!1}){let r=e.bands.filter(e=>e.id!==`all`&&e.count>0),i=r.reduce((e,t)=>e+t.count,0)||1;return(0,P.jsx)(`span`,{className:`meter ${n?`is-compact`:``}`,"aria-hidden":`true`,children:r.map(e=>{let n=t?.size?e.keys.reduce((e,n)=>e+ +!!t.has(n),0):0;return(0,P.jsx)(`span`,{className:`meter-seg band-${e.id}`,style:{"--share":e.count/i},title:`${wa[e.id].label}: ${n} of ${e.count} done`,children:(0,P.jsx)(`span`,{className:`meter-fill`,style:{"--fill":`${n/e.count*100}%`}})},e.id)})})}var Go=6,Ko=(e,t,n)=>{if(!t.length)return!0;let r=n.get(e.slug);return t.every(e=>r.includes(e))};function qo({topic:e,solved:t,onOpen:n}){return(0,P.jsx)(`span`,{className:`part-links`,children:e.bands.map(e=>{let r=t.size?e.keys.reduce((e,n)=>e+ +!!t.has(n),0):0;return(0,P.jsxs)(`button`,{type:`button`,className:`part-link band-${e.id} ${r===e.count?`is-done`:``}`,onClick:t=>{t.stopPropagation(),n(e.route)},title:`${e.blurb} ${r} of ${e.count} done.`,children:[e.label,(0,P.jsx)(`span`,{className:`band-count`,children:r?`${r}/${e.count}`:e.count})]},e.id)})})}function Jo({topic:e,band:t,solved:n,onOpen:r}){let i=nt(),a=e.bands.find(e=>e.id===t)??e.bands[0],{done:o,total:s}=Ka(e,t,n);return(0,P.jsxs)(`article`,{className:`card dsa-card ${o&&o===s?`is-complete`:``}`,style:{"--accent":e.accent},onClick:()=>r(a.route),...i,children:[(0,P.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,P.jsx)(`span`,{className:`dsa-rule`,"aria-hidden":`true`}),(0,P.jsxs)(`header`,{className:`dsa-card-head`,children:[(0,P.jsx)(`span`,{className:`dsa-order`,"aria-hidden":`true`,children:String(e.order).padStart(2,`0`)}),(0,P.jsx)(`span`,{className:`badge badge-article`,children:e.track}),(0,P.jsx)(Uo,{done:o,total:s,label:`${o} of ${s} ${wa[t].label.toLowerCase()} problems done`})]}),(0,P.jsx)(`h3`,{children:(0,P.jsx)(`button`,{type:`button`,className:`card-link`,onClick:e=>{e.stopPropagation(),r(a.route)},children:e.title})}),(0,P.jsx)(`p`,{className:`tagline`,children:e.tagline}),(0,P.jsxs)(`div`,{className:`dsa-meta`,children:[(0,P.jsx)(Wo,{topic:e,solved:n}),(0,P.jsxs)(`span`,{className:`dsa-meta-line`,children:[(0,P.jsx)(`strong`,{children:s}),` `,s===1?`problem`:`problems`,o>0&&(0,P.jsxs)(P.Fragment,{children:[` · `,(0,P.jsxs)(`span`,{className:`dsa-done`,children:[o,` done`]})]})]})]}),a.headline.length>0&&(0,P.jsxs)(`p`,{className:`dsa-headline`,title:`The three asked most often in this topic`,children:[(0,P.jsx)(`span`,{className:`dim`,children:`Most asked`}),` `,a.headline.join(` · `)]}),(0,P.jsx)(`footer`,{className:`card-foot`,children:(0,P.jsx)(qo,{topic:e,solved:n,onOpen:r})})]})}function Yo({topic:e,band:t,solved:n,onOpen:r}){let i=e.bands.find(e=>e.id===t)??e.bands[0],{done:a,total:o}=Ka(e,t,n);return(0,P.jsxs)(`li`,{className:`dsa-row ${a&&a===o?`is-complete`:``}`,style:{"--accent":e.accent},children:[(0,P.jsx)(`span`,{className:`dsa-order`,"aria-hidden":`true`,children:String(e.order).padStart(2,`0`)}),(0,P.jsxs)(`span`,{className:`dsa-row-main`,children:[(0,P.jsx)(`button`,{type:`button`,className:`dsa-row-title`,onClick:()=>r(i.route),children:e.title}),(0,P.jsx)(`span`,{className:`dim`,children:e.tagline}),(0,P.jsx)(qo,{topic:e,solved:n,onOpen:r})]}),(0,P.jsxs)(`span`,{className:`dsa-row-meter`,children:[(0,P.jsx)(Wo,{topic:e,solved:n,compact:!0}),(0,P.jsxs)(`span`,{className:`dim`,children:[a,` / `,o]})]}),(0,P.jsx)(Uo,{done:a,total:o,size:32})]})}function Xo({query:e,onQuery:t,band:n,onBand:r,track:i,onTrack:a,status:o,onStatus:s,topics:c,onTopics:l,onToggleTopic:u,sort:d,onSort:f,view:p,onView:m,solved:h,onReset:g,onClearFilters:v,onOpen:y}){let[b,x]=(0,_.useState)(!1),S=(0,_.useMemo)(()=>e.trim().toLowerCase().split(/\s+/).filter(Boolean),[e]),C=(0,_.useMemo)(()=>new Map(Ta.map(e=>[e,new Map(V.map(t=>[t.slug,Ha(t,e)]))])),[]),w=C.get(n),T=(0,_.useMemo)(()=>V.filter(e=>e.counts[n]>0&&(c.length===0||c.includes(e.slug))&&(!i||e.track===i)&&Ko(e,S,w)).map(e=>{let t=Ka(e,n,h);return{...e,...t,status:za(t)}}),[n,i,c,S,w,h]),ee=(0,_.useMemo)(()=>{let e={any:T.length};for(let t of Object.keys(Ra))t!==`any`&&(e[t]=T.filter(e=>e.status===t).length);return e},[T]),E=(0,_.useMemo)(()=>{let e=V.filter(e=>e.counts[n]>0&&(c.length===0||c.includes(e.slug))&&Ko(e,S,w)&&(o===`any`||za(Ka(e,n,h))===o)),t={__all:e.length};for(let n of Oa)t[n]=e.filter(e=>e.track===n).length;return t},[n,o,c,S,w,h]),D=(0,_.useMemo)(()=>{let e={};for(let t of Ta){let n=C.get(t);e[t]=V.filter(e=>e.counts[t]>0&&(c.length===0||c.includes(e.slug))&&(!i||e.track===i)&&Ko(e,S,n)&&(o===`any`||za(Ka(e,t,h))===o)).reduce((e,n)=>e+n.counts[t],0)}return e},[i,o,c,S,C,h]),te=(0,_.useMemo)(()=>new Set(V.filter(e=>e.counts[n]>0&&(!i||e.track===i)&&Ko(e,S,w)&&(o===`any`||za(Ka(e,n,h))===o)).map(e=>e.slug)),[n,i,o,S,w,h]),O=(0,_.useMemo)(()=>{let e=T.filter(e=>o===`any`||e.status===o),t=(qa[d]??qa.order).compare;return[...e].sort((e,r)=>t(e,r,n))},[T,o,d,n]),ne=(0,_.useMemo)(()=>{if(b)return V;let e=V.slice(0,Go),t=V.filter(t=>c.includes(t.slug)&&!e.includes(t));return[...e,...t]},[b,c]),k=b?0:V.length-ne.length,re=d===`order`&&!S.length,A=(0,_.useMemo)(()=>re?Ua(O):[],[re,O]),ie=n!==`all`||i!==null||o!==`any`||c.length>0||S.length>0,j=O.reduce((e,t)=>e+t.counts[n],0),M=O.reduce((e,t)=>e+t.done,0),ae=Ba(h),oe=ka?Math.round(ae/ka*100):0,se=e=>p===`list`?(0,P.jsx)(`ol`,{className:`dsa-rows`,children:e.map(e=>(0,P.jsx)(Yo,{topic:e,band:n,solved:h,onOpen:y},e.slug))}):(0,P.jsx)(`div`,{className:`catalog catalog-grid`,children:e.map((e,t)=>(0,P.jsx)(Ct,{className:`reveal-cell`,delay:Ge(t%8),children:(0,P.jsx)(Jo,{topic:e,band:n,solved:h,onOpen:y})},e.slug))});return(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(St,{children:[(0,P.jsxs)(`p`,{className:`eyebrow`,children:[(0,P.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`The practice sheet, sorted`]}),(0,P.jsxs)(`h1`,{children:[`Every problem worth`,(0,P.jsx)(`br`,{}),(0,P.jsx)(`span`,{className:`grad`,children:`doing twice.`})]}),(0,P.jsxs)(`p`,{className:`lede`,children:[ka,` problems across `,V.length,` topics, each split into three bands. `,(0,P.jsx)(`strong`,{children:`Easy`}),` is where the pattern is introduced,`,` `,(0,P.jsx)(`strong`,{children:`Medium`}),` is the shape an interview usually asks for, and`,` `,(0,P.jsx)(`strong`,{children:`Hard`}),` is the same pattern with a twist on top. Tick a problem off as you solve it — the ticks are kept in this browser and nowhere else, and a problem listed under three topics is ticked in all three at once.`]}),(0,P.jsx)(`ul`,{className:`hero-stats`,children:[[ka,`problems`],[V.length,`topics`],[Wa(`easy`),`easy`],[Wa(`medium`),`medium`],[Wa(`hard`),`hard`]].map(([e,t],n)=>(0,P.jsxs)(`li`,{style:{"--i":n},children:[(0,P.jsx)(`strong`,{children:e}),(0,P.jsx)(`span`,{children:t})]},t))}),(0,P.jsxs)(`div`,{className:`dsa-overall`,children:[(0,P.jsx)(Uo,{done:ae,total:ka,size:52,label:`${ae} of ${ka} problems done`}),(0,P.jsxs)(`span`,{className:`dsa-overall-text`,children:[(0,P.jsxs)(`strong`,{children:[ae,` of `,ka,` solved`]}),(0,P.jsx)(`span`,{className:`dim`,children:ae===0?`Nothing ticked yet. Open a topic and start at the top.`:`${oe}% of the sheet. A problem more than one topic lists counts in each of them, and one tick does all of them.`})]}),ae>0&&(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:g,children:`Reset progress`})]})]}),(0,P.jsxs)(`div`,{className:`toolbar dsa-filters`,children:[(0,P.jsxs)(`div`,{className:`toolbar-row`,children:[(0,P.jsxs)(`div`,{className:`search`,children:[(0,P.jsx)(F,{name:`search`,size:17,className:`search-icon`}),(0,P.jsx)(`input`,{type:`search`,value:e,placeholder:`Search topics and problems…`,onChange:e=>t(e.target.value),"aria-label":`Search the DSA sheet`})]}),(0,P.jsxs)(`div`,{className:`toolbar-controls`,children:[(0,P.jsxs)(`label`,{className:`select`,children:[(0,P.jsx)(`span`,{className:`sr-only`,children:`Order`}),(0,P.jsx)(`select`,{value:d,onChange:e=>f(e.target.value),children:Object.entries(qa).map(([e,t])=>(0,P.jsx)(`option`,{value:e,children:t.label},e))})]}),(0,P.jsx)(`div`,{className:`segmented`,role:`group`,"aria-label":`Layout`,children:[`grid`,`list`].map(e=>(0,P.jsx)(`button`,{type:`button`,className:p===e?`is-active`:``,onClick:()=>m(e),"aria-pressed":p===e,title:e===`grid`?`Cards`:`List`,children:(0,P.jsx)(F,{name:e,size:15})},e))})]})]}),(0,P.jsxs)(`div`,{className:`filter-row`,children:[(0,P.jsx)(`span`,{className:`filter-label`,id:`filter-band`,children:`Difficulty`}),(0,P.jsx)(`div`,{className:`chips`,role:`group`,"aria-labelledby":`filter-band`,children:Ta.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`chip band-chip band-${e} ${n===e?`is-active`:``}`,onClick:()=>r(e),"aria-pressed":n===e,title:wa[e].blurb,disabled:D[e]===0&&n!==e,children:[wa[e].label,(0,P.jsx)(`span`,{className:`band-count`,children:D[e]})]},e))})]}),(0,P.jsxs)(`div`,{className:`filter-row`,children:[(0,P.jsx)(`span`,{className:`filter-label`,id:`filter-track`,children:`Category`}),(0,P.jsxs)(`div`,{className:`chips`,role:`group`,"aria-labelledby":`filter-track`,children:[(0,P.jsxs)(`button`,{type:`button`,className:`chip ${i===null?`is-active`:``}`,onClick:()=>a(null),"aria-pressed":i===null,children:[`All`,(0,P.jsx)(`span`,{className:`band-count`,children:E.__all})]}),Oa.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`chip ${i===e?`is-active`:``}`,onClick:()=>a(i===e?null:e),"aria-pressed":i===e,disabled:E[e]===0&&i!==e,children:[e,(0,P.jsx)(`span`,{className:`band-count`,children:E[e]})]},e))]})]}),(0,P.jsxs)(`div`,{className:`filter-row`,children:[(0,P.jsx)(`span`,{className:`filter-label`,id:`filter-topic`,children:`Pattern`}),(0,P.jsxs)(`div`,{className:`chips`,role:`group`,"aria-labelledby":`filter-topic`,children:[(0,P.jsxs)(`button`,{type:`button`,className:`chip ${c.length===0?`is-active`:``}`,onClick:()=>l([]),"aria-pressed":c.length===0,children:[`All`,(0,P.jsx)(`span`,{className:`band-count`,children:D[n]})]}),ne.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`chip ${c.includes(e.slug)?`is-active`:``}`,onClick:()=>u(e.slug),"aria-pressed":c.includes(e.slug),title:e.pattern?`${e.tagline} — ${[e.pattern.spot].flat().join(` `)}`:e.tagline,disabled:!te.has(e.slug)&&!c.includes(e.slug),children:[e.title,(0,P.jsx)(`span`,{className:`band-count`,children:e.counts[n]})]},e.slug)),k>0&&(0,P.jsxs)(`button`,{type:`button`,className:`chip chip-more`,onClick:()=>x(!0),"aria-expanded":!1,children:[`+`,k,` more`]}),b&&(0,P.jsx)(`button`,{type:`button`,className:`chip chip-more`,onClick:()=>x(!1),"aria-expanded":!0,children:`Show fewer`})]})]}),(0,P.jsxs)(`div`,{className:`filter-row`,children:[(0,P.jsx)(`span`,{className:`filter-label`,id:`filter-status`,children:`Progress`}),(0,P.jsx)(`div`,{className:`chips`,role:`group`,"aria-labelledby":`filter-status`,children:Object.entries(Ra).map(([e,t])=>(0,P.jsxs)(`button`,{type:`button`,className:`chip status-chip status-${e} ${o===e?`is-active`:``}`,onClick:()=>s(e),"aria-pressed":o===e,title:t.blurb,disabled:ee[e]===0&&o!==e,children:[t.label,(0,P.jsx)(`span`,{className:`band-count`,children:ee[e]})]},e))})]}),(0,P.jsxs)(`div`,{className:`filter-foot`,children:[(0,P.jsxs)(`span`,{className:`chips-count`,children:[M>0&&(0,P.jsxs)(`strong`,{children:[M,` of `]}),j,` `,j===1?`problem`:`problems`,` in `,O.length,` `,O.length===1?`topic`:`topics`]}),ie&&(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:v,children:[(0,P.jsx)(F,{name:`close`,size:14}),(0,P.jsx)(`span`,{children:`Clear filters`})]})]})]}),O.length===0?(0,P.jsxs)(`div`,{className:`empty empty-in`,children:[(0,P.jsx)(F,{name:`search`,size:28}),(0,P.jsx)(`h3`,{children:`Nothing matches that`}),(0,P.jsx)(`p`,{children:`Four filters are on at once here. A problem in the Easy list will not turn up while Hard is selected, and a topic you have finished will not turn up under Not started.`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:v,children:`Clear filters`})]}):re?A.map(e=>(0,P.jsxs)(`section`,{className:`dsa-track`,children:[(0,P.jsxs)(Ct,{as:`h2`,className:`section-heading`,variant:`left`,children:[e.track,(0,P.jsxs)(`span`,{className:`dim`,children:[e.topics.reduce((e,t)=>e+t.counts[n],0),` problems`]}),(0,P.jsx)(`span`,{className:`section-rule`,"aria-hidden":`true`}),(0,P.jsxs)(`span`,{className:`dim`,children:[e.topics.reduce((e,t)=>e+t.done,0),` done`]})]}),se(e.topics)]},e.track)):(0,P.jsx)(`div`,{className:`dsa-track`,children:se(O)})]})}var Zo=`modulepreload`,Qo=function(e,t){return new URL(e,t).href},$o={},W=function(e,t,n){let r=Promise.resolve();if(t&&t.length>0){let e=document.getElementsByTagName(`link`),i=document.querySelector(`meta[property=csp-nonce]`),a=i?.nonce||i?.getAttribute(`nonce`);function o(e){return Promise.all(e.map(e=>Promise.resolve(e).then(e=>({status:`fulfilled`,value:e}),e=>({status:`rejected`,reason:e}))))}function s(e){return import.meta.resolve?import.meta.resolve(e):new URL(e,import.meta.url).href}r=o(t.map(t=>{if(t=Qo(t,n),t=s(t),t in $o)return;$o[t]=!0;let r=t.endsWith(`.css`);for(let n=e.length-1;n>=0;n--){let i=e[n];if(i.href===t&&(!r||i.rel===`stylesheet`))return}let i=document.createElement(`link`);if(i.rel=r?`stylesheet`:Zo,r||(i.as=`script`),i.crossOrigin=``,i.href=t,a&&i.setAttribute(`nonce`,a),document.head.appendChild(i),r)return new Promise((e,n)=>{i.addEventListener(`load`,e),i.addEventListener(`error`,()=>n(Error(`Unable to preload CSS for ${t}`)))})}))}function i(e){let t=new Event(`vite:preloadError`,{cancelable:!0});if(t.payload=e,window.dispatchEvent(t),!t.defaultPrevented)throw e}return r.then(t=>{for(let e of t||[])e.status===`rejected`&&i(e.reason);return e().catch(i)})},es=Object.assign({"../content/dsa/advanced-topics-notes.md":()=>W(()=>import(`./advanced-topics-notes-D9dCW2MP.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/advanced-topics-revision.md":()=>W(()=>import(`./advanced-topics-revision-DEjA6gB4.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/arrays-notes.md":()=>W(()=>import(`./arrays-notes-enS5UBxk.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/arrays-revision.md":()=>W(()=>import(`./arrays-revision-B1hBc80N.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/basic-notes.md":()=>W(()=>import(`./basic-notes-7Tr3PO0Z.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/basic-revision.md":()=>W(()=>import(`./basic-revision-BjUDahAG.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/binary-search-notes.md":()=>W(()=>import(`./binary-search-notes-DfeS2Jap.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/binary-search-revision.md":()=>W(()=>import(`./binary-search-revision-C8jDPvDt.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/binary-tree-notes.md":()=>W(()=>import(`./binary-tree-notes-BkHZwseJ.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/binary-tree-revision.md":()=>W(()=>import(`./binary-tree-revision-COPGqsYi.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/bit-manipulation-notes.md":()=>W(()=>import(`./bit-manipulation-notes-C7a9ca58.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/bit-manipulation-revision.md":()=>W(()=>import(`./bit-manipulation-revision-BOotobK_.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/bst-ordered-set-notes.md":()=>W(()=>import(`./bst-ordered-set-notes-guHAjJH2.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/bst-ordered-set-revision.md":()=>W(()=>import(`./bst-ordered-set-revision-Dgb7_Xi8.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/coding-foundations-notes.md":()=>W(()=>import(`./coding-foundations-notes-CKwtiWYb.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/coding-foundations-revision.md":()=>W(()=>import(`./coding-foundations-revision-zb__cMdU.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/data-structure-design-notes.md":()=>W(()=>import(`./data-structure-design-notes-DSKODqPl.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/data-structure-design-revision.md":()=>W(()=>import(`./data-structure-design-revision-DQDc-djN.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/deque-notes.md":()=>W(()=>import(`./deque-notes-RMJRs89g.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/deque-revision.md":()=>W(()=>import(`./deque-revision-Bm48dwm3.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/divide-and-conquer-notes.md":()=>W(()=>import(`./divide-and-conquer-notes-i2QqEig5.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/divide-and-conquer-revision.md":()=>W(()=>import(`./divide-and-conquer-revision-BageNrla.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/dsa-foundations-notes.md":()=>W(()=>import(`./dsa-foundations-notes-CE2sGl3Q.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/dsa-foundations-revision.md":()=>W(()=>import(`./dsa-foundations-revision-DOpcmGXg.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/dynamic-programming-notes.md":()=>W(()=>import(`./dynamic-programming-notes-CkKEqPAx.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/dynamic-programming-revision.md":()=>W(()=>import(`./dynamic-programming-revision-oeLEdrRS.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/fenwick-tree-binary-indexed-tree-notes.md":()=>W(()=>import(`./fenwick-tree-binary-indexed-tree-notes-BeVwlM12.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/fenwick-tree-binary-indexed-tree-revision.md":()=>W(()=>import(`./fenwick-tree-binary-indexed-tree-revision-DWP4beUa.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/graphs-notes.md":()=>W(()=>import(`./graphs-notes-DK3NBSSj.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/graphs-revision.md":()=>W(()=>import(`./graphs-revision-31ZHodZ0.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/greedy-notes.md":()=>W(()=>import(`./greedy-notes-BfTgwWWI.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/greedy-revision.md":()=>W(()=>import(`./greedy-revision-Dkfz6vfH.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/hash-tables-notes.md":()=>W(()=>import(`./hash-tables-notes-eG_pl2dE.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/hash-tables-revision.md":()=>W(()=>import(`./hash-tables-revision-y2nq0APF.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/heaps-notes.md":()=>W(()=>import(`./heaps-notes-CC1SPRku.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/heaps-revision.md":()=>W(()=>import(`./heaps-revision-DWJq6IOQ.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/intervals-notes.md":()=>W(()=>import(`./intervals-notes--T81R1kj.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/intervals-revision.md":()=>W(()=>import(`./intervals-revision-BTQ4FNdb.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/kadanes-algorithm-notes.md":()=>W(()=>import(`./kadanes-algorithm-notes-Cik6iQQ2.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/kadanes-algorithm-revision.md":()=>W(()=>import(`./kadanes-algorithm-revision-CgggpDdE.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/linked-list-notes.md":()=>W(()=>import(`./linked-list-notes-De8b0qqg.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/linked-list-revision.md":()=>W(()=>import(`./linked-list-revision-D0hC4k-j.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/maths-geometry-notes.md":()=>W(()=>import(`./maths-geometry-notes-D6c8YROY.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/maths-geometry-revision.md":()=>W(()=>import(`./maths-geometry-revision-DZDahWFe.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/matrix-notes.md":()=>W(()=>import(`./matrix-notes-D6L554pr.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/matrix-revision.md":()=>W(()=>import(`./matrix-revision-Cadc9dna.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/prefix-sum-notes.md":()=>W(()=>import(`./prefix-sum-notes-DJY2n0RY.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/prefix-sum-revision.md":()=>W(()=>import(`./prefix-sum-revision-XjAW7xfl.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/queues-notes.md":()=>W(()=>import(`./queues-notes-DX1hEPyW.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/queues-revision.md":()=>W(()=>import(`./queues-revision-in9rGMNd.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/recursion-and-backtracking-notes.md":()=>W(()=>import(`./recursion-and-backtracking-notes-BWoHqtth.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/recursion-and-backtracking-revision.md":()=>W(()=>import(`./recursion-and-backtracking-revision-F9vSWk1H.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/segment-tree-notes.md":()=>W(()=>import(`./segment-tree-notes-C_o_dGI0.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/segment-tree-revision.md":()=>W(()=>import(`./segment-tree-revision-B1RY9NV0.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/sliding-window-notes.md":()=>W(()=>import(`./sliding-window-notes-DC45T-d_.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/sliding-window-revision.md":()=>W(()=>import(`./sliding-window-revision-aGo624xW.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/sorting-notes.md":()=>W(()=>import(`./sorting-notes-DhGXWQNq.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/sorting-revision.md":()=>W(()=>import(`./sorting-revision-QUCb5kzX.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/sparse-table-notes.md":()=>W(()=>import(`./sparse-table-notes-BaGr_7a2.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/sparse-table-revision.md":()=>W(()=>import(`./sparse-table-revision-CzYTQ1pn.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/stacks-notes.md":()=>W(()=>import(`./stacks-notes-lhQuj1pA.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/stacks-revision.md":()=>W(()=>import(`./stacks-revision-d4Moqa7Q.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/strings-notes.md":()=>W(()=>import(`./strings-notes-Sbi0y10W.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/strings-revision.md":()=>W(()=>import(`./strings-revision-BYwFoGBn.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/tries-notes.md":()=>W(()=>import(`./tries-notes-CL46Jj6b.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/tries-revision.md":()=>W(()=>import(`./tries-revision-F7ckpU7T.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/two-pointers-notes.md":()=>W(()=>import(`./two-pointers-notes-U3HRVSoY.js`).then(e=>e.default),[],import.meta.url),"../content/dsa/two-pointers-revision.md":()=>W(()=>import(`./two-pointers-revision-C1MYMi5r.js`).then(e=>e.default),[],import.meta.url)}),ts={notes:{label:`Notes`,blurb:`The pattern from nothing, in Java.`},revision:{label:`Quick revision`,blurb:`The same thing, compressed.`}},ns=[`notes`,`revision`],rs=(e,t)=>`../content/dsa/${e}-${t}.md`,is=e=>rs(e,`notes`)in es,as=e=>ns.filter(t=>rs(e,t)in es).map(t=>({id:t,...ts[t],route:`dsa/${e}/${t}`})),os=(e,t)=>as(e).find(e=>e.id===t)??null,ss=(e,t)=>{let n=es[rs(e,t)];return n?n().then(C):Promise.resolve(null)},cs=320,ls=8,us=34,ds=78,fs=104,ps=.45;function ms({ghost:e=!1,floating:t=!1,accent:n,ariaLabel:r,previous:i,next:a,position:o,total:s,onNavigate:c,children:l}){let u=(0,_.useRef)(null),d=(0,_.useRef)(0),f=(0,_.useRef)(null),[p,m]=(0,_.useState)(!1),h=(0,_.useRef)(null),g=(0,_.useRef)(0),v=(0,_.useRef)({x:0,t:0}),y=(0,_.useRef)(0),b=(0,_.useRef)(null),[x,S]=(0,_.useState)(null),C=(0,_.useCallback)(e=>{g.current=e,h.current&&(h.current.style.transform=`translateX(${e}px)`);let t=e<=-34*.55?`prev`:e>=us*.55?`next`:null;t!==b.current&&(b.current=t,S(t))},[]),w=(0,_.useCallback)(()=>{clearTimeout(d.current),f.current=null,m(!1),h.current&&(h.current.style.transform=``),g.current=0,b.current=null,S(null)},[]),T=(0,_.useCallback)(e=>{w(),e&&c(e.route)},[w,c]);(0,_.useEffect)(()=>{let t=u.current;if(!t||e)return;let n=e=>{e.button!=null&&e.button!==0||(f.current={x:e.clientX,y:e.clientY,id:e.pointerId},v.current={x:e.clientX,t:e.timeStamp},y.current=0,clearTimeout(d.current),d.current=setTimeout(()=>{f.current!==null&&(m(!0),t.setPointerCapture?.(e.pointerId),t.classList.add(`is-dragging`),document.getSelection?.()?.removeAllRanges?.(),navigator.vibrate?.(8))},cs))},r=e=>{if(f.current===null)return;let t=e.clientX-f.current.x,n=e.clientY-f.current.y;if(!p){(Math.abs(t)>ls||Math.abs(n)>ls)&&(clearTimeout(d.current),f.current=null);return}e.preventDefault();let r=e.timeStamp,o=r-v.current.t;o>0&&(y.current=(e.clientX-v.current.x)/o,v.current={x:e.clientX,t:r});let s=Math.abs(t),c=Math.max(0,s-ds),l=Math.min(s,ds)+c*.28,u=Math.sign(t)*Math.min(fs,l);C(t<0&&!i||t>0&&!a?u*.22:u)},o=e=>{if(clearTimeout(d.current),!p||f.current===null){f.current=null;return}let n=e.clientX-f.current.x,r=y.current;t.releasePointerCapture?.(e.pointerId),t.classList.remove(`is-dragging`),n<=-34||r<=-.45&&n<-8?T(i):n>=us||r>=ps&&n>ls?T(a):w()},s=e=>p&&e.preventDefault();return t.addEventListener(`contextmenu`,s),t.addEventListener(`pointerdown`,n),t.addEventListener(`pointermove`,r,{passive:!1}),t.addEventListener(`pointerup`,o),t.addEventListener(`pointercancel`,w),t.addEventListener(`pointerleave`,e=>!p&&o(e)),()=>{clearTimeout(d.current),t.removeEventListener(`contextmenu`,s),t.removeEventListener(`pointerdown`,n),t.removeEventListener(`pointermove`,r),t.removeEventListener(`pointerup`,o),t.removeEventListener(`pointercancel`,w)}},[e,p,i,a,T,w,C]),(0,_.useEffect)(()=>{if(!p)return;let e=e=>{e.key===`Escape`?w():e.key===`ArrowLeft`?T(i):e.key===`ArrowRight`&&T(a)};return window.addEventListener(`keydown`,e),()=>window.removeEventListener(`keydown`,e)},[p,i,a,T,w]);let ee=x===`prev`?i:x===`next`?a:null;return(0,P.jsxs)(`nav`,{ref:u,className:`part-switch ${e?`is-ghost`:t?`is-floating`:``} ${p?`is-held`:``} ${x?`is-${x}`:``}`,"aria-label":r,style:n?{"--accent":n}:void 0,children:[(0,P.jsx)(`div`,{className:`part-tabs`,"aria-hidden":p||void 0,children:l}),p&&(0,P.jsxs)(`div`,{className:`puck-layer`,children:[(0,P.jsxs)(`span`,{className:`puck-side is-prev ${i?``:`is-end`}`,"aria-hidden":`true`,children:[(0,P.jsx)(F,{name:`arrow`,size:14,className:`flip`}),i?.title??`Start of the list`]}),(0,P.jsxs)(`button`,{ref:h,type:`button`,className:`puck`,onClick:()=>ee&&T(ee),"aria-label":`${o} of ${s}. Slide left for the previous topic, right for the next.`,children:[(0,P.jsx)(`span`,{className:`puck-grip`,"aria-hidden":`true`}),(0,P.jsx)(`span`,{className:`puck-label`,children:ee?ee.title:`${o} / ${s}`})]}),(0,P.jsxs)(`span`,{className:`puck-side is-next ${a?``:`is-end`}`,"aria-hidden":`true`,children:[a?.title??`End of the list`,(0,P.jsx)(F,{name:`arrow`,size:14})]})]})]})}function hs({topic:e,part:t,onNavigate:n}){let r=(0,_.useRef)(null),i=Ye(r),[a,o]=(0,_.useState)(null),s=(0,_.useMemo)(()=>V.filter(e=>is(e.slug)),[]),c=s.findIndex(t=>t.slug===e.slug),l=e=>{let n=s[c+e];return n?{...n,route:`dsa/${n.slug}/${t.id}`}:null},u=l(-1),d=l(1),f=(0,_.useMemo)(()=>as(e.slug),[e.slug]);(0,_.useEffect)(()=>{let n=!0;return o(null),ss(e.slug,t.id).then(e=>{n&&o(e??``)}),()=>{n=!1}},[e.slug,t.id]),(0,_.useEffect)(()=>{window.scrollTo({top:0})},[e.slug,t.id]),tt({onLeft:(0,_.useCallback)(()=>d&&n(d.route),[d,n]),onRight:(0,_.useCallback)(()=>u&&n(u.route),[u,n])});let p=(0,_.useMemo)(()=>a?wi(a):[],[a]),m=(r=!1)=>(0,P.jsxs)(ms,{ghost:r,floating:i,accent:e.accent,ariaLabel:`Depth`,previous:u,next:d,position:c+1,total:s.length,onNavigate:n,children:[f.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`part-tab ${e.id===t.id?`is-active`:``}`,onClick:()=>n(e.route),"aria-current":e.id===t.id?`page`:void 0,title:e.blurb,children:e.label},e.id)),(0,P.jsxs)(`button`,{type:`button`,className:`part-tab is-aside`,onClick:()=>n(e.route),title:`The ${e.counts.all} problems in this topic.`,children:[`Problems`,(0,P.jsx)(`span`,{className:`band-count`,children:e.counts.all})]})]});return(0,P.jsxs)(`article`,{className:`article topic dsa-notes`,style:{"--accent":e.accent},children:[(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm back`,onClick:()=>n(e.route),children:[(0,P.jsx)(F,{name:`arrow`,size:15,className:`flip`}),(0,P.jsxs)(`span`,{children:[e.title,` problems`]})]}),(0,P.jsxs)(`header`,{className:`article-head`,children:[(0,P.jsxs)(`div`,{className:`article-meta`,children:[(0,P.jsx)(`span`,{className:`badge badge-article`,children:e.track}),(0,P.jsxs)(`span`,{className:`dim`,children:[`Pattern `,String(e.order).padStart(2,`0`),` of `,V.length]})]}),(0,P.jsx)(`h1`,{children:e.title}),(0,P.jsx)(`p`,{className:`tagline dsa-tagline`,children:e.tagline}),(0,P.jsx)(`p`,{className:`lede`,children:t.id===`revision`?`${e.title}, compressed to the lines worth having in front of you. If one of them is news, the notes are where it comes from.`:`How the pattern works, from nothing. Written for somebody who has not met it, in Java, with every step spelled out.`})]}),(0,P.jsx)(`div`,{className:`part-dock`,ref:r,"aria-hidden":i||void 0,children:m(i)}),i&&(0,y.createPortal)(m(),document.body),a===null?(0,P.jsx)(`div`,{className:`article-body`,children:(0,P.jsx)(_o,{})}):a===``?(0,P.jsxs)(`div`,{className:`empty empty-in`,children:[(0,P.jsx)(F,{name:`search`,size:28}),(0,P.jsx)(`h3`,{children:`Nothing written here yet`}),(0,P.jsx)(`p`,{children:`This pattern has no write-up at this depth. The problems are still there.`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>n(e.route),children:`Back to the problems`})]}):(0,P.jsxs)(`div`,{className:`article-body ${p.length>2?`has-toc`:``}`,children:[p.length>2&&(0,P.jsx)(Ti,{headings:p}),(0,P.jsx)(Mo,{content:a,variant:t.id})]}),(0,P.jsxs)(`footer`,{className:`article-foot`,children:[(0,P.jsx)(`p`,{className:`hint`,children:`Written for this site rather than copied from anywhere. Every program with a Run button was compiled and run before it was printed, and the output shown is what it produced. Nothing here solves a problem on the sheet for you — the point of the pattern is that you can then write the solution yourself.`}),(0,P.jsxs)(`nav`,{className:`article-nav`,children:[u?(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link`,onClick:()=>n(u.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Previous pattern`}),(0,P.jsx)(`strong`,{children:u.title})]}):(0,P.jsx)(`span`,{}),d&&(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link is-next`,onClick:()=>n(d.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Next pattern`}),(0,P.jsx)(`strong`,{children:d.title})]})]})]})]})}var gs=Object.assign({"../content/dsa-problems/add-digits-hints.md":()=>W(()=>import(`./add-digits-hints-CXQ8uK7L.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/add-digits-notes.md":()=>W(()=>import(`./add-digits-notes-C1uyisSY.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/add-two-integers-hints.md":()=>W(()=>import(`./add-two-integers-hints-9Pdz71ZW.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/add-two-integers-notes.md":()=>W(()=>import(`./add-two-integers-notes-phCjk4Mq.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/butterfly-pattern-notes.md":()=>W(()=>import(`./butterfly-pattern-notes-BYOXQYzk.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/count-primes-notes.md":()=>W(()=>import(`./count-primes-notes-OWfVYQ5k.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/cross-x-star-pattern-notes.md":()=>W(()=>import(`./cross-x-star-pattern-notes-DfG2SABD.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/determine-color-of-a-chessboard-square-hints.md":()=>W(()=>import(`./determine-color-of-a-chessboard-square-hints-B8s9KO24.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/determine-color-of-a-chessboard-square-notes.md":()=>W(()=>import(`./determine-color-of-a-chessboard-square-notes-DcCmbJnM.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/diamond-star-pattern-notes.md":()=>W(()=>import(`./diamond-star-pattern-notes-Cieon9eH.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/factorial-trailing-zeroes-hints.md":()=>W(()=>import(`./factorial-trailing-zeroes-hints-C5vlmVEg.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/factorial-trailing-zeroes-notes.md":()=>W(()=>import(`./factorial-trailing-zeroes-notes-KcDkABLv.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/fibonacci-number-hints.md":()=>W(()=>import(`./fibonacci-number-hints-DSuVL8tA.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/fibonacci-number-notes.md":()=>W(()=>import(`./fibonacci-number-notes-s3n3CWSN.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/fizz-buzz-hints.md":()=>W(()=>import(`./fizz-buzz-hints-NctCpzei.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/fizz-buzz-notes.md":()=>W(()=>import(`./fizz-buzz-notes-DTb44rwi.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/full-pyramid-notes.md":()=>W(()=>import(`./full-pyramid-notes-C0VCVkHY.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/happy-number-hints.md":()=>W(()=>import(`./happy-number-hints-TuvynsYE.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/happy-number-notes.md":()=>W(()=>import(`./happy-number-notes-DEDdh9_S.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/hollow-butterfly-pattern-notes.md":()=>W(()=>import(`./hollow-butterfly-pattern-notes-Barq4Bqh.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/hollow-diamond-pattern-notes.md":()=>W(()=>import(`./hollow-diamond-pattern-notes-1VbsXLZ5.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/hollow-hourglass-pattern-notes.md":()=>W(()=>import(`./hollow-hourglass-pattern-notes-ClsuR4VF.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/hollow-pyramid-pattern-notes.md":()=>W(()=>import(`./hollow-pyramid-pattern-notes-BWzVmmjV.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/hollow-square-pattern-notes.md":()=>W(()=>import(`./hollow-square-pattern-notes-8ERiUz-x.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/hourglass-pattern-notes.md":()=>W(()=>import(`./hourglass-pattern-notes-BZTD16SJ.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/inverted-full-pyramid-notes.md":()=>W(()=>import(`./inverted-full-pyramid-notes-CZ8KLQQY.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/inverted-left-half-pyramid-notes.md":()=>W(()=>import(`./inverted-left-half-pyramid-notes-C0kVDBmu.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/inverted-right-half-pyramid-notes.md":()=>W(()=>import(`./inverted-right-half-pyramid-notes-ateJ_N0y.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/left-half-pyramid-notes.md":()=>W(()=>import(`./left-half-pyramid-notes-BSfhDB7s.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/maximum-69-number-hints.md":()=>W(()=>import(`./maximum-69-number-hints-i0Pi4fC5.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/maximum-69-number-notes.md":()=>W(()=>import(`./maximum-69-number-notes-BVdpI9WX.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/number-of-steps-to-reduce-a-number-to-zero-hints.md":()=>W(()=>import(`./number-of-steps-to-reduce-a-number-to-zero-hints-TmHSLQVZ.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/number-of-steps-to-reduce-a-number-to-zero-notes.md":()=>W(()=>import(`./number-of-steps-to-reduce-a-number-to-zero-notes-D6RfI9my.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/palindrome-number-hints.md":()=>W(()=>import(`./palindrome-number-hints-k0AVFvGL.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/palindrome-number-notes.md":()=>W(()=>import(`./palindrome-number-notes-BFFJymKe.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/pascals-triangle-hints.md":()=>W(()=>import(`./pascals-triangle-hints-C3Tfdnu_.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/pascals-triangle-ii-hints.md":()=>W(()=>import(`./pascals-triangle-ii-hints-AQGuapVQ.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/pascals-triangle-ii-notes.md":()=>W(()=>import(`./pascals-triangle-ii-notes-vWIcAG0l.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/pascals-triangle-notes.md":()=>W(()=>import(`./pascals-triangle-notes-CLPtRXUe.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/perfect-number-hints.md":()=>W(()=>import(`./perfect-number-hints-BRVMPPWf.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/perfect-number-notes.md":()=>W(()=>import(`./perfect-number-notes-hmN-2sIH.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/plus-star-pattern-notes.md":()=>W(()=>import(`./plus-star-pattern-notes-D4U0mUxq.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/power-of-three-hints.md":()=>W(()=>import(`./power-of-three-hints-CiOI2DtG.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/power-of-three-notes.md":()=>W(()=>import(`./power-of-three-notes-Dp4pIkLE.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/power-of-two-hints.md":()=>W(()=>import(`./power-of-two-hints-C-8nheVz.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/power-of-two-notes.md":()=>W(()=>import(`./power-of-two-notes-30L5uWxV.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/powx-n-hints.md":()=>W(()=>import(`./powx-n-hints-CfpT99xX.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/powx-n-notes.md":()=>W(()=>import(`./powx-n-notes-DF-mpPAJ.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/right-half-pyramid-hints.md":()=>W(()=>import(`./right-half-pyramid-hints-CmTVDSWD.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/right-half-pyramid-notes.md":()=>W(()=>import(`./right-half-pyramid-notes-CMCN5NM-.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/right-pascal-s-triangle-pattern-notes.md":()=>W(()=>import(`./right-pascal-s-triangle-pattern-notes-CO9bDAxu.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/spiral-matrix-ii-hints.md":()=>W(()=>import(`./spiral-matrix-ii-hints-Cdwgs91V.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/spiral-matrix-ii-notes.md":()=>W(()=>import(`./spiral-matrix-ii-notes-Cazn59uU.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/sqrtx-notes.md":()=>W(()=>import(`./sqrtx-notes-D81Jw8cR.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/square-star-pattern-notes.md":()=>W(()=>import(`./square-star-pattern-notes-Dw-4SRSm.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/ugly-number-hints.md":()=>W(()=>import(`./ugly-number-hints-ZuXMTPHY.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/ugly-number-notes.md":()=>W(()=>import(`./ugly-number-notes-CRzqUjrt.js`).then(e=>e.default),[],import.meta.url),"../content/dsa-problems/zigzag-star-pattern-notes.md":()=>W(()=>import(`./zigzag-star-pattern-notes-DbEELBX_.js`).then(e=>e.default),[],import.meta.url)}),_s={notes:{label:`Notes`,blurb:`The solution, why it works, and what it costs.`},hints:{label:`Hints`,blurb:`How to get there yourself — one step at a time.`}},vs=[`notes`,`hints`],ys=(e,t)=>`../content/dsa-problems/${e}-${t}.md`,bs=e=>ys(e,`notes`)in gs,xs=e=>ys(e,`hints`)in gs,Ss=(e,t,n=`notes`)=>`dsa/${e}/problem/${t}${n===`notes`?``:`/${n}`}`,Cs=(e,t)=>vs.filter(e=>ys(t,e)in gs).map(n=>({id:n,..._s[n],route:Ss(e,t,n)}));function ws(e,t){if(!t||!bs(t))return null;let n=e.bands[0].problems.find(e=>e.key===t);return n?{...n,route:Ss(e.slug,t)}:null}var Ts=(e,t,n)=>{let r=Cs(e,t);return r.find(e=>e.id===n)??r[0]??null},Es=()=>V.flatMap(e=>e.bands[0].problems.filter(e=>bs(e.key)).map(t=>({...t,topic:e,route:Ss(e.slug,t.key)}))),Ds=(e,t=`notes`)=>{let n=gs[ys(e,t)];return n?n().then(C):Promise.resolve(null)};function Os({topic:e,problem:t,part:n,onNavigate:r}){let i=(0,_.useRef)(null),a=Ye(i),[o,s]=(0,_.useState)(null),c=(0,_.useMemo)(()=>Cs(e.slug,t.key),[e.slug,t.key]),l=(0,_.useMemo)(()=>Es(),[]),u=l.findIndex(e=>e.key===t.key),d=l[u-1]??null,f=l[u+1]??null;(0,_.useEffect)(()=>{let e=!0;return s(null),Ds(t.key,n.id).then(t=>{e&&s(t??``)}),()=>{e=!1}},[t.key,n.id]),(0,_.useEffect)(()=>{window.scrollTo({top:0})},[t.key,n.id]),tt({onLeft:(0,_.useCallback)(()=>f&&r(f.route),[f,r]),onRight:(0,_.useCallback)(()=>d&&r(d.route),[d,r])});let p=(0,_.useMemo)(()=>o?wi(o):[],[o]),m=Pa(t.frequency),h=(t=!1)=>(0,P.jsxs)(ms,{ghost:t,floating:a,accent:e.accent,ariaLabel:`This problem`,previous:d,next:f,position:u+1,total:l.length,onNavigate:r,children:[c.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`part-tab ${e.id===n.id?`is-active`:``}`,onClick:()=>r(e.route),"aria-current":e.id===n.id?`page`:void 0,title:e.blurb,children:e.label},e.id)),(0,P.jsxs)(`button`,{type:`button`,className:`part-tab is-aside`,onClick:()=>r(e.route),title:`The ${e.counts.all} problems in ${e.title}.`,children:[`Problems`,(0,P.jsx)(`span`,{className:`band-count`,children:e.counts.all})]})]});return(0,P.jsxs)(`article`,{className:`article topic dsa-problem`,style:{"--accent":e.accent},children:[(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm back`,onClick:()=>r(e.route),children:[(0,P.jsx)(F,{name:`arrow`,size:15,className:`flip`}),(0,P.jsxs)(`span`,{children:[e.title,` problems`]})]}),(0,P.jsxs)(`header`,{className:`article-head`,children:[(0,P.jsxs)(`div`,{className:`article-meta`,children:[(0,P.jsx)(`span`,{className:`badge badge-article band-pill band-${t.band}`,children:wa[t.band].label}),(0,P.jsx)(`span`,{className:`dim`,children:e.title}),(0,P.jsxs)(`span`,{className:`dim freq-key tier-${m}`,children:[(0,P.jsx)(`span`,{className:`freq-swatch`,"aria-hidden":`true`}),Ia(t.frequency),` companies asked`,(0,P.jsxs)(`span`,{className:`sr-only`,children:[`, `,Na[m-1].label]})]})]}),(0,P.jsx)(`h1`,{children:t.name}),(0,P.jsx)(`p`,{className:`lede`,children:n.id===`hints`?`A ladder of nudges, smallest first. Read one, go back to the problem, and only come back for the next if you are still stuck — there is no code here and that is on purpose.`:`How to approach it, the solution, and what it costs. Nothing here is a substitute for writing it yourself — read it after you have tried, or when you are stuck on why rather than on what.`}),t.url&&(0,P.jsx)(`p`,{className:`dsa-problem-links`,children:(0,P.jsxs)(`a`,{className:`btn btn-ghost btn-sm`,href:t.url,target:`_blank`,rel:`noreferrer`,children:[(0,P.jsx)(`span`,{children:`Open the problem`}),(0,P.jsx)(F,{name:`link`,size:14})]})})]}),(0,P.jsx)(`div`,{className:`part-dock`,ref:i,"aria-hidden":a||void 0,children:h(a)}),a&&(0,y.createPortal)(h(),document.body),o===null?(0,P.jsx)(`div`,{className:`article-body`,children:(0,P.jsx)(_o,{})}):o===``?(0,P.jsxs)(`div`,{className:`empty empty-in`,children:[(0,P.jsx)(F,{name:`search`,size:28}),(0,P.jsx)(`h3`,{children:`Nothing written here yet`}),(0,P.jsx)(`p`,{children:`This problem has no write-up. The list it belongs to is still there.`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>r(e.route),children:`Back to the problems`})]}):(0,P.jsxs)(`div`,{className:`article-body ${p.length>2?`has-toc`:``}`,children:[p.length>2&&(0,P.jsx)(Ti,{headings:p}),(0,P.jsx)(Mo,{content:o,variant:n.id===`hints`?`hints`:`problem`})]}),(0,P.jsxs)(`footer`,{className:`article-foot`,children:[(0,P.jsx)(`p`,{className:`hint`,children:n.id===`hints`?`Hints are written to be read one at a time and stopped at. If the last one has not been enough, the notes have the whole solution — but a problem you finish after a nudge is worth several you read the answer to.`:`Written for this site rather than copied from anywhere. Every program with a Run button was compiled and run before it was printed, and the output shown is what it produced. The complexity charts are drawn from the code above them — they show what this solution costs and nothing else.`}),(0,P.jsxs)(`nav`,{className:`article-nav`,children:[d?(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link`,onClick:()=>r(d.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Previous problem`}),(0,P.jsx)(`strong`,{children:d.name})]}):(0,P.jsx)(`span`,{}),f&&(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link is-next`,onClick:()=>r(f.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Next problem`}),(0,P.jsx)(`strong`,{children:f.name})]})]})]})]})}function ks({kind:e,label:t,points:n}){let r=Array.isArray(n)?n:[n];return(0,P.jsxs)(`div`,{className:`pattern-note is-${e}`,children:[(0,P.jsx)(`span`,{className:`pattern-label`,children:t}),(0,P.jsx)(`ul`,{children:r.map(e=>(0,P.jsx)(`li`,{children:As(e)},e))})]})}var As=e=>e.split("`").map((e,t)=>t%2?(0,P.jsx)(`code`,{children:e},t):(0,P.jsx)(`span`,{children:e},t));function js({pattern:e,title:t,slug:n,onNavigate:r}){let i=(0,_.useMemo)(()=>gi(e.shape,`java`),[e.shape]),a=(0,_.useMemo)(()=>as(n),[n]);return(0,P.jsxs)(`section`,{className:`pattern`,"aria-label":`${t}: how the pattern works`,children:[(0,P.jsxs)(`header`,{className:`pattern-head`,children:[(0,P.jsx)(`h2`,{children:`How this pattern works`}),(0,P.jsx)(`span`,{className:`pattern-cost`,title:`What it costs when written correctly`,children:e.cost})]}),(0,P.jsxs)(`div`,{className:`pattern-grid`,children:[(0,P.jsx)(ks,{kind:`spot`,label:`Spot it when`,points:e.spot}),(0,P.jsx)(ks,{kind:`watch`,label:`Watch for`,points:e.watch})]}),(0,P.jsxs)(`figure`,{className:`pattern-shape`,children:[(0,P.jsx)(`figcaption`,{children:`The shape`}),(0,P.jsx)(`pre`,{children:(0,P.jsx)(`code`,{className:`language-java hljs`,dangerouslySetInnerHTML:{__html:i}})})]}),a.length>0&&(0,P.jsxs)(`div`,{className:`pattern-more`,children:[a.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-sm ${e.id===`notes`?`btn-primary`:`btn-ghost`}`,onClick:()=>r(e.route),title:e.blurb,children:[(0,P.jsx)(F,{name:e.id===`notes`?`book`:`bolt`,size:15}),(0,P.jsx)(`span`,{children:e.id===`notes`?`Read the notes`:e.label})]},e.id)),(0,P.jsx)(`span`,{className:`pattern-more-hint dim`,children:`The pattern from nothing — worked examples, the Java, and the mistakes in the order people make them.`})]})]})}function Ms({problem:e,slug:t,onNavigate:n}){return bs(e.key)?(0,P.jsxs)(`span`,{className:`written`,children:[(0,P.jsx)(`button`,{type:`button`,className:`written-go`,title:`The full write-up for ${e.name}`,onClick:()=>n(Ss(t,e.key)),children:`Read the notes`}),xs(e.key)&&(0,P.jsx)(`button`,{type:`button`,className:`written-hint`,title:`A nudge towards ${e.name}, without the answer`,onClick:()=>n(Ss(t,e.key,`hints`)),children:`Hint`})]}):(0,P.jsx)(`span`,{className:`written-none`,"aria-hidden":`true`,children:`—`})}function Ns({problem:e,done:t,onSolve:n}){return(0,P.jsx)(`button`,{type:`button`,className:`solve`,"aria-pressed":t,"aria-label":`Mark ${e.name} as solved`,title:t?`Solved — press to clear`:`Mark as solved`,onClick:()=>n(e.key),children:(0,P.jsx)(`span`,{className:`solve-box`,"aria-hidden":`true`,children:(0,P.jsx)(F,{name:`check`,size:12})})})}function Ps({value:e,peak:t}){let n=Pa(e),r=Na[n-1];return(0,P.jsxs)(`span`,{className:`freq tier-${n}`,title:`${e} companies — ${r.label}: ${r.blurb}`,children:[(0,P.jsx)(`span`,{className:`freq-bar`,style:{"--fill":`${e/t*100}%`},"aria-hidden":`true`}),(0,P.jsx)(`span`,{className:`freq-value`,children:Ia(e)}),(0,P.jsxs)(`span`,{className:`sr-only`,children:[`, asked by `,e,` companies, `,r.label]})]})}function Fs({band:e,slug:t,showBand:n=!1,solved:r,onSolve:i,onNavigate:a}){let[o,s]=(0,_.useState)(null),[c,l]=(0,_.useState)(``),[u,d]=(0,_.useState)(`all`),[f,p]=(0,_.useState)(0),m=(0,_.useMemo)(()=>Math.max(1,...e.problems.map(e=>e.frequency)),[e]),h=(0,_.useMemo)(()=>{let t=c.trim().toLowerCase(),n=e.problems.filter(e=>!(t&&!e.name.toLowerCase().includes(t)||u===`todo`&&r.has(e.key)||u===`done`&&!r.has(e.key)||f&&Pa(e.frequency)<f));if(o){let[e,t]=o,r=t===`up`?1:-1;n=[...n].sort((t,n)=>e===`name`?r*t.name.localeCompare(n.name):r*(t.frequency-n.frequency))}return n},[e,o,c,u,f,r]),g=e=>s(t=>{let n=e===`frequency`?`down`:`up`;return t?.[0]===e?t[1]===n?[e,n===`up`?`down`:`up`]:null:[e,n]}),v=e.problems.filter(e=>r.has(e.key)).length,y={all:e.count,todo:e.count-v,done:v},b=c.trim()!==``||u!==`all`||f!==0||o!==null,x=()=>{l(``),d(`all`),p(0),s(null)};return(0,P.jsxs)(`section`,{className:`problems`,"aria-label":`${e.label} problems`,children:[(0,P.jsxs)(`header`,{className:`problems-head`,children:[(0,P.jsx)(`h2`,{id:e.id,children:e.label===`All`?`Every problem`:e.label}),(0,P.jsxs)(`span`,{className:`problems-count`,children:[v>0&&(0,P.jsxs)(P.Fragment,{children:[(0,P.jsx)(`strong`,{className:`problems-done`,children:v}),(0,P.jsx)(`span`,{className:`dim`,children:` of `})]}),(0,P.jsx)(`strong`,{children:e.count}),(0,P.jsxs)(`span`,{className:`dim`,children:[` `,e.count===1?`problem`:`problems`]})]})]}),(0,P.jsxs)(`div`,{className:`problems-filters`,children:[(0,P.jsxs)(`div`,{className:`search search-inline`,children:[(0,P.jsx)(F,{name:`search`,size:15,className:`search-icon`}),(0,P.jsx)(`input`,{type:`search`,value:c,placeholder:`Find a problem…`,onChange:e=>l(e.target.value),"aria-label":`Find a problem in ${e.label}`})]}),(0,P.jsx)(`div`,{className:`segmented segmented-text`,role:`group`,"aria-label":`Show`,children:[[`all`,`All`],[`todo`,`To do`],[`done`,`Done`]].map(([e,t])=>(0,P.jsxs)(`button`,{type:`button`,className:u===e?`is-active`:``,onClick:()=>d(e),"aria-pressed":u===e,children:[t,(0,P.jsx)(`span`,{className:`band-count`,children:y[e]})]},e))}),(0,P.jsxs)(`label`,{className:`select`,children:[(0,P.jsx)(`span`,{className:`sr-only`,children:`Companies asked`}),(0,P.jsx)(`select`,{value:f,onChange:e=>p(Number(e.target.value)),children:Object.entries(La).map(([e,t])=>(0,P.jsx)(`option`,{value:e,children:t},e))})]}),b&&(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:x,children:`Clear`})]}),h.length===0?(0,P.jsxs)(`p`,{className:`problems-empty`,children:[`No problem here matches that. `,(0,P.jsx)(`button`,{type:`button`,className:`link-button`,onClick:x,children:`Clear the filters`}),` to see all `,e.count,`.`]}):(0,P.jsxs)(`table`,{className:`problem-table`,children:[(0,P.jsxs)(`colgroup`,{children:[(0,P.jsx)(`col`,{className:`col-check`}),(0,P.jsx)(`col`,{className:`col-index`}),n&&(0,P.jsx)(`col`,{className:`col-band`}),(0,P.jsx)(`col`,{className:`col-name`}),(0,P.jsx)(`col`,{className:`col-written`}),(0,P.jsx)(`col`,{className:`col-freq`})]}),(0,P.jsx)(`thead`,{children:(0,P.jsxs)(`tr`,{children:[(0,P.jsx)(`th`,{scope:`col`,className:`col-check`,children:(0,P.jsx)(`span`,{className:`sr-only`,children:`Solved`})}),(0,P.jsx)(`th`,{scope:`col`,className:`col-index`,children:`#`}),n&&(0,P.jsx)(`th`,{scope:`col`,className:`col-band`,children:`Difficulty`}),(0,P.jsx)(`th`,{scope:`col`,className:`col-name`,"aria-sort":o?.[0]===`name`?o[1]===`up`?`ascending`:`descending`:`none`,children:(0,P.jsxs)(`button`,{type:`button`,className:`col-sort`,onClick:()=>g(`name`),children:[`Problem`,(0,P.jsx)(`span`,{className:`col-caret`,"aria-hidden":`true`,children:o?.[0]===`name`?o[1]===`up`?`▲`:`▼`:`↕`})]})}),(0,P.jsx)(`th`,{scope:`col`,className:`col-written`,children:`Notes`}),(0,P.jsx)(`th`,{scope:`col`,className:`col-freq`,"aria-sort":o?.[0]===`frequency`?o[1]===`up`?`ascending`:`descending`:`none`,children:(0,P.jsxs)(`button`,{type:`button`,className:`col-sort`,onClick:()=>g(`frequency`),children:[`Companies asked`,(0,P.jsx)(`span`,{className:`col-caret`,"aria-hidden":`true`,children:o?.[0]===`frequency`?o[1]===`up`?`▲`:`▼`:`↕`})]})})]})}),(0,P.jsx)(`tbody`,{children:h.map((e,o)=>{let s=r.has(e.key);return(0,P.jsxs)(`tr`,{className:`${s?`is-solved`:``} band-${e.band}`,children:[(0,P.jsx)(`td`,{className:`col-check`,children:(0,P.jsx)(Ns,{problem:e,done:s,onSolve:i})}),(0,P.jsx)(`td`,{className:`col-index`,children:o+1}),n&&(0,P.jsx)(`td`,{className:`col-band`,children:(0,P.jsx)(`span`,{className:`band-pill`,children:wa[e.band].label})}),(0,P.jsx)(`td`,{className:`col-name`,children:e.url?(0,P.jsxs)(`a`,{href:e.url,target:`_blank`,rel:`noreferrer`,children:[(0,P.jsx)(`span`,{className:`problem-name`,children:e.name}),(0,P.jsx)(F,{name:`link`,size:12,className:`problem-out`})]}):bs(e.key)?(0,P.jsx)(`button`,{type:`button`,className:`problem-open`,title:`The write-up for ${e.name}`,onClick:()=>a(Ss(t,e.key)),children:(0,P.jsx)(`span`,{className:`problem-name`,children:e.name})}):(0,P.jsx)(`span`,{className:`problem-plain`,children:e.name})}),(0,P.jsx)(`td`,{className:`col-written`,children:(0,P.jsx)(Ms,{problem:e,slug:t,onNavigate:a})}),(0,P.jsx)(`td`,{className:`col-freq`,children:(0,P.jsx)(Ps,{value:e.frequency,peak:m})})]},`${e.key}-${o}`)})})]}),(0,P.jsxs)(`p`,{className:`freq-legend`,children:[(0,P.jsx)(`span`,{className:`dim`,children:`Companies asked`}),Na.map((e,t)=>(0,P.jsxs)(`span`,{className:`freq-key tier-${e.id}`,children:[(0,P.jsx)(`span`,{className:`freq-swatch`,"aria-hidden":`true`}),e.label,(0,P.jsx)(`span`,{className:`dim`,children:t===0?` under ${Ma[0]}`:t===Na.length-1?` ${Ma[2]}+`:` ${Ma[t-1]}–${Ma[t]-1}`})]},e.id))]})]})}function Is({topic:e,band:t,solved:n,onSolve:r,onClear:i,onNavigate:a}){let o=(0,_.useRef)(null),s=Ye(o),c=V.findIndex(t=>t.slug===e.slug),l=V[c-1]??null,u=V[c+1]??null,d=Ga(t,n),f=t.count>0&&d===t.count;(0,_.useEffect)(()=>{window.scrollTo({top:0})},[e.slug,t.id]),tt({onLeft:(0,_.useCallback)(()=>u&&a(u.route),[u,a]),onRight:(0,_.useCallback)(()=>l&&a(l.route),[l,a])});let p=(r=!1)=>(0,P.jsx)(ms,{ghost:r,floating:s,accent:e.accent,ariaLabel:`Difficulty`,previous:l,next:u,position:e.order,total:V.length,onNavigate:a,children:e.bands.map(e=>{let r=Ga(e,n);return(0,P.jsxs)(`button`,{type:`button`,className:`part-tab band-${e.id} ${e.id===t.id?`is-active`:``} ${r===e.count?`is-done`:``}`,onClick:()=>a(e.route),"aria-current":e.id===t.id?`page`:void 0,title:`${e.blurb} ${r} of ${e.count} done.`,children:[e.label,(0,P.jsx)(`span`,{className:`band-count`,children:r?`${r}/${e.count}`:e.count})]},e.id)})});return(0,P.jsxs)(`article`,{className:`article topic dsa-sheet`,style:{"--accent":e.accent},children:[(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm back`,onClick:()=>a(`dsa`),children:[(0,P.jsx)(F,{name:`arrow`,size:15,className:`flip`}),(0,P.jsx)(`span`,{children:`All DSA topics`})]}),(0,P.jsxs)(`header`,{className:`article-head`,children:[(0,P.jsxs)(`div`,{className:`article-meta`,children:[(0,P.jsx)(`span`,{className:`badge badge-article`,children:e.track}),(0,P.jsxs)(`span`,{className:`dim`,children:[`Topic `,String(e.order).padStart(2,`0`),` of `,V.length]})]}),(0,P.jsx)(`h1`,{children:e.title}),(0,P.jsx)(`p`,{className:`tagline dsa-tagline`,children:e.tagline}),(0,P.jsx)(`p`,{className:`lede`,children:e.summary})]}),(0,P.jsxs)(`section`,{className:`dsa-status`,"aria-label":`Progress`,children:[(0,P.jsx)(Uo,{done:d,total:t.count,size:54,label:`${d} of ${t.count} ${t.label.toLowerCase()} problems done`}),(0,P.jsxs)(`div`,{className:`dsa-status-text`,children:[(0,P.jsxs)(`strong`,{children:[d,` of `,t.count,` done`,f?` — the whole band`:``]}),(0,P.jsx)(Wo,{topic:e,solved:n}),(0,P.jsx)(`span`,{className:`dim`,children:e.bands.filter(e=>e.id!==`all`).map(e=>`${e.label} ${Ga(e,n)}/${e.count}`).join(` · `)})]}),d>0&&(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:()=>i(e),children:`Clear this topic`})]}),(0,P.jsx)(`div`,{className:`part-dock`,ref:o,"aria-hidden":s||void 0,children:p(s)}),s&&(0,y.createPortal)(p(),document.body),e.note&&(0,P.jsx)(`p`,{className:`hint dsa-note`,children:e.note}),e.pattern&&(0,P.jsx)(js,{pattern:e.pattern,title:e.title,slug:e.slug,onNavigate:a}),(0,P.jsx)(Fs,{band:t,slug:e.slug,showBand:t.id===`all`,solved:n,onSolve:r,onNavigate:a}),(0,P.jsxs)(`footer`,{className:`article-foot`,children:[(0,P.jsxs)(`p`,{className:`hint`,children:[`The order inside a band is the order the sheet was written in, not a ranking — press a column heading to sort by it, and press it again to get the sheet's order back. `,(0,P.jsx)(`strong`,{children:`Companies asked`}),` is how many companies the sheet has asking a problem, drawn as a bar against the largest in this table and written as a band — `,(0,P.jsx)(`code`,{children:`30+`}),` — because the gap between a 32 and a 33 is not one to act on. The exact figure is on the row's tooltip, and sorting uses it. Ticks are kept in this browser only: nothing is sent anywhere, and clearing site data clears them. Nothing here is solved for you — the link is the problem, and the point is to write it yourself.`]}),(0,P.jsxs)(`nav`,{className:`article-nav`,children:[l?(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link`,onClick:()=>a(l.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Previous`}),(0,P.jsx)(`strong`,{children:l.title})]}):(0,P.jsx)(`span`,{}),u&&(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link is-next`,onClick:()=>a(u.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Next`}),(0,P.jsx)(`strong`,{children:u.title})]})]})]})]})}function Ls({onSupport:e,onCredits:t,articles:n=!1}){return(0,P.jsxs)(`footer`,{className:`site-footer`,children:[(0,P.jsxs)(Ct,{className:`footer-cta`,variant:`zoom`,children:[(0,P.jsx)(`h2`,{children:n?`Something wrong in a guide?`:`Something missing?`}),(0,P.jsx)(`p`,{children:n?`The guides are public. Open an issue with what did not work on your machine, or send a pull request with the fix — steps that only work on one setup are worth knowing about.`:`The repositories are private. Ask for access and you can open an issue, send a pull request, or fork any of them and make it yours.`}),(0,P.jsxs)(`div`,{className:`footer-buttons`,children:[(0,P.jsxs)(`a`,{className:`btn btn-primary`,href:n?`https://github.com/${Dt}/${Wt}/issues`:`https://github.com/${Dt}`,target:`_blank`,rel:`noreferrer`,children:[(0,P.jsx)(F,{name:`github`,size:16}),(0,P.jsx)(`span`,{children:n?`Open an issue`:`Request access`}),(0,P.jsx)(F,{name:`arrow`,size:15})]}),(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-coffee`,onClick:e,children:[(0,P.jsx)(bt,{name:`coffee-love`,size:30,children:(0,P.jsx)(F,{name:`coffee`,size:16})}),(0,P.jsx)(`span`,{children:`Buy me a coffee`})]})]})]}),(0,P.jsxs)(`div`,{className:`footer-bar`,children:[(0,P.jsxs)(`span`,{children:[`© `,new Date().getFullYear(),` `,Ot,` · MIT licensed`]}),(0,P.jsx)(`button`,{type:`button`,className:`footer-link`,onClick:t,children:`Credits`})]})]})}var Rs=[{id:`study`,label:`Course`,sections:[{id:`study`,label:`Code`,route:null},{id:`dsa`,label:`DSA`,route:`dsa`},{id:`design`,label:`System Design`,route:`design`},{id:`advanced`,label:`Spring`,route:`advanced`},{id:`interview`,label:`Interview`,route:`interview`}]},{id:`apps`,label:`Apps`,sections:[{id:`tools`,label:`Tools`,route:`tools`},{id:`apps`,label:`Apps`,route:`apps`},{id:`articles`,label:`Articles`,route:`articles`}]}],zs=new Map(Rs.flatMap(e=>e.sections.map(t=>[t.id,e]))),Bs=e=>zs.get(e)??Rs[0],Vs=e=>e.sections[0].route,Hs=()=>{let e=[];for(let t of O)for(let n of t.parts)e.push({id:n.route,kind:`Study`,title:t.title,detail:`${n.label} · ${t.subject}`,route:n.route,accent:t.accent,text:`${t.title} ${t.summary} ${n.label} ${t.subject} ${n.content}`.toLowerCase()});for(let t of Le)for(let n of t.parts)e.push({id:n.route,kind:t.subject===`Spring`?`Spring`:`Deep dive`,title:t.title,detail:`${n.label} · ${t.stage}`,route:n.route,accent:t.accent,text:`${t.title} ${t.tagline} ${t.summary} ${n.label} ${t.subject} ${n.content}`.toLowerCase()});for(let t of ya)for(let n of t.parts)e.push({id:n.route,kind:`Interview`,title:t.title,detail:`${n.label} · ${t.stage}`,route:n.route,accent:t.accent,text:`${t.title} ${t.tagline} ${t.summary} ${n.label} ${t.subject} ${n.content}`.toLowerCase()});for(let t of oa)for(let n of t.parts)e.push({id:n.route,kind:`Design`,title:t.title,detail:`${n.label} · ${t.stage}`,route:n.route,accent:t.accent,text:`${t.title} ${t.tagline} ${t.summary} ${n.label} ${t.subject} ${n.content}`.toLowerCase()});for(let t of V)for(let n of t.bands.filter(e=>e.id!==`all`))e.push({id:n.route,kind:`DSA`,title:t.title,detail:`${n.label} · ${n.count} problems`,route:n.route,accent:t.accent,text:`${t.title} ${t.tagline} ${t.summary} ${t.track} ${n.label} ${n.problems.map(e=>e.name).join(` `)}`.toLowerCase()});for(let t of Kt)e.push({id:t.route,kind:`Guide`,title:t.title,detail:t.category,route:t.route,accent:t.accent,text:`${t.title} ${t.summary} ${t.tags.join(` `)} ${t.content}`.toLowerCase()});for(let t of Xa)e.push({id:t.route,kind:`Tool`,title:t.name,detail:t.status===`live`?t.tagline:`Upcoming`,route:t.status===`live`?t.route:`tools`,accent:t.accent,text:`${t.name} ${t.tagline} ${t.summary} ${t.tags.join(` `)}`.toLowerCase()});for(let t of jt)e.push({id:t.id,kind:`App`,title:t.name,detail:t.tagline,route:t.id,accent:t.accent,text:`${t.name} ${t.tagline} ${t.summary} ${t.tags.join(` `)}`.toLowerCase()});return e},Us=(e,t)=>{let n=e.title.toLowerCase(),r=0;for(let i of t){if(!e.text.includes(i))return 0;n===i?r+=100:n.startsWith(i)?r+=50:n.includes(i)?r+=30:e.detail.toLowerCase().includes(i)?r+=10:r+=1}return r};function Ws({onNavigate:e}){let[t,n]=(0,_.useState)(!1),[r,i]=(0,_.useState)(``),[a,o]=(0,_.useState)(0),s=(0,_.useRef)(null),c=(0,_.useMemo)(Hs,[]),l=(0,_.useMemo)(()=>{let e=r.trim().toLowerCase().split(/\s+/).filter(Boolean);return e.length?c.map(t=>({row:t,score:Us(t,e)})).filter(e=>e.score>0).sort((e,t)=>t.score-e.score).slice(0,12).map(e=>e.row):[]},[r,c]);(0,_.useEffect)(()=>{let e=e=>{(e.ctrlKey||e.metaKey)&&e.key.toLowerCase()===`k`&&(e.preventDefault(),n(!0)),e.key===`Escape`&&n(!1)};return window.addEventListener(`keydown`,e),()=>window.removeEventListener(`keydown`,e)},[]),(0,_.useEffect)(()=>{t?s.current?.focus():(i(``),o(0))},[t]),(0,_.useEffect)(()=>o(0),[r]);let u=t=>{t&&(n(!1),e(t.route))};return(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(`button`,{type:`button`,className:`global-search-trigger`,onClick:()=>n(!0),"aria-label":`Search everything`,children:[(0,P.jsx)(F,{name:`search`,size:15}),(0,P.jsx)(`span`,{className:`hide-sm`,children:`Search`}),(0,P.jsx)(`kbd`,{className:`hide-sm`,children:`Ctrl K`})]}),t&&(0,y.createPortal)((0,P.jsx)(`div`,{className:`search-overlay`,onClick:()=>n(!1),role:`presentation`,children:(0,P.jsxs)(`div`,{className:`search-modal`,onClick:e=>e.stopPropagation(),role:`dialog`,"aria-modal":`true`,"aria-label":`Search everything`,children:[(0,P.jsxs)(`div`,{className:`search-modal-input`,children:[(0,P.jsx)(F,{name:`search`,size:17}),(0,P.jsx)(`input`,{ref:s,type:`text`,value:r,placeholder:`Search notes, questions, guides, apps, tools…`,onChange:e=>i(e.target.value),onKeyDown:e=>{e.key===`ArrowDown`?(e.preventDefault(),o(e=>Math.min(l.length-1,e+1))):e.key===`ArrowUp`?(e.preventDefault(),o(e=>Math.max(0,e-1))):e.key===`Enter`&&(e.preventDefault(),u(l[a]))}}),(0,P.jsxs)(`button`,{type:`button`,className:`search-close`,onClick:()=>n(!1),"aria-label":`Close search`,children:[(0,P.jsx)(`kbd`,{"aria-hidden":`true`,children:`Esc`}),(0,P.jsx)(F,{name:`close`,size:16,className:`search-close-icon`})]})]}),r.trim()!==``&&(0,P.jsxs)(`ul`,{className:`search-results`,children:[l.length===0&&(0,P.jsx)(`li`,{className:`search-empty`,children:`Nothing matches that.`}),l.map((e,t)=>(0,P.jsx)(`li`,{children:(0,P.jsxs)(`button`,{type:`button`,className:`search-result ${t===a?`is-active`:``}`,style:{"--accent":e.accent},onMouseEnter:()=>o(t),onClick:()=>u(e),children:[(0,P.jsx)(`span`,{className:`search-kind`,children:e.kind}),(0,P.jsxs)(`span`,{className:`search-text`,children:[(0,P.jsx)(`strong`,{children:e.title}),(0,P.jsx)(`span`,{className:`dim`,children:e.detail})]}),(0,P.jsx)(F,{name:`arrow`,size:14})]})},e.id))]})]})}),document.body)]})}function Gs(e){let t=(0,_.useRef)(null),[n,r]=(0,_.useState)(null);return(0,_.useLayoutEffect)(()=>{let e=()=>{let e=t.current,n=e?.querySelector(`[data-active="true"]`);if(!n)return;let i=e.getBoundingClientRect(),a=n.getBoundingClientRect();r({"--tab-x":`${a.left-i.left}px`,"--tab-w":`${a.width}px`,"--tab-ready":1})};e();let n=typeof ResizeObserver<`u`?new ResizeObserver(e):null;return t.current&&n?.observe(t.current),document.fonts?.ready.then(e).catch(()=>{}),window.addEventListener(`resize`,e),()=>{n?.disconnect(),window.removeEventListener(`resize`,e)}},[e]),[t,n]}function Ks({items:e,active:t,onSelect:n,className:r=``,label:i}){let[a,o]=Gs(`${t}|${e.map(e=>e.id).join(`,`)}`);return(0,P.jsx)(`nav`,{className:`tabs ${r}`,"aria-label":i,ref:a,style:o??void 0,children:e.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`tab ${t===e.id?`is-active`:``}`,"data-active":t===e.id,onClick:()=>n(e),"aria-current":t===e.id?`page`:void 0,children:e.label},e.id))})}var qs={light:`sun`,relax:`eye`,dark:`moon`,system:`monitor`},Js={light:`Light`,relax:`Eye Relax`,dark:`Dark`,system:`System`};function Ys({theme:e,onCycleTheme:t,onSupport:n,tab:r,onNavigate:i,scrolled:a,hidden:o}){let[s,c]=(0,_.useState)(0);(0,_.useEffect)(()=>c(e=>e+1),[e]);let l=Bs(r);return(0,P.jsxs)(`header`,{className:`site-header ${a?`is-scrolled`:``} ${o?`is-hidden`:``}`,children:[(0,P.jsxs)(`button`,{type:`button`,className:`brand`,onClick:()=>{i(null),window.scrollTo({top:0})},children:[(0,P.jsx)(`span`,{className:`brand-mark`,"aria-hidden":`true`,children:(0,P.jsx)(F,{name:`moon`,size:17})}),(0,P.jsxs)(`span`,{className:`brand-text`,children:[`After Hours`,(0,P.jsx)(`span`,{className:`brand-dot`,children:`.`})]})]}),(0,P.jsx)(Ks,{items:Rs,active:l.id,onSelect:e=>i(Vs(e)),label:`Sections`}),(0,P.jsxs)(`nav`,{className:`header-actions`,children:[(0,P.jsx)(Ws,{onNavigate:i}),(0,P.jsxs)(`a`,{className:`btn btn-ghost hide-sm`,href:`https://github.com/${Dt}`,target:`_blank`,rel:`noreferrer`,children:[(0,P.jsx)(F,{name:`github`,size:16}),(0,P.jsx)(`span`,{children:`GitHub`})]}),(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-coffee`,onClick:n,children:[(0,P.jsx)(bt,{name:`coffee-love`,size:30,children:(0,P.jsx)(F,{name:`coffee`,size:16})}),(0,P.jsx)(`span`,{className:`hide-sm`,children:`Buy me a coffee`})]}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-icon`,onClick:t,title:`Theme: ${Js[e]} — click to change`,"aria-label":`Theme: ${Js[e]}. Click to change.`,children:(0,P.jsx)(`span`,{className:`theme-icon`,children:(0,P.jsx)(F,{name:qs[e],size:17})},s)})]})]})}function Xs({total:e,platforms:t,guides:n,onArticles:r}){return(0,P.jsxs)(St,{id:`top`,className:`hero`,name:`design`,width:380,height:380,children:[(0,P.jsxs)(`p`,{className:`eyebrow`,children:[(0,P.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`Free, no accounts, no telemetry`]}),(0,P.jsxs)(`h1`,{children:[`Small desktop tools that`,(0,P.jsx)(`br`,{}),(0,P.jsx)(`span`,{className:`grad`,children:`do one thing well.`})]}),(0,P.jsx)(`p`,{className:`lede`,children:`Windows utilities built after hours — grab a build, free and with nothing to sign up for. The repositories are private, so ask if you want to read the source. Everything here runs entirely on your machine.`}),(0,P.jsx)(`ul`,{className:`hero-stats`,children:[[e,`apps`],[n,`guides`],[t,`platforms`],[0,`trackers`]].map(([e,t],n)=>(0,P.jsxs)(`li`,{style:{"--i":n},children:[(0,P.jsx)(`strong`,{children:e}),(0,P.jsx)(`span`,{children:t})]},t))}),(0,P.jsxs)(`p`,{className:`hero-aside`,children:[`There are also`,` `,(0,P.jsxs)(`button`,{type:`button`,className:`link-button`,onClick:r,children:[n,` written guides`]}),` `,`— Windows fixes and Raspberry Pi setups, start to finish, nothing to download.`]}),(0,P.jsxs)(`div`,{className:`hero-notes`,children:[(0,P.jsxs)(`span`,{children:[(0,P.jsx)(F,{name:`shield`,size:14}),` No telemetry`]}),(0,P.jsxs)(`span`,{children:[(0,P.jsx)(F,{name:`github`,size:14}),` Source on request`]}),(0,P.jsxs)(`span`,{children:[(0,P.jsx)(F,{name:`download`,size:14}),` Portable builds`]})]})]})}var Zs=(e,t,n)=>{if(!t.length)return!0;let r=n.get(e.slug);return t.every(e=>r.includes(e))};function Qs({topic:e,onOpen:t}){return(0,P.jsx)(`span`,{className:`part-links`,children:e.parts.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`part-link is-${e.id}`,onClick:n=>{n.stopPropagation(),t(e.route)},children:e.label},e.id))})}function $s({subject:e,terms:t,haystacks:n,onOpen:r}){let i=(0,_.useMemo)(()=>re(e).map(e=>({...e,topics:e.topics.filter(e=>Zs(e,t,n))})).filter(e=>e.topics.length>0),[e,t,n]),a=i.reduce((e,t)=>e+t.topics.length,0);if(a===0)return null;let o=0;return(0,P.jsxs)(`section`,{className:`syllabus`,"aria-label":`${e} contents`,children:[(0,P.jsxs)(`header`,{className:`syllabus-head`,children:[(0,P.jsx)(`h2`,{children:e}),(0,P.jsxs)(`span`,{className:`dim`,children:[a,` `,a===1?`topic`:`topics`,`, in order — notes, quick revision and interview questions for each`]})]}),i.map(e=>(0,P.jsxs)(Ct,{className:`syllabus-stage`,variant:`blur`,children:[(0,P.jsxs)(`h3`,{children:[(0,P.jsx)(`span`,{className:`stage-dot`,"aria-hidden":`true`}),e.stage]}),(0,P.jsx)(`ol`,{children:e.topics.map((e,t)=>{o+=1;let n=e.chapter??o;return(0,P.jsx)(`li`,{style:{"--accent":e.accent,"--i":t},children:(0,P.jsxs)(`div`,{className:`syllabus-item`,children:[(0,P.jsx)(`span`,{className:`syllabus-number`,children:e.chapter===null?`••`:String(n).padStart(2,`0`)}),(0,P.jsxs)(`span`,{className:`syllabus-text`,children:[(0,P.jsx)(`button`,{type:`button`,className:`syllabus-title`,onClick:()=>r(e.route),children:e.title}),(0,P.jsx)(`span`,{className:`dim`,children:e.tagline??e.summary}),(0,P.jsx)(Qs,{topic:e,onOpen:r})]})]})},e.slug)})})]},e.stage))]})}function ec({query:e,onQuery:t,subject:n,onSubject:r,onOpen:i}){let a=(0,_.useMemo)(()=>e.trim().toLowerCase().split(/\s+/).filter(Boolean),[e]),o=n&&k.includes(n)?n:null,s=(0,_.useMemo)(()=>new Map(O.map(e=>[e.slug,ne(e)])),[]),c=(0,_.useMemo)(()=>O.filter(e=>Zs(e,a,s)),[a,s]),l=o?[o]:k;return(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(St,{children:[(0,P.jsxs)(`p`,{className:`eyebrow`,children:[(0,P.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`Java notes and interview prep`]}),(0,P.jsxs)(`h1`,{children:[`Learn it, revise it,`,(0,P.jsx)(`br`,{}),(0,P.jsx)(`span`,{className:`grad`,children:`then get asked about it.`})]}),(0,P.jsxs)(`p`,{className:`lede`,children:[`Every chapter comes in three passes. `,(0,P.jsx)(`strong`,{children:`Notes`}),` explains it from nothing. `,(0,P.jsx)(`strong`,{children:`Quick revision`}),` is the same chapter compressed to what you would want in front of you the morning of an interview. `,(0,P.jsx)(`strong`,{children:`Interview`}),` is the same ground as questions with full answers, graded easy to hard. Jump straight to any of them from here, or switch between them once a topic is open — and when you want the layer underneath the language, that is the`,` `,(0,P.jsx)(`strong`,{children:`Advanced`}),` section.`]})]}),(0,P.jsxs)(`div`,{className:`toolbar`,children:[(0,P.jsx)(`div`,{className:`toolbar-row`,children:(0,P.jsxs)(`div`,{className:`search`,children:[(0,P.jsx)(F,{name:`search`,size:17,className:`search-icon`}),(0,P.jsx)(`input`,{type:`search`,value:e,placeholder:`Search topics, questions, code…`,onChange:e=>t(e.target.value),"aria-label":`Search study notes`})]})}),(0,P.jsxs)(`div`,{className:`chips`,children:[(0,P.jsx)(`button`,{type:`button`,className:`chip ${o?``:`is-active`}`,onClick:()=>r(null),children:`All`}),k.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`chip ${o===e?`is-active`:``}`,onClick:()=>r(o===e?null:e),"aria-pressed":o===e,children:e},e)),(0,P.jsxs)(`span`,{className:`chips-count`,children:[c.length,` `,c.length===1?`topic`:`topics`]})]})]}),c.length===0?(0,P.jsxs)(`div`,{className:`empty empty-in`,children:[(0,P.jsx)(F,{name:`search`,size:28}),(0,P.jsx)(`h3`,{children:`Nothing matches that`}),(0,P.jsx)(`p`,{children:`Try a different word, or clear the filter.`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>{t(``),r(null)},children:`Clear filters`})]}):l.map(e=>(0,P.jsx)($s,{subject:e,terms:a,haystacks:s,onOpen:i},e))]})}function tc({section:e,onNavigate:t}){let n=Bs(e);return(0,P.jsxs)(`div`,{className:`subnav`,children:[(0,P.jsx)(`span`,{className:`subnav-label`,children:n.label}),(0,P.jsx)(Ks,{items:n.sections,active:e,onSelect:e=>t(e.route),className:`subtabs`,label:`${n.label} sections`},n.id)]})}var nc=e=>e.split(/\s+/).slice(0,2).map(e=>e[0]).join(``);function rc({project:e,size:t=52}){let[n,r]=(0,_.useState)(!1),i={"--size":`${t}px`,"--accent":e.accent};return e.icon&&!n?(0,P.jsx)(`span`,{className:`app-icon`,style:i,children:(0,P.jsx)(`img`,{src:e.icon,alt:``,onError:()=>r(!0),loading:`lazy`})}):(0,P.jsx)(`span`,{className:`app-icon app-icon-mono`,style:i,"aria-hidden":`true`,children:nc(e.name)})}function ic({project:e,view:t,isFavorite:n,onToggleFavorite:r,onOpen:i}){let a=nt();return(0,P.jsxs)(`article`,{className:`card card-${t}`,style:{"--accent":e.accent},onClick:()=>i(e.id),...a,children:[(0,P.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,P.jsx)(`button`,{type:`button`,className:`fav ${n?`is-active`:``}`,onClick:t=>{t.stopPropagation(),r(e.id)},"aria-pressed":n,"aria-label":`${n?`Remove`:`Add`} ${e.name} ${n?`from`:`to`} favourites`,children:(0,P.jsx)(F,{name:`star`,size:15})}),(0,P.jsxs)(`header`,{className:`card-head`,children:[(0,P.jsx)(rc,{project:e,size:t===`list`?44:54}),(0,P.jsxs)(`div`,{className:`card-title`,children:[(0,P.jsx)(`h3`,{children:(0,P.jsx)(`button`,{type:`button`,className:`card-link`,onClick:t=>{t.stopPropagation(),i(e.id)},children:e.name})}),(0,P.jsx)(`p`,{className:`tagline`,children:e.tagline})]})]}),(0,P.jsx)(`p`,{className:`card-summary`,children:e.summary}),(0,P.jsx)(`ul`,{className:`tag-row`,children:e.tags.slice(0,t===`list`?5:3).map(e=>(0,P.jsx)(`li`,{children:e},e))}),(0,P.jsxs)(`footer`,{className:`card-foot`,children:[(0,P.jsxs)(`div`,{className:`meta`,children:[(0,P.jsx)(`span`,{className:`badge badge-${e.status}`,children:Nt[e.status]}),(0,P.jsxs)(`span`,{className:`dim`,children:[`v`,e.version]}),(0,P.jsx)(`span`,{className:`dim hide-sm`,children:e.size})]}),(0,P.jsxs)(`div`,{className:`card-actions`,children:[(0,P.jsxs)(`a`,{className:`btn btn-ghost btn-sm`,href:e.url,target:`_blank`,rel:`noreferrer`,onClick:e=>e.stopPropagation(),"aria-label":`${e.name} source on GitHub`,children:[(0,P.jsx)(F,{name:`github`,size:15}),(0,P.jsx)(`span`,{className:`hide-sm`,children:`Source`})]}),(0,P.jsxs)(`a`,{className:`btn btn-primary btn-sm`,href:e.downloadUrl,target:`_blank`,rel:`noreferrer`,onClick:e=>e.stopPropagation(),children:[(0,P.jsx)(F,{name:`download`,size:15}),(0,P.jsx)(`span`,{children:`Download`})]})]})]})]})}function ac({value:e,label:t=`Copy`,className:n=``}){let[r,i]=(0,_.useState)(!1),a=(0,_.useRef)(null);return(0,_.useEffect)(()=>()=>clearTimeout(a.current),[]),(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost ${r?`copy-pop is-copied`:``} ${n}`,onClick:async()=>{await eo(e),i(!0),clearTimeout(a.current),a.current=setTimeout(()=>i(!1),2e3)},"aria-label":r?`Copied`:t||`Copy`,children:[(0,P.jsx)(F,{name:r?`check`:`copy`,size:15}),(0,P.jsx)(`span`,{children:r?`Copied`:t})]})}function oc({project:e,onClose:t,isFavorite:n,onToggleFavorite:r}){return(0,P.jsx)(So,{onClose:t,labelledBy:`sheet-title`,style:{"--accent":e.accent},children:(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(`div`,{className:`sheet-hero`,children:[(0,P.jsx)(rc,{project:e,size:72}),(0,P.jsxs)(`div`,{children:[(0,P.jsxs)(`div`,{className:`sheet-badges`,children:[(0,P.jsx)(`span`,{className:`badge badge-${e.status}`,children:Nt[e.status]}),(0,P.jsxs)(`span`,{className:`dim`,children:[`v`,e.version]}),(0,P.jsxs)(`span`,{className:`dim`,children:[`Updated `,Yt(e.updated)]})]}),(0,P.jsx)(`h2`,{id:`sheet-title`,children:e.name}),(0,P.jsx)(`p`,{className:`tagline`,children:e.tagline})]})]}),(0,P.jsx)(`p`,{className:`sheet-summary`,children:e.summary}),(0,P.jsxs)(`div`,{className:`sheet-cta`,children:[(0,P.jsxs)(`a`,{className:`btn btn-primary`,href:e.downloadUrl,target:`_blank`,rel:`noreferrer`,children:[(0,P.jsx)(F,{name:`download`,size:16}),(0,P.jsx)(`span`,{children:`Download for Windows`})]}),(0,P.jsxs)(`a`,{className:`btn btn-ghost`,href:e.url,target:`_blank`,rel:`noreferrer`,children:[(0,P.jsx)(F,{name:`github`,size:16}),(0,P.jsx)(`span`,{children:`Repository`})]}),(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost ${n?`is-active`:``}`,onClick:()=>r(e.id),"aria-pressed":n,children:[(0,P.jsx)(F,{name:`star`,size:16}),(0,P.jsx)(`span`,{children:n?`Saved`:`Save`})]})]}),(0,P.jsx)(`p`,{className:`hint`,children:`Downloads are hosted on MediaFire as a portable zip — unpack it anywhere and run the app, no installer. Windows SmartScreen may warn on first run because the binaries are unsigned.`}),(0,P.jsxs)(`p`,{className:`hint`,children:[`The repository itself is private, so the source and issue links open only for accounts that have been granted access.`,` `,(0,P.jsx)(`a`,{href:`https://github.com/${Dt}`,target:`_blank`,rel:`noreferrer`,children:`Ask for access`}),` `,`and mention which app you need.`]}),(0,P.jsxs)(`div`,{className:`sheet-grid`,children:[(0,P.jsxs)(`section`,{children:[(0,P.jsxs)(`h3`,{children:[(0,P.jsx)(F,{name:`bolt`,size:15}),` What it does`]}),(0,P.jsx)(`ul`,{className:`bullets`,children:e.highlights.map((e,t)=>(0,P.jsx)(`li`,{style:{"--i":t},children:e},e))})]}),(0,P.jsxs)(`div`,{className:`sheet-side`,children:[(0,P.jsxs)(`section`,{children:[(0,P.jsxs)(`h3`,{children:[(0,P.jsx)(F,{name:`monitor`,size:15}),` Requirements`]}),(0,P.jsx)(`ul`,{className:`bullets`,children:e.requirements.map((e,t)=>(0,P.jsx)(`li`,{style:{"--i":t},children:e},e))})]}),(0,P.jsxs)(`section`,{children:[(0,P.jsxs)(`h3`,{children:[(0,P.jsx)(F,{name:`shield`,size:15}),` Privacy`]}),(0,P.jsx)(`p`,{className:`muted`,children:e.privacy})]}),(0,P.jsxs)(`section`,{children:[(0,P.jsxs)(`h3`,{children:[(0,P.jsx)(F,{name:`terminal`,size:15}),` Build from source`]}),(0,P.jsxs)(`div`,{className:`code`,children:[(0,P.jsx)(`code`,{children:e.cloneCommand}),(0,P.jsx)(ac,{value:e.cloneCommand,label:``,className:`btn-sm`})]}),(0,P.jsxs)(`p`,{className:`muted small`,children:[`Then `,(0,P.jsx)(`code`,{children:`npm install`}),` and `,(0,P.jsx)(`code`,{children:`npm start`}),`. Cloning needs repository access and a signed-in Git credential.`]})]})]})]}),(0,P.jsxs)(`footer`,{className:`sheet-foot`,children:[(0,P.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,P.jsx)(`li`,{children:e},e))}),(0,P.jsxs)(`div`,{className:`sheet-foot-links`,children:[(0,P.jsxs)(`a`,{href:e.issuesUrl,target:`_blank`,rel:`noreferrer`,children:[(0,P.jsx)(F,{name:`link`,size:14}),` Report an issue`]}),(0,P.jsxs)(`span`,{className:`dim`,children:[e.license,` licensed`]})]})]})]})})}function sc(){return rt(),(0,P.jsx)(`div`,{className:`scroll-progress`,"aria-hidden":`true`})}var cc=[{id:`reference`,title:`Reference`,note:`Worked through while writing the notes and the DSA sheet.`,items:[{name:`GeeksforGeeks`,href:`https://www.geeksforgeeks.org/`,note:`Explanations and problem write-ups`},{name:`AlgorithmXLR8`,href:`https://algorithmxlr8.io/`,note:`Algorithm practice and visualisations`}]},{id:`animation`,title:`Animation`,note:`The cats and the coffee cup, and the player that draws them.`,items:[{name:`LottieFiles`,href:`https://lottiefiles.com/`,note:`Library and the lottie-web player`},{name:`wbprxltvl7`,href:`https://lottiefiles.com/wbprxltvl7`,note:`Animation author`},{name:`Animoox`,href:`https://lottiefiles.com/animoox`,note:`Animation author`},{name:`UX Animation`,href:`https://lottiefiles.com/uxanimation`,note:`Animation author`},{name:`Lifesigner`,href:`https://lottiefiles.com/lifesigner`,note:`Animation author`}]}];function lc({onClose:e}){return(0,P.jsx)(So,{onClose:e,labelledBy:`credits-title`,className:`sheet-sm`,children:(0,P.jsxs)(`div`,{className:`credits`,children:[(0,P.jsx)(`h2`,{id:`credits-title`,children:`Credits`}),(0,P.jsx)(`p`,{className:`muted`,children:`Built on other people’s work. These are the sources and the authors it came from.`}),cc.map(e=>(0,P.jsxs)(`section`,{className:`credit-group`,children:[(0,P.jsx)(`h3`,{children:e.title}),(0,P.jsx)(`p`,{className:`dim`,children:e.note}),(0,P.jsx)(`ul`,{children:e.items.map(e=>(0,P.jsx)(`li`,{children:(0,P.jsxs)(`a`,{href:e.href,target:`_blank`,rel:`noreferrer`,children:[(0,P.jsx)(`span`,{className:`credit-name`,children:e.name}),(0,P.jsx)(`span`,{className:`credit-note`,children:e.note}),(0,P.jsx)(F,{name:`link`,size:15})]})},e.href))})]},e.id))]})})}var uc=`itsadityakr27-8@okicici`,dc=`Aditya Kumar`,fc=`upi://pay?pa=${uc}&pn=${encodeURIComponent(dc)}&cu=INR`,pc=`./upi-qr.svg`,mc=[49,99,199],hc=5e3,gc=e=>`${fc}&am=${e.toFixed(2)}`;function _c({onClose:e}){let[t,n]=(0,_.useState)(1);(0,_.useEffect)(()=>{if(!t)return;let e=setTimeout(()=>n(0),hc);return()=>clearTimeout(e)},[t]);let r=()=>n(e=>e+1);return(0,P.jsx)(So,{onClose:e,labelledBy:`support-title`,className:`sheet-sm`,children:(0,P.jsxs)(`div`,{className:`support`,children:[(0,P.jsxs)(`h2`,{id:`support-title`,children:[(0,P.jsx)(bt,{name:`coffee-love`,size:46,children:(0,P.jsx)(F,{name:`coffee`,size:26})}),(0,P.jsx)(`span`,{children:`Buy me a coffee`})]}),(0,P.jsx)(`p`,{className:`muted`,children:`If something here saved you some time, a coffee is a nice way to say so.`}),(0,P.jsxs)(`figure`,{className:`qr`,children:[(0,P.jsx)(`img`,{className:t>0?`is-behind-cat`:``,src:pc,alt:`UPI QR code for ${uc}`,width:`220`,height:`220`}),t>0&&(0,P.jsx)(`button`,{type:`button`,className:`support-cat`,onClick:()=>n(0),"aria-label":`Hide the cat`,children:(0,P.jsx)(bt,{name:`cat-love`,size:290})},t),(0,P.jsx)(`figcaption`,{children:`Scan with GPay, PhonePe, Paytm, BHIM — any UPI app`})]}),(0,P.jsxs)(`div`,{className:`upi-id`,children:[(0,P.jsx)(`code`,{children:uc}),(0,P.jsx)(ac,{value:uc,label:`Copy UPI ID`,className:`btn-sm`})]}),(0,P.jsxs)(`p`,{className:`support-payee dim`,children:[`Payments go to `,dc]}),(0,P.jsxs)(`div`,{className:`support-amounts`,children:[mc.map(e=>(0,P.jsxs)(`a`,{className:`btn btn-ghost btn-sm`,href:gc(e),onClick:r,children:[`₹`,e]},e)),(0,P.jsxs)(`a`,{className:`btn btn-primary btn-sm`,href:fc,onClick:r,children:[(0,P.jsx)(bt,{name:`coffee-love`,size:26,children:(0,P.jsx)(F,{name:`coffee`,size:15})}),(0,P.jsx)(`span`,{children:`Open UPI app`})]})]}),(0,P.jsx)(`p`,{role:`status`,className:`sr-only`,children:t>1?`Thank you!`:``})]})})}var vc=[{id:`recent`,label:`Recently updated`},{id:`name`,label:`Name (A–Z)`},{id:`status`,label:`Stable first`}];function yc({query:e,onQuery:t,activeTags:n,onToggleTag:r,onClearTags:i,sort:a,onSort:o,view:s,onView:c,favoritesOnly:l,onFavoritesOnly:u,favoriteCount:d,count:f}){let p=(0,_.useRef)(null);return(0,_.useEffect)(()=>{let e=e=>{let n=/^(INPUT|TEXTAREA|SELECT)$/.test(e.target.tagName);e.key===`/`&&!n?(e.preventDefault(),p.current?.focus()):e.key===`Escape`&&e.target===p.current&&(t(``),p.current.blur())};return window.addEventListener(`keydown`,e),()=>window.removeEventListener(`keydown`,e)},[t]),(0,P.jsxs)(`div`,{className:`toolbar`,children:[(0,P.jsxs)(`div`,{className:`toolbar-row`,children:[(0,P.jsxs)(`div`,{className:`search`,children:[(0,P.jsx)(F,{name:`search`,size:17,className:`search-icon`}),(0,P.jsx)(`input`,{ref:p,type:`search`,value:e,placeholder:`Search apps, tags, features…`,onChange:e=>t(e.target.value),"aria-label":`Search apps`}),(0,P.jsx)(`kbd`,{className:`search-kbd`,children:`/`})]}),(0,P.jsxs)(`div`,{className:`toolbar-controls`,children:[(0,P.jsxs)(`label`,{className:`select`,children:[(0,P.jsx)(`span`,{className:`sr-only`,children:`Sort by`}),(0,P.jsx)(`select`,{value:a,onChange:e=>o(e.target.value),children:vc.map(e=>(0,P.jsx)(`option`,{value:e.id,children:e.label},e.id))})]}),(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-icon ${l?`is-active`:``}`,onClick:()=>u(e=>!e),title:`Show favourites only`,"aria-pressed":l,children:[(0,P.jsx)(F,{name:`star`,size:16}),d>0&&(0,P.jsx)(`span`,{className:`pip`,children:d})]}),(0,P.jsx)(`div`,{className:`segmented`,role:`group`,"aria-label":`Layout`,children:[`grid`,`list`].map(e=>(0,P.jsx)(`button`,{type:`button`,className:s===e?`is-active`:``,onClick:()=>c(e),"aria-pressed":s===e,title:`${e[0].toUpperCase()}${e.slice(1)} view`,children:(0,P.jsx)(F,{name:e,size:15})},e))})]})]}),(0,P.jsxs)(`div`,{className:`chips`,children:[(0,P.jsx)(`button`,{type:`button`,className:`chip ${n.length===0?`is-active`:``}`,onClick:i,children:`All`}),Mt.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`chip ${n.includes(e)?`is-active`:``}`,onClick:()=>r(e),"aria-pressed":n.includes(e),children:e},e)),(0,P.jsxs)(`span`,{className:`chips-count`,children:[f,` `,f===1?`app`:`apps`]})]})]})}var bc=`Written for this site rather than copied from a repository. Every program above was compiled and run before it was printed, and the output shown is what it produced — on one machine, with one JDK. Yours may differ in the numbers; it should not differ in the shape.`;function xc({topic:e,part:t,onNavigate:n,onFollowTerm:r,onDetour:i=!1,list:a=O,backRoute:o=`study`,backLabel:s=`All study notes`,note:c=bc}){let l=(0,_.useMemo)(()=>wi(t.content),[t]),u=(0,_.useRef)(null),d=Ye(u),f=a.findIndex(t=>t.slug===e.slug),p=a[f-1]??null,m=a[f+1]??null;(0,_.useEffect)(()=>{window.scrollTo({top:0})},[e.slug,t.id]),tt({onLeft:(0,_.useCallback)(()=>m&&n(m.route),[m,n]),onRight:(0,_.useCallback)(()=>p&&n(p.route),[p,n])});let h=(r=!1)=>(0,P.jsx)(ms,{ghost:r,floating:d,accent:e.accent,ariaLabel:`Depth`,previous:p,next:m,position:f+1,total:a.length,onNavigate:n,children:e.parts.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`part-tab ${e.id===t.id?`is-active`:``}`,onClick:()=>n(e.route),"aria-current":e.id===t.id?`page`:void 0,title:e.blurb,children:e.label},e.id))});return(0,P.jsxs)(`article`,{className:`article topic`,style:{"--accent":e.accent},children:[(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm back`,onClick:()=>n(o),children:[(0,P.jsx)(F,{name:`arrow`,size:15,className:`flip`}),(0,P.jsx)(`span`,{children:s})]}),(0,P.jsxs)(`header`,{className:`article-head`,children:[(0,P.jsxs)(`div`,{className:`article-meta`,children:[(0,P.jsx)(`span`,{className:`badge badge-article`,children:e.subject}),(0,P.jsx)(`span`,{className:`dim`,children:e.stage})]}),(0,P.jsxs)(`h1`,{children:[e.chapter?(0,P.jsx)(`span`,{className:`chapter-number`,children:e.chapter}):null,e.title]}),(0,P.jsx)(`p`,{className:`lede`,children:e.summary})]}),(0,P.jsx)(`div`,{className:`part-dock`,ref:u,"aria-hidden":d||void 0,children:h(d)}),d&&!i&&(0,y.createPortal)(h(),document.body),(0,P.jsxs)(`div`,{className:`article-body ${l.length>2?`has-toc`:``}`,children:[l.length>2&&(0,P.jsx)(Ti,{headings:l}),(0,P.jsx)(Mo,{content:t.content,variant:t.id,onFollowTerm:r})]}),(0,P.jsxs)(`footer`,{className:`article-foot`,children:[(0,P.jsx)(`p`,{className:`hint`,children:c}),(0,P.jsxs)(`nav`,{className:`article-nav`,children:[p?(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link`,onClick:()=>n(p.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Previous`}),(0,P.jsx)(`strong`,{children:p.title})]}):(0,P.jsx)(`span`,{}),m&&(0,P.jsxs)(`button`,{type:`button`,className:`article-nav-link is-next`,onClick:()=>n(m.route),children:[(0,P.jsx)(`span`,{className:`dim`,children:`Next`}),(0,P.jsx)(`strong`,{children:m.title})]})]})]})]})}var Sc=(e,t=0,n=1)=>Math.min(n,Math.max(t,e)),Cc=(e,t=2)=>Number(e.toFixed(t)),wc=({r:e,g:t,b:n,a:r=1},i=!1)=>{let a=e=>Math.round(Sc(e,0,255)).toString(16).padStart(2,`0`),o=`#${a(e)}${a(t)}${a(n)}`;return r<1||i?`${o}${a(r*255)}`:o},Tc=e=>{let t=e.trim().replace(/^#/,``);if((t.length===3||t.length===4)&&(t=[...t].map(e=>e+e).join(``)),t.length!==6&&t.length!==8||!/^[0-9a-f]+$/i.test(t))return null;let n=e=>parseInt(t.slice(e,e+2),16);return{r:n(0),g:n(2),b:n(4),a:t.length===8?n(6)/255:1}};function Ec({r:e,g:t,b:n,a:r=1}){let[i,a,o]=[e/255,t/255,n/255],s=Math.max(i,a,o),c=Math.min(i,a,o),l=s-c,u=(s+c)/2,d=0;l!==0&&(d=s===i?(a-o)/l%6:s===a?(o-i)/l+2:(i-a)/l+4,d*=60,d<0&&(d+=360));let f=l===0?0:l/(1-Math.abs(2*u-1));return{h:d,s:f*100,l:u*100,a:r}}function Dc({h:e,s:t,l:n,a:r=1}){let i=t/100,a=n/100,o=(1-Math.abs(2*a-1))*i,s=(e%360+360)%360/60,c=o*(1-Math.abs(s%2-1)),[l,u,d]=s<1?[o,c,0]:s<2?[c,o,0]:s<3?[0,o,c]:s<4?[0,c,o]:s<5?[c,0,o]:[o,0,c],f=a-o/2;return{r:(l+f)*255,g:(u+f)*255,b:(d+f)*255,a:r}}function Oc({r:e,g:t,b:n,a:r=1}){let[i,a,o]=[e/255,t/255,n/255],s=Math.max(i,a,o),c=s-Math.min(i,a,o),l=0;return c!==0&&(l=s===i?(a-o)/c%6:s===a?(o-i)/c+2:(i-a)/c+4,l*=60,l<0&&(l+=360)),{h:l,s:(s===0?0:c/s)*100,v:s*100,a:r}}function kc({h:e,s:t,v:n,a:r=1}){let i=t/100,a=n/100,o=a*i,s=(e%360+360)%360/60,c=o*(1-Math.abs(s%2-1)),[l,u,d]=s<1?[o,c,0]:s<2?[c,o,0]:s<3?[0,o,c]:s<4?[0,c,o]:s<5?[c,0,o]:[o,0,c],f=a-o;return{r:(l+f)*255,g:(u+f)*255,b:(d+f)*255,a:r}}var Ac=e=>e<=.04045?e/12.92:((e+.055)/1.055)**2.4,jc=e=>e<=.0031308?e*12.92:1.055*e**(1/2.4)-.055;function Mc({r:e,g:t,b:n,a:r=1}){let i=Ac(e/255),a=Ac(t/255),o=Ac(n/255),s=Math.cbrt(.4122214708*i+.5363325363*a+.0514459929*o),c=Math.cbrt(.2119034982*i+.6806995451*a+.1073969566*o),l=Math.cbrt(.0883024619*i+.2817188376*a+.6299787005*o),u=.2104542553*s+.793617785*c-.0040720468*l,d=1.9779984951*s-2.428592205*c+.4505937099*l,f=.0259040371*s+.7827717662*c-.808675766*l,p=Math.sqrt(d*d+f*f),m=Math.atan2(f,d)*180/Math.PI;return m<0&&(m+=360),{l:u*100,c:p,h:p<1e-6?0:m,a:r}}function Nc({l:e,c:t,h:n,a:r=1}){let i=e/100,a=n*Math.PI/180,o=t*Math.cos(a),s=t*Math.sin(a),c=(i+.3963377774*o+.2158037573*s)**3,l=(i-.1055613458*o-.0638541728*s)**3,u=(i-.0894841775*o-1.291485548*s)**3,d=4.0767416621*c-3.3077115913*l+.2309699292*u,f=-1.2684380046*c+2.6097574011*l-.3413193965*u,p=-.0041960863*c-.7034186147*l+1.707614701*u;return{r:Sc(jc(d),0,1)*255,g:Sc(jc(f),0,1)*255,b:Sc(jc(p),0,1)*255,a:r}}var Pc=({l:e,c:t,h:n})=>{let r=e/100,i=n*Math.PI/180,a=t*Math.cos(i),o=t*Math.sin(i),s=(r+.3963377774*a+.2158037573*o)**3,c=(r-.1055613458*a-.0638541728*o)**3,l=(r-.0894841775*a-1.291485548*o)**3;return[4.0767416621*s-3.3077115913*c+.2309699292*l,-1.2684380046*s+2.6097574011*c-.3413193965*l,-.0041960863*s-.7034186147*c+1.707614701*l].every(e=>e>=-1e-4&&e<=1.0001)},Fc=({r:e,g:t,b:n})=>.2126*Ac(e/255)+.7152*Ac(t/255)+.0722*Ac(n/255);function Ic(e,t){let n=Fc(e),r=Fc(t),[i,a]=n>r?[n,r]:[r,n];return(i+.05)/(a+.05)}var G=e=>({aaBody:e>=4.5,aaLarge:e>=3,aaaBody:e>=7,aaaLarge:e>=4.5}),Lc={black:`#000000`,white:`#ffffff`,red:`#ff0000`,lime:`#00ff00`,blue:`#0000ff`,yellow:`#ffff00`,cyan:`#00ffff`,magenta:`#ff00ff`,silver:`#c0c0c0`,gray:`#808080`,grey:`#808080`,maroon:`#800000`,olive:`#808000`,green:`#008000`,purple:`#800080`,teal:`#008080`,navy:`#000080`,orange:`#ffa500`,pink:`#ffc0cb`,brown:`#a52a2a`,gold:`#ffd700`,indigo:`#4b0082`,violet:`#ee82ee`,tomato:`#ff6347`,salmon:`#fa8072`,crimson:`#dc143c`,coral:`#ff7f50`,khaki:`#f0e68c`,plum:`#dda0dd`,orchid:`#da70d6`,turquoise:`#40e0d0`,tan:`#d2b48c`,beige:`#f5f5dc`,ivory:`#fffff0`,lavender:`#e6e6fa`,transparent:`#00000000`},Rc=e=>[...e.matchAll(/-?[\d.]+/g)].map(e=>Number(e[0]));function zc(e){if(!e)return null;let t=String(e).trim().toLowerCase();if(Lc[t])return Tc(Lc[t]);if(t.startsWith(`#`))return Tc(t);if(t.startsWith(`rgb`)){let[e,n,r,i]=Rc(t);if([e,n,r].some(e=>e===void 0))return null;let a=(t.match(/%/g)??[]).length>=3;return{r:a?e/100*255:e,g:a?n/100*255:n,b:a?r/100*255:r,a:i===void 0?1:t.includes(`%`)&&i>1?i/100:i}}if(t.startsWith(`hsl`)){let[e,n,r,i]=Rc(t);return[e,n,r].some(e=>e===void 0)?null:Dc({h:e,s:n,l:r,a:i===void 0?1:i>1?i/100:i})}if(t.startsWith(`hsb`)||t.startsWith(`hsv`)){let[e,n,r,i]=Rc(t);return[e,n,r].some(e=>e===void 0)?null:kc({h:e,s:n,v:r,a:i===void 0?1:i>1?i/100:i})}if(t.startsWith(`oklch`)){let[e,n,r,i]=Rc(t);return[e,n,r].some(e=>e===void 0)?null:Nc({l:t.includes(`%`)||e>1?e:e*100,c:n,h:r,a:i===void 0?1:i>1?i/100:i})}return/^[0-9a-f]{3,8}$/.test(t)?Tc(t):null}function Bc(e){let t=Ec(e),n=Oc(e),r=Mc(e),i=e=>Math.round(e),a=e.a??1;return[{label:`HEX`,value:wc(e)},{label:`HEX 8`,value:wc(e,!0)},{label:`RGB`,value:a<1?`rgb(${i(e.r)} ${i(e.g)} ${i(e.b)} / ${Cc(a)})`:`rgb(${i(e.r)} ${i(e.g)} ${i(e.b)})`},{label:`HSL`,value:a<1?`hsl(${i(t.h)} ${i(t.s)}% ${i(t.l)}% / ${Cc(a)})`:`hsl(${i(t.h)} ${i(t.s)}% ${i(t.l)}%)`},{label:`HSB`,value:`hsb(${i(n.h)}, ${i(n.s)}%, ${i(n.v)}%)`},{label:`OKLCH`,value:a<1?`oklch(${Cc(r.l,1)}% ${Cc(r.c,3)} ${Cc(r.h,1)} / ${Cc(a)})`:`oklch(${Cc(r.l,1)}% ${Cc(r.c,3)} ${Cc(r.h,1)})`}]}function Vc(e,t,n){if(Pc({l:e,c:t,h:n}))return t;let r=0,i=t;for(let t=0;t<18;t++){let t=(r+i)/2;Pc({l:e,c:t,h:n})?r=t:i=t}return r}function Hc(e){let t=Mc(e),n=[96,91,83,73,63,53,45,37,29,21,13],r=[50,100,200,300,400,500,600,700,800,900,950];return n.map((e,i)=>{let a=i/(n.length-1),o=.45+.55*Math.sin(Math.PI*a),s=Vc(e,t.c*o,t.h);return{name:r[i],rgb:Nc({l:e,c:s,h:t.h,a:1})}})}var Uc=(e,t)=>{let{l:n,c:r,h:i,a}=Mc(e),o=(i+t+360)%360;return Nc({l:n,c:Vc(n,r,o),h:o,a})},Wc=e=>[{name:`Complementary`,colors:[e,Uc(e,180)]},{name:`Analogous`,colors:[Uc(e,-30),e,Uc(e,30)]},{name:`Triadic`,colors:[e,Uc(e,120),Uc(e,240)]},{name:`Split complementary`,colors:[e,Uc(e,150),Uc(e,210)]},{name:`Tetradic`,colors:[e,Uc(e,90),Uc(e,180),Uc(e,270)]}];function Gc(e,t,n){let r=Mc(e),i=Mc(t),a=i.h-r.h;return a>180&&(a-=360),a<-180&&(a+=360),Nc({l:r.l+(i.l-r.l)*n,c:r.c+(i.c-r.c)*n,h:r.h+a*n,a:(e.a??1)+((t.a??1)-(e.a??1))*n})}var Kc=()=>({r:Math.floor(Math.random()*256),g:Math.floor(Math.random()*256),b:Math.floor(Math.random()*256),a:1}),qc=({r:e,g:t,b:n,a:r=1})=>`rgba(${Math.round(e)}, ${Math.round(t)}, ${Math.round(n)}, ${r})`;function Jc({label:e,value:t,wide:n=!1}){let[r,i]=(0,_.useState)(!1);return(0,P.jsxs)(`button`,{type:`button`,className:`copyable ${n?`is-wide`:``} ${r?`is-copied`:``}`,onClick:async()=>{await eo(t),i(!0),setTimeout(()=>i(!1),1400)},title:`Copy ${t}`,children:[(0,P.jsx)(`span`,{className:`copyable-label`,children:e}),(0,P.jsx)(`span`,{className:`copyable-value`,children:t}),(0,P.jsx)(F,{name:r?`check`:`copy`,size:13})]})}function Yc({hsv:e,onChange:t}){let n=(0,_.useRef)(null),r=(0,_.useRef)(!1),i=(0,_.useCallback)(r=>{let i=n.current?.getBoundingClientRect();if(!i)return;let a=Math.min(1,Math.max(0,(r.clientX-i.left)/i.width)),o=Math.min(1,Math.max(0,(r.clientY-i.top)/i.height));t({...e,s:a*100,v:(1-o)*100})},[e,t]);return(0,_.useEffect)(()=>{let e=e=>r.current&&i(e),t=()=>{r.current=!1};return window.addEventListener(`pointermove`,e),window.addEventListener(`pointerup`,t),()=>{window.removeEventListener(`pointermove`,e),window.removeEventListener(`pointerup`,t)}},[i]),(0,P.jsxs)(`div`,{className:`picker`,children:[(0,P.jsx)(`div`,{ref:n,className:`picker-area`,style:{"--hue":`hsl(${e.h} 100% 50%)`},tabIndex:0,role:`slider`,"aria-label":`Saturation and brightness`,"aria-valuetext":`saturation ${Math.round(e.s)}%, brightness ${Math.round(e.v)}%`,onPointerDown:e=>{r.current=!0,e.currentTarget.focus(),i(e)},onKeyDown:n=>{let r=n.shiftKey?10:1,i={ArrowRight:{s:Math.min(100,e.s+r)},ArrowLeft:{s:Math.max(0,e.s-r)},ArrowUp:{v:Math.min(100,e.v+r)},ArrowDown:{v:Math.max(0,e.v-r)}};i[n.key]&&(n.preventDefault(),t({...e,...i[n.key]}))},children:(0,P.jsx)(`span`,{className:`picker-dot`,style:{left:`${e.s}%`,top:`${100-e.v}%`,background:wc(kc({...e,a:1}))}})}),(0,P.jsxs)(`label`,{className:`picker-slider hue`,children:[(0,P.jsx)(`span`,{className:`sr-only`,children:`Hue`}),(0,P.jsx)(`input`,{type:`range`,min:0,max:360,step:1,value:Math.round(e.h),onChange:n=>t({...e,h:Number(n.target.value)})})]}),(0,P.jsxs)(`label`,{className:`picker-slider alpha`,style:{"--solid":wc(kc({...e,a:1}))},children:[(0,P.jsx)(`span`,{className:`sr-only`,children:`Alpha`}),(0,P.jsx)(`input`,{type:`range`,min:0,max:100,step:1,value:Math.round((e.a??1)*100),onChange:n=>t({...e,a:Number(n.target.value)/100})})]})]})}function Xc({label:e,ratio:t}){let n=G(t);return(0,P.jsxs)(`div`,{className:`contrast-row`,children:[(0,P.jsx)(`span`,{children:e}),(0,P.jsx)(`strong`,{children:t.toFixed(2)}),(0,P.jsxs)(`span`,{className:`contrast-badges`,children:[(0,P.jsx)(`em`,{className:n.aaBody?`ok`:`no`,children:`AA`}),(0,P.jsx)(`em`,{className:n.aaaBody?`ok`:`no`,children:`AAA`}),(0,P.jsx)(`em`,{className:n.aaLarge?`ok`:`no`,children:`AA large`})]})]})}function Zc(){let[e,t]=(0,_.useState)(()=>Oc({r:139,g:108,b:255,a:1})),[n,r]=(0,_.useState)(`#8B6CFF`),[i,a]=(0,_.useState)(!1),o=(0,_.useMemo)(()=>kc(e),[e]),s=(0,_.useMemo)(()=>Mc(o),[o]),c=(0,_.useMemo)(()=>Bc(o),[o]),l=(0,_.useMemo)(()=>Hc(o),[o]),u=(0,_.useMemo)(()=>Wc(o),[o]),d=Ic(o,{r:255,g:255,b:255}),f=Ic(o,{r:0,g:0,b:0});(0,_.useEffect)(()=>r(wc(o)),[o]);let p=e=>{r(e);let n=zc(e);n?(a(!1),t(Oc(n))):a(e.trim()!==``)},m=e=>t(Oc(e));return(0,P.jsxs)(`div`,{className:`tool tool-color`,children:[(0,P.jsxs)(`section`,{className:`tool-panel`,children:[(0,P.jsx)(`h2`,{children:`Pick`}),(0,P.jsx)(Yc,{hsv:e,onChange:t}),(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{htmlFor:`color-input`,children:`Any format`}),(0,P.jsxs)(`div`,{className:`field-control`,children:[(0,P.jsx)(`input`,{id:`color-input`,className:`selector-input ${i?`is-bad`:``}`,type:`text`,value:n,spellCheck:`false`,onChange:e=>p(e.target.value)}),(0,P.jsx)(`button`,{type:`button`,className:`icon-button`,onClick:()=>m(Kc()),title:`Random colour`,"aria-label":`Random colour`,children:(0,P.jsx)(F,{name:`bolt`,size:15})})]})]}),(0,P.jsxs)(`p`,{className:`tool-note`,children:[`Paste anything: `,(0,P.jsx)(`code`,{children:`#8b6cff`}),`, `,(0,P.jsx)(`code`,{children:`rgb(139 108 255)`}),`,`,` `,(0,P.jsx)(`code`,{children:`hsl(252 100% 71%)`}),`, `,(0,P.jsx)(`code`,{children:`oklch(62% 0.23 288)`}),`, or a name like `,(0,P.jsx)(`code`,{children:`tomato`}),`.`,!Pc(s)&&(0,P.jsxs)(P.Fragment,{children:[` `,`This OKLCH value sits outside sRGB, so the swatch is the nearest colour a normal screen can show.`]})]})]}),(0,P.jsxs)(`section`,{className:`tool-panel`,children:[(0,P.jsx)(`h2`,{children:`Values`}),(0,P.jsx)(`div`,{className:`color-preview`,style:{"--c":qc(o)},"aria-label":`Preview of ${wc(o)}`,children:(0,P.jsx)(`span`,{style:{color:d>f?`#fff`:`#000`},children:`Aa`})}),(0,P.jsx)(`div`,{className:`copyables`,children:c.map(e=>(0,P.jsx)(Jc,{label:e.label,value:e.value},e.label))}),(0,P.jsx)(`h2`,{children:`Contrast`}),(0,P.jsx)(Xc,{label:`On white`,ratio:d}),(0,P.jsx)(Xc,{label:`On black`,ratio:f}),(0,P.jsxs)(`p`,{className:`tool-note`,children:[`WCAG 2 wants `,(0,P.jsx)(`strong`,{children:`4.5`}),` for body text and `,(0,P.jsx)(`strong`,{children:`3`}),` `,`for text at 24px or 19px bold. AAA raises those to 7 and 4.5.`]})]}),(0,P.jsxs)(`section`,{className:`tool-panel is-full`,children:[(0,P.jsx)(`h2`,{children:`Scale`}),(0,P.jsx)(`p`,{className:`tool-note`,children:`Stepped through OKLCH with chroma easing off at both ends, then fitted to the sRGB gamut — so the pale and dark steps read as tints of the colour rather than as neon. Click a step to make it the colour.`}),(0,P.jsx)(`div`,{className:`swatch-scale`,children:l.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`scale-step`,style:{background:wc(e.rgb)},onClick:()=>m(e.rgb),title:`${e.name} — ${wc(e.rgb)}`,children:[(0,P.jsx)(`span`,{style:{color:Ic(e.rgb,{r:0,g:0,b:0})>4.5?`#000`:`#fff`},children:e.name}),(0,P.jsx)(`small`,{style:{color:Ic(e.rgb,{r:0,g:0,b:0})>4.5?`#000`:`#fff`},children:wc(e.rgb)})]},e.name))}),(0,P.jsx)(Jc,{label:`Tokens`,value:l.map(e=>`--c-${e.name}: ${wc(e.rgb)};`).join(`
`)})]}),(0,P.jsxs)(`section`,{className:`tool-panel is-full`,children:[(0,P.jsx)(`h2`,{children:`Harmonies`}),(0,P.jsx)(`p`,{className:`tool-note`,children:`Rotated in OKLCH rather than HSL, which keeps the perceived lightness and chroma steady across the set instead of letting the greens and yellows jump out.`}),(0,P.jsx)(`div`,{className:`harmonies`,children:u.map(e=>(0,P.jsxs)(`div`,{className:`harmony`,children:[(0,P.jsx)(`span`,{className:`dim`,children:e.name}),(0,P.jsx)(`div`,{className:`harmony-row`,children:e.colors.map((e,t)=>(0,P.jsx)(`button`,{type:`button`,style:{background:wc(e)},onClick:()=>m(e),title:wc(e),"aria-label":`Use ${wc(e)}`,children:(0,P.jsx)(`span`,{children:wc(e)})},t))})]},e.name))})]})]})}var Qc=[{id:`linear`,label:`Linear`},{id:`radial`,label:`Radial`},{id:`conic`,label:`Conic`}],$c=[`circle`,`ellipse`],el=[`farthest-corner`,`farthest-side`,`closest-corner`,`closest-side`],tl=[{id:`srgb`,label:`sRGB`},{id:`oklch`,label:`OKLCH`},{id:`oklab`,label:`OKLab`},{id:`hsl`,label:`HSL`}],nl=[{name:`Dusk`,type:`linear`,angle:135,stops:[[`#8b6cff`,0],[`#f472b6`,55],[`#f97316`,100]]},{name:`Mint`,type:`linear`,angle:160,stops:[[`#0f766e`,0],[`#34d399`,100]]},{name:`Ember`,type:`radial`,angle:90,stops:[[`#fbbf24`,0],[`#dc2626`,70],[`#1c1917`,100]]},{name:`Steel`,type:`linear`,angle:180,stops:[[`#1e293b`,0],[`#64748b`,100]]},{name:`Wheel`,type:`conic`,angle:0,stops:[[`#ef4444`,0],[`#eab308`,25],[`#22c55e`,50],[`#3b82f6`,75],[`#ef4444`,100]]}],rl=({r:e,g:t,b:n,a:r=1})=>r<1?`rgb(${Math.round(e)} ${Math.round(t)} ${Math.round(n)} / ${Number(r.toFixed(2))})`:wc({r:e,g:t,b:n}),il=1,al=(e,t)=>({id:il++,color:e,at:t}),ol=e=>({type:e.type,angle:e.angle,shape:`circle`,extent:`farthest-corner`,cx:50,cy:50,space:`oklch`,repeating:!1,stops:e.stops.map(([e,t])=>al(zc(e),t))});function sl(e){let t=[...e.stops].sort((e,t)=>e.at-t.at).map(e=>`${rl(e.color)} ${Math.round(e.at)}%`).join(`, `),n=`${e.repeating?`repeating-`:``}${e.type}-gradient`,r=e.type===`linear`?`${e.angle}deg`:e.type===`radial`?`${e.shape} ${e.extent} at ${e.cx}% ${e.cy}%`:`from ${e.angle}deg at ${e.cx}% ${e.cy}%`;return{modern:`${n}(${r}${e.space===`srgb`?``:` in ${e.space}`}, ${t})`,fallback:`${n}(${r}, ${t})`}}function cl({gradient:e,selected:t,onSelect:n,onMove:r,onAdd:i}){let a=(0,_.useRef)(null),o=(0,_.useRef)(null),s=(0,_.useCallback)(e=>{let t=a.current?.getBoundingClientRect();return t?Math.min(100,Math.max(0,(e.clientX-t.left)/t.width*100)):0},[]);return(0,_.useEffect)(()=>{let e=e=>{o.current!==null&&r(o.current,s(e))},t=()=>{o.current=null};return window.addEventListener(`pointermove`,e),window.addEventListener(`pointerup`,t),()=>{window.removeEventListener(`pointermove`,e),window.removeEventListener(`pointerup`,t)}},[r,s]),(0,P.jsx)(`div`,{ref:a,className:`stop-track`,style:{"--bar":`linear-gradient(90deg, ${[...e.stops].sort((e,t)=>e.at-t.at).map(e=>`${rl(e.color)} ${e.at}%`).join(`, `)})`},onDoubleClick:e=>i(s(e)),title:`Double-click to add a stop`,children:e.stops.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`stop-handle ${e.id===t?`is-selected`:``}`,style:{left:`${e.at}%`,"--c":rl(e.color)},onPointerDown:t=>{t.stopPropagation(),o.current=e.id,n(e.id)},"aria-label":`Stop at ${Math.round(e.at)} percent`},e.id))})}function ll(){let[e,t]=(0,_.useState)(()=>ol(nl[0])),[n,r]=(0,_.useState)(null),[i,a]=(0,_.useState)(!1),o=e.stops.find(e=>e.id===n)??e.stops[0],{modern:s,fallback:c}=(0,_.useMemo)(()=>sl(e),[e]),l=`background-image: ${c};\nbackground-image: ${s};`,u=(0,_.useMemo)(()=>gi(l,`css`),[l]),d=e=>t(t=>({...t,...e})),f=(0,_.useCallback)((e,n)=>t(t=>({...t,stops:t.stops.map(t=>t.id===e?{...t,at:n}:t)})),[]),p=(0,_.useCallback)(e=>t(t=>{let n=[...t.stops].sort((e,t)=>e.at-t.at),i=n.findLast?.(t=>t.at<=e)??n[0],a=n.find(t=>t.at>e)??n[n.length-1],o=a.at-i.at||1,s=al(Gc(i.color,a.color,(e-i.at)/o),e);return r(s.id),{...t,stops:[...t.stops,s]}}),[]),m=e=>t(t=>t.stops.length<=2?t:{...t,stops:t.stops.filter(t=>t.id!==e)}),h=(e,n)=>{let r=zc(n);r&&t(t=>({...t,stops:t.stops.map(t=>t.id===e?{...t,color:r}:t)}))},g=()=>t(e=>({...e,stops:e.stops.map(e=>({...e,color:Kc()}))})),v=async()=>{await eo(l),a(!0),setTimeout(()=>a(!1),2e3)};return(0,P.jsxs)(`div`,{className:`tool tool-gradient`,children:[(0,P.jsxs)(`section`,{className:`tool-panel is-full`,children:[(0,P.jsx)(`h2`,{children:`Preview`}),(0,P.jsx)(`div`,{className:`gradient-preview`,style:{backgroundImage:s}}),(0,P.jsx)(`div`,{className:`gradient-preview is-text`,style:{backgroundImage:s},children:(0,P.jsx)(`span`,{children:`Gradient text`})})]}),(0,P.jsxs)(`section`,{className:`tool-panel`,children:[(0,P.jsx)(`h2`,{children:`Shape`}),(0,P.jsx)(`div`,{className:`segmented`,children:Qc.map(t=>(0,P.jsx)(`button`,{type:`button`,className:e.type===t.id?`is-active`:``,onClick:()=>d({type:t.id}),children:t.label},t.id))}),e.type!==`radial`&&(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{children:`Angle`}),(0,P.jsxs)(`div`,{className:`field-control size`,children:[(0,P.jsx)(`input`,{type:`range`,min:0,max:360,value:e.angle,onChange:e=>d({angle:Number(e.target.value)})}),(0,P.jsxs)(`span`,{className:`size-value`,children:[e.angle,`°`]})]})]}),e.type===`radial`&&(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{children:`Shape`}),(0,P.jsx)(`div`,{className:`segmented small`,children:$c.map(t=>(0,P.jsx)(`button`,{type:`button`,className:e.shape===t?`is-active`:``,onClick:()=>d({shape:t}),children:t},t))})]}),(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{htmlFor:`grad-extent`,children:`Size`}),(0,P.jsx)(`div`,{className:`field-control`,children:(0,P.jsx)(`select`,{id:`grad-extent`,className:`selector-input`,value:e.extent,onChange:e=>d({extent:e.target.value}),children:el.map(e=>(0,P.jsx)(`option`,{value:e,children:e},e))})})]})]}),e.type!==`linear`&&(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{children:`Centre X`}),(0,P.jsxs)(`div`,{className:`field-control size`,children:[(0,P.jsx)(`input`,{type:`range`,min:0,max:100,value:e.cx,onChange:e=>d({cx:Number(e.target.value)})}),(0,P.jsxs)(`span`,{className:`size-value`,children:[e.cx,`%`]})]})]}),(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{children:`Centre Y`}),(0,P.jsxs)(`div`,{className:`field-control size`,children:[(0,P.jsx)(`input`,{type:`range`,min:0,max:100,value:e.cy,onChange:e=>d({cy:Number(e.target.value)})}),(0,P.jsxs)(`span`,{className:`size-value`,children:[e.cy,`%`]})]})]})]}),(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{htmlFor:`grad-space`,children:`Interpolate in`}),(0,P.jsx)(`div`,{className:`field-control`,children:(0,P.jsx)(`select`,{id:`grad-space`,className:`selector-input`,value:e.space,onChange:e=>d({space:e.target.value}),children:tl.map(e=>(0,P.jsx)(`option`,{value:e.id,children:e.label},e.id))})})]}),(0,P.jsxs)(`label`,{className:`tool-check`,children:[(0,P.jsx)(`input`,{type:`checkbox`,checked:e.repeating,onChange:e=>d({repeating:e.target.checked})}),(0,P.jsx)(`span`,{children:`Repeating`})]}),(0,P.jsx)(`h2`,{children:`Presets`}),(0,P.jsx)(`div`,{className:`presets`,children:nl.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`preset`,onClick:()=>{t(ol(e)),r(null)},style:{"--swatch":e.stops[1]?.[0]??e.stops[0][0]},children:[(0,P.jsx)(`span`,{className:`preset-dot`,"aria-hidden":`true`}),e.name]},e.name))}),(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:g,children:[(0,P.jsx)(F,{name:`bolt`,size:14}),(0,P.jsx)(`span`,{children:`Shuffle colours`})]})]}),(0,P.jsxs)(`section`,{className:`tool-panel`,children:[(0,P.jsx)(`h2`,{children:`Stops`}),(0,P.jsx)(cl,{gradient:e,selected:o?.id,onSelect:r,onMove:f,onAdd:p}),(0,P.jsx)(`p`,{className:`tool-note`,children:`Drag a handle to move it. Double-click the bar to add one — the new stop takes the colour the gradient already had at that point.`}),(0,P.jsx)(`ul`,{className:`stop-list`,children:[...e.stops].sort((e,t)=>e.at-t.at).map(t=>(0,P.jsxs)(`li`,{className:t.id===o?.id?`is-selected`:``,children:[(0,P.jsx)(`input`,{type:`color`,value:wc({...t.color,a:1}),onChange:e=>h(t.id,e.target.value),"aria-label":`Stop colour`}),(0,P.jsx)(`input`,{className:`stop-hex`,type:`text`,value:rl(t.color),spellCheck:`false`,onChange:e=>h(t.id,e.target.value),onFocus:()=>r(t.id),"aria-label":`Stop colour value`}),(0,P.jsx)(`input`,{className:`stop-at`,type:`number`,min:0,max:100,value:Math.round(t.at),onChange:e=>f(t.id,Number(e.target.value)),"aria-label":`Stop position`}),(0,P.jsx)(`button`,{type:`button`,className:`icon-button`,onClick:()=>m(t.id),disabled:e.stops.length<=2,"aria-label":`Remove stop`,children:(0,P.jsx)(F,{name:`close`,size:13})})]},t.id))}),(0,P.jsxs)(`h2`,{children:[`CSS`,(0,P.jsxs)(`button`,{type:`button`,className:`code-action code-copy ${i?`is-copied`:``}`,onClick:v,children:[(0,P.jsx)(F,{name:i?`check`:`copy`,size:13}),i?`Copied`:`Copy`]})]}),(0,P.jsx)(`pre`,{className:`tool-code hljs`,children:(0,P.jsx)(`code`,{dangerouslySetInnerHTML:{__html:u}})}),(0,P.jsxs)(`p`,{className:`tool-note`,children:[`Two declarations on purpose. A browser that cannot parse`,` `,(0,P.jsxs)(`code`,{children:[`in `,e.space]}),` ignores the second line and keeps the first, so the gradient degrades to sRGB interpolation rather than disappearing. The second preview above is the same gradient clipped to text with `,(0,P.jsx)(`code`,{children:`background-clip: text`}),`.`]})]})]})}var ul=12,dl=[{label:`1920 × 1080`,w:1920,h:1080},{label:`1280 × 720`,w:1280,h:720},{label:`1200 × 630`,w:1200,h:630},{label:`1080 × 1080`,w:1080,h:1080},{label:`1080 × 1920`,w:1080,h:1920}],fl=[`#ff0080`,`#7928ca`,`#00dfd8`,`#ff4d4d`,`#f9cb28`,`#4338ca`,`#10b981`],pl=1,ml=(e,t,n,r=70,i=70)=>({id:pl++,x:e,y:t,color:n,rx:r,ry:i}),hl=[{name:`Aurora`,base:`#1a0b2e`,points:[[18,22,`#7928ca`,85,80],[78,18,`#ff0080`,70,75],[30,82,`#00dfd8`,75,70],[88,76,`#4338ca`,80,85]]},{name:`Sunset`,base:`#1c1017`,points:[[12,78,`#f9cb28`,80,70],[55,30,`#ff4d4d`,75,80],[92,62,`#7928ca`,85,75]]},{name:`Deep`,base:`#04070f`,points:[[24,30,`#10b981`,70,65],[72,24,`#0ea5e9`,75,70],[50,90,`#4338ca`,90,80]]},{name:`Blush`,base:`#fdf2f8`,points:[[20,25,`#f9a8d4`,80,75],[80,30,`#c4b5fd`,75,80],[50,85,`#fde68a`,85,70]]}],gl=e=>({base:e.base,points:e.points.map(([e,t,n,r,i])=>ml(e,t,zc(n),r,i))}),_l=e=>{let{r:t,g:n,b:r}=e.color,i=`${Math.round(t)}, ${Math.round(n)}, ${Math.round(r)}`;return`radial-gradient(ellipse ${Math.round(e.rx)}% ${Math.round(e.ry)}% at ${Math.round(e.x)}% ${Math.round(e.y)}%, rgba(${i}, 1) 0%, rgba(${i}, 0) 100%)`};function vl(e){let t=e.points.map(_l);return[`.mesh {`,`  background-color: ${e.base};`,t.length?`  background-image:\n${t.map(e=>`    ${e}`).join(`,
`)};`:`  background-image: none;`,`}`].join(`
`)}function yl(e,t,n,r){let i=document.createElement(`canvas`);i.width=t,i.height=n;let a=i.getContext(`2d`);a.fillStyle=e.base,a.fillRect(0,0,t,n);for(let r of e.points){let{r:e,g:i,b:o}=r.color,s=`${Math.round(e)}, ${Math.round(i)}, ${Math.round(o)}`,c=r.rx/100*t,l=r.ry/100*n;if(c<=0||l<=0)continue;a.save(),a.translate(r.x/100*t,r.y/100*n),a.scale(c,l);let u=a.createRadialGradient(0,0,0,0,0,1);u.addColorStop(0,`rgba(${s}, 1)`),u.addColorStop(1,`rgba(${s}, 0)`),a.fillStyle=u;let d=Math.max(t/c,n/l)+2;a.fillRect(-d,-d,d*2,d*2),a.restore()}i.toBlob(e=>{if(!e)return;let t=URL.createObjectURL(e),n=document.createElement(`a`);n.href=t,n.download=r,document.body.appendChild(n),n.click(),n.remove(),setTimeout(()=>URL.revokeObjectURL(t),1e3)},`image/png`)}function bl(){let[e,t]=(0,_.useState)(()=>gl(hl[0])),[n,r]=(0,_.useState)(null),[i,a]=(0,_.useState)(dl[0]),[o,s]=(0,_.useState)(!1),c=(0,_.useRef)(null),l=(0,_.useRef)(null),u=(0,_.useMemo)(()=>vl(e),[e]),d=(0,_.useMemo)(()=>gi(u,`css`),[u]),f=(0,_.useMemo)(()=>({backgroundColor:e.base,backgroundImage:e.points.length?e.points.map(_l).join(`, `):`none`}),[e]),p=(0,_.useCallback)((e,n,r)=>{let i=c.current?.getBoundingClientRect();if(!i)return;let a=Math.max(-15,Math.min(115,(n-i.left)/i.width*100)),o=Math.max(-15,Math.min(115,(r-i.top)/i.height*100));t(t=>({...t,points:t.points.map(t=>t.id===e?{...t,x:a,y:o}:t)}))},[]);(0,_.useEffect)(()=>{let e=e=>{l.current!==null&&(e.preventDefault(),p(l.current,e.clientX,e.clientY))},t=()=>{l.current=null};return window.addEventListener(`pointermove`,e,{passive:!1}),window.addEventListener(`pointerup`,t),()=>{window.removeEventListener(`pointermove`,e),window.removeEventListener(`pointerup`,t)}},[p]);let m=(e,n)=>t(t=>({...t,points:t.points.map(t=>t.id===e?{...t,...n}:t)})),h=()=>t(e=>{if(e.points.length>=ul)return e;let t=zc(fl[e.points.length%fl.length]),n=ml(20+Math.random()*60,20+Math.random()*60,t);return r(n.id),{...e,points:[...e.points,n]}}),g=e=>t(t=>({...t,points:t.points.filter(t=>t.id!==e)})),v=()=>t(e=>({...e,points:e.points.map(e=>({...e,x:10+Math.random()*80,y:10+Math.random()*80,color:Kc(),rx:55+Math.random()*45,ry:55+Math.random()*45}))})),y=async()=>{await eo(u),s(!0),setTimeout(()=>s(!1),2e3)},b=e.points.find(e=>e.id===n)??null;return(0,P.jsxs)(`div`,{className:`tool tool-mesh`,children:[(0,P.jsxs)(`section`,{className:`tool-panel is-full mesh-stage`,children:[(0,P.jsxs)(`h2`,{children:[`Canvas`,(0,P.jsxs)(`span`,{className:`dim`,children:[i.label,` · `,e.points.length,`/`,ul,` points — drag them`]})]}),(0,P.jsxs)(`div`,{ref:c,className:`mesh-canvas`,style:{...f,"--ar":`${i.w} / ${i.h}`,"--arnum":i.w/i.h},onPointerDown:()=>r(null),children:[e.points.map(e=>(0,P.jsx)(`button`,{type:`button`,className:`mesh-point ${e.id===n?`is-selected`:``}`,style:{left:`${e.x}%`,top:`${e.y}%`,background:wc(e.color)},onPointerDown:t=>{t.stopPropagation(),l.current=e.id,r(e.id)},onKeyDown:t=>{let n=t.shiftKey?5:1,r={ArrowLeft:{x:e.x-n},ArrowRight:{x:e.x+n},ArrowUp:{y:e.y-n},ArrowDown:{y:e.y+n}};r[t.key]&&(t.preventDefault(),m(e.id,r[t.key]))},"aria-label":`Point at ${Math.round(e.x)}, ${Math.round(e.y)}`},e.id)),e.points.length===0&&(0,P.jsx)(`p`,{className:`mesh-empty`,children:`Add a point to start`})]})]}),(0,P.jsxs)(`section`,{className:`tool-panel`,children:[(0,P.jsx)(`h2`,{children:`Mesh`}),(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{htmlFor:`mesh-base`,children:`Base colour`}),(0,P.jsxs)(`div`,{className:`field-control colour`,children:[(0,P.jsx)(`input`,{id:`mesh-base`,type:`color`,value:e.base,onChange:e=>t(t=>({...t,base:e.target.value}))}),(0,P.jsx)(`input`,{className:`colour-hex`,type:`text`,value:e.base,spellCheck:`false`,onChange:e=>{let n=zc(e.target.value);t(t=>({...t,base:n?wc(n):e.target.value}))},"aria-label":`Base colour value`})]})]}),(0,P.jsxs)(`div`,{className:`mesh-actions`,children:[(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:h,disabled:e.points.length>=ul,children:[(0,P.jsx)(F,{name:`star`,size:14}),(0,P.jsx)(`span`,{children:`Add point`})]}),(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:v,children:[(0,P.jsx)(F,{name:`bolt`,size:14}),(0,P.jsx)(`span`,{children:`Shuffle`})]})]}),(0,P.jsx)(`h2`,{children:`Presets`}),(0,P.jsx)(`div`,{className:`presets`,children:hl.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`preset`,style:{"--swatch":e.points[0][2]},onClick:()=>{t(gl(e)),r(null)},children:[(0,P.jsx)(`span`,{className:`preset-dot`,"aria-hidden":`true`}),e.name]},e.name))}),(0,P.jsx)(`h2`,{children:`Export`}),(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{htmlFor:`mesh-size`,children:`Image size`}),(0,P.jsx)(`div`,{className:`field-control`,children:(0,P.jsx)(`select`,{id:`mesh-size`,className:`selector-input`,value:i.label,onChange:e=>a(dl.find(t=>t.label===e.target.value)),children:dl.map(e=>(0,P.jsx)(`option`,{value:e.label,children:e.label},e.label))})})]}),(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-primary`,onClick:()=>yl(e,i.w,i.h,`mesh-${i.w}x${i.h}.png`),children:[(0,P.jsx)(F,{name:`download`,size:15}),(0,P.jsx)(`span`,{children:`Download PNG`})]}),(0,P.jsx)(`p`,{className:`tool-note`,children:`The canvas above takes this shape, because every position and radius in a mesh is a percentage — the same points in a wider box make a different picture, so a preview that ignored the ratio would be lying.`}),(0,P.jsx)(`p`,{className:`tool-note`,children:`The PNG is painted on a canvas from the same numbers as the CSS, in this tab. Nothing is uploaded and no screenshot is taken, so the image is as sharp as the size you pick rather than as sharp as your screen.`})]}),(0,P.jsxs)(`section`,{className:`tool-panel`,children:[(0,P.jsx)(`h2`,{children:`Points`}),e.points.length===0&&(0,P.jsx)(`p`,{className:`tool-note`,children:`No points yet.`}),(0,P.jsx)(`ul`,{className:`mesh-list`,children:e.points.map((e,t)=>(0,P.jsxs)(`li`,{className:e.id===n?`is-selected`:``,children:[(0,P.jsxs)(`div`,{className:`mesh-list-head`,children:[(0,P.jsx)(`input`,{type:`color`,value:wc(e.color),onChange:t=>m(e.id,{color:zc(t.target.value)}),"aria-label":`Point ${t+1} colour`}),(0,P.jsxs)(`button`,{type:`button`,className:`mesh-list-name`,onClick:()=>r(e.id),children:[`Point `,t+1,(0,P.jsx)(`span`,{className:`dim`,children:wc(e.color)})]}),(0,P.jsx)(`button`,{type:`button`,className:`icon-button`,onClick:()=>g(e.id),"aria-label":`Remove point ${t+1}`,children:(0,P.jsx)(F,{name:`close`,size:13})})]}),e.id===n&&(0,P.jsxs)(`div`,{className:`mesh-list-body`,children:[(0,P.jsxs)(`label`,{children:[(0,P.jsx)(`span`,{children:`Spread across`}),(0,P.jsx)(`input`,{type:`range`,min:10,max:140,value:Math.round(e.rx),onChange:t=>m(e.id,{rx:Number(t.target.value)})}),(0,P.jsxs)(`em`,{children:[Math.round(e.rx),`%`]})]}),(0,P.jsxs)(`label`,{children:[(0,P.jsx)(`span`,{children:`Spread down`}),(0,P.jsx)(`input`,{type:`range`,min:10,max:140,value:Math.round(e.ry),onChange:t=>m(e.id,{ry:Number(t.target.value)})}),(0,P.jsxs)(`em`,{children:[Math.round(e.ry),`%`]})]})]})]},e.id))}),(0,P.jsxs)(`h2`,{children:[`CSS`,(0,P.jsxs)(`button`,{type:`button`,className:`code-action code-copy ${o?`is-copied`:``}`,onClick:y,children:[(0,P.jsx)(F,{name:o?`check`:`copy`,size:13}),o?`Copied`:`Copy`]})]}),(0,P.jsx)(`pre`,{className:`tool-code hljs`,children:(0,P.jsx)(`code`,{dangerouslySetInnerHTML:{__html:d}})}),b&&(0,P.jsxs)(`p`,{className:`tool-note`,children:[`Point `,e.points.indexOf(b)+1,` selected. Arrow keys nudge it while it has focus; hold shift for bigger steps.`]})]})]})}var xl={thumb:`#8b6cff`,track:`#1c1c26`,size:12,radius:8,borderWidth:0,borderColor:`#1c1c26`,selector:`body`},Sl=[{name:`Brand`,thumb:`#8b6cff`,track:`#1c1c26`,size:12,radius:8,borderWidth:0},{name:`Hairline`,thumb:`#6b6a80`,track:`transparent`,size:6,radius:999,borderWidth:0},{name:`Inset`,thumb:`#34d399`,track:`#0f1a16`,size:16,radius:999,borderWidth:4},{name:`Classic`,thumb:`#9aa0aa`,track:`#e6e6ee`,size:14,radius:0,borderWidth:0}],Cl=e=>/^#[0-9a-f]{6}$/i.test(e)?e.toUpperCase():e;function wl(e,t=e.selector){let n=e.borderWidth>0;return`${t} {
  --sb-track-color: ${Cl(e.track)};
  --sb-thumb-color: ${Cl(e.thumb)};
  --sb-size: ${e.size}px;${n?`\n  --sb-thumb-border: ${Cl(e.borderColor)};`:``}
}

${t}::-webkit-scrollbar {
  width: var(--sb-size);
}

${t}::-webkit-scrollbar-track {
  background: var(--sb-track-color);
  border-radius: ${e.radius}px;
}

${t}::-webkit-scrollbar-thumb {
  background: var(--sb-thumb-color);
  border-radius: ${e.radius}px;${n?`
  border: ${e.borderWidth}px solid var(--sb-thumb-border);
  background-clip: padding-box;`:``}
}

@supports not selector(::-webkit-scrollbar) {
  ${t} {
    scrollbar-color: var(--sb-thumb-color)
                     var(--sb-track-color);
    scrollbar-width: ${e.size<=8?`thin`:`auto`};
  }
}`}function Tl({label:e,value:t,onChange:n}){return(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{htmlFor:`c-${e}`,children:e}),(0,P.jsxs)(`div`,{className:`field-control colour`,children:[(0,P.jsx)(`input`,{id:`c-${e}`,type:`color`,value:/^#[0-9a-f]{6}$/i.test(t)?t:`#000000`,onChange:e=>n(e.target.value),"aria-label":e}),(0,P.jsx)(`input`,{className:`colour-hex`,type:`text`,value:t,spellCheck:`false`,onChange:e=>n(e.target.value.trim()),"aria-label":`${e} value`})]})]})}function El({label:e,value:t,onChange:n,min:r,max:i}){let a=e=>Math.min(i,Math.max(r,e));return(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{children:e}),(0,P.jsxs)(`div`,{className:`field-control size`,children:[(0,P.jsx)(`button`,{type:`button`,onClick:()=>n(a(t-1)),disabled:t<=r,children:`−`}),(0,P.jsx)(`input`,{type:`range`,min:r,max:i,value:t,onChange:e=>n(Number(e.target.value)),"aria-label":e}),(0,P.jsx)(`button`,{type:`button`,onClick:()=>n(a(t+1)),disabled:t>=i,children:`+`}),(0,P.jsxs)(`span`,{className:`size-value`,children:[t,`px`]})]})]})}function Dl(){let[e,t]=(0,_.useState)(xl),[n,r]=(0,_.useState)(!1),i=e=>n=>t(t=>({...t,[e]:n})),a=(0,_.useMemo)(()=>wl(e),[e]),o=(0,_.useMemo)(()=>gi(a,`css`),[a]),s=(0,_.useMemo)(()=>wl(e,`.sb-preview`),[e]);return(0,P.jsxs)(`div`,{className:`tool`,children:[(0,P.jsx)(`style`,{children:s}),(0,P.jsxs)(`section`,{className:`tool-panel`,children:[(0,P.jsx)(`h2`,{children:`Settings`}),(0,P.jsx)(`div`,{className:`presets`,children:Sl.map(e=>(0,P.jsxs)(`button`,{type:`button`,className:`preset`,onClick:()=>t(t=>({...t,...e,borderColor:e.track})),style:{"--swatch":e.thumb},children:[(0,P.jsx)(`span`,{className:`preset-dot`,"aria-hidden":`true`}),e.name]},e.name))}),(0,P.jsx)(Tl,{label:`Thumb`,value:e.thumb,onChange:i(`thumb`)}),(0,P.jsx)(Tl,{label:`Track`,value:e.track,onChange:i(`track`)}),(0,P.jsx)(El,{label:`Width`,value:e.size,onChange:i(`size`),min:2,max:32}),(0,P.jsx)(El,{label:`Corner radius`,value:e.radius,onChange:i(`radius`),min:0,max:20}),(0,P.jsx)(El,{label:`Thumb border`,value:e.borderWidth,onChange:i(`borderWidth`),min:0,max:8}),e.borderWidth>0&&(0,P.jsx)(Tl,{label:`Border colour`,value:e.borderColor,onChange:i(`borderColor`)}),(0,P.jsxs)(`div`,{className:`field`,children:[(0,P.jsx)(`label`,{htmlFor:`sb-selector`,children:`Applies to`}),(0,P.jsx)(`div`,{className:`field-control`,children:(0,P.jsx)(`input`,{id:`sb-selector`,className:`selector-input`,type:`text`,value:e.selector,spellCheck:`false`,onChange:e=>i(`selector`)(e.target.value||`body`)})})]}),(0,P.jsxs)(`p`,{className:`tool-note`,children:[(0,P.jsx)(`code`,{children:`body`}),` styles the page itself. A class such as`,` `,(0,P.jsx)(`code`,{children:`.panel`}),` styles one scrolling element — and a scrollbar only appears where `,(0,P.jsx)(`code`,{children:`overflow: auto`}),` meets content taller than the box.`]}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:()=>t(xl),children:`Reset`})]}),(0,P.jsxs)(`section`,{className:`tool-panel is-code`,children:[(0,P.jsxs)(`h2`,{children:[`CSS`,(0,P.jsxs)(`button`,{type:`button`,className:`code-action code-copy ${n?`is-copied`:``}`,onClick:async()=>{await eo(a),r(!0),setTimeout(()=>r(!1),2e3)},children:[(0,P.jsx)(F,{name:n?`check`:`copy`,size:13}),n?`Copied`:`Copy`]})]}),(0,P.jsx)(`pre`,{className:`tool-code hljs`,children:(0,P.jsx)(`code`,{dangerouslySetInnerHTML:{__html:o}})})]}),(0,P.jsxs)(`section`,{className:`tool-panel`,children:[(0,P.jsx)(`h2`,{children:`Live`}),(0,P.jsxs)(`div`,{className:`sb-preview`,children:[(0,P.jsx)(`p`,{children:`This box is styled by the CSS beside it. Drag the bar, change a value, watch it move.`}),Array.from({length:18},(e,t)=>(0,P.jsxs)(`p`,{children:[`Line `,t+1,` — the `,(0,P.jsx)(`strong`,{children:`thumb`}),` is the part you drag, the`,` `,(0,P.jsx)(`strong`,{children:`track`}),` is the groove it runs in.`]},t))]}),(0,P.jsxs)(`p`,{className:`tool-note`,children:[`Firefox uses the `,(0,P.jsx)(`code`,{children:`@supports`}),` block at the bottom, which is why the corner radius and border have no effect there — the standard properties only expose colour and width.`]})]})]})}var Ol={scrollbar:Dl,colour:Zc,gradient:ll,mesh:bl};function kl({tool:e,onNavigate:t}){let n=Ol[e.slug];return(0,_.useEffect)(()=>{window.scrollTo({top:0})},[e.slug]),(0,P.jsxs)(`article`,{className:`article`,style:{"--accent":e.accent},children:[(0,P.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm back`,onClick:()=>t(`tools`),children:[(0,P.jsx)(F,{name:`arrow`,size:15,className:`flip`}),(0,P.jsx)(`span`,{children:`All tools`})]}),(0,P.jsxs)(`header`,{className:`article-head`,children:[(0,P.jsxs)(`div`,{className:`article-meta`,children:[(0,P.jsx)(`span`,{className:`badge badge-stable`,children:`Ready`}),(0,P.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,P.jsx)(`li`,{children:e},e))})]}),(0,P.jsx)(`h1`,{children:e.name}),(0,P.jsx)(`p`,{className:`lede`,children:e.summary})]}),n?(0,P.jsx)(n,{}):(0,P.jsx)(`p`,{className:`hint`,children:`This one is not built yet.`}),(0,P.jsx)(`footer`,{className:`article-foot`,children:(0,P.jsx)(`p`,{className:`hint`,children:`Everything above happens in this tab. Nothing is sent anywhere, nothing is stored, and closing the page leaves no trace of what you did with it.`})})]})}function Al({tool:e,onOpen:t}){let n=e.status===`live`,r=nt();return(0,P.jsxs)(`article`,{className:`card tool-card ${n?``:`is-upcoming`}`,style:{"--accent":e.accent},onClick:n?()=>t(e.route):void 0,...r,children:[(0,P.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,P.jsxs)(`header`,{className:`article-card-head`,children:[(0,P.jsx)(`span`,{className:`badge ${n?`badge-stable`:`badge-wip`}`,children:n?`Ready`:`Upcoming`}),(0,P.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,P.jsx)(`li`,{children:e},e))})]}),(0,P.jsx)(`h3`,{children:n?(0,P.jsx)(`button`,{type:`button`,className:`card-link`,onClick:n=>{n.stopPropagation(),t(e.route)},children:e.name}):e.name}),(0,P.jsx)(`p`,{className:`tagline`,children:e.tagline}),(0,P.jsx)(`p`,{className:`card-summary`,children:e.summary}),n&&(0,P.jsx)(`footer`,{className:`card-foot`,children:(0,P.jsxs)(`span`,{className:`article-more`,children:[`Open `,(0,P.jsx)(F,{name:`arrow`,size:15})]})})]})}function jl({onOpen:e}){let t=Xa.filter(e=>e.status===`live`),n=Xa.filter(e=>e.status!==`live`);return(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(St,{name:`design`,width:380,height:380,children:[(0,P.jsxs)(`p`,{className:`eyebrow`,children:[(0,P.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`Free tools, no sign up`]}),(0,P.jsxs)(`h1`,{children:[`Free tools that run`,(0,P.jsx)(`br`,{}),(0,P.jsx)(`span`,{className:`grad`,children:`in your browser.`})]}),(0,P.jsx)(`p`,{className:`lede`,children:`The same promise as the desktop apps, applied to the things that never needed a desktop app. Everything here runs entirely in your browser — no upload, no account, and nothing to install. Close the tab and there is no trace of it anywhere.`})]}),(0,P.jsx)(`div`,{className:`catalog catalog-grid`,children:t.map((t,n)=>(0,P.jsx)(Ct,{className:`reveal-cell`,delay:Ge(n),children:(0,P.jsx)(Al,{tool:t,onOpen:e})},t.slug))}),n.length>0&&(0,P.jsxs)(P.Fragment,{children:[(0,P.jsxs)(Ct,{as:`h2`,className:`section-heading`,variant:`left`,children:[`On the list`,(0,P.jsxs)(`span`,{className:`dim`,children:[n.length,` planned`]})]}),(0,P.jsx)(`div`,{className:`catalog catalog-grid`,children:n.map((t,n)=>(0,P.jsx)(Ct,{className:`reveal-cell`,delay:Ge(n),children:(0,P.jsx)(Al,{tool:t,onOpen:e})},t.slug))})]})]})}var Ml=()=>decodeURIComponent(window.location.hash.replace(/^#\/?/,``));function Nl(){let[e,t]=(0,_.useState)(Ml);return(0,_.useEffect)(()=>{let e=()=>t(Ml());return window.addEventListener(`hashchange`,e),()=>window.removeEventListener(`hashchange`,e)},[]),[e,(0,_.useCallback)(e=>{e?window.location.hash=`/${e}`:window.location.hash&&history.replaceState(null,``,window.location.pathname+window.location.search),t(e??``)},[])]}var Pl={stable:0,beta:1,wip:2},Fl=e=>[e.name,e.tagline,e.summary,...e.tags,...e.highlights,...e.platforms].join(` `).toLowerCase();function Il(){let{theme:e,cycle:t}=dt(),[n,r]=Nl(),[i,a]=(0,_.useState)(null),o=(0,_.useCallback)(e=>{e!==n&&(a({from:n,to:e,label:$a(n),y:window.scrollY}),r(e))},[n,r]),s=i&&i.to===n?i:null,c=(0,_.useRef)(null);(0,_.useEffect)(()=>{if(c.current===null)return;let e=c.current;c.current=null;let t=0,n=0,r=()=>{window.scrollTo({top:e}),Math.abs(window.scrollY-e)>2&&(n+=1)<30&&(t=requestAnimationFrame(r))};t=requestAnimationFrame(r);let i=()=>cancelAnimationFrame(t),a=[`wheel`,`touchstart`,`keydown`,`pointerdown`];return a.forEach(e=>window.addEventListener(e,i,{passive:!0})),()=>{cancelAnimationFrame(t),a.forEach(e=>window.removeEventListener(e,i))}},[n]);let l=it(),[u,d]=I(`query`,``),[f,p]=I(`articleQuery`,``),[m,h]=I(`articleCategory`,null),[g,v]=I(`studyQuery`,``),[b,x]=I(`studySubject`,null),[S,C]=I(`designQuery`,``),[w,T]=I(`advancedQuery`,``),[ee,E]=I(`interviewQuery`,``),[D,te]=I(`dsaQuery`,``),[O,ne]=I(`dsaBand`,`all`),[k,re]=I(`dsaTrack`,null),[j,M]=I(`dsaStatus`,`any`),[ae,oe]=I(`dsaTopics`,[]),[se,ce]=I(`dsaSort`,`order`),[le,N]=I(`dsaView`,`grid`),[ue,de]=I(`dsaSolved`,[]),fe=(0,_.useMemo)(()=>new Set(ue),[ue]),pe=(0,_.useCallback)(e=>de(t=>t.includes(e)?t.filter(t=>t!==e):[...t,e]),[de]),me=(0,_.useCallback)(e=>{let t=new Set(e.bands.find(e=>e.id===`all`)?.keys??[]);de(e=>e.filter(e=>!t.has(e)))},[de]),he=(0,_.useCallback)(()=>{te(``),ne(`all`),re(null),M(`any`),oe([])},[te,ne,re,M,oe]),ge=(0,_.useCallback)(e=>oe(t=>t.includes(e)?t.filter(t=>t!==e):[...t,e]),[oe]),[_e,ve]=I(`tags`,[]),[ye,be]=I(`sort`,`recent`),[xe,Se]=I(`view`,`grid`),[Ce,we]=I(`favorites`,[]),[Te,Ee]=I(`favoritesOnly`,!1),De=(0,_.useCallback)(e=>ve(t=>t.includes(e)?t.filter(t=>t!==e):[...t,e]),[ve]),Oe=(0,_.useCallback)(e=>we(t=>t.includes(e)?t.filter(t=>t!==e):[...t,e]),[we]),ke=(0,_.useMemo)(()=>{let e=u.trim().toLowerCase(),t=e?e.split(/\s+/):[],n=jt.filter(e=>{if(Te&&!Ce.includes(e.id)||_e.length&&!_e.every(t=>e.tags.includes(t)))return!1;if(!t.length)return!0;let n=Fl(e);return t.every(e=>n.includes(e))}),r={name:(e,t)=>e.name.localeCompare(t.name),recent:(e,t)=>t.updated.localeCompare(e.updated),status:(e,t)=>Pl[e.status]-Pl[t.status]||e.name.localeCompare(t.name)};return[...n].sort(r[ye]??r.recent)},[u,_e,ye,Ce,Te]),Ae=n.startsWith(`articles/`)?n.slice(9):null,je=Ae?Jt(Ae):null,Me=n.startsWith(`study/`)?n.slice(6).split(`/`):null,Ne=Me?A(Me[0]):null,Pe=Ne?ie(Ne,Me[1]):null,Fe=n.startsWith(`design/`)?n.slice(7).split(`/`):null,Ie=Fe?la(Fe[0]):null,Re=Ie?ua(Ie,Fe[1]):null,Ve=n.startsWith(`advanced/`)?n.slice(9).split(`/`):null,He=Ve?ze(Ve[0]):null,Ue=He?Be(He,Ve[1]):null,We=n.startsWith(`interview/`)?n.slice(10).split(`/`):null,Ke=We?xa(We[0]):null,qe=Ke?Sa(Ke,We[1]):null;(0,_.useEffect)(()=>{if(!n.startsWith(`study/`))return;let[e,...t]=n.slice(6).split(`/`);ze(e)&&window.location.replace(`#/${[`advanced`,e,...t].join(`/`)}`)},[n]),(0,_.useEffect)(()=>{if(!n.startsWith(`advanced/`))return;let[e,...t]=n.slice(9).split(`/`);xa(e)&&window.location.replace(`#/${[`interview`,e,...t].join(`/`)}`)},[n]);let Je=n.startsWith(`tools/`)?n.slice(6):null,Ye=Je?Za(Je):null,Xe=n.startsWith(`dsa/`)?n.slice(4).split(`/`):null,Ze=Xe?Ja(Xe[0]):null,Qe=Ze?os(Ze.slug,Xe[1]):null,$e=Ze&&Xe[1]===`problem`?ws(Ze,Xe[2]):null,et=$e?Ts(Ze.slug,$e.key,Xe[3]):null,tt=Ze&&!Qe&&!$e?Ya(Ze,Xe[1]):null,nt=n===`articles`||Ae!==null,rt=!nt&&(n===`tools`||Je!==null),at=!nt&&!rt&&(n===`dsa`||Xe!==null),ot=!nt&&!rt&&!at&&(n===`design`||Fe!==null),st=!nt&&!rt&&!at&&!ot&&(n===`advanced`||Ve!==null),ct=!nt&&!rt&&!at&&!ot&&!st&&(n===`interview`||We!==null),lt=!nt&&!at&&!ot&&!st&&!ct&&(n===``||n===`study`||Me!==null),ut=!nt&&!at&&!ot&&!st&&!ct&&!lt,ft=nt?`articles`:rt?`tools`:at?`dsa`:ot?`design`:st?`advanced`:ct?`interview`:lt?`study`:`apps`,pt=ut?jt.find(e=>e.id===n)??null:null,mt=n===`support`,ht=n===`credits`,gt=new Set(jt.flatMap(e=>e.platforms)).size,_t=rt?Ye?`tool:${Ye.slug}`:`tools`:at?Ze?$e?`dsa-problem:${$e.key}`:`dsa${Qe?`-notes`:``}:${Ze.slug}`:`dsa`:ot?Ie?`design:${Ie.slug}`:`design`:st?He?`advanced:${He.slug}`:`advanced`:ct?Ke?`interview:${Ke.slug}`:`interview`:lt?Ne?`topic:${Ne.slug}`:`study`:nt?je?`article:${je.slug}`:`articles`:`apps`;return(0,P.jsxs)(`div`,{className:`shell ${l.hidden?`header-hidden`:``} ${l.scrolled?`is-scrolled`:``}`,children:[(0,P.jsx)(`div`,{className:`bg-glow`,"aria-hidden":`true`}),(0,P.jsx)(sc,{}),(0,P.jsx)(Ys,{scrolled:l.scrolled,hidden:l.hidden,theme:e,onCycleTheme:t,onSupport:()=>r(`support`),tab:ft,onNavigate:r}),s&&(0,y.createPortal)((0,P.jsxs)(`button`,{type:`button`,className:`term-return`,onClick:()=>{c.current=s.y??0,a(null),r(s.from)},children:[(0,P.jsx)(F,{name:`arrow`,size:15,className:`flip`}),(0,P.jsxs)(`span`,{children:[`Back to `,(0,P.jsx)(`strong`,{children:s.label})]})]}),document.body),(0,P.jsx)(tc,{section:ft,onNavigate:r}),(0,P.jsx)(`main`,{className:`page-transition`,children:rt?Ye?(0,P.jsx)(kl,{tool:Ye,onNavigate:r}):(0,P.jsx)(jl,{onOpen:r}):at?Ze&&Qe?(0,P.jsx)(hs,{topic:Ze,part:Qe,onNavigate:r}):Ze&&$e?(0,P.jsx)(Os,{topic:Ze,problem:$e,part:et,onNavigate:r}):Ze?(0,P.jsx)(Is,{topic:Ze,band:tt,solved:fe,onSolve:pe,onClear:me,onNavigate:r}):(0,P.jsx)(Xo,{query:D,onQuery:te,band:O,onBand:ne,track:k,onTrack:re,status:j,onStatus:M,topics:ae,onTopics:oe,onToggleTopic:ge,sort:se,onSort:ce,view:le,onView:N,solved:fe,onReset:()=>de([]),onClearFilters:he,onOpen:r}):ot?Ie?(0,P.jsx)(xc,{topic:Ie,part:Re,list:oa,backRoute:`design`,backLabel:`All design principles`,note:`Written for this site. The code above is illustration rather than a program — the point of each pair is the shape of the design, not the output, so the snippets are the smallest thing that shows the difference and nothing more.`,onNavigate:r,onFollowTerm:o,onDetour:!!s}):(0,P.jsx)(Lo,{query:S,onQuery:C,onOpen:r}):st?He?(0,P.jsx)(xc,{topic:He,part:Ue,list:Le,backRoute:`advanced`,backLabel:`All Spring topics`,onNavigate:r,onFollowTerm:o,onDetour:!!s}):(0,P.jsx)(Et,{query:w,onQuery:T,onOpen:r}):ct?Ke?(0,P.jsx)(xc,{topic:Ke,part:qe,list:ya,backRoute:`interview`,backLabel:`All interview questions`,onNavigate:r,onFollowTerm:o,onDetour:!!s}):(0,P.jsx)(Vo,{query:ee,onQuery:E,onOpen:r}):lt?Ne?(0,P.jsx)(xc,{topic:Ne,part:Pe,onNavigate:r,onFollowTerm:o,onDetour:!!s}):(0,P.jsx)(ec,{query:g,onQuery:v,subject:b,onSubject:x,onOpen:r}):nt?je?(0,P.jsx)(No,{article:je,onNavigate:r,onFollowTerm:o}):(0,P.jsx)(Qt,{query:f,onQuery:p,category:m,onCategory:h,onOpen:r}):(0,P.jsxs)(P.Fragment,{children:[(0,P.jsx)(Xs,{total:jt.length,platforms:gt,guides:Kt.length,onArticles:()=>r(`articles`)}),(0,P.jsx)(yc,{query:u,onQuery:d,activeTags:_e,onToggleTag:De,onClearTags:()=>ve([]),sort:ye,onSort:be,view:xe,onView:Se,favoritesOnly:Te,onFavoritesOnly:Ee,favoriteCount:Ce.length,count:ke.length}),ke.length>0?(0,P.jsx)(`div`,{className:`catalog catalog-${xe}`,children:ke.map((e,t)=>(0,P.jsx)(Ct,{className:`reveal-cell`,delay:Ge(t),children:(0,P.jsx)(ic,{project:e,view:xe,isFavorite:Ce.includes(e.id),onToggleFavorite:Oe,onOpen:r})},e.id))}):(0,P.jsxs)(`div`,{className:`empty empty-in`,children:[(0,P.jsx)(F,{name:`search`,size:28}),(0,P.jsx)(`h3`,{children:`Nothing matches that`}),(0,P.jsx)(`p`,{children:Te&&Ce.length===0?`You have not saved any apps yet — tap the star on a card.`:`Try a different word, or clear the filters.`}),(0,P.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>{d(``),ve([]),Ee(!1)},children:`Clear filters`})]})]})},_t),(0,P.jsx)(Ls,{onSupport:()=>r(`support`),onCredits:()=>r(`credits`),articles:nt}),(0,P.jsx)($t,{raised:!!(Ne||Ze||Ie||He)}),pt&&(0,P.jsx)(oc,{project:pt,onClose:()=>r(`apps`),isFavorite:Ce.includes(pt.id),onToggleFavorite:Oe}),mt&&(0,P.jsx)(_c,{onClose:()=>r(`apps`)}),ht&&(0,P.jsx)(lc,{onClose:()=>r(`apps`)})]})}(0,v.createRoot)(document.getElementById(`root`)).render((0,P.jsx)(_.StrictMode,{children:(0,P.jsx)(Il,{})})),requestAnimationFrame(()=>requestAnimationFrame(()=>document.documentElement.removeAttribute(`data-booting`)));