var e='The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## What it needs\n\n![The number of probes barely grows as the input grows a thousandfold](diagrams/binary-search-revision-probe-count.jpg)\n\n- Not a sorted array — a **monotone predicate**: `F F F T T T`, never false again\n  after it turns true. Say that sentence out loud before writing code.\n- Binary search finds that boundary and nothing else.\n- `log₂ n` probes: 10 for a thousand, 20 for a million, 30 for a billion, 60 for\n  10¹⁸.\n- `int mid = lo + (hi - lo) / 2`. `(lo + hi) / 2` overflows once the bounds pass\n  a billion, which an answer-space search reaches routinely.\n\n## The one form to keep\n\n```java\nint lo = 0, hi = n;            // half-open [lo, hi)\nwhile (lo < hi) {\n    int mid = lo + (hi - lo) / 2;\n    if (p(mid)) hi = mid;      // mid might be the answer\n    else        lo = mid + 1;  // mid is definitely not\n}\nreturn lo;                     // the first index where p holds\n```\n\n- Invariant: everything before `lo` is false, everything from `hi` on is true.\n- Terminates because `mid < hi` always, so both branches shrink the range.\n- `lo == n` at the end means no index satisfies it — an answer, not an error.\n  Never `return mid`.\n- Mixing forms is the infinite loop: `while (lo <= hi)` with `hi = mid` hangs\n  when `lo == hi == mid`.\n\n## Lower and upper bound\n\n| Name | Returns | Predicate |\n|---|---|---|\n| `lowerBound(a, x)` | first index with `a[i] >= x` | `a[mid] >= x` |\n| `upperBound(a, x)` | first index with `a[i] > x` | `a[mid] > x` |\n\n- present: `lowerBound < n && a[lowerBound] == x`\n- first position `lowerBound` · last position `upperBound - 1`\n- count of x: `upperBound - lowerBound` · insertion point: `lowerBound`\n- values `< x`: `lowerBound` · values `<= x`: `upperBound`\n- Every "find the first/last position" question is one of these two.\n\n## Exact match, and the library\n\n- The closed form is `lo = 0, hi = n - 1`, `while (lo <= hi)`, `return mid` on a\n  hit, and `mid ± 1` on both misses so the range always shrinks. `-1` at the end.\n- `Arrays.binarySearch(a, key)`: found gives an index, and *which* one is\n  unspecified with duplicates. Missed gives `-(insertionPoint) - 1`, so\n  `-result - 1` recovers the position. Unsorted input gives a wrong number, not\n  an exception.\n- `TreeMap`/`TreeSet` give `floor`, `ceiling`, `higher`, `lower` over a set that\n  changes between queries.\n\n## Rotated\n\n![A rotated sorted array split into two ordinary sorted runs](diagrams/binary-search-revision-rotated-halves.jpg)\n\n- Cut anywhere and **at least one half is ordinary sorted**. Test `a[lo] <= a[mid]`.\n- Left sorted: the key is in it when `a[lo] <= key && key < a[mid]`, else go right.\n- Right sorted: the key is in it when `a[mid] < key && key <= a[hi]`.\n- Finding the minimum compares against `a[hi]`, not `a[lo]` — against `a[lo]` the\n  not-rotated case walks away from the answer.\n- Duplicates break the guarantee and degrade it to O(n).\n\n## Binary search on the answer\n\nName the answer as one number, bound it loosely, write `feasible(x)` as a plain\ngreedy scan, say why it is monotone, then run the half-open loop.\n\n| Problem | Answer searched | `feasible(x)` |\n|---|---|---|\n| Koko Eating Bananas | bananas per hour | hours at speed `x` is at most `h` |\n| Capacity to Ship Packages | ship capacity | loading in order needs at most `d` days |\n| Split Array Largest Sum | largest piece sum | cutting at `x` needs at most `k` pieces |\n\n- Bounds: `lo` the largest single element, `hi` the total. Both obvious and safe.\n- Ceiling division is `(a + b - 1) / b` for positive values, not `Math.ceil`.\n- The predicate is where overflow lives. Sum into a `long`.\n- Wanting the *largest* feasible value flips it to `T T T F F` — keep `mid` on\n  the other side.\n\n## The bugs\n\n- `(lo + hi) / 2`, `hi = a.length - 1` in the half-open form, or mixing\n  `lo <= hi` with `hi = mid`.\n- A predicate that is not monotone — right on the small test, wrong on the large.\n- `int` inside the check when the sum can pass two billion.\n- Bounds that exclude the answer. Loose costs one probe; wrong costs the answer.\n- Trusting `Arrays.binarySearch` for the *first* of several equal elements.\n\n## Worth remembering\n\n- Sorting to enable one search is O(n log n) to save O(n) — a loss unless there\n  are many queries, or the sort also unlocks\n  [two pointers](#/dsa/two-pointers/notes).\n- Peak-finding is a binary search on an unsorted array, which is the proof that\n  the pattern is about predicates rather than order.\n- It is [divide and conquer](#/dsa/divide-and-conquer/notes) that throws one half\n  away instead of solving both — hence `log n` rather than `n log n`.\n';export{e as default};