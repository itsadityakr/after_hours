var e="`Math.pow` is off the table, so you write the loop yourself — and the loop is the\nwhole problem. Multiplying `x` by itself `n` times is correct and, at the top of\nthe range, two billion multiplications. Squaring gets to the same answer in\nthirty-two.\n\n## 1. The problem\n\nImplement `pow(x, n)`, which raises `x` to the power `n`.\n\n- **In** — `x`, a `double` with `−100.0 < x < 100.0`, and `n`, an `int` with\n  `−2³¹ <= n <= 2³¹ − 1`.\n- **Out** — `xⁿ`, as a `double`.\n- **Guaranteed** — either `x != 0` or `n > 0`, and the answer sits inside\n  `−10⁴ <= xⁿ <= 10⁴`.\n\nThree facts about the input decide almost everything on this page.\n\n**`n` can be negative,** and a negative exponent is a reciprocal: `x⁻³` is\n`1 / x³`. There are two places to put that division — once at the start on `x`,\nor once at the end on the answer — and the first is the one that keeps the loop\nfree of special cases.\n\n**`n` can be `−2³¹`, which has no positive counterpart in an `int`.** `2³¹` is\none past `Integer.MAX_VALUE`, so `-n` on that value gives back `−2147483648`\nunchanged, silently. This is the single input that breaks most first attempts,\nand it breaks them in the worst way: no exception, no warning, just a loop\ncondition that is false from the first turn and an answer of `1.0`.\n\n**`n` can be 0,** and `x⁰` is 1 for every `x` the constraints allow. Nothing\nneeds to be written for it — a loop that starts at `n` and counts down simply\nnever runs — but it is the first thing to test.\n\n## 2. The brute force\n\nMultiply, `n` times. The reciprocal is taken first so the loop only ever has to\ncount upwards.\n\n```java PowScan.java @run-powx-n-pow-scan\nstatic double myPow(double x, int n) {\n\n    long power = n;\n    double ans = 1;\n\n    if (power < 0) {\n        x = 1 / x;\n        power = -power;\n    }\n\n    for (long i = 0; i < power; i++) {\n        ans = ans * x;\n    }\n\n    return ans;\n}\n\nstatic long multiplications(int n) {\n    return n < 0 ? -(long) n : n;\n}\n```\n\n```output @run-powx-n-pow-scan\nmyPow(2.0, 0)                -> 1.0\nmyPow(2.0, 10)               -> 1024.0\nmyPow(2.1, 3)                -> 9.261000000000001\nmyPow(2.0, -2)               -> 0.25\nmultiplications(10)          -> 10\nmultiplications(-2147483648) -> 2147483648\n```\n\n```demo PowScan.java\nmyPow(2.0, 0)\nmyPow(2.0, 10)\nmyPow(2.1, 3)\nmyPow(2.0, -2)\nmultiplications(10)\nmultiplications(-2147483648)\n```\n\n### The code, line by line\n\n- `long power = n;` — **the widening is the fix, and it has to happen before the\n  negation.** In a `long` there is plenty of room for 2147483648, so `-power` is\n  a real number rather than a wrap. Written as `int power = n;` the method is\n  wrong on exactly one input out of four billion, and that input is on the test\n  set.\n- `double ans = 1;` — the identity for multiplication, which is also the correct\n  answer when the loop never runs. `n = 0` is handled here, by not being handled\n  anywhere.\n- `if (power < 0) { x = 1 / x; power = -power; }` — **turn the problem positive\n  once, at the top.** `x⁻ⁿ` and `(1/x)ⁿ` are the same number, so after these two\n  lines the rest of the method can pretend negative exponents do not exist. The\n  alternative — computing `xⁿ` and returning `1 / ans` at the end — works too,\n  and costs an extra branch at the bottom instead of here.\n- `for (long i = 0; i < power; i++)` — **`long i`, not `int i`.** `power` can be\n  2147483648; an `int` counter can never reach it, so the loop would never end.\n- `ans = ans * x;` — one factor of `x` per turn. This is the line that runs two\n  billion times.\n- `multiplications` is not part of the answer. It reports how many turns the loop\n  above would take, so the cost is a number in the output panel rather than a\n  claim in the prose.\n\n## 3. Dry run of the brute force\n\n`x = 2.0`, `n = 10`. The sign check does nothing, and the loop runs ten times.\n\n| turn | i | ans before | ans after |\n|---|---|---|---|\n| 1 | 0 | 1 | 2 |\n| 2 | 1 | 2 | 4 |\n| 3 | 2 | 4 | 8 |\n| 4 | 3 | 8 | 16 |\n| 5 | 4 | 16 | 32 |\n| 6 | 5 | 32 | 64 |\n| 7 | 6 | 64 | 128 |\n| 8 | 7 | 128 | 256 |\n| 9 | 8 | 256 | 512 |\n| 10 | 9 | 512 | **1024** |\n\nNow `x = 2.0`, `n = -2`, where the guard earns its place.\n\n| step | x | power | ans |\n|---|---|---|---|\n| start | 2.0 | −2 | 1 |\n| after the guard | 0.5 | 2 | 1 |\n| turn 1 | 0.5 | 2 | 0.5 |\n| turn 2 | 0.5 | 2 | **0.25** |\n\nAnd `n = −2147483648`, which is the one worth walking through slowly:\n\n| step | with `long power` | with `int power` |\n|---|---|---|\n| start | −2147483648 | −2147483648 |\n| `power < 0` | true | true |\n| `power = -power` | **2147483648** | **−2147483648** |\n| loop condition `i < power` | true, runs | false, never runs |\n| result | correct | `1.0`, always |\n\n**The `int` version does not crash and does not warn.** It returns 1.0 for every\n`x`, and it returns it instantly, which is why the failure is so easy to miss\nwhen the only inputs you tried were small.\n\n## 4. Why it is not enough\n\nTime is **O(n)** — one multiplication per unit of the exponent — and space is\nO(1). Read `multiplications(-2147483648)` in the output above: **2147483648\nturns.** At a few hundred million simple operations a second that is several\nseconds for one call, and the time limit is nowhere near it.\n\nThe waste is easier to see if you write out what the loop actually computes. To\nreach `x¹⁰` it builds, in order:\n\n```text\nx¹  x²  x³  x⁴  x⁵  x⁶  x⁷  x⁸  x⁹  x¹⁰\n```\n\nTen values, each one multiplication further along than the last. But `x¹⁰` is\n`(x⁵)²`, and `x⁵` is `x · (x²)²` — **the answer can be reached by a chain of\nsquarings that doubles the exponent each time instead of adding one to it.**\n\n```text\nx¹  x²  x⁴  x⁸  x¹⁶  x³²  …\n```\n\nThat chain reaches 2147483648 in thirty-two steps. The only question left is how\nto pick the answer out of it when `n` is not a power of two, and the answer to\nthat is already written down for you: **it is the binary form of `n`.** Ten is\n`1010`, so `x¹⁰` is `x⁸ · x²` — the two rungs of the ladder whose bits are set.\n\n## 5. The plan, in pseudocode\n\n```pseudo\nmyPow(x, n):\n\n    power <- n, as a long         so that negating it cannot overflow\n    ans   <- 1\n\n    if power < 0:\n        x     <- 1 / x            a negative exponent is a reciprocal\n        power <- -power\n\n    while power > 0:\n\n        if power is odd:\n            ans <- ans × x        this rung of the ladder is one we need\n\n        x     <- x × x            climb: x, x², x⁴, x⁸ …\n        power <- power / 2        and read the next bit of the exponent\n\n    return ans\n```\n\nTwo things are happening in the same loop and it helps to name them separately.\n**`x` is climbing the ladder** — after `k` turns it holds `x` raised to `2ᵏ`.\n**`power` is being read one bit at a time**, lowest bit first, because halving an\ninteger is the same as dropping its last binary digit. `ans` collects the rungs\nwhose bit was a 1.\n\n## 6. Halving the exponent\n\n```java Pow.java @run-powx-n-pow\nstatic double myPow(double x, int n) {\n\n    long power = n;\n    double ans = 1;\n\n    if (power < 0) {\n        x = 1 / x;\n        power = -power;\n    }\n\n    while (power > 0) {\n\n        if (power % 2 == 1) {\n            ans *= x;\n        }\n\n        x *= x;\n        power /= 2;\n    }\n\n    return ans;\n}\n\nstatic int turns(int n) {\n\n    long power = n < 0 ? -(long) n : n;\n    int t = 0;\n\n    while (power > 0) {\n        t++;\n        power /= 2;\n    }\n\n    return t;\n}\n```\n\n```output @run-powx-n-pow\nmyPow(2.0, 0)           -> 1.0\nmyPow(2.0, 10)          -> 1024.0\nmyPow(2.1, 3)           -> 9.261000000000001\nmyPow(2.0, -2)          -> 0.25\nmyPow(2.0, -2147483648) -> 0.0\nturns(10)               -> 4\nturns(-2147483648)      -> 32\n```\n\n```demo Pow.java\nmyPow(2.0, 0)\nmyPow(2.0, 10)\nmyPow(2.1, 3)\nmyPow(2.0, -2)\nmyPow(2.0, -2147483648)\nturns(10)\nturns(-2147483648)\n```\n\n### The code, line by line\n\n- `long power = n;` and the reciprocal guard — **unchanged from the brute force,\n  and for the same reasons.** The overflow trap does not go away because the loop\n  got faster; if anything it is easier to miss here, because the loop condition\n  `power > 0` also quietly returns 1.0 on a wrapped negative.\n- `while (power > 0)` — the loop is now driven by the exponent shrinking, not by\n  a counter growing. Halving reaches zero in `log₂ n` turns.\n- `if (power % 2 == 1) { ans *= x; }` — **the odd test is a bit test.** `power`\n  is odd exactly when its lowest binary digit is 1, which is exactly when the\n  rung `x` is currently holding belongs in the answer. `power & 1` is the same\n  check written the other way; both are fine, and `%` is the one that still reads\n  as arithmetic.\n- `x *= x;` — the climb. Squaring doubles the exponent `x` represents, so the\n  sequence is `x`, `x²`, `x⁴`, `x⁸`. **This line runs one more time than it needs\n  to** — on the final turn the new value is never used — and that last square can\n  overflow to infinity or underflow to zero without affecting the answer, because\n  nothing reads it.\n- `power /= 2;` — drop the bit just consumed. Integer division floors, so an odd\n  `power` loses its 1 here rather than needing a separate subtraction.\n- `return ans;` — every rung that was needed has already been multiplied in.\n- `turns` is again only there to put the cost on screen beside the answer.\n\n## 7. Dry run of the fast version\n\n`x = 2.0`, `n = 10`. Ten in binary is `1010`.\n\n| turn | power | odd? | ans after | x after | exponent x now holds |\n|---|---|---|---|---|---|\n| 1 | 10 | no | 1 | 4 | x² |\n| 2 | 5 | **yes** | 4 | 16 | x⁴ |\n| 3 | 2 | no | 4 | 256 | x⁸ |\n| 4 | 1 | **yes** | **1024** | 65536 | x¹⁶ |\n| end | 0 | — | 1024 | — | — |\n\nFour turns where the brute force took ten. The two multiplications into `ans`\nwere `4` and `256` — that is `x²` and `x⁸`, and `2 + 8` is 10.\n\n![The squaring ladder for 2 to the tenth, with only the rungs whose bit is set reaching the answer](diagrams/powx-n-notes-ladder.jpg)\n\nGrey is a rung that was climbed past, green is a rung that was taken, and blue is\nthe answer being assembled. **Every value on the top row is one squaring from the\none before it** — that is the whole saving, drawn.\n\nNow the extreme input, `x = 2.0`, `n = −2147483648`:\n\n| step | what happens |\n|---|---|\n| `long power = n` | −2147483648, in a type with room to negate it |\n| the guard | `x` becomes 0.5, `power` becomes 2147483648 |\n| the loop | 2147483648 is `2³¹`, so exactly one bit is set — 32 turns, and only the last one multiplies into `ans` |\n| `x *= x` | 0.5 squared over and over underflows to 0.0 long before the last turn |\n| result | **0.0**, which is the right answer: 2⁻²¹⁴⁷⁴⁸³⁶⁴⁸ is far below the smallest `double` |\n\nRead `turns(-2147483648)` in the output above: **32 turns instead of two\nbillion.** The underflow is not a bug — the true value really is smaller than a\n`double` can represent, so 0.0 is the closest thing there is.\n\n## 8. Key takeaways\n\n- **Widen before you negate.** `long power = n;` is the whole defence against\n  `n = −2³¹`, whose positive form does not exist in an `int`. The wrong version\n  returns 1.0 without complaining, on one input.\n- **A negative exponent is a reciprocal, taken once at the top.** `x = 1 / x`\n  and `power = -power` leave a loop that only ever counts one direction.\n- **`x⁰` is 1, and `ans = 1` is why nothing has to be written for it.** The\n  identity for multiplication doubles as the answer for the empty product.\n- **Squaring doubles the exponent; adding one increments it.** That is the gap\n  between O(n) and O(log n) — 2147483648 turns against 32.\n- **Halving the exponent is reading it in binary, lowest bit first.** `power % 2`\n  asks whether the current rung is in the answer; `power / 2` moves to the next\n  bit. `x¹⁰` is `x⁸ · x²` because ten is `1010`.\n- **The final `x *= x` is dead work and can overflow harmlessly.** Nothing reads\n  it after the loop ends, so an infinity or a zero there never reaches `ans`.\n- **O(log n) time, O(1) space.** The recursive form — `half = myPow(x, n / 2)`,\n  then `half * half`, times `x` again if `n` was odd — is the same algorithm and\n  costs O(log n) stack; this loop is the version with nothing to unwind.\n- **Test `n = 0`, `n = −2`, and `n = −2147483648`.** The last one is the problem.\n";export{e as default};