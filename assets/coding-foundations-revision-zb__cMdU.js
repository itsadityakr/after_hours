var e='The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## The digit loop\n\n- `n % 10` is the last digit. `n / 10` is the number without it.\n- `while (n > 0) { int d = n % 10; n /= 10; }` — right to left, ends on its own.\n- Copy `n` first if you still need it. The loop destroys what it is given.\n- `n = 0` produces **no** iterations. Use a `do-while` or a special case.\n- `while (n >= 0)` never ends — `0 / 10` is `0`.\n- Reverse with `rev = rev * 10 + d`. Accumulate in a `long`; the reverse of an\n  `int` need not fit in an `int`.\n- Java\'s `%` takes the sign of the left operand: `-27 % 10` is `-7`.\n\n## Divisors and primes\n\n- Divisors come in pairs around the square root, so test `d` only while\n  `d * d <= n`.\n- Write `(long) d * d <= n`, not `d <= Math.sqrt(n)`. Integers are exact and\n  cannot overflow into a negative loop condition.\n- One number, is it prime: **O(√n)**.\n- All primes below a limit: **sieve of Eratosthenes**, O(n log log n). Cross out\n  multiples starting at `p * p` — everything smaller was struck by a smaller\n  prime.\n- Trailing zeroes of `n!` is the count of 5s: `n/5 + n/25 + n/125 + …`. Never\n  build the factorial.\n\n## Powers\n\n![Fast exponentiation halving the exponent thirteen down to zero in four passes](diagrams/coding-foundations-revision-halving-the-exponent.jpg)\n\n- Fast exponentiation halves the exponent: square the base, and take one factor\n  out when the exponent is odd. **O(log n)**.\n- `(e & 1) == 1` is "odd". `e >>= 1` is "halve it".\n- Power of two: `n > 0 && (n & (n - 1)) == 0`.\n- Power of three or any other base: divide while it divides evenly, then check\n  you landed on exactly 1.\n- `Math.pow` returns a `double`. If the answer must be exact, do not use it.\n\n## Overflow\n\n![The reverse of an int overflowing past Integer.MAX_VALUE](diagrams/coding-foundations-revision-int-ceiling.jpg)\n\n- An `int` runs to 2147483647. Anything that can pass it goes in a `long`.\n- `d * d`, `rev * 10 + d` and `a + b` are the three that bite.\n- `Math.abs(Integer.MIN_VALUE)` is still `Integer.MIN_VALUE`. There is no\n  positive counterpart.\n- Check *before* multiplying — `if (rev > Integer.MAX_VALUE / 10)` — rather than\n  checking the wreckage afterwards.\n\n## Costs\n\n| Job | Cost |\n|---|---|\n| Walk the digits of n | O(log n) |\n| Is n prime | O(√n) |\n| Every prime below n | O(n log log n) |\n| x to the power n | O(log n) |\n| Trailing zeroes of n! | O(log n) |\n\n## Worth remembering\n\n- `c - \'0\'` turns a digit character into its value.\n- `(n & 1) == 0` is "even", and is the same thing as `n % 2 == 0`.\n- Integer division truncates towards zero — it does not round.\n- If a problem forbids `/`, `*` or `%`, it wants shifts and subtraction, and the\n  answer is a loop over doubling.\n- If the answer is a whole number, keep every step of the arithmetic whole.\n';export{e as default};