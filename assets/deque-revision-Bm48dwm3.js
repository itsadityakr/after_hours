var e='The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## The structure\n\n- Add and remove at both ends, all O(1). A stack uses one end; a queue uses one\n  each way; this is the general case.\n- `Deque<Integer> dq = new ArrayDeque<>()` — the one class that is a good stack,\n  a good queue and a good deque. It rejects `null`, because `peekFirst()` uses\n  `null` to mean empty.\n- `Deque` has no `get(i)`. If you need random access it is the wrong structure.\n\n## The names\n\n| Explicit | Short alias | End |\n|---|---|---|\n| `addFirst` | `push` | front |\n| `removeFirst` | `pop` | front |\n| `offerLast` | `offer` | back |\n| `pollFirst` | `poll` | front |\n| `peekFirst` | `peek` | front |\n\n- `push` and `offer` add to **opposite** ends. Use `addFirst`/`addLast` whenever\n  the code uses both.\n- Throwing forms `addFirst`, `removeFirst`, `getFirst`; returning forms\n  `offerFirst`, `pollFirst`, `peekFirst` — `null` when empty.\n- Iterating goes front to back, so a stack prints top-first — the thing\n  `java.util.Stack` gets backwards.\n\n## Why not the other two\n\n- `java.util.Stack` — synchronised, extends `Vector`, iterates the wrong way.\n- `LinkedList` — implements `Deque`, one heap object per element, no cache\n  locality, and its one advantage is not exposed by the interface.\n\n## The monotonic deque\n\n![Once a bigger value arrives later, the smaller earlier one can never win again](diagrams/deque-revision-dead-forever.svg)\n\n```java\nDeque<Integer> dq = new ArrayDeque<>();          // indices, values decreasing\nfor (int i = 0; i < a.length; i++) {\n    if (!dq.isEmpty() && dq.peekFirst() <= i - k) dq.pollFirst();    // expired\n    while (!dq.isEmpty() && a[dq.peekLast()] <= a[i]) dq.pollLast(); // useless now\n    dq.addLast(i);\n    if (i >= k - 1) out[i - k + 1] = a[dq.peekFirst()];\n}\n```\n\n- The claim: if `i < j` and `a[i] <= a[j]` then `a[i]` can never be a maximum\n  again, because every future window holding `i` also holds `j`.\n- Front is the answer. Back is where arrivals push out the useless. Both ends,\n  two reasons — that is why a stack or a queue will not do.\n- Expiry is an `if` (one index leaves per step). The back drop is a `while` (one\n  arrival can clear several). Order: expire, drop, push, read.\n- **Store indices.** "Has the front left the window" is a question about\n  position, and a value cannot answer it.\n- Sliding minimum is the same loop with `<=` flipped to `>=`.\n\n## 0-1 BFS\n\n- Every edge costs 0 or 1: `addFirst` for a zero-weight edge, `addLast` for a\n  one-weight edge, and the deque stays in distance order. O(V + E), no heap.\n- A node can come off more than once — compare distances, do not keep a `seen`\n  set.\n\n## Circular deque\n\n![Going backwards round a circular buffer needs the plus n before the modulo](diagrams/deque-revision-wrap-backwards.jpg)\n\n- Array plus `head` and `size`; the back slot is `(head + size) % n`, and\n  keeping `size` rather than a `tail` is what tells full from empty.\n- Going backwards: `(head - 1 + n) % n`. Java\'s `%` keeps the sign of the left\n  operand, so without the `+ n` you get `-1`.\n\n## Costs\n\n| Thing | Cost |\n|---|---|\n| Either end, add or remove | O(1) |\n| Growth | amortised O(1) — doubling |\n| `contains`, `remove(Object)` | O(n) |\n| Sliding window maximum | O(n) time, O(k) space |\n| 0-1 BFS | O(V + E) |\n\n## The bugs\n\n- Values stored where indices were needed.\n- Expiry checked after the push instead of before, or an answer recorded before\n  `i >= k - 1`.\n- `push` and `offer` mixed in one method.\n- `(head - 1) % n` going negative, or a `seen` set used in 0-1 BFS.\n\n## Worth remembering\n\n- If a sliding window asks for a maximum or a minimum, it is this. For a sum or\n  a count it is a plain [sliding window](#/dsa/sliding-window/notes).\n- Any dynamic-programming recurrence of the form\n  `best[i] = a[i] + max(best[i-k] .. best[i-1])` is this loop over the array you\n  are still filling in.\n';export{e as default};