var e='The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## Why it exists\n\n- Plain array: update O(1), range sum O(n). Prefix sum array: update O(n), range\n  sum O(1). Fenwick: both O(log n). Use it when the two are interleaved; if\n  nothing changes, a prefix sum array is shorter and faster.\n- Only for operations you can undo, because a range is `sum(r) - sum(l - 1)`.\n  Sum, XOR, count — yes. Min, max, gcd — no, that is a segment tree.\n\n## The whole structure\n\n![Why index zero makes the update loop spin forever](diagrams/fenwick-tree-binary-indexed-tree-revision-one-based.jpg)\n\n```java\nint n; long[] t;                              // t has n + 1 slots, slot 0 unused\n\nvoid add(int i, long v) { for (; i <= n; i += i & -i) t[i] += v; }\nlong sum(int i)  { long s = 0; for (; i > 0; i -= i & -i) s += t[i]; return s; }\nlong range(int l, int r) { return sum(r) - sum(l - 1); }\n```\n\n- `i & -i` is the lowest set bit. `t[i]` holds the sum of the `i & -i` elements\n  ending at `i`.\n- `add` walks up to wider blocks; `sum` walks down, peeling one whole block off\n  each step.\n- **One-based, not optional.** `0 & -0 == 0`, so `add(0, v)` never advances and\n  loops forever.\n\n## The blocks, for n = 8\n\n![The prefix a1..a7 as three power-of-two blocks laid end to end](diagrams/fenwick-tree-binary-indexed-tree-revision-blocks-of-seven.jpg)\n\n```text\n         a1   a2   a3   a4   a5   a6   a7   a8\nt[1]    [==]\nt[2]    [=======]\nt[3]              [==]\nt[4]    [=================]\nt[5]                        [==]\nt[6]                        [=======]\nt[7]                                  [==]\nt[8]    [=====================================]\n```\n\n- `sum(7)` = t[7] + t[6] + t[4], one bar per set bit of 7 (0111).\n- `add(5, v)` touches t[5], t[6], t[8] — every bar covering position 5.\n\n## Build in O(n)\n\n```java\nfor (int i = 1; i <= n; i++) t[i] += a[i - 1];\nfor (int i = 1; i <= n; i++) { int p = i + (i & -i); if (p <= n) t[p] += t[i]; }\n```\n\n`p > i` always, so `t[i]` is final by the time it is pushed. n calls to `add`\nwould be O(n log n).\n\n## The four arrangements\n\n| Need | Do |\n|---|---|\n| Point update, range sum | The plain tree |\n| Range update, point read | `add(l, v)`, `add(r + 1, -v)`; value is `sum(i)`. Size `n + 2` |\n| Range update, range sum | Two trees: a linear term and a constant term |\n| Two dimensions | `long[][] t`, two nested low-bit loops, O(log r × log c) |\n\n## Tree of counts\n\n- Index the tree by **value**, store 1 per occurrence. Then `sum(v)` is "how\n  many seen so far are ≤ v".\n- Count of Smaller After Self: walk right to left, read `sum(rank - 1)`, then\n  `add(rank, 1)`. Inversions are the identical loop, totalled.\n- **Coordinate compression** first: sort a copy, dedupe, `Arrays.binarySearch`\n  each value, use rank + 1. Ranks preserve order and fit in 1..n.\n\n## The bugs\n\n- Zero-based index passed in — infinite loop.\n- Array sized `n` instead of `n + 1` (or `n + 2` for the difference variant).\n- `t[i]` treated as `a[i]`. It is not; `a[i]` is `sum(i) - sum(i - 1)`.\n- `set` written as `add` — you must add `val - old`, so keep the plain array too.\n- `int` instead of `long`, overflowing silently.\n- `sum(rank)` where you meant `sum(rank - 1)`. Only fails on duplicates.\n\n## The API and what to remember\n\n| Want | Write |\n|---|---|\n| Lowest set bit | `i & -i` or `Integer.lowestOneBit(i)` |\n| Steps a query takes | `Integer.bitCount(i)` |\n| Sorted copy | `int[] s = a.clone(); Arrays.sort(s);` |\n| Rank | `Arrays.binarySearch(s, 0, distinct, v) + 1` |\n\n- Ten lines, one array, no recursion — the smallest and fastest structure that\n  does this job. Reach for a segment tree only when the operation has no inverse\n  or the updates are ranges.\n- If a hard problem counts pairs by order, it is a tree of counts over\n  compressed values, read from one end.\n';export{e as default};