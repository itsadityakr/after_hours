var e="The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## The type\n\n- A `String` is immutable. Every method that looks like it changes one returns a\n  new string.\n- `s += c` in a loop is **O(n²)**. Use `StringBuilder` whenever you assemble.\n\n![Character copies for n appends, += against StringBuilder](diagrams/strings-revision-append-cost.jpg)\n- `==` compares references. Literals are pooled, run-time strings are not, so it\n  works until the text comes from input. Use `.equals`.\n- `s.length()` is a method; `a.length` is a field; `list.size()` is a third\n  spelling.\n- `substring` **copies** since Java 7. In a loop it is quadratic.\n\n## Characters are numbers\n\n- `c - 'a'` maps a lowercase letter to 0–25. `c - '0'` maps a digit to its value.\n- `(char) (c + 1)` needs the cast — arithmetic on `char` widens to `int`.\n- `Character.isLetter`, `isDigit`, `isLetterOrDigit`, `toLowerCase`.\n\n## The counting array\n\n```java\nint[] count = new int[26];\nfor (char c : s.toCharArray()) count[c - 'a']++;\n```\n\n- O(n) time, O(1) space — the alphabet does not grow with the input.\n- `new int[128]` and index by `c` for general ASCII. A `HashMap` only for\n  Unicode.\n- Anagram: count up for one string, down for the other, in one loop. Check the\n  lengths first.\n- Anagram **key** for grouping: the sorted characters.\n\n## Palindromes\n\n![Two pointers walking in from the ends of a palindrome](diagrams/strings-revision-palindrome-pointers.svg)\n\n- Two pointers from the ends, `while (left < right)`. The middle is its own\n  mirror.\n- Skipping non-letters needs `left < right` inside the skip loops too, or a\n  string of pure punctuation runs off the end.\n- Longest palindromic substring: **expand around a centre**, and there are\n  `2n - 1` centres — every character and every gap.\n- After the expand loop the length is `right - left - 1`, and the start is\n  `centre - (len - 1) / 2`.\n- O(n²) time, O(1) space. Manacher's is O(n) and almost never asked for.\n\n## The bugs\n\n- `==` instead of `.equals`.\n- `c - 'a'` on an uppercase letter → negative index → exception.\n- No guard for `\"\"`. `charAt(0)` throws.\n- `substring` or `split` inside a loop over the input.\n- `isLetter` where the problem wanted `isLetterOrDigit`.\n- `length()` counts UTF-16 units, so an emoji counts as 2.\n\n## The API\n\n| Want | Write |\n|---|---|\n| Character at | `s.charAt(i)` |\n| As array | `s.toCharArray()` |\n| Slice | `s.substring(from, to)` — `to` exclusive |\n| Find | `s.indexOf(t)` — `-1` when absent |\n| Split on runs of space | `s.trim().split(\"\\\\s+\")` |\n| Join | `String.join(\" \", parts)` |\n| Reverse | `new StringBuilder(s).reverse().toString()` |\n| Sorted letters | `char[] c = s.toCharArray(); Arrays.sort(c); new String(c)` |\n| Order | `s.compareTo(t)` |\n| Repeat | `s.repeat(k)` |\n\n## Worth remembering\n\n- Structure the loop so the awkward case cannot arise — consume a whole run\n  inside the loop rather than handling \"the last run\" afterwards.\n- Anything asking about every window of a string wants a sliding window or a\n  rolling hash, never `substring` per position.\n- The matching problems — wildcard, interleaving, word break — are dynamic\n  programming in string clothing. Do that topic first.\n";export{e as default};