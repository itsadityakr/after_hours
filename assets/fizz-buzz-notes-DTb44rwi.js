var e='Count from 1 to `n`. Say `Fizz` for a multiple of three, `Buzz` for a multiple of\nfive, `FizzBuzz` for a multiple of both, and otherwise the number itself. Nothing\nis hidden — it checks one thing only, that you can take a rule written in English\nand write the loop that obeys it.\n\n## 1. The problem\n\nGiven an integer `n`, return a list of `n` strings answering the rule for\n`1, 2, … , n`.\n\n- **In** — `n`, an `int`, and `1 <= n <= 10⁴`.\n- **Out** — `List<String>` of length `n`, indexed from 0 but describing 1 to `n`.\n- **The rule** — `"FizzBuzz"` if divisible by 3 **and** 5, `"Fizz"` if by 3,\n  `"Buzz"` if by 5, otherwise the number as text.\n\nTwo things to settle before writing anything. **The list is 0-indexed and the\nnumbers start at 1**, so entry 0 describes 1 — a loop counting from 0 has to say\n`i + 1` in every test, which is why counting from 1 is simpler here. And **the\nnumbers come back as strings**, not integers, so even the plain case needs\nconverting.\n\n## 2. The brute force\n\nTake the four cases in the order the rule states them and test each in turn.\n\n```java FizzBuzz.java @run-fizz-buzz-fizz-buzz\nstatic List<String> fizzBuzz(int n) {\n\n    List<String> out = new ArrayList<>();\n\n    for (int i = 1; i <= n; i++) {\n\n        if (i % 3 == 0 && i % 5 == 0) {\n            out.add("FizzBuzz");\n        } else if (i % 3 == 0) {\n            out.add("Fizz");\n        } else if (i % 5 == 0) {\n            out.add("Buzz");\n        } else {\n            out.add(Integer.toString(i));\n        }\n    }\n\n    return out;\n}\n```\n\n```output @run-fizz-buzz-fizz-buzz\nfizzBuzz(1)  -> [1]\nfizzBuzz(3)  -> [1, 2, Fizz]\nfizzBuzz(5)  -> [1, 2, Fizz, 4, Buzz]\nfizzBuzz(15) -> [1, 2, Fizz, 4, Buzz, Fizz, 7, 8, Fizz, Buzz, 11, Fizz, 13, 14, FizzBuzz]\n```\n\n```demo FizzBuzz.java\nfizzBuzz(1)\nfizzBuzz(3)\nfizzBuzz(5)\nfizzBuzz(15)\n```\n\n### The code, line by line\n\n- `for (int i = 1; i <= n; i++)` — counting from 1 and using `<=`, because the\n  problem talks about 1 to `n` inclusive. Count from 0 instead and every test\n  becomes `(i + 1) % 3`, which is the same answer written worse.\n- `if (i % 3 == 0 && i % 5 == 0)` — **the both case has to be tested first.**\n  This is the entire difficulty of the problem. Put it last and 15 matches\n  `i % 3 == 0` on the way past, adds `"Fizz"`, and never reaches the branch that\n  was meant to catch it.\n- `else if (i % 3 == 0)` — reached only when the first test failed, so by the\n  time you are here `i` is *not* also a multiple of 5. The `else` is doing real\n  work: it is what makes each later test mean "and not any earlier case".\n- `out.add(Integer.toString(i));` — the fall-through. The return type is\n  `List<String>`, so the number needs converting; `String.valueOf(i)` and\n  `"" + i` are the same thing and either is fine.\n- `return out;` — built in order, so nothing needs sorting or reversing.\n\n## 3. Dry run of the brute force\n\n`n = 15`. One row per turn, and the "first branch that matched" column is the\npoint of the whole table.\n\n| i | i % 3 | i % 5 | first branch that matched | added |\n|---|---|---|---|---|\n| 1 | 1 | 1 | none — falls to `else` | `"1"` |\n| 2 | 2 | 2 | `else` | `"2"` |\n| 3 | **0** | 3 | second — `i % 3 == 0` | `"Fizz"` |\n| 4 | 1 | 4 | `else` | `"4"` |\n| 5 | 2 | **0** | third — `i % 5 == 0` | `"Buzz"` |\n| 6 | **0** | 1 | second | `"Fizz"` |\n| 7 | 1 | 2 | `else` | `"7"` |\n| 8 | 2 | 3 | `else` | `"8"` |\n| 9 | **0** | 4 | second | `"Fizz"` |\n| 10 | 1 | **0** | third | `"Buzz"` |\n| 11 | 2 | 1 | `else` | `"11"` |\n| 12 | **0** | 2 | second | `"Fizz"` |\n| 13 | 1 | 3 | `else` | `"13"` |\n| 14 | 2 | 4 | `else` | `"14"` |\n| 15 | **0** | **0** | **first** — both | `"FizzBuzz"` |\n\nThe fifteen answers, coloured by which branch produced them:\n\n![3. Dry run of the brute force — diagram](diagrams/fizz-buzz-notes-mm-1.jpg)\n\n**Only one cell in fifteen is green**, and it is the one every wrong version gets\nwrong. 15 is the first number where both tests pass, so a solution that never\ntests "both" is correct for the first fourteen answers and wrong on the\nfifteenth. That is why `n = 15` is the test to run and `n = 5` proves nothing.\n\nNow `n = 1`:\n\n| i | tests | added |\n|---|---|---|\n| 1 | both fail | `"1"` |\n\nOne turn, one string. **No guard needed** — `1 <= n` means the loop always runs\nat least once.\n\n## 4. Why it is not enough\n\nIt is enough. Time is O(n) — one turn per number, and `n` answers have to be\nproduced — and space is O(n), which is the list you were asked to return. Neither\ncan be improved.\n\nWhat can be improved is the *shape*. Count the divisibility tests: this version\nperforms up to four `%` operations per number, and two of them re-ask a question\nit has already answered. `i % 3` is computed in the first branch and computed\nagain in the second.\n\nBuild the word instead of choosing it, and each question is asked once:\n\n```text\nword = ""\nif i divisible by 3:   word = word + "Fizz"\nif i divisible by 5:   word = word + "Buzz"\nif word is still empty: word = the number as text\n```\n\nSame answers, two `%` per number instead of four — and, far more usefully, **the\n"both" case stops being a case at all.** It falls out of asking the two questions\nindependently, so there is no ordering left to get wrong.\n\nThat is the real point of the problem. When the follow-up arrives — "now also say\n`Bang` for multiples of seven" — the building version takes one more line, and\nthe branching version needs eight branches covering every combination.\nInterviewers ask FizzBuzz because they intend to extend it.\n\n## 5. Key takeaways\n\n- **Test the combined case first.** `FizzBuzz` before `Fizz` and `Buzz`, or 15\n  matches an earlier branch and never reaches its own.\n- **`n = 15` is the test.** Everything is correct up to 14 whatever you wrote.\n- **The `else` chain is what makes each later test mean "and not the earlier\n  ones".** Four independent `if`s would add two entries for 15.\n- **Count from 1 with `<=`.** The problem is stated over 1 to `n`; counting from\n  0 forces `i + 1` into every test.\n- **The output is strings**, so the plain case still needs a conversion.\n- **Building the word beats choosing it** — and the reason is extensibility, not\n  speed. A fourth word costs one line instead of doubling the branches.\n';export{e as default};