var e='The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## The operators\n\n| Write | Does |\n|---|---|\n| `a & b` | 1 where both are 1 |\n| <code>a &#124; b</code> | 1 where either is 1 |\n| `a ^ b` | 1 where they differ |\n| `~a` | flips every bit — equals `-a - 1` |\n| `a << k` | up `k` places, ×2^k |\n| `a >> k` | down `k`, **keeps the sign** |\n| `a >>> k` | down `k`, fills with 0 |\n\n- Use `>>>` whenever the value is a bag of bits. `>>` on a negative number is\n  why the loop never ends.\n- `&`, `|`, `^` bind looser than `==`. Always bracket: `(x & 1) == 0`.\n\n## The four idioms\n\n```java\nx & 1        // lowest bit — odd?\nx >>> 1      // drop the lowest bit\na ^ a == 0   // XOR cancels a pair\nx & (x - 1)  // clear the lowest set bit\n```\n\n- `x & -x` keeps **only** the lowest set bit. That one runs the Fenwick tree.\n- `x & (x - 1) == 0` with `x > 0` is "power of two".\n- Looping on `x &= x - 1` runs once per set bit — Kernighan\'s popcount.\n\n## One bit at a time\n\n```java\nint mask = 1 << k;\nboolean set = (x & mask) != 0;\nx |= mask;      // set\nx &= ~mask;     // clear\nx ^= mask;      // flip\n```\n\n- An `int` used this way is a set of up to 32 items. `1 << n` is 2ⁿ, so\n  `for (int m = 0; m < (1 << n); m++)` walks every subset.\n- `1 << 31` is negative and `1 << 32` is `1` — Java takes the shift count modulo\n  32. Use `1L <<` past 30.\n\n## XOR problems\n\n- Everything twice except one → XOR the lot.\n- Missing number → XOR the values **and** the indices.\n- Two singles → XOR all, take `x & -x` to find a differing bit, split into two\n  groups by that bit.\n- Everything k times except one → count the 1s per column, take each count\n  modulo k. Works for any k.\n- Addition without `+`: `sum = a ^ b`, `carry = (a & b) << 1`, repeat until the\n  carry is zero.\n\n## Counting bits in O(n)\n\n- `bits[i] = bits[i >>> 1] + (i & 1)`, or `bits[i] = bits[i & (i - 1)] + 1`.\n- Both say the same thing: every answer is one already computed plus one bit.\n\n## The API\n\n| Want | Write |\n|---|---|\n| Set bits | `Integer.bitCount(x)` |\n| Highest set bit | `Integer.highestOneBit(x)` |\n| Trailing zeroes | `Integer.numberOfTrailingZeros(x)` |\n| Reverse bits | `Integer.reverse(x)` |\n| As text | `Integer.toBinaryString(x)` |\n| Literal | `0b1011_0110` |\n\n## Worth remembering\n\n- Cost is O(1) per operation and O(32) for a scan of an `int` — treat both as\n  constant.\n- "Without extra memory" in a problem statement about pairs almost always means\n  XOR.\n- A 26-bit mask per word turns "do these share a letter" into one `&`.\n- `Integer.bitCount` is a single instruction. Write the loop only when asked to\n  show how it works.\n';export{e as default};