var e='The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## The structure\n\n- Index is O(1). Searching an unsorted array is O(n). Length is fixed at\n  creation.\n- Indices run `0` to `length - 1`. `a.length`, `s.length()`, `list.size()`.\n- Out of range throws `ArrayIndexOutOfBoundsException` — Java checks every\n  access.\n- `int[] b = a` is a second name, not a copy. Use `a.clone()` or\n  `Arrays.copyOf`.\n\n## The one-pass shape\n\n```java\nint write = 0;\nfor (int read = 0; read < a.length; read++)\n    if (keep(a[read])) a[write++] = a[read];\n```\n\n- `write <= read` always, so nothing unread is ever overwritten. That is why it\n  works in place.\n- Name the indices for what they do. `i` and `j` is where these get unreadable.\n- Compare against `a[write - 1]` (last kept), not `a[read - 1]` (last seen).\n  On a run of equal values those differ, and that is the classic bug.\n\n## Tricks worth having by heart\n\n- **Rotate by k**: reverse all, reverse first `k`, reverse the rest. Do\n  `k %= n` first.\n- **Product except self**: prefix pass, then suffix pass. Same shape with `+` is\n  a prefix sum.\n- **Best profit**: carry the cheapest seen; best today is today minus that.\n- **Majority element**: Boyer–Moore voting, O(n) time and O(1) space.\n- **Missing number**: XOR everything, or `n(n+1)/2` minus the sum.\n- **In-place reverse**: swap the ends, walk inwards.\n\n## When it is sorted\n\n![What sorting buys and what it costs](diagrams/arrays-revision-sorted-tradeoff.jpg)\n\n- Duplicates are adjacent — one pass finds them.\n- A pair summing to a target — one pointer at each end.\n- Membership — binary search, O(log n).\n- Sorting costs O(n log n) and **destroys the original indices**. If the answer\n  is indices, keep them alongside or use a map instead.\n\n## The bugs\n\n- `i <= a.length` reads one past the end.\n\n![The last index is length minus one, and length itself is off the end](diagrams/arrays-revision-off-by-one.jpg)\n- No guard for `[]` or `[x]`.\n- `==` compares references. Use `Arrays.equals`.\n- `println(a)` prints a hash. Use `Arrays.toString(a)`.\n- Sums of large arrays overflow `int`. Use `long`.\n- Removing inside a `for-each` throws `ConcurrentModificationException`. Loop\n  backwards by index, or `removeIf`.\n\n## The API\n\n| Want | Write |\n|---|---|\n| Sort | `Arrays.sort(a)` |\n| Sort rows by column 0 | `Arrays.sort(g, (x, y) -> Integer.compare(x[0], y[0]))` |\n| Copy | `Arrays.copyOf(a, n)` / `copyOfRange(a, from, to)` |\n| Fill | `Arrays.fill(a, v)` |\n| Search sorted | `Arrays.binarySearch(a, k)` — miss returns `-(insertion) - 1` |\n| Print | `Arrays.toString(a)` / `deepToString` |\n| To list | `Arrays.stream(a).boxed().toList()` |\n\n## Worth remembering\n\n- Ask "what do I need to know at position i, and can I carry it forward" before\n  writing a second loop. That question is most of the medium band.\n- The obvious answer is nested loops; the intended answer is nearly always one\n  pass plus one carried value or one map.\n';export{e as default};