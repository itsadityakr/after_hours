var e='The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## What it buys\n\n- One question, answered instantly: **have I seen this before**.\n- O(1) average for `put`, `get`, `containsKey`, `remove`. O(log n) worst case\n  in modern Java, because a long bucket becomes a tree.\n- O(n) space. That is the trade: memory spent to buy back a second pass.\n- **No order.** `HashMap` iteration order is arbitrary and can change.\n\n## Which one\n\n| Type | Use it when |\n|---|---|\n| `HashSet` | membership only |\n| `HashMap` | count, index, or a value per key |\n| `LinkedHashMap` | insertion order matters — LRU, stable output |\n| `TreeMap` | you need neighbours: `floorKey`, `ceilingKey` |\n| `int[26]` / `int[128]` | small fixed key range. Faster and clearer |\n\n## The shape\n\n```java\nMap<Integer, Integer> seen = new HashMap<>();\nfor (int i = 0; i < a.length; i++) {\n    if (seen.containsKey(target - a[i])) return new int[]{seen.get(target - a[i]), i};\n    seen.put(a[i], i);\n}\n```\n\n- **Check, then insert.** The other order lets an element pair with itself.\n- You never look for `a[i]` — you look for its **complement**. That is the move\n  that turns O(n²) into O(n).\n\n## The four idioms\n\n- Count: `map.merge(k, 1, Integer::sum)`\n- Read a count: `map.getOrDefault(k, 0)`\n- Group: `map.computeIfAbsent(k, x -> new ArrayList<>()).add(v)`\n- Seen: `if (!set.add(x)) …` — `add` returns false if it was already there\n\n## Choosing the key is the problem\n\n![Only values with no predecessor start a consecutive run](diagrams/hash-tables-revision-run-starts.jpg)\n\n| Problem | Key |\n|---|---|\n| Group Anagrams | the letters, sorted |\n| Subarray Sum Equals K | the running prefix sum, seeded with `put(0, 1)` |\n| Contains Duplicate II | value → last index |\n| Word Pattern | **two** maps, both directions |\n| 4Sum II | the sum of a pair from the first two arrays |\n| Longest Consecutive Sequence | the value in a set, so you can ask for `x - 1` |\n\n- Longest consecutive: only start counting at `x` when `x - 1` is absent. That\n  `continue` is what makes it O(n) instead of quadratic.\n\n## The bugs\n\n![An int array key hashes by identity and is never found again](diagrams/hash-tables-revision-key-identity.jpg)\n\n- Inserting before checking.\n- `map.get(k)` returning `null` and unboxing into an `int` → `NullPointerException`\n  with no `null` in the line.\n- `int[]` as a key — hashes by identity, nothing is ever found. Use a `record`,\n  a `String`, or `r * cols + c`.\n- `==` on boxed `Integer`s. Works to 127, fails above it.\n- Modifying the map while iterating → `ConcurrentModificationException`.\n- A custom key class with `equals` but no `hashCode`.\n- A map where an `int[26]` would do.\n\n## Worth remembering\n\n- `HashMap<Integer, Integer>` boxes every key and value. When the range is small\n  and known, the array wins on speed and on reading.\n- A `record Point(int r, int c) {}` generates `equals` and `hashCode` correctly\n  and is the cheapest safe compound key.\n- If order matters, the answer is `LinkedHashMap` or `TreeMap` — never\n  "`HashMap` seems to keep them in order".\n';export{e as default};