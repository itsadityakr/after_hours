var e='The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## Spotting it\n\n- Many range queries over data that does not change. "Immutable" in the title.\n- "How many subarrays sum to k", or "the longest subarray summing to k".\n- Equal counts of two things — score them `+1` and `-1`.\n- Many range updates and one read at the end — that is a difference array.\n\n## The skeleton\n\n```java\nint[] pre = new int[a.length + 1];\nfor (int i = 0; i < a.length; i++) pre[i + 1] = pre[i] + a[i];\n// sum of a[l..r] == pre[r + 1] - pre[l]\n```\n\n- `pre[i]` is everything strictly **before** `i`; `pre[0] = 0` is the empty\n  prefix.\n- Size it `n + 1`. That leading zero is what removes every `l == 0` branch.\n- `long[]` when the values are large — the last slot holds the whole total.\n\n## The map version\n\n```java\nMap<Integer, Integer> seen = new HashMap<>();\nseen.put(0, 1);                                  // the empty prefix, seen once\nint running = 0, count = 0;\nfor (int x : a) {\n    running += x;\n    count += seen.getOrDefault(running - k, 0);  // count first\n    seen.merge(running, 1, Integer::sum);        // then record\n}\n```\n\n- `a[l..r]` sums to `k` exactly when `pre[l] == pre[r+1] - k`. That rewrite is\n  the whole trick.\n- **Counting** subarrays: value to count, seeded `put(0, 1)`.\n- **Longest** subarray: value to first index, seeded `put(0, -1)`, never\n  overwritten.\n- Count before recording, or with `k = 0` a prefix pairs with itself.\n- Prefix XOR is the same with `^`: want `running ^ k`, because `x ^ x == 0`.\n- A [sliding window](#/dsa/sliding-window/notes) shrinks on "the sum is too big",\n  which a negative value breaks — dropping a `-5` grows the sum. Negatives, or\n  "how many", means prefix sum with a map. All non-negative and "longest" or\n  "shortest" means a window, at O(1) space.\n\n## The variants\n\n| Variant | Build | Answers |\n|---|---|---|\n| 1-D prefix | `pre[i+1] = pre[i] + a[i]` | any range sum, O(1) |\n| Prefix and a map | value to count | subarrays summing to k |\n| Prefix and a map | value to first index | longest subarray summing to k |\n| Prefix XOR | `running ^= a[i]` | subarrays XOR-ing to k |\n| Prefix count | `+1` / `-1` scoring | equal counts of two things |\n| 2-D prefix | inclusion–exclusion | any rectangle, O(1) |\n| Difference array | `+v` at `l`, `-v` at `r+1` | many range updates |\n\n## Two dimensions\n\n```java\npre[r+1][c+1] = m[r][c] + pre[r][c+1] + pre[r+1][c] - pre[r][c];\nsum = pre[r2+1][c2+1] - pre[r1][c2+1] - pre[r2+1][c1] + pre[r1][c1];\n```\n\n- The overlap block is in both bands, so it comes off once on the build and back\n  once on the query.\n- Harder grid problems: fix a pair of rows, collapse the strip to a 1-D array of\n  column sums, run the map trick on it. See [matrix](#/dsa/matrix/notes).\n\n## Costs and bugs\n\n| Thing | Time | Space |\n|---|---|---|\n| Build 1-D, then each query | O(n), then O(1) | O(n) |\n| q queries versus the loop it replaces | O(n + q) versus O(nq) | O(n) |\n| Build 2-D, map version | O(rows × cols), O(n) average | same |\n\n- Sizing it `n`, so `l = 0` needs `pre[-1]` and grows a special case.\n- `pre[r] - pre[l]` instead of `pre[r + 1] - pre[l]` — one element short.\n- No `put(0, 1)`, so every subarray starting at index 0 is missed.\n- `put(0, 0)` instead of `put(0, -1)` in the longest version, or overwriting an\n  entry there and shortening every run.\n- Recording before counting, so a prefix pairs with itself when `k = 0`.\n- `int` overflow in the totals; use `long`. Dropping the `+pre[r1][c1]` in 2-D,\n  so the corner is subtracted twice.\n\n## The API and what to remember\n\n| Want | Write |\n|---|---|\n| Zero-filled prefix array | `new int[n + 1]` / `new long[n + 1]` |\n| Read with a fallback | `map.getOrDefault(k, 0)` |\n| Insert 1, or add 1 | `map.merge(k, 1, Integer::sum)` |\n| Keep the first sighting | `map.putIfAbsent(k, i)` |\n| 2-D prefix grid | `new int[rows + 1][cols + 1]` |\n\n- "Exactly k of something" becomes a sum: score that something 1, everything\n  else 0.\n- Prefix sum is range-read plus point-update; difference array is range-update\n  plus point-read. Needing both at once is a Fenwick tree.\n- One query over the whole array is a plain loop; prefix sums pay only when the\n  queries outnumber the build.\n';export{e as default};