var e='The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## The structure\n\n- First in, first out. Join the back, served from the front.\n- `Queue<Integer> q = new ArrayDeque<>()`; `offer` / `poll` / `peek`, all O(1),\n  and `null` is rejected. `LinkedList` works and allocates a node for nothing.\n- `PriorityQueue` implements `Queue` and is **not** FIFO — it serves the smallest.\n\n## Two families of method\n\n| Job | Throws | Returns instead |\n|---|---|---|\n| Add | `add(x)` | `offer(x)` — `false` |\n| Remove front | `remove()` | `poll()` — `null` |\n| Look at front | `element()` | `peek()` — `null` |\n\n- Use `offer`/`poll`/`peek`; they read with `while (!q.isEmpty())`.\n- `remove()` serves the front. `remove(Object)` searches in O(n) — on a\n  `Queue<Integer>`, `q.remove(3)` is the second one.\n- `int x = q.poll()` on an empty queue is a `NullPointerException` from unboxing.\n- `push` is `addFirst`, `offer` is `offerLast`. They touch opposite ends.\n\n## BFS, the reason it exists\n\n![Marking a cell when you serve it lets the same cell enter the queue twice](diagrams/queues-revision-mark-on-push.jpg)\n\n```java\nQueue<int[]> q = new ArrayDeque<>();\nq.offer(new int[] { r, c, 0 });\nseen[r][c] = true;                     // marked on push, never on serve\nwhile (!q.isEmpty()) {\n    int[] cell = q.poll();\n    // ... offer each unseen neighbour, marking it as you push\n}\n```\n\n- Arrival order is distance order, so the first time you reach a cell is by a\n  shortest route.\n- Mark on push, and mark the start before the loop. Marking on serve lets a cell\n  enter the queue several times.\n- A stack here gives depth-first, which finds a path and not the shortest one.\n\n## Level by level\n\n```java\nwhile (!q.isEmpty()) {\n    int size = q.size();               // fix it first\n    for (int i = 0; i < size; i++) { /* poll, visit, offer children */ }\n}\n```\n\n- Reading `q.size()` in the loop bound re-reads it as it grows — the level never\n  ends, and nothing tells you. Increment the level counter outside the inner\n  loop.\n\n## The other shapes\n\n![A circular buffer kept with a head index and a size, wrapping past the end](diagrams/queues-revision-ring.jpg)\n\n- **Expiring window** ([Number of Recent Calls](problem:number-of-recent-calls)):\n  offer the new time, poll while the front is too old, size is the answer.\n- **Queue from two stacks**: push to `in`; to pop, pour `in` into `out` only\n  when `out` is empty. Each element crosses once, so amortised O(1).\n- **Circular buffer**: an array plus `head` and `size`. Back is\n  `(head + size) % n`, and `size` is what tells full from empty.\n- **Rotation** ([Dota2 Senate](problem:dota2-senate)): poll the front, offer it\n  straight back with its position advanced.\n\n## Costs and bugs\n\n| Thing | Cost |\n|---|---|\n| `offer`, `poll`, `peek` | O(1) |\n| `contains`, `remove(Object)` | O(n) — use a `boolean[] seen` |\n| BFS over a graph / a grid | O(V + E) / O(rows × cols) |\n| Two-stack queue | amortised O(1) |\n| BFS space | O(widest level) — not O(depth) |\n\n- `q.size()` read inside the loop bound, or seen marked on serve, or the start\n  cell never marked at all.\n- `PriorityQueue` where FIFO was meant; a `null` from `poll` unboxed into an\n  `int`; `q.remove(3)` deleting the value 3.\n\n## The API\n\n| Want | Write |\n|---|---|\n| A queue | `Queue<Integer> q = new ArrayDeque<>()` |\n| Join / serve / look | `q.offer(x)` / `q.poll()` / `q.peek()` |\n| Seed it | `new ArrayDeque<>(List.of(root))` |\n| Triples | `Queue<int[]> q` with `new int[] { r, c, d }` |\n| Seen, grid or state | `new boolean[h][w]` / `new HashSet<String>()` |\n| Neighbours | `int[][] steps = { {1,0}, {-1,0}, {0,1}, {0,-1} }` |\n\n## Worth remembering\n\n- "Fewest moves" over states that are not obviously a graph — a lock, a word, a\n  board — is still BFS. Decide what a state is, and the loop is the same one.\n- When the state gains a dimension (walls broken, keys held), index `seen` by\n  the whole state and nothing else changes.\n- Window maxima need both ends, so that lives in [deque](#/dsa/deque/notes).\n';export{e as default};