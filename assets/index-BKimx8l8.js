var e=Object.create,t=Object.defineProperty,n=Object.getOwnPropertyDescriptor,r=Object.getOwnPropertyNames,i=Object.getPrototypeOf,a=Object.prototype.hasOwnProperty,o=(e,t)=>()=>(t||(e((t={exports:{}}).exports,t),e=null),t.exports),s=(e,i,o,s)=>{if(i&&typeof i==`object`||typeof i==`function`)for(var c=r(i),l=0,u=c.length,d;l<u;l++)d=c[l],!a.call(e,d)&&d!==o&&t(e,d,{get:(e=>i[e]).bind(null,d),enumerable:!(s=n(i,d))||s.enumerable});return e},c=(n,r,a)=>(a=n==null?{}:e(i(n)),s(r||!n||!n.__esModule?t(a,`default`,{value:n,enumerable:!0}):a,n));(function(){let e=document.createElement(`link`).relList;if(e&&e.supports&&e.supports(`modulepreload`))return;for(let e of document.querySelectorAll(`link[rel="modulepreload"]`))n(e);new MutationObserver(e=>{for(let t of e)if(t.type===`childList`)for(let e of t.addedNodes)e.tagName===`LINK`&&e.rel===`modulepreload`&&n(e)}).observe(document,{childList:!0,subtree:!0});function t(e){let t={};return e.integrity&&(t.integrity=e.integrity),e.referrerPolicy&&(t.referrerPolicy=e.referrerPolicy),e.crossOrigin===`use-credentials`?t.credentials=`include`:e.crossOrigin===`anonymous`?t.credentials=`omit`:t.credentials=`same-origin`,t}function n(e){if(e.ep)return;e.ep=!0;let n=t(e);fetch(e.href,n)}})();var l=o((e=>{var t=Symbol.for(`react.transitional.element`),n=Symbol.for(`react.portal`),r=Symbol.for(`react.fragment`),i=Symbol.for(`react.strict_mode`),a=Symbol.for(`react.profiler`),o=Symbol.for(`react.consumer`),s=Symbol.for(`react.context`),c=Symbol.for(`react.forward_ref`),l=Symbol.for(`react.suspense`),u=Symbol.for(`react.memo`),d=Symbol.for(`react.lazy`),f=Symbol.for(`react.activity`),p=Symbol.iterator;function m(e){return typeof e!=`object`||!e?null:(e=p&&e[p]||e[`@@iterator`],typeof e==`function`?e:null)}var h={isMounted:function(){return!1},enqueueForceUpdate:function(){},enqueueReplaceState:function(){},enqueueSetState:function(){}},g=Object.assign,_={};function v(e,t,n){this.props=e,this.context=t,this.refs=_,this.updater=n||h}v.prototype.isReactComponent={},v.prototype.setState=function(e,t){if(typeof e!=`object`&&typeof e!=`function`&&e!=null)throw Error(`takes an object of state variables to update or a function which returns an object of state variables.`);this.updater.enqueueSetState(this,e,t,`setState`)},v.prototype.forceUpdate=function(e){this.updater.enqueueForceUpdate(this,e,`forceUpdate`)};function y(){}y.prototype=v.prototype;function b(e,t,n){this.props=e,this.context=t,this.refs=_,this.updater=n||h}var x=b.prototype=new y;x.constructor=b,g(x,v.prototype),x.isPureReactComponent=!0;var ee=Array.isArray;function S(){}var C={H:null,A:null,T:null,S:null},te=Object.prototype.hasOwnProperty;function ne(e,n,r){var i=r.ref;return{$$typeof:t,type:e,key:n,ref:i===void 0?null:i,props:r}}function w(e,t){return ne(e.type,t,e.props)}function T(e){return typeof e==`object`&&!!e&&e.$$typeof===t}function re(e){var t={"=":`=0`,":":`=2`};return`$`+e.replace(/[=:]/g,function(e){return t[e]})}var E=/\/+/g;function ie(e,t){return typeof e==`object`&&e&&e.key!=null?re(``+e.key):t.toString(36)}function D(e){switch(e.status){case`fulfilled`:return e.value;case`rejected`:throw e.reason;default:switch(typeof e.status==`string`?e.then(S,S):(e.status=`pending`,e.then(function(t){e.status===`pending`&&(e.status=`fulfilled`,e.value=t)},function(t){e.status===`pending`&&(e.status=`rejected`,e.reason=t)})),e.status){case`fulfilled`:return e.value;case`rejected`:throw e.reason}}throw e}function O(e,r,i,a,o){var s=typeof e;(s===`undefined`||s===`boolean`)&&(e=null);var c=!1;if(e===null)c=!0;else switch(s){case`bigint`:case`string`:case`number`:c=!0;break;case`object`:switch(e.$$typeof){case t:case n:c=!0;break;case d:return c=e._init,O(c(e._payload),r,i,a,o)}}if(c)return o=o(e),c=a===``?`.`+ie(e,0):a,ee(o)?(i=``,c!=null&&(i=c.replace(E,`$&/`)+`/`),O(o,r,i,``,function(e){return e})):o!=null&&(T(o)&&(o=w(o,i+(o.key==null||e&&e.key===o.key?``:(``+o.key).replace(E,`$&/`)+`/`)+c)),r.push(o)),1;c=0;var l=a===``?`.`:a+`:`;if(ee(e))for(var u=0;u<e.length;u++)a=e[u],s=l+ie(a,u),c+=O(a,r,i,s,o);else if(u=m(e),typeof u==`function`)for(e=u.call(e),u=0;!(a=e.next()).done;)a=a.value,s=l+ie(a,u++),c+=O(a,r,i,s,o);else if(s===`object`){if(typeof e.then==`function`)return O(D(e),r,i,a,o);throw r=String(e),Error(`Objects are not valid as a React child (found: `+(r===`[object Object]`?`object with keys {`+Object.keys(e).join(`, `)+`}`:r)+`). If you meant to render a collection of children, use an array instead.`)}return c}function k(e,t,n){if(e==null)return e;var r=[],i=0;return O(e,r,``,``,function(e){return t.call(n,e,i++)}),r}function ae(e){if(e._status===-1){var t=e._result;t=t(),t.then(function(t){(e._status===0||e._status===-1)&&(e._status=1,e._result=t)},function(t){(e._status===0||e._status===-1)&&(e._status=2,e._result=t)}),e._status===-1&&(e._status=0,e._result=t)}if(e._status===1)return e._result.default;throw e._result}var A=typeof reportError==`function`?reportError:function(e){if(typeof window==`object`&&typeof window.ErrorEvent==`function`){var t=new window.ErrorEvent(`error`,{bubbles:!0,cancelable:!0,message:typeof e==`object`&&e&&typeof e.message==`string`?String(e.message):String(e),error:e});if(!window.dispatchEvent(t))return}else if(typeof process==`object`&&typeof process.emit==`function`){process.emit(`uncaughtException`,e);return}console.error(e)},j={map:k,forEach:function(e,t,n){k(e,function(){t.apply(this,arguments)},n)},count:function(e){var t=0;return k(e,function(){t++}),t},toArray:function(e){return k(e,function(e){return e})||[]},only:function(e){if(!T(e))throw Error(`React.Children.only expected to receive a single React element child.`);return e}};e.Activity=f,e.Children=j,e.Component=v,e.Fragment=r,e.Profiler=a,e.PureComponent=b,e.StrictMode=i,e.Suspense=l,e.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE=C,e.__COMPILER_RUNTIME={__proto__:null,c:function(e){return C.H.useMemoCache(e)}},e.cache=function(e){return function(){return e.apply(null,arguments)}},e.cacheSignal=function(){return null},e.cloneElement=function(e,t,n){if(e==null)throw Error(`The argument must be a React element, but you passed `+e+`.`);var r=g({},e.props),i=e.key;if(t!=null)for(a in t.key!==void 0&&(i=``+t.key),t)!te.call(t,a)||a===`key`||a===`__self`||a===`__source`||a===`ref`&&t.ref===void 0||(r[a]=t[a]);var a=arguments.length-2;if(a===1)r.children=n;else if(1<a){for(var o=Array(a),s=0;s<a;s++)o[s]=arguments[s+2];r.children=o}return ne(e.type,i,r)},e.createContext=function(e){return e={$$typeof:s,_currentValue:e,_currentValue2:e,_threadCount:0,Provider:null,Consumer:null},e.Provider=e,e.Consumer={$$typeof:o,_context:e},e},e.createElement=function(e,t,n){var r,i={},a=null;if(t!=null)for(r in t.key!==void 0&&(a=``+t.key),t)te.call(t,r)&&r!==`key`&&r!==`__self`&&r!==`__source`&&(i[r]=t[r]);var o=arguments.length-2;if(o===1)i.children=n;else if(1<o){for(var s=Array(o),c=0;c<o;c++)s[c]=arguments[c+2];i.children=s}if(e&&e.defaultProps)for(r in o=e.defaultProps,o)i[r]===void 0&&(i[r]=o[r]);return ne(e,a,i)},e.createRef=function(){return{current:null}},e.forwardRef=function(e){return{$$typeof:c,render:e}},e.isValidElement=T,e.lazy=function(e){return{$$typeof:d,_payload:{_status:-1,_result:e},_init:ae}},e.memo=function(e,t){return{$$typeof:u,type:e,compare:t===void 0?null:t}},e.startTransition=function(e){var t=C.T,n={};C.T=n;try{var r=e(),i=C.S;i!==null&&i(n,r),typeof r==`object`&&r&&typeof r.then==`function`&&r.then(S,A)}catch(e){A(e)}finally{t!==null&&n.types!==null&&(t.types=n.types),C.T=t}},e.unstable_useCacheRefresh=function(){return C.H.useCacheRefresh()},e.use=function(e){return C.H.use(e)},e.useActionState=function(e,t,n){return C.H.useActionState(e,t,n)},e.useCallback=function(e,t){return C.H.useCallback(e,t)},e.useContext=function(e){return C.H.useContext(e)},e.useDebugValue=function(){},e.useDeferredValue=function(e,t){return C.H.useDeferredValue(e,t)},e.useEffect=function(e,t){return C.H.useEffect(e,t)},e.useEffectEvent=function(e){return C.H.useEffectEvent(e)},e.useId=function(){return C.H.useId()},e.useImperativeHandle=function(e,t,n){return C.H.useImperativeHandle(e,t,n)},e.useInsertionEffect=function(e,t){return C.H.useInsertionEffect(e,t)},e.useLayoutEffect=function(e,t){return C.H.useLayoutEffect(e,t)},e.useMemo=function(e,t){return C.H.useMemo(e,t)},e.useOptimistic=function(e,t){return C.H.useOptimistic(e,t)},e.useReducer=function(e,t,n){return C.H.useReducer(e,t,n)},e.useRef=function(e){return C.H.useRef(e)},e.useState=function(e){return C.H.useState(e)},e.useSyncExternalStore=function(e,t,n){return C.H.useSyncExternalStore(e,t,n)},e.useTransition=function(){return C.H.useTransition()},e.version=`19.2.7`})),u=o(((e,t)=>{t.exports=l()})),d=o((e=>{function t(e,t){var n=e.length;e.push(t);a:for(;0<n;){var r=n-1>>>1,a=e[r];if(0<i(a,t))e[r]=t,e[n]=a,n=r;else break a}}function n(e){return e.length===0?null:e[0]}function r(e){if(e.length===0)return null;var t=e[0],n=e.pop();if(n!==t){e[0]=n;a:for(var r=0,a=e.length,o=a>>>1;r<o;){var s=2*(r+1)-1,c=e[s],l=s+1,u=e[l];if(0>i(c,n))l<a&&0>i(u,c)?(e[r]=u,e[l]=n,r=l):(e[r]=c,e[s]=n,r=s);else if(l<a&&0>i(u,n))e[r]=u,e[l]=n,r=l;else break a}}return t}function i(e,t){var n=e.sortIndex-t.sortIndex;return n===0?e.id-t.id:n}if(e.unstable_now=void 0,typeof performance==`object`&&typeof performance.now==`function`){var a=performance;e.unstable_now=function(){return a.now()}}else{var o=Date,s=o.now();e.unstable_now=function(){return o.now()-s}}var c=[],l=[],u=1,d=null,f=3,p=!1,m=!1,h=!1,g=!1,_=typeof setTimeout==`function`?setTimeout:null,v=typeof clearTimeout==`function`?clearTimeout:null,y=typeof setImmediate<`u`?setImmediate:null;function b(e){for(var i=n(l);i!==null;){if(i.callback===null)r(l);else if(i.startTime<=e)r(l),i.sortIndex=i.expirationTime,t(c,i);else break;i=n(l)}}function x(e){if(h=!1,b(e),!m)if(n(c)!==null)m=!0,ee||(ee=!0,T());else{var t=n(l);t!==null&&ie(x,t.startTime-e)}}var ee=!1,S=-1,C=5,te=-1;function ne(){return g?!0:!(e.unstable_now()-te<C)}function w(){if(g=!1,ee){var t=e.unstable_now();te=t;var i=!0;try{a:{m=!1,h&&(h=!1,v(S),S=-1),p=!0;var a=f;try{b:{for(b(t),d=n(c);d!==null&&!(d.expirationTime>t&&ne());){var o=d.callback;if(typeof o==`function`){d.callback=null,f=d.priorityLevel;var s=o(d.expirationTime<=t);if(t=e.unstable_now(),typeof s==`function`){d.callback=s,b(t),i=!0;break b}d===n(c)&&r(c),b(t)}else r(c);d=n(c)}if(d!==null)i=!0;else{var u=n(l);u!==null&&ie(x,u.startTime-t),i=!1}}break a}finally{d=null,f=a,p=!1}i=void 0}}finally{i?T():ee=!1}}}var T;if(typeof y==`function`)T=function(){y(w)};else if(typeof MessageChannel<`u`){var re=new MessageChannel,E=re.port2;re.port1.onmessage=w,T=function(){E.postMessage(null)}}else T=function(){_(w,0)};function ie(t,n){S=_(function(){t(e.unstable_now())},n)}e.unstable_IdlePriority=5,e.unstable_ImmediatePriority=1,e.unstable_LowPriority=4,e.unstable_NormalPriority=3,e.unstable_Profiling=null,e.unstable_UserBlockingPriority=2,e.unstable_cancelCallback=function(e){e.callback=null},e.unstable_forceFrameRate=function(e){0>e||125<e?console.error(`forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported`):C=0<e?Math.floor(1e3/e):5},e.unstable_getCurrentPriorityLevel=function(){return f},e.unstable_next=function(e){switch(f){case 1:case 2:case 3:var t=3;break;default:t=f}var n=f;f=t;try{return e()}finally{f=n}},e.unstable_requestPaint=function(){g=!0},e.unstable_runWithPriority=function(e,t){switch(e){case 1:case 2:case 3:case 4:case 5:break;default:e=3}var n=f;f=e;try{return t()}finally{f=n}},e.unstable_scheduleCallback=function(r,i,a){var o=e.unstable_now();switch(typeof a==`object`&&a?(a=a.delay,a=typeof a==`number`&&0<a?o+a:o):a=o,r){case 1:var s=-1;break;case 2:s=250;break;case 5:s=1073741823;break;case 4:s=1e4;break;default:s=5e3}return s=a+s,r={id:u++,callback:i,priorityLevel:r,startTime:a,expirationTime:s,sortIndex:-1},a>o?(r.sortIndex=a,t(l,r),n(c)===null&&r===n(l)&&(h?(v(S),S=-1):h=!0,ie(x,a-o))):(r.sortIndex=s,t(c,r),m||p||(m=!0,ee||(ee=!0,T()))),r},e.unstable_shouldYield=ne,e.unstable_wrapCallback=function(e){var t=f;return function(){var n=f;f=t;try{return e.apply(this,arguments)}finally{f=n}}}})),f=o(((e,t)=>{t.exports=d()})),p=o((e=>{var t=u();function n(e){var t=`https://react.dev/errors/`+e;if(1<arguments.length){t+=`?args[]=`+encodeURIComponent(arguments[1]);for(var n=2;n<arguments.length;n++)t+=`&args[]=`+encodeURIComponent(arguments[n])}return`Minified React error #`+e+`; visit `+t+` for the full message or use the non-minified dev environment for full errors and additional helpful warnings.`}function r(){}var i={d:{f:r,r:function(){throw Error(n(522))},D:r,C:r,L:r,m:r,X:r,S:r,M:r},p:0,findDOMNode:null},a=Symbol.for(`react.portal`);function o(e,t,n){var r=3<arguments.length&&arguments[3]!==void 0?arguments[3]:null;return{$$typeof:a,key:r==null?null:``+r,children:e,containerInfo:t,implementation:n}}var s=t.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;function c(e,t){if(e===`font`)return``;if(typeof t==`string`)return t===`use-credentials`?t:``}e.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE=i,e.createPortal=function(e,t){var r=2<arguments.length&&arguments[2]!==void 0?arguments[2]:null;if(!t||t.nodeType!==1&&t.nodeType!==9&&t.nodeType!==11)throw Error(n(299));return o(e,t,null,r)},e.flushSync=function(e){var t=s.T,n=i.p;try{if(s.T=null,i.p=2,e)return e()}finally{s.T=t,i.p=n,i.d.f()}},e.preconnect=function(e,t){typeof e==`string`&&(t?(t=t.crossOrigin,t=typeof t==`string`?t===`use-credentials`?t:``:void 0):t=null,i.d.C(e,t))},e.prefetchDNS=function(e){typeof e==`string`&&i.d.D(e)},e.preinit=function(e,t){if(typeof e==`string`&&t&&typeof t.as==`string`){var n=t.as,r=c(n,t.crossOrigin),a=typeof t.integrity==`string`?t.integrity:void 0,o=typeof t.fetchPriority==`string`?t.fetchPriority:void 0;n===`style`?i.d.S(e,typeof t.precedence==`string`?t.precedence:void 0,{crossOrigin:r,integrity:a,fetchPriority:o}):n===`script`&&i.d.X(e,{crossOrigin:r,integrity:a,fetchPriority:o,nonce:typeof t.nonce==`string`?t.nonce:void 0})}},e.preinitModule=function(e,t){if(typeof e==`string`)if(typeof t==`object`&&t){if(t.as==null||t.as===`script`){var n=c(t.as,t.crossOrigin);i.d.M(e,{crossOrigin:n,integrity:typeof t.integrity==`string`?t.integrity:void 0,nonce:typeof t.nonce==`string`?t.nonce:void 0})}}else t??i.d.M(e)},e.preload=function(e,t){if(typeof e==`string`&&typeof t==`object`&&t&&typeof t.as==`string`){var n=t.as,r=c(n,t.crossOrigin);i.d.L(e,n,{crossOrigin:r,integrity:typeof t.integrity==`string`?t.integrity:void 0,nonce:typeof t.nonce==`string`?t.nonce:void 0,type:typeof t.type==`string`?t.type:void 0,fetchPriority:typeof t.fetchPriority==`string`?t.fetchPriority:void 0,referrerPolicy:typeof t.referrerPolicy==`string`?t.referrerPolicy:void 0,imageSrcSet:typeof t.imageSrcSet==`string`?t.imageSrcSet:void 0,imageSizes:typeof t.imageSizes==`string`?t.imageSizes:void 0,media:typeof t.media==`string`?t.media:void 0})}},e.preloadModule=function(e,t){if(typeof e==`string`)if(t){var n=c(t.as,t.crossOrigin);i.d.m(e,{as:typeof t.as==`string`&&t.as!==`script`?t.as:void 0,crossOrigin:n,integrity:typeof t.integrity==`string`?t.integrity:void 0})}else i.d.m(e)},e.requestFormReset=function(e){i.d.r(e)},e.unstable_batchedUpdates=function(e,t){return e(t)},e.useFormState=function(e,t,n){return s.H.useFormState(e,t,n)},e.useFormStatus=function(){return s.H.useHostTransitionStatus()},e.version=`19.2.7`})),m=o(((e,t)=>{function n(){if(!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__>`u`||typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE!=`function`))try{__REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(n)}catch(e){console.error(e)}}n(),t.exports=p()})),h=o((e=>{var t=f(),n=u(),r=m();function i(e){var t=`https://react.dev/errors/`+e;if(1<arguments.length){t+=`?args[]=`+encodeURIComponent(arguments[1]);for(var n=2;n<arguments.length;n++)t+=`&args[]=`+encodeURIComponent(arguments[n])}return`Minified React error #`+e+`; visit `+t+` for the full message or use the non-minified dev environment for full errors and additional helpful warnings.`}function a(e){return!(!e||e.nodeType!==1&&e.nodeType!==9&&e.nodeType!==11)}function o(e){var t=e,n=e;if(e.alternate)for(;t.return;)t=t.return;else{e=t;do t=e,t.flags&4098&&(n=t.return),e=t.return;while(e)}return t.tag===3?n:null}function s(e){if(e.tag===13){var t=e.memoizedState;if(t===null&&(e=e.alternate,e!==null&&(t=e.memoizedState)),t!==null)return t.dehydrated}return null}function c(e){if(e.tag===31){var t=e.memoizedState;if(t===null&&(e=e.alternate,e!==null&&(t=e.memoizedState)),t!==null)return t.dehydrated}return null}function l(e){if(o(e)!==e)throw Error(i(188))}function d(e){var t=e.alternate;if(!t){if(t=o(e),t===null)throw Error(i(188));return t===e?e:null}for(var n=e,r=t;;){var a=n.return;if(a===null)break;var s=a.alternate;if(s===null){if(r=a.return,r!==null){n=r;continue}break}if(a.child===s.child){for(s=a.child;s;){if(s===n)return l(a),e;if(s===r)return l(a),t;s=s.sibling}throw Error(i(188))}if(n.return!==r.return)n=a,r=s;else{for(var c=!1,u=a.child;u;){if(u===n){c=!0,n=a,r=s;break}if(u===r){c=!0,r=a,n=s;break}u=u.sibling}if(!c){for(u=s.child;u;){if(u===n){c=!0,n=s,r=a;break}if(u===r){c=!0,r=s,n=a;break}u=u.sibling}if(!c)throw Error(i(189))}}if(n.alternate!==r)throw Error(i(190))}if(n.tag!==3)throw Error(i(188));return n.stateNode.current===n?e:t}function p(e){var t=e.tag;if(t===5||t===26||t===27||t===6)return e;for(e=e.child;e!==null;){if(t=p(e),t!==null)return t;e=e.sibling}return null}var h=Object.assign,g=Symbol.for(`react.element`),_=Symbol.for(`react.transitional.element`),v=Symbol.for(`react.portal`),y=Symbol.for(`react.fragment`),b=Symbol.for(`react.strict_mode`),x=Symbol.for(`react.profiler`),ee=Symbol.for(`react.consumer`),S=Symbol.for(`react.context`),C=Symbol.for(`react.forward_ref`),te=Symbol.for(`react.suspense`),ne=Symbol.for(`react.suspense_list`),w=Symbol.for(`react.memo`),T=Symbol.for(`react.lazy`),re=Symbol.for(`react.activity`),E=Symbol.for(`react.memo_cache_sentinel`),ie=Symbol.iterator;function D(e){return typeof e!=`object`||!e?null:(e=ie&&e[ie]||e[`@@iterator`],typeof e==`function`?e:null)}var O=Symbol.for(`react.client.reference`);function k(e){if(e==null)return null;if(typeof e==`function`)return e.$$typeof===O?null:e.displayName||e.name||null;if(typeof e==`string`)return e;switch(e){case y:return`Fragment`;case x:return`Profiler`;case b:return`StrictMode`;case te:return`Suspense`;case ne:return`SuspenseList`;case re:return`Activity`}if(typeof e==`object`)switch(e.$$typeof){case v:return`Portal`;case S:return e.displayName||`Context`;case ee:return(e._context.displayName||`Context`)+`.Consumer`;case C:var t=e.render;return e=e.displayName,e||=(e=t.displayName||t.name||``,e===``?`ForwardRef`:`ForwardRef(`+e+`)`),e;case w:return t=e.displayName||null,t===null?k(e.type)||`Memo`:t;case T:t=e._payload,e=e._init;try{return k(e(t))}catch{}}return null}var ae=Array.isArray,A=n.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE,j=r.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE,oe={pending:!1,data:null,method:null,action:null},se=[],ce=-1;function le(e){return{current:e}}function ue(e){0>ce||(e.current=se[ce],se[ce]=null,ce--)}function M(e,t){ce++,se[ce]=e.current,e.current=t}var de=le(null),fe=le(null),pe=le(null),me=le(null);function he(e,t){switch(M(pe,t),M(fe,e),M(de,null),t.nodeType){case 9:case 11:e=(e=t.documentElement)&&(e=e.namespaceURI)?Vd(e):0;break;default:if(e=t.tagName,t=t.namespaceURI)t=Vd(t),e=Hd(t,e);else switch(e){case`svg`:e=1;break;case`math`:e=2;break;default:e=0}}ue(de),M(de,e)}function ge(){ue(de),ue(fe),ue(pe)}function _e(e){e.memoizedState!==null&&M(me,e);var t=de.current,n=Hd(t,e.type);t!==n&&(M(fe,e),M(de,n))}function ve(e){fe.current===e&&(ue(de),ue(fe)),me.current===e&&(ue(me),Qf._currentValue=oe)}var ye,be;function xe(e){if(ye===void 0)try{throw Error()}catch(e){var t=e.stack.trim().match(/\n( *(at )?)/);ye=t&&t[1]||``,be=-1<e.stack.indexOf(`
    at`)?` (<anonymous>)`:-1<e.stack.indexOf(`@`)?`@unknown:0:0`:``}return`
`+ye+e+be}var Se=!1;function N(e,t){if(!e||Se)return``;Se=!0;var n=Error.prepareStackTrace;Error.prepareStackTrace=void 0;try{var r={DetermineComponentFrameRoot:function(){try{if(t){var n=function(){throw Error()};if(Object.defineProperty(n.prototype,"props",{set:function(){throw Error()}}),typeof Reflect==`object`&&Reflect.construct){try{Reflect.construct(n,[])}catch(e){var r=e}Reflect.construct(e,[],n)}else{try{n.call()}catch(e){r=e}e.call(n.prototype)}}else{try{throw Error()}catch(e){r=e}(n=e())&&typeof n.catch==`function`&&n.catch(function(){})}}catch(e){if(e&&r&&typeof e.stack==`string`)return[e.stack,r.stack]}return[null,null]}};r.DetermineComponentFrameRoot.displayName=`DetermineComponentFrameRoot`;var i=Object.getOwnPropertyDescriptor(r.DetermineComponentFrameRoot,`name`);i&&i.configurable&&Object.defineProperty(r.DetermineComponentFrameRoot,"name",{value:`DetermineComponentFrameRoot`});var a=r.DetermineComponentFrameRoot(),o=a[0],s=a[1];if(o&&s){var c=o.split(`
`),l=s.split(`
`);for(i=r=0;r<c.length&&!c[r].includes(`DetermineComponentFrameRoot`);)r++;for(;i<l.length&&!l[i].includes(`DetermineComponentFrameRoot`);)i++;if(r===c.length||i===l.length)for(r=c.length-1,i=l.length-1;1<=r&&0<=i&&c[r]!==l[i];)i--;for(;1<=r&&0<=i;r--,i--)if(c[r]!==l[i]){if(r!==1||i!==1)do if(r--,i--,0>i||c[r]!==l[i]){var u=`
`+c[r].replace(` at new `,` at `);return e.displayName&&u.includes(`<anonymous>`)&&(u=u.replace(`<anonymous>`,e.displayName)),u}while(1<=r&&0<=i);break}}}finally{Se=!1,Error.prepareStackTrace=n}return(n=e?e.displayName||e.name:``)?xe(n):``}function Ce(e,t){switch(e.tag){case 26:case 27:case 5:return xe(e.type);case 16:return xe(`Lazy`);case 13:return e.child!==t&&t!==null?xe(`Suspense Fallback`):xe(`Suspense`);case 19:return xe(`SuspenseList`);case 0:case 15:return N(e.type,!1);case 11:return N(e.type.render,!1);case 1:return N(e.type,!0);case 31:return xe(`Activity`);default:return``}}function we(e){try{var t=``,n=null;do t+=Ce(e,n),n=e,e=e.return;while(e);return t}catch(e){return`
Error generating stack: `+e.message+`
`+e.stack}}var P=Object.prototype.hasOwnProperty,Te=t.unstable_scheduleCallback,Ee=t.unstable_cancelCallback,De=t.unstable_shouldYield,Oe=t.unstable_requestPaint,ke=t.unstable_now,Ae=t.unstable_getCurrentPriorityLevel,je=t.unstable_ImmediatePriority,Me=t.unstable_UserBlockingPriority,Ne=t.unstable_NormalPriority,Pe=t.unstable_LowPriority,Fe=t.unstable_IdlePriority,Ie=t.log,Le=t.unstable_setDisableYieldValue,Re=null,ze=null;function Be(e){if(typeof Ie==`function`&&Le(e),ze&&typeof ze.setStrictMode==`function`)try{ze.setStrictMode(Re,e)}catch{}}var Ve=Math.clz32?Math.clz32:We,He=Math.log,Ue=Math.LN2;function We(e){return e>>>=0,e===0?32:31-(He(e)/Ue|0)|0}var Ge=256,Ke=262144,qe=4194304;function Je(e){var t=e&42;if(t!==0)return t;switch(e&-e){case 1:return 1;case 2:return 2;case 4:return 4;case 8:return 8;case 16:return 16;case 32:return 32;case 64:return 64;case 128:return 128;case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:return e&261888;case 262144:case 524288:case 1048576:case 2097152:return e&3932160;case 4194304:case 8388608:case 16777216:case 33554432:return e&62914560;case 67108864:return 67108864;case 134217728:return 134217728;case 268435456:return 268435456;case 536870912:return 536870912;case 1073741824:return 0;default:return e}}function Ye(e,t,n){var r=e.pendingLanes;if(r===0)return 0;var i=0,a=e.suspendedLanes,o=e.pingedLanes;e=e.warmLanes;var s=r&134217727;return s===0?(s=r&~a,s===0?o===0?n||(n=r&~e,n!==0&&(i=Je(n))):i=Je(o):i=Je(s)):(r=s&~a,r===0?(o&=s,o===0?n||(n=s&~e,n!==0&&(i=Je(n))):i=Je(o)):i=Je(r)),i===0?0:t!==0&&t!==i&&(t&a)===0&&(a=i&-i,n=t&-t,a>=n||a===32&&n&4194048)?t:i}function Xe(e,t){return(e.pendingLanes&~(e.suspendedLanes&~e.pingedLanes)&t)===0}function Ze(e,t){switch(e){case 1:case 2:case 4:case 8:case 64:return t+250;case 16:case 32:case 128:case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:return t+5e3;case 4194304:case 8388608:case 16777216:case 33554432:return-1;case 67108864:case 134217728:case 268435456:case 536870912:case 1073741824:return-1;default:return-1}}function Qe(){var e=qe;return qe<<=1,!(qe&62914560)&&(qe=4194304),e}function $e(e){for(var t=[],n=0;31>n;n++)t.push(e);return t}function et(e,t){e.pendingLanes|=t,t!==268435456&&(e.suspendedLanes=0,e.pingedLanes=0,e.warmLanes=0)}function tt(e,t,n,r,i,a){var o=e.pendingLanes;e.pendingLanes=n,e.suspendedLanes=0,e.pingedLanes=0,e.warmLanes=0,e.expiredLanes&=n,e.entangledLanes&=n,e.errorRecoveryDisabledLanes&=n,e.shellSuspendCounter=0;var s=e.entanglements,c=e.expirationTimes,l=e.hiddenUpdates;for(n=o&~n;0<n;){var u=31-Ve(n),d=1<<u;s[u]=0,c[u]=-1;var f=l[u];if(f!==null)for(l[u]=null,u=0;u<f.length;u++){var p=f[u];p!==null&&(p.lane&=-536870913)}n&=~d}r!==0&&nt(e,r,0),a!==0&&i===0&&e.tag!==0&&(e.suspendedLanes|=a&~(o&~t))}function nt(e,t,n){e.pendingLanes|=t,e.suspendedLanes&=~t;var r=31-Ve(t);e.entangledLanes|=t,e.entanglements[r]=e.entanglements[r]|1073741824|n&261930}function F(e,t){var n=e.entangledLanes|=t;for(e=e.entanglements;n;){var r=31-Ve(n),i=1<<r;i&t|e[r]&t&&(e[r]|=t),n&=~i}}function rt(e,t){var n=t&-t;return n=n&42?1:it(n),(n&(e.suspendedLanes|t))===0?n:0}function it(e){switch(e){case 2:e=1;break;case 8:e=4;break;case 32:e=16;break;case 256:case 512:case 1024:case 2048:case 4096:case 8192:case 16384:case 32768:case 65536:case 131072:case 262144:case 524288:case 1048576:case 2097152:case 4194304:case 8388608:case 16777216:case 33554432:e=128;break;case 268435456:e=134217728;break;default:e=0}return e}function at(e){return e&=-e,2<e?8<e?e&134217727?32:268435456:8:2}function ot(){var e=j.p;return e===0?(e=window.event,e===void 0?32:mp(e.type)):e}function st(e,t){var n=j.p;try{return j.p=e,t()}finally{j.p=n}}var ct=Math.random().toString(36).slice(2),lt=`__reactFiber$`+ct,ut=`__reactProps$`+ct,dt=`__reactContainer$`+ct,ft=`__reactEvents$`+ct,pt=`__reactListeners$`+ct,mt=`__reactHandles$`+ct,ht=`__reactResources$`+ct,gt=`__reactMarker$`+ct;function _t(e){delete e[lt],delete e[ut],delete e[ft],delete e[pt],delete e[mt]}function vt(e){var t=e[lt];if(t)return t;for(var n=e.parentNode;n;){if(t=n[dt]||n[lt]){if(n=t.alternate,t.child!==null||n!==null&&n.child!==null)for(e=df(e);e!==null;){if(n=e[lt])return n;e=df(e)}return t}e=n,n=e.parentNode}return null}function yt(e){if(e=e[lt]||e[dt]){var t=e.tag;if(t===5||t===6||t===13||t===31||t===26||t===27||t===3)return e}return null}function bt(e){var t=e.tag;if(t===5||t===26||t===27||t===6)return e.stateNode;throw Error(i(33))}function xt(e){var t=e[ht];return t||=e[ht]={hoistableStyles:new Map,hoistableScripts:new Map},t}function St(e){e[gt]=!0}var Ct=new Set,wt={};function Tt(e,t){Et(e,t),Et(e+`Capture`,t)}function Et(e,t){for(wt[e]=t,e=0;e<t.length;e++)Ct.add(t[e])}var Dt=RegExp(`^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$`),Ot={},kt={};function At(e){return P.call(kt,e)?!0:P.call(Ot,e)?!1:Dt.test(e)?kt[e]=!0:(Ot[e]=!0,!1)}function jt(e,t,n){if(At(t))if(n===null)e.removeAttribute(t);else{switch(typeof n){case`undefined`:case`function`:case`symbol`:e.removeAttribute(t);return;case`boolean`:var r=t.toLowerCase().slice(0,5);if(r!==`data-`&&r!==`aria-`){e.removeAttribute(t);return}}e.setAttribute(t,``+n)}}function Mt(e,t,n){if(n===null)e.removeAttribute(t);else{switch(typeof n){case`undefined`:case`function`:case`symbol`:case`boolean`:e.removeAttribute(t);return}e.setAttribute(t,``+n)}}function Nt(e,t,n,r){if(r===null)e.removeAttribute(n);else{switch(typeof r){case`undefined`:case`function`:case`symbol`:case`boolean`:e.removeAttribute(n);return}e.setAttributeNS(t,n,``+r)}}function Pt(e){switch(typeof e){case`bigint`:case`boolean`:case`number`:case`string`:case`undefined`:return e;case`object`:return e;default:return``}}function Ft(e){var t=e.type;return(e=e.nodeName)&&e.toLowerCase()===`input`&&(t===`checkbox`||t===`radio`)}function It(e,t,n){var r=Object.getOwnPropertyDescriptor(e.constructor.prototype,t);if(!e.hasOwnProperty(t)&&r!==void 0&&typeof r.get==`function`&&typeof r.set==`function`){var i=r.get,a=r.set;return Object.defineProperty(e,t,{configurable:!0,get:function(){return i.call(this)},set:function(e){n=``+e,a.call(this,e)}}),Object.defineProperty(e,t,{enumerable:r.enumerable}),{getValue:function(){return n},setValue:function(e){n=``+e},stopTracking:function(){e._valueTracker=null,delete e[t]}}}}function Lt(e){if(!e._valueTracker){var t=Ft(e)?`checked`:`value`;e._valueTracker=It(e,t,``+e[t])}}function Rt(e){if(!e)return!1;var t=e._valueTracker;if(!t)return!0;var n=t.getValue(),r=``;return e&&(r=Ft(e)?e.checked?`true`:`false`:e.value),e=r,e===n?!1:(t.setValue(e),!0)}function zt(e){if(e||=typeof document<`u`?document:void 0,e===void 0)return null;try{return e.activeElement||e.body}catch{return e.body}}var Bt=/[\n"\\]/g;function Vt(e){return e.replace(Bt,function(e){return`\\`+e.charCodeAt(0).toString(16)+` `})}function Ht(e,t,n,r,i,a,o,s){e.name=``,o!=null&&typeof o!=`function`&&typeof o!=`symbol`&&typeof o!=`boolean`?e.type=o:e.removeAttribute(`type`),t==null?o!==`submit`&&o!==`reset`||e.removeAttribute(`value`):o===`number`?(t===0&&e.value===``||e.value!=t)&&(e.value=``+Pt(t)):e.value!==``+Pt(t)&&(e.value=``+Pt(t)),t==null?n==null?r!=null&&e.removeAttribute(`value`):Wt(e,o,Pt(n)):Wt(e,o,Pt(t)),i==null&&a!=null&&(e.defaultChecked=!!a),i!=null&&(e.checked=i&&typeof i!=`function`&&typeof i!=`symbol`),s!=null&&typeof s!=`function`&&typeof s!=`symbol`&&typeof s!=`boolean`?e.name=``+Pt(s):e.removeAttribute(`name`)}function Ut(e,t,n,r,i,a,o,s){if(a!=null&&typeof a!=`function`&&typeof a!=`symbol`&&typeof a!=`boolean`&&(e.type=a),t!=null||n!=null){if(!(a!==`submit`&&a!==`reset`||t!=null)){Lt(e);return}n=n==null?``:``+Pt(n),t=t==null?n:``+Pt(t),s||t===e.value||(e.value=t),e.defaultValue=t}r??=i,r=typeof r!=`function`&&typeof r!=`symbol`&&!!r,e.checked=s?e.checked:!!r,e.defaultChecked=!!r,o!=null&&typeof o!=`function`&&typeof o!=`symbol`&&typeof o!=`boolean`&&(e.name=o),Lt(e)}function Wt(e,t,n){t===`number`&&zt(e.ownerDocument)===e||e.defaultValue===``+n||(e.defaultValue=``+n)}function Gt(e,t,n,r){if(e=e.options,t){t={};for(var i=0;i<n.length;i++)t[`$`+n[i]]=!0;for(n=0;n<e.length;n++)i=t.hasOwnProperty(`$`+e[n].value),e[n].selected!==i&&(e[n].selected=i),i&&r&&(e[n].defaultSelected=!0)}else{for(n=``+Pt(n),t=null,i=0;i<e.length;i++){if(e[i].value===n){e[i].selected=!0,r&&(e[i].defaultSelected=!0);return}t!==null||e[i].disabled||(t=e[i])}t!==null&&(t.selected=!0)}}function Kt(e,t,n){if(t!=null&&(t=``+Pt(t),t!==e.value&&(e.value=t),n==null)){e.defaultValue!==t&&(e.defaultValue=t);return}e.defaultValue=n==null?``:``+Pt(n)}function qt(e,t,n,r){if(t==null){if(r!=null){if(n!=null)throw Error(i(92));if(ae(r)){if(1<r.length)throw Error(i(93));r=r[0]}n=r}n??=``,t=n}n=Pt(t),e.defaultValue=n,r=e.textContent,r===n&&r!==``&&r!==null&&(e.value=r),Lt(e)}function Jt(e,t){if(t){var n=e.firstChild;if(n&&n===e.lastChild&&n.nodeType===3){n.nodeValue=t;return}}e.textContent=t}var Yt=new Set(`animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp`.split(` `));function Xt(e,t,n){var r=t.indexOf(`--`)===0;n==null||typeof n==`boolean`||n===``?r?e.setProperty(t,``):t===`float`?e.cssFloat=``:e[t]=``:r?e.setProperty(t,n):typeof n!=`number`||n===0||Yt.has(t)?t===`float`?e.cssFloat=n:e[t]=(``+n).trim():e[t]=n+`px`}function Zt(e,t,n){if(t!=null&&typeof t!=`object`)throw Error(i(62));if(e=e.style,n!=null){for(var r in n)!n.hasOwnProperty(r)||t!=null&&t.hasOwnProperty(r)||(r.indexOf(`--`)===0?e.setProperty(r,``):r===`float`?e.cssFloat=``:e[r]=``);for(var a in t)r=t[a],t.hasOwnProperty(a)&&n[a]!==r&&Xt(e,a,r)}else for(var o in t)t.hasOwnProperty(o)&&Xt(e,o,t[o])}function Qt(e){if(e.indexOf(`-`)===-1)return!1;switch(e){case`annotation-xml`:case`color-profile`:case`font-face`:case`font-face-src`:case`font-face-uri`:case`font-face-format`:case`font-face-name`:case`missing-glyph`:return!1;default:return!0}}var $t=new Map([[`acceptCharset`,`accept-charset`],[`htmlFor`,`for`],[`httpEquiv`,`http-equiv`],[`crossOrigin`,`crossorigin`],[`accentHeight`,`accent-height`],[`alignmentBaseline`,`alignment-baseline`],[`arabicForm`,`arabic-form`],[`baselineShift`,`baseline-shift`],[`capHeight`,`cap-height`],[`clipPath`,`clip-path`],[`clipRule`,`clip-rule`],[`colorInterpolation`,`color-interpolation`],[`colorInterpolationFilters`,`color-interpolation-filters`],[`colorProfile`,`color-profile`],[`colorRendering`,`color-rendering`],[`dominantBaseline`,`dominant-baseline`],[`enableBackground`,`enable-background`],[`fillOpacity`,`fill-opacity`],[`fillRule`,`fill-rule`],[`floodColor`,`flood-color`],[`floodOpacity`,`flood-opacity`],[`fontFamily`,`font-family`],[`fontSize`,`font-size`],[`fontSizeAdjust`,`font-size-adjust`],[`fontStretch`,`font-stretch`],[`fontStyle`,`font-style`],[`fontVariant`,`font-variant`],[`fontWeight`,`font-weight`],[`glyphName`,`glyph-name`],[`glyphOrientationHorizontal`,`glyph-orientation-horizontal`],[`glyphOrientationVertical`,`glyph-orientation-vertical`],[`horizAdvX`,`horiz-adv-x`],[`horizOriginX`,`horiz-origin-x`],[`imageRendering`,`image-rendering`],[`letterSpacing`,`letter-spacing`],[`lightingColor`,`lighting-color`],[`markerEnd`,`marker-end`],[`markerMid`,`marker-mid`],[`markerStart`,`marker-start`],[`overlinePosition`,`overline-position`],[`overlineThickness`,`overline-thickness`],[`paintOrder`,`paint-order`],[`panose-1`,`panose-1`],[`pointerEvents`,`pointer-events`],[`renderingIntent`,`rendering-intent`],[`shapeRendering`,`shape-rendering`],[`stopColor`,`stop-color`],[`stopOpacity`,`stop-opacity`],[`strikethroughPosition`,`strikethrough-position`],[`strikethroughThickness`,`strikethrough-thickness`],[`strokeDasharray`,`stroke-dasharray`],[`strokeDashoffset`,`stroke-dashoffset`],[`strokeLinecap`,`stroke-linecap`],[`strokeLinejoin`,`stroke-linejoin`],[`strokeMiterlimit`,`stroke-miterlimit`],[`strokeOpacity`,`stroke-opacity`],[`strokeWidth`,`stroke-width`],[`textAnchor`,`text-anchor`],[`textDecoration`,`text-decoration`],[`textRendering`,`text-rendering`],[`transformOrigin`,`transform-origin`],[`underlinePosition`,`underline-position`],[`underlineThickness`,`underline-thickness`],[`unicodeBidi`,`unicode-bidi`],[`unicodeRange`,`unicode-range`],[`unitsPerEm`,`units-per-em`],[`vAlphabetic`,`v-alphabetic`],[`vHanging`,`v-hanging`],[`vIdeographic`,`v-ideographic`],[`vMathematical`,`v-mathematical`],[`vectorEffect`,`vector-effect`],[`vertAdvY`,`vert-adv-y`],[`vertOriginX`,`vert-origin-x`],[`vertOriginY`,`vert-origin-y`],[`wordSpacing`,`word-spacing`],[`writingMode`,`writing-mode`],[`xmlnsXlink`,`xmlns:xlink`],[`xHeight`,`x-height`]]),en=/^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;function tn(e){return en.test(``+e)?`javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')`:e}function nn(){}var rn=null;function an(e){return e=e.target||e.srcElement||window,e.correspondingUseElement&&(e=e.correspondingUseElement),e.nodeType===3?e.parentNode:e}var on=null,sn=null;function cn(e){var t=yt(e);if(t&&(e=t.stateNode)){var n=e[ut]||null;a:switch(e=t.stateNode,t.type){case`input`:if(Ht(e,n.value,n.defaultValue,n.defaultValue,n.checked,n.defaultChecked,n.type,n.name),t=n.name,n.type===`radio`&&t!=null){for(n=e;n.parentNode;)n=n.parentNode;for(n=n.querySelectorAll(`input[name="`+Vt(``+t)+`"][type="radio"]`),t=0;t<n.length;t++){var r=n[t];if(r!==e&&r.form===e.form){var a=r[ut]||null;if(!a)throw Error(i(90));Ht(r,a.value,a.defaultValue,a.defaultValue,a.checked,a.defaultChecked,a.type,a.name)}}for(t=0;t<n.length;t++)r=n[t],r.form===e.form&&Rt(r)}break a;case`textarea`:Kt(e,n.value,n.defaultValue);break a;case`select`:t=n.value,t!=null&&Gt(e,!!n.multiple,t,!1)}}}var ln=!1;function un(e,t,n){if(ln)return e(t,n);ln=!0;try{return e(t)}finally{if(ln=!1,(on!==null||sn!==null)&&(bu(),on&&(t=on,e=sn,sn=on=null,cn(t),e)))for(t=0;t<e.length;t++)cn(e[t])}}function dn(e,t){var n=e.stateNode;if(n===null)return null;var r=n[ut]||null;if(r===null)return null;n=r[t];a:switch(t){case`onClick`:case`onClickCapture`:case`onDoubleClick`:case`onDoubleClickCapture`:case`onMouseDown`:case`onMouseDownCapture`:case`onMouseMove`:case`onMouseMoveCapture`:case`onMouseUp`:case`onMouseUpCapture`:case`onMouseEnter`:(r=!r.disabled)||(e=e.type,r=!(e===`button`||e===`input`||e===`select`||e===`textarea`)),e=!r;break a;default:e=!1}if(e)return null;if(n&&typeof n!=`function`)throw Error(i(231,t,typeof n));return n}var fn=!(typeof window>`u`||window.document===void 0||window.document.createElement===void 0),pn=!1;if(fn)try{var mn={};Object.defineProperty(mn,"passive",{get:function(){pn=!0}}),window.addEventListener(`test`,mn,mn),window.removeEventListener(`test`,mn,mn)}catch{pn=!1}var hn=null,gn=null,_n=null;function vn(){if(_n)return _n;var e,t=gn,n=t.length,r,i=`value`in hn?hn.value:hn.textContent,a=i.length;for(e=0;e<n&&t[e]===i[e];e++);var o=n-e;for(r=1;r<=o&&t[n-r]===i[a-r];r++);return _n=i.slice(e,1<r?1-r:void 0)}function yn(e){var t=e.keyCode;return`charCode`in e?(e=e.charCode,e===0&&t===13&&(e=13)):e=t,e===10&&(e=13),32<=e||e===13?e:0}function bn(){return!0}function xn(){return!1}function Sn(e){function t(t,n,r,i,a){for(var o in this._reactName=t,this._targetInst=r,this.type=n,this.nativeEvent=i,this.target=a,this.currentTarget=null,e)e.hasOwnProperty(o)&&(t=e[o],this[o]=t?t(i):i[o]);return this.isDefaultPrevented=(i.defaultPrevented==null?!1===i.returnValue:i.defaultPrevented)?bn:xn,this.isPropagationStopped=xn,this}return h(t.prototype,{preventDefault:function(){this.defaultPrevented=!0;var e=this.nativeEvent;e&&(e.preventDefault?e.preventDefault():typeof e.returnValue!=`unknown`&&(e.returnValue=!1),this.isDefaultPrevented=bn)},stopPropagation:function(){var e=this.nativeEvent;e&&(e.stopPropagation?e.stopPropagation():typeof e.cancelBubble!=`unknown`&&(e.cancelBubble=!0),this.isPropagationStopped=bn)},persist:function(){},isPersistent:bn}),t}var Cn={eventPhase:0,bubbles:0,cancelable:0,timeStamp:function(e){return e.timeStamp||Date.now()},defaultPrevented:0,isTrusted:0},wn=Sn(Cn),Tn=h({},Cn,{view:0,detail:0}),En=Sn(Tn),Dn,On,kn,An=h({},Tn,{screenX:0,screenY:0,clientX:0,clientY:0,pageX:0,pageY:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,getModifierState:Bn,button:0,buttons:0,relatedTarget:function(e){return e.relatedTarget===void 0?e.fromElement===e.srcElement?e.toElement:e.fromElement:e.relatedTarget},movementX:function(e){return`movementX`in e?e.movementX:(e!==kn&&(kn&&e.type===`mousemove`?(Dn=e.screenX-kn.screenX,On=e.screenY-kn.screenY):On=Dn=0,kn=e),Dn)},movementY:function(e){return`movementY`in e?e.movementY:On}}),jn=Sn(An),I=Sn(h({},An,{dataTransfer:0})),Mn=Sn(h({},Tn,{relatedTarget:0})),Nn=Sn(h({},Cn,{animationName:0,elapsedTime:0,pseudoElement:0})),Pn=Sn(h({},Cn,{clipboardData:function(e){return`clipboardData`in e?e.clipboardData:window.clipboardData}})),Fn=Sn(h({},Cn,{data:0})),In={Esc:`Escape`,Spacebar:` `,Left:`ArrowLeft`,Up:`ArrowUp`,Right:`ArrowRight`,Down:`ArrowDown`,Del:`Delete`,Win:`OS`,Menu:`ContextMenu`,Apps:`ContextMenu`,Scroll:`ScrollLock`,MozPrintableKey:`Unidentified`},Ln={8:`Backspace`,9:`Tab`,12:`Clear`,13:`Enter`,16:`Shift`,17:`Control`,18:`Alt`,19:`Pause`,20:`CapsLock`,27:`Escape`,32:` `,33:`PageUp`,34:`PageDown`,35:`End`,36:`Home`,37:`ArrowLeft`,38:`ArrowUp`,39:`ArrowRight`,40:`ArrowDown`,45:`Insert`,46:`Delete`,112:`F1`,113:`F2`,114:`F3`,115:`F4`,116:`F5`,117:`F6`,118:`F7`,119:`F8`,120:`F9`,121:`F10`,122:`F11`,123:`F12`,144:`NumLock`,145:`ScrollLock`,224:`Meta`},Rn={Alt:`altKey`,Control:`ctrlKey`,Meta:`metaKey`,Shift:`shiftKey`};function zn(e){var t=this.nativeEvent;return t.getModifierState?t.getModifierState(e):(e=Rn[e])?!!t[e]:!1}function Bn(){return zn}var Vn=Sn(h({},Tn,{key:function(e){if(e.key){var t=In[e.key]||e.key;if(t!==`Unidentified`)return t}return e.type===`keypress`?(e=yn(e),e===13?`Enter`:String.fromCharCode(e)):e.type===`keydown`||e.type===`keyup`?Ln[e.keyCode]||`Unidentified`:``},code:0,location:0,ctrlKey:0,shiftKey:0,altKey:0,metaKey:0,repeat:0,locale:0,getModifierState:Bn,charCode:function(e){return e.type===`keypress`?yn(e):0},keyCode:function(e){return e.type===`keydown`||e.type===`keyup`?e.keyCode:0},which:function(e){return e.type===`keypress`?yn(e):e.type===`keydown`||e.type===`keyup`?e.keyCode:0}})),Hn=Sn(h({},An,{pointerId:0,width:0,height:0,pressure:0,tangentialPressure:0,tiltX:0,tiltY:0,twist:0,pointerType:0,isPrimary:0})),Un=Sn(h({},Tn,{touches:0,targetTouches:0,changedTouches:0,altKey:0,metaKey:0,ctrlKey:0,shiftKey:0,getModifierState:Bn})),Wn=Sn(h({},Cn,{propertyName:0,elapsedTime:0,pseudoElement:0})),Gn=Sn(h({},An,{deltaX:function(e){return`deltaX`in e?e.deltaX:`wheelDeltaX`in e?-e.wheelDeltaX:0},deltaY:function(e){return`deltaY`in e?e.deltaY:`wheelDeltaY`in e?-e.wheelDeltaY:`wheelDelta`in e?-e.wheelDelta:0},deltaZ:0,deltaMode:0})),Kn=Sn(h({},Cn,{newState:0,oldState:0})),qn=[9,13,27,32],Jn=fn&&`CompositionEvent`in window,Yn=null;fn&&`documentMode`in document&&(Yn=document.documentMode);var Xn=fn&&`TextEvent`in window&&!Yn,Zn=fn&&(!Jn||Yn&&8<Yn&&11>=Yn),Qn=` `,$n=!1;function er(e,t){switch(e){case`keyup`:return qn.indexOf(t.keyCode)!==-1;case`keydown`:return t.keyCode!==229;case`keypress`:case`mousedown`:case`focusout`:return!0;default:return!1}}function tr(e){return e=e.detail,typeof e==`object`&&`data`in e?e.data:null}var nr=!1;function rr(e,t){switch(e){case`compositionend`:return tr(t);case`keypress`:return t.which===32?($n=!0,Qn):null;case`textInput`:return e=t.data,e===Qn&&$n?null:e;default:return null}}function ir(e,t){if(nr)return e===`compositionend`||!Jn&&er(e,t)?(e=vn(),_n=gn=hn=null,nr=!1,e):null;switch(e){case`paste`:return null;case`keypress`:if(!(t.ctrlKey||t.altKey||t.metaKey)||t.ctrlKey&&t.altKey){if(t.char&&1<t.char.length)return t.char;if(t.which)return String.fromCharCode(t.which)}return null;case`compositionend`:return Zn&&t.locale!==`ko`?null:t.data;default:return null}}var ar={color:!0,date:!0,datetime:!0,"datetime-local":!0,email:!0,month:!0,number:!0,password:!0,range:!0,search:!0,tel:!0,text:!0,time:!0,url:!0,week:!0};function or(e){var t=e&&e.nodeName&&e.nodeName.toLowerCase();return t===`input`?!!ar[e.type]:t===`textarea`}function sr(e,t,n,r){on?sn?sn.push(r):sn=[r]:on=r,t=Ed(t,`onChange`),0<t.length&&(n=new wn(`onChange`,`change`,null,n,r),e.push({event:n,listeners:t}))}var cr=null,lr=null;function ur(e){yd(e,0)}function dr(e){if(Rt(bt(e)))return e}function fr(e,t){if(e===`change`)return t}var pr=!1;if(fn){var mr;if(fn){var hr=`oninput`in document;if(!hr){var gr=document.createElement(`div`);gr.setAttribute(`oninput`,`return;`),hr=typeof gr.oninput==`function`}mr=hr}else mr=!1;pr=mr&&(!document.documentMode||9<document.documentMode)}function _r(){cr&&(cr.detachEvent(`onpropertychange`,vr),lr=cr=null)}function vr(e){if(e.propertyName===`value`&&dr(lr)){var t=[];sr(t,lr,e,an(e)),un(ur,t)}}function yr(e,t,n){e===`focusin`?(_r(),cr=t,lr=n,cr.attachEvent(`onpropertychange`,vr)):e===`focusout`&&_r()}function br(e){if(e===`selectionchange`||e===`keyup`||e===`keydown`)return dr(lr)}function xr(e,t){if(e===`click`)return dr(t)}function Sr(e,t){if(e===`input`||e===`change`)return dr(t)}function Cr(e,t){return e===t&&(e!==0||1/e==1/t)||e!==e&&t!==t}var wr=typeof Object.is==`function`?Object.is:Cr;function Tr(e,t){if(wr(e,t))return!0;if(typeof e!=`object`||!e||typeof t!=`object`||!t)return!1;var n=Object.keys(e),r=Object.keys(t);if(n.length!==r.length)return!1;for(r=0;r<n.length;r++){var i=n[r];if(!P.call(t,i)||!wr(e[i],t[i]))return!1}return!0}function Er(e){for(;e&&e.firstChild;)e=e.firstChild;return e}function Dr(e,t){var n=Er(e);e=0;for(var r;n;){if(n.nodeType===3){if(r=e+n.textContent.length,e<=t&&r>=t)return{node:n,offset:t-e};e=r}a:{for(;n;){if(n.nextSibling){n=n.nextSibling;break a}n=n.parentNode}n=void 0}n=Er(n)}}function Or(e,t){return e&&t?e===t?!0:e&&e.nodeType===3?!1:t&&t.nodeType===3?Or(e,t.parentNode):`contains`in e?e.contains(t):e.compareDocumentPosition?!!(e.compareDocumentPosition(t)&16):!1:!1}function kr(e){e=e!=null&&e.ownerDocument!=null&&e.ownerDocument.defaultView!=null?e.ownerDocument.defaultView:window;for(var t=zt(e.document);t instanceof e.HTMLIFrameElement;){try{var n=typeof t.contentWindow.location.href==`string`}catch{n=!1}if(n)e=t.contentWindow;else break;t=zt(e.document)}return t}function Ar(e){var t=e&&e.nodeName&&e.nodeName.toLowerCase();return t&&(t===`input`&&(e.type===`text`||e.type===`search`||e.type===`tel`||e.type===`url`||e.type===`password`)||t===`textarea`||e.contentEditable===`true`)}var jr=fn&&`documentMode`in document&&11>=document.documentMode,Mr=null,Nr=null,Pr=null,Fr=!1;function Ir(e,t,n){var r=n.window===n?n.document:n.nodeType===9?n:n.ownerDocument;Fr||Mr==null||Mr!==zt(r)||(r=Mr,`selectionStart`in r&&Ar(r)?r={start:r.selectionStart,end:r.selectionEnd}:(r=(r.ownerDocument&&r.ownerDocument.defaultView||window).getSelection(),r={anchorNode:r.anchorNode,anchorOffset:r.anchorOffset,focusNode:r.focusNode,focusOffset:r.focusOffset}),Pr&&Tr(Pr,r)||(Pr=r,r=Ed(Nr,`onSelect`),0<r.length&&(t=new wn(`onSelect`,`select`,null,t,n),e.push({event:t,listeners:r}),t.target=Mr)))}function Lr(e,t){var n={};return n[e.toLowerCase()]=t.toLowerCase(),n[`Webkit`+e]=`webkit`+t,n[`Moz`+e]=`moz`+t,n}var Rr={animationend:Lr(`Animation`,`AnimationEnd`),animationiteration:Lr(`Animation`,`AnimationIteration`),animationstart:Lr(`Animation`,`AnimationStart`),transitionrun:Lr(`Transition`,`TransitionRun`),transitionstart:Lr(`Transition`,`TransitionStart`),transitioncancel:Lr(`Transition`,`TransitionCancel`),transitionend:Lr(`Transition`,`TransitionEnd`)},zr={},Br={};fn&&(Br=document.createElement(`div`).style,`AnimationEvent`in window||(delete Rr.animationend.animation,delete Rr.animationiteration.animation,delete Rr.animationstart.animation),`TransitionEvent`in window||delete Rr.transitionend.transition);function Vr(e){if(zr[e])return zr[e];if(!Rr[e])return e;var t=Rr[e],n;for(n in t)if(t.hasOwnProperty(n)&&n in Br)return zr[e]=t[n];return e}var Hr=Vr(`animationend`),Ur=Vr(`animationiteration`),Wr=Vr(`animationstart`),Gr=Vr(`transitionrun`),Kr=Vr(`transitionstart`),qr=Vr(`transitioncancel`),Jr=Vr(`transitionend`),Yr=new Map,Xr=`abort auxClick beforeToggle cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel`.split(` `);Xr.push(`scrollEnd`);function Zr(e,t){Yr.set(e,t),Tt(t,[e])}var Qr=typeof reportError==`function`?reportError:function(e){if(typeof window==`object`&&typeof window.ErrorEvent==`function`){var t=new window.ErrorEvent(`error`,{bubbles:!0,cancelable:!0,message:typeof e==`object`&&e&&typeof e.message==`string`?String(e.message):String(e),error:e});if(!window.dispatchEvent(t))return}else if(typeof process==`object`&&typeof process.emit==`function`){process.emit(`uncaughtException`,e);return}console.error(e)},$r=[],ei=0,ti=0;function ni(){for(var e=ei,t=ti=ei=0;t<e;){var n=$r[t];$r[t++]=null;var r=$r[t];$r[t++]=null;var i=$r[t];$r[t++]=null;var a=$r[t];if($r[t++]=null,r!==null&&i!==null){var o=r.pending;o===null?i.next=i:(i.next=o.next,o.next=i),r.pending=i}a!==0&&oi(n,i,a)}}function ri(e,t,n,r){$r[ei++]=e,$r[ei++]=t,$r[ei++]=n,$r[ei++]=r,ti|=r,e.lanes|=r,e=e.alternate,e!==null&&(e.lanes|=r)}function ii(e,t,n,r){return ri(e,t,n,r),si(e)}function ai(e,t){return ri(e,null,null,t),si(e)}function oi(e,t,n){e.lanes|=n;var r=e.alternate;r!==null&&(r.lanes|=n);for(var i=!1,a=e.return;a!==null;)a.childLanes|=n,r=a.alternate,r!==null&&(r.childLanes|=n),a.tag===22&&(e=a.stateNode,e===null||e._visibility&1||(i=!0)),e=a,a=a.return;return e.tag===3?(a=e.stateNode,i&&t!==null&&(i=31-Ve(n),e=a.hiddenUpdates,r=e[i],r===null?e[i]=[t]:r.push(t),t.lane=n|536870912),a):null}function si(e){if(50<du)throw du=0,fu=null,Error(i(185));for(var t=e.return;t!==null;)e=t,t=e.return;return e.tag===3?e.stateNode:null}var ci={};function li(e,t,n,r){this.tag=e,this.key=n,this.sibling=this.child=this.return=this.stateNode=this.type=this.elementType=null,this.index=0,this.refCleanup=this.ref=null,this.pendingProps=t,this.dependencies=this.memoizedState=this.updateQueue=this.memoizedProps=null,this.mode=r,this.subtreeFlags=this.flags=0,this.deletions=null,this.childLanes=this.lanes=0,this.alternate=null}function ui(e,t,n,r){return new li(e,t,n,r)}function di(e){return e=e.prototype,!(!e||!e.isReactComponent)}function fi(e,t){var n=e.alternate;return n===null?(n=ui(e.tag,t,e.key,e.mode),n.elementType=e.elementType,n.type=e.type,n.stateNode=e.stateNode,n.alternate=e,e.alternate=n):(n.pendingProps=t,n.type=e.type,n.flags=0,n.subtreeFlags=0,n.deletions=null),n.flags=e.flags&65011712,n.childLanes=e.childLanes,n.lanes=e.lanes,n.child=e.child,n.memoizedProps=e.memoizedProps,n.memoizedState=e.memoizedState,n.updateQueue=e.updateQueue,t=e.dependencies,n.dependencies=t===null?null:{lanes:t.lanes,firstContext:t.firstContext},n.sibling=e.sibling,n.index=e.index,n.ref=e.ref,n.refCleanup=e.refCleanup,n}function pi(e,t){e.flags&=65011714;var n=e.alternate;return n===null?(e.childLanes=0,e.lanes=t,e.child=null,e.subtreeFlags=0,e.memoizedProps=null,e.memoizedState=null,e.updateQueue=null,e.dependencies=null,e.stateNode=null):(e.childLanes=n.childLanes,e.lanes=n.lanes,e.child=n.child,e.subtreeFlags=0,e.deletions=null,e.memoizedProps=n.memoizedProps,e.memoizedState=n.memoizedState,e.updateQueue=n.updateQueue,e.type=n.type,t=n.dependencies,e.dependencies=t===null?null:{lanes:t.lanes,firstContext:t.firstContext}),e}function mi(e,t,n,r,a,o){var s=0;if(r=e,typeof e==`function`)di(e)&&(s=1);else if(typeof e==`string`)s=Uf(e,n,de.current)?26:e===`html`||e===`head`||e===`body`?27:5;else a:switch(e){case re:return e=ui(31,n,t,a),e.elementType=re,e.lanes=o,e;case y:return hi(n.children,a,o,t);case b:s=8,a|=24;break;case x:return e=ui(12,n,t,a|2),e.elementType=x,e.lanes=o,e;case te:return e=ui(13,n,t,a),e.elementType=te,e.lanes=o,e;case ne:return e=ui(19,n,t,a),e.elementType=ne,e.lanes=o,e;default:if(typeof e==`object`&&e)switch(e.$$typeof){case S:s=10;break a;case ee:s=9;break a;case C:s=11;break a;case w:s=14;break a;case T:s=16,r=null;break a}s=29,n=Error(i(130,e===null?`null`:typeof e,``)),r=null}return t=ui(s,n,t,a),t.elementType=e,t.type=r,t.lanes=o,t}function hi(e,t,n,r){return e=ui(7,e,r,t),e.lanes=n,e}function L(e,t,n){return e=ui(6,e,null,t),e.lanes=n,e}function gi(e){var t=ui(18,null,null,0);return t.stateNode=e,t}function _i(e,t,n){return t=ui(4,e.children===null?[]:e.children,e.key,t),t.lanes=n,t.stateNode={containerInfo:e.containerInfo,pendingChildren:null,implementation:e.implementation},t}var vi=new WeakMap;function yi(e,t){if(typeof e==`object`&&e){var n=vi.get(e);return n===void 0?(t={value:e,source:t,stack:we(t)},vi.set(e,t),t):n}return{value:e,source:t,stack:we(t)}}var bi=[],xi=0,Si=null,Ci=0,wi=[],Ti=0,Ei=null,Di=1,Oi=``;function ki(e,t){bi[xi++]=Ci,bi[xi++]=Si,Si=e,Ci=t}function Ai(e,t,n){wi[Ti++]=Di,wi[Ti++]=Oi,wi[Ti++]=Ei,Ei=e;var r=Di;e=Oi;var i=32-Ve(r)-1;r&=~(1<<i),n+=1;var a=32-Ve(t)+i;if(30<a){var o=i-i%5;a=(r&(1<<o)-1).toString(32),r>>=o,i-=o,Di=1<<32-Ve(t)+i|n<<i|r,Oi=a+e}else Di=1<<a|n<<i|r,Oi=e}function ji(e){e.return!==null&&(ki(e,1),Ai(e,1,0))}function Mi(e){for(;e===Si;)Si=bi[--xi],bi[xi]=null,Ci=bi[--xi],bi[xi]=null;for(;e===Ei;)Ei=wi[--Ti],wi[Ti]=null,Oi=wi[--Ti],wi[Ti]=null,Di=wi[--Ti],wi[Ti]=null}function Ni(e,t){wi[Ti++]=Di,wi[Ti++]=Oi,wi[Ti++]=Ei,Di=t.id,Oi=t.overflow,Ei=e}var Pi=null,R=null,z=!1,Fi=null,Ii=!1,Li=Error(i(519));function Ri(e){throw Wi(yi(Error(i(418,1<arguments.length&&arguments[1]!==void 0&&arguments[1]?`text`:`HTML`,``)),e)),Li}function zi(e){var t=e.stateNode,n=e.type,r=e.memoizedProps;switch(t[lt]=e,t[ut]=r,n){case`dialog`:Q(`cancel`,t),Q(`close`,t);break;case`iframe`:case`object`:case`embed`:Q(`load`,t);break;case`video`:case`audio`:for(n=0;n<_d.length;n++)Q(_d[n],t);break;case`source`:Q(`error`,t);break;case`img`:case`image`:case`link`:Q(`error`,t),Q(`load`,t);break;case`details`:Q(`toggle`,t);break;case`input`:Q(`invalid`,t),Ut(t,r.value,r.defaultValue,r.checked,r.defaultChecked,r.type,r.name,!0);break;case`select`:Q(`invalid`,t);break;case`textarea`:Q(`invalid`,t),qt(t,r.value,r.defaultValue,r.children)}n=r.children,typeof n!=`string`&&typeof n!=`number`&&typeof n!=`bigint`||t.textContent===``+n||!0===r.suppressHydrationWarning||Md(t.textContent,n)?(r.popover!=null&&(Q(`beforetoggle`,t),Q(`toggle`,t)),r.onScroll!=null&&Q(`scroll`,t),r.onScrollEnd!=null&&Q(`scrollend`,t),r.onClick!=null&&(t.onclick=nn),t=!0):t=!1,t||Ri(e,!0)}function Bi(e){for(Pi=e.return;Pi;)switch(Pi.tag){case 5:case 31:case 13:Ii=!1;return;case 27:case 3:Ii=!0;return;default:Pi=Pi.return}}function Vi(e){if(e!==Pi)return!1;if(!z)return Bi(e),z=!0,!1;var t=e.tag,n;if((n=t!==3&&t!==27)&&((n=t===5)&&(n=e.type,n=!(n!==`form`&&n!==`button`)||Ud(e.type,e.memoizedProps)),n=!n),n&&R&&Ri(e),Bi(e),t===13){if(e=e.memoizedState,e=e===null?null:e.dehydrated,!e)throw Error(i(317));R=uf(e)}else if(t===31){if(e=e.memoizedState,e=e===null?null:e.dehydrated,!e)throw Error(i(317));R=uf(e)}else t===27?(t=R,Zd(e.type)?(e=lf,lf=null,R=e):R=t):R=Pi?cf(e.stateNode.nextSibling):null;return!0}function Hi(){R=Pi=null,z=!1}function Ui(){var e=Fi;return e!==null&&(Zl===null?Zl=e:Zl.push.apply(Zl,e),Fi=null),e}function Wi(e){Fi===null?Fi=[e]:Fi.push(e)}var Gi=le(null),Ki=null,qi=null;function Ji(e,t,n){M(Gi,t._currentValue),t._currentValue=n}function Yi(e){e._currentValue=Gi.current,ue(Gi)}function Xi(e,t,n){for(;e!==null;){var r=e.alternate;if((e.childLanes&t)===t?r!==null&&(r.childLanes&t)!==t&&(r.childLanes|=t):(e.childLanes|=t,r!==null&&(r.childLanes|=t)),e===n)break;e=e.return}}function Zi(e,t,n,r){var a=e.child;for(a!==null&&(a.return=e);a!==null;){var o=a.dependencies;if(o!==null){var s=a.child;o=o.firstContext;a:for(;o!==null;){var c=o;o=a;for(var l=0;l<t.length;l++)if(c.context===t[l]){o.lanes|=n,c=o.alternate,c!==null&&(c.lanes|=n),Xi(o.return,n,e),r||(s=null);break a}o=c.next}}else if(a.tag===18){if(s=a.return,s===null)throw Error(i(341));s.lanes|=n,o=s.alternate,o!==null&&(o.lanes|=n),Xi(s,n,e),s=null}else s=a.child;if(s!==null)s.return=a;else for(s=a;s!==null;){if(s===e){s=null;break}if(a=s.sibling,a!==null){a.return=s.return,s=a;break}s=s.return}a=s}}function Qi(e,t,n,r){e=null;for(var a=t,o=!1;a!==null;){if(!o){if(a.flags&524288)o=!0;else if(a.flags&262144)break}if(a.tag===10){var s=a.alternate;if(s===null)throw Error(i(387));if(s=s.memoizedProps,s!==null){var c=a.type;wr(a.pendingProps.value,s.value)||(e===null?e=[c]:e.push(c))}}else if(a===me.current){if(s=a.alternate,s===null)throw Error(i(387));s.memoizedState.memoizedState!==a.memoizedState.memoizedState&&(e===null?e=[Qf]:e.push(Qf))}a=a.return}e!==null&&Zi(t,e,n,r),t.flags|=262144}function $i(e){for(e=e.firstContext;e!==null;){if(!wr(e.context._currentValue,e.memoizedValue))return!0;e=e.next}return!1}function ea(e){Ki=e,qi=null,e=e.dependencies,e!==null&&(e.firstContext=null)}function ta(e){return ra(Ki,e)}function na(e,t){return Ki===null&&ea(e),ra(e,t)}function ra(e,t){var n=t._currentValue;if(t={context:t,memoizedValue:n,next:null},qi===null){if(e===null)throw Error(i(308));qi=t,e.dependencies={lanes:0,firstContext:t},e.flags|=524288}else qi=qi.next=t;return n}var ia=typeof AbortController<`u`?AbortController:function(){var e=[],t=this.signal={aborted:!1,addEventListener:function(t,n){e.push(n)}};this.abort=function(){t.aborted=!0,e.forEach(function(e){return e()})}},aa=t.unstable_scheduleCallback,oa=t.unstable_NormalPriority,B={$$typeof:S,Consumer:null,Provider:null,_currentValue:null,_currentValue2:null,_threadCount:0};function sa(){return{controller:new ia,data:new Map,refCount:0}}function ca(e){e.refCount--,e.refCount===0&&aa(oa,function(){e.controller.abort()})}var la=null,ua=0,da=0,fa=null;function pa(e,t){if(la===null){var n=la=[];ua=0,da=dd(),fa={status:`pending`,value:void 0,then:function(e){n.push(e)}}}return ua++,t.then(ma,ma),t}function ma(){if(--ua===0&&la!==null){fa!==null&&(fa.status=`fulfilled`);var e=la;la=null,da=0,fa=null;for(var t=0;t<e.length;t++)(0,e[t])()}}function ha(e,t){var n=[],r={status:`pending`,value:null,reason:null,then:function(e){n.push(e)}};return e.then(function(){r.status=`fulfilled`,r.value=t;for(var e=0;e<n.length;e++)(0,n[e])(t)},function(e){for(r.status=`rejected`,r.reason=e,e=0;e<n.length;e++)(0,n[e])(void 0)}),r}var ga=A.S;A.S=function(e,t){eu=ke(),typeof t==`object`&&t&&typeof t.then==`function`&&pa(e,t),ga!==null&&ga(e,t)};var _a=le(null);function va(){var e=_a.current;return e===null?K.pooledCache:e}function ya(e,t){t===null?M(_a,_a.current):M(_a,t.pool)}function ba(){var e=va();return e===null?null:{parent:B._currentValue,pool:e}}var xa=Error(i(460)),Sa=Error(i(474)),Ca=Error(i(542)),wa={then:function(){}};function Ta(e){return e=e.status,e===`fulfilled`||e===`rejected`}function Ea(e,t,n){switch(n=e[n],n===void 0?e.push(t):n!==t&&(t.then(nn,nn),t=n),t.status){case`fulfilled`:return t.value;case`rejected`:throw e=t.reason,Aa(e),e;default:if(typeof t.status==`string`)t.then(nn,nn);else{if(e=K,e!==null&&100<e.shellSuspendCounter)throw Error(i(482));e=t,e.status=`pending`,e.then(function(e){if(t.status===`pending`){var n=t;n.status=`fulfilled`,n.value=e}},function(e){if(t.status===`pending`){var n=t;n.status=`rejected`,n.reason=e}})}switch(t.status){case`fulfilled`:return t.value;case`rejected`:throw e=t.reason,Aa(e),e}throw Oa=t,xa}}function Da(e){try{var t=e._init;return t(e._payload)}catch(e){throw typeof e==`object`&&e&&typeof e.then==`function`?(Oa=e,xa):e}}var Oa=null;function ka(){if(Oa===null)throw Error(i(459));var e=Oa;return Oa=null,e}function Aa(e){if(e===xa||e===Ca)throw Error(i(483))}var ja=null,Ma=0;function Na(e){var t=Ma;return Ma+=1,ja===null&&(ja=[]),Ea(ja,e,t)}function Pa(e,t){t=t.props.ref,e.ref=t===void 0?null:t}function Fa(e,t){throw t.$$typeof===g?Error(i(525)):(e=Object.prototype.toString.call(t),Error(i(31,e===`[object Object]`?`object with keys {`+Object.keys(t).join(`, `)+`}`:e)))}function Ia(e){function t(t,n){if(e){var r=t.deletions;r===null?(t.deletions=[n],t.flags|=16):r.push(n)}}function n(n,r){if(!e)return null;for(;r!==null;)t(n,r),r=r.sibling;return null}function r(e){for(var t=new Map;e!==null;)e.key===null?t.set(e.index,e):t.set(e.key,e),e=e.sibling;return t}function a(e,t){return e=fi(e,t),e.index=0,e.sibling=null,e}function o(t,n,r){return t.index=r,e?(r=t.alternate,r===null?(t.flags|=67108866,n):(r=r.index,r<n?(t.flags|=67108866,n):r)):(t.flags|=1048576,n)}function s(t){return e&&t.alternate===null&&(t.flags|=67108866),t}function c(e,t,n,r){return t===null||t.tag!==6?(t=L(n,e.mode,r),t.return=e,t):(t=a(t,n),t.return=e,t)}function l(e,t,n,r){var i=n.type;return i===y?d(e,t,n.props.children,r,n.key):t!==null&&(t.elementType===i||typeof i==`object`&&i&&i.$$typeof===T&&Da(i)===t.type)?(t=a(t,n.props),Pa(t,n),t.return=e,t):(t=mi(n.type,n.key,n.props,null,e.mode,r),Pa(t,n),t.return=e,t)}function u(e,t,n,r){return t===null||t.tag!==4||t.stateNode.containerInfo!==n.containerInfo||t.stateNode.implementation!==n.implementation?(t=_i(n,e.mode,r),t.return=e,t):(t=a(t,n.children||[]),t.return=e,t)}function d(e,t,n,r,i){return t===null||t.tag!==7?(t=hi(n,e.mode,r,i),t.return=e,t):(t=a(t,n),t.return=e,t)}function f(e,t,n){if(typeof t==`string`&&t!==``||typeof t==`number`||typeof t==`bigint`)return t=L(``+t,e.mode,n),t.return=e,t;if(typeof t==`object`&&t){switch(t.$$typeof){case _:return n=mi(t.type,t.key,t.props,null,e.mode,n),Pa(n,t),n.return=e,n;case v:return t=_i(t,e.mode,n),t.return=e,t;case T:return t=Da(t),f(e,t,n)}if(ae(t)||D(t))return t=hi(t,e.mode,n,null),t.return=e,t;if(typeof t.then==`function`)return f(e,Na(t),n);if(t.$$typeof===S)return f(e,na(e,t),n);Fa(e,t)}return null}function p(e,t,n,r){var i=t===null?null:t.key;if(typeof n==`string`&&n!==``||typeof n==`number`||typeof n==`bigint`)return i===null?c(e,t,``+n,r):null;if(typeof n==`object`&&n){switch(n.$$typeof){case _:return n.key===i?l(e,t,n,r):null;case v:return n.key===i?u(e,t,n,r):null;case T:return n=Da(n),p(e,t,n,r)}if(ae(n)||D(n))return i===null?d(e,t,n,r,null):null;if(typeof n.then==`function`)return p(e,t,Na(n),r);if(n.$$typeof===S)return p(e,t,na(e,n),r);Fa(e,n)}return null}function m(e,t,n,r,i){if(typeof r==`string`&&r!==``||typeof r==`number`||typeof r==`bigint`)return e=e.get(n)||null,c(t,e,``+r,i);if(typeof r==`object`&&r){switch(r.$$typeof){case _:return e=e.get(r.key===null?n:r.key)||null,l(t,e,r,i);case v:return e=e.get(r.key===null?n:r.key)||null,u(t,e,r,i);case T:return r=Da(r),m(e,t,n,r,i)}if(ae(r)||D(r))return e=e.get(n)||null,d(t,e,r,i,null);if(typeof r.then==`function`)return m(e,t,n,Na(r),i);if(r.$$typeof===S)return m(e,t,n,na(t,r),i);Fa(t,r)}return null}function h(i,a,s,c){for(var l=null,u=null,d=a,h=a=0,g=null;d!==null&&h<s.length;h++){d.index>h?(g=d,d=null):g=d.sibling;var _=p(i,d,s[h],c);if(_===null){d===null&&(d=g);break}e&&d&&_.alternate===null&&t(i,d),a=o(_,a,h),u===null?l=_:u.sibling=_,u=_,d=g}if(h===s.length)return n(i,d),z&&ki(i,h),l;if(d===null){for(;h<s.length;h++)d=f(i,s[h],c),d!==null&&(a=o(d,a,h),u===null?l=d:u.sibling=d,u=d);return z&&ki(i,h),l}for(d=r(d);h<s.length;h++)g=m(d,i,h,s[h],c),g!==null&&(e&&g.alternate!==null&&d.delete(g.key===null?h:g.key),a=o(g,a,h),u===null?l=g:u.sibling=g,u=g);return e&&d.forEach(function(e){return t(i,e)}),z&&ki(i,h),l}function g(a,s,c,l){if(c==null)throw Error(i(151));for(var u=null,d=null,h=s,g=s=0,_=null,v=c.next();h!==null&&!v.done;g++,v=c.next()){h.index>g?(_=h,h=null):_=h.sibling;var y=p(a,h,v.value,l);if(y===null){h===null&&(h=_);break}e&&h&&y.alternate===null&&t(a,h),s=o(y,s,g),d===null?u=y:d.sibling=y,d=y,h=_}if(v.done)return n(a,h),z&&ki(a,g),u;if(h===null){for(;!v.done;g++,v=c.next())v=f(a,v.value,l),v!==null&&(s=o(v,s,g),d===null?u=v:d.sibling=v,d=v);return z&&ki(a,g),u}for(h=r(h);!v.done;g++,v=c.next())v=m(h,a,g,v.value,l),v!==null&&(e&&v.alternate!==null&&h.delete(v.key===null?g:v.key),s=o(v,s,g),d===null?u=v:d.sibling=v,d=v);return e&&h.forEach(function(e){return t(a,e)}),z&&ki(a,g),u}function b(e,r,o,c){if(typeof o==`object`&&o&&o.type===y&&o.key===null&&(o=o.props.children),typeof o==`object`&&o){switch(o.$$typeof){case _:a:{for(var l=o.key;r!==null;){if(r.key===l){if(l=o.type,l===y){if(r.tag===7){n(e,r.sibling),c=a(r,o.props.children),c.return=e,e=c;break a}}else if(r.elementType===l||typeof l==`object`&&l&&l.$$typeof===T&&Da(l)===r.type){n(e,r.sibling),c=a(r,o.props),Pa(c,o),c.return=e,e=c;break a}n(e,r);break}else t(e,r);r=r.sibling}o.type===y?(c=hi(o.props.children,e.mode,c,o.key),c.return=e,e=c):(c=mi(o.type,o.key,o.props,null,e.mode,c),Pa(c,o),c.return=e,e=c)}return s(e);case v:a:{for(l=o.key;r!==null;){if(r.key===l)if(r.tag===4&&r.stateNode.containerInfo===o.containerInfo&&r.stateNode.implementation===o.implementation){n(e,r.sibling),c=a(r,o.children||[]),c.return=e,e=c;break a}else{n(e,r);break}else t(e,r);r=r.sibling}c=_i(o,e.mode,c),c.return=e,e=c}return s(e);case T:return o=Da(o),b(e,r,o,c)}if(ae(o))return h(e,r,o,c);if(D(o)){if(l=D(o),typeof l!=`function`)throw Error(i(150));return o=l.call(o),g(e,r,o,c)}if(typeof o.then==`function`)return b(e,r,Na(o),c);if(o.$$typeof===S)return b(e,r,na(e,o),c);Fa(e,o)}return typeof o==`string`&&o!==``||typeof o==`number`||typeof o==`bigint`?(o=``+o,r!==null&&r.tag===6?(n(e,r.sibling),c=a(r,o),c.return=e,e=c):(n(e,r),c=L(o,e.mode,c),c.return=e,e=c),s(e)):n(e,r)}return function(e,t,n,r){try{Ma=0;var i=b(e,t,n,r);return ja=null,i}catch(t){if(t===xa||t===Ca)throw t;var a=ui(29,t,null,e.mode);return a.lanes=r,a.return=e,a}}}var La=Ia(!0),Ra=Ia(!1),za=!1;function Ba(e){e.updateQueue={baseState:e.memoizedState,firstBaseUpdate:null,lastBaseUpdate:null,shared:{pending:null,lanes:0,hiddenCallbacks:null},callbacks:null}}function Va(e,t){e=e.updateQueue,t.updateQueue===e&&(t.updateQueue={baseState:e.baseState,firstBaseUpdate:e.firstBaseUpdate,lastBaseUpdate:e.lastBaseUpdate,shared:e.shared,callbacks:null})}function Ha(e){return{lane:e,tag:0,payload:null,callback:null,next:null}}function Ua(e,t,n){var r=e.updateQueue;if(r===null)return null;if(r=r.shared,G&2){var i=r.pending;return i===null?t.next=t:(t.next=i.next,i.next=t),r.pending=t,t=si(e),oi(e,null,n),t}return ri(e,r,t,n),si(e)}function Wa(e,t,n){if(t=t.updateQueue,t!==null&&(t=t.shared,n&4194048)){var r=t.lanes;r&=e.pendingLanes,n|=r,t.lanes=n,F(e,n)}}function Ga(e,t){var n=e.updateQueue,r=e.alternate;if(r!==null&&(r=r.updateQueue,n===r)){var i=null,a=null;if(n=n.firstBaseUpdate,n!==null){do{var o={lane:n.lane,tag:n.tag,payload:n.payload,callback:null,next:null};a===null?i=a=o:a=a.next=o,n=n.next}while(n!==null);a===null?i=a=t:a=a.next=t}else i=a=t;n={baseState:r.baseState,firstBaseUpdate:i,lastBaseUpdate:a,shared:r.shared,callbacks:r.callbacks},e.updateQueue=n;return}e=n.lastBaseUpdate,e===null?n.firstBaseUpdate=t:e.next=t,n.lastBaseUpdate=t}var Ka=!1;function qa(){if(Ka){var e=fa;if(e!==null)throw e}}function Ja(e,t,n,r){Ka=!1;var i=e.updateQueue;za=!1;var a=i.firstBaseUpdate,o=i.lastBaseUpdate,s=i.shared.pending;if(s!==null){i.shared.pending=null;var c=s,l=c.next;c.next=null,o===null?a=l:o.next=l,o=c;var u=e.alternate;u!==null&&(u=u.updateQueue,s=u.lastBaseUpdate,s!==o&&(s===null?u.firstBaseUpdate=l:s.next=l,u.lastBaseUpdate=c))}if(a!==null){var d=i.baseState;o=0,u=l=c=null,s=a;do{var f=s.lane&-536870913,p=f!==s.lane;if(p?(J&f)===f:(r&f)===f){f!==0&&f===da&&(Ka=!0),u!==null&&(u=u.next={lane:0,tag:s.tag,payload:s.payload,callback:null,next:null});a:{var m=e,g=s;f=t;var _=n;switch(g.tag){case 1:if(m=g.payload,typeof m==`function`){d=m.call(_,d,f);break a}d=m;break a;case 3:m.flags=m.flags&-65537|128;case 0:if(m=g.payload,f=typeof m==`function`?m.call(_,d,f):m,f==null)break a;d=h({},d,f);break a;case 2:za=!0}}f=s.callback,f!==null&&(e.flags|=64,p&&(e.flags|=8192),p=i.callbacks,p===null?i.callbacks=[f]:p.push(f))}else p={lane:f,tag:s.tag,payload:s.payload,callback:s.callback,next:null},u===null?(l=u=p,c=d):u=u.next=p,o|=f;if(s=s.next,s===null){if(s=i.shared.pending,s===null)break;p=s,s=p.next,p.next=null,i.lastBaseUpdate=p,i.shared.pending=null}}while(1);u===null&&(c=d),i.baseState=c,i.firstBaseUpdate=l,i.lastBaseUpdate=u,a===null&&(i.shared.lanes=0),Gl|=o,e.lanes=o,e.memoizedState=d}}function Ya(e,t){if(typeof e!=`function`)throw Error(i(191,e));e.call(t)}function Xa(e,t){var n=e.callbacks;if(n!==null)for(e.callbacks=null,e=0;e<n.length;e++)Ya(n[e],t)}var Za=le(null),Qa=le(0);function $a(e,t){e=Wl,M(Qa,e),M(Za,t),Wl=e|t.baseLanes}function eo(){M(Qa,Wl),M(Za,Za.current)}function to(){Wl=Qa.current,ue(Za),ue(Qa)}var no=le(null),ro=null;function io(e){var t=e.alternate;M(lo,lo.current&1),M(no,e),ro===null&&(t===null||Za.current!==null||t.memoizedState!==null)&&(ro=e)}function ao(e){M(lo,lo.current),M(no,e),ro===null&&(ro=e)}function oo(e){e.tag===22?(M(lo,lo.current),M(no,e),ro===null&&(ro=e)):so(e)}function so(){M(lo,lo.current),M(no,no.current)}function co(e){ue(no),ro===e&&(ro=null),ue(lo)}var lo=le(0);function uo(e){for(var t=e;t!==null;){if(t.tag===13){var n=t.memoizedState;if(n!==null&&(n=n.dehydrated,n===null||af(n)||of(n)))return t}else if(t.tag===19&&(t.memoizedProps.revealOrder===`forwards`||t.memoizedProps.revealOrder===`backwards`||t.memoizedProps.revealOrder===`unstable_legacy-backwards`||t.memoizedProps.revealOrder===`together`)){if(t.flags&128)return t}else if(t.child!==null){t.child.return=t,t=t.child;continue}if(t===e)break;for(;t.sibling===null;){if(t.return===null||t.return===e)return null;t=t.return}t.sibling.return=t.return,t=t.sibling}return null}var fo=0,V=null,H=null,po=null,mo=!1,ho=!1,go=!1,_o=0,vo=0,yo=null,bo=0;function xo(){throw Error(i(321))}function So(e,t){if(t===null)return!1;for(var n=0;n<t.length&&n<e.length;n++)if(!wr(e[n],t[n]))return!1;return!0}function Co(e,t,n,r,i,a){return fo=a,V=t,t.memoizedState=null,t.updateQueue=null,t.lanes=0,A.H=e===null||e.memoizedState===null?Bs:Vs,go=!1,a=n(r,i),go=!1,ho&&(a=To(t,n,r,i)),wo(e),a}function wo(e){A.H=zs;var t=H!==null&&H.next!==null;if(fo=0,po=H=V=null,mo=!1,vo=0,yo=null,t)throw Error(i(300));e===null||ic||(e=e.dependencies,e!==null&&$i(e)&&(ic=!0))}function To(e,t,n,r){V=e;var a=0;do{if(ho&&(yo=null),vo=0,ho=!1,25<=a)throw Error(i(301));if(a+=1,po=H=null,e.updateQueue!=null){var o=e.updateQueue;o.lastEffect=null,o.events=null,o.stores=null,o.memoCache!=null&&(o.memoCache.index=0)}A.H=Hs,o=t(n,r)}while(ho);return o}function Eo(){var e=A.H,t=e.useState()[0];return t=typeof t.then==`function`?No(t):t,e=e.useState()[0],(H===null?null:H.memoizedState)!==e&&(V.flags|=1024),t}function Do(){var e=_o!==0;return _o=0,e}function Oo(e,t,n){t.updateQueue=e.updateQueue,t.flags&=-2053,e.lanes&=~n}function ko(e){if(mo){for(e=e.memoizedState;e!==null;){var t=e.queue;t!==null&&(t.pending=null),e=e.next}mo=!1}fo=0,po=H=V=null,ho=!1,vo=_o=0,yo=null}function Ao(){var e={memoizedState:null,baseState:null,baseQueue:null,queue:null,next:null};return po===null?V.memoizedState=po=e:po=po.next=e,po}function jo(){if(H===null){var e=V.alternate;e=e===null?null:e.memoizedState}else e=H.next;var t=po===null?V.memoizedState:po.next;if(t!==null)po=t,H=e;else{if(e===null)throw V.alternate===null?Error(i(467)):Error(i(310));H=e,e={memoizedState:H.memoizedState,baseState:H.baseState,baseQueue:H.baseQueue,queue:H.queue,next:null},po===null?V.memoizedState=po=e:po=po.next=e}return po}function Mo(){return{lastEffect:null,events:null,stores:null,memoCache:null}}function No(e){var t=vo;return vo+=1,yo===null&&(yo=[]),e=Ea(yo,e,t),t=V,(po===null?t.memoizedState:po.next)===null&&(t=t.alternate,A.H=t===null||t.memoizedState===null?Bs:Vs),e}function Po(e){if(typeof e==`object`&&e){if(typeof e.then==`function`)return No(e);if(e.$$typeof===S)return ta(e)}throw Error(i(438,String(e)))}function Fo(e){var t=null,n=V.updateQueue;if(n!==null&&(t=n.memoCache),t==null){var r=V.alternate;r!==null&&(r=r.updateQueue,r!==null&&(r=r.memoCache,r!=null&&(t={data:r.data.map(function(e){return e.slice()}),index:0})))}if(t??={data:[],index:0},n===null&&(n=Mo(),V.updateQueue=n),n.memoCache=t,n=t.data[t.index],n===void 0)for(n=t.data[t.index]=Array(e),r=0;r<e;r++)n[r]=E;return t.index++,n}function Io(e,t){return typeof t==`function`?t(e):t}function Lo(e){return Ro(jo(),H,e)}function Ro(e,t,n){var r=e.queue;if(r===null)throw Error(i(311));r.lastRenderedReducer=n;var a=e.baseQueue,o=r.pending;if(o!==null){if(a!==null){var s=a.next;a.next=o.next,o.next=s}t.baseQueue=a=o,r.pending=null}if(o=e.baseState,a===null)e.memoizedState=o;else{t=a.next;var c=s=null,l=null,u=t,d=!1;do{var f=u.lane&-536870913;if(f===u.lane?(fo&f)===f:(J&f)===f){var p=u.revertLane;if(p===0)l!==null&&(l=l.next={lane:0,revertLane:0,gesture:null,action:u.action,hasEagerState:u.hasEagerState,eagerState:u.eagerState,next:null}),f===da&&(d=!0);else if((fo&p)===p){u=u.next,p===da&&(d=!0);continue}else f={lane:0,revertLane:u.revertLane,gesture:null,action:u.action,hasEagerState:u.hasEagerState,eagerState:u.eagerState,next:null},l===null?(c=l=f,s=o):l=l.next=f,V.lanes|=p,Gl|=p;f=u.action,go&&n(o,f),o=u.hasEagerState?u.eagerState:n(o,f)}else p={lane:f,revertLane:u.revertLane,gesture:u.gesture,action:u.action,hasEagerState:u.hasEagerState,eagerState:u.eagerState,next:null},l===null?(c=l=p,s=o):l=l.next=p,V.lanes|=f,Gl|=f;u=u.next}while(u!==null&&u!==t);if(l===null?s=o:l.next=c,!wr(o,e.memoizedState)&&(ic=!0,d&&(n=fa,n!==null)))throw n;e.memoizedState=o,e.baseState=s,e.baseQueue=l,r.lastRenderedState=o}return a===null&&(r.lanes=0),[e.memoizedState,r.dispatch]}function zo(e){var t=jo(),n=t.queue;if(n===null)throw Error(i(311));n.lastRenderedReducer=e;var r=n.dispatch,a=n.pending,o=t.memoizedState;if(a!==null){n.pending=null;var s=a=a.next;do o=e(o,s.action),s=s.next;while(s!==a);wr(o,t.memoizedState)||(ic=!0),t.memoizedState=o,t.baseQueue===null&&(t.baseState=o),n.lastRenderedState=o}return[o,r]}function Bo(e,t,n){var r=V,a=jo(),o=z;if(o){if(n===void 0)throw Error(i(407));n=n()}else n=t();var s=!wr((H||a).memoizedState,n);if(s&&(a.memoizedState=n,ic=!0),a=a.queue,ds(Uo.bind(null,r,a,e),[e]),a.getSnapshot!==t||s||po!==null&&po.memoizedState.tag&1){if(r.flags|=2048,os(9,{destroy:void 0},Ho.bind(null,r,a,n,t),null),K===null)throw Error(i(349));o||fo&127||Vo(r,t,n)}return n}function Vo(e,t,n){e.flags|=16384,e={getSnapshot:t,value:n},t=V.updateQueue,t===null?(t=Mo(),V.updateQueue=t,t.stores=[e]):(n=t.stores,n===null?t.stores=[e]:n.push(e))}function Ho(e,t,n,r){t.value=n,t.getSnapshot=r,Wo(t)&&Go(e)}function Uo(e,t,n){return n(function(){Wo(t)&&Go(e)})}function Wo(e){var t=e.getSnapshot;e=e.value;try{var n=t();return!wr(e,n)}catch{return!0}}function Go(e){var t=ai(e,2);t!==null&&hu(t,e,2)}function Ko(e){var t=Ao();if(typeof e==`function`){var n=e;if(e=n(),go){Be(!0);try{n()}finally{Be(!1)}}}return t.memoizedState=t.baseState=e,t.queue={pending:null,lanes:0,dispatch:null,lastRenderedReducer:Io,lastRenderedState:e},t}function qo(e,t,n,r){return e.baseState=n,Ro(e,H,typeof r==`function`?r:Io)}function Jo(e,t,n,r,a){if(Is(e))throw Error(i(485));if(e=t.action,e!==null){var o={payload:a,action:e,next:null,isTransition:!0,status:`pending`,value:null,reason:null,listeners:[],then:function(e){o.listeners.push(e)}};A.T===null?o.isTransition=!1:n(!0),r(o),n=t.pending,n===null?(o.next=t.pending=o,Yo(t,o)):(o.next=n.next,t.pending=n.next=o)}}function Yo(e,t){var n=t.action,r=t.payload,i=e.state;if(t.isTransition){var a=A.T,o={};A.T=o;try{var s=n(i,r),c=A.S;c!==null&&c(o,s),Xo(e,t,s)}catch(n){Qo(e,t,n)}finally{a!==null&&o.types!==null&&(a.types=o.types),A.T=a}}else try{a=n(i,r),Xo(e,t,a)}catch(n){Qo(e,t,n)}}function Xo(e,t,n){typeof n==`object`&&n&&typeof n.then==`function`?n.then(function(n){Zo(e,t,n)},function(n){return Qo(e,t,n)}):Zo(e,t,n)}function Zo(e,t,n){t.status=`fulfilled`,t.value=n,$o(t),e.state=n,t=e.pending,t!==null&&(n=t.next,n===t?e.pending=null:(n=n.next,t.next=n,Yo(e,n)))}function Qo(e,t,n){var r=e.pending;if(e.pending=null,r!==null){r=r.next;do t.status=`rejected`,t.reason=n,$o(t),t=t.next;while(t!==r)}e.action=null}function $o(e){e=e.listeners;for(var t=0;t<e.length;t++)(0,e[t])()}function es(e,t){return t}function ts(e,t){if(z){var n=K.formState;if(n!==null){a:{var r=V;if(z){if(R){b:{for(var i=R,a=Ii;i.nodeType!==8;){if(!a){i=null;break b}if(i=cf(i.nextSibling),i===null){i=null;break b}}a=i.data,i=a===`F!`||a===`F`?i:null}if(i){R=cf(i.nextSibling),r=i.data===`F!`;break a}}Ri(r)}r=!1}r&&(t=n[0])}}return n=Ao(),n.memoizedState=n.baseState=t,r={pending:null,lanes:0,dispatch:null,lastRenderedReducer:es,lastRenderedState:t},n.queue=r,n=Ns.bind(null,V,r),r.dispatch=n,r=Ko(!1),a=Fs.bind(null,V,!1,r.queue),r=Ao(),i={state:t,dispatch:null,action:e,pending:null},r.queue=i,n=Jo.bind(null,V,i,a,n),i.dispatch=n,r.memoizedState=e,[t,n,!1]}function ns(e){return rs(jo(),H,e)}function rs(e,t,n){if(t=Ro(e,t,es)[0],e=Lo(Io)[0],typeof t==`object`&&t&&typeof t.then==`function`)try{var r=No(t)}catch(e){throw e===xa?Ca:e}else r=t;t=jo();var i=t.queue,a=i.dispatch;return n!==t.memoizedState&&(V.flags|=2048,os(9,{destroy:void 0},is.bind(null,i,n),null)),[r,a,e]}function is(e,t){e.action=t}function as(e){var t=jo(),n=H;if(n!==null)return rs(t,n,e);jo(),t=t.memoizedState,n=jo();var r=n.queue.dispatch;return n.memoizedState=e,[t,r,!1]}function os(e,t,n,r){return e={tag:e,create:n,deps:r,inst:t,next:null},t=V.updateQueue,t===null&&(t=Mo(),V.updateQueue=t),n=t.lastEffect,n===null?t.lastEffect=e.next=e:(r=n.next,n.next=e,e.next=r,t.lastEffect=e),e}function ss(){return jo().memoizedState}function cs(e,t,n,r){var i=Ao();V.flags|=e,i.memoizedState=os(1|t,{destroy:void 0},n,r===void 0?null:r)}function ls(e,t,n,r){var i=jo();r=r===void 0?null:r;var a=i.memoizedState.inst;H!==null&&r!==null&&So(r,H.memoizedState.deps)?i.memoizedState=os(t,a,n,r):(V.flags|=e,i.memoizedState=os(1|t,a,n,r))}function us(e,t){cs(8390656,8,e,t)}function ds(e,t){ls(2048,8,e,t)}function fs(e){V.flags|=4;var t=V.updateQueue;if(t===null)t=Mo(),V.updateQueue=t,t.events=[e];else{var n=t.events;n===null?t.events=[e]:n.push(e)}}function ps(e){var t=jo().memoizedState;return fs({ref:t,nextImpl:e}),function(){if(G&2)throw Error(i(440));return t.impl.apply(void 0,arguments)}}function ms(e,t){return ls(4,2,e,t)}function hs(e,t){return ls(4,4,e,t)}function gs(e,t){if(typeof t==`function`){e=e();var n=t(e);return function(){typeof n==`function`?n():t(null)}}if(t!=null)return e=e(),t.current=e,function(){t.current=null}}function _s(e,t,n){n=n==null?null:n.concat([e]),ls(4,4,gs.bind(null,t,e),n)}function vs(){}function ys(e,t){var n=jo();t=t===void 0?null:t;var r=n.memoizedState;return t!==null&&So(t,r[1])?r[0]:(n.memoizedState=[e,t],e)}function bs(e,t){var n=jo();t=t===void 0?null:t;var r=n.memoizedState;if(t!==null&&So(t,r[1]))return r[0];if(r=e(),go){Be(!0);try{e()}finally{Be(!1)}}return n.memoizedState=[r,t],r}function xs(e,t,n){return n===void 0||fo&1073741824&&!(J&261930)?e.memoizedState=t:(e.memoizedState=n,e=mu(),V.lanes|=e,Gl|=e,n)}function Ss(e,t,n,r){return wr(n,t)?n:Za.current===null?!(fo&42)||fo&1073741824&&!(J&261930)?(ic=!0,e.memoizedState=n):(e=mu(),V.lanes|=e,Gl|=e,t):(e=xs(e,n,r),wr(e,t)||(ic=!0),e)}function Cs(e,t,n,r,i){var a=j.p;j.p=a!==0&&8>a?a:8;var o=A.T,s={};A.T=s,Fs(e,!1,t,n);try{var c=i(),l=A.S;l!==null&&l(s,c),typeof c==`object`&&c&&typeof c.then==`function`?Ps(e,t,ha(c,r),pu(e)):Ps(e,t,r,pu(e))}catch(n){Ps(e,t,{then:function(){},status:`rejected`,reason:n},pu())}finally{j.p=a,o!==null&&s.types!==null&&(o.types=s.types),A.T=o}}function ws(){}function Ts(e,t,n,r){if(e.tag!==5)throw Error(i(476));var a=Es(e).queue;Cs(e,a,t,oe,n===null?ws:function(){return Ds(e),n(r)})}function Es(e){var t=e.memoizedState;if(t!==null)return t;t={memoizedState:oe,baseState:oe,baseQueue:null,queue:{pending:null,lanes:0,dispatch:null,lastRenderedReducer:Io,lastRenderedState:oe},next:null};var n={};return t.next={memoizedState:n,baseState:n,baseQueue:null,queue:{pending:null,lanes:0,dispatch:null,lastRenderedReducer:Io,lastRenderedState:n},next:null},e.memoizedState=t,e=e.alternate,e!==null&&(e.memoizedState=t),t}function Ds(e){var t=Es(e);t.next===null&&(t=e.alternate.memoizedState),Ps(e,t.next.queue,{},pu())}function Os(){return ta(Qf)}function ks(){return jo().memoizedState}function As(){return jo().memoizedState}function js(e){for(var t=e.return;t!==null;){switch(t.tag){case 24:case 3:var n=pu();e=Ha(n);var r=Ua(t,e,n);r!==null&&(hu(r,t,n),Wa(r,t,n)),t={cache:sa()},e.payload=t;return}t=t.return}}function Ms(e,t,n){var r=pu();n={lane:r,revertLane:0,gesture:null,action:n,hasEagerState:!1,eagerState:null,next:null},Is(e)?Ls(t,n):(n=ii(e,t,n,r),n!==null&&(hu(n,e,r),Rs(n,t,r)))}function Ns(e,t,n){Ps(e,t,n,pu())}function Ps(e,t,n,r){var i={lane:r,revertLane:0,gesture:null,action:n,hasEagerState:!1,eagerState:null,next:null};if(Is(e))Ls(t,i);else{var a=e.alternate;if(e.lanes===0&&(a===null||a.lanes===0)&&(a=t.lastRenderedReducer,a!==null))try{var o=t.lastRenderedState,s=a(o,n);if(i.hasEagerState=!0,i.eagerState=s,wr(s,o))return ri(e,t,i,0),K===null&&ni(),!1}catch{}if(n=ii(e,t,i,r),n!==null)return hu(n,e,r),Rs(n,t,r),!0}return!1}function Fs(e,t,n,r){if(r={lane:2,revertLane:dd(),gesture:null,action:r,hasEagerState:!1,eagerState:null,next:null},Is(e)){if(t)throw Error(i(479))}else t=ii(e,n,r,2),t!==null&&hu(t,e,2)}function Is(e){var t=e.alternate;return e===V||t!==null&&t===V}function Ls(e,t){ho=mo=!0;var n=e.pending;n===null?t.next=t:(t.next=n.next,n.next=t),e.pending=t}function Rs(e,t,n){if(n&4194048){var r=t.lanes;r&=e.pendingLanes,n|=r,t.lanes=n,F(e,n)}}var zs={readContext:ta,use:Po,useCallback:xo,useContext:xo,useEffect:xo,useImperativeHandle:xo,useLayoutEffect:xo,useInsertionEffect:xo,useMemo:xo,useReducer:xo,useRef:xo,useState:xo,useDebugValue:xo,useDeferredValue:xo,useTransition:xo,useSyncExternalStore:xo,useId:xo,useHostTransitionStatus:xo,useFormState:xo,useActionState:xo,useOptimistic:xo,useMemoCache:xo,useCacheRefresh:xo};zs.useEffectEvent=xo;var Bs={readContext:ta,use:Po,useCallback:function(e,t){return Ao().memoizedState=[e,t===void 0?null:t],e},useContext:ta,useEffect:us,useImperativeHandle:function(e,t,n){n=n==null?null:n.concat([e]),cs(4194308,4,gs.bind(null,t,e),n)},useLayoutEffect:function(e,t){return cs(4194308,4,e,t)},useInsertionEffect:function(e,t){cs(4,2,e,t)},useMemo:function(e,t){var n=Ao();t=t===void 0?null:t;var r=e();if(go){Be(!0);try{e()}finally{Be(!1)}}return n.memoizedState=[r,t],r},useReducer:function(e,t,n){var r=Ao();if(n!==void 0){var i=n(t);if(go){Be(!0);try{n(t)}finally{Be(!1)}}}else i=t;return r.memoizedState=r.baseState=i,e={pending:null,lanes:0,dispatch:null,lastRenderedReducer:e,lastRenderedState:i},r.queue=e,e=e.dispatch=Ms.bind(null,V,e),[r.memoizedState,e]},useRef:function(e){var t=Ao();return e={current:e},t.memoizedState=e},useState:function(e){e=Ko(e);var t=e.queue,n=Ns.bind(null,V,t);return t.dispatch=n,[e.memoizedState,n]},useDebugValue:vs,useDeferredValue:function(e,t){return xs(Ao(),e,t)},useTransition:function(){var e=Ko(!1);return e=Cs.bind(null,V,e.queue,!0,!1),Ao().memoizedState=e,[!1,e]},useSyncExternalStore:function(e,t,n){var r=V,a=Ao();if(z){if(n===void 0)throw Error(i(407));n=n()}else{if(n=t(),K===null)throw Error(i(349));J&127||Vo(r,t,n)}a.memoizedState=n;var o={value:n,getSnapshot:t};return a.queue=o,us(Uo.bind(null,r,o,e),[e]),r.flags|=2048,os(9,{destroy:void 0},Ho.bind(null,r,o,n,t),null),n},useId:function(){var e=Ao(),t=K.identifierPrefix;if(z){var n=Oi,r=Di;n=(r&~(1<<32-Ve(r)-1)).toString(32)+n,t=`_`+t+`R_`+n,n=_o++,0<n&&(t+=`H`+n.toString(32)),t+=`_`}else n=bo++,t=`_`+t+`r_`+n.toString(32)+`_`;return e.memoizedState=t},useHostTransitionStatus:Os,useFormState:ts,useActionState:ts,useOptimistic:function(e){var t=Ao();t.memoizedState=t.baseState=e;var n={pending:null,lanes:0,dispatch:null,lastRenderedReducer:null,lastRenderedState:null};return t.queue=n,t=Fs.bind(null,V,!0,n),n.dispatch=t,[e,t]},useMemoCache:Fo,useCacheRefresh:function(){return Ao().memoizedState=js.bind(null,V)},useEffectEvent:function(e){var t=Ao(),n={impl:e};return t.memoizedState=n,function(){if(G&2)throw Error(i(440));return n.impl.apply(void 0,arguments)}}},Vs={readContext:ta,use:Po,useCallback:ys,useContext:ta,useEffect:ds,useImperativeHandle:_s,useInsertionEffect:ms,useLayoutEffect:hs,useMemo:bs,useReducer:Lo,useRef:ss,useState:function(){return Lo(Io)},useDebugValue:vs,useDeferredValue:function(e,t){return Ss(jo(),H.memoizedState,e,t)},useTransition:function(){var e=Lo(Io)[0],t=jo().memoizedState;return[typeof e==`boolean`?e:No(e),t]},useSyncExternalStore:Bo,useId:ks,useHostTransitionStatus:Os,useFormState:ns,useActionState:ns,useOptimistic:function(e,t){return qo(jo(),H,e,t)},useMemoCache:Fo,useCacheRefresh:As};Vs.useEffectEvent=ps;var Hs={readContext:ta,use:Po,useCallback:ys,useContext:ta,useEffect:ds,useImperativeHandle:_s,useInsertionEffect:ms,useLayoutEffect:hs,useMemo:bs,useReducer:zo,useRef:ss,useState:function(){return zo(Io)},useDebugValue:vs,useDeferredValue:function(e,t){var n=jo();return H===null?xs(n,e,t):Ss(n,H.memoizedState,e,t)},useTransition:function(){var e=zo(Io)[0],t=jo().memoizedState;return[typeof e==`boolean`?e:No(e),t]},useSyncExternalStore:Bo,useId:ks,useHostTransitionStatus:Os,useFormState:as,useActionState:as,useOptimistic:function(e,t){var n=jo();return H===null?(n.baseState=e,[e,n.queue.dispatch]):qo(n,H,e,t)},useMemoCache:Fo,useCacheRefresh:As};Hs.useEffectEvent=ps;function Us(e,t,n,r){t=e.memoizedState,n=n(r,t),n=n==null?t:h({},t,n),e.memoizedState=n,e.lanes===0&&(e.updateQueue.baseState=n)}var Ws={enqueueSetState:function(e,t,n){e=e._reactInternals;var r=pu(),i=Ha(r);i.payload=t,n!=null&&(i.callback=n),t=Ua(e,i,r),t!==null&&(hu(t,e,r),Wa(t,e,r))},enqueueReplaceState:function(e,t,n){e=e._reactInternals;var r=pu(),i=Ha(r);i.tag=1,i.payload=t,n!=null&&(i.callback=n),t=Ua(e,i,r),t!==null&&(hu(t,e,r),Wa(t,e,r))},enqueueForceUpdate:function(e,t){e=e._reactInternals;var n=pu(),r=Ha(n);r.tag=2,t!=null&&(r.callback=t),t=Ua(e,r,n),t!==null&&(hu(t,e,n),Wa(t,e,n))}};function Gs(e,t,n,r,i,a,o){return e=e.stateNode,typeof e.shouldComponentUpdate==`function`?e.shouldComponentUpdate(r,a,o):t.prototype&&t.prototype.isPureReactComponent?!Tr(n,r)||!Tr(i,a):!0}function Ks(e,t,n,r){e=t.state,typeof t.componentWillReceiveProps==`function`&&t.componentWillReceiveProps(n,r),typeof t.UNSAFE_componentWillReceiveProps==`function`&&t.UNSAFE_componentWillReceiveProps(n,r),t.state!==e&&Ws.enqueueReplaceState(t,t.state,null)}function qs(e,t){var n=t;if(`ref`in t)for(var r in n={},t)r!==`ref`&&(n[r]=t[r]);if(e=e.defaultProps)for(var i in n===t&&(n=h({},n)),e)n[i]===void 0&&(n[i]=e[i]);return n}function Js(e){Qr(e)}function Ys(e){console.error(e)}function Xs(e){Qr(e)}function Zs(e,t){try{var n=e.onUncaughtError;n(t.value,{componentStack:t.stack})}catch(e){setTimeout(function(){throw e})}}function Qs(e,t,n){try{var r=e.onCaughtError;r(n.value,{componentStack:n.stack,errorBoundary:t.tag===1?t.stateNode:null})}catch(e){setTimeout(function(){throw e})}}function $s(e,t,n){return n=Ha(n),n.tag=3,n.payload={element:null},n.callback=function(){Zs(e,t)},n}function ec(e){return e=Ha(e),e.tag=3,e}function tc(e,t,n,r){var i=n.type.getDerivedStateFromError;if(typeof i==`function`){var a=r.value;e.payload=function(){return i(a)},e.callback=function(){Qs(t,n,r)}}var o=n.stateNode;o!==null&&typeof o.componentDidCatch==`function`&&(e.callback=function(){Qs(t,n,r),typeof i!=`function`&&(ru===null?ru=new Set([this]):ru.add(this));var e=r.stack;this.componentDidCatch(r.value,{componentStack:e===null?``:e})})}function nc(e,t,n,r,a){if(n.flags|=32768,typeof r==`object`&&r&&typeof r.then==`function`){if(t=n.alternate,t!==null&&Qi(t,n,a,!0),n=no.current,n!==null){switch(n.tag){case 31:case 13:return ro===null?Du():n.alternate===null&&X===0&&(X=3),n.flags&=-257,n.flags|=65536,n.lanes=a,r===wa?n.flags|=16384:(t=n.updateQueue,t===null?n.updateQueue=new Set([r]):t.add(r),Gu(e,r,a)),!1;case 22:return n.flags|=65536,r===wa?n.flags|=16384:(t=n.updateQueue,t===null?(t={transitions:null,markerInstances:null,retryQueue:new Set([r])},n.updateQueue=t):(n=t.retryQueue,n===null?t.retryQueue=new Set([r]):n.add(r)),Gu(e,r,a)),!1}throw Error(i(435,n.tag))}return Gu(e,r,a),Du(),!1}if(z)return t=no.current,t===null?(r!==Li&&(t=Error(i(423),{cause:r}),Wi(yi(t,n))),e=e.current.alternate,e.flags|=65536,a&=-a,e.lanes|=a,r=yi(r,n),a=$s(e.stateNode,r,a),Ga(e,a),X!==4&&(X=2)):(!(t.flags&65536)&&(t.flags|=256),t.flags|=65536,t.lanes=a,r!==Li&&(e=Error(i(422),{cause:r}),Wi(yi(e,n)))),!1;var o=Error(i(520),{cause:r});if(o=yi(o,n),Xl===null?Xl=[o]:Xl.push(o),X!==4&&(X=2),t===null)return!0;r=yi(r,n),n=t;do{switch(n.tag){case 3:return n.flags|=65536,e=a&-a,n.lanes|=e,e=$s(n.stateNode,r,e),Ga(n,e),!1;case 1:if(t=n.type,o=n.stateNode,!(n.flags&128)&&(typeof t.getDerivedStateFromError==`function`||o!==null&&typeof o.componentDidCatch==`function`&&(ru===null||!ru.has(o))))return n.flags|=65536,a&=-a,n.lanes|=a,a=ec(a),tc(a,e,n,r),Ga(n,a),!1}n=n.return}while(n!==null);return!1}var rc=Error(i(461)),ic=!1;function ac(e,t,n,r){t.child=e===null?Ra(t,null,n,r):La(t,e.child,n,r)}function oc(e,t,n,r,i){n=n.render;var a=t.ref;if(`ref`in r){var o={};for(var s in r)s!==`ref`&&(o[s]=r[s])}else o=r;return ea(t),r=Co(e,t,n,o,a,i),s=Do(),e!==null&&!ic?(Oo(e,t,i),Ac(e,t,i)):(z&&s&&ji(t),t.flags|=1,ac(e,t,r,i),t.child)}function sc(e,t,n,r,i){if(e===null){var a=n.type;return typeof a==`function`&&!di(a)&&a.defaultProps===void 0&&n.compare===null?(t.tag=15,t.type=a,cc(e,t,a,r,i)):(e=mi(n.type,null,r,t,t.mode,i),e.ref=t.ref,e.return=t,t.child=e)}if(a=e.child,!jc(e,i)){var o=a.memoizedProps;if(n=n.compare,n=n===null?Tr:n,n(o,r)&&e.ref===t.ref)return Ac(e,t,i)}return t.flags|=1,e=fi(a,r),e.ref=t.ref,e.return=t,t.child=e}function cc(e,t,n,r,i){if(e!==null){var a=e.memoizedProps;if(Tr(a,r)&&e.ref===t.ref)if(ic=!1,t.pendingProps=r=a,jc(e,i))e.flags&131072&&(ic=!0);else return t.lanes=e.lanes,Ac(e,t,i)}return gc(e,t,n,r,i)}function lc(e,t,n,r){var i=r.children,a=e===null?null:e.memoizedState;if(e===null&&t.stateNode===null&&(t.stateNode={_visibility:1,_pendingMarkers:null,_retryCache:null,_transitions:null}),r.mode===`hidden`){if(t.flags&128){if(a=a===null?n:a.baseLanes|n,e!==null){for(r=t.child=e.child,i=0;r!==null;)i=i|r.lanes|r.childLanes,r=r.sibling;r=i&~a}else r=0,t.child=null;return dc(e,t,a,n,r)}if(n&536870912)t.memoizedState={baseLanes:0,cachePool:null},e!==null&&ya(t,a===null?null:a.cachePool),a===null?eo():$a(t,a),oo(t);else return r=t.lanes=536870912,dc(e,t,a===null?n:a.baseLanes|n,n,r)}else a===null?(e!==null&&ya(t,null),eo(),so(t)):(ya(t,a.cachePool),$a(t,a),so(t),t.memoizedState=null);return ac(e,t,i,n),t.child}function uc(e,t){return e!==null&&e.tag===22||t.stateNode!==null||(t.stateNode={_visibility:1,_pendingMarkers:null,_retryCache:null,_transitions:null}),t.sibling}function dc(e,t,n,r,i){var a=va();return a=a===null?null:{parent:B._currentValue,pool:a},t.memoizedState={baseLanes:n,cachePool:a},e!==null&&ya(t,null),eo(),oo(t),e!==null&&Qi(e,t,r,!0),t.childLanes=i,null}function fc(e,t){return t=Tc({mode:t.mode,children:t.children},e.mode),t.ref=e.ref,e.child=t,t.return=e,t}function pc(e,t,n){return La(t,e.child,null,n),e=fc(t,t.pendingProps),e.flags|=2,co(t),t.memoizedState=null,e}function mc(e,t,n){var r=t.pendingProps,a=(t.flags&128)!=0;if(t.flags&=-129,e===null){if(z){if(r.mode===`hidden`)return e=fc(t,r),t.lanes=536870912,uc(null,e);if(ao(t),(e=R)?(e=rf(e,Ii),e=e!==null&&e.data===`&`?e:null,e!==null&&(t.memoizedState={dehydrated:e,treeContext:Ei===null?null:{id:Di,overflow:Oi},retryLane:536870912,hydrationErrors:null},n=gi(e),n.return=t,t.child=n,Pi=t,R=null)):e=null,e===null)throw Ri(t);return t.lanes=536870912,null}return fc(t,r)}var o=e.memoizedState;if(o!==null){var s=o.dehydrated;if(ao(t),a)if(t.flags&256)t.flags&=-257,t=pc(e,t,n);else if(t.memoizedState!==null)t.child=e.child,t.flags|=128,t=null;else throw Error(i(558));else if(ic||Qi(e,t,n,!1),a=(n&e.childLanes)!==0,ic||a){if(r=K,r!==null&&(s=rt(r,n),s!==0&&s!==o.retryLane))throw o.retryLane=s,ai(e,s),hu(r,e,s),rc;Du(),t=pc(e,t,n)}else e=o.treeContext,R=cf(s.nextSibling),Pi=t,z=!0,Fi=null,Ii=!1,e!==null&&Ni(t,e),t=fc(t,r),t.flags|=4096;return t}return e=fi(e.child,{mode:r.mode,children:r.children}),e.ref=t.ref,t.child=e,e.return=t,e}function hc(e,t){var n=t.ref;if(n===null)e!==null&&e.ref!==null&&(t.flags|=4194816);else{if(typeof n!=`function`&&typeof n!=`object`)throw Error(i(284));(e===null||e.ref!==n)&&(t.flags|=4194816)}}function gc(e,t,n,r,i){return ea(t),n=Co(e,t,n,r,void 0,i),r=Do(),e!==null&&!ic?(Oo(e,t,i),Ac(e,t,i)):(z&&r&&ji(t),t.flags|=1,ac(e,t,n,i),t.child)}function _c(e,t,n,r,i,a){return ea(t),t.updateQueue=null,n=To(t,r,n,i),wo(e),r=Do(),e!==null&&!ic?(Oo(e,t,a),Ac(e,t,a)):(z&&r&&ji(t),t.flags|=1,ac(e,t,n,a),t.child)}function vc(e,t,n,r,i){if(ea(t),t.stateNode===null){var a=ci,o=n.contextType;typeof o==`object`&&o&&(a=ta(o)),a=new n(r,a),t.memoizedState=a.state!==null&&a.state!==void 0?a.state:null,a.updater=Ws,t.stateNode=a,a._reactInternals=t,a=t.stateNode,a.props=r,a.state=t.memoizedState,a.refs={},Ba(t),o=n.contextType,a.context=typeof o==`object`&&o?ta(o):ci,a.state=t.memoizedState,o=n.getDerivedStateFromProps,typeof o==`function`&&(Us(t,n,o,r),a.state=t.memoizedState),typeof n.getDerivedStateFromProps==`function`||typeof a.getSnapshotBeforeUpdate==`function`||typeof a.UNSAFE_componentWillMount!=`function`&&typeof a.componentWillMount!=`function`||(o=a.state,typeof a.componentWillMount==`function`&&a.componentWillMount(),typeof a.UNSAFE_componentWillMount==`function`&&a.UNSAFE_componentWillMount(),o!==a.state&&Ws.enqueueReplaceState(a,a.state,null),Ja(t,r,a,i),qa(),a.state=t.memoizedState),typeof a.componentDidMount==`function`&&(t.flags|=4194308),r=!0}else if(e===null){a=t.stateNode;var s=t.memoizedProps,c=qs(n,s);a.props=c;var l=a.context,u=n.contextType;o=ci,typeof u==`object`&&u&&(o=ta(u));var d=n.getDerivedStateFromProps;u=typeof d==`function`||typeof a.getSnapshotBeforeUpdate==`function`,s=t.pendingProps!==s,u||typeof a.UNSAFE_componentWillReceiveProps!=`function`&&typeof a.componentWillReceiveProps!=`function`||(s||l!==o)&&Ks(t,a,r,o),za=!1;var f=t.memoizedState;a.state=f,Ja(t,r,a,i),qa(),l=t.memoizedState,s||f!==l||za?(typeof d==`function`&&(Us(t,n,d,r),l=t.memoizedState),(c=za||Gs(t,n,c,r,f,l,o))?(u||typeof a.UNSAFE_componentWillMount!=`function`&&typeof a.componentWillMount!=`function`||(typeof a.componentWillMount==`function`&&a.componentWillMount(),typeof a.UNSAFE_componentWillMount==`function`&&a.UNSAFE_componentWillMount()),typeof a.componentDidMount==`function`&&(t.flags|=4194308)):(typeof a.componentDidMount==`function`&&(t.flags|=4194308),t.memoizedProps=r,t.memoizedState=l),a.props=r,a.state=l,a.context=o,r=c):(typeof a.componentDidMount==`function`&&(t.flags|=4194308),r=!1)}else{a=t.stateNode,Va(e,t),o=t.memoizedProps,u=qs(n,o),a.props=u,d=t.pendingProps,f=a.context,l=n.contextType,c=ci,typeof l==`object`&&l&&(c=ta(l)),s=n.getDerivedStateFromProps,(l=typeof s==`function`||typeof a.getSnapshotBeforeUpdate==`function`)||typeof a.UNSAFE_componentWillReceiveProps!=`function`&&typeof a.componentWillReceiveProps!=`function`||(o!==d||f!==c)&&Ks(t,a,r,c),za=!1,f=t.memoizedState,a.state=f,Ja(t,r,a,i),qa();var p=t.memoizedState;o!==d||f!==p||za||e!==null&&e.dependencies!==null&&$i(e.dependencies)?(typeof s==`function`&&(Us(t,n,s,r),p=t.memoizedState),(u=za||Gs(t,n,u,r,f,p,c)||e!==null&&e.dependencies!==null&&$i(e.dependencies))?(l||typeof a.UNSAFE_componentWillUpdate!=`function`&&typeof a.componentWillUpdate!=`function`||(typeof a.componentWillUpdate==`function`&&a.componentWillUpdate(r,p,c),typeof a.UNSAFE_componentWillUpdate==`function`&&a.UNSAFE_componentWillUpdate(r,p,c)),typeof a.componentDidUpdate==`function`&&(t.flags|=4),typeof a.getSnapshotBeforeUpdate==`function`&&(t.flags|=1024)):(typeof a.componentDidUpdate!=`function`||o===e.memoizedProps&&f===e.memoizedState||(t.flags|=4),typeof a.getSnapshotBeforeUpdate!=`function`||o===e.memoizedProps&&f===e.memoizedState||(t.flags|=1024),t.memoizedProps=r,t.memoizedState=p),a.props=r,a.state=p,a.context=c,r=u):(typeof a.componentDidUpdate!=`function`||o===e.memoizedProps&&f===e.memoizedState||(t.flags|=4),typeof a.getSnapshotBeforeUpdate!=`function`||o===e.memoizedProps&&f===e.memoizedState||(t.flags|=1024),r=!1)}return a=r,hc(e,t),r=(t.flags&128)!=0,a||r?(a=t.stateNode,n=r&&typeof n.getDerivedStateFromError!=`function`?null:a.render(),t.flags|=1,e!==null&&r?(t.child=La(t,e.child,null,i),t.child=La(t,null,n,i)):ac(e,t,n,i),t.memoizedState=a.state,e=t.child):e=Ac(e,t,i),e}function yc(e,t,n,r){return Hi(),t.flags|=256,ac(e,t,n,r),t.child}var bc={dehydrated:null,treeContext:null,retryLane:0,hydrationErrors:null};function xc(e){return{baseLanes:e,cachePool:ba()}}function Sc(e,t,n){return e=e===null?0:e.childLanes&~n,t&&(e|=Jl),e}function Cc(e,t,n){var r=t.pendingProps,a=!1,o=(t.flags&128)!=0,s;if((s=o)||(s=e!==null&&e.memoizedState===null?!1:(lo.current&2)!=0),s&&(a=!0,t.flags&=-129),s=(t.flags&32)!=0,t.flags&=-33,e===null){if(z){if(a?io(t):so(t),(e=R)?(e=rf(e,Ii),e=e!==null&&e.data!==`&`?e:null,e!==null&&(t.memoizedState={dehydrated:e,treeContext:Ei===null?null:{id:Di,overflow:Oi},retryLane:536870912,hydrationErrors:null},n=gi(e),n.return=t,t.child=n,Pi=t,R=null)):e=null,e===null)throw Ri(t);return of(e)?t.lanes=32:t.lanes=536870912,null}var c=r.children;return r=r.fallback,a?(so(t),a=t.mode,c=Tc({mode:`hidden`,children:c},a),r=hi(r,a,n,null),c.return=t,r.return=t,c.sibling=r,t.child=c,r=t.child,r.memoizedState=xc(n),r.childLanes=Sc(e,s,n),t.memoizedState=bc,uc(null,r)):(io(t),wc(t,c))}var l=e.memoizedState;if(l!==null&&(c=l.dehydrated,c!==null)){if(o)t.flags&256?(io(t),t.flags&=-257,t=Ec(e,t,n)):t.memoizedState===null?(so(t),c=r.fallback,a=t.mode,r=Tc({mode:`visible`,children:r.children},a),c=hi(c,a,n,null),c.flags|=2,r.return=t,c.return=t,r.sibling=c,t.child=r,La(t,e.child,null,n),r=t.child,r.memoizedState=xc(n),r.childLanes=Sc(e,s,n),t.memoizedState=bc,t=uc(null,r)):(so(t),t.child=e.child,t.flags|=128,t=null);else if(io(t),of(c)){if(s=c.nextSibling&&c.nextSibling.dataset,s)var u=s.dgst;s=u,r=Error(i(419)),r.stack=``,r.digest=s,Wi({value:r,source:null,stack:null}),t=Ec(e,t,n)}else if(ic||Qi(e,t,n,!1),s=(n&e.childLanes)!==0,ic||s){if(s=K,s!==null&&(r=rt(s,n),r!==0&&r!==l.retryLane))throw l.retryLane=r,ai(e,r),hu(s,e,r),rc;af(c)||Du(),t=Ec(e,t,n)}else af(c)?(t.flags|=192,t.child=e.child,t=null):(e=l.treeContext,R=cf(c.nextSibling),Pi=t,z=!0,Fi=null,Ii=!1,e!==null&&Ni(t,e),t=wc(t,r.children),t.flags|=4096);return t}return a?(so(t),c=r.fallback,a=t.mode,l=e.child,u=l.sibling,r=fi(l,{mode:`hidden`,children:r.children}),r.subtreeFlags=l.subtreeFlags&65011712,u===null?(c=hi(c,a,n,null),c.flags|=2):c=fi(u,c),c.return=t,r.return=t,r.sibling=c,t.child=r,uc(null,r),r=t.child,c=e.child.memoizedState,c===null?c=xc(n):(a=c.cachePool,a===null?a=ba():(l=B._currentValue,a=a.parent===l?a:{parent:l,pool:l}),c={baseLanes:c.baseLanes|n,cachePool:a}),r.memoizedState=c,r.childLanes=Sc(e,s,n),t.memoizedState=bc,uc(e.child,r)):(io(t),n=e.child,e=n.sibling,n=fi(n,{mode:`visible`,children:r.children}),n.return=t,n.sibling=null,e!==null&&(s=t.deletions,s===null?(t.deletions=[e],t.flags|=16):s.push(e)),t.child=n,t.memoizedState=null,n)}function wc(e,t){return t=Tc({mode:`visible`,children:t},e.mode),t.return=e,e.child=t}function Tc(e,t){return e=ui(22,e,null,t),e.lanes=0,e}function Ec(e,t,n){return La(t,e.child,null,n),e=wc(t,t.pendingProps.children),e.flags|=2,t.memoizedState=null,e}function Dc(e,t,n){e.lanes|=t;var r=e.alternate;r!==null&&(r.lanes|=t),Xi(e.return,t,n)}function Oc(e,t,n,r,i,a){var o=e.memoizedState;o===null?e.memoizedState={isBackwards:t,rendering:null,renderingStartTime:0,last:r,tail:n,tailMode:i,treeForkCount:a}:(o.isBackwards=t,o.rendering=null,o.renderingStartTime=0,o.last=r,o.tail=n,o.tailMode=i,o.treeForkCount=a)}function kc(e,t,n){var r=t.pendingProps,i=r.revealOrder,a=r.tail;r=r.children;var o=lo.current,s=(o&2)!=0;if(s?(o=o&1|2,t.flags|=128):o&=1,M(lo,o),ac(e,t,r,n),r=z?Ci:0,!s&&e!==null&&e.flags&128)a:for(e=t.child;e!==null;){if(e.tag===13)e.memoizedState!==null&&Dc(e,n,t);else if(e.tag===19)Dc(e,n,t);else if(e.child!==null){e.child.return=e,e=e.child;continue}if(e===t)break a;for(;e.sibling===null;){if(e.return===null||e.return===t)break a;e=e.return}e.sibling.return=e.return,e=e.sibling}switch(i){case`forwards`:for(n=t.child,i=null;n!==null;)e=n.alternate,e!==null&&uo(e)===null&&(i=n),n=n.sibling;n=i,n===null?(i=t.child,t.child=null):(i=n.sibling,n.sibling=null),Oc(t,!1,i,n,a,r);break;case`backwards`:case`unstable_legacy-backwards`:for(n=null,i=t.child,t.child=null;i!==null;){if(e=i.alternate,e!==null&&uo(e)===null){t.child=i;break}e=i.sibling,i.sibling=n,n=i,i=e}Oc(t,!0,n,null,a,r);break;case`together`:Oc(t,!1,null,null,void 0,r);break;default:t.memoizedState=null}return t.child}function Ac(e,t,n){if(e!==null&&(t.dependencies=e.dependencies),Gl|=t.lanes,(n&t.childLanes)===0)if(e!==null){if(Qi(e,t,n,!1),(n&t.childLanes)===0)return null}else return null;if(e!==null&&t.child!==e.child)throw Error(i(153));if(t.child!==null){for(e=t.child,n=fi(e,e.pendingProps),t.child=n,n.return=t;e.sibling!==null;)e=e.sibling,n=n.sibling=fi(e,e.pendingProps),n.return=t;n.sibling=null}return t.child}function jc(e,t){return(e.lanes&t)===0?(e=e.dependencies,!!(e!==null&&$i(e))):!0}function Mc(e,t,n){switch(t.tag){case 3:he(t,t.stateNode.containerInfo),Ji(t,B,e.memoizedState.cache),Hi();break;case 27:case 5:_e(t);break;case 4:he(t,t.stateNode.containerInfo);break;case 10:Ji(t,t.type,t.memoizedProps.value);break;case 31:if(t.memoizedState!==null)return t.flags|=128,ao(t),null;break;case 13:var r=t.memoizedState;if(r!==null)return r.dehydrated===null?(n&t.child.childLanes)===0?(io(t),e=Ac(e,t,n),e===null?null:e.sibling):Cc(e,t,n):(io(t),t.flags|=128,null);io(t);break;case 19:var i=(e.flags&128)!=0;if(r=(n&t.childLanes)!==0,r||=(Qi(e,t,n,!1),(n&t.childLanes)!==0),i){if(r)return kc(e,t,n);t.flags|=128}if(i=t.memoizedState,i!==null&&(i.rendering=null,i.tail=null,i.lastEffect=null),M(lo,lo.current),r)break;return null;case 22:return t.lanes=0,lc(e,t,n,t.pendingProps);case 24:Ji(t,B,e.memoizedState.cache)}return Ac(e,t,n)}function Nc(e,t,n){if(e!==null)if(e.memoizedProps!==t.pendingProps)ic=!0;else{if(!jc(e,n)&&!(t.flags&128))return ic=!1,Mc(e,t,n);ic=!!(e.flags&131072)}else ic=!1,z&&t.flags&1048576&&Ai(t,Ci,t.index);switch(t.lanes=0,t.tag){case 16:a:{var r=t.pendingProps;if(e=Da(t.elementType),t.type=e,typeof e==`function`)di(e)?(r=qs(e,r),t.tag=1,t=vc(null,t,e,r,n)):(t.tag=0,t=gc(null,t,e,r,n));else{if(e!=null){var a=e.$$typeof;if(a===C){t.tag=11,t=oc(null,t,e,r,n);break a}else if(a===w){t.tag=14,t=sc(null,t,e,r,n);break a}}throw t=k(e)||e,Error(i(306,t,``))}}return t;case 0:return gc(e,t,t.type,t.pendingProps,n);case 1:return r=t.type,a=qs(r,t.pendingProps),vc(e,t,r,a,n);case 3:a:{if(he(t,t.stateNode.containerInfo),e===null)throw Error(i(387));r=t.pendingProps;var o=t.memoizedState;a=o.element,Va(e,t),Ja(t,r,null,n);var s=t.memoizedState;if(r=s.cache,Ji(t,B,r),r!==o.cache&&Zi(t,[B],n,!0),qa(),r=s.element,o.isDehydrated)if(o={element:r,isDehydrated:!1,cache:s.cache},t.updateQueue.baseState=o,t.memoizedState=o,t.flags&256){t=yc(e,t,r,n);break a}else if(r!==a){a=yi(Error(i(424)),t),Wi(a),t=yc(e,t,r,n);break a}else{switch(e=t.stateNode.containerInfo,e.nodeType){case 9:e=e.body;break;default:e=e.nodeName===`HTML`?e.ownerDocument.body:e}for(R=cf(e.firstChild),Pi=t,z=!0,Fi=null,Ii=!0,n=Ra(t,null,r,n),t.child=n;n;)n.flags=n.flags&-3|4096,n=n.sibling}else{if(Hi(),r===a){t=Ac(e,t,n);break a}ac(e,t,r,n)}t=t.child}return t;case 26:return hc(e,t),e===null?(n=kf(t.type,null,t.pendingProps,null))?t.memoizedState=n:z||(n=t.type,e=t.pendingProps,r=Bd(pe.current).createElement(n),r[lt]=t,r[ut]=e,Pd(r,n,e),St(r),t.stateNode=r):t.memoizedState=kf(t.type,e.memoizedProps,t.pendingProps,e.memoizedState),null;case 27:return _e(t),e===null&&z&&(r=t.stateNode=ff(t.type,t.pendingProps,pe.current),Pi=t,Ii=!0,a=R,Zd(t.type)?(lf=a,R=cf(r.firstChild)):R=a),ac(e,t,t.pendingProps.children,n),hc(e,t),e===null&&(t.flags|=4194304),t.child;case 5:return e===null&&z&&((a=r=R)&&(r=tf(r,t.type,t.pendingProps,Ii),r===null?a=!1:(t.stateNode=r,Pi=t,R=cf(r.firstChild),Ii=!1,a=!0)),a||Ri(t)),_e(t),a=t.type,o=t.pendingProps,s=e===null?null:e.memoizedProps,r=o.children,Ud(a,o)?r=null:s!==null&&Ud(a,s)&&(t.flags|=32),t.memoizedState!==null&&(a=Co(e,t,Eo,null,null,n),Qf._currentValue=a),hc(e,t),ac(e,t,r,n),t.child;case 6:return e===null&&z&&((e=n=R)&&(n=nf(n,t.pendingProps,Ii),n===null?e=!1:(t.stateNode=n,Pi=t,R=null,e=!0)),e||Ri(t)),null;case 13:return Cc(e,t,n);case 4:return he(t,t.stateNode.containerInfo),r=t.pendingProps,e===null?t.child=La(t,null,r,n):ac(e,t,r,n),t.child;case 11:return oc(e,t,t.type,t.pendingProps,n);case 7:return ac(e,t,t.pendingProps,n),t.child;case 8:return ac(e,t,t.pendingProps.children,n),t.child;case 12:return ac(e,t,t.pendingProps.children,n),t.child;case 10:return r=t.pendingProps,Ji(t,t.type,r.value),ac(e,t,r.children,n),t.child;case 9:return a=t.type._context,r=t.pendingProps.children,ea(t),a=ta(a),r=r(a),t.flags|=1,ac(e,t,r,n),t.child;case 14:return sc(e,t,t.type,t.pendingProps,n);case 15:return cc(e,t,t.type,t.pendingProps,n);case 19:return kc(e,t,n);case 31:return mc(e,t,n);case 22:return lc(e,t,n,t.pendingProps);case 24:return ea(t),r=ta(B),e===null?(a=va(),a===null&&(a=K,o=sa(),a.pooledCache=o,o.refCount++,o!==null&&(a.pooledCacheLanes|=n),a=o),t.memoizedState={parent:r,cache:a},Ba(t),Ji(t,B,a)):((e.lanes&n)!==0&&(Va(e,t),Ja(t,null,null,n),qa()),a=e.memoizedState,o=t.memoizedState,a.parent===r?(r=o.cache,Ji(t,B,r),r!==a.cache&&Zi(t,[B],n,!0)):(a={parent:r,cache:r},t.memoizedState=a,t.lanes===0&&(t.memoizedState=t.updateQueue.baseState=a),Ji(t,B,r))),ac(e,t,t.pendingProps.children,n),t.child;case 29:throw t.pendingProps}throw Error(i(156,t.tag))}function Pc(e){e.flags|=4}function Fc(e,t,n,r,i){if((t=(e.mode&32)!=0)&&(t=!1),t){if(e.flags|=16777216,(i&335544128)===i)if(e.stateNode.complete)e.flags|=8192;else if(wu())e.flags|=8192;else throw Oa=wa,Sa}else e.flags&=-16777217}function Ic(e,t){if(t.type!==`stylesheet`||t.state.loading&4)e.flags&=-16777217;else if(e.flags|=16777216,!Wf(t))if(wu())e.flags|=8192;else throw Oa=wa,Sa}function Lc(e,t){t!==null&&(e.flags|=4),e.flags&16384&&(t=e.tag===22?536870912:Qe(),e.lanes|=t,Yl|=t)}function Rc(e,t){if(!z)switch(e.tailMode){case`hidden`:t=e.tail;for(var n=null;t!==null;)t.alternate!==null&&(n=t),t=t.sibling;n===null?e.tail=null:n.sibling=null;break;case`collapsed`:n=e.tail;for(var r=null;n!==null;)n.alternate!==null&&(r=n),n=n.sibling;r===null?t||e.tail===null?e.tail=null:e.tail.sibling=null:r.sibling=null}}function U(e){var t=e.alternate!==null&&e.alternate.child===e.child,n=0,r=0;if(t)for(var i=e.child;i!==null;)n|=i.lanes|i.childLanes,r|=i.subtreeFlags&65011712,r|=i.flags&65011712,i.return=e,i=i.sibling;else for(i=e.child;i!==null;)n|=i.lanes|i.childLanes,r|=i.subtreeFlags,r|=i.flags,i.return=e,i=i.sibling;return e.subtreeFlags|=r,e.childLanes=n,t}function zc(e,t,n){var r=t.pendingProps;switch(Mi(t),t.tag){case 16:case 15:case 0:case 11:case 7:case 8:case 12:case 9:case 14:return U(t),null;case 1:return U(t),null;case 3:return n=t.stateNode,r=null,e!==null&&(r=e.memoizedState.cache),t.memoizedState.cache!==r&&(t.flags|=2048),Yi(B),ge(),n.pendingContext&&(n.context=n.pendingContext,n.pendingContext=null),(e===null||e.child===null)&&(Vi(t)?Pc(t):e===null||e.memoizedState.isDehydrated&&!(t.flags&256)||(t.flags|=1024,Ui())),U(t),null;case 26:var a=t.type,o=t.memoizedState;return e===null?(Pc(t),o===null?(U(t),Fc(t,a,null,r,n)):(U(t),Ic(t,o))):o?o===e.memoizedState?(U(t),t.flags&=-16777217):(Pc(t),U(t),Ic(t,o)):(e=e.memoizedProps,e!==r&&Pc(t),U(t),Fc(t,a,e,r,n)),null;case 27:if(ve(t),n=pe.current,a=t.type,e!==null&&t.stateNode!=null)e.memoizedProps!==r&&Pc(t);else{if(!r){if(t.stateNode===null)throw Error(i(166));return U(t),null}e=de.current,Vi(t)?zi(t,e):(e=ff(a,r,n),t.stateNode=e,Pc(t))}return U(t),null;case 5:if(ve(t),a=t.type,e!==null&&t.stateNode!=null)e.memoizedProps!==r&&Pc(t);else{if(!r){if(t.stateNode===null)throw Error(i(166));return U(t),null}if(o=de.current,Vi(t))zi(t,o);else{var s=Bd(pe.current);switch(o){case 1:o=s.createElementNS(`http://www.w3.org/2000/svg`,a);break;case 2:o=s.createElementNS(`http://www.w3.org/1998/Math/MathML`,a);break;default:switch(a){case`svg`:o=s.createElementNS(`http://www.w3.org/2000/svg`,a);break;case`math`:o=s.createElementNS(`http://www.w3.org/1998/Math/MathML`,a);break;case`script`:o=s.createElement(`div`),o.innerHTML=`<script><\/script>`,o=o.removeChild(o.firstChild);break;case`select`:o=typeof r.is==`string`?s.createElement(`select`,{is:r.is}):s.createElement(`select`),r.multiple?o.multiple=!0:r.size&&(o.size=r.size);break;default:o=typeof r.is==`string`?s.createElement(a,{is:r.is}):s.createElement(a)}}o[lt]=t,o[ut]=r;a:for(s=t.child;s!==null;){if(s.tag===5||s.tag===6)o.appendChild(s.stateNode);else if(s.tag!==4&&s.tag!==27&&s.child!==null){s.child.return=s,s=s.child;continue}if(s===t)break a;for(;s.sibling===null;){if(s.return===null||s.return===t)break a;s=s.return}s.sibling.return=s.return,s=s.sibling}t.stateNode=o;a:switch(Pd(o,a,r),a){case`button`:case`input`:case`select`:case`textarea`:r=!!r.autoFocus;break a;case`img`:r=!0;break a;default:r=!1}r&&Pc(t)}}return U(t),Fc(t,t.type,e===null?null:e.memoizedProps,t.pendingProps,n),null;case 6:if(e&&t.stateNode!=null)e.memoizedProps!==r&&Pc(t);else{if(typeof r!=`string`&&t.stateNode===null)throw Error(i(166));if(e=pe.current,Vi(t)){if(e=t.stateNode,n=t.memoizedProps,r=null,a=Pi,a!==null)switch(a.tag){case 27:case 5:r=a.memoizedProps}e[lt]=t,e=!!(e.nodeValue===n||r!==null&&!0===r.suppressHydrationWarning||Md(e.nodeValue,n)),e||Ri(t,!0)}else e=Bd(e).createTextNode(r),e[lt]=t,t.stateNode=e}return U(t),null;case 31:if(n=t.memoizedState,e===null||e.memoizedState!==null){if(r=Vi(t),n!==null){if(e===null){if(!r)throw Error(i(318));if(e=t.memoizedState,e=e===null?null:e.dehydrated,!e)throw Error(i(557));e[lt]=t}else Hi(),!(t.flags&128)&&(t.memoizedState=null),t.flags|=4;U(t),e=!1}else n=Ui(),e!==null&&e.memoizedState!==null&&(e.memoizedState.hydrationErrors=n),e=!0;if(!e)return t.flags&256?(co(t),t):(co(t),null);if(t.flags&128)throw Error(i(558))}return U(t),null;case 13:if(r=t.memoizedState,e===null||e.memoizedState!==null&&e.memoizedState.dehydrated!==null){if(a=Vi(t),r!==null&&r.dehydrated!==null){if(e===null){if(!a)throw Error(i(318));if(a=t.memoizedState,a=a===null?null:a.dehydrated,!a)throw Error(i(317));a[lt]=t}else Hi(),!(t.flags&128)&&(t.memoizedState=null),t.flags|=4;U(t),a=!1}else a=Ui(),e!==null&&e.memoizedState!==null&&(e.memoizedState.hydrationErrors=a),a=!0;if(!a)return t.flags&256?(co(t),t):(co(t),null)}return co(t),t.flags&128?(t.lanes=n,t):(n=r!==null,e=e!==null&&e.memoizedState!==null,n&&(r=t.child,a=null,r.alternate!==null&&r.alternate.memoizedState!==null&&r.alternate.memoizedState.cachePool!==null&&(a=r.alternate.memoizedState.cachePool.pool),o=null,r.memoizedState!==null&&r.memoizedState.cachePool!==null&&(o=r.memoizedState.cachePool.pool),o!==a&&(r.flags|=2048)),n!==e&&n&&(t.child.flags|=8192),Lc(t,t.updateQueue),U(t),null);case 4:return ge(),e===null&&Sd(t.stateNode.containerInfo),U(t),null;case 10:return Yi(t.type),U(t),null;case 19:if(ue(lo),r=t.memoizedState,r===null)return U(t),null;if(a=(t.flags&128)!=0,o=r.rendering,o===null)if(a)Rc(r,!1);else{if(X!==0||e!==null&&e.flags&128)for(e=t.child;e!==null;){if(o=uo(e),o!==null){for(t.flags|=128,Rc(r,!1),e=o.updateQueue,t.updateQueue=e,Lc(t,e),t.subtreeFlags=0,e=n,n=t.child;n!==null;)pi(n,e),n=n.sibling;return M(lo,lo.current&1|2),z&&ki(t,r.treeForkCount),t.child}e=e.sibling}r.tail!==null&&ke()>tu&&(t.flags|=128,a=!0,Rc(r,!1),t.lanes=4194304)}else{if(!a)if(e=uo(o),e!==null){if(t.flags|=128,a=!0,e=e.updateQueue,t.updateQueue=e,Lc(t,e),Rc(r,!0),r.tail===null&&r.tailMode===`hidden`&&!o.alternate&&!z)return U(t),null}else 2*ke()-r.renderingStartTime>tu&&n!==536870912&&(t.flags|=128,a=!0,Rc(r,!1),t.lanes=4194304);r.isBackwards?(o.sibling=t.child,t.child=o):(e=r.last,e===null?t.child=o:e.sibling=o,r.last=o)}return r.tail===null?(U(t),null):(e=r.tail,r.rendering=e,r.tail=e.sibling,r.renderingStartTime=ke(),e.sibling=null,n=lo.current,M(lo,a?n&1|2:n&1),z&&ki(t,r.treeForkCount),e);case 22:case 23:return co(t),to(),r=t.memoizedState!==null,e===null?r&&(t.flags|=8192):e.memoizedState!==null!==r&&(t.flags|=8192),r?n&536870912&&!(t.flags&128)&&(U(t),t.subtreeFlags&6&&(t.flags|=8192)):U(t),n=t.updateQueue,n!==null&&Lc(t,n.retryQueue),n=null,e!==null&&e.memoizedState!==null&&e.memoizedState.cachePool!==null&&(n=e.memoizedState.cachePool.pool),r=null,t.memoizedState!==null&&t.memoizedState.cachePool!==null&&(r=t.memoizedState.cachePool.pool),r!==n&&(t.flags|=2048),e!==null&&ue(_a),null;case 24:return n=null,e!==null&&(n=e.memoizedState.cache),t.memoizedState.cache!==n&&(t.flags|=2048),Yi(B),U(t),null;case 25:return null;case 30:return null}throw Error(i(156,t.tag))}function Bc(e,t){switch(Mi(t),t.tag){case 1:return e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 3:return Yi(B),ge(),e=t.flags,e&65536&&!(e&128)?(t.flags=e&-65537|128,t):null;case 26:case 27:case 5:return ve(t),null;case 31:if(t.memoizedState!==null){if(co(t),t.alternate===null)throw Error(i(340));Hi()}return e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 13:if(co(t),e=t.memoizedState,e!==null&&e.dehydrated!==null){if(t.alternate===null)throw Error(i(340));Hi()}return e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 19:return ue(lo),null;case 4:return ge(),null;case 10:return Yi(t.type),null;case 22:case 23:return co(t),to(),e!==null&&ue(_a),e=t.flags,e&65536?(t.flags=e&-65537|128,t):null;case 24:return Yi(B),null;case 25:return null;default:return null}}function Vc(e,t){switch(Mi(t),t.tag){case 3:Yi(B),ge();break;case 26:case 27:case 5:ve(t);break;case 4:ge();break;case 31:t.memoizedState!==null&&co(t);break;case 13:co(t);break;case 19:ue(lo);break;case 10:Yi(t.type);break;case 22:case 23:co(t),to(),e!==null&&ue(_a);break;case 24:Yi(B)}}function Hc(e,t){try{var n=t.updateQueue,r=n===null?null:n.lastEffect;if(r!==null){var i=r.next;n=i;do{if((n.tag&e)===e){r=void 0;var a=n.create,o=n.inst;r=a(),o.destroy=r}n=n.next}while(n!==i)}}catch(e){Z(t,t.return,e)}}function Uc(e,t,n){try{var r=t.updateQueue,i=r===null?null:r.lastEffect;if(i!==null){var a=i.next;r=a;do{if((r.tag&e)===e){var o=r.inst,s=o.destroy;if(s!==void 0){o.destroy=void 0,i=t;var c=n,l=s;try{l()}catch(e){Z(i,c,e)}}}r=r.next}while(r!==a)}}catch(e){Z(t,t.return,e)}}function Wc(e){var t=e.updateQueue;if(t!==null){var n=e.stateNode;try{Xa(t,n)}catch(t){Z(e,e.return,t)}}}function Gc(e,t,n){n.props=qs(e.type,e.memoizedProps),n.state=e.memoizedState;try{n.componentWillUnmount()}catch(n){Z(e,t,n)}}function Kc(e,t){try{var n=e.ref;if(n!==null){switch(e.tag){case 26:case 27:case 5:var r=e.stateNode;break;case 30:r=e.stateNode;break;default:r=e.stateNode}typeof n==`function`?e.refCleanup=n(r):n.current=r}}catch(n){Z(e,t,n)}}function qc(e,t){var n=e.ref,r=e.refCleanup;if(n!==null)if(typeof r==`function`)try{r()}catch(n){Z(e,t,n)}finally{e.refCleanup=null,e=e.alternate,e!=null&&(e.refCleanup=null)}else if(typeof n==`function`)try{n(null)}catch(n){Z(e,t,n)}else n.current=null}function Jc(e){var t=e.type,n=e.memoizedProps,r=e.stateNode;try{a:switch(t){case`button`:case`input`:case`select`:case`textarea`:n.autoFocus&&r.focus();break a;case`img`:n.src?r.src=n.src:n.srcSet&&(r.srcset=n.srcSet)}}catch(t){Z(e,e.return,t)}}function Yc(e,t,n){try{var r=e.stateNode;Fd(r,e.type,n,t),r[ut]=t}catch(t){Z(e,e.return,t)}}function Xc(e){return e.tag===5||e.tag===3||e.tag===26||e.tag===27&&Zd(e.type)||e.tag===4}function Zc(e){a:for(;;){for(;e.sibling===null;){if(e.return===null||Xc(e.return))return null;e=e.return}for(e.sibling.return=e.return,e=e.sibling;e.tag!==5&&e.tag!==6&&e.tag!==18;){if(e.tag===27&&Zd(e.type)||e.flags&2||e.child===null||e.tag===4)continue a;e.child.return=e,e=e.child}if(!(e.flags&2))return e.stateNode}}function Qc(e,t,n){var r=e.tag;if(r===5||r===6)e=e.stateNode,t?(n.nodeType===9?n.body:n.nodeName===`HTML`?n.ownerDocument.body:n).insertBefore(e,t):(t=n.nodeType===9?n.body:n.nodeName===`HTML`?n.ownerDocument.body:n,t.appendChild(e),n=n._reactRootContainer,n!=null||t.onclick!==null||(t.onclick=nn));else if(r!==4&&(r===27&&Zd(e.type)&&(n=e.stateNode,t=null),e=e.child,e!==null))for(Qc(e,t,n),e=e.sibling;e!==null;)Qc(e,t,n),e=e.sibling}function $c(e,t,n){var r=e.tag;if(r===5||r===6)e=e.stateNode,t?n.insertBefore(e,t):n.appendChild(e);else if(r!==4&&(r===27&&Zd(e.type)&&(n=e.stateNode),e=e.child,e!==null))for($c(e,t,n),e=e.sibling;e!==null;)$c(e,t,n),e=e.sibling}function el(e){var t=e.stateNode,n=e.memoizedProps;try{for(var r=e.type,i=t.attributes;i.length;)t.removeAttributeNode(i[0]);Pd(t,r,n),t[lt]=e,t[ut]=n}catch(t){Z(e,e.return,t)}}var tl=!1,nl=!1,rl=!1,il=typeof WeakSet==`function`?WeakSet:Set,al=null;function ol(e,t){if(e=e.containerInfo,Rd=sp,e=kr(e),Ar(e)){if(`selectionStart`in e)var n={start:e.selectionStart,end:e.selectionEnd};else a:{n=(n=e.ownerDocument)&&n.defaultView||window;var r=n.getSelection&&n.getSelection();if(r&&r.rangeCount!==0){n=r.anchorNode;var a=r.anchorOffset,o=r.focusNode;r=r.focusOffset;try{n.nodeType,o.nodeType}catch{n=null;break a}var s=0,c=-1,l=-1,u=0,d=0,f=e,p=null;b:for(;;){for(var m;f!==n||a!==0&&f.nodeType!==3||(c=s+a),f!==o||r!==0&&f.nodeType!==3||(l=s+r),f.nodeType===3&&(s+=f.nodeValue.length),(m=f.firstChild)!==null;)p=f,f=m;for(;;){if(f===e)break b;if(p===n&&++u===a&&(c=s),p===o&&++d===r&&(l=s),(m=f.nextSibling)!==null)break;f=p,p=f.parentNode}f=m}n=c===-1||l===-1?null:{start:c,end:l}}else n=null}n||={start:0,end:0}}else n=null;for(zd={focusedElem:e,selectionRange:n},sp=!1,al=t;al!==null;)if(t=al,e=t.child,t.subtreeFlags&1028&&e!==null)e.return=t,al=e;else for(;al!==null;){switch(t=al,o=t.alternate,e=t.flags,t.tag){case 0:if(e&4&&(e=t.updateQueue,e=e===null?null:e.events,e!==null))for(n=0;n<e.length;n++)a=e[n],a.ref.impl=a.nextImpl;break;case 11:case 15:break;case 1:if(e&1024&&o!==null){e=void 0,n=t,a=o.memoizedProps,o=o.memoizedState,r=n.stateNode;try{var h=qs(n.type,a);e=r.getSnapshotBeforeUpdate(h,o),r.__reactInternalSnapshotBeforeUpdate=e}catch(e){Z(n,n.return,e)}}break;case 3:if(e&1024){if(e=t.stateNode.containerInfo,n=e.nodeType,n===9)ef(e);else if(n===1)switch(e.nodeName){case`HEAD`:case`HTML`:case`BODY`:ef(e);break;default:e.textContent=``}}break;case 5:case 26:case 27:case 6:case 4:case 17:break;default:if(e&1024)throw Error(i(163))}if(e=t.sibling,e!==null){e.return=t.return,al=e;break}al=t.return}}function sl(e,t,n){var r=n.flags;switch(n.tag){case 0:case 11:case 15:xl(e,n),r&4&&Hc(5,n);break;case 1:if(xl(e,n),r&4)if(e=n.stateNode,t===null)try{e.componentDidMount()}catch(e){Z(n,n.return,e)}else{var i=qs(n.type,t.memoizedProps);t=t.memoizedState;try{e.componentDidUpdate(i,t,e.__reactInternalSnapshotBeforeUpdate)}catch(e){Z(n,n.return,e)}}r&64&&Wc(n),r&512&&Kc(n,n.return);break;case 3:if(xl(e,n),r&64&&(e=n.updateQueue,e!==null)){if(t=null,n.child!==null)switch(n.child.tag){case 27:case 5:t=n.child.stateNode;break;case 1:t=n.child.stateNode}try{Xa(e,t)}catch(e){Z(n,n.return,e)}}break;case 27:t===null&&r&4&&el(n);case 26:case 5:xl(e,n),t===null&&r&4&&Jc(n),r&512&&Kc(n,n.return);break;case 12:xl(e,n);break;case 31:xl(e,n),r&4&&fl(e,n);break;case 13:xl(e,n),r&4&&pl(e,n),r&64&&(e=n.memoizedState,e!==null&&(e=e.dehydrated,e!==null&&(n=Ju.bind(null,n),sf(e,n))));break;case 22:if(r=n.memoizedState!==null||tl,!r){t=t!==null&&t.memoizedState!==null||nl,i=tl;var a=nl;tl=r,(nl=t)&&!a?Cl(e,n,(n.subtreeFlags&8772)!=0):xl(e,n),tl=i,nl=a}break;case 30:break;default:xl(e,n)}}function cl(e){var t=e.alternate;t!==null&&(e.alternate=null,cl(t)),e.child=null,e.deletions=null,e.sibling=null,e.tag===5&&(t=e.stateNode,t!==null&&_t(t)),e.stateNode=null,e.return=null,e.dependencies=null,e.memoizedProps=null,e.memoizedState=null,e.pendingProps=null,e.stateNode=null,e.updateQueue=null}var W=null,ll=!1;function ul(e,t,n){for(n=n.child;n!==null;)dl(e,t,n),n=n.sibling}function dl(e,t,n){if(ze&&typeof ze.onCommitFiberUnmount==`function`)try{ze.onCommitFiberUnmount(Re,n)}catch{}switch(n.tag){case 26:nl||qc(n,t),ul(e,t,n),n.memoizedState?n.memoizedState.count--:n.stateNode&&(n=n.stateNode,n.parentNode.removeChild(n));break;case 27:nl||qc(n,t);var r=W,i=ll;Zd(n.type)&&(W=n.stateNode,ll=!1),ul(e,t,n),pf(n.stateNode),W=r,ll=i;break;case 5:nl||qc(n,t);case 6:if(r=W,i=ll,W=null,ul(e,t,n),W=r,ll=i,W!==null)if(ll)try{(W.nodeType===9?W.body:W.nodeName===`HTML`?W.ownerDocument.body:W).removeChild(n.stateNode)}catch(e){Z(n,t,e)}else try{W.removeChild(n.stateNode)}catch(e){Z(n,t,e)}break;case 18:W!==null&&(ll?(e=W,Qd(e.nodeType===9?e.body:e.nodeName===`HTML`?e.ownerDocument.body:e,n.stateNode),Np(e)):Qd(W,n.stateNode));break;case 4:r=W,i=ll,W=n.stateNode.containerInfo,ll=!0,ul(e,t,n),W=r,ll=i;break;case 0:case 11:case 14:case 15:Uc(2,n,t),nl||Uc(4,n,t),ul(e,t,n);break;case 1:nl||(qc(n,t),r=n.stateNode,typeof r.componentWillUnmount==`function`&&Gc(n,t,r)),ul(e,t,n);break;case 21:ul(e,t,n);break;case 22:nl=(r=nl)||n.memoizedState!==null,ul(e,t,n),nl=r;break;default:ul(e,t,n)}}function fl(e,t){if(t.memoizedState===null&&(e=t.alternate,e!==null&&(e=e.memoizedState,e!==null))){e=e.dehydrated;try{Np(e)}catch(e){Z(t,t.return,e)}}}function pl(e,t){if(t.memoizedState===null&&(e=t.alternate,e!==null&&(e=e.memoizedState,e!==null&&(e=e.dehydrated,e!==null))))try{Np(e)}catch(e){Z(t,t.return,e)}}function ml(e){switch(e.tag){case 31:case 13:case 19:var t=e.stateNode;return t===null&&(t=e.stateNode=new il),t;case 22:return e=e.stateNode,t=e._retryCache,t===null&&(t=e._retryCache=new il),t;default:throw Error(i(435,e.tag))}}function hl(e,t){var n=ml(e);t.forEach(function(t){if(!n.has(t)){n.add(t);var r=Yu.bind(null,e,t);t.then(r,r)}})}function gl(e,t){var n=t.deletions;if(n!==null)for(var r=0;r<n.length;r++){var a=n[r],o=e,s=t,c=s;a:for(;c!==null;){switch(c.tag){case 27:if(Zd(c.type)){W=c.stateNode,ll=!1;break a}break;case 5:W=c.stateNode,ll=!1;break a;case 3:case 4:W=c.stateNode.containerInfo,ll=!0;break a}c=c.return}if(W===null)throw Error(i(160));dl(o,s,a),W=null,ll=!1,o=a.alternate,o!==null&&(o.return=null),a.return=null}if(t.subtreeFlags&13886)for(t=t.child;t!==null;)vl(t,e),t=t.sibling}var _l=null;function vl(e,t){var n=e.alternate,r=e.flags;switch(e.tag){case 0:case 11:case 14:case 15:gl(t,e),yl(e),r&4&&(Uc(3,e,e.return),Hc(3,e),Uc(5,e,e.return));break;case 1:gl(t,e),yl(e),r&512&&(nl||n===null||qc(n,n.return)),r&64&&tl&&(e=e.updateQueue,e!==null&&(r=e.callbacks,r!==null&&(n=e.shared.hiddenCallbacks,e.shared.hiddenCallbacks=n===null?r:n.concat(r))));break;case 26:var a=_l;if(gl(t,e),yl(e),r&512&&(nl||n===null||qc(n,n.return)),r&4){var o=n===null?null:n.memoizedState;if(r=e.memoizedState,n===null)if(r===null)if(e.stateNode===null){a:{r=e.type,n=e.memoizedProps,a=a.ownerDocument||a;b:switch(r){case`title`:o=a.getElementsByTagName(`title`)[0],(!o||o[gt]||o[lt]||o.namespaceURI===`http://www.w3.org/2000/svg`||o.hasAttribute(`itemprop`))&&(o=a.createElement(r),a.head.insertBefore(o,a.querySelector(`head > title`))),Pd(o,r,n),o[lt]=e,St(o),r=o;break a;case`link`:var s=Vf(`link`,`href`,a).get(r+(n.href||``));if(s){for(var c=0;c<s.length;c++)if(o=s[c],o.getAttribute(`href`)===(n.href==null||n.href===``?null:n.href)&&o.getAttribute(`rel`)===(n.rel==null?null:n.rel)&&o.getAttribute(`title`)===(n.title==null?null:n.title)&&o.getAttribute(`crossorigin`)===(n.crossOrigin==null?null:n.crossOrigin)){s.splice(c,1);break b}}o=a.createElement(r),Pd(o,r,n),a.head.appendChild(o);break;case`meta`:if(s=Vf(`meta`,`content`,a).get(r+(n.content||``))){for(c=0;c<s.length;c++)if(o=s[c],o.getAttribute(`content`)===(n.content==null?null:``+n.content)&&o.getAttribute(`name`)===(n.name==null?null:n.name)&&o.getAttribute(`property`)===(n.property==null?null:n.property)&&o.getAttribute(`http-equiv`)===(n.httpEquiv==null?null:n.httpEquiv)&&o.getAttribute(`charset`)===(n.charSet==null?null:n.charSet)){s.splice(c,1);break b}}o=a.createElement(r),Pd(o,r,n),a.head.appendChild(o);break;default:throw Error(i(468,r))}o[lt]=e,St(o),r=o}e.stateNode=r}else Hf(a,e.type,e.stateNode);else e.stateNode=If(a,r,e.memoizedProps);else o===r?r===null&&e.stateNode!==null&&Yc(e,e.memoizedProps,n.memoizedProps):(o===null?n.stateNode!==null&&(n=n.stateNode,n.parentNode.removeChild(n)):o.count--,r===null?Hf(a,e.type,e.stateNode):If(a,r,e.memoizedProps))}break;case 27:gl(t,e),yl(e),r&512&&(nl||n===null||qc(n,n.return)),n!==null&&r&4&&Yc(e,e.memoizedProps,n.memoizedProps);break;case 5:if(gl(t,e),yl(e),r&512&&(nl||n===null||qc(n,n.return)),e.flags&32){a=e.stateNode;try{Jt(a,``)}catch(t){Z(e,e.return,t)}}r&4&&e.stateNode!=null&&(a=e.memoizedProps,Yc(e,a,n===null?a:n.memoizedProps)),r&1024&&(rl=!0);break;case 6:if(gl(t,e),yl(e),r&4){if(e.stateNode===null)throw Error(i(162));r=e.memoizedProps,n=e.stateNode;try{n.nodeValue=r}catch(t){Z(e,e.return,t)}}break;case 3:if(Bf=null,a=_l,_l=gf(t.containerInfo),gl(t,e),_l=a,yl(e),r&4&&n!==null&&n.memoizedState.isDehydrated)try{Np(t.containerInfo)}catch(t){Z(e,e.return,t)}rl&&(rl=!1,bl(e));break;case 4:r=_l,_l=gf(e.stateNode.containerInfo),gl(t,e),yl(e),_l=r;break;case 12:gl(t,e),yl(e);break;case 31:gl(t,e),yl(e),r&4&&(r=e.updateQueue,r!==null&&(e.updateQueue=null,hl(e,r)));break;case 13:gl(t,e),yl(e),e.child.flags&8192&&e.memoizedState!==null!=(n!==null&&n.memoizedState!==null)&&($l=ke()),r&4&&(r=e.updateQueue,r!==null&&(e.updateQueue=null,hl(e,r)));break;case 22:a=e.memoizedState!==null;var l=n!==null&&n.memoizedState!==null,u=tl,d=nl;if(tl=u||a,nl=d||l,gl(t,e),nl=d,tl=u,yl(e),r&8192)a:for(t=e.stateNode,t._visibility=a?t._visibility&-2:t._visibility|1,a&&(n===null||l||tl||nl||Sl(e)),n=null,t=e;;){if(t.tag===5||t.tag===26){if(n===null){l=n=t;try{if(o=l.stateNode,a)s=o.style,typeof s.setProperty==`function`?s.setProperty(`display`,`none`,`important`):s.display=`none`;else{c=l.stateNode;var f=l.memoizedProps.style,p=f!=null&&f.hasOwnProperty(`display`)?f.display:null;c.style.display=p==null||typeof p==`boolean`?``:(``+p).trim()}}catch(e){Z(l,l.return,e)}}}else if(t.tag===6){if(n===null){l=t;try{l.stateNode.nodeValue=a?``:l.memoizedProps}catch(e){Z(l,l.return,e)}}}else if(t.tag===18){if(n===null){l=t;try{var m=l.stateNode;a?$d(m,!0):$d(l.stateNode,!1)}catch(e){Z(l,l.return,e)}}}else if((t.tag!==22&&t.tag!==23||t.memoizedState===null||t===e)&&t.child!==null){t.child.return=t,t=t.child;continue}if(t===e)break a;for(;t.sibling===null;){if(t.return===null||t.return===e)break a;n===t&&(n=null),t=t.return}n===t&&(n=null),t.sibling.return=t.return,t=t.sibling}r&4&&(r=e.updateQueue,r!==null&&(n=r.retryQueue,n!==null&&(r.retryQueue=null,hl(e,n))));break;case 19:gl(t,e),yl(e),r&4&&(r=e.updateQueue,r!==null&&(e.updateQueue=null,hl(e,r)));break;case 30:break;case 21:break;default:gl(t,e),yl(e)}}function yl(e){var t=e.flags;if(t&2){try{for(var n,r=e.return;r!==null;){if(Xc(r)){n=r;break}r=r.return}if(n==null)throw Error(i(160));switch(n.tag){case 27:var a=n.stateNode;$c(e,Zc(e),a);break;case 5:var o=n.stateNode;n.flags&32&&(Jt(o,``),n.flags&=-33),$c(e,Zc(e),o);break;case 3:case 4:var s=n.stateNode.containerInfo;Qc(e,Zc(e),s);break;default:throw Error(i(161))}}catch(t){Z(e,e.return,t)}e.flags&=-3}t&4096&&(e.flags&=-4097)}function bl(e){if(e.subtreeFlags&1024)for(e=e.child;e!==null;){var t=e;bl(t),t.tag===5&&t.flags&1024&&t.stateNode.reset(),e=e.sibling}}function xl(e,t){if(t.subtreeFlags&8772)for(t=t.child;t!==null;)sl(e,t.alternate,t),t=t.sibling}function Sl(e){for(e=e.child;e!==null;){var t=e;switch(t.tag){case 0:case 11:case 14:case 15:Uc(4,t,t.return),Sl(t);break;case 1:qc(t,t.return);var n=t.stateNode;typeof n.componentWillUnmount==`function`&&Gc(t,t.return,n),Sl(t);break;case 27:pf(t.stateNode);case 26:case 5:qc(t,t.return),Sl(t);break;case 22:t.memoizedState===null&&Sl(t);break;case 30:Sl(t);break;default:Sl(t)}e=e.sibling}}function Cl(e,t,n){for(n&&=(t.subtreeFlags&8772)!=0,t=t.child;t!==null;){var r=t.alternate,i=e,a=t,o=a.flags;switch(a.tag){case 0:case 11:case 15:Cl(i,a,n),Hc(4,a);break;case 1:if(Cl(i,a,n),r=a,i=r.stateNode,typeof i.componentDidMount==`function`)try{i.componentDidMount()}catch(e){Z(r,r.return,e)}if(r=a,i=r.updateQueue,i!==null){var s=r.stateNode;try{var c=i.shared.hiddenCallbacks;if(c!==null)for(i.shared.hiddenCallbacks=null,i=0;i<c.length;i++)Ya(c[i],s)}catch(e){Z(r,r.return,e)}}n&&o&64&&Wc(a),Kc(a,a.return);break;case 27:el(a);case 26:case 5:Cl(i,a,n),n&&r===null&&o&4&&Jc(a),Kc(a,a.return);break;case 12:Cl(i,a,n);break;case 31:Cl(i,a,n),n&&o&4&&fl(i,a);break;case 13:Cl(i,a,n),n&&o&4&&pl(i,a);break;case 22:a.memoizedState===null&&Cl(i,a,n),Kc(a,a.return);break;case 30:break;default:Cl(i,a,n)}t=t.sibling}}function wl(e,t){var n=null;e!==null&&e.memoizedState!==null&&e.memoizedState.cachePool!==null&&(n=e.memoizedState.cachePool.pool),e=null,t.memoizedState!==null&&t.memoizedState.cachePool!==null&&(e=t.memoizedState.cachePool.pool),e!==n&&(e!=null&&e.refCount++,n!=null&&ca(n))}function Tl(e,t){e=null,t.alternate!==null&&(e=t.alternate.memoizedState.cache),t=t.memoizedState.cache,t!==e&&(t.refCount++,e!=null&&ca(e))}function El(e,t,n,r){if(t.subtreeFlags&10256)for(t=t.child;t!==null;)Dl(e,t,n,r),t=t.sibling}function Dl(e,t,n,r){var i=t.flags;switch(t.tag){case 0:case 11:case 15:El(e,t,n,r),i&2048&&Hc(9,t);break;case 1:El(e,t,n,r);break;case 3:El(e,t,n,r),i&2048&&(e=null,t.alternate!==null&&(e=t.alternate.memoizedState.cache),t=t.memoizedState.cache,t!==e&&(t.refCount++,e!=null&&ca(e)));break;case 12:if(i&2048){El(e,t,n,r),e=t.stateNode;try{var a=t.memoizedProps,o=a.id,s=a.onPostCommit;typeof s==`function`&&s(o,t.alternate===null?`mount`:`update`,e.passiveEffectDuration,-0)}catch(e){Z(t,t.return,e)}}else El(e,t,n,r);break;case 31:El(e,t,n,r);break;case 13:El(e,t,n,r);break;case 23:break;case 22:a=t.stateNode,o=t.alternate,t.memoizedState===null?a._visibility&2?El(e,t,n,r):(a._visibility|=2,Ol(e,t,n,r,(t.subtreeFlags&10256)!=0||!1)):a._visibility&2?El(e,t,n,r):kl(e,t),i&2048&&wl(o,t);break;case 24:El(e,t,n,r),i&2048&&Tl(t.alternate,t);break;default:El(e,t,n,r)}}function Ol(e,t,n,r,i){for(i&&=(t.subtreeFlags&10256)!=0||!1,t=t.child;t!==null;){var a=e,o=t,s=n,c=r,l=o.flags;switch(o.tag){case 0:case 11:case 15:Ol(a,o,s,c,i),Hc(8,o);break;case 23:break;case 22:var u=o.stateNode;o.memoizedState===null?(u._visibility|=2,Ol(a,o,s,c,i)):u._visibility&2?Ol(a,o,s,c,i):kl(a,o),i&&l&2048&&wl(o.alternate,o);break;case 24:Ol(a,o,s,c,i),i&&l&2048&&Tl(o.alternate,o);break;default:Ol(a,o,s,c,i)}t=t.sibling}}function kl(e,t){if(t.subtreeFlags&10256)for(t=t.child;t!==null;){var n=e,r=t,i=r.flags;switch(r.tag){case 22:kl(n,r),i&2048&&wl(r.alternate,r);break;case 24:kl(n,r),i&2048&&Tl(r.alternate,r);break;default:kl(n,r)}t=t.sibling}}var Al=8192;function jl(e,t,n){if(e.subtreeFlags&Al)for(e=e.child;e!==null;)Ml(e,t,n),e=e.sibling}function Ml(e,t,n){switch(e.tag){case 26:jl(e,t,n),e.flags&Al&&e.memoizedState!==null&&Gf(n,_l,e.memoizedState,e.memoizedProps);break;case 5:jl(e,t,n);break;case 3:case 4:var r=_l;_l=gf(e.stateNode.containerInfo),jl(e,t,n),_l=r;break;case 22:e.memoizedState===null&&(r=e.alternate,r!==null&&r.memoizedState!==null?(r=Al,Al=16777216,jl(e,t,n),Al=r):jl(e,t,n));break;default:jl(e,t,n)}}function Nl(e){var t=e.alternate;if(t!==null&&(e=t.child,e!==null)){t.child=null;do t=e.sibling,e.sibling=null,e=t;while(e!==null)}}function Pl(e){var t=e.deletions;if(e.flags&16){if(t!==null)for(var n=0;n<t.length;n++){var r=t[n];al=r,Ll(r,e)}Nl(e)}if(e.subtreeFlags&10256)for(e=e.child;e!==null;)Fl(e),e=e.sibling}function Fl(e){switch(e.tag){case 0:case 11:case 15:Pl(e),e.flags&2048&&Uc(9,e,e.return);break;case 3:Pl(e);break;case 12:Pl(e);break;case 22:var t=e.stateNode;e.memoizedState!==null&&t._visibility&2&&(e.return===null||e.return.tag!==13)?(t._visibility&=-3,Il(e)):Pl(e);break;default:Pl(e)}}function Il(e){var t=e.deletions;if(e.flags&16){if(t!==null)for(var n=0;n<t.length;n++){var r=t[n];al=r,Ll(r,e)}Nl(e)}for(e=e.child;e!==null;){switch(t=e,t.tag){case 0:case 11:case 15:Uc(8,t,t.return),Il(t);break;case 22:n=t.stateNode,n._visibility&2&&(n._visibility&=-3,Il(t));break;default:Il(t)}e=e.sibling}}function Ll(e,t){for(;al!==null;){var n=al;switch(n.tag){case 0:case 11:case 15:Uc(8,n,t);break;case 23:case 22:if(n.memoizedState!==null&&n.memoizedState.cachePool!==null){var r=n.memoizedState.cachePool.pool;r!=null&&r.refCount++}break;case 24:ca(n.memoizedState.cache)}if(r=n.child,r!==null)r.return=n,al=r;else a:for(n=e;al!==null;){r=al;var i=r.sibling,a=r.return;if(cl(r),r===n){al=null;break a}if(i!==null){i.return=a,al=i;break a}al=a}}}var Rl={getCacheForType:function(e){var t=ta(B),n=t.data.get(e);return n===void 0&&(n=e(),t.data.set(e,n)),n},cacheSignal:function(){return ta(B).controller.signal}},zl=typeof WeakMap==`function`?WeakMap:Map,G=0,K=null,q=null,J=0,Y=0,Bl=null,Vl=!1,Hl=!1,Ul=!1,Wl=0,X=0,Gl=0,Kl=0,ql=0,Jl=0,Yl=0,Xl=null,Zl=null,Ql=!1,$l=0,eu=0,tu=1/0,nu=null,ru=null,iu=0,au=null,ou=null,su=0,cu=0,lu=null,uu=null,du=0,fu=null;function pu(){return G&2&&J!==0?J&-J:A.T===null?ot():dd()}function mu(){if(Jl===0)if(!(J&536870912)||z){var e=Ke;Ke<<=1,!(Ke&3932160)&&(Ke=262144),Jl=e}else Jl=536870912;return e=no.current,e!==null&&(e.flags|=32),Jl}function hu(e,t,n){(e===K&&(Y===2||Y===9)||e.cancelPendingCommit!==null)&&(Su(e,0),yu(e,J,Jl,!1)),et(e,n),(!(G&2)||e!==K)&&(e===K&&(!(G&2)&&(Kl|=n),X===4&&yu(e,J,Jl,!1)),rd(e))}function gu(e,t,n){if(G&6)throw Error(i(327));var r=!n&&(t&127)==0&&(t&e.expiredLanes)===0||Xe(e,t),a=r?Au(e,t):Ou(e,t,!0),o=r;do{if(a===0){Hl&&!r&&yu(e,t,0,!1);break}else{if(n=e.current.alternate,o&&!vu(n)){a=Ou(e,t,!1),o=!1;continue}if(a===2){if(o=t,e.errorRecoveryDisabledLanes&o)var s=0;else s=e.pendingLanes&-536870913,s=s===0?s&536870912?536870912:0:s;if(s!==0){t=s;a:{var c=e;a=Xl;var l=c.current.memoizedState.isDehydrated;if(l&&(Su(c,s).flags|=256),s=Ou(c,s,!1),s!==2){if(Ul&&!l){c.errorRecoveryDisabledLanes|=o,Kl|=o,a=4;break a}o=Zl,Zl=a,o!==null&&(Zl===null?Zl=o:Zl.push.apply(Zl,o))}a=s}if(o=!1,a!==2)continue}}if(a===1){Su(e,0),yu(e,t,0,!0);break}a:{switch(r=e,o=a,o){case 0:case 1:throw Error(i(345));case 4:if((t&4194048)!==t)break;case 6:yu(r,t,Jl,!Vl);break a;case 2:Zl=null;break;case 3:case 5:break;default:throw Error(i(329))}if((t&62914560)===t&&(a=$l+300-ke(),10<a)){if(yu(r,t,Jl,!Vl),Ye(r,0,!0)!==0)break a;su=t,r.timeoutHandle=Kd(_u.bind(null,r,n,Zl,nu,Ql,t,Jl,Kl,Yl,Vl,o,`Throttled`,-0,0),a);break a}_u(r,n,Zl,nu,Ql,t,Jl,Kl,Yl,Vl,o,null,-0,0)}}break}while(1);rd(e)}function _u(e,t,n,r,i,a,o,s,c,l,u,d,f,p){if(e.timeoutHandle=-1,d=t.subtreeFlags,d&8192||(d&16785408)==16785408){d={stylesheets:null,count:0,imgCount:0,imgBytes:0,suspenseyImages:[],waitingForImages:!0,waitingForViewTransition:!1,unsuspend:nn},Ml(t,a,d);var m=(a&62914560)===a?$l-ke():(a&4194048)===a?eu-ke():0;if(m=qf(d,m),m!==null){su=a,e.cancelPendingCommit=m(Lu.bind(null,e,t,a,n,r,i,o,s,c,u,d,null,f,p)),yu(e,a,o,!l);return}}Lu(e,t,a,n,r,i,o,s,c)}function vu(e){for(var t=e;;){var n=t.tag;if((n===0||n===11||n===15)&&t.flags&16384&&(n=t.updateQueue,n!==null&&(n=n.stores,n!==null)))for(var r=0;r<n.length;r++){var i=n[r],a=i.getSnapshot;i=i.value;try{if(!wr(a(),i))return!1}catch{return!1}}if(n=t.child,t.subtreeFlags&16384&&n!==null)n.return=t,t=n;else{if(t===e)break;for(;t.sibling===null;){if(t.return===null||t.return===e)return!0;t=t.return}t.sibling.return=t.return,t=t.sibling}}return!0}function yu(e,t,n,r){t&=~ql,t&=~Kl,e.suspendedLanes|=t,e.pingedLanes&=~t,r&&(e.warmLanes|=t),r=e.expirationTimes;for(var i=t;0<i;){var a=31-Ve(i),o=1<<a;r[a]=-1,i&=~o}n!==0&&nt(e,n,t)}function bu(){return G&6?!0:(id(0,!1),!1)}function xu(){if(q!==null){if(Y===0)var e=q.return;else e=q,qi=Ki=null,ko(e),ja=null,Ma=0,e=q;for(;e!==null;)Vc(e.alternate,e),e=e.return;q=null}}function Su(e,t){var n=e.timeoutHandle;n!==-1&&(e.timeoutHandle=-1,qd(n)),n=e.cancelPendingCommit,n!==null&&(e.cancelPendingCommit=null,n()),su=0,xu(),K=e,q=n=fi(e.current,null),J=t,Y=0,Bl=null,Vl=!1,Hl=Xe(e,t),Ul=!1,Yl=Jl=ql=Kl=Gl=X=0,Zl=Xl=null,Ql=!1,t&8&&(t|=t&32);var r=e.entangledLanes;if(r!==0)for(e=e.entanglements,r&=t;0<r;){var i=31-Ve(r),a=1<<i;t|=e[i],r&=~a}return Wl=t,ni(),n}function Cu(e,t){V=null,A.H=zs,t===xa||t===Ca?(t=ka(),Y=3):t===Sa?(t=ka(),Y=4):Y=t===rc?8:typeof t==`object`&&t&&typeof t.then==`function`?6:1,Bl=t,q===null&&(X=1,Zs(e,yi(t,e.current)))}function wu(){var e=no.current;return e===null?!0:(J&4194048)===J?ro===null:(J&62914560)===J||J&536870912?e===ro:!1}function Tu(){var e=A.H;return A.H=zs,e===null?zs:e}function Eu(){var e=A.A;return A.A=Rl,e}function Du(){X=4,Vl||(J&4194048)!==J&&no.current!==null||(Hl=!0),!(Gl&134217727)&&!(Kl&134217727)||K===null||yu(K,J,Jl,!1)}function Ou(e,t,n){var r=G;G|=2;var i=Tu(),a=Eu();(K!==e||J!==t)&&(nu=null,Su(e,t)),t=!1;var o=X;a:do try{if(Y!==0&&q!==null){var s=q,c=Bl;switch(Y){case 8:xu(),o=6;break a;case 3:case 2:case 9:case 6:no.current===null&&(t=!0);var l=Y;if(Y=0,Bl=null,Pu(e,s,c,l),n&&Hl){o=0;break a}break;default:l=Y,Y=0,Bl=null,Pu(e,s,c,l)}}ku(),o=X;break}catch(t){Cu(e,t)}while(1);return t&&e.shellSuspendCounter++,qi=Ki=null,G=r,A.H=i,A.A=a,q===null&&(K=null,J=0,ni()),o}function ku(){for(;q!==null;)Mu(q)}function Au(e,t){var n=G;G|=2;var r=Tu(),a=Eu();K!==e||J!==t?(nu=null,tu=ke()+500,Su(e,t)):Hl=Xe(e,t);a:do try{if(Y!==0&&q!==null){t=q;var o=Bl;b:switch(Y){case 1:Y=0,Bl=null,Pu(e,t,o,1);break;case 2:case 9:if(Ta(o)){Y=0,Bl=null,Nu(t);break}t=function(){Y!==2&&Y!==9||K!==e||(Y=7),rd(e)},o.then(t,t);break a;case 3:Y=7;break a;case 4:Y=5;break a;case 7:Ta(o)?(Y=0,Bl=null,Nu(t)):(Y=0,Bl=null,Pu(e,t,o,7));break;case 5:var s=null;switch(q.tag){case 26:s=q.memoizedState;case 5:case 27:var c=q;if(s?Wf(s):c.stateNode.complete){Y=0,Bl=null;var l=c.sibling;if(l!==null)q=l;else{var u=c.return;u===null?q=null:(q=u,Fu(u))}break b}}Y=0,Bl=null,Pu(e,t,o,5);break;case 6:Y=0,Bl=null,Pu(e,t,o,6);break;case 8:xu(),X=6;break a;default:throw Error(i(462))}}ju();break}catch(t){Cu(e,t)}while(1);return qi=Ki=null,A.H=r,A.A=a,G=n,q===null?(K=null,J=0,ni(),X):0}function ju(){for(;q!==null&&!De();)Mu(q)}function Mu(e){var t=Nc(e.alternate,e,Wl);e.memoizedProps=e.pendingProps,t===null?Fu(e):q=t}function Nu(e){var t=e,n=t.alternate;switch(t.tag){case 15:case 0:t=_c(n,t,t.pendingProps,t.type,void 0,J);break;case 11:t=_c(n,t,t.pendingProps,t.type.render,t.ref,J);break;case 5:ko(t);default:Vc(n,t),t=q=pi(t,Wl),t=Nc(n,t,Wl)}e.memoizedProps=e.pendingProps,t===null?Fu(e):q=t}function Pu(e,t,n,r){qi=Ki=null,ko(t),ja=null,Ma=0;var i=t.return;try{if(nc(e,i,t,n,J)){X=1,Zs(e,yi(n,e.current)),q=null;return}}catch(t){if(i!==null)throw q=i,t;X=1,Zs(e,yi(n,e.current)),q=null;return}t.flags&32768?(z||r===1?e=!0:Hl||J&536870912?e=!1:(Vl=e=!0,(r===2||r===9||r===3||r===6)&&(r=no.current,r!==null&&r.tag===13&&(r.flags|=16384))),Iu(t,e)):Fu(t)}function Fu(e){var t=e;do{if(t.flags&32768){Iu(t,Vl);return}e=t.return;var n=zc(t.alternate,t,Wl);if(n!==null){q=n;return}if(t=t.sibling,t!==null){q=t;return}q=t=e}while(t!==null);X===0&&(X=5)}function Iu(e,t){do{var n=Bc(e.alternate,e);if(n!==null){n.flags&=32767,q=n;return}if(n=e.return,n!==null&&(n.flags|=32768,n.subtreeFlags=0,n.deletions=null),!t&&(e=e.sibling,e!==null)){q=e;return}q=e=n}while(e!==null);X=6,q=null}function Lu(e,t,n,r,a,o,s,c,l){e.cancelPendingCommit=null;do Hu();while(iu!==0);if(G&6)throw Error(i(327));if(t!==null){if(t===e.current)throw Error(i(177));if(o=t.lanes|t.childLanes,o|=ti,tt(e,n,o,s,c,l),e===K&&(q=K=null,J=0),ou=t,au=e,su=n,cu=o,lu=a,uu=r,t.subtreeFlags&10256||t.flags&10256?(e.callbackNode=null,e.callbackPriority=0,Xu(Ne,function(){return Uu(),null})):(e.callbackNode=null,e.callbackPriority=0),r=(t.flags&13878)!=0,t.subtreeFlags&13878||r){r=A.T,A.T=null,a=j.p,j.p=2,s=G,G|=4;try{ol(e,t,n)}finally{G=s,j.p=a,A.T=r}}iu=1,Ru(),zu(),Bu()}}function Ru(){if(iu===1){iu=0;var e=au,t=ou,n=(t.flags&13878)!=0;if(t.subtreeFlags&13878||n){n=A.T,A.T=null;var r=j.p;j.p=2;var i=G;G|=4;try{vl(t,e);var a=zd,o=kr(e.containerInfo),s=a.focusedElem,c=a.selectionRange;if(o!==s&&s&&s.ownerDocument&&Or(s.ownerDocument.documentElement,s)){if(c!==null&&Ar(s)){var l=c.start,u=c.end;if(u===void 0&&(u=l),`selectionStart`in s)s.selectionStart=l,s.selectionEnd=Math.min(u,s.value.length);else{var d=s.ownerDocument||document,f=d&&d.defaultView||window;if(f.getSelection){var p=f.getSelection(),m=s.textContent.length,h=Math.min(c.start,m),g=c.end===void 0?h:Math.min(c.end,m);!p.extend&&h>g&&(o=g,g=h,h=o);var _=Dr(s,h),v=Dr(s,g);if(_&&v&&(p.rangeCount!==1||p.anchorNode!==_.node||p.anchorOffset!==_.offset||p.focusNode!==v.node||p.focusOffset!==v.offset)){var y=d.createRange();y.setStart(_.node,_.offset),p.removeAllRanges(),h>g?(p.addRange(y),p.extend(v.node,v.offset)):(y.setEnd(v.node,v.offset),p.addRange(y))}}}}for(d=[],p=s;p=p.parentNode;)p.nodeType===1&&d.push({element:p,left:p.scrollLeft,top:p.scrollTop});for(typeof s.focus==`function`&&s.focus(),s=0;s<d.length;s++){var b=d[s];b.element.scrollLeft=b.left,b.element.scrollTop=b.top}}sp=!!Rd,zd=Rd=null}finally{G=i,j.p=r,A.T=n}}e.current=t,iu=2}}function zu(){if(iu===2){iu=0;var e=au,t=ou,n=(t.flags&8772)!=0;if(t.subtreeFlags&8772||n){n=A.T,A.T=null;var r=j.p;j.p=2;var i=G;G|=4;try{sl(e,t.alternate,t)}finally{G=i,j.p=r,A.T=n}}iu=3}}function Bu(){if(iu===4||iu===3){iu=0,Oe();var e=au,t=ou,n=su,r=uu;t.subtreeFlags&10256||t.flags&10256?iu=5:(iu=0,ou=au=null,Vu(e,e.pendingLanes));var i=e.pendingLanes;if(i===0&&(ru=null),at(n),t=t.stateNode,ze&&typeof ze.onCommitFiberRoot==`function`)try{ze.onCommitFiberRoot(Re,t,void 0,(t.current.flags&128)==128)}catch{}if(r!==null){t=A.T,i=j.p,j.p=2,A.T=null;try{for(var a=e.onRecoverableError,o=0;o<r.length;o++){var s=r[o];a(s.value,{componentStack:s.stack})}}finally{A.T=t,j.p=i}}su&3&&Hu(),rd(e),i=e.pendingLanes,n&261930&&i&42?e===fu?du++:(du=0,fu=e):du=0,id(0,!1)}}function Vu(e,t){(e.pooledCacheLanes&=t)===0&&(t=e.pooledCache,t!=null&&(e.pooledCache=null,ca(t)))}function Hu(){return Ru(),zu(),Bu(),Uu()}function Uu(){if(iu!==5)return!1;var e=au,t=cu;cu=0;var n=at(su),r=A.T,a=j.p;try{j.p=32>n?32:n,A.T=null,n=lu,lu=null;var o=au,s=su;if(iu=0,ou=au=null,su=0,G&6)throw Error(i(331));var c=G;if(G|=4,Fl(o.current),Dl(o,o.current,s,n),G=c,id(0,!1),ze&&typeof ze.onPostCommitFiberRoot==`function`)try{ze.onPostCommitFiberRoot(Re,o)}catch{}return!0}finally{j.p=a,A.T=r,Vu(e,t)}}function Wu(e,t,n){t=yi(n,t),t=$s(e.stateNode,t,2),e=Ua(e,t,2),e!==null&&(et(e,2),rd(e))}function Z(e,t,n){if(e.tag===3)Wu(e,e,n);else for(;t!==null;){if(t.tag===3){Wu(t,e,n);break}else if(t.tag===1){var r=t.stateNode;if(typeof t.type.getDerivedStateFromError==`function`||typeof r.componentDidCatch==`function`&&(ru===null||!ru.has(r))){e=yi(n,e),n=ec(2),r=Ua(t,n,2),r!==null&&(tc(n,r,t,e),et(r,2),rd(r));break}}t=t.return}}function Gu(e,t,n){var r=e.pingCache;if(r===null){r=e.pingCache=new zl;var i=new Set;r.set(t,i)}else i=r.get(t),i===void 0&&(i=new Set,r.set(t,i));i.has(n)||(Ul=!0,i.add(n),e=Ku.bind(null,e,t,n),t.then(e,e))}function Ku(e,t,n){var r=e.pingCache;r!==null&&r.delete(t),e.pingedLanes|=e.suspendedLanes&n,e.warmLanes&=~n,K===e&&(J&n)===n&&(X===4||X===3&&(J&62914560)===J&&300>ke()-$l?!(G&2)&&Su(e,0):ql|=n,Yl===J&&(Yl=0)),rd(e)}function qu(e,t){t===0&&(t=Qe()),e=ai(e,t),e!==null&&(et(e,t),rd(e))}function Ju(e){var t=e.memoizedState,n=0;t!==null&&(n=t.retryLane),qu(e,n)}function Yu(e,t){var n=0;switch(e.tag){case 31:case 13:var r=e.stateNode,a=e.memoizedState;a!==null&&(n=a.retryLane);break;case 19:r=e.stateNode;break;case 22:r=e.stateNode._retryCache;break;default:throw Error(i(314))}r!==null&&r.delete(t),qu(e,n)}function Xu(e,t){return Te(e,t)}var Zu=null,Qu=null,$u=!1,ed=!1,td=!1,nd=0;function rd(e){e!==Qu&&e.next===null&&(Qu===null?Zu=Qu=e:Qu=Qu.next=e),ed=!0,$u||($u=!0,ud())}function id(e,t){if(!td&&ed){td=!0;do for(var n=!1,r=Zu;r!==null;){if(!t)if(e!==0){var i=r.pendingLanes;if(i===0)var a=0;else{var o=r.suspendedLanes,s=r.pingedLanes;a=(1<<31-Ve(42|e)+1)-1,a&=i&~(o&~s),a=a&201326741?a&201326741|1:a?a|2:0}a!==0&&(n=!0,ld(r,a))}else a=J,a=Ye(r,r===K?a:0,r.cancelPendingCommit!==null||r.timeoutHandle!==-1),!(a&3)||Xe(r,a)||(n=!0,ld(r,a));r=r.next}while(n);td=!1}}function ad(){od()}function od(){ed=$u=!1;var e=0;nd!==0&&Gd()&&(e=nd);for(var t=ke(),n=null,r=Zu;r!==null;){var i=r.next,a=sd(r,t);a===0?(r.next=null,n===null?Zu=i:n.next=i,i===null&&(Qu=n)):(n=r,(e!==0||a&3)&&(ed=!0)),r=i}iu!==0&&iu!==5||id(e,!1),nd!==0&&(nd=0)}function sd(e,t){for(var n=e.suspendedLanes,r=e.pingedLanes,i=e.expirationTimes,a=e.pendingLanes&-62914561;0<a;){var o=31-Ve(a),s=1<<o,c=i[o];c===-1?((s&n)===0||(s&r)!==0)&&(i[o]=Ze(s,t)):c<=t&&(e.expiredLanes|=s),a&=~s}if(t=K,n=J,n=Ye(e,e===t?n:0,e.cancelPendingCommit!==null||e.timeoutHandle!==-1),r=e.callbackNode,n===0||e===t&&(Y===2||Y===9)||e.cancelPendingCommit!==null)return r!==null&&r!==null&&Ee(r),e.callbackNode=null,e.callbackPriority=0;if(!(n&3)||Xe(e,n)){if(t=n&-n,t===e.callbackPriority)return t;switch(r!==null&&Ee(r),at(n)){case 2:case 8:n=Me;break;case 32:n=Ne;break;case 268435456:n=Fe;break;default:n=Ne}return r=cd.bind(null,e),n=Te(n,r),e.callbackPriority=t,e.callbackNode=n,t}return r!==null&&r!==null&&Ee(r),e.callbackPriority=2,e.callbackNode=null,2}function cd(e,t){if(iu!==0&&iu!==5)return e.callbackNode=null,e.callbackPriority=0,null;var n=e.callbackNode;if(Hu()&&e.callbackNode!==n)return null;var r=J;return r=Ye(e,e===K?r:0,e.cancelPendingCommit!==null||e.timeoutHandle!==-1),r===0?null:(gu(e,r,t),sd(e,ke()),e.callbackNode!=null&&e.callbackNode===n?cd.bind(null,e):null)}function ld(e,t){if(Hu())return null;gu(e,t,!0)}function ud(){Yd(function(){G&6?Te(je,ad):od()})}function dd(){if(nd===0){var e=da;e===0&&(e=Ge,Ge<<=1,!(Ge&261888)&&(Ge=256)),nd=e}return nd}function fd(e){return e==null||typeof e==`symbol`||typeof e==`boolean`?null:typeof e==`function`?e:tn(``+e)}function pd(e,t){var n=t.ownerDocument.createElement(`input`);return n.name=t.name,n.value=t.value,e.id&&n.setAttribute(`form`,e.id),t.parentNode.insertBefore(n,t),e=new FormData(e),n.parentNode.removeChild(n),e}function md(e,t,n,r,i){if(t===`submit`&&n&&n.stateNode===i){var a=fd((i[ut]||null).action),o=r.submitter;o&&(t=(t=o[ut]||null)?fd(t.formAction):o.getAttribute(`formAction`),t!==null&&(a=t,o=null));var s=new wn(`action`,`action`,null,r,i);e.push({event:s,listeners:[{instance:null,listener:function(){if(r.defaultPrevented){if(nd!==0){var e=o?pd(i,o):new FormData(i);Ts(n,{pending:!0,data:e,method:i.method,action:a},null,e)}}else typeof a==`function`&&(s.preventDefault(),e=o?pd(i,o):new FormData(i),Ts(n,{pending:!0,data:e,method:i.method,action:a},a,e))},currentTarget:i}]})}}for(var hd=0;hd<Xr.length;hd++){var gd=Xr[hd];Zr(gd.toLowerCase(),`on`+(gd[0].toUpperCase()+gd.slice(1)))}Zr(Hr,`onAnimationEnd`),Zr(Ur,`onAnimationIteration`),Zr(Wr,`onAnimationStart`),Zr(`dblclick`,`onDoubleClick`),Zr(`focusin`,`onFocus`),Zr(`focusout`,`onBlur`),Zr(Gr,`onTransitionRun`),Zr(Kr,`onTransitionStart`),Zr(qr,`onTransitionCancel`),Zr(Jr,`onTransitionEnd`),Et(`onMouseEnter`,[`mouseout`,`mouseover`]),Et(`onMouseLeave`,[`mouseout`,`mouseover`]),Et(`onPointerEnter`,[`pointerout`,`pointerover`]),Et(`onPointerLeave`,[`pointerout`,`pointerover`]),Tt(`onChange`,`change click focusin focusout input keydown keyup selectionchange`.split(` `)),Tt(`onSelect`,`focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange`.split(` `)),Tt(`onBeforeInput`,[`compositionend`,`keypress`,`textInput`,`paste`]),Tt(`onCompositionEnd`,`compositionend focusout keydown keypress keyup mousedown`.split(` `)),Tt(`onCompositionStart`,`compositionstart focusout keydown keypress keyup mousedown`.split(` `)),Tt(`onCompositionUpdate`,`compositionupdate focusout keydown keypress keyup mousedown`.split(` `));var _d=`abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting`.split(` `),vd=new Set(`beforetoggle cancel close invalid load scroll scrollend toggle`.split(` `).concat(_d));function yd(e,t){t=(t&4)!=0;for(var n=0;n<e.length;n++){var r=e[n],i=r.event;r=r.listeners;a:{var a=void 0;if(t)for(var o=r.length-1;0<=o;o--){var s=r[o],c=s.instance,l=s.currentTarget;if(s=s.listener,c!==a&&i.isPropagationStopped())break a;a=s,i.currentTarget=l;try{a(i)}catch(e){Qr(e)}i.currentTarget=null,a=c}else for(o=0;o<r.length;o++){if(s=r[o],c=s.instance,l=s.currentTarget,s=s.listener,c!==a&&i.isPropagationStopped())break a;a=s,i.currentTarget=l;try{a(i)}catch(e){Qr(e)}i.currentTarget=null,a=c}}}}function Q(e,t){var n=t[ft];n===void 0&&(n=t[ft]=new Set);var r=e+`__bubble`;n.has(r)||(Cd(t,e,2,!1),n.add(r))}function bd(e,t,n){var r=0;t&&(r|=4),Cd(n,e,r,t)}var xd=`_reactListening`+Math.random().toString(36).slice(2);function Sd(e){if(!e[xd]){e[xd]=!0,Ct.forEach(function(t){t!==`selectionchange`&&(vd.has(t)||bd(t,!1,e),bd(t,!0,e))});var t=e.nodeType===9?e:e.ownerDocument;t===null||t[xd]||(t[xd]=!0,bd(`selectionchange`,!1,t))}}function Cd(e,t,n,r){switch(mp(t)){case 2:var i=cp;break;case 8:i=lp;break;default:i=up}n=i.bind(null,t,n,e),i=void 0,!pn||t!==`touchstart`&&t!==`touchmove`&&t!==`wheel`||(i=!0),r?i===void 0?e.addEventListener(t,n,!0):e.addEventListener(t,n,{capture:!0,passive:i}):i===void 0?e.addEventListener(t,n,!1):e.addEventListener(t,n,{passive:i})}function wd(e,t,n,r,i){var a=r;if(!(t&1)&&!(t&2)&&r!==null)a:for(;;){if(r===null)return;var s=r.tag;if(s===3||s===4){var c=r.stateNode.containerInfo;if(c===i)break;if(s===4)for(s=r.return;s!==null;){var l=s.tag;if((l===3||l===4)&&s.stateNode.containerInfo===i)return;s=s.return}for(;c!==null;){if(s=vt(c),s===null)return;if(l=s.tag,l===5||l===6||l===26||l===27){r=a=s;continue a}c=c.parentNode}}r=r.return}un(function(){var r=a,i=an(n),s=[];a:{var c=Yr.get(e);if(c!==void 0){var l=wn,u=e;switch(e){case`keypress`:if(yn(n)===0)break a;case`keydown`:case`keyup`:l=Vn;break;case`focusin`:u=`focus`,l=Mn;break;case`focusout`:u=`blur`,l=Mn;break;case`beforeblur`:case`afterblur`:l=Mn;break;case`click`:if(n.button===2)break a;case`auxclick`:case`dblclick`:case`mousedown`:case`mousemove`:case`mouseup`:case`mouseout`:case`mouseover`:case`contextmenu`:l=jn;break;case`drag`:case`dragend`:case`dragenter`:case`dragexit`:case`dragleave`:case`dragover`:case`dragstart`:case`drop`:l=I;break;case`touchcancel`:case`touchend`:case`touchmove`:case`touchstart`:l=Un;break;case Hr:case Ur:case Wr:l=Nn;break;case Jr:l=Wn;break;case`scroll`:case`scrollend`:l=En;break;case`wheel`:l=Gn;break;case`copy`:case`cut`:case`paste`:l=Pn;break;case`gotpointercapture`:case`lostpointercapture`:case`pointercancel`:case`pointerdown`:case`pointermove`:case`pointerout`:case`pointerover`:case`pointerup`:l=Hn;break;case`toggle`:case`beforetoggle`:l=Kn}var d=(t&4)!=0,f=!d&&(e===`scroll`||e===`scrollend`),p=d?c===null?null:c+`Capture`:c;d=[];for(var m=r,h;m!==null;){var g=m;if(h=g.stateNode,g=g.tag,g!==5&&g!==26&&g!==27||h===null||p===null||(g=dn(m,p),g!=null&&d.push(Td(m,g,h))),f)break;m=m.return}0<d.length&&(c=new l(c,u,null,n,i),s.push({event:c,listeners:d}))}}if(!(t&7)){a:{if(c=e===`mouseover`||e===`pointerover`,l=e===`mouseout`||e===`pointerout`,c&&n!==rn&&(u=n.relatedTarget||n.fromElement)&&(vt(u)||u[dt]))break a;if((l||c)&&(c=i.window===i?i:(c=i.ownerDocument)?c.defaultView||c.parentWindow:window,l?(u=n.relatedTarget||n.toElement,l=r,u=u?vt(u):null,u!==null&&(f=o(u),d=u.tag,u!==f||d!==5&&d!==27&&d!==6)&&(u=null)):(l=null,u=r),l!==u)){if(d=jn,g=`onMouseLeave`,p=`onMouseEnter`,m=`mouse`,(e===`pointerout`||e===`pointerover`)&&(d=Hn,g=`onPointerLeave`,p=`onPointerEnter`,m=`pointer`),f=l==null?c:bt(l),h=u==null?c:bt(u),c=new d(g,m+`leave`,l,n,i),c.target=f,c.relatedTarget=h,g=null,vt(i)===r&&(d=new d(p,m+`enter`,u,n,i),d.target=h,d.relatedTarget=f,g=d),f=g,l&&u)b:{for(d=Dd,p=l,m=u,h=0,g=p;g;g=d(g))h++;g=0;for(var _=m;_;_=d(_))g++;for(;0<h-g;)p=d(p),h--;for(;0<g-h;)m=d(m),g--;for(;h--;){if(p===m||m!==null&&p===m.alternate){d=p;break b}p=d(p),m=d(m)}d=null}else d=null;l!==null&&Od(s,c,l,d,!1),u!==null&&f!==null&&Od(s,f,u,d,!0)}}a:{if(c=r?bt(r):window,l=c.nodeName&&c.nodeName.toLowerCase(),l===`select`||l===`input`&&c.type===`file`)var v=fr;else if(or(c))if(pr)v=Sr;else{v=br;var y=yr}else l=c.nodeName,!l||l.toLowerCase()!==`input`||c.type!==`checkbox`&&c.type!==`radio`?r&&Qt(r.elementType)&&(v=fr):v=xr;if(v&&=v(e,r)){sr(s,v,n,i);break a}y&&y(e,c,r),e===`focusout`&&r&&c.type===`number`&&r.memoizedProps.value!=null&&Wt(c,`number`,c.value)}switch(y=r?bt(r):window,e){case`focusin`:(or(y)||y.contentEditable===`true`)&&(Mr=y,Nr=r,Pr=null);break;case`focusout`:Pr=Nr=Mr=null;break;case`mousedown`:Fr=!0;break;case`contextmenu`:case`mouseup`:case`dragend`:Fr=!1,Ir(s,n,i);break;case`selectionchange`:if(jr)break;case`keydown`:case`keyup`:Ir(s,n,i)}var b;if(Jn)b:{switch(e){case`compositionstart`:var x=`onCompositionStart`;break b;case`compositionend`:x=`onCompositionEnd`;break b;case`compositionupdate`:x=`onCompositionUpdate`;break b}x=void 0}else nr?er(e,n)&&(x=`onCompositionEnd`):e===`keydown`&&n.keyCode===229&&(x=`onCompositionStart`);x&&(Zn&&n.locale!==`ko`&&(nr||x!==`onCompositionStart`?x===`onCompositionEnd`&&nr&&(b=vn()):(hn=i,gn=`value`in hn?hn.value:hn.textContent,nr=!0)),y=Ed(r,x),0<y.length&&(x=new Fn(x,e,null,n,i),s.push({event:x,listeners:y}),b?x.data=b:(b=tr(n),b!==null&&(x.data=b)))),(b=Xn?rr(e,n):ir(e,n))&&(x=Ed(r,`onBeforeInput`),0<x.length&&(y=new Fn(`onBeforeInput`,`beforeinput`,null,n,i),s.push({event:y,listeners:x}),y.data=b)),md(s,e,r,n,i)}yd(s,t)})}function Td(e,t,n){return{instance:e,listener:t,currentTarget:n}}function Ed(e,t){for(var n=t+`Capture`,r=[];e!==null;){var i=e,a=i.stateNode;if(i=i.tag,i!==5&&i!==26&&i!==27||a===null||(i=dn(e,n),i!=null&&r.unshift(Td(e,i,a)),i=dn(e,t),i!=null&&r.push(Td(e,i,a))),e.tag===3)return r;e=e.return}return[]}function Dd(e){if(e===null)return null;do e=e.return;while(e&&e.tag!==5&&e.tag!==27);return e||null}function Od(e,t,n,r,i){for(var a=t._reactName,o=[];n!==null&&n!==r;){var s=n,c=s.alternate,l=s.stateNode;if(s=s.tag,c!==null&&c===r)break;s!==5&&s!==26&&s!==27||l===null||(c=l,i?(l=dn(n,a),l!=null&&o.unshift(Td(n,l,c))):i||(l=dn(n,a),l!=null&&o.push(Td(n,l,c)))),n=n.return}o.length!==0&&e.push({event:t,listeners:o})}var kd=/\r\n?/g,Ad=/\u0000|\uFFFD/g;function jd(e){return(typeof e==`string`?e:``+e).replace(kd,`
`).replace(Ad,``)}function Md(e,t){return t=jd(t),jd(e)===t}function $(e,t,n,r,a,o){switch(n){case`children`:typeof r==`string`?t===`body`||t===`textarea`&&r===``||Jt(e,r):(typeof r==`number`||typeof r==`bigint`)&&t!==`body`&&Jt(e,``+r);break;case`className`:Mt(e,`class`,r);break;case`tabIndex`:Mt(e,`tabindex`,r);break;case`dir`:case`role`:case`viewBox`:case`width`:case`height`:Mt(e,n,r);break;case`style`:Zt(e,r,o);break;case`data`:if(t!==`object`){Mt(e,`data`,r);break}case`src`:case`href`:if(r===``&&(t!==`a`||n!==`href`)){e.removeAttribute(n);break}if(r==null||typeof r==`function`||typeof r==`symbol`||typeof r==`boolean`){e.removeAttribute(n);break}r=tn(``+r),e.setAttribute(n,r);break;case`action`:case`formAction`:if(typeof r==`function`){e.setAttribute(n,`javascript:throw new Error('A React form was unexpectedly submitted. If you called form.submit() manually, consider using form.requestSubmit() instead. If you\\'re trying to use event.stopPropagation() in a submit event handler, consider also calling event.preventDefault().')`);break}else typeof o==`function`&&(n===`formAction`?(t!==`input`&&$(e,t,`name`,a.name,a,null),$(e,t,`formEncType`,a.formEncType,a,null),$(e,t,`formMethod`,a.formMethod,a,null),$(e,t,`formTarget`,a.formTarget,a,null)):($(e,t,`encType`,a.encType,a,null),$(e,t,`method`,a.method,a,null),$(e,t,`target`,a.target,a,null)));if(r==null||typeof r==`symbol`||typeof r==`boolean`){e.removeAttribute(n);break}r=tn(``+r),e.setAttribute(n,r);break;case`onClick`:r!=null&&(e.onclick=nn);break;case`onScroll`:r!=null&&Q(`scroll`,e);break;case`onScrollEnd`:r!=null&&Q(`scrollend`,e);break;case`dangerouslySetInnerHTML`:if(r!=null){if(typeof r!=`object`||!(`__html`in r))throw Error(i(61));if(n=r.__html,n!=null){if(a.children!=null)throw Error(i(60));e.innerHTML=n}}break;case`multiple`:e.multiple=r&&typeof r!=`function`&&typeof r!=`symbol`;break;case`muted`:e.muted=r&&typeof r!=`function`&&typeof r!=`symbol`;break;case`suppressContentEditableWarning`:case`suppressHydrationWarning`:case`defaultValue`:case`defaultChecked`:case`innerHTML`:case`ref`:break;case`autoFocus`:break;case`xlinkHref`:if(r==null||typeof r==`function`||typeof r==`boolean`||typeof r==`symbol`){e.removeAttribute(`xlink:href`);break}n=tn(``+r),e.setAttributeNS(`http://www.w3.org/1999/xlink`,`xlink:href`,n);break;case`contentEditable`:case`spellCheck`:case`draggable`:case`value`:case`autoReverse`:case`externalResourcesRequired`:case`focusable`:case`preserveAlpha`:r!=null&&typeof r!=`function`&&typeof r!=`symbol`?e.setAttribute(n,``+r):e.removeAttribute(n);break;case`inert`:case`allowFullScreen`:case`async`:case`autoPlay`:case`controls`:case`default`:case`defer`:case`disabled`:case`disablePictureInPicture`:case`disableRemotePlayback`:case`formNoValidate`:case`hidden`:case`loop`:case`noModule`:case`noValidate`:case`open`:case`playsInline`:case`readOnly`:case`required`:case`reversed`:case`scoped`:case`seamless`:case`itemScope`:r&&typeof r!=`function`&&typeof r!=`symbol`?e.setAttribute(n,``):e.removeAttribute(n);break;case`capture`:case`download`:!0===r?e.setAttribute(n,``):!1!==r&&r!=null&&typeof r!=`function`&&typeof r!=`symbol`?e.setAttribute(n,r):e.removeAttribute(n);break;case`cols`:case`rows`:case`size`:case`span`:r!=null&&typeof r!=`function`&&typeof r!=`symbol`&&!isNaN(r)&&1<=r?e.setAttribute(n,r):e.removeAttribute(n);break;case`rowSpan`:case`start`:r==null||typeof r==`function`||typeof r==`symbol`||isNaN(r)?e.removeAttribute(n):e.setAttribute(n,r);break;case`popover`:Q(`beforetoggle`,e),Q(`toggle`,e),jt(e,`popover`,r);break;case`xlinkActuate`:Nt(e,`http://www.w3.org/1999/xlink`,`xlink:actuate`,r);break;case`xlinkArcrole`:Nt(e,`http://www.w3.org/1999/xlink`,`xlink:arcrole`,r);break;case`xlinkRole`:Nt(e,`http://www.w3.org/1999/xlink`,`xlink:role`,r);break;case`xlinkShow`:Nt(e,`http://www.w3.org/1999/xlink`,`xlink:show`,r);break;case`xlinkTitle`:Nt(e,`http://www.w3.org/1999/xlink`,`xlink:title`,r);break;case`xlinkType`:Nt(e,`http://www.w3.org/1999/xlink`,`xlink:type`,r);break;case`xmlBase`:Nt(e,`http://www.w3.org/XML/1998/namespace`,`xml:base`,r);break;case`xmlLang`:Nt(e,`http://www.w3.org/XML/1998/namespace`,`xml:lang`,r);break;case`xmlSpace`:Nt(e,`http://www.w3.org/XML/1998/namespace`,`xml:space`,r);break;case`is`:jt(e,`is`,r);break;case`innerText`:case`textContent`:break;default:(!(2<n.length)||n[0]!==`o`&&n[0]!==`O`||n[1]!==`n`&&n[1]!==`N`)&&(n=$t.get(n)||n,jt(e,n,r))}}function Nd(e,t,n,r,a,o){switch(n){case`style`:Zt(e,r,o);break;case`dangerouslySetInnerHTML`:if(r!=null){if(typeof r!=`object`||!(`__html`in r))throw Error(i(61));if(n=r.__html,n!=null){if(a.children!=null)throw Error(i(60));e.innerHTML=n}}break;case`children`:typeof r==`string`?Jt(e,r):(typeof r==`number`||typeof r==`bigint`)&&Jt(e,``+r);break;case`onScroll`:r!=null&&Q(`scroll`,e);break;case`onScrollEnd`:r!=null&&Q(`scrollend`,e);break;case`onClick`:r!=null&&(e.onclick=nn);break;case`suppressContentEditableWarning`:case`suppressHydrationWarning`:case`innerHTML`:case`ref`:break;case`innerText`:case`textContent`:break;default:if(!wt.hasOwnProperty(n))a:{if(n[0]===`o`&&n[1]===`n`&&(a=n.endsWith(`Capture`),t=n.slice(2,a?n.length-7:void 0),o=e[ut]||null,o=o==null?null:o[n],typeof o==`function`&&e.removeEventListener(t,o,a),typeof r==`function`)){typeof o!=`function`&&o!==null&&(n in e?e[n]=null:e.hasAttribute(n)&&e.removeAttribute(n)),e.addEventListener(t,r,a);break a}n in e?e[n]=r:!0===r?e.setAttribute(n,``):jt(e,n,r)}}}function Pd(e,t,n){switch(t){case`div`:case`span`:case`svg`:case`path`:case`a`:case`g`:case`p`:case`li`:break;case`img`:Q(`error`,e),Q(`load`,e);var r=!1,a=!1,o;for(o in n)if(n.hasOwnProperty(o)){var s=n[o];if(s!=null)switch(o){case`src`:r=!0;break;case`srcSet`:a=!0;break;case`children`:case`dangerouslySetInnerHTML`:throw Error(i(137,t));default:$(e,t,o,s,n,null)}}a&&$(e,t,`srcSet`,n.srcSet,n,null),r&&$(e,t,`src`,n.src,n,null);return;case`input`:Q(`invalid`,e);var c=o=s=a=null,l=null,u=null;for(r in n)if(n.hasOwnProperty(r)){var d=n[r];if(d!=null)switch(r){case`name`:a=d;break;case`type`:s=d;break;case`checked`:l=d;break;case`defaultChecked`:u=d;break;case`value`:o=d;break;case`defaultValue`:c=d;break;case`children`:case`dangerouslySetInnerHTML`:if(d!=null)throw Error(i(137,t));break;default:$(e,t,r,d,n,null)}}Ut(e,o,c,l,u,s,a,!1);return;case`select`:for(a in Q(`invalid`,e),r=s=o=null,n)if(n.hasOwnProperty(a)&&(c=n[a],c!=null))switch(a){case`value`:o=c;break;case`defaultValue`:s=c;break;case`multiple`:r=c;default:$(e,t,a,c,n,null)}t=o,n=s,e.multiple=!!r,t==null?n!=null&&Gt(e,!!r,n,!0):Gt(e,!!r,t,!1);return;case`textarea`:for(s in Q(`invalid`,e),o=a=r=null,n)if(n.hasOwnProperty(s)&&(c=n[s],c!=null))switch(s){case`value`:r=c;break;case`defaultValue`:a=c;break;case`children`:o=c;break;case`dangerouslySetInnerHTML`:if(c!=null)throw Error(i(91));break;default:$(e,t,s,c,n,null)}qt(e,r,a,o);return;case`option`:for(l in n)if(n.hasOwnProperty(l)&&(r=n[l],r!=null))switch(l){case`selected`:e.selected=r&&typeof r!=`function`&&typeof r!=`symbol`;break;default:$(e,t,l,r,n,null)}return;case`dialog`:Q(`beforetoggle`,e),Q(`toggle`,e),Q(`cancel`,e),Q(`close`,e);break;case`iframe`:case`object`:Q(`load`,e);break;case`video`:case`audio`:for(r=0;r<_d.length;r++)Q(_d[r],e);break;case`image`:Q(`error`,e),Q(`load`,e);break;case`details`:Q(`toggle`,e);break;case`embed`:case`source`:case`link`:Q(`error`,e),Q(`load`,e);case`area`:case`base`:case`br`:case`col`:case`hr`:case`keygen`:case`meta`:case`param`:case`track`:case`wbr`:case`menuitem`:for(u in n)if(n.hasOwnProperty(u)&&(r=n[u],r!=null))switch(u){case`children`:case`dangerouslySetInnerHTML`:throw Error(i(137,t));default:$(e,t,u,r,n,null)}return;default:if(Qt(t)){for(d in n)n.hasOwnProperty(d)&&(r=n[d],r!==void 0&&Nd(e,t,d,r,n,void 0));return}}for(c in n)n.hasOwnProperty(c)&&(r=n[c],r!=null&&$(e,t,c,r,n,null))}function Fd(e,t,n,r){switch(t){case`div`:case`span`:case`svg`:case`path`:case`a`:case`g`:case`p`:case`li`:break;case`input`:var a=null,o=null,s=null,c=null,l=null,u=null,d=null;for(m in n){var f=n[m];if(n.hasOwnProperty(m)&&f!=null)switch(m){case`checked`:break;case`value`:break;case`defaultValue`:l=f;default:r.hasOwnProperty(m)||$(e,t,m,null,r,f)}}for(var p in r){var m=r[p];if(f=n[p],r.hasOwnProperty(p)&&(m!=null||f!=null))switch(p){case`type`:o=m;break;case`name`:a=m;break;case`checked`:u=m;break;case`defaultChecked`:d=m;break;case`value`:s=m;break;case`defaultValue`:c=m;break;case`children`:case`dangerouslySetInnerHTML`:if(m!=null)throw Error(i(137,t));break;default:m!==f&&$(e,t,p,m,r,f)}}Ht(e,s,c,l,u,d,o,a);return;case`select`:for(o in m=s=c=p=null,n)if(l=n[o],n.hasOwnProperty(o)&&l!=null)switch(o){case`value`:break;case`multiple`:m=l;default:r.hasOwnProperty(o)||$(e,t,o,null,r,l)}for(a in r)if(o=r[a],l=n[a],r.hasOwnProperty(a)&&(o!=null||l!=null))switch(a){case`value`:p=o;break;case`defaultValue`:c=o;break;case`multiple`:s=o;default:o!==l&&$(e,t,a,o,r,l)}t=c,n=s,r=m,p==null?!!r!=!!n&&(t==null?Gt(e,!!n,n?[]:``,!1):Gt(e,!!n,t,!0)):Gt(e,!!n,p,!1);return;case`textarea`:for(c in m=p=null,n)if(a=n[c],n.hasOwnProperty(c)&&a!=null&&!r.hasOwnProperty(c))switch(c){case`value`:break;case`children`:break;default:$(e,t,c,null,r,a)}for(s in r)if(a=r[s],o=n[s],r.hasOwnProperty(s)&&(a!=null||o!=null))switch(s){case`value`:p=a;break;case`defaultValue`:m=a;break;case`children`:break;case`dangerouslySetInnerHTML`:if(a!=null)throw Error(i(91));break;default:a!==o&&$(e,t,s,a,r,o)}Kt(e,p,m);return;case`option`:for(var h in n)if(p=n[h],n.hasOwnProperty(h)&&p!=null&&!r.hasOwnProperty(h))switch(h){case`selected`:e.selected=!1;break;default:$(e,t,h,null,r,p)}for(l in r)if(p=r[l],m=n[l],r.hasOwnProperty(l)&&p!==m&&(p!=null||m!=null))switch(l){case`selected`:e.selected=p&&typeof p!=`function`&&typeof p!=`symbol`;break;default:$(e,t,l,p,r,m)}return;case`img`:case`link`:case`area`:case`base`:case`br`:case`col`:case`embed`:case`hr`:case`keygen`:case`meta`:case`param`:case`source`:case`track`:case`wbr`:case`menuitem`:for(var g in n)p=n[g],n.hasOwnProperty(g)&&p!=null&&!r.hasOwnProperty(g)&&$(e,t,g,null,r,p);for(u in r)if(p=r[u],m=n[u],r.hasOwnProperty(u)&&p!==m&&(p!=null||m!=null))switch(u){case`children`:case`dangerouslySetInnerHTML`:if(p!=null)throw Error(i(137,t));break;default:$(e,t,u,p,r,m)}return;default:if(Qt(t)){for(var _ in n)p=n[_],n.hasOwnProperty(_)&&p!==void 0&&!r.hasOwnProperty(_)&&Nd(e,t,_,void 0,r,p);for(d in r)p=r[d],m=n[d],!r.hasOwnProperty(d)||p===m||p===void 0&&m===void 0||Nd(e,t,d,p,r,m);return}}for(var v in n)p=n[v],n.hasOwnProperty(v)&&p!=null&&!r.hasOwnProperty(v)&&$(e,t,v,null,r,p);for(f in r)p=r[f],m=n[f],!r.hasOwnProperty(f)||p===m||p==null&&m==null||$(e,t,f,p,r,m)}function Id(e){switch(e){case`css`:case`script`:case`font`:case`img`:case`image`:case`input`:case`link`:return!0;default:return!1}}function Ld(){if(typeof performance.getEntriesByType==`function`){for(var e=0,t=0,n=performance.getEntriesByType(`resource`),r=0;r<n.length;r++){var i=n[r],a=i.transferSize,o=i.initiatorType,s=i.duration;if(a&&s&&Id(o)){for(o=0,s=i.responseEnd,r+=1;r<n.length;r++){var c=n[r],l=c.startTime;if(l>s)break;var u=c.transferSize,d=c.initiatorType;u&&Id(d)&&(c=c.responseEnd,o+=u*(c<s?1:(s-l)/(c-l)))}if(--r,t+=8*(a+o)/(i.duration/1e3),e++,10<e)break}}if(0<e)return t/e/1e6}return navigator.connection&&(e=navigator.connection.downlink,typeof e==`number`)?e:5}var Rd=null,zd=null;function Bd(e){return e.nodeType===9?e:e.ownerDocument}function Vd(e){switch(e){case`http://www.w3.org/2000/svg`:return 1;case`http://www.w3.org/1998/Math/MathML`:return 2;default:return 0}}function Hd(e,t){if(e===0)switch(t){case`svg`:return 1;case`math`:return 2;default:return 0}return e===1&&t===`foreignObject`?0:e}function Ud(e,t){return e===`textarea`||e===`noscript`||typeof t.children==`string`||typeof t.children==`number`||typeof t.children==`bigint`||typeof t.dangerouslySetInnerHTML==`object`&&t.dangerouslySetInnerHTML!==null&&t.dangerouslySetInnerHTML.__html!=null}var Wd=null;function Gd(){var e=window.event;return e&&e.type===`popstate`?e===Wd?!1:(Wd=e,!0):(Wd=null,!1)}var Kd=typeof setTimeout==`function`?setTimeout:void 0,qd=typeof clearTimeout==`function`?clearTimeout:void 0,Jd=typeof Promise==`function`?Promise:void 0,Yd=typeof queueMicrotask==`function`?queueMicrotask:Jd===void 0?Kd:function(e){return Jd.resolve(null).then(e).catch(Xd)};function Xd(e){setTimeout(function(){throw e})}function Zd(e){return e===`head`}function Qd(e,t){var n=t,r=0;do{var i=n.nextSibling;if(e.removeChild(n),i&&i.nodeType===8)if(n=i.data,n===`/$`||n===`/&`){if(r===0){e.removeChild(i),Np(t);return}r--}else if(n===`$`||n===`$?`||n===`$~`||n===`$!`||n===`&`)r++;else if(n===`html`)pf(e.ownerDocument.documentElement);else if(n===`head`){n=e.ownerDocument.head,pf(n);for(var a=n.firstChild;a;){var o=a.nextSibling,s=a.nodeName;a[gt]||s===`SCRIPT`||s===`STYLE`||s===`LINK`&&a.rel.toLowerCase()===`stylesheet`||n.removeChild(a),a=o}}else n===`body`&&pf(e.ownerDocument.body);n=i}while(n);Np(t)}function $d(e,t){var n=e;e=0;do{var r=n.nextSibling;if(n.nodeType===1?t?(n._stashedDisplay=n.style.display,n.style.display=`none`):(n.style.display=n._stashedDisplay||``,n.getAttribute(`style`)===``&&n.removeAttribute(`style`)):n.nodeType===3&&(t?(n._stashedText=n.nodeValue,n.nodeValue=``):n.nodeValue=n._stashedText||``),r&&r.nodeType===8)if(n=r.data,n===`/$`){if(e===0)break;e--}else n!==`$`&&n!==`$?`&&n!==`$~`&&n!==`$!`||e++;n=r}while(n)}function ef(e){var t=e.firstChild;for(t&&t.nodeType===10&&(t=t.nextSibling);t;){var n=t;switch(t=t.nextSibling,n.nodeName){case`HTML`:case`HEAD`:case`BODY`:ef(n),_t(n);continue;case`SCRIPT`:case`STYLE`:continue;case`LINK`:if(n.rel.toLowerCase()===`stylesheet`)continue}e.removeChild(n)}}function tf(e,t,n,r){for(;e.nodeType===1;){var i=n;if(e.nodeName.toLowerCase()!==t.toLowerCase()){if(!r&&(e.nodeName!==`INPUT`||e.type!==`hidden`))break}else if(!r)if(t===`input`&&e.type===`hidden`){var a=i.name==null?null:``+i.name;if(i.type===`hidden`&&e.getAttribute(`name`)===a)return e}else return e;else if(!e[gt])switch(t){case`meta`:if(!e.hasAttribute(`itemprop`))break;return e;case`link`:if(a=e.getAttribute(`rel`),a===`stylesheet`&&e.hasAttribute(`data-precedence`)||a!==i.rel||e.getAttribute(`href`)!==(i.href==null||i.href===``?null:i.href)||e.getAttribute(`crossorigin`)!==(i.crossOrigin==null?null:i.crossOrigin)||e.getAttribute(`title`)!==(i.title==null?null:i.title))break;return e;case`style`:if(e.hasAttribute(`data-precedence`))break;return e;case`script`:if(a=e.getAttribute(`src`),(a!==(i.src==null?null:i.src)||e.getAttribute(`type`)!==(i.type==null?null:i.type)||e.getAttribute(`crossorigin`)!==(i.crossOrigin==null?null:i.crossOrigin))&&a&&e.hasAttribute(`async`)&&!e.hasAttribute(`itemprop`))break;return e;default:return e}if(e=cf(e.nextSibling),e===null)break}return null}function nf(e,t,n){if(t===``)return null;for(;e.nodeType!==3;)if((e.nodeType!==1||e.nodeName!==`INPUT`||e.type!==`hidden`)&&!n||(e=cf(e.nextSibling),e===null))return null;return e}function rf(e,t){for(;e.nodeType!==8;)if((e.nodeType!==1||e.nodeName!==`INPUT`||e.type!==`hidden`)&&!t||(e=cf(e.nextSibling),e===null))return null;return e}function af(e){return e.data===`$?`||e.data===`$~`}function of(e){return e.data===`$!`||e.data===`$?`&&e.ownerDocument.readyState!==`loading`}function sf(e,t){var n=e.ownerDocument;if(e.data===`$~`)e._reactRetry=t;else if(e.data!==`$?`||n.readyState!==`loading`)t();else{var r=function(){t(),n.removeEventListener(`DOMContentLoaded`,r)};n.addEventListener(`DOMContentLoaded`,r),e._reactRetry=r}}function cf(e){for(;e!=null;e=e.nextSibling){var t=e.nodeType;if(t===1||t===3)break;if(t===8){if(t=e.data,t===`$`||t===`$!`||t===`$?`||t===`$~`||t===`&`||t===`F!`||t===`F`)break;if(t===`/$`||t===`/&`)return null}}return e}var lf=null;function uf(e){e=e.nextSibling;for(var t=0;e;){if(e.nodeType===8){var n=e.data;if(n===`/$`||n===`/&`){if(t===0)return cf(e.nextSibling);t--}else n!==`$`&&n!==`$!`&&n!==`$?`&&n!==`$~`&&n!==`&`||t++}e=e.nextSibling}return null}function df(e){e=e.previousSibling;for(var t=0;e;){if(e.nodeType===8){var n=e.data;if(n===`$`||n===`$!`||n===`$?`||n===`$~`||n===`&`){if(t===0)return e;t--}else n!==`/$`&&n!==`/&`||t++}e=e.previousSibling}return null}function ff(e,t,n){switch(t=Bd(n),e){case`html`:if(e=t.documentElement,!e)throw Error(i(452));return e;case`head`:if(e=t.head,!e)throw Error(i(453));return e;case`body`:if(e=t.body,!e)throw Error(i(454));return e;default:throw Error(i(451))}}function pf(e){for(var t=e.attributes;t.length;)e.removeAttributeNode(t[0]);_t(e)}var mf=new Map,hf=new Set;function gf(e){return typeof e.getRootNode==`function`?e.getRootNode():e.nodeType===9?e:e.ownerDocument}var _f=j.d;j.d={f:vf,r:yf,D:Sf,C:Cf,L:wf,m:Tf,X:Df,S:Ef,M:Of};function vf(){var e=_f.f(),t=bu();return e||t}function yf(e){var t=yt(e);t!==null&&t.tag===5&&t.type===`form`?Ds(t):_f.r(e)}var bf=typeof document>`u`?null:document;function xf(e,t,n){var r=bf;if(r&&typeof t==`string`&&t){var i=Vt(t);i=`link[rel="`+e+`"][href="`+i+`"]`,typeof n==`string`&&(i+=`[crossorigin="`+n+`"]`),hf.has(i)||(hf.add(i),e={rel:e,crossOrigin:n,href:t},r.querySelector(i)===null&&(t=r.createElement(`link`),Pd(t,`link`,e),St(t),r.head.appendChild(t)))}}function Sf(e){_f.D(e),xf(`dns-prefetch`,e,null)}function Cf(e,t){_f.C(e,t),xf(`preconnect`,e,t)}function wf(e,t,n){_f.L(e,t,n);var r=bf;if(r&&e&&t){var i=`link[rel="preload"][as="`+Vt(t)+`"]`;t===`image`&&n&&n.imageSrcSet?(i+=`[imagesrcset="`+Vt(n.imageSrcSet)+`"]`,typeof n.imageSizes==`string`&&(i+=`[imagesizes="`+Vt(n.imageSizes)+`"]`)):i+=`[href="`+Vt(e)+`"]`;var a=i;switch(t){case`style`:a=Af(e);break;case`script`:a=Pf(e)}mf.has(a)||(e=h({rel:`preload`,href:t===`image`&&n&&n.imageSrcSet?void 0:e,as:t},n),mf.set(a,e),r.querySelector(i)!==null||t===`style`&&r.querySelector(jf(a))||t===`script`&&r.querySelector(Ff(a))||(t=r.createElement(`link`),Pd(t,`link`,e),St(t),r.head.appendChild(t)))}}function Tf(e,t){_f.m(e,t);var n=bf;if(n&&e){var r=t&&typeof t.as==`string`?t.as:`script`,i=`link[rel="modulepreload"][as="`+Vt(r)+`"][href="`+Vt(e)+`"]`,a=i;switch(r){case`audioworklet`:case`paintworklet`:case`serviceworker`:case`sharedworker`:case`worker`:case`script`:a=Pf(e)}if(!mf.has(a)&&(e=h({rel:`modulepreload`,href:e},t),mf.set(a,e),n.querySelector(i)===null)){switch(r){case`audioworklet`:case`paintworklet`:case`serviceworker`:case`sharedworker`:case`worker`:case`script`:if(n.querySelector(Ff(a)))return}r=n.createElement(`link`),Pd(r,`link`,e),St(r),n.head.appendChild(r)}}}function Ef(e,t,n){_f.S(e,t,n);var r=bf;if(r&&e){var i=xt(r).hoistableStyles,a=Af(e);t||=`default`;var o=i.get(a);if(!o){var s={loading:0,preload:null};if(o=r.querySelector(jf(a)))s.loading=5;else{e=h({rel:`stylesheet`,href:e,"data-precedence":t},n),(n=mf.get(a))&&Rf(e,n);var c=o=r.createElement(`link`);St(c),Pd(c,`link`,e),c._p=new Promise(function(e,t){c.onload=e,c.onerror=t}),c.addEventListener(`load`,function(){s.loading|=1}),c.addEventListener(`error`,function(){s.loading|=2}),s.loading|=4,Lf(o,t,r)}o={type:`stylesheet`,instance:o,count:1,state:s},i.set(a,o)}}}function Df(e,t){_f.X(e,t);var n=bf;if(n&&e){var r=xt(n).hoistableScripts,i=Pf(e),a=r.get(i);a||(a=n.querySelector(Ff(i)),a||(e=h({src:e,async:!0},t),(t=mf.get(i))&&zf(e,t),a=n.createElement(`script`),St(a),Pd(a,`link`,e),n.head.appendChild(a)),a={type:`script`,instance:a,count:1,state:null},r.set(i,a))}}function Of(e,t){_f.M(e,t);var n=bf;if(n&&e){var r=xt(n).hoistableScripts,i=Pf(e),a=r.get(i);a||(a=n.querySelector(Ff(i)),a||(e=h({src:e,async:!0,type:`module`},t),(t=mf.get(i))&&zf(e,t),a=n.createElement(`script`),St(a),Pd(a,`link`,e),n.head.appendChild(a)),a={type:`script`,instance:a,count:1,state:null},r.set(i,a))}}function kf(e,t,n,r){var a=(a=pe.current)?gf(a):null;if(!a)throw Error(i(446));switch(e){case`meta`:case`title`:return null;case`style`:return typeof n.precedence==`string`&&typeof n.href==`string`?(t=Af(n.href),n=xt(a).hoistableStyles,r=n.get(t),r||(r={type:`style`,instance:null,count:0,state:null},n.set(t,r)),r):{type:`void`,instance:null,count:0,state:null};case`link`:if(n.rel===`stylesheet`&&typeof n.href==`string`&&typeof n.precedence==`string`){e=Af(n.href);var o=xt(a).hoistableStyles,s=o.get(e);if(s||(a=a.ownerDocument||a,s={type:`stylesheet`,instance:null,count:0,state:{loading:0,preload:null}},o.set(e,s),(o=a.querySelector(jf(e)))&&!o._p&&(s.instance=o,s.state.loading=5),mf.has(e)||(n={rel:`preload`,as:`style`,href:n.href,crossOrigin:n.crossOrigin,integrity:n.integrity,media:n.media,hrefLang:n.hrefLang,referrerPolicy:n.referrerPolicy},mf.set(e,n),o||Nf(a,e,n,s.state))),t&&r===null)throw Error(i(528,``));return s}if(t&&r!==null)throw Error(i(529,``));return null;case`script`:return t=n.async,n=n.src,typeof n==`string`&&t&&typeof t!=`function`&&typeof t!=`symbol`?(t=Pf(n),n=xt(a).hoistableScripts,r=n.get(t),r||(r={type:`script`,instance:null,count:0,state:null},n.set(t,r)),r):{type:`void`,instance:null,count:0,state:null};default:throw Error(i(444,e))}}function Af(e){return`href="`+Vt(e)+`"`}function jf(e){return`link[rel="stylesheet"][`+e+`]`}function Mf(e){return h({},e,{"data-precedence":e.precedence,precedence:null})}function Nf(e,t,n,r){e.querySelector(`link[rel="preload"][as="style"][`+t+`]`)?r.loading=1:(t=e.createElement(`link`),r.preload=t,t.addEventListener(`load`,function(){return r.loading|=1}),t.addEventListener(`error`,function(){return r.loading|=2}),Pd(t,`link`,n),St(t),e.head.appendChild(t))}function Pf(e){return`[src="`+Vt(e)+`"]`}function Ff(e){return`script[async]`+e}function If(e,t,n){if(t.count++,t.instance===null)switch(t.type){case`style`:var r=e.querySelector(`style[data-href~="`+Vt(n.href)+`"]`);if(r)return t.instance=r,St(r),r;var a=h({},n,{"data-href":n.href,"data-precedence":n.precedence,href:null,precedence:null});return r=(e.ownerDocument||e).createElement(`style`),St(r),Pd(r,`style`,a),Lf(r,n.precedence,e),t.instance=r;case`stylesheet`:a=Af(n.href);var o=e.querySelector(jf(a));if(o)return t.state.loading|=4,t.instance=o,St(o),o;r=Mf(n),(a=mf.get(a))&&Rf(r,a),o=(e.ownerDocument||e).createElement(`link`),St(o);var s=o;return s._p=new Promise(function(e,t){s.onload=e,s.onerror=t}),Pd(o,`link`,r),t.state.loading|=4,Lf(o,n.precedence,e),t.instance=o;case`script`:return o=Pf(n.src),(a=e.querySelector(Ff(o)))?(t.instance=a,St(a),a):(r=n,(a=mf.get(o))&&(r=h({},n),zf(r,a)),e=e.ownerDocument||e,a=e.createElement(`script`),St(a),Pd(a,`link`,r),e.head.appendChild(a),t.instance=a);case`void`:return null;default:throw Error(i(443,t.type))}else t.type===`stylesheet`&&!(t.state.loading&4)&&(r=t.instance,t.state.loading|=4,Lf(r,n.precedence,e));return t.instance}function Lf(e,t,n){for(var r=n.querySelectorAll(`link[rel="stylesheet"][data-precedence],style[data-precedence]`),i=r.length?r[r.length-1]:null,a=i,o=0;o<r.length;o++){var s=r[o];if(s.dataset.precedence===t)a=s;else if(a!==i)break}a?a.parentNode.insertBefore(e,a.nextSibling):(t=n.nodeType===9?n.head:n,t.insertBefore(e,t.firstChild))}function Rf(e,t){e.crossOrigin??=t.crossOrigin,e.referrerPolicy??=t.referrerPolicy,e.title??=t.title}function zf(e,t){e.crossOrigin??=t.crossOrigin,e.referrerPolicy??=t.referrerPolicy,e.integrity??=t.integrity}var Bf=null;function Vf(e,t,n){if(Bf===null){var r=new Map,i=Bf=new Map;i.set(n,r)}else i=Bf,r=i.get(n),r||(r=new Map,i.set(n,r));if(r.has(e))return r;for(r.set(e,null),n=n.getElementsByTagName(e),i=0;i<n.length;i++){var a=n[i];if(!(a[gt]||a[lt]||e===`link`&&a.getAttribute(`rel`)===`stylesheet`)&&a.namespaceURI!==`http://www.w3.org/2000/svg`){var o=a.getAttribute(t)||``;o=e+o;var s=r.get(o);s?s.push(a):r.set(o,[a])}}return r}function Hf(e,t,n){e=e.ownerDocument||e,e.head.insertBefore(n,t===`title`?e.querySelector(`head > title`):null)}function Uf(e,t,n){if(n===1||t.itemProp!=null)return!1;switch(e){case`meta`:case`title`:return!0;case`style`:if(typeof t.precedence!=`string`||typeof t.href!=`string`||t.href===``)break;return!0;case`link`:if(typeof t.rel!=`string`||typeof t.href!=`string`||t.href===``||t.onLoad||t.onError)break;switch(t.rel){case`stylesheet`:return e=t.disabled,typeof t.precedence==`string`&&e==null;default:return!0}case`script`:if(t.async&&typeof t.async!=`function`&&typeof t.async!=`symbol`&&!t.onLoad&&!t.onError&&t.src&&typeof t.src==`string`)return!0}return!1}function Wf(e){return!(e.type===`stylesheet`&&!(e.state.loading&3))}function Gf(e,t,n,r){if(n.type===`stylesheet`&&(typeof r.media!=`string`||!1!==matchMedia(r.media).matches)&&!(n.state.loading&4)){if(n.instance===null){var i=Af(r.href),a=t.querySelector(jf(i));if(a){t=a._p,typeof t==`object`&&t&&typeof t.then==`function`&&(e.count++,e=Jf.bind(e),t.then(e,e)),n.state.loading|=4,n.instance=a,St(a);return}a=t.ownerDocument||t,r=Mf(r),(i=mf.get(i))&&Rf(r,i),a=a.createElement(`link`),St(a);var o=a;o._p=new Promise(function(e,t){o.onload=e,o.onerror=t}),Pd(a,`link`,r),n.instance=a}e.stylesheets===null&&(e.stylesheets=new Map),e.stylesheets.set(n,t),(t=n.state.preload)&&!(n.state.loading&3)&&(e.count++,n=Jf.bind(e),t.addEventListener(`load`,n),t.addEventListener(`error`,n))}}var Kf=0;function qf(e,t){return e.stylesheets&&e.count===0&&Xf(e,e.stylesheets),0<e.count||0<e.imgCount?function(n){var r=setTimeout(function(){if(e.stylesheets&&Xf(e,e.stylesheets),e.unsuspend){var t=e.unsuspend;e.unsuspend=null,t()}},6e4+t);0<e.imgBytes&&Kf===0&&(Kf=62500*Ld());var i=setTimeout(function(){if(e.waitingForImages=!1,e.count===0&&(e.stylesheets&&Xf(e,e.stylesheets),e.unsuspend)){var t=e.unsuspend;e.unsuspend=null,t()}},(e.imgBytes>Kf?50:800)+t);return e.unsuspend=n,function(){e.unsuspend=null,clearTimeout(r),clearTimeout(i)}}:null}function Jf(){if(this.count--,this.count===0&&(this.imgCount===0||!this.waitingForImages)){if(this.stylesheets)Xf(this,this.stylesheets);else if(this.unsuspend){var e=this.unsuspend;this.unsuspend=null,e()}}}var Yf=null;function Xf(e,t){e.stylesheets=null,e.unsuspend!==null&&(e.count++,Yf=new Map,t.forEach(Zf,e),Yf=null,Jf.call(e))}function Zf(e,t){if(!(t.state.loading&4)){var n=Yf.get(e);if(n)var r=n.get(null);else{n=new Map,Yf.set(e,n);for(var i=e.querySelectorAll(`link[data-precedence],style[data-precedence]`),a=0;a<i.length;a++){var o=i[a];(o.nodeName===`LINK`||o.getAttribute(`media`)!==`not all`)&&(n.set(o.dataset.precedence,o),r=o)}r&&n.set(null,r)}i=t.instance,o=i.getAttribute(`data-precedence`),a=n.get(o)||r,a===r&&n.set(null,i),n.set(o,i),this.count++,r=Jf.bind(this),i.addEventListener(`load`,r),i.addEventListener(`error`,r),a?a.parentNode.insertBefore(i,a.nextSibling):(e=e.nodeType===9?e.head:e,e.insertBefore(i,e.firstChild)),t.state.loading|=4}}var Qf={$$typeof:S,Provider:null,Consumer:null,_currentValue:oe,_currentValue2:oe,_threadCount:0};function $f(e,t,n,r,i,a,o,s,c){this.tag=1,this.containerInfo=e,this.pingCache=this.current=this.pendingChildren=null,this.timeoutHandle=-1,this.callbackNode=this.next=this.pendingContext=this.context=this.cancelPendingCommit=null,this.callbackPriority=0,this.expirationTimes=$e(-1),this.entangledLanes=this.shellSuspendCounter=this.errorRecoveryDisabledLanes=this.expiredLanes=this.warmLanes=this.pingedLanes=this.suspendedLanes=this.pendingLanes=0,this.entanglements=$e(0),this.hiddenUpdates=$e(null),this.identifierPrefix=r,this.onUncaughtError=i,this.onCaughtError=a,this.onRecoverableError=o,this.pooledCache=null,this.pooledCacheLanes=0,this.formState=c,this.incompleteTransitions=new Map}function ep(e,t,n,r,i,a,o,s,c,l,u,d){return e=new $f(e,t,n,o,c,l,u,d,s),t=1,!0===a&&(t|=24),a=ui(3,null,null,t),e.current=a,a.stateNode=e,t=sa(),t.refCount++,e.pooledCache=t,t.refCount++,a.memoizedState={element:r,isDehydrated:n,cache:t},Ba(a),e}function tp(e){return e?(e=ci,e):ci}function np(e,t,n,r,i,a){i=tp(i),r.context===null?r.context=i:r.pendingContext=i,r=Ha(t),r.payload={element:n},a=a===void 0?null:a,a!==null&&(r.callback=a),n=Ua(e,r,t),n!==null&&(hu(n,e,t),Wa(n,e,t))}function rp(e,t){if(e=e.memoizedState,e!==null&&e.dehydrated!==null){var n=e.retryLane;e.retryLane=n!==0&&n<t?n:t}}function ip(e,t){rp(e,t),(e=e.alternate)&&rp(e,t)}function ap(e){if(e.tag===13||e.tag===31){var t=ai(e,67108864);t!==null&&hu(t,e,67108864),ip(e,67108864)}}function op(e){if(e.tag===13||e.tag===31){var t=pu();t=it(t);var n=ai(e,t);n!==null&&hu(n,e,t),ip(e,t)}}var sp=!0;function cp(e,t,n,r){var i=A.T;A.T=null;var a=j.p;try{j.p=2,up(e,t,n,r)}finally{j.p=a,A.T=i}}function lp(e,t,n,r){var i=A.T;A.T=null;var a=j.p;try{j.p=8,up(e,t,n,r)}finally{j.p=a,A.T=i}}function up(e,t,n,r){if(sp){var i=dp(r);if(i===null)wd(e,t,r,fp,n),Cp(e,r);else if(Tp(i,e,t,n,r))r.stopPropagation();else if(Cp(e,r),t&4&&-1<Sp.indexOf(e)){for(;i!==null;){var a=yt(i);if(a!==null)switch(a.tag){case 3:if(a=a.stateNode,a.current.memoizedState.isDehydrated){var o=Je(a.pendingLanes);if(o!==0){var s=a;for(s.pendingLanes|=2,s.entangledLanes|=2;o;){var c=1<<31-Ve(o);s.entanglements[1]|=c,o&=~c}rd(a),!(G&6)&&(tu=ke()+500,id(0,!1))}}break;case 31:case 13:s=ai(a,2),s!==null&&hu(s,a,2),bu(),ip(a,2)}if(a=dp(r),a===null&&wd(e,t,r,fp,n),a===i)break;i=a}i!==null&&r.stopPropagation()}else wd(e,t,r,null,n)}}function dp(e){return e=an(e),pp(e)}var fp=null;function pp(e){if(fp=null,e=vt(e),e!==null){var t=o(e);if(t===null)e=null;else{var n=t.tag;if(n===13){if(e=s(t),e!==null)return e;e=null}else if(n===31){if(e=c(t),e!==null)return e;e=null}else if(n===3){if(t.stateNode.current.memoizedState.isDehydrated)return t.tag===3?t.stateNode.containerInfo:null;e=null}else t!==e&&(e=null)}}return fp=e,null}function mp(e){switch(e){case`beforetoggle`:case`cancel`:case`click`:case`close`:case`contextmenu`:case`copy`:case`cut`:case`auxclick`:case`dblclick`:case`dragend`:case`dragstart`:case`drop`:case`focusin`:case`focusout`:case`input`:case`invalid`:case`keydown`:case`keypress`:case`keyup`:case`mousedown`:case`mouseup`:case`paste`:case`pause`:case`play`:case`pointercancel`:case`pointerdown`:case`pointerup`:case`ratechange`:case`reset`:case`resize`:case`seeked`:case`submit`:case`toggle`:case`touchcancel`:case`touchend`:case`touchstart`:case`volumechange`:case`change`:case`selectionchange`:case`textInput`:case`compositionstart`:case`compositionend`:case`compositionupdate`:case`beforeblur`:case`afterblur`:case`beforeinput`:case`blur`:case`fullscreenchange`:case`focus`:case`hashchange`:case`popstate`:case`select`:case`selectstart`:return 2;case`drag`:case`dragenter`:case`dragexit`:case`dragleave`:case`dragover`:case`mousemove`:case`mouseout`:case`mouseover`:case`pointermove`:case`pointerout`:case`pointerover`:case`scroll`:case`touchmove`:case`wheel`:case`mouseenter`:case`mouseleave`:case`pointerenter`:case`pointerleave`:return 8;case`message`:switch(Ae()){case je:return 2;case Me:return 8;case Ne:case Pe:return 32;case Fe:return 268435456;default:return 32}default:return 32}}var hp=!1,gp=null,_p=null,vp=null,yp=new Map,bp=new Map,xp=[],Sp=`mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset`.split(` `);function Cp(e,t){switch(e){case`focusin`:case`focusout`:gp=null;break;case`dragenter`:case`dragleave`:_p=null;break;case`mouseover`:case`mouseout`:vp=null;break;case`pointerover`:case`pointerout`:yp.delete(t.pointerId);break;case`gotpointercapture`:case`lostpointercapture`:bp.delete(t.pointerId)}}function wp(e,t,n,r,i,a){return e===null||e.nativeEvent!==a?(e={blockedOn:t,domEventName:n,eventSystemFlags:r,nativeEvent:a,targetContainers:[i]},t!==null&&(t=yt(t),t!==null&&ap(t)),e):(e.eventSystemFlags|=r,t=e.targetContainers,i!==null&&t.indexOf(i)===-1&&t.push(i),e)}function Tp(e,t,n,r,i){switch(t){case`focusin`:return gp=wp(gp,e,t,n,r,i),!0;case`dragenter`:return _p=wp(_p,e,t,n,r,i),!0;case`mouseover`:return vp=wp(vp,e,t,n,r,i),!0;case`pointerover`:var a=i.pointerId;return yp.set(a,wp(yp.get(a)||null,e,t,n,r,i)),!0;case`gotpointercapture`:return a=i.pointerId,bp.set(a,wp(bp.get(a)||null,e,t,n,r,i)),!0}return!1}function Ep(e){var t=vt(e.target);if(t!==null){var n=o(t);if(n!==null){if(t=n.tag,t===13){if(t=s(n),t!==null){e.blockedOn=t,st(e.priority,function(){op(n)});return}}else if(t===31){if(t=c(n),t!==null){e.blockedOn=t,st(e.priority,function(){op(n)});return}}else if(t===3&&n.stateNode.current.memoizedState.isDehydrated){e.blockedOn=n.tag===3?n.stateNode.containerInfo:null;return}}}e.blockedOn=null}function Dp(e){if(e.blockedOn!==null)return!1;for(var t=e.targetContainers;0<t.length;){var n=dp(e.nativeEvent);if(n===null){n=e.nativeEvent;var r=new n.constructor(n.type,n);rn=r,n.target.dispatchEvent(r),rn=null}else return t=yt(n),t!==null&&ap(t),e.blockedOn=n,!1;t.shift()}return!0}function Op(e,t,n){Dp(e)&&n.delete(t)}function kp(){hp=!1,gp!==null&&Dp(gp)&&(gp=null),_p!==null&&Dp(_p)&&(_p=null),vp!==null&&Dp(vp)&&(vp=null),yp.forEach(Op),bp.forEach(Op)}function Ap(e,n){e.blockedOn===n&&(e.blockedOn=null,hp||(hp=!0,t.unstable_scheduleCallback(t.unstable_NormalPriority,kp)))}var jp=null;function Mp(e){jp!==e&&(jp=e,t.unstable_scheduleCallback(t.unstable_NormalPriority,function(){jp===e&&(jp=null);for(var t=0;t<e.length;t+=3){var n=e[t],r=e[t+1],i=e[t+2];if(typeof r!=`function`){if(pp(r||n)===null)continue;break}var a=yt(n);a!==null&&(e.splice(t,3),t-=3,Ts(a,{pending:!0,data:i,method:n.method,action:r},r,i))}}))}function Np(e){function t(t){return Ap(t,e)}gp!==null&&Ap(gp,e),_p!==null&&Ap(_p,e),vp!==null&&Ap(vp,e),yp.forEach(t),bp.forEach(t);for(var n=0;n<xp.length;n++){var r=xp[n];r.blockedOn===e&&(r.blockedOn=null)}for(;0<xp.length&&(n=xp[0],n.blockedOn===null);)Ep(n),n.blockedOn===null&&xp.shift();if(n=(e.ownerDocument||e).$$reactFormReplay,n!=null)for(r=0;r<n.length;r+=3){var i=n[r],a=n[r+1],o=i[ut]||null;if(typeof a==`function`)o||Mp(n);else if(o){var s=null;if(a&&a.hasAttribute(`formAction`)){if(i=a,o=a[ut]||null)s=o.formAction;else if(pp(i)!==null)continue}else s=o.action;typeof s==`function`?n[r+1]=s:(n.splice(r,3),r-=3),Mp(n)}}}function Pp(){function e(e){e.canIntercept&&e.info===`react-transition`&&e.intercept({handler:function(){return new Promise(function(e){return i=e})},focusReset:`manual`,scroll:`manual`})}function t(){i!==null&&(i(),i=null),r||setTimeout(n,20)}function n(){if(!r&&!navigation.transition){var e=navigation.currentEntry;e&&e.url!=null&&navigation.navigate(e.url,{state:e.getState(),info:`react-transition`,history:`replace`})}}if(typeof navigation==`object`){var r=!1,i=null;return navigation.addEventListener(`navigate`,e),navigation.addEventListener(`navigatesuccess`,t),navigation.addEventListener(`navigateerror`,t),setTimeout(n,100),function(){r=!0,navigation.removeEventListener(`navigate`,e),navigation.removeEventListener(`navigatesuccess`,t),navigation.removeEventListener(`navigateerror`,t),i!==null&&(i(),i=null)}}}function Fp(e){this._internalRoot=e}Ip.prototype.render=Fp.prototype.render=function(e){var t=this._internalRoot;if(t===null)throw Error(i(409));var n=t.current;np(n,pu(),e,t,null,null)},Ip.prototype.unmount=Fp.prototype.unmount=function(){var e=this._internalRoot;if(e!==null){this._internalRoot=null;var t=e.containerInfo;np(e.current,2,null,e,null,null),bu(),t[dt]=null}};function Ip(e){this._internalRoot=e}Ip.prototype.unstable_scheduleHydration=function(e){if(e){var t=ot();e={blockedOn:null,target:e,priority:t};for(var n=0;n<xp.length&&t!==0&&t<xp[n].priority;n++);xp.splice(n,0,e),n===0&&Ep(e)}};var Lp=n.version;if(Lp!==`19.2.7`)throw Error(i(527,Lp,`19.2.7`));j.findDOMNode=function(e){var t=e._reactInternals;if(t===void 0)throw typeof e.render==`function`?Error(i(188)):(e=Object.keys(e).join(`,`),Error(i(268,e)));return e=d(t),e=e===null?null:p(e),e=e===null?null:e.stateNode,e};var Rp={bundleType:0,version:`19.2.7`,rendererPackageName:`react-dom`,currentDispatcherRef:A,reconcilerVersion:`19.2.7`};if(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__<`u`){var zp=__REACT_DEVTOOLS_GLOBAL_HOOK__;if(!zp.isDisabled&&zp.supportsFiber)try{Re=zp.inject(Rp),ze=zp}catch{}}e.createRoot=function(e,t){if(!a(e))throw Error(i(299));var n=!1,r=``,o=Js,s=Ys,c=Xs;return t!=null&&(!0===t.unstable_strictMode&&(n=!0),t.identifierPrefix!==void 0&&(r=t.identifierPrefix),t.onUncaughtError!==void 0&&(o=t.onUncaughtError),t.onCaughtError!==void 0&&(s=t.onCaughtError),t.onRecoverableError!==void 0&&(c=t.onRecoverableError)),t=ep(e,1,!1,null,null,n,r,null,o,s,c,Pp),e[dt]=t.current,Sd(e),new Fp(t)}})),g=o(((e,t)=>{function n(){if(!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__>`u`||typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE!=`function`))try{__REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(n)}catch(e){console.error(e)}}n(),t.exports=h()})),_=u(),v=g(),y=/[\p{Extended_Pictographic}\u{1F3FB}-\u{1F3FF}]/gu,b=/[\u{FE0F}\u{20E3}]/gu,x=e=>e.replace(y,``).replace(b,``).replace(/\*\*/g,``).replace(/\s{2,}/g,` `).replace(/\s+([:.,])/g,`$1`).trim(),ee=e=>e.replace(/^\uFEFF/,``).replace(/\r\n/g,`
`).replace(/^#[ \t][^\n]*\n+/,``).replace(/\n+---[ \t]*\n+-[ \t]*by[^\n]*\s*$/i,``).replace(/^(#{2,6})[ \t]+(.+)$/gm,(e,t,n)=>`${t} ${x(n)}`).replace(/\n{2,}-{3,}[ \t]*(?=\n{2,})/g,``).trim(),S=e=>Math.max(1,Math.round(e.split(/\s+/).length/200)),C=e=>t=>({...t,content:ee(t.markdown),minutes:S(t.markdown),route:`${e}/${t.slug}`}),te=`itsadityakr`,ne=`Aditya Kumar`,w=e=>`https://github.com/${te}/${e}`,T=e=>`./apps/${e}`,re=[{id:`me-not-afk`,name:`Me Not AFK`,repo:`me_not_afk`,tagline:`Stay awake & show as Available.`,summary:`Keeps your screen on and your computer looking busy — so presence indicators in Teams or Slack stay green during short breaks.`,version:`0.1.0`,status:`stable`,updated:`2026-05-18`,icon:T(`me-not-afk.png`),download:`https://www.mediafire.com/file/59jq4n7vgt8jgy6/Me_Not_AFK-win32-x64.zip/file`,accent:`#34d399`,tags:[`Productivity`,`Utility`],platforms:[`Windows`],size:`141 MB`,license:`MIT`,highlights:[`One-tap toggle with a live session timer and progress ring.`,`Choose how often it checks in — 15s, 30s, 1m, 2m or 5m.`,`Auto-off timer (30m, 1h or custom up to 12h), and it can quit when done.`,`Light / Dark / System themes that follow your OS live.`,`Switches off cleanly — nothing left running behind your back.`],requirements:[`Windows 10 (1809+) or 11, x64 for full functionality`,`macOS and Linux keep the screen awake only`],privacy:`No internet connection and no tracking. Your settings stay on your machine.`},{id:`shut-me-down`,name:`Shut Me Down`,repo:`shut_me_down`,tagline:`Set a timer, walk away.`,summary:`Schedules a power action — shut down, restart, sleep, hibernate, lock or sign out — after a delay or at a wall-clock time, with a live countdown dial and one-tap cancel.`,version:`0.1.0`,status:`stable`,updated:`2026-06-02`,icon:T(`shut-me-down.png`),download:`https://www.mediafire.com/file/m921qt2losiqfac/Shut_Me_Down-win32-x64.zip/file`,accent:`#fb923c`,tags:[`Utility`,`Power`],platforms:[`Windows`],size:`141 MB`,license:`MIT`,highlights:[`Six actions — shut down, restart, sleep, hibernate, lock, sign out.`,`Schedule by delay (15m to 7 days) or by clock time, rolling to tomorrow if past.`,`Countdown dial that turns red in the final minute.`,`Shut down and restart still go ahead even if you close the app.`,`Force-close toggle, a confirmation before arming, and cancel any time.`],requirements:[`Windows 10 (1809+) or 11, x64`,`Scheduling works on Windows only`],privacy:`No internet connection and no tracking. Your preferences stay on your machine.`},{id:`context-menu-tool`,name:`Context Menu Tool`,repo:`reg_e`,tagline:`Own your right-click menu.`,summary:`Add, edit and remove your own Windows right-click entries from a small desktop app — for files, folders, folder background and the desktop.`,version:`1.0.0`,status:`stable`,updated:`2026-04-27`,icon:T(`context-menu-tool.png`),download:`https://www.mediafire.com/file/8wfj0yg04fb3k7j/Context_Menu_Tool-win32-x64.zip/file`,accent:`#60a5fa`,tags:[`Utility`,`Registry`],platforms:[`Windows`],size:`111 MB`,license:`MIT`,highlights:[`Four targets — files, folders, folder background and desktop background.`,`Give each entry a name, an icon and whatever you want it to open.`,`List, edit and remove the entries you created.`,`No administrator rights needed.`],requirements:[`Windows 10 or 11, x64`],privacy:`Changes apply to your account only, and nothing is sent anywhere.`},{id:`control-deck`,name:`Control Deck`,repo:`control_deck`,tagline:`Your PC, from your phone.`,summary:`Scan a QR code on your PC and your phone becomes a remote — volume, displays, media keys and system actions, all over your own WiFi.`,version:`0.1.0`,status:`beta`,updated:`2026-07-11`,icon:null,download:`https://www.mediafire.com/file/7b0q3mvi48g3fcb/Control_Deck-win32-x64.zip/file`,accent:`#a78bfa`,tags:[`Remote`,`Productivity`],platforms:[`Windows`,`Phone browser`],size:`110 MB`,license:`MIT`,highlights:[`Pair by scanning a QR code — nothing to install on the phone.`,`Volume, display, media and system controls, straight from your pocket.`,`Runs entirely on your own WiFi — nothing goes out to the internet.`,`Lives quietly in the system tray.`],requirements:[`Windows 10 or 11, x64`,`PC and phone on the same WiFi network`],privacy:`Traffic never leaves your local network, and pairing lasts only for the session.`}].map(e=>({...e,url:w(e.repo),downloadUrl:e.download,cloneCommand:`git clone ${w(e.repo)}.git`,issuesUrl:`${w(e.repo)}/issues`})),E=[...new Set(re.flatMap(e=>e.tags))].sort(),ie={stable:`Stable`,beta:`Beta`,wip:`In progress`},D=`The Microsoft Store has a particular way of failing. The download does not error
and it does not stop — it sits at \`Pending\`, or crawls at a fraction of the speed
the same connection gives everything else. Nothing in the interface explains why,
so the fix is a short list of usual suspects, worked through cheapest first.

> Start at Method 1 and stop as soon as the download moves. Method 3 re-registers
> the Store app itself and is the heaviest of the three — there is no reason to
> reach for it if a metered-connection toggle was the whole problem.

## Why it happens

Three things throttle Store traffic more often than a slow connection does:

- **Delivery Optimization**, which caps how much bandwidth background downloads
  may use and is set conservatively out of the box.
- **A metered connection**, which Windows sets automatically on some Wi-Fi and
  mobile networks and which pauses Store downloads almost entirely.
- **A stale Store cache**, where a half-finished download blocks the queue behind
  it and never times out on its own.

## Method 1: Give the Store its bandwidth back

### 1. Find out what else is using the connection

Press \`Ctrl + Shift + Esc\` for Task Manager, open the **Performance** tab and
watch the network graph, or sort the **Processes** tab by **Network**. A sync
client or a game launcher updating in the background is the single most common
cause. End what you do not need right now.

### 2. Turn off the metered connection

Press \`Win + I\` for Settings, then **Network & internet**, then your active
adapter — **Wi-Fi** or **Ethernet** — and open its properties. Turn
**Metered connection** off.

> On a metered connection Windows deliberately holds back Store downloads and
> most updates. If this toggle was on, it is very likely the answer.

### 3. Raise the Delivery Optimization limit

Go to **Settings → Windows Update → Advanced options → Delivery Optimization →
Advanced options**. Switch the download limits from **Absolute bandwidth** to
**Percentage of measured bandwidth**, and set both the foreground and background
sliders to \`100%\`.

### 4. Repair and reset the Store app

Search the Start menu for **Microsoft Store**, right-click it and choose
**App settings**. Set **Let this app run in the background** to **Always**, then
click **Terminate**, then **Repair**. If the download still sticks, come back and
click **Reset**.

> **Repair** keeps your data and preferences. **Reset** clears the app's local
> state, which includes the stuck download queue — that is exactly what you want
> here, and it does not touch your installed apps or your Microsoft account.

### 5. Clear the network and Store caches

Open **Command Prompt** as administrator — \`Win + X\`, then **Terminal (Admin)** —
and run these in order:

\`\`\`shell
ipconfig /flushdns
netsh winsock reset
wsreset
\`\`\`

\`wsreset\` opens an empty Store window when it finishes, which is how you know it
worked. \`netsh winsock reset\` needs a restart to fully take effect.

> A \`netsh winsock reset\` drops custom socket layers some VPN clients install. If
> a VPN stops connecting afterwards, reinstall its client — it will put its own
> layer back.

## Method 2: Point DNS somewhere faster

The Store resolves a lot of hostnames per download, and a slow resolver shows up
as a long stall before the transfer ever starts.

### 1. Open the adapter properties

Press \`Win + R\`, type \`ncpa.cpl\` and press Enter. Right-click your active
connection and choose **Properties**.

### 2. Set the DNS servers by hand

Select **Internet Protocol Version 4 (TCP/IPv4)**, click **Properties**, choose
**Use the following DNS server addresses** and fill in a public pair:

| Provider   | Preferred | Alternate |
| ---------- | --------- | --------- |
| Google     | \`8.8.8.8\` | \`8.8.4.4\` |
| Cloudflare | \`1.1.1.1\` | \`1.0.0.1\` |

Click **OK**, then flush the cache so nothing stale survives the change:

\`\`\`shell
ipconfig /flushdns
\`\`\`

### 3. Check which one is actually closer to you

\`\`\`shell
ping 8.8.8.8
ping 1.1.1.1
\`\`\`

Whichever returns the lower average time is the one to keep as **Preferred**.
The difference is usually small; if it is large, that alone was probably the
bottleneck.

## Method 3: Re-register the Store

If the Store still refuses, rebuild its registration. Open **PowerShell as
administrator** and run:

\`\`\`powershell
Get-AppxPackage -AllUsers Microsoft.WindowsStore* | ForEach-Object { Add-AppxPackage -DisableDevelopmentMode -Register "$($_.InstallLocation)\\AppXManifest.xml" }
\`\`\`

Red text scrolling past is normal here — the command touches packages it cannot
always re-register, and the Store itself usually succeeds anyway. Restart the PC
afterwards; the registration is not fully live until you do.

## If none of it works

- Check for pending Windows updates in **Settings → Windows Update**. The Store
  depends on components that ship with them.
- Try a different network — a phone hotspot is enough to prove whether the
  problem is the machine or the connection.
- Sign out of the Store and back in. A stale token presents as a permanent
  \`Pending\`, with no error to explain it.
- Run \`sfc /scannow\`, then \`DISM /Online /Cleanup-Image /RestoreHealth\`, if the
  machine has other unexplained app problems as well.
`,O=`Office LTSC is the perpetual, offline-first build of Office: Word, Excel,
PowerPoint and Outlook installed once, with security updates but no feature
churn and no cloud requirement. It installs through the **Office Deployment
Tool** — \`setup.exe\` plus an XML file that says which apps you want — rather than
through a wizard, which is what makes it worth writing down.

> This covers a licensed install: an account that carries an Office licence, a
> product key you hold, or a volume-licence deployment against your
> organisation's KMS host. If none of those apply, Microsoft 365 has a personal
> subscription and there is a free web version.

## Before you start

- **Administrator rights** — the installer writes to \`Program Files\`.
- **About 4 GB free**, plus room for the download.
- **The Office Deployment Tool**, from Microsoft's Download Center. It is a
  self-extracting executable that gives you \`setup.exe\` and a sample XML.
- **No other Office installed.** Mixing an existing Click-to-Run Office with a
  new LTSC install is the most common way this goes wrong — uninstall the old one
  from **Settings → Apps** first.

Extract the deployment tool somewhere short and easy to type. \`C:\\office2024\`
works and is what the commands below assume.

## Step 1: Write the configuration file

This is the whole install, described in one file — which apps, which language,
32- or 64-bit, and how quietly it runs:

\`\`\`xml configuration.xml
<Configuration>
  <Add OfficeClientEdition="64" Channel="PerpetualVL2024">
    <Product ID="ProPlus2024Volume" PIDKEY="">
      <Language ID="en-us" />
      <ExcludeApp ID="Groove" />
      <ExcludeApp ID="Lync" />
      <ExcludeApp ID="OneDrive" />
      <ExcludeApp ID="Bing" />
    </Product>
  </Add>

  <Property Name="SharedComputerLicensing" Value="0" />
  <Property Name="DeviceBasedLicensing" Value="0" />
  <Property Name="AUTOACTIVATE" Value="0" />

  <Updates Enabled="TRUE" />
  <Display Level="Full" AcceptEULA="TRUE" />
  <Logging Level="Standard" Path="%temp%" />
</Configuration>
\`\`\`

Save it next to \`setup.exe\`. The parts worth changing:

- **\`OfficeClientEdition\`** — \`64\` unless you have a specific reason for \`32\`.
- **\`Channel\`** — \`PerpetualVL2024\` for Office LTSC 2024, \`PerpetualVL2021\` for
  the 2021 release.
- **\`Product ID\`** — \`ProPlus2024Volume\` for the full suite,
  \`Standard2024Volume\` for the Standard edition.
- **\`PIDKEY\`** — leave it empty to activate later, or put your key in it.
- **\`ExcludeApp\`** — one line per app you do not want. \`Access\`, \`Publisher\`,
  \`Outlook\` and \`Teams\` are the other names people usually reach for.
- **\`Display Level\`** — \`Full\` shows the progress window; \`None\` installs
  silently.

> Use \`Display Level="None"\` only in a script. Run interactively it looks like
> nothing is happening for ten minutes, and people reboot halfway through.

## Step 2: Install from an elevated prompt

Press \`Win + S\`, type \`cmd\`, right-click **Command Prompt** and choose
**Run as administrator**. Then:

\`\`\`cmd
cd /d C:\\office2024
setup.exe /configure configuration.xml
\`\`\`

Downloading and installing takes anywhere from a few minutes to half an hour
depending on the connection. The window closes when it is done.

To download the payload once and install several machines from it offline:

\`\`\`cmd
setup.exe /download configuration.xml
\`\`\`

That pulls the files into an \`Office\` folder next to \`setup.exe\`; copy the whole
folder and run the \`/configure\` line on each machine.

> If setup exits immediately, look in \`%temp%\` for the log the \`Logging\` element
> writes. A wrong \`Channel\` / \`Product ID\` pairing and a leftover Office install
> account for most instant failures.

## Step 3: Activate

**With a Microsoft account that holds a licence** — open Word, go to
**File → Account → Sign in**, and it activates itself.

**With a product key you hold** — **File → Account → Change Product Key**, type
it in, restart the app.

**On a volume licence, against your KMS host** — Office ships with a public
generic volume-licence key already applied, and activates against the KMS server
your network points it at. Set the host explicitly and trigger activation from
the Office install folder:

\`\`\`cmd
cd /d "C:\\Program Files\\Microsoft Office\\Office16"
cscript ospp.vbs /sethst:kms.your-domain.internal
cscript ospp.vbs /act
\`\`\`

Check where it stands at any time:

\`\`\`cmd
cscript ospp.vbs /dstatus
\`\`\`

\`LICENSE STATUS: ---LICENSED---\` is what you are looking for. Without a KMS host
to talk to, this path simply will not activate — that is the design, not a fault.

## Step 4: Confirm the install

- Open Word. **File → Account** shows the edition, the channel and the
  activation state on one screen.
- **Settings → Apps → Installed apps** lists a single Office entry. Two entries
  means an older install survived and should be removed.
- **File → Account → Update Options → Update Now** confirms updates work.

## When it goes wrong

- **"We're sorry, we had a problem installing"** — an existing Office is in the
  way. Uninstall it, or use Microsoft's Support and Recovery Assistant to remove
  the remnants, then run setup again.
- **Setup runs but nothing appears** — check the log in \`%temp%\`; a typo in
  \`configuration.xml\` fails silently at the parse stage.
- **Activation cannot reach the server** — from the machine, confirm the KMS host
  resolves and that TCP 1688 is open to it.
- **Outlook missing** — it is excluded in the sample above. Delete its
  \`ExcludeApp\` line and re-run \`/configure\`; only the missing app is added.
`,k=`A USB webcam is the cheapest camera you can put on a Pi, and it works without any
of the ribbon-cable ceremony the official camera modules need. The catch is that
"it appears as \`/dev/video0\`" is not the same as "it works" — permissions, formats
and frame rates all have their own ways of failing quietly.

This gets a Logitech C270 — or any UVC webcam — from plugged in to streaming
frames into OpenCV, checking at each step that it actually did something.

## What you need

- Raspberry Pi 5 running Raspberry Pi OS (64-bit)
- A USB webcam. The C270 is what this was written against; anything UVC-class
  behaves the same
- An internet connection for the packages

> Plug the camera into a **USB 2.0** port (the black ones) rather than USB 3.0 if
> you have interference problems on 2.4 GHz Wi-Fi. USB 3.0 is a well-documented
> source of 2.4 GHz noise, and a camera streaming continuously is exactly the
> load that shows it up.

## Step 1: Plug it in and update

\`\`\`bash
sudo apt update && sudo apt full-upgrade -y
\`\`\`

## Step 2: Confirm the Pi sees it

\`\`\`bash
lsusb
\`\`\`

Look for a line naming the camera:

\`\`\`text
Bus 001 Device 005: ID 046d:0825 Logitech, Inc. Webcam C270
\`\`\`

More detail, including the USB speed it negotiated:

\`\`\`bash
usb-devices
\`\`\`

And the device node it was given:

\`\`\`bash
ls -l /dev/video*
\`\`\`

\`\`\`text
crw-rw----+ 1 root video 81, 0 Sep 10 12:34 /dev/video0
\`\`\`

> Most webcams claim **two** nodes — \`/dev/video0\` for frames and \`/dev/video1\`
> for metadata. Opening the wrong one gives you a camera that reports success and
> returns nothing. If index \`0\` yields no frames, try \`1\`.

## Step 3: Install the tools

\`\`\`bash
sudo apt install -y v4l-utils fswebcam ffmpeg
\`\`\`

- **\`v4l-utils\`** — \`v4l2-ctl\`, which asks the camera what it can do
- **\`fswebcam\`** — grabs a still from the command line
- **\`ffmpeg\`** — brings \`ffplay\`, for watching the live feed

## Step 4: Fix the permissions

Your user needs to be in the \`video\` group:

\`\`\`bash
groups
sudo usermod -aG video "$USER"
\`\`\`

Log out and back in — group membership is only picked up by new sessions. Then
confirm:

\`\`\`bash
groups | grep video
\`\`\`

If the device node still is not owned by the \`video\` group, add a udev rule:

\`\`\`bash
sudo nano /etc/udev/rules.d/99-webcam.rules
\`\`\`

\`\`\`text 99-webcam.rules
SUBSYSTEM=="video4linux", ATTR{name}=="C270 HD WEBCAM", GROUP="video", MODE="0660"
\`\`\`

Save with \`Ctrl + X\`, \`Y\`, \`Enter\`, then reload and replug the camera:

\`\`\`bash
sudo udevadm control --reload-rules && sudo udevadm trigger
\`\`\`

## Step 5: Ask the camera what it supports

This is the step that saves the most time later:

\`\`\`bash
v4l2-ctl --list-devices
v4l2-ctl -d /dev/video0 --list-formats-ext
\`\`\`

You get every format with its resolutions and frame rates. A C270 will show
\`YUYV\` at up to 1280x960 and \`MJPG\` at 1280x720.

> **Ask for MJPG.** On \`YUYV\` the camera sends raw frames and a C270 manages
> about 5 fps at 720p over USB 2.0. In \`MJPG\` the camera compresses first and
> gives you 30. Nearly every "my webcam is so slow" problem is this.

Check the controls it exposes — brightness, autofocus, exposure:

\`\`\`bash
v4l2-ctl -d /dev/video0 --list-ctrls
\`\`\`

## Step 6: Take a picture

\`\`\`bash
fswebcam -d /dev/video0 -r 1280x720 --no-banner test.jpg
\`\`\`

Open \`test.jpg\`. If it is dark or green, the camera needed a moment to settle its
auto-exposure — tell \`fswebcam\` to throw the first frames away:

\`\`\`bash
fswebcam -d /dev/video0 -r 1280x720 -S 10 --no-banner test.jpg
\`\`\`

## Step 7: Watch it live

With a desktop, over VNC or with X forwarding:

\`\`\`bash
ffplay -f v4l2 -input_format mjpeg -video_size 1280x720 /dev/video0
\`\`\`

There is also \`cheese\`, if you want a graphical app with a shutter button:

\`\`\`bash
sudo apt install -y cheese
cheese
\`\`\`

## Step 8: Read frames in OpenCV

Inside a virtual environment with \`opencv-python\` installed:

\`\`\`python test_webcam.py
import cv2

# Ask V4L2 directly — the default backend probes several APIs and is slower
# to open on Linux.
cap = cv2.VideoCapture(0, cv2.CAP_V4L2)

# MJPG before size: the camera switches format, then applies the resolution.
cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter_fourcc(*"MJPG"))
cap.set(cv2.CAP_PROP_FRAME_WIDTH, 1280)
cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 720)
cap.set(cv2.CAP_PROP_FPS, 30)

if not cap.isOpened():
    raise SystemExit("Could not open the camera — check /dev/video* and groups")

print("Capturing at {}x{} @ {} fps".format(
    int(cap.get(cv2.CAP_PROP_FRAME_WIDTH)),
    int(cap.get(cv2.CAP_PROP_FRAME_HEIGHT)),
    cap.get(cv2.CAP_PROP_FPS),
))

try:
    while True:
        ok, frame = cap.read()
        if not ok:
            print("Dropped a frame")
            continue

        cv2.imshow("Webcam", frame)

        key = cv2.waitKey(1) & 0xFF
        if key == ord("q"):
            break
        if key == ord("s"):
            cv2.imwrite("snapshot.jpg", frame)
            print("Saved snapshot.jpg")
finally:
    cap.release()
    cv2.destroyAllWindows()
\`\`\`

\`\`\`bash
python test_webcam.py
\`\`\`

\`q\` quits, \`s\` saves a still. The \`finally\` block matters: leave without
releasing the device and the next run finds the camera busy.

Running headless? Drop \`cv2.imshow\` and write frames instead:

\`\`\`python capture_headless.py
import cv2

cap = cv2.VideoCapture(0, cv2.CAP_V4L2)
cap.set(cv2.CAP_PROP_FOURCC, cv2.VideoWriter_fourcc(*"MJPG"))

for i in range(5):
    ok, frame = cap.read()
    if ok:
        cv2.imwrite(f"frame_{i:02d}.jpg", frame)

cap.release()
print("Wrote 5 frames")
\`\`\`

## When it does not work

- **\`Could not open the camera\`** — something else holds it. \`sudo fuser -v
  /dev/video0\` names the process; \`cheese\` and \`ffplay\` are the usual suspects.
- **Permission denied** — you are not in \`video\` yet, or you have not logged out
  since being added.
- **Black or green frames** — auto-exposure has not settled. Discard the first
  few frames, or fix exposure with \`v4l2-ctl -d /dev/video0 -c
  auto_exposure=1 -c exposure_time_absolute=200\`.
- **Very low frame rate** — you are on \`YUYV\`. Set \`MJPG\` before the resolution,
  as above.
- **The camera vanishes mid-session** — power. A camera on a long unpowered
  extension browns out; use a powered hub or a shorter cable.
- **\`select() timeout\`** — a marginal USB cable, almost every time.

## Further reading

- [fswebcam manual](http://manpages.ubuntu.com/manpages/bionic/man1/fswebcam.1.html)
- [Video4Linux (v4l-utils)](https://www.linuxtv.org/wiki/index.php/Video4Linux)
`,ae=`A Raspberry Pi does not need a monitor, a keyboard or an Ethernet cable to be set
up. Raspberry Pi Imager can write the Wi-Fi credentials, the hostname, your user
account and an SSH key into the card before it ever boots — so the first time the
board powers on, it joins your network and waits for you.

This is the whole route from a blank microSD card to a desktop on your screen:
write the card, find the board, SSH in, turn on VNC. Plus the trick that makes it
survive a change of network — a phone hotspot with the same SSID.

## What you need

**Hardware**

- Raspberry Pi 5 and its 27 W USB-C power supply — an underpowered phone charger
  causes reboots that look like software faults
- microSD card, 16 GB or larger, and a reader
- A Wi-Fi network, or a phone that can make a hotspot

**Software on the Windows machine**

- [Raspberry Pi Imager](https://www.raspberrypi.com/software/) — writes the card
- [Angry IP Scanner](https://angryip.org/) — finds the board on the network
- [PuTTY](https://www.putty.org/) — SSH, if you would rather not use the built-in
  \`ssh\` command
- [RealVNC Viewer](https://www.realvnc.com/en/connect/download/viewer/) — the
  desktop

> Windows 10 and 11 both ship with OpenSSH, so \`ssh pi@raspberrypi.local\` in
> Terminal does everything PuTTY does. PuTTY is listed because its saved sessions
> are convenient when you have several boards.

## Step 1: Write the card

Download the image first if your connection is unreliable — Imager can fetch it
for you, but a failed download halfway through a 3 GB image wastes more time than
it saves. Grab **Raspberry Pi OS (64-bit) with desktop** from the
[OS downloads page](https://www.raspberrypi.com/software/operating-systems/).

1. Open Imager.
2. **Choose Device** → Raspberry Pi 5.
3. **Choose OS** → the downloaded file via **Use custom**, or pick
   **Raspberry Pi OS (64-bit)** to let Imager download it.
4. **Choose Storage** → your card. Check the size; it is about to be erased.
5. Click **Next**, then **Edit Settings** when it offers OS customisation. This
   is the part that makes the setup headless:

   - **Set hostname**: \`raspberrypi5\` — you will reach it as
     \`raspberrypi5.local\`
   - **Set username and password**: \`pi\` and something you will remember
   - **Configure wireless LAN**: your SSID and password, exactly as typed
     elsewhere — both are case-sensitive
   - **Wireless LAN country**: required, and the radio stays off without it
   - **Locale settings**: timezone and keyboard layout
   - **Services tab → Enable SSH**: password authentication, or paste a public
     key if you have one

6. **Save**, then **Yes** to apply the settings, then **Yes** to erase and write.

> On a 2.4 GHz-only network, make sure the phone or router is not broadcasting
> the same SSID on 5 GHz with different credentials. Mismatched bands under one
> name are a common reason a board silently never appears.

## Step 2: First boot

Put the card in, connect power, and leave it alone for **four or five minutes**.
The first boot resizes the filesystem and reboots itself once. Later boots take
two or three.

The green LED flickering is disk activity — steady green with nothing happening
usually means the card was written badly. Write it again before assuming worse.

## Step 3: Find it on the network

Try the name first, from Windows Terminal:

\`\`\`powershell
ping raspberrypi5.local
\`\`\`

If a reply comes back, you have the address and can skip the scanner. mDNS is not
always allowed on managed or guest networks, in which case:

1. Find your own subnet:

   \`\`\`powershell
   ipconfig
   \`\`\`

   Look at the **IPv4 Address** of your active adapter — say \`192.168.1.24\`.

2. Open Angry IP Scanner and set the range to that subnet: \`192.168.1.0\` to
   \`192.168.1.255\`.
3. **Start**, and watch for the hostname you set. Note the address, for example
   \`192.168.1.50\`.

> Give the board a reserved address in your router's DHCP settings once you find
> it. Then the address stops changing and none of this has to be repeated.

## Step 4: SSH in

From Windows Terminal:

\`\`\`powershell
ssh pi@raspberrypi5.local
\`\`\`

or with PuTTY: host \`raspberrypi5.local\` (or the IP), port \`22\`, connection type
**SSH**, then **Open**.

The first connection asks you to trust the host key — that is expected exactly
once per board. Then log in with the username and password from Step 1.

Update everything before you build anything on top of it:

\`\`\`bash
sudo apt update && sudo apt full-upgrade -y
\`\`\`

> Host key warnings on a later connection usually mean the address was reused by
> a different machine, not that anything is wrong. Remove the stale entry with
> \`ssh-keygen -R raspberrypi5.local\` and connect again.

## Step 5: Turn on VNC

\`\`\`bash
sudo raspi-config
\`\`\`

- **Interface Options → VNC → Yes.** Wait for the confirmation that the server is
  enabled.
- **Display Options → VNC Resolution**, and pick something real like
  \`1920x1080\`. Headless boards have no monitor to negotiate with, and the default
  is a small window.
- **Advanced Options → Wayland**, and choose **X11** if VNC refuses to connect
  later. RealVNC's viewer is happier with X11 on Bookworm.

Finish, then:

\`\`\`bash
sudo reboot
\`\`\`

The SSH session drops, which is expected.

## Step 6: Open the desktop

Wait two or three minutes for the reboot, then open **VNC Viewer**, enter
\`raspberrypi5.local\` or the IP address, and sign in with the same credentials.
The full desktop appears.

Check what you are running once you are in:

\`\`\`bash
hostnamectl
vcgencmd measure_temp
\`\`\`

## Keeping access when the network changes

A board configured for one Wi-Fi network is unreachable anywhere else. The fix is
a hotspot that pretends to be that network:

1. On your phone, open **Settings → Hotspot**.
2. Set the hotspot **name and password to exactly the SSID and password you gave
   Imager**.
3. Turn it on before powering the board, and connect your laptop to the same
   hotspot.

The Pi joins what it thinks is home, your laptop is on the same network, and
everything above works unchanged — useful on a desk that is not yours.

To add a second network properly rather than by impersonation:

\`\`\`bash
sudo nmcli device wifi connect "OtherNetwork" password "secret"
sudo nmcli connection show
\`\`\`

Bookworm uses NetworkManager, so both networks are remembered and it joins
whichever it can see.

## When it does not work

- **Nothing on the scan.** Confirm the Wi-Fi country was set, the SSID has no
  stray space, and the network is 2.4 GHz-capable. Give it a full five minutes.
- **\`ssh: connect to host ... port 22: Connection refused\`.** SSH was not enabled
  in Imager. Put the card back in the reader and create an empty file named \`ssh\`
  in the boot partition — the Pi enables it on the next boot.
- **VNC connects to a black screen.** The desktop session is not running: switch
  to X11 in \`raspi-config\`, set a real resolution, reboot.
- **\`Cannot currently show the desktop\`.** Same cause — the Wayland session is
  not shareable. X11 fixes it.
- **Random reboots under load.** Power. A Pi 5 wants the 27 W supply, especially
  with an SSD or a camera attached.
`,A=`You already own a decent camera: the one in your laptop lid. This points it at
the Pi — FFmpeg encodes on the laptop and throws H.264 over UDP, and a short
Python script on the Pi displays the feed and lets you grab stills or record
clips from it.

It is useful when the Pi is the machine doing the vision work but sits where a
camera cannot, and it costs nothing but a Wi-Fi network both machines are on.

> UDP is chosen deliberately. It drops frames rather than queueing them, which is
> exactly what you want for a live view — TCP would buy reliability at the price
> of latency that climbs the whole time it runs.

## The shape of it

\`\`\`text
Laptop                             Raspberry Pi 5
──────                             ──────────────
webcam → ffmpeg (H.264)  ── UDP ──►  OpenCV window
                                     s → still, v → clip
\`\`\`

## What you need

- A Windows laptop with a working camera
- A Raspberry Pi 5 on the **same network**, and its IP address
- FFmpeg on both

## Step 1: FFmpeg on the laptop

\`\`\`powershell
winget install --id Gyan.FFmpeg -e
\`\`\`

Close and reopen the terminal so \`PATH\` picks it up, then:

\`\`\`powershell
ffmpeg -version
\`\`\`

## Step 2: Find the camera's exact name

\`\`\`powershell
ffmpeg -list_devices true -f dshow -i dummy
\`\`\`

The command ends with an error — that is normal, \`dummy\` is not a real input. The
part you want is above it:

\`\`\`text
[dshow @ ...] "HP TrueVision HD Camera" (video)
[dshow @ ...]   Alternative name "@device_pnp_\\\\?\\usb#vid_..."
\`\`\`

Copy the quoted name **character for character**, including spaces.

Then ask what that camera can actually do:

\`\`\`powershell
ffmpeg -f dshow -list_options true -i video="HP TrueVision HD Camera"
\`\`\`

> If the name has awkward characters, use the **Alternative name** string
> instead. It is ugly, but it never gets misquoted.

## Step 3: Prepare the Pi

\`\`\`bash
sudo apt update && sudo apt full-upgrade -y
sudo apt install -y ffmpeg python3-opencv
\`\`\`

Get its address — you need it for the laptop command:

\`\`\`bash
hostname -I
\`\`\`

## Step 4: Start streaming

On the laptop, with the Pi's address in place of \`192.168.1.50\`:

\`\`\`powershell
ffmpeg -f dshow -i video="HP TrueVision HD Camera" -vcodec libx264 -preset ultrafast -tune zerolatency -pix_fmt yuv420p -b:v 1500k -f mpegts udp://192.168.1.50:1234
\`\`\`

What each part is doing:

| Flag                  | Why it is there                                        |
| --------------------- | ------------------------------------------------------ |
| \`-f dshow\`            | DirectShow — the Windows capture API                   |
| \`-vcodec libx264\`     | H.264, which the Pi decodes cheaply                    |
| \`-preset ultrafast\`   | Encode speed over file size; latency is the priority   |
| \`-tune zerolatency\`   | No B-frames, no lookahead                              |
| \`-pix_fmt yuv420p\`    | The pixel format every decoder accepts                 |
| \`-b:v 1500k\`          | 1.5 Mbit/s — fine for 720p over Wi-Fi                  |
| \`-f mpegts\`           | A container that survives packet loss                  |
| \`udp://IP:1234\`       | Where it goes                                          |

Confirm packets are arriving before writing any Python. On the Pi:

\`\`\`bash
ffplay -fflags nobuffer -flags low_delay -i udp://@:1234
\`\`\`

A window with your face in it means the network half is done.

## Step 5: Receive it in Python

\`\`\`python receive_stream.py
import datetime

import cv2

# @ means "listen on every interface" rather than "connect to a host".
STREAM_URL = "udp://@0.0.0.0:1234"

cap = cv2.VideoCapture(STREAM_URL, cv2.CAP_FFMPEG)
if not cap.isOpened():
    raise SystemExit("No stream. Is ffmpeg running on the laptop?")

# One frame of buffer: show the newest picture, drop whatever fell behind.
cap.set(cv2.CAP_PROP_BUFFERSIZE, 1)

fourcc = cv2.VideoWriter_fourcc(*"XVID")
writer = None


def stamp():
    return datetime.datetime.now().strftime("%Y%m%d_%H%M%S")


print("s = save a still   v = start/stop recording   q = quit")

try:
    while True:
        ok, frame = cap.read()
        if not ok:
            print("No frame — waiting for the stream")
            continue

        cv2.imshow("Video Stream", frame)
        key = cv2.waitKey(1) & 0xFF

        if key == ord("s"):
            name = f"image_{stamp()}.jpg"
            cv2.imwrite(name, frame)
            print("Saved", name)

        elif key == ord("v"):
            if writer is None:
                name = f"video_{stamp()}.avi"
                height, width = frame.shape[:2]
                writer = cv2.VideoWriter(name, fourcc, 20.0, (width, height))
                print("Recording to", name)
            else:
                writer.release()
                writer = None
                print("Recording stopped")

        elif key == ord("q"):
            break

        if writer is not None:
            writer.write(frame)
finally:
    cap.release()
    if writer is not None:
        writer.release()
    cv2.destroyAllWindows()
\`\`\`

Run it on the Pi, with the laptop already streaming:

\`\`\`bash
python3 receive_stream.py
\`\`\`

> \`writer\` doubles as the recording flag — if it is not \`None\`, recording is in
> progress. One variable instead of two cannot fall out of sync with itself.

## Checking the port

If nothing arrives, prove the Pi is listening before blaming the code:

\`\`\`bash
sudo apt install -y netcat-openbsd
nc -ul 1234
\`\`\`

Running with no output means the port is open and quiet. Bytes scrolling past
mean the stream is reaching the Pi and the problem is downstream, in OpenCV.

Check the firewall too — Windows blocks outbound UDP from new applications on
first use, and the prompt is easy to dismiss without reading:

\`\`\`powershell
Get-NetFirewallRule -DisplayName "*ffmpeg*" | Select-Object DisplayName, Action, Enabled
\`\`\`

## Tuning it

- **Too much latency** — lower the bitrate, drop the resolution, and add
  \`-g 30\` so keyframes are more frequent. Wi-Fi is nearly always the limit.
- **Torn or smeared picture** — packet loss. Reduce the bitrate; move closer to
  the access point; prefer 5 GHz.
- **Better quality** — raise \`-b:v\` to \`3000k\` and set the size explicitly with
  \`-video_size 1280x720\`. Watch latency as you go up.
- **Sound as well** — add the microphone as a second dshow input:
  \`-f dshow -i video="Camera":audio="Microphone (Realtek)"\`.

## When it does not work

- **\`Could not run graph\`** — the camera name is wrong, or another app has the
  camera. Close Teams, Zoom and the Camera app.
- **\`Error: Could not open video stream\`** — start FFmpeg first. OpenCV gives up
  quickly if nothing is arriving when it opens the socket.
- **Works with \`ffplay\`, not with Python** — you are opening \`udp://@0.0.0.0:1234\`
  rather than a host address, and only one process can hold that port. Stop
  \`ffplay\` before running the script.
- **Different subnets** — a laptop on guest Wi-Fi cannot reach a Pi on the main
  network. Compare \`ipconfig\` and \`hostname -I\` and put them on the same one.
`,j=`An 8x8 matrix on a MAX7219 board is about the cheapest thing you can wire to a Pi
that visibly does something. Five jumper wires, one SPI bus, and a library that
gives you a drawing surface — after which lighting a pixel is one line of Python.

This wires it up, gets SPI running on a Pi 5, lights a single LED at coordinates
you choose, and then scrolls text across it.

## What you need

- Raspberry Pi 5
- MAX7219 8x8 LED matrix module — the common \`FC-16\` or \`MAX7219 dot matrix\`
  board
- Five female-to-female jumper wires
- A breadboard, optionally

## Wiring

Power off the Pi before touching any of this.

| MAX7219 | Pi 5 pin  | GPIO           |
| ------- | --------- | -------------- |
| VCC     | Pin 2     | 5 V            |
| GND     | Pin 6     | Ground         |
| DIN     | Pin 19    | GPIO 10 (MOSI) |
| CS      | Pin 24    | GPIO 8 (CE0)   |
| CLK     | Pin 23    | GPIO 11 (SCLK) |

> **5 V, not 3.3 V.** The matrix is dim and unstable on 3.3 V. The data lines are
> fine at the Pi's 3.3 V logic levels — the MAX7219 reads them happily — so no
> level shifter is needed for a single module.
>
> One 8x8 module at full brightness draws a few hundred milliamps. Several
> chained modules want their own 5 V supply with its ground tied to the Pi's,
> rather than the Pi's 5 V rail.

## Step 1: Turn on SPI

\`\`\`bash
sudo raspi-config
\`\`\`

**Interface Options → SPI → Yes**, then reboot. Check afterwards:

\`\`\`bash
ls /dev/spi*
\`\`\`

\`\`\`text
/dev/spidev0.0  /dev/spidev0.1
\`\`\`

No devices means SPI is still off, or the overlay was disabled in
\`/boot/firmware/config.txt\` — \`dtparam=spi=on\` should be there and uncommented.

## Step 2: Install the library

Bookworm will not let pip write to the system Python, so this goes in a virtual
environment:

\`\`\`bash
mkdir -p ~/projects/matrix && cd ~/projects/matrix
python3 -m venv venv
source venv/bin/activate
pip install --upgrade pip
pip install luma.led_matrix
\`\`\`

\`luma.led_matrix\` pulls in \`luma.core\`, \`spidev\` and \`Pillow\`. You do not need
\`rm-hull/max7219\` — that is the older library this one replaced.

Your user needs access to the SPI device:

\`\`\`bash
sudo usermod -aG spi,gpio "$USER"
\`\`\`

Log out and back in. With that in place none of this needs \`sudo\`.

## Step 3: Light one LED

\`\`\`python single_led.py
from time import sleep

from luma.core.interface.serial import noop, spi
from luma.core.render import canvas
from luma.led_matrix.device import max7219

# port=0, device=0 is /dev/spidev0.0 — the CE0 pin you wired CS to.
serial = spi(port=0, device=0, gpio=noop())
device = max7219(serial, width=8, height=8, block_orientation=0, rotate=0)

# 0-255. Anything above about 32 is uncomfortable to look at indoors.
device.contrast(16)

# canvas draws into a buffer and flushes it to the matrix on exit, so the
# display never shows a half-drawn frame.
with canvas(device) as draw:
    draw.point((2, 3), fill="white")

sleep(5)
\`\`\`

\`\`\`bash
python single_led.py
\`\`\`

One LED, third column, fourth row — \`(x, y)\` counted from the top-left corner,
starting at zero. \`fill="white"\` is simply "on"; the matrix has no colour.

> Nothing lights up? Check \`block_orientation\`. Modules from different batches
> mount the chip rotated, and \`0\`, \`90\` or \`-90\` is the difference between a
> picture and nothing. Try each.

## Step 4: Draw a bit more

The \`draw\` object is a Pillow drawing surface — anything Pillow can do onto 8x8
pixels works:

\`\`\`python shapes.py
from time import sleep

from luma.core.interface.serial import noop, spi
from luma.core.render import canvas
from luma.led_matrix.device import max7219

serial = spi(port=0, device=0, gpio=noop())
device = max7219(serial, width=8, height=8, block_orientation=0)
device.contrast(16)

# A border
with canvas(device) as draw:
    draw.rectangle((0, 0, 7, 7), outline="white", fill="black")
sleep(2)

# A diagonal
with canvas(device) as draw:
    draw.line((0, 0, 7, 7), fill="white")
sleep(2)

# A dot walking the top row
for x in range(8):
    with canvas(device) as draw:
        draw.point((x, 0), fill="white")
    sleep(0.1)

device.clear()
\`\`\`

## Step 5: Scroll a message

\`\`\`python scroll_text.py
from luma.core.interface.serial import noop, spi
from luma.core.legacy import show_message
from luma.core.legacy.font import LCD_FONT, proportional
from luma.led_matrix.device import max7219

serial = spi(port=0, device=0, gpio=noop())
device = max7219(serial, width=8, height=8, block_orientation=0)
device.contrast(16)

show_message(
    device,
    "HELLO",
    fill="white",
    font=proportional(LCD_FONT),
    scroll_delay=0.06,
)
\`\`\`

\`scroll_delay\` is seconds per column — smaller is faster. \`proportional()\` trims
the blank columns either side of each glyph, which matters a great deal when the
display is eight pixels wide.

## Chaining modules

Four modules soldered in a row are one 32x8 display as far as the library is
concerned. Wire \`DOUT\` of each board to \`DIN\` of the next, share power and
ground, and tell it how many there are:

\`\`\`python
device = max7219(serial, cascaded=4, block_orientation=-90, rotate=0)
\`\`\`

\`cascaded\` is the module count; \`block_orientation\` is per-module and usually
needs \`-90\` on the common FC-16 strips.

## When it does not work

- **Nothing at all.** Recheck the wiring, then confirm \`/dev/spidev0.0\` exists.
  DIN and CLK swapped is the most common mistake.
- **All LEDs on, permanently.** No data is reaching the chip — that is its
  power-on state. Suspect CS, and confirm you are on \`device=0\` if CS is on CE0.
- **Garbled or shifted output.** \`block_orientation\` again — try \`90\` and \`-90\`.
- **Flickering.** Power. A single module is fine off the Pi; several are not.
- **\`No such file or directory: '/dev/spidev0.0'\`.** SPI is not enabled, or the
  reboot after enabling it has not happened.
- **\`PermissionError\`.** Not in the \`spi\` group yet, or not logged out since
  being added.

## Where to take it

- A clock, using \`show_message\` on a timer
- A scrolling notifier fed by a sensor reading
- Chained modules for text that fits more than five letters
- \`luma.core.sprite_system\` for frame-by-frame animation
`,oe=`Raspberry Pi OS Bookworm refuses \`pip install\` outside a virtual environment. It
is not being difficult: the system Python is managed by \`apt\`, and pip writing
into it is how a Pi ends up with a broken desktop after a routine upgrade. The
error even tells you the answer — \`externally-managed-environment\`.

So OpenCV goes in a virtual environment. This sets one up at a known path,
installs OpenCV and NumPy into it, proves the camera stack works, and gives you a
requirements file so the whole thing can be rebuilt from scratch.

## What you need

- Raspberry Pi 5 running **Raspberry Pi OS (64-bit)**, Bookworm or newer
- An internet connection
- A terminal — locally, over SSH, or through VNC

## Step 1: Update first

\`\`\`bash
sudo apt update && sudo apt full-upgrade -y
\`\`\`

\`update\` refreshes the package lists; \`full-upgrade\` installs the newer versions,
including kernel and firmware. Reboot afterwards if the firmware changed.

## Step 2: Create the environment

Install the \`venv\` module if it is not already there:

\`\`\`bash
sudo apt install -y python3-venv python3-pip
\`\`\`

Then make a project folder and an environment inside it:

\`\`\`bash
mkdir -p ~/projects/vision
cd ~/projects/vision
python3 -m venv venv
\`\`\`

> Add \`--system-site-packages\` if you also want the \`apt\`-installed system
> packages visible inside the environment — \`picamera2\` and \`gpiozero\` ship that
> way and are painful to install with pip:
>
> \`\`\`bash
> python3 -m venv --system-site-packages venv
> \`\`\`

## Step 3: Activate it

\`\`\`bash
source ~/projects/vision/venv/bin/activate
\`\`\`

The prompt gains a \`(venv)\` prefix. That prefix is the only reliable sign you are
inside it — check for it before every \`pip install\`.

Save yourself the typing with a shell alias:

\`\`\`bash
echo "alias vision='source ~/projects/vision/venv/bin/activate'" >> ~/.bashrc
source ~/.bashrc
\`\`\`

## Step 4: Install OpenCV

Upgrade pip first, then install:

\`\`\`bash
pip install --upgrade pip
pip install numpy opencv-python
\`\`\`

On a Pi 5 with 64-bit Bookworm this pulls a prebuilt wheel and takes a couple of
minutes. If pip starts compiling from source instead, you are on a 32-bit OS or
an older release — stop it, and install the system build instead:

\`\`\`bash
sudo apt install -y python3-opencv
\`\`\`

Building OpenCV from source on the Pi works but takes hours; do it only when you
need a module the wheels leave out.

> Want the extra algorithms — SIFT, tracking, the \`ximgproc\` filters? Install
> \`opencv-contrib-python\` **instead of** \`opencv-python\`, never both. Having both
> in one environment produces import errors that make no sense.

## Step 5: Check it works

\`\`\`python check_opencv.py
import cv2
import numpy as np

print("OpenCV:", cv2.__version__)
print("NumPy :", np.__version__)

# A gradient, written out as a file — proves the imaging path end to end
# without needing a camera or a display attached.
image = np.zeros((240, 320, 3), dtype=np.uint8)
image[:, :, 1] = np.linspace(0, 255, 320, dtype=np.uint8)
cv2.imwrite("check.png", image)

print("Wrote check.png")
\`\`\`

Run it:

\`\`\`bash
python check_opencv.py
\`\`\`

Three lines of output and a \`check.png\` next to the script means the install is
sound.

## Step 6: Leave the environment

\`\`\`bash
deactivate
\`\`\`

The prompt loses its prefix and \`python\` means the system interpreter again.
Nothing is uninstalled — activate it again whenever you come back.

## Recording what you installed

Write the exact versions down once things work:

\`\`\`bash
pip freeze > requirements.txt
\`\`\`

Then the environment can be rebuilt anywhere:

\`\`\`bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
\`\`\`

Keep \`requirements.txt\` in the project folder and out of the environment folder —
\`venv/\` is disposable and should not be copied around or committed.

For a vision project that also does machine learning, this is the set worth
starting from:

\`\`\`bash
pip install numpy opencv-python matplotlib pillow tqdm
pip install mediapipe          # face, hand and pose landmarks
pip install scikit-learn       # classical models, no GPU needed
\`\`\`

> TensorFlow on a Pi 5 is possible and slow. If inference is the goal rather than
> training, reach for **TensorFlow Lite** or MediaPipe's bundled models instead —
> both run several times faster on this hardware.

## When it does not work

- **\`error: externally-managed-environment\`** — you are outside the virtual
  environment. Activate it. Do not pass \`--break-system-packages\`; the name is a
  fair description of what it does.
- **\`ImportError: libGL.so.1: cannot open shared object file\`** — the headless
  OS image lacks the GL libraries OpenCV's GUI functions want:

  \`\`\`bash
  sudo apt install -y libgl1 libglib2.0-0
  \`\`\`

  Or use \`opencv-python-headless\` if you never call \`cv2.imshow\`.
- **\`cv2.imshow\` fails over SSH** — there is no display to draw on. Connect with
  \`ssh -X\`, work through VNC, or write frames to disk with \`cv2.imwrite\`.
- **\`pip\` is killed part-way through** — it ran out of memory. Add swap
  temporarily, or install one package at a time.
- **The camera is not found** — the modern camera stack is \`picamera2\`, not
  OpenCV's \`VideoCapture\`. Install it with \`apt\`, and create the environment with
  \`--system-site-packages\` so it is importable.

## Further reading

- [OpenCV documentation](https://docs.opencv.org/)
- [Python virtual environments](https://docs.python.org/3/tutorial/venv.html)
`,se=`The hosts file is the last word on name resolution: whatever it says wins, before
DNS is ever consulted. That makes it useful for pointing a domain at a test
server and equally useful for blocking things — which is why installers, ad
blockers and licence-patching scripts all like to write to it, and why a site
that "works everywhere except this PC" usually traces back here.

Resetting it puts the file back to the contents Windows ships with, which is to
say a header comment and nothing else.

> Everything in the file is lost when you reset it. If you put entries there on
> purpose — a staging domain, a local service, a block list — copy them out
> first. Step 1 does exactly that.

## Before you start

- **Administrator rights.** The file lives under \`System32\` and is not writable
  by a standard user.
- **Windows PowerShell**, which is already installed. Open it with \`Win + X\` and
  **Terminal (Admin)** or **Windows PowerShell (Admin)**, and accept the UAC
  prompt.
- **Know what a reset costs you.** If a program you rely on wrote entries here,
  it will need to write them again.

The file itself is always at:

\`\`\`text
C:\\Windows\\System32\\drivers\\etc\\hosts
\`\`\`

## Step 1: Read it, then back it up

Look at what is actually in there before you throw it away:

\`\`\`powershell
Get-Content "$env:SystemRoot\\System32\\drivers\\etc\\hosts"
\`\`\`

Then keep a timestamped copy next to the original:

\`\`\`powershell
$hosts = "$env:SystemRoot\\System32\\drivers\\etc\\hosts"
Copy-Item $hosts "$hosts.backup-$(Get-Date -Format 'yyyyMMdd-HHmmss')"
\`\`\`

The backup sits in the same folder, so restoring is a rename away.

> Anything mapping a licensing or activation domain to \`127.0.0.1\` or \`0.0.0.0\`
> was put there to block that domain. Removing it is the point of the reset —
> just know that whatever depended on the block will start reaching the internet
> again.

## Step 2: Write the default file back

This is the file exactly as Windows ships it — every line a comment, no active
mappings:

\`\`\`powershell reset-hosts.ps1
$hosts = "$env:SystemRoot\\System32\\drivers\\etc\\hosts"

Set-Content -Path $hosts -Encoding ASCII -Value @"
# Copyright (c) 1993-2009 Microsoft Corp.
#
# This is a sample HOSTS file used by Microsoft TCP/IP for Windows.
#
# This file contains the mappings of IP addresses to host names. Each
# entry should be kept on an individual line. The IP address should
# be placed in the first column followed by the corresponding host name.
# The IP address and the host name should be separated by at least one
# space.
#
# Additionally, comments (such as these) may be inserted on individual
# lines or following the machine name denoted by a '#' symbol.
#
# For example:
#
#      102.54.94.97     rhino.acme.com          # source server
#       38.25.63.10     x.acme.com              # x client host

# localhost name resolution is handled within DNS itself.
#	127.0.0.1       localhost
#	::1             localhost
"@
\`\`\`

\`-Encoding ASCII\` matters. Windows reads the hosts file happily as ASCII or
UTF-8 without a BOM, and PowerShell 5.1 will otherwise write UTF-16 — which the
resolver treats as garbage and ignores wholesale.

> \`127.0.0.1 localhost\` is commented out on purpose. Modern Windows resolves
> \`localhost\` internally and does not need the line; uncommenting it is harmless
> but pointless.

## Step 3: Flush the cache

Entries already resolved stay in the cache until it is cleared, so the reset can
look like it did nothing:

\`\`\`powershell
ipconfig /flushdns
\`\`\`

No restart is needed — the hosts file is read on demand.

## Step 4: Check that it took

Print the file back:

\`\`\`powershell
Get-Content "$env:SystemRoot\\System32\\drivers\\etc\\hosts"
\`\`\`

Then confirm a domain that was previously redirected now resolves to a real
address:

\`\`\`powershell
Resolve-DnsName example.com | Select-Object Name, IPAddress
\`\`\`

If the address still points at \`127.0.0.1\` or \`0.0.0.0\`, the cache did not clear
or something rewrote the file — see below.

## When it does not work

- **"Access to the path is denied."** PowerShell is not elevated. Close it and
  reopen with **Run as administrator**.
- **The file reverts on its own.** Something is putting the entries back —
  usually security software or an activation tool with a scheduled task. Check
  \`Task Scheduler\` and your antivirus quarantine.
- **The file is read-only.** Clear the attribute and try again:

  \`\`\`powershell
  Set-ItemProperty "$env:SystemRoot\\System32\\drivers\\etc\\hosts" -Name IsReadOnly -Value $false
  \`\`\`

- **Windows Defender blocks the write.** Controlled folder access counts
  \`System32\` as protected. Allow the app under **Windows Security → Virus &
  threat protection → Ransomware protection**, or turn the feature off long
  enough to make the change.
- **You want the old entries back.** Rename the backup from Step 1 over the top
  of \`hosts\`, or copy the lines you actually wanted out of it.

## Further reading

- [Microsoft on the hosts file](https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/hosts)
- [PowerShell scripting overview](https://docs.microsoft.com/en-us/powershell/scripting/overview?view=powershell-7.1)
`,ce=`Per-project virtual environments are the right default. They are also tedious
when the "project" is a scratch file you will delete in an hour — and on Windows,
\`pip install\` into the system Python is how a machine ends up with three Pythons
and a \`PATH\` nobody can explain.

The middle ground is one environment at a fixed path, \`C:\\global_env\`, driven by
four small batch files: install Python, create the environment, open a shell
inside it, list what is in it. VS Code points at it once and forgets about it.

> Every script below is printed in full. Use **Save** on a block to write the
> \`.bat\` straight out of this page, or copy it into Notepad yourself — nothing is
> hosted anywhere, and you should read a script before you run it either way.

## What you end up with

\`\`\`text
C:\\Python\\Python-3.10.10\\   the interpreter
C:\\global_env\\              the virtual environment
  Scripts\\activate.bat      what the shells call
  Scripts\\python.exe        what VS Code points at
  Lib\\site-packages\\        everything you pip install
\`\`\`

Keep the four \`.bat\` files together in a folder you can find again — \`C:\\pyenv\`
or a **Tools** folder on the desktop. They are pinnable to the taskbar, which is
most of the convenience.

## Step 1: Install Python

If Python is already installed and \`python --version\` prints something you are
happy with, skip to Step 2 and point \`PYTHON_PATH\` at it instead.

This downloads a known version and installs it silently at a known path — no
wizard, no per-user surprise location:

\`\`\`batch 1_install_python.bat
@echo off
REM Downloads and installs a specific Python at a fixed path.
set "PYTHON_VERSION=3.10.10"
set "PYTHON_URL=https://www.python.org/ftp/python/%PYTHON_VERSION%/python-%PYTHON_VERSION%-amd64.exe"
set "INSTALL_PATH=C:\\Python\\Python-%PYTHON_VERSION%"

echo Downloading Python %PYTHON_VERSION%...
curl -o python_installer.exe %PYTHON_URL%
if not exist python_installer.exe (
    echo Download failed. Check your connection and try again.
    pause
    exit /b 1
)

echo Installing Python %PYTHON_VERSION% to %INSTALL_PATH%...
python_installer.exe /quiet InstallAllUsers=1 PrependPath=1 TargetDir="%INSTALL_PATH%"

del python_installer.exe
echo Done. Python %PYTHON_VERSION% is at %INSTALL_PATH%.
pause
\`\`\`

Run it **as administrator** — \`InstallAllUsers=1\` writes outside your profile.

What the installer flags mean:

- **\`/quiet\`** — no wizard.
- **\`InstallAllUsers=1\`** — machine-wide rather than per-user.
- **\`PrependPath=1\`** — puts Python on \`PATH\`, so \`python\` works in a new shell.
- **\`TargetDir\`** — the fixed path the next script depends on.

> Pinning the version is deliberate. \`3.10.10\` is old enough that every library
> in the list at the bottom has a wheel for it — TensorFlow and MediaPipe in
> particular lag behind the newest Python by a year or more. Change the version
> only after checking what you actually install still ships for it.

## Step 2: Create the environment

\`\`\`batch 2_create_venv.bat
@echo off
set "PYTHON_PATH=C:\\Python\\Python-3.10.10\\python.exe"
set "VENV_PATH=C:\\global_env"

if not exist "%PYTHON_PATH%" (
    echo Python not found at %PYTHON_PATH%. Run 1_install_python.bat first.
    pause
    exit /b 1
)

if not exist "%VENV_PATH%" (
    echo Creating directory at %VENV_PATH%...
    mkdir "%VENV_PATH%"
) else (
    echo Directory already exists: %VENV_PATH%
)

echo Creating virtual environment...
"%PYTHON_PATH%" -m venv "%VENV_PATH%"

echo Virtual environment created in %VENV_PATH%.
pause
\`\`\`

It calls the interpreter by full path rather than whatever \`python\` happens to
mean in that shell, which is the difference between this working every time and
working on the machine you wrote it on.

> Creating a folder directly under \`C:\\\` needs administrator rights on most
> setups. If it fails, either run the file as administrator or move \`VENV_PATH\`
> somewhere inside your profile, such as \`%USERPROFILE%\\global_env\` — update the
> other scripts to match.

## Step 3: Open a shell inside it

\`\`\`batch 3_activate_venv.bat
@echo off
set "VENV_PATH=C:\\global_env"
set "VENV_ACTIVATE=%VENV_PATH%\\Scripts\\activate.bat"

if exist "%VENV_ACTIVATE%" (
    echo Activating the virtual environment in a new terminal...
    start cmd /k "%VENV_ACTIVATE%"
) else (
    echo Virtual environment not found. Run 2_create_venv.bat first.
    pause
)
\`\`\`

\`start cmd /k\` opens a new Command Prompt and leaves it open — activating in the
window that runs the batch file would be pointless, because the shell dies with
the script. The new prompt is prefixed \`(global_env)\`.

## Step 4: See what is installed

\`\`\`batch 4_list_packages.bat
@echo off
set "VENV_PATH=C:\\global_env"
set "VENV_ACTIVATE=%VENV_PATH%\\Scripts\\activate.bat"

if exist "%VENV_ACTIVATE%" (
    echo Listing installed packages...
    start cmd /k "call %VENV_ACTIVATE% && pip list"
) else (
    echo Virtual environment not found. Run 2_create_venv.bat first.
    pause
)
\`\`\`

## Step 5: Point VS Code at it

1. Install the **Python** extension — \`Ctrl + Shift + X\`, search \`Python\`, the
   Microsoft one.
2. \`Ctrl + Shift + P\` → **Python: Select Interpreter** → **Enter interpreter
   path**.
3. Give it \`C:\\global_env\\Scripts\\python.exe\`.

The interpreter shows in the status bar, new terminals open already activated,
and **Run Python File** uses it without further ceremony.

To make it the default for every folder you open, put this in your user
\`settings.json\` — \`Ctrl + Shift + P\` → **Preferences: Open User Settings (JSON)**:

\`\`\`json settings.json
{
  "python.defaultInterpreterPath": "C:\\\\global_env\\\\Scripts\\\\python.exe",
  "python.terminal.activateEnvironment": true
}
\`\`\`

## Step 6: Install what you need

From an activated prompt — Step 3 gives you one:

\`\`\`batch
python.exe -m pip install --upgrade pip
pip install numpy pandas matplotlib
\`\`\`

The set this environment was originally built for, if you want the whole thing
in one go:

\`\`\`batch install_libs.bat
@echo off
call C:\\global_env\\Scripts\\activate.bat

python.exe -m pip install --upgrade pip
pip install flask pymongo flask_pymongo python-dotenv ^
    pandas numpy tqdm nltk ^
    matplotlib seaborn scikit-learn ^
    opencv-python mediapipe Pillow ^
    tensorflow keras

pip list
pause
\`\`\`

That is a large download — TensorFlow alone is several hundred megabytes. Install
what you need and come back for the rest.

Write down what ended up in there, so the environment can be rebuilt:

\`\`\`batch
pip freeze > C:\\global_env\\requirements.txt
\`\`\`

## When it goes wrong

- **\`'python' is not recognized\`** — the \`PATH\` change from the installer only
  applies to shells opened afterwards. Open a new terminal. If it still fails,
  call the interpreter by full path.
- **Activation does nothing** — check \`C:\\global_env\\Scripts\\activate.bat\`
  exists. If it does not, Step 2 did not complete; delete the folder and run it
  again.
- **VS Code ignores the interpreter** — reload the window (\`Ctrl + Shift + P\` →
  **Developer: Reload Window**). The Python extension caches its list.
- **The file saved as \`create_env.bat.txt\`** — Explorer is hiding known
  extensions. Turn on **View → Show → File name extensions** and rename it.
- **PowerShell refuses \`activate.ps1\`** — that is the execution policy, and it is
  why these scripts use \`cmd\`. Either keep using Command Prompt or allow local
  scripts for your user:

  \`\`\`powershell
  Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
  \`\`\`

- **One project needs a different version of something** — that is the moment to
  give that project its own environment. The global one is for scratch work, not
  for pinning a dependency tree.

## Further reading

- [Python virtual environments](https://docs.python.org/3/tutorial/venv.html)
- [Python in VS Code](https://code.visualstudio.com/docs/python/python-tutorial)
`,le=`LTSC is the version of Windows 11 built for machines that are supposed to do one
job for years: no Microsoft Store, no Copilot, no Teams, no widgets, no
preinstalled games, and feature updates that arrive every few years instead of
twice a year. On an older laptop that is not a compromise, it is the point — the
same hardware boots faster and idles with noticeably less running behind it.

This walks the whole thing: getting an ISO, writing it with Rufus, installing
clean, and the handful of things worth doing before you call the machine done.

> LTSC is a volume-licensing edition. Microsoft's Evaluation Center offers a
> 90-day evaluation ISO of **Windows 11 Enterprise LTSC** that anyone can
> download, which is fine for trying it on a spare machine; running it long term
> needs a licence that covers it.

## What the hardware needs

| Component | Minimum                                                     |
| --------- | ----------------------------------------------------------- |
| CPU       | 1 GHz, 2+ cores, 64-bit                                     |
| RAM       | 4 GB                                                        |
| Storage   | 64 GB                                                       |
| Firmware  | UEFI, Secure Boot capable                                   |
| TPM       | 2.0                                                         |
| Graphics  | DirectX 12 / WDDM 2.x                                       |
| Display   | 9" or larger, 720p                                          |

You also need a USB drive of **8 GB or more** — it gets wiped — and a second
working machine to write it from.

> Older hardware often fails on TPM 2.0 or Secure Boot alone. Rufus can write an
> installer that skips those checks (see the Rufus step); Microsoft does not
> support the result, and such machines may not receive updates. Decide before
> you wipe the disk, not after.

## Step 1: Get the ISO

1. Go to the **Microsoft Evaluation Center** and find **Windows 11 Enterprise
   LTSC**, or download the ISO from your volume-licensing portal if you have one.
2. Pick your language and architecture (**x64**).
3. Save the ISO somewhere with room to spare — it is 4–5 GB.

Check the download before you trust it. Compare the hash against the one the
download page lists:

\`\`\`powershell
Get-FileHash .\\Windows11_LTSC.iso -Algorithm SHA256
\`\`\`

> Download the ISO from Microsoft. The "free Windows ISO" mirrors that turn up
> first in search results are the single most common way a machine gets
> compromised before it has even finished installing.

## Step 2: Install Rufus

Rufus writes the ISO to the USB drive and handles the partitioning for you.

1. Download it from [rufus.ie](https://rufus.ie) — take the portable build if
   you would rather not install anything.
2. Run it. It asks for administrator rights, because writing a raw disk needs
   them.

## Step 3: Write the USB drive

1. Plug in the USB drive. **Everything on it will be erased.**
2. In Rufus, pick it under **Device** — check the size and letter twice; Rufus
   will happily overwrite an external hard disk if you point it at one.
3. Click **SELECT** and choose the ISO.
4. Set the options:
   - **Image option**: \`Standard Windows installation\`
   - **Partition scheme**: \`GPT\` for UEFI, which is nearly everything from about
     2013 onwards; \`MBR\` only for genuinely old BIOS machines
   - **Target system**: fills itself in from the partition scheme
   - **File system**: \`NTFS\`
   - **Volume label**: anything — \`WIN11LTSC\` does the job
5. Click **START**. Rufus may offer to **remove the requirement for 4 GB+ RAM,
   Secure Boot and TPM 2.0**, and to skip the Microsoft-account requirement. Tick
   those only if your machine actually needs them.
6. Wait for **READY**, then eject the drive properly rather than pulling it out.

## Step 4: Boot the target PC from it

1. Plug the drive into the machine you are installing on.
2. Restart and hold the boot-menu key. It differs by make:

   | Make               | Boot menu   | Firmware setup |
   | ------------------ | ----------- | -------------- |
   | Dell               | \`F12\`       | \`F2\`           |
   | HP                 | \`F9\`        | \`F10\`          |
   | Lenovo             | \`F12\`       | \`F1\` / \`F2\`    |
   | Acer               | \`F12\`       | \`F2\`           |
   | ASUS               | \`Esc\`/\`F8\`  | \`F2\` / \`Del\`   |
   | Most desktop boards| \`F11\`/\`F8\`  | \`Del\`          |

3. Choose the USB entry. Prefer the one prefixed **UEFI:** if both appear.

> No USB entry in the list? Turn **Fast Boot** off in firmware setup, and if the
> drive was written as GPT/UEFI make sure **CSM / Legacy boot** is off too. A
> mismatch between how the stick was written and how the firmware boots is the
> usual reason it does not show up.

## Step 5: Install

1. Confirm language, time and keyboard, then **Next → Install now**.
2. At the product key prompt, choose **I don't have a product key** if you plan
   to activate later.
3. Choose **Custom: Install Windows only (advanced)**. Upgrade is not what you
   want on a machine you are rebuilding.
4. Partition the disk. For a clean install, delete every partition on the target
   disk until it is a single block of unallocated space, select it and click
   **Next** — Windows creates the EFI, MSR and recovery partitions itself.

   > This erases the disk. If anything on it matters, stop here and copy it off
   > first. Double-check you are looking at the right disk if the machine has
   > more than one.

5. Let it run. It restarts a few times; pull the USB drive out after the first
   restart so it does not boot the installer again.
6. Work through the out-of-box setup — region, keyboard, network, account name.

## Step 6: Activate

**Settings → System → Activation.** Enter the key you hold, or connect the
machine to the network that handles activation for you. Evaluation installs run
for 90 days without one.

## Step 7: Drivers and the first hour

1. **Windows Update** first: **Settings → Windows Update**, then
   **Advanced options → Optional updates**, which is where most driver updates
   hide.
2. **The manufacturer's own drivers** next, from their support page for your
   exact model — chipset, graphics, Wi-Fi, audio. LTSC ships a thin driver set on
   purpose.
3. Check nothing is left unrecognised:

   \`\`\`powershell
   Get-PnpDevice -Status Error, Unknown | Select-Object FriendlyName, Class
   \`\`\`

4. Install what you actually use — a browser, an archiver, your editor. There is
   no Store here to fall back on, so \`winget\` is the fastest route if you want
   one:

   \`\`\`powershell
   winget install --id Mozilla.Firefox -e
   \`\`\`

5. Confirm the machine is protected: **Windows Security → Virus & threat
   protection** should report Defender as active.

## Worth knowing afterwards

- **No Microsoft Store.** Some apps distribute only there. Check that the ones
  you depend on have a standalone installer before you commit a machine to LTSC.
- **Feature updates are rare by design.** Security updates still arrive monthly;
  the interface simply stops changing under you.
- **It is genuinely lighter.** On 4 GB machines the difference in idle memory
  against a standard Windows 11 install is the whole reason to do this.
`,ue=`awesome-windows`,M=e=>`https://github.com/${te}/${ue}/tree/main/${encodeURIComponent(e)}`,de=[{slug:`windows-11-ltsc-setup`,title:`Install Windows 11 LTSC on a low-spec PC`,tagline:`The lean Windows, from ISO to first boot.`,summary:`LTSC drops the Store, Copilot and the preinstalled clutter, which is exactly what an old machine wants. Download the ISO, write it with Rufus, install, and clean up afterwards.`,category:`Windows`,tags:[`Windows`,`Install`,`LTSC`],updated:`2026-07-26`,accent:`#60a5fa`,folder:`Windows - Windows 11 LTSC Setup`,markdown:le},{slug:`office-365-ltsc-setup`,title:`Install and activate Office 365 LTSC`,tagline:`One XML file, one command.`,summary:`Office LTSC installs through the Office Deployment Tool rather than a wizard. Write the configuration file, run setup from an elevated prompt, then activate — with the licence you hold or against your KMS host.`,category:`Windows`,tags:[`Windows`,`Office`,`LTSC`],updated:`2026-07-26`,accent:`#f97316`,folder:`Windows - Office 365 LTSC Setup`,markdown:O},{slug:`microsoft-store-slow-downloads`,title:`Fix slow or stuck Microsoft Store downloads`,tagline:`When the progress bar refuses to move.`,summary:`Four methods, cheapest first — free the bandwidth, reset the Store cache, clear the delivery-optimisation files, then re-register the app if it is still sulking.`,category:`Windows`,tags:[`Windows`,`Troubleshooting`,`Store`],updated:`2026-07-26`,accent:`#34d399`,folder:`Windows - Microsoft Store Slow Downloading Fix`,markdown:D},{slug:`reset-windows-hosts-file`,title:`Reset the Windows hosts file`,tagline:`Put DNS back the way Windows shipped it.`,summary:`A blocked site or a stale override usually traces back to the hosts file. Back up what is there, write the default contents back with PowerShell, and flush the DNS cache.`,category:`Windows`,tags:[`Windows`,`Networking`,`PowerShell`],updated:`2026-07-26`,accent:`#a78bfa`,folder:`Windows - Reset Windows Host`,markdown:se},{slug:`vscode-global-python-venv`,title:`A global Python virtual environment for VS Code`,tagline:`One environment, four batch files.`,summary:`Keeps a single virtual environment at C:\\global_env and drives it with batch files you save straight off the page — install Python, create it, open a shell inside it, list what is in it — then points VS Code at the interpreter.`,category:`Developer setup`,tags:[`Windows`,`Python`,`VS Code`],updated:`2026-07-26`,accent:`#38bdf8`,folder:`VS Code (Windows) - Setting Up a Global Python Virtual Environment`,markdown:ce},{slug:`raspberry-pi-5-headless-setup`,title:`Set up a Raspberry Pi 5 headless, from Windows`,tagline:`No monitor, no keyboard — just WiFi.`,summary:`Flash Raspberry Pi OS with Imager and preconfigure SSH and WiFi, find the board with Angry IP Scanner, connect over PuTTY and VNC, and keep a phone hotspot as the fallback route in.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`SSH`,`Setup`],updated:`2026-07-26`,accent:`#f472b6`,folder:`Raspberry Pi 5 - Setup Your Pi on Windows`,markdown:ae},{slug:`raspberry-pi-5-opencv-venv`,title:`OpenCV in a virtual environment on the Pi 5`,tagline:`Install it once, keep it contained.`,summary:`Bookworm blocks pip at system level, so OpenCV goes into a virtual environment. Create it, install, run a script against it and deactivate when you are done.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`Python`,`OpenCV`],updated:`2026-07-26`,accent:`#22d3ee`,folder:`Raspberry Pi 5 - Initializing Virtual Environment and OpenCV`,markdown:oe},{slug:`raspberry-pi-5-external-webcam`,title:`Use a USB webcam with the Raspberry Pi 5`,tagline:`Plug it in, prove it works.`,summary:`Confirm the Pi sees a Logitech C270, check what resolutions it actually supports with v4l-utils, grab a still with fswebcam and then read frames from OpenCV.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`Camera`,`OpenCV`],updated:`2026-07-26`,accent:`#facc15`,folder:`Raspberry Pi 5 - External Webcam Setup`,markdown:k},{slug:`raspberry-pi-5-laptop-webcam-stream`,title:`Stream a laptop webcam to the Raspberry Pi 5`,tagline:`Borrow the camera you already own.`,summary:`FFmpeg pushes the laptop camera over UDP, the Pi displays the feed with OpenCV, and a small Python script lets you snap stills and record clips from the stream.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`Camera`,`FFmpeg`],updated:`2026-07-26`,accent:`#fb7185`,folder:`Raspberry Pi 5 - Laptop Webcam Setup Wirelessly`,markdown:A},{slug:`raspberry-pi-5-max7219-led-matrix`,title:`Drive a MAX7219 8x8 LED matrix from the Pi 5`,tagline:`Eight by eight, one pixel at a time.`,summary:`Wiring table for the five pins, the libraries to install, and a first Python script that lights a single LED at the coordinates you ask for.`,category:`Raspberry Pi`,tags:[`Raspberry Pi`,`Electronics`,`Python`],updated:`2026-07-26`,accent:`#c084fc`,folder:`Raspberry Pi 5 - Integration of MAX7219 (8x8) LED Matrix Display`,markdown:j}].map(C(`articles`)).map(e=>({...e,sourceUrl:M(e.folder)})),fe=[...new Set(de.map(e=>e.category))].sort(),pe=e=>de.find(t=>t.slug===e)??null,me=`(prefers-reduced-motion: reduce)`,he=e=>Math.min(e,8)*55;function ge(){let[e,t]=(0,_.useState)(()=>window.matchMedia?.(me).matches??!1);return(0,_.useEffect)(()=>{let e=window.matchMedia?.(me);if(!e)return;let n=()=>t(e.matches);return e.addEventListener(`change`,n),()=>e.removeEventListener(`change`,n)},[]),e}function _e({threshold:e=.12,rootMargin:t=`0px 0px -8% 0px`}={}){let n=(0,_.useRef)(null),[r,i]=(0,_.useState)(!1);return(0,_.useEffect)(()=>{let r=n.current;if(!r)return;if(typeof IntersectionObserver>`u`){i(!0);return}let a=new IntersectionObserver(([e])=>{e.isIntersecting&&(i(!0),a.disconnect())},{threshold:e,rootMargin:t});return a.observe(r),()=>a.disconnect()},[e,t]),[n,r]}function ve(){let e=(0,_.useRef)(null);return{ref:e,onPointerMove:(0,_.useCallback)(t=>{let n=e.current;if(!n||t.pointerType===`touch`)return;let r=n.getBoundingClientRect();n.style.setProperty(`--mx`,`${(t.clientX-r.left)/r.width*100}%`),n.style.setProperty(`--my`,`${(t.clientY-r.top)/r.height*100}%`)},[]),onPointerLeave:(0,_.useCallback)(()=>{let t=e.current;t&&(t.style.removeProperty(`--mx`),t.style.removeProperty(`--my`))},[])}}function ye(){(0,_.useEffect)(()=>{let e=0,t=()=>{e=0;let t=document.documentElement.scrollHeight-window.innerHeight,n=t>0?window.scrollY/t:0;document.documentElement.style.setProperty(`--scroll-progress`,String(Math.min(1,Math.max(0,n))))},n=()=>{e||=requestAnimationFrame(t)};return t(),window.addEventListener(`scroll`,n,{passive:!0}),window.addEventListener(`resize`,n,{passive:!0}),()=>{cancelAnimationFrame(e),window.removeEventListener(`scroll`,n),window.removeEventListener(`resize`,n)}},[])}function be({threshold:e=6,hideAfter:t=220}={}){let[n,r]=(0,_.useState)({scrolled:!1,hidden:!1});return(0,_.useEffect)(()=>{let n=window.scrollY,i=0,a=()=>{i=0;let a=window.scrollY,o=a-n;if(Math.abs(o)<e)return;n=a;let s={scrolled:a>8,hidden:o>0&&a>t};r(e=>e.scrolled===s.scrolled&&e.hidden===s.hidden?e:s)},o=()=>{i||=requestAnimationFrame(a)};return window.addEventListener(`scroll`,o,{passive:!0}),()=>{cancelAnimationFrame(i),window.removeEventListener(`scroll`,o)}},[e,t]),n}var xe=e=>new Date(e).toLocaleDateString(void 0,{year:`numeric`,month:`short`,day:`numeric`}),Se=o((e=>{var t=Symbol.for(`react.transitional.element`),n=Symbol.for(`react.fragment`);function r(e,n,r){var i=null;if(r!==void 0&&(i=``+r),n.key!==void 0&&(i=``+n.key),`key`in n)for(var a in r={},n)a!==`key`&&(r[a]=n[a]);else r=n;return n=r.ref,{$$typeof:t,type:e,key:i,ref:n===void 0?null:n,props:r}}e.Fragment=n,e.jsx=r,e.jsxs=r})),N=o(((e,t)=>{t.exports=Se()}))(),Ce={search:`M11 4a7 7 0 1 0 4.2 12.6l3.6 3.6 1.4-1.4-3.6-3.6A7 7 0 0 0 11 4Zm0 2a5 5 0 1 1 0 10 5 5 0 0 1 0-10Z`,download:`M12 3v10.2l3.6-3.6 1.4 1.4-6 6-6-6 1.4-1.4L10 13.2V3h2ZM4 19h16v2H4v-2Z`,github:`M12 2a10 10 0 0 0-3.2 19.5c.5.1.7-.2.7-.5v-1.7c-2.8.6-3.4-1.3-3.4-1.3-.4-1.2-1.1-1.5-1.1-1.5-.9-.6.1-.6.1-.6 1 .1 1.5 1 1.5 1 .9 1.5 2.3 1.1 2.9.8.1-.7.4-1.1.6-1.4-2.2-.2-4.6-1.1-4.6-5 0-1.1.4-2 1-2.7-.1-.3-.4-1.3.1-2.6 0 0 .8-.3 2.7 1a9.4 9.4 0 0 1 5 0c1.9-1.3 2.7-1 2.7-1 .5 1.3.2 2.3.1 2.6.6.7 1 1.6 1 2.7 0 3.9-2.4 4.8-4.6 5 .4.3.7.9.7 1.9v2.8c0 .3.2.6.7.5A10 10 0 0 0 12 2Z`,close:`M6.4 5 5 6.4 10.6 12 5 17.6 6.4 19 12 13.4 17.6 19 19 17.6 13.4 12 19 6.4 17.6 5 12 10.6 6.4 5Z`,star:`M12 3.5l2.6 5.3 5.9.9-4.2 4.1 1 5.8-5.3-2.8-5.3 2.8 1-5.8-4.2-4.1 5.9-.9L12 3.5Z`,copy:`M8 2h10a2 2 0 0 1 2 2v12h-2V4H8V2ZM4 6h10a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V8a2 2 0 0 1 2-2Zm0 2v12h10V8H4Z`,check:`M9.5 16.2 5.3 12l-1.4 1.4 5.6 5.6L20.1 8.4l-1.4-1.4L9.5 16.2Z`,grid:`M3 3h8v8H3V3Zm10 0h8v8h-8V3ZM3 13h8v8H3v-8Zm10 0h8v8h-8v-8Z`,list:`M3 5h18v3H3V5Zm0 5.5h18v3H3v-3ZM3 16h18v3H3v-3Z`,sun:`M12 7a5 5 0 1 0 0 10 5 5 0 0 0 0-10Zm0-5h0v3m0 14v3M2 12h3m14 0h3M4.9 4.9l2.1 2.1m10 10 2.1 2.1m0-14.2-2.1 2.1m-10 10-2.1 2.1`,moon:`M20 14.5A8.5 8.5 0 0 1 9.5 4 8.5 8.5 0 1 0 20 14.5Z`,monitor:`M3 4h18v12H3V4Zm2 2v8h14V6H5Zm3 12h8v2H8v-2Z`,link:`M10.6 13.4a4 4 0 0 1 0-5.7l3-3a4 4 0 0 1 5.7 5.7l-1.3 1.3-1.4-1.4 1.3-1.3a2 2 0 0 0-2.8-2.9l-3 3a2 2 0 0 0 0 2.9l-1.5 1.4Zm2.8-2.8a4 4 0 0 1 0 5.7l-3 3a4 4 0 0 1-5.7-5.7l1.3-1.3 1.4 1.4-1.3 1.3a2 2 0 0 0 2.8 2.9l3-3a2 2 0 0 0 0-2.9l1.5-1.4Z`,shield:`M12 2 4 5v6.5c0 4.7 3.2 9 8 10.5 4.8-1.5 8-5.8 8-10.5V5l-8-3Zm0 2.2 6 2.2v5.1c0 3.6-2.4 7-6 8.3-3.6-1.3-6-4.7-6-8.3V6.4l6-2.2Z`,bolt:`M13 2 4 14h6l-1 8 9-12h-6l1-8Z`,arrow:`M13.2 5 11.8 6.4l4.6 4.6H3v2h13.4l-4.6 4.6 1.4 1.4 7-7-7-7Z`,terminal:`M3 4h18v16H3V4Zm2 2v12h14V6H5Zm2 2.5L8.4 7l3.6 3.6L8.4 14 7 12.6l2.2-2L7 8.5ZM13 13h4v1.6h-4V13Z`,book:`M5 3h9l5 5v13H5V3Zm2 2v14h10V9h-4V5H7Zm2 6h6v1.6H9V11Zm0 3.4h6V16H9v-1.6Z`,coffee:`M4 3h12v8a6 6 0 0 1-12 0V3Zm13 2h1.5a3.5 3.5 0 0 1 0 7H17v-2h1.5a1.5 1.5 0 0 0 0-3H17V5ZM3 19h14v2H3v-2Z`},we=new Set([`sun`]);function P({name:e,size:t=18,className:n=``}){let r=Ce[e];if(!r)return null;let i=we.has(e);return(0,N.jsx)(`svg`,{className:`icon ${n}`,width:t,height:t,viewBox:`0 0 24 24`,fill:i?`none`:`currentColor`,stroke:i?`currentColor`:`none`,strokeWidth:i?2:0,strokeLinecap:`round`,"aria-hidden":`true`,focusable:`false`,children:(0,N.jsx)(`path`,{d:r})})}function Te({article:e,onOpen:t}){let n=ve();return(0,N.jsxs)(`article`,{className:`card article-card`,style:{"--accent":e.accent},onClick:()=>t(e.route),...n,children:[(0,N.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,N.jsxs)(`header`,{className:`article-card-head`,children:[(0,N.jsx)(`span`,{className:`badge badge-article`,children:e.category}),(0,N.jsxs)(`span`,{className:`dim`,children:[e.minutes,` min read`]})]}),(0,N.jsx)(`h3`,{children:(0,N.jsx)(`button`,{type:`button`,className:`card-link`,onClick:n=>{n.stopPropagation(),t(e.route)},children:e.title})}),(0,N.jsx)(`p`,{className:`tagline`,children:e.tagline}),(0,N.jsx)(`p`,{className:`card-summary`,children:e.summary}),(0,N.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,N.jsx)(`li`,{children:e},e))}),(0,N.jsxs)(`footer`,{className:`card-foot`,children:[(0,N.jsxs)(`span`,{className:`dim`,children:[`Updated `,xe(e.updated)]}),(0,N.jsxs)(`span`,{className:`article-more`,children:[`Read `,(0,N.jsx)(P,{name:`arrow`,size:15})]})]})]})}function Ee({as:e=`div`,variant:t=`up`,delay:n=0,className:r=``,style:i,children:a,...o}){let[s,c]=_e();return(0,N.jsx)(e,{ref:s,className:`reveal-on-scroll reveal-${t} ${c?`is-in`:``} ${r}`,style:{"--delay":`${n}ms`,...i},...o,children:a})}var De=e=>[e.title,e.tagline,e.summary,e.category,...e.tags,e.content].join(` `).toLowerCase();function Oe({query:e,onQuery:t,category:n,onCategory:r,onOpen:i}){let a=(0,_.useMemo)(()=>{let t=e.trim().toLowerCase().split(/\s+/).filter(Boolean);return de.filter(e=>{if(n&&e.category!==n)return!1;if(!t.length)return!0;let r=De(e);return t.every(e=>r.includes(e))}).sort((e,t)=>t.updated.localeCompare(e.updated))},[e,n]);return(0,N.jsxs)(N.Fragment,{children:[(0,N.jsxs)(`section`,{className:`page-head`,children:[(0,N.jsxs)(`p`,{className:`eyebrow`,children:[(0,N.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`Written down once, in full`]}),(0,N.jsxs)(`h1`,{children:[`Things worth`,(0,N.jsx)(`br`,{}),(0,N.jsx)(`span`,{className:`grad`,children:`not looking up twice.`})]}),(0,N.jsx)(`p`,{className:`lede`,children:`Windows fixes and Raspberry Pi setups I had to work out the hard way. Every step is here on the page — nothing to download, nothing behind a drive link. Where a guide needs a script, the script is printed in full so you can read it, save it yourself and know what it does before you run it.`})]}),(0,N.jsxs)(`div`,{className:`toolbar`,children:[(0,N.jsx)(`div`,{className:`toolbar-row`,children:(0,N.jsxs)(`div`,{className:`search`,children:[(0,N.jsx)(P,{name:`search`,size:17,className:`search-icon`}),(0,N.jsx)(`input`,{type:`search`,value:e,placeholder:`Search guides, commands, steps…`,onChange:e=>t(e.target.value),"aria-label":`Search guides`})]})}),(0,N.jsxs)(`div`,{className:`chips`,children:[(0,N.jsx)(`button`,{type:`button`,className:`chip ${n?``:`is-active`}`,onClick:()=>r(null),children:`All`}),fe.map(e=>(0,N.jsx)(`button`,{type:`button`,className:`chip ${n===e?`is-active`:``}`,onClick:()=>r(n===e?null:e),"aria-pressed":n===e,children:e},e)),(0,N.jsxs)(`span`,{className:`chips-count`,children:[a.length,` `,a.length===1?`guide`:`guides`]})]})]}),a.length>0?(0,N.jsx)(`div`,{className:`catalog catalog-grid`,children:a.map((e,t)=>(0,N.jsx)(Ee,{className:`reveal-cell`,delay:he(t),children:(0,N.jsx)(Te,{article:e,onOpen:i})},e.slug))}):(0,N.jsxs)(`div`,{className:`empty empty-in`,children:[(0,N.jsx)(P,{name:`search`,size:28}),(0,N.jsx)(`h3`,{children:`No guide matches that`}),(0,N.jsx)(`p`,{children:`Try a different word, or clear the filter.`}),(0,N.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>{t(``),r(null)},children:`Clear filters`})]})]})}function ke({raised:e=!1}){let[t,n]=(0,_.useState)(!1),r=ge();return(0,_.useEffect)(()=>{let e=0,t=()=>{e=0,n(window.scrollY>640)},r=()=>{e||=requestAnimationFrame(t)};return t(),window.addEventListener(`scroll`,r,{passive:!0}),()=>{cancelAnimationFrame(e),window.removeEventListener(`scroll`,r)}},[]),(0,N.jsx)(`button`,{type:`button`,className:`back-to-top ${t?`is-shown`:``} ${e?`is-raised`:``}`,onClick:()=>window.scrollTo({top:0,behavior:r?`auto`:`smooth`}),"aria-label":`Back to top`,tabIndex:t?0:-1,"aria-hidden":!t||void 0,children:(0,N.jsx)(P,{name:`arrow`,size:17})})}var Ae=c(o(((e,t)=>{function n(e){return e instanceof Map?e.clear=e.delete=e.set=function(){throw Error(`map is read-only`)}:e instanceof Set&&(e.add=e.clear=e.delete=function(){throw Error(`set is read-only`)}),Object.freeze(e),Object.getOwnPropertyNames(e).forEach(t=>{let r=e[t],i=typeof r;(i===`object`||i===`function`)&&!Object.isFrozen(r)&&n(r)}),e}var r=class{constructor(e){e.data===void 0&&(e.data={}),this.data=e.data,this.isMatchIgnored=!1}ignoreMatch(){this.isMatchIgnored=!0}};function i(e){return e.replace(/&/g,`&amp;`).replace(/</g,`&lt;`).replace(/>/g,`&gt;`).replace(/"/g,`&quot;`).replace(/'/g,`&#x27;`)}function a(e,...t){let n=Object.create(null);for(let t in e)n[t]=e[t];return t.forEach(function(e){for(let t in e)n[t]=e[t]}),n}var o=`</span>`,s=e=>!!e.scope,c=(e,{prefix:t})=>{if(e.startsWith(`language:`))return e.replace(`language:`,`language-`);if(e.includes(`.`)){let n=e.split(`.`);return[`${t}${n.shift()}`,...n.map((e,t)=>`${e}${`_`.repeat(t+1)}`)].join(` `)}return`${t}${e}`},l=class{constructor(e,t){this.buffer=``,this.classPrefix=t.classPrefix,e.walk(this)}addText(e){this.buffer+=i(e)}openNode(e){if(!s(e))return;let t=c(e.scope,{prefix:this.classPrefix});this.span(t)}closeNode(e){s(e)&&(this.buffer+=o)}value(){return this.buffer}span(e){this.buffer+=`<span class="${e}">`}},u=(e={})=>{let t={children:[]};return Object.assign(t,e),t},d=class e{constructor(){this.rootNode=u(),this.stack=[this.rootNode]}get top(){return this.stack[this.stack.length-1]}get root(){return this.rootNode}add(e){this.top.children.push(e)}openNode(e){let t=u({scope:e});this.add(t),this.stack.push(t)}closeNode(){if(this.stack.length>1)return this.stack.pop()}closeAllNodes(){for(;this.closeNode(););}toJSON(){return JSON.stringify(this.rootNode,null,4)}walk(e){return this.constructor._walk(e,this.rootNode)}static _walk(e,t){return typeof t==`string`?e.addText(t):t.children&&(e.openNode(t),t.children.forEach(t=>this._walk(e,t)),e.closeNode(t)),e}static _collapse(t){typeof t!=`string`&&t.children&&(t.children.every(e=>typeof e==`string`)?t.children=[t.children.join(``)]:t.children.forEach(t=>{e._collapse(t)}))}},f=class extends d{constructor(e){super(),this.options=e}addText(e){e!==``&&this.add(e)}startScope(e){this.openNode(e)}endScope(){this.closeNode()}__addSublanguage(e,t){let n=e.root;t&&(n.scope=`language:${t}`),this.add(n)}toHTML(){return new l(this,this.options).value()}finalize(){return this.closeAllNodes(),!0}};function p(e){return e?typeof e==`string`?e:e.source:null}function m(e){return _(`(?=`,e,`)`)}function h(e){return _(`(?:`,e,`)*`)}function g(e){return _(`(?:`,e,`)?`)}function _(...e){return e.map(e=>p(e)).join(``)}function v(e){let t=e[e.length-1];return typeof t==`object`&&t.constructor===Object?(e.splice(e.length-1,1),t):{}}function y(...e){return`(`+(v(e).capture?``:`?:`)+e.map(e=>p(e)).join(`|`)+`)`}function b(e){return RegExp(e.toString()+`|`).exec(``).length-1}function x(e,t){let n=e&&e.exec(t);return n&&n.index===0}var ee=/\[(?:[^\\\]]|\\.)*\]|\(\??|\\([1-9][0-9]*)|\\./;function S(e,{joinWith:t}){let n=0;return e.map(e=>{n+=1;let t=n,r=p(e),i=``;for(;r.length>0;){let e=ee.exec(r);if(!e){i+=r;break}i+=r.substring(0,e.index),r=r.substring(e.index+e[0].length),e[0][0]===`\\`&&e[1]?i+=`\\`+String(Number(e[1])+t):(i+=e[0],e[0]===`(`&&n++)}return i}).map(e=>`(${e})`).join(t)}var C=/\b\B/,te=`[a-zA-Z]\\w*`,ne=`[a-zA-Z_]\\w*`,w=`\\b\\d+(\\.\\d+)?`,T=`(-?)(\\b0[xX][a-fA-F0-9]+|(\\b\\d+(\\.\\d*)?|\\.\\d+)([eE][-+]?\\d+)?)`,re=`\\b(0b[01]+)`,E=`!|!=|!==|%|%=|&|&&|&=|\\*|\\*=|\\+|\\+=|,|-|-=|/=|/|:|;|<<|<<=|<=|<|===|==|=|>>>=|>>=|>=|>>>|>>|>|\\?|\\[|\\{|\\(|\\^|\\^=|\\||\\|=|\\|\\||~`,ie=(e={})=>{let t=/^#![ ]*\//;return e.binary&&(e.begin=_(t,/.*\b/,e.binary,/\b.*/)),a({scope:`meta`,begin:t,end:/$/,relevance:0,"on:begin":(e,t)=>{e.index!==0&&t.ignoreMatch()}},e)},D={begin:`\\\\[\\s\\S]`,relevance:0},O={scope:`string`,begin:`'`,end:`'`,illegal:`\\n`,contains:[D]},k={scope:`string`,begin:`"`,end:`"`,illegal:`\\n`,contains:[D]},ae={begin:/\b(a|an|the|are|I'm|isn't|don't|doesn't|won't|but|just|should|pretty|simply|enough|gonna|going|wtf|so|such|will|you|your|they|like|more)\b/},A=function(e,t,n={}){let r=a({scope:`comment`,begin:e,end:t,contains:[]},n);r.contains.push({scope:`doctag`,begin:`[ ]*(?=(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):)`,end:/(TODO|FIXME|NOTE|BUG|OPTIMIZE|HACK|XXX):/,excludeBegin:!0,relevance:0});let i=y(`I`,`a`,`is`,`so`,`us`,`to`,`at`,`if`,`in`,`it`,`on`,/[A-Za-z]+['](d|ve|re|ll|t|s|n)/,/[A-Za-z]+[-][a-z]+/,/[A-Za-z][a-z]{2,}/);return r.contains.push({begin:_(/[ ]+/,`(`,i,/[.]?[:]?([.][ ]|[ ])/,`){3}`)}),r},j=A(`//`,`$`),oe=A(`/\\*`,`\\*/`),se=A(`#`,`$`),ce=Object.freeze({__proto__:null,APOS_STRING_MODE:O,BACKSLASH_ESCAPE:D,BINARY_NUMBER_MODE:{scope:`number`,begin:re,relevance:0},BINARY_NUMBER_RE:re,COMMENT:A,C_BLOCK_COMMENT_MODE:oe,C_LINE_COMMENT_MODE:j,C_NUMBER_MODE:{scope:`number`,begin:T,relevance:0},C_NUMBER_RE:T,END_SAME_AS_BEGIN:function(e){return Object.assign(e,{"on:begin":(e,t)=>{t.data._beginMatch=e[1]},"on:end":(e,t)=>{t.data._beginMatch!==e[1]&&t.ignoreMatch()}})},HASH_COMMENT_MODE:se,IDENT_RE:te,MATCH_NOTHING_RE:C,METHOD_GUARD:{begin:`\\.\\s*[a-zA-Z_]\\w*`,relevance:0},NUMBER_MODE:{scope:`number`,begin:w,relevance:0},NUMBER_RE:w,PHRASAL_WORDS_MODE:ae,QUOTE_STRING_MODE:k,REGEXP_MODE:{scope:`regexp`,begin:/\/(?=[^/\n]*\/)/,end:/\/[gimuy]*/,contains:[D,{begin:/\[/,end:/\]/,relevance:0,contains:[D]}]},RE_STARTERS_RE:E,SHEBANG:ie,TITLE_MODE:{scope:`title`,begin:te,relevance:0},UNDERSCORE_IDENT_RE:ne,UNDERSCORE_TITLE_MODE:{scope:`title`,begin:ne,relevance:0}});function le(e,t){e.input[e.index-1]===`.`&&t.ignoreMatch()}function ue(e,t){e.className!==void 0&&(e.scope=e.className,delete e.className)}function M(e,t){t&&e.beginKeywords&&(e.begin=`\\b(`+e.beginKeywords.split(` `).join(`|`)+`)(?!\\.)(?=\\b|\\s)`,e.__beforeBegin=le,e.keywords=e.keywords||e.beginKeywords,delete e.beginKeywords,e.relevance===void 0&&(e.relevance=0))}function de(e,t){Array.isArray(e.illegal)&&(e.illegal=y(...e.illegal))}function fe(e,t){if(e.match){if(e.begin||e.end)throw Error(`begin & end are not supported with match`);e.begin=e.match,delete e.match}}function pe(e,t){e.relevance===void 0&&(e.relevance=1)}var me=(e,t)=>{if(!e.beforeMatch)return;if(e.starts)throw Error(`beforeMatch cannot be used with starts`);let n=Object.assign({},e);Object.keys(e).forEach(t=>{delete e[t]}),e.keywords=n.keywords,e.begin=_(n.beforeMatch,m(n.begin)),e.starts={relevance:0,contains:[Object.assign(n,{endsParent:!0})]},e.relevance=0,delete n.beforeMatch},he=[`of`,`and`,`for`,`in`,`not`,`or`,`if`,`then`,`parent`,`list`,`value`],ge=`keyword`;function _e(e,t,n=ge){let r=Object.create(null);return typeof e==`string`?i(n,e.split(` `)):Array.isArray(e)?i(n,e):Object.keys(e).forEach(function(n){Object.assign(r,_e(e[n],t,n))}),r;function i(e,n){t&&(n=n.map(e=>e.toLowerCase())),n.forEach(function(t){let n=t.split(`|`);r[n[0]]=[e,ve(n[0],n[1])]})}}function ve(e,t){return t?Number(t):+!ye(e)}function ye(e){return he.includes(e.toLowerCase())}var be={},xe=e=>{console.error(e)},Se=(e,...t)=>{console.log(`WARN: ${e}`,...t)},N=(e,t)=>{be[`${e}/${t}`]||(console.log(`Deprecated as of ${e}. ${t}`),be[`${e}/${t}`]=!0)},Ce=Error();function we(e,t,{key:n}){let r=0,i=e[n],a={},o={};for(let e=1;e<=t.length;e++)o[e+r]=i[e],a[e+r]=!0,r+=b(t[e-1]);e[n]=o,e[n]._emit=a,e[n]._multi=!0}function P(e){if(Array.isArray(e.begin)){if(e.skip||e.excludeBegin||e.returnBegin)throw xe(`skip, excludeBegin, returnBegin not compatible with beginScope: {}`),Ce;if(typeof e.beginScope!=`object`||e.beginScope===null)throw xe(`beginScope must be object`),Ce;we(e,e.begin,{key:`beginScope`}),e.begin=S(e.begin,{joinWith:``})}}function Te(e){if(Array.isArray(e.end)){if(e.skip||e.excludeEnd||e.returnEnd)throw xe(`skip, excludeEnd, returnEnd not compatible with endScope: {}`),Ce;if(typeof e.endScope!=`object`||e.endScope===null)throw xe(`endScope must be object`),Ce;we(e,e.end,{key:`endScope`}),e.end=S(e.end,{joinWith:``})}}function Ee(e){e.scope&&typeof e.scope==`object`&&e.scope!==null&&(e.beginScope=e.scope,delete e.scope)}function De(e){Ee(e),typeof e.beginScope==`string`&&(e.beginScope={_wrap:e.beginScope}),typeof e.endScope==`string`&&(e.endScope={_wrap:e.endScope}),P(e),Te(e)}function Oe(e){function t(t,n){return new RegExp(p(t),`m`+(e.case_insensitive?`i`:``)+(e.unicodeRegex?`u`:``)+(n?`g`:``))}class n{constructor(){this.matchIndexes={},this.regexes=[],this.matchAt=1,this.position=0}addRule(e,t){t.position=this.position++,this.matchIndexes[this.matchAt]=t,this.regexes.push([t,e]),this.matchAt+=b(e)+1}compile(){this.regexes.length===0&&(this.exec=()=>null);let e=this.regexes.map(e=>e[1]);this.matcherRe=t(S(e,{joinWith:`|`}),!0),this.lastIndex=0}exec(e){this.matcherRe.lastIndex=this.lastIndex;let t=this.matcherRe.exec(e);if(!t)return null;let n=t.findIndex((e,t)=>t>0&&e!==void 0),r=this.matchIndexes[n];return t.splice(0,n),Object.assign(t,r)}}class r{constructor(){this.rules=[],this.multiRegexes=[],this.count=0,this.lastIndex=0,this.regexIndex=0}getMatcher(e){if(this.multiRegexes[e])return this.multiRegexes[e];let t=new n;return this.rules.slice(e).forEach(([e,n])=>t.addRule(e,n)),t.compile(),this.multiRegexes[e]=t,t}resumingScanAtSamePosition(){return this.regexIndex!==0}considerAll(){this.regexIndex=0}addRule(e,t){this.rules.push([e,t]),t.type===`begin`&&this.count++}exec(e){let t=this.getMatcher(this.regexIndex);t.lastIndex=this.lastIndex;let n=t.exec(e);if(this.resumingScanAtSamePosition()&&!(n&&n.index===this.lastIndex)){let t=this.getMatcher(0);t.lastIndex=this.lastIndex+1,n=t.exec(e)}return n&&(this.regexIndex+=n.position+1,this.regexIndex===this.count&&this.considerAll()),n}}function i(e){let t=new r;return e.contains.forEach(e=>t.addRule(e.begin,{rule:e,type:`begin`})),e.terminatorEnd&&t.addRule(e.terminatorEnd,{type:`end`}),e.illegal&&t.addRule(e.illegal,{type:`illegal`}),t}function o(n,r){let a=n;if(n.isCompiled)return a;[ue,fe,De,me].forEach(e=>e(n,r)),e.compilerExtensions.forEach(e=>e(n,r)),n.__beforeBegin=null,[M,de,pe].forEach(e=>e(n,r)),n.isCompiled=!0;let s=null;return typeof n.keywords==`object`&&n.keywords.$pattern&&(n.keywords=Object.assign({},n.keywords),s=n.keywords.$pattern,delete n.keywords.$pattern),s||=/\w+/,n.keywords&&=_e(n.keywords,e.case_insensitive),a.keywordPatternRe=t(s,!0),r&&(n.begin||=/\B|\b/,a.beginRe=t(a.begin),!n.end&&!n.endsWithParent&&(n.end=/\B|\b/),n.end&&(a.endRe=t(a.end)),a.terminatorEnd=p(a.end)||``,n.endsWithParent&&r.terminatorEnd&&(a.terminatorEnd+=(n.end?`|`:``)+r.terminatorEnd)),n.illegal&&(a.illegalRe=t(n.illegal)),n.contains||=[],n.contains=[].concat(...n.contains.map(function(e){return Ae(e===`self`?n:e)})),n.contains.forEach(function(e){o(e,a)}),n.starts&&o(n.starts,r),a.matcher=i(a),a}if(e.compilerExtensions||=[],e.contains&&e.contains.includes(`self`))throw Error("ERR: contains `self` is not supported at the top-level of a language.  See documentation.");return e.classNameAliases=a(e.classNameAliases||{}),o(e)}function ke(e){return e?e.endsWithParent||ke(e.starts):!1}function Ae(e){return e.variants&&!e.cachedVariants&&(e.cachedVariants=e.variants.map(function(t){return a(e,{variants:null},t)})),e.cachedVariants?e.cachedVariants:ke(e)?a(e,{starts:e.starts?a(e.starts):null}):Object.isFrozen(e)?a(e):e}var je=`11.11.1`,Me=class extends Error{constructor(e,t){super(e),this.name=`HTMLInjectionError`,this.html=t}},Ne=i,Pe=a,Fe=Symbol(`nomatch`),Ie=7,Le=function(e){let t=Object.create(null),i=Object.create(null),a=[],o=!0,s=`Could not find the language '{}', did you forget to load/include a language module?`,c={disableAutodetect:!0,name:`Plain text`,contains:[]},l={ignoreUnescapedHTML:!1,throwUnescapedHTML:!1,noHighlightRe:/^(no-?highlight)$/i,languageDetectRe:/\blang(?:uage)?-([\w-]+)\b/i,classPrefix:`hljs-`,cssSelector:`pre code`,languages:null,__emitter:f};function u(e){return l.noHighlightRe.test(e)}function d(e){let t=e.className+` `;t+=e.parentNode?e.parentNode.className:``;let n=l.languageDetectRe.exec(t);if(n){let t=O(n[1]);return t||(Se(s.replace(`{}`,n[1])),Se(`Falling back to no-highlight mode for this block.`,e)),t?n[1]:`no-highlight`}return t.split(/\s+/).find(e=>u(e)||O(e))}function p(e,t,n){let r=``,i=``;typeof t==`object`?(r=e,n=t.ignoreIllegals,i=t.language):(N(`10.7.0`,`highlight(lang, code, ...args) has been deprecated.`),N(`10.7.0`,`Please use highlight(code, options) instead.
https://github.com/highlightjs/highlight.js/issues/2277`),i=e,r=t),n===void 0&&(n=!0);let a={code:r,language:i};se(`before:highlight`,a);let o=a.result?a.result:v(a.language,a.code,n);return o.code=a.code,se(`after:highlight`,o),o}function v(e,n,i,a){let c=Object.create(null);function u(e,t){return e.keywords[t]}function d(){if(!E.keywords){D.addText(k);return}let e=0;E.keywordPatternRe.lastIndex=0;let t=E.keywordPatternRe.exec(k),n=``;for(;t;){n+=k.substring(e,t.index);let r=w.case_insensitive?t[0].toLowerCase():t[0],i=u(E,r);if(i){let[e,a]=i;if(D.addText(n),n=``,c[r]=(c[r]||0)+1,c[r]<=Ie&&(ae+=a),e.startsWith(`_`))n+=t[0];else{let n=w.classNameAliases[e]||e;m(t[0],n)}}else n+=t[0];e=E.keywordPatternRe.lastIndex,t=E.keywordPatternRe.exec(k)}n+=k.substring(e),D.addText(n)}function f(){if(k===``)return;let e=null;if(typeof E.subLanguage==`string`){if(!t[E.subLanguage]){D.addText(k);return}e=v(E.subLanguage,k,!0,ie[E.subLanguage]),ie[E.subLanguage]=e._top}else e=ee(k,E.subLanguage.length?E.subLanguage:null);E.relevance>0&&(ae+=e.relevance),D.__addSublanguage(e._emitter,e.language)}function p(){E.subLanguage==null?d():f(),k=``}function m(e,t){e!==``&&(D.startScope(t),D.addText(e),D.endScope())}function h(e,t){let n=1,r=t.length-1;for(;n<=r;){if(!e._emit[n]){n++;continue}let r=w.classNameAliases[e[n]]||e[n],i=t[n];r?m(i,r):(k=i,d(),k=``),n++}}function g(e,t){return e.scope&&typeof e.scope==`string`&&D.openNode(w.classNameAliases[e.scope]||e.scope),e.beginScope&&(e.beginScope._wrap?(m(k,w.classNameAliases[e.beginScope._wrap]||e.beginScope._wrap),k=``):e.beginScope._multi&&(h(e.beginScope,t),k=``)),E=Object.create(e,{parent:{value:E}}),E}function _(e,t,n){let i=x(e.endRe,n);if(i){if(e[`on:end`]){let n=new r(e);e[`on:end`](t,n),n.isMatchIgnored&&(i=!1)}if(i){for(;e.endsParent&&e.parent;)e=e.parent;return e}}if(e.endsWithParent)return _(e.parent,t,n)}function y(e){return E.matcher.regexIndex===0?(k+=e[0],1):(oe=!0,0)}function b(e){let t=e[0],n=e.rule,i=new r(n),a=[n.__beforeBegin,n[`on:begin`]];for(let n of a)if(n&&(n(e,i),i.isMatchIgnored))return y(t);return n.skip?k+=t:(n.excludeBegin&&(k+=t),p(),!n.returnBegin&&!n.excludeBegin&&(k=t)),g(n,e),n.returnBegin?0:t.length}function S(e){let t=e[0],r=n.substring(e.index),i=_(E,e,r);if(!i)return Fe;let a=E;E.endScope&&E.endScope._wrap?(p(),m(t,E.endScope._wrap)):E.endScope&&E.endScope._multi?(p(),h(E.endScope,e)):a.skip?k+=t:(a.returnEnd||a.excludeEnd||(k+=t),p(),a.excludeEnd&&(k=t));do E.scope&&D.closeNode(),!E.skip&&!E.subLanguage&&(ae+=E.relevance),E=E.parent;while(E!==i.parent);return i.starts&&g(i.starts,e),a.returnEnd?0:t.length}function C(){let e=[];for(let t=E;t!==w;t=t.parent)t.scope&&e.unshift(t.scope);e.forEach(e=>D.openNode(e))}let te={};function ne(t,r){let a=r&&r[0];if(k+=t,a==null)return p(),0;if(te.type===`begin`&&r.type===`end`&&te.index===r.index&&a===``){if(k+=n.slice(r.index,r.index+1),!o){let t=Error(`0 width match regex (${e})`);throw t.languageName=e,t.badRule=te.rule,t}return 1}if(te=r,r.type===`begin`)return b(r);if(r.type===`illegal`&&!i){let e=Error(`Illegal lexeme "`+a+`" for mode "`+(E.scope||`<unnamed>`)+`"`);throw e.mode=E,e}else if(r.type===`end`){let e=S(r);if(e!==Fe)return e}if(r.type===`illegal`&&a===``)return k+=`
`,1;if(j>1e5&&j>r.index*3)throw Error(`potential infinite loop, way more iterations than matches`);return k+=a,a.length}let w=O(e);if(!w)throw xe(s.replace(`{}`,e)),Error(`Unknown language: "`+e+`"`);let T=Oe(w),re=``,E=a||T,ie={},D=new l.__emitter(l);C();let k=``,ae=0,A=0,j=0,oe=!1;try{if(w.__emitTokens)w.__emitTokens(n,D);else{for(E.matcher.considerAll();;){j++,oe?oe=!1:E.matcher.considerAll(),E.matcher.lastIndex=A;let e=E.matcher.exec(n);if(!e)break;let t=ne(n.substring(A,e.index),e);A=e.index+t}ne(n.substring(A))}return D.finalize(),re=D.toHTML(),{language:e,value:re,relevance:ae,illegal:!1,_emitter:D,_top:E}}catch(t){if(t.message&&t.message.includes(`Illegal`))return{language:e,value:Ne(n),illegal:!0,relevance:0,_illegalBy:{message:t.message,index:A,context:n.slice(A-100,A+100),mode:t.mode,resultSoFar:re},_emitter:D};if(o)return{language:e,value:Ne(n),illegal:!1,relevance:0,errorRaised:t,_emitter:D,_top:E};throw t}}function b(e){let t={value:Ne(e),illegal:!1,relevance:0,_top:c,_emitter:new l.__emitter(l)};return t._emitter.addText(e),t}function ee(e,n){n=n||l.languages||Object.keys(t);let r=b(e),i=n.filter(O).filter(ae).map(t=>v(t,e,!1));i.unshift(r);let[a,o]=i.sort((e,t)=>{if(e.relevance!==t.relevance)return t.relevance-e.relevance;if(e.language&&t.language){if(O(e.language).supersetOf===t.language)return 1;if(O(t.language).supersetOf===e.language)return-1}return 0}),s=a;return s.secondBest=o,s}function S(e,t,n){let r=t&&i[t]||n;e.classList.add(`hljs`),e.classList.add(`language-${r}`)}function C(e){let t=null,n=d(e);if(u(n))return;if(se(`before:highlightElement`,{el:e,language:n}),e.dataset.highlighted){console.log("Element previously highlighted. To highlight again, first unset `dataset.highlighted`.",e);return}if(e.children.length>0&&(l.ignoreUnescapedHTML||(console.warn(`One of your code blocks includes unescaped HTML. This is a potentially serious security risk.`),console.warn(`https://github.com/highlightjs/highlight.js/wiki/security`),console.warn(`The element with unescaped HTML:`),console.warn(e)),l.throwUnescapedHTML))throw new Me(`One of your code blocks includes unescaped HTML.`,e.innerHTML);t=e;let r=t.textContent,i=n?p(r,{language:n,ignoreIllegals:!0}):ee(r);e.innerHTML=i.value,e.dataset.highlighted=`yes`,S(e,n,i.language),e.result={language:i.language,re:i.relevance,relevance:i.relevance},i.secondBest&&(e.secondBest={language:i.secondBest.language,relevance:i.secondBest.relevance}),se(`after:highlightElement`,{el:e,result:i,text:r})}function te(e){l=Pe(l,e)}let ne=()=>{re(),N(`10.6.0`,`initHighlighting() deprecated.  Use highlightAll() now.`)};function w(){re(),N(`10.6.0`,`initHighlightingOnLoad() deprecated.  Use highlightAll() now.`)}let T=!1;function re(){function e(){re()}if(document.readyState===`loading`){T||window.addEventListener(`DOMContentLoaded`,e,!1),T=!0;return}document.querySelectorAll(l.cssSelector).forEach(C)}function E(n,r){let i=null;try{i=r(e)}catch(e){if(xe(`Language definition for '{}' could not be registered.`.replace(`{}`,n)),o)xe(e);else throw e;i=c}i.name||=n,t[n]=i,i.rawDefinition=r.bind(null,e),i.aliases&&k(i.aliases,{languageName:n})}function ie(e){delete t[e];for(let t of Object.keys(i))i[t]===e&&delete i[t]}function D(){return Object.keys(t)}function O(e){return e=(e||``).toLowerCase(),t[e]||t[i[e]]}function k(e,{languageName:t}){typeof e==`string`&&(e=[e]),e.forEach(e=>{i[e.toLowerCase()]=t})}function ae(e){let t=O(e);return t&&!t.disableAutodetect}function A(e){e[`before:highlightBlock`]&&!e[`before:highlightElement`]&&(e[`before:highlightElement`]=t=>{e[`before:highlightBlock`](Object.assign({block:t.el},t))}),e[`after:highlightBlock`]&&!e[`after:highlightElement`]&&(e[`after:highlightElement`]=t=>{e[`after:highlightBlock`](Object.assign({block:t.el},t))})}function j(e){A(e),a.push(e)}function oe(e){let t=a.indexOf(e);t!==-1&&a.splice(t,1)}function se(e,t){let n=e;a.forEach(function(e){e[n]&&e[n](t)})}function le(e){return N(`10.7.0`,`highlightBlock will be removed entirely in v12.0`),N(`10.7.0`,`Please use highlightElement now.`),C(e)}Object.assign(e,{highlight:p,highlightAuto:ee,highlightAll:re,highlightElement:C,highlightBlock:le,configure:te,initHighlighting:ne,initHighlightingOnLoad:w,registerLanguage:E,unregisterLanguage:ie,listLanguages:D,getLanguage:O,registerAliases:k,autoDetection:ae,inherit:Pe,addPlugin:j,removePlugin:oe}),e.debugMode=function(){o=!1},e.safeMode=function(){o=!0},e.versionString=je,e.regex={concat:_,lookahead:m,either:y,optional:g,anyNumberOfTimes:h};for(let e in ce)typeof ce[e]==`object`&&n(ce[e]);return Object.assign(e,ce),e},Re=Le({});Re.newInstance=()=>Le({}),t.exports=Re,Re.HighlightJS=Re,Re.default=Re}))()).default;function je(e){let t=e.regex,n={},r={begin:/\$\{/,end:/\}/,contains:[`self`,{begin:/:-/,contains:[n]}]};Object.assign(n,{className:`variable`,variants:[{begin:t.concat(/\$[\w\d#@][\w\d_]*/,`(?![\\w\\d])(?![$])`)},r]});let i={className:`subst`,begin:/\$\(/,end:/\)/,contains:[e.BACKSLASH_ESCAPE]},a=e.inherit(e.COMMENT(),{match:[/(^|\s)/,/#.*$/],scope:{2:`comment`}}),o={begin:/<<-?\s*(?=\w+)/,starts:{contains:[e.END_SAME_AS_BEGIN({begin:/(\w+)/,end:/(\w+)/,className:`string`})]}},s={className:`string`,begin:/"/,end:/"/,contains:[e.BACKSLASH_ESCAPE,n,i]};i.contains.push(s);let c={match:/\\"/},l={className:`string`,begin:/'/,end:/'/},u={match:/\\'/},d={begin:/\$?\(\(/,end:/\)\)/,contains:[{begin:/\d+#[0-9a-f]+/,className:`number`},e.NUMBER_MODE,n]},f=e.SHEBANG({binary:`(${[`fish`,`bash`,`zsh`,`sh`,`csh`,`ksh`,`tcsh`,`dash`,`scsh`].join(`|`)})`,relevance:10}),p={className:`function`,begin:/\w[\w\d_]*\s*\(\s*\)\s*\{/,returnBegin:!0,contains:[e.inherit(e.TITLE_MODE,{begin:/\w[\w\d_]*/})],relevance:0},m=[`if`,`then`,`else`,`elif`,`fi`,`time`,`for`,`while`,`until`,`in`,`do`,`done`,`case`,`esac`,`coproc`,`function`,`select`],h=[`true`,`false`],g={match:/(\/[a-z._-]+)+/},_=[`break`,`cd`,`continue`,`eval`,`exec`,`exit`,`export`,`getopts`,`hash`,`pwd`,`readonly`,`return`,`shift`,`test`,`times`,`trap`,`umask`,`unset`],v=[`alias`,`bind`,`builtin`,`caller`,`command`,`declare`,`echo`,`enable`,`help`,`let`,`local`,`logout`,`mapfile`,`printf`,`read`,`readarray`,`source`,`sudo`,`type`,`typeset`,`ulimit`,`unalias`],y=`autoload.bg.bindkey.bye.cap.chdir.clone.comparguments.compcall.compctl.compdescribe.compfiles.compgroups.compquote.comptags.comptry.compvalues.dirs.disable.disown.echotc.echoti.emulate.fc.fg.float.functions.getcap.getln.history.integer.jobs.kill.limit.log.noglob.popd.print.pushd.pushln.rehash.sched.setcap.setopt.stat.suspend.ttyctl.unfunction.unhash.unlimit.unsetopt.vared.wait.whence.where.which.zcompile.zformat.zftp.zle.zmodload.zparseopts.zprof.zpty.zregexparse.zsocket.zstyle.ztcp`.split(`.`),b=`chcon.chgrp.chown.chmod.cp.dd.df.dir.dircolors.ln.ls.mkdir.mkfifo.mknod.mktemp.mv.realpath.rm.rmdir.shred.sync.touch.truncate.vdir.b2sum.base32.base64.cat.cksum.comm.csplit.cut.expand.fmt.fold.head.join.md5sum.nl.numfmt.od.paste.ptx.pr.sha1sum.sha224sum.sha256sum.sha384sum.sha512sum.shuf.sort.split.sum.tac.tail.tr.tsort.unexpand.uniq.wc.arch.basename.chroot.date.dirname.du.echo.env.expr.factor.groups.hostid.id.link.logname.nice.nohup.nproc.pathchk.pinky.printenv.printf.pwd.readlink.runcon.seq.sleep.stat.stdbuf.stty.tee.test.timeout.tty.uname.unlink.uptime.users.who.whoami.yes`.split(`.`);return{name:`Bash`,aliases:[`sh`,`zsh`],keywords:{$pattern:/\b[a-z][a-z0-9._-]+\b/,keyword:m,literal:h,built_in:[..._,...v,`set`,`shopt`,...y,...b]},contains:[f,e.SHEBANG(),p,d,a,o,g,s,c,l,u,n]}}var Me=e=>({IMPORTANT:{scope:`meta`,begin:`!important`},BLOCK_COMMENT:e.C_BLOCK_COMMENT_MODE,HEXCOLOR:{scope:`number`,begin:/#(([0-9a-fA-F]{3,4})|(([0-9a-fA-F]{2}){3,4}))\b/},FUNCTION_DISPATCH:{className:`built_in`,begin:/[\w-]+(?=\()/},ATTRIBUTE_SELECTOR_MODE:{scope:`selector-attr`,begin:/\[/,end:/\]/,illegal:`$`,contains:[e.APOS_STRING_MODE,e.QUOTE_STRING_MODE]},CSS_NUMBER_MODE:{scope:`number`,begin:e.NUMBER_RE+`(%|em|ex|ch|rem|vw|vh|vmin|vmax|cm|mm|in|pt|pc|px|deg|grad|rad|turn|s|ms|Hz|kHz|dpi|dpcm|dppx)?`,relevance:0},CSS_VARIABLE:{className:`attr`,begin:/--[A-Za-z_][A-Za-z0-9_-]*/}}),Ne=`a.abbr.address.article.aside.audio.b.blockquote.body.button.canvas.caption.cite.code.dd.del.details.dfn.div.dl.dt.em.fieldset.figcaption.figure.footer.form.h1.h2.h3.h4.h5.h6.header.hgroup.html.i.iframe.img.input.ins.kbd.label.legend.li.main.mark.menu.nav.object.ol.optgroup.option.p.picture.q.quote.samp.section.select.source.span.strong.summary.sup.table.tbody.td.textarea.tfoot.th.thead.time.tr.ul.var.video`.split(`.`),Pe=`defs.g.marker.mask.pattern.svg.switch.symbol.feBlend.feColorMatrix.feComponentTransfer.feComposite.feConvolveMatrix.feDiffuseLighting.feDisplacementMap.feFlood.feGaussianBlur.feImage.feMerge.feMorphology.feOffset.feSpecularLighting.feTile.feTurbulence.linearGradient.radialGradient.stop.circle.ellipse.image.line.path.polygon.polyline.rect.text.use.textPath.tspan.foreignObject.clipPath`.split(`.`),Fe=[...Ne,...Pe],Ie=`any-hover.any-pointer.aspect-ratio.color.color-gamut.color-index.device-aspect-ratio.device-height.device-width.display-mode.forced-colors.grid.height.hover.inverted-colors.monochrome.orientation.overflow-block.overflow-inline.pointer.prefers-color-scheme.prefers-contrast.prefers-reduced-motion.prefers-reduced-transparency.resolution.scan.scripting.update.width.min-width.max-width.min-height.max-height`.split(`.`).sort().reverse(),Le=`active.any-link.blank.checked.current.default.defined.dir.disabled.drop.empty.enabled.first.first-child.first-of-type.fullscreen.future.focus.focus-visible.focus-within.has.host.host-context.hover.indeterminate.in-range.invalid.is.lang.last-child.last-of-type.left.link.local-link.not.nth-child.nth-col.nth-last-child.nth-last-col.nth-last-of-type.nth-of-type.only-child.only-of-type.optional.out-of-range.past.placeholder-shown.read-only.read-write.required.right.root.scope.target.target-within.user-invalid.valid.visited.where`.split(`.`).sort().reverse(),Re=[`after`,`backdrop`,`before`,`cue`,`cue-region`,`first-letter`,`first-line`,`grammar-error`,`marker`,`part`,`placeholder`,`selection`,`slotted`,`spelling-error`].sort().reverse(),ze=`accent-color.align-content.align-items.align-self.alignment-baseline.all.anchor-name.animation.animation-composition.animation-delay.animation-direction.animation-duration.animation-fill-mode.animation-iteration-count.animation-name.animation-play-state.animation-range.animation-range-end.animation-range-start.animation-timeline.animation-timing-function.appearance.aspect-ratio.backdrop-filter.backface-visibility.background.background-attachment.background-blend-mode.background-clip.background-color.background-image.background-origin.background-position.background-position-x.background-position-y.background-repeat.background-size.baseline-shift.block-size.border.border-block.border-block-color.border-block-end.border-block-end-color.border-block-end-style.border-block-end-width.border-block-start.border-block-start-color.border-block-start-style.border-block-start-width.border-block-style.border-block-width.border-bottom.border-bottom-color.border-bottom-left-radius.border-bottom-right-radius.border-bottom-style.border-bottom-width.border-collapse.border-color.border-end-end-radius.border-end-start-radius.border-image.border-image-outset.border-image-repeat.border-image-slice.border-image-source.border-image-width.border-inline.border-inline-color.border-inline-end.border-inline-end-color.border-inline-end-style.border-inline-end-width.border-inline-start.border-inline-start-color.border-inline-start-style.border-inline-start-width.border-inline-style.border-inline-width.border-left.border-left-color.border-left-style.border-left-width.border-radius.border-right.border-right-color.border-right-style.border-right-width.border-spacing.border-start-end-radius.border-start-start-radius.border-style.border-top.border-top-color.border-top-left-radius.border-top-right-radius.border-top-style.border-top-width.border-width.bottom.box-align.box-decoration-break.box-direction.box-flex.box-flex-group.box-lines.box-ordinal-group.box-orient.box-pack.box-shadow.box-sizing.break-after.break-before.break-inside.caption-side.caret-color.clear.clip.clip-path.clip-rule.color.color-interpolation.color-interpolation-filters.color-profile.color-rendering.color-scheme.column-count.column-fill.column-gap.column-rule.column-rule-color.column-rule-style.column-rule-width.column-span.column-width.columns.contain.contain-intrinsic-block-size.contain-intrinsic-height.contain-intrinsic-inline-size.contain-intrinsic-size.contain-intrinsic-width.container.container-name.container-type.content.content-visibility.counter-increment.counter-reset.counter-set.cue.cue-after.cue-before.cursor.cx.cy.direction.display.dominant-baseline.empty-cells.enable-background.field-sizing.fill.fill-opacity.fill-rule.filter.flex.flex-basis.flex-direction.flex-flow.flex-grow.flex-shrink.flex-wrap.float.flood-color.flood-opacity.flow.font.font-display.font-family.font-feature-settings.font-kerning.font-language-override.font-optical-sizing.font-palette.font-size.font-size-adjust.font-smooth.font-smoothing.font-stretch.font-style.font-synthesis.font-synthesis-position.font-synthesis-small-caps.font-synthesis-style.font-synthesis-weight.font-variant.font-variant-alternates.font-variant-caps.font-variant-east-asian.font-variant-emoji.font-variant-ligatures.font-variant-numeric.font-variant-position.font-variation-settings.font-weight.forced-color-adjust.gap.glyph-orientation-horizontal.glyph-orientation-vertical.grid.grid-area.grid-auto-columns.grid-auto-flow.grid-auto-rows.grid-column.grid-column-end.grid-column-start.grid-gap.grid-row.grid-row-end.grid-row-start.grid-template.grid-template-areas.grid-template-columns.grid-template-rows.hanging-punctuation.height.hyphenate-character.hyphenate-limit-chars.hyphens.icon.image-orientation.image-rendering.image-resolution.ime-mode.initial-letter.initial-letter-align.inline-size.inset.inset-area.inset-block.inset-block-end.inset-block-start.inset-inline.inset-inline-end.inset-inline-start.isolation.justify-content.justify-items.justify-self.kerning.left.letter-spacing.lighting-color.line-break.line-height.line-height-step.list-style.list-style-image.list-style-position.list-style-type.margin.margin-block.margin-block-end.margin-block-start.margin-bottom.margin-inline.margin-inline-end.margin-inline-start.margin-left.margin-right.margin-top.margin-trim.marker.marker-end.marker-mid.marker-start.marks.mask.mask-border.mask-border-mode.mask-border-outset.mask-border-repeat.mask-border-slice.mask-border-source.mask-border-width.mask-clip.mask-composite.mask-image.mask-mode.mask-origin.mask-position.mask-repeat.mask-size.mask-type.masonry-auto-flow.math-depth.math-shift.math-style.max-block-size.max-height.max-inline-size.max-width.min-block-size.min-height.min-inline-size.min-width.mix-blend-mode.nav-down.nav-index.nav-left.nav-right.nav-up.none.normal.object-fit.object-position.offset.offset-anchor.offset-distance.offset-path.offset-position.offset-rotate.opacity.order.orphans.outline.outline-color.outline-offset.outline-style.outline-width.overflow.overflow-anchor.overflow-block.overflow-clip-margin.overflow-inline.overflow-wrap.overflow-x.overflow-y.overlay.overscroll-behavior.overscroll-behavior-block.overscroll-behavior-inline.overscroll-behavior-x.overscroll-behavior-y.padding.padding-block.padding-block-end.padding-block-start.padding-bottom.padding-inline.padding-inline-end.padding-inline-start.padding-left.padding-right.padding-top.page.page-break-after.page-break-before.page-break-inside.paint-order.pause.pause-after.pause-before.perspective.perspective-origin.place-content.place-items.place-self.pointer-events.position.position-anchor.position-visibility.print-color-adjust.quotes.r.resize.rest.rest-after.rest-before.right.rotate.row-gap.ruby-align.ruby-position.scale.scroll-behavior.scroll-margin.scroll-margin-block.scroll-margin-block-end.scroll-margin-block-start.scroll-margin-bottom.scroll-margin-inline.scroll-margin-inline-end.scroll-margin-inline-start.scroll-margin-left.scroll-margin-right.scroll-margin-top.scroll-padding.scroll-padding-block.scroll-padding-block-end.scroll-padding-block-start.scroll-padding-bottom.scroll-padding-inline.scroll-padding-inline-end.scroll-padding-inline-start.scroll-padding-left.scroll-padding-right.scroll-padding-top.scroll-snap-align.scroll-snap-stop.scroll-snap-type.scroll-timeline.scroll-timeline-axis.scroll-timeline-name.scrollbar-color.scrollbar-gutter.scrollbar-width.shape-image-threshold.shape-margin.shape-outside.shape-rendering.speak.speak-as.src.stop-color.stop-opacity.stroke.stroke-dasharray.stroke-dashoffset.stroke-linecap.stroke-linejoin.stroke-miterlimit.stroke-opacity.stroke-width.tab-size.table-layout.text-align.text-align-all.text-align-last.text-anchor.text-combine-upright.text-decoration.text-decoration-color.text-decoration-line.text-decoration-skip.text-decoration-skip-ink.text-decoration-style.text-decoration-thickness.text-emphasis.text-emphasis-color.text-emphasis-position.text-emphasis-style.text-indent.text-justify.text-orientation.text-overflow.text-rendering.text-shadow.text-size-adjust.text-transform.text-underline-offset.text-underline-position.text-wrap.text-wrap-mode.text-wrap-style.timeline-scope.top.touch-action.transform.transform-box.transform-origin.transform-style.transition.transition-behavior.transition-delay.transition-duration.transition-property.transition-timing-function.translate.unicode-bidi.user-modify.user-select.vector-effect.vertical-align.view-timeline.view-timeline-axis.view-timeline-inset.view-timeline-name.view-transition-name.visibility.voice-balance.voice-duration.voice-family.voice-pitch.voice-range.voice-rate.voice-stress.voice-volume.white-space.white-space-collapse.widows.width.will-change.word-break.word-spacing.word-wrap.writing-mode.x.y.z-index.zoom`.split(`.`).sort().reverse();function Be(e){let t=e.regex,n=Me(e),r={begin:/-(webkit|moz|ms|o)-(?=[a-z])/},i=/@-?\w[\w]*(-\w+)*/,a=[e.APOS_STRING_MODE,e.QUOTE_STRING_MODE];return{name:`CSS`,case_insensitive:!0,illegal:/[=|'\$]/,keywords:{keyframePosition:`from to`},classNameAliases:{keyframePosition:`selector-tag`},contains:[n.BLOCK_COMMENT,r,n.CSS_NUMBER_MODE,{className:`selector-id`,begin:/#[A-Za-z0-9_-]+/,relevance:0},{className:`selector-class`,begin:`\\.[a-zA-Z-][a-zA-Z0-9_-]*`,relevance:0},n.ATTRIBUTE_SELECTOR_MODE,{className:`selector-pseudo`,variants:[{begin:`:(`+Le.join(`|`)+`)`},{begin:`:(:)?(`+Re.join(`|`)+`)`}]},n.CSS_VARIABLE,{className:`attribute`,begin:`\\b(`+ze.join(`|`)+`)\\b`},{begin:/:/,end:/[;}{]/,contains:[n.BLOCK_COMMENT,n.HEXCOLOR,n.IMPORTANT,n.CSS_NUMBER_MODE,...a,{begin:/(url|data-uri)\(/,end:/\)/,relevance:0,keywords:{built_in:`url data-uri`},contains:[...a,{className:`string`,begin:/[^)]/,endsWithParent:!0,excludeEnd:!0}]},n.FUNCTION_DISPATCH]},{begin:t.lookahead(/@/),end:`[{;]`,relevance:0,illegal:/:/,contains:[{className:`keyword`,begin:i},{begin:/\s/,endsWithParent:!0,excludeEnd:!0,relevance:0,keywords:{$pattern:/[a-z-]+/,keyword:`and or not only`,attribute:Ie.join(` `)},contains:[{begin:/[a-z-]+(?=:)/,className:`attribute`},...a,n.CSS_NUMBER_MODE]}]},{className:`selector-tag`,begin:`\\b(`+Fe.join(`|`)+`)\\b`}]}}function Ve(e){let t=e.COMMENT(/^\s*@?rem\b/,/$/,{relevance:10});return{name:`Batch file (DOS)`,aliases:[`bat`,`cmd`],case_insensitive:!0,illegal:/\/\*/,keywords:{keyword:[`if`,`else`,`goto`,`for`,`in`,`do`,`call`,`exit`,`not`,`exist`,`errorlevel`,`defined`,`equ`,`neq`,`lss`,`leq`,`gtr`,`geq`],built_in:`prn.nul.lpt3.lpt2.lpt1.con.com4.com3.com2.com1.aux.shift.cd.dir.echo.setlocal.endlocal.set.pause.copy.append.assoc.at.attrib.break.cacls.cd.chcp.chdir.chkdsk.chkntfs.cls.cmd.color.comp.compact.convert.date.dir.diskcomp.diskcopy.doskey.erase.fs.find.findstr.format.ftype.graftabl.help.keyb.label.md.mkdir.mode.more.move.path.pause.print.popd.pushd.promt.rd.recover.rem.rename.replace.restore.rmdir.shift.sort.start.subst.time.title.tree.type.ver.verify.vol.ping.net.ipconfig.taskkill.xcopy.ren.del`.split(`.`)},contains:[{className:`variable`,begin:/%%[^ ]|%[^ ]+?%|![^ ]+?!/},{className:`function`,begin:{className:`symbol`,begin:`^\\s*[A-Za-z._?][A-Za-z0-9_$#@~.?]*(:|\\s+label)`,relevance:0}.begin,end:`goto:eof`,contains:[e.inherit(e.TITLE_MODE,{begin:`([_a-zA-Z]\\w*\\.)*([_a-zA-Z]\\w*:)?[_a-zA-Z]\\w*`}),t]},{className:`number`,begin:`\\b\\d+`,relevance:0},t]}}var He=`[0-9](_*[0-9])*`,Ue=`\\.(${He})`,We=`[0-9a-fA-F](_*[0-9a-fA-F])*`,Ge={className:`number`,variants:[{begin:`(\\b(${He})((${Ue})|\\.)?|(${Ue}))[eE][+-]?(${He})[fFdD]?\\b`},{begin:`\\b(${He})((${Ue})[fFdD]?\\b|\\.([fFdD]\\b)?)`},{begin:`(${Ue})[fFdD]?\\b`},{begin:`\\b(${He})[fFdD]\\b`},{begin:`\\b0[xX]((${We})\\.?|(${We})?\\.(${We}))[pP][+-]?(${He})[fFdD]?\\b`},{begin:`\\b(0|[1-9](_*[0-9])*)[lL]?\\b`},{begin:`\\b0[xX](${We})[lL]?\\b`},{begin:`\\b0(_*[0-7])*[lL]?\\b`},{begin:`\\b0[bB][01](_*[01])*[lL]?\\b`}],relevance:0};function Ke(e,t,n){return n===-1?``:e.replace(t,r=>Ke(e,t,n-1))}function qe(e){let t=e.regex,n=`[À-ʸa-zA-Z_$][À-ʸa-zA-Z_$0-9]*`,r=n+Ke(`(?:<[À-ʸa-zA-Z_$][À-ʸa-zA-Z_$0-9]*~~~(?:\\s*,\\s*[À-ʸa-zA-Z_$][À-ʸa-zA-Z_$0-9]*~~~)*>)?`,/~~~/g,2),i={keyword:`synchronized.abstract.private.var.static.if.const .for.while.strictfp.finally.protected.import.native.final.void.enum.else.break.transient.catch.instanceof.volatile.case.assert.package.default.public.try.switch.continue.throws.protected.public.private.module.requires.exports.do.sealed.yield.permits.goto.when`.split(`.`),literal:[`false`,`true`,`null`],type:[`char`,`boolean`,`long`,`float`,`int`,`byte`,`short`,`double`],built_in:[`super`,`this`]},a={className:`meta`,begin:`@[À-ʸa-zA-Z_$][À-ʸa-zA-Z_$0-9]*`,contains:[{begin:/\(/,end:/\)/,contains:[`self`]}]},o={className:`params`,begin:/\(/,end:/\)/,keywords:i,relevance:0,contains:[e.C_BLOCK_COMMENT_MODE],endsParent:!0};return{name:`Java`,aliases:[`jsp`],keywords:i,illegal:/<\/|#/,contains:[e.COMMENT(`/\\*\\*`,`\\*/`,{relevance:0,contains:[{begin:/\w+@/,relevance:0},{className:`doctag`,begin:`@[A-Za-z]+`}]}),{begin:/import java\.[a-z]+\./,keywords:`import`,relevance:2},e.C_LINE_COMMENT_MODE,e.C_BLOCK_COMMENT_MODE,{begin:/"""/,end:/"""/,className:`string`,contains:[e.BACKSLASH_ESCAPE]},e.APOS_STRING_MODE,e.QUOTE_STRING_MODE,{match:[/\b(?:class|interface|enum|extends|implements|new)/,/\s+/,n],className:{1:`keyword`,3:`title.class`}},{match:/non-sealed/,scope:`keyword`},{begin:[t.concat(/(?!else)/,n),/\s+/,n,/\s+/,/=(?!=)/],className:{1:`type`,3:`variable`,5:`operator`}},{begin:[/record/,/\s+/,n],className:{1:`keyword`,3:`title.class`},contains:[o,e.C_LINE_COMMENT_MODE,e.C_BLOCK_COMMENT_MODE]},{beginKeywords:`new throw return else`,relevance:0},{begin:[`(?:`+r+`\\s+)`,e.UNDERSCORE_IDENT_RE,/\s*(?=\()/],className:{2:`title.function`},keywords:i,contains:[{className:`params`,begin:/\(/,end:/\)/,keywords:i,relevance:0,contains:[a,e.APOS_STRING_MODE,e.QUOTE_STRING_MODE,Ge,e.C_BLOCK_COMMENT_MODE]},e.C_LINE_COMMENT_MODE,e.C_BLOCK_COMMENT_MODE]},Ge,a]}}function Je(e){let t={className:`attr`,begin:/"(\\.|[^\\"\r\n])*"(?=\s*:)/,relevance:1.01},n={match:/[{}[\],:]/,className:`punctuation`,relevance:0},r=[`true`,`false`,`null`],i={scope:`literal`,beginKeywords:r.join(` `)};return{name:`JSON`,aliases:[`jsonc`],keywords:{literal:r},contains:[t,n,e.QUOTE_STRING_MODE,i,e.C_NUMBER_MODE,e.C_LINE_COMMENT_MODE,e.C_BLOCK_COMMENT_MODE],illegal:`\\S`}}function Ye(e){let t=[`string`,`char`,`byte`,`int`,`long`,`bool`,`decimal`,`single`,`double`,`DateTime`,`xml`,`array`,`hashtable`,`void`],n={$pattern:/-?[A-z\.\-]+\b/,keyword:`if else foreach return do while until elseif begin for trap data dynamicparam end break throw param continue finally in switch exit filter try process catch hidden static parameter`,built_in:`ac asnp cat cd CFS chdir clc clear clhy cli clp cls clv cnsn compare copy cp cpi cpp curl cvpa dbp del diff dir dnsn ebp echo|0 epal epcsv epsn erase etsn exsn fc fhx fl ft fw gal gbp gc gcb gci gcm gcs gdr gerr ghy gi gin gjb gl gm gmo gp gps gpv group gsn gsnp gsv gtz gu gv gwmi h history icm iex ihy ii ipal ipcsv ipmo ipsn irm ise iwmi iwr kill lp ls man md measure mi mount move mp mv nal ndr ni nmo npssc nsn nv ogv oh popd ps pushd pwd r rbp rcjb rcsn rd rdr ren ri rjb rm rmdir rmo rni rnp rp rsn rsnp rujb rv rvpa rwmi sajb sal saps sasv sbp sc scb select set shcm si sl sleep sls sort sp spjb spps spsv start stz sujb sv swmi tee trcm type wget where wjb write`},r=/\w[\w\d]*((-)[\w\d]+)*/,i={begin:"`[\\s\\S]",relevance:0},a={className:`variable`,variants:[{begin:/\$\B/},{className:`keyword`,begin:/\$this/},{begin:/\$[\w\d][\w\d_:]*/}]},o={className:`literal`,begin:/\$(null|true|false)\b/},s={className:`string`,variants:[{begin:/"/,end:/"/},{begin:/@"/,end:/^"@/}],contains:[i,a,{className:`variable`,begin:/\$[A-z]/,end:/[^A-z]/}]},c={className:`string`,variants:[{begin:/'/,end:/'/},{begin:/@'/,end:/^'@/}]},l=e.inherit(e.COMMENT(null,null),{variants:[{begin:/#/,end:/$/},{begin:/<#/,end:/#>/}],contains:[{className:`doctag`,variants:[{begin:/\.(synopsis|description|example|inputs|outputs|notes|link|component|role|functionality)/},{begin:/\.(parameter|forwardhelptargetname|forwardhelpcategory|remotehelprunspace|externalhelp)\s+\S+/}]}]}),u={className:`built_in`,variants:[{begin:`(Add|Clear|Close|Copy|Enter|Exit|Find|Format|Get|Hide|Join|Lock|Move|New|Open|Optimize|Pop|Push|Redo|Remove|Rename|Reset|Resize|Search|Select|Set|Show|Skip|Split|Step|Switch|Undo|Unlock|Watch|Backup|Checkpoint|Compare|Compress|Convert|ConvertFrom|ConvertTo|Dismount|Edit|Expand|Export|Group|Import|Initialize|Limit|Merge|Mount|Out|Publish|Restore|Save|Sync|Unpublish|Update|Approve|Assert|Build|Complete|Confirm|Deny|Deploy|Disable|Enable|Install|Invoke|Register|Request|Restart|Resume|Start|Stop|Submit|Suspend|Uninstall|Unregister|Wait|Debug|Measure|Ping|Repair|Resolve|Test|Trace|Connect|Disconnect|Read|Receive|Send|Write|Block|Grant|Protect|Revoke|Unblock|Unprotect|Use|ForEach|Sort|Tee|Where)+(-)[\\w\\d]+`}]},d={className:`class`,beginKeywords:`class enum`,end:/\s*[{]/,excludeEnd:!0,relevance:0,contains:[e.TITLE_MODE]},f={className:`function`,begin:/function\s+/,end:/\s*\{|$/,excludeEnd:!0,returnBegin:!0,relevance:0,contains:[{begin:`function`,relevance:0,className:`keyword`},{className:`title`,begin:r,relevance:0},{begin:/\(/,end:/\)/,className:`params`,relevance:0,contains:[a]}]},p={begin:/using\s/,end:/$/,returnBegin:!0,contains:[s,c,{className:`keyword`,begin:/(using|assembly|command|module|namespace|type)/}]},m={variants:[{className:`operator`,begin:`(-and|-as|-band|-bnot|-bor|-bxor|-casesensitive|-ccontains|-ceq|-cge|-cgt|-cle|-clike|-clt|-cmatch|-cne|-cnotcontains|-cnotlike|-cnotmatch|-contains|-creplace|-csplit|-eq|-exact|-f|-file|-ge|-gt|-icontains|-ieq|-ige|-igt|-ile|-ilike|-ilt|-imatch|-in|-ine|-inotcontains|-inotlike|-inotmatch|-ireplace|-is|-isnot|-isplit|-join|-le|-like|-lt|-match|-ne|-not|-notcontains|-notin|-notlike|-notmatch|-or|-regex|-replace|-shl|-shr|-split|-wildcard|-xor)\\b`},{className:`literal`,begin:/(-){1,2}[\w\d-]+/,relevance:0}]},h={className:`selector-tag`,begin:/@\B/,relevance:0},g={className:`function`,begin:/\[.*\]\s*[\w]+[ ]??\(/,end:/$/,returnBegin:!0,relevance:0,contains:[{className:`keyword`,begin:`(${n.keyword.toString().replace(/\s/g,`|`)})\\b`,endsParent:!0,relevance:0},e.inherit(e.TITLE_MODE,{endsParent:!0})]},_=[g,l,i,e.NUMBER_MODE,s,c,u,a,o,h],v={begin:/\[/,end:/\]/,excludeBegin:!0,excludeEnd:!0,relevance:0,contains:[`self`].concat(_,{begin:`(`+t.join(`|`)+`)`,className:`built_in`,relevance:0},{className:`type`,begin:/[\.\w\d]+/,relevance:0})};return g.contains.unshift(v),{name:`PowerShell`,aliases:[`pwsh`,`ps`,`ps1`],case_insensitive:!0,keywords:n,contains:_.concat(d,f,p,m,v)}}function Xe(e){let t=e.regex,n=/[\p{XID_Start}_]\p{XID_Continue}*/u,r=`and.as.assert.async.await.break.case.class.continue.def.del.elif.else.except.finally.for.from.global.if.import.in.is.lambda.match.nonlocal|10.not.or.pass.raise.return.try.while.with.yield`.split(`.`),i={$pattern:/[A-Za-z]\w+|__\w+__/,keyword:r,built_in:`__import__.abs.all.any.ascii.bin.bool.breakpoint.bytearray.bytes.callable.chr.classmethod.compile.complex.delattr.dict.dir.divmod.enumerate.eval.exec.filter.float.format.frozenset.getattr.globals.hasattr.hash.help.hex.id.input.int.isinstance.issubclass.iter.len.list.locals.map.max.memoryview.min.next.object.oct.open.ord.pow.print.property.range.repr.reversed.round.set.setattr.slice.sorted.staticmethod.str.sum.super.tuple.type.vars.zip`.split(`.`),literal:[`__debug__`,`Ellipsis`,`False`,`None`,`NotImplemented`,`True`],type:[`Any`,`Callable`,`Coroutine`,`Dict`,`List`,`Literal`,`Generic`,`Optional`,`Sequence`,`Set`,`Tuple`,`Type`,`Union`]},a={className:`meta`,begin:/^(>>>|\.\.\.) /},o={className:`subst`,begin:/\{/,end:/\}/,keywords:i,illegal:/#/},s={begin:/\{\{/,relevance:0},c={className:`string`,contains:[e.BACKSLASH_ESCAPE],variants:[{begin:/([uU]|[bB]|[rR]|[bB][rR]|[rR][bB])?'''/,end:/'''/,contains:[e.BACKSLASH_ESCAPE,a],relevance:10},{begin:/([uU]|[bB]|[rR]|[bB][rR]|[rR][bB])?"""/,end:/"""/,contains:[e.BACKSLASH_ESCAPE,a],relevance:10},{begin:/([fF][rR]|[rR][fF]|[fF])'''/,end:/'''/,contains:[e.BACKSLASH_ESCAPE,a,s,o]},{begin:/([fF][rR]|[rR][fF]|[fF])"""/,end:/"""/,contains:[e.BACKSLASH_ESCAPE,a,s,o]},{begin:/([uU]|[rR])'/,end:/'/,relevance:10},{begin:/([uU]|[rR])"/,end:/"/,relevance:10},{begin:/([bB]|[bB][rR]|[rR][bB])'/,end:/'/},{begin:/([bB]|[bB][rR]|[rR][bB])"/,end:/"/},{begin:/([fF][rR]|[rR][fF]|[fF])'/,end:/'/,contains:[e.BACKSLASH_ESCAPE,s,o]},{begin:/([fF][rR]|[rR][fF]|[fF])"/,end:/"/,contains:[e.BACKSLASH_ESCAPE,s,o]},e.APOS_STRING_MODE,e.QUOTE_STRING_MODE]},l=`[0-9](_?[0-9])*`,u=`(\\b(${l}))?\\.(${l})|\\b(${l})\\.`,d=`\\b|${r.join(`|`)}`,f={className:`number`,relevance:0,variants:[{begin:`(\\b(${l})|(${u}))[eE][+-]?(${l})[jJ]?(?=${d})`},{begin:`(${u})[jJ]?`},{begin:`\\b([1-9](_?[0-9])*|0+(_?0)*)[lLjJ]?(?=${d})`},{begin:`\\b0[bB](_?[01])+[lL]?(?=${d})`},{begin:`\\b0[oO](_?[0-7])+[lL]?(?=${d})`},{begin:`\\b0[xX](_?[0-9a-fA-F])+[lL]?(?=${d})`},{begin:`\\b(${l})[jJ](?=${d})`}]},p={className:`comment`,begin:t.lookahead(/# type:/),end:/$/,keywords:i,contains:[{begin:/# type:/},{begin:/#/,end:/\b\B/,endsWithParent:!0}]},m={className:`params`,variants:[{className:``,begin:/\(\s*\)/,skip:!0},{begin:/\(/,end:/\)/,excludeBegin:!0,excludeEnd:!0,keywords:i,contains:[`self`,a,f,c,e.HASH_COMMENT_MODE]}]};return o.contains=[c,f,a],{name:`Python`,aliases:[`py`,`gyp`,`ipython`],unicodeRegex:!0,keywords:i,illegal:/(<\/|\?)|=>/,contains:[a,f,{scope:`variable.language`,match:/\bself\b/},{beginKeywords:`if`,relevance:0},{match:/\bor\b/,scope:`keyword`},c,p,e.HASH_COMMENT_MODE,{match:[/\bdef/,/\s+/,n],scope:{1:`keyword`,3:`title.function`},contains:[m]},{variants:[{match:[/\bclass/,/\s+/,n,/\s*/,/\(\s*/,n,/\s*\)/]},{match:[/\bclass/,/\s+/,n]}],scope:{1:`keyword`,3:`title.class`,6:`title.class.inherited`}},{className:`meta`,begin:/^[\t ]*@/,end:/(?=#)|$/,contains:[f,m,c]}]}}function Ze(e){return{name:`Shell Session`,aliases:[`console`,`shellsession`],contains:[{className:`meta.prompt`,begin:/^\s{0,3}[/~\w\d[\]()@-]*[>%$#][ ]?/,starts:{end:/[^\\](?=\s*$)/,subLanguage:`bash`}}]}}function Qe(){return{async:!1,breaks:!1,extensions:null,gfm:!0,hooks:null,pedantic:!1,renderer:null,silent:!1,tokenizer:null,walkTokens:null}}var $e=Qe();function et(e){$e=e}var tt={exec:()=>null};function nt(e){let t=[];return n=>{let r=Math.max(0,Math.min(3,n-1)),i=t[r];return i||(i=e(r),t[r]=i),i}}function F(e,t=``){let n=typeof e==`string`?e:e.source,r={replace:(e,t)=>{let i=typeof t==`string`?t:t.source;return i=i.replace(it.caret,`$1`),n=n.replace(e,i),r},getRegex:()=>new RegExp(n,t)};return r}var rt=((e=``)=>{try{return!!RegExp(`(?<=1)(?<!1)`+e)}catch{return!1}})(),it={codeRemoveIndent:/^(?: {1,4}| {0,3}\t)/gm,outputLinkReplace:/\\([\[\]])/g,indentCodeCompensation:/^(\s+)(?:```)/,beginningSpace:/^\s+/,endingHash:/#$/,startingSpaceChar:/^ /,endingSpaceChar:/ $/,nonSpaceChar:/[^ ]/,newLineCharGlobal:/\n/g,tabCharGlobal:/\t/g,multipleSpaceGlobal:/\s+/g,blankLine:/^[ \t]*$/,doubleBlankLine:/\n[ \t]*\n[ \t]*$/,blockquoteStart:/^ {0,3}>/,blockquoteSetextReplace:/\n {0,3}((?:=+|-+) *)(?=\n|$)/g,blockquoteSetextReplace2:/^ {0,3}>[ \t]?/gm,listReplaceNesting:/^ {1,4}(?=( {4})*[^ ])/g,listIsTask:/^\[[ xX]\] +\S/,listReplaceTask:/^\[[ xX]\] +/,listTaskCheckbox:/\[[ xX]\]/,anyLine:/\n.*\n/,hrefBrackets:/^<(.*)>$/,tableDelimiter:/[:|]/,tableAlignChars:/^\||\| *$/g,tableRowBlankLine:/\n[ \t]*$/,tableAlignRight:/^ *-+: *$/,tableAlignCenter:/^ *:-+: *$/,tableAlignLeft:/^ *:-+ *$/,startATag:/^<a /i,endATag:/^<\/a>/i,startPreScriptTag:/^<(pre|code|kbd|script)(\s|>)/i,endPreScriptTag:/^<\/(pre|code|kbd|script)(\s|>)/i,startAngleBracket:/^</,endAngleBracket:/>$/,pedanticHrefTitle:/^([^'"]*[^\s])\s+(['"])(.*)\2/,unicodeAlphaNumeric:/[\p{L}\p{N}]/u,escapeTest:/[&<>"']/,escapeReplace:/[&<>"']/g,escapeTestNoEncode:/[<>"']|&(?!(#\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\w+);)/,escapeReplaceNoEncode:/[<>"']|&(?!(#\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\w+);)/g,caret:/(^|[^\[])\^/g,percentDecode:/%25/g,findPipe:/\|/g,splitPipe:/ \|/,slashPipe:/\\\|/g,carriageReturn:/\r\n|\r/g,spaceLine:/^ +$/gm,notSpaceStart:/^\S*/,endingNewline:/\n$/,listItemRegex:e=>RegExp(`^( {0,3}${e})((?:[	 ][^\\n]*)?(?:\\n|$))`),nextBulletRegex:nt(e=>RegExp(`^ {0,${e}}(?:[*+-]|\\d{1,9}[.)])((?:[ 	][^\\n]*)?(?:\\n|$))`)),hrRegex:nt(e=>RegExp(`^ {0,${e}}((?:- *){3,}|(?:_ *){3,}|(?:\\* *){3,})(?:\\n+|$)`)),fencesBeginRegex:nt(e=>RegExp(`^ {0,${e}}(?:\`\`\`|~~~)`)),headingBeginRegex:nt(e=>RegExp(`^ {0,${e}}#`)),htmlBeginRegex:nt(e=>RegExp(`^ {0,${e}}<(?:[a-z].*>|!--)`,`i`)),blockquoteBeginRegex:nt(e=>RegExp(`^ {0,${e}}>`))},at=/^(?:[ \t]*(?:\n|$))+/,ot=/^((?: {4}| {0,3}\t)[^\n]+(?:\n(?:[ \t]*(?:\n|$))*)?)+/,st=/^ {0,3}(`{3,}(?=[^`\n]*(?:\n|$))|~{3,})([^\n]*)(?:\n|$)(?:|([\s\S]*?)(?:\n|$))(?: {0,3}\1[~`]* *(?=\n|$)|$)/,ct=/^ {0,3}((?:-[\t ]*){3,}|(?:_[ \t]*){3,}|(?:\*[ \t]*){3,})(?:\n+|$)/,lt=/^ {0,3}(#{1,6})(?=\s|$)(.*)(?:\n+|$)/,ut=/ {0,3}(?:[*+-]|\d{1,9}[.)])/,dt=/^(?!bull |blockCode|fences|blockquote|heading|html|table)((?:.|\n(?!\s*?\n|bull |blockCode|fences|blockquote|heading|html|table))+?)\n {0,3}(=+|-+) *(?:\n+|$)/,ft=F(dt).replace(/bull/g,ut).replace(/blockCode/g,/(?: {4}| {0,3}\t)/).replace(/fences/g,/ {0,3}(?:`{3,}|~{3,})/).replace(/blockquote/g,/ {0,3}>/).replace(/heading/g,/ {0,3}#{1,6}(?:\s|$)/).replace(/html/g,/ {0,3}<[^\n>]+>\n/).replace(/\|table/g,``).getRegex(),pt=F(dt).replace(/bull/g,ut).replace(/blockCode/g,/(?: {4}| {0,3}\t)/).replace(/fences/g,/ {0,3}(?:`{3,}|~{3,})/).replace(/blockquote/g,/ {0,3}>/).replace(/heading/g,/ {0,3}#{1,6}(?:\s|$)/).replace(/html/g,/ {0,3}<[^\n>]+>\n/).replace(/table/g,/ {0,3}\|?(?:[:\- ]*\|)+[\:\- ]*\n/).getRegex(),mt=/^([^\n]+(?:\n(?!hr|heading|lheading|blockquote|fences|list|html|table|[ \t]+\n)[^\n]+)*)/,ht=/^[^\n]+/,gt=/(?!\s*\])(?:\\[\s\S]|[^\[\]\\])+/,_t=F(/^ {0,3}\[(label)\]: *(?:\n[ \t]*)?([^<\s][^\s]*|<.*?>)(?:(?: +(?:\n[ \t]*)?| *\n[ \t]*)(title))? *(?:\n+|$)/).replace(`label`,gt).replace(`title`,/(?:"(?:\\"?|[^"\\])*"|'[^'\n]*(?:\n[^'\n]+)*\n?'|\([^()]*\))/).getRegex(),vt=F(/^(bull)([ \t][^\n]*?)?(?:\n|$)/).replace(/bull/g,ut).getRegex(),yt=`address|article|aside|base|basefont|blockquote|body|caption|center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|footer|form|frame|frameset|h[1-6]|head|header|hr|html|iframe|legend|li|link|main|menu|menuitem|meta|nav|noframes|ol|optgroup|option|p|param|search|section|summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul`,bt=/<!--(?:-?>|[\s\S]*?(?:-->|$))/,xt=F(`^ {0,3}(?:<(script|pre|style|textarea)[\\s>][\\s\\S]*?(?:</\\1>[^\\n]*\\n*|$)|comment[^\\n]*(\\n+|$)|<\\?[\\s\\S]*?(?:\\?>[^\\n]*\\n*|$)|<![A-Z][\\s\\S]*?(?:>[^\\n]*\\n*|$)|<!\\[CDATA\\[[\\s\\S]*?(?:\\]\\]>[^\\n]*\\n*|$)|</?(tag)(?: +|\\n|/?>)[\\s\\S]*?(?:(?:\\n[ 	]*)+\\n|$)|<(?!script|pre|style|textarea)([a-z][\\w-]*)(?:attribute)*? */?>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n[ 	]*)+\\n|$)|</(?!script|pre|style|textarea)[a-z][\\w-]*\\s*>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n[ 	]*)+\\n|$))`,`i`).replace(`comment`,bt).replace(`tag`,yt).replace(`attribute`,/ +[a-zA-Z:_][\w.:-]*(?: *= *"[^"\n]*"| *= *'[^'\n]*'| *= *[^\s"'=<>`]+)?/).getRegex(),St=e=>F(mt).replace(`hr`,ct).replace(`heading`,` {0,3}#{1,6}(?:\\s|$)`).replace(`|lheading`,``).replace(`|table`,``).replace(`blockquote`,` {0,3}>`).replace(`fences`," {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~~~)[^\\n]*\\n").replace(`list`,e).replace(`html`,`</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)`).replace(`tag`,yt).getRegex(),Ct=St(/ {0,3}(?:[*+-]|1[.)])[ \t]+[^ \t\n]/),wt=St(/ {0,3}(?:[*+-]|\d{1,9}[.)])(?:[ \t]|\n|$)/),Tt={blockquote:F(/^( {0,3}> ?(paragraph|[^\n]*)(?:\n|$))+/).replace(`paragraph`,wt).getRegex(),code:ot,def:_t,fences:st,heading:lt,hr:ct,html:xt,lheading:ft,list:vt,newline:at,paragraph:Ct,table:tt,text:ht},Et=F(`^ *([^\\n ].*)\\n {0,3}((?:\\| *)?:?-+:? *(?:\\| *:?-+:? *)*(?:\\| *)?)(?:\\n((?:(?! *\\n|hr|heading|blockquote|code|fences|list|html).*(?:\\n|$))*)\\n*|$)`).replace(`hr`,ct).replace(`heading`,` {0,3}#{1,6}(?:\\s|$)`).replace(`blockquote`,` {0,3}>`).replace(`code`,`(?: {4}| {0,3}	)[^\\n]`).replace(`fences`," {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~~~)[^\\n]*\\n").replace(`list`,` {0,3}(?:[*+-]|1[.)])[ \\t]`).replace(`html`,`</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)`).replace(`tag`,yt).getRegex(),Dt={...Tt,lheading:pt,table:Et,paragraph:F(mt).replace(`hr`,ct).replace(`heading`,` {0,3}#{1,6}(?:\\s|$)`).replace(`|lheading`,``).replace(`table`,Et).replace(`blockquote`,` {0,3}>`).replace(`fences`," {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~~~)[^\\n]*\\n").replace(`list`,` {0,3}(?:[*+-]|1[.)])[ \\t]+[^ \\t\\n]`).replace(`html`,`</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)`).replace(`tag`,yt).getRegex()},Ot={...Tt,html:F(`^ *(?:comment *(?:\\n|\\s*$)|<(tag)[\\s\\S]+?</\\1> *(?:\\n{2,}|\\s*$)|<tag(?:"[^"]*"|'[^']*'|\\s[^'"/>\\s]*)*?/?> *(?:\\n{2,}|\\s*$))`).replace(`comment`,bt).replace(/tag/g,`(?!(?:a|em|strong|small|s|cite|q|dfn|abbr|data|time|code|var|samp|kbd|sub|sup|i|b|u|mark|ruby|rt|rp|bdi|bdo|span|br|wbr|ins|del|img)\\b)\\w+(?!:|[^\\w\\s@]*@)\\b`).getRegex(),def:/^ *\[([^\]]+)\]: *<?([^\s>]+)>?(?: +(["(][^\n]+[")]))? *(?:\n+|$)/,heading:/^(#{1,6})(.*)(?:\n+|$)/,fences:tt,lheading:/^(.+?)\n {0,3}(=+|-+) *(?:\n+|$)/,paragraph:F(mt).replace(`hr`,ct).replace(`heading`,` *#{1,6} *[^
]`).replace(`lheading`,ft).replace(`|table`,``).replace(`blockquote`,` {0,3}>`).replace(`|fences`,``).replace(`|list`,``).replace(`|html`,``).replace(`|tag`,``).getRegex()},kt=/^\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/,At=/^(`+)([^`]|[^`][\s\S]*?[^`])\1(?!`)/,jt=/^( {2,}|\\)\n(?!\s*$)/,Mt=/^(`+|[^`])(?:(?= {2,}\n)|[\s\S]*?(?:(?=[\\<!\[`*_]|\b_|$)|[^ ](?= {2,}\n)))/,Nt=/[\p{P}\p{S}]/u,Pt=/[\s\p{P}\p{S}]/u,Ft=/[^\s\p{P}\p{S}]/u,It=F(/^((?![*_])punctSpace)/,`u`).replace(/punctSpace/g,Pt).getRegex(),Lt=/(?!~)[\p{P}\p{S}]/u,Rt=/(?!~)[\s\p{P}\p{S}]/u,zt=/(?:[^\s\p{P}\p{S}]|~)/u,Bt=F(/link|precode-code|html/,`g`).replace(`link`,/\[(?:[^\[\]`]|(?<a>`+)[^`]+\k<a>(?!`))*?\]\((?:\\[\s\S]|[^\\\(\)]|\((?:\\[\s\S]|[^\\\(\)])*\))*\)/).replace(`precode-`,rt?"(?<!`)()":"(^^|[^`])").replace(`code`,/(?<b>`+)[^`]+\k<b>(?!`)/).replace(`html`,/<(?! )[^<>]*?>/).getRegex(),Vt=/^(?:\*+(?:((?!\*)punct)|([^\s*]))?)|^_+(?:((?!_)punct)|([^\s_]))?/,Ht=F(Vt,`u`).replace(/punct/g,Nt).getRegex(),Ut=F(Vt,`u`).replace(/punct/g,Lt).getRegex(),Wt=`^[^_*]*?__[^_*]*?\\*[^_*]*?(?=__)|[^*]+(?=[^*])|(?!\\*)punct(\\*+)(?=[\\s]|$)|notPunctSpace(\\*+)(?!\\*)(?=punctSpace|$)|(?!\\*)punctSpace(\\*+)(?=notPunctSpace)|[\\s](\\*+)(?!\\*)(?=punct)|(?!\\*)punct(\\*+)(?!\\*)(?=punct)|notPunctSpace(\\*+)(?=notPunctSpace)`,Gt=F(Wt,`gu`).replace(/notPunctSpace/g,Ft).replace(/punctSpace/g,Pt).replace(/punct/g,Nt).getRegex(),Kt=F(Wt,`gu`).replace(/notPunctSpace/g,zt).replace(/punctSpace/g,Rt).replace(/punct/g,Lt).getRegex(),qt=F(`^[^_*]*?\\*\\*[^_*]*?_[^_*]*?(?=\\*\\*)|[^_]+(?=[^_])|(?!_)punct(_+)(?=[\\s]|$)|notPunctSpace(_+)(?!_)(?=punctSpace|$)|(?!_)punctSpace(_+)(?=notPunctSpace)|[\\s](_+)(?!_)(?=punct)|(?!_)punct(_+)(?!_)(?=punct)`,`gu`).replace(/notPunctSpace/g,Ft).replace(/punctSpace/g,Pt).replace(/punct/g,Nt).getRegex(),Jt=F(/^~~?(?:((?!~)punct)|[^\s~])/,`u`).replace(/punct/g,Nt).getRegex(),Yt=F(`^[^~]+(?=[^~])|(?!~)punct(~~?)(?=[\\s]|$)|notPunctSpace(~~?)(?!~)(?=punctSpace|$)|(?!~)punctSpace(~~?)(?=notPunctSpace)|[\\s](~~?)(?!~)(?=punct)|(?!~)punct(~~?)(?!~)(?=punct)|notPunctSpace(~~?)(?=notPunctSpace)`,`gu`).replace(/notPunctSpace/g,Ft).replace(/punctSpace/g,Pt).replace(/punct/g,Nt).getRegex(),Xt=F(/\\(punct)/,`gu`).replace(/punct/g,Nt).getRegex(),Zt=F(/^<(scheme:[^\s\x00-\x1f<>]*|email)>/).replace(`scheme`,/[a-zA-Z][a-zA-Z0-9+.-]{1,31}/).replace(`email`,/[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+(@)[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+(?![-_])/).getRegex(),Qt=F(bt).replace(`(?:-->|$)`,`-->`).getRegex(),$t=F(`^comment|^</[a-zA-Z][\\w:-]*\\s*>|^<[a-zA-Z][\\w-]*(?:attribute)*?\\s*/?>|^<\\?[\\s\\S]*?\\?>|^<![a-zA-Z]+\\s[\\s\\S]*?>|^<!\\[CDATA\\[[\\s\\S]*?\\]\\]>`).replace(`comment`,Qt).replace(`attribute`,/\s+[a-zA-Z:_][\w.:-]*(?:\s*=\s*"[^"]*"|\s*=\s*'[^']*'|\s*=\s*[^\s"'=<>`]+)?/).getRegex(),en=/(?:\[(?:\\[\s\S]|[^\[\]\\])*\]|\\[\s\S]|`+(?!`)[^`]*?`+(?!`)|``+(?=\])|[^\[\]\\`])*?/,tn=F(/^!?\[(label)\]\(\s*(href)(?:(?:[ \t]+(?:\n[ \t]*)?|\n[ \t]*)(title))?\s*\)/).replace(`label`,en).replace(`href`,/<(?:\\.|[^\n<>\\])+>|[^ \t\n\x00-\x1f]+|(?=\))/).replace(`title`,/"(?:\\"?|[^"\\])*"|'(?:\\'?|[^'\\])*'|\((?:\\\)?|[^)\\])*\)/).getRegex(),nn=F(/^!?\[(label)\]\[(ref)\]/).replace(`label`,en).replace(`ref`,gt).getRegex(),rn=F(/^!?\[(ref)\](?:\[\])?/).replace(`ref`,gt).getRegex(),an=F(`reflink|nolink(?!\\()`,`g`).replace(`reflink`,nn).replace(`nolink`,rn).getRegex(),on=/[hH][tT][tT][pP][sS]?|[fF][tT][pP]/,sn={_backpedal:tt,anyPunctuation:Xt,autolink:Zt,blockSkip:Bt,br:jt,code:At,del:tt,delLDelim:tt,delRDelim:tt,emStrongLDelim:Ht,emStrongRDelimAst:Gt,emStrongRDelimUnd:qt,escape:kt,link:tn,nolink:rn,punctuation:It,reflink:nn,reflinkSearch:an,tag:$t,text:Mt,url:tt},cn={...sn,link:F(/^!?\[(label)\]\((.*?)\)/).replace(`label`,en).getRegex(),reflink:F(/^!?\[(label)\]\s*\[([^\]]*)\]/).replace(`label`,en).getRegex()},ln={...sn,emStrongRDelimAst:Kt,emStrongLDelim:Ut,delLDelim:Jt,delRDelim:Yt,url:F(/^((?:protocol):\/\/|www\.)(?:[a-zA-Z0-9\-]+\.?)+[^\s<]*|^email/).replace(`protocol`,on).replace(`email`,/[A-Za-z0-9._+-]+(@)[a-zA-Z0-9-_]+(?:\.[a-zA-Z0-9-_]*[a-zA-Z0-9])+(?![-_])/).getRegex(),_backpedal:/(?:[^?!.,:;*_'"~()&]+|\([^)]*\)|&(?![a-zA-Z0-9]+;$)|[?!.,:;*_'"~)]+(?!$))+/,del:/^(~~?)(?=[^\s~])((?:\\[\s\S]|[^\\])*?(?:\\[\s\S]|[^\s~\\]))\1(?=[^~]|$)/,text:F(/^(`+|~+|[^`~])(?:(?=[`~])|(?= {2,}\n)|(?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)|[\s\S]*?(?:(?=[\\<!\[`*~_]|\b_|protocol:\/\/|www\.|$)|[^ ](?= {2,}\n)|[^a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-](?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)))/).replace(`protocol`,on).getRegex()},un={...ln,br:F(jt).replace(`{2,}`,`*`).getRegex(),text:F(ln.text).replace(`\\b_`,`\\b_| {2,}\\n`).replace(/\{2,\}/g,`*`).getRegex()},dn={normal:Tt,gfm:Dt,pedantic:Ot},fn={normal:sn,gfm:ln,breaks:un,pedantic:cn},pn={"&":`&amp;`,"<":`&lt;`,">":`&gt;`,'"':`&quot;`,"'":`&#39;`},mn=e=>pn[e];function hn(e,t){if(t){if(it.escapeTest.test(e))return e.replace(it.escapeReplace,mn)}else if(it.escapeTestNoEncode.test(e))return e.replace(it.escapeReplaceNoEncode,mn);return e}function gn(e){try{e=encodeURI(e).replace(it.percentDecode,`%`)}catch{return null}return e}function _n(e,t){let n=e.replace(it.findPipe,(e,t,n)=>{let r=!1,i=t;for(;--i>=0&&n[i]===`\\`;)r=!r;return r?`|`:` |`}).split(it.splitPipe),r=0;if(n[0].trim()||n.shift(),n.length>0&&!n.at(-1)?.trim()&&n.pop(),t)if(n.length>t)n.splice(t);else for(;n.length<t;)n.push(``);for(;r<n.length;r++)n[r]=n[r].trim().replace(it.slashPipe,`|`);return n}function vn(e,t,n){let r=e.length;if(r===0)return``;let i=0;for(;i<r;){let a=e.charAt(r-i-1);if(a===t&&!n)i++;else if(a!==t&&n)i++;else break}return e.slice(0,r-i)}function yn(e){let t=e.split(`
`),n=t.length-1;for(;n>=0&&it.blankLine.test(t[n]);)n--;return t.length-n<=2?e:t.slice(0,n+1).join(`
`)}function bn(e,t){if(e.indexOf(t[1])===-1)return-1;let n=0;for(let r=0;r<e.length;r++)if(e[r]===`\\`)r++;else if(e[r]===t[0])n++;else if(e[r]===t[1]&&(n--,n<0))return r;return n>0?-2:-1}function xn(e,t=0){let n=t,r=``;for(let t of e)if(t===`	`){let e=4-n%4;r+=` `.repeat(e),n+=e}else r+=t,n++;return r}function Sn(e,t,n,r,i){let a=t.href,o=t.title||null,s=e[1].replace(i.other.outputLinkReplace,`$1`);r.state.inLink=!0;let c={type:e[0].charAt(0)===`!`?`image`:`link`,raw:n,href:a,title:o,text:s,tokens:r.inlineTokens(s)};return r.state.inLink=!1,c}function Cn(e,t,n){let r=e.match(n.other.indentCodeCompensation);if(r===null)return t;let i=r[1];return t.split(`
`).map(e=>{let t=e.match(n.other.beginningSpace);if(t===null)return e;let[r]=t;return r.length>=i.length?e.slice(i.length):e}).join(`
`)}var wn=class{options;rules;lexer;constructor(e){this.options=e||$e}space(e){let t=this.rules.block.newline.exec(e);if(t&&t[0].length>0)return{type:`space`,raw:t[0]}}code(e){let t=this.rules.block.code.exec(e);if(t){let e=this.options.pedantic?t[0]:yn(t[0]);return{type:`code`,raw:e,codeBlockStyle:`indented`,text:e.replace(this.rules.other.codeRemoveIndent,``)}}}fences(e){let t=this.rules.block.fences.exec(e);if(t){let e=t[0],n=Cn(e,t[3]||``,this.rules);return{type:`code`,raw:e,lang:t[2]?t[2].trim().replace(this.rules.inline.anyPunctuation,`$1`):t[2],text:n}}}heading(e){let t=this.rules.block.heading.exec(e);if(t){let e=t[2].trim();if(this.rules.other.endingHash.test(e)){let t=vn(e,`#`);(this.options.pedantic||!t||this.rules.other.endingSpaceChar.test(t))&&(e=t.trim())}return{type:`heading`,raw:vn(t[0],`
`),depth:t[1].length,text:e,tokens:this.lexer.inline(e)}}}hr(e){let t=this.rules.block.hr.exec(e);if(t)return{type:`hr`,raw:vn(t[0],`
`)}}blockquote(e){let t=this.rules.block.blockquote.exec(e);if(t){let e=vn(t[0],`
`).split(`
`),n=``,r=``,i=[];for(;e.length>0;){let t=!1,a=[],o;for(o=0;o<e.length;o++)if(this.rules.other.blockquoteStart.test(e[o]))a.push(e[o]),t=!0;else if(!t)a.push(e[o]);else break;e=e.slice(o);let s=a.join(`
`),c=s.replace(this.rules.other.blockquoteSetextReplace,`
    $1`).replace(this.rules.other.blockquoteSetextReplace2,``);n=n?`${n}
${s}`:s,r=r?`${r}
${c}`:c;let l=this.lexer.state.top;if(this.lexer.state.top=!0,this.lexer.blockTokens(c,i,!0),this.lexer.state.top=l,e.length===0)break;let u=i.at(-1);if(u?.type===`code`)break;if(u?.type===`blockquote`){let t=u,a=t.raw+`
`+e.join(`
`),o=this.blockquote(a);i[i.length-1]=o,n=n.substring(0,n.length-t.raw.length)+o.raw,r=r.substring(0,r.length-t.text.length)+o.text;break}else if(u?.type===`list`){let t=u,a=t.raw+`
`+e.join(`
`),o=this.list(a);i[i.length-1]=o,n=n.substring(0,n.length-u.raw.length)+o.raw,r=r.substring(0,r.length-t.raw.length)+o.raw,e=a.substring(i.at(-1).raw.length).split(`
`);continue}}return{type:`blockquote`,raw:n,tokens:i,text:r}}}list(e){let t=this.rules.block.list.exec(e);if(t){let n=t[1].trim(),r=n.length>1,i={type:`list`,raw:``,ordered:r,start:r?+n.slice(0,-1):``,loose:!1,items:[]};n=r?`\\d{1,9}\\${n.slice(-1)}`:`\\${n}`,this.options.pedantic&&(n=r?n:`[*+-]`);let a=this.rules.other.listItemRegex(n),o=!1;for(;e;){let n=!1,r=``,s=``;if(!(t=a.exec(e))||this.rules.block.hr.test(e))break;r=t[0],e=e.substring(r.length);let c=xn(t[2].split(`
`,1)[0],t[1].length),l=e.split(`
`,1)[0],u=!c.trim(),d=0;if(this.options.pedantic?(d=2,s=c.trimStart()):u?d=t[1].length+1:(d=c.search(this.rules.other.nonSpaceChar),d=d>4?1:d,s=c.slice(d),d+=t[1].length),u&&this.rules.other.blankLine.test(l)&&(r+=l+`
`,e=e.substring(l.length+1),n=!0),!n){let t=this.rules.other.nextBulletRegex(d),n=this.rules.other.hrRegex(d),i=this.rules.other.fencesBeginRegex(d),a=this.rules.other.headingBeginRegex(d),o=this.rules.other.htmlBeginRegex(d),f=this.rules.other.blockquoteBeginRegex(d);for(;e;){let p=e.split(`
`,1)[0],m;if(l=p,this.options.pedantic?(l=l.replace(this.rules.other.listReplaceNesting,`  `),m=l):m=l.replace(this.rules.other.tabCharGlobal,`    `),i.test(l)||a.test(l)||o.test(l)||f.test(l)||t.test(l)||n.test(l))break;if(m.search(this.rules.other.nonSpaceChar)>=d||!l.trim())s+=`
`+m.slice(d);else{if(u||c.replace(this.rules.other.tabCharGlobal,`    `).search(this.rules.other.nonSpaceChar)>=4||i.test(c)||a.test(c)||n.test(c))break;s+=`
`+l}u=!l.trim(),r+=p+`
`,e=e.substring(p.length+1),c=m.slice(d)}}i.loose||(o?i.loose=!0:this.rules.other.doubleBlankLine.test(r)&&(o=!0)),i.items.push({type:`list_item`,raw:r,task:!!this.options.gfm&&this.rules.other.listIsTask.test(s),loose:!1,text:s,tokens:[]}),i.raw+=r}let s=i.items.at(-1);if(s)s.raw=s.raw.trimEnd(),s.text=s.text.trimEnd();else return;i.raw=i.raw.trimEnd();for(let e of i.items){this.lexer.state.top=!1,e.tokens=this.lexer.blockTokens(e.text,[]);let t=e.tokens[0];if(e.task&&(t?.type===`text`||t?.type===`paragraph`)){e.text=e.text.replace(this.rules.other.listReplaceTask,``),t.raw=t.raw.replace(this.rules.other.listReplaceTask,``),t.text=t.text.replace(this.rules.other.listReplaceTask,``);for(let e=this.lexer.inlineQueue.length-1;e>=0;e--)if(this.rules.other.listIsTask.test(this.lexer.inlineQueue[e].src)){this.lexer.inlineQueue[e].src=this.lexer.inlineQueue[e].src.replace(this.rules.other.listReplaceTask,``);break}let n=this.rules.other.listTaskCheckbox.exec(e.raw);if(n){let t={type:`checkbox`,raw:n[0]+` `,checked:n[0]!==`[ ]`};e.checked=t.checked,i.loose?e.tokens[0]&&[`paragraph`,`text`].includes(e.tokens[0].type)&&`tokens`in e.tokens[0]&&e.tokens[0].tokens?(e.tokens[0].raw=t.raw+e.tokens[0].raw,e.tokens[0].text=t.raw+e.tokens[0].text,e.tokens[0].tokens.unshift(t)):e.tokens.unshift({type:`paragraph`,raw:t.raw,text:t.raw,tokens:[t]}):e.tokens.unshift(t)}}else e.task&&=!1;if(!i.loose){let t=e.tokens.filter(e=>e.type===`space`);i.loose=t.length>0&&t.some(e=>this.rules.other.anyLine.test(e.raw))}}if(i.loose)for(let e of i.items){e.loose=!0;for(let t of e.tokens)t.type===`text`&&(t.type=`paragraph`)}return i}}html(e){let t=this.rules.block.html.exec(e);if(t){let e=yn(t[0]);return{type:`html`,block:!0,raw:e,pre:t[1]===`pre`||t[1]===`script`||t[1]===`style`,text:e}}}def(e){let t=this.rules.block.def.exec(e);if(t){let e=t[1].toLowerCase().replace(this.rules.other.multipleSpaceGlobal,` `),n=t[2]?t[2].replace(this.rules.other.hrefBrackets,`$1`).replace(this.rules.inline.anyPunctuation,`$1`):``,r=t[3]?t[3].substring(1,t[3].length-1).replace(this.rules.inline.anyPunctuation,`$1`):t[3];return{type:`def`,tag:e,raw:vn(t[0],`
`),href:n,title:r}}}table(e){let t=this.rules.block.table.exec(e);if(!t||!this.rules.other.tableDelimiter.test(t[2]))return;let n=_n(t[1]),r=t[2].replace(this.rules.other.tableAlignChars,``).split(`|`),i=t[3]?.trim()?t[3].replace(this.rules.other.tableRowBlankLine,``).split(`
`):[],a={type:`table`,raw:vn(t[0],`
`),header:[],align:[],rows:[]};if(n.length===r.length){for(let e of r)this.rules.other.tableAlignRight.test(e)?a.align.push(`right`):this.rules.other.tableAlignCenter.test(e)?a.align.push(`center`):this.rules.other.tableAlignLeft.test(e)?a.align.push(`left`):a.align.push(null);for(let e=0;e<n.length;e++)a.header.push({text:n[e],tokens:this.lexer.inline(n[e]),header:!0,align:a.align[e]});for(let e of i)a.rows.push(_n(e,a.header.length).map((e,t)=>({text:e,tokens:this.lexer.inline(e),header:!1,align:a.align[t]})));return a}}lheading(e){let t=this.rules.block.lheading.exec(e);if(t){let e=t[1].trim();return{type:`heading`,raw:vn(t[0],`
`),depth:t[2].charAt(0)===`=`?1:2,text:e,tokens:this.lexer.inline(e)}}}paragraph(e){let t=this.rules.block.paragraph.exec(e);if(t){let e=t[1].charAt(t[1].length-1)===`
`?t[1].slice(0,-1):t[1];return{type:`paragraph`,raw:t[0],text:e,tokens:this.lexer.inline(e)}}}text(e){let t=this.rules.block.text.exec(e);if(t)return{type:`text`,raw:t[0],text:t[0],tokens:this.lexer.inline(t[0])}}escape(e){let t=this.rules.inline.escape.exec(e);if(t)return{type:`escape`,raw:t[0],text:t[1]}}tag(e){let t=this.rules.inline.tag.exec(e);if(t)return!this.lexer.state.inLink&&this.rules.other.startATag.test(t[0])?this.lexer.state.inLink=!0:this.lexer.state.inLink&&this.rules.other.endATag.test(t[0])&&(this.lexer.state.inLink=!1),!this.lexer.state.inRawBlock&&this.rules.other.startPreScriptTag.test(t[0])?this.lexer.state.inRawBlock=!0:this.lexer.state.inRawBlock&&this.rules.other.endPreScriptTag.test(t[0])&&(this.lexer.state.inRawBlock=!1),{type:`html`,raw:t[0],inLink:this.lexer.state.inLink,inRawBlock:this.lexer.state.inRawBlock,block:!1,text:t[0]}}link(e){let t=this.rules.inline.link.exec(e);if(t){let e=t[2].trim();if(!this.options.pedantic&&this.rules.other.startAngleBracket.test(e)){if(!this.rules.other.endAngleBracket.test(e))return;let t=vn(e.slice(0,-1),`\\`);if((e.length-t.length)%2==0)return}else{let e=bn(t[2],`()`);if(e===-2)return;if(e>-1){let n=(t[0].indexOf(`!`)===0?5:4)+t[1].length+e;t[2]=t[2].substring(0,e),t[0]=t[0].substring(0,n).trim(),t[3]=``}}let n=t[2],r=``;if(this.options.pedantic){let e=this.rules.other.pedanticHrefTitle.exec(n);e&&(n=e[1],r=e[3])}else r=t[3]?t[3].slice(1,-1):``;return n=n.trim(),this.rules.other.startAngleBracket.test(n)&&(n=this.options.pedantic&&!this.rules.other.endAngleBracket.test(e)?n.slice(1):n.slice(1,-1)),Sn(t,{href:n&&n.replace(this.rules.inline.anyPunctuation,`$1`),title:r&&r.replace(this.rules.inline.anyPunctuation,`$1`)},t[0],this.lexer,this.rules)}}reflink(e,t){let n;if((n=this.rules.inline.reflink.exec(e))||(n=this.rules.inline.nolink.exec(e))){let e=t[(n[2]||n[1]).replace(this.rules.other.multipleSpaceGlobal,` `).toLowerCase()];if(!e){let e=n[0].charAt(0);return{type:`text`,raw:e,text:e}}return Sn(n,e,n[0],this.lexer,this.rules)}}emStrong(e,t,n=``){let r=this.rules.inline.emStrongLDelim.exec(e);if(!(!r||!r[1]&&!r[2]&&!r[3]&&!r[4]||r[4]&&n.match(this.rules.other.unicodeAlphaNumeric))&&(!(r[1]||r[3])||!n||this.rules.inline.punctuation.exec(n))){let n=[...r[0]].length-1,i,a,o=n,s=0,c=r[0][0]===`*`?this.rules.inline.emStrongRDelimAst:this.rules.inline.emStrongRDelimUnd;for(c.lastIndex=0,t=t.slice(-1*e.length+n);(r=c.exec(t))!==null;){if(i=r[1]||r[2]||r[3]||r[4]||r[5]||r[6],!i)continue;if(a=[...i].length,r[3]||r[4]){o+=a;continue}else if((r[5]||r[6])&&n%3&&!((n+a)%3)){s+=a;continue}if(o-=a,o>0)continue;a=Math.min(a,a+o+s);let t=[...r[0]][0].length,c=e.slice(0,n+r.index+t+a);if(Math.min(n,a)%2){let e=c.slice(1,-1);return{type:`em`,raw:c,text:e,tokens:this.lexer.inlineTokens(e)}}let l=c.slice(2,-2);return{type:`strong`,raw:c,text:l,tokens:this.lexer.inlineTokens(l)}}}}codespan(e){let t=this.rules.inline.code.exec(e);if(t){let e=t[2].replace(this.rules.other.newLineCharGlobal,` `),n=this.rules.other.nonSpaceChar.test(e),r=this.rules.other.startingSpaceChar.test(e)&&this.rules.other.endingSpaceChar.test(e);return n&&r&&(e=e.substring(1,e.length-1)),{type:`codespan`,raw:t[0],text:e}}}br(e){let t=this.rules.inline.br.exec(e);if(t)return{type:`br`,raw:t[0]}}del(e,t,n=``){let r=this.rules.inline.delLDelim.exec(e);if(r&&(!r[1]||!n||this.rules.inline.punctuation.exec(n))){let n=[...r[0]].length-1,i,a,o=n,s=this.rules.inline.delRDelim;for(s.lastIndex=0,t=t.slice(-1*e.length+n);(r=s.exec(t))!==null;){if(i=r[1]||r[2]||r[3]||r[4]||r[5]||r[6],!i||(a=[...i].length,a!==n))continue;if(r[3]||r[4]){o+=a;continue}if(o-=a,o>0)continue;a=Math.min(a,a+o);let t=[...r[0]][0].length,s=e.slice(0,n+r.index+t+a),c=s.slice(n,-n);return{type:`del`,raw:s,text:c,tokens:this.lexer.inlineTokens(c)}}}}autolink(e){let t=this.rules.inline.autolink.exec(e);if(t){let e,n;return t[2]===`@`?(e=t[1],n=`mailto:`+e):(e=t[1],n=e),{type:`link`,raw:t[0],text:e,href:n,tokens:[{type:`text`,raw:e,text:e}]}}}url(e){let t;if(t=this.rules.inline.url.exec(e)){let e,n;if(t[2]===`@`)e=t[0],n=`mailto:`+e;else{let r;do r=t[0],t[0]=this.rules.inline._backpedal.exec(t[0])?.[0]??``;while(r!==t[0]);e=t[0],n=t[1]===`www.`?`http://`+t[0]:t[0]}return{type:`link`,raw:t[0],text:e,href:n,tokens:[{type:`text`,raw:e,text:e}]}}}inlineText(e){let t=this.rules.inline.text.exec(e);if(t){let e=this.lexer.state.inRawBlock;return{type:`text`,raw:t[0],text:t[0],escaped:e}}}},Tn=class e{tokens;options;state;inlineQueue;tokenizer;constructor(e){this.tokens=[],this.tokens.links=Object.create(null),this.options=e||$e,this.options.tokenizer=this.options.tokenizer||new wn,this.tokenizer=this.options.tokenizer,this.tokenizer.options=this.options,this.tokenizer.lexer=this,this.inlineQueue=[],this.state={inLink:!1,inRawBlock:!1,top:!0};let t={other:it,block:dn.normal,inline:fn.normal};this.options.pedantic?(t.block=dn.pedantic,t.inline=fn.pedantic):this.options.gfm&&(t.block=dn.gfm,this.options.breaks?t.inline=fn.breaks:t.inline=fn.gfm),this.tokenizer.rules=t}static get rules(){return{block:dn,inline:fn}}static lex(t,n){return new e(n).lex(t)}static lexInline(t,n){return new e(n).inlineTokens(t)}lex(e){e=e.replace(it.carriageReturn,`
`),this.blockTokens(e,this.tokens);for(let e=0;e<this.inlineQueue.length;e++){let t=this.inlineQueue[e];this.inlineTokens(t.src,t.tokens)}return this.inlineQueue=[],this.tokens}blockTokens(e,t=[],n=!1){this.tokenizer.lexer=this,this.options.pedantic&&(e=e.replace(it.tabCharGlobal,`    `).replace(it.spaceLine,``));let r=1/0;for(;e;){if(e.length<r)r=e.length;else{this.infiniteLoopError(e.charCodeAt(0));break}let i;if(this.options.extensions?.block?.some(n=>(i=n.call({lexer:this},e,t))?(e=e.substring(i.raw.length),t.push(i),!0):!1))continue;if(i=this.tokenizer.space(e)){e=e.substring(i.raw.length);let n=t.at(-1);i.raw.length===1&&n!==void 0?n.raw+=`
`:t.push(i);continue}if(i=this.tokenizer.code(e)){e=e.substring(i.raw.length);let n=t.at(-1);n?.type===`paragraph`||n?.type===`text`?(n.raw+=(n.raw.endsWith(`
`)?``:`
`)+i.raw,n.text+=`
`+i.text,this.inlineQueue.at(-1).src=n.text):t.push(i);continue}if(i=this.tokenizer.fences(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.heading(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.hr(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.blockquote(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.list(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.html(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.def(e)){e=e.substring(i.raw.length);let n=t.at(-1);n?.type===`paragraph`||n?.type===`text`?(n.raw+=(n.raw.endsWith(`
`)?``:`
`)+i.raw,n.text+=`
`+i.raw,this.inlineQueue.at(-1).src=n.text):this.tokens.links[i.tag]||(this.tokens.links[i.tag]={href:i.href,title:i.title},t.push(i));continue}if(i=this.tokenizer.table(e)){e=e.substring(i.raw.length),t.push(i);continue}if(i=this.tokenizer.lheading(e)){e=e.substring(i.raw.length),t.push(i);continue}let a=e;if(this.options.extensions?.startBlock){let t=1/0,n=e.slice(1),r;this.options.extensions.startBlock.forEach(e=>{r=e.call({lexer:this},n),typeof r==`number`&&r>=0&&(t=Math.min(t,r))}),t<1/0&&t>=0&&(a=e.substring(0,t+1))}if(this.state.top&&(i=this.tokenizer.paragraph(a))){let r=t.at(-1);n&&r?.type===`paragraph`?(r.raw+=(r.raw.endsWith(`
`)?``:`
`)+i.raw,r.text+=`
`+i.text,this.inlineQueue.pop(),this.inlineQueue.at(-1).src=r.text):t.push(i),n=a.length!==e.length,e=e.substring(i.raw.length);continue}if(i=this.tokenizer.text(e)){e=e.substring(i.raw.length);let n=t.at(-1);n?.type===`text`?(n.raw+=(n.raw.endsWith(`
`)?``:`
`)+i.raw,n.text+=`
`+i.text,this.inlineQueue.pop(),this.inlineQueue.at(-1).src=n.text):t.push(i);continue}if(e){this.infiniteLoopError(e.charCodeAt(0));break}}return this.state.top=!0,t}inline(e,t=[]){return this.inlineQueue.push({src:e,tokens:t}),t}inlineTokens(e,t=[]){this.tokenizer.lexer=this;let n=e;if(this.tokens.links){let e=Object.keys(this.tokens.links);e.length>0&&(n=n.replace(this.tokenizer.rules.inline.reflinkSearch,t=>e.includes(t.slice(t.lastIndexOf(`[`)+1,-1))?`[`+`a`.repeat(t.length-2)+`]`:t))}n=n.replace(this.tokenizer.rules.inline.anyPunctuation,`++`),n=n.replace(this.tokenizer.rules.inline.blockSkip,(e,t,n)=>{let r=n?n.length:0;return e.slice(0,r)+`[`+`a`.repeat(e.length-r-2)+`]`}),n=this.options.hooks?.emStrongMask?.call({lexer:this},n)??n;let r=!1,i=``,a=1/0;for(;e;){if(e.length<a)a=e.length;else{this.infiniteLoopError(e.charCodeAt(0));break}r||(i=``),r=!1;let o;if(this.options.extensions?.inline?.some(n=>(o=n.call({lexer:this},e,t))?(e=e.substring(o.raw.length),t.push(o),!0):!1))continue;if(o=this.tokenizer.escape(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.tag(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.link(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.reflink(e,this.tokens.links)){e=e.substring(o.raw.length);let n=t.at(-1);o.type===`text`&&n?.type===`text`?(n.raw+=o.raw,n.text+=o.text):t.push(o);continue}if(o=this.tokenizer.emStrong(e,n,i)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.codespan(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.br(e)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.del(e,n,i)){e=e.substring(o.raw.length),t.push(o);continue}if(o=this.tokenizer.autolink(e)){e=e.substring(o.raw.length),t.push(o);continue}if(!this.state.inLink&&(o=this.tokenizer.url(e))){e=e.substring(o.raw.length),t.push(o);continue}let s=e;if(this.options.extensions?.startInline){let t=1/0,n=e.slice(1),r;this.options.extensions.startInline.forEach(e=>{r=e.call({lexer:this},n),typeof r==`number`&&r>=0&&(t=Math.min(t,r))}),t<1/0&&t>=0&&(s=e.substring(0,t+1))}if(o=this.tokenizer.inlineText(s)){e=e.substring(o.raw.length),o.raw.slice(-1)!==`_`&&(i=o.raw.slice(-1)),r=!0;let n=t.at(-1);n?.type===`text`?(n.raw+=o.raw,n.text+=o.text):t.push(o);continue}if(e){this.infiniteLoopError(e.charCodeAt(0));break}}return t}infiniteLoopError(e){let t=`Infinite loop on byte: `+e;if(this.options.silent)console.error(t);else throw Error(t)}},En=class{options;parser;constructor(e){this.options=e||$e}space(e){return``}code({text:e,lang:t,escaped:n}){let r=(t||``).match(it.notSpaceStart)?.[0],i=e.replace(it.endingNewline,``)+`
`;return r?`<pre><code class="language-`+hn(r)+`">`+(n?i:hn(i,!0))+`</code></pre>
`:`<pre><code>`+(n?i:hn(i,!0))+`</code></pre>
`}blockquote({tokens:e}){return`<blockquote>
${this.parser.parse(e)}</blockquote>
`}html({text:e}){return e}def(e){return``}heading({tokens:e,depth:t}){return`<h${t}>${this.parser.parseInline(e)}</h${t}>
`}hr(e){return`<hr>
`}list(e){let t=e.ordered,n=e.start,r=``;for(let t=0;t<e.items.length;t++){let n=e.items[t];r+=this.listitem(n)}let i=t?`ol`:`ul`,a=t&&n!==1?` start="`+n+`"`:``;return`<`+i+a+`>
`+r+`</`+i+`>
`}listitem(e){return`<li>${this.parser.parse(e.tokens)}</li>
`}checkbox({checked:e}){return`<input `+(e?`checked="" `:``)+`disabled="" type="checkbox"> `}paragraph({tokens:e}){return`<p>${this.parser.parseInline(e)}</p>
`}table(e){let t=``,n=``;for(let t=0;t<e.header.length;t++)n+=this.tablecell(e.header[t]);t+=this.tablerow({text:n});let r=``;for(let t=0;t<e.rows.length;t++){let i=e.rows[t];n=``;for(let e=0;e<i.length;e++)n+=this.tablecell(i[e]);r+=this.tablerow({text:n})}return r&&=`<tbody>${r}</tbody>`,`<table>
<thead>
`+t+`</thead>
`+r+`</table>
`}tablerow({text:e}){return`<tr>
${e}</tr>
`}tablecell(e){let t=this.parser.parseInline(e.tokens),n=e.header?`th`:`td`;return(e.align?`<${n} align="${e.align}">`:`<${n}>`)+t+`</${n}>
`}strong({tokens:e}){return`<strong>${this.parser.parseInline(e)}</strong>`}em({tokens:e}){return`<em>${this.parser.parseInline(e)}</em>`}codespan({text:e}){return`<code>${hn(e,!0)}</code>`}br(e){return`<br>`}del({tokens:e}){return`<del>${this.parser.parseInline(e)}</del>`}link({href:e,title:t,tokens:n}){let r=this.parser.parseInline(n),i=gn(e);if(i===null)return r;e=i;let a=`<a href="`+e+`"`;return t&&(a+=` title="`+hn(t)+`"`),a+=`>`+r+`</a>`,a}image({href:e,title:t,text:n,tokens:r}){r&&(n=this.parser.parseInline(r,this.parser.textRenderer));let i=gn(e);if(i===null)return hn(n);e=i;let a=`<img src="${e}" alt="${hn(n)}"`;return t&&(a+=` title="${hn(t)}"`),a+=`>`,a}text(e){return`tokens`in e&&e.tokens?this.parser.parseInline(e.tokens):`escaped`in e&&e.escaped?e.text:hn(e.text)}},Dn=class{strong({text:e}){return e}em({text:e}){return e}codespan({text:e}){return e}del({text:e}){return e}html({text:e}){return e}text({text:e}){return e}link({text:e}){return``+e}image({text:e}){return``+e}br(){return``}checkbox({raw:e}){return e}},On=class e{options;renderer;textRenderer;constructor(e){this.options=e||$e,this.options.renderer=this.options.renderer||new En,this.renderer=this.options.renderer,this.renderer.options=this.options,this.renderer.parser=this,this.textRenderer=new Dn}static parse(t,n){return new e(n).parse(t)}static parseInline(t,n){return new e(n).parseInline(t)}parse(e){this.renderer.parser=this;let t=``;for(let n=0;n<e.length;n++){let r=e[n];if(this.options.extensions?.renderers?.[r.type]){let e=r,n=this.options.extensions.renderers[e.type].call({parser:this},e);if(n!==!1||![`space`,`hr`,`heading`,`code`,`table`,`blockquote`,`list`,`html`,`def`,`paragraph`,`text`].includes(e.type)){t+=n||``;continue}}let i=r;switch(i.type){case`space`:t+=this.renderer.space(i);break;case`hr`:t+=this.renderer.hr(i);break;case`heading`:t+=this.renderer.heading(i);break;case`code`:t+=this.renderer.code(i);break;case`table`:t+=this.renderer.table(i);break;case`blockquote`:t+=this.renderer.blockquote(i);break;case`list`:t+=this.renderer.list(i);break;case`checkbox`:t+=this.renderer.checkbox(i);break;case`html`:t+=this.renderer.html(i);break;case`def`:t+=this.renderer.def(i);break;case`paragraph`:t+=this.renderer.paragraph(i);break;case`text`:t+=this.renderer.text(i);break;default:{let e=`Token with "`+i.type+`" type was not found.`;if(this.options.silent)return console.error(e),``;throw Error(e)}}}return t}parseInline(e,t=this.renderer){this.renderer.parser=this;let n=``;for(let r=0;r<e.length;r++){let i=e[r];if(this.options.extensions?.renderers?.[i.type]){let e=this.options.extensions.renderers[i.type].call({parser:this},i);if(e!==!1||![`escape`,`html`,`link`,`image`,`strong`,`em`,`codespan`,`br`,`del`,`text`].includes(i.type)){n+=e||``;continue}}let a=i;switch(a.type){case`escape`:n+=t.text(a);break;case`html`:n+=t.html(a);break;case`link`:n+=t.link(a);break;case`image`:n+=t.image(a);break;case`checkbox`:n+=t.checkbox(a);break;case`strong`:n+=t.strong(a);break;case`em`:n+=t.em(a);break;case`codespan`:n+=t.codespan(a);break;case`br`:n+=t.br(a);break;case`del`:n+=t.del(a);break;case`text`:n+=t.text(a);break;default:{let e=`Token with "`+a.type+`" type was not found.`;if(this.options.silent)return console.error(e),``;throw Error(e)}}}return n}},kn=class{options;block;constructor(e){this.options=e||$e}static passThroughHooks=new Set([`preprocess`,`postprocess`,`processAllTokens`,`emStrongMask`]);static passThroughHooksRespectAsync=new Set([`preprocess`,`postprocess`,`processAllTokens`]);preprocess(e){return e}postprocess(e){return e}processAllTokens(e){return e}emStrongMask(e){return e}provideLexer(e=this.block){return e?Tn.lex:Tn.lexInline}provideParser(e=this.block){return e?On.parse:On.parseInline}},An=class{defaults=Qe();options=this.setOptions;parse=this.parseMarkdown(!0);parseInline=this.parseMarkdown(!1);Parser=On;Renderer=En;TextRenderer=Dn;Lexer=Tn;Tokenizer=wn;Hooks=kn;constructor(...e){this.use(...e)}walkTokens(e,t){let n=[];for(let r of e)switch(n=n.concat(t.call(this,r)),r.type){case`table`:{let e=r;for(let r of e.header)n=n.concat(this.walkTokens(r.tokens,t));for(let r of e.rows)for(let e of r)n=n.concat(this.walkTokens(e.tokens,t));break}case`list`:{let e=r;n=n.concat(this.walkTokens(e.items,t));break}default:{let e=r;this.defaults.extensions?.childTokens?.[e.type]?this.defaults.extensions.childTokens[e.type].forEach(r=>{let i=e[r].flat(1/0);n=n.concat(this.walkTokens(i,t))}):e.tokens&&(n=n.concat(this.walkTokens(e.tokens,t)))}}return n}use(...e){let t=this.defaults.extensions||{renderers:{},childTokens:{}};return e.forEach(e=>{let n={...e};if(n.async=this.defaults.async||n.async||!1,e.extensions&&(e.extensions.forEach(e=>{if(!e.name)throw Error(`extension name required`);if(`renderer`in e){let n=t.renderers[e.name];n?t.renderers[e.name]=function(...t){let r=e.renderer.apply(this,t);return r===!1&&(r=n.apply(this,t)),r}:t.renderers[e.name]=e.renderer}if(`tokenizer`in e){if(!e.level||e.level!==`block`&&e.level!==`inline`)throw Error(`extension level must be 'block' or 'inline'`);let n=t[e.level];n?n.unshift(e.tokenizer):t[e.level]=[e.tokenizer],e.start&&(e.level===`block`?t.startBlock?t.startBlock.push(e.start):t.startBlock=[e.start]:e.level===`inline`&&(t.startInline?t.startInline.push(e.start):t.startInline=[e.start]))}`childTokens`in e&&e.childTokens&&(t.childTokens[e.name]=e.childTokens)}),n.extensions=t),e.renderer){let t=this.defaults.renderer||new En(this.defaults);for(let n in e.renderer){if(!(n in t))throw Error(`renderer '${n}' does not exist`);if([`options`,`parser`].includes(n))continue;let r=n,i=e.renderer[r],a=t[r];t[r]=(...e)=>{let n=i.apply(t,e);return n===!1&&(n=a.apply(t,e)),n||``}}n.renderer=t}if(e.tokenizer){let t=this.defaults.tokenizer||new wn(this.defaults);for(let n in e.tokenizer){if(!(n in t))throw Error(`tokenizer '${n}' does not exist`);if([`options`,`rules`,`lexer`].includes(n))continue;let r=n,i=e.tokenizer[r],a=t[r];t[r]=(...e)=>{let n=i.apply(t,e);return n===!1&&(n=a.apply(t,e)),n}}n.tokenizer=t}if(e.hooks){let t=this.defaults.hooks||new kn;for(let n in e.hooks){if(!(n in t))throw Error(`hook '${n}' does not exist`);if([`options`,`block`].includes(n))continue;let r=n,i=e.hooks[r],a=t[r];kn.passThroughHooks.has(n)?t[r]=e=>{if(this.defaults.async&&kn.passThroughHooksRespectAsync.has(n))return(async()=>{let n=await i.call(t,e);return a.call(t,n)})();let r=i.call(t,e);return a.call(t,r)}:t[r]=(...e)=>{if(this.defaults.async)return(async()=>{let n=await i.apply(t,e);return n===!1&&(n=await a.apply(t,e)),n})();let n=i.apply(t,e);return n===!1&&(n=a.apply(t,e)),n}}n.hooks=t}if(e.walkTokens){let t=this.defaults.walkTokens,r=e.walkTokens;n.walkTokens=function(e){let n=[];return n.push(r.call(this,e)),t&&(n=n.concat(t.call(this,e))),n}}this.defaults={...this.defaults,...n}}),this}setOptions(e){return this.defaults={...this.defaults,...e},this}lexer(e,t){return Tn.lex(e,t??this.defaults)}parser(e,t){return On.parse(e,t??this.defaults)}parseMarkdown(e){return(t,n)=>{let r={...n},i={...this.defaults,...r},a=this.onError(!!i.silent,!!i.async);if(this.defaults.async===!0&&r.async===!1)return a(Error(`marked(): The async option was set to true by an extension. Remove async: false from the parse options object to return a Promise.`));if(typeof t>`u`||t===null)return a(Error(`marked(): input parameter is undefined or null`));if(typeof t!=`string`)return a(Error(`marked(): input parameter is of type `+Object.prototype.toString.call(t)+`, string expected`));if(i.hooks&&(i.hooks.options=i,i.hooks.block=e),i.async)return(async()=>{let n=i.hooks?await i.hooks.preprocess(t):t,r=await(i.hooks?await i.hooks.provideLexer(e):e?Tn.lex:Tn.lexInline)(n,i),a=i.hooks?await i.hooks.processAllTokens(r):r;i.walkTokens&&await Promise.all(this.walkTokens(a,i.walkTokens));let o=await(i.hooks?await i.hooks.provideParser(e):e?On.parse:On.parseInline)(a,i);return i.hooks?await i.hooks.postprocess(o):o})().catch(a);try{i.hooks&&(t=i.hooks.preprocess(t));let n=(i.hooks?i.hooks.provideLexer(e):e?Tn.lex:Tn.lexInline)(t,i);i.hooks&&(n=i.hooks.processAllTokens(n)),i.walkTokens&&this.walkTokens(n,i.walkTokens);let r=(i.hooks?i.hooks.provideParser(e):e?On.parse:On.parseInline)(n,i);return i.hooks&&(r=i.hooks.postprocess(r)),r}catch(e){return a(e)}}}onError(e,t){return n=>{if(n.message+=`
Please report this to https://github.com/markedjs/marked.`,e){let e=`<p>An error occurred:</p><pre>`+hn(n.message+``,!0)+`</pre>`;return t?Promise.resolve(e):e}if(t)return Promise.reject(n);throw n}}},jn=new An;function I(e,t){return jn.parse(e,t)}I.options=I.setOptions=function(e){return jn.setOptions(e),I.defaults=jn.defaults,et(I.defaults),I},I.getDefaults=Qe,I.defaults=$e,I.use=function(...e){return jn.use(...e),I.defaults=jn.defaults,et(I.defaults),I},I.walkTokens=function(e,t){return jn.walkTokens(e,t)},I.parseInline=jn.parseInline,I.Parser=On,I.parser=On.parse,I.Renderer=En,I.TextRenderer=Dn,I.Lexer=Tn,I.lexer=Tn.lex,I.Tokenizer=wn,I.Hooks=kn,I.parse=I,I.options,I.setOptions,I.use,I.walkTokens,I.parseInline,On.parse,Tn.lex,Ae.registerLanguage(`bash`,je),Ae.registerLanguage(`css`,Be),Ae.registerLanguage(`dos`,Ve),Ae.registerLanguage(`java`,qe),Ae.registerLanguage(`json`,Je),Ae.registerLanguage(`powershell`,Ye),Ae.registerLanguage(`python`,Xe),Ae.registerLanguage(`shell`,Ze);var Mn=e=>e.toLowerCase().replace(/[`*_~]/g,``).replace(/[^\p{L}\p{N}]+/gu,`-`).replace(/^-+|-+$/g,``),Nn=new An({gfm:!0}),Pn=e=>e.replace(/[&<>"]/g,e=>({"&":`&amp;`,"<":`&lt;`,">":`&gt;`,'"':`&quot;`})[e]),Fn={bash:`Bash`,sh:`Shell`,shell:`Shell`,batch:`Batch`,bat:`Batch`,cmd:`Command Prompt`,powershell:`PowerShell`,ps1:`PowerShell`,python:`Python`,py:`Python`,java:`Java`,json:`JSON`,css:`CSS`,yaml:`YAML`,ini:`INI`,text:`Text`,plaintext:`Text`},In=(e,t)=>Ae.getLanguage(t)?Ae.highlight(e,{language:t,ignoreIllegals:!0}).value:e,Ln={bash:`bash`,sh:`shell`,shell:`shell`,batch:`dos`,bat:`dos`,cmd:`dos`,powershell:`powershell`,ps1:`powershell`,python:`python`,py:`python`,java:`java`,json:`json`},Rn=(e,t)=>{if(!t||!Ae.getLanguage(t))return Pn(e);try{return Ae.highlight(e,{language:t,ignoreIllegals:!0}).value}catch{return Pn(e)}},zn=/^```output[ \t]+@?([^\n]+?)[ \t]*\n([\s\S]*?)\n?^```[ \t]*$/gm,Bn=new Map;Nn.use({renderer:{heading({tokens:e,depth:t,text:n}){let r=this.parser.parseInline(e);return`<h${t} id="${Mn(n)}">${r}</h${t}>\n`},code({text:e,lang:t}){let[n=``,...r]=(t??``).trim().split(/\s+/),i=n.toLowerCase(),a=Fn[i]??i,o=i?` class="language-${Pn(i)} hljs"`:` class="hljs"`,s=r.find(e=>!e.startsWith(`@`))??``,c=r.find(e=>e.startsWith(`@`)),l=c?c.slice(1):s,u=l?Bn.get(l):void 0;return`<pre${[a?` data-lang="${Pn(a)}"`:``,s?` data-file="${Pn(s)}"`:``,u===void 0?``:` data-output="${Pn(u)}"`].join(``)}><code${o}>${Rn(e,Ln[i])}</code></pre>\n`},link({href:e,title:t,tokens:n}){let r=this.parser.parseInline(n),i=t?` title="${Pn(t)}"`:``,a=/^[a-z]+:/i.test(e)?` target="_blank" rel="noreferrer"`:``;return`<a href="${Pn(e)}"${i}${a}>${r}</a>`}}});var Vn=e=>{Bn=new Map;let t=e.replace(zn,(e,t,n)=>(Bn.set(t.trim(),n),``));return Nn.parse(t)};function Hn(e){let t=[];for(let n of Nn.lexer(e))n.type===`heading`&&n.depth===2&&t.push({id:Mn(n.text),text:n.text.replace(/[`*_~]/g,``).trim()});return t}var Un={JVM:`The Java Virtual Machine — the program that actually runs your compiled Java. It reads bytecode, manages memory for you, and exists in a version for every operating system.`,JDK:`Java Development Kit — what you install to write Java. It contains everything needed to run Java, plus the compiler and other tools.`,JRE:`Java Runtime Environment — enough to run a Java program, but not to compile one. It has no compiler.`,bytecode:`What the Java compiler produces instead of machine code: instructions for an imagined computer that the JVM understands. The same bytecode file runs on any machine that has a JVM.`,"machine code":`Instructions a real processor executes directly. Specific to one kind of chip, which is why a program compiled to machine code on one platform will not run on another.`,compiler:"A program that turns source code you wrote into something a machine can run. In Java that is `javac`, and what comes out is bytecode.",interpreter:`A program that reads instructions one at a time and performs them, rather than translating them all up front. Quick to start, slower to run.`,JIT:`Just-In-Time compiler. Part of the JVM that watches which methods run most often and translates those into fast machine code while the program is running.`,"class loader":`The part of the JVM that finds a class and reads it into memory. There is a chain of them, and each asks its parent before looking itself.`,classpath:`The list of folders and jar files the JVM searches when it needs to find a class. First match wins.`,jar:`A zip file containing compiled Java classes, plus a small description of what is inside. The normal way Java code is shipped.`,"constant pool":`A table inside every compiled class file holding its strings, numbers, and the names of everything it refers to. Method calls are stored as names, not addresses, and resolved when first used.`,bootstrap:`The very first step of a process — here, the built-in class loader that loads the JDK itself, before anything else exists to do it.`,deoptimisation:`When the JVM throws away fast machine code it had produced because an assumption behind it turned out to be wrong, and falls back to interpreting until it can try again.`,inlining:`Replacing a call to a small method with the body of that method, so there is no call at all. The single most valuable thing an optimiser does, because it makes other optimisations possible.`,invokedynamic:`A bytecode instruction that says "work out what to call the first time this runs, then remember it". Lambdas and string joining both use it.`,lambda:"A short function written inline, without giving it a name or a class — `x -> x * 2`. Java compiles it into a class at run time rather than at compile time.",reflection:`Code inspecting or calling other code by name at run time, rather than being wired up at compile time. Frameworks use it heavily; it is also why some of them need extra permissions on newer Java.`,module:`A named group of packages that declares what it needs and what it lets others see. Stricter than the classpath, and what makes it possible to ship a trimmed-down Java runtime.`,heap:`The shared pool of memory where every object lives. The garbage collector works here.`,stack:`A per-thread scratch area holding local variables and the trail of method calls currently in progress. Each call pushes a frame; returning pops it.`,frame:`One slot on the stack, created for a single method call, holding its local variables. It disappears when the method returns.`,reference:`A value that says where an object is, rather than holding the object itself. Copying a reference does not copy the object.`,primitive:"One of Java’s built-in simple values — `int`, `double`, `boolean` and friends. Stored directly rather than through a reference.",object:"A piece of data created with `new`, living on the heap, reached through a reference.",immutable:`Cannot be changed after it is created. Any method that looks like it changes the value actually returns a new one.`,"String Pool":`A store the JVM keeps of text written directly in your source, so the same words are stored once rather than many times.`,intern:`To swap a piece of text for the shared copy in the String Pool, so identical text becomes literally the same object.`,autoboxing:"Java quietly converting between a simple value like `int` and its object wrapper `Integer`. Convenient, and the source of several classic bugs.",metaspace:`Memory holding the JVM’s description of your classes. It sits outside the heap, so it can run out while the heap is nearly empty.`,TLAB:`Thread-Local Allocation Buffer — a private slice of memory each thread gets, so creating an object needs no coordination with other threads.`,"escape analysis":`The optimiser working out that an object never leaves the method that made it, which sometimes means it need not be created at all.`,"compressed oops":`Storing references as 4 bytes instead of 8 by treating them as offsets. Works only below a 32 GB heap.`,"native memory":"Memory the operating system gives the process directly, outside the Java heap. Thread stacks and some buffers live here, and `-Xmx` does not limit it.","garbage collection":`The JVM automatically reclaiming memory used by objects your program can no longer reach, so you never free memory by hand.`,"garbage collector":`The part of the JVM that finds objects nothing can reach any more and takes their memory back.`,reachability:`Whether an object can still be found by following references from somewhere the program is definitely using. If it cannot, it is garbage.`,"GC root":`A starting point the collector trusts as definitely in use: local variables of running methods, static fields, live threads.`,generation:`An age group in the heap. New objects go in the young generation; ones that survive long enough are moved to the old generation.`,Eden:`The part of the heap where brand-new objects are placed. Named for the garden — everything starts here.`,"survivor space":`A holding area for objects that made it through one collection but are not old enough to be moved to the old generation.`,promotion:`Moving an object from the young part of the heap to the old part, because it has survived enough collections to look long-lived.`,"stop-the-world":`A moment when the JVM pauses every thread in your program so the collector can work on memory that is not changing underneath it.`,safepoint:`A place in running code where a thread can be safely stopped. Pauses cannot begin until every thread has reached one.`,"write barrier":`A few extra instructions the JVM runs after your program stores a reference, so the collector can keep track of what points where.`,region:`A fixed-size block the heap is divided into by the G1 collector. Each one is used for a purpose, and that purpose can change over its life.`,humongous:`An object too big to fit in the normal way — over half a region — so it gets a run of regions to itself. Too many of them fragment the heap.`,"heap dump":`A snapshot file of everything in the heap at one moment, which you open in a tool to find out what was holding memory.`,"memory leak":`In Java, an object still reachable but no longer needed. The collector correctly refuses to reclaim it, and the heap slowly fills.`,thread:`One sequence of work running on its own, so a program can do several things at once. Each thread has its own stack.`,"platform thread":`An ordinary Java thread, which is a thin wrapper over a thread the operating system provides. Expensive enough that programs pool them.`,"virtual thread":`A thread managed by the JVM rather than the operating system. Cheap enough to create millions, so waiting no longer wastes an expensive resource.`,"carrier thread":`A real operating-system thread that virtual threads take turns running on. There are only a handful, usually one per processor core.`,blocking:`Waiting for something to finish — a network reply, a file, a lock — while doing nothing.`,concurrency:`Several pieces of work in progress at the same time. Not the same as doing them simultaneously; mostly it means interleaving them.`,pool:`A set of expensive things kept and reused instead of created and thrown away — threads, database connections.`,pinning:`When a virtual thread cannot be moved off the real thread it is running on, so waiting occupies that real thread anyway and the benefit is lost.`,continuation:`A saved copy of where a piece of work had got to, so it can be paused and picked up again later — possibly somewhere else.`,"back-pressure":`Slowing down work coming in when the thing it depends on cannot keep up, instead of piling requests up until something breaks.`,Semaphore:`A counter used to limit how many things may do something at once. Take a permit before, give it back after.`,ThreadLocal:`A value attached to one thread, so each thread sees its own. Awkward when there are a million threads instead of a pooled few.`,"race condition":`A bug where the result depends on which thread happened to get there first.`,"structured concurrency":`Treating a group of tasks started together as one unit that succeeds, fails and is cancelled together, matching the shape of the code that started them.`,class:`A definition: what fields something has and what it can do. Nothing exists until you make an object from it.`,constructor:`A special block that runs when an object is created, to set its starting values. Same name as the class, and no return type.`,method:`A named block of code you can call, optionally taking values in and giving one back.`,field:`A variable that belongs to an object or to a class, rather than to one method.`,parameter:`A variable in a method’s declaration, standing for whatever the caller passes in. The value actually passed is the argument.`,variable:`A name for a value you can read, and usually change.`,literal:'A value written directly in the source — `42`, `"hello"`, `true` — rather than computed.',syntax:`The rules for how code must be written down. Break them and the compiler stops you before anything runs.`,"compile-time":`While the compiler is turning your source into bytecode, before the program runs. Errors caught here are the cheap ones.`,runtime:`While the program is actually running.`,instance:`One particular object made from a class.`,static:`Belonging to the class rather than to any object made from it. One copy, shared by everything.`,encapsulation:`Keeping fields private and letting the outside world touch them only through methods, so you can check what is being set and change the internals later.`,inheritance:"A class taking on the fields and methods of another, so shared behaviour is written once. `extends` is the keyword.",superclass:`The class being extended — the parent.`,subclass:`The class doing the extending — the child.`,overriding:`A subclass replacing an inherited method with its own version. Which one runs is decided from the actual object, while the program is running.`,overloading:`Several methods in one class sharing a name but taking different parameters. Which one runs is decided by the compiler.`,interface:`A list of things a type must be able to do, with no instance data of its own. A class can implement many of them.`,abstract:`Declared but not finished. An abstract class cannot be made into an object, and an abstract method has no body — a subclass must supply it.`,enum:`A type whose values are a fixed, named list — the days of the week, the states of an order.`,annotation:`A note attached to code that does nothing by itself. Something else — the compiler, a framework, your own code — has to read it.`,constant:`A value that is fixed once set and never changes afterwards.`,scope:`The region of code where a name means something. Outside it, the name does not exist.`,argument:`The actual value handed to a method when you call it.`,"return type":"The kind of value a method hands back. `void` means it hands back nothing.",exception:`An object describing something that went wrong, thrown at the point of failure and caught somewhere that can deal with it.`,"stack trace":`The list of method calls that were in progress when something failed, innermost first. It tells you how the program got there.`,cast:`Telling the compiler to treat a value as a different type. Some casts are safe; some can fail at run time.`,API:`The set of things a library or class offers you to call. The surface you use without needing to read the inside.`,record:`A short way of declaring a class whose only job is to carry a few values. Java writes the constructor, the accessors and the equality checks for you.`,sealed:`A type that names exactly which other types are allowed to extend or implement it, so the compiler knows the full list.`,exhaustive:"Covering every possible case. An exhaustive `switch` needs no fallback branch, because nothing can be left over.","pattern matching":`Checking what shape a value has and pulling its pieces out in one step, rather than testing the type and then casting.`,erasure:"Generic type information being discarded at compile time, so `List<String>` and `List<Integer>` are the same type at run time.","checked exception":`An error the compiler forces you to either handle or declare. Meant for things a caller could reasonably recover from.`,polymorphism:`Calling the same method on different types and getting each type’s own behaviour, decided by the actual object rather than the declared type.`,"preview feature":`A finished-looking Java feature that is deliberately not final yet. It needs a flag to use, and its shape can still change between releases.`,LTS:`Long-Term Support — a Java release that keeps getting security and bug fixes for years, rather than being superseded in six months.`,volatile:`A marker on a field promising that a value written by one thread becomes visible to others, and stopping the compiler reordering around it.`,"hash code":`A number derived from an object’s contents, used to decide which bucket it goes in inside a map or set. Equal objects must produce equal numbers.`,cgroup:`The Linux mechanism a container uses to cap how much memory and CPU a process may use. Modern JVMs read it instead of the machine’s totals.`},Wn=Object.keys(Un).sort((e,t)=>t.length-e.length);async function Gn(e){try{return await navigator.clipboard.writeText(e),!0}catch{let t=document.createElement(`textarea`);t.value=e,t.setAttribute(`readonly`,``),t.style.position=`fixed`,t.style.opacity=`0`,document.body.appendChild(t),t.select();let n=!1;try{n=document.execCommand(`copy`)}catch{n=!1}return t.remove(),n}}var Kn=e=>e.replace(/[.*+?^${}()|[\]\\]/g,`\\$&`),qn=`pre, code, a, h1, h2, h3, h4, h5, h6, .term`;function Jn(e){for(let t of e.querySelectorAll(`h2`))t.classList.add(`qa-level`),t.dataset.level=t.textContent.trim().toLowerCase();for(let t of[...e.querySelectorAll(`h3`)]){let e=document.createElement(`section`);e.className=`qa`;let n=document.createElement(`div`);n.className=`qa-answer`,t.replaceWith(e),t.classList.add(`qa-question`),e.append(t);let r=e.nextSibling;for(;r&&!(r.nodeType===1&&/^H[1-3]$/.test(r.tagName));){let e=r.nextSibling;n.append(r),r=e}e.append(n)}}function Yn(){let e=document.createElement(`div`);return e.className=`term-tip`,e.setAttribute(`role`,`tooltip`),e.hidden=!0,document.body.appendChild(e),{el:e,show:(t,n)=>{e.textContent=n,e.hidden=!1;let r=t.getBoundingClientRect(),i=e.getBoundingClientRect(),a=r.left+r.width/2-i.width/2;a=Math.max(12,Math.min(a,window.innerWidth-i.width-12));let o=r.top>i.height+12,s=o?r.top-i.height-9:r.bottom+9;e.style.left=`${a}px`,e.style.top=`${s}px`,e.classList.toggle(`is-below`,!o)},hide:()=>{e.hidden=!0}}}function Xn(e){let t=new Map(Wn.map(e=>[e.toLowerCase(),e])),n=[],r=document.createTreeWalker(e,NodeFilter.SHOW_TEXT,{acceptNode(e){if(!e.nodeValue.trim())return NodeFilter.FILTER_REJECT;let t=e.parentElement;return!t||t.closest(qn)?NodeFilter.FILTER_REJECT:NodeFilter.FILTER_ACCEPT}}),i=[];for(;r.nextNode();)i.push(r.currentNode);for(let e of i)for(;e&&t.size;){let r=null;for(let[n,i]of t){let t=RegExp(`\\b${Kn(i)}\\b`,`i`).exec(e.nodeValue);t&&(r===null||t.index<r.index)&&(r={index:t.index,text:t[0],key:n,term:i})}if(!r)break;let i=e.splitText(r.index),a=i.splitText(r.text.length),o=document.createElement(`span`);o.className=`term`,o.tabIndex=0,o.setAttribute(`role`,`button`),o.dataset.term=r.term,o.setAttribute(`aria-label`,`${r.term}: ${Un[r.term]}`),o.textContent=r.text,i.replaceWith(o),t.delete(r.key),n.push(o),e=a}return n}function Zn(e,t){let n=URL.createObjectURL(new Blob([t],{type:`text/plain;charset=utf-8`})),r=document.createElement(`a`);r.href=n,r.download=e,document.body.appendChild(r),r.click(),r.remove(),setTimeout(()=>URL.revokeObjectURL(n),1e3)}function Qn(e,t,n){let r=t.split(`
`);e.textContent=``,e.hidden=!1;let i=0,a=()=>{if(i>=r.length){e.classList.remove(`is-running`);return}e.textContent+=(i?`
`:``)+r[i],i+=1,n.push(setTimeout(a,45))};e.classList.add(`is-running`),a()}function $n({content:e,variant:t=``}){let n=(0,_.useRef)(null),r=(0,_.useMemo)(()=>Vn(e),[e]);return(0,_.useEffect)(()=>{let e=n.current;if(!e)return;let r=[],i=[];t===`interview`&&Jn(e);let a=Xn(e),o=Yn(),s=t=>{let n=t.target.closest?.(`.term`);n&&e.contains(n)&&o.show(n,Un[n.dataset.term])},c=e=>{e.target.closest?.(`.term`)&&o.hide()};e.addEventListener(`mouseover`,s),e.addEventListener(`mouseout`,c),e.addEventListener(`focusin`,s),e.addEventListener(`focusout`,c),window.addEventListener(`scroll`,o.hide,{passive:!0});for(let t of e.querySelectorAll(`pre`)){let e=t.querySelector(`code`),n=()=>e?e.textContent:t.textContent,a=document.createElement(`div`);a.className=`code-actions`;let o=t.dataset.file;if(o){let e=document.createElement(`button`);e.type=`button`,e.className=`code-action code-save`,e.textContent=`Save ${o}`,e.addEventListener(`click`,()=>Zn(o,n())),a.appendChild(e)}let s=t.dataset.output;if(s!==void 0){let e=document.createElement(`pre`);e.className=`code-output`,e.hidden=!0,t.insertAdjacentElement(`afterend`,e),i.push(e);let n=document.createElement(`button`);n.type=`button`,n.className=`code-action code-run`,n.innerHTML=`<span class="play" aria-hidden="true"></span>Run`,n.title=`Replay the output this program produced`,n.addEventListener(`click`,()=>Qn(e,s,r)),a.appendChild(n)}let c=document.createElement(`button`);c.type=`button`,c.className=`code-action code-copy`,c.textContent=`Copy`,c.addEventListener(`click`,async()=>{await Gn(n()),c.textContent=`Copied`,c.classList.add(`is-copied`),r.push(setTimeout(()=>{c.textContent=`Copy`,c.classList.remove(`is-copied`)},2e3))}),a.appendChild(c),t.appendChild(a),i.push(a)}let l=t=>{let n=t.target.closest(`a[href^="#"]`);if(!n||!e.contains(n))return;let r=decodeURIComponent(n.getAttribute(`href`).slice(1)),i=e.querySelector(`[id="${CSS.escape(r)}"]`);i&&(t.preventDefault(),i.scrollIntoView({behavior:`smooth`,block:`start`}))};return e.addEventListener(`click`,l),()=>{e.removeEventListener(`click`,l),e.removeEventListener(`mouseover`,s),e.removeEventListener(`mouseout`,c),e.removeEventListener(`focusin`,s),e.removeEventListener(`focusout`,c),window.removeEventListener(`scroll`,o.hide),o.el.remove(),i.forEach(e=>e.remove()),a.forEach(e=>e.replaceWith(e.textContent)),r.forEach(clearTimeout)}},[r,t]),(0,N.jsx)(`div`,{className:`prose${t?` prose-${t}`:``}`,ref:n,dangerouslySetInnerHTML:{__html:r}})}function er({article:e,onNavigate:t}){let n=(0,_.useMemo)(()=>Hn(e.content),[e]),r=de.findIndex(t=>t.slug===e.slug),i=de[r-1]??null,a=de[r+1]??null;(0,_.useEffect)(()=>{window.scrollTo({top:0})},[e.slug]);let o=(e,t)=>{e.preventDefault(),document.getElementById(t)?.scrollIntoView({behavior:`smooth`,block:`start`})};return(0,N.jsxs)(`article`,{className:`article`,style:{"--accent":e.accent},children:[(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm back`,onClick:()=>t(`articles`),children:[(0,N.jsx)(P,{name:`arrow`,size:15,className:`flip`}),(0,N.jsx)(`span`,{children:`All articles`})]}),(0,N.jsxs)(`header`,{className:`article-head`,children:[(0,N.jsxs)(`div`,{className:`article-meta`,children:[(0,N.jsx)(`span`,{className:`badge badge-article`,children:e.category}),(0,N.jsxs)(`span`,{className:`dim`,children:[e.minutes,` min read`]}),(0,N.jsxs)(`span`,{className:`dim`,children:[`Updated `,xe(e.updated)]})]}),(0,N.jsx)(`h1`,{children:e.title}),(0,N.jsx)(`p`,{className:`lede`,children:e.summary}),(0,N.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,N.jsx)(`li`,{children:e},e))})]}),(0,N.jsxs)(`div`,{className:`article-body ${n.length>2?`has-toc`:``}`,children:[n.length>2&&(0,N.jsxs)(`aside`,{className:`article-toc`,"aria-label":`On this page`,children:[(0,N.jsx)(`h2`,{children:`On this page`}),(0,N.jsx)(`ol`,{children:n.map(e=>(0,N.jsx)(`li`,{children:(0,N.jsx)(`a`,{href:`#${e.id}`,onClick:t=>o(t,e.id),children:e.text})},e.id))})]}),(0,N.jsx)($n,{content:e.content})]}),(0,N.jsxs)(`footer`,{className:`article-foot`,children:[(0,N.jsxs)(`p`,{className:`hint`,children:[`Written for my own machine and kept here because it worked. Commands that touch the registry, the hosts file or a bootable drive deserve a read before a run.`,` `,(0,N.jsx)(`a`,{href:e.sourceUrl,target:`_blank`,rel:`noreferrer`,children:`The original lives on GitHub`}),` `,`if you would rather clone it.`]}),(0,N.jsxs)(`nav`,{className:`article-nav`,children:[i?(0,N.jsxs)(`button`,{type:`button`,className:`article-nav-link`,onClick:()=>t(i.route),children:[(0,N.jsx)(`span`,{className:`dim`,children:`Previous`}),(0,N.jsx)(`strong`,{children:i.title})]}):(0,N.jsx)(`span`,{}),a&&(0,N.jsxs)(`button`,{type:`button`,className:`article-nav-link is-next`,onClick:()=>t(a.route),children:[(0,N.jsx)(`span`,{className:`dim`,children:`Next`}),(0,N.jsx)(`strong`,{children:a.title})]})]})]})]})}function tr({onSupport:e,articles:t=!1}){return(0,N.jsxs)(`footer`,{className:`site-footer`,children:[(0,N.jsxs)(Ee,{className:`footer-cta`,variant:`zoom`,children:[(0,N.jsx)(`h2`,{children:t?`Something wrong in a guide?`:`Something missing?`}),(0,N.jsx)(`p`,{children:t?`The guides are public. Open an issue with what did not work on your machine, or send a pull request with the fix — steps that only work on one setup are worth knowing about.`:`The repositories are private. Ask for access and you can open an issue, send a pull request, or fork any of them and make it yours.`}),(0,N.jsxs)(`div`,{className:`footer-buttons`,children:[(0,N.jsxs)(`a`,{className:`btn btn-primary`,href:t?`https://github.com/${te}/${ue}/issues`:`https://github.com/${te}`,target:`_blank`,rel:`noreferrer`,children:[(0,N.jsx)(P,{name:`github`,size:16}),(0,N.jsx)(`span`,{children:t?`Open an issue`:`Request access`}),(0,N.jsx)(P,{name:`arrow`,size:15})]}),(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-coffee`,onClick:e,children:[(0,N.jsx)(P,{name:`coffee`,size:16}),(0,N.jsx)(`span`,{children:`Buy me a coffee`})]})]})]}),(0,N.jsx)(`div`,{className:`footer-bar`,children:(0,N.jsxs)(`span`,{children:[`© `,new Date().getFullYear(),` `,ne,` · MIT licensed`]})})]})}var nr=m(),rr=[{slug:`hello-world`,title:`Hello World`,subject:`Java`,stage:`Fundamentals`,chapter:1,accent:`#34d399`,summary:`This is the very first program that every Java developer writes. The "Hello World" program is a tradition in programming that dates back to the 1970s. The purpose of this program is to verify that your development environment is set up correctly and to introduce you to the basic structure of a Java program.`,parts:{notes:`## Introduction

This is the very first program that every Java developer writes. The "Hello World" program is a tradition in programming that dates back to the 1970s. The purpose of this program is to verify that your development environment is set up correctly and to introduce you to the basic structure of a Java program.

In this example, the program simply prints a greeting message to the console.

---

## Prerequisites

Before running this program, make sure you have the following installed on your system:

- **JDK (Java Development Kit)**: Version 8 or above. You can download it from the official Oracle website or use OpenJDK.
- **A Text Editor or IDE**: Notepad, VS Code, IntelliJ IDEA, or Eclipse.
- **Command Line Access**: Terminal (macOS/Linux) or Command Prompt/PowerShell (Windows).

To verify your installation, open a terminal and run:

\`\`\`bash
java -version
javac -version
\`\`\`

Both commands should display a version number without any errors.

---

## What is Java

Java is a high-level, object-oriented, platform-independent programming language developed by Sun Microsystems (now owned by Oracle) in 1995. Java follows the principle of "Write Once, Run Anywhere" (WORA), meaning code compiled on one platform can run on any other platform that has a Java Virtual Machine (JVM).

Key characteristics of Java:

- **Object-Oriented**: Everything in Java revolves around classes and objects.
- **Platform-Independent**: Java code is compiled into bytecode, which runs on the JVM regardless of the operating system.
- **Strongly Typed**: Every variable must have a declared data type.
- **Compiled and Interpreted**: Java source code is first compiled into bytecode (.class file), and then interpreted by the JVM at runtime.

---

## Understanding the Code

\`\`\`java
class Hello {

    public static void main(String args[]) {
        System.out.println("Hi this is Aditya");
    }
}
\`\`\`

### Structure Overview

Every Java program must have at least one class, and the execution starts from the \`main\` method. Think of a class as a container that holds your code, and the \`main\` method as the entry door through which the program begins execution.

---

## Line-by-Line Explanation

### Line 1: \`class Hello {\`

- **\`class\`** is a keyword in Java used to declare a class.
- **\`Hello\`** is the name of the class. In Java, class names should start with an uppercase letter by convention.
- **\`{\`** marks the beginning of the class body.

### Line 2: \`public static void main(String args[]) {\`

This is the most important line for any beginner. Let us break down every word:

| Keyword    | Meaning                                                                                          |
|------------|--------------------------------------------------------------------------------------------------|
| \`public\`   | An access modifier. It means this method can be accessed from anywhere, including the JVM.       |
| \`static\`   | This means the method belongs to the class itself, not to any specific object of the class.      |
| \`void\`     | The return type. \`void\` means this method does not return any value.                             |
| \`main\`     | The name of the method. The JVM looks specifically for a method named \`main\` to start execution. |
| \`String args[]\` | An array of Strings that can accept command-line arguments when the program is run.         |

If even one of these keywords is missing or misspelled, the program will not run.

### Line 3: \`System.out.println("Hi this is Aditya");\`

- **\`System\`** is a built-in class in the \`java.lang\` package.
- **\`out\`** is a static member of the \`System\` class. It is an object of type \`PrintStream\`.
- **\`println\`** is a method of \`PrintStream\` that prints the given text followed by a new line.
- **\`"Hi this is Aditya"\`** is a String literal, which is the text that will be displayed on the console.
- The semicolon **\`;\`** at the end marks the end of the statement. Every statement in Java must end with a semicolon.

### Line 4 and 5: Closing braces \`}\`

- The first \`}\` closes the \`main\` method.
- The second \`}\` closes the \`Hello\` class.

---

## How to Compile and Run

### Step 1: Save the file

Save the file as \`Hello.java\`. The file name must match the class inside it whenever that class is \`public\` — and matching it always is the habit worth having.

### Step 2: Compile

From the parent directory, compile using the package path:

\`\`\`bash
javac Hello.java
\`\`\`

This command invokes the Java Compiler (\`javac\`), which converts the \`.java\` source file into a \`.class\` bytecode file.

### Step 3: Run

Run using the fully qualified name (\`package.ClassName\`):

\`\`\`bash
java Hello
\`\`\`

This command invokes the Java Virtual Machine (JVM), which reads the \`Hello.class\` file and executes the \`main\` method.

### Expected Output

\`\`\`
Hi this is Aditya
\`\`\`

---

## What Happens Behind the Scenes

1. You write the source code in a \`.java\` file (human-readable).
2. The Java Compiler (\`javac\`) converts it into bytecode stored in a \`.class\` file.
3. The JVM loads the \`.class\` file and interprets the bytecode.
4. The JVM finds the \`main\` method and begins executing the instructions inside it.
5. \`System.out.println()\` sends the text to the standard output stream, which is your console/terminal.

\`\`\`
Source Code (.java)  -->  Compiler (javac)  -->  Bytecode (.class)  -->  JVM  -->  Output
\`\`\`

---

## Common Errors Beginners Face

### Error 1: Class name does not match file name (when class is public)

If you declare the class as \`public class Hello\`, the file name MUST be \`Hello.java\`. Otherwise, you will get a compilation error.

### Error 2: Missing semicolon

\`\`\`
error: ';' expected
\`\`\`

Every statement in Java must end with a semicolon.

### Error 3: \`main\` method signature is wrong

If you write \`public static void main(String args)\` (without the square brackets), the JVM will not recognize it as the entry point.

### Error 4: Case sensitivity

Java is case-sensitive. \`System\` is not the same as \`system\`. \`String\` is not the same as \`string\`.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Hello.java\`

The whole program is one class holding one method. \`main\` is where the JVM
starts â€” its exact signature, \`public static void main(String[] args)\`, is what
makes it findable, and a program with no method matching it will compile but
refuse to launch.

Everything else in the file exists because Java requires code to live inside a
class.

\`\`\`java Hello.java @run-hello-world-hello
class Hello {

    public static void main(String args[]) {
        System.out.println("Hi this is Aditya");
    }
}

\`\`\`

\`\`\`output @run-hello-world-hello
Hi this is Aditya
\`\`\`
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- `javac Hello.java` compiles — **file** name, with the extension. `java Hello`\n  runs — **class** name, no extension.\n- The file name must match the `public class` inside it, capitals included.\n- `main` is `public static void main(String[] args)`: static so it can be called\n  before any object exists, `String[]` for command-line arguments.\n- `System.out.println` adds a newline; `print` does not.\n",interview:"## Interview questions\n\n### Q1: Why is the `main` method declared as `public static void`?\n\n**Answer:** The `main` method is `public` so that the JVM can access it from outside the class. It is `static` because the JVM calls it without creating an object of the class. It is `void` because it does not return any value to the JVM.\n\n### Q2: Can we run a Java program without the `main` method?\n\n**Answer:** Starting from Java 7, the answer is no. In Java 6 and earlier, you could use a static block to execute code without a `main` method, but this is no longer supported in modern versions of Java.\n\n### Q3: What is the difference between `System.out.println()` and `System.out.print()`?\n\n**Answer:** `println()` prints the text and then moves the cursor to the next line. `print()` prints the text but keeps the cursor on the same line.\n\n### Q4: What is bytecode?\n\n**Answer:** Bytecode is the intermediate code generated by the Java compiler. It is not machine code specific to any operating system. Instead, it is designed to be executed by the JVM, which makes Java platform-independent.\n\n### Q5: Can we have multiple `main` methods in a Java program?\n\n**Answer:** Yes, you can have multiple `main` methods in different classes within the same program. However, the JVM will only execute the `main` method of the class you specify when running the program.\n\n### Q6: What happens if we write `static public void main` instead of `public static void main`?\n\n**Answer:** The program will still compile and run correctly. The order of `public` and `static` does not matter in Java. However, the conventional and recommended order is `public static void main`.\n"}},{slug:`jvm-and-memory`,title:`JVM, JDK, JRE and memory`,subject:`Java`,stage:`Fundamentals`,chapter:2,accent:`#f97316`,summary:`In Chapter 1 you compiled and ran a program with javac and java. But what are those tools, and where do your variables and objects actually live while the program runs? This chapter explains the three things people often confuse — JDK, JRE, and JVM — and the memory model (Stack, Heap, and the String Pool) that the rest of this course relies on.`,parts:{notes:`## Introduction

In Chapter 1 you compiled and ran a program with \`javac\` and \`java\`. But what *are* those tools, and where do your variables and objects actually live while the program runs? This chapter explains the three things people often confuse — **JDK, JRE, and JVM** — and the **memory model** (Stack, Heap, and the String Pool) that the rest of this course relies on.

This is a concepts chapter, with one small runnable demo (\`MemoryDemo.java\`) that lets you *see* the memory behaviour.

---

## How Java Runs: Compile Once, Run Anywhere

Java's famous promise is **"Write Once, Run Anywhere" (WORA)**. It works in two steps:

\`\`\`
   YourProgram.java   --(javac, the compiler)-->   YourProgram.class (bytecode)
   YourProgram.class  --(java, the JVM)-->         runs on any operating system
\`\`\`

1. The **compiler** (\`javac\`) turns your human-readable source code into **bytecode** (\`.class\` files). Bytecode is not machine code; it is an intermediate language the JVM understands.
2. The **JVM** reads that bytecode and executes it on the current machine.

Because every platform (Windows, macOS, Linux) has its own JVM, the *same* \`.class\` file runs everywhere without recompiling. The bytecode is portable; the JVM is platform-specific.

---

## JDK, JRE, and JVM

These three are nested, like boxes inside boxes:

\`\`\`
+-------------------------------------------------------+
|  JDK  (Java Development Kit)                           |
|  Tools to DEVELOP: javac, jar, javadoc, debugger ...  |
|                                                       |
|   +-----------------------------------------------+   |
|   |  JRE  (Java Runtime Environment)              |   |
|   |  Everything needed to RUN Java apps           |   |
|   |  Core libraries + supporting files            |   |
|   |                                               |   |
|   |    +-------------------------------------+    |   |
|   |    |  JVM  (Java Virtual Machine)        |    |   |
|   |    |  Executes bytecode, manages memory  |    |   |
|   |    +-------------------------------------+    |   |
|   +-----------------------------------------------+   |
+-------------------------------------------------------+
\`\`\`

| Term | Stands for | Contains | You need it to… |
|------|------------|----------|-----------------|
| **JDK** | Java Development Kit | JRE **+** developer tools (\`javac\`, \`jar\`, \`javadoc\`, debugger) | **write and compile** Java programs |
| **JRE** | Java Runtime Environment | JVM **+** core class libraries | **run** Java programs (no compiling) |
| **JVM** | Java Virtual Machine | The engine that executes bytecode | **execute** the \`.class\` bytecode |

**One-line summary:** JDK is for developers (it includes the JRE); the JRE is for running apps (it includes the JVM); the JVM is the engine that actually runs the bytecode.

> As a learner you install the **JDK**, because you need to both compile (\`javac\`) and run (\`java\`).

---

## What the JVM Does

The JVM is more than a simple interpreter. Its main jobs are:

- **Class Loading** – finds and loads \`.class\` files into memory.
- **Bytecode Verification** – checks the bytecode is safe and valid.
- **Execution** – interprets bytecode and, for "hot" code, uses the **JIT (Just-In-Time) compiler** to turn frequently used bytecode into native machine code for speed.
- **Memory Management** – allocates objects on the heap and reclaims unused ones via the **Garbage Collector (GC)**, so you rarely free memory manually.

---

## Java Memory Areas

When the JVM runs your program, it divides memory into areas. The two you must understand first are the **Stack** and the **Heap**.

\`\`\`
        STACK (per thread)                 HEAP (shared)
   +------------------------+        +-------------------------+
   | main() frame           |        |   int[]{99,2,3}  <------|---+
   |   a = 10               |        |                         |   |
   |   b = 20               |        |   String "Java" (pool)  |   |
   |   x  --------------------------> (reference points here)  |   |
   |   y  ------------------------------------------------------+  |
   +------------------------+        +-------------------------+
   stores values & references         stores the actual objects
\`\`\`

---

## Stack Memory

- Stores **local variables**, **method parameters**, and **references** to objects.
- Organised as **frames** — one frame per method call, removed when the method returns (LIFO: Last In, First Out).
- **Primitives are stored by value** here. Copying a primitive copies the value, so the copies are independent.
- Fast, automatically managed, and limited in size (a runaway recursion causes a \`StackOverflowError\`).

\`\`\`java
int a = 10;
int b = a;
b = 20;
\`\`\`

---

## Heap Memory

- Stores **all objects** created with \`new\` (and arrays, and Strings).
- **Shared** across the whole application; multiple references can point to the same object.
- Reclaimed automatically by the **Garbage Collector** when no references remain.
- Copying a **reference** does not copy the object — both references point to the *same* object.

\`\`\`java
int[] x = {1, 2, 3};
int[] y = x;
y[0] = 99;
\`\`\`

---

## The String Pool

The **String Pool** (also called the String Constant Pool) is a special area the JVM uses to **reuse** String literals and save memory.

- When you write a literal like \`"Java"\`, the JVM checks the pool. If \`"Java"\` already exists there, it **reuses** the same object instead of creating a new one.
- Therefore two identical literals refer to the **same** object, and \`==\` is \`true\`.
- Using \`new String("Java")\` **forces** a brand-new object on the heap (outside the pool), so \`==\` is \`false\` even though the text is equal.
- \`intern()\` returns the pooled instance for any String.

\`\`\`java
String s1 = "Java";
String s2 = "Java";
String s3 = new String("Java");

s1 == s2
s1 == s3
s1.equals(s3)
s1 == s3.intern()
\`\`\`

> This is exactly why you compare String **text** with \`.equals()\`, not \`==\`.

---

## Understanding the Code

\`\`\`java
int a = 10;
int b = a; b = 20;

int[] x = {1, 2, 3};
int[] y = x; y[0] = 99;

String s1 = "Java";
String s2 = "Java";
String s3 = new String("Java");
\`\`\`

Running the demo prints \`a = 10, b = 20\` (the copy is independent), \`x[0] = 99\` (both references share one array), and the String comparisons that prove pooling.

---

## How to Compile and Run

\`\`\`bash
javac MemoryDemo.java
java MemoryDemo
\`\`\`

### Expected Output

\`\`\`
a = 10, b = 20
x[0] = 99
s1 == s2      : true
s1 == s3      : false
s1.equals(s3) : true
s1 == s3.intern(): true
\`\`\`

---

## Common Mistakes

### Mistake 1: Thinking the JVM compiles \`.java\` files

The **compiler** \`javac\` (part of the JDK) compiles \`.java\` to \`.class\`. The **JVM** only *runs* the resulting bytecode.

### Mistake 2: Installing only the JRE, then trying to compile

The JRE can run programs but has no \`javac\`. To develop, install the **JDK**.

### Mistake 3: Comparing Strings with \`==\`

\`==\` compares references. Two Strings with the same text can be different objects (especially with \`new String(...)\`). Use \`.equals()\` for text.

### Mistake 4: Assuming assigning an object copies it

\`\`\`java
int[] y = x;
\`\`\`

Both variables now point to the same object; changes are visible through both.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`MemoryDemo.java\`

Four printed lines, each one showing a different consequence of where a value
actually lives.

- **\`b = a\` copies the value.** \`a\` and \`b\` are primitives on the stack, so
  changing \`b\` leaves \`a\` at \`10\`.
- **\`y = x\` copies the reference.** Both variables now point at the *same* array
  on the heap, so writing through \`y\` changes what \`x\` sees.
- **\`s1\` and \`s2\` are the same object.** Identical string literals are pooled, so
  \`==\` is true. \`new String("Java")\` deliberately skips the pool, so \`s1 == s3\`
  is false while \`s1.equals(s3)\` is true.
- **\`intern()\` puts it back.** It returns the pooled instance, so \`s1 ==
  s3.intern()\` is true again.

\`\`\`java MemoryDemo.java @run-jvm-and-memory-memory-demo
public class MemoryDemo {

    public static void main(String[] args) {

        int a = 10;
        int b = a;
        b = 20;
        System.out.println("a = " + a + ", b = " + b);

        int[] x = { 1, 2, 3 };
        int[] y = x;
        y[0] = 99;
        System.out.println("x[0] = " + x[0]);

        String s1 = "Java";
        String s2 = "Java";
        String s3 = new String("Java");

        System.out.println("s1 == s2      : " + (s1 == s2));
        System.out.println("s1 == s3      : " + (s1 == s3));
        System.out.println("s1.equals(s3) : " + s1.equals(s3));
        System.out.println("s1 == s3.intern(): " + (s1 == s3.intern()));
    }
}

\`\`\`

\`\`\`output @run-jvm-and-memory-memory-demo
a = 10, b = 20
x[0] = 99
s1 == s2      : true
s1 == s3      : false
s1.equals(s3) : true
s1 == s3.intern(): true
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- **JDK ⊃ JRE ⊃ JVM.** JDK to write, JRE to run, JVM to execute. Install the JDK.
- \`javac\` produces **bytecode**, not machine code. Bytecode is portable; the JVM
  is platform-specific. That is "write once, run anywhere".
- **Stack** — locals, parameters, references; one frame per call; per-thread.
- **Heap** — every object made with \`new\`; shared; cleaned by the collector.
- **String Pool** — literals stored once and reused, which is why \`"a" == "a"\`.
- The JVM also does class loading, bytecode verification, JIT compilation and
  garbage collection.
`,interview:`## Interview questions

### Q1: What is the difference between JDK, JRE, and JVM?

**Answer:** The JVM is the engine that executes Java bytecode and manages memory. The JRE is the JVM plus the core libraries needed to *run* Java applications. The JDK is the JRE plus development tools such as \`javac\`, so it is used to *write and compile* programs. They are nested: JDK ⊃ JRE ⊃ JVM.

### Q2: What is bytecode, and why does it make Java portable?

**Answer:** Bytecode is the platform-independent intermediate code produced by \`javac\` (the \`.class\` files). Because every platform has its own JVM that understands the same bytecode, one compiled file runs on any OS — "Write Once, Run Anywhere."

### Q3: What is the difference between stack and heap memory?

**Answer:** The stack stores local variables, parameters, and references in per-method frames that are removed when the method returns; primitives are stored by value. The heap stores all objects created with \`new\` and is shared across the program; objects are reclaimed by the garbage collector when unreferenced.

### Q4: What is the String Pool?

**Answer:** A special memory area where the JVM stores String literals and reuses them. Identical literals point to the same pooled object, which saves memory and is why \`"Java" == "Java"\` is \`true\`.

### Q5: Why is \`new String("Java") == "Java"\` false?

**Answer:** \`new String(...)\` always creates a separate object on the heap, distinct from the pooled literal \`"Java"\`. The references differ, so \`==\` is \`false\`, even though \`.equals()\` is \`true\` because the characters match.

### Q6: What is the Garbage Collector?

**Answer:** A part of the JVM that automatically frees heap memory occupied by objects that are no longer reachable by any reference, so Java programmers usually do not free memory manually.

### Q7: What does the JIT compiler do?

**Answer:** The Just-In-Time compiler is part of the JVM. It compiles frequently executed bytecode ("hot" code) into native machine code at runtime to make the program run faster than pure interpretation.
`}},{slug:`type-casting`,title:`Type casting`,subject:`Java`,stage:`Fundamentals`,chapter:3,accent:`#a78bfa`,summary:`Type casting is one of the fundamental concepts in Java that every developer must understand. When you work with different data types (such as int, float, double, long), there are situations where you need to convert a value from one type to another. This process is called type casting.`,parts:{notes:'## Introduction\n\nType casting is one of the fundamental concepts in Java that every developer must understand. When you work with different data types (such as `int`, `float`, `double`, `long`), there are situations where you need to convert a value from one type to another. This process is called type casting.\n\nThis program demonstrates explicit type casting, where a `float` value is manually converted to an `int` before performing a mathematical operation.\n\n---\n\n## What is Type Casting\n\nType casting is the process of converting a value from one data type to another. In Java, every variable has a specific data type, and the compiler enforces strict type rules. If you try to assign a value of one type to a variable of a different type, the compiler will either do it automatically (if it is safe) or ask you to do it manually (if there is a risk of data loss).\n\nThink of it like pouring water from a large bucket into a small glass. If the glass is big enough, the water fits perfectly (automatic/widening casting). If the glass is too small, some water will spill (manual/narrowing casting with data loss).\n\n---\n\n## Types of Type Casting\n\n### 1. Widening Casting (Implicit / Automatic)\n\nThis happens automatically when you convert a smaller data type to a larger data type. There is no risk of data loss.\n\n```\nbyte --> short --> int --> long --> float --> double\n```\n\n**Example:**\n\n```java\nint a = 100;\ndouble b = a;\n```\n\nThe compiler does this on its own because a `double` can hold any `int` value without losing any information.\n\n### 2. Narrowing Casting (Explicit / Manual)\n\nThis must be done manually by the programmer when you convert a larger data type to a smaller data type. There IS a risk of data loss.\n\n```\ndouble --> float --> long --> int --> short --> byte\n```\n\n**Example:**\n\n```java\ndouble a = 9.78;\nint b = (int) a;\n```\n\nYou must explicitly write `(int)` to tell the compiler: "I know this might lose data, but I want to do it anyway."\n\n---\n\n## Understanding the Code\n\n```java\npublic class TypeCasting {\n\n    public static void main(String[] args) {\n        int a = 5;\n        float b = 6.1f;\n\n        int c = a * (int) b;\n\n        System.out.println(c);\n    }\n}\n```\n\n---\n\n## Line-by-Line Explanation\n\n### `int a = 5;`\n\n- Declares an integer variable `a` and assigns it the value `5`.\n- `int` is a 32-bit data type that can store whole numbers from -2,147,483,648 to 2,147,483,647.\n\n### `float b = 6.1f;`\n\n- Declares a floating-point variable `b` and assigns it the value `6.1`.\n- The `f` suffix is mandatory for `float` literals in Java. Without it, the compiler treats `6.1` as a `double` (which is 64-bit), and assigning a `double` to a `float` (which is 32-bit) would cause a compilation error.\n- `float` is a 32-bit data type that stores decimal numbers with approximately 6-7 significant digits of precision.\n\n### `int c = a * (int) b;`\n\nThis is the most important line in this program. Let us break it down step by step:\n\n1. **`(int) b`** -- This is explicit type casting. The `float` value `6.1` is being converted to an `int`. When this happens, the decimal part (`.1`) is completely discarded (truncated, NOT rounded). So `(int) 6.1` becomes `6`.\n\n2. **`a * (int) b`** -- This becomes `5 * 6`, which equals `30`.\n\n3. **`int c = 30`** -- The result `30` is stored in the integer variable `c`.\n\n### `System.out.println(c);`\n\n- Prints the value of `c` to the console, which is `30`.\n\n---\n\n## What Happens When You Convert Between Types\n\n### int to float (Widening - Safe)\n\n```java\nint x = 100;\nfloat y = x;\n```\n\n- No data loss occurs for most values.\n- However, for very large `int` values (above 16,777,216), there can be a loss of precision because `float` has only 23 bits for the significand.\n\n### float to int (Narrowing - Data Loss)\n\n```java\nfloat x = 9.99f;\nint y = (int) x;\n```\n\n- The decimal part is completely removed (truncated).\n- Important: This is truncation, NOT rounding. `9.99` becomes `9`, not `10`.\n\n### int to double (Widening - Safe)\n\n```java\nint x = 100;\ndouble y = x;\n```\n\n- `double` is 64-bit and can hold any `int` value without any loss.\n\n### double to int (Narrowing - Data Loss)\n\n```java\ndouble x = 99.99;\nint y = (int) x;\n```\n\n- Same as float to int: the decimal part is truncated.\n\n### int to long (Widening - Safe)\n\n```java\nint x = 100;\nlong y = x;\n```\n\n- `long` is 64-bit and can hold any `int` value.\n\n### long to int (Narrowing - Possible Data Loss)\n\n```java\nlong x = 3000000000L;\nint y = (int) x;\n```\n\n- If the `long` value exceeds the `int` range, the result will overflow and produce an incorrect value.\n\n---\n\n## Data Loss in Type Casting\n\nHere is a detailed table showing what can go wrong with narrowing conversions:\n\n| Conversion      | What Happens                                      | Example                    | Result   |\n|-----------------|--------------------------------------------------|----------------------------|----------|\n| float to int    | Decimal part is truncated                        | `(int) 6.9f`              | `6`      |\n| double to int   | Decimal part is truncated                        | `(int) 99.99`             | `99`     |\n| double to float | Precision may be reduced                         | `(float) 3.141592653589`  | `3.1415927` |\n| long to int     | Higher bits are discarded, may cause overflow    | `(int) 3000000000L`       | `-1294967296` |\n| int to byte     | Only the lowest 8 bits are kept                  | `(byte) 130`              | `-126`   |\n| int to short    | Only the lowest 16 bits are kept                 | `(short) 40000`           | `-25536` |\n\n---\n\n## Type Promotion in Expressions\n\nWhen different data types are used together in an expression, Java automatically promotes the smaller type to the larger type before performing the operation. This is called type promotion.\n\n**Rules of Type Promotion:**\n\n1. `byte`, `short`, and `char` are promoted to `int` in any expression.\n2. If any operand is `long`, the entire expression is promoted to `long`.\n3. If any operand is `float`, the entire expression is promoted to `float`.\n4. If any operand is `double`, the entire expression is promoted to `double`.\n\n**Example:**\n\n```java\nbyte a = 10;\nbyte b = 20;\n// byte c = a + b;  // ERROR: a + b is promoted to int\nint c = a + b;      // Correct\n```\n\n---\n\n## How to Compile and Run\n\n```bash\njavac TypeCasting.java\njava TypeCasting\n```\n\n### Expected Output\n\n```\n30\n```\n\n---\n\n## Common Mistakes\n\n### Mistake 1: Forgetting the `f` suffix for float literals\n\n```java\nfloat b = 6.1;  // ERROR: 6.1 is treated as double\nfloat b = 6.1f; // CORRECT\n```\n\n### Mistake 2: Assuming narrowing happens automatically\n\n```java\nfloat x = 6.1f;\nint y = x;        // ERROR: cannot convert float to int automatically\nint y = (int) x;  // CORRECT: explicit cast required\n```\n\n### Mistake 3: Expecting rounding instead of truncation\n\n```java\nint y = (int) 6.9f;\n```\n\nIf you want rounding, use `Math.round()`:\n\n```java\nint y = Math.round(6.9f);\n```\n\n---\n\n## The source files\n\nThe complete programs, renamed from the numbered file names the repository\nuses so they compile on their own. **Save** writes the file out of this page;\n**Run** replays the output it produced when this page was built.\n\n```bash\njavac Something.java\njava Something\n```\n\n### `TypeCasting.java`\n\nOne explicit cast, and what it costs. `(int) 6.1f` is `6` â€” the cast to a\nnarrower type drops the fractional part rather than rounding it â€” so the\nmultiplication that follows is `5 * 6` and prints `30`.\n\nWithout the cast the expression would not compile at all: Java will not quietly\nturn a `float` into an `int`, because doing so loses data.\n\n```java TypeCasting.java @run-type-casting-type-casting\npublic class TypeCasting {\n\n    public static void main(String[] args) {\n        int a = 5;\n        float b = 6.1f;\n\n        int c = a * (int) b;\n\n        System.out.println(c);\n    }\n}\n\n```\n\n```output @run-type-casting-type-casting\n30\n```\n',revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- **Widening** is automatic and safe: `byte → short → int → long → float → double`.\n- **Narrowing** needs an explicit cast and can lose data: `(int) 9.99` is `9`.\n- Casting a `double` to `int` **truncates**; it does not round.\n- In an expression, anything smaller than `int` is promoted to `int` first —\n  which is why `byte + byte` will not assign back to a `byte` without a cast.\n",interview:"## Interview questions\n\n### Q1: What is type casting in Java?\n\n**Answer:** Type casting is the process of converting a value from one data type to another. There are two types: widening (automatic, from smaller to larger type) and narrowing (manual, from larger to smaller type).\n\n### Q2: What is the difference between implicit and explicit type casting?\n\n**Answer:** Implicit casting (widening) is done automatically by the compiler when there is no risk of data loss, such as converting `int` to `double`. Explicit casting (narrowing) must be done manually by the programmer using the cast operator, such as converting `double` to `int`, because there is a risk of losing data.\n\n### Q3: What happens when you cast a `float` to an `int`?\n\n**Answer:** The decimal (fractional) part is completely discarded through truncation. For example, `(int) 6.9f` results in `6`, not `7`. The value is NOT rounded.\n\n### Q4: Why do we need to add `f` at the end of a float literal?\n\n**Answer:** By default, Java treats all decimal literals as `double`. Since `double` is 64-bit and `float` is 32-bit, assigning a `double` to a `float` would be a narrowing conversion. The `f` suffix explicitly tells the compiler that the literal is a `float`, not a `double`.\n\n### Q5: What is type promotion in Java?\n\n**Answer:** Type promotion is the automatic conversion of smaller data types to larger data types within an expression. For example, when you add a `byte` and a `short`, both are automatically promoted to `int` before the addition is performed. This prevents overflow during intermediate calculations.\n\n### Q6: Can you cast a `boolean` to an `int` in Java?\n\n**Answer:** No. In Java, `boolean` is not considered a numeric type. You cannot cast between `boolean` and any other data type. This is different from languages like C and C++, where `true` and `false` can be treated as `1` and `0`.\n\n### Q7: What is the output of `(int) -9.7`?\n\n**Answer:** The output is `-9`. The casting truncates toward zero, discarding the decimal part. It does not round to `-10`.\n\n### Q8: What happens if you cast a large `long` value to `int`?\n\n**Answer:** If the `long` value exceeds the range of `int` (-2,147,483,648 to 2,147,483,647), the higher-order bits are discarded, and the result wraps around due to overflow, producing an unexpected and incorrect value.\n\n### Q9: What is the difference between `float` and `double`?\n\n**Answer:**\n\n| Feature       | float          | double          |\n|---------------|----------------|-----------------|\n| Size          | 32 bits        | 64 bits         |\n| Precision     | ~6-7 digits    | ~15-16 digits   |\n| Default       | No (needs `f`) | Yes             |\n| Memory Usage  | Less           | More            |\n| Use Case      | Memory-critical applications | General-purpose decimal calculations |\n\n### Q10: Is type casting applicable to objects in Java?\n\n**Answer:** Yes. In Java, you can also cast objects. Upcasting (child to parent) is implicit and always safe. Downcasting (parent to child) is explicit and can throw a `ClassCastException` at runtime if the object is not actually an instance of the target class.\n"}},{slug:`data-types`,title:`Data types`,subject:`Java`,stage:`Fundamentals`,chapter:4,accent:`#f472b6`,summary:`Every variable in Java has a data type. The data type tells the compiler what kind of value the variable can hold, how much memory to reserve for it, and what operations are allowed on it. Choosing the right data type is the very first decision you make when storing any value.`,parts:{notes:`## Introduction

Every variable in Java has a **data type**. The data type tells the compiler what kind of value the variable can hold, how much memory to reserve for it, and what operations are allowed on it. Choosing the right data type is the very first decision you make when storing any value.

This chapter is split into five small programs:

| File | Topic |
|------|-------|
| \`DatatypesPrimitive.java\`     | The eight primitive types |
| \`DatatypesNonPrimitive.java\` | Reference types (String, arrays) |
| \`DatatypesChar.java\`          | The \`char\` type |
| \`DatatypesStrings.java\`       | Strings and String methods |
| \`DatatypesLiterals.java\`      | Literals |

---

## What is a Data Type

A data type is a classification that specifies:

- **What kind of value** a variable can store (a whole number, a decimal, a character, text, true/false).
- **How much memory** the variable occupies.
- **What operations** can be performed on it.

Java is a **statically typed** language. This means every variable must have a declared type, and that type is checked at compile time. You cannot put a decimal into an \`int\` variable without conversion.

\`\`\`java
int age = 25;
int age = "twenty";  // ERROR: a String is not an int
\`\`\`

---

## Two Categories of Data Types

\`\`\`
Data Types in Java
│
├── Primitive (8 built-in types)
│   ├── Integer types : byte, short, int, long
│   ├── Decimal types : float, double
│   ├── Character type : char
│   └── Boolean type  : boolean
│
└── Non-Primitive / Reference
    ├── String
    ├── Arrays
    ├── Classes
    └── Interfaces
\`\`\`

| Aspect          | Primitive                          | Non-Primitive (Reference)                |
|-----------------|------------------------------------|------------------------------------------|
| Stores          | The actual value                   | A reference (address) to an object       |
| Memory          | Usually on the stack               | Object lives on the heap                 |
| Default value   | \`0\`, \`0.0\`, \`false\`, \`'\\u0000'\`    | \`null\`                                   |
| Can be \`null\`?  | No                                 | Yes                                      |
| Has methods?    | No                                 | Yes                                      |
| Examples        | \`int\`, \`double\`, \`char\`, \`boolean\` | \`String\`, \`int[]\`, any class             |

---

## Primitive Data Types

Java has exactly **eight** primitive types.

| Type      | Size    | Range / Values                                  | Example          |
|-----------|---------|-------------------------------------------------|------------------|
| \`byte\`    | 1 byte  | -128 to 127                                     | \`byte b = 100;\`  |
| \`short\`   | 2 bytes | -32,768 to 32,767                               | \`short s = 2000;\`|
| \`int\`     | 4 bytes | about -2.1 billion to 2.1 billion               | \`int i = 100000;\`|
| \`long\`    | 8 bytes | very large whole numbers (needs \`L\` suffix)     | \`long l = 15000000000L;\` |
| \`float\`   | 4 bytes | single-precision decimal (needs \`f\` suffix)     | \`float f = 5.75f;\` |
| \`double\`  | 8 bytes | double-precision decimal (default for decimals) | \`double d = 19.99;\` |
| \`char\`    | 2 bytes | a single Unicode character                      | \`char c = 'A';\`  |
| \`boolean\` | 1 bit*  | \`true\` or \`false\`                               | \`boolean ok = true;\` |

\\*The JVM does not precisely define the size of a \`boolean\`; conceptually it holds one bit of information.

**Key points:**

- \`int\` is the default type for whole numbers.
- \`double\` is the default type for decimal numbers.
- A \`long\` literal needs an \`L\` suffix, and a \`float\` literal needs an \`f\` suffix, otherwise the compiler treats the number as \`int\` or \`double\`.

---

## Non-Primitive (Reference) Data Types

Reference types do not store the value directly. They store a **reference** (a memory address) that points to an actual object on the heap.

\`\`\`java
String name = "Java";
int[] numbers = {10, 20};
String empty = null;
\`\`\`

Because they are objects, reference types:

- Have **methods** you can call (e.g. \`name.toUpperCase()\`).
- Can be \`null\` (pointing to no object).
- Are compared with \`.equals()\` for content, not \`==\`.

---

## The \`char\` Type in Detail

A \`char\` holds a **single** character written in single quotes: \`'A'\`, \`'9'\`, \`'$'\`.

Internally a \`char\` is just a number — its Unicode (and for English letters, ASCII) code point. This is why a \`char\` can take part in arithmetic.

\`\`\`java
char letter = 'A';
int code = letter;
char next = (char)(letter + 1);
\`\`\`

| Character | Code |
|-----------|------|
| \`'A'\`     | 65   |
| \`'B'\`     | 66   |
| \`'a'\`     | 97   |
| \`'0'\`     | 48   |

A cast \`(char)\` is needed when converting back from \`int\`, because \`char + int\` produces an \`int\`.

---

## Strings in Detail

A \`String\` is a sequence of characters written in **double quotes**: \`"Hello"\`. It is a class, not a primitive, and it is **immutable** — once created its contents can never change. Methods that look like they modify a String actually return a brand-new String.

Common String methods:

| Method                | Purpose                                  | Example result for \`"Hello World"\` |
|-----------------------|------------------------------------------|------------------------------------|
| \`length()\`            | number of characters                     | \`11\`                               |
| \`toUpperCase()\`       | upper-case copy                          | \`"HELLO WORLD"\`                    |
| \`toLowerCase()\`       | lower-case copy                          | \`"hello world"\`                    |
| \`charAt(i)\`           | character at index \`i\`                   | \`charAt(0)\` -> \`'H'\`               |
| \`indexOf("x")\`        | index of a substring (or -1)             | \`indexOf("World")\` -> \`6\`          |
| \`substring(a, b)\`     | part of the string                       | \`substring(0,5)\` -> \`"Hello"\`      |
| \`replace('l','L')\`    | replace characters                       | \`"HeLLo WorLd"\`                    |
| \`contains("x")\`       | does it contain the text?                | \`true\`                             |
| \`equals(other)\`       | compare contents                         | \`true\` / \`false\`                   |

> **Always use \`equals()\` to compare text, not \`==\`.** The \`==\` operator compares references (addresses), not the actual characters.

---

## Literals in Java

A **literal** is a fixed value written directly in source code.

| Literal kind | Examples                              |
|--------------|---------------------------------------|
| Integer      | \`100\`, \`0b1100100\` (binary), \`0144\` (octal), \`0x64\` (hex) |
| Long         | \`10000000000L\`                        |
| Floating     | \`3.14\`, \`2.5f\`, \`1.5e3\`               |
| Character    | \`'A'\`, \`'\\n'\`, \`'A'\`             |
| String       | \`"Hello"\`                             |
| Boolean      | \`true\`, \`false\`                       |

Helpful details:

- **Underscores** improve readability and are ignored by the compiler: \`1_000_000\` is the same as \`1000000\`.
- **Prefixes** choose the number system: \`0b\` (binary), \`0\` (octal), \`0x\` (hexadecimal).
- **Suffixes** choose the type: \`L\` (long), \`f\` (float).
- **Escape sequences** inside characters/strings: \`\\n\` (newline), \`\\t\` (tab), \`\\\\\` (backslash), \`\\"\` (double quote).

---

## How to Compile and Run

Each file has its own \`main\` method, so run them one at a time.

\`\`\`bash
javac DatatypesPrimitive.java
java DatatypesPrimitive

javac DatatypesNonPrimitive.java
java DatatypesNonPrimitive

javac DatatypesChar.java
java DatatypesChar

javac DatatypesStrings.java
java DatatypesStrings

javac DatatypesLiterals.java
java DatatypesLiterals
\`\`\`

---

## Common Mistakes

### Mistake 1: Forgetting the \`L\` or \`f\` suffix

\`\`\`java
long big = 10000000000;   // ERROR: integer number too large
float f = 5.75;           // ERROR: 5.75 is a double, not a float
\`\`\`

Use \`10000000000L\` and \`5.75f\`.

### Mistake 2: Using \`==\` to compare Strings

\`\`\`java
String a = "Java";
String b = new String("Java");
System.out.println(a == b);
System.out.println(a.equals(b));
\`\`\`

### Mistake 3: Assigning a larger type to a smaller one

\`\`\`java
int i = 100;
byte b = i;   // ERROR: possible loss of precision
byte b2 = (byte) i;  // OK with explicit cast
\`\`\`

### Mistake 4: Using double quotes for a \`char\`

\`\`\`java
char c = "A";   // ERROR: "A" is a String
char c = 'A';   // correct
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`DatatypesPrimitive.java\`

All eight primitives in one place. A primitive stores its value **directly** â€”
on the stack, for a local variable â€” and is not an object. The size of the type
is what fixes the range of values it can hold.

Two literals need a suffix, and the file would not compile without them: \`L\`
marks a \`long\` (otherwise the number is read as an \`int\`), and \`f\` marks a
\`float\` (otherwise a decimal literal is a \`double\`).

\`\`\`java DatatypesPrimitive.java @run-data-types-datatypes-primitive
public class DatatypesPrimitive {

    public static void main(String[] args) {

        byte myByte = 100;

        short myShort = 20000;

        int myInt = 100000;

        long myLong = 15000000000L;

        float myFloat = 5.75f;

        double myDouble = 19.99;

        char myChar = 'A';

        boolean myBoolean = true;

        System.out.println("byte    : " + myByte);
        System.out.println("short   : " + myShort);
        System.out.println("int     : " + myInt);
        System.out.println("long    : " + myLong);
        System.out.println("float   : " + myFloat);
        System.out.println("double  : " + myDouble);
        System.out.println("char    : " + myChar);
        System.out.println("boolean : " + myBoolean);
    }
}

\`\`\`

\`\`\`output @run-data-types-datatypes-primitive
byte    : 100
short   : 20000
int     : 100000
long    : 15000000000
float   : 5.75
double  : 19.99
char    : A
boolean : true
\`\`\`

### \`DatatypesNonPrimitive.java\`

A non-primitive â€” a *reference* type â€” does not hold the value at all. It holds
the address of an object on the heap. \`String\`, arrays, classes and interfaces
are all reference types.

That difference shows up twice in the output. A reference can be **\`null\`**,
meaning it points at no object, which a primitive can never be. And because the
thing it points at is an object, it has **methods** you can call â€” which is why
\`toUpperCase()\` works on a \`String\` and nothing of the sort exists for an \`int\`.

\`\`\`java DatatypesNonPrimitive.java @run-data-types-datatypes-non-primitive
public class DatatypesNonPrimitive {

    public static void main(String[] args) {

        String name = "Java";

        int[] numbers = { 10, 20, 30 };

        String empty = null;

        System.out.println("String        : " + name);
        System.out.println("Array length  : " + numbers.length);
        System.out.println("Array[0]      : " + numbers[0]);
        System.out.println("Null reference: " + empty);

        System.out.println("Uppercase     : " + name.toUpperCase());
        System.out.println("Char count    : " + name.length());
    }
}

\`\`\`

\`\`\`output @run-data-types-datatypes-non-primitive
String        : Java
Array length  : 3
Array[0]      : 10
Null reference: null
Uppercase     : JAVA
Char count    : 4
\`\`\`

### \`DatatypesChar.java\`

A \`char\` holds one 16-bit Unicode character, written in single quotes. But
underneath it is a **number** â€” the character's code point â€” and that is what
makes the rest of the file possible.

\`'A'\` is \`65\`, so assigning it to an \`int\` prints \`65\`. Going the other way,
\`char fromCode = 66\` gives \`'B'\`. Arithmetic moves along the character set:
\`letter + 1\` is \`66\`, which is \`'B'\` â€” and the \`(char)\` cast is required,
because \`char + int\` produces an \`int\`.

\`\`\`java DatatypesChar.java @run-data-types-datatypes-char
public class DatatypesChar {

    public static void main(String[] args) {

        char letter = 'A';

        int code = letter;

        char fromCode = 66;

        char unicodeChar = 'A';

        System.out.println("letter        : " + letter);
        System.out.println("code of 'A'   : " + code);
        System.out.println("char of 66    : " + fromCode);
        System.out.println("unicode esc   : " + unicodeChar);

        char next = (char) (letter + 1);
        System.out.println("letter + 1    : " + next);
    }
}

\`\`\`

\`\`\`output @run-data-types-datatypes-char
letter        : A
code of 'A'   : 65
char of 66    : B
unicode esc   : A
letter + 1    : B
\`\`\`

### \`DatatypesStrings.java\`

A \`String\` is a class, not a primitive, and it is **immutable**: once created
its contents can never change. Every "modifying" method here â€” \`toUpperCase\`,
\`replace\`, \`substring\` â€” leaves the original alone and hands back a brand new
\`String\`.

The comparison at the end is the one to remember. \`equals()\` compares the
**text**; \`==\` compares **references**, i.e. whether the two variables point at
the same object. For strings you almost always want \`equals()\`.

\`\`\`java DatatypesStrings.java @run-data-types-datatypes-strings
public class DatatypesStrings {

    public static void main(String[] args) {

        String first = "Hello";
        String second = "World";

        String message = first + " " + second;

        System.out.println("message        : " + message);
        System.out.println("length         : " + message.length());
        System.out.println("upper case     : " + message.toUpperCase());
        System.out.println("lower case     : " + message.toLowerCase());
        System.out.println("char at 0      : " + message.charAt(0));
        System.out.println("index of World : " + message.indexOf("World"));
        System.out.println("substring(0,5) : " + message.substring(0, 5));
        System.out.println("replace l->L   : " + message.replace('l', 'L'));
        System.out.println("contains 'World': " + message.contains("World"));

        String a = "Java";
        String b = "Java";
        System.out.println("a.equals(b)    : " + a.equals(b));
    }
}

\`\`\`

\`\`\`output @run-data-types-datatypes-strings
message        : Hello World
length         : 11
upper case     : HELLO WORLD
lower case     : hello world
char at 0      : H
index of World : 6
substring(0,5) : Hello
replace l->L   : HeLLo WorLd
contains 'World': true
a.equals(b)    : true
\`\`\`

### \`DatatypesLiterals.java\`

A literal is a fixed value written straight into the source. The first four are
worth reading together: \`100\`, \`0b1100100\`, \`0144\` and \`0x64\` are the **same
number**, written in base 10, base 2 (prefix \`0b\`), base 8 (a leading \`0\`) and
base 16 (prefix \`0x\`).

The rest are the notations that trip people up. Underscores group digits purely
for readability (\`1_000_000\`). \`1.5e3\` is scientific notation for \`1500.0\`. The
\`f\` and \`L\` suffixes pin a literal to \`float\` and \`long\`. And \`'\\n'\` is an
escape sequence â€” one character, written as two.

\`\`\`java DatatypesLiterals.java @run-data-types-datatypes-literals
public class DatatypesLiterals {

    public static void main(String[] args) {

        int decimal = 100;
        int binary = 0b1100100;
        int octal = 0144;
        int hex = 0x64;

        int million = 1_000_000;

        double d = 3.14;
        double scientific = 1.5e3;
        float f = 2.5f;
        long big = 10000000000L;

        char ch = 'Z';
        char newline = '\\n';
        String text = "Literals in Java";

        boolean flag = true;

        System.out.println("decimal    : " + decimal);
        System.out.println("binary     : " + binary);
        System.out.println("octal      : " + octal);
        System.out.println("hex        : " + hex);
        System.out.println("million    : " + million);
        System.out.println("double     : " + d);
        System.out.println("scientific : " + scientific);
        System.out.println("float      : " + f);
        System.out.println("long       : " + big);
        System.out.println("char       : " + ch);
        System.out.println("string     : " + text);
        System.out.println("boolean    : " + flag);
        System.out.print("escape used: line1" + newline + "line2");
    }
}

\`\`\`

\`\`\`output @run-data-types-datatypes-literals
decimal    : 100
binary     : 100
octal      : 100
hex        : 100
million    : 1000000
double     : 3.14
scientific : 1500.0
float      : 2.5
long       : 10000000000
char       : Z
string     : Literals in Java
boolean    : true
escape used: line1
line2
\`\`\`
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- Eight primitives: `byte` (1), `short` (2), `int` (4), `long` (8), `float` (4),\n  `double` (8), `char` (2), `boolean`.\n- Defaults are `0`, `0.0`, `'\\u0000'` for `char`, `false`. **Locals have no default** — the\n  compiler refuses to read an uninitialised local.\n- `long` literals need `L`, `float` literals need `f`.\n- Primitives cannot be `null`; their wrapper classes can.\n",interview:"## Interview questions\n\n### Q1: How many primitive data types are there in Java, and what are they?\n\n**Answer:** Eight: `byte`, `short`, `int`, `long`, `float`, `double`, `char`, and `boolean`.\n\n### Q2: What is the difference between a primitive and a reference type?\n\n**Answer:** A primitive stores the actual value directly and is never `null`. A reference type stores the address of an object on the heap, can be `null`, and has methods. For example, `int` is primitive; `String` and arrays are reference types.\n\n### Q3: Why does a `long` literal need an `L` suffix?\n\n**Answer:** Without a suffix, a whole-number literal is treated as an `int`. If the value is larger than the `int` range, the compiler reports an error. The `L` suffix tells the compiler to treat the literal as a `long`.\n\n### Q4: Why is `char` considered a numeric type?\n\n**Answer:** A `char` stores the Unicode code point of a character, which is an integer (for example `'A'` is 65). Because of this, a `char` can be used in arithmetic and can be converted to and from `int`.\n\n### Q5: What is the default type of `100` and `3.14` in Java?\n\n**Answer:** `100` is an `int` and `3.14` is a `double`. To make them `long` or `float` you must add the `L` or `f` suffix.\n\n### Q6: Why is `String` immutable?\n\n**Answer:** Once a `String` object is created its characters cannot be changed. Methods like `toUpperCase()` return a new `String` rather than modifying the original. Immutability makes Strings safe to share and allows the JVM to cache them in the String pool.\n\n### Q7: What is the difference between `==` and `.equals()` for Strings?\n\n**Answer:** `==` compares references (whether two variables point to the same object). `.equals()` compares the actual character contents. To compare text, always use `.equals()`.\n\n### Q8: What are the default values of primitive types?\n\n**Answer:**\n\n| Type      | Default     |\n|-----------|-------------|\n| `byte`/`short`/`int`/`long` | `0` |\n| `float`/`double`            | `0.0` |\n| `char`                      | `'\\u0000'` |\n| `boolean`                   | `false` |\n\n(Reference types default to `null`.)\n"}},{slug:`operators`,title:`Operators`,subject:`Java`,stage:`Fundamentals`,chapter:5,accent:`#facc15`,summary:`An operator is a symbol that performs an operation on one or more values. For example, in a + b, the + is an operator and a and b are operands. Operators are how you actually do something with the data you stored in Chapter 4.`,parts:{notes:'## Introduction\n\nAn **operator** is a symbol that performs an operation on one or more values. For example, in `a + b`, the `+` is an operator and `a` and `b` are operands. Operators are how you actually *do* something with the data you stored in Chapter 4.\n\nThis chapter is split into three small programs:\n\n| File | Topic |\n|------|-------|\n| `ArithmeticOperators.java`  | `+ - * / %` and `++ --` |\n| `AssignmentOperators.java`  | `=` and `+= -= *= /= %=` |\n| `LogicalOperators.java`     | `&& || !` and relational operators |\n\n---\n\n## What is an Operator\n\nAn operator performs an action on **operands** (the values it works on).\n\n```java\nint sum = a + b;\n```\n\nOperators are grouped by how many operands they take:\n\n- **Unary** – one operand (e.g. `-x`, `!flag`, `count++`)\n- **Binary** – two operands (e.g. `a + b`, `a > b`)\n- **Ternary** – three operands (the `? :` operator, covered in Chapter 6)\n\n---\n\n## Categories of Operators\n\n| Category    | Operators                          | Purpose                              |\n|-------------|------------------------------------|--------------------------------------|\n| Arithmetic  | `+  -  *  /  %`                    | Math calculations                    |\n| Assignment  | `=  +=  -=  *=  /=  %=`            | Store / update values                |\n| Relational  | `==  !=  >  <  >=  <=`             | Compare values, produce a boolean    |\n| Logical     | `&&  ||  !`                        | Combine boolean conditions           |\n| Unary       | `++  --  -  !`                    | Act on a single operand              |\n\n---\n\n## Arithmetic Operators\n\n| Operator | Name           | Example  | Result (for `a=10`, `b=3`) |\n|----------|----------------|----------|----------------------------|\n| `+`      | Addition       | `a + b`  | `13`                       |\n| `-`      | Subtraction    | `a - b`  | `7`                        |\n| `*`      | Multiplication | `a * b`  | `30`                       |\n| `/`      | Division       | `a / b`  | `3` (integer division)     |\n| `%`      | Modulus        | `a % b`  | `1` (remainder)            |\n\n> **Integer division drops the decimal part.** `10 / 3` is `3`, not `3.33`. If you need the decimals, at least one operand must be a floating-point type: `10.0 / 3` gives `3.333...`.\n\nThe **modulus** operator `%` returns the remainder of a division. It is commonly used to test whether a number is even (`n % 2 == 0`) or to wrap values around a range.\n\n---\n\n## Assignment Operators\n\nThe `=` operator stores a value in a variable. **Compound** assignment operators combine arithmetic with assignment.\n\n| Operator | Example   | Equivalent to | \n|----------|-----------|---------------|\n| `=`      | `x = 5`   | store 5 in x  |\n| `+=`     | `x += 5`  | `x = x + 5`   |\n| `-=`     | `x -= 5`  | `x = x - 5`   |\n| `*=`     | `x *= 5`  | `x = x * 5`   |\n| `/=`     | `x /= 5`  | `x = x / 5`   |\n| `%=`     | `x %= 5`  | `x = x % 5`   |\n\n`+=` also works on Strings: `text += " more"` appends to `text`.\n\n---\n\n## Relational Operators\n\nRelational (comparison) operators compare two values and always produce a `boolean` (`true` or `false`).\n\n| Operator | Meaning                  | Example   | Result |\n|----------|--------------------------|-----------|--------|\n| `==`     | equal to                 | `5 == 5`  | `true` |\n| `!=`     | not equal to             | `5 != 3`  | `true` |\n| `>`      | greater than             | `5 > 3`   | `true` |\n| `<`      | less than                | `5 < 3`   | `false`|\n| `>=`     | greater than or equal to | `5 >= 5`  | `true` |\n| `<=`     | less than or equal to    | `5 <= 4`  | `false`|\n\n> For objects (like `String`), use `.equals()` to compare contents. `==` on objects compares references, not values.\n\n---\n\n## Logical Operators\n\nLogical operators combine boolean values.\n\n| Operator | Name | Result is `true` when…             |\n|----------|------|------------------------------------|\n| `&&`     | AND  | **both** sides are true            |\n| `||`     | OR   | **at least one** side is true      |\n| `!`      | NOT  | the single operand is false        |\n\n**Truth table:**\n\n| A     | B     | `A && B` | `A || B` |\n|-------|-------|----------|----------|\n| true  | true  | true     | true     |\n| true  | false | false    | true     |\n| false | true  | false    | true     |\n| false | false | false    | false    |\n\n**Short-circuit evaluation:** `&&` stops as soon as the left side is `false` (the result cannot be `true`), and `||` stops as soon as the left side is `true`. This is useful for safety:\n\n```java\nif (x != 0 && 10 / x > 1) { ... }\n```\n\n---\n\n## Increment and Decrement\n\n| Operator | Name      | Effect              |\n|----------|-----------|---------------------|\n| `++`     | increment | adds 1              |\n| `--`     | decrement | subtracts 1         |\n\nPosition matters:\n\n| Form  | Name           | Behaviour                                  |\n|-------|----------------|--------------------------------------------|\n| `x++` | post-increment | use the current value, **then** add 1      |\n| `++x` | pre-increment  | add 1 **first**, then use the new value    |\n\n```java\nint p = 5;\nSystem.out.println(p++);\nSystem.out.println(++p);\n```\n\n---\n\n## Operator Precedence\n\nWhen several operators appear in one expression, Java evaluates them in a fixed order (highest first):\n\n1. `()` parentheses\n2. `++ -- !` (unary)\n3. `* / %`\n4. `+ -`\n5. `< <= > >=`\n6. `== !=`\n7. `&&`\n8. `||`\n9. `= += -= ...` (assignment)\n\n```java\nint result = 2 + 3 * 4;\nint result2 = (2 + 3) * 4;\n```\n\n> When in doubt, **use parentheses** to make the order explicit and the code readable.\n\n---\n\n## How to Compile and Run\n\n```bash\njavac ArithmeticOperators.java\njava ArithmeticOperators\n\njavac AssignmentOperators.java\njava AssignmentOperators\n\njavac LogicalOperators.java\njava LogicalOperators\n```\n\n---\n\n## Common Mistakes\n\n### Mistake 1: Expecting decimals from integer division\n\n```java\ndouble half = 1 / 2;\ndouble half2 = 1.0 / 2;\n```\n\n### Mistake 2: Using `=` instead of `==`\n\n```java\nif (x = 5) { ... }   // ERROR: = assigns, it does not compare\nif (x == 5) { ... }  // correct\n```\n\n### Mistake 3: Comparing Strings with `==`\n\n```java\nif (name == "Java") { ... }\nif (name.equals("Java")) { ... }\n```\n\n### Mistake 4: Confusing `p++` and `++p`\n\n```java\nint p = 5;\nint a = p++;\nint b = ++p;\n```\n\n---\n\n## The source files\n\nThe complete programs, renamed from the numbered file names the repository\nuses so they compile on their own. **Save** writes the file out of this page;\n**Run** replays the output it produced when this page was built.\n\n```bash\njavac Something.java\njava Something\n```\n\n### `ArithmeticOperators.java`\n\nThe five arithmetic operators, plus the two that catch people out.\n\n`10 / 3` is **`3`**, not `3.33`. With two `int` operands Java does integer\ndivision and throws the fraction away; to keep it, at least one side has to be a\nfloating-point type. `%` gives the remainder that division discarded.\n\nThe increment pair matters where it sits: **pre-increment** (`++x`) changes the\nvalue and then hands it over, while **post-increment** (`x++`) hands over the\nold value and changes it afterwards. Same variable, different result in the same\nexpression.\n\n```java ArithmeticOperators.java @run-operators-arithmetic-operators\npublic class ArithmeticOperators {\n\n    public static void main(String[] args) {\n\n        int a = 10;\n        int b = 3;\n\n        System.out.println("a + b = " + (a + b));\n        System.out.println("a - b = " + (a - b));\n        System.out.println("a * b = " + (a * b));\n        System.out.println("a / b = " + (a / b));\n        System.out.println("a % b = " + (a % b));\n\n        double x = 10;\n        double y = 3;\n        System.out.println("x / y = " + (x / y));\n\n        int count = 5;\n        count++;\n        count--;\n        System.out.println("count = " + count);\n\n        int p = 5;\n        System.out.println("p++ gives   : " + (p++));\n        System.out.println("now p is    : " + p);\n        System.out.println("++p gives   : " + (++p));\n    }\n}\n\n```\n\n```output @run-operators-arithmetic-operators\na + b = 13\na - b = 7\na * b = 30\na / b = 3\na % b = 1\nx / y = 3.3333333333333335\ncount = 5\np++ gives   : 5\nnow p is    : 6\n++p gives   : 7\n```\n\n### `AssignmentOperators.java`\n\n`=` stores a value. The compound forms â€” `+=`, `-=`, `*=`, `/=`, `%=` â€” are\nshorthand that reads the variable, applies the operation, and stores the result\nback. `num += 5` is exactly `num = num + 5`.\n\n`+=` works on `String` too, where it appends rather than adds.\n\n```java AssignmentOperators.java @run-operators-assignment-operators\npublic class AssignmentOperators {\n\n    public static void main(String[] args) {\n\n        int num = 10;\n        System.out.println("start       : " + num);\n\n        num += 5;\n        System.out.println("num += 5    : " + num);\n\n        num -= 3;\n        System.out.println("num -= 3    : " + num);\n\n        num *= 2;\n        System.out.println("num *= 2    : " + num);\n\n        num /= 4;\n        System.out.println("num /= 4    : " + num);\n\n        num %= 4;\n        System.out.println("num %= 4    : " + num);\n\n        String text = "Java";\n        text += " Programming";\n        System.out.println("text        : " + text);\n    }\n}\n\n```\n\n```output @run-operators-assignment-operators\nstart       : 10\nnum += 5    : 15\nnum -= 3    : 12\nnum *= 2    : 24\nnum /= 4    : 6\nnum %= 4    : 2\ntext        : Java Programming\n```\n\n### `LogicalOperators.java`\n\nTwo families that work together. **Relational** operators (`>`, `<`, `>=`, `<=`,\n`==`, `!=`) compare values and produce the booleans that **logical** operators\nthen combine.\n\n`&&` needs both sides true, `||` needs at least one, and `!` flips whichever it\nis given. The last section shows the detail worth knowing: `&&` and `||`\n**short-circuit**. If the left side of `&&` is already false the right side is\nnever evaluated at all â€” which is what makes `if (obj != null && obj.size() > 0)`\nsafe to write.\n\n```java LogicalOperators.java @run-operators-logical-operators\npublic class LogicalOperators {\n\n    public static void main(String[] args) {\n\n        int age = 25;\n        boolean hasLicense = true;\n\n        System.out.println("age > 18      : " + (age > 18));\n        System.out.println("age == 25     : " + (age == 25));\n        System.out.println("age != 30     : " + (age != 30));\n\n        boolean canDrive = (age >= 18) && hasLicense;\n        System.out.println("canDrive (AND): " + canDrive);\n\n        boolean weekend = false;\n        boolean holiday = true;\n        boolean dayOff = weekend || holiday;\n        System.out.println("dayOff (OR)   : " + dayOff);\n\n        System.out.println("!hasLicense   : " + (!hasLicense));\n\n        int x = 0;\n        boolean safe = (x != 0) && (10 / x > 1);\n        System.out.println("safe (short)  : " + safe);\n    }\n}\n\n```\n\n```output @run-operators-logical-operators\nage > 18      : true\nage == 25     : true\nage != 30     : true\ncanDrive (AND): true\ndayOff (OR)   : true\n!hasLicense   : false\nsafe (short)  : false\n```\n',revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- Integer division truncates: `7 / 2` is `3`. `%` keeps the sign of the left\n  operand.\n- `&&` and `||` **short-circuit** — the right side may never run. `&` and `|` do\n  not.\n- `i++` returns the old value, `++i` the new one.\n- `=` assigns, `==` compares. Precedence: unary → arithmetic → relational →\n  logical → assignment.\n",interview:"## Interview questions\n\n### Q1: What is the difference between `/` and `%`?\n\n**Answer:** `/` returns the quotient of a division and `%` returns the remainder. For example, `17 / 5` is `3` and `17 % 5` is `2`.\n\n### Q2: Why does `5 / 2` give `2` instead of `2.5`?\n\n**Answer:** Both operands are `int`, so Java performs integer division and discards the decimal part. To get `2.5`, make at least one operand a floating-point value: `5.0 / 2`.\n\n### Q3: What is the difference between `x++` and `++x`?\n\n**Answer:** `x++` (post-increment) returns the current value of `x` and then increases it. `++x` (pre-increment) increases `x` first and then returns the new value.\n\n### Q4: What is short-circuit evaluation?\n\n**Answer:** With `&&`, if the left operand is `false`, the right operand is never evaluated because the result is already `false`. With `||`, if the left operand is `true`, the right operand is skipped. This avoids unnecessary work and can prevent errors such as division by zero.\n\n### Q5: What is the difference between `==` and `.equals()`?\n\n**Answer:** For primitives, `==` compares values. For objects, `==` compares references (whether they are the same object), while `.equals()` compares the actual contents. Use `.equals()` to compare String text.\n\n### Q6: What does the `%` (modulus) operator do, and give a use case.\n\n**Answer:** It returns the remainder of a division. A common use is checking whether a number is even or odd: `n % 2 == 0` is true for even numbers.\n\n### Q7: What is operator precedence?\n\n**Answer:** It is the order in which operators are evaluated in an expression. For example, `*` and `/` are evaluated before `+` and `-`. Parentheses have the highest precedence and can be used to override the default order.\n"}},{slug:`conditionals`,title:`Conditional statements`,subject:`Java`,stage:`Fundamentals`,chapter:6,accent:`#22d3ee`,summary:`So far our programs have run every line from top to bottom. Conditional statements let a program make decisions — run some code only when a condition is true, and other code otherwise. This is what makes a program behave differently for different inputs.`,parts:{notes:`## Introduction

So far our programs have run every line from top to bottom. **Conditional statements** let a program make decisions — run some code only when a condition is true, and other code otherwise. This is what makes a program behave differently for different inputs.

This chapter is split into three small programs:

| File | Topic |
|------|-------|
| \`IfElse.java\`  | \`if\`, \`if-else\`, \`if-else-if\`, nested \`if\` |
| \`Ternary.java\`  | The \`? :\` ternary operator |
| \`Switch.java\`   | The \`switch\` statement |

---

## What is a Conditional Statement

A conditional statement evaluates a **boolean condition** (something that is \`true\` or \`false\`, built using the relational and logical operators from Chapter 5) and chooses which block of code to run.

\`\`\`
            condition?
             /     \\
          true     false
           |         |
        run block  skip / run else block
\`\`\`

---

## The \`if\` Statement

Runs a block **only** when the condition is true.

\`\`\`java
if (marks >= 33) {
    System.out.println("Pass");
}
\`\`\`

If the condition is \`false\`, the block is skipped entirely.

---

## The \`if-else\` Statement

Provides an alternative block for when the condition is false. Exactly one of the two blocks always runs.

\`\`\`java
if (number % 2 == 0) {
    System.out.println("even");
} else {
    System.out.println("odd");
}
\`\`\`

---

## The \`if-else-if\` Ladder

Tests several conditions **in order**. The first one that is true runs, and all the rest are skipped. The final \`else\` is optional and acts as a catch-all.

\`\`\`java
if (marks >= 90) {
    grade = 'A';
} else if (marks >= 75) {
    grade = 'B';
} else if (marks >= 60) {
    grade = 'C';
} else {
    grade = 'F';
}
\`\`\`

> Order matters. Because the ladder stops at the first true condition, you usually arrange conditions from the most specific/most restrictive to the least.

---

## Nested \`if\`

An \`if\` placed inside another \`if\`. The inner condition is only checked when the outer one is true.

\`\`\`java
if (age >= 18) {
    if (hasId) {
        System.out.println("Entry allowed");
    }
}
\`\`\`

This is equivalent to \`if (age >= 18 && hasId)\`, but nesting is handy when each level needs its own \`else\`.

---

## The Ternary Operator

The ternary operator is a compact \`if-else\` that **produces a value**:

\`\`\`java
result = condition ? valueIfTrue : valueIfFalse;
\`\`\`

Example:

\`\`\`java
int max = (a > b) ? a : b;
String type = (n % 2 == 0) ? "even" : "odd";
\`\`\`

| Part            | Meaning                              |
|-----------------|--------------------------------------|
| \`condition\`     | a boolean expression                 |
| \`valueIfTrue\`   | used when the condition is true      |
| \`valueIfFalse\`  | used when the condition is false     |

Use it for short either/or choices. For anything complex, a normal \`if-else\` is more readable.

---

## The \`switch\` Statement

A \`switch\` compares one variable against several constant \`case\` labels and runs the matching block. It is cleaner than a long ladder when you test a single value against fixed options.

\`\`\`java
switch (day) {
    case 1:
        dayName = "Monday";
        break;
    case 2:
        dayName = "Tuesday";
        break;
    default:
        dayName = "Weekend";
}
\`\`\`

**Key parts:**

- **\`case\`** – a constant value to compare against.
- **\`break\`** – stops execution and exits the switch. Without it, execution "falls through" into the next case.
- **\`default\`** – runs when no case matches (like the final \`else\`). It is optional.

**Fall-through** can be used on purpose by stacking cases that share a block:

\`\`\`java
switch (d) {
    case 1: case 2: case 3: case 4: case 5:
        System.out.println("Weekday");
        break;
    case 6: case 7:
        System.out.println("Weekend");
        break;
}
\`\`\`

A \`switch\` works with \`int\` (and smaller integer types), \`char\`, \`String\`, and \`enum\`.

---

## \`if-else\` vs \`switch\`

| Aspect            | \`if-else-if\`                          | \`switch\`                                |
|-------------------|---------------------------------------|-----------------------------------------|
| Condition type    | Any boolean expression (ranges, etc.) | Equality against constant values only   |
| Best for          | Ranges, complex/combined conditions   | One variable vs many fixed values       |
| Readability       | Can get long                          | Cleaner for many discrete cases         |
| Fall-through      | Not applicable                        | Possible (and sometimes useful)         |

Use \`if-else\` when testing ranges or combined conditions (\`marks >= 75\`). Use \`switch\` when checking one value against a list of exact options (\`day == 1\`, \`2\`, \`3\`...).

---

## How to Compile and Run

\`\`\`bash
javac IfElse.java
java IfElse

javac Ternary.java
java Ternary

javac Switch.java
java Switch
\`\`\`

---

## Common Mistakes

### Mistake 1: Using \`=\` instead of \`==\`

\`\`\`java
if (x = 5) { ... }   // ERROR: = assigns; a condition needs ==
if (x == 5) { ... }  // correct
\`\`\`

### Mistake 2: Forgetting \`break\` in a switch

\`\`\`java
switch (n) {
    case 1:
        System.out.println("one");
    case 2:
        System.out.println("two");
        break;
}
\`\`\`

Add \`break\` unless you intentionally want fall-through.

### Mistake 3: A misplaced semicolon after \`if\`

\`\`\`java
if (x > 0);
{
    System.out.println("positive");
}
\`\`\`

Do not put a semicolon directly after the \`if (...)\` condition.

### Mistake 4: Overusing nested ternaries

Deeply nested \`? :\` expressions are hard to read. Prefer an \`if-else-if\` ladder once there is more than one level.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`IfElse.java\`

Three shapes of the same idea. A plain **\`if\`** runs its block only when the
condition holds. **\`if-else\`** adds the other path. An **\`if-else-if\` ladder**
tries each condition in order and stops at the first true one â€” the rest are
never even evaluated, which is why the order of the rungs is part of the logic.

The last example nests an \`if\` inside another, for a decision that only makes
sense once an earlier one has passed.

\`\`\`java IfElse.java @run-conditionals-if-else
public class IfElse {

    public static void main(String[] args) {

        int marks = 75;

        if (marks >= 33) {
            System.out.println("Result: Pass");
        }

        int number = 7;
        if (number % 2 == 0) {
            System.out.println(number + " is even");
        } else {
            System.out.println(number + " is odd");
        }

        char grade;
        if (marks >= 90) {
            grade = 'A';
        } else if (marks >= 75) {
            grade = 'B';
        } else if (marks >= 60) {
            grade = 'C';
        } else if (marks >= 33) {
            grade = 'D';
        } else {
            grade = 'F';
        }
        System.out.println("Grade: " + grade);

        int age = 20;
        boolean hasId = true;
        if (age >= 18) {
            if (hasId) {
                System.out.println("Entry allowed");
            } else {
                System.out.println("ID required");
            }
        }
    }
}

\`\`\`

\`\`\`output @run-conditionals-if-else
Result: Pass
7 is odd
Grade: B
Entry allowed
\`\`\`

### \`Ternary.java\`

The ternary is the only Java operator taking three operands, and the compact
form of \`if-else\` â€” with one crucial difference: it **produces a value**. A
plain \`if\` statement does not, which is why a ternary can sit on the right of an
assignment or straight inside a \`println\`.

\`condition ? whenTrue : whenFalse\`. The file picks the larger of two numbers,
turns a number into the string \`"even"\` or \`"odd"\`, and shows the same logic
written out as \`if-else\` for comparison. Ternaries nest, as the last example
does â€” but that is usually where readability starts to lose.

\`\`\`java Ternary.java @run-conditionals-ternary
public class Ternary {

    public static void main(String[] args) {

        int a = 10;
        int b = 20;

        int max = (a > b) ? a : b;
        System.out.println("max         : " + max);

        int number = 7;
        String type = (number % 2 == 0) ? "even" : "odd";
        System.out.println("7 is        : " + type);

        // String type;
        // if (number % 2 == 0) type = "even"; else type = "odd";

        int marks = 82;
        char grade = (marks >= 90) ? 'A'
                   : (marks >= 75) ? 'B'
                   : (marks >= 60) ? 'C'
                   : 'F';
        System.out.println("grade       : " + grade);

        int age = 16;
        System.out.println("category    : " + (age >= 18 ? "Adult" : "Minor"));
    }
}

\`\`\`

\`\`\`output @run-conditionals-ternary
max         : 20
7 is        : odd
grade       : B
category    : Minor
\`\`\`

### \`Switch.java\`

A \`switch\` compares one value against a list of constant \`case\` labels and jumps
straight to the match, instead of walking down a ladder of conditions.

\`break\` is what stops execution there. Leave it out and control **falls through**
into the next case and keeps going â€” occasionally what you want, and otherwise a
classic bug. \`default\` catches everything that matched nothing.

\`\`\`java Switch.java @run-conditionals-switch
public class Switch {

    public static void main(String[] args) {

        int day = 3;
        String dayName;

        switch (day) {
            case 1:
                dayName = "Monday";
                break;
            case 2:
                dayName = "Tuesday";
                break;
            case 3:
                dayName = "Wednesday";
                break;
            case 4:
                dayName = "Thursday";
                break;
            case 5:
                dayName = "Friday";
                break;
            default:
                dayName = "Weekend";
        }
        System.out.println("day " + day + " is " + dayName);

        char grade = 'B';
        switch (grade) {
            case 'A':
                System.out.println("Excellent");
                break;
            case 'B':
                System.out.println("Good");
                break;
            case 'C':
                System.out.println("Average");
                break;
            default:
                System.out.println("Needs improvement");
        }

        int d = 6;
        switch (d) {
            case 1:
            case 2:
            case 3:
            case 4:
            case 5:
                System.out.println("Weekday");
                break;
            case 6:
            case 7:
                System.out.println("Weekend");
                break;
        }
    }
}

\`\`\`

\`\`\`output @run-conditionals-switch
day 3 is Wednesday
Good
Weekend
\`\`\`
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- `if` takes a `boolean`, not a number — `if (x = 5)` will not compile, which is\n  a whole class of C bugs Java simply does not have.\n- The `if-else-if` ladder stops at the first match.\n- `switch` works on `int`, `char`, `String` and `enum`. Forget `break` and it\n  falls through.\n- `?:` is an expression, so it produces a value. `if` is a statement.\n",interview:'## Interview questions\n\n### Q1: What is the difference between `if-else` and `switch`?\n\n**Answer:** `if-else` can test any boolean condition, including ranges and combined conditions. `switch` only tests equality of a single variable against constant values, but it is cleaner and often faster when checking one value against many fixed options.\n\n### Q2: What is the purpose of `break` in a `switch`?\n\n**Answer:** `break` exits the switch after a case runs. Without it, execution "falls through" and continues into the following cases until a `break` or the end of the switch is reached.\n\n### Q3: What is the `default` case?\n\n**Answer:** It is the block that runs when no `case` matches the switch value, similar to the final `else` in an if-else-if ladder. It is optional.\n\n### Q4: What is the ternary operator and when should you use it?\n\n**Answer:** It is a three-operand conditional, `condition ? valueIfTrue : valueIfFalse`, that produces a value. Use it for short, simple either/or choices. For complex logic, a normal `if-else` is more readable.\n\n### Q5: Which data types can a `switch` work with?\n\n**Answer:** `byte`, `short`, `int`, `char`, their wrapper classes, `String`, and `enum` types. It cannot switch on `long`, `float`, `double`, or `boolean`.\n\n### Q6: In an if-else-if ladder, what happens if two conditions are true?\n\n**Answer:** Only the **first** true condition (from top to bottom) runs; the rest are skipped. This is why the order of conditions matters.\n\n### Q7: Can you switch on a `String` in Java?\n\n**Answer:** Yes, since Java 7 a `switch` can use `String` values. The comparison uses `String.equals()` internally and is case-sensitive.\n'}},{slug:`loops`,title:`Loops`,subject:`Java`,stage:`Fundamentals`,chapter:7,accent:`#fb7185`,summary:`A loop repeats a block of code as long as a condition stays true. Instead of writing the same line five times, you write it once inside a loop and let it run five times. Loops are how programs process lists, repeat actions, and count.`,parts:{notes:`## Introduction

A **loop** repeats a block of code as long as a condition stays true. Instead of writing the same line five times, you write it once inside a loop and let it run five times. Loops are how programs process lists, repeat actions, and count.

This chapter uses one program, \`WhileDoWhileForLoops.java\`, that demonstrates all three loop types plus \`for-each\`, \`break\`, \`continue\`, and nesting.

---

## What is a Loop

Every loop has three logical parts, no matter the syntax:

1. **Initialisation** – a starting point (e.g. \`i = 1\`).
2. **Condition** – a boolean checked each round; the loop continues while it is true.
3. **Update** – a change that moves the loop toward ending (e.g. \`i++\`).

> If the update never makes the condition false, you get an **infinite loop**. Always make sure the loop can end.

---

## The \`while\` Loop

Checks the condition **before** each iteration. The body may run **zero** times if the condition starts false.

\`\`\`java
int i = 1;
while (i <= 5) {
    System.out.print(i + " ");
    i++;
}
\`\`\`

Use \`while\` when you do not know in advance how many times to loop (e.g. "keep reading until the input ends").

---

## The \`do-while\` Loop

Runs the body **first**, then checks the condition. So it always runs **at least once**, even if the condition is false.

\`\`\`java
int j = 1;
do {
    System.out.print(j + " ");
    j++;
} while (j <= 5);
\`\`\`

Use \`do-while\` when the body must execute at least once (e.g. show a menu, then ask whether to repeat).

> Note the **semicolon** after the \`while(...)\` in a do-while.

---

## The \`for\` Loop

Packs initialisation, condition, and update onto one line. Ideal when you know how many times to repeat.

\`\`\`java
for (int k = 1; k <= 5; k++) {
    System.out.print(k + " ");
}
\`\`\`

\`\`\`
for ( init ; condition ; update )
       |         |          |
   runs once  checked    runs after
   at start   each round  each round
\`\`\`

---

## The Enhanced \`for-each\` Loop

Iterates over **every element** of an array or collection without a counter or index. It is cleaner and avoids index errors when you simply need each value.

\`\`\`java
int[] nums = {10, 20, 30};
for (int n : nums) {
    System.out.print(n + " ");
}
\`\`\`

Read \`for (int n : nums)\` as "for each int \`n\` in \`nums\`". You cannot use it to change array elements by index or to know the current index — use a normal \`for\` loop for that.

---

## \`break\` and \`continue\`

| Keyword    | Effect                                                        |
|------------|--------------------------------------------------------------|
| \`break\`    | Exits the **entire** loop immediately.                       |
| \`continue\` | Skips the rest of the **current** iteration and moves on.    |

\`\`\`java
for (int b = 1; b <= 10; b++) {
    if (b == 4) break;
    System.out.print(b + " ");
}

for (int c = 1; c <= 6; c++) {
    if (c % 2 == 0) continue;
    System.out.print(c + " ");
}
\`\`\`

---

## Nested Loops

A loop placed inside another loop. For **each** step of the outer loop, the inner loop runs fully. Common for grids, tables, and patterns.

\`\`\`java
for (int row = 1; row <= 3; row++) {
    for (int col = 1; col <= 3; col++) {
        System.out.print("* ");
    }
    System.out.println();
}
\`\`\`

---

## Which Loop Should I Use

| Situation                                   | Best loop      |
|---------------------------------------------|----------------|
| You know the exact number of repetitions    | \`for\`          |
| You loop until some condition changes       | \`while\`        |
| The body must run at least once             | \`do-while\`     |
| You just need each element of an array       | \`for-each\`     |

---

## How to Compile and Run

\`\`\`bash
javac WhileDoWhileForLoops.java
java WhileDoWhileForLoops
\`\`\`

### Expected Output

\`\`\`
while  : 1 2 3 4 5
do-while: 1 2 3 4 5
for    : 1 2 3 4 5
for-each: 10 20 30
break  : 1 2 3
continue: 1 3 5
nested :
* * *
* * *
* * *
\`\`\`

---

## Common Mistakes

### Mistake 1: Forgetting the update (infinite loop)

\`\`\`java
int i = 1;
while (i <= 5) {
    System.out.println(i);
}
\`\`\`

### Mistake 2: An off-by-one error

\`\`\`java
for (int i = 0; i <= 5; i++)
for (int i = 0; i <  5; i++)
\`\`\`

Decide carefully between \`<\` and \`<=\`.

### Mistake 3: A stray semicolon after \`for\`/\`while\`

\`\`\`java
for (int i = 0; i < 5; i++);
{
    System.out.println(i);
}
\`\`\`

### Mistake 4: Forgetting the semicolon in \`do-while\`

\`\`\`java
do {
    ...
} while (i < 5)   // ERROR: needs a trailing semicolon
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`WhileDoWhileForLoops.java\`

All four looping forms in one run, and the differences between them are exactly
where the condition sits:

- **\`while\`** checks the condition **before** each pass. If it is already false,
  the body never runs at all.
- **\`do-while\`** runs the body **first** and checks afterwards, so it always runs
  at least once â€” even when the condition was false from the start.
- **\`for\`** is the same before-check as \`while\`, with the three parts â€”
  initialise, condition, update â€” gathered onto one line where you can see them
  together.
- **\`for-each\`** walks every element of an array or collection with no counter
  at all.

\`\`\`java WhileDoWhileForLoops.java @run-loops-while-do-while-for-loops
public class WhileDoWhileForLoops {

    public static void main(String[] args) {

        System.out.print("while  : ");
        int i = 1;
        while (i <= 5) {
            System.out.print(i + " ");
            i++;
        }
        System.out.println();

        System.out.print("do-while: ");
        int j = 1;
        do {
            System.out.print(j + " ");
            j++;
        } while (j <= 5);
        System.out.println();

        System.out.print("for    : ");
        for (int k = 1; k <= 5; k++) {
            System.out.print(k + " ");
        }
        System.out.println();

        System.out.print("for-each: ");
        int[] nums = { 10, 20, 30 };
        for (int n : nums) {
            System.out.print(n + " ");
        }
        System.out.println();

        System.out.print("break  : ");
        for (int b = 1; b <= 10; b++) {
            if (b == 4) {
                break;
            }
            System.out.print(b + " ");
        }
        System.out.println();

        System.out.print("continue: ");
        for (int c = 1; c <= 6; c++) {
            if (c % 2 == 0) {
                continue;
            }
            System.out.print(c + " ");
        }
        System.out.println();

        System.out.println("nested :");
        for (int row = 1; row <= 3; row++) {
            for (int col = 1; col <= 3; col++) {
                System.out.print("* ");
            }
            System.out.println();
        }
    }
}

\`\`\`

\`\`\`output @run-loops-while-do-while-for-loops
while  : 1 2 3 4 5 
do-while: 1 2 3 4 5 
for    : 1 2 3 4 5 
for-each: 10 20 30 
break  : 1 2 3 
continue: 1 3 5 
nested :
* * * 
* * * 
* * *
\`\`\`
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- `while` checks first, `do-while` runs at least once.\n- `for` when you know how many iterations, `while` when you do not, `for-each` to\n  read every element.\n- `for-each` gives you a **copy** of each element, so assigning to the loop\n  variable changes nothing.\n- `break` leaves the loop, `continue` skips to the next iteration.\n",interview:"## Interview questions\n\n### Q1: What is the difference between `while` and `do-while`?\n\n**Answer:** A `while` loop checks the condition before the first iteration, so its body may run zero times. A `do-while` loop checks the condition after the body, so the body always runs at least once.\n\n### Q2: When would you use a `for` loop instead of a `while` loop?\n\n**Answer:** Use a `for` loop when the number of iterations is known or counter-based, because initialisation, condition, and update are grouped together. Use a `while` loop when you loop until some condition changes and the count is not known in advance.\n\n### Q3: What is the difference between `break` and `continue`?\n\n**Answer:** `break` exits the entire loop immediately. `continue` skips the remaining statements in the current iteration and proceeds to the next iteration.\n\n### Q4: What is an infinite loop and how does it happen?\n\n**Answer:** An infinite loop never ends because its condition never becomes false — usually because the update step is missing or wrong (e.g. forgetting `i++`). \n\n### Q5: What is the enhanced for-each loop, and what are its limits?\n\n**Answer:** It iterates over every element of an array or collection without an index: `for (int n : nums)`. It is concise but does not expose the index and cannot be used to replace elements by position or to iterate backward.\n\n### Q6: In a nested loop, how many times does the inner loop run?\n\n**Answer:** The inner loop runs fully for each iteration of the outer loop. If the outer loop runs `m` times and the inner runs `n` times, the inner body executes `m × n` times in total.\n"}},{slug:`classes-and-objects`,title:`Classes and objects`,subject:`Java`,stage:`Classes and objects`,chapter:8,accent:`#c084fc`,summary:`Classes and objects are the two most fundamental building blocks of Object-Oriented Programming (OOP). Java is an object-oriented language, which means almost everything in Java revolves around classes and objects. If you do not understand classes and objects, you cannot progress further in Java.`,parts:{notes:`## Introduction

Classes and objects are the two most fundamental building blocks of Object-Oriented Programming (OOP). Java is an object-oriented language, which means almost everything in Java revolves around classes and objects. If you do not understand classes and objects, you cannot progress further in Java.

This program demonstrates how to create a simple \`Calculator\` class with a method, create an object of that class, and call the method using that object.

---

## What is a Class

A class is a blueprint or template that defines the structure and behavior of objects. It describes what properties (variables) and actions (methods) an object will have, but it does not occupy any memory by itself.

Think of a class like an architectural blueprint for a house. The blueprint describes the number of rooms, the size of windows, and the layout, but the blueprint itself is not a house. You build actual houses (objects) based on the blueprint (class).

**Syntax:**

\`\`\`java
class ClassName {
}
\`\`\`

**Key Points:**

- A class is declared using the \`class\` keyword.
- A class name should start with an uppercase letter (convention).
- A class can contain variables (also called fields or attributes) and methods.
- A class does not consume memory until an object is created from it.

---

## What is an Object

An object is an instance of a class. When you create an object, Java allocates memory for it and initializes its properties. Each object has its own copy of the instance variables defined in the class.

Using the house analogy: if the class is the blueprint, then each house built from that blueprint is an object. You can build multiple houses from the same blueprint, and each house can have different paint colors, furniture, and residents.

**Syntax to create an object:**

\`\`\`java
ClassName objectName = new ClassName();
\`\`\`

**Breaking it down:**

| Part                | Meaning                                                            |
|---------------------|--------------------------------------------------------------------|
| \`ClassName\`         | The type of the object (which class it belongs to)                |
| \`objectName\`        | The reference variable that points to the object in memory        |
| \`new\`               | A keyword that allocates memory for the object on the heap        |
| \`ClassName()\`       | The constructor call that initializes the object                  |

---

## Difference Between Class and Object

| Aspect            | Class                                    | Object                                    |
|-------------------|------------------------------------------|-------------------------------------------|
| Definition        | A blueprint or template                  | An instance of a class                    |
| Memory            | Does not occupy memory                   | Occupies memory when created              |
| Creation          | Defined using the \`class\` keyword        | Created using the \`new\` keyword           |
| Quantity          | Defined once                             | Can be created multiple times             |
| Contains          | Variable declarations and methods        | Actual values for those variables         |
| Real-world analogy| Recipe for a cake                        | The actual cake made from the recipe      |

---

## Understanding the Code

\`\`\`java
class Calculator {

    int a;

    public int add(int num1, int num2) {
        return num1 + num2;
    }
}

public class Classes {

    public static void main(String[] args) {
        int n1 = 2,
            n2 = 4;

        Calculator calc = new Calculator();

        int add = calc.add(n1, n2);

        System.out.println(add);
    }
}
\`\`\`

---

## Line-by-Line Explanation

### \`class Calculator {\`

- Declares a class named \`Calculator\`.
- This class is not \`public\`, which means it can only be accessed within the same file or package.
- This is the blueprint for creating Calculator objects.

### \`int a;\`

- This is an instance variable (also called a field or attribute).
- Every object of \`Calculator\` will have its own copy of \`a\`.
- Since it is an \`int\`, its default value is \`0\` if not explicitly assigned.
- In this program, \`a\` is declared but not used. It is there to show that a class can have properties.

### \`public int add(int num1, int num2) {\`

- **\`public\`**: This method can be accessed from outside the class.
- **\`int\`**: The return type. This method will return an integer value.
- **\`add\`**: The name of the method.
- **\`int num1, int num2\`**: These are parameters. When you call this method, you must pass two integer values.

### \`return num1 + num2;\`

- Adds \`num1\` and \`num2\` together and returns the result to the caller.
- The \`return\` keyword sends the value back and exits the method.

### \`Calculator calc = new Calculator();\`

- **\`Calculator calc\`**: Declares a reference variable \`calc\` of type \`Calculator\`.
- **\`new Calculator()\`**: Creates a new object of the \`Calculator\` class in heap memory.
- The reference variable \`calc\` now points to this newly created object.

### \`int add = calc.add(n1, n2);\`

- Calls the \`add\` method on the \`calc\` object, passing \`n1\` (2) and \`n2\` (4) as arguments.
- The method returns \`6\`, which is stored in the variable \`add\`.
- The dot operator (\`.\`) is used to access methods and properties of an object.

### \`System.out.println(add);\`

- Prints the value \`6\` to the console.

---

## How Object Creation Works in Memory

When you write \`Calculator calc = new Calculator();\`, the following happens internally:

1. **Stack Memory**: The reference variable \`calc\` is created on the stack. It holds the memory address (reference) of the actual object.

2. **Heap Memory**: The \`new\` keyword allocates memory on the heap for the \`Calculator\` object. The instance variable \`a\` is initialized to its default value (\`0\` for \`int\`).

3. **Reference Assignment**: The address of the newly created object on the heap is stored in the \`calc\` variable on the stack.

\`\`\`
Stack Memory              Heap Memory
+----------+              +-------------------+
| calc  ---|------------->| Calculator Object |
+----------+              | a = 0             |
                           | add() method ref  |
                           +-------------------+
\`\`\`

---

## Methods in Java

A method is a block of code that performs a specific task. Methods allow you to write reusable code.

**Types of Methods:**

| Type                     | Description                                                    | Example                           |
|--------------------------|----------------------------------------------------------------|-----------------------------------|
| Instance method          | Belongs to an object, requires object to call                  | \`calc.add(2, 4)\`                 |
| Static method            | Belongs to the class, does not require an object               | \`Math.max(2, 4)\`                 |
| Method with return value | Returns a value using the \`return\` keyword                     | \`public int add(int a, int b)\`   |
| Void method              | Does not return any value                                      | \`public void printHello()\`       |

**Key Terminology:**

- **Parameters**: Variables declared in the method definition. Example: \`int num1, int num2\` in \`add(int num1, int num2)\`.
- **Arguments**: Actual values passed when calling the method. Example: \`n1, n2\` in \`calc.add(n1, n2)\`.
- **Return Type**: The data type of the value the method sends back. If it returns nothing, use \`void\`.

---

## How to Compile and Run

\`\`\`bash
javac Classes.java
java Classes
\`\`\`

### Expected Output

\`\`\`
6
\`\`\`

---

## Common Mistakes

### Mistake 1: Trying to call an instance method without creating an object

\`\`\`java
Calculator.add(2, 4);  // ERROR: add is not a static method
\`\`\`

You must create an object first and then call the method using that object.

### Mistake 2: Forgetting the \`new\` keyword

\`\`\`java
Calculator calc;
calc.add(2, 4);  // ERROR: calc is null, NullPointerException at runtime
\`\`\`

Declaring a reference variable does not create an object. You must use \`new\` to allocate memory.

### Mistake 3: Mismatching return types

\`\`\`java
public int add(int a, int b) {
    return;  // ERROR: must return an int value
}
\`\`\`

If the return type is \`int\`, the method must return an integer value.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Classes.java\`

The smallest useful pair: a \`Calculator\` class that knows how to add, and a
\`main\` that creates one and asks it to.

Two steps are worth separating in your head. \`new Calculator()\` builds an
**object** from the **class** â€” the class is the description, the object is the
thing. \`calc.add(2, 4)\` then calls a method on that object and gets \`6\` back.

\`\`\`java Classes.java @run-classes-and-objects-classes
class Calculator {

    int a;

    public int add(int num1, int num2) {
        return num1 + num2;
    }
}

public class Classes {

    public static void main(String[] args) {
        int n1 = 2,
            n2 = 4;

        Calculator calc = new Calculator();

        int add = calc.add(n1, n2);

        System.out.println(add);
    }
}

\`\`\`

\`\`\`output @run-classes-and-objects-classes
6
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- A **class** is the definition; an **object** is one instance of it, made with
  \`new\` and living on the heap.
- The reference lives on the stack, the object it points at lives on the heap.
- Fields get default values; locals do not.
- A no-argument constructor is supplied only if you write no constructor at all.
`,interview:`## Interview questions

### Q1: What is the difference between a class and an object?

**Answer:** A class is a blueprint that defines properties and methods. An object is an instance of a class created at runtime. A class does not occupy memory, but an object does. You can create multiple objects from a single class, each with its own set of property values.

### Q2: What is the \`new\` keyword used for in Java?

**Answer:** The \`new\` keyword is used to create a new object. It allocates memory on the heap for the object, initializes the instance variables to their default values, calls the constructor, and returns a reference to the newly created object.

### Q3: What are default values of instance variables in Java?

**Answer:**

| Data Type | Default Value |
|-----------|---------------|
| int       | 0             |
| float     | 0.0f          |
| double    | 0.0           |
| boolean   | false         |
| char      | '\\u0000'      |
| String    | null          |
| Object    | null          |

### Q4: What is the difference between a parameter and an argument?

**Answer:** A parameter is the variable declared in the method definition. An argument is the actual value passed to the method when it is called. For example, in \`public int add(int num1, int num2)\`, \`num1\` and \`num2\` are parameters. In \`calc.add(2, 4)\`, \`2\` and \`4\` are arguments.

### Q5: Can a class have multiple methods with the same name?

**Answer:** Yes, this is called method overloading. Multiple methods can have the same name as long as they have different parameter lists (different number of parameters, different types, or different order of types).

### Q6: What is the difference between stack and heap memory?

**Answer:** Stack memory stores local variables and reference variables. It follows LIFO (Last In, First Out) order and is faster. Heap memory stores objects created with the \`new\` keyword. It is larger but slower. When a method finishes executing, its stack frame is removed, but objects on the heap persist until garbage collected.

### Q7: What happens if you do not initialize an instance variable?

**Answer:** Java automatically assigns default values to instance variables. For \`int\` it is \`0\`, for \`boolean\` it is \`false\`, and for object references it is \`null\`. However, local variables (declared inside a method) are NOT given default values and must be initialized before use.

### Q8: Can you have two public classes in the same Java file?

**Answer:** No. A Java source file can have only one public class, and the file name must match the name of that public class. You can have multiple non-public classes in the same file.
`}},{slug:`methods`,title:`Methods`,subject:`Java`,stage:`Classes and objects`,chapter:9,accent:`#60a5fa`,summary:`A method is a named block of code that performs a specific task. You define it once and call it whenever you need it. Methods make programs shorter, easier to read, and easier to fix, because the logic lives in one place.`,parts:{notes:`## Introduction

A **method** is a named block of code that performs a specific task. You define it once and call it whenever you need it. Methods make programs shorter, easier to read, and easier to fix, because the logic lives in one place.

This chapter has three programs:

| File | Topic |
|------|-------|
| \`MethodsBasics.java\`    | Declaring, calling, returning, \`void\` |
| \`MethodOverloading.java\`| Same name, different parameters (compile-time) |
| \`MethodOverriding.java\` | Child redefines a parent method (run-time) |

You already saw a method in Chapter 8 (\`Calculator.add\`). This chapter goes deeper.

---

## What is a Method

Instead of repeating the same code, you wrap it in a method and call it by name:

\`\`\`java
static int add(int a, int b) {
    return a + b;
}

int sum = add(5, 3);
\`\`\`

Benefits: **reuse** (write once, call many times), **readability** (a good name explains intent), and **maintainability** (fix a bug in one place).

---

## Anatomy of a Method

\`\`\`java
public static int add(int num1, int num2) {
    return num1 + num2;
}
\`\`\`

| Part            | Meaning                                                        |
|-----------------|----------------------------------------------------------------|
| Access modifier | Who can call it (\`public\`, \`private\`, …)                       |
| \`static\`        | Belongs to the class; callable without creating an object      |
| Return type     | The type of value returned, or \`void\` for nothing             |
| Method name     | What you call it (convention: starts lowercase, a verb)        |
| Parameters      | Inputs the method needs, in parentheses                        |
| Body            | The code that runs, in braces                                  |

---

## Parameters vs Arguments

- **Parameters** are the variables in the method *declaration*: \`add(int num1, int num2)\`.
- **Arguments** are the actual values you *pass* when calling: \`add(5, 3)\` — here \`5\` and \`3\` are arguments.

---

## Return Values and \`void\`

A method either returns a value or returns nothing:

\`\`\`java
static int square(int n) {
    return n * n;
}

static void greet(String name) {
    System.out.println("Hello, " + name);
}
\`\`\`

- If the return type is not \`void\`, the method **must** \`return\` a value of that type.
- \`return\` also immediately exits the method.
- A \`void\` method can still use a bare \`return;\` to exit early.

---

## Method Overloading

**Overloading** = multiple methods with the **same name** but **different parameter lists** (different number, types, or order). The compiler picks the right one based on the arguments. This is decided at **compile time** (also called *static* or *compile-time polymorphism*).

\`\`\`java
static int    add(int a, int b)        { return a + b; }
static int    add(int a, int b, int c) { return a + b + c; }
static double add(double a, double b)  { return a + b; }
static String add(int a, String b)     { return a + b; }
\`\`\`

\`\`\`java
add(2, 3);
add(2, 3, 4);
add(2.5, 3.5);
add(5, "th");
\`\`\`

> **The return type alone does not count.** Two methods that differ *only* by return type are a compile error. The **parameter lists** must differ.

---

## Method Overriding

**Overriding** = a **child class** provides its **own version** of a method already defined in its **parent**, with the **same name and same parameters**. Which version runs is decided at **run time** by the actual object — this is *runtime* (dynamic) polymorphism.

\`\`\`java
class Animal {
    void sound() { System.out.println("Some generic animal sound"); }
}

class Dog extends Animal {
    @Override
    void sound() { System.out.println("Woof"); }
}
\`\`\`

\`\`\`java
Animal a = new Dog();
a.sound();
\`\`\`

The \`@Override\` annotation is optional but recommended: the compiler will error if the method does not actually override anything (e.g. a typo in the name), catching bugs early.

> Overriding builds on **inheritance**, covered fully in Chapter 15, and underpins **polymorphism** in Chapter 17.

---

## Overloading vs Overriding

| Aspect            | Overloading                          | Overriding                          |
|-------------------|--------------------------------------|-------------------------------------|
| Method name       | Same                                 | Same                                |
| Parameter list    | **Different**                        | **Same**                            |
| Where             | Same class                           | Parent class and child class        |
| Inheritance       | Not required                         | Required (\`extends\`)                |
| Decided at        | Compile time (static)                | Run time (dynamic)                  |
| Also called       | Compile-time polymorphism            | Runtime polymorphism                |

---

## How to Compile and Run

\`\`\`bash
javac MethodsBasics.java
java MethodsBasics

javac MethodOverloading.java
java MethodOverloading

javac MethodOverriding.java
java MethodOverriding
\`\`\`

---

## Common Mistakes

### Mistake 1: Overloading by return type only

\`\`\`java
int  value() { return 1; }
double value() { return 1.0; } // ERROR: same name and parameters
\`\`\`

The parameter lists must differ, not just the return type.

### Mistake 2: A non-void method that does not return on every path

\`\`\`java
static int max(int a, int b) {
    if (a > b) return a;
}
\`\`\`

### Mistake 3: Thinking the reference type chooses the overridden method

\`\`\`java
Animal a = new Dog();
a.sound();
\`\`\`

### Mistake 4: Changing the parameter list when you meant to override

\`\`\`java
class Dog extends Animal {
    void sound(String s) { ... }
}
\`\`\`

Use \`@Override\` to catch this — the compiler will complain.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`MethodsBasics.java\`

A method is a named block of code you can call by name, which is what stops the
same lines being written out repeatedly. The file shows the three shapes you
will write most:

- one that **returns a value**, so its return type is \`int\` and the caller can
  store or print the result;
- one that returns **nothing**, declared \`void\` â€” it acts rather than answers;
- one with **no parameters** that still returns a value.

The last call shows a method used inside a larger expression, which works
because a call with a return type *is* a value.

\`\`\`java MethodsBasics.java @run-methods-methods-basics
public class MethodsBasics {

    static int add(int num1, int num2) {
        return num1 + num2;
    }

    static void greet(String name) {
        System.out.println("Hello, " + name + "!");
    }

    static double pi() {
        return 3.14159;
    }

    public static void main(String[] args) {

        int sum = add(5, 3);
        System.out.println("add(5, 3)  = " + sum);

        greet("Aditya");

        System.out.println("pi()       = " + pi());

        System.out.println("add nested = " + add(add(1, 2), 4));
    }
}

\`\`\`

\`\`\`output @run-methods-methods-basics
add(5, 3)  = 8
Hello, Aditya!
pi()       = 3.14159
add nested = 7
\`\`\`

### \`MethodOverloading.java\`

**Overloading** is several methods sharing one name but taking different
parameter lists. All four here are called \`add\`; they differ in how many
arguments they take and of what type.

That is enough for the compiler to pick the right one at the call site â€” the
choice is made from the arguments alone, before the program ever runs. Note what
is *not* enough: a different return type by itself does not overload anything.

\`\`\`java MethodOverloading.java @run-methods-method-overloading
public class MethodOverloading {

    static int add(int a, int b) {
        return a + b;
    }

    static int add(int a, int b, int c) {
        return a + b + c;
    }

    static double add(double a, double b) {
        return a + b;
    }

    static String add(int a, String b) {
        return a + b;
    }

    public static void main(String[] args) {
        System.out.println("add(2, 3)        = " + add(2, 3));
        System.out.println("add(2, 3, 4)     = " + add(2, 3, 4));
        System.out.println("add(2.5, 3.5)    = " + add(2.5, 3.5));
        System.out.println("add(5, \\"th\\")     = " + add(5, "th"));
    }
}

\`\`\`

\`\`\`output @run-methods-method-overloading
add(2, 3)        = 5
add(2, 3, 4)     = 9
add(2.5, 3.5)    = 6.0
add(5, "th")     = 5th
\`\`\`

### \`MethodOverriding.java\`

**Overriding** is a child class replacing a method it inherited from its parent.
Same name, same parameters â€” different body.

The three calls at the end are the point. The reference is typed \`Animal\` every
time, but the method that runs is the one belonging to the **object**, so a
\`Dog\` barks and a \`Cat\` meows. The reference type decides what you may call; the
object decides what actually happens.

\`\`\`java MethodOverriding.java @run-methods-method-overriding

class Animal {
    void sound() {
        System.out.println("Some generic animal sound");
    }
}

class Dog extends Animal {
    @Override
    void sound() {
        System.out.println("Woof");
    }
}

class Cat extends Animal {
    @Override
    void sound() {
        System.out.println("Meow");
    }
}

public class MethodOverriding {
    public static void main(String[] args) {

        Animal a1 = new Animal();
        Animal a2 = new Dog();
        Animal a3 = new Cat();

        a1.sound();
        a2.sound();
        a3.sound();
    }
}

\`\`\`

\`\`\`output @run-methods-method-overriding
Some generic animal sound
Woof
Meow
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- Signature = name + parameter list. Return type is **not** part of it.
- **Overloading** — same name, different parameters, resolved at compile time.
- **Overriding** — a subclass replacing an inherited method with the same
  signature, resolved at run time.
- Java is **pass by value**, always. Passing an object copies the reference, so
  the method can change the object's contents but cannot repoint the caller's
  variable.
`,interview:`## Interview questions

### Q1: What is a method in Java?

**Answer:** A method is a named block of code that performs a task. It can take parameters as input and may return a value. Methods enable code reuse, readability, and easier maintenance.

### Q2: What is the difference between a parameter and an argument?

**Answer:** A parameter is the variable declared in the method definition; an argument is the actual value passed when the method is called. In \`add(int a, int b)\`, \`a\` and \`b\` are parameters; in \`add(5, 3)\`, \`5\` and \`3\` are arguments.

### Q3: What is method overloading?

**Answer:** Defining multiple methods with the same name but different parameter lists (number, type, or order). The compiler chooses which to call based on the arguments. It is resolved at compile time.

### Q4: What is method overriding?

**Answer:** A subclass provides its own implementation of a method already defined in its superclass, with the same name and parameter list. The version that runs is chosen at run time based on the actual object type.

### Q5: Can you overload a method by changing only the return type?

**Answer:** No. Overloaded methods must differ in their parameter lists. Two methods with the same name and parameters but different return types cause a compile error.

### Q6: What is the difference between overloading and overriding?

**Answer:** Overloading uses the same name with different parameters in the same class and is resolved at compile time (compile-time polymorphism). Overriding uses the same name and parameters across a parent and child class, requires inheritance, and is resolved at run time (runtime polymorphism).

### Q7: What does the \`@Override\` annotation do?

**Answer:** It tells the compiler the method is intended to override a superclass method. If it does not actually override one (for example due to a wrong name or parameter list), the compiler reports an error, which prevents subtle bugs.
`}},{slug:`arrays`,title:`Arrays`,subject:`Java`,stage:`Classes and objects`,chapter:10,accent:`#38bdf8`,summary:`Arrays are one of the most basic and widely used data structures in programming. In Java, an array is an object that stores a fixed number of values of the same data type in contiguous memory locations. Understanding arrays is essential because they form the foundation for more advanced data structures like ArrayLists, Stacks, and Queues.`,parts:{notes:`## Introduction

Arrays are one of the most basic and widely used data structures in programming. In Java, an array is an object that stores a fixed number of values of the same data type in contiguous memory locations. Understanding arrays is essential because they form the foundation for more advanced data structures like ArrayLists, Stacks, and Queues.

This chapter is split into four programs:

| File | Topic |
|------|-------|
| \`Arrays.java\`            | Declaring, instantiating, and traversing a 1D array |
| \`EnhancedForLoop.java\` | The enhanced \`for-each\` loop over arrays |
| \`TwoDArray.java\`        | Two-dimensional (grid/matrix) arrays |
| \`JaggedArray.java\`      | Jagged arrays (rows of different lengths) |

---

## What is an Array

An array is a container object that holds a fixed number of values of a single type. The length of an array is established when the array is created, and after creation, its length is fixed and cannot be changed.

Think of an array as a row of numbered lockers in a school. Each locker (element) has a number (index) starting from 0, and each locker can hold exactly one item (value) of the same type.

\`\`\`
Index:    [0]   [1]   [2]   [3]   [4]
Values:   [ 0 ] [ 0 ] [ 0 ] [ 0 ] [ 0 ]
\`\`\`

**Key Characteristics:**

- Arrays in Java are zero-indexed (the first element is at index 0).
- Arrays have a fixed size that cannot change after creation.
- All elements in an array must be of the same data type.
- Arrays in Java are objects, meaning they are stored on the heap.
- The \`length\` property tells you how many elements the array can hold.

---

## Why Do We Need Arrays

Without arrays, if you wanted to store the marks of 100 students, you would need to declare 100 separate variables:

\`\`\`java
int marks1 = 80;
int marks2 = 75;
int marks3 = 90;
\`\`\`

This is impractical. With an array, you can store all 100 values in a single variable:

\`\`\`java
int[] marks = new int[100];
\`\`\`

Now you can access any student's marks using the index: \`marks[0]\`, \`marks[1]\`, \`marks[99]\`, and so on.

---

## Types of Arrays

### 1. Single-Dimensional Array

A simple list of elements.

\`\`\`java
int[] numbers = {10, 20, 30, 40, 50};
\`\`\`

### 2. Multi-Dimensional Array

An array of arrays, commonly used to represent matrices or tables.

\`\`\`java
int[][] matrix = {
    {1, 2, 3},
    {4, 5, 6},
    {7, 8, 9}
};
\`\`\`

### 3. Jagged Array

A multi-dimensional array where each row can have a different number of columns.

\`\`\`java
int[][] jagged = new int[3][];
jagged[0] = new int[2];
jagged[1] = new int[4];
jagged[2] = new int[1];
\`\`\`

---

## Understanding the Code

\`\`\`java
public class Arrays {

    public static void main(String[] args) {
        int nums[] = new int[5];

        for (int i : nums) {
            System.out.println(i);
        }

        System.out.println(nums + " Hi ");
    }
}
\`\`\`

---

## Line-by-Line Explanation

### \`int nums[] = new int[5];\`

This single line does two things:

1. **Declaration**: \`int nums[]\` declares a variable \`nums\` that can hold a reference to an integer array. Note: \`int[] nums\` and \`int nums[]\` are both valid syntaxes in Java, but \`int[] nums\` is the preferred Java style.

2. **Instantiation**: \`new int[5]\` allocates memory on the heap for an array of 5 integers. All 5 elements are automatically initialized to the default value for \`int\`, which is \`0\`.

After this line executes, the array looks like this:

\`\`\`
nums --> [0, 0, 0, 0, 0]
          0  1  2  3  4   (indices)
\`\`\`

### \`for (int i : nums) {\`

This is the **enhanced for-loop** (also called for-each loop), introduced in Java 5. It iterates over every element in the array without needing an index variable.

- \`int i\` is a temporary variable that takes the value of each element in the array, one at a time.
- \`nums\` is the array being iterated over.

In the first iteration, \`i = nums[0]\` which is \`0\`. In the second iteration, \`i = nums[1]\` which is \`0\`. And so on.

Important: In the enhanced for-loop, \`i\` is NOT the index. It is the VALUE of the element at each position.

### \`System.out.println(i);\`

Prints the current value of \`i\` on each iteration. Since all elements are \`0\`, this prints \`0\` five times.

### \`System.out.println(nums + " Hi ");\`

This line is very instructive for understanding how arrays work in Java:

- \`nums\` is a reference variable. When you try to print it directly, Java calls the \`toString()\` method inherited from the \`Object\` class.
- The default \`toString()\` method of an array returns a string in the format: \`[TypeCode@HashCode]\`. For example: \`[I@1b6d3586\`.
  - \`[I\` means it is an array of integers.
  - \`@1b6d3586\` is the hexadecimal representation of the object's hash code.
- \`+ " Hi "\` concatenates the string " Hi " to this representation.
- The output will be something like: \`[I@1b6d3586 Hi\`

If you want to print the actual contents of the array, use \`Arrays.toString(nums)\`:

\`\`\`java
import java.util.Arrays;
System.out.println(Arrays.toString(nums));
\`\`\`

---

## How Arrays Work in Memory

\`\`\`
Stack Memory              Heap Memory
+----------+              +---+---+---+---+---+
| nums  ---|------------->| 0 | 0 | 0 | 0 | 0 |
+----------+              +---+---+---+---+---+
                           [0] [1] [2] [3] [4]
\`\`\`

- The reference variable \`nums\` is stored on the stack.
- The actual array data is stored on the heap.
- \`nums\` holds the memory address of the array on the heap.
- This is why printing \`nums\` directly shows a hash code instead of the array contents.

---

## Different Ways to Declare and Initialize Arrays

\`\`\`java
int[] arr;
arr = new int[5];

int[] arr = new int[5];

int[] arr = {10, 20, 30, 40, 50};

int[] arr = new int[]{10, 20, 30, 40, 50};

int arr[] = new int[5];
\`\`\`

---

## Iterating Over Arrays

### Method 1: Traditional for-loop

\`\`\`java
for (int i = 0; i < nums.length; i++) {
    System.out.println(nums[i]);
}
\`\`\`

Use this when you need the index value.

### Method 2: Enhanced for-loop (for-each)

\`\`\`java
for (int num : nums) {
    System.out.println(num);
}
\`\`\`

Use this when you only need the values and do not care about the index.

### Method 3: While loop

\`\`\`java
int i = 0;
while (i < nums.length) {
    System.out.println(nums[i]);
    i++;
}
\`\`\`

### Comparison

| Feature              | Traditional for-loop | Enhanced for-loop |
|----------------------|---------------------|-------------------|
| Access to index      | Yes                 | No                |
| Can modify elements  | Yes                 | No (modifies local copy) |
| Readability          | Moderate            | High              |
| Risk of index errors | Yes                 | No                |

---

## How to Compile and Run

\`\`\`bash
javac Arrays.java
java Arrays

javac EnhancedForLoop.java
java EnhancedForLoop

javac TwoDArray.java
java TwoDArray

javac JaggedArray.java
java JaggedArray
\`\`\`

### Expected Output (\`Arrays\`)

\`\`\`
0
0
0
0
0
[I@<some_hashcode> Hi
\`\`\`

---

## Common Mistakes

### Mistake 1: ArrayIndexOutOfBoundsException

\`\`\`java
int[] arr = new int[5];
arr[5] = 10;  // ERROR: valid indices are 0 to 4
\`\`\`

Arrays are zero-indexed, so an array of size 5 has indices 0 through 4.

### Mistake 2: Confusing array size with last index

\`\`\`java
int[] arr = new int[5];
\`\`\`

### Mistake 3: Trying to resize an array

\`\`\`java
int[] arr = new int[5];
\`\`\`

If you need a resizable collection, use \`ArrayList\` instead.

### Mistake 4: Comparing arrays with \`==\`

\`\`\`java
int[] a = {1, 2, 3};
int[] b = {1, 2, 3};
System.out.println(a == b);
System.out.println(Arrays.equals(a, b));
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Arrays.java\`

The smallest possible array program, and it makes two points. \`new int[5]\`
does not leave the slots empty â€” it fills them with the default for the type,
which for \`int\` is \`0\`, so the loop prints five zeros.

The last line prints the array *reference* rather than its contents.
\`[I@1b6d3586\` is Java's type signature for "array of int", an \`@\`, and a hash
code that changes on every run. Use \`Arrays.toString(...)\` when you want the
values.

\`\`\`java Arrays.java @run-arrays-arrays
public class Arrays {

    public static void main(String[] args) {
        int nums[] = new int[5];

        for (int i : nums) {
            System.out.println(i);
        }

        System.out.println(nums + " Hi ");
    }
}

\`\`\`

\`\`\`output @run-arrays-arrays
0
0
0
0
0
[I@2b2fa4f7 Hi
\`\`\`

### \`EnhancedForLoop.java\`

The enhanced for loop â€” \`for (type variable : array)\` â€” reads each element in
order without an index variable. It is the right tool whenever the index itself
is not part of the job, and it works on arrays of objects (\`String[]\`) exactly
as it does on primitives.

One limit worth knowing: the loop variable holds a **copy** of each value, so
assigning to it changes nothing in the array. To modify elements by position you
need an ordinary indexed \`for\` loop.

\`\`\`java EnhancedForLoop.java @run-arrays-enhanced-for-loop
public class EnhancedForLoop {

    public static void main(String[] args) {

        int[] marks = { 85, 90, 75, 60, 95 };

        int total = 0;
        System.out.print("marks: ");
        for (int mark : marks) {
            System.out.print(mark + " ");
            total += mark;
        }
        System.out.println();
        System.out.println("total: " + total);
        System.out.println("average: " + (total / marks.length));

        String[] fruits = { "Apple", "Banana", "Cherry" };
        System.out.print("fruits: ");
        for (String fruit : fruits) {
            System.out.print(fruit + " ");
        }
        System.out.println();

    }
}

\`\`\`

\`\`\`output @run-arrays-enhanced-for-loop
marks: 85 90 75 60 95 
total: 405
average: 81
fruits: Apple Banana Cherry
\`\`\`

### \`TwoDArray.java\`

A 2D array is an array of arrays â€” a grid, addressed with two indices as
\`matrix[row][col]\`. \`matrix.length\` gives the number of rows, and
\`matrix[i].length\` the number of columns in row \`i\`.

The file builds one grid from literal values and prints it with nested loops,
then creates an empty \`new int[2][3]\` and fills two cells by hand â€” showing that
a 2D array can be declared with its contents or sized first and populated later.

\`\`\`java TwoDArray.java @run-arrays-two-darray
public class TwoDArray {

    public static void main(String[] args) {

        int[][] matrix = {
            { 1, 2, 3 },
            { 4, 5, 6 },
            { 7, 8, 9 }
        };

        System.out.println("rows: " + matrix.length + ", cols: " + matrix[0].length);

        System.out.println("matrix[1][2] = " + matrix[1][2]);

        System.out.println("grid:");
        for (int row = 0; row < matrix.length; row++) {
            for (int col = 0; col < matrix[row].length; col++) {
                System.out.print(matrix[row][col] + " ");
            }
            System.out.println();
        }

        int[][] table = new int[2][3];
        table[0][0] = 10;
        table[1][2] = 99;
        System.out.println("table[0][0] = " + table[0][0] + ", table[1][2] = " + table[1][2]);
    }
}

\`\`\`

\`\`\`output @run-arrays-two-darray
rows: 3, cols: 3
matrix[1][2] = 6
grid:
1 2 3 
4 5 6 
7 8 9 
table[0][0] = 10, table[1][2] = 99
\`\`\`

### \`JaggedArray.java\`

A jagged array is a 2D array whose rows have **different** lengths. You create
the outer array with the column size left empty â€” \`new int[3][]\` â€” and then give
each row its own length.

Because each row is its own array, \`jagged[row].length\` differs per row, and
that is what the printing loop uses. The \`direct\` array at the end shows the
same thing built from literals instead.

\`\`\`java JaggedArray.java @run-arrays-jagged-array
public class JaggedArray {

    public static void main(String[] args) {

        int[][] jagged = new int[3][];

        jagged[0] = new int[2];
        jagged[1] = new int[4];
        jagged[2] = new int[1];

        for (int row = 0; row < jagged.length; row++) {
            for (int col = 0; col < jagged[row].length; col++) {
                jagged[row][col] = (row + 1) * (col + 1);
            }
        }

        System.out.println("jagged array:");
        for (int row = 0; row < jagged.length; row++) {
            System.out.print("row " + row + " (len " + jagged[row].length + "): ");
            for (int value : jagged[row]) {
                System.out.print(value + " ");
            }
            System.out.println();
        }

        int[][] direct = {
            { 1 },
            { 1, 2, 3 },
            { 1, 2 }
        };
        System.out.println("direct row 1 length: " + direct[1].length);
    }
}

\`\`\`

\`\`\`output @run-arrays-jagged-array
jagged array:
row 0 (len 2): 1 2 
row 1 (len 4): 2 4 6 8 
row 2 (len 1): 3 
direct row 1 length: 3
\`\`\`
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- Fixed length, set at creation, readable as `.length` — a field, not a method.\n- Indexes run `0` to `length - 1`. Outside that is\n  `ArrayIndexOutOfBoundsException`.\n- Elements start at the type's default: `0`, `null`, `false`.\n- The array is an object on the heap; the variable is a reference, so `b = a`\n  does not copy it.\n",interview:'## Interview questions\n\n### Q1: What is an array in Java?\n\n**Answer:** An array is a fixed-size, contiguous data structure that stores multiple values of the same data type. It is an object in Java, stored on the heap, and accessed via a reference variable on the stack. Elements are accessed using zero-based indices.\n\n### Q2: What is the default value of elements in an integer array?\n\n**Answer:** The default value is `0`. For `boolean` arrays, the default is `false`. For `double` and `float` arrays, the default is `0.0`. For object arrays (such as `String[]`), the default is `null`.\n\n### Q3: What is the difference between `int[] arr` and `int arr[]`?\n\n**Answer:** Both are valid syntaxes for declaring an array in Java. `int[] arr` is the preferred Java convention because it clearly indicates that `arr` is of type "integer array". `int arr[]` is the C/C++ style and is less commonly used in Java. Functionally, they are identical.\n\n### Q4: Can the size of an array be changed after creation?\n\n**Answer:** No. Once an array is created, its size is fixed and cannot be modified. If you need a dynamic-size collection, use `ArrayList` from the `java.util` package.\n\n### Q5: What is the difference between an array and an ArrayList?\n\n**Answer:**\n\n| Feature        | Array                    | ArrayList                 |\n|----------------|--------------------------|---------------------------|\n| Size           | Fixed                    | Dynamic (grows/shrinks)   |\n| Type           | Can hold primitives      | Can only hold objects     |\n| Performance    | Faster                   | Slightly slower           |\n| Methods        | Only `.length`           | Rich API (add, remove, etc.) |\n| Syntax         | `int[] arr = new int[5]` | `ArrayList<Integer> list = new ArrayList<>()` |\n\n### Q6: What happens when you print an array directly using `System.out.println(arr)`?\n\n**Answer:** It prints the class name followed by the `@` symbol and the hexadecimal hash code of the array object, such as `[I@1b6d3586`. The `[I` indicates it is an integer array. To print the actual contents, use `Arrays.toString(arr)`.\n\n### Q7: What is the enhanced for-loop and when should you use it?\n\n**Answer:** The enhanced for-loop (for-each loop) is a simplified syntax introduced in Java 5 for iterating over arrays and collections. It should be used when you need to read all elements sequentially and do not need the index. It cannot be used to modify the original array elements or iterate in reverse.\n\n### Q8: What exception is thrown if you access an invalid array index?\n\n**Answer:** `ArrayIndexOutOfBoundsException`, which is a runtime exception. It is thrown when you try to access an index that is less than 0 or greater than or equal to the array\'s length.\n'}},{slug:`arrays-of-objects`,title:`Arrays of objects`,subject:`Java`,stage:`Classes and objects`,chapter:11,accent:`#34d399`,summary:`In the previous chapter, we worked with arrays that store primitive data types like int. But in real-world applications, we often need to store complex data -- for example, a student's roll number, name, and marks together as a single unit. This is where the combination of classes and arrays becomes powerful.`,parts:{notes:`## Introduction

In the previous chapter, we worked with arrays that store primitive data types like \`int\`. But in real-world applications, we often need to store complex data -- for example, a student's roll number, name, and marks together as a single unit. This is where the combination of classes and arrays becomes powerful.

This program demonstrates how to create a custom \`Student\` class with properties and then create multiple objects of that class, each with different values.

---

## What are Object Properties

Object properties (also called fields, attributes, or instance variables) are variables that belong to an object. They define the state or data that an object holds.

For example, a \`Student\` object might have the following properties:

- \`rollno\` -- the student's roll number
- \`name\` -- the student's name
- \`marks\` -- the student's marks

Each student object has its own independent copy of these properties. Changing the name of one student does not affect the name of another student.

---

## Understanding the Code

\`\`\`java
class Student {

    int rollno;
    String name;
    float marks;

    public void printDetails() {
        System.out.println(rollno + ", " + name + ", " + marks);
    }
}

public class ArraysProperties {

    public static void main(String[] args) {
        Student s1 = new Student();
        s1.rollno = 1;
        s1.name = "Navin";
        s1.marks = 77;
        s1.printDetails();

        Student s2 = new Student();
        s2.rollno = 2;
        s2.name = "Aditya";
        s2.marks = 65;

        Student s3 = new Student();
        s3.rollno = 3;
        s3.name = "Tappu";
        s3.marks = 98;
    }
}
\`\`\`

---

## Line-by-Line Explanation

### The Student Class

#### \`class Student {\`

Declares a class named \`Student\`. This serves as the blueprint for creating student objects.

#### \`int rollno;\`

An instance variable of type \`int\` to store the student's roll number. Default value is \`0\`.

#### \`String name;\`

An instance variable of type \`String\` to store the student's name. Default value is \`null\` because \`String\` is a reference type.

#### \`float marks;\`

An instance variable of type \`float\` to store the student's marks. Default value is \`0.0f\`.

#### \`public void printDetails() {\`

A method that prints the student's details. It is \`void\` because it does not return any value. It simply prints output to the console.

#### \`System.out.println(rollno + ", " + name + ", " + marks);\`

This line uses string concatenation. The \`+\` operator, when used with a \`String\`, converts the other operands to their string representations and joins them together. So for a student with rollno=1, name="Navin", and marks=77, the output will be: \`1, Navin, 77.0\`.

Note that \`marks\` prints as \`77.0\` (not \`77\`) because it is a \`float\`.

### The Main Class

#### \`Student s1 = new Student();\`

Creates a new \`Student\` object and stores its reference in \`s1\`. At this point, all properties have their default values: \`rollno = 0\`, \`name = null\`, \`marks = 0.0\`.

#### \`s1.rollno = 1;\`

Uses the dot operator (\`.\`) to access the \`rollno\` property of the \`s1\` object and assigns it the value \`1\`. This is called direct field access.

#### \`s1.name = "Navin";\`

Assigns the string \`"Navin"\` to the \`name\` property of \`s1\`.

#### \`s1.marks = 77;\`

Assigns \`77\` to the \`marks\` property. Even though \`77\` is an integer literal, Java automatically converts it to \`77.0f\` because \`marks\` is of type \`float\` (widening conversion).

#### \`s1.printDetails();\`

Calls the \`printDetails()\` method on the \`s1\` object, which prints: \`1, Navin, 77.0\`.

#### Creating s2 and s3

The same process is repeated for \`s2\` and \`s3\`, but with different values. Notice that \`s2.printDetails()\` and \`s3.printDetails()\` are not called in this code, so their details will not be printed.

---

## How Multiple Objects Work in Memory

\`\`\`
Stack Memory              Heap Memory

+----------+              +-------------------+
| s1    ---|------------->| Student Object    |
+----------+              | rollno = 1        |
                           | name = "Navin"    |
+----------+              | marks = 77.0      |
| s2    ---|------+       +-------------------+
+----------+      |
                   +----->+-------------------+
+----------+              | Student Object    |
| s3    ---|------+       | rollno = 2        |
+----------+      |       | name = "Aditya"   |
                   |       | marks = 65.0      |
                   |       +-------------------+
                   |
                   +----->+-------------------+
                           | Student Object    |
                           | rollno = 3        |
                           | name = "Tappu"    |
                           | marks = 98.0      |
                           +-------------------+
\`\`\`

Each object is completely independent. Changing \`s1.name\` has no effect on \`s2.name\` or \`s3.name\`.

---

## Creating an Array of Objects

Instead of creating separate variables \`s1\`, \`s2\`, \`s3\`, you can create an array to hold all the student objects:

\`\`\`java
Student[] students = new Student[3];

students[0] = new Student();
students[0].rollno = 1;
students[0].name = "Navin";
students[0].marks = 77;

students[1] = new Student();
students[1].rollno = 2;
students[1].name = "Aditya";
students[1].marks = 65;

students[2] = new Student();
students[2].rollno = 3;
students[2].name = "Tappu";
students[2].marks = 98;
\`\`\`

Important: When you create \`new Student[3]\`, it creates an array of 3 references, all initialized to \`null\`. You must separately create each \`Student\` object using \`new Student()\` and assign it to the array element.

\`\`\`java
Student[] students = new Student[3];

students[0] = new Student();
\`\`\`

---

## Constructors vs Manual Assignment

In this code, properties are set manually one by one after creating the object. This is not the recommended approach for production code. A better approach is to use a constructor:

\`\`\`java
class Student {
    int rollno;
    String name;
    float marks;

    Student(int rollno, String name, float marks) {
        this.rollno = rollno;
        this.name = name;
        this.marks = marks;
    }
}

Student s1 = new Student(1, "Navin", 77);
\`\`\`

This is cleaner, less error-prone, and ensures that every student object is fully initialized upon creation.

---

## How to Compile and Run

\`\`\`bash
javac ArraysProperties.java
java ArraysProperties
\`\`\`

### Expected Output

\`\`\`
1, Navin, 77.0
\`\`\`

Only \`s1.printDetails()\` is called, so only the first student's details are printed.

---

## Common Mistakes

### Mistake 1: Accessing properties on a null reference

\`\`\`java
Student s1;
s1.name = "Aditya";  // ERROR: NullPointerException (s1 is null)
\`\`\`

You must create the object using \`new\` before accessing its properties.

### Mistake 2: Forgetting that object arrays contain null references

\`\`\`java
Student[] students = new Student[3];
students[0].name = "Aditya";  // ERROR: NullPointerException
\`\`\`

Creating the array only allocates space for references. You must create each object individually.

### Mistake 3: Confusing instance variables with local variables

Instance variables (declared in the class) have default values. Local variables (declared inside a method) do NOT have default values and must be initialized before use.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`ArraysProperties.java\`

\`Student\` is an ordinary model class â€” three fields and a \`printDetails()\` that
prints them.

Read \`main\` carefully, because the output is smaller than it looks. Three
students are created and filled in, but only \`s1.printDetails()\` is called, so
only the first one prints. \`s2\` and \`s3\` exist in memory and say nothing â€”
creating an object and using it are two separate steps.

\`\`\`java ArraysProperties.java @run-arrays-of-objects-arrays-properties
class Student {

    int rollno;
    String name;
    float marks;

    public void printDetails() {
        System.out.println(rollno + ", " + name + ", " + marks);
    }
}

public class ArraysProperties {

    public static void main(String[] args) {
        Student s1 = new Student();
        s1.rollno = 1;
        s1.name = "Navin";
        s1.marks = 77;
        s1.printDetails();

        Student s2 = new Student();
        s2.rollno = 2;
        s2.name = "Aditya";
        s2.marks = 65;

        Student s3 = new Student();
        s3.rollno = 3;
        s3.name = "Tappu";
        s3.marks = 98;
    }
}

\`\`\`

\`\`\`output @run-arrays-of-objects-arrays-properties
1, Navin, 77.0
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- \`new Student[3]\` creates three **null** slots, not three students. Each element
  still needs its own \`new\`.
- Forgetting that is the classic \`NullPointerException\` in this chapter.
- A constructor sets fields at creation time, which is safer than assigning them
  one by one afterwards and forgetting one.
`,interview:'## Interview questions\n\n### Q1: What is the difference between instance variables and local variables?\n\n**Answer:**\n\n| Feature           | Instance Variables                     | Local Variables                       |\n|-------------------|----------------------------------------|---------------------------------------|\n| Declared in       | Class body, outside methods            | Inside a method, constructor, or block|\n| Default value     | Yes (0, false, null, etc.)             | No (must be initialized)             |\n| Scope             | Entire class                           | Only within the declaring block       |\n| Stored in         | Heap (as part of the object)           | Stack                                 |\n| Access modifiers  | Can have (public, private, etc.)       | Cannot have access modifiers          |\n\n### Q2: What is NullPointerException and when does it occur?\n\n**Answer:** `NullPointerException` is a runtime exception that occurs when you try to use a reference variable that points to `null` (no object). Common causes include calling a method on a null object, accessing a property of a null object, or trying to use an element of an object array that has not been initialized.\n\n### Q3: What is the default value of a String instance variable?\n\n**Answer:** The default value is `null`, not an empty string (`""`). `null` means the variable does not point to any `String` object in memory. An empty string is a valid `String` object that happens to have zero characters.\n\n### Q4: Can you create an array of objects in Java?\n\n**Answer:** Yes. You can create an array that holds references to objects. However, creating the array only creates space for references (initialized to `null`). You must separately create each object using the `new` keyword and assign it to the corresponding array index.\n\n### Q5: What is the dot operator (`.`) used for in Java?\n\n**Answer:** The dot operator is used to access members (properties and methods) of an object. For example, `s1.name` accesses the `name` property of the object referenced by `s1`, and `s1.printDetails()` calls the `printDetails()` method on that object.\n\n### Q6: What is string concatenation and how does the `+` operator work with Strings?\n\n**Answer:** String concatenation is the process of joining two or more strings together. When the `+` operator is used and at least one operand is a `String`, Java converts the other operand to its `String` representation and joins them. For example, `1 + ", " + "Navin"` produces `"1, Navin"`. Internally, Java uses `StringBuilder` for this conversion.\n\n### Q7: Why does `marks` print as `77.0` instead of `77`?\n\n**Answer:** Because `marks` is declared as `float`. When a `float` is converted to a `String` for printing, Java always includes the decimal point. Even though the value is a whole number, it is stored as a floating-point number (`77.0`), and that is how it is displayed.\n'}},{slug:`strings`,title:`Strings and StringBuilder`,subject:`Java`,stage:`Classes and objects`,chapter:12,accent:`#f97316`,summary:`Text is everywhere in programs — names, messages, file contents. In Java, text is handled by the String class. Strings are so important they get special syntax (double quotes) and their own memory area (the String Pool, from Chapter 2). This chapter covers Strings and their mutable companion, StringBuilder.`,parts:{notes:`## Introduction

Text is everywhere in programs — names, messages, file contents. In Java, text is handled by the **\`String\`** class. Strings are so important they get special syntax (double quotes) and their own memory area (the String Pool, from Chapter 2). This chapter covers Strings and their **mutable** companion, **\`StringBuilder\`**.

This chapter has two programs:

| File | Topic |
|------|-------|
| \`StringDemo.java\`         | The \`String\` class, methods, immutability |
| \`StringBuilderDemo.java\` | \`StringBuilder\` for efficient, mutable text |

---

## What is a String

A \`String\` is a sequence of characters. It is a **class** (a reference type), not a primitive, written between double quotes:

\`\`\`java
String name = "Java";
\`\`\`

Each character has an index starting at 0, just like an array:

\`\`\`
 J   a   v   a
[0] [1] [2] [3]
\`\`\`

---

## Creating Strings

\`\`\`java
String a = "Hello";
String b = new String("Hello");
\`\`\`

- A **literal** (\`"Hello"\`) is placed in the **String Pool** and reused if the same text already exists there.
- \`new String("Hello")\` **forces** a separate object on the heap, even if the same text is pooled. (See Chapter 2 for the String Pool.)

---

## String Immutability

**Strings are immutable** — once created, their characters can never be changed. Any method that appears to modify a String actually returns a **new** String, leaving the original untouched.

\`\`\`java
String text = "Java Programming";
text.toUpperCase();
System.out.println(text);

String upper = text.toUpperCase();
System.out.println(upper);
\`\`\`

**Why immutable?** It makes Strings safe to share (including across threads), allows the String Pool to reuse them, and makes them reliable as keys in collections.

---

## Common String Methods

For \`String text = "Java Programming";\`

| Method                | Purpose                            | Result            |
|-----------------------|------------------------------------|-------------------|
| \`length()\`            | number of characters               | \`16\`              |
| \`toUpperCase()\`       | upper-case copy                    | \`"JAVA PROGRAMMING"\` |
| \`toLowerCase()\`       | lower-case copy                    | \`"java programming"\` |
| \`charAt(5)\`           | character at an index              | \`'P'\`             |
| \`indexOf("Prog")\`     | index of a substring (or -1)       | \`5\`               |
| \`substring(0, 4)\`     | part of the string \`[start, end)\`  | \`"Java"\`          |
| \`replace('a', 'A')\`   | replace characters                 | \`"JAvA ProgrAmming"\` |
| \`contains("Java")\`    | does it contain the text?          | \`true\`            |
| \`trim()\`              | remove leading/trailing spaces     | \`"spaced"\`        |

> Every one of these returns a **new** String. The original \`text\` is never changed.

---

## Comparing Strings

\`\`\`java
String a = "Hello";
String b = new String("Hello");

a == b
a.equals(b)
\`\`\`

- Use **\`.equals()\`** to compare text.
- Use **\`.equalsIgnoreCase()\`** to ignore upper/lower case.
- Avoid **\`==\`** for text; it compares object identity, not content.

---

## Why StringBuilder Exists

Because String is immutable, building text with \`+\` in a loop creates a **new** String on every step, which is wasteful:

\`\`\`java
String result = "";
for (int i = 0; i < 1000; i++) {
    result += i;
}
\`\`\`

\`StringBuilder\` is **mutable**: it changes its internal buffer in place, with no throwaway objects:

\`\`\`java
StringBuilder sb = new StringBuilder();
for (int i = 0; i < 1000; i++) {
    sb.append(i);
}
String result = sb.toString();
\`\`\`

---

## StringBuilder Methods

Starting from \`new StringBuilder("Hello")\`:

| Method                | Effect                                  | Result after call |
|-----------------------|-----------------------------------------|-------------------|
| \`append(" World")\`    | add to the end                          | \`Hello World\`     |
| \`insert(5, ",")\`      | insert at an index                      | \`Hello, World\`    |
| \`replace(0, 5, "Hi")\` | replace chars in range \`[start, end)\`   | \`Hi, World\`       |
| \`deleteCharAt(2)\`     | remove one character                    | \`Hi World\`        |
| \`reverse()\`           | reverse the whole sequence              | \`dlroW iH\`        |
| \`length()\`            | current length                          | \`8\`               |
| \`toString()\`          | convert back to an immutable \`String\`   | \`"Hi World"\`      |

(The table follows the exact sequence of calls in \`StringBuilderDemo.java\`.)

---

## String vs StringBuilder vs StringBuffer

| Feature        | \`String\`              | \`StringBuilder\`         | \`StringBuffer\`            |
|----------------|-----------------------|-------------------------|---------------------------|
| Mutable?       | No (immutable)        | Yes                     | Yes                       |
| Thread-safe?   | Yes (because immutable)| No                     | Yes (synchronised)        |
| Speed          | Slow for many edits   | **Fastest** for edits   | Slower than StringBuilder |
| Use when       | Text rarely changes   | Building text in one thread | Building text across threads |

> Rule of thumb: use \`String\` for fixed text, \`StringBuilder\` when you build or modify text repeatedly, and \`StringBuffer\` only when multiple threads share the buffer.

---

## How to Compile and Run

\`\`\`bash
javac StringDemo.java
java StringDemo

javac StringBuilderDemo.java
java StringBuilderDemo
\`\`\`

---

## Common Mistakes

### Mistake 1: Comparing text with \`==\`

\`\`\`java
if (name == "Java") { ... }
if (name.equals("Java")) { ... }  // correct: compares characters
\`\`\`

### Mistake 2: Expecting a String method to change the original

\`\`\`java
String s = "hello";
s.toUpperCase();
s = s.toUpperCase();        // correct: reassign to keep the result
\`\`\`

### Mistake 3: Building large text with \`+\` in a loop

\`\`\`java
String r = "";
for (...) r += x;
\`\`\`

### Mistake 4: \`substring\` end index confusion

\`\`\`java
"Java".substring(0, 4);
"Java".substring(1);
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`StringDemo.java\`

A \`String\` is a sequence of characters and, crucially, **immutable** â€” every
method that looks like it edits one actually returns a new \`String\` and leaves
the original untouched.

The file creates a string both ways, runs the common methods over it, and then
prints the original again. That last print is the proof: after all the calls,
the source string still reads exactly as it did.

Concatenation is no exception â€” joining two strings produces a third rather than
extending either.

\`\`\`java StringDemo.java @run-strings-string-demo
public class StringDemo {

    public static void main(String[] args) {

        String a = "Hello";
        String b = new String("Hello");

        System.out.println("a == b        : " + (a == b));
        System.out.println("a.equals(b)   : " + a.equals(b));

        String text = "Java Programming";

        System.out.println("length        : " + text.length());
        System.out.println("toUpperCase   : " + text.toUpperCase());
        System.out.println("toLowerCase   : " + text.toLowerCase());
        System.out.println("charAt(5)     : " + text.charAt(5));
        System.out.println("indexOf 'Prog': " + text.indexOf("Prog"));
        System.out.println("substring(0,4): " + text.substring(0, 4));
        System.out.println("replace a->A  : " + text.replace('a', 'A'));
        System.out.println("contains Java : " + text.contains("Java"));
        System.out.println("trim          : '" + "  spaced  ".trim() + "'");

        System.out.println("original text : " + text);

        String greet = "Hi, " + "there";
        System.out.println("concatenated  : " + greet);
    }
}

\`\`\`

\`\`\`output @run-strings-string-demo
a == b        : false
a.equals(b)   : true
length        : 16
toUpperCase   : JAVA PROGRAMMING
toLowerCase   : java programming
charAt(5)     : P
indexOf 'Prog': 5
substring(0,4): Java
replace a->A  : JAvA ProgrAmming
contains Java : true
trim          : 'spaced'
original text : Java Programming
concatenated  : Hi, there
\`\`\`

### \`StringBuilderDemo.java\`

\`StringBuilder\` is the mutable companion to \`String\`. \`append\`, \`insert\`,
\`replace\` and \`delete\` change the object **in place** rather than handing back a
new one, and \`toString()\` converts it to an ordinary \`String\` when you are done.

The reason it exists is the loop at the end. Because \`String\` is immutable,
building one with \`+\` inside a loop creates a fresh object on every pass and
throws the previous one away. A \`StringBuilder\` builds the same text in one
object.

\`\`\`java StringBuilderDemo.java @run-strings-string-builder-demo
public class StringBuilderDemo {

    public static void main(String[] args) {

        StringBuilder sb = new StringBuilder("Hello");

        sb.append(" World");
        System.out.println("after append : " + sb);

        sb.insert(5, ",");
        System.out.println("after insert : " + sb);

        sb.replace(0, 5, "Hi");
        System.out.println("after replace: " + sb);

        sb.deleteCharAt(2);
        System.out.println("after delete : " + sb);

        sb.reverse();
        System.out.println("after reverse: " + sb);

        sb.reverse();
        System.out.println("length       : " + sb.length());

        String result = sb.toString();
        System.out.println("toString     : " + result);

        StringBuilder csv = new StringBuilder();
        for (int i = 1; i <= 5; i++) {
            csv.append(i);
            if (i < 5) {
                csv.append(",");
            }
        }
        System.out.println("built in loop: " + csv);
    }
}

\`\`\`

\`\`\`output @run-strings-string-builder-demo
after append : Hello World
after insert : Hello, World
after replace: Hi, World
after delete : Hi World
after reverse: dlroW iH
length       : 8
toString     : Hi World
built in loop: 1,2,3,4,5
\`\`\`
`,revision:'The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- Strings are **immutable**. Every method that looks like it changes one returns\n  a new one.\n- `==` compares references, `.equals()` compares text. Always `.equals()`.\n- Literals are pooled and shared; `new String("x")` deliberately is not, so `==`\n  fails on it.\n- `+=` in a loop is O(n²). Use `StringBuilder`, which mutates one buffer.\n- `StringBuffer` is the synchronised `StringBuilder`, and almost never what you\n  want.\n',interview:'## Interview questions\n\n### Q1: Why are Strings immutable in Java?\n\n**Answer:** Once created, a String\'s contents cannot change. Immutability allows safe sharing (including between threads), enables the String Pool to reuse literals, and makes Strings reliable as keys in hash-based collections. Methods that seem to modify a String return a new one instead.\n\n### Q2: What is the difference between `==` and `.equals()` for Strings?\n\n**Answer:** `==` compares references (whether two variables point to the same object), while `.equals()` compares the actual character contents. To compare text, always use `.equals()`.\n\n### Q3: What is the difference between `String` and `StringBuilder`?\n\n**Answer:** `String` is immutable, so each modification creates a new object. `StringBuilder` is mutable and modifies its internal buffer in place, which is far more efficient when building or repeatedly changing text.\n\n### Q4: What is the difference between `StringBuilder` and `StringBuffer`?\n\n**Answer:** Both are mutable. `StringBuffer` is synchronised (thread-safe) but slower; `StringBuilder` is not synchronised and is faster. Use `StringBuilder` for single-threaded code and `StringBuffer` only when a buffer is shared across threads.\n\n### Q5: What happens with `String s = "Hi"; s = s + " there";`?\n\n**Answer:** The original `"Hi"` is not changed. A new String `"Hi there"` is created and `s` is reassigned to point to it. The old object becomes eligible for garbage collection if nothing else references it.\n\n### Q6: Why is concatenating Strings with `+` in a loop discouraged?\n\n**Answer:** Because String is immutable, each `+=` creates a new String and copies the old contents, which is O(n²) work over a loop. A `StringBuilder` modifies one buffer in place, making it much faster.\n\n### Q7: What does `substring(a, b)` return?\n\n**Answer:** The portion of the String from index `a` (inclusive) to index `b` (exclusive). For example, `"Java".substring(0, 4)` returns `"Java"`, and `"Java".substring(1)` returns `"ava"`.\n'}},{slug:`encapsulation`,title:`Encapsulation`,subject:`Java`,stage:`Classes and objects`,chapter:13,accent:`#a78bfa`,summary:`Encapsulation is one of the four fundamental principles of Object-Oriented Programming (OOP), along with Inheritance, Polymorphism, and Abstraction. It is the mechanism of bundling data (variables) and the methods that operate on that data into a single unit (class), while restricting direct access to the internal data from outside the class.`,parts:{notes:`## Introduction

Encapsulation is one of the four fundamental principles of Object-Oriented Programming (OOP), along with Inheritance, Polymorphism, and Abstraction. It is the mechanism of bundling data (variables) and the methods that operate on that data into a single unit (class), while restricting direct access to the internal data from outside the class.

This program demonstrates how to make a variable private and provide controlled access to it through getter and setter methods.

---

## What is Encapsulation

Encapsulation means wrapping the data (variables) and code (methods) together as a single unit and controlling who can access what. The core idea is simple: hide the internal details and expose only what is necessary.

In Java, encapsulation is achieved by:

1. Declaring the instance variables of a class as \`private\`.
2. Providing \`public\` getter and setter methods to access and modify those variables.

Think of it like an ATM machine. You can withdraw money, check your balance, and deposit money (public methods). But you cannot directly access the vault, modify the database, or see the internal circuitry (private data). The ATM provides a controlled interface to interact with your account.

---

## Why Do We Need Encapsulation

### Problem Without Encapsulation

\`\`\`java
class Human {
    int age;
}

Human person = new Human();
person.age = -500;
\`\`\`

Without encapsulation, anyone can set \`age\` to an invalid value like \`-500\`. There is no validation, no control, and no way to enforce business rules.

### Solution With Encapsulation

\`\`\`java
class Human {
    private int age;

    public void setAge(int age) {
        if (age > 0 && age < 150) {
            this.age = age;
        } else {
            System.out.println("Invalid age");
        }
    }

    public int getAge() {
        return age;
    }
}
\`\`\`

Now the \`age\` variable is \`private\`, and the only way to set it is through the \`setAge\` method, which can include validation logic.

### Benefits of Encapsulation

| Benefit                | Description                                                      |
|------------------------|------------------------------------------------------------------|
| Data Hiding            | Internal data is hidden from outside classes                     |
| Data Validation        | Setter methods can validate data before assignment               |
| Flexibility            | Internal implementation can change without affecting external code|
| Read-Only/Write-Only   | You can create properties that are only readable or only writable|
| Maintainability        | Code is easier to maintain and debug                             |

---

## Access Modifiers in Java

Access modifiers control the visibility of classes, methods, and variables. Java has four access modifiers:

| Modifier    | Same Class | Same Package | Subclass (different package) | Everywhere |
|-------------|:----------:|:------------:|:----------------------------:|:----------:|
| \`private\`   | Yes        | No           | No                           | No         |
| (default)   | Yes        | Yes          | No                           | No         |
| \`protected\` | Yes        | Yes          | Yes                          | No         |
| \`public\`    | Yes        | Yes          | Yes                          | Yes        |

- **\`private\`**: Most restrictive. Accessible only within the same class. Used for encapsulation.
- **\`default\`** (no keyword): Accessible within the same package.
- **\`protected\`**: Accessible within the same package and by subclasses in other packages.
- **\`public\`**: Accessible from everywhere.

---

## Getters and Setters

### What is a Getter?

A getter (also called an accessor) is a public method that returns the value of a private variable. The naming convention is \`getVariableName()\`.

\`\`\`java
public int getAge() {
    return age;
}
\`\`\`

### What is a Setter?

A setter (also called a mutator) is a public method that sets or updates the value of a private variable. The naming convention is \`setVariableName(value)\`.

\`\`\`java
public void setAge(int age) {
    this.age = age;
}
\`\`\`

### Naming Convention

For a property named \`propertyName\`:

- Getter: \`getPropertyName()\` (for boolean: \`isPropertyName()\`)
- Setter: \`setPropertyName(type value)\`

This convention is called the JavaBeans naming convention and is used extensively in frameworks like Spring and Hibernate.

---

## Understanding the Code

\`\`\`java
class Human {

    private int age;

    public int getAge() {
        return age;
    }

    public void setAge(int age, Human obj) {
        Human obj1 = obj;
        obj1.age = age;
    }
}

public class Encapsulation {

    public static void main(String[] args) {

        Human obj = new Human();
        obj.setAge(4, obj);
        System.out.println(obj.getAge());
    }
}
\`\`\`

Note: The \`setAge\` method in this code has an unconventional design for learning purposes. In real-world code, you would not pass the object as a parameter to its own setter.

---

## Line-by-Line Explanation

### \`private int age;\`

- Declares an instance variable \`age\` as \`private\`.
- This means \`age\` cannot be accessed directly from outside the \`Human\` class.
- Any attempt like \`obj.age = 5;\` from another class will result in a compilation error.

### \`public int getAge() { return age; }\`

- A public getter method that returns the value of the private \`age\` variable.
- This is the only way for outside code to read the value of \`age\`.
- The return type is \`int\` because \`age\` is an \`int\`.

### \`public void setAge(int age, Human obj) {\`

- A setter method that accepts two parameters: the new age value and a reference to a \`Human\` object.
- Note: This is an unusual setter design. The standard convention is \`public void setAge(int age)\`.

### \`Human obj1 = obj;\`

- Creates a new reference variable \`obj1\` that points to the same object as \`obj\`.
- No new object is created here. Both \`obj\` and \`obj1\` point to the same \`Human\` object in heap memory.

### \`obj1.age = age;\`

- Sets the \`age\` property of the object referenced by \`obj1\`.
- Since \`obj1\` and \`obj\` point to the same object, this also changes the \`age\` accessible through \`obj\`.
- Note: Even though \`age\` is \`private\`, it can be accessed from within the same class. The \`private\` restriction only applies to access from other classes.

### \`Human obj = new Human();\`

- Creates a new \`Human\` object with the default age value of \`0\`.

### \`obj.setAge(4, obj);\`

- Calls the setter method, passing \`4\` as the new age and \`obj\` itself as the \`Human\` reference.
- After this call, \`obj.age\` is \`4\`.

### \`System.out.println(obj.getAge());\`

- Calls the getter to retrieve the age and prints \`4\`.

---

## What Happens Without Encapsulation

If \`age\` were \`public\`:

\`\`\`java
class Human {
    public int age;
}

Human obj = new Human();
obj.age = -100;
obj.age = 999999;
System.out.println(obj.age);
\`\`\`

There is no way to enforce rules like "age must be between 0 and 150." With encapsulation, you can add validation inside the setter method.

---

## Best Practices for Encapsulation

1. **Always declare instance variables as \`private\`.**
2. **Provide getters only if external code needs to read the value.**
3. **Provide setters only if external code needs to modify the value.** If a property should be set only once (for example, during object creation), do not provide a setter -- use a constructor instead.
4. **Add validation logic inside setters.**
5. **Follow the JavaBeans naming convention** for getters and setters.
6. **Use the \`this\` keyword** to differentiate between the parameter and the instance variable when they have the same name.

Standard setter example:

\`\`\`java
public void setAge(int age) {
    if (age > 0 && age < 150) {
        this.age = age;
    }
}
\`\`\`

---

## How to Compile and Run

\`\`\`bash
javac Encapsulation.java
java Encapsulation
\`\`\`

### Expected Output

\`\`\`
4
\`\`\`

---

## Common Mistakes

### Mistake 1: Shadowing without using \`this\`

\`\`\`java
public void setAge(int age) {
    age = age;
}
\`\`\`

The correct way is:

\`\`\`java
public void setAge(int age) {
    this.age = age;
}
\`\`\`

### Mistake 2: Making getters and setters for every field blindly

Do not create getters and setters for every field just because it is a convention. Only expose what needs to be accessed or modified from outside. Creating unnecessary public methods defeats the purpose of encapsulation.

### Mistake 3: Trying to access private fields from outside the class

\`\`\`java
Human obj = new Human();
obj.age = 5;
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Encapsulation.java\`

\`Human\` keeps its \`age\` field **private** and exposes it through a public getter
and setter. That is encapsulation: the data is not reachable from outside the
class, and every read or write goes through a method you control â€” which is
where validation would live in real code.

\`main\` sets the age through the setter and reads it back through the getter.
Nothing outside \`Human\` ever touches the field directly.

\`\`\`java Encapsulation.java @run-encapsulation-encapsulation
class Human {

    private int age;

    public int getAge() {
        return age;
    }

    public void setAge(int age, Human obj) {
        Human obj1 = obj;
        obj1.age = age;
        // age = age;
    }
}

public class Encapsulation {

    public static void main(String[] args) {

        Human obj = new Human();
        obj.setAge(4, obj);
        System.out.println(obj.getAge());
    }
}

\`\`\`

\`\`\`output @run-encapsulation-encapsulation
4
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- Fields \`private\`, access through public getters and setters.
- Access levels, widest to narrowest: \`public\` → \`protected\` → *(default,
  package)* → \`private\`.
- The point is not hiding data; it is being able to **validate** in the setter
  and change the internals later without breaking callers.
`,interview:'## Interview questions\n\n### Q1: What is encapsulation in Java?\n\n**Answer:** Encapsulation is the OOP principle of bundling data (variables) and methods that operate on that data into a single unit (class), while hiding the internal data from outside access. It is achieved by making variables `private` and providing `public` getter and setter methods.\n\n### Q2: What is the difference between encapsulation and abstraction?\n\n**Answer:** Encapsulation is about hiding the internal data by making variables private and controlling access through methods. Abstraction is about hiding the implementation details and showing only the essential features. Encapsulation is about "how" data is protected. Abstraction is about "what" is exposed to the user.\n\n### Q3: What are the advantages of encapsulation?\n\n**Answer:** The advantages include: data hiding (internal state is protected from unauthorized access), data validation (setter methods can validate input before assignment), flexibility (internal implementation can change without affecting external code), read-only or write-only properties (by providing only a getter or only a setter), and improved maintainability.\n\n### Q4: What is the `this` keyword in Java?\n\n**Answer:** `this` is a reference variable that refers to the current object. It is commonly used to distinguish between instance variables and parameters when they have the same name. For example, in `this.age = age;`, `this.age` refers to the instance variable and `age` refers to the method parameter.\n\n### Q5: Can a private variable be accessed from another object of the same class?\n\n**Answer:** Yes. The `private` modifier restricts access at the class level, not the object level. Any method within the same class can access private members of any object of that class. This is why in the code, `obj1.age = age;` works inside the `Human` class even though `age` is private.\n\n### Q6: What is data hiding?\n\n**Answer:** Data hiding is a software development technique where the internal data of an object is hidden from outside access. In Java, it is implemented by declaring variables as `private`. Data hiding is a subset of encapsulation. Encapsulation includes both data hiding and providing controlled access through methods.\n\n### Q7: What is the JavaBeans convention for getters and setters?\n\n**Answer:** According to the JavaBeans specification, for a property named `xyz`, the getter should be named `getXyz()` (or `isXyz()` for boolean properties) and the setter should be named `setXyz(Type value)`. The first letter of the property name is capitalized. This convention is used by many Java frameworks for reflection-based access.\n\n### Q8: Can you achieve encapsulation without getters and setters?\n\n**Answer:** Partially. You can declare all variables as `private` and use constructors to set values and methods to operate on the data without explicitly providing getters and setters. However, getters and setters are the standard and most widely used approach to provide controlled access to private data.\n'}},{slug:`static-keyword`,title:`The static keyword`,subject:`Java`,stage:`Classes and objects`,chapter:14,accent:`#f472b6`,summary:`The static keyword is one of the most important and frequently misunderstood concepts in Java. It fundamentally changes the behavior of a variable, method, or block by associating it with the class itself rather than with any specific object of that class.`,parts:{notes:`## Introduction

The \`static\` keyword is one of the most important and frequently misunderstood concepts in Java. It fundamentally changes the behavior of a variable, method, or block by associating it with the class itself rather than with any specific object of that class.

This program demonstrates how a static variable behaves differently from an instance variable. Specifically, it shows that changing a static variable affects all objects of the class because there is only one copy of a static variable shared among all instances.

---

## What is the Static Keyword

The \`static\` keyword in Java indicates that a member (variable, method, or block) belongs to the class itself, not to any specific instance (object) of the class. This means:

- A static variable has only one copy in memory, shared by all objects of the class.
- A static method can be called without creating an object of the class.
- A static block is executed once when the class is first loaded into memory.

Think of it like the Wi-Fi password in a hostel. Every room (object) has its own room number and furniture (instance variables), but the Wi-Fi password (static variable) is the same for every room. If the hostel changes the Wi-Fi password, it changes for everyone.

---

## Static Variables vs Instance Variables

| Feature                  | Static Variable                         | Instance Variable                      |
|--------------------------|----------------------------------------|----------------------------------------|
| Belongs to               | The class                              | The object                             |
| Number of copies         | One (shared by all objects)            | One per object                         |
| Memory allocation        | When class is loaded                   | When object is created                 |
| Accessed via             | ClassName.variableName (preferred)     | objectName.variableName                |
| Default value            | Same as instance variables (0, null, etc.) | Same (0, null, etc.)              |
| Also known as            | Class variable                         | Non-static variable                    |

---

## Understanding the Code

\`\`\`java
class Phone{
    static String name;
    int price;

    public void show(){
        System.out.println(name + " : " + price);
    }
}

public class StaticKeyword {
    public static void main(String[] args) {
        Phone obj1 = new Phone();
        Phone.name = "Samsung";
        obj1.price = 1_45_000;

        Phone obj2 = new Phone();
        Phone.name = "Apple";
        obj2.price = 90_000;

        Phone.name = "Phone";

        obj1.show();
        obj2.show();
    }
}
\`\`\`

---

## Line-by-Line Explanation

### \`static String name;\`

- Declares \`name\` as a static variable.
- There is only ONE copy of \`name\` in memory, regardless of how many \`Phone\` objects you create.
- It is accessed using \`Phone.name\`, not \`obj.name\` (though \`obj.name\` would also work, it is discouraged).

### \`int price;\`

- Declares \`price\` as an instance variable.
- Each \`Phone\` object gets its own separate copy of \`price\`.
- \`obj1.price\` and \`obj2.price\` are independent of each other.

### \`Phone.name = "Samsung";\`

- Sets the static variable \`name\` to \`"Samsung"\`.
- Since \`name\` is static, this change is visible through all objects.
- Accessed using the class name \`Phone.name\`, which is the correct way to access static members.

### \`obj1.price = 1_45_000;\`

- Sets the \`price\` of \`obj1\` to 145000.
- Note: The underscores (\`_\`) in \`1_45_000\` are numeric literals separators introduced in Java 7. They make large numbers easier to read but have no effect on the value.
- \`1_45_000\` is the same as \`145000\`.

### \`Phone.name = "Apple";\`

- Changes the static variable \`name\` to \`"Apple"\`.
- This overwrites the previous value \`"Samsung"\`.
- Since \`name\` is static, \`obj1\`'s \`name\` is also now \`"Apple"\` because there is only one shared copy.

### \`Phone.name = "Phone";\`

- Changes the static variable \`name\` again to \`"Phone"\`.
- Both \`obj1\` and \`obj2\` will now see \`name\` as \`"Phone"\`.

### \`obj1.show();\`

- Prints: \`Phone : 145000\`
- \`name\` is \`"Phone"\` (the latest static value), and \`obj1.price\` is \`145000\`.

### \`obj2.show();\`

- Prints: \`Phone : 90000\`
- \`name\` is still \`"Phone"\` (same static variable), and \`obj2.price\` is \`90000\`.

---

## Step-by-Step Execution Walkthrough

| Step | Code                          | Static \`name\` | \`obj1.price\` | \`obj2.price\` |
|------|-------------------------------|:-------------:|:------------:|:------------:|
| 1    | \`new Phone()\` (obj1)          | null          | 0            | -            |
| 2    | \`Phone.name = "Samsung"\`      | "Samsung"     | 0            | -            |
| 3    | \`obj1.price = 1_45_000\`       | "Samsung"     | 145000       | -            |
| 4    | \`new Phone()\` (obj2)          | "Samsung"     | 145000       | 0            |
| 5    | \`Phone.name = "Apple"\`        | "Apple"       | 145000       | 0            |
| 6    | \`obj2.price = 90_000\`         | "Apple"       | 145000       | 90000        |
| 7    | \`Phone.name = "Phone"\`        | "Phone"       | 145000       | 90000        |
| 8    | \`obj1.show()\`                 | Output: \`Phone : 145000\`                      |
| 9    | \`obj2.show()\`                 | Output: \`Phone : 90000\`                       |

Notice that both objects print \`"Phone"\` as the name, even though \`obj1\` was created when \`name\` was \`"Samsung"\` and \`obj2\` was created when \`name\` was \`"Apple"\`. This is because \`name\` is static and there is only one copy.

---

## How Static Variables Work in Memory

\`\`\`
Method Area (Class Data)
+----------------------------+
| Phone Class                |
| static name = "Phone"      |
+----------------------------+

Stack Memory              Heap Memory
+----------+              +-------------------+
| obj1  ---|------------->| Phone Object      |
+----------+              | price = 145000    |
                           +-------------------+
+----------+
| obj2  ---|------------->+-------------------+
+----------+              | Phone Object      |
                           | price = 90000     |
                           +-------------------+
\`\`\`

- The static variable \`name\` is stored in the Method Area (or Class Data area), NOT inside any object.
- Each object only stores its own instance variables (\`price\`).
- When any code reads \`name\`, it goes to the one shared copy in the Method Area.

---

## Static Methods

A static method belongs to the class and can be called without creating an object.

\`\`\`java
class MathHelper {
    public static int square(int n) {
        return n * n;
    }
}

int result = MathHelper.square(5);
\`\`\`

### Rules for Static Methods

1. A static method can access static variables and other static methods directly.
2. A static method CANNOT access instance variables or instance methods directly (because there is no object context).
3. A static method CANNOT use the \`this\` or \`super\` keyword.

\`\`\`java
class Example {
    static int x = 10;
    int y = 20;

    public static void staticMethod() {
        System.out.println(x);     // OK: accessing static variable
        // System.out.println(y);  // ERROR: cannot access instance variable
        // System.out.println(this.x); // ERROR: cannot use 'this' in static context
    }
}
\`\`\`

---

## Static Block

A static block is a block of code that runs once when the class is loaded into memory, before any objects are created and before the \`main\` method executes.

\`\`\`java
class Demo {
    static {
        System.out.println("Static block executed");
    }

    Demo() {
        System.out.println("Constructor executed");
    }
}
\`\`\`

If you create two objects of \`Demo\`, the output will be:

\`\`\`
Static block executed
Constructor executed
Constructor executed
\`\`\`

The static block runs only once, while the constructor runs for every new object.

---

## When to Use Static

| Use Case                            | Example                              |
|--------------------------------------|--------------------------------------|
| Constants shared across all objects   | \`static final double PI = 3.14159\`  |
| Utility methods                       | \`Math.sqrt()\`, \`Arrays.sort()\`      |
| Counters or shared data              | Counting number of objects created   |
| Factory methods                       | \`Integer.parseInt("123")\`           |
| Main method                          | \`public static void main(String[])\` |

---

## How to Compile and Run

\`\`\`bash
javac StaticKeyword.java
java StaticKeyword
\`\`\`

### Expected Output

\`\`\`
Phone : 145000
Phone : 90000
\`\`\`

---

## Common Mistakes

### Mistake 1: Accessing instance variables from a static method

\`\`\`java
public static void main(String[] args) {
    System.out.println(price);  // ERROR: non-static variable cannot be referenced from a static context
}
\`\`\`

### Mistake 2: Using \`this\` in a static method

\`\`\`java
public static void display() {
    System.out.println(this.name);  // ERROR: 'this' cannot be used in a static context
}
\`\`\`

### Mistake 3: Thinking static means constant

\`static\` means class-level. It does NOT mean the value cannot change. For a constant, you need \`static final\`:

\`\`\`java
static final double PI = 3.14159;
static int count = 0;
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`StaticKeyword.java\`

\`Phone\` holds one of each kind of field: \`name\` is **static**, so it belongs to
the class and every object shares the single copy, while the price is an
**instance** field that each object owns separately.

The output is the demonstration. Two phones are created with different names,
then \`Phone.name\` is assigned once more â€” and because that one variable is
shared, *both* objects now report the new value. Their prices, being instance
fields, stay different.

\`\`\`java StaticKeyword.java @run-static-keyword-static-keyword
class Phone{
    static String name;
    int price;

    public void show(){
        System.out.println(name + " : " + price);
    }
}

public class StaticKeyword {
    public static void main(String[] args) {
        Phone obj1 = new Phone();
        Phone.name = "Samsung";
        obj1.price = 1_45_000;

        Phone obj2 = new Phone();
        Phone.name = "Apple";
        obj2.price = 90_000;

        Phone.name = "Phone";

        obj1.show();
        obj2.show();
    }
}

\`\`\`

\`\`\`output @run-static-keyword-static-keyword
Phone : 145000
Phone : 90000
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- \`static\` means it belongs to the **class**, not to an instance. One copy,
  shared by every object.
- A static method cannot use \`this\` or touch instance fields directly.
- A static block runs once, when the class is first loaded — before any
  constructor.
- \`main\` is static because the JVM has to call it before any object exists.
`,interview:`## Interview questions

### Q1: What is the static keyword in Java?

**Answer:** The \`static\` keyword is used to create class-level members. A static variable or method belongs to the class rather than to any specific object. Static variables have one shared copy across all instances. Static methods can be called without creating an object.

### Q2: What is the difference between a static variable and an instance variable?

**Answer:** A static variable belongs to the class and has only one copy shared by all objects. An instance variable belongs to individual objects, and each object has its own copy. Static variables are stored in the Method Area, while instance variables are stored on the heap as part of the object.

### Q3: Can a static method access non-static (instance) variables?

**Answer:** No. A static method cannot directly access instance variables or instance methods because there is no specific object context. To access instance members from a static method, you must first create an object and then use that object to access them.

### Q4: Why is the \`main\` method static in Java?

**Answer:** The \`main\` method is static because the JVM needs to call it without creating an object of the class. When the program starts, no objects exist yet. If \`main\` were not static, the JVM would need to create an object first, but it would not know which constructor to call or what arguments to pass.

### Q5: What is a static block and when is it executed?

**Answer:** A static block is a block of code that runs automatically when the class is first loaded into memory. It runs before the \`main\` method and before any constructors. It is used for static initialization, such as setting up complex static variables or loading configuration.

### Q6: Can we access static members using an object reference?

**Answer:** Yes, technically you can access static members using an object reference (e.g., \`obj1.name\`), but it is strongly discouraged. It creates confusion because it looks like an instance member access. The correct and recommended way is to use the class name (e.g., \`Phone.name\`). Most IDEs will warn you if you access static members through an object.

### Q7: Can you override a static method in Java?

**Answer:** No. Static methods cannot be overridden because they belong to the class, not to objects. If a subclass defines a static method with the same signature, it is called method hiding, not overriding. The method that gets called is determined by the reference type at compile time, not the object type at runtime.

### Q8: What are the numeric literal separators in Java?

**Answer:** Since Java 7, you can use underscores (\`_\`) as separators in numeric literals to improve readability. For example, \`1_00_000\` is the same as \`100000\`, and \`3.14_15_92\` is the same as \`3.141592\`. The underscores are ignored by the compiler and do not affect the value.
`}},{slug:`inheritance`,title:`Inheritance`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:15,accent:`#facc15`,summary:`Inheritance is one of the four pillars of Object-Oriented Programming (OOP). It allows a class to inherit properties and methods from another class. The class that inherits is called the child class (or subclass, or derived class), and the class being inherited from is called the parent class (or superclass, or base class).`,parts:{notes:`## Introduction

Inheritance is one of the four pillars of Object-Oriented Programming (OOP). It allows a class to inherit properties and methods from another class. The class that inherits is called the child class (or subclass, or derived class), and the class being inherited from is called the parent class (or superclass, or base class).

This program demonstrates multi-level inheritance (A -> B -> C), constructor chaining, and method overriding.

---

## What is Inheritance

Inheritance is a mechanism where a new class acquires the properties and behaviors of an existing class. The child class can use all the non-private members of the parent class without rewriting them.

Think of it like a family. A child inherits certain traits from their parents (eye color, height potential, etc.). The child has all the parent's traits plus potentially their own unique traits. In programming, the child class has all the parent's methods and variables, plus its own additions.

**Syntax:**

\`\`\`java
class ParentClass {
}

class ChildClass extends ParentClass {
}
\`\`\`

---

## Why Do We Need Inheritance

### Without Inheritance (Code Duplication)

\`\`\`java
class Car {
    String brand;
    int speed;
    public void start() { System.out.println("Starting..."); }
    public void stop() { System.out.println("Stopping..."); }
}

class ElectricCar {
    String brand;
    int speed;
    int batteryLevel;
    public void start() { System.out.println("Starting..."); }
    public void stop() { System.out.println("Stopping..."); }
    public void charge() { System.out.println("Charging..."); }
}
\`\`\`

### With Inheritance (Code Reuse)

\`\`\`java
class Car {
    String brand;
    int speed;
    public void start() { System.out.println("Starting..."); }
    public void stop() { System.out.println("Stopping..."); }
}

class ElectricCar extends Car {
    int batteryLevel;
    public void charge() { System.out.println("Charging..."); }
}
\`\`\`

### Benefits

| Benefit            | Description                                                   |
|--------------------|---------------------------------------------------------------|
| Code Reusability   | Write once in the parent, use in all children                 |
| Maintainability    | Fix a bug in one place, it is fixed for all subclasses        |
| Extensibility      | Add new features by creating subclasses without modifying parent |
| Polymorphism       | Treat objects of different subclasses uniformly through the parent type |

---

## Types of Inheritance in Java

### 1. Single Inheritance

One child class inherits from one parent class.

\`\`\`java
class A { }
class B extends A { }
\`\`\`

### 2. Multi-Level Inheritance

A child class inherits from a parent, which itself inherits from another parent.

\`\`\`java
class A { }
class B extends A { }
class C extends B { }
\`\`\`

### 3. Hierarchical Inheritance

Multiple child classes inherit from the same parent class.

\`\`\`java
class A { }
class B extends A { }
class C extends A { }
\`\`\`

### 4. Multiple Inheritance (NOT supported with classes in Java)

A class inheriting from more than one parent class. Java does NOT support this to avoid the Diamond Problem. However, multiple inheritance is possible through interfaces.

\`\`\`java
class C extends A, B { }

interface A { }
interface B { }
class C implements A, B { }  // OK
\`\`\`

---

## Understanding the Code

\`\`\`java
class A {
    A() {
        System.out.println("A");
    }

    public void show() {
        System.out.println("Show Method : A");
    }
}

class C extends B {
    C() {
        System.out.println("C");
    }
}

class B extends A {
    B() {
        System.out.println("B");
    }

    public void show() {
        System.out.println("Show Method : B");
    }
}

public class Inheritance {
    public static void main(String[] args) {
        C obj = new C();
        obj.show();
    }
}
\`\`\`

### Inheritance Hierarchy

\`\`\`
    A (Grandparent)
    |
    B (Parent)      -- inherits from A, overrides show()
    |
    C (Child)       -- inherits from B (and transitively from A)
\`\`\`

---

## Line-by-Line Explanation

### Class A

\`\`\`java
class A {
    A() {
        System.out.println("A");
    }

    public void show() {
        System.out.println("Show Method : A");
    }
}
\`\`\`

- Class \`A\` is the base class (the topmost class in our hierarchy).
- It has a default constructor that prints \`"A"\`.
- It has a \`show()\` method that prints \`"Show Method : A"\`.

### Class B

\`\`\`java
class B extends A {
    B() {
        System.out.println("B");
    }

    public void show() {
        System.out.println("Show Method : B");
    }
}
\`\`\`

- Class \`B\` extends \`A\`, meaning \`B\` inherits everything from \`A\`.
- \`B\` has its own constructor that prints \`"B"\`.
- \`B\` overrides the \`show()\` method. When \`show()\` is called on a \`B\` object, it prints \`"Show Method : B"\` instead of \`"Show Method : A"\`.

### Class C

\`\`\`java
class C extends B {
    C() {
        System.out.println("C");
    }
}
\`\`\`

- Class \`C\` extends \`B\`, meaning \`C\` inherits from \`B\` (and transitively from \`A\`).
- \`C\` has its own constructor that prints \`"C"\`.
- \`C\` does NOT override the \`show()\` method. So when \`show()\` is called on a \`C\` object, it uses the version from \`B\` (the nearest parent that has the method).

### Main Method

\`\`\`java
C obj = new C();
obj.show();
\`\`\`

- \`new C()\` creates a new object of type \`C\`, which triggers constructor chaining.
- \`obj.show()\` calls the \`show()\` method. Since \`C\` does not have its own \`show()\`, Java looks up the hierarchy and finds it in \`B\`.

---

## Step-by-Step Execution Walkthrough

When \`new C()\` is executed:

| Step | What Happens                                           | Output |
|------|--------------------------------------------------------|--------|
| 1    | \`C()\` constructor is called                            | -      |
| 2    | Before executing \`C()\`'s body, Java calls \`B()\` (parent constructor) | - |
| 3    | Before executing \`B()\`'s body, Java calls \`A()\` (parent constructor) | - |
| 4    | Before executing \`A()\`'s body, Java calls \`Object()\` (every class implicitly extends Object) | - |
| 5    | \`Object()\` completes (does nothing visible)            | -      |
| 6    | \`A()\` body executes: \`System.out.println("A")\`         | A      |
| 7    | \`B()\` body executes: \`System.out.println("B")\`         | B      |
| 8    | \`C()\` body executes: \`System.out.println("C")\`         | C      |
| 9    | \`obj.show()\` -- \`C\` has no \`show()\`, looks at \`B\`      | Show Method : B |

**Key Insight:** Constructors are called from top to bottom (parent first, child last). This is called constructor chaining.

---

## Constructor Chaining in Inheritance

When you create an object of a subclass, Java automatically calls the constructors of all parent classes in order, starting from the topmost parent (Object) down to the current class.

This happens because every constructor implicitly has a \`super()\` call as its first statement (unless you explicitly call another constructor).

\`\`\`java
class A {
    A() {
        // super();  <-- invisible call to Object()
        System.out.println("A");
    }
}

class B extends A {
    B() {
        // super();  <-- invisible call to A()
        System.out.println("B");
    }
}
\`\`\`

---

## Method Overriding

Method overriding occurs when a subclass provides its own implementation of a method that is already defined in its parent class.

### Rules for Method Overriding

1. The method in the child class must have the exact same name, return type, and parameter list as the method in the parent class.
2. The access modifier of the overriding method must be the same or less restrictive.
3. The overriding method cannot throw broader checked exceptions.
4. Static methods cannot be overridden (they can only be hidden).
5. Constructors cannot be overridden.
6. The \`@Override\` annotation is recommended (though optional) to catch errors at compile time.

### How Java Decides Which Method to Call

When you call \`obj.show()\` on a \`C\` object:

1. Java first looks in class \`C\`. If \`show()\` exists in \`C\`, it calls that version.
2. If not found in \`C\`, Java looks in the parent class \`B\`. If \`show()\` exists in \`B\`, it calls that version.
3. If not found in \`B\`, Java looks in \`A\`, and so on up the hierarchy.

In our code, \`C\` does not have \`show()\`, so Java finds and calls \`B\`'s \`show()\`.

---

## The \`extends\` Keyword

The \`extends\` keyword is used to create a child class that inherits from a parent class.

\`\`\`java
class Child extends Parent {
}
\`\`\`

What is inherited:

- All \`public\` and \`protected\` members (variables and methods)
- All \`default\` (package-private) members if the child is in the same package
- Constructors are NOT inherited, but they are called through constructor chaining

What is NOT inherited:

- \`private\` members (they exist in the parent but cannot be accessed directly from the child)
- Constructors

---

## How to Compile and Run

\`\`\`bash
javac Inheritance.java
java Inheritance
\`\`\`

### Expected Output

\`\`\`
A
B
C
Show Method : B
\`\`\`

---

## Common Mistakes

### Mistake 1: Thinking constructors are inherited

\`\`\`java
class A {
    A(int x) { }
}

class B extends A {
}
\`\`\`

You must explicitly define constructors in the child class and call the appropriate parent constructor using \`super()\`.

### Mistake 2: Thinking private members are not inherited

Private members ARE inherited (they exist in the child object's memory), but they cannot be accessed directly from the child class. They can only be accessed through public methods (getters/setters) defined in the parent class.

### Mistake 3: Forgetting that constructor chaining calls parent constructors first

The parent constructor always executes before the child constructor. You cannot skip it.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Inheritance.java\`

Three classes in a chain: \`A\`, then \`B extends A\`, then \`C extends B\`. That is
**multi-level** inheritance, and the output shows what it costs at construction
time.

Creating a single \`C\` prints \`A\`, \`B\` and \`C\` in that order, because every
constructor calls its parent's before running its own body. Construction always
starts at the top of the hierarchy and works down.

The final line is overriding at work: \`B\` supplies its own \`show()\`, and that is
the version that runs.

\`\`\`java Inheritance.java @run-inheritance-inheritance
class A {
    A() {
        System.out.println("A");
    }

    public void show() {
        System.out.println("Show Method : A");
    }
}

class C extends B {
    C() {
        System.out.println("C");
    }

    // public void show() {
    // System.out.println("Show Method : C");
    // }
}

class B extends A {
    B() {
        System.out.println("B");
    }

    public void show() {
        System.out.println("Show Method : B");
    }
}

public class Inheritance {
    public static void main(String[] args) {
        C obj = new C();
        obj.show();
    }
}

\`\`\`

\`\`\`output @run-inheritance-inheritance
A
B
C
Show Method : B
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- \`extends\` — a subclass gets the superclass's fields and methods.
- Java has **no multiple inheritance of classes**. Interfaces are how you get the
  effect without the ambiguity.
- Constructors are **not** inherited, and a subclass constructor always calls a
  superclass constructor first — implicitly \`super()\` if you write nothing.
- Types available: single, multilevel, hierarchical. Multiple and hybrid are
  class-level only via interfaces.
`,interview:`## Interview questions

### Q1: What is inheritance in Java?

**Answer:** Inheritance is an OOP mechanism where a child class acquires the properties and methods of a parent class using the \`extends\` keyword. It promotes code reusability, maintainability, and forms the basis for polymorphism.

### Q2: What types of inheritance does Java support?

**Answer:** Java supports single inheritance, multi-level inheritance, and hierarchical inheritance through classes. Java does NOT support multiple inheritance through classes (to avoid the Diamond Problem), but it supports multiple inheritance through interfaces.

### Q3: What is the Diamond Problem?

**Answer:** The Diamond Problem occurs in multiple inheritance when a class inherits from two classes that both inherit from a common base class. If both parent classes have a method with the same signature, the compiler cannot determine which version to use. Java avoids this by not allowing multiple inheritance with classes.

### Q4: What is method overriding?

**Answer:** Method overriding is when a subclass provides a specific implementation for a method that is already defined in its parent class. The overriding method must have the same name, return type, and parameter list. At runtime, Java calls the overridden version based on the actual object type, not the reference type.

### Q5: What is the difference between method overloading and method overriding?

**Answer:**

| Feature          | Overloading                        | Overriding                          |
|------------------|------------------------------------|-------------------------------------|
| Definition       | Same name, different parameters    | Same name, same parameters          |
| Occurs in        | Same class                         | Parent and child classes            |
| Binding          | Compile-time (static binding)      | Runtime (dynamic binding)           |
| Return type      | Can be different                   | Must be same or covariant           |
| Access modifier  | Can be different                   | Cannot be more restrictive          |

### Q6: What is constructor chaining?

**Answer:** Constructor chaining is the process where constructors call other constructors in a chain. In inheritance, when a child object is created, the parent class constructor is called first (via implicit or explicit \`super()\` call), then the child class constructor executes. This ensures the parent part of the object is properly initialized before the child part.

### Q7: Can a child class access private members of the parent class?

**Answer:** No, a child class cannot directly access private members. However, private members do exist in the child object. They can be accessed indirectly through public or protected getter/setter methods provided by the parent class.

### Q8: What is the \`Object\` class in Java?

**Answer:** The \`Object\` class (in \`java.lang\` package) is the root of the class hierarchy. Every class in Java directly or indirectly extends \`Object\`. It provides fundamental methods like \`toString()\`, \`equals()\`, \`hashCode()\`, \`getClass()\`, \`clone()\`, \`wait()\`, \`notify()\`, and \`finalize()\`.

### Q9: Can you extend multiple classes in Java?

**Answer:** No. A class can extend only one class in Java. This is a design decision to avoid the Diamond Problem. If you need to inherit behavior from multiple sources, use interfaces.

### Q10: What happens if the parent class does not have a default (no-argument) constructor?

**Answer:** If the parent class only has parameterized constructors (no default constructor), the child class must explicitly call one of those parameterized constructors using \`super(arguments)\` in its constructor. If the child does not do this, the compiler will report an error because the implicit \`super()\` call cannot find a matching no-argument constructor in the parent.
`}},{slug:`super-and-this`,title:`super and this`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:16,accent:`#22d3ee`,summary:`The this and super keywords are two of the most important keywords in Java, especially when working with constructors and inheritance. They control how constructors call other constructors, how variables are resolved when there are naming conflicts, and how parent class members are accessed from a child class.`,parts:{notes:`## Introduction

The \`this\` and \`super\` keywords are two of the most important keywords in Java, especially when working with constructors and inheritance. They control how constructors call other constructors, how variables are resolved when there are naming conflicts, and how parent class members are accessed from a child class.

This program demonstrates constructor chaining using both \`this()\` and \`super()\`, showing the exact order in which constructors are called when an object is created.

---

## What is the \`this\` Keyword

The \`this\` keyword is a reference to the current object -- the object on which a method or constructor is being invoked. It always points to the instance of the class in which it is used.

### Simple Analogy

Think of \`this\` as the word "I" in English. When a person says "I am going to the store," "I" refers to the person speaking. Similarly, when an object uses \`this\`, it refers to itself.

### Basic Usage

\`\`\`java
class Student {
    String name;

    Student(String name) {
        this.name = name;
    }
}
\`\`\`

Without \`this\`, both \`name\` references on the left and right would refer to the parameter, and the instance variable would never be set.

---

## What is the \`super\` Keyword

The \`super\` keyword is a reference to the parent (superclass) object. It is used to access members of the parent class from the child class, especially when the child class has overridden or shadowed those members.

### Simple Analogy

Think of \`super\` as saying "my parent." When a child wants something from the parent's house, they say "let me get it from my parent." Similarly, when a child class wants to use a parent class member, it uses \`super\`.

### Basic Usage

\`\`\`java
class Animal {
    String type = "Animal";
}

class Dog extends Animal {
    String type = "Dog";

    void printTypes() {
        System.out.println(this.type);
        System.out.println(super.type);
    }
}
\`\`\`

---

## Difference Between \`this\` and \`super\`

| Feature                   | \`this\`                                    | \`super\`                                    |
|---------------------------|-------------------------------------------|--------------------------------------------|
| Refers to                 | Current class object                      | Parent class object                        |
| Used for                  | Accessing current class members           | Accessing parent class members             |
| Constructor call          | \`this()\` calls another constructor of the same class | \`super()\` calls a constructor of the parent class |
| Can be used in            | Any non-static method or constructor      | Any non-static method or constructor       |
| Must be first statement   | Yes (when used as constructor call)       | Yes (when used as constructor call)        |
| Implicit behavior         | Not added automatically                   | \`super()\` is implicitly added to every constructor if no \`this()\` or \`super()\` is present |

---

## Understanding the Code

\`\`\`java

class A {
    public A() {
        super();
        System.out.println("A : Default Constructor");
    }

    public A(int num) {
        this();
        System.out.println("A : Parameterized Constructor");
    }
}

class B extends A {
    public B() {
        super(6);
        System.out.println("B : Default Constructor");
    }

    public B(int num) {
        super();
        System.out.println("B : Parameterized Constructor");
    }
}

public class SuperAndThis {
    public static void main(String[] args) {
        B obj = new B();
    }
}
\`\`\`

### Class Hierarchy

\`\`\`
    Object (root of all classes)
      |
      A (has two constructors: default and parameterized)
      |
      B (has two constructors: default and parameterized)
\`\`\`

---

## Line-by-Line Explanation

### Class A - Default Constructor

\`\`\`java
public A() {
    super();
    System.out.println("A : Default Constructor");
}
\`\`\`

- \`super()\` calls the default constructor of the \`Object\` class (since \`A\` implicitly extends \`Object\`).
- Even if you do not write \`super()\`, the compiler adds it automatically.
- After the parent constructor completes, it prints \`"A : Default Constructor"\`.

### Class A - Parameterized Constructor

\`\`\`java
public A(int num) {
    this();
    System.out.println("A : Parameterized Constructor");
}
\`\`\`

- \`this()\` calls the default constructor of class \`A\` (the one defined above).
- This is constructor chaining within the same class.
- After the default constructor completes (which first calls \`Object()\` then prints "A : Default Constructor"), this constructor then prints \`"A : Parameterized Constructor"\`.

### Class B - Default Constructor

\`\`\`java
public B() {
    super(6);
    System.out.println("B : Default Constructor");
}
\`\`\`

- \`super(6)\` explicitly calls the parameterized constructor \`A(int num)\` of the parent class, passing \`6\` as the argument.
- This means \`A(int num)\` will execute, which internally calls \`this()\` (A's default constructor), creating a chain.
- After the parent's constructor chain completes, it prints \`"B : Default Constructor"\`.

### Class B - Parameterized Constructor

\`\`\`java
public B(int num) {
    super();
    System.out.println("B : Parameterized Constructor");
}
\`\`\`

- \`super()\` calls the default constructor \`A()\` of the parent class.
- This constructor is not called in the current program because we only create \`new B()\` (the default constructor of B).

---

## Step-by-Step Execution Walkthrough

When \`new B()\` is executed, the following chain of constructor calls occurs:

| Step | Constructor Called         | Why                                       | Output                         |
|------|---------------------------|-------------------------------------------|--------------------------------|
| 1    | \`B()\`                     | We wrote \`new B()\`                       | (not yet, body runs after super) |
| 2    | \`A(int num)\` with num=6  | \`B()\` has \`super(6)\`                     | (not yet, body runs after this) |
| 3    | \`A()\`                     | \`A(int num)\` has \`this()\`                | (not yet, body runs after super) |
| 4    | \`Object()\`                | \`A()\` has \`super()\`                      | (nothing visible)              |
| 5    | \`A()\` body executes       | Back to step 3                           | A : Default Constructor        |
| 6    | \`A(int num)\` body executes| Back to step 2                           | A : Parameterized Constructor  |
| 7    | \`B()\` body executes       | Back to step 1                           | B : Default Constructor        |

### Visual Call Stack

\`\`\`
new B()
  |
  +-- B() calls super(6)
        |
        +-- A(int num) calls this()
              |
              +-- A() calls super()
                    |
                    +-- Object() [completes]
                    |
              A() prints: "A : Default Constructor"
              |
        A(int num) prints: "A : Parameterized Constructor"
        |
  B() prints: "B : Default Constructor"
\`\`\`

---

## Uses of \`this\` Keyword

### 1. Distinguishing instance variables from parameters

\`\`\`java
class Student {
    String name;

    Student(String name) {
        this.name = name;
    }
}
\`\`\`

### 2. Calling another constructor of the same class

\`\`\`java
class Student {
    String name;
    int age;

    Student() {
        this("Unknown", 0);
    }

    Student(String name, int age) {
        this.name = name;
        this.age = age;
    }
}
\`\`\`

### 3. Passing the current object as an argument

\`\`\`java
class Printer {
    void print(Student s) {
        System.out.println(s.name);
    }
}

class Student {
    String name = "Aditya";

    void display(Printer p) {
        p.print(this);
    }
}
\`\`\`

### 4. Returning the current object

\`\`\`java
class Builder {
    int value;

    Builder setValue(int value) {
        this.value = value;
        return this;
    }
}

\`\`\`

---

## Uses of \`super\` Keyword

### 1. Calling the parent class constructor

\`\`\`java
class Animal {
    Animal(String type) {
        System.out.println("Animal: " + type);
    }
}

class Dog extends Animal {
    Dog() {
        super("Dog");
    }
}
\`\`\`

### 2. Accessing parent class methods (when overridden)

\`\`\`java
class Animal {
    void sound() {
        System.out.println("Some sound");
    }
}

class Dog extends Animal {
    void sound() {
        super.sound();
        System.out.println("Bark");
    }
}
\`\`\`

### 3. Accessing parent class variables (when shadowed)

\`\`\`java
class Animal {
    String type = "Animal";
}

class Dog extends Animal {
    String type = "Dog";

    void print() {
        System.out.println(this.type);
        System.out.println(super.type);
    }
}
\`\`\`

---

## Rules and Restrictions

### Rule 1: \`this()\` and \`super()\` must be the first statement in a constructor

\`\`\`java
class B extends A {
    B() {
        System.out.println("Hello");  // ERROR if super() or this() follows
        super();
    }
}
\`\`\`

### Rule 2: You cannot use both \`this()\` and \`super()\` in the same constructor

Since both must be the first statement, you can only use one of them.

\`\`\`java
class B extends A {
    B() {
        super();
        this();  // ERROR: call to this must be first statement
    }
}
\`\`\`

### Rule 3: \`super()\` is added implicitly if no \`this()\` or \`super()\` is present

If you do not write \`this()\` or \`super()\` in a constructor, the compiler automatically inserts \`super()\` (a call to the parent's no-argument constructor).

### Rule 4: \`this\` and \`super\` cannot be used in static methods

\`\`\`java
public static void main(String[] args) {
    // System.out.println(this);   // ERROR: 'this' in static context
    // System.out.println(super.x); // ERROR: 'super' in static context
}
\`\`\`

### Rule 5: Recursive constructor calls are not allowed

\`\`\`java
class A {
    A() {
        this();  // ERROR: recursive constructor invocation
    }
}
\`\`\`

---

## How to Compile and Run

The file declares no package declaration, so compile and run from the
parent directory using the package path:

\`\`\`bash
javac SuperAndThis.java
java SuperAndThis
\`\`\`

### Expected Output

\`\`\`
A : Default Constructor
A : Parameterized Constructor
B : Default Constructor
\`\`\`

---

## Common Mistakes

### Mistake 1: Forgetting that \`super()\` is added automatically

\`\`\`java
class A {
    A(int x) { }
}

class B extends A {
    B() {
    }
}
\`\`\`

Solution: Explicitly call \`super(someValue)\` or add a default constructor to the parent class.

### Mistake 2: Using \`this()\` and \`super()\` in the same constructor

This is not allowed. You can only use one of them.

### Mistake 3: Creating infinite constructor recursion

\`\`\`java
class A {
    A() {
        this(5);
    }
    A(int x) {
        this();  // ERROR: recursive constructor invocation
    }
}
\`\`\`

Java detects direct circular constructor calls at compile time.

### Mistake 4: Confusing \`this\` (reference) with \`this()\` (constructor call)

- \`this\` (without parentheses) is a reference to the current object. It can be used anywhere in non-static methods.
- \`this()\` (with parentheses) is a constructor call. It can ONLY be used as the first statement inside a constructor.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`SuperAndThis.java\`

Both keywords used as **constructor calls**, which is the part that confuses
people. \`super(...)\` calls a constructor of the parent class; \`this(...)\` calls
another constructor of the *same* class. Either one must be the first statement
in the constructor, so you can never use both.

The thing to notice: \`super()\` is not optional. If you do not write a call
yourself, Java inserts a no-argument \`super()\` for you â€” which is why a parent
constructor always runs even when nothing in the child mentions it.

Follow the chain the output traces: \`new B()\` calls \`super(6)\`, landing in
\`A(int)\`, which itself calls \`this()\` to reach \`A()\`. So \`A\`'s default
constructor finishes first, then \`A(int)\`, then \`B()\`.

\`\`\`java SuperAndThis.java @run-super-and-this-super-and-this

class A {
    public A() {
        super();
        System.out.println("A : Default Constructor");
    }

    public A(int num) {
        // super();
        this();
        System.out.println("A : Parameterized Constructor");
    }
}

class B extends A {
    public B() {
        super(6);
        System.out.println("B : Default Constructor");
    }

    public B(int num) {
        super();
        System.out.println("B : Parameterized Constructor");
    }
}

public class SuperAndThis {
    public static void main(String[] args) {
        B obj = new B();
    }
}

\`\`\`

\`\`\`output @run-super-and-this-super-and-this
A : Default Constructor
A : Parameterized Constructor
B : Default Constructor
\`\`\`
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- `this` = this object. `super` = the parent part of this object.\n- `this()` and `super()` must both be the **first statement** in a constructor,\n  so you can use only one of them.\n- `this` disambiguates a parameter shadowing a field — `this.name = name`.\n- `super.method()` calls the parent's version from an override.\n- Neither exists in a `static` context.\n",interview:"## Interview questions\n\n### Q1: What is the `this` keyword in Java?\n\n**Answer:** `this` is a reference variable that refers to the current object. It is used to access instance variables when they are shadowed by parameters, to call other constructors of the same class using `this()`, to pass the current object as an argument, and to return the current object from a method.\n\n### Q2: What is the `super` keyword in Java?\n\n**Answer:** `super` is a reference variable that refers to the immediate parent class object. It is used to call the parent class constructor using `super()`, to access parent class methods that have been overridden, and to access parent class variables that have been shadowed by child class variables.\n\n### Q3: Why must `super()` or `this()` be the first statement in a constructor?\n\n**Answer:** Because the parent class must be fully initialized before the child class can use any inherited members. If the parent constructor were called in the middle or at the end, the child class might try to use uninitialized inherited members, leading to unpredictable behavior. Java enforces this rule at the compiler level.\n\n### Q4: What happens if you do not write `super()` in a constructor?\n\n**Answer:** The compiler automatically inserts `super()` (a call to the parent's no-argument constructor) as the first statement. If the parent class does not have a no-argument constructor, the code will not compile, and you must explicitly call a parameterized parent constructor using `super(arguments)`.\n\n### Q5: Can `this()` and `super()` be used together in the same constructor?\n\n**Answer:** No. Both `this()` and `super()` must be the first statement in a constructor, and only one statement can be first. Therefore, you can use either `this()` or `super()`, but not both in the same constructor.\n\n### Q6: What is constructor chaining?\n\n**Answer:** Constructor chaining is the process of one constructor calling another constructor. It can happen within the same class using `this()` or across the inheritance hierarchy using `super()`. The purpose is to avoid code duplication and ensure proper initialization. Every object creation in Java ultimately chains up to the `Object` class constructor.\n\n### Q7: Can we use `this` and `super` inside a static method?\n\n**Answer:** No. `this` and `super` refer to object instances, but static methods belong to the class and do not have an object context. Using `this` or `super` in a static method will result in a compilation error: \"non-static variable this/super cannot be referenced from a static context.\"\n\n### Q8: What is the difference between `this()` and `this`?\n\n**Answer:** `this` (without parentheses) is a reference to the current object and can be used in any non-static context. `this()` (with parentheses) is a constructor call that invokes another constructor of the same class and can only be used as the first statement inside a constructor.\n\n### Q9: In the code example, why does `A : Default Constructor` appear before `A : Parameterized Constructor`?\n\n**Answer:** Because the parameterized constructor `A(int num)` uses `this()` to call the default constructor `A()` first. The call stack is: `B()` calls `super(6)` which invokes `A(int)`, then `A(int)` calls `this()` which invokes `A()`, then `A()` calls `super()` which invokes `Object()`. The execution then unwinds: `A()` prints first, then `A(int)` prints, then `B()` prints. Constructors always execute from parent to child.\n\n### Q10: What is the `Object` class and how is it related to `super()`?\n\n**Answer:** The `Object` class is the root superclass of every class in Java. Even if a class does not explicitly extend any class, it implicitly extends `Object`. When `super()` is called in the topmost user-defined class, it calls `Object()`'s constructor, which initializes the base object infrastructure (hash code, class metadata, etc.).\n"}},{slug:`polymorphism`,title:`Polymorphism`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:17,accent:`#fb7185`,summary:`Polymorphism is one of the four pillars of Object-Oriented Programming (OOP). The word "polymorphism" comes from Greek and means "many forms". In Java, polymorphism allows a single action to behave differently depending on the object that performs it.`,parts:{notes:`## Introduction

Polymorphism is one of the four pillars of Object-Oriented Programming (OOP). The word "polymorphism" comes from Greek and means "many forms". In Java, polymorphism allows a single action to behave differently depending on the object that performs it.

This program demonstrates runtime polymorphism, where a parent class reference (\`Animal\`) points to objects of different child classes (\`Dog\`, \`Fish\`, \`Bird\`), and the overridden \`move()\` method that runs is decided at runtime based on the actual object type.

---

## What is Polymorphism

Polymorphism is the ability of an object, method, or reference to take on many forms. The same method call can produce different behavior depending on the actual object it is invoked on.

Think of the word "draw". An artist draws a painting, a gunslinger draws a pistol, and a gambler draws a card. The same word means different actions depending on who performs it. Similarly, calling \`move()\` on different animals produces different movement.

**Syntax:**

\`\`\`java
class Parent {
    public void action() {
        System.out.println("Parent action");
    }
}

class Child extends Parent {
    @Override
    public void action() {
        System.out.println("Child action");
    }
}

Parent p = new Child();
p.action();
\`\`\`

---

## Why Do We Need Polymorphism

### Without Polymorphism (Rigid Code)

\`\`\`java
void makeItMove(Dog dog)  { dog.move(); }
void makeItMove(Fish fish) { fish.move(); }
void makeItMove(Bird bird) { bird.move(); }
\`\`\`

### With Polymorphism (Flexible Code)

\`\`\`java
void makeItMove(Animal animal) {
    animal.move();  // Works for Dog, Fish, Bird, or any future Animal
}
\`\`\`

### Benefits

| Benefit            | Description                                                       |
|--------------------|-------------------------------------------------------------------|
| Code Reusability   | One method can handle objects of many related types               |
| Extensibility      | Add new subclasses without changing existing code                 |
| Maintainability    | Logic lives in one place instead of being duplicated per type     |
| Flexibility        | Program to the parent type, decide the actual behavior at runtime  |

---

## Types of Polymorphism in Java

### 1. Compile-Time Polymorphism (Static)

Resolved by the compiler. Achieved through **method overloading** (same method name, different parameter lists).

\`\`\`
Same method name -> Different parameters -> Decided at compile time
\`\`\`

### 2. Runtime Polymorphism (Dynamic)

Resolved during execution (the focus of this chapter's code). Achieved through **method overriding** combined with upcasting.

\`\`\`
Parent reference -> Child object -> Overridden method -> Decided at runtime
\`\`\`

---

## Understanding the Code

\`\`\`java
class Animal {
    public void move() {
        System.out.println("Animal moves");
    }
}

class Dog extends Animal {
    public void move() {
        System.out.println("Dog runs");
    }
}

class Fish extends Animal {
    public void move() {
        System.out.println("Fish swims");
    }
}

class Bird extends Animal {
    public void move() {
        System.out.println("Bird flies");
    }
}

public class PolymorphismRuntime {
    public static void main(String[] args) {
        Animal animal = new Animal();
        Dog dog = new Dog();
        Fish fish = new Fish();
        Bird bird = new Bird();

        Animal a1 = new Animal();
        Animal a2 = new Dog();
        Animal a3 = new Fish();
        Animal a4 = new Bird();

        animal.move();
        dog.move();
        fish.move();
        bird.move();

        a1.move();
        a2.move();
        a3.move();
        a4.move();
    }
}
\`\`\`

### Class Hierarchy

\`\`\`
            Animal  (defines move())
           /   |    \\
        Dog   Fish   Bird   (each overrides move())
\`\`\`

---

## Line-by-Line Explanation

### The Parent Class: Animal

\`\`\`java
class Animal {
    public void move() {
        System.out.println("Animal moves");
    }
}
\`\`\`

- \`Animal\` is the base class with a \`move()\` method that prints \`"Animal moves"\`.
- This is the method that the child classes will override.

### The Child Classes: Dog, Fish, Bird

\`\`\`java
class Dog extends Animal {
    public void move() {
        System.out.println("Dog runs");
    }
}
\`\`\`

- \`Dog\`, \`Fish\`, and \`Bird\` each extend \`Animal\`.
- Each provides its own version of \`move()\`, replacing the parent's behavior. This is **method overriding**.
- \`Dog\` prints \`"Dog runs"\`, \`Fish\` prints \`"Fish swims"\`, and \`Bird\` prints \`"Bird flies"\`.

### Direct Object References

\`\`\`java
Animal animal = new Animal();
Dog dog = new Dog();
Fish fish = new Fish();
Bird bird = new Bird();
\`\`\`

- Each variable's reference type matches its object type.
- Calling \`move()\` on each runs the method belonging to that exact class.

### Parent References to Child Objects (Upcasting)

\`\`\`java
Animal a1 = new Animal();
Animal a2 = new Dog();
Animal a3 = new Fish();
Animal a4 = new Bird();
\`\`\`

- All four variables are of type \`Animal\`, but \`a2\`, \`a3\`, and \`a4\` point to child objects.
- This is called **upcasting** -- treating a subclass object as its superclass type.
- Even though the reference type is \`Animal\`, the actual object determines which \`move()\` runs.

---

## Step-by-Step Execution Walkthrough

When \`main\` runs, the following output is produced:

| Statement      | Reference Type | Actual Object | Method Called  | Output         |
|----------------|----------------|---------------|----------------|----------------|
| \`animal.move()\`| \`Animal\`       | \`Animal\`      | \`Animal.move()\`| Animal moves   |
| \`dog.move()\`   | \`Dog\`          | \`Dog\`         | \`Dog.move()\`   | Dog runs       |
| \`fish.move()\`  | \`Fish\`         | \`Fish\`        | \`Fish.move()\`  | Fish swims     |
| \`bird.move()\`  | \`Bird\`         | \`Bird\`        | \`Bird.move()\`  | Bird flies     |
| \`a1.move()\`    | \`Animal\`       | \`Animal\`      | \`Animal.move()\`| Animal moves   |
| \`a2.move()\`    | \`Animal\`       | \`Dog\`         | \`Dog.move()\`   | Dog runs       |
| \`a3.move()\`    | \`Animal\`       | \`Fish\`        | \`Fish.move()\`  | Fish swims     |
| \`a4.move()\`    | \`Animal\`       | \`Bird\`        | \`Bird.move()\`  | Bird flies     |

**Key Insight:** \`a2\`, \`a3\`, and \`a4\` all have the reference type \`Animal\`, yet they print different results. The method that runs is chosen by the *actual object*, not the *reference type*. This is the essence of runtime polymorphism.

---

## Upcasting and Dynamic Method Dispatch

### Upcasting

Upcasting is assigning a child object to a parent reference:

\`\`\`java
Animal a2 = new Dog();
\`\`\`

- This is always safe and happens automatically.
- The reference type (\`Animal\`) decides which methods are *accessible*.
- The object type (\`Dog\`) decides which version actually *runs*.

### Dynamic Method Dispatch

Dynamic method dispatch is the mechanism by which a call to an overridden method is resolved at runtime rather than compile time.

\`\`\`java
Animal a2 = new Dog();
a2.move();
\`\`\`

- At compile time, Java only checks that \`Animal\` has a \`move()\` method.
- At runtime, the JVM looks at the real object and dispatches to its overridden method.

---

## Compile-Time Polymorphism (Method Overloading)

While this chapter's code demonstrates runtime polymorphism, Java also supports compile-time polymorphism through **method overloading** -- multiple methods with the same name but different parameter lists in the same class.

\`\`\`java
class Calculator {
    int add(int a, int b) {
        return a + b;
    }

    double add(double a, double b) {
        return a + b;
    }

    int add(int a, int b, int c) {
        return a + b + c;
    }
}
\`\`\`

### Rules for Method Overloading

- The method name must be the same.
- The parameter list must differ in number, type, or order.
- The return type alone is NOT enough to overload a method.
- The compiler decides which version to call based on the arguments.

---

## Overloading vs Overriding

| Feature           | Overloading                     | Overriding                          |
|-------------------|---------------------------------|-------------------------------------|
| Polymorphism Type | Compile-time (static)           | Runtime (dynamic)                   |
| Parameters        | Must be different               | Must be the same                    |
| Inheritance       | Not required                    | Required (parent-child relationship)|
| Return Type       | Can be different                | Must be the same or covariant       |
| Resolved At       | Compile time                    | Runtime                             |
| Scope             | Same class                      | Across parent and child classes     |

---

## How to Compile and Run

\`\`\`bash
javac PolymorphismRuntime.java
java PolymorphismRuntime
\`\`\`

> Note: The file declares no package declaration. If you compile from the parent directory, use:
>
> \`\`\`bash
> javac PolymorphismRuntime.java
> java PolymorphismRuntime
> \`\`\`

### Expected Output

\`\`\`
Animal moves
Dog runs
Fish swims
Bird flies
Animal moves
Dog runs
Fish swims
Bird flies
\`\`\`

---

## Common Mistakes

### Mistake 1: Confusing overloading and overriding

Overloading is "same name, different parameters" in the same class (compile-time). Overriding is "same name, same parameters" across parent and child classes (runtime). They are different mechanisms.

### Mistake 2: Thinking the reference type decides the method

\`\`\`java
Animal a2 = new Dog();
a2.move();
\`\`\`

The actual object type decides which overridden method runs, not the reference type.

### Mistake 3: Trying to call child-specific methods through a parent reference

\`\`\`java
class Dog extends Animal {
    public void bark() { System.out.println("Bark"); }
}

Animal a2 = new Dog();
// a2.bark();  // ERROR: Animal reference does not know about bark()
\`\`\`

The reference type (\`Animal\`) limits which methods are accessible at compile time. To call \`bark()\`, you must downcast: \`((Dog) a2).bark();\`.

### Mistake 4: Forgetting the @Override annotation

While optional, \`@Override\` lets the compiler verify that you are actually overriding a parent method. Without it, a typo in the method name silently creates a new method instead of overriding.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`PolymorphismRuntime.java\`

Runtime polymorphism in its plainest form. \`a2\`, \`a3\` and \`a4\` are all declared
as \`Animal\` references, but each points at a \`Dog\`, a \`Fish\` and a \`Bird\`.

Calling the same method on each one produces different behaviour, and it is the
**object** that decides which version runs â€” not the reference type, and not
anything the compiler worked out. That decision happens while the program is
running, which is why it is called runtime (or dynamic) dispatch.

\`\`\`java PolymorphismRuntime.java @run-polymorphism-polymorphism-runtime
class Animal {
    public void move() {
        System.out.println("Animal moves");
    }
}

class Dog extends Animal {
    public void move() {
        System.out.println("Dog runs");
    }
}

class Fish extends Animal {
    public void move() {
        System.out.println("Fish swims");
    }
}

class Bird extends Animal {
    public void move() {
        System.out.println("Bird flies");
    }
}

public class PolymorphismRuntime {
    public static void main(String[] args) {
        Animal animal = new Animal();
        Dog dog = new Dog();
        Fish fish = new Fish();
        Bird bird = new Bird();

        Animal a1 = new Animal();
        Animal a2 = new Dog();
        Animal a3 = new Fish();
        Animal a4 = new Bird();

        animal.move();
        dog.move();
        fish.move();
        bird.move();

        a1.move();
        a2.move();
        a3.move();
        a4.move();
    }
}

\`\`\`

\`\`\`output @run-polymorphism-polymorphism-runtime
Animal moves
Dog runs
Fish swims
Bird flies
Animal moves
Dog runs
Fish swims
Bird flies
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- **Compile-time** — overloading, chosen by the compiler from the declared types.
- **Run-time** — overriding, chosen by the JVM from the actual object.
- **Upcasting** is automatic: \`Animal a = new Dog()\`. The reference type decides
  what you may *call*; the object decides what actually *runs*.
- That split is dynamic method dispatch, and it is the whole point of
  inheritance.
`,interview:`## Interview questions

### Q1: What is polymorphism in Java?

**Answer:** Polymorphism means "many forms". It is the ability of a single method call or reference to behave differently based on the actual object involved. Java supports compile-time polymorphism (method overloading) and runtime polymorphism (method overriding with dynamic method dispatch).

### Q2: What are the two types of polymorphism in Java?

**Answer:** Compile-time polymorphism (also called static polymorphism), achieved through method overloading and resolved by the compiler; and runtime polymorphism (also called dynamic polymorphism), achieved through method overriding and resolved by the JVM at runtime.

### Q3: What is the difference between method overloading and method overriding?

**Answer:**

| Feature       | Overloading                    | Overriding                          |
|---------------|--------------------------------|-------------------------------------|
| Definition    | Same name, different parameters| Same name, same parameters          |
| Occurs in     | Same class                     | Parent and child classes            |
| Binding       | Compile-time (static binding)  | Runtime (dynamic binding)           |
| Return type   | Can be different               | Must be same or covariant           |
| Inheritance   | Not required                   | Required                            |

### Q4: What is dynamic method dispatch?

**Answer:** Dynamic method dispatch is the mechanism by which a call to an overridden method is resolved at runtime rather than at compile time. When a parent reference points to a child object and an overridden method is called, the JVM determines the actual object type and invokes that object's version of the method.

### Q5: What is upcasting?

**Answer:** Upcasting is assigning a subclass object to a superclass reference, such as \`Animal a = new Dog();\`. It is done implicitly and safely. The reference type determines which methods are accessible at compile time, while the actual object type determines which overridden method runs at runtime.

### Q6: Can return type alone be used to overload a method?

**Answer:** No. Two methods that differ only in return type but have identical names and parameter lists will cause a compilation error. Overloading requires the parameter lists to differ in number, type, or order.

### Q7: Why does a parent reference to a child object call the child's method?

**Answer:** Because of dynamic method dispatch. Java uses late binding for overridden instance methods -- the method to call is chosen based on the actual object type at runtime, not the declared reference type. So \`Animal a = new Dog(); a.move();\` calls \`Dog\`'s \`move()\`.

### Q8: Can static methods be overridden?

**Answer:** No. Static methods belong to the class, not to instances, so they are not subject to dynamic dispatch. If a subclass declares a static method with the same signature, it is *method hiding*, not overriding. The version called depends on the reference type, not the object type.

### Q9: How is polymorphism related to inheritance?

**Answer:** Runtime polymorphism depends on inheritance. A child class must inherit from a parent and override its method for dynamic dispatch to occur. Inheritance establishes the "is-a" relationship that allows a parent reference to hold a child object.

### Q10: What is the benefit of programming to the parent type?

**Answer:** Programming to the parent type (for example, accepting an \`Animal\` parameter instead of \`Dog\`, \`Fish\`, and \`Bird\` separately) makes code reusable and extensible. New subclasses can be added without modifying existing methods, because the correct behavior is selected automatically at runtime.
`}},{slug:`final-keyword`,title:`The final keyword`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:18,accent:`#c084fc`,summary:`The final keyword in Java is a non-access modifier used to apply restrictions. It tells the compiler that something cannot be changed after it has been defined. Depending on where it is used, final restricts a variable, a method, or an entire class.`,parts:{notes:`## Introduction\r
\r
The \`final\` keyword in Java is a non-access modifier used to apply restrictions. It tells the compiler that something cannot be changed after it has been defined. Depending on where it is used, \`final\` restricts a variable, a method, or an entire class.\r
\r
This chapter is split across three files, one for each use:\r
\r
| File | Demonstrates |\r
|------|--------------|\r
| \`FinalVariable.java\` | \`final\` variables (constants and final references) |\r
| \`FinalClassDemo.java\`    | \`final\` classes (cannot be extended)               |\r
| \`FinalMethod.java\`   | \`final\` methods (cannot be overridden)             |\r
\r
---\r
\r
## What is the \`final\` Keyword\r
\r
\`final\` means "this cannot change". It is the keyword you use whenever you want to lock something down so that it cannot be reassigned, overridden, or extended.\r
\r
- A \`final\` **variable** can be assigned only once -- it becomes a constant.\r
- A \`final\` **method** cannot be overridden by a subclass.\r
- A \`final\` **class** cannot be extended (subclassed) at all.\r
\r
---\r
\r
## Three Uses of \`final\`\r
\r
\`\`\`\r
            final\r
          /   |   \\\r
   variable  method  class\r
   (constant) (no    (no\r
              override) subclass)\r
\`\`\`\r
\r
Each use applies the same idea -- "no further change" -- to a different building block of the language.\r
\r
---\r
\r
## Part 1: \`final\` Variables\r
\r
### Code\r
\r
\`\`\`java\r
final class Base {\r
    public final void show() {\r
        System.out.println("Base show");\r
    }\r
}\r
\r
/*\r
 * class Derived extends Base {\r
 * // ERROR: Cannot override final method\r
 * public void show() {\r
 * System.out.println("Derived show");\r
 * }\r
 * }\r
 */\r
\r
public class FinalVariable {\r
    final int MAX = 100;\r
\r
    public static void main(String[] args) {\r
        final int AGE = 20;\r
\r
        // AGE = 30; // ERROR: final variable cannot be reassigned\r
        // MAX = 50; // ERROR: final variable cannot be reassigned\r
\r
        final double PI = 3.14159;\r
        System.out.println("Value of PI: " + PI);\r
\r
        double circumference = 2 * PI * 5;\r
        System.out.println("Circumference: " + circumference);\r
\r
        final StringBuilder sb = new StringBuilder("Hello");\r
        sb.append(" World"); // OK: modifying content\r
        System.out.println(sb);\r
\r
        // sb = new StringBuilder("Goodbye"); // ERROR: final reference cannot be reassigned\r
    }\r
}\r
\`\`\`\r
\r
### Explanation\r
\r
- \`final int MAX = 100;\` is a final **instance variable** (a constant tied to each object). It cannot be reassigned.\r
- \`final int AGE = 20;\` and \`final double PI = 3.14159;\` are final **local variables**. Once set, they cannot change. Attempting \`AGE = 30;\` is a compile-time error.\r
- Final variables can be freely **read** and used in expressions -- for example, \`2 * PI * 5\`.\r
- \`final StringBuilder sb = new StringBuilder("Hello");\` makes the **reference** \`sb\` final, not the object it points to:\r
  - \`sb.append(" World");\` is allowed -- you are modifying the object's contents.\r
  - \`sb = new StringBuilder("Goodbye");\` is NOT allowed -- you cannot point \`sb\` at a different object.\r
\r
### Key Insight\r
\r
For reference types, \`final\` locks the reference, not the object it points to. The object's internal state can still change.\r
\r
### Expected Output\r
\r
\`\`\`\r
Value of PI: 3.14159\r
Circumference: 31.4159\r
Hello World\r
\`\`\`\r
\r
---\r
\r
## Part 2: \`final\` Classes\r
\r
### Code\r
\r
\`\`\`java\r
public class FinalClassDemo {\r
    public static void main(String[] args) {\r
        FinalClass finalClass = new FinalClass();\r
        finalClass.show();\r
    }\r
}\r
\r
final class FinalClass {\r
    public void show() {\r
        System.out.println("Final class method");\r
    }\r
}\r
\r
// class A extends FinalClass {\r
//     public void show() {\r
//         System.out.println("Derived class method");\r
//     }\r
// }\r
\r
/*\r
 * class Derived extends FinalClass {\r
 * // ERROR: Cannot extend final class\r
 * public void show() {\r
 * System.out.println("Derived class method");\r
 * }\r
 * }\r
 */\r
\`\`\`\r
\r
### Explanation\r
\r
- \`FinalClass\` is declared \`final\`, so it cannot be extended. The commented-out \`class A extends FinalClass\` and \`class Derived extends FinalClass\` would both cause compile-time errors.\r
- You can still **create objects** of a final class and call its methods, as \`main\` does with \`new FinalClass()\` and \`finalClass.show()\`.\r
- This is exactly how Java's own \`String\` class is built -- it is \`final\` so no one can subclass and alter its behavior.\r
\r
### Why Make a Class \`final\`?\r
\r
- To prevent inheritance for security or design reasons.\r
- To guarantee that the class's behavior cannot be altered by a subclass.\r
- To make the class safely immutable.\r
\r
### Expected Output\r
\r
\`\`\`\r
Final class method\r
\`\`\`\r
\r
---\r
\r
## Part 3: \`final\` Methods\r
\r
### Code\r
\r
\`\`\`java\r
class A {\r
    public final void show() {\r
        System.out.println("A show");\r
    }\r
}\r
\r
class B extends A {\r
    // public void show() {\r
    //     System.out.println("B show");\r
    // }\r
}\r
\r
public class FinalMethod {\r
    public static void main(String[] args) {\r
        B b = new B();\r
        b.show();\r
    }\r
}\r
\`\`\`\r
\r
### Explanation\r
\r
- \`A\` declares \`show()\` as \`final\`. This means no subclass is allowed to override it.\r
- In \`B\`, the \`show()\` override is **commented out**, so \`B\` simply inherits \`A\`'s final \`show()\`. The program compiles and \`b.show()\` prints \`A show\`.\r
- If you **uncomment** \`B\`'s \`show()\`, the file fails to compile with: *"show() in B cannot override show() in A; overriding method is final"* -- which is exactly the rule this example teaches.\r
- A \`final\` method can still be **inherited** and **called** -- it just cannot be **redefined**.\r
\r
### Key Insight\r
\r
\`final\` on a method blocks overriding (changing behavior in a subclass), not inheritance.\r
\r
### Why Make a Method \`final\`?\r
\r
- To preserve critical behavior that subclasses must not change.\r
- To prevent accidental overriding of methods that the class relies on internally.\r
\r
---\r
\r
## Summary Table\r
\r
| Applied To | Meaning                                   | What is Blocked            | Still Allowed                        |\r
|------------|-------------------------------------------|----------------------------|--------------------------------------|\r
| Variable   | Value/reference assigned only once        | Reassignment               | Reading; modifying referenced object |\r
| Method     | Cannot be redefined in a subclass         | Overriding                 | Inheriting and calling the method    |\r
| Class      | Cannot be subclassed                      | Extending (\`extends\`)      | Creating objects of the class        |\r
\r
---\r
\r
## How to Compile and Run\r
\r
None of these files declares a package. Compile and run them from the parent directory using the package path:\r
\r
\`\`\`bash\r
# Final variable demo\r
javac FinalVariable.java\r
java FinalVariable\r
\r
# Final class demo\r
javac FinalClassDemo.java\r
java FinalClassDemo\r
\r
# Final method demo (override in B is commented out, so it prints "A show")\r
javac FinalMethod.java\r
java FinalMethod\r
\`\`\`\r
\r
> In \`FinalMethod.java\`, \`B\`'s \`show()\` override is commented out, so the file compiles and prints \`A show\`. Uncommenting that override produces the compile-time error "overriding method is final" -- which is the lesson the example teaches.\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Thinking \`final\` on a reference makes the object immutable\r
\r
\`\`\`java\r
final StringBuilder sb = new StringBuilder("Hello");\r
sb.append(" World");\r
sb = new StringBuilder("Hi");  // Error -- the reference IS final\r
\`\`\`\r
\r
\`final\` locks the reference, not the contents of the object it points to.\r
\r
### Mistake 2: Confusing \`final\` with \`finally\` and \`finalize()\`\r
\r
- \`final\` -- a keyword that restricts variables, methods, and classes.\r
- \`finally\` -- a block in exception handling that always runs.\r
- \`finalize()\` -- a (deprecated) method called by the garbage collector before an object is destroyed.\r
\r
These three are unrelated despite the similar names.\r
\r
### Mistake 3: Trying to override a \`final\` method\r
\r
A subclass cannot override a \`final\` method (as \`FinalMethod.java\` shows). The compiler rejects it. To allow overriding, remove \`final\` from the parent method.\r
\r
### Mistake 4: Trying to extend a \`final\` class\r
\r
A \`final\` class cannot appear after \`extends\`. Use composition (holding an instance of the class) instead of inheritance.\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`FinalVariable.java\`\r
\r
\`\`\`java FinalVariable.java @run-final-keyword-final-variable\r
final class Base {\r
    public final void show() {\r
        System.out.println("Base show");\r
    }\r
}\r
\r
/*\r
 * class Derived extends Base {\r
 * // ERROR: Cannot override final method\r
 * public void show() {\r
 * System.out.println("Derived show");\r
 * }\r
 * }\r
 */\r
\r
public class FinalVariable {\r
    final int MAX = 100;\r
\r
    public static void main(String[] args) {\r
        final int AGE = 20;\r
\r
        // AGE = 30; // ERROR: final variable cannot be reassigned\r
        // MAX = 50; // ERROR: final variable cannot be reassigned\r
\r
        final double PI = 3.14159;\r
\r
        System.out.println("Value of PI: " + PI);\r
\r
        double circumference = 2 * PI * 5;\r
        System.out.println("Circumference: " + circumference);\r
\r
        final StringBuilder sb = new StringBuilder("Hello");\r
        sb.append(" World"); // OK: modifying content\r
        System.out.println(sb);\r
\r
        // sb = new StringBuilder("Goodbye"); // ERROR: final reference cannot be\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-final-keyword-final-variable\r
Value of PI: 3.14159\r
Circumference: 31.4159\r
Hello World\r
\`\`\`\r
\r
### \`FinalClassDemo.java\`\r
\r
\`\`\`java FinalClassDemo.java @run-final-keyword-final-class-demo\r
public class FinalClassDemo {\r
    public static void main(String[] args) {\r
        FinalClass finalClass = new FinalClass();\r
        finalClass.show();\r
    }\r
}\r
\r
final class FinalClass {\r
    public void show() {\r
        System.out.println("Final class method");\r
    }   \r
}\r
\r
// class A extends FinalClass {\r
//     public void show() {\r
//         System.out.println("Derived class method");\r
//     }\r
// }\r
\r
/*\r
 * class Derived extends FinalClass {\r
 * // ERROR: Cannot extend final class\r
 * public void show() {\r
 * System.out.println("Derived class method");\r
 * }\r
 * }\r
 */\r
\`\`\`\r
\r
\`\`\`output @run-final-keyword-final-class-demo\r
Final class method\r
\`\`\`\r
\r
### \`FinalMethod.java\`\r
\r
\`\`\`java FinalMethod.java @run-final-keyword-final-method\r
class A {\r
    public final void show() {\r
        System.out.println("A show");\r
    }\r
}\r
\r
class B extends A {\r
    // public void show() {\r
    //     System.out.println("B show");\r
    // }\r
}\r
\r
public class FinalMethod {\r
    public static void main(String[] args) {\r
        B b = new B();\r
        b.show();\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-final-keyword-final-method\r
A show\r
\`\`\`\r
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- \`final\` **variable** — cannot be reassigned. The object it points at can still
  be mutated.
- \`final\` **method** — cannot be overridden.
- \`final\` **class** — cannot be extended. \`String\` is one.
- A blank \`final\` field must be assigned exactly once, in the constructor.
`,interview:'## Interview questions\n\n### Q1: What are the three uses of the `final` keyword in Java?\n\n**Answer:** `final` can be applied to a variable (it becomes a constant that can be assigned only once), a method (it cannot be overridden by a subclass), and a class (it cannot be extended/subclassed).\n\n### Q2: Does `final` make an object immutable?\n\n**Answer:** No. When applied to a reference variable, `final` only prevents the reference from being reassigned. The object\'s internal state can still be changed. For example, a `final StringBuilder` can still have text appended to it, but the variable cannot be pointed at a new `StringBuilder`.\n\n### Q3: Can a `final` method be inherited?\n\n**Answer:** Yes. A `final` method is inherited by subclasses and can be called normally. `final` only prevents the method from being *overridden*, not from being inherited or used.\n\n### Q4: Why is the `String` class declared `final`?\n\n**Answer:** To guarantee immutability and security. Because `String` cannot be subclassed, no one can create a malicious subclass that changes string behavior, and the JVM can safely cache and share string literals.\n\n### Q5: What is the difference between `final`, `finally`, and `finalize()`?\n\n**Answer:** `final` is a modifier that restricts variables, methods, and classes. `finally` is a block used with try-catch that always executes regardless of whether an exception occurs. `finalize()` is a method historically called by the garbage collector before reclaiming an object (now deprecated).\n\n### Q6: Can a `final` variable be initialized later (not at declaration)?\n\n**Answer:** Yes, a `final` variable can be a "blank final" -- declared without a value and assigned exactly once, either in a constructor (for instance variables) or before first use (for local variables). After that single assignment, it cannot be changed.\n\n### Q7: Can a constructor be declared `final`?\n\n**Answer:** No. Constructors are never inherited and therefore cannot be overridden, so marking a constructor `final` is meaningless and is a compile-time error.\n\n### Q8: What happens if you try to override a `final` method?\n\n**Answer:** The code fails to compile with an error such as "overridden method is final". The subclass is not permitted to provide a new implementation. This is exactly what `FinalMethod.java` demonstrates.\n\n### Q9: Can a `final` class have non-final methods?\n\n**Answer:** Yes. A `final` class can contain ordinary (non-final) methods, as `FinalClass` does with `show()`. Since the class itself cannot be subclassed, those methods can never be overridden anyway, so marking them `final` is unnecessary.\n\n### Q10: When should you use the `final` keyword?\n\n**Answer:** Use `final` for constants (`final` variables) to express values that must not change, for methods whose behavior is critical and must not be altered by subclasses, and for classes that are designed to be complete and not extended (often for immutability or security).\n'}},{slug:`object-methods`,title:`Object methods: toString, equals, hashCode`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:19,accent:`#60a5fa`,summary:`In Java, every class implicitly extends the Object class, inheriting a set of methods that every object has -- including toString(), equals(), and hashCode(). By default these methods work in terms of an object's identity (its memory address), which is rarely what we want. This chapter shows how to override all three so objects have a meaningful text form and are compared by their data instead of their identity.`,parts:{notes:`## Introduction

In Java, every class implicitly extends the \`Object\` class, inheriting a set of methods that every object has -- including \`toString()\`, \`equals()\`, and \`hashCode()\`. By default these methods work in terms of an object's identity (its memory address), which is rarely what we want. This chapter shows how to **override all three** so objects have a meaningful text form and are compared by their data instead of their identity.

This program defines a \`Laptop\` class that overrides \`toString()\`, \`equals()\`, and \`hashCode()\`, then demonstrates how each behaves and how they relate to the \`==\` operator.

---

## What is an Object

An object is an instance of a class -- a concrete "thing" created from a class blueprint, holding its own copy of the fields defined by that class.

\`\`\`java
Laptop obj1 = new Laptop();
obj1.brand = "Dell";
obj1.price = 50000;
\`\`\`

- \`new Laptop()\` creates the object in memory.
- \`obj1\` is a **reference** -- it stores the address of that object, not the object itself.
- Two references can point to the same object, or to two different objects that happen to hold the same data. Telling these cases apart is what \`==\`, \`equals()\`, and \`hashCode()\` are about.

---

## The \`Object\` Class and Its Methods

\`Object\` (in \`java.lang\`) is the root of every class hierarchy. The three methods we override most often are:

| Method        | Default Behavior                                  | Why Override It                                  |
|---------------|---------------------------------------------------|--------------------------------------------------|
| \`toString()\`  | Returns \`ClassName@hexHashCode\`                   | To print a readable description of the object    |
| \`equals()\`    | Compares references (same as \`==\`)                | To compare objects by their data (field values)  |
| \`hashCode()\`  | A number based on the object's identity           | To keep equal objects consistent in hash collections |

These three are commonly overridden together because they describe what an object *is* and when two objects are *the same*.

---

## The \`toString()\` Method

The default \`toString()\` returns something like \`Laptop@1b6d3586\` (class name + \`@\` + hash code in hex). By overriding it we control what gets printed:

\`\`\`java
@Override
public String toString() {
    return "Laptop{brand='" + brand + "', price=" + price + "}";
}
\`\`\`

\`toString()\` is called **automatically** whenever an object is used where a \`String\` is expected:

\`\`\`java
System.out.println("obj1.toString() : " + obj1);
\`\`\`

---

## Object Equality: \`==\` vs \`equals()\`

There are two very different questions you can ask about two objects:

| Comparison         | Question Asked                          | Compares       |
|--------------------|-----------------------------------------|----------------|
| \`obj1 == obj2\`     | Are these the *same object* in memory?  | References     |
| \`obj1.equals(obj2)\`| Do these objects *hold the same data*?  | Field values (when overridden) |

\`\`\`java
obj1 == obj2;
obj1.equals(obj2);
\`\`\`

A correct \`equals()\` override examines the fields:

\`\`\`java
@Override
public boolean equals(Object o) {
    if (this == o) return true;
    if (o == null || getClass() != o.getClass())
        return false;
    Laptop other = (Laptop) o;
    return price == other.price && Objects.equals(brand, other.brand);
}
\`\`\`

> Note the parameter type is \`Object\`, not \`Laptop\`. This is what makes it a genuine override of \`Object.equals\`. Using \`equals(Laptop)\` would create an *overload* that collections never call.

---

## The \`hashCode()\` Method

A hash code is an \`int\` derived from an object's data. Hash-based collections (\`HashMap\`, \`HashSet\`) use it to decide which "bucket" an object goes into. The rule: **equal objects must have equal hash codes**. So whenever you override \`equals()\`, you must also override \`hashCode()\`:

\`\`\`java
@Override
public int hashCode() {
    return Objects.hash(brand, price);
}
\`\`\`

\`Objects.hash(...)\` builds a combined hash from the listed fields -- the same fields used in \`equals()\`.

---

## Understanding the Code

\`\`\`java
import java.util.Objects;

class Laptop {
    String brand;
    int price;

    @Override
    public String toString() {
        return "Laptop{brand='" + brand + "', price=" + price + "}";
    }

    @Override
    public boolean equals(Object o) {
        if (this == o)
            return true;
        if (o == null || getClass() != o.getClass())
            return false;
        Laptop other = (Laptop) o;
        return price == other.price && Objects.equals(brand, other.brand);
    }

    @Override
    public int hashCode() {
        return Objects.hash(brand, price);
    }
}

public class ObjectsDemo {
    public static void main(String[] args) {
        Laptop obj1 = new Laptop();
        obj1.brand = "Dell";
        obj1.price = 50000;

        Laptop obj2 = new Laptop();
        obj2.brand = "Dell";
        obj2.price = 50000;

        Laptop obj3 = new Laptop();
        obj3.brand = "HP";
        obj3.price = 60000;

        System.out.println("obj1.toString() : " + obj1);
        System.out.println("obj3.toString() : " + obj3);

        System.out.println("obj1 == obj2      : " + (obj1 == obj2));
        System.out.println("obj1.equals(obj2) : " + obj1.equals(obj2));
        System.out.println("obj1.equals(obj3) : " + obj1.equals(obj3));

        System.out.println("obj1.hashCode()   : " + obj1.hashCode());
        System.out.println("obj2.hashCode()   : " + obj2.hashCode());
        System.out.println("obj3.hashCode()   : " + obj3.hashCode());
    }
}
\`\`\`

---

## Line-by-Line Explanation

### \`import java.util.Objects;\`

- Imports the \`Objects\` utility class, which provides the null-safe helpers \`Objects.equals(a, b)\` and \`Objects.hash(...)\` used inside our overrides.

### The \`Laptop\` fields

\`\`\`java
String brand;
int price;
\`\`\`

- The two fields that define a laptop -- and that we use to decide equality and to build the hash code.

### \`toString()\`

\`\`\`java
@Override
public String toString() {
    return "Laptop{brand='" + brand + "', price=" + price + "}";
}
\`\`\`

- Replaces the default \`Laptop@hashcode\` output with a readable description.

### \`equals(Object o)\`

\`\`\`java
if (this == o) return true;
if (o == null || getClass() != o.getClass()) return false;
Laptop other = (Laptop) o;
return price == other.price && Objects.equals(brand, other.brand);
\`\`\`

- **Step 1:** if both references are the same object, they are equal.
- **Step 2:** if the other object is \`null\` or a different class, they are not equal.
- **Step 3:** cast to \`Laptop\` and compare the defining fields. \`price\` is a primitive, compared with \`==\`; \`brand\` is a \`String\`, compared null-safely with \`Objects.equals\`.

### \`hashCode()\`

\`\`\`java
@Override
public int hashCode() {
    return Objects.hash(brand, price);
}
\`\`\`

- Produces a hash from the same fields used in \`equals()\`, so equal laptops always get the same hash code.

### \`main\` -- creating objects

- \`obj1\` and \`obj2\` hold identical data (\`Dell\`, \`50000\`); \`obj3\` is different (\`HP\`, \`60000\`).

### \`main\` -- the comparisons

- \`obj1 == obj2\` checks references -> \`false\` (different objects).
- \`obj1.equals(obj2)\` checks data -> \`true\`; \`obj1.equals(obj3)\` -> \`false\`.
- \`hashCode()\` is identical for the equal pair \`obj1\`/\`obj2\`, and different for \`obj3\`.

---

## Step-by-Step Execution Walkthrough

| Statement                | What Happens                                      | Output                                 |
|--------------------------|---------------------------------------------------|----------------------------------------|
| \`"... " + obj1\`          | Calls \`obj1.toString()\`                            | Laptop{brand='Dell', price=50000}      |
| \`"... " + obj3\`          | Calls \`obj3.toString()\`                            | Laptop{brand='HP', price=60000}        |
| \`obj1 == obj2\`           | Reference comparison of two distinct objects       | false                                  |
| \`obj1.equals(obj2)\`      | Field comparison; same brand and price             | true                                   |
| \`obj1.equals(obj3)\`      | Field comparison; different brand and price        | false                                  |
| \`obj1.hashCode()\`        | Hash from ("Dell", 50000)                          | 65966416                               |
| \`obj2.hashCode()\`        | Same data as obj1 -> same hash                     | 65966416                               |
| \`obj3.hashCode()\`        | Hash from ("HP", 60000)                            | 132633                                 |

**Key Insight:** \`obj1\` and \`obj2\` are different objects (\`==\` is false) yet are *equal* by data (\`equals()\` is true) and therefore share the **same hash code** -- exactly what hash-based collections require.

---

## The \`equals()\` / \`hashCode()\` Contract

- If \`a.equals(b)\` is \`true\`, then \`a.hashCode()\` must equal \`b.hashCode()\`.
- The reverse is **not** required: two unequal objects *may* share a hash code (a "collision"); that is allowed and handled by the collection.
- Therefore, **whenever you override \`equals()\`, you must also override \`hashCode()\`** using the same fields. Overriding only one breaks \`HashMap\`/\`HashSet\`.

A correct \`equals()\` must also be:

- **Reflexive:** \`a.equals(a)\` is true.
- **Symmetric:** \`a.equals(b)\` implies \`b.equals(a)\`.
- **Transitive:** \`a.equals(b)\` and \`b.equals(c)\` imply \`a.equals(c)\`.
- **Consistent:** repeated calls return the same result.
- \`a.equals(null)\` is always false.

> Most IDEs can generate correct \`equals()\`, \`hashCode()\`, and \`toString()\` for you via "Source Action" -- the same menu used to generate getters and setters.

---

## How to Compile and Run

The file declares no package declaration, so compile and run from the parent directory using the package path:

\`\`\`bash
javac ObjectsDemo.java
java ObjectsDemo
\`\`\`

### Expected Output

\`\`\`
obj1.toString() : Laptop{brand='Dell', price=50000}
obj3.toString() : Laptop{brand='HP', price=60000}
obj1 == obj2      : false
obj1.equals(obj2) : true
obj1.equals(obj3) : false
obj1.hashCode()   : 65966416
obj2.hashCode()   : 65966416
obj3.hashCode()   : 132633
\`\`\`

> The exact hash code numbers come from \`Objects.hash(brand, price)\` and depend only on the field values, so they are reproducible for the same data.

---

## Common Mistakes

### Mistake 1: Using \`==\` to compare object contents

\`\`\`java
if (obj1 == obj2) { ... }
\`\`\`

\`==\` is \`true\` only when both references point to the exact same object. To compare data, use \`equals()\`.

### Mistake 2: Overriding \`equals()\` but not \`hashCode()\`

This breaks \`HashSet\` and \`HashMap\`: two "equal" objects can land in different buckets, so lookups and de-duplication fail. Always override both, using the same fields.

### Mistake 3: Writing \`equals(Laptop)\` instead of \`equals(Object)\`

\`\`\`java
public boolean equals(Laptop other) { ... }
\`\`\`

The real \`Object.equals\` takes an \`Object\`. With the wrong parameter type you create a new overloaded method, and collections still call the inherited \`equals(Object)\`. Use \`@Override\` so the compiler catches this.

### Mistake 4: Using \`==\` to compare Strings inside \`equals()\`

\`\`\`java
return brand == other.brand;
return Objects.equals(brand, other.brand);  // correct, null-safe content compare
\`\`\`

### Mistake 5: Forgetting \`@Override\`

Without it, a small signature mistake silently becomes a new method instead of an override, and the bug is hard to spot. The annotation makes the compiler verify the override.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`ObjectsDemo.java\`

Every class inherits \`toString()\`, \`equals()\` and \`hashCode()\` from \`Object\`,
and by default all three work on **identity** â€” the object's address in memory.
\`Laptop\` overrides them so that two laptops with the same brand and price count
as equal:

- **\`toString()\`** is called automatically whenever an object is printed or
  joined to a \`String\`. Without the override you would see \`Laptop@1b6d35\`.
- **\`equals(Object o)\`** takes an \`Object\`, not a \`Laptop\`. That matters: narrow
  the parameter type and you have written an *overload*, not an override, and
  nothing will call it.
- **\`hashCode()\`** must be overridden whenever \`equals()\` is. Equal objects are
  required to produce equal hash codes, and \`HashMap\` and \`HashSet\` depend on
  that contract holding.

\`java.util.Objects\` supplies the null-safe helpers the overrides are built from â€”
\`Objects.equals(...)\` and \`Objects.hash(...)\`.

In \`main\`, \`obj1\` and \`obj2\` carry identical data and \`obj3\` differs. Watch the
output: \`==\` is **false** for \`obj1\` and \`obj2\` because they are two separate
objects, while \`equals()\` is **true** because their data matches â€” and their hash
codes come out identical, exactly as the contract demands.

\`\`\`java ObjectsDemo.java @run-object-methods-objects-demo
import java.util.Objects;

class Laptop {
    String brand;
    int price;

    @Override
    public String toString() {
        return "Laptop{brand='" + brand + "', price=" + price + "}";
    }

    @Override
    public boolean equals(Object o) {
        if (this == o)
            return true;

        if (o == null || getClass() != o.getClass())
            return false;

        Laptop other = (Laptop) o;
        return price == other.price && Objects.equals(brand, other.brand);
    }

    @Override
    public int hashCode() {
        return Objects.hash(brand, price);
    }
}

public class ObjectsDemo {
    public static void main(String[] args) {
        Laptop obj1 = new Laptop();
        obj1.brand = "Dell";
        obj1.price = 50000;

        Laptop obj2 = new Laptop();
        obj2.brand = "Dell";
        obj2.price = 50000;

        Laptop obj3 = new Laptop();
        obj3.brand = "HP";
        obj3.price = 60000;

        System.out.println("obj1.toString() : " + obj1);
        System.out.println("obj3.toString() : " + obj3);

        System.out.println("obj1 == obj2      : " + (obj1 == obj2));

        System.out.println("obj1.equals(obj2) : " + obj1.equals(obj2));
        System.out.println("obj1.equals(obj3) : " + obj1.equals(obj3));

        System.out.println("obj1.hashCode()   : " + obj1.hashCode());
        System.out.println("obj2.hashCode()   : " + obj2.hashCode());
        System.out.println("obj3.hashCode()   : " + obj3.hashCode());
    }
}

\`\`\`

\`\`\`output @run-object-methods-objects-demo
obj1.toString() : Laptop{brand='Dell', price=50000}
obj3.toString() : Laptop{brand='HP', price=60000}
obj1 == obj2      : false
obj1.equals(obj2) : true
obj1.equals(obj3) : false
obj1.hashCode()   : 65966416
obj2.hashCode()   : 65966416
obj3.hashCode()   : 132633
\`\`\`
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- Every class extends `Object` whether you say so or not.\n- Default `toString` prints `ClassName@hashcode`; override it for anything you\n  will print.\n- Default `equals` is `==` — identity. Override it for value equality.\n- **The contract:** equal objects must have equal hash codes. Override `equals`\n  without `hashCode` and hash-based collections silently stop finding things.\n",interview:"## Interview questions\n\n### Q1: What is the `Object` class in Java?\n\n**Answer:** `Object` (in `java.lang`) is the root of the class hierarchy. Every class directly or indirectly extends it, inheriting methods such as `toString()`, `equals()`, `hashCode()`, `getClass()`, `clone()`, `wait()`, and `notify()`.\n\n### Q2: What does the default `toString()` return, and why override it?\n\n**Answer:** It returns the class name, `@`, and the object's hash code in hex (e.g. `Laptop@1b6d3586`). We override it to provide a meaningful, human-readable description of the object's state.\n\n### Q3: What is the difference between `==` and `equals()`?\n\n**Answer:** `==` compares references -- whether two variables point to the same object (for primitives, it compares values). `equals()` is a method that, when overridden, compares the logical content of two objects. By default (not overridden), `equals()` behaves like `==`.\n\n### Q4: Why must you override `hashCode()` when you override `equals()`?\n\n**Answer:** Because of the contract: equal objects must have equal hash codes. Hash-based collections use `hashCode()` to pick a bucket and `equals()` to confirm a match. If equal objects had different hash codes, they could be placed in different buckets and never be recognized as equal.\n\n### Q5: Can two unequal objects have the same hash code?\n\n**Answer:** Yes. That is called a hash collision and is perfectly legal. The contract only requires equal objects to share a hash code, not that unequal objects have different ones. Collections resolve collisions by also calling `equals()`.\n\n### Q6: What is the `java.util.Objects` class?\n\n**Answer:** A utility class of static helper methods for working with objects safely, including `Objects.equals(a, b)` (null-safe equality), `Objects.hash(...)` (combined hash code), `Objects.isNull`, `Objects.nonNull`, `Objects.requireNonNull`, and `Objects.toString`.\n\n### Q7: Why must the parameter of `equals()` be `Object`?\n\n**Answer:** Because `Object.equals(Object)` is the method being overridden. If you declare `equals(Laptop)`, you create an overload, not an override, and Java's collections -- which call `equals(Object)` -- will ignore it. `@Override` makes the compiler enforce the correct signature.\n\n### Q8: What does the `@Override` annotation do?\n\n**Answer:** It tells the compiler that a method is intended to override a superclass method. If it does not actually override anything (e.g. due to a wrong signature), the compiler reports an error, preventing subtle bugs.\n\n### Q9: What is the full contract of a correct `equals()` method?\n\n**Answer:** It must be reflexive (`a.equals(a)` is true), symmetric (`a.equals(b)` implies `b.equals(a)`), transitive (if `a.equals(b)` and `b.equals(c)` then `a.equals(c)`), consistent (repeated calls give the same result), and `a.equals(null)` must be false.\n\n### Q10: How do you compare two Strings correctly?\n\n**Answer:** Use `.equals()` (or `.equalsIgnoreCase()` for case-insensitive comparison), or `Objects.equals()` for a null-safe version. Avoid `==`, which compares references and may return `false` even for identical text when the strings are separate objects.\n"}},{slug:`upcasting-downcasting`,title:`Upcasting and downcasting`,subject:`Java`,stage:`Inheritance and polymorphism`,chapter:20,accent:`#38bdf8`,summary:`Casting means converting a value from one type to another. Java uses the word "casting" for two related but quite different ideas:`,parts:{notes:`## Introduction

**Casting** means converting a value from one type to another. Java uses the word
"casting" for two related but quite different ideas:

1. **Primitive type casting** -- converting between number types like \`int\`,
   \`double\`, \`byte\`, and \`char\`.
2. **Reference (object) casting** -- viewing an object through a different type
   in its inheritance hierarchy (**upcasting** and **downcasting**).

This chapter covers all three across three programs:

| File | Demonstrates |
|------|--------------|
| \`TypeCasting.java\` | Widening and narrowing of **primitive** types |
| \`UpCasting.java\`   | **Upcasting** a subclass object (\`B\`) to a superclass reference (\`A\`) |
| \`DownCasting.java\` | **Downcasting** a superclass reference (\`A\`) back to the subclass (\`B\`) |

Both object-casting examples use the same simple hierarchy: a superclass \`A\` with
a method \`showA()\`, and a subclass \`B extends A\` that adds \`showB()\`. In each file
\`A\` and \`B\` are declared as **static nested classes** so that both files can keep
the same \`A\`/\`B\` names and still compile together in one package without clashing.

---

## Two Different Worlds of Casting

| | Primitive casting | Reference casting |
|---|---|---|
| Works on | \`int\`, \`double\`, \`byte\`, \`char\`, ... | objects (class instances) |
| Changes | the actual value/representation | only the *view* (the reference type) |
| Risk | data loss (truncation, overflow) | \`ClassCastException\` at runtime |
| Directions | widening / narrowing | upcasting / downcasting |

The key mental model: **primitive casting changes the data; reference casting
never changes the object -- it only changes which members you are allowed to
see.**

---

## Primitive Type Casting

Java's numeric primitives can be ordered by size/capacity:

\`\`\`
byte -> short -> int -> long -> float -> double
                char -> int
\`\`\`

### Widening (Implicit)

Moving **up** this chain (small type into a bigger one) is always safe, so Java
does it automatically -- no cast needed, no data lost.

\`\`\`java
int a = 10;
long b = a;
double c = b;
\`\`\`

Both assignments happen without a cast. \`c\` prints as \`10.0\` — the value is
unchanged, only the type it is held in got wider.

### Narrowing (Explicit)

Moving **down** the chain (big type into a smaller one) may lose data, so Java
forces you to write an explicit \`(type)\` cast to show you accept the risk.

\`\`\`java
double d = 9.99;
int e = (int) d;

int big = 257;
byte sb = (byte) big;

int code = 65;
char ch = (char) code;
\`\`\`

Three different ways of losing something:

- \`e\` is **\`9\`** — the fraction is dropped, not rounded.
- \`sb\` is **\`1\`** — a \`byte\` only holds \`-128..127\`, so \`257\` overflows and wraps
  around.
- \`ch\` is **\`'A'\`** — \`65\` is the character code for \`A\`, so nothing is lost
  here; the number is simply reinterpreted as a character.

> Narrowing does **not** round. \`(int) 9.99\` is \`9\`, and \`(int) 9.0001\` is also \`9\`.

---

## Reference Casting: Upcasting

**Upcasting** treats a subclass object through a superclass reference. It is
**implicit** (no cast needed) and **always safe**, because a subclass object
genuinely *is* a kind of its superclass.

\`\`\`java
B b = new B();
A ref = b;
ref.showA();      // OK: showA() is declared in A
// ref.showB();   // ERROR: showB() is not declared in A
\`\`\`

The rule at play: **the reference type decides what is visible.** Through an \`A\`
reference you can only call methods declared in \`A\`, even though the object is
really a \`B\`.

---

## Reference Casting: Downcasting

**Downcasting** converts a superclass reference back to the subclass type, so the
subclass-specific members become available again. It must be written
**explicitly**, and it is only safe if the object *really is* that subclass --
otherwise Java throws a \`ClassCastException\` at runtime.

\`\`\`java
A ref = new B();
ref.showA();
// ref.showB();   // ERROR: not visible through A

if (ref instanceof B) {
    B b = (B) ref;
    b.showB();
}
\`\`\`

Upcast first, so only \`A\`'s members are visible. \`instanceof\` then confirms the
object really is a \`B\`, and inside that block the \`(B)\` cast is guaranteed safe —
\`showB()\` becomes callable again.

If you downcast an object that is **not** actually the target type, it compiles
but crashes at runtime with a \`ClassCastException\`:

\`\`\`java
A onlyA = new A();
B bad = (B) onlyA;
\`\`\`

---

## The \`instanceof\` Operator

\`instanceof\` tests whether an object is an instance of a given type (or subtype).
It returns a \`boolean\` and is the standard guard before a downcast:

\`\`\`java
if (ref instanceof B) {
    B b = (B) ref;
    b.showB();
}
\`\`\`

This prevents \`ClassCastException\` by confirming the real type first.

> **Modern shortcut (Java 16+):** *pattern matching for \`instanceof\`* lets you
> test and cast in one step, binding the result to a new variable:
>
> \`\`\`java
> if (ref instanceof B b) { // checks the type AND casts into 'b'
>     b.showB();
> }
> \`\`\`
>
> This chapter keeps the explicit \`(B) ref\` cast on purpose, so the downcast step
> is visible while you are learning what casting actually does. The pattern form
> is the idiomatic choice in real code. (Your IDE may show an informational hint
> suggesting it -- that is a style tip, not an error.)

---

## Understanding the Code

### \`TypeCasting.java\`

\`\`\`java
int a = 10;
long b = a;
double c = b;

double d = 9.99;
int e = (int) d;
int big = 257;
byte sb = (byte) big;
int code = 65;
char ch = (char) code;
\`\`\`

### \`UpCasting.java\`

\`\`\`java
public class UpCasting {
    static class A {
        public void showA() { System.out.println("In Class A"); }
    }
    static class B extends A {
        public void showB() { System.out.println("In Class B"); }
    }

    public static void main(String[] args) {
        B b = new B();
        A ref = b;
        ref.showA();
    }
}
\`\`\`

### \`DownCasting.java\`

\`\`\`java
public class DownCasting {
    static class A {
        public void showA() { System.out.println("In Class A"); }
    }
    static class B extends A {
        public void showB() { System.out.println("In Class B"); }
    }

    public static void main(String[] args) {
        A ref = new B();
        if (ref instanceof B) {
            B b = (B) ref;
            b.showB();
        }
    }
}
\`\`\`

---

## Line-by-Line Explanation

### Widening

- \`long b = a;\` -- an \`int\` always fits in a \`long\`, so Java converts it for you.
- \`double c = b;\` -- a \`long\` fits in a \`double\`; the value gains a \`.0\`.

### Narrowing

- \`(int) d\` -- discards everything after the decimal point (\`9.99 -> 9\`).
- \`(byte) big\` -- a \`byte\` holds only \`-128..127\`; \`257\` wraps around to \`1\`.
- \`(char) code\` -- interprets the integer \`65\` as a Unicode/ASCII code -> \`'A'\`.

### Upcasting

- \`A ref = b;\` -- implicit upcast; no \`(A)\` needed.
- \`ref.showA();\` -- works because \`showA()\` is declared in \`A\`.
- \`ref.showB();\` -- would not compile: \`showB()\` is not part of \`A\`.

### Downcasting

- \`A ref = new B();\` -- upcast; reference is \`A\`, object is \`B\`.
- \`ref instanceof B\` -- confirms the real type before casting.
- \`(B) ref\` -- explicit downcast; now \`showB()\` is reachable.
- Casting a plain \`A\` to \`B\` would throw \`ClassCastException\`.

---

## How to Compile and Run

None of these files declares a package. Because each file keeps its \`A\`
and \`B\` as **static nested classes**, the files are fully self-contained -- you
can compile them individually or all at once. Compile and run from the parent
directory using the package path:

\`\`\`bash
# Compile everything in the chapter at once...
javac *.java

# ...or one file at a time, e.g.:
# javac TypeCasting.java

# Then run each program:
java TypeCasting
java UpCasting
java DownCasting
\`\`\`

### Expected Output

the chapter folder:

\`\`\`
Widening:
int a    = 10
long b   = 10
double c = 10.0

Narrowing (double -> int):
double d = 9.99
int e    = 9

Narrowing (int -> byte, overflow):
int big = 257
byte sb = 1

Narrowing (int -> char):
int code = 65
char ch  = A
\`\`\`

the chapter folder:

\`\`\`
In Class A
In Class A
In Class B
\`\`\`

the chapter folder:

\`\`\`
In Class A
In Class B
onlyA is not a B - skipping downcast
\`\`\`

---

## Common Mistakes

### Mistake 1: Expecting narrowing to round

\`\`\`java
int e = (int) 9.99;
\`\`\`

Casting a floating-point value to an integer drops the decimal part. Use
\`Math.round()\` if you want rounding.

### Mistake 2: Forgetting the explicit cast when narrowing

\`\`\`java
int big = 257;
byte sb = big; // COMPILE ERROR: possible lossy conversion
byte ok = (byte) big; // correct -- you acknowledge the risk
\`\`\`

### Mistake 3: Calling subclass methods through a superclass reference

\`\`\`java
A ref = new B();
ref.showB(); // COMPILE ERROR: showB() not visible via A
\`\`\`

The reference type limits visibility. Downcast to \`B\` first (after an
\`instanceof\` check).

### Mistake 4: Downcasting without checking the real type

\`\`\`java
A onlyA = new A();
B b = (B) onlyA;
\`\`\`

Always guard a downcast with \`instanceof\` unless you are certain of the type.

### Mistake 5: Confusing the reference type with the object type

Upcasting/downcasting never changes the object -- only the *view* of it. A \`B\`
seen through an \`A\` reference is still a \`B\`, which is why a later downcast back
to \`B\` succeeds.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`TypeCasting.java\`

Primitives only. It runs the three widening steps, then the three narrowing
ones, printing each value before and after so the truncation, the overflow and
the code-point conversion are all visible in the output rather than asserted.

\`\`\`java TypeCasting.java @run-upcasting-downcasting-type-casting
public class TypeCasting {
    public static void main(String[] args) {

        int a = 10;
        long b = a;
        double c = b;

        System.out.println("Widening:");
        System.out.println("int a    = " + a);
        System.out.println("long b   = " + b);
        System.out.println("double c = " + c);

        double d = 9.99;
        int e = (int) d;
        System.out.println("\\nNarrowing (double -> int):");
        System.out.println("double d = " + d);
        System.out.println("int e    = " + e);

        int big = 257;
        byte sb = (byte) big;
        System.out.println("\\nNarrowing (int -> byte, overflow):");
        System.out.println("int big = " + big);
        System.out.println("byte sb = " + sb);

        int code = 65;
        char ch = (char) code;
        System.out.println("\\nNarrowing (int -> char):");
        System.out.println("int code = " + code);
        System.out.println("char ch  = " + ch);
    }
}

\`\`\`

\`\`\`output @run-upcasting-downcasting-type-casting
Widening:
int a    = 10
long b   = 10
double c = 10.0

Narrowing (double -> int):
double d = 9.99
int e    = 9

Narrowing (int -> byte, overflow):
int big = 257
byte sb = 1

Narrowing (int -> char):
int code = 65
char ch  = A
\`\`\`

### \`DownCasting.java\`

\`A\` and \`B\` are declared as static nested classes so the file is self-contained.
\`main\` upcasts a \`B\` into an \`A\` reference, guards with \`instanceof\`, downcasts,
and then repeats the attempt on a plain \`A\` — which fails the guard and is
skipped rather than crashing.

\`\`\`java DownCasting.java @run-upcasting-downcasting-down-casting
public class DownCasting {

    static class A {
        public void showA() {
            System.out.println("In Class A");
        }
    }

    static class B extends A {
        public void showB() {
            System.out.println("In Class B");
        }
    }

    public static void main(String[] args) {
        A ref = new B();
        ref.showA();

        if (ref instanceof B) {
            B b = (B) ref;
            b.showB();
        }

        A onlyA = new A();
        if (onlyA instanceof B) {
            B b2 = (B) onlyA;
            b2.showB();
        } else {
            System.out.println("onlyA is not a B - skipping downcast");
        }

    }
}

\`\`\`

\`\`\`output @run-upcasting-downcasting-down-casting
In Class A
In Class B
onlyA is not a B - skipping downcast
\`\`\`

### \`UpCasting.java\`

The shorter half of the pair: assign a \`B\` to an \`A\` reference with no cast at
all, and call the one method that is visible through it.

\`\`\`java UpCasting.java @run-upcasting-downcasting-up-casting
//      Superclass ref = new Subclass();
public class UpCasting {

    static class A {
        public void showA() {
            System.out.println("In Class A");
        }
    }

    static class B extends A {
        public void showB() {
            System.out.println("In Class B");
        }
    }

    public static void main(String[] args) {
        B b = new B();

        A ref = b;

        ref.showA();

        b.showA();
        b.showB();
    }
}

\`\`\`

\`\`\`output @run-upcasting-downcasting-up-casting
In Class A
In Class A
In Class B
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- Two separate worlds: primitive casting (values) and reference casting (types).
- **Upcasting** — subclass to superclass. Always safe, always implicit.
- **Downcasting** — superclass to subclass. Explicit, and throws
  \`ClassCastException\` if the object is not really that type.
- Check first with \`instanceof\`; since Java 16 it binds the variable for you:
  \`if (a instanceof Dog d)\`.
`,interview:`## Interview questions

### Q1: What is type casting in Java?

**Answer:** Converting a value or reference from one type to another. For
primitives it changes the numeric representation (widening/narrowing); for
objects it changes the reference type used to view an object (upcasting/downcasting).

### Q2: What is the difference between widening and narrowing?

**Answer:** Widening converts a smaller primitive type to a larger one
(e.g. \`int\` -> \`double\`); it is implicit and lossless. Narrowing converts a
larger type to a smaller one (e.g. \`double\` -> \`int\`); it requires an explicit
cast and may lose data through truncation or overflow.

### Q3: Does casting a \`double\` to an \`int\` round the value?

**Answer:** No. It truncates -- the fractional part is discarded. \`(int) 9.99\`
is \`9\`. Use \`Math.round()\` for rounding.

### Q4: What is upcasting? Is a cast required?

**Answer:** Upcasting is referring to a subclass object through a superclass
reference (e.g. \`A ref = new B();\`). No explicit cast is required because it is
always safe -- a subclass object *is a* superclass.

### Q5: What is downcasting and why is it risky?

**Answer:** Downcasting converts a superclass reference back to a subclass type
to access subclass-specific members. It is risky because if the object is not
actually that subclass, Java throws a \`ClassCastException\` at runtime. It must be
written explicitly and is usually guarded with \`instanceof\`.

### Q6: Can you access subclass-specific methods through a superclass reference?

**Answer:** No. The reference type determines visibility, so only members
declared in the superclass are accessible. To use subclass-specific methods you
must downcast to the subclass type first.

### Q7: What does the \`instanceof\` operator do?

**Answer:** It returns \`true\` if an object is an instance of the specified class
or one of its subclasses (and is not \`null\`). It is the standard way to verify a
type before performing a downcast safely.

### Q8: What happens if you downcast incorrectly?

**Answer:** The code compiles (the types are related), but at runtime the JVM
throws a \`java.lang.ClassCastException\` because the object is not really an
instance of the target subclass.

### Q9: Why is upcasting implicit but downcasting explicit?

**Answer:** Upcasting is always safe -- every subclass object is also an instance
of its superclass -- so Java does it automatically. Downcasting may be invalid
(the object might not be the target subclass), so Java requires an explicit cast
to make you take responsibility for the risk.

### Q10: Does casting an object change the object itself?

**Answer:** No. Reference casting only changes how the object is *viewed* (which
members are accessible); the underlying object and its actual type are unchanged.

---

*This is part of a Java learning series. It builds on the chapters on Inheritance
and Objects, and underpins polymorphism, interfaces, and the collections
framework, where upcasting and \`instanceof\` are used constantly.*
`}},{slug:`wrapper-classes`,title:`Wrapper classes`,subject:`Java`,stage:`Types and contracts`,chapter:21,accent:`#34d399`,summary:`A wrapper class wraps a primitive value (int, double, char, ...) inside an object. Each of Java's eight primitive types has a matching wrapper class in the java.lang package.`,parts:{notes:`## Introduction\r
\r
A **wrapper class** wraps a primitive value (\`int\`, \`double\`, \`char\`, ...) inside\r
an **object**. Each of Java's eight primitive types has a matching wrapper class\r
in the \`java.lang\` package.\r
\r
The wrapper lets a primitive behave like an object: it can be stored in\r
collections, can be \`null\`, and comes with a set of handy static helper methods.\r
\r
This chapter covers boxing/unboxing and the most common conversions in a single\r
program:\r
\r
| File | Demonstrates |\r
|------|--------------|\r
| \`WrapperClasses.java\` | Autoboxing, auto-unboxing, \`parseInt\`, \`valueOf\`, and \`toString\` |\r
\r
---\r
\r
## The Eight Wrapper Classes\r
\r
| Primitive | Wrapper class |\r
|-----------|---------------|\r
| \`byte\`    | \`Byte\`        |\r
| \`short\`   | \`Short\`       |\r
| \`int\`     | \`Integer\`     |\r
| \`long\`    | \`Long\`        |\r
| \`float\`   | \`Float\`       |\r
| \`double\`  | \`Double\`      |\r
| \`char\`    | \`Character\`   |\r
| \`boolean\` | \`Boolean\`     |\r
\r
> Note the two that don't simply capitalize the primitive name: \`int\` -> **\`Integer\`**\r
> and \`char\` -> **\`Character\`**.\r
\r
---\r
\r
## Why Wrapper Classes Exist\r
\r
- **Collections store objects, not primitives.** \`ArrayList<Integer>\`,\r
  \`HashMap<String, Double>\`, etc. can only hold objects, so numbers must be\r
  wrapped.\r
- **Nullability.** A primitive \`int\` is always a number; an \`Integer\` can be\r
  \`null\` to represent "no value".\r
- **Utility methods and constants.** Wrappers provide static helpers such as\r
  \`Integer.parseInt\`, \`Integer.valueOf\`, \`Integer.MAX_VALUE\`, and\r
  \`Integer.MIN_VALUE\`.\r
\r
---\r
\r
## Boxing and Autoboxing\r
\r
**Boxing** is wrapping a primitive into its wrapper object. **Autoboxing** is Java\r
doing that automatically (since Java 5):\r
\r
\`\`\`java\r
Integer num1 = 7;\r
\`\`\`\r
\r
You should **never** use \`new Integer(...)\`; it is deprecated. Let autoboxing (or\r
\`Integer.valueOf\`) do the work.\r
\r
---\r
\r
## Unboxing and Auto-unboxing\r
\r
**Unboxing** is extracting the primitive back out of the wrapper. **Auto-unboxing**\r
is Java doing it for you:\r
\r
\`\`\`java\r
Integer num1 = 7;\r
int num2 = num1;\r
// int manual = num1.intValue(); // the explicit, manual form\r
\`\`\`\r
\r
---\r
\r
## Converting Between Strings and Numbers\r
\r
A very common task is turning text (e.g. user input \`"10"\`) into a number, or a\r
number back into text.\r
\r
### \`parseInt\` vs \`valueOf\`\r
\r
Both read a numeric \`String\`, but they return **different types**:\r
\r
\`\`\`java\r
int num3      = Integer.parseInt("10");\r
Integer num5  = Integer.valueOf("10");\r
\`\`\`\r
\r
| Method | Returns | Use when you want... |\r
|--------|---------|----------------------|\r
| \`Integer.parseInt(s)\` | primitive \`int\` | a plain number |\r
| \`Integer.valueOf(s)\`  | \`Integer\` object | a wrapper object (e.g. for a collection) |\r
\r
### \`toString\`\r
\r
Going the other way, a wrapper object can produce its text form:\r
\r
\`\`\`java\r
Integer num4 = 10;\r
String s2 = num4.toString();\r
\`\`\`\r
\r
\`s2\` is the two-character string \`"10"\`, not the number.\r
\r
---\r
\r
## Understanding the Code\r
\r
\`\`\`java\r
public class WrapperClasses {\r
    public static void main(String[] args) {\r
        int num = 7;\r
        Integer num1 = 7;\r
        int num2 = num1;\r
\r
        String str = "10";\r
        int num3 = Integer.parseInt(str);\r
\r
        Integer num4 = 10;\r
        String s2 = num4.toString();\r
\r
        String str3 = "10";\r
        Integer num5 = Integer.valueOf(str3);\r
    }\r
}\r
\`\`\`\r
\r
---\r
\r
## Line-by-Line Explanation\r
\r
- \`int num = 7;\` -- an ordinary primitive \`int\`.\r
- \`Integer num1 = 7;\` -- **autoboxing**; the \`int\` is wrapped into an \`Integer\`.\r
- \`int num2 = num1;\` -- **auto-unboxing**; the \`Integer\` is unwrapped back to \`int\`.\r
- \`Integer.parseInt(str)\` -- parses the \`String\` \`"10"\` into a **primitive** \`int\`.\r
- \`num4.toString()\` -- converts the \`Integer\` object into the \`String\` \`"10"\`.\r
- \`Integer.valueOf(str3)\` -- parses the \`String\` \`"10"\` into an **\`Integer\` object**.\r
\r
---\r
\r
## How to Compile and Run\r
\r
The file declares no package declaration. Compile and run from the parent\r
directory using the package path:\r
\r
\`\`\`bash\r
# Compile\r
javac WrapperClasses.java\r
\r
# Run\r
java WrapperClasses\r
\`\`\`\r
\r
### Expected Output\r
\r
\`\`\`\r
num  (primitive int)      = 7\r
num2 (auto-unboxed)       = 7\r
parseInt("10")            = 10\r
Integer(10).toString()    = 10\r
valueOf("10")             = 10\r
\`\`\`\r
\r
---\r
\r
## Common Mistakes\r
\r
### Mistake 1: Using \`new Integer(...)\`\r
\r
\`\`\`java\r
Integer x = new Integer(8);\r
Integer y = 8;              // correct -- autoboxing\r
Integer z = Integer.valueOf(8);\r
\`\`\`\r
\r
The constructors were deprecated because they always create a new object and\r
waste memory; \`valueOf\`/autoboxing can reuse cached objects.\r
\r
### Mistake 2: Confusing \`parseInt\` with \`valueOf\`\r
\r
\`\`\`java\r
int a      = Integer.parseInt("10");\r
Integer b  = Integer.valueOf("10");\r
\`\`\`\r
\r
\`parseInt\` returns a primitive; \`valueOf\` returns a wrapper object. Picking the\r
wrong one leads to needless boxing/unboxing.\r
\r
### Mistake 3: Parsing a non-numeric String\r
\r
\`\`\`java\r
int n = Integer.parseInt("abc");\r
\`\`\`\r
\r
\`parseInt\`/\`valueOf\` throw \`NumberFormatException\` if the text is not a valid\r
number. Validate or wrap the call in a \`try/catch\`.\r
\r
### Mistake 4: Unboxing a \`null\`\r
\r
\`\`\`java\r
Integer maybe = null;\r
int n = maybe;\r
\`\`\`\r
\r
A wrapper can be \`null\`; auto-unboxing \`null\` crashes. Check for \`null\` first.\r
\r
### Mistake 5: Comparing wrappers with \`==\`\r
\r
\`\`\`java\r
Integer a = 1000, b = 1000;\r
System.out.println(a == b);\r
System.out.println(a.equals(b));\r
\`\`\`\r
\r
Use \`.equals()\` (or unbox to primitives) to compare wrapper **values**. \`==\`\r
compares object identity, which is unreliable outside the small cached range\r
(-128..127).\r
\r
---\r
\r
## The source files\r
\r
The complete programs, renamed from the numbered file names the repository\r
uses so they compile on their own. **Save** writes the file out of this page;\r
**Run** replays the output it produced when this page was built.\r
\r
\`\`\`bash\r
javac Something.java\r
java Something\r
\`\`\`\r
\r
### \`WrapperClasses.java\`\r
\r
\`\`\`java WrapperClasses.java @run-wrapper-classes-wrapper-classes\r
public class WrapperClasses {\r
    public static void main(String[] args) {\r
\r
        int num = 7;\r
\r
        Integer num1 = 7;\r
\r
        // int num2 = num1.intValue(); // manual unboxing using intValue()\r
        int num2 = num1;\r
\r
        System.out.println("num  (primitive int)      = " + num);\r
        System.out.println("num2 (auto-unboxed)       = " + num2);\r
\r
        String str = "10";\r
        int num3 = Integer.parseInt(str);\r
        System.out.println("parseInt(\\"10\\")            = " + num3);\r
\r
        Integer num4 = 10;\r
        String s2 = num4.toString();\r
        System.out.println("Integer(10).toString()    = " + s2);\r
\r
        String str3 = "10";\r
        Integer num5 = Integer.valueOf(str3);\r
        System.out.println("valueOf(\\"10\\")             = " + num5);\r
    }\r
}\r
\r
\`\`\`\r
\r
\`\`\`output @run-wrapper-classes-wrapper-classes\r
num  (primitive int)      = 7\r
num2 (auto-unboxed)       = 7\r
parseInt("10")            = 10\r
Integer(10).toString()    = 10\r
valueOf("10")             = 10\r
\`\`\`\r
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- Eight of them, one per primitive: `Integer`, `Double`, `Character`, `Boolean`,\n  and so on.\n- They exist because collections and generics hold objects, not primitives.\n- **Autoboxing** and **auto-unboxing** convert automatically — and unboxing a\n  `null` throws `NullPointerException` on a line with no `null` in sight.\n- `Integer.valueOf` caches **-128 to 127**, which is why `==` appears to work on\n  small values and fails on large ones. Use `.equals()`.\n- `parseInt` returns a primitive; `valueOf` returns an object.\n",interview:'## Interview questions\n\n### Q1: What is a wrapper class?\n\n**Answer:** A class that wraps a primitive value inside an object. Each primitive\nhas a corresponding wrapper in `java.lang` (`int` -> `Integer`, `char` ->\n`Character`, etc.).\n\n### Q2: Why do we need wrapper classes?\n\n**Answer:** Because collections and generics work with objects, not primitives;\nbecause a wrapper can be `null`; and because wrappers provide useful static\nhelpers and constants.\n\n### Q3: What is autoboxing and auto-unboxing?\n\n**Answer:** Autoboxing is the automatic conversion of a primitive into its\nwrapper (`int` -> `Integer`); auto-unboxing is the reverse (`Integer` -> `int`).\nJava has done this automatically since Java 5.\n\n### Q4: What is the difference between `parseInt` and `valueOf`?\n\n**Answer:** `Integer.parseInt(s)` returns a primitive `int`; `Integer.valueOf(s)`\nreturns an `Integer` object. `valueOf` may also reuse cached objects for small\nvalues.\n\n### Q5: Why is `new Integer(7)` discouraged?\n\n**Answer:** The wrapper constructors are deprecated. They always allocate a new\nobject, whereas autoboxing or `valueOf` can reuse cached instances and is clearer.\n\n### Q6: What happens if you unbox a `null` wrapper?\n\n**Answer:** It throws a `NullPointerException` at runtime, because there is no\nprimitive value to extract.\n\n### Q7: Why can comparing two `Integer` objects with `==` give surprising results?\n\n**Answer:** `==` compares object references, not values. Java caches `Integer`\nobjects for values -128..127, so small equal values may share a reference and\ncompare `true`, while larger ones don\'t. Always use `.equals()` for value\ncomparison.\n\n### Q8: What exception does `parseInt` throw on bad input?\n\n**Answer:** `NumberFormatException`, thrown when the string is not a valid number.\n\n---\n\n*This is part of a Java learning series. Wrapper classes underpin the collections\nframework and generics, where every "number" you store is really a boxed object.*\n'}},{slug:`abstract-keyword`,title:`The abstract keyword`,subject:`Java`,stage:`Types and contracts`,chapter:22,accent:`#f97316`,summary:`The abstract keyword marks something as incomplete -- a partial design that must be finished by a subclass. It is the foundation of abstraction: hiding implementation detail behind a contract.`,parts:{notes:`## Introduction

The \`abstract\` keyword marks something as **incomplete** -- a partial design that
must be finished by a subclass. It is the foundation of **abstraction**: hiding
implementation detail behind a contract.

There are two things you can declare \`abstract\`:

- an **abstract class** -- a blueprint that cannot be instantiated on its own, and
- an **abstract method** -- a method with a signature but no body, which subclasses
  must implement.

This chapter demonstrates both with a small \`Car\` hierarchy:

| File | Demonstrates |
|------|--------------|
| \`AbstractKeyword.java\` | An abstract class \`Car\` with one abstract method (\`drive\`) and one concrete method (\`playMusic\`), implemented by the concrete class \`WagonR\` |

---

## Abstract Classes

An **abstract class** is declared with the \`abstract\` keyword and **cannot be
instantiated** directly:

\`\`\`java
abstract class Car {
    // Car c = new Car();   // ERROR: Car is abstract
    public abstract void drive();
    public void playMusic() {
        System.out.println("Playing Music");
    }
}
\`\`\`

\`drive()\` has no body and ends in a semicolon — every concrete subclass must
supply one. \`playMusic()\` has a body, so subclasses inherit it as it stands and
never have to think about it. \`new Car()\` will not compile.

It exists to be **extended**. It can freely mix abstract methods (which subclasses
must implement) with concrete methods (which subclasses inherit ready-made).

---

## Abstract Methods

An **abstract method** declares only a signature and ends with a semicolon -- it
has **no body**:

\`\`\`java
public abstract void drive();
\`\`\`

Every **concrete** subclass is forced to **override** it and supply a real
implementation. This is the "contract": *any real Car must define how it drives.*

---

## The Rules of \`abstract\`

1. An abstract method **can only exist inside an abstract class**.
2. You **cannot create an object** of an abstract class (\`new Car()\` fails).
3. An abstract class **may have no abstract methods at all** -- it can still be
   abstract simply to prevent instantiation.
4. But an abstract method **must** live in an abstract class.
5. The first concrete subclass **must implement every** inherited abstract method
   (otherwise it, too, must be declared abstract).

---

## Concrete Classes

A **concrete class** is a normal, fully-implemented class. When it extends an
abstract class, it must implement all the inherited abstract methods, which makes
it "complete" and therefore instantiable:

\`\`\`java
class WagonR extends Car {
    public void drive() {
        System.out.println("Driving");
    }
}
\`\`\`

---

## Using an Abstract Type

Although you can't instantiate an abstract class, you **can** use it as a
reference type and point it at a concrete subclass object (this is **upcasting**):

\`\`\`java
Car obj = new WagonR();
obj.drive();
obj.playMusic();
\`\`\`

The reference is the abstract type; the object is a real \`WagonR\`. \`drive()\`
prints \`Driving\` from \`WagonR\`'s implementation, and \`playMusic()\` prints
\`Playing Music\` from the concrete method \`Car\` provided.

This is how abstraction is used in practice: code is written against the abstract
type, while the actual behaviour comes from whichever concrete subclass is plugged
in.

---

## Understanding the Code

\`\`\`java
abstract class Car {
    public abstract void drive();
    public void playMusic() {
        System.out.println("Playing Music");
    }
}

class WagonR extends Car {
    public void drive() {
        System.out.println("Driving");
    }
}

public class AbstractKeyword {
    public static void main(String[] args) {
        Car obj = new WagonR();
        obj.drive();
        obj.playMusic();
    }
}
\`\`\`

---

## Line-by-Line Explanation

- \`abstract class Car\` -- declares an incomplete class that cannot be instantiated.
- \`public abstract void drive();\` -- an abstract method: no body; subclasses must
  implement it.
- \`public void playMusic() { ... }\` -- a concrete method inherited as-is by every
  subclass.
- \`class WagonR extends Car\` -- a concrete subclass.
- \`public void drive() { ... }\` -- implements (overrides) the abstract method.
- \`Car obj = new WagonR();\` -- upcasts the \`WagonR\` object to a \`Car\` reference.
- \`obj.drive();\` -- calls \`WagonR\`'s overridden version -> "Driving".
- \`obj.playMusic();\` -- calls \`Car\`'s inherited concrete method -> "Playing Music".

---

## How to Compile and Run

All classes declare no package declaration. Compile and run from the
parent directory using the package path:

\`\`\`bash
# Compile
javac AbstractKeyword.java

# Run
java AbstractKeyword
\`\`\`

### Expected Output

\`\`\`
Driving
Playing Music
\`\`\`

---

## Abstract Class vs Interface

| | Abstract class | Interface |
|---|----------------|-----------|
| Methods | abstract **and** concrete | abstract (plus \`default\`/\`static\` since Java 8) |
| Fields | instance fields allowed | only \`public static final\` constants |
| Inheritance | a class extends **one** | a class implements **many** |
| Constructors | yes | no |
| Use when | classes share state/behaviour and are closely related | you only need a contract many unrelated classes can fulfil |

> A useful rule of thumb: an abstract class models an **"is-a"** relationship with
> shared code; an interface models a **"can-do"** capability.

---

## Common Mistakes

### Mistake 1: Trying to instantiate an abstract class

\`\`\`java
Car c = new Car(); // COMPILE ERROR: Car is abstract; cannot be instantiated
\`\`\`

Instantiate a concrete subclass instead: \`Car c = new WagonR();\`.

### Mistake 2: Forgetting to implement an abstract method

\`\`\`java
class Sedan extends Car { } // COMPILE ERROR: must implement drive()
\`\`\`

A concrete subclass must implement **every** inherited abstract method, or be
declared \`abstract\` itself.

### Mistake 3: Giving an abstract method a body

\`\`\`java
public abstract void drive() { } // ERROR: abstract methods have NO body
\`\`\`

An abstract method ends with a semicolon and has no \`{ }\` block.

### Mistake 4: Declaring an abstract method in a non-abstract class

\`\`\`java
class Car {
    public abstract void drive(); // COMPILE ERROR
}
\`\`\`

If a class has even one abstract method, the class itself must be \`abstract\`.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`AbstractKeyword.java\`

The whole idea in one file. \`Car\` is abstract and mixes the two method kinds:
\`drive()\` is a contract with no body, \`playMusic()\` is finished behaviour every
subclass gets free. \`WagonR\` is concrete — it implements \`drive()\`, so it is
complete and can be instantiated.

Two rules the file quietly depends on: an abstract method can only live inside an
abstract class, and an abstract class is allowed to have no abstract methods at
all. The reverse is not allowed.

\`\`\`java AbstractKeyword.java @run-abstract-keyword-abstract-keyword
abstract class Car {
    public abstract void drive();

    public void playMusic() {
        System.out.println("Playing Music");
    }
}

class WagonR extends Car {
    public void drive() {
        System.out.println("Driving");
    }
}

public class AbstractKeyword {
    public static void main(String[] args) {

        Car obj = new WagonR();

        obj.drive();
        obj.playMusic();
    }
}

\`\`\`

\`\`\`output @run-abstract-keyword-abstract-keyword
Driving
Playing Music
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- An abstract class **cannot be instantiated**. It exists to be extended.
- An abstract method has no body; any concrete subclass must implement it.
- An abstract class can still have constructors, fields and ordinary methods —
  that is the difference from an interface.
- \`abstract\` and \`final\` are contradictory, as are \`abstract\` and \`private\`.
`,interview:`## Interview questions

### Q1: What is an abstract class?

**Answer:** A class declared with the \`abstract\` keyword that cannot be
instantiated. It is meant to be extended and can contain both abstract and
concrete methods.

### Q2: What is an abstract method?

**Answer:** A method declared without a body (ending in a semicolon). It defines a
contract that every concrete subclass must implement.

### Q3: Can an abstract class have a constructor?

**Answer:** Yes. The constructor is not used to create an abstract object
directly, but it runs (via \`super(...)\`) when a concrete subclass is instantiated,
to initialize inherited fields.

### Q4: Can an abstract class have no abstract methods?

**Answer:** Yes. A class can be abstract purely to prevent direct instantiation,
even if all its methods have bodies.

### Q5: Can an abstract method exist in a non-abstract class?

**Answer:** No. Any class containing an abstract method must itself be declared
\`abstract\`.

### Q6: What happens if a subclass doesn't implement all abstract methods?

**Answer:** It is still incomplete, so it must be declared \`abstract\` too;
otherwise the code won't compile.

### Q7: Can you create a reference of an abstract type?

**Answer:** Yes. You can't instantiate the abstract class, but you can use it as a
reference pointing to a concrete subclass object (\`Car obj = new WagonR();\`).

### Q8: What is the difference between an abstract class and an interface?

**Answer:** An abstract class can have instance fields, constructors, and concrete
methods, and a class can extend only one. An interface mainly declares a contract
(plus \`default\`/\`static\` methods since Java 8), holds only constants, and a class
can implement many. Use an abstract class for closely related classes that share
state/behaviour; use an interface for a capability many unrelated classes can
provide.

---

*This is part of a Java learning series. It builds on Inheritance and
Polymorphism, and leads into Interfaces -- together they form the basis of
abstraction in Java.*
`}},{slug:`inner-classes`,title:`Inner classes`,subject:`Java`,stage:`Types and contracts`,chapter:23,accent:`#a78bfa`,summary:`An inner class is a class declared inside another class. Java uses them to group a helper type with the class it belongs to, to access the enclosing object's members, and to create small one-off types without cluttering the package with named files.`,parts:{notes:`## Introduction

An **inner class** is a class declared **inside another class**. Java uses them to
group a helper type with the class it belongs to, to access the enclosing object's
members, and to create small one-off types without cluttering the package with
named files.

This chapter covers the everyday flavours across three programs:

| File | Demonstrates |
|------|--------------|
| \`InnerClass.java\` | A **member (non-static) inner class** \`B\` and a **static nested class** \`C\` inside an outer class \`A\` |
| \`AnonymousInnerClass.java\` | An **anonymous inner class** -- a one-off, unnamed subclass of \`A\` -- contrasted with a normal named subclass \`B\` |
| \`AbstractAnonymousInnerClass.java\` | An **anonymous inner class implementing an abstract class** \`A\` |

> **Important:** all three files declare a top-level \`class A\` in the **same
> package** (the chapter folder). That means they **cannot be compiled together**
> (\`javac *.java\` fails with *"duplicate class: A"*). Compile and
> run them **one file at a time** -- see [How to Compile and Run](#how-to-compile-and-run).

---

## Kinds of Inner Class

| Kind | Declared | Needs an outer instance? | Created with |
|------|----------|--------------------------|--------------|
| Member (non-static) inner | inside the class body | **yes** | \`outer.new Inner()\` |
| Static nested | inside the body, marked \`static\` | no | \`new Outer.Inner()\` |
| Anonymous | inline, no name | depends on context | \`new Type() { ... }\` |

> The **outer (top-level) class itself can never be \`static\`** -- only a *nested*
> class may carry the \`static\` keyword.

---

## Member (Non-static) Inner Class

A member inner class is tied to an **instance** of the outer class, so it can use
that object's fields and methods. To create one you need an outer object first:

\`\`\`java
A obj = new A();
A.B obj2 = obj.new B();
obj2.show();
\`\`\`

The type is written \`A.B\` because \`B\` is a member of \`A\`.

---

## Static Nested Class

A static nested class is marked \`static\` and is **not** tied to any instance, so
you create it directly:

\`\`\`java
A.C obj3 = new A.C();
obj3.show();
\`\`\`

Use a static nested class when the helper type does not need access to a specific
outer object.

---

## Anonymous Inner Class

An **anonymous inner class** defines and instantiates an unnamed subclass in a
single expression. It is the shortcut for "I need a one-off subclass right here
and will never reuse it."

\`\`\`java
A obj1 = new A() {
    public void show() {
        System.out.println("Showing in class A (Anonymous)");
    }
};
obj1.show();
\`\`\`

Writing a whole separate named subclass (\`class B extends A { ... }\`) is the
right choice when the behaviour is reused. When it is needed **only once**, an
anonymous class avoids the overhead of naming and saving an extra class.

---

## Anonymous Subclass of an Abstract Class

You cannot instantiate an abstract class with \`new A()\`. But \`new A() { ... }\`
does **not** create an \`A\`; it creates an instance of an **anonymous subclass**
that extends \`A\` and supplies the missing implementation:

\`\`\`java
abstract class A {
    abstract public void show();
}

// A obj = new A();   // ERROR: cannot instantiate the abstract class A

A obj1 = new A() {
    public void show() {
        System.out.println("Showing in class A (Anonymous)");
    }
};
obj1.show();
\`\`\`

This is the most common everyday use of anonymous classes -- providing a one-off
implementation of an abstract type or interface.

---

## Understanding the Code

### \`InnerClass.java\`

\`\`\`java
class A {
    public void show() { System.out.println("Showing in class A"); }
    class B {
        public void show()    { System.out.println("Showing in class B"); }
        public void Display() { System.out.println("Displaying in class B"); }
    }
    static class C {
        public void show() { System.out.println("Showing in class C"); }
    }
}

public class InnerClass {
    public static void main(String[] args) {
        A obj = new A();
        obj.show();

        A.B obj2 = obj.new B();
        obj2.show();
        obj2.Display();

        A.C obj3 = new A.C();
        obj3.show();
    }
}
\`\`\`

### \`AnonymousInnerClass.java\`

\`\`\`java
class A { public void show() { System.out.println("Showing in class A"); } }
class B extends A { public void show() { System.out.println("Showing in class B"); } }

public class AnonymousInnerClass {
    public static void main(String[] args) {
        A obj = new B();
        obj.show();

        A obj1 = new A() {
            public void show() { System.out.println("Showing in class A (Anonymous)"); }
        };
        obj1.show();
    }
}
\`\`\`

### \`AbstractAnonymousInnerClass.java\`

\`\`\`java
abstract class A { abstract public void show(); }

public class AbstractAnonymousInnerClass {
    public static void main(String[] args) {
        A obj1 = new A() {
            public void show() { System.out.println("Showing in class A (Anonymous)"); }
        };
        obj1.show();
    }
}
\`\`\`

---

## Line-by-Line Explanation

### File 1 -- member & static nested

- \`class B\` inside \`A\` -- a non-static inner class, bound to an \`A\` instance.
- \`static class C\` -- a static nested class, independent of any \`A\` instance.
- \`A obj = new A();\` -- ordinary outer object.
- \`obj.new B();\` -- creates the inner \`B\`; requires the outer object \`obj\`.
- \`new A.C();\` -- creates the static \`C\` directly, no outer object needed.

### File 2 -- anonymous vs named

- \`class B extends A\` -- the traditional named subclass overriding \`show()\`.
- \`A obj = new B();\` -- uses that named subclass.
- \`new A() { public void show() {...} }\` -- an anonymous subclass created inline;
  \`obj1\` is typed \`A\` but refers to this one-off subclass.

### File 3 -- anonymous + abstract

- \`abstract class A\` with \`abstract public void show();\` -- cannot be instantiated.
- \`new A() { ... }\` -- builds an anonymous subclass that implements \`show()\` and
  instantiates **that** subclass (not \`A\` itself).

---

## How to Compile and Run

Each file declares no package declaration. Because **all three files define a
top-level \`class A\`**, they conflict if compiled together -- compile and run them
**one at a time**:

\`\`\`bash
# Member & static nested inner classes
javac InnerClass.java
java InnerClass

# Anonymous inner class
javac AnonymousInnerClass.java
java AnonymousInnerClass

# Anonymous subclass of an abstract class
javac AbstractAnonymousInnerClass.java
java AbstractAnonymousInnerClass
\`\`\`

> Do **not** use \`javac *.java\` here -- it fails with
> *"duplicate class: \\_16\\_InnerClass.A"* because three files each declare \`class A\`.

### Expected Output

\`InnerClass\`:

\`\`\`
Showing in class A
Showing in class B
Displaying in class B
Showing in class C
\`\`\`

\`AnonymousInnerClass\`:

\`\`\`
Showing in class B
Showing in class A (Anonymous)
\`\`\`

\`AbstractAnonymousInnerClass\`:

\`\`\`
Showing in class A (Anonymous)
\`\`\`

---

## Common Mistakes

### Mistake 1: Creating a non-static inner class without an outer object

\`\`\`java
A.B b = new A.B();   // COMPILE ERROR
A a = new A();
A.B b2 = a.new B();  // correct
\`\`\`

A non-static inner class needs an enclosing instance; use \`outerObject.new Inner()\`.

### Mistake 2: Adding \`static\` to the outer class

\`\`\`java
static class A { ... } // ERROR at top level
\`\`\`

Only *nested* classes can be \`static\`; a top-level class cannot.

### Mistake 3: Forgetting the semicolon after an anonymous class

\`\`\`java
A obj1 = new A() {
    public void show() { ... }
}   // ERROR: missing ';'
\`\`\`

\`new Type() { ... }\` is part of a statement, so it must end with \`;\`.

### Mistake 4: Thinking \`new A() { ... }\` instantiates the abstract class

It does not. It creates an **anonymous subclass** of \`A\` and instantiates that.
The abstract \`A\` is never directly instantiated.

### Mistake 5: Compiling all files in this chapter together

\`\`\`bash
javac *.java   # fails: duplicate class A
\`\`\`

Compile each file individually, as shown above.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`InnerClass.java\`

Both member kinds in one outer class. \`B\` is the non-static inner class — it is
tied to an *instance* of \`A\`, so it can reach that object's fields, and creating
one needs the \`obj.new B()\` form. \`C\` is the static nested class — it belongs to
the class rather than to any object, so \`new A.C()\` is enough.

Note what \`A\` itself is **not**: a top-level class can never carry \`static\`. Only
nested classes can.

\`\`\`java InnerClass.java @run-inner-classes-inner-class
class A {
    public void show() {
        System.out.println("Showing in class A");
    }

    class B {
        public void show() {
            System.out.println("Showing in class B");
        }

        public void Display() {
            System.out.println("Displaying in class B");
        }
    }

    static class C {
        public void show() {
            System.out.println("Showing in class C");
        }
    }
}

public class InnerClass {
    public static void main(String[] args) {
        A obj = new A();
        obj.show();

        A.B obj2 = obj.new B();
        obj2.show();
        obj2.Display();

        A.C obj3 = new A.C();
        obj3.show();
    }
}

\`\`\`

\`\`\`output @run-inner-classes-inner-class
Showing in class A
Showing in class B
Displaying in class B
Showing in class C
\`\`\`

### \`AnonymousInnerClass.java\`

The same override done twice, so the two routes sit side by side. \`class B
extends A\` is the traditional named subclass — worth writing when the behaviour
gets reused. \`new A() { ... }\` is the anonymous form: it declares an unnamed
subclass of \`A\` and instantiates it in a single expression.

\`obj1\` is *declared* as an \`A\`, but the object behind it is that one-off
subclass. The trailing \`;\` after the closing brace is required, because the
whole construct is one statement.

\`\`\`java AnonymousInnerClass.java @run-inner-classes-anonymous-inner-class
class A {
    public void show() {
        System.out.println("Showing in class A");
    }
}

class B extends A {
    public void show() {
        System.out.println("Showing in class B");
    }
}

public class AnonymousInnerClass {
    public static void main(String[] args) {
        A obj = new B();
        obj.show();

        A obj1 = new A() {
            public void show() {
                System.out.println("Showing in class A (Anonymous)");
            }
        };
        obj1.show();
    }
}

\`\`\`

\`\`\`output @run-inner-classes-anonymous-inner-class
Showing in class B
Showing in class A (Anonymous)
\`\`\`

### \`AbstractAnonymousInnerClass.java\`

This is the two ideas combined: the \`abstract\` keyword from
[Chapter 22](#/study/abstract-keyword) and the anonymous class above.

\`A\` is abstract, so \`new A()\` will not compile. \`new A() { ... }\` does compile —
and it is not creating an \`A\`. It creates an instance of an anonymous subclass
that extends \`A\` and fills in the missing \`show()\`. That distinction is the
whole file, and it is how abstract types get used everywhere in real Java without
a named subclass for each one.

\`\`\`java AbstractAnonymousInnerClass.java @run-inner-classes-abstract-anonymous-inner-class
abstract class A {
    abstract public void show();
}

public class AbstractAnonymousInnerClass {
    public static void main(String[] args) {

        // obj.show();

        A obj1 = new A() {
            public void show() {
                System.out.println("Showing in class A (Anonymous)");
            }
        };
        obj1.show();
    }
}

\`\`\`

\`\`\`output @run-inner-classes-abstract-anonymous-inner-class
Showing in class A (Anonymous)
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- **Member inner** — needs an instance of the outer class:
  \`outer.new Inner()\`. Can see the outer object's fields.
- **Static nested** — no outer instance needed: \`new Outer.Nested()\`.
- **Anonymous** — declared and instantiated in one expression, usually to
  implement an interface or extend a class once.
- Anonymous classes produce \`Outer$1.class\` files. Lambdas do not, which is one
  visible difference between them.
`,interview:`## Interview questions

### Q1: What is an inner class?

**Answer:** A class declared inside another class. It groups a helper type with
its enclosing class and (for non-static inner classes) can access the outer
object's members.

### Q2: What is the difference between a static nested class and a non-static inner class?

**Answer:** A non-static (member) inner class is tied to an instance of the outer
class and is created with \`outerObject.new Inner()\`. A static nested class is not
tied to any instance and is created directly with \`new Outer.Inner()\`.

### Q3: How do you instantiate a non-static inner class?

**Answer:** Through an outer-class object: \`Outer o = new Outer(); Outer.Inner i =
o.new Inner();\`.

### Q4: Can a top-level class be declared \`static\`?

**Answer:** No. Only nested classes may be \`static\`; the \`static\` keyword is not
allowed on a top-level class.

### Q5: What is an anonymous inner class?

**Answer:** A class with no name that is defined and instantiated in one
expression (\`new Type() { ... }\`). It creates a one-off subclass of a class (or
implementation of an interface), typically for a single use.

### Q6: When would you use an anonymous inner class instead of a named subclass?

**Answer:** When the subclass/implementation is needed only once. Writing a
separate named class to use it a single time is unnecessary overhead; an anonymous
class keeps the one-off behaviour inline.

### Q7: Can you create an anonymous inner class from an abstract class or interface?

**Answer:** Yes. \`new AbstractType() { ... }\` creates an anonymous subclass that
implements the abstract methods and instantiates that subclass -- the abstract
type itself is never directly instantiated.

### Q8: Does \`new A() { ... }\` create an object of \`A\` when \`A\` is abstract?

**Answer:** No. It creates an instance of an anonymous subclass of \`A\` that
provides the missing implementation. \`A\` itself cannot be instantiated.

---

*This is part of a Java learning series. It builds directly on Inheritance
(Chapter 15), Polymorphism (Chapter 17), and the \`abstract\` keyword (Chapter 22),
and anonymous inner classes are the stepping stone to interfaces and lambda
expressions.*
`}},{slug:`interfaces`,title:`Interfaces`,subject:`Java`,stage:`Types and contracts`,chapter:24,accent:`#f472b6`,summary:`An interface is a fully abstract blueprint. It declares what a class can do (a set of method signatures) without saying how it does it. Any class that "signs the contract" by implementing the interface must provide the body for every method.`,parts:{notes:`## Introduction

An **interface** is a fully abstract blueprint. It declares *what* a class can do (a set of method signatures) without saying *how* it does it. Any class that "signs the contract" by implementing the interface must provide the body for every method.

Interfaces are how Java achieves **100% abstraction** and a safe form of **multiple inheritance**. This builds directly on the \`abstract\` keyword from Chapter 22.

---

## What is an Interface

An interface is declared with the \`interface\` keyword instead of \`class\`:

\`\`\`java
interface A {
    void show();
    void config();
}
\`\`\`

Key facts:

- Every method in an interface is **\`public\` and \`abstract\` by default** — you do not need to write those keywords.
- An interface cannot be instantiated with \`new\` (it has no method bodies to run).
- A class connects to an interface using the \`implements\` keyword and must override **every** method.
- Since Java 8, interfaces can also have \`default\` and \`static\` methods (with bodies), but the classic role is a pure contract.

---

## \`extends\` vs \`implements\`

The relationship keyword depends on what is connecting to what:

| From      | To        | Keyword      |
|-----------|-----------|--------------|
| class     | class     | \`extends\`    |
| class     | interface | \`implements\` |
| interface | class     | (not allowed) |
| interface | interface | \`extends\`    |

A class can \`implement\` many interfaces at once, but can only \`extend\` one class.

---

## Understanding the Code

\`\`\`java
interface A {

    String area = "In Interfaces we need to initialize the fields by default";

    void show();
    void config();
}

interface X {
    void run();
}

class B implements A, X {
    public void show() {
        System.out.println("Show");
    }

    public void config() {
        System.out.println("Config");
    }

    public void run() {
        System.out.println("Run");
    }
}

public class Interface {
    public static void main(String[] args) {
        A obj;
        // obj = new A(); // ERROR: an interface cannot be instantiated
        obj = new B();

        obj.show();
        obj.config();
    }
}
\`\`\`

---

## Line-by-Line Explanation

### \`interface A { ... }\`

- Declares an interface named \`A\`. It is a pure contract.
- \`void show();\` and \`void config();\` are abstract methods — only signatures, no bodies.

### \`class B implements A, X { ... }\`

- \`B\` promises to provide bodies for **all** methods of both \`A\` and \`X\`.
- The overriding methods must be declared \`public\`, because interface methods are \`public\` by default and an override cannot reduce visibility.

### \`A obj; obj = new B();\`

- \`obj\` is a reference of the **interface type** \`A\`.
- It points to an object of the implementing class \`B\`. This is upcasting (Chapter 20): you program to the interface, not the concrete class.
- \`obj.show()\` runs \`B\`'s version of \`show()\` at runtime (dynamic dispatch, Chapter 17).

---

## Fields in an Interface

\`\`\`java
interface A {
    String area = "...";
}
\`\`\`

- Every field in an interface is automatically **\`public static final\`** — a constant.
- Because it is \`final\`, it **must be initialised** at declaration. Leaving it uninitialised (\`int age;\`) is a compile error.

---

## Why Interface Fields Are \`public static final\`

An interface is just a **blueprint**, so its fields behave like shared constants, not per-object state:

- **Why \`static\`?** A blueprint has no objects of its own, so the field belongs to the interface itself, shared by all implementers — not duplicated per object.
- **Why \`final\`?** If implementers could change the value, the blueprint would no longer be a fixed contract. A constant keeps the contract reliable.
- **Why this matters:** if every object had its own changeable copy (say \`obj1.age = 20\`, \`obj2.age = 30\`), the "shared contract" value would be ambiguous. Making fields \`static final\` removes that ambiguity.

---

## Multiple Inheritance with Interfaces

Java does **not** allow a class to extend more than one class (to avoid the "diamond problem" of conflicting inherited code). But a class **can implement many interfaces**:

\`\`\`java
class B implements A, X { ... }
\`\`\`

Because interfaces only declare method signatures (no conflicting bodies), combining several of them is safe. This is how Java gives you multiple inheritance of *type*.

---

## Interface vs Abstract Class

| Aspect                | Interface                                  | Abstract Class                          |
|-----------------------|--------------------------------------------|-----------------------------------------|
| Keyword to connect    | \`implements\`                               | \`extends\`                               |
| Multiple inheritance  | Yes (implement many)                       | No (extend only one)                    |
| Methods               | Abstract by default (+ \`default\`/\`static\`) | Mix of abstract and concrete            |
| Fields                | \`public static final\` constants only       | Any kind of field, including state      |
| Constructor           | No                                         | Yes                                     |
| When to use           | A capability/contract many unrelated types share | A common base with shared code/state |

---

## How to Compile and Run

\`\`\`bash
javac Interface.java
java Interface
\`\`\`

### Expected Output

\`\`\`
Show
Config
\`\`\`

---

## Common Mistakes

### Mistake 1: Trying to instantiate an interface

\`\`\`java
A obj = new A();  // ERROR: A is abstract; cannot be instantiated
A obj = new B();  // correct: use an implementing class
\`\`\`

### Mistake 2: Not overriding every method

\`\`\`java
class B implements A, X {
    public void show() { ... }
}
\`\`\`

A non-abstract class must implement **all** inherited interface methods.

### Mistake 3: Reducing visibility of an overridden method

\`\`\`java
class B implements A {
    void show() { ... }  // ERROR: must be public (interface methods are public)
}
\`\`\`

### Mistake 4: Forgetting to initialise an interface field

\`\`\`java
interface A {
    int age;        // ERROR: must be initialised (it is final)
    int age = 0;    // correct
}
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Interface.java\`

Which keyword you use depends on what is on each side:

| From | To | Keyword |
|------|----|---------|
| class | class | \`extends\` |
| class | interface | \`implements\` |
| interface | class | \`implements\` |
| interface | interface | \`extends\` |

\`B implements A, X\` takes both interfaces at once â€” something a class could
never do with \`extends\`, and the main reason interfaces exist. Every method
declared in either one has to be defined in \`B\`, or the file will not compile.

Inside an interface you can drop \`public abstract\` from a method: that is what
methods are by default. Fields work the same way â€” they are implicitly
\`public static final\`, which is why \`area\` **must** be given a value at
declaration.

That default is not arbitrary. An interface is a blueprint, not an object:

- **\`static\`**, because a blueprint has no per-object state. If two classes
  implemented \`A\` and each got its own \`age\`, there would be no answer to "what
  is \`A.age\`?"
- **\`final\`**, because a blueprint that anything can rewrite is not a blueprint
  any more.

\`\`\`java Interface.java @run-interfaces-interface

// abstract class A {
//     public abstract void show();
//     public abstract void config();
// }

interface A {

    // int age; // in interfaces fields are final static by default
    String area = "In Interfaces we need to initialize the fields by default";

    void show();
    void config();
}

interface X{
    void run();
}

class B implements A,X {
    public void show(){
        System.out.println("Show");
    }

    public void config(){
        System.out.println("Config");
    }

    public void run(){
        System.out.println("Run");
    }
}

public class Interface {
    public static void main(String[] args) {
        A obj;
        // obj = new A(); // A is abstract; cannot be instantiated
        obj = new B();

        obj.show();
        obj.config();
    }
}
\`\`\`

\`\`\`output @run-interfaces-interface
Show
Config
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- A contract: what a type can do, with no instance state.
- Fields are implicitly \`public static final\`; methods were implicitly
  \`public abstract\` before default methods arrived.
- A class \`implements\` interfaces and \`extends\` one class. An interface
  \`extends\` other interfaces.
- **Multiple inheritance of type** is allowed here, because there is no state to
  conflict.
`,interview:`## Interview questions

### Q1: What is an interface in Java?

**Answer:** An interface is a reference type that declares a set of abstract methods (a contract). Classes implement the interface and provide the method bodies. By default its methods are \`public abstract\` and its fields are \`public static final\`.

### Q2: What is the difference between an interface and an abstract class?

**Answer:** A class can implement multiple interfaces but extend only one abstract class. Interfaces traditionally contain only abstract methods and constants and have no constructor, while abstract classes can have constructors, instance fields, and a mix of abstract and concrete methods. Use an interface for a capability shared by unrelated types; use an abstract class for a common base with shared code/state.

### Q3: Why are interface methods \`public\` and \`abstract\` by default?

**Answer:** An interface is a contract meant to be used by other code, so its methods are \`public\`. They are \`abstract\` because the interface only specifies *what* must be done, leaving *how* to the implementing class.

### Q4: Why are interface fields \`public static final\`?

**Answer:** An interface is a blueprint with no objects of its own, so fields are \`static\` (shared) and \`final\` (constant) to keep the contract fixed and unambiguous. Because they are \`final\`, they must be initialised when declared.

### Q5: How does Java achieve multiple inheritance through interfaces?

**Answer:** A class can implement several interfaces at once (\`class B implements A, X\`). Since interfaces declare only method signatures and no conflicting bodies, there is no diamond-problem ambiguity, so combining them is safe.

### Q6: Can an interface extend another interface?

**Answer:** Yes. An interface uses \`extends\` (not \`implements\`) to inherit from one or more other interfaces, combining their contracts.

### Q7: Can you create an object of an interface?

**Answer:** No. An interface cannot be instantiated because its methods have no bodies. You instantiate a class that implements it, and you may hold that object in a reference variable of the interface type.
`}},{slug:`enums`,title:`Enums`,subject:`Java`,stage:`Types and contracts`,chapter:25,accent:`#facc15`,summary:`An enum (short for enumeration) is a special type that represents a fixed set of named constants. When a variable can only take one value out of a small, known list — the status of a request, the days of the week, the suits in a deck of cards — an enum makes that list explicit and type-safe.`,parts:{notes:`## Introduction

An **enum** (short for *enumeration*) is a special type that represents a fixed set of named constants. When a variable can only take one value out of a small, known list — the status of a request, the days of the week, the suits in a deck of cards — an enum makes that list explicit and type-safe.

This chapter has two programs:

| File | Topic |
|------|-------|
| \`Enums.java\` | A basic enum, its methods, comparison, and use in \`switch\` |
| \`Enums2.java\` | An enum with fields, constructors, and methods |

---

## What is an Enum

An enum is declared with the \`enum\` keyword. Each named value listed inside is a constant.

\`\`\`java
enum Status {
    Running, Failed, Pending, Success;
}
\`\`\`

Here \`Status\` behaves like a class, and \`Running\`, \`Failed\`, \`Pending\`, and \`Success\` are its only objects. You can never create another \`Status\` value beyond these four.

\`\`\`java
Status s = Status.Running;
\`\`\`

---

## Why Use Enums

Imagine a server that returns a status. Without enums you might use raw numbers or strings:

\`\`\`java
int status = 1;
String status = "Runing";
\`\`\`

With an enum:

\`\`\`java
Status status = Status.Running;
\`\`\`

Benefits:

- **Type safety** – only the defined values are allowed; a typo is a compile error.
- **Readability** – \`Status.Running\` is clearer than \`1\`.
- **Fixed set** – the compiler guarantees no other values exist.
- **Works with \`switch\`** – clean branching over the constants.

> Enums **cannot be extended** — you cannot add new constants by subclassing. The set is final by design.

---

## Example 1: A Basic Enum

\`\`\`java
enum Status {
    Running, Failed, Pending, Success;
}

public class Enums {
    public static void main(String[] args) {
        Status s = Status.Running;

        System.out.println(s);
        System.out.println(s.ordinal());

        Status[] all = Status.values();
        for (Status status : all) {
            System.out.println(status);
        }

        if (s == Status.Running) {
            System.out.println("Running");
        }

        switch (s) {
            case Running: System.out.println("Running"); break;
            case Failed:  System.out.println("Failed");  break;
            case Pending: System.out.println("Pending"); break;
            case Success: System.out.println("Success"); break;
            default:      System.out.println("Unknown");
        }
    }
}
\`\`\`

---

## Useful Enum Methods

Every enum automatically gets these methods:

| Method        | Returns                                              | Example                       |
|---------------|-----------------------------------------------------|-------------------------------|
| \`name()\`      | the exact name of the constant as a \`String\`        | \`Status.Running.name()\` -> \`"Running"\` |
| \`ordinal()\`   | the zero-based position in the declaration order    | \`Status.Running.ordinal()\` -> \`0\` |
| \`values()\`    | an array of all constants (static method)           | \`Status.values()\`            |
| \`valueOf("x")\`| the constant matching the given name                | \`Status.valueOf("Failed")\`   |

> Do **not** use \`ordinal()\` as a stored/business value. If you reorder the constants, every ordinal changes. Use it only for display or iteration.

---

## Comparing Enums

Enum constants are singletons (only one object per constant), so you can compare them with \`==\`:

\`\`\`java
if (s == Status.Running) { ... }
\`\`\`

\`==\` is safe here, is null-safe, and reads clearly. \`.equals()\` also works but \`==\` is the idiomatic choice for enums.

---

## Enums in a \`switch\`

A \`switch\` pairs naturally with an enum. Inside the \`case\` labels you write the constant name **without** the enum prefix:

\`\`\`java
switch (s) {
    case Running:
        ...
        break;
}
\`\`\`

The compiler also helps you spot a \`switch\` that forgets one of the constants.

---

## Example 2: Enums with Fields and Constructors

Enums are full classes, so each constant can carry data. Here each laptop constant stores a price.

\`\`\`java
enum Laptop {
    Alienware(120000),
    Lenovo(100000),
    HP,
    Dell(130000);

    private int price;

    Laptop() {
        this.price = 110000;
    }

    Laptop(int price) {
        this.price = price;
    }

    public int getPrice() {
        return price;
    }
}

public class Enums2 {
    public static void main(String[] args) {
        Laptop lap = Laptop.Alienware;
        System.out.println(lap + " : " + lap.getPrice());
        System.out.println(lap.ordinal());
        System.out.println(lap.name());
    }
}
\`\`\`

**What happens:**

- The value in parentheses after a constant (\`Alienware(120000)\`) is passed to the matching enum **constructor**.
- \`HP\` has no parentheses, so the **no-argument constructor** runs and assigns the default price \`110000\`.
- An enum constructor is always **private** in effect — you can never call \`new Laptop()\` yourself. The constants are created once, when the enum is loaded.

---

## How Enums Work Internally

Behind the scenes, an enum is compiled to a special \`final\` class that extends \`java.lang.Enum\`. Each constant becomes a \`public static final\` object of that class, created once. This is why:

- The set of values is fixed and an enum **cannot be subclassed**.
- Constants are singletons, so \`==\` comparison is correct.
- They can have fields, constructors, and methods like any other class.

---

## How to Compile and Run

\`\`\`bash
javac Enums.java
java Enums

javac Enums2.java
java Enums2
\`\`\`

### Expected Output (Example 2)

\`\`\`
Alienware : 120000
0
Alienware
\`\`\`

---

## Common Mistakes

### Mistake 1: Trying to instantiate an enum

\`\`\`java
Status s = new Status();  // ERROR: enum constructors are not accessible
Status s = Status.Running; // correct
\`\`\`

### Mistake 2: Prefixing the enum name inside a \`switch\` case

\`\`\`java
switch (s) {
    case Status.Running:  // ERROR
    case Running:         // correct
}
\`\`\`

### Mistake 3: Relying on \`ordinal()\` as a permanent value

\`\`\`java
\`\`\`

### Mistake 4: Forgetting the semicolon before members

\`\`\`java
enum Laptop {
    HP, Dell   // ERROR: need a ';' here before the field/constructor
    private int price;
}
\`\`\`

When an enum has fields or methods, the list of constants must end with a semicolon.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Enums.java\`

An \`enum\` is a fixed set of named constants â€” here the four states a job can be
in. Because the set is closed, the compiler knows every possible value, which is
what makes an enum safer than passing strings or \`int\` codes around.

The file prints one of the constants and shows it working with a \`switch\`, where
you write the bare constant name rather than qualifying it.

\`\`\`java Enums.java @run-enums-enums

enum Status {
    Running, Failed, Pending, Success;
}

public class Enums {
    public static void main(String[] args) {
        int i = 5;

        Status s = Status.Running;

        System.out.println(s);
        System.out.println(s.ordinal());

        Status[] ss = Status.values();
        for (Status status : ss) {
            System.out.println(status);
        }

        if (s == Status.Running) {
            System.out.println("Running");
        }

        switch (s) {
            case Running:
                System.out.println("Running");
                break;
            case Failed:
                System.out.println("Failed");
                break;
            case Pending:
                System.out.println("Pending");
                break;
            case Success:
                System.out.println("Success");
                break;
            default:
                System.out.println("Unknown");
        }
    }
}
\`\`\`

\`\`\`output @run-enums-enums
Running
0
Running
Failed
Pending
Success
Running
Running
\`\`\`

### \`Enums2.java\`

An enum is really a class, and this one proves it: each constant carries a
price, passed as a **constructor argument**. That means an enum can have fields,
a constructor and methods like any other type.

Look at \`HP\` â€” it is declared with no argument, so it needs a constructor that
takes none. Add data to an enum and every constant must match some constructor
you have provided.

\`\`\`java Enums2.java @run-enums-enums2
enum Laptop {
    Alienware(120000),
    Lenovo(100000),
    HP,
    Dell(130000);

    private int price;

    Laptop() {
        this.price = 110000;
    }

    Laptop(int price) {
        this.price = price;
    }

    public int getPrice() {
        return price;
    }
}

public class Enums2 {
    public static void main(String[] args) {
        Laptop lap = Laptop.Alienware;
        System.out.println(lap + " : " + lap.getPrice());
        System.out.println(lap.ordinal());
        System.out.println(lap.name());
    }
}
\`\`\`

\`\`\`output @run-enums-enums2
Alienware : 120000
0
Alienware
\`\`\`
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- A fixed set of named constants, and a real class underneath — it can have\n  fields, constructors and methods.\n- `values()`, `ordinal()`, `name()`, `valueOf()` are the four you will use.\n- Compare with `==`; enum constants are singletons.\n- `switch` on an enum takes the bare constant name, not `Enum.CONSTANT`.\n- Never persist `ordinal()` — reordering the constants silently changes it.\n",interview:`## Interview questions

### Q1: What is an enum in Java?

**Answer:** An enum is a special type that defines a fixed set of named constants. Each constant is a singleton object of the enum type. Enums are type-safe and are used when a variable should only hold one value from a known list.

### Q2: Can an enum have a constructor?

**Answer:** Yes. An enum can have constructors, fields, and methods. The constructor is effectively private and is called once for each constant when the enum is loaded. You provide arguments in parentheses after the constant name.

### Q3: Why can't you create an enum object with \`new\`?

**Answer:** Enum constructors are not accessible from outside the enum. The fixed set of constants is created automatically when the enum class is loaded, so creating more instances would break the guarantee that the value set is fixed.

### Q4: What is the difference between \`name()\` and \`ordinal()\`?

**Answer:** \`name()\` returns the constant's name as a String; \`ordinal()\` returns its zero-based position in the declaration order. \`ordinal()\` should not be used as a stored business value because it changes if the constants are reordered.

### Q5: Can you use an enum in a \`switch\` statement?

**Answer:** Yes. Enums work cleanly in a \`switch\`, and the case labels use the constant name without the enum prefix (e.g. \`case Running:\`).

### Q6: Can an enum implement an interface or extend a class?

**Answer:** An enum can implement interfaces, but it cannot extend a class because it already implicitly extends \`java.lang.Enum\`. For the same reason, an enum cannot be subclassed.

### Q7: How do you compare two enum values, and why?

**Answer:** Use \`==\`. Because each enum constant is a singleton, \`==\` compares the correct identity, is null-safe, and is the idiomatic approach. \`.equals()\` works too but is not needed.

---

*This is the final chapter of the Java learning series. Congratulations on completing it — revisit any chapter to reinforce the fundamentals.*
`}},{slug:`annotations`,title:`Annotations`,subject:`Java`,stage:`Types and contracts`,chapter:26,accent:`#22d3ee`,summary:`An annotation is extra information you attach to your code using the @ symbol. The most important idea to understand on day one:`,parts:{notes:`## Introduction

An **annotation** is **extra information you attach to your code** using the \`@\` symbol. The most important idea to understand on day one:

> An annotation by itself **does nothing**. It is just a label. Its power comes from **someone reading that label and reacting to it**.

Think of an annotation like a **sticky note** on a folder, or a **luggage tag** on a suitcase. The note does not move the suitcase — but the airport staff who *read* the tag know where to send it.

This whole chapter lives in **one runnable file**: [\`Annotations.java\`](Annotations.java). Run it once and you will watch all five topics print their results in order.

---

## What Is an Annotation

You attach an annotation by writing \`@Name\` directly above a class, method, field, or parameter:

\`\`\`java
@Override
public void shows() { ... }

@Deprecated
public void payOld() { ... }
\`\`\`

Some annotations can also carry **values** inside parentheses:

\`\`\`java
@Author(name = "Aditya", date = "2026-06-03")
class TaskRunner { }
\`\`\`

That is all the syntax there is. The interesting part is *who reads it*.

---

## Who Reads Annotations

There are exactly **three** audiences that read annotations. Knowing which audience an annotation is for explains everything about how it behaves.

| # | Reader | Example annotations | When it acts |
|---|--------|---------------------|--------------|
| 1 | **The compiler** | \`@Override\`, \`@Deprecated\`, \`@SuppressWarnings\` | While you compile |
| 2 | **Tools & frameworks** | JUnit \`@Test\`, Spring \`@Autowired\`, JPA \`@Entity\` | When the tool runs |
| 3 | **Your own code** (via Reflection) | any custom annotation you write | While the program runs |

This chapter shows all three: the compiler ones (Sections 1–2), how to *build* annotations for the framework/runtime audience (Sections 3–4), and how to *read them yourself* at runtime (Section 5).

---

## Section 1: \`@Override\`

\`@Override\` is the annotation you will use the most. It tells the compiler:

> "I am **on purpose** replacing a method from my parent class. Please **check** that a matching method really exists up there."

### The bug it catches

Our code has a parent \`A\` with a method named \`shows()\` (note the **'s'**):

\`\`\`java
class A {
    public void shows() {
        System.out.println("In A.shows()");
    }
}

class B extends A {
    @Override
    public void shows() {
        System.out.println("In B.shows() (correctly overrides A.shows())");
    }
}
\`\`\`

Now imagine you **mistype** the name in \`B\` as \`show()\` (missing the 's'):

| Without \`@Override\` | With \`@Override\` |
|---------------------|------------------|
| Compiler stays silent. It thinks \`show()\` is a brand-new method. \`A\`'s old \`shows()\` keeps running. **Silent bug** that you only notice much later. | Compiler **immediately errors**: *"method does not override a method from its superclass"*. You fix the typo in seconds. |

That is the whole point: **\`@Override\` turns a silent runtime bug into a loud compile-time error.**

> 💡 \`@Override\` is a *compile-time-only* check. It is thrown away by the compiler and never reaches the running program (this is called \`SOURCE\` retention — see Section 3).

---

## Section 2: The Other Built-in Annotations

Java ships four more everyday annotations in the \`java.lang\` package. All four are read by the **compiler**.

### \`@Deprecated\` — "old, don't use me"

Marks something as outdated so the compiler warns anyone who uses it.

\`\`\`java
@Deprecated(since = "2.0", forRemoval = true)
public void payOld() { ... }

public void pay() { ... }
\`\`\`

- \`since\` — the version it became outdated.
- \`forRemoval = true\` — it is scheduled to be **deleted** in a future version.

> ⚠️ **Advanced nuance (real Java behaviour):** when \`forRemoval = true\`, the warning belongs to the **\`"removal"\`** category, **not** \`"deprecation"\`. So to silence it you suppress \`"removal"\`, not \`"deprecation"\`. The code comments call this out.

### \`@SuppressWarnings\` — "mute these warnings here"

Tells the compiler to hide specific warning types for one element.

\`\`\`java
@SuppressWarnings({ "removal", "unused" })
public static void main(String[] args) { ... }
\`\`\`

Common values: \`"deprecation"\`, \`"removal"\`, \`"unchecked"\`, \`"unused"\`, \`"all"\`.
Use it **sparingly** — a hidden warning is a warning you chose to take responsibility for.

### \`@SafeVarargs\` — "my generic varargs is safe"

A generic varargs parameter \`(T...)\` makes Java nervous (it warns about "unchecked / heap pollution"). If you know your method only **reads** the values, this annotation promises that and silences the warning.

\`\`\`java
@SafeVarargs
private static <T> List<T> listOf(T... items) {
    List<T> list = new ArrayList<>();
    for (T item : items) list.add(item);
    return list;
}
\`\`\`

It is only allowed on methods that **can't be overridden** (\`static\`, \`final\`, or \`private\`), so the promise can't be broken by a subclass.

### \`@FunctionalInterface\` — "exactly one abstract method"

Declares that an interface is meant to have **a single abstract method** so it can be used with a lambda. The compiler then **enforces** that rule. (Full treatment in [Chapter 27](#/study/interface-types).)

\`\`\`java
@FunctionalInterface
interface Calculator {
    int operate(int a, int b);
    default void banner() { ... }
}

Calculator add = (x, y) -> x + y;
\`\`\`

---

## Section 3: Meta-Annotations

A **meta-annotation** is **an annotation that you put on *another annotation*** to configure how it behaves. Think of them as the **settings** for the custom labels you create. There are five.

### \`@Retention\` — how long the annotation lives

This is the most important meta-annotation. It has three settings:

| Policy | Kept in \`.class\` file? | Visible at runtime? | Example / use |
|--------|------------------------|---------------------|---------------|
| \`SOURCE\`  | ❌ no  | ❌ no | \`@Override\` — compiler check only, then discarded |
| \`CLASS\`   | ✅ yes | ❌ no | the **default** if you don't specify |
| \`RUNTIME\` | ✅ yes | ✅ **yes** | needed so **frameworks/reflection can read it** |

> 🔑 **Rule of thumb:** if you want to read an annotation with reflection (Section 5), it **must** be \`@Retention(RUNTIME)\`. This single fact explains 90% of "why is my annotation not being found?" problems.

### \`@Target\` — where the annotation may be used

Restricts an annotation to certain places. If you use it elsewhere, it's a compile error.

\`\`\`java
@Target({ ElementType.TYPE, ElementType.METHOD })
@interface Author { ... }
\`\`\`

Common targets: \`TYPE\` (class/interface/enum), \`METHOD\`, \`FIELD\`, \`PARAMETER\`, \`CONSTRUCTOR\`.

### \`@Documented\` — show it in Javadoc

Asks the Javadoc tool to display the annotation in the generated API documentation.

### \`@Inherited\` — let subclasses inherit it

Normally a subclass does **not** see an annotation placed on its parent. \`@Inherited\` flips that:

\`\`\`java
@Inherited
@interface Framework { String value(); }

@Framework("Spring")
class BaseEntity { }

class ChildEntity extends BaseEntity { }
\`\`\`

Yet asking \`ChildEntity\` for \`@Framework\` returns **"Spring"**, because the parent's annotation is \`@Inherited\`. (Works for class annotations only.)

### \`@Repeatable\` — apply the same annotation many times

By default you can use an annotation on something only once. \`@Repeatable\` allows repeats, but you must also create a **container** annotation to hold them:

\`\`\`java
@Repeatable(Schedules.class)
@interface Schedule { String day(); }

@interface Schedules { Schedule[] value(); }

@Schedule(day = "Monday")
@Schedule(day = "Friday")
class TaskRunner { }
\`\`\`

You read all repeats at once with \`getAnnotationsByType(Schedule.class)\`.

---

## Section 4: Writing Your Own Annotation

You declare an annotation with **\`@interface\`** (the \`@\` is part of it). Inside, each "method" is an **element** — a named attribute you can set.

\`\`\`java
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.METHOD)
@interface TestCase {
    String description();
    Priority priority() default Priority.MEDIUM;
    int timeoutMillis() default 1000;
    String[] tags() default {};
}
\`\`\`

\`description()\` has no \`default\`, so it is **required** — every use must supply
it. The other three all have defaults, so they are **optional**: an enum
default, a primitive default, and an empty-array default.

### Element rules (the things people forget)

- An element's type must be a **primitive, \`String\`, \`Class\`, an enum, another annotation, or an array** of those.
- Elements have **no parameters and no body**.
- \`default <value>\` makes an element **optional**. With no default, it is **required**.
- An element named exactly **\`value\`** can be set without writing \`value=\`:

\`\`\`java
@interface Role { String value(); }

@Role("ADMIN")
\`\`\`

### Using them

\`\`\`java
@TestCase(
    description = "login succeeds with valid credentials",
    priority = Priority.HIGH,
    tags = { "auth", "smoke" })
public void testLogin() { }

@TestCase(description = "cart total adds up")
public void testCart() { }
\`\`\`

---

## Section 5: Reading Annotations by Reflection

This is **why annotations exist**. Frameworks like JUnit, Spring, and Jackson read *your* annotations at runtime using **Reflection** (the ability of a running program to inspect its own classes, methods, and fields) and then act on them.

In the file we build **two tiny frameworks ourselves** to prove the idea.

### Mini-framework 1: a JSON serializer driven by \`@JsonField\`

\`\`\`java
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
@interface JsonField { String name() default ""; }

class Account {
    @JsonField(name = "account_id") int id;
    @JsonField                      String owner;
    @JsonField(name = "balance")    int balance;
    String internalNote;
}
\`\`\`

\`owner\` is tagged with no \`name\`, so its default \`""\` applies and the key falls
back to the real field name. \`internalNote\` is tagged with nothing at all.

The serializer walks every field, and **only fields tagged with \`@JsonField\`** appear in the JSON:

\`\`\`text
{"account_id": 1, "owner": "Ada", "balance": 500}
\`\`\`

Notice \`internalNote\` is **missing** — proving the output is driven by the annotation, not by the raw field list.

### Mini-framework 2: a validator driven by \`@Range\`

\`\`\`java
@interface Range { int min(); int max(); }

@Range(min = 0, max = 1_000_000)
int balance;
\`\`\`

The validator reads each \`@Range\` and reports any value outside the bounds:

\`\`\`text
   OK     : balance = 500
   INVALID: balance = -50 (allowed 0..1000000)
\`\`\`

### The key reflection calls

| Call | What it does |
|------|--------------|
| \`SomeClass.class.getAnnotation(X.class)\` | get one annotation on a class (or \`null\`) |
| \`method.getAnnotation(X.class)\` | get an annotation on a method |
| \`field.getAnnotation(X.class)\` | get an annotation on a field |
| \`SomeClass.class.getAnnotationsByType(X.class)\` | get **all** repeats of a \`@Repeatable\` annotation |
| \`field.setAccessible(true)\` then \`field.get(obj)\` | read the field's value |

> 🔑 Remember: **none** of this works unless the annotation is \`@Retention(RUNTIME)\`.

---

## How to Compile and Run

From the repository root (\`d:\\Repos\\java\`):

\`\`\`bash
javac Annotations.java
java Annotations
\`\`\`

### Expected Output

\`\`\`text
===== SECTION 1 : @Override =====
In B.shows() (correctly overrides A.shows())
In B.shows() (correctly overrides A.shows())

===== SECTION 2 : other built-in annotations =====
Old payment flow (DEPRECATED - will be removed)
New payment flow
listOf(...) = [Ada, Linus, Grace]
[Calculator] combines two ints into one
add(2, 3) = 5
mul(2, 3) = 6

===== SECTION 3 : meta-annotations =====
@Author name = Aditya, date = 2026-06-03
BaseEntity  @Framework = Spring
ChildEntity @Framework = Spring   <-- inherited via @Inherited
TaskRunner runs on 2 day(s):
   - Monday
   - Friday

===== SECTION 4 : custom annotations =====
Class-level @Role value = ADMIN
Scanning methods for @TestCase ...
  testLogin() -> desc='login succeeds with valid credentials', priority=HIGH, timeout=1000, tags=[auth, smoke]
  testCart() -> desc='cart total adds up', priority=MEDIUM, timeout=1000, tags=[]

===== SECTION 5 : reflection mini-framework =====
JSON (driven by @JsonField):
  {"account_id": 1, "owner": "Ada", "balance": 500}
  {"account_id": 2, "owner": "Linus", "balance": -50}
Validation (driven by @Range):
 good account:
   OK     : balance = 500
 bad account:
   INVALID: balance = -50 (allowed 0..1000000)
\`\`\`

> The order of \`testLogin\` / \`testCart\` in Section 4 may swap — \`getDeclaredMethods()\` does not guarantee an order.

---

## Quick Reference Tables

### All annotations used in this chapter

| Annotation | Kind | One-line meaning |
|-----------|------|------------------|
| \`@Override\` | built-in | "I really override a parent method" (compiler checks) |
| \`@Deprecated\` | built-in | "old, don't use me" |
| \`@SuppressWarnings\` | built-in | "hide these warnings here" |
| \`@SafeVarargs\` | built-in | "my generic varargs is type-safe" |
| \`@FunctionalInterface\` | built-in | "this interface has exactly one abstract method" |
| \`@Retention\` | meta | how long the annotation lives |
| \`@Target\` | meta | where the annotation may be used |
| \`@Documented\` | meta | show it in Javadoc |
| \`@Inherited\` | meta | subclasses inherit it |
| \`@Repeatable\` | meta | may be applied more than once |
| \`@Author\`, \`@TestCase\`, \`@Role\`, \`@JsonField\`, \`@Range\` | custom | annotations we built ourselves |

### \`@Retention\` cheat-sheet

| Policy | Survives to runtime? | Use when |
|--------|----------------------|----------|
| \`SOURCE\` | No | Pure compiler checks (\`@Override\`) |
| \`CLASS\` (default) | No | You rarely need this explicitly |
| \`RUNTIME\` | **Yes** | **Anything read by reflection / frameworks** |

---

## Common Mistakes

### Mistake 1: Expecting an annotation to "do" something on its own

\`\`\`java
@Range(min = 0, max = 100)
int balance = -50;
\`\`\`

An annotation is just a label. **Something must read it** (our \`validate()\` method) for it to have any effect.

### Mistake 2: Forgetting \`@Retention(RUNTIME)\`

\`\`\`java
@interface JsonField { }
field.getAnnotation(JsonField.class);
\`\`\`

If reflection should see it, you **must** write \`@Retention(RetentionPolicy.RUNTIME)\`.

### Mistake 3: Suppressing the wrong warning category

\`\`\`java
@Deprecated(forRemoval = true)
@SuppressWarnings("deprecation")          // does NOT silence it!
@SuppressWarnings("removal")              // correct
\`\`\`

### Mistake 4: Giving a required element no value

\`\`\`java
@interface TestCase { String description(); }

@TestCase                                       // ERROR: description is missing
@TestCase(description = "ok")                    // correct
\`\`\`

### Mistake 5: Using an annotation where its \`@Target\` forbids it

\`\`\`java
@Target(ElementType.METHOD)
@interface TestCase { ... }

@TestCase                       // ERROR if placed on a field/class
int x;
\`\`\`

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`Annotations.java\`

Everything above, in one runnable file. It is laid out the way Java requires —
every supporting type first, then the single \`public\` class whose \`main\` runs
the five demos in order and prints a banner for each.

A few things in it exist only to be demonstrated, and are easy to misread as
mistakes:

- **\`A.shows()\` has a trailing \`s\`.** That is the typo trap from Section 1. Drop
  the \`s\` from \`B\`'s copy and \`@Override\` turns a silent bug into a compile
  error.
- **\`int neverUsed = 42;\`** is never used on purpose — it is what the \`"unused"\`
  half of \`@SuppressWarnings\` is hiding.
- **\`@SuppressWarnings({ "removal", "unused" })\`** says \`removal\`, not
  \`deprecation\`. \`payOld()\` is marked \`forRemoval = true\`, and that puts its
  warning in a different lint category.
- **\`Account.internalNote\` carries no \`@JsonField\`.** It is left bare so the JSON
  output proves the serializer is driven by the annotation rather than by the
  field list.
- **\`ChildEntity\` declares no \`@Framework\`** and still reports one, because
  \`@Framework\` is \`@Inherited\`.


\`\`\`java Annotations.java @run-annotations-annotations
import java.lang.annotation.Documented;
import java.lang.annotation.ElementType;
import java.lang.annotation.Inherited;
import java.lang.annotation.Repeatable;
import java.lang.annotation.Retention;
import java.lang.annotation.RetentionPolicy;
import java.lang.annotation.Target;
import java.lang.reflect.Field;
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

class A {
    public void shows() {
        System.out.println("In A.shows()");
    }
}

class B extends A {

    @Override
    public void shows() {
        System.out.println("In B.shows() (correctly overrides A.shows())");
    }
}

class LegacyPaymentApi {

    @Deprecated(since = "2.0", forRemoval = true)
    public void payOld() {
        System.out.println("Old payment flow (DEPRECATED - will be removed)");
    }

    public void pay() {
        System.out.println("New payment flow");
    }
}

@FunctionalInterface
interface Calculator {
    int operate(int a, int b);

    default void banner() {
        System.out.println("[Calculator] combines two ints into one");
    }
}

@Documented
@Retention(RetentionPolicy.RUNTIME)
@Target({ ElementType.TYPE, ElementType.METHOD })
@interface Author {
    String name();
    String date() default "unknown";
}

@Inherited
@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.TYPE)
@interface Framework {
    String value();
}

@Framework("Spring")
class BaseEntity {
}

class ChildEntity extends BaseEntity {
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.TYPE)
@Repeatable(Schedules.class)
@interface Schedule {
    String day();
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.TYPE)
@interface Schedules {
    Schedule[] value();
}

@Author(name = "Aditya", date = "2026-06-03")
@Schedule(day = "Monday")
@Schedule(day = "Friday")
class TaskRunner {
}

enum Priority {
    LOW, MEDIUM, HIGH
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.METHOD)
@interface TestCase {
    String description();
    Priority priority() default Priority.MEDIUM;
    int timeoutMillis() default 1000;
    String[] tags() default {};
}

@Retention(RetentionPolicy.RUNTIME)
@Target({ ElementType.TYPE, ElementType.METHOD })
@interface Role {
    String value();
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
@interface JsonField {
    String name() default "";
}

@Retention(RetentionPolicy.RUNTIME)
@Target(ElementType.FIELD)
@interface Range {
    int min();
    int max();
}

class Account {

    @JsonField(name = "account_id")
    int id;

    @JsonField
    String owner;

    @JsonField(name = "balance")
    @Range(min = 0, max = 1_000_000)
    int balance;

    String internalNote;

    Account(int id, String owner, int balance, String internalNote) {
        this.id = id;
        this.owner = owner;
        this.balance = balance;
        this.internalNote = internalNote;
    }
}

@Role("ADMIN")
public class Annotations {

    @TestCase(
            description = "login succeeds with valid credentials",
            priority = Priority.HIGH,
            tags = { "auth", "smoke" })
    public void testLogin() {
    }

    @TestCase(description = "cart total adds up")
    public void testCart() {
    }

    @SafeVarargs
    private static <T> List<T> listOf(T... items) {
        List<T> list = new ArrayList<>();
        for (T item : items) {
            list.add(item);
        }
        return list;
    }

    static String toJson(Object obj) throws IllegalAccessException {
        StringBuilder json = new StringBuilder("{");
        boolean first = true;

        for (Field field : obj.getClass().getDeclaredFields()) {
            JsonField jf = field.getAnnotation(JsonField.class);
            if (jf == null) {
                continue;
            }
            field.setAccessible(true);
            String key = jf.name().isEmpty() ? field.getName() : jf.name();
            Object value = field.get(obj);

            if (!first) {
                json.append(", ");
            }
            first = false;

            if (value instanceof String) {
                json.append('"').append(key).append("\\": \\"").append(value).append('"');
            } else {
                json.append('"').append(key).append("\\": ").append(value);
            }
        }
        return json.append("}").toString();
    }

    static void validate(Object obj) throws IllegalAccessException {
        for (Field field : obj.getClass().getDeclaredFields()) {
            Range range = field.getAnnotation(Range.class);
            if (range == null) {
                continue;
            }
            field.setAccessible(true);
            int value = field.getInt(obj);
            if (value < range.min() || value > range.max()) {
                System.out.println("   INVALID: " + field.getName() + " = " + value
                        + " (allowed " + range.min() + ".." + range.max() + ")");
            } else {
                System.out.println("   OK     : " + field.getName() + " = " + value);
            }
        }
    }

    @SuppressWarnings({ "removal", "unused" })
    public static void main(String[] args) throws IllegalAccessException {

        System.out.println("===== SECTION 1 : @Override =====");
        B b = new B();
        b.shows();
        A upcast = new B();
        upcast.shows();

        System.out.println("\\n===== SECTION 2 : other built-in annotations =====");

        LegacyPaymentApi api = new LegacyPaymentApi();
        api.payOld();
        api.pay();

        int neverUsed = 42;

        List<String> names = listOf("Ada", "Linus", "Grace");
        System.out.println("listOf(...) = " + names);

        Calculator add = (x, y) -> x + y;
        Calculator mul = (x, y) -> x * y;
        add.banner();
        System.out.println("add(2, 3) = " + add.operate(2, 3));
        System.out.println("mul(2, 3) = " + mul.operate(2, 3));

        System.out.println("\\n===== SECTION 3 : meta-annotations =====");

        Author author = TaskRunner.class.getAnnotation(Author.class);
        System.out.println("@Author name = " + author.name() + ", date = " + author.date());

        Framework base = BaseEntity.class.getAnnotation(Framework.class);
        Framework child = ChildEntity.class.getAnnotation(Framework.class);
        System.out.println("BaseEntity  @Framework = " + (base != null ? base.value() : "none"));
        System.out.println("ChildEntity @Framework = " + (child != null ? child.value() : "none")
                + "   <-- inherited via @Inherited");

        Schedule[] schedules = TaskRunner.class.getAnnotationsByType(Schedule.class);
        System.out.println("TaskRunner runs on " + schedules.length + " day(s):");
        for (Schedule s : schedules) {
            System.out.println("   - " + s.day());
        }

        System.out.println("\\n===== SECTION 4 : custom annotations =====");

        Role role = Annotations.class.getAnnotation(Role.class);
        System.out.println("Class-level @Role value = " + role.value());

        System.out.println("Scanning methods for @TestCase ...");
        for (var method : Annotations.class.getDeclaredMethods()) {
            TestCase tc = method.getAnnotation(TestCase.class);
            if (tc == null) {
                continue;
            }
            System.out.println("  " + method.getName() + "() -> "
                    + "desc='" + tc.description() + "'"
                    + ", priority=" + tc.priority()
                    + ", timeout=" + tc.timeoutMillis()
                    + ", tags=" + Arrays.toString(tc.tags()));
        }

        System.out.println("\\n===== SECTION 5 : reflection mini-framework =====");

        Account good = new Account(1, "Ada", 500, "secret memo");
        Account bad = new Account(2, "Linus", -50, "another memo");

        System.out.println("JSON (driven by @JsonField):");
        System.out.println("  " + toJson(good));
        System.out.println("  " + toJson(bad));

        System.out.println("Validation (driven by @Range):");
        System.out.println(" good account:");
        validate(good);
        System.out.println(" bad account:");
        validate(bad);
    }
}
\`\`\`

\`\`\`output @run-annotations-annotations
===== SECTION 1 : @Override =====
In B.shows() (correctly overrides A.shows())
In B.shows() (correctly overrides A.shows())

===== SECTION 2 : other built-in annotations =====
Old payment flow (DEPRECATED - will be removed)
New payment flow
listOf(...) = [Ada, Linus, Grace]
[Calculator] combines two ints into one
add(2, 3) = 5
mul(2, 3) = 6

===== SECTION 3 : meta-annotations =====
@Author name = Aditya, date = 2026-06-03
BaseEntity  @Framework = Spring
ChildEntity @Framework = Spring   <-- inherited via @Inherited
TaskRunner runs on 2 day(s):
   - Monday
   - Friday

===== SECTION 4 : custom annotations =====
Class-level @Role value = ADMIN
Scanning methods for @TestCase ...
  testLogin() -> desc='login succeeds with valid credentials', priority=HIGH, timeout=1000, tags=[auth, smoke]
  testCart() -> desc='cart total adds up', priority=MEDIUM, timeout=1000, tags=[]

===== SECTION 5 : reflection mini-framework =====
JSON (driven by @JsonField):
  {"account_id": 1, "owner": "Ada", "balance": 500}
  {"account_id": 2, "owner": "Linus", "balance": -50}
Validation (driven by @Range):
 good account:
   OK     : balance = 500
 bad account:
   INVALID: balance = -50 (allowed 0..1000000)
\`\`\`
`,revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- Metadata attached to code. They do nothing by themselves — something has to\n  **read** them: the compiler, a framework, or your own reflection.\n- Built-in: `@Override`, `@Deprecated`, `@SuppressWarnings`,\n  `@FunctionalInterface`, `@SafeVarargs`.\n- Meta-annotations describe annotations: `@Retention`, `@Target`,\n  `@Documented`, `@Inherited`.\n- `@Retention(RUNTIME)` is required for reflection to see one at all — `SOURCE`\n  and `CLASS` are discarded before then.\n- `@Override` is optional and worth always writing: it turns a typo'd override\n  into a compile error.\n",interview:"## Interview questions\n\n### Q1: What is an annotation in Java?\n\n**Answer:** It is metadata attached to code using `@`. By itself it changes nothing; it is read by the compiler, by tools/frameworks, or by your own code via reflection, which then act on it.\n\n### Q2: What does `@Override` do, and why is it useful?\n\n**Answer:** It tells the compiler that a method is intended to override a superclass method, and the compiler verifies a matching method exists. This catches typos and signature mismatches at compile time instead of letting them become silent runtime bugs.\n\n### Q3: What is the difference between `@Deprecated` and `@SuppressWarnings`?\n\n**Answer:** `@Deprecated` marks an element as outdated so the compiler *produces* a warning at every use. `@SuppressWarnings` does the opposite — it *hides* named categories of warnings for the element it is placed on.\n\n### Q4: What is a meta-annotation? Name them.\n\n**Answer:** A meta-annotation is an annotation applied to another annotation to configure it. The five built-in ones are `@Retention`, `@Target`, `@Documented`, `@Inherited`, and `@Repeatable`.\n\n### Q5: Explain the three retention policies.\n\n**Answer:** `SOURCE` is discarded by the compiler (e.g. `@Override`). `CLASS` is stored in the `.class` file but not loaded into the JVM (the default). `RUNTIME` is stored and loaded, so it is readable via reflection — required for frameworks.\n\n### Q6: How do you read an annotation at runtime?\n\n**Answer:** The annotation must use `@Retention(RUNTIME)`. Then you use reflection — e.g. `clazz.getAnnotation(MyAnno.class)`, `method.getAnnotation(...)`, `field.getAnnotation(...)`, or `getAnnotationsByType(...)` for repeatable ones — and read its elements.\n\n### Q7: How do you create a custom annotation, and what types can its elements have?\n\n**Answer:** Declare it with `@interface`. Each element looks like a method with no body; its type must be a primitive, `String`, `Class`, enum, another annotation, or an array of those. `default` makes an element optional, and an element named `value` can be set without naming it.\n\n### Q8: What is `@Repeatable` and what does it require?\n\n**Answer:** It lets the same annotation be applied multiple times on one element. It requires a **container** annotation whose single `value` element is an array of the repeatable type, and you read the repeats with `getAnnotationsByType(...)`.\n"}},{slug:`interface-types`,title:`Types of interface`,subject:`Java`,stage:`Types and contracts`,chapter:27,accent:`#fb7185`,summary:`In Chapter 24 you learned what an interface is. This chapter is about the three flavours an interface can take, named by how many abstract methods they contain:`,parts:{notes:`## Introduction

In [Chapter 24](#/study/interfaces) you learned **what** an interface is. This chapter is about the **three flavours** an interface can take, named by **how many abstract methods they contain**:

| Abstract methods | Name of the interface |
|------------------|-----------------------|
| **2 or more** | **Normal** (regular) interface |
| **Exactly 1** | **Functional** (SAM) interface |
| **Zero** | **Marker** (tagging) interface |

That single number — how many abstract methods — is the entire idea behind this chapter. Everything else follows from it.

---

## Quick Recap: What Is an Interface

An interface is a **contract**: a list of methods a class promises to provide. A class signs the contract with \`implements\` and must supply a body for every abstract method.

\`\`\`java
interface Animal { void sound(); }

class Dog implements Animal {
    public void sound() { System.out.println("Woof"); }
}
\`\`\`

- Interface methods are \`public abstract\` **by default**.
- Interface fields are \`public static final\` **by default** (constants).
- You cannot create an interface with \`new\`; you create a class that implements it.

---

## The Three Types at a Glance

\`\`\`text
                 ┌──────────────────────────────────────────────┐
                 │              INTERFACE TYPES                  │
                 └──────────────────────────────────────────────┘
                                     │
        ┌────────────────────────────┼────────────────────────────┐
        ▼                            ▼                             ▼
  NORMAL (2+ methods)        FUNCTIONAL (1 method)         MARKER (0 methods)
  a full contract            replaceable by a lambda       just a type "tag"
  e.g. RemoteControl         e.g. Calculator               e.g. Serializable
\`\`\`

---

## File Map

| File | Type | Topic |
|------|------|-------|
| [\`NormalInterface.java\`](NormalInterface.java) | Normal | many methods + constant, \`default\`, \`static\`, \`private\` members |
| [\`FunctionalORSAMInterface.java\`](FunctionalORSAMInterface.java) | Functional | one method, lambdas, method references, built-ins |
| [\`MarkerInterface.java\`](MarkerInterface.java) | Marker | empty interface used as a tag with \`instanceof\` |

---

## Type 1: Normal (Regular) Interface

A **normal** interface has **two or more abstract methods**. It is the everyday contract from Chapter 24. Since Java 8 and 9, an interface can hold a lot more than abstract methods, and this file shows **all five** kinds of members in one place.

### The five member kinds

| Member | Has a body? | Who provides it | Since |
|--------|-------------|-----------------|-------|
| **constant** (\`int MAX_VOLUME = 100;\`) | n/a | the interface (a shared \`public static final\` value) | always |
| **abstract method** | ❌ no | the **implementing class must** | always |
| **\`default\` method** | ✅ yes | the interface (class may override) | Java 8 |
| **\`static\` method** | ✅ yes | the interface (called on the interface) | Java 8 |
| **\`private\` method** | ✅ yes | the interface (hidden helper) | Java 9 |

\`\`\`java
interface RemoteControl {

    int MAX_VOLUME = 100;

    void turnOn();
    void turnOff();
    void setVolume(int level);

    default void mute() {
        setVolume(0);
        System.out.println("Muted (via default method)");
    }

    static boolean isValidVolume(int level) {
        return level >= 0 && level <= MAX_VOLUME;
    }

    private void log(String action) {
        System.out.println("[RemoteControl] " + action);
    }

    default void reset() {
        log("reset requested");
        turnOff();
        turnOn();
    }
}
\`\`\`

### Why \`default\` methods exist

Before Java 8, adding a method to an interface **broke every class** that implemented it (they suddenly had an unimplemented method). \`default\` methods solve this: the interface ships a ready-made body, so old classes keep working without changes — and any class is still free to **override** it. In the code, \`AcRemote\` overrides \`mute()\` to refuse muting:

\`\`\`java
class AcRemote implements RemoteControl {
    @Override
    public void mute() {
        System.out.println("AC cannot be muted (overrode the default mute())");
    }
}
\`\`\`

### Why \`static\` and \`private\` methods exist

- **\`static\`** methods are utilities that belong to the interface itself, called as \`RemoteControl.isValidVolume(80)\`. They are **not** inherited by implementing classes.
- **\`private\`** methods (Java 9+) let two \`default\` methods share code **without** exposing that helper as part of the public contract.

---

## Type 2: Functional (SAM) Interface

A **functional** interface has **exactly one abstract method**. **SAM = Single Abstract Method.** Because there is only one method to fill in, the compiler lets you replace a whole class with a tiny **lambda**.

\`\`\`java
@FunctionalInterface
interface A {
    void show(int i);
}
\`\`\`

> \`@FunctionalInterface\` (from [Chapter 26](#/study/annotations)) is optional, but adding it means the compiler will **error** if anyone later adds a second abstract method — protecting every lambda that depends on this interface.

### From verbose class to one-line lambda

The file shows the same logic getting shorter and shorter:

\`\`\`java
A oldWay = new A() {
    @Override public void show(int i) { System.out.println("Anonymous class show: " + i); }
};

A lambda   = (int i) -> System.out.println("Lambda show: " + i);

A inferred = (i)     -> System.out.println("Inferred-type show: " + i);

A shortest = i       -> System.out.println("Shortest show: " + i);

A methodRef = FunctionalORSAMInterface::printLoudly;
\`\`\`

All five do the **same thing** — they just get progressively shorter. Each step
drops one more piece of ceremony: the anonymous class becomes a lambda, the
lambda drops its parameter **type** (it is inferred), then its **parentheses**
(one parameter needs none), and finally the body itself becomes a **method
reference** with \`::\`.

### Lambdas can return values too

\`\`\`java
@FunctionalInterface
interface Calculator { int operate(int a, int b); }

Calculator add = (a, b) -> a + b;
Calculator max = (a, b) -> a > b ? a : b;
add.operate(7, 8);
\`\`\`

The lambda body *is* the return value here — no \`return\` keyword needed for a
single expression. \`add.operate(7, 8)\` gives \`15\`.

### You rarely need to write your own

The \`java.util.function\` package already provides ready-made functional interfaces:

| Interface | Shape | Meaning |
|-----------|-------|---------|
| \`Runnable\` | \`() -> void\` | do something, return nothing |
| \`Supplier<T>\` | \`() -> T\` | supply a value |
| \`Consumer<T>\` | \`(T) -> void\` | consume a value |
| \`Function<T,R>\` | \`(T) -> R\` | transform T into R |
| \`Predicate<T>\` | \`(T) -> boolean\` | test a condition |
| \`BiFunction<T,U,R>\` | \`(T,U) -> R\` | combine two inputs into one output |

\`\`\`java
Predicate<Integer> isEven = n -> n % 2 == 0;
isEven.test(10);
\`\`\`

---

## Type 3: Marker (Tagging) Interface

A **marker** interface is **completely empty** — no methods, no constants. It does not add behaviour; it adds a **type tag**. Code can then ask *"is this object an instance of the marker?"* with \`instanceof\` and decide what to do.

\`\`\`java
interface Deletable { }
interface Downloadable { }

class Document implements Deletable, Downloadable { ... }
class Movie    implements Downloadable { ... }
\`\`\`

### How the tag is used

A service checks the tag before acting. Forget the tag, and the object simply cannot be deleted:

\`\`\`java
static void delete(Object item) {
    if (item instanceof Deletable) {
        System.out.println("Deleting: " + item.getClass().getSimpleName());
    } else {
        System.out.println("REFUSED to delete " + item.getClass().getSimpleName()
                + " - it is not marked Deletable");
    }
}
\`\`\`

### Why an *empty* interface is useful

- It attaches metadata at the **type level**, checkable by both the compiler and the runtime, with no fields or methods.
- The JDK uses several markers you already rely on:
  - \`java.io.Serializable\` → "this object may be turned into bytes"
  - \`java.lang.Cloneable\` → "\`Object.clone()\` is allowed here"
  - \`java.util.RandomAccess\` → "indexed \`get(i)\` is fast on this list"

\`\`\`java
"hello" instanceof java.io.Serializable
movie   instanceof java.io.Serializable
\`\`\`

The first is \`true\` — \`String\` implements \`Serializable\`. The second is \`false\`,
because our \`Movie\` never claimed the tag.

> **Marker vs annotation:** Today, annotations (Chapter 26) often replace markers. But a marker is a real **type**, so it gives **compile-time** checks an annotation can't — for example, a method can demand a \`Deletable\` parameter and the compiler will reject anything untagged.

---

## How to Compile and Run

From the repository root (\`d:\\Repos\\java\`):

\`\`\`bash
javac NormalInterface.java
javac FunctionalORSAMInterface.java
javac MarkerInterface.java

java NormalInterface
java FunctionalORSAMInterface
java MarkerInterface
\`\`\`

### Expected Output — Normal interface (the chapter folder)

\`\`\`text
TV: ON
TV volume set to 45
Rejected volume 250 (must be 0..100)
TV volume set to 0
Muted (via default method)
[RemoteControl] reset requested
TV: OFF
TV: ON
TV: OFF

AC: ON
AC cannot be muted (overrode the default mute())
AC: OFF

Is 80 a valid volume?  true
Is 120 a valid volume? false
MAX_VOLUME = 100
\`\`\`

### Expected Output — Functional interface (the chapter folder)

\`\`\`text
Anonymous class show: 1
Lambda show: 2
Inferred-type show: 3
Shortest show: 4
>>> 5 <<<

[Calculator] I combine two ints into one
add(7, 8) = 15
max(7, 8) = 8

isEven(10) = true
isEven(7)  = false
\`\`\`

### Expected Output — Marker interface (the chapter folder)

\`\`\`text
=== delete() respects the Deletable marker ===
Deleting: Document
REFUSED to delete Movie - it is not marked Deletable

=== download() respects the Downloadable marker ===
Downloading: Document
Downloading: Movie

=== a built-in marker: java.io.Serializable ===
Is "hello" Serializable? true
Is a Movie Serializable?  false
\`\`\`

---

## Side-by-Side Comparison

| Aspect | Normal | Functional (SAM) | Marker |
|--------|--------|------------------|--------|
| Abstract methods | 2 or more | exactly 1 | 0 |
| Usable as a lambda? | No | **Yes** | No |
| Typical use | a full capability contract | a single action passed as a value | a type tag checked with \`instanceof\` |
| Special annotation | — | \`@FunctionalInterface\` | — |
| JDK example | \`List\`, \`Map\` | \`Runnable\`, \`Comparator\` | \`Serializable\`, \`Cloneable\` |

---

## Common Mistakes

### Mistake 1: Putting two abstract methods in a \`@FunctionalInterface\`

\`\`\`java
@FunctionalInterface
interface A {
    void show(int i);
    void hide();        // ERROR: a functional interface allows only ONE
}
\`\`\`

### Mistake 2: Thinking \`default\`/\`static\` methods break "functional"

They don't. Only **abstract** methods count toward the "exactly one" rule. A functional interface may have many \`default\`/\`static\`/\`private\` methods.

### Mistake 3: Forgetting to implement every abstract method of a normal interface

\`\`\`java
class TvRemote implements RemoteControl {
    public void turnOn() { ... }
}
\`\`\`

### Mistake 4: Reducing visibility when implementing

\`\`\`java
class TvRemote implements RemoteControl {
    void turnOn() { ... }    // ERROR: must be public (interface methods are public)
}
\`\`\`

### Mistake 5: Putting methods inside a marker interface

A marker interface must stay **empty**. The moment you add a method, it stops being a marker and becomes a normal/functional interface.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`NormalInterface.java\`

A **normal** interface is one with two or more abstract methods, and this file
puts all five member kinds into one of them. \`RemoteControl\` declares the
constant \`MAX_VOLUME\`, three abstract methods that every implementer must
provide, a \`default mute()\` that ships a body so adding it broke nobody, a
\`static isValidVolume()\` that belongs to the interface itself and is never
inherited, and a \`private log()\` that exists only so the two default methods can
share code without exposing it in the contract.

\`TvRemote\` implements the three abstract methods and takes the rest as they
come. \`AcRemote\` implements them too but **overrides \`mute()\`**, because an air
conditioner has nothing to mute — proof that a default is a default, not a fixed
behaviour.

\`\`\`java NormalInterface.java @run-interface-types-normal-interface

interface RemoteControl {

    int MAX_VOLUME = 100;

    void turnOn();
    void turnOff();
    void setVolume(int level);

    default void mute() {
        setVolume(0);
        System.out.println("Muted (via default method)");
    }

    static boolean isValidVolume(int level) {
        return level >= 0 && level <= MAX_VOLUME;
    }

    private void log(String action) {
        System.out.println("[RemoteControl] " + action);
    }

    default void reset() {
        log("reset requested");
        turnOff();
        turnOn();
    }
}

class TvRemote implements RemoteControl {

    private int volume = 10;

    @Override
    public void turnOn() {
        System.out.println("TV: ON");
    }

    @Override
    public void turnOff() {
        System.out.println("TV: OFF");
    }

    @Override
    public void setVolume(int level) {
        if (RemoteControl.isValidVolume(level)) {
            this.volume = level;
            System.out.println("TV volume set to " + volume);
        } else {
            System.out.println("Rejected volume " + level + " (must be 0.." + MAX_VOLUME + ")");
        }
    }
}

class AcRemote implements RemoteControl {

    @Override
    public void turnOn() {
        System.out.println("AC: ON");
    }

    @Override
    public void turnOff() {
        System.out.println("AC: OFF");
    }

    @Override
    public void setVolume(int level) {
        System.out.println("AC has no volume control; ignoring " + level);
    }

    @Override
    public void mute() {
        System.out.println("AC cannot be muted (overrode the default mute())");
    }
}

public class NormalInterface {
    public static void main(String[] args) {

        RemoteControl tv = new TvRemote();
        tv.turnOn();
        tv.setVolume(45);
        tv.setVolume(250);
        tv.mute();
        tv.reset();
        tv.turnOff();

        System.out.println();

        RemoteControl ac = new AcRemote();
        ac.turnOn();
        ac.mute();
        ac.turnOff();

        System.out.println();

        System.out.println("Is 80 a valid volume?  " + RemoteControl.isValidVolume(80));
        System.out.println("Is 120 a valid volume? " + RemoteControl.isValidVolume(120));

        System.out.println("MAX_VOLUME = " + RemoteControl.MAX_VOLUME);
    }
}
\`\`\`

\`\`\`output @run-interface-types-normal-interface
TV: ON
TV volume set to 45
Rejected volume 250 (must be 0..100)
TV volume set to 0
Muted (via default method)
[RemoteControl] reset requested
TV: OFF
TV: ON
TV: OFF

AC: ON
AC cannot be muted (overrode the default mute())
AC: OFF

Is 80 a valid volume?  true
Is 120 a valid volume? false
MAX_VOLUME = 100
\`\`\`

### \`FunctionalORSAMInterface.java\`

Two functional interfaces and five ways to fill the first one in. \`A.show()\` is
implemented as an anonymous class, then as a lambda with an explicit type, then
with the type inferred, then without parentheses, and finally as a method
reference — and the output shows all five doing the same job.

\`Calculator\` proves two further points: a \`default\` method does **not** break
the single-abstract-method rule, and a lambda whose body is one expression
returns that expression without a \`return\`. The \`Predicate<Integer>\` at the end
is the reminder that \`java.util.function\` already ships most of the interfaces
you would otherwise write.

\`\`\`java FunctionalORSAMInterface.java @run-interface-types-functional-orsaminterface

@FunctionalInterface
interface A {
    void show(int i);
}

@FunctionalInterface
interface Calculator {
    int operate(int a, int b);

    default void describe() {
        System.out.println("[Calculator] I combine two ints into one");
    }
}

public class FunctionalORSAMInterface {

    static void printLoudly(int i) {
        System.out.println(">>> " + i + " <<<");
    }

    public static void main(String[] args) {

        A oldWay = new A() {
            @Override
            public void show(int i) {
                System.out.println("Anonymous class show: " + i);
            }
        };
        oldWay.show(1);

        A lambda = (int i) -> System.out.println("Lambda show: " + i);
        lambda.show(2);

        A inferred = (i) -> System.out.println("Inferred-type show: " + i);
        inferred.show(3);

        A shortest = i -> System.out.println("Shortest show: " + i);
        shortest.show(4);

        A methodRef = FunctionalORSAMInterface::printLoudly;
        methodRef.show(5);

        System.out.println();

        Calculator add = (a, b) -> a + b;
        Calculator max = (a, b) -> a > b ? a : b;
        add.describe();
        System.out.println("add(7, 8) = " + add.operate(7, 8));
        System.out.println("max(7, 8) = " + max.operate(7, 8));

        System.out.println();

        java.util.function.Predicate<Integer> isEven = n -> n % 2 == 0;
        System.out.println("isEven(10) = " + isEven.test(10));
        System.out.println("isEven(7)  = " + isEven.test(7));
    }
}
\`\`\`

\`\`\`output @run-interface-types-functional-orsaminterface
Anonymous class show: 1
Lambda show: 2
Inferred-type show: 3
Shortest show: 4
>>> 5 <<<

[Calculator] I combine two ints into one
add(7, 8) = 15
max(7, 8) = 8

isEven(10) = true
isEven(7)  = false
\`\`\`

### \`MarkerInterface.java\`

\`Deletable\` and \`Downloadable\` are empty on purpose — that emptiness *is* the
interface. They carry no methods and no fields; the only thing they do is let
\`instanceof\` answer a question about a type.

\`Document\` claims both tags, \`Movie\` claims only one, and \`delete()\` refuses
anything that is not \`Deletable\`. Nothing enforces this but the check itself,
which is exactly how \`Serializable\` and \`Cloneable\` work in the JDK.

\`\`\`java MarkerInterface.java @run-interface-types-marker-interface

interface Deletable {
}

interface Downloadable {
}

class Document implements Deletable, Downloadable {
    String name;

    Document(String name) {
        this.name = name;
    }
}

class Movie implements Downloadable {
    String title;

    Movie(String title) {
        this.title = title;
    }
}

public class MarkerInterface {

    static void delete(Object item) {
        if (item instanceof Deletable) {
            System.out.println("Deleting: " + item.getClass().getSimpleName());
        } else {
            System.out.println("REFUSED to delete " + item.getClass().getSimpleName()
                    + " - it is not marked Deletable");
        }
    }

    static void download(Object item) {
        if (item instanceof Downloadable) {
            System.out.println("Downloading: " + item.getClass().getSimpleName());
        } else {
            System.out.println("Cannot download " + item.getClass().getSimpleName());
        }
    }

    public static void main(String[] args) {
        Document doc = new Document("resume.pdf");
        Movie movie = new Movie("Inception");

        System.out.println("=== delete() respects the Deletable marker ===");
        delete(doc);
        delete(movie);

        System.out.println("\\n=== download() respects the Downloadable marker ===");
        download(doc);
        download(movie);

        System.out.println("\\n=== a built-in marker: java.io.Serializable ===");
        System.out.println("Is \\"hello\\" Serializable? " + ("hello" instanceof java.io.Serializable));
        System.out.println("Is a Movie Serializable?  " + (movie instanceof java.io.Serializable));
    }
}
\`\`\`

\`\`\`output @run-interface-types-marker-interface
=== delete() respects the Deletable marker ===
Deleting: Document
REFUSED to delete Movie - it is not marked Deletable

=== download() respects the Downloadable marker ===
Downloading: Document
Downloading: Movie

=== a built-in marker: java.io.Serializable ===
Is "hello" Serializable? true
Is a Movie Serializable?  false
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- **Normal** — two or more abstract methods. The ordinary contract.
- **Functional (SAM)** — exactly one abstract method, so a lambda can implement
  it. \`@FunctionalInterface\` makes the compiler enforce that.
- **Marker** — no methods at all. It exists so code can ask \`instanceof\`.
  \`Serializable\` and \`Cloneable\` are the examples.
- Default and static methods do not count against the single-abstract-method
  rule.
`,interview:`## Interview questions

### Q1: What are the three types of interfaces in Java?

**Answer:** Normal (two or more abstract methods), Functional/SAM (exactly one abstract method), and Marker (zero methods). They are distinguished by how many abstract methods they declare.

### Q2: What is a functional interface and why does it matter?

**Answer:** An interface with exactly one abstract method (a SAM). It matters because it can be implemented with a lambda expression or method reference, making code much shorter. \`@FunctionalInterface\` makes the compiler enforce the single-method rule.

### Q3: Do \`default\` and \`static\` methods stop an interface from being functional?

**Answer:** No. Only abstract methods are counted. A functional interface can have any number of \`default\`, \`static\`, or \`private\` methods and still qualify.

### Q4: Why were \`default\` methods added in Java 8?

**Answer:** To let new methods be added to existing interfaces without breaking the classes that already implement them. The interface supplies a body, so old implementers keep compiling, and they may override it if needed.

### Q5: What is a marker interface? Give examples.

**Answer:** An empty interface used purely as a type tag, checked with \`instanceof\`. Examples include \`java.io.Serializable\`, \`java.lang.Cloneable\`, and \`java.util.RandomAccess\`.

### Q6: Marker interface vs annotation — when would you choose each?

**Answer:** Both attach metadata. A marker interface is a real type, so it enables compile-time checks (a method can require the marker type as a parameter). An annotation is more flexible and can carry data (elements) and target many places, but is usually checked at runtime via reflection.

### Q7: Can a class implement more than one interface?

**Answer:** Yes. A class can implement many interfaces (e.g. \`class Document implements Deletable, Downloadable\`), which is how Java provides multiple inheritance of type. It can still only \`extend\` one class.
`}},{slug:`exceptions`,title:`Exceptions`,subject:`Java`,stage:`Errors and input`,chapter:28,accent:`#c084fc`,summary:`An exception is an event that disrupts the normal flow of a program while it is running. Java represents every such event as an object — and gives you a structured way to catch it and recover, instead of letting the whole program crash.`,parts:{notes:`## Introduction

An **exception** is an event that disrupts the normal flow of a program **while it is running**. Java represents every such event as an **object** — and gives you a structured way to **catch** it and recover, instead of letting the whole program crash.

But before we talk about handling, we need to be precise about **what can go wrong** in a Java program. Not every "error" is an exception, and confusing the categories is the single most common beginner mistake. This chapter sorts them out with three tiny, runnable files.

> The big idea: there are **three kinds of failure** — *compile-time*, *logical*, and *runtime* — and **only the third kind** (runtime) involves exceptions and \`try\`/\`catch\`.

---

## The Three Kinds of Errors

| Kind | When it happens | Does it compile? | Does it run? | Who detects it |
|------|-----------------|------------------|--------------|----------------|
| **Compile-time** | Before the program starts | ❌ **No** \`.class\` produced | never starts | the **compiler** (\`javac\`) |
| **Logical** | While running | ✅ yes | ✅ yes, to the end | **a human** (Java can't) |
| **Runtime** | While running | ✅ yes | 💥 crashes partway | the **JVM**, by throwing an exception |

\`\`\`text
            ┌──────────────────────────────────────────────────────────┐
            │                   SOMETHING IS WRONG                       │
            └──────────────────────────────────────────────────────────┘
                                       │
        ┌──────────────────────────────┼──────────────────────────────┐
        ▼                              ▼                              ▼
  COMPILE-TIME                     LOGICAL                        RUNTIME
  won't even build           builds & runs, wrong answer     builds, then throws
  e.g. missing ';'           e.g. 1 + 0 when you meant 10     e.g. 1 / 0 at runtime
  fix: the compiler tells    fix: read/test your logic       fix: try/catch or
       you exactly where                                          fix the cause
\`\`\`

---

## File Map

| File | Demonstrates | Key idea |
|------|--------------|----------|
| [\`CompileTimeError.java\`](CompileTimeError.java) | Compile-time errors (and the \`1/0\` myth) | the compiler refuses to build; \`1/0\` is **not** one of these |
| [\`LogicalError.java\`](LogicalError.java) | A logical error | compiles and runs, but the answer is wrong |
| [\`RuntimeError.java\`](RuntimeError.java) | A runtime error + \`try\`/\`catch\` | catch the exception so the program keeps running |

---

## File 1: Compile-Time Error (and a Famous Misconception)

A **compile-time error** is one the compiler (\`javac\`) catches **before the program ever runs**. No \`.class\` file is produced, so there is nothing to launch. These are almost always **syntax** or **type** mistakes:

\`\`\`java
int x = "hello";
int y = 5
System.out.println(z);
notAMethod();
\`\`\`

Four lines, four different refusals: a **type mismatch** (a \`String\` cannot
become an \`int\`), a **missing \`;\`**, an **undeclared variable** \`z\`, and a call
to a **method that does not exist**. Put any one of them in a file and \`javac\`
stops with an error pointing at the exact line — which is why none of them are
in the runnable file below.

### The famous misconception: \`1 / 0\`

It is tempting to think this line is a compile-time error:

\`\`\`java
int result = 1 / 0;
\`\`\`

**It is not.** This file proves it:

1. \`javac CompileTimeError.java\` — **compiles successfully**, a \`.class\` file appears.
2. \`java CompileTimeError\` — **crashes at runtime**:

\`\`\`text
Exception in thread "main" java.lang.ArithmeticException: / by zero
\`\`\`

So integer **divide-by-zero is a *runtime* error** (an \`ArithmeticException\`) — exactly the family we learn to handle in File 3 — **not** a compile-time error. The file keeps this line on purpose so you can compile it (no error) and then run it (it throws) and see the difference with your own eyes.

> 💡 **Why does this trip people up?** Some IDEs underline \`1/0\` with a *warning*, which looks like a compile error. But the **language** only forbids it at runtime, because in general a divisor's value isn't known until the program runs.

---

## File 2: Logical Error

A **logical error** is the sneakiest bug of all, because **nothing complains**:

- The code is legal, so it **compiles**.
- It **runs** all the way through with no crash.
- But the **result is wrong**, because your logic didn't match your intent.

\`\`\`java
int result = 1 + 0;
System.out.println(result);
\`\`\`

The programmer meant \`10\`; the expression legally evaluates to \`1\`, and \`1\` is
what prints. There is no error message anywhere — Java cannot read your mind. The only way to catch a logical error is to **test the program and notice the output is wrong**. This is exactly why we write unit tests.

> The compiler checks **grammar and types**, never **correctness of intent**. \`1 + 0\` is grammatically perfect; it's just not what you wanted.

---

## File 3: Runtime Error and \`try\`/\`catch\`

A **runtime error** happens **while the program is running**: it compiled, it started, and then an illegal operation threw an **exception**. If nobody handles it, the JVM prints a stack trace and stops at that line.

**Exception handling** is how we deal with this gracefully. We wrap risky code in a \`try\` block; if it throws, control jumps to a matching \`catch\` block instead of crashing:

\`\`\`java
int i = 9, j = 0, result = 0;

try {
    result = i / j;
} catch (Exception e) {
    System.out.println("Exception: " + e.getMessage());
}

System.out.println("Result is : " + result);
System.out.println("Worked normally");
\`\`\`

\`i / j\` is \`9 / 0\`, so it throws right there and control jumps into the \`catch\`
instead of crashing. Both \`println\`s after the block still run.

### The key insight: a *caught* exception does NOT stop the program

Running the file prints:

\`\`\`text
Exception: / by zero
Result is : 0
Worked normally
\`\`\`

Notice that **\`Worked normally\` prints**. Because the exception was **caught**, the program did **not** crash — it simply continued after the \`try\`/\`catch\`. Contrast this with the situation if there were **no** \`try\`/\`catch\`: the throw would crash the program at \`result = i / j\`, and **neither** of the final two lines would ever run.

| | With \`try\`/\`catch\` | Without \`try\`/\`catch\` |
|---|---|---|
| At \`i / j\` | exception caught | exception thrown, **uncaught** |
| Lines after | **run normally** | **never run** (program dies) |
| Exit | normal | stack trace, exit code 1 |

### \`e.getMessage()\` vs printing the whole exception

\`\`\`java
System.out.println("Exception: " + e);
System.out.println("Exception: " + e.getMessage());
\`\`\`

The first prints the **type and the message** — \`ArithmeticException: / by zero\`.
The second prints the **message only** — \`/ by zero\`.

### Ordering multiple \`catch\` blocks (most specific first)

When you catch several types, you must list them **most specific first, most general last**:

\`\`\`java
try {
    ...
} catch (ArithmeticException e) {
    ...
} catch (ArrayIndexOutOfBoundsException e) {
    ...
} catch (Exception e) {
    ...
}
\`\`\`

Two specific types, then the general one last.

\`Exception\` is the parent of all the others, so a single \`catch (Exception e)\` can catch everything. Because it is the most general, placing it **before** a more specific catch makes that specific block **unreachable** — which is a **compile error**.

---

## The Exception Hierarchy

Everything you can throw or catch descends from **\`Throwable\`**:

\`\`\`text
Object
 └─ Throwable                         (root of everything throwable/catchable)
     ├─ Error                          serious JVM problems - normally NOT caught
     │   ├─ ThreadDeath                (deprecated)
     │   ├─ IOError
     │   └─ VirtualMachineError
     │       ├─ OutOfMemoryError
     │       └─ StackOverflowError
     │
     └─ Exception                      problems your code CAN and SHOULD handle
         ├─ RuntimeException           (UNCHECKED - compiler does not force handling)
         │   ├─ ArithmeticException
         │   ├─ ArrayIndexOutOfBoundsException
         │   ├─ NumberFormatException
         │   ├─ StringIndexOutOfBoundsException
         │   └─ NullPointerException
         │
         ├─ IOException                (CHECKED - compiler forces handling)
         │   └─ FileNotFoundException
         └─ SQLException               (CHECKED - e.g. a database failure)
\`\`\`

| Branch | What it means | Should you catch it? |
|--------|---------------|----------------------|
| **\`Error\`** | Something went badly wrong inside the JVM (out of memory, stack overflow). | **No** — usually unrecoverable; let it crash. |
| **\`RuntimeException\`** | Usually a **bug in your code** (bad index, null, divide by zero). | Fix the bug; catch only when sensible. |
| **Other \`Exception\`s** | External/expected failures (file missing, DB down). | **Yes** — must be handled or declared. |

---

## Checked vs Unchecked Exceptions

This single distinction explains *why the compiler forces \`try\`/\`catch\` in some cases but not others*:

| | **Unchecked** | **Checked** |
|---|---|---|
| Base type | \`RuntimeException\` (and \`Error\`) | \`Exception\` (but **not** \`RuntimeException\`) |
| Compiler forces handling? | ❌ No | ✅ **Yes** — \`try/catch\` **or** \`throws\` |
| Typical cause | a **bug** in your logic | an **external** condition you can't control |
| Examples | \`ArithmeticException\`, \`NullPointerException\`, \`ArrayIndexOutOfBoundsException\` | \`IOException\`, \`FileNotFoundException\`, \`SQLException\` |

> \`ArithmeticException\` from \`1/0\` is **unchecked**, which is why File 1 compiled without any \`try\`/\`catch\` — the compiler never demanded it. You only found out at runtime.

---

## How to Compile and Run

From the repository root (\`d:\\Repos\\java\`):

\`\`\`bash
javac CompileTimeError.java
javac LogicalError.java
javac RuntimeError.java

java CompileTimeError
java LogicalError
java RuntimeError
\`\`\`

### Expected Output — File 1 (the chapter folder)

It **compiles** with no error, but **running** it throws:

\`\`\`text
Exception in thread "main" java.lang.ArithmeticException: / by zero
	at CompileTimeError.main(CompileTimeError.java:6)
\`\`\`

### Expected Output — File 2 (the chapter folder)

\`\`\`text
1
\`\`\`

(No error — that's the whole point of a *logical* error. You only know \`1\` is "wrong" if you expected \`10\`.)

### Expected Output — File 3 (the chapter folder)

\`\`\`text
Exception: / by zero
Result is : 0
Worked normally
\`\`\`

---

## Side-by-Side Comparison

| Aspect | Compile-time | Logical | Runtime |
|--------|--------------|---------|---------|
| Detected by | \`javac\` | a human / a test | the JVM (throws an exception) |
| Program builds? | No | Yes | Yes |
| Program runs? | Never starts | Runs fully | Crashes (unless caught) |
| Example in this chapter | a missing \`;\` (commented) | \`1 + 0\` | \`1 / 0\`, \`i / j\` |
| How you fix it | follow the compiler message | check your logic | \`try\`/\`catch\`, or fix the cause |

---

## Common Mistakes

### Mistake 1: Thinking \`1 / 0\` is a compile-time error

It compiles fine and throws \`ArithmeticException\` **at runtime**. Divide-by-zero is an unchecked runtime exception, not a syntax error.

### Mistake 2: Expecting Java to catch logical errors

\`\`\`java
int total = price - tax;
\`\`\`

That was meant to be \`price + tax\`. It compiles, it runs, and it is silently
wrong. Only **testing** reveals this — the compiler and JVM see nothing wrong.

### Mistake 3: Ordering \`catch\` blocks general-first

\`\`\`java
} catch (Exception e) { ... }
} catch (ArithmeticException e) { ... }  // ERROR: unreachable code
\`\`\`

Put the **specific** types first, \`Exception\` **last**.

### Mistake 4: Assuming a caught exception stops the program

A caught exception lets the program **continue** after the \`try\`/\`catch\`. Only an **uncaught** exception terminates it.

### Mistake 5: Catching \`Error\` to "be safe"

\`\`\`java
catch (Throwable t) { }
\`\`\`

\`Error\`s signal an unrecoverable JVM state. Catch \`Exception\`, not \`Throwable\`/\`Error\`.

---

## The source files

The complete programs, renamed from the numbered file names the repository
uses so they compile on their own. **Save** writes the file out of this page;
**Run** replays the output it produced when this page was built.

> Two of these end in a stack trace on purpose — that is the thing the chapter
> is demonstrating. The recorded output is what the program really printed,
> exit code and all.

\`\`\`bash
javac Something.java
java Something
\`\`\`

### \`CompileTimeError.java\`

Despite the name, this file has no compile-time error in it at all — that is the
joke, and the lesson. It compiles cleanly and produces a \`.class\` file; the
failure only arrives when you run it. The \`println\` never executes, because the
program has already stopped on the line above it.

Real compile-time errors would look like \`int x = "hello";\` (type mismatch),
\`int y = 5\` (missing semicolon), or a call to a method that does not exist —
none of which are in the file, because a file containing one could not be built
at all.

\`\`\`java CompileTimeError.java @run-exceptions-compile-time-error
// no package

public class CompileTimeError {
    public static void main(String[] args) {

        int result = 1 / 0;
        System.out.println(result);
    }
}
\`\`\`

\`\`\`output @run-exceptions-compile-time-error
Exception in thread "main" java.lang.ArithmeticException: / by zero
	at CompileTimeError.main(CompileTimeError.java:6)
\`\`\`

### \`LogicalError.java\`

The sneakiest of the three. This file compiles, runs to the end, crashes
nowhere, and is still wrong. The programmer meant to compute \`10\` and wrote
\`1 + 0\`, which legally evaluates to \`1\`.

Nothing warns you, because there is nothing to warn about — the compiler checks
grammar and types, not whether your arithmetic is what you intended. Only a
human reading the output, or a failing test, catches this one.

\`\`\`java LogicalError.java @run-exceptions-logical-error
// no package

public class LogicalError {
    public static void main(String[] args) {

        int result = 1 + 0;
        System.out.println(result);
    }
}
\`\`\`

\`\`\`output @run-exceptions-logical-error
1
\`\`\`

### \`RuntimeError.java\`

The third file throws the same \`ArithmeticException\` as the first one, but wraps
it. That single difference is the whole chapter:

- **\`result = i / j\`** is \`9 / 0\`, so it throws. The assignment never completes,
  which is why \`result\` is still \`0\` at the end rather than anything sensible.
- **\`catch (Exception e)\`** is the parent of every exception type, so it catches
  it. \`ArithmeticException\` would work here too and would be the better choice
  in real code — catch the narrowest type that covers what can actually go
  wrong.
- With several \`catch\` blocks you order them **most specific first**. \`Exception\`
  must come last: a more specific block placed after it is unreachable and will
  not compile.
- **Both \`println\`s at the bottom run.** The exception was caught, so it stopped
  the \`try\` block, not the program. Remove the \`try\`/\`catch\` and neither line
  would ever print — that contrast is the point of the file.

For where \`ArithmeticException\` sits among the rest, see
[the hierarchy above](#the-exception-hierarchy).

\`\`\`java RuntimeError.java @run-exceptions-runtime-error
// no package

public class RuntimeError {
    public static void main(String[] args) {

        int i = 9;
        int j = 0;
        int result = 0;

        try {
            result = i / j;
        } catch (Exception e) {
            System.out.println("Exception: " + e.getMessage());
        }

        System.out.println("Result is : " + result);
        System.out.println("Worked normally");
    }
}
\`\`\`

\`\`\`output @run-exceptions-runtime-error
Exception: / by zero
Result is : 0
Worked normally
\`\`\`
`,revision:`The chapter in six lines. If a line here is news, the **Notes** part is where
it comes from.

## Worth remembering

- Three kinds of problem: **compile-time** errors (the compiler stops you),
  **logical** errors (it runs and is wrong), **runtime** exceptions (it stops
  mid-run).
- Hierarchy: \`Throwable\` → \`Error\` and \`Exception\`; \`Exception\` →
  \`RuntimeException\` and everything else.
- **Checked** — extends \`Exception\`, must be caught or declared. Recoverable
  conditions: missing file, refused connection.
- **Unchecked** — extends \`RuntimeException\`, needs no declaration. Programming
  errors: null dereference, bad index.
- \`finally\` runs whether or not anything was thrown. Try-with-resources closes
  for you and is better.
- Never catch \`Exception\` and do nothing. A swallowed exception is a bug you will
  meet again later, with no clue where it came from.
`,interview:"## Interview questions\n\n### Q1: What is the difference between a compile-time error, a logical error, and a runtime error?\n\n**Answer:** A compile-time error is caught by the compiler before the program runs (e.g. a syntax or type mistake) and produces no `.class` file. A logical error compiles and runs to completion but yields the wrong result because the logic is incorrect — Java cannot detect it. A runtime error occurs while running, when an illegal operation throws an exception; uncaught, it crashes the program.\n\n### Q2: Is `int x = 1 / 0;` a compile-time or runtime error?\n\n**Answer:** Runtime. It compiles successfully and throws `java.lang.ArithmeticException: / by zero` when executed, because it is an unchecked runtime exception, not a syntax error.\n\n### Q3: What is an exception?\n\n**Answer:** An object representing an event that disrupts normal program flow at runtime. Every exception descends from `Throwable`. You handle exceptions with `try`/`catch`/`finally` or declare them with `throws`.\n\n### Q4: Explain the exception hierarchy.\n\n**Answer:** `Throwable` is the root, with two branches: `Error` (serious JVM problems like `OutOfMemoryError`, normally not caught) and `Exception` (recoverable problems). Under `Exception`, `RuntimeException` and its subclasses are unchecked; all other `Exception` subclasses (like `IOException`, `SQLException`) are checked.\n\n### Q5: What is the difference between checked and unchecked exceptions?\n\n**Answer:** Checked exceptions (subclasses of `Exception` but not `RuntimeException`) must be handled with `try`/`catch` or declared with `throws`, or the code won't compile. Unchecked exceptions (`RuntimeException` and `Error` subclasses) are not enforced by the compiler — they usually indicate bugs.\n\n### Q6: Why must specific `catch` blocks come before general ones?\n\n**Answer:** Because `catch` blocks are checked top-to-bottom and `Exception` is the parent of all the others. A general `catch (Exception e)` placed first would catch everything, making any specific `catch` after it unreachable — which is a compile error.\n\n### Q7: Does catching an exception stop the program?\n\n**Answer:** No. A caught exception transfers control to the `catch` block and the program continues afterward. Only an uncaught exception propagates up and terminates the program with a stack trace.\n"}},{slug:`throw-and-throws`,title:`throw, throws and custom exceptions`,subject:`Java`,stage:`Errors and input`,chapter:29,accent:`#60a5fa`,summary:`In Chapter 28 we caught exceptions that Java threw for us — like ArithmeticException from 1 / 0. This chapter is the other side of the coin, and it teaches the two distinct verbs of exception production and the two ways to deal with one:`,parts:{notes:'## Introduction\n\nIn [Chapter 28](#/study/exceptions) we **caught** exceptions that Java threw for us — like `ArithmeticException` from `1 / 0`. This chapter is the **other side of the coin**, and it teaches the two distinct verbs of exception *production* and the two ways to deal with one:\n\n1. **File 1** — how **you** raise an exception on purpose with the **`throw`** keyword, using your **own** exception type (a custom exception class), and **handle** it on the spot with `try`/`catch`.\n2. **File 2** — how a method **declares** an exception with the **`throws`** keyword and **propagates** it up the call stack instead of handling it.\n\nTogether these let you signal *your own* error conditions ("balance can\'t be negative", "age must be positive") and choose **where** in the program they get dealt with.\n\n> **One-line summary:** `throw` *raises* an exception; `try`/`catch` *handles* it here (File 1); `throws` *declares* it and *passes it up* to the caller (File 2).\n\n---\n\n## `throw` vs `throws` (Don\'t Confuse Them)\n\nThese look almost identical but do **opposite jobs**. This is the most common confusion in the whole topic:\n\n| | `throw` | `throws` |\n|---|---------|----------|\n| What it is | a **statement** that raises an exception **now** | a **clause** in a method signature |\n| Where it goes | **inside** a method body | after the **parameter list**, before `{` |\n| How many | one exception object at a time | a comma-separated **list of types** |\n| Meaning | "raise this exception right here" | "this method *might* throw these; caller beware" |\n| Example | `throw new CustomException("bad");` | `void load() throws IOException { ... }` |\n\nBoth appear in the same four lines below. The clause on the signature is\n`throws` — a warning to callers. The statement in the body is `throw` — the\nthing that actually happens.\n\n```java\nvoid readFile(String path) throws IOException {\n    if (path == null)\n        throw new IOException("path is null");\n}\n```\n\n**File 1** uses **`throw`** (it catches everything inside the same method, so it needs no `throws`). **File 2** uses **`throws`** (it declares the exception and lets it travel up instead of catching it).\n\n---\n\n## File Map\n\n| File | Keyword | Strategy | Demonstrates |\n|------|---------|----------|--------------|\n| [`Throw.java`](Throw.java) | `throw` | **handle here** | a custom exception class, throwing it with `throw`, and catching it among several `catch` blocks |\n| [`Throws.java`](Throws.java) | `throws` | **pass it up** | declaring a checked exception with `throws`, propagating it through `show()` → `main()` → JVM, and a `static{}` block |\n\nFile 1 contains **two classes**:\n\n- `CustomException` — our own exception type.\n- `Throw` — the `public` class with `main`, which throws and catches it.\n\nFile 2 also contains **two classes**:\n\n- `A` — a helper whose `show()` method declares `throws` and does **not** catch.\n- `Throws` — the `public` class with `main` (and a `static{}` block) that also declares `throws`.\n\n---\n\n## Building a Custom Exception: the `CustomException` Class\n\nA custom exception is just **a class that extends an existing exception class**. Here we extend `Exception`:\n\n```java\nclass CustomException extends Exception {\n    CustomException(String message) {\n        super(message);\n    }\n}\n```\n\n- **`extends Exception`** makes `CustomException` a real exception — it can be `throw`n and `catch`ed exactly like a built-in one ("is-a" relationship).\n- The **constructor** takes a message and passes it to the parent with **`super(message)`**. That stored message is what **`e.getMessage()`** returns later.\n- Giving it a meaningful name (`CustomException`, or better `InsufficientBalanceException`) makes both the **`catch` blocks** and the **error messages** self-documenting.\n\n> Because it extends `Exception` (and not `RuntimeException`), `CustomException` is a **checked** exception — see [the section below](#exception-vs-runtimeexception-as-a-base-class) for what that implies.\n\n---\n\n## Throwing It with `throw`\n\nThe `throw` keyword raises an exception object immediately. As soon as it runs, normal execution stops and Java starts hunting for a matching `catch`:\n\n```java\nif (j == 0) {\n    // throw new ArithmeticException();\n    // throw new ArithmeticException("...j = 0");\n    throw new CustomException("This is arithmetic exception where j = 0");\n}\n```\n\nThe file shows **three things you *could* throw**, two of them commented out. Each one would be caught by a **different** `catch` block below, which is a neat way to see how the type you throw decides where control lands:\n\n- The third line is the live one. It throws `CustomException`, so **{Block 2}** catches it.\n- Uncomment either `ArithmeticException` line instead and **{Block 1}** catches it.\n\n> **Syntax:** `throw` needs an **object** (`throw new X(...)`), not a class. `throw CustomException;` is a compile error; it must be `throw new CustomException(...);`.\n\n---\n\n## Catching It: Ordering the `catch` Blocks\n\nReading down: **{Block 2}** is the exact type we threw, **{Block 1}** would take\nan `ArithmeticException`, and the last one is the safety net — the parent type,\ncatching anything the other two missed.\n\n```java\n} catch (CustomException e) {\n    j = 18 / 1;\n    System.out.println("Exception: " + e.getMessage());\n} catch (ArithmeticException e) {\n    j = 18 / 1;\n    System.out.println("Exception: " + e.getMessage());\n} catch (Exception e) {\n    System.out.println(e);\n}\n```\n\nThree rules at work here:\n\n1. **The thrown type picks the block.** We threw a `CustomException`, so **{Block 2}** runs and the others are skipped.\n2. **Most specific first, most general last.** `CustomException` and `ArithmeticException` are specific; `Exception` (their ancestor) comes **last** as a catch-all. Reverse the order and the specific blocks become **unreachable** — a compile error.\n3. **Recovery.** Each catch sets `j = 18 / 1` (i.e. `18`), so after handling, the program has a sensible value to print instead of crashing.\n\n`e.getMessage()` prints **only** the message text; `e` (or `System.out.println(e)`) prints the **type and** the message.\n\n---\n\n## Walkthrough: What File 1 Does\n\n```java\nint i = 20;\nint j = 0;\n\ntry {\n    j = 18 / i;\n    if (j == 0) {\n        throw new CustomException("This is arithmetic exception where j = 0");\n    }\n} catch (CustomException e) {\n    j = 18 / 1;\n    System.out.println("Exception: " + e.getMessage());\n}\n\nSystem.out.println("Result is : " + j);\n```\n\nStep by step:\n\n1. `j = 18 / i` → `18 / 20`. Because both are `int`, this is **integer division**, which truncates `0.9` to **`0`**.\n2. `if (j == 0)` is **true**, so we `throw new CustomException(...)`.\n3. The throw transfers control to the **`CustomException`** catch block (**{Block 2}**).\n4. Inside, `j = 18 / 1` sets `j` to **`18`**, and we print the message.\n5. Execution continues past the try/catch and prints the recovered value.\n\n### Expected Output\n\n```text\nException: This is arithmetic exception where j = 0\nResult is : 18\n```\n\n> Note the message text says *"arithmetic exception"*, but the object thrown is actually a **`CustomException`** — the wording is just the human message we chose; the **type** is what controls which `catch` runs.\n\n---\n\n## Declaring with `throws`: Propagating Up the Call Stack\n\nFile 1 **handled** the exception where it happened. File 2 ([`Throws.java`](Throws.java)) does the opposite: it **refuses to handle** the exception and instead **declares** it with `throws`, pushing the responsibility onto the caller.\n\n```java\nclass A {\n    public void show() throws ClassNotFoundException {\n        Class.forName("Calc");\n    }\n}\n```\n\n`Class.forName("Calc")` asks the JVM to load a class **by name at runtime**. There is no class called `Calc`, so it throws **`ClassNotFoundException`** — a **checked** exception. *Checked* means the compiler will **not** let the code build unless every method in the path either:\n\n- **catches** it with `try`/`catch`, **or**\n- **declares** it with `throws ClassNotFoundException`.\n\nHere `show()` chooses to **declare**, so the exception escapes the method untouched.\n\n> **Why pass it up instead of catching it?** Often the method where an error *occurs* doesn\'t know how to *respond* to it. A low-level `show()` can\'t decide whether to retry, log, or warn the user — so it reports the problem upward and lets a higher-level method (which has that context) decide. `throws` is how it reports upward.\n\n---\n\n## Combined Methods: How Propagation Chains Together\n\nThe power of `throws` is that it **chains**. Each method in the call stack that declares (rather than catches) the exception passes it one level higher, until something catches it — or the JVM ends the program:\n\n```java\npublic static void main(String[] args) throws ClassNotFoundException {\n    A a = new A();\n    a.show();\n}\n```\n\n```text\n   Class.forName("Calc")        throws ClassNotFoundException\n            │  (show() declares throws — does not catch)\n            ▼\n   A.show()                     re-throws it to its caller\n            │  (main() declares throws — does not catch)\n            ▼\n   Throws.main()     re-throws it to its caller\n            │  (the caller of main IS the JVM)\n            ▼\n   JVM                          nobody caught it -> print stack trace, end program\n```\n\nThe real stack trace from running the file proves this exact chain (read it **bottom-up** — that is the order the calls were made):\n\n```text\n	at A.show(Throws.java:5)\n	at Throws.main(Throws.java:18)\n```\n\n> This is what **"combined methods and class throw"** means: a chain of methods that each declare `throws`, so one exception travels through several methods before anything stops it.\n\n---\n\n## The `static` Block and Class Loading\n\nFile 2 also shows a **`static{}` block** — a chunk of code that runs **once, when the class is first loaded/initialized**, which is just **before `main()` starts**:\n\n```java\npublic class Throws {\n    static {\n        System.out.println("Class Loaded");\n    }\n\n    public static void main(String[] args) throws ClassNotFoundException { ... }\n}\n```\n\nThat is why the program\'s output begins with `Class Loaded` **before** the exception appears: class initialization happens first, then `main` runs and triggers the failing `Class.forName`.\n\n| Runs... | When |\n|---------|------|\n| `static {}` block | once, at class load — **before** `main` |\n| instance initializer / constructor | every time an object is created with `new` |\n| `main()` body | after the class is fully initialized |\n\n---\n\n## Walkthrough: What File 2 Does\n\n```java\nclass A {\n    public void show() throws ClassNotFoundException {\n        Class.forName("Calc");\n    }\n}\n\npublic class Throws {\n    static { System.out.println("Class Loaded"); }\n\n    public static void main(String[] args) throws ClassNotFoundException {\n        A a = new A();\n        a.show();\n    }\n}\n```\n\nStep by step:\n\n1. The JVM loads `Throws`; its **`static{}`** block runs and prints `Class Loaded`.\n2. `main()` runs, creates an `A`, and calls `a.show()`.\n3. Inside `show()`, `Class.forName("Calc")` finds no such class and **throws `ClassNotFoundException`**.\n4. `show()` declared `throws`, so it does **not** catch — the exception goes back to `main()`.\n5. `main()` also declared `throws`, so it does **not** catch — the exception goes to the **JVM**.\n6. The JVM prints a stack trace and ends the program. The "finished normally" lines never run.\n\n### Expected Output\n\n```text\nClass Loaded\nException in thread "main" java.lang.ClassNotFoundException: Calc\n	at java.base/jdk.internal.loader.BuiltinClassLoader.loadClass(BuiltinClassLoader.java:580)\n	at java.base/java.lang.ClassLoader.loadClass(ClassLoader.java:502)\n	at java.base/java.lang.Class.forName0(Native Method)\n	at java.base/java.lang.Class.forName(Class.java:478)\n	at java.base/java.lang.Class.forName(Class.java:468)\n	at A.show(Throws.java:5)\n	at Throws.main(Throws.java:18)\n```\n\n> **File 1 vs File 2 in one line:** File 1 **caught** the exception, so it printed a tidy message and continued (`Result is : 18`). File 2 **declared** the exception and let it escape, so the program **crashed** with a stack trace. Same machinery, opposite strategy.\n\n---\n\n## `Exception` vs `RuntimeException` as a Base Class\n\nWhen you create a custom exception, **what you extend decides whether it is checked or unchecked**:\n\n| You extend... | Your exception is... | Compiler forces handling? |\n|---------------|----------------------|---------------------------|\n| `Exception` | **checked** | ✅ Yes — `try/catch` **or** `throws` required |\n| `RuntimeException` | **unchecked** | ❌ No — handling is optional |\n\n```java\nclass CheckedProblem   extends Exception        { }\nclass UncheckedProblem extends RuntimeException  { }\n```\n\nOur `CustomException extends Exception`, so it is **checked**. In the file this is invisible because the `throw` and the `catch` are in the **same method** — but if a *method* threw it and let it escape, that method would need a `throws CustomException` clause.\n\n**Rule of thumb:** extend `Exception` for *recoverable, expected* conditions you want callers to deal with deliberately; extend `RuntimeException` for *programming bugs* that shouldn\'t normally happen.\n\n---\n\n## How to Compile and Run\n\nFrom the repository root (`d:\\Repos\\java`):\n\n```bash\n# File 1 — throw + catch\njavac Throw.java\njava Throw\n\n# File 2 — throws + propagation\njavac Throws.java\njava Throws\n```\n\nEach command compiles **two classes** into the folder, because each source file holds two (`CustomException` + `Throw` for File 1; `A` + `Throws` for File 2).\n\n### Expected Output — File 1 (`Throw`)\n\n```text\nException: This is arithmetic exception where j = 0\nResult is : 18\n```\n\n### Expected Output — File 2 (`Throws`)\n\n```text\nClass Loaded\nException in thread "main" java.lang.ClassNotFoundException: Calc\n	...\n	at A.show(Throws.java:5)\n	at Throws.main(Throws.java:18)\n```\n\nFile 2 **crashes on purpose** (exit code 1) — that is the point: nobody caught the propagated exception, so the JVM reports it.\n\n### Try it yourself\n\n**File 1:** swap the active `throw` for one of the commented ones to watch a **different** `catch` block fire:\n\n```java\nthrow new ArithmeticException("now I land in Block 1");\n```\n\nOutput becomes `Exception: now I land in Block 1` — handled by the `ArithmeticException` block instead.\n\n**File 2:** stop the crash by *handling* the exception instead of declaring it — wrap the call in `main` and drop the `throws`:\n\n```java\npublic static void main(String[] args) {\n    try {\n        new A().show();\n    } catch (ClassNotFoundException e) {\n        System.out.println("Handled: " + e.getMessage());\n    }\n}\n```\n\nNow the program prints `Class Loaded` then `Handled: Calc` and exits normally — propagation stopped because *you* caught it.\n\n---\n\n## Common Mistakes\n\n### Mistake 1: Confusing `throw` and `throws`\n\n`throw` **does** the throwing (a statement, inside the method). `throws` **declares** that a method may throw (a clause in the signature). See [the table above](#throw-vs-throws-dont-confuse-them).\n\n### Mistake 2: Throwing a class instead of an object\n\n```java\nthrow CustomException;            // ERROR\nthrow new CustomException("x");   // correct - throw an INSTANCE\n```\n\n### Mistake 3: Catching the parent before the child\n\n```java\n} catch (Exception e) { ... }\n} catch (CustomException e) { ... }    // ERROR: unreachable code\n```\n\nList `CustomException` (specific) **before** `Exception` (general).\n\n### Mistake 4: Forgetting `super(message)` in the constructor\n\n```java\nclass CustomException extends Exception {\n    CustomException(String message) { }\n}\n```\n\nCall `super(message)` so `getMessage()` can return it.\n\n### Mistake 5: Extending `Exception` but never handling it\n\nBecause `Exception` is **checked**, a method that throws it and lets it escape **must** declare `throws CustomException`, or the code won\'t compile. (It works in File 1 only because the throw is caught in the same method.)\n\n### Mistake 6: Calling a `throws` method without handling or re-declaring\n\n```java\nvoid caller() {\n    new A().show();   // ERROR: unhandled ClassNotFoundException\n}\n```\n\nIf `show()` declares `throws ClassNotFoundException`, then `caller()` must **either** wrap it in `try`/`catch` **or** add `throws ClassNotFoundException` to its own signature (as File 2\'s `main` does). You cannot silently ignore a checked exception.\n\n### Mistake 7: Thinking `throws` *handles* the exception\n\n`throws` does **not** handle anything — it only **declares** and **passes the problem upward**. If every method (including `main`) just declares `throws`, the exception reaches the JVM and the program **still crashes**, as File 2 demonstrates. To actually stop it, some method must `catch` it.\n\n---\n\n## The source files\n\nThe complete programs, renamed from the numbered file names the repository\nuses so they compile on their own. **Save** writes the file out of this page;\n**Run** replays the output it produced when this page was built.\n\n> Two of these end in a stack trace on purpose — that is the thing the chapter\n> is demonstrating. The recorded output is what the program really printed,\n> exit code and all.\n\n```bash\njavac Something.java\njava Something\n```\n\n### `Throw.java`\n\nThe first file raises an exception on purpose and deals with it in the same\nmethod. Reading it top to bottom:\n\n- **`class CustomException extends Exception`** is the whole of what a custom\n  exception type is. The constructor forwards its message to `Exception` with\n  `super(message)`, and that stored message is what `e.getMessage()` returns\n  further down.\n- **`j = 18 / i`** is integer division — `18 / 20` truncates to `0` rather than\n  `0.9`, which is what makes the `if (j == 0)` branch run at all.\n- The **two commented-out `throw`s** are alternatives, left in deliberately.\n  Uncomment either and the `ArithmeticException` block catches it instead; the\n  live line throws `CustomException`, so the first `catch` wins. The type you\n  throw is what decides where control lands.\n- The **`catch` order** is specific → general. `Exception` has to come last: it\n  is the ancestor of the other two, so putting it first would make them\n  unreachable and the file would not compile.\n- Each `catch` sets **`j = 18 / 1`**, so the program recovers instead of dying\n  and the final `println` has something sensible to report.\n\n```java Throw.java @run-throw-and-throws-throw\n// no package\n\nclass CustomException extends Exception {\n    CustomException(String message) {\n        super(message);\n    }\n}\n\npublic class Throw {\n    public static void main(String[] args) {\n        int i = 20;\n        int j = 0;\n\n        try {\n            j = 18 / i;\n            if (j == 0) {\n                // throw new ArithmeticException();\n                // throw new ArithmeticException("This is arithmetic exception where j = 0");\n                throw new CustomException("This is arithmetic exception where j = 0");\n            }\n        } catch (CustomException e) {\n            j = 18 / 1;\n            System.out.println("Exception: " + e.getMessage());\n        } catch (ArithmeticException e) {\n            j = 18 / 1;\n            System.out.println("Exception: " + e.getMessage());\n        } catch (Exception e) {\n            System.out.println(e);\n        }\n\n        System.out.println("Result is : " + j);\n    }\n}\n```\n\n```output @run-throw-and-throws-throw\nException: This is arithmetic exception where j = 0\nResult is : 18\n```\n\n### `Throws.java`\n\nThe second file never catches anything. It declares instead, and the exception\nwalks out of the program:\n\n- **`Class.forName("Calc")`** looks a class up by name at runtime. There is no\n  class called `Calc`, so it throws `ClassNotFoundException` — a *checked*\n  exception, which means the compiler forces every method in the chain to either\n  catch it or declare it.\n- **`show()` declares `throws ClassNotFoundException`** rather than catching, so\n  the exception escapes to whoever called it.\n- **`main()` declares it too**, so it escapes again — this time to the JVM,\n  which prints a stack trace and ends the program with a non-zero exit code.\n  That chain, `show()` → `main()` → JVM, is the whole point of the file.\n- The two **`println` calls after the throwing lines never run.** Once an\n  exception is raised, control leaves the method immediately.\n- The **`static {}` block** runs once, when the class is loaded — which is\n  *before* `main()` starts. That is why `Class Loaded` appears above the stack\n  trace rather than below it.\n\n```java Throws.java @run-throw-and-throws-throws\n// no package\n\nclass A {\n    public void show() throws ClassNotFoundException {\n        Class.forName("Calc");\n        System.out.println("show() finished normally");\n    }\n}\n\npublic class Throws {\n\n    static {\n        System.out.println("Class Loaded");\n    }\n\n    public static void main(String[] args) throws ClassNotFoundException {\n        A a = new A();\n        a.show();\n\n        System.out.println("main() finished normally");\n    }\n}\n```\n\n```output @run-throw-and-throws-throws\nClass Loaded\nException in thread "main" java.lang.ClassNotFoundException: Calc\n	at java.base/jdk.internal.loader.BuiltinClassLoader.loadClass(BuiltinClassLoader.java:580)\n	at java.base/java.lang.ClassLoader.loadClass(ClassLoader.java:502)\n	at java.base/java.lang.Class.forName0(Native Method)\n	at java.base/java.lang.Class.forName(Class.java:478)\n	at java.base/java.lang.Class.forName(Class.java:468)\n	at A.show(Throws.java:5)\n	at Throws.main(Throws.java:18)\n```\n',revision:"The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- **`throw`** is a statement that throws one object, now. **`throws`** is a\n  declaration on a method saying it might.\n- A custom exception extends `Exception` (checked) or `RuntimeException`\n  (unchecked). Give it a constructor that passes a message to `super`.\n- `catch` blocks are checked **in order**, so the most specific type must come\n  first — a `catch (Exception e)` above a specific one will not compile.\n- An undeclared, uncaught checked exception propagates up the call stack until\n  something handles it or the program ends.\n",interview:"## Interview questions\n\n### Q1: What is the difference between `throw` and `throws`?\n\n**Answer:** `throw` is a statement used inside a method to actually raise an exception object (`throw new X(...)`). `throws` is a clause in a method's signature that declares the checked exceptions the method might propagate to its caller. One performs the action; the other documents the possibility.\n\n### Q2: How do you create a custom exception?\n\n**Answer:** Define a class that extends an existing exception type — `Exception` for a checked exception or `RuntimeException` for an unchecked one — and provide a constructor that passes a message to `super(message)` so `getMessage()` works.\n\n### Q3: Why call `super(message)` in a custom exception's constructor?\n\n**Answer:** The message is stored by the `Throwable` base class. Passing it up with `super(message)` lets `getMessage()` (and the printed stack trace) report it. Omitting it leaves the message `null`.\n\n### Q4: Should a custom exception extend `Exception` or `RuntimeException`?\n\n**Answer:** Extend `Exception` for checked exceptions — recoverable, expected conditions that the compiler forces callers to handle or declare. Extend `RuntimeException` for unchecked exceptions — typically programming bugs that callers shouldn't be forced to catch.\n\n### Q5: Can you throw a built-in exception manually?\n\n**Answer:** Yes. `throw new IllegalArgumentException(\"...\")` or `throw new ArithmeticException(\"...\")` is common for validating inputs. You are not limited to custom types.\n\n### Q6: When you `throw` an exception, which `catch` block handles it?\n\n**Answer:** The first `catch` whose declared type matches the thrown object's type (or a supertype of it), checked top to bottom. That's why specific types must be listed before general ones — otherwise the general block would catch it first and the specific block would be unreachable.\n\n### Q7: What happens to code after a `throw` statement?\n\n**Answer:** It is skipped. `throw` immediately transfers control to the nearest matching `catch` (or propagates out of the method if none matches), so any statements after it in the same block do not execute.\n\n### Q8: What does it mean to *propagate* an exception, and how does `throws` do it?\n\n**Answer:** Propagating means letting an exception travel up the call stack instead of catching it where it occurs. A method declares `throws SomeException` to say \"I won't handle this — my caller must.\" If every method in the chain declares it (e.g. `show()` → `main()`), the exception reaches the JVM, which prints a stack trace and ends the program. Some method must `catch` it to stop the propagation.\n\n### Q9: Why must `main` (or some caller) declare `throws` for a checked exception?\n\n**Answer:** Checked exceptions are enforced by the compiler: any method that calls code which may throw a checked exception must either catch it or re-declare it with `throws`. If a method like `show()` declares `throws ClassNotFoundException`, then `main` must do the same (or use `try`/`catch`), or the program won't compile.\n\n### Q10: When does a `static{}` block run, relative to `main`?\n\n**Answer:** A `static{}` block runs **once**, when the class is first loaded and initialized — which happens **before** `main` executes. That is why File 2 prints `Class Loaded` before the exception. Static blocks are typically used for one-time setup of static state.\n"}},{slug:`reading-input`,title:`Reading input`,subject:`Java`,stage:`Errors and input`,chapter:30,accent:`#38bdf8`,summary:`Every program so far has had its data written into the source. Real programs get it from somewhere else, and the simplest somewhere else is the keyboard.`,parts:{notes:'Every program so far has had its data written into the source. Real programs get\nit from somewhere else, and the simplest somewhere else is the keyboard.\n\nJava gives you `System.in` for that. It is the counterpart to `System.out`, and\nit is deliberately primitive: it hands you one **byte** at a time and leaves the\nrest to you. This chapter covers the three ways up from there.\n\n## What `System.in` actually is\n\n`System.in` is an `InputStream` — a source of raw bytes, not text. That single\nfact explains everything that surprises people about input in Java.\n\n```\n   keyboard  ──▶  System.in  ──▶  one byte at a time (an int, 0-255)\n                     │\n                     ├──▶  InputStreamReader   turns bytes into characters\n                     │            │\n                     │            └──▶  BufferedReader   gives you whole lines\n                     │\n                     └──▶  Scanner             parses numbers and words directly\n```\n\nEach layer adds convenience. You will use the top of the stack almost always,\nbut knowing the bottom exists is what makes the errors readable.\n\n## Reading a single byte\n\n```java Input.java\nimport java.io.IOException;\n\npublic class Input {\n    public static void main(String[] args) throws IOException {\n\n        System.out.println();\n\n        int n = System.in.read();\n\n        System.out.println(n);\n\n        System.out.println((char) n);\n    }\n}\n```\n\n```bash @run-input-1\njavac Input.java\njava Input\n```\n\nType `A` and press Enter:\n\n```output @run-input-1\n65\nA\n```\n\nTwo things worth pulling out.\n\n**`read()` returns an `int`, not a `char`.** It returns the byte value, which is\n65 for `A`, and `-1` when the input has ended. That is why it cannot be a\n`char` — there would be no value left over to mean "nothing more".\n\n**It reads one byte, not one keypress.** Pressing Enter puts a newline into the\nstream as well, so the next `read()` would return 10 rather than waiting for you.\nThis is the source of most "my program skipped a question" bugs.\n\n**`throws IOException` is not optional.** Reading can fail, so `read()` declares a\nchecked exception. Either declare it on `main` as above, or wrap the call in\n`try`/`catch`.\n\n## Reading a whole line with `BufferedReader`\n\nOne byte at a time is unusable for real input. `InputStreamReader` turns the byte\nstream into characters, and `BufferedReader` turns those into lines.\n\n```java BufferReader.java\nimport java.io.BufferedReader;\nimport java.io.InputStreamReader;\n\npublic class BufferReader {\n    public static void main(String[] args) throws Exception {\n\n        System.out.println("Enter a number");\n\n        InputStreamReader S_Reader = new InputStreamReader(System.in);\n\n        BufferedReader BR = new BufferedReader(S_Reader);\n\n        int n = Integer.parseInt(BR.readLine());\n\n        System.out.println(n);\n    }\n}\n```\n\n```bash @run-input-2\njavac BufferReader.java\njava BufferReader\n```\n\nType `42` and press Enter:\n\n```output @run-input-2\nEnter a number\n42\n```\n\n`readLine()` returns a **`String`** — always. There is no `readInt()`. Converting\nis your job, and `Integer.parseInt` is how:\n\n| Want | Use |\n|---|---|\n| `int` | `Integer.parseInt(line)` |\n| `long` | `Long.parseLong(line)` |\n| `double` | `Double.parseDouble(line)` |\n| `boolean` | `Boolean.parseBoolean(line)` |\n\n`readLine()` returns `null` at end of input, and `parseInt` throws\n`NumberFormatException` if the text is not a number. Both are worth handling in\nanything that will meet a real user.\n\nThe two wrappers are usually written as one expression:\n\n```java\nBufferedReader br = new BufferedReader(new InputStreamReader(System.in));\n```\n\n## Reading with `Scanner`\n\n`Scanner` does the parsing for you, which makes it the usual choice while\nlearning:\n\n```java\nimport java.util.Scanner;\n\nScanner sc = new Scanner(System.in);\n\nSystem.out.print("Name: ");\nString name = sc.nextLine();\n\nSystem.out.print("Age: ");\nint age = sc.nextInt();\n\nSystem.out.println(name + " is " + age);\n\nsc.close();\n```\n\n| Method | Reads |\n|---|---|\n| `nextLine()` | the rest of the line, as a `String` |\n| `next()` | one word, up to the next space |\n| `nextInt()` | an `int` |\n| `nextDouble()` | a `double` |\n| `nextBoolean()` | a `boolean` |\n| `hasNextInt()` | checks before reading, so you can validate |\n\n## `Scanner` or `BufferedReader`?\n\n| | `Scanner` | `BufferedReader` |\n|---|---|---|\n| Parsing | built in — `nextInt()` | you call `Integer.parseInt` |\n| Speed | slower | considerably faster |\n| Buffer | small | large |\n| Errors | `InputMismatchException` | `NumberFormatException` |\n| Thread safety | not synchronised | synchronised |\n\nUse `Scanner` while learning and for anything interactive. Use `BufferedReader`\nwhen input volume matters — competitive programming problems that read a hundred\nthousand lines are the standard example, and the difference there is seconds, not\nmilliseconds.\n\n## How to Compile and Run\n\n```bash\njavac Input.java\njava Input\n\njavac BufferReader.java\njava BufferReader\n```\n\nBoth wait for you to type. Enter a value and press Enter.\n\n## Common Mistakes\n\n### Mistake 1: `nextInt()` followed by `nextLine()` reading nothing\n\n```java\nint age = sc.nextInt();\nString name = sc.nextLine();\n```\n\n`nextInt()` consumes the digits and stops. The Enter key is still sitting in the\nstream, so the next `nextLine()` finds it immediately and returns `""`. Fix it by\nadding a throwaway `sc.nextLine()` after the number, or by reading everything\nwith `nextLine()` and parsing yourself.\n\nThis is the single most common input bug in Java, and it looks like the program\nskipped a question.\n\n### Mistake 2: Expecting `read()` to give you a character\n\n`System.in.read()` returns an `int` — the byte value. Cast it with `(char) n`\nwhen you want the character. And remember `-1` means the input has ended.\n\n### Mistake 3: Forgetting that `readLine()` returns text\n\n```java\nint n = br.readLine();              // does not compile\nint n = Integer.parseInt(br.readLine());  // correct\n```\n\n### Mistake 4: Not handling `IOException`\n\n`read()` and `readLine()` both throw a checked exception. `throws IOException` on\n`main` is fine for a learning program; real code catches it.\n\n### Mistake 5: Closing `System.in`\n\n`sc.close()` closes the underlying `System.in` as well. Open a second `Scanner`\nafterwards and it will find the stream already closed. Create one `Scanner` for\nthe whole program.\n\n### Mistake 6: Assuming input is valid\n\n`Integer.parseInt("abc")` throws `NumberFormatException`, and `sc.nextInt()` on\nthe same input throws `InputMismatchException`. Check with `hasNextInt()`, or\ncatch the exception.\n\n## The source files\n\nThe complete programs, renamed from the numbered file names the repository\nuses so they compile on their own. **Save** writes the file out of this page;\n**Run** replays the output it produced when this page was built.\n\n```bash\njavac Something.java\njava Something\n```\n\n### `Input.java`\n\nThe rawest way to read input there is. `System.in.read()` returns **one byte**,\nas an `int` — so typing `A` gives `65`, and casting that to `char` gives `A`\nback. It reads a single character, not a line, and it hands you a number rather\nthan text.\n\n`main` declares `throws IOException` because `read()` is a checked-exception\nmethod and nothing here catches it.\n\n```java Input.java @run-reading-input-input\nimport java.io.IOException;\n\npublic class Input {\n    public static void main(String[] args) throws IOException {\n        \n        System.out.println();\n        \n        int n = System.in.read();\n\n        System.out.println(n);\n\n        System.out.println((char)n);\n    }\n}\n```\n\n```output @run-reading-input-input\n\n65\nA\n```\n\n### `BufferReader.java`\n\nThe practical version. `BufferedReader` wrapped around an\n`InputStreamReader` reads a whole **line** at a time as a `String`, which is\nwhat you almost always want.\n\nThe cost is that a line of text is not a number: `Integer.parseInt(...)` has to\nconvert it, and it throws `NumberFormatException` if what arrived was not\nnumeric.\n\n```java BufferReader.java @run-reading-input-buffer-reader\nimport java.io.BufferedReader;\nimport java.io.InputStreamReader;\n\npublic class BufferReader {\n    public static void main(String[] args) throws Exception {\n\n        System.out.println("Enter a number");\n\n        InputStreamReader S_Reader = new InputStreamReader(System.in);\n\n        BufferedReader BR = new BufferedReader(S_Reader);\n\n        int n = Integer.parseInt(BR.readLine());\n\n        System.out.println(n);\n    }\n}\n```\n\n```output @run-reading-input-buffer-reader\nEnter a number\n42\n```\n',revision:'The chapter in six lines. If a line here is news, the **Notes** part is where\nit comes from.\n\n## Worth remembering\n\n- `System.in` gives you raw **bytes**. `read()` returns an `int`, and `-1` at end\n  of input.\n- `new BufferedReader(new InputStreamReader(System.in))` gets you `readLine()`,\n  which always returns a `String` — convert with `Integer.parseInt`.\n- `Scanner` parses for you and is slower; `BufferedReader` is faster and does\n  not.\n- `nextInt()` leaves the newline behind, so the next `nextLine()` returns `""`.\n  The most common input bug in Java.\n- Closing a `Scanner` closes `System.in` with it. Create one for the whole\n  program.\n',interview:'## Interview questions\n\n### Q1: What is `System.in`, and what does it give you?\n\n**Answer:** `System.in` is an `InputStream` — a source of raw **bytes**, not\ntext. Its `read()` method returns one byte at a time as an `int`, and `-1` when\nthe input has ended. Everything else — `InputStreamReader`, `BufferedReader`,\n`Scanner` — is a layer on top that turns those bytes into characters, lines or\nparsed values.\n\n### Q2: Why does `System.in.read()` return an `int` rather than a `char`?\n\n**Answer:** Because it needs a value that no real byte can take, to signal that\nthe input has ended. A byte is 0 to 255; `read()` returns those values plus `-1`\nfor end of stream. A `char` has no spare value to mean "nothing more", so the\nreturn type has to be wider.\n\n### Q3: What is the difference between `Scanner` and `BufferedReader`?\n\n**Answer:** `Scanner` parses for you — `nextInt()`, `nextDouble()`, `next()` —\nwhich makes it convenient but slower, and it uses a small buffer.\n`BufferedReader` only gives you `readLine()`, returning a `String` you convert\nyourself with `Integer.parseInt`, but it is considerably faster and uses a large\nbuffer. `Scanner` for interactive programs, `BufferedReader` when the input is\nlarge.\n\n### Q4: Why do you need `InputStreamReader` between `System.in` and `BufferedReader`?\n\n**Answer:** Because they speak different things. `System.in` produces bytes;\n`BufferedReader` reads characters. `InputStreamReader` is the bridge that decodes\nbytes into characters using a character set. That is why the two are almost\nalways written together as\n`new BufferedReader(new InputStreamReader(System.in))`.\n\n### Q5: Why does `nextLine()` return an empty String straight after `nextInt()`?\n\n**Answer:** `nextInt()` reads the digits and stops, leaving the newline from the\nEnter key in the stream. The following `nextLine()` finds that newline\nimmediately and returns everything before it, which is nothing. The fix is either\nan extra throwaway `sc.nextLine()` after the number, or reading every line with\n`nextLine()` and parsing the values yourself. It is the most common input bug in\nJava, and it looks like the program skipped a question.\n\n### Q6: How do you convert the `String` from `readLine()` into a number?\n\n**Answer:** With the wrapper class parse methods: `Integer.parseInt`,\n`Long.parseLong`, `Double.parseDouble`, `Boolean.parseBoolean`. All of them throw\n`NumberFormatException` if the text is not a valid number, so anything meeting a\nreal user should catch it or validate first.\n\n### Q7: Why do input methods throw `IOException`, and what must you do about it?\n\n**Answer:** Reading from a stream can fail — the source can be closed or\nunavailable — so `read()` and `readLine()` declare `IOException`, which is a\n**checked** exception. The compiler forces you to either catch it or declare\n`throws IOException` on the enclosing method. Declaring it on `main` is\nacceptable in a small program; real code handles it.\n\n### Q8: What happens if you close a `Scanner` wrapping `System.in`?\n\n**Answer:** It closes `System.in` itself, because closing a wrapper closes what\nit wraps. Any `Scanner` created afterwards will find the stream already closed\nand fail. Create one `Scanner` for the whole program rather than one per method.\n'}}],ir=`Every Java tutorial says the same sentence and then moves on: *"Java has a garbage\r
collector, so you don't have to free memory."* True, and useless. It tells you\r
nothing about why your service pauses for 400 ms every few minutes, or why a\r
program with a garbage collector can still die of \`OutOfMemoryError\`.\r
\r
This walks through what the collector actually does — the one rule it follows,\r
the shape of the heap it works on, and the flags that make it narrate its own\r
work. Two small programs come with it. Both run on any JDK 8 or newer, no\r
libraries, no build tool.\r
\r
> Everything here is observation, not configuration advice. Do not tune a\r
> collector because a guide told you to — measure first, and change one flag at a\r
> time.\r
\r
## The one rule: reachability\r
\r
The collector does not care how old an object is, how big it is, or whether you\r
are finished with it. It asks exactly one question:\r
\r
**Can this object still be reached, by following references, from a GC root?**\r
\r
GC roots are the places the JVM trusts as definitely alive:\r
\r
- local variables and parameters in every method currently running\r
- \`static\` fields of loaded classes\r
- live threads\r
- references held by native code\r
\r
It starts at those roots and follows every reference it can. What it reaches is\r
live. Everything else is garbage, whatever you intended.\r
\r
\`\`\`text\r
        GC ROOTS                         THE HEAP\r
   +----------------+          +-------------------------------+\r
   | main() frame   |          |                               |\r
   |   note ---------------------->  Note("keep me")    LIVE   |\r
   |   cache -------------------->   ArrayList          LIVE   |\r
   +----------------+          |         |                     |\r
   | static fields  |          |         +---> Note("cached")  |\r
   |   CONFIG ------------------->   Config             LIVE   |\r
   +----------------+          |                               |\r
                               |     Note("abandoned")  GARBAGE|\r
                               |     (nothing points here)     |\r
                               +-------------------------------+\r
\`\`\`\r
\r
Two consequences fall straight out of this, and both matter:\r
\r
- **Reference cycles are not a problem.** Two objects pointing at each other are\r
  collected happily, because no root reaches either. This is the thing Python's\r
  old reference-counting collector had to add a whole extra cycle detector for.\r
- **A reference you forgot about keeps an object alive forever.** That is the\r
  entire mechanism behind a Java memory leak, and there is a section on it below.\r
\r
## Watching an object die\r
\r
\`WeakReference\` lets you point at an object *without* keeping it alive. It is the\r
only clean way to observe collection happening, so it makes a good first demo.\r
\r
\`\`\`java Reachability.java\r
import java.lang.ref.WeakReference;\r
import java.util.ArrayList;\r
import java.util.List;\r
\r
public class Reachability {\r
\r
    static class Note {\r
        final String text;\r
        Note(String text) { this.text = text; }\r
    }\r
\r
    public static void main(String[] args) {\r
\r
        // A strong reference keeps the object alive through a collection.\r
        Note note = new Note("keep me");\r
        WeakReference<Note> watcher = new WeakReference<>(note);\r
\r
        System.gc();\r
        System.out.println("still alive while referenced : " + (watcher.get() != null));\r
\r
        note = null;   // the last path to the object is gone\r
        System.gc();\r
        System.out.println("alive after reference dropped: " + (watcher.get() != null));\r
\r
        // The shape of every Java memory leak: something else is still holding it.\r
        List<Note> cache = new ArrayList<>();\r
        Note cached = new Note("forgotten");\r
        WeakReference<Note> cacheWatcher = new WeakReference<>(cached);\r
\r
        cache.add(cached);   // the list now holds a second strong reference\r
        cached = null;       // dropping OUR reference is not enough\r
        System.gc();\r
        System.out.println("alive while list holds it   : " + (cacheWatcher.get() != null));\r
\r
        cache.clear();\r
        System.gc();\r
        System.out.println("alive after list cleared    : " + (cacheWatcher.get() != null));\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-reach\r
javac Reachability.java\r
java Reachability\r
\`\`\`\r
\r
\`\`\`output @run-reach\r
still alive while referenced : true\r
alive after reference dropped: false\r
alive while list holds it   : true\r
alive after list cleared    : false\r
\`\`\`\r
\r
Line two is the collector doing its job. Line three is the bug that will page you\r
at 3 a.m.: the object is unreachable *from your code* and perfectly reachable\r
*from the heap*, so it stays.\r
\r
> \`System.gc()\` is a request, not a command. It works here because these objects\r
> are plainly unreachable and the JVM has nothing better to do. Never put it in\r
> real code — see the mistakes section.\r
\r
## The heap, drawn\r
\r
Decades of measurement produced one dominant observation, usually called the\r
**weak generational hypothesis**:\r
\r
> Most objects die very young. The ones that survive a while tend to keep\r
> surviving.\r
\r
So the JVM sorts the heap by age and collects the young part often and cheaply:\r
\r
\`\`\`text\r
+--------------------------------------------------------------------+\r
|                             THE HEAP                               |\r
|                                                                    |\r
|  YOUNG GENERATION  (collected often, cheap)                        |\r
|  +------------------------+  +---------+  +---------+              |\r
|  |          Eden          |  |   S0    |  |   S1    |              |\r
|  | every 'new' lands here |  | survivor|  | survivor|              |\r
|  +------------------------+  +---------+  +---------+              |\r
|                                                                    |\r
|  OLD GENERATION / TENURED  (collected rarely, expensive)           |\r
|  +--------------------------------------------------------------+  |\r
|  |   objects that survived enough young collections              |  |\r
|  +--------------------------------------------------------------+  |\r
|                                                                    |\r
+--------------------------------------------------------------------+\r
\r
  METASPACE — not the heap. Class metadata, in native memory.\r
\`\`\`\r
\r
**Eden** is where \`new\` puts almost everything. Allocation there is a pointer\r
bump, which is why creating objects in Java is so much cheaper than people\r
assume. The two **survivor** spaces are a holding area, and only one is in use at\r
a time. The **old generation** is where objects go once they have proved they are\r
sticking around.\r
\r
Metaspace is worth naming because it trips people up: it holds class metadata,\r
lives outside the heap in native memory, and has its own \`OutOfMemoryError\`.\r
\r
## The life of one object\r
\r
Follow a single object through the machine.\r
\r
**It is allocated in Eden**, alongside everything else.\r
\r
\`\`\`text\r
Eden: [ A ][ B ][ C ][ D ]     S0: (empty)   S1: (empty)   Old: (empty)\r
\`\`\`\r
\r
**Eden fills up, so a minor GC runs.** Say only \`B\` and \`D\` are still reachable.\r
\r
\`\`\`text\r
Eden: [ ..... empty ..... ]    S0: [ B(1) ][ D(1) ]        Old: (empty)\r
                                    ^ survivors copied out, age 1\r
                                      A and C are simply gone\r
\`\`\`\r
\r
Look closely at what happened to the dead objects: **nothing**. They were not\r
deleted one at a time. The survivors were copied out and the whole of Eden was\r
declared empty in a single step. Garbage costs nothing to collect. Only surviving\r
costs anything — which is why a young collection where almost everything died is\r
nearly free.\r
\r
**More allocation, another minor GC.** Survivors move to the *other* survivor\r
space and their age goes up.\r
\r
\`\`\`text\r
Eden: [ empty ]     S0: (empty)     S1: [ B(2) ][ E(1) ]     Old: (empty)\r
                                          D died this round\r
\`\`\`\r
\r
**\`B\` keeps surviving.** Once its age crosses the tenuring threshold — 15 by\r
default — it is promoted out of the young generation for good.\r
\r
\`\`\`text\r
Eden: [ empty ]     S0: [ F(1) ]    S1: (empty)     Old: [ B ]\r
\`\`\`\r
\r
**The old generation fills up** over minutes or hours, and now the expensive\r
collection runs, because it has to walk the whole heap.\r
\r
| | Cleans | Frequency | Typical pause |\r
|---|---|---|---|\r
| **Minor / young GC** | Young generation | Very often | Under a millisecond to a few ms |\r
| **Major / old GC** | Old generation | Rarely | Noticeably longer |\r
| **Full GC** | Whole heap **+** metaspace | Rarest | The spike on your latency graph |\r
\r
**Stop-the-world** means the JVM suspends every application thread while the\r
collector works. Every collector does this at some point. The difference between\r
them is how long those pauses last and how much work happens concurrently, next\r
to your running code.\r
\r
## Watching the heap move\r
\r
The second program prints the three numbers \`Runtime\` exposes, so you can watch\r
memory get used and handed back.\r
\r
\`\`\`java HeapWatch.java\r
public class HeapWatch {\r
\r
    static void report(String label) {\r
        Runtime rt = Runtime.getRuntime();\r
        long max = rt.maxMemory();            // the ceiling set by -Xmx\r
        long committed = rt.totalMemory();    // currently reserved from the OS\r
        long used = committed - rt.freeMemory();\r
        System.out.printf("%-22s used=%4d MB  committed=%4d MB  max=%4d MB%n",\r
                label, mb(used), mb(committed), mb(max));\r
    }\r
\r
    static long mb(long bytes) { return bytes / (1024 * 1024); }\r
\r
    public static void main(String[] args) throws InterruptedException {\r
        report("at startup");\r
\r
        // Two million Strings, each abandoned by the next iteration.\r
        // Exactly the "most objects die young" pattern the JVM is built for.\r
        String last = null;\r
        for (int i = 0; i < 2_000_000; i++) {\r
            last = "object number " + i;\r
        }\r
        System.out.println("last allocated        : " + last);\r
        report("after allocating");\r
\r
        System.gc();\r
        Thread.sleep(100);\r
        report("after System.gc()");\r
\r
        // These stay reachable, so no collection can reclaim them.\r
        byte[][] retained = new byte[200][];\r
        for (int i = 0; i < retained.length; i++) {\r
            retained[i] = new byte[256 * 1024];   // 50 MB in total\r
        }\r
\r
        System.gc();\r
        Thread.sleep(100);\r
        report("holding 50 MB live");\r
        System.out.println("retained blocks       : " + retained.length);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-heap\r
javac HeapWatch.java\r
java HeapWatch\r
\`\`\`\r
\r
Your numbers will differ — the default heap size depends on how much RAM the\r
machine has. The pattern is the point:\r
\r
\`\`\`output @run-heap\r
at startup             used=   1 MB  committed=  10 MB  max=4024 MB\r
last allocated        : object number 1999999\r
after allocating       used=  53 MB  committed=  90 MB  max=4024 MB\r
after System.gc()      used=   2 MB  committed=   8 MB  max=4024 MB\r
holding 50 MB live     used=  51 MB  committed= 108 MB  max=4024 MB\r
retained blocks       : 200\r
\`\`\`\r
\r
Two million Strings were created, and usage peaked at 53 MB rather than the\r
hundred-odd megabytes that would imply — the collector was already clearing up\r
in the background while the loop ran. After the explicit collection it drops to\r
almost nothing. The last line stays high because \`retained\` still points at that\r
memory, and reachable memory does not come back no matter how often you collect.\r
\r
Notice \`committed\` going both up and down. The JVM borrows memory from the OS\r
when it needs it and may hand some back afterwards. \`max\` is a ceiling, not a\r
reservation.\r
\r
## Making the JVM narrate\r
\r
You do not need a profiler for the first pass. The JVM will log its own\r
collections:\r
\r
\`\`\`bash\r
java -Xlog:gc HeapWatch\r
\`\`\`\r
\r
Real output from the program above:\r
\r
\`\`\`text\r
[0.007s][info][gc] Using G1\r
[0.058s][info][gc] GC(0) Pause Young (Normal) (G1 Evacuation Pause) 5M->1M(10M) 1.333ms\r
[0.070s][info][gc] GC(3) Pause Young (Normal) (G1 Evacuation Pause) 17M->1M(30M) 0.701ms\r
[0.080s][info][gc] GC(6) Pause Young (Normal) (G1 Evacuation Pause) 17M->1M(90M) 0.972ms\r
[0.102s][info][gc] GC(7) Pause Full (System.gc()) 53M->1M(8M) 3.163ms\r
\`\`\`\r
\r
One line, taken apart:\r
\r
\`\`\`text\r
GC(3) Pause Young (Normal) (G1 Evacuation Pause) 17M->1M(30M) 0.701ms\r
 |     |                                          |    |  |     |\r
 |     |                                          |    |  |     +-- how long the app was paused\r
 |     |                                          |    |  +-------- total heap size at that moment\r
 |     |                                          |    +----------- heap used AFTER  the collection\r
 |     |                                          +---------------- heap used BEFORE the collection\r
 |     +--------------------------------------------------------- a young collection, not a full one\r
 +--------------------------------------------------------------- collection number 3\r
\`\`\`\r
\r
That third line is the whole article in one sentence: **17 MB of objects existed,\r
1 MB was still reachable, and reclaiming the other 16 MB took 0.7 milliseconds.**\r
\r
\`-Xlog:gc*\` prints every phase if you want the detail.\r
\`-Xlog:gc:file=gc.log\` writes to a file instead of the console. On Java 8 the\r
older spelling is \`-XX:+PrintGCDetails\`.\r
\r
## Which collector you are running\r
\r
The JVM ships several. You pick one with a flag; the code never changes.\r
\r
| Collector | Flag | Suits | Trade-off |\r
|---|---|---|---|\r
| **Serial** | \`-XX:+UseSerialGC\` | Small heaps, one CPU, containers | Single-threaded, simple, predictable |\r
| **Parallel** | \`-XX:+UseParallelGC\` | Batch jobs | Best throughput, longest pauses |\r
| **G1** *(default)* | \`-XX:+UseG1GC\` | General server work | Balances throughput against pause time |\r
| **ZGC** | \`-XX:+UseZGC\` | Large heaps, low latency | Sub-millisecond pauses even on huge heaps |\r
\r
**G1** — Garbage-First — has been the default since Java 9. It does not use three\r
fixed blocks. It splits the heap into a couple of thousand equal regions, labels\r
each one Eden, survivor or old, and collects the regions holding the most garbage\r
first. Hence the name. You can ask it for a pause target with\r
\`-XX:MaxGCPauseMillis=200\`, and it will try.\r
\r
Check what you are actually getting:\r
\r
\`\`\`powershell @flags\r
java -XX:+PrintFlagsFinal -version | findstr "UseG1GC UseZGC UseSerialGC UseParallelGC"\r
\`\`\`\r
\r
\`\`\`output @flags\r
bool UseG1GC                = true    {product} {ergonomic}\r
bool UseParallelGC          = false   {product} {default}\r
bool UseSerialGC            = false   {product} {default}\r
bool UseZGC                 = false   {product} {default}\r
\`\`\`\r
\r
\`{ergonomic}\` means the JVM chose it for you based on the machine it found.\r
\r
## Leaks, in a language that cannot leak\r
\r
You never forget to \`free\`, so the C-style leak is gone. You can still exhaust\r
the heap, and it always looks like this:\r
\r
> A Java memory leak is an object that is still **reachable** but no longer\r
> **useful**.\r
\r
The collector sees a live reference and correctly refuses to touch it. The usual\r
suspects:\r
\r
- **A collection that only grows.** A \`static Map\` used as a cache with no\r
  eviction policy. Every entry stays reachable for the life of the process.\r
- **Listeners registered and never removed.** The publisher holds a reference to\r
  your object forever, and your object holds whatever it was holding.\r
- **Long-lived objects holding short-lived ones.** A session that keeps every\r
  request it has handled.\r
- **\`ThreadLocal\` in a thread pool.** The thread outlives the request, so the\r
  value stays attached until something removes it explicitly.\r
\r
When the heap fills you get:\r
\r
\`\`\`text\r
Exception in thread "main" java.lang.OutOfMemoryError: Java heap space\r
\`\`\`\r
\r
That is not the collector failing. It means it ran, found everything still\r
reachable, and had nothing to reclaim. Look at your own object graph.\r
\r
## When it goes wrong\r
\r
- **\`OutOfMemoryError: Java heap space\`** — either the workload genuinely needs\r
  more than \`-Xmx\` allows, or something is holding references it should have\r
  dropped. Run with \`-XX:+HeapDumpOnOutOfMemoryError\` and open the resulting\r
  \`.hprof\` in Eclipse MAT or VisualVM; the dominator tree points at the culprit\r
  in about a minute.\r
- **\`OutOfMemoryError: Metaspace\`** — the heap is fine and class metadata is not.\r
  Usually a container or framework loading classes dynamically without bound.\r
  \`-XX:MaxMetaspaceSize\` caps it so it fails early instead of eating the machine.\r
- **\`OutOfMemoryError: GC overhead limit exceeded\`** — the JVM is spending more\r
  than 98% of its time collecting and recovering less than 2%. The heap is\r
  effectively full; this is the polite version of the error above.\r
- **\`StackOverflowError\`** — nothing to do with the heap. That is a runaway\r
  recursion filling a thread's stack. \`-Xss\` sizes it, but the recursion is\r
  usually the bug.\r
- **Regular latency spikes in production** — turn on GC logging before guessing.\r
  If the spikes line up with \`Pause Full\` lines, you have a promotion problem, not\r
  a slow database.\r
- **The program runs fine, then dies on a smaller machine** — the default heap is\r
  a fraction of physical RAM, so it silently changes between your laptop and the\r
  server. Set \`-Xmx\` explicitly for anything you deploy.\r
\r
## Things worth unlearning\r
\r
**\`System.gc()\` does not force a collection.** It is a hint, it can be disabled\r
outright with \`-XX:+DisableExplicitGC\`, and when it is honoured it usually\r
triggers a full GC — the expensive one. It belongs in demos like the ones above,\r
not in application code.\r
\r
**Setting a local variable to \`null\` does not help.** The JVM already knows a\r
local is dead after its last use. It only matters for a long-lived *field* that\r
holds something large well past its usefulness.\r
\r
**\`finalize()\` is not cleanup.** It may run late, on any thread, or never. It has\r
been deprecated for removal since Java 9. Use try-with-resources:\r
\r
\`\`\`java\r
try (var reader = new BufferedReader(new FileReader("data.txt"))) {\r
    // close() is guaranteed, collector or no collector\r
}\r
\`\`\`\r
\r
**\`-Xmx\` is not memory used.** It is a ceiling. \`maxMemory()\` reporting 4 GB\r
means the JVM is allowed 4 GB, not that it has taken it.\r
\r
**The heap is not all the memory Java uses.** Metaspace, thread stacks,\r
JIT-compiled code and direct \`ByteBuffer\`s all live outside it. A Java process\r
can comfortably use more OS memory than \`-Xmx\` — which is exactly how a container\r
with a memory limit ends up killed while the heap looks healthy.\r
\r
## Worth trying\r
\r
Run \`HeapWatch\` with a small heap and watch the behaviour change:\r
\r
\`\`\`bash\r
java -Xmx64m -Xlog:gc HeapWatch\r
\`\`\`\r
\r
It still finishes, because the collector keeps clearing Eden — it just has to\r
work much harder, and the log shows it. Drop to \`-Xmx32m\` and it fails, because\r
the retained 50 MB no longer fits anywhere. That failure is the clearest possible\r
demonstration of the difference between garbage and live data.\r
\r
## Further reading\r
\r
- [HotSpot garbage collection tuning guide](https://docs.oracle.com/en/java/javase/21/gctuning/index.html)\r
- [JEP 248: G1 as the default collector](https://openjdk.org/jeps/248)\r
- [Unified JVM logging (\`-Xlog\`)](https://docs.oracle.com/en/java/javase/21/docs/specs/man/java.html#enable-logging-with-the-jvm-unified-logging-framework)\r
- [Eclipse Memory Analyzer](https://eclipse.dev/mat/) — for when a heap dump is the only way forward\r
`,ar=`The notes end with a working mental model: reachability decides what dies,\r
generations make young collections cheap, and the log tells you what happened.\r
That is enough to reason about a program. It is not enough to diagnose a\r
production pause or choose a collector on purpose.\r
\r
This is the layer underneath: how G1 actually works, what a safepoint costs, how\r
the low-latency collectors avoid stopping the world at all, and what to do when\r
the heap fills anyway.\r
\r
## G1, properly\r
\r
G1 does not have three fixed blocks. It splits the heap into **1024 to 2048\r
equal-sized regions**, between 1 MB and 32 MB each depending on heap size, and\r
labels each region by role:\r
\r
\`\`\`text\r
 ┌────┬────┬────┬────┬────┬────┬────┬────┐\r
 │ E  │ O  │ O  │ E  │ S  │ O  │ F  │ E  │   E = Eden      S = Survivor\r
 ├────┼────┼────┼────┼────┼────┼────┼────┤   O = Old       F = Free\r
 │ O  │ F  │ H  │ H  │ O  │ E  │ O  │ F  │   H = Humongous\r
 ├────┼────┼────┼────┼────┼────┼────┼────┤\r
 │ F  │ O  │ E  │ O  │ F  │ S  │ O  │ O  │\r
 └────┴────┴────┴────┴────┴────┴────┴────┘\r
\`\`\`\r
\r
The roles are **not contiguous**. Eden is wherever the Eden-labelled regions\r
happen to be, and a region changes role over its life. That is the design that\r
lets G1 target a pause time: it estimates how long collecting each region will\r
take, sorts by how much garbage each holds, and collects as many as fit in the\r
budget you asked for. Garbage first.\r
\r
\`\`\`bash\r
java -XX:+UseG1GC -XX:MaxGCPauseMillis=100 -XX:G1HeapRegionSize=8m MyApp\r
\`\`\`\r
\r
### Humongous objects\r
\r
An object larger than **half a region** cannot be allocated normally. It becomes\r
a **humongous** allocation: a run of contiguous regions reserved for one object,\r
rounded up to a whole number of regions.\r
\r
This is a real operational problem and it looks like a mystery. With 1 MB\r
regions, an array of 600 KB is humongous and occupies a full 1 MB region.\r
Allocate a lot of them and you fragment the heap into unusable gaps, then get\r
**humongous allocation failures** that trigger full GCs while the heap looks\r
half empty.\r
\r
\`\`\`text\r
[gc] GC(41) Pause Young (Concurrent Start) (G1 Humongous Allocation) 812M->790M(1024M)\r
\`\`\`\r
\r
Seeing \`G1 Humongous Allocation\` as a pause *cause* is the signal. The fixes, in\r
order of preference: allocate smaller buffers, or raise\r
\`-XX:G1HeapRegionSize\` so the objects stop being humongous.\r
\r
### Remembered sets and the write barrier\r
\r
To collect one region without scanning the whole heap, G1 must know which\r
references point **into** it from elsewhere. It keeps that in a per-region\r
**remembered set**.\r
\r
Keeping remembered sets accurate means intercepting reference writes. Every\r
\`obj.field = other\` in your program compiles to the store **plus a write\r
barrier** — a few instructions that check whether the write crosses regions and,\r
if so, queue it for the remembered-set machinery.\r
\r
That is the hidden cost of G1: a small tax on every reference write in the whole\r
application, plus the memory the remembered sets occupy, which on a\r
reference-dense heap can be several percent. It buys the ability to collect a\r
subset of regions, which is what makes pause targets possible.\r
\r
### Concurrent marking, and mixed collections\r
\r
Old regions are not collected by young pauses. They are dealt with by a\r
concurrent cycle:\r
\r
1. **Initial mark** — piggybacked on a young pause, marks the roots.\r
2. **Concurrent mark** — walks the object graph *while the application runs*.\r
3. **Remark** — a short stop-the-world pause to finish up.\r
4. **Cleanup** — reclaims regions that turned out to be entirely garbage, for\r
   free, without evacuating anything.\r
5. **Mixed collections** — subsequent young pauses also collect the old regions\r
   with the most garbage.\r
\r
The cycle starts when heap occupancy crosses **IHOP**, adaptive by default and\r
pinnable with \`-XX:InitiatingHeapOccupancyPercent=45\`.\r
\r
Marking concurrently while the graph mutates needs an algorithm that cannot lose\r
a live object. G1 uses **SATB** — snapshot-at-the-beginning: the write barrier\r
records the *old* value of any overwritten reference, so anything reachable when\r
marking started stays marked. The cost is **floating garbage**: objects that\r
died during the cycle are not collected until the next one. That is a deliberate\r
trade — a little wasted space in exchange for never pausing to mark.\r
\r
> If the concurrent cycle cannot finish before the heap fills, G1 falls back to a\r
> **full, single-threaded, stop-the-world** collection. \`Pause Full\` in the log\r
> of a G1 application is a failure, not a routine event. It means the collector\r
> lost the race, and the answer is usually a bigger heap or a lower IHOP.\r
\r
## Safepoints\r
\r
Almost every GC operation needs the application to stop at a point where the JVM\r
can walk the stacks and know what is a reference. Those points are\r
**safepoints**, and a stop-the-world pause has two parts:\r
\r
\`\`\`text\r
   request  ──▶  all threads reach a safepoint  ──▶  do the work  ──▶  resume\r
                 └─── time to safepoint ───┘         └── GC time ──┘\r
\`\`\`\r
\r
The GC log reports the second part. The first can be worse and does not show up\r
in naive monitoring. A thread only polls for safepoints at method returns and\r
loop back-edges — and a **counted loop over an \`int\`** historically had its poll\r
optimised away, so a long-running tight loop could hold every other thread\r
waiting for milliseconds.\r
\r
\`\`\`bash\r
java -Xlog:safepoint MyApp\r
java -XX:+UnlockDiagnosticVMOptions -XX:+PrintSafepointStatistics MyApp\r
\`\`\`\r
\r
Long time-to-safepoint is worth suspecting when pause *duration* looks fine but\r
latency does not. And note that GC is not the only cause: biased-lock revocation,\r
\`Thread.getAllStackTraces\`, class redefinition by an agent and heap dumps all run\r
at safepoints. A profiler that samples via \`getStackTrace\` is itself a safepoint\r
storm, which is why async profilers exist.\r
\r
## The low-latency collectors\r
\r
**ZGC** and **Shenandoah** take a different approach: do essentially everything\r
concurrently, including moving objects, so pause times stop scaling with heap\r
size at all. Sub-millisecond pauses on a terabyte heap are the claim, and it\r
holds.\r
\r
Moving an object while the application is reading it requires the reader to be\r
intercepted. ZGC uses **coloured pointers** — spare bits in a 64-bit reference\r
carry marking and relocation state — plus a **load barrier** on every reference\r
read. If a load hits a reference to a moved object, the barrier forwards it and\r
fixes the reference in place. Shenandoah reaches the same end with a forwarding\r
pointer in each object header.\r
\r
| | Pause behaviour | Cost |\r
|---|---|---|\r
| **Serial** | Scales with heap | None; one thread; smallest footprint |\r
| **Parallel** | Scales with heap | Best raw throughput |\r
| **G1** | Targeted, tens of ms | Write barrier, remembered sets |\r
| **ZGC / Shenandoah** | Sub-millisecond, flat | Load/read barrier, ~10-15% throughput |\r
\r
Generational ZGC — the default ZGC mode since Java 23 — recovers much of that\r
throughput by applying the same weak generational hypothesis G1 relies on.\r
\r
**Choose deliberately.** A batch job that runs for four hours and nobody watches\r
should use Parallel: pauses are irrelevant and throughput is everything. A\r
request-serving API with a p99 latency target should use G1, or ZGC if the heap\r
is large. A 200 MB container running one small service is often best on Serial,\r
where the other collectors' bookkeeping costs more than it saves.\r
\r
## The three references that are not strong\r
\r
Reachability is not binary. Four strengths exist, and they behave differently\r
under memory pressure:\r
\r
\`\`\`java References.java\r
import java.lang.ref.PhantomReference;\r
import java.lang.ref.Reference;\r
import java.lang.ref.ReferenceQueue;\r
import java.lang.ref.SoftReference;\r
import java.lang.ref.WeakReference;\r
\r
public class References {\r
\r
    record Payload(byte[] bytes) {\r
        Payload(int mb) { this(new byte[mb * 1024 * 1024]); }\r
    }\r
\r
    public static void main(String[] args) throws Exception {\r
        ReferenceQueue<Payload> queue = new ReferenceQueue<>();\r
\r
        Payload weakTarget = new Payload(1);\r
        Payload softTarget = new Payload(1);\r
        Payload phantomTarget = new Payload(1);\r
\r
        WeakReference<Payload> weak = new WeakReference<>(weakTarget);\r
        SoftReference<Payload> soft = new SoftReference<>(softTarget);\r
        PhantomReference<Payload> phantom = new PhantomReference<>(phantomTarget, queue);\r
\r
        System.out.println("phantom.get() is always null : " + (phantom.get() == null));\r
\r
        weakTarget = null;\r
        softTarget = null;\r
        phantomTarget = null;\r
\r
        System.gc();\r
        Thread.sleep(150);\r
\r
        System.out.println("weak survived a collection    : " + (weak.get() != null));\r
        System.out.println("soft survived a collection    : " + (soft.get() != null));\r
\r
        Reference<? extends Payload> dead = queue.poll();\r
        System.out.println("phantom was enqueued          : " + (dead == phantom));\r
\r
        // Now make the JVM genuinely short of memory. Catching OutOfMemoryError\r
        // is only acceptable because this is a demonstration of exactly that.\r
        try {\r
            java.util.List<byte[]> hog = new java.util.ArrayList<>();\r
            while (true) hog.add(new byte[8 * 1024 * 1024]);\r
        } catch (OutOfMemoryError expected) {\r
            System.out.println("soft cleared under pressure   : " + (soft.get() == null));\r
        }\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-refs\r
javac References.java\r
java -Xmx128m References\r
\`\`\`\r
\r
\`\`\`output @run-refs\r
phantom.get() is always null : true\r
weak survived a collection    : false\r
soft survived a collection    : true\r
phantom was enqueued          : true\r
soft cleared under pressure   : true\r
\`\`\`\r
\r
That is the whole hierarchy in five lines:\r
\r
- **Strong** — an ordinary variable. Always prevents collection.\r
- **Weak** — cleared as soon as no strong reference remains. \`WeakHashMap\` is\r
  built on it. Use it to attach metadata to objects you do not own, so the entry\r
  disappears when the key does.\r
- **Soft** — cleared only when the JVM is short of memory. Intended for caches,\r
  though in practice a bounded cache with an explicit policy behaves far more\r
  predictably, because "short of memory" is decided by the collector and not by\r
  you.\r
- **Phantom** — never returns the object; it is enqueued *after* the object has\r
  been finalised and is unreachable. It exists for cleanup that must happen after\r
  collection, and \`Cleaner\` is the supported way to use it.\r
\r
\`finalize()\` is deprecated for removal and should never be used: it can run late,\r
on any thread, or never, it can resurrect the object, and its mere presence adds\r
a whole extra collection cycle before the object can be reclaimed. Deterministic\r
cleanup belongs in \`AutoCloseable\` and try-with-resources; \`Cleaner\` is the\r
GC-triggered fallback.\r
\r
## Diagnosing a real problem\r
\r
Reach for these in order. Do not start by changing flags.\r
\r
**1. Turn on logging, always.** In production, permanently:\r
\r
\`\`\`bash\r
java -Xlog:gc*:file=gc.log:time,uptime,level,tags:filecount=5,filesize=20m MyApp\r
\`\`\`\r
\r
Then read it with [GCEasy](https://gceasy.io/) or\r
[GCViewer](https://github.com/chewiebug/GCViewer) rather than by eye. What you\r
are looking for: is the *live* heap after each full collection trending upward?\r
That, not pause count, is the signature of a leak.\r
\r
**2. Get a heap dump.** Either on failure or on demand:\r
\r
\`\`\`bash\r
java -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=/dumps MyApp\r
\r
jcmd <pid> GC.heap_dump /dumps/live.hprof\r
jcmd <pid> GC.class_histogram\r
\`\`\`\r
\r
**3. Open it in [Eclipse MAT](https://eclipse.dev/mat/).** Go straight to the\r
**dominator tree** — it answers "if this object were collected, how much memory\r
would come back?", which is the question you actually have. *Leak Suspects*\r
produces a usable answer in under a minute for most real leaks.\r
\r
The distinction MAT teaches is **shallow versus retained** size. A \`HashMap\` with\r
a million entries has a small shallow size and an enormous retained size. Sorting\r
by retained size finds the cache nobody bounded.\r
\r
**4. Only now, tune.** And tune three things at most:\r
\r
| Flag | What it does |\r
|---|---|\r
| \`-Xmx\` | The ceiling. Set it explicitly for anything you deploy. |\r
| \`-XX:MaxGCPauseMillis\` | G1's pause target. It is a goal, not a guarantee. |\r
| \`-XX:+UseZGC\` etc. | Change collector, when the workload genuinely calls for it. |\r
\r
Everything else — survivor ratios, tenuring thresholds, region sizes — is\r
adaptive by default and is usually made worse by hand. If a flag fixes something,\r
you should be able to say which log line proved it.\r
\r
### In a container\r
\r
Modern JVMs are container-aware and read the cgroup limit rather than the host's\r
memory. But the default heap is a *fraction* of that limit, and the rest of the\r
process — metaspace, thread stacks, JIT code cache, direct buffers — lives\r
outside \`-Xmx\`. A container killed by the OOM killer while the heap looks healthy\r
is almost always this.\r
\r
\`\`\`bash\r
java -XX:MaxRAMPercentage=75 -XX:+ExitOnOutOfMemoryError -jar app.jar\r
\`\`\`\r
\r
\`MaxRAMPercentage\` scales with the limit instead of hardcoding a number, and\r
\`ExitOnOutOfMemoryError\` makes the container die honestly rather than limping\r
along with a poisoned heap.\r
\r
Use \`-XX:NativeMemoryTracking=summary\` plus \`jcmd <pid> VM.native_memory\` when\r
the heap is fine and the process still grows.\r
\r
## When it goes wrong\r
\r
- **\`Pause Full\` in a G1 log** — the concurrent cycle lost the race. Raise the\r
  heap or lower \`InitiatingHeapOccupancyPercent\`. Do not treat it as normal.\r
- **\`G1 Humongous Allocation\` as a pause cause** — objects larger than half a\r
  region. Allocate smaller buffers or raise \`G1HeapRegionSize\`.\r
- **\`OutOfMemoryError: GC overhead limit exceeded\`** — more than 98% of time in\r
  GC recovering under 2% of the heap. Effectively full; find the leak.\r
- **\`OutOfMemoryError: Metaspace\` with a near-empty heap** — unbounded dynamic\r
  class loading. Cap it with \`-XX:MaxMetaspaceSize\` so it fails early and\r
  visibly.\r
- **\`OutOfMemoryError: unable to create native thread\`** — nothing to do with the\r
  heap. Thread stacks are native memory; you have hit an OS or cgroup limit.\r
- **Pause times fine, latency terrible** — long time-to-safepoint. \`-Xlog:safepoint\`.\r
- **Container killed, heap healthy** — off-heap growth. Native memory tracking.\r
- **A performance change after a JDK upgrade** — the default collector or its\r
  ergonomics changed. \`-XX:+PrintFlagsFinal\` on both versions and diff them.\r
\r
## Further reading\r
\r
- [HotSpot GC Tuning Guide, JDK 21](https://docs.oracle.com/en/java/javase/21/gctuning/index.html)\r
- [JEP 333: ZGC](https://openjdk.org/jeps/333) and [JEP 439: Generational ZGC](https://openjdk.org/jeps/439)\r
- [Eclipse Memory Analyzer](https://eclipse.dev/mat/)\r
- [Unified JVM logging](https://docs.oracle.com/en/java/javase/21/docs/specs/man/java.html#enable-logging-with-the-jvm-unified-logging-framework)\r
`,or=`Fourteen questions on the collector. The Easy set is asked of everyone; the
Medium set separates people who have read about GC from people who have watched a
log; the Hard set is asked when the role involves keeping something running.

The **Notes** cover Easy and most of Medium. The **Advanced** page covers the
rest.

## Easy

### 1. What does the garbage collector do, and what do you give up for it?

It reclaims heap memory occupied by objects your program can no longer reach, so
you never free memory by hand. That removes the whole family of manual
memory-management bugs — leaks, dangling pointers, double frees. What you give up
is control over *when* memory is released, and you accept pauses while the
collector works.

### 2. How does the collector decide an object is garbage?

By **reachability**, not reference counting. Starting from **GC roots** — local
variables and parameters on thread stacks, static fields, live threads, JNI
references — it follows every reference it can. Whatever it reaches is live;
everything else is garbage. A useful consequence: two objects referring to each
other are still collected, because no root reaches either. Reference-counting
collectors need a separate cycle detector for exactly this case.

### 3. Does \`System.gc()\` force a collection?

No. It is a request. It can be disabled outright with \`-XX:+DisableExplicitGC\`,
and when it is honoured it usually triggers a **full** GC, the most expensive
kind. It is legitimate in a demo that must prove an object was collected. It does
not belong in application code.

### 4. Does setting a variable to \`null\` help the garbage collector?

Almost never. The JVM already knows a local variable is dead after its last use,
and the compiled code will not keep the reference alive. It only matters for a
**long-lived field** holding something large well past its usefulness — a cache
entry, a field on a session object. Nulling a local before returning from a
method achieves nothing.

### 5. Why should you not use \`finalize()\`?

There is no guarantee it runs promptly, on a particular thread, or at all. It can
resurrect the object. Its mere presence forces an extra collection cycle before
the object can be reclaimed. It has been deprecated for removal since Java 9.
Deterministic cleanup belongs in \`AutoCloseable\` with try-with-resources;
\`Cleaner\` is the supported GC-triggered fallback.

## Medium

### 6. Why is the heap generational?

Because of the **weak generational hypothesis**: most objects die very young, and
those that survive tend to keep surviving. Splitting the heap lets the JVM
collect the small young generation frequently and cheaply rather than scanning
everything each time. The key detail is that a collection copies out the
**survivors** rather than deleting the dead — so a young collection where almost
everything died costs almost nothing. Garbage is free to collect; only surviving
costs.

### 7. Walk me through the life of an object.

It is allocated in **Eden**. When Eden fills, a minor GC copies whatever is still
reachable into a **survivor** space and declares all of Eden empty in one step.
Each subsequent young collection moves survivors to the *other* survivor space
and increments their age. Once the age crosses the tenuring threshold —
15 by default — the object is **promoted** to the old generation, which is
collected far more rarely and far more expensively.

### 8. What is stop-the-world, and does every collector do it?

It is a phase where the JVM suspends all application threads so the collector can
work on a stable object graph. Every collector has stop-the-world phases; the
difference is how long they last and how much work is moved into concurrent
phases running alongside the application. ZGC and Shenandoah do essentially
everything concurrently, including moving objects, so their pauses stay
sub-millisecond regardless of heap size.

### 9. Can a Java program leak memory? What does one look like?

Yes — not by forgetting to free, but by holding a reference to something no
longer useful. The canonical shapes: a \`static\` collection used as a cache with
no eviction, a listener registered and never removed, a \`ThreadLocal\` in a pooled
thread that outlives the request, and a long-lived object retaining every
short-lived one it has seen. The collector correctly refuses to reclaim reachable
objects, the heap fills, and you get \`OutOfMemoryError: Java heap space\`. The bug
is in your object graph.

### 10. \`OutOfMemoryError\` has several messages. What do they mean?

**\`Java heap space\`** — the heap is genuinely full of reachable objects.
**\`GC overhead limit exceeded\`** — over 98% of time spent collecting, recovering
under 2%; the polite version of the same thing. **\`Metaspace\`** — class metadata
exhausted, which lives in native memory outside the heap, so \`-Xmx\` will not
help. **\`unable to create native thread\`** — an OS or cgroup thread limit;
nothing to do with the heap at all. **\`Direct buffer memory\`** — off-heap
\`ByteBuffer\`s, bounded by \`-XX:MaxDirectMemorySize\`.

### 11. What is the default collector, and why that one?

**G1**, since Java 9. It divides the heap into a couple of thousand equal regions
rather than fixed generational blocks, labels each Eden, survivor or old, and
collects the regions holding the most garbage first — hence Garbage-First. That
gives a reasonable balance of throughput and predictable pauses for typical
server work, and it lets you state a pause target with \`-XX:MaxGCPauseMillis\`
that the collector will try to meet by choosing how many regions to include.

## Hard

### 12. How can G1 collect one region without scanning the whole heap?

Each region keeps a **remembered set** of references pointing into it from
elsewhere. Keeping those accurate requires a **write barrier**: every reference
store in your program is followed by a few instructions that check whether the
write crosses regions and queue it if so. That is G1's hidden cost — a tax on
every reference write, plus the memory the remembered sets occupy. What it buys
is the ability to collect a chosen subset of regions, which is what makes a pause
target possible at all.

### 13. What is a humongous allocation, and why does it cause trouble?

An object larger than **half a G1 region** cannot be allocated normally, so it is
given a run of contiguous regions rounded up to a whole number. With 1 MB
regions, a 600 KB array occupies a full region. Allocate many and the heap
fragments into unusable gaps, producing humongous allocation failures and full
GCs while the heap looks half empty. It shows in the log as
\`G1 Humongous Allocation\` as the *cause* of a pause. Fix by allocating smaller
buffers or raising \`-XX:G1HeapRegionSize\`.

### 14. What is a safepoint, and why can pause time look fine while latency is not?

A safepoint is a point in compiled code where the JVM can stop a thread and
reliably walk its stack. A stop-the-world pause has two parts: getting every
thread *to* a safepoint, and then doing the work. GC logs report the second.
Threads only poll at method returns and loop back-edges, so a long counted loop
can hold everything else waiting — time-to-safepoint that naive monitoring never
sees. \`-Xlog:safepoint\` shows it. GC is not the only trigger: heap dumps, class
redefinition, biased-lock revocation and \`Thread.getAllStackTraces\` all need
safepoints, which is why safepoint-based profilers distort what they measure.

### 15. Weak, soft, phantom — what is each for?

A **weak** reference is cleared as soon as no strong reference remains;
\`WeakHashMap\` is built on it, and it is right for attaching metadata to objects
you do not own. A **soft** reference is cleared only when the JVM is short of
memory, which is intended for caches — though a bounded cache with an explicit
policy behaves far more predictably, because "short of memory" is the collector's
judgement, not yours. A **phantom** reference never returns the object; it is
enqueued after collection as a hook for cleanup, and \`Cleaner\` is the supported
way to use it.

### 16. A service pauses for 400 ms every few minutes. How do you find out why?

Do not change flags first. Turn on \`-Xlog:gc*\` to a rotating file and read it
with a tool rather than by eye. Ask whether the *live* heap after each full
collection is trending upward — that, not pause count, is the signature of a
leak. If it is, take a heap dump with \`-XX:+HeapDumpOnOutOfMemoryError\` or
\`jcmd GC.heap_dump\`, open it in Eclipse MAT, and go to the **dominator tree**,
which answers "how much would come back if this went away". Sort by **retained**
size, not shallow. Only once the log or the dump has named a cause should a flag
change — and you should be able to point at the line that proved it.

## What to revise next

Question 12 is the one that most often exposes a memorised answer, because it
requires you to explain a mechanism rather than name a thing. Question 16 is the
one that gets asked as "tell me about a time" — have a real log or dump you can
describe.

If a role mentions latency or scale, the **Advanced** page's sections on
safepoints and the low-latency collectors are where the follow-ups come from.
`,sr=`The chapter in a page. If a line here is news, the **Notes** or **Advanced**
part is where it comes from.

## The one rule

- **Reachability.** Can the object still be reached, by following references,
  from a GC root? If not, it is garbage.
- GC roots: locals and parameters of running methods, \`static\` fields, live
  threads, native references.
- Age, size and whether you are "finished with it" are all irrelevant.
- **Reference cycles are fine.** Two objects pointing at each other are still
  collected, because no root reaches either.

## The heap

- **Weak generational hypothesis**: most objects die very young; survivors keep
  surviving.
- **Eden** → **survivor spaces** → **old generation**, promoted after ~15
  collections.
- **Metaspace** is not the heap. Class metadata, native memory, its own
  \`OutOfMemoryError\`.
- A collection copies out the **survivors** and declares the rest empty in one
  step. Garbage is free to collect; only surviving costs.

## The three kinds of pause

| | Cleans | Frequency | Pause |
|---|---|---|---|
| Minor / young | Young generation | Very often | Under a ms to a few ms |
| Major / old | Old generation | Rarely | Noticeably longer |
| Full | Whole heap + metaspace | Rarest | The spike on your graph |

**Stop-the-world** means every application thread is suspended. Every collector
does it; they differ in how long and how much runs concurrently.

## The collectors

- **Serial** — one thread, small heaps, containers.
- **Parallel** — best throughput, longest pauses. Right for batch jobs.
- **G1** *(default since Java 9)* — heap split into 1024–2048 equal **regions**,
  collects the ones with most garbage first. \`-XX:MaxGCPauseMillis\` is a goal.
- **ZGC / Shenandoah** — sub-millisecond pauses at any heap size, for ~10–15%
  throughput.

## G1 specifics

- Roles are per-region and change over time. Eden is wherever the Eden regions
  are.
- **Humongous** = larger than half a region, given whole regions. Too many
  fragment the heap — look for \`G1 Humongous Allocation\` as a pause *cause*.
- **Remembered sets** track references pointing into each region, maintained by a
  **write barrier** on every reference store. That is G1's hidden cost.
- **SATB** marking means objects that die mid-cycle survive to the next one —
  floating garbage, traded for never pausing to mark.
- \`Pause Full\` in a G1 log is a **failure**, not routine.

## Safepoints

- A pause is: get every thread to a safepoint, *then* do the work. Logs report
  only the second half.
- Threads poll at method returns and loop back-edges, so a long counted loop
  holds everything else waiting.
- \`-Xlog:safepoint\`. GC is not the only trigger — heap dumps, class redefinition
  and \`Thread.getAllStackTraces\` all need one.

## Reference strengths

| | Cleared when |
|---|---|
| **Strong** | Never — it is what keeps things alive |
| **Weak** | No strong reference remains. \`WeakHashMap\` |
| **Soft** | The JVM is short of memory. Intended for caches |
| **Phantom** | Never returns the object; enqueued after collection. Use \`Cleaner\` |

\`finalize()\` is deprecated for removal: may run late, on any thread, or never,
and adds a whole extra collection cycle. Use \`AutoCloseable\`.

## Leaks and errors

- A **Java memory leak** is an object still *reachable* but no longer *useful*:
  an unbounded static cache, an unremoved listener, a \`ThreadLocal\` in a pool.
- \`Java heap space\` — genuinely full of reachable objects.
- \`GC overhead limit exceeded\` — >98% of time collecting, <2% recovered.
- \`Metaspace\` — class metadata, outside the heap. \`-Xmx\` will not help.
- \`unable to create native thread\` — an OS or cgroup limit. Not the heap at all.

## Diagnosing, in order

1. \`-Xlog:gc*\` to a rotating file, always, in production.
2. Ask whether the **live heap after each full GC** is trending upward. That, not
   pause count, is the leak signature.
3. Heap dump → **Eclipse MAT** → **dominator tree**. Sort by **retained** size,
   not shallow.
4. Only then change a flag, and only \`-Xmx\`, \`-XX:MaxGCPauseMillis\`, or the
   collector. Everything else is adaptive and usually made worse by hand.

## Things worth unlearning

- \`System.gc()\` does not force anything, and usually triggers a full GC.
- Setting a **local** to \`null\` achieves nothing. A long-lived **field** is
  different.
- \`-Xmx\` is a ceiling, not a reservation.
- The heap is not all the memory Java uses — metaspace, thread stacks, JIT code
  cache and direct buffers all sit outside it, which is how a container gets
  killed while the heap looks healthy.
`,cr=`Three initials, one letter apart, and every Java course throws them at you in the\r
first ten minutes without saying what they are for. Then you install "Java" from\r
a download page, type \`javac\`, and the terminal says it does not know that\r
command.\r
\r
So start here. This is the piece everything else in this section stands on: what\r
happens between the file you type and the program that runs, and which of those\r
three things you actually need on your machine.\r
\r
Nothing here assumes you know Java. If you have never written a line of it, this\r
is the right place to start.\r
\r
## The promise, and how it is kept\r
\r
Java's old slogan was **write once, run anywhere**. Most languages, before it,\r
compiled straight to machine code — instructions for one specific processor on\r
one specific operating system. Build on Windows, and the result would not run on\r
a Mac.\r
\r
Java splits that in two:\r
\r
\`\`\`text\r
   Hello.java   ──(javac, the compiler)──▶  Hello.class   (bytecode)\r
                                                 │\r
                                                 ▼\r
                                          ──(java, the JVM)──▶  runs\r
                                          on Windows, macOS, Linux, a phone,\r
                                          a Raspberry Pi, whatever has a JVM\r
\`\`\`\r
\r
\`javac\` does **not** produce machine code. It produces **bytecode** — an\r
instruction set for a computer that does not exist, a made-up processor that\r
Java's designers invented. Then a real program, the **JVM**, reads that bytecode\r
and executes it on whatever machine it happens to be running on.\r
\r
Every platform gets its own JVM. The bytecode never changes. That is the whole\r
trick: the portable part is the \`.class\` file, and the platform-specific part is\r
somebody else's problem.\r
\r
## The three names\r
\r
They are not three alternatives. They are three boxes, one inside the next:\r
\r
\`\`\`text\r
┌───────────────────────────────────────────────────────────┐\r
│  JDK — Java Development Kit                               │\r
│  Tools to WRITE code: javac, javap, jar, javadoc, jdb     │\r
│                                                           │\r
│   ┌─────────────────────────────────────────────────┐     │\r
│   │  JRE — Java Runtime Environment                 │     │\r
│   │  Everything needed to RUN a Java program         │     │\r
│   │  The standard library: String, List, HashMap...  │     │\r
│   │                                                 │     │\r
│   │    ┌───────────────────────────────────────┐    │     │\r
│   │    │  JVM — Java Virtual Machine           │    │     │\r
│   │    │  Executes bytecode, manages memory    │    │     │\r
│   │    └───────────────────────────────────────┘    │     │\r
│   └─────────────────────────────────────────────────┘     │\r
└───────────────────────────────────────────────────────────┘\r
\`\`\`\r
\r
| | Stands for | What is in it | You need it to |\r
|---|---|---|---|\r
| **JDK** | Java Development Kit | The JRE **plus** \`javac\`, \`javap\`, \`jar\`, the debugger | **write and compile** Java |\r
| **JRE** | Java Runtime Environment | The JVM **plus** the standard library | **run** Java that someone else compiled |\r
| **JVM** | Java Virtual Machine | The engine itself | **execute** bytecode |\r
\r
One sentence to keep: **the JDK is for people who write Java, the JRE is for\r
people who only run it, and the JVM is the engine inside both.**\r
\r
> Install the **JDK**. Always, as a learner. You need \`javac\`, and only the JDK\r
> has it. Since Java 11 there is no separate JRE download anyway — the JDK is the\r
> thing you get.\r
\r
## Which JDK, and from where\r
\r
"Java" as a download is a small mess of names. What you want:\r
\r
- **[Eclipse Temurin](https://adoptium.net/)** — free, no account, no licence\r
  question to think about. This is the sensible default.\r
- Oracle's own JDK is fine for personal use but has had licence changes people\r
  remember badly. Amazon Corretto and Microsoft's build are equally fine.\r
\r
Pick a **long-term support** version: **21** or **17**. LTS releases get years of\r
updates; the versions in between are fine to play with but are superseded every\r
six months.\r
\r
Check what you have:\r
\r
\`\`\`bash @check\r
java -version\r
javac -version\r
\`\`\`\r
\r
Both should print a version, and the two should agree. On the machine these\r
guides were written on:\r
\r
\`\`\`output @check\r
java version "26" 2026-03-17\r
Java(TM) SE Runtime Environment (build 26+35-2893)\r
Java HotSpot(TM) 64-Bit Server VM (build 26+35-2893, mixed mode, sharing)\r
javac 26\r
\`\`\`\r
\r
If \`java\` works but \`javac\` does not, you have a runtime and not a kit — install\r
the JDK. If neither works, the installer did not put Java on your \`PATH\`; on\r
Windows the easiest fix is to reinstall and tick that box, and a **new** terminal\r
is required afterwards because \`PATH\` changes only reach shells opened later.\r
\r
## Compile it, run it, look inside it\r
\r
The smallest program that proves the whole chain works:\r
\r
\`\`\`java Hello.java\r
public class Hello {\r
    public static void main(String[] args) {\r
        System.out.println("It compiled, and it ran.");\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-hello\r
javac Hello.java\r
java Hello\r
\`\`\`\r
\r
\`\`\`output @run-hello\r
It compiled, and it ran.\r
\`\`\`\r
\r
Two things trip people up here, both worth saying plainly:\r
\r
- **\`javac Hello.java\`** takes the file name, with the extension. It writes\r
  \`Hello.class\` next to it.\r
- **\`java Hello\`** takes the **class** name, with *no* extension. \`java\r
  Hello.class\` is a common first error and does not work.\r
\r
The file name must match the public class inside it. \`public class Hello\` has to\r
live in \`Hello.java\`, or the compiler refuses.\r
\r
Now look at what \`javac\` actually produced. \`javap\` ships with the JDK and prints\r
bytecode back as text:\r
\r
\`\`\`bash @javap\r
javap -c Hello\r
\`\`\`\r
\r
\`\`\`output @javap\r
Compiled from "Hello.java"\r
public class Hello {\r
  public Hello();\r
    Code:\r
         0: aload_0\r
         1: invokespecial #1                  // Method java/lang/Object."<init>":()V\r
         4: return\r
\r
  public static void main(java.lang.String[]);\r
    Code:\r
         0: getstatic     #7                  // Field java/lang/System.out:Ljava/io/PrintStream;\r
         3: ldc           #13                 // String It compiled, and it ran.\r
         5: invokevirtual #15                 // Method java/io/PrintStream.println:(Ljava/lang/String;)V\r
         8: return\r
}\r
\`\`\`\r
\r
That is the bytecode. \`getstatic\` fetches \`System.out\`, \`ldc\` loads the string\r
constant, \`invokevirtual\` calls \`println\`. It is not machine code and no\r
processor can run it — it is instructions for the imaginary machine the JVM\r
pretends to be.\r
\r
You will never write this. It is worth seeing once, because it makes "the JVM\r
executes bytecode" stop being a sentence you repeat and start being a thing you\r
have looked at.\r
\r
## What the JVM does while your program runs\r
\r
The JVM is not a simple interpreter. Four jobs run the whole time:\r
\r
**Class loading.** Classes are found and loaded *when first used*, not up front.\r
This is why a missing library gives you \`NoClassDefFoundError\` halfway through a\r
run rather than at startup.\r
\r
**Verification.** Before running anything, the JVM checks the bytecode is\r
structurally valid and cannot corrupt memory. A hand-edited \`.class\` file gets\r
rejected here.\r
\r
**Execution, and then compilation.** The JVM starts by *interpreting* bytecode,\r
one instruction at a time — quick to start, slow to run. While that happens it\r
counts how often each method is called. Methods that get hot are handed to the\r
**JIT** — the Just-In-Time compiler — which compiles them to real machine code\r
for the processor it is actually on.\r
\r
**Memory management.** Objects are allocated on the heap and reclaimed by the\r
**garbage collector** when nothing refers to them any more. You never free\r
memory by hand. That is [its own subject](#/study/deep-gc), and\r
there is a whole piece on it here.\r
\r
The JIT is the interesting one, because you can watch it happen.\r
\r
## Watching the JIT warm up\r
\r
Call the same method ten times and time each call. Nothing about the method\r
changes — but the machine underneath it does.\r
\r
\`\`\`java HowJavaRuns.java\r
public class HowJavaRuns {\r
\r
    /** Deliberately simple arithmetic, called a great many times. */\r
    static long work(int rounds) {\r
        long total = 0;\r
        for (int i = 1; i <= rounds; i++) {\r
            total += (i % 7) * (i % 13);\r
        }\r
        return total;\r
    }\r
\r
    public static void main(String[] args) {\r
        System.out.println("running on : " + System.getProperty("java.vm.name"));\r
        System.out.println("version    : " + System.getProperty("java.version"));\r
        System.out.println("vendor     : " + System.getProperty("java.vendor"));\r
        System.out.println();\r
        System.out.println("same method, ten times over:");\r
\r
        for (int round = 1; round <= 10; round++) {\r
            long start = System.nanoTime();\r
            work(3_000_000);\r
            long micros = (System.nanoTime() - start) / 1000;\r
            System.out.printf("  run %2d : %6d microseconds%n", round, micros);\r
        }\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-jit\r
javac HowJavaRuns.java\r
java HowJavaRuns\r
\`\`\`\r
\r
\`\`\`output @run-jit\r
running on : Java HotSpot(TM) 64-Bit Server VM\r
version    : 26\r
vendor     : Oracle Corporation\r
\r
same method, ten times over:\r
  run  1 :   5763 microseconds\r
  run  2 :   4744 microseconds\r
  run  3 :   3388 microseconds\r
  run  4 :   3362 microseconds\r
  run  5 :   3401 microseconds\r
  run  6 :   3820 microseconds\r
  run  7 :   3596 microseconds\r
  run  8 :   3555 microseconds\r
  run  9 :   3579 microseconds\r
  run 10 :   3517 microseconds\r
\`\`\`\r
\r
The first run is interpreted. By the third the JIT has compiled the method and it\r
is running roughly 40% faster, and it stays there. Your numbers will be\r
different — the shape will not be.\r
\r
This is also the single biggest reason naive Java benchmarks lie. Timing\r
something once, cold, measures the interpreter. Real benchmarks warm the code up\r
first and then measure, which is what [JMH](https://github.com/openjdk/jmh)\r
exists to do properly.\r
\r
> Try \`java -Xint HowJavaRuns\` to turn the JIT off entirely and force pure\r
> interpretation. The numbers get dramatically worse and stay flat — no warm-up,\r
> because there is nothing warming up. That contrast is the JIT's whole\r
> contribution, in one flag.\r
\r
## Packages, and the folder problem\r
\r
The moment you write more than one file, you will meet packages. A package is a\r
namespace, and it must match the folder layout:\r
\r
\`\`\`java\r
package tools.text;      // must live in tools/text/Formatter.java\r
\r
public class Formatter { ... }\r
\`\`\`\r
\r
Compile and run from the folder **above** the package root:\r
\r
\`\`\`bash\r
javac tools/text/Formatter.java\r
java tools.text.Formatter\r
\`\`\`\r
\r
The run command uses dots, not slashes, and no \`.class\`. Getting\r
\`ClassNotFoundException\` here almost always means you ran it from the wrong\r
directory.\r
\r
## When it goes wrong\r
\r
- **\`'javac' is not recognized\` / \`command not found\`** — you have a runtime, not\r
  a JDK, or \`PATH\` was not updated. Install the JDK and open a **new** terminal.\r
- **\`error: class Hello is public, should be declared in a file named Hello.java\`** —\r
  the file name and the public class name must match exactly, capitals included.\r
- **\`Could not find or load main class Hello\`** — you probably typed\r
  \`java Hello.class\`, or you are in the wrong directory, or the class declares a\r
  \`package\` and you did not run it by its full dotted name.\r
- **\`UnsupportedClassVersionError: ... has been compiled by a more recent version\`** —\r
  the \`.class\` was built by a newer JDK than the \`java\` running it. Either\r
  upgrade the runtime or compile with \`javac --release 17 Hello.java\`.\r
- **Two Javas installed and the wrong one wins** — on Windows, \`where java\` shows\r
  which one \`PATH\` finds first; \`JAVA_HOME\` is what build tools read, and the two\r
  disagreeing is a classic afternoon lost.\r
- **\`Exception in thread "main" java.lang.NoClassDefFoundError\`** — the class\r
  compiled but is not on the classpath at run time. Check \`-cp\`.\r
\r
## What to read next\r
\r
The two things the JVM does that shape how you write code both have their own\r
pieces here:\r
\r
- **[Stack, heap and the String Pool](#/study/deep-memory)** — where\r
  your variables actually live, and why \`==\` lies about Strings.\r
- **[How the garbage collector works](#/study/deep-gc)** — what\r
  happens to objects when you stop using them.\r
\r
## Further reading\r
\r
- [Eclipse Temurin downloads](https://adoptium.net/)\r
- [The Java Virtual Machine Specification](https://docs.oracle.com/javase/specs/jvms/se21/html/index.html)\r
- [\`javap\` documentation](https://docs.oracle.com/en/java/javase/21/docs/specs/man/javap.html)\r
`,lr=`The notes stop at "the JVM loads your class and runs it". That sentence hides\r
four mechanisms that decide how your program starts, how fast it gets, and what\r
happens when a dependency is missing. This is the layer underneath.\r
\r
You do not need any of this to write Java. You need most of it to debug Java that\r
is behaving strangely, and interviewers past the junior level ask about all of\r
it.\r
\r
## Class loading, and the delegation model\r
\r
Classes are not loaded at startup. They are loaded **the first time they are\r
actively used** — the first \`new\`, the first static field read, the first static\r
method call. That laziness is why a missing library often surfaces twenty minutes\r
into a run rather than at launch.\r
\r
Loading is done by class loaders arranged in a chain:\r
\r
\`\`\`text\r
        ┌──────────────────────────────┐\r
        │  Bootstrap  (built into JVM) │   java.lang, java.util — the JDK itself\r
        └──────────────┬───────────────┘\r
                       │ parent\r
        ┌──────────────▼───────────────┐\r
        │  Platform (was: Extension)   │   the rest of the JDK modules\r
        └──────────────┬───────────────┘\r
                       │ parent\r
        ┌──────────────▼───────────────┐\r
        │  Application / System        │   your classes, your jars, the classpath\r
        └──────────────┬───────────────┘\r
                       │ parent\r
        ┌──────────────▼───────────────┐\r
        │  Your own loader, if any     │   app servers, plugin systems, agents\r
        └──────────────────────────────┘\r
\`\`\`\r
\r
The rule is **parent-first delegation**: before a loader looks for a class\r
itself, it asks its parent. So \`java.lang.String\` always resolves to the real one\r
in the JDK, no matter what a jar on your classpath calls itself. That is a\r
security property, not a convenience — without it, a jar could ship its own\r
\`java.lang.String\` and every program that loaded it would be compromised.\r
\r
Two consequences worth knowing:\r
\r
- **A class's identity is \`(name, class loader)\`, not just the name.** Two loaders\r
  can load the same bytes and produce two incompatible types. This is why an\r
  application server can run two versions of the same library, and also why you\r
  occasionally get the deeply confusing \`ClassCastException: com.acme.Thing\r
  cannot be cast to com.acme.Thing\`.\r
- **Web containers deliberately break parent-first.** Servlet containers use\r
  child-first for the web application's own classes so a war can override a\r
  library the container also ships. That inversion is the source of most\r
  "works standalone, fails in Tomcat" reports.\r
\r
### The three phases\r
\r
Loading a class runs **load → link → initialise**:\r
\r
1. **Load** — find the bytes, produce a \`Class\` object.\r
2. **Link** — *verify* the bytecode is structurally safe, *prepare* static fields\r
   with default values (\`0\`, \`null\`, \`false\`), and *resolve* symbolic references,\r
   which the JVM is allowed to do lazily.\r
3. **Initialise** — run static initialisers and static field assignments, in\r
   source order, exactly once, thread-safely.\r
\r
That last guarantee is doing real work. The JVM holds a lock per class during\r
initialisation, which is why the **static holder idiom** is a correct lazy\r
singleton with no synchronisation of your own:\r
\r
\`\`\`java\r
public class Config {\r
    private Config() {}\r
\r
    private static class Holder {\r
        static final Config INSTANCE = new Config();\r
    }\r
\r
    public static Config get() {\r
        return Holder.INSTANCE;   // Holder loads on first call, once, safely\r
    }\r
}\r
\`\`\`\r
\r
\`Holder\` is not initialised until \`Holder.INSTANCE\` is first read. The class\r
loader provides the thread safety. No \`volatile\`, no double-checked locking, no\r
way to get it wrong.\r
\r
## Inside a \`.class\` file\r
\r
A class file is a well-defined binary format, and it is smaller than people\r
expect:\r
\r
\`\`\`text\r
magic          0xCAFEBABE          always, since 1995\r
minor/major    e.g. 65 = Java 21   what UnsupportedClassVersionError compares\r
constant pool  every string, class name, method signature, literal\r
access flags   public, final, abstract, interface...\r
this / super   indexes into the constant pool\r
interfaces     indexes\r
fields         name, descriptor, flags, attributes\r
methods        name, descriptor, flags, Code attribute (the bytecode)\r
attributes     SourceFile, LineNumberTable, Signature, annotations...\r
\`\`\`\r
\r
The **constant pool** is the interesting part. Every method call in your code is\r
stored as a *symbolic* reference — a string naming the class, the method and its\r
descriptor — not an address. Resolution to something concrete happens at run\r
time, on first use. That indirection is what makes it possible to recompile one\r
class and not the ones that call it.\r
\r
Look at any of it:\r
\r
\`\`\`bash @javap-v\r
javap -v -p Hello\r
\`\`\`\r
\r
\`\`\`output @javap-v\r
Classfile /C:/scratch/Hello.class\r
  Last modified 26 Jul 2026; size 428 bytes\r
  SHA-256 checksum 853d1229ba6ac2f0accdd8901bcc6448d4aac8c241cb1690a8954fd2bff5a4fc\r
  Compiled from "Hello.java"\r
public class Hello\r
  minor version: 0\r
  major version: 70\r
  flags: (0x0021) ACC_PUBLIC, ACC_SUPER\r
  this_class: #21                         // Hello\r
  super_class: #2                         // java/lang/Object\r
  interfaces: 0, fields: 0, methods: 2, attributes: 1\r
Constant pool:\r
   #1 = Methodref          #2.#3          // java/lang/Object."<init>":()V\r
   #2 = Class              #4             // java/lang/Object\r
   #3 = NameAndType        #5:#6          // "<init>":()V\r
   #4 = Utf8               java/lang/Object\r
   #5 = Utf8               <init>\r
   #6 = Utf8               ()V\r
   #7 = Fieldref           #8.#9          // java/lang/System.out:Ljava/io/PrintStream;\r
   #8 = Class              #10            // java/lang/System\r
   #9 = NameAndType        #11:#12        // out:Ljava/io/PrintStream;\r
  #10 = Utf8               java/lang/System\r
  #11 = Utf8               out\r
  #12 = Utf8               Ljava/io/PrintStream;\r
  #13 = String             #14            // It compiled, and it ran.\r
  #14 = Utf8               It compiled, and it ran.\r
  #15 = Methodref          #16.#17        // java/io/PrintStream.println:(Ljava/lang/String;)V\r
\`\`\`\r
\r
Major version 70 is Java 26; 65 is Java 21, 61 is 17, 52 is Java 8. When you get\r
\`UnsupportedClassVersionError\`, the number in the message is this field, and\r
subtracting 44 gives you the Java version.\r
\r
### Method descriptors\r
\r
The odd strings in \`javap\` output are **descriptors**, and reading them is a\r
five-minute skill worth having:\r
\r
| Descriptor | Type |\r
|---|---|\r
| \`I\` \`J\` \`D\` \`F\` \`Z\` \`B\` \`C\` \`S\` | int, long, double, float, boolean, byte, char, short |\r
| \`V\` | void |\r
| \`Ljava/lang/String;\` | a reference type |\r
| \`[I\` | \`int[]\` |\r
| \`[[Ljava/lang/String;\` | \`String[][]\` |\r
\r
So \`(Ljava/lang/String;I)V\` is \`void something(String, int)\`. Descriptors are why\r
**erasure** is visible: \`List<String>\` and \`List<Integer>\` both compile to\r
\`Ljava/util/List;\`, which is exactly why you cannot overload on them.\r
\r
### The five invoke instructions\r
\r
| Instruction | Used for |\r
|---|---|\r
| \`invokestatic\` | static methods |\r
| \`invokespecial\` | constructors, \`private\` methods, \`super.x()\` |\r
| \`invokevirtual\` | ordinary instance methods — dispatched on the runtime type |\r
| \`invokeinterface\` | calls through an interface reference |\r
| \`invokedynamic\` | lambdas, method references, string concatenation, records |\r
\r
\`invokedynamic\` is the one worth knowing about. A lambda does **not** compile to\r
an anonymous inner class. It compiles to an \`invokedynamic\` whose first execution\r
calls a *bootstrap method* — \`LambdaMetafactory\` — which spins the implementing\r
class at run time and caches it. That is why lambdas do not produce a\r
\`MyClass$1.class\` file on disk, and why the first lambda in a process is\r
measurably slower than the rest.\r
\r
Since Java 9, string concatenation uses \`invokedynamic\` too, via\r
\`StringConcatFactory\`, rather than the \`StringBuilder\` chain older compilers\r
emitted.\r
\r
## The JIT, in detail\r
\r
The notes showed the method getting faster. Here is what is happening.\r
\r
HotSpot uses **tiered compilation** — two compilers and five levels:\r
\r
\`\`\`text\r
level 0   interpreter                      counting invocations and loop backedges\r
level 1   C1, no profiling                 trivial methods, compile once, done\r
level 2   C1, limited profiling\r
level 3   C1, full profiling               the usual warm path\r
level 4   C2, fully optimising             the hot path\r
\`\`\`\r
\r
A method starts interpreted. Cross a threshold and C1 compiles it quickly, with\r
instrumentation. Stay hot and C2 recompiles it using everything the\r
instrumentation learned. C2 is slow to compile and produces excellent code, which\r
is exactly the trade you want for the 1% of methods that run 99% of the time.\r
\r
What C2 does with the profile is the part that has no ahead-of-time equivalent:\r
\r
- **Inlining.** The single most valuable optimisation, because it enables all the\r
  others. Small hot methods disappear into their callers.\r
- **Monomorphic dispatch.** If a call site has only ever seen one receiver type,\r
  C2 compiles a direct call plus a type guard, rather than a virtual dispatch.\r
  This is why \`interface\`-heavy Java is not slow in practice — and why a call\r
  site that sees three or more types (*megamorphic*) genuinely is slower.\r
- **Escape analysis.** If an object provably never leaves the method, it need not\r
  be heap-allocated at all; its fields become registers. See the\r
  [memory notes](#/study/deep-memory/advanced) for what that does to allocation costs.\r
- **Loop unrolling, dead code elimination, constant folding**, and range-check\r
  elimination on array accesses it can prove are in bounds.\r
\r
### Deoptimisation\r
\r
Every one of those is **speculative**. C2 compiles code that is correct *given\r
what has happened so far*. When the assumption breaks — a second implementation\r
of that interface finally shows up, a branch never taken is taken — the JVM\r
**deoptimises**: it discards the compiled code, falls back to the interpreter\r
mid-method, and recompiles later with the wider profile.\r
\r
That is why a long-running service can get *slower* after a deployment that\r
introduced a second implementation of a hot interface, and why benchmark numbers\r
from a five-second run are worthless.\r
\r
Watch it happen:\r
\r
\`\`\`bash\r
java -XX:+PrintCompilation HowJavaRuns\r
java -XX:+UnlockDiagnosticVMOptions -XX:+PrintInlining HowJavaRuns\r
\`\`\`\r
\r
\`PrintCompilation\` marks deoptimisation with \`made not entrant\`. Compare against\r
\`-Xint\` (interpret only) and \`-XX:TieredStopAtLevel=1\` (C1 only) to see the\r
contribution of each tier.\r
\r
> Never benchmark by timing a loop. Use [JMH](https://github.com/openjdk/jmh),\r
> which handles warm-up, dead-code elimination and constant folding — all three\r
> of which will otherwise make your microbenchmark measure nothing at all.\r
\r
## Classpath, jars and the module path\r
\r
A **jar** is a zip with a \`META-INF/MANIFEST.MF\`. The classpath is an ordered\r
list of places to look; **first match wins**, which is what "jar hell" means —\r
two jars containing the same class, and the winner decided by ordering you did\r
not choose.\r
\r
\`\`\`bash\r
javac -d out $(find src -name '*.java')\r
jar --create --file app.jar --main-class Hello -C out .\r
java -jar app.jar\r
java -cp "app.jar;lib/*" Hello        # Windows uses ; — Unix uses :\r
\`\`\`\r
\r
Note that \`-jar\` **ignores \`-cp\` entirely**. The manifest's \`Class-Path\` is the\r
only classpath a \`-jar\` launch respects, which surprises people regularly.\r
\r
### Modules, briefly\r
\r
Java 9 added a second, stricter mechanism. A module declares what it needs and\r
what it exposes:\r
\r
\`\`\`java module-info.java\r
module com.acme.app {\r
    requires java.net.http;\r
    requires transitive com.acme.model;\r
\r
    exports com.acme.app.api;\r
    opens com.acme.app.entity;      // reflection allowed, e.g. for a framework\r
}\r
\`\`\`\r
\r
On the **module path**, anything not \`exports\`ed is genuinely inaccessible — not\r
merely discouraged. That is where \`InaccessibleObjectException\` comes from when a\r
framework reflects into a package that was never opened, and why \`--add-opens\`\r
exists as an escape hatch.\r
\r
Most applications still run on the classpath, where everything lands in the\r
"unnamed module" and the old rules apply. The JDK itself is fully modularised\r
regardless, which is what makes \`jlink\` possible:\r
\r
\`\`\`bash\r
jlink --add-modules java.base,java.net.http --output runtime --strip-debug\r
\`\`\`\r
\r
That produces a runtime containing only the modules you named — tens of\r
megabytes instead of hundreds, which is why it matters for containers.\r
\r
## Startup, and the two ways to shorten it\r
\r
Class loading and JIT warm-up are both startup costs. Two mechanisms attack them:\r
\r
**Class Data Sharing (CDS)** memory-maps a pre-parsed archive of class metadata,\r
shared between processes. The default archive covers the JDK's own classes; you\r
can add your application's:\r
\r
\`\`\`bash\r
java -XX:ArchiveClassesAtExit=app.jsa -jar app.jar\r
java -XX:SharedArchiveFile=app.jsa -jar app.jar\r
\`\`\`\r
\r
**Ahead-of-time compilation** sidesteps the JVM entirely — GraalVM Native Image\r
produces a native binary that starts in milliseconds. The cost is a closed world:\r
reflection, dynamic proxies and runtime class loading must all be declared up\r
front, which is why frameworks need explicit native-image support.\r
\r
## When it goes wrong\r
\r
- **\`ClassCastException: X cannot be cast to X\`** — two class loaders, two\r
  identities. Look for a duplicated jar on both the container's and the\r
  application's classpath.\r
- **\`NoSuchMethodError\`** — the class was found, but not the method signature the\r
  caller was compiled against. Classic version skew: compiled against v2, running\r
  with v1 on the classpath.\r
- **\`NoClassDefFoundError\` versus \`ClassNotFoundException\`** — the second means\r
  nobody could find the class. The first often means the class *was* found but\r
  its **static initialiser threw**, so the class is permanently marked\r
  unusable — the real exception is in the first occurrence, further up the log.\r
- **\`UnsupportedClassVersionError\`** — compiled by a newer JDK than the one\r
  running it. Compile with \`--release\`, and note that \`-source\`/\`-target\` without\r
  \`--release\` will happily let you link against APIs that do not exist in the\r
  older runtime.\r
- **\`InaccessibleObjectException\`** — a framework reflected into a package that\r
  was not \`opens\`ed. \`--add-opens java.base/java.lang=ALL-UNNAMED\` is the\r
  sledgehammer.\r
- **\`-jar\` ignoring your \`-cp\`** — as above. Use \`-cp app.jar:libs/*\` with an\r
  explicit main class instead.\r
- **A performance cliff after a deploy** — a new implementation of a hot\r
  interface turned a monomorphic call site megamorphic. \`-XX:+PrintInlining\`\r
  shows it.\r
\r
## Further reading\r
\r
- [The Java Virtual Machine Specification, SE 21](https://docs.oracle.com/javase/specs/jvms/se21/html/index.html)\r
- [JEP 341: Default CDS archives](https://openjdk.org/jeps/341)\r
- [JMH — the only correct way to microbenchmark Java](https://github.com/openjdk/jmh)\r
- [The Java Module System](https://openjdk.org/projects/jigsaw/spec/)\r
`,ur=`Fourteen questions on how Java runs, in the order an interview tends to walk
them. Answer each one out loud before reading on — the gap between "I know this"
and "I can say this in two clean sentences" is exactly what the interview
measures.

The **Notes** cover everything in the Easy set. The **Advanced** page covers
everything in Medium and Hard.

## Easy

### 1. What is the difference between the JDK, the JRE and the JVM?

They are nested, not alternatives. The **JVM** is the engine that executes
bytecode and manages memory. The **JRE** is the JVM plus the standard library —
everything needed to *run* a Java program. The **JDK** is the JRE plus
development tools such as \`javac\` and \`javap\`, so it is what you need to *write*
Java. Install the JDK; it contains the other two. Since Java 11 there is no
separate JRE download at all.

### 2. What is bytecode, and why does it make Java portable?

\`javac\` does not produce machine code. It produces **bytecode** — instructions
for a specified virtual machine, stored in \`.class\` files. Every platform ships
its own JVM that understands the same bytecode, so one compiled artefact runs
anywhere without recompiling. The portable part is the \`.class\` file; the
platform-specific part is the JVM, which is somebody else's problem.

### 3. Why is \`main\` declared \`public static void main(String[] args)\`?

**\`static\`** because the JVM must call it before any object exists, so there is
no instance to call it on. **\`public\`** so the launcher can reach it from outside
the class. **\`void\`** because the exit status comes from \`System.exit\`, not from
a return value. **\`String[]\`** carries the command-line arguments. Since Java 25,
instance \`main\` methods in compact source files relax most of this for small
programs, but the classic form is what you will be asked about.

### 4. What is the difference between \`javac Hello.java\` and \`java Hello\`?

\`javac\` takes a **file name**, with the \`.java\` extension, and writes
\`Hello.class\`. \`java\` takes a **class name** — no extension, and dots rather than
slashes if the class is in a package. \`java Hello.class\` is the standard first
mistake and does not work.

### 5. What does \`PATH\` have to do with \`JAVA_HOME\`?

\`PATH\` decides which \`java\` and \`javac\` your shell finds. \`JAVA_HOME\` is a
convention read by build tools — Maven, Gradle, application servers — to locate a
JDK. They are set independently, so they can disagree, and a machine where
\`java -version\` says one thing and Gradle says another is almost always this.

## Medium

### 6. What does the JIT compiler actually do?

The JVM starts by **interpreting** bytecode, which is fast to start and slow to
run, while counting invocations and loop back-edges. Methods that cross a
threshold are compiled to native machine code — first by **C1**, quickly and with
profiling instrumentation, then by **C2** for the genuinely hot ones, using
everything that profiling learned. Because it compiles with runtime knowledge, it
can do things an ahead-of-time compiler cannot: inline aggressively, turn a call
site that has only ever seen one type into a direct call, and eliminate bounds
checks it can prove are unnecessary.

### 7. What is deoptimisation, and why does it exist?

Most JIT optimisations are **speculative** — correct given the behaviour observed
so far. When an assumption breaks, such as a second implementation of a hot
interface finally appearing, the JVM discards the compiled code, resumes in the
interpreter mid-method, and recompiles later with the wider profile. It exists
because speculation is what makes the optimisations possible; without a way to
back out, the JIT could only apply what it could prove statically.

### 8. Why is timing a method in a loop a bad benchmark?

Because you are usually measuring the interpreter, the JIT compiling, or nothing
at all. A cold first run measures interpretation. A hot loop whose result is
unused gets deleted by dead-code elimination. A constant input gets folded away
entirely. Real benchmarks warm the code up, consume the result, and randomise
inputs — which is what JMH does, and why hand-rolled timing loops routinely
report impossible numbers.

### 9. Explain the parent-first delegation model, and why it matters.

Before a class loader searches for a class, it asks its parent, all the way up to
the bootstrap loader. So \`java.lang.String\` always resolves to the JDK's version
regardless of what is on the classpath. It is a **security** property: without
it, a malicious jar could supply its own \`java.lang.String\`. Servlet containers
deliberately invert it for web application classes so a war can override a
library the container also ships, which is the source of most "works standalone,
fails in Tomcat" bugs.

### 10. When is a class actually loaded and initialised?

**Loaded** lazily, on first active use. **Initialised** — static initialisers and
static field assignments run — on first \`new\`, first static method call, or first
non-constant static field access. Reading a \`static final\` compile-time constant
does *not* trigger initialisation, because the compiler inlined the value. The
JVM guarantees initialisation happens exactly once and is thread-safe, which is
what makes the static holder idiom a correct lazy singleton with no
synchronisation of your own.

### 11. What is the difference between \`ClassNotFoundException\` and \`NoClassDefFoundError\`?

\`ClassNotFoundException\` is checked and comes from an explicit lookup —
\`Class.forName\`, or a loader that genuinely could not find the bytes.
\`NoClassDefFoundError\` is an \`Error\` thrown by the JVM: the class was present at
compile time but is missing or unusable now. The trap is that it is also thrown
when a class's **static initialiser threw** — the class is marked permanently
unusable, and the real exception is the *first* occurrence, further up the log.

## Hard

### 12. How does a lambda compile, and why is it not an anonymous inner class?

It compiles to an **\`invokedynamic\`** instruction. On first execution the JVM
calls a bootstrap method — \`LambdaMetafactory\` — which generates the implementing
class at run time and caches the call site for subsequent calls. That is why
lambdas produce no \`MyClass$1.class\` on disk, why the first lambda in a process
is measurably slower than the rest, and why the strategy can change between JDK
versions without recompiling anything. Since Java 9, string concatenation uses
the same mechanism via \`StringConcatFactory\`.

### 13. Why does a class's identity include its class loader?

Because the runtime type is the pair \`(fully-qualified name, defining loader)\`.
Two loaders can load identical bytes and produce two types that are not
assignment-compatible. This is what lets an application server run two versions
of the same library in isolation — and what produces the notorious
\`ClassCastException: com.acme.Thing cannot be cast to com.acme.Thing\`, which
means exactly one thing: the same jar is visible through two loaders.

### 14. What does the module system change, and what breaks because of it?

On the **module path**, a package that is not \`exports\`ed is genuinely
inaccessible rather than merely discouraged, and reflection into a package
requires \`opens\`. That gives real encapsulation of internals and makes \`jlink\`
possible — a runtime image containing only the modules you declared. What breaks
is any framework that reflects into JDK internals: hence
\`InaccessibleObjectException\` and the \`--add-opens\` escape hatch. Applications on
the classpath land in the unnamed module and mostly keep the old behaviour, which
is why most code never noticed.

## What to revise next

If questions 6 to 8 were shaky, the JIT section of the **Advanced** page is
where they come from. If 9 to 11 were, read the class loading section — those
three are the same mechanism asked three ways, and interviewers know it.
`,dr=`The chapter in a page. If a line here is news, the **Notes** or **Advanced**
part is where it comes from.

## The three names

- **JDK ⊃ JRE ⊃ JVM.** JDK to write, JRE to run, JVM to execute. Install the JDK.
- \`javac\` takes a **file** name; \`java\` takes a **class** name, dots not slashes,
  no extension.
- Bytecode is not machine code. It is instructions for a specified virtual
  machine, which is why one \`.class\` file runs everywhere.

## What the JVM does

- **Load** lazily, on first active use → **link** (verify, prepare, resolve) →
  **initialise** (static blocks, once, thread-safely).
- That per-class initialisation lock is why the **static holder idiom** is a
  correct lazy singleton with no synchronisation of your own.
- **Verify** rejects bytecode that could corrupt memory.
- **Execute** — interpret first, then JIT-compile what gets hot.
- **Collect** — reclaim unreachable objects.

## Class loading

- Chain: bootstrap → platform → application → yours. **Parent-first**: ask the
  parent before looking yourself.
- That is a security property. Without it a jar could ship its own
  \`java.lang.String\`.
- A class's identity is **\`(name, class loader)\`**. Two loaders, two
  incompatible types — which is what
  \`ClassCastException: com.acme.Thing cannot be cast to com.acme.Thing\` means.
- Servlet containers deliberately invert it to child-first, which is why things
  work standalone and fail in Tomcat.

## Inside a \`.class\`

- \`0xCAFEBABE\`, then the version. **Major 65 = Java 21**, 61 = 17, 52 = 8;
  subtract 44 from the number in \`UnsupportedClassVersionError\`.
- The **constant pool** holds every name symbolically, resolved at run time. That
  indirection is why you can recompile one class and not its callers.
- Descriptors: \`I\` int, \`J\` long, \`Z\` boolean, \`V\` void, \`Ljava/lang/String;\` a
  reference, \`[I\` an int array. So \`(Ljava/lang/String;I)V\` is
  \`void f(String, int)\`.
- Five invokes: \`static\`, \`special\` (constructors, private, \`super\`), \`virtual\`,
  \`interface\`, **\`dynamic\`** — which is how lambdas and string concatenation
  work, and why lambdas produce no extra \`.class\` file.

## The JIT

- **Tiered**: interpreter → C1 with profiling → C2 fully optimising. Slow to
  compile, excellent output, applied only to the small hot fraction.
- **Inlining** is the enabler for everything else.
- A call site that has seen one type compiles to a direct call; one that has seen
  three or more is **megamorphic** and genuinely slower.
- Optimisations are **speculative**, so the JVM can **deoptimise** — throw the
  compiled code away and fall back — when an assumption breaks.
- Which is why timing a loop is not a benchmark. Use JMH.

## Classpath and modules

- Classpath is ordered; **first match wins**. That is jar hell.
- \`-jar\` **ignores \`-cp\`**. Only the manifest's \`Class-Path\` applies.
- On the module path, a package that is not \`exports\`ed is genuinely
  inaccessible, and reflection needs \`opens\` — hence
  \`InaccessibleObjectException\` and \`--add-opens\`.
- \`jlink\` builds a runtime with only the modules you named.

## The errors, decoded

| Message | What it means |
|---|---|
| \`ClassNotFoundException\` | Nobody could find the class |
| \`NoClassDefFoundError\` | Found at compile time, missing now — **or its static initialiser threw** |
| \`NoSuchMethodError\` | Class found, signature not. Version skew |
| \`UnsupportedClassVersionError\` | Compiled by a newer JDK than is running it |
| \`ClassCastException: X to X\` | Same class, two loaders |
`,fr=`Two questions come up in almost every Java interview, and they are the same\r
question wearing different hats. *Is Java pass by value or pass by reference?*\r
And *why does \`==\` sometimes work on Strings and sometimes not?*\r
\r
Both answers fall out of one picture: where your variables actually live while\r
the program runs. Get that picture right and a whole category of confusing\r
behaviour stops being confusing.\r
\r
This follows on from [JDK, JRE and the JVM](#/study/deep-jvm), but it\r
stands on its own. You need to be able to compile and run a file; that is all.\r
\r
## Two places, two rules\r
\r
When the JVM runs your program it splits memory into areas. Two of them matter\r
now.\r
\r
\`\`\`text\r
        STACK (one per thread)              HEAP (shared by everything)\r
   ┌────────────────────────────┐      ┌──────────────────────────────────┐\r
   │ main() frame               │      │                                  │\r
   │   int a  = 10              │      │   int[] { 99, 2, 3 }  ◀──────────┼──┐\r
   │   int b  = 20              │      │                                  │  │\r
   │   x  ──────────────────────┼─────▶│   String "Java"   (in the pool)  │  │\r
   │   y  ──────────────────────┼──────┼──────────────────────────────────┼──┘\r
   └────────────────────────────┘      └──────────────────────────────────┘\r
     values and references                 the objects themselves\r
\`\`\`\r
\r
**The stack** holds local variables, method parameters, and *references* to\r
objects. It is organised as frames — one per method call, pushed when the method\r
is entered and thrown away when it returns. That is why a local variable\r
disappears when the method ends, and why runaway recursion gives you\r
\`StackOverflowError\`. It is fast, it is per-thread, and it is small.\r
\r
**The heap** holds every object: everything made with \`new\`, every array, every\r
String. It is shared across the whole program, several references can point at\r
one object, and it is cleaned up by the garbage collector rather than by you.\r
\r
The rule that follows:\r
\r
> A **primitive** (\`int\`, \`double\`, \`boolean\`, \`char\`…) stores its value\r
> directly. An **object** variable stores a reference — the address of something\r
> on the heap.\r
\r
Everything below is a consequence of that one line.\r
\r
## Copying a primitive, copying a reference\r
\r
\`\`\`java\r
int a = 10;\r
int b = a;      // copies the VALUE\r
b = 20;         // a is still 10\r
\r
int[] x = { 1, 2, 3 };\r
int[] y = x;    // copies the REFERENCE\r
y[0] = 99;      // x[0] is now 99 as well\r
\`\`\`\r
\r
Nothing was cloned in the second case. \`x\` and \`y\` are two names for one array.\r
This is not a special rule about arrays — it is true of every object. Assigning\r
an object never copies it.\r
\r
## Pass by value, always\r
\r
Here is the sentence that settles the interview question:\r
\r
> **Java is always pass by value.** When you pass an object, the value being\r
> copied is the reference.\r
\r
Which means a method can change what an object *contains*, but can never change\r
which object the caller's variable points at:\r
\r
\`\`\`java\r
static void tryToChange(int number, int[] array, String text) {\r
    number = 999;      // changes the local copy — caller unaffected\r
    array[0] = 999;    // changes the shared object — caller sees it\r
    text = "changed";  // rebinds the local copy — caller unaffected\r
}\r
\`\`\`\r
\r
\`array[0] = 999\` reaches through the reference and edits the object. \`array =\r
new int[]{999}\` would not: it would point the *local* copy of the reference at a\r
new array and leave the caller's alone.\r
\r
## The String Pool\r
\r
Strings get a memory area of their own, and it exists for one reason: programs\r
use the same short strings over and over, and storing a thousand copies of\r
\`"active"\` would be a waste.\r
\r
So the JVM keeps a **String Pool**. When the compiler sees a literal like\r
\`"Java"\`, it checks the pool. If that text is already there, it reuses the\r
existing object rather than making a new one.\r
\r
\`\`\`text\r
        THE POOL                       ELSEWHERE ON THE HEAP\r
   ┌────────────────────┐         ┌────────────────────────────┐\r
   │  "Java"  ◀───┬─── s1        │   "Java"  ◀─── s3           │\r
   │              └─── s2        │   (forced by new String())  │\r
   │  "hello"                    │                             │\r
   └────────────────────┘         └────────────────────────────┘\r
\r
   s1 == s2  →  true   (one pooled object, two names)\r
   s1 == s3  →  false  (different objects, same characters)\r
\`\`\`\r
\r
\`new String("Java")\` deliberately opts out: it *always* creates a fresh object on\r
the heap, outside the pool. Which is exactly why you should not write it.\r
\`intern()\` goes the other way — it returns the pooled instance for any String.\r
\r
## \`==\` versus \`.equals()\`\r
\r
- **\`==\`** asks *are these the same object?* It compares references.\r
- **\`.equals()\`** asks *do these say the same thing?* For String it compares\r
  characters.\r
\r
You want \`.equals()\` essentially always. \`==\` on Strings appears to work,\r
because the pool makes literals identical — and then quietly fails the day the\r
text arrives from a file, a database, or \`Scanner\`, because that String was built\r
at run time and was never pooled.\r
\r
This is the most common real bug in beginner Java, and it is not a syntax error.\r
It compiles, it runs, and it returns the wrong answer.\r
\r
## See all of it at once\r
\r
\`\`\`java Memory.java\r
public class Memory {\r
\r
    static void tryToChange(int number, int[] array, String text) {\r
        number = 999;      // changes the local copy only\r
        array[0] = 999;    // changes the object both names point at\r
        text = "changed";  // rebinds the local copy only\r
    }\r
\r
    public static void main(String[] args) {\r
\r
        int a = 10;\r
        int b = a;\r
        b = 20;\r
        System.out.println("a = " + a + ", b = " + b);\r
\r
        int[] x = { 1, 2, 3 };\r
        int[] y = x;\r
        y[0] = 99;\r
        System.out.println("x[0] = " + x[0] + ", y[0] = " + y[0]);\r
\r
        int number = 1;\r
        int[] array = { 1 };\r
        String text = "original";\r
        tryToChange(number, array, text);\r
        System.out.println("number = " + number + ", array[0] = " + array[0] + ", text = " + text);\r
\r
        System.out.println();\r
\r
        String s1 = "Java";\r
        String s2 = "Java";\r
        String s3 = new String("Java");\r
        String s4 = s3.intern();\r
\r
        System.out.println("s1 == s2          : " + (s1 == s2));\r
        System.out.println("s1 == s3          : " + (s1 == s3));\r
        System.out.println("s1.equals(s3)     : " + s1.equals(s3));\r
        System.out.println("s1 == s4          : " + (s1 == s4));\r
\r
        // Compile-time constants are folded and pooled; runtime work is not.\r
        String part = "Ja";\r
        String built = part + "va";\r
        final String constPart = "Ja";\r
        String folded = constPart + "va";\r
\r
        System.out.println("s1 == built       : " + (s1 == built));\r
        System.out.println("s1 == folded      : " + (s1 == folded));\r
        System.out.println("s1 == \\"Ja\\" + \\"va\\" : " + (s1 == "Ja" + "va"));\r
\r
        System.out.println();\r
\r
        String greeting = "hello";\r
        String shouted = greeting.toUpperCase();\r
        System.out.println("greeting = " + greeting + ", shouted = " + shouted);\r
\r
        StringBuilder sb = new StringBuilder("hello");\r
        sb.append(" world").insert(0, ">> ");\r
        System.out.println("builder  = " + sb);\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-memory\r
javac Memory.java\r
java Memory\r
\`\`\`\r
\r
\`\`\`output @run-memory\r
a = 10, b = 20\r
x[0] = 99, y[0] = 99\r
number = 1, array[0] = 999, text = original\r
\r
s1 == s2          : true\r
s1 == s3          : false\r
s1.equals(s3)     : true\r
s1 == s4          : true\r
s1 == built       : false\r
s1 == folded      : true\r
s1 == "Ja" + "va" : true\r
\r
greeting = hello, shouted = HELLO\r
builder  = >> hello world\r
\`\`\`\r
\r
Every line is one of the rules above, made visible. Three deserve a note.\r
\r
**\`s1 == built\` is false.** \`part + "va"\` is concatenation performed at *run*\r
time. The result is a brand-new object that never entered the pool, even though\r
the characters are identical. This is the trap in a nutshell.\r
\r
**\`s1 == folded\` is true.** \`constPart\` is \`final\` and initialised from a\r
literal, so the compiler can work the answer out while compiling and fold it into\r
a pooled literal. Same for \`"Ja" + "va"\`, which is two literals and no variables\r
at all. Whether \`==\` succeeds depends on what the *compiler* could figure out —\r
which is a fine reason never to rely on it.\r
\r
**\`text\` is still \`"original"\`.** The method reassigned its own copy of the\r
reference. The caller never saw it.\r
\r
## Immutability, and why it is a feature\r
\r
A String cannot be changed after it is created. \`toUpperCase()\` does not shout at\r
the original; it returns a new String and leaves the old one alone. So does\r
\`trim()\`, \`replace()\`, \`substring()\` and the rest.\r
\r
That is what makes the pool safe in the first place — if \`s1\` could be edited in\r
place, and \`s2\` shared the same object, editing one would silently change the\r
other. Immutability also makes Strings safe to use as \`HashMap\` keys and safe to\r
share between threads without locking.\r
\r
The cost shows up in loops:\r
\r
\`\`\`java\r
// O(n²): every += builds a whole new String\r
String result = "";\r
for (String word : words) result += word;\r
\r
// O(n): one buffer, appended to\r
StringBuilder sb = new StringBuilder();\r
for (String word : words) sb.append(word);\r
String result = sb.toString();\r
\`\`\`\r
\r
For a handful of items nobody cares. For ten thousand, the first version is the\r
difference between instant and noticeably slow. A single \`a + b + c\` outside a\r
loop is fine — the compiler turns it into a builder for you.\r
\r
## Where this leads\r
\r
The heap is also where the garbage collector does its work — once nothing on the\r
stack refers to an object any more, it becomes eligible for collection. That is\r
the subject of the next piece:\r
**[how the garbage collector works](#/study/deep-gc)**.\r
\r
## When it goes wrong\r
\r
- **Two Strings look identical but \`==\` is false** — one of them was built at run\r
  time. Use \`.equals()\`. This is not a bug to work around with \`intern()\`.\r
- **\`NullPointerException\` comparing Strings** — \`input.equals("yes")\` explodes\r
  when \`input\` is null. Write \`"yes".equals(input)\`, or use\r
  \`Objects.equals(a, b)\`.\r
- **\`StackOverflowError\`** — recursion with no base case, or one that is never\r
  reached. The heap is not involved; \`-Xss\` resizes the stack but rarely fixes\r
  the actual problem.\r
- **\`equals()\` works but the object vanishes from a \`HashSet\`** — you overrode\r
  \`equals\` and not \`hashCode\`. The two must always be overridden together.\r
- **String comparison ignoring case fails on some inputs** —\r
  \`equalsIgnoreCase\` handles the common cases; locale-sensitive text (Turkish\r
  dotted \`i\` is the classic) needs \`toLowerCase(Locale.ROOT)\` first.\r
- **A huge String is not being collected** — before Java 7u6, \`substring()\`\r
  shared the parent's character array, so a small substring pinned a large\r
  String. Modern versions copy, so this is history, but it still turns up in\r
  interview questions.\r
\r
## Further reading\r
\r
- [\`String\` API documentation](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/String.html)\r
- [JLS §3.10.5: String literals and interning](https://docs.oracle.com/javase/specs/jls/se21/html/jls-3.html#jls-3.10.5)\r
- [\`StringBuilder\` versus \`StringBuffer\`](https://docs.oracle.com/en/java/javase/21/docs/api/java.base/java/lang/StringBuilder.html)\r
`,pr=`The notes gave you the two-box picture: primitives on the stack, objects on the\r
heap, Strings pooled. Every one of those is a simplification, and the places\r
where it leaks are exactly where the confusing bugs live.\r
\r
This is the layer underneath: what an object actually costs, why allocation is\r
nearly free, why \`==\` on boxed integers works up to 127 and then stops, and what\r
a String really looks like in memory.\r
\r
## What an object costs\r
\r
An object is not just its fields. On a 64-bit HotSpot JVM:\r
\r
\`\`\`text\r
┌──────────────────────────────────────────────────────────┐\r
│ mark word           8 bytes   hash, GC age, lock state   │\r
│ class pointer       4 bytes   compressed (8 if heap>32GB)│\r
│ [array length]      4 bytes   arrays only                │\r
│ fields              ...       reordered by the JVM       │\r
│ padding             ...       up to an 8-byte boundary   │\r
└──────────────────────────────────────────────────────────┘\r
\`\`\`\r
\r
So \`new Object()\` is **16 bytes**, not zero. An \`Integer\` holding one \`int\` is\r
16 bytes plus a 4-byte reference to reach it — five times the cost of the \`int\`\r
it wraps. A \`Long\` is 24. This is the entire argument for primitive arrays over\r
\`List<Integer>\` in anything numeric, and the reason Project Valhalla exists.\r
\r
The **mark word** is doing several jobs at once: it stores the identity hash code\r
once one has been requested, the GC age used for promotion, and the object's lock\r
state. That overloading is why calling \`hashCode()\` on an object can interfere\r
with biased locking, and why \`System.identityHashCode\` is not free the first\r
time.\r
\r
**Compressed oops** are why a 4-byte class pointer works: below a 32 GB heap the\r
JVM stores references as 32-bit offsets scaled by 8. Cross 32 GB and every\r
reference doubles in width, so a 33 GB heap can hold *less* live data than a 31 GB\r
one. If you are sizing a heap near that line, stay under it.\r
\r
## Why allocation is nearly free\r
\r
Allocating on a shared heap should require a lock. It does not, because each\r
thread gets a **Thread-Local Allocation Buffer** — a private slab of Eden. A\r
\`new\` inside it is a pointer bump and a bounds check: a handful of instructions,\r
no synchronisation.\r
\r
\`\`\`text\r
   Eden\r
   ├── TLAB (thread 1)   [used ████░░░░░]  ← bump this pointer\r
   ├── TLAB (thread 2)   [used ██░░░░░░░]\r
   └── TLAB (thread 3)   [used ███████░░]\r
\`\`\`\r
\r
A thread that fills its TLAB gets another. Only that hand-out is synchronised.\r
The practical consequence: **short-lived objects are cheap**, and the "don't\r
allocate in a loop" advice inherited from C++ is mostly wrong in Java. What is\r
expensive is objects that *survive* — those get copied by the collector, possibly\r
several times, and eventually promoted.\r
\r
\`-XX:+PrintTLAB\` shows the sizes if you are curious.\r
\r
### Escape analysis, and objects that are never allocated\r
\r
C2 goes further. If it can prove an object never escapes the method that created\r
it, it does not allocate it at all — a transformation called **scalar\r
replacement**, where the object's fields become local variables in registers.\r
\r
\`\`\`java\r
// The Point never escapes. C2 may compile this to plain arithmetic\r
// on two ints, with no allocation and nothing for the GC to collect.\r
static double distance(int x1, int y1, int x2, int y2) {\r
    Point a = new Point(x1, y1);\r
    Point b = new Point(x2, y2);\r
    return Math.hypot(a.x() - b.x(), a.y() - b.y());\r
}\r
\`\`\`\r
\r
Escape analysis only runs on **inlined** code, so it depends on the whole call\r
chain being small enough to inline. It is not something to rely on — but it is\r
why "wrapping values in a small object" is often free in practice, and why\r
measuring beats assuming.\r
\r
\`\`\`bash\r
java -XX:+UnlockDiagnosticVMOptions -XX:+PrintEscapeAnalysis MyApp\r
java -XX:-DoEscapeAnalysis MyApp        # turn it off and compare\r
\`\`\`\r
\r
## The boxing cache\r
\r
\`Integer.valueOf\` does not always create an object. Values from **-128 to 127**\r
come from a cache built at class initialisation, so identical small values are\r
the same object — and \`==\` appears to work.\r
\r
\`\`\`java Boxing.java\r
public class Boxing {\r
    public static void main(String[] args) {\r
\r
        Integer a = 127, b = 127;\r
        Integer c = 128, d = 128;\r
\r
        System.out.println("127 == 127 : " + (a == b));\r
        System.out.println("128 == 128 : " + (c == d));\r
        System.out.println("128 equals : " + c.equals(d));\r
\r
        Integer present = 42;\r
        int primitive = 42;\r
        System.out.println("mixed  ==  : " + (present == primitive));\r
\r
        System.out.println("Long  100  : " + (Long.valueOf(100) == Long.valueOf(100)));\r
        System.out.println("Long  1000 : " + (Long.valueOf(1000) == Long.valueOf(1000)));\r
\r
        String s = "a fairly long string to hash";\r
        long first = time(s);\r
        long second = time(s);\r
        System.out.println("hash cached: " + (second <= first));\r
\r
        System.out.println("latin1 len : " + "hello".length());\r
        System.out.println("utf16 len  : " + "hello 世界".length());\r
    }\r
\r
    static long time(String s) {\r
        long start = System.nanoTime();\r
        for (int i = 0; i < 1000; i++) s.hashCode();\r
        return System.nanoTime() - start;\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-boxing\r
javac Boxing.java\r
java Boxing\r
\`\`\`\r
\r
\`\`\`output @run-boxing\r
127 == 127 : true\r
128 == 128 : false\r
128 equals : true\r
mixed  ==  : true\r
Long  100  : true\r
Long  1000 : false\r
hash cached: true\r
latin1 len : 5\r
utf16 len  : 8\r
\`\`\`\r
\r
Three things in there are worth pulling out.\r
\r
**Lines one and two are the same code with a different number.** A comparison\r
that passes every test and fails in production the day an id exceeds 127 is a\r
genuinely nasty bug, and it is entirely created by \`==\` on boxed types. Use\r
\`.equals()\`, or compare \`int\` values.\r
\r
**\`mixed ==\` is true** because comparing an \`Integer\` with an \`int\` **unboxes**\r
the object rather than boxing the primitive. Convenient — until the \`Integer\` is\r
\`null\`, at which point the same line throws \`NullPointerException\` with no \`null\`\r
visible anywhere in the source. This is the single most common surprise in\r
Java's autoboxing.\r
\r
**The upper bound is configurable.** \`-XX:AutoBoxCacheMax=1000\` raises it, which\r
means code that "works" because of the cache can behave differently on a\r
differently-configured JVM. Another reason not to depend on it.\r
\r
## What a String really is\r
\r
Since Java 9, \`String\` does not hold a \`char[]\`. It holds a \`byte[]\` plus a\r
one-byte coder flag:\r
\r
\`\`\`java\r
private final byte[] value;\r
private final byte coder;      // 0 = LATIN1, 1 = UTF16\r
private int hash;              // cached, computed lazily\r
\`\`\`\r
\r
That is **compact strings**, and for typical Latin-1 text it halves the memory a\r
String occupies. The output above shows the flip side: \`"hello 世界"\` reports\r
length 8 because \`length()\` counts UTF-16 code units, not characters. Anything\r
outside the Basic Multilingual Plane — emoji, most notably — occupies **two** code\r
units, so \`"👍".length()\` is 2 and \`charAt(0)\` returns half a character. Use\r
\`codePointCount\` and \`codePoints()\` when correctness matters.\r
\r
\`hash\` being a non-final cached field is a deliberate benign data race: two\r
threads computing it concurrently produce the same value, so no synchronisation\r
is needed. It also means the first \`hashCode()\` on a long String is O(n) and\r
every subsequent one is O(1) — visible in the demo above.\r
\r
### The pool is a native hash table\r
\r
The String Pool is not a normal Java collection. It is the **StringTable**, a\r
fixed-bucket hash table in native memory holding references to String objects on\r
the heap. Two flags matter if you use \`intern()\` heavily:\r
\r
\`\`\`bash\r
java -XX:+PrintStringTableStatistics MyApp\r
java -XX:StringTableSize=1000003 MyApp     # a prime, default is 65536\r
\`\`\`\r
\r
An overloaded StringTable degrades to linked-list scans in each bucket, and\r
\`intern()\` — which is a native call — gets progressively slower. Before Java 7 the\r
pool lived in PermGen and interning too much would exhaust it outright; it moved\r
to the heap in 7, which is why \`intern()\` stopped being actively dangerous and\r
became merely a bad default.\r
\r
**String deduplication** is the modern alternative and it needs no code change.\r
G1 can spot Strings with identical contents during collection and point them at\r
one shared \`byte[]\`:\r
\r
\`\`\`bash\r
java -XX:+UseStringDeduplication -Xlog:stringdedup MyApp\r
\`\`\`\r
\r
Applications that read a lot of repeated text — parsers, log processors — often\r
find several percent of the heap here.\r
\r
## Concatenation, and what the compiler emits\r
\r
\`a + b\` on Strings has not compiled to a \`StringBuilder\` chain since Java 9. It\r
compiles to an **\`invokedynamic\`** that calls \`StringConcatFactory\`, which\r
generates a specialised concatenation method at run time — usually one that sizes\r
the byte array exactly once and fills it, beating what a \`StringBuilder\` chain\r
would do.\r
\r
Which means:\r
\r
\`\`\`java\r
// Fine. One invokedynamic, one allocation.\r
String message = "user " + id + " logged in at " + time;\r
\r
// Not fine. n invokedynamics, n intermediate Strings, O(n²) copying.\r
String all = "";\r
for (String line : lines) all += line;\r
\r
// Correct. One buffer, grown geometrically.\r
StringBuilder sb = new StringBuilder();\r
for (String line : lines) sb.append(line);\r
\`\`\`\r
\r
The rule is not "never use \`+\`". It is "never use \`+\` **in a loop**", and the\r
reason is that the loop makes the intermediate results survive.\r
\r
\`StringBuilder\` itself starts at 16 characters and doubles, copying on each\r
growth. If you know roughly how big the result is, \`new StringBuilder(4096)\`\r
removes those copies. \`StringBuffer\` is the synchronised version and is almost\r
never the right answer — a buffer shared between threads has bigger design\r
problems than its own locking.\r
\r
## Immutability, defensive copies and \`final\`\r
\r
\`final\` on a field prevents **reassignment**, not mutation:\r
\r
\`\`\`java\r
final List<String> names = new ArrayList<>();\r
names.add("ada");           // perfectly legal\r
names = new ArrayList<>();  // compile error\r
\`\`\`\r
\r
Real immutability needs four things: final fields, immutable field *types*, no\r
setters, and defensive copies of anything mutable that crosses the boundary in\r
either direction.\r
\r
\`\`\`java\r
public final class Order {\r
    private final List<Item> items;\r
\r
    public Order(List<Item> items) {\r
        this.items = List.copyOf(items);          // copy in\r
    }\r
\r
    public List<Item> items() {\r
        return items;                             // already unmodifiable\r
    }\r
}\r
\`\`\`\r
\r
A \`record\` gets you final fields and no setters for free, but **not** deep\r
immutability — \`record Order(List<Item> items)\` still hands out a reference to a\r
mutable list. If that matters, add a compact constructor that copies:\r
\r
\`\`\`java\r
record Order(List<Item> items) {\r
    Order {\r
        items = List.copyOf(items);\r
    }\r
}\r
\`\`\`\r
\r
There is one more guarantee worth knowing: **final fields are safely published**.\r
The JVM's memory model promises that a thread which sees a reference to a\r
correctly-constructed object also sees its final fields fully initialised, with\r
no synchronisation. Non-final fields carry no such promise — which is precisely\r
why the broken double-checked locking singleton is broken.\r
\r
## When it goes wrong\r
\r
- **\`NullPointerException\` on a line with no \`null\` in it** — autoboxing. A\r
  \`Integer\` compared to an \`int\`, or assigned to an \`int\`, unboxes and throws.\r
  Very common with \`Map.get\` returning \`null\` for a missing key.\r
- **A comparison that works in tests and fails in production** — \`==\` on boxed\r
  values, and the production id went past 127.\r
- **\`OutOfMemoryError\` from a \`HashMap\` that should be small** — keys that\r
  override \`equals\` but not \`hashCode\`, so every put lands in its own entry and\r
  nothing is ever found or replaced.\r
- **\`String.length()\` disagreeing with what the user typed** — emoji and other\r
  supplementary characters take two UTF-16 code units. Use \`codePointCount\`.\r
- **\`substring\` retaining a huge parent String** — true before Java 7u6, where\r
  substrings shared the parent's array. Fixed since, but still asked about.\r
- **Heap dominated by \`char[]\`/\`byte[]\`** — duplicated Strings. Turn on\r
  \`-XX:+UseStringDeduplication\` before reaching for \`intern()\`.\r
- **A 33 GB heap performing worse than 31 GB** — compressed oops switched off.\r
  Every reference is now 8 bytes.\r
\r
## Further reading\r
\r
- [JEP 254: Compact Strings](https://openjdk.org/jeps/254)\r
- [JEP 280: Indified String Concatenation](https://openjdk.org/jeps/280)\r
- [JOL — Java Object Layout, for measuring any of this](https://openjdk.org/projects/code-tools/jol/)\r
- [\`Integer.valueOf\` and the cache, JLS §5.1.7](https://docs.oracle.com/javase/specs/jls/se21/html/jls-5.html#jls-5.1.7)\r
`,mr=`Fourteen questions on where values live and what they cost. This topic produces
more interview questions per page than any other in Java, because almost every
confusing behaviour in the language traces back to one of them.

The **Notes** cover the Easy set. The **Advanced** page covers Medium and Hard.

## Easy

### 1. What is the difference between the stack and the heap?

The **stack** holds local variables, method parameters and references, organised
as one frame per method call and discarded when the method returns. It is
per-thread, fast, and small — overflow it and you get \`StackOverflowError\`. The
**heap** holds every object created with \`new\`, is shared across the whole
program, and is reclaimed by the garbage collector. Primitives are stored by
value; objects are stored on the heap and reached through references.

### 2. What is the difference between a primitive and a reference type?

A primitive stores its value directly. A reference type stores the address of an
object on the heap. Copying a primitive copies the value, so the copies are
independent. Copying a reference copies the address, so both names refer to one
object — which is why \`int[] y = x; y[0] = 99;\` changes \`x[0]\` too.

### 3. Is Java pass by value or pass by reference?

**Always pass by value.** When you pass an object, the value copied is the
reference. So a method can change what an object *contains* — \`list.add(...)\`,
\`array[0] = 9\` — and the caller sees it. But reassigning the parameter itself
only rebinds the method's own copy, and the caller sees nothing.

### 4. What is the String Pool, and why does it exist?

A region where the JVM stores String literals so identical text is stored once.
When the compiler sees \`"Java"\` it reuses the pooled object if it already exists.
It exists because programs use the same short strings constantly and storing a
thousand copies of \`"active"\` would be waste. It is why two identical literals
are \`==\`, and why \`new String("Java")\` — which deliberately allocates outside the
pool — is not.

### 5. What is the difference between \`==\` and \`.equals()\`?

\`==\` compares references: *is this the same object?* \`.equals()\` compares content
as the class defines it. For Strings, two variables can hold identical text in
different objects, so \`==\` returns false while \`.equals()\` returns true. Use
\`.equals()\` for value comparison; \`==\` is correct for primitives and for
deliberate identity checks.

### 6. Why are Strings immutable?

Because immutability is what makes the pool safe — if one variable could edit a
shared String in place, every other variable pointing at it would change too. It
also makes Strings safe as \`HashMap\` keys, because the hash cannot go stale, and
safe to share across threads without locking, and safe to pass to a method that
might keep a reference.

## Medium

### 7. Why does \`Integer a = 127, b = 127; a == b\` return true, but 128 return false?

\`Integer.valueOf\` returns cached instances for **-128 to 127**, so small
identical values really are the same object and \`==\` succeeds. Above the cache,
each boxing allocates, so \`==\` compares two different objects and fails. The
upper bound is even tunable with \`-XX:AutoBoxCacheMax\`. This is why \`==\` on boxed
types is a bug that passes every test and fails the day an id exceeds 127.

### 8. How can a line with no \`null\` in it throw \`NullPointerException\`?

Autoboxing. Comparing an \`Integer\` to an \`int\`, or assigning one to an \`int\`,
**unboxes** the object by calling \`intValue()\` — and that call throws if the
reference is null. \`if (map.get(key) == someInt)\` is the classic: a missing key
returns \`null\`, the unboxing throws, and nothing in the source looks like a
dereference.

### 9. Why is \`"Ja" + "va" == "Java"\` true but \`part + "va" == "Java"\` false?

Constant folding. Two literals, or a \`final\` variable initialised from a literal,
can be evaluated by the **compiler** and folded into a single pooled literal, so
\`==\` succeeds. Concatenation involving a non-constant variable happens at **run**
time and produces a fresh object that never entered the pool, so \`==\` fails. That
the answer depends on what the compiler could work out is a fine reason never to
rely on it.

### 10. What is the \`equals\` / \`hashCode\` contract, and what breaks if you ignore it?

If two objects are \`equals\`, they must return the same \`hashCode\`. Unequal
objects *may* collide. Override \`equals\` without \`hashCode\` and hash-based
collections stop working: you \`put\` an object into a \`HashMap\`, look it up with
an equal object, land in a different bucket and get \`null\` — the entry is not
lost, it is unreachable. The same object also appears twice in a \`HashSet\`.
Records generate both correctly.

### 11. When would you use \`StringBuilder\` over \`+\`?

**In a loop.** Outside a loop, \`a + b + c\` compiles to a single \`invokedynamic\`
that sizes the result once — since Java 9 it often beats a hand-written
\`StringBuilder\`. Inside a loop, \`result += x\` allocates a new String every
iteration and copies everything so far, turning O(n) work into O(n²).
\`StringBuilder\` keeps one buffer and grows it geometrically.

### 12. Does \`final\` make an object immutable?

No. \`final\` prevents **reassignment** of the variable, not mutation of what it
points at. \`final List<String> names = new ArrayList<>()\` still allows
\`names.add(...)\`. Real immutability needs final fields, immutable field types, no
setters, and defensive copies in both directions. \`final\` does buy one real
guarantee: **safe publication** — a thread that sees a reference to a properly
constructed object also sees its final fields fully initialised, with no
synchronisation.

## Hard

### 13. What does an object actually cost in memory?

On 64-bit HotSpot: a 8-byte **mark word** (identity hash, GC age, lock state), a
4-byte **class pointer** with compressed oops, an extra 4-byte length for arrays,
then fields, then padding to an 8-byte boundary. So \`new Object()\` is 16 bytes,
and an \`Integer\` wrapping a 4-byte \`int\` costs 16 plus a 4-byte reference to
reach it. Compressed oops stop working above a 32 GB heap, at which point every
reference doubles — so a 33 GB heap can hold *less* live data than a 31 GB one.

### 14. If allocation goes to a shared heap, why does it not need a lock?

Because each thread gets a **Thread-Local Allocation Buffer**, a private slab of
Eden. A \`new\` inside it is a pointer bump and a bounds check — no
synchronisation. Only handing out a fresh TLAB is synchronised. On top of that,
C2's **escape analysis** can prove an object never leaves its method and apply
**scalar replacement**, turning its fields into registers so nothing is allocated
at all. Together they are why "avoid allocation" advice inherited from C++ is
mostly wrong in Java: what costs is objects that *survive*, not objects that are
created.

## What to revise next

Questions 7 and 8 are the same mechanism — the boxing cache and implicit
unboxing — and interviewers ask them back to back. Question 13 tends to be
followed by "so how would you store a million integers efficiently?", and the
answer is \`int[]\`, for exactly the reasons in that answer.

Where objects go when nothing refers to them any more is the next topic:
**[garbage collection](#/study/deep-gc)**.
`,hr=`The chapter in a page. If a line here is news, the **Notes** or **Advanced**
part is where it comes from.

## Where things live

- **Stack** — locals, parameters, references. One frame per call, discarded on
  return. Per-thread, fast, small.
- **Heap** — every object made with \`new\`. Shared, collected automatically.
- A **primitive** holds its value. A **reference** holds the address of something
  on the heap.
- Copying a primitive copies the value. Copying a reference copies the address,
  so both names point at one object.

## Pass by value, always

- When you pass an object, the value copied is the **reference**.
- So a method can change what the object *contains* and the caller sees it.
- Reassigning the parameter itself rebinds only the method's copy. The caller
  sees nothing.

## The String Pool

- Literals are stored once and shared, so \`"a" == "a"\` is true.
- \`new String("a")\` deliberately allocates outside the pool, so \`==\` fails.
- \`intern()\` returns the pooled instance for any String.
- \`"Ja" + "va" == "Java"\` is **true** — two literals, folded by the compiler.
  \`part + "va" == "Java"\` is **false** — built at run time, never pooled.
- Use \`.equals()\`. Every time.

## Strings, underneath

- Since Java 9 a String holds a \`byte[]\` plus a coder flag — **compact strings**,
  one byte per character for Latin-1.
- \`length()\` counts **UTF-16 code units**, not characters. Emoji count as 2, so
  \`"👍".length()\` is 2.
- The hash is cached in a non-final field after the first \`hashCode()\`. A benign
  data race — both threads compute the same number.
- The pool is the native **StringTable**. \`-XX:+UseStringDeduplication\` is the
  modern answer to duplicate text, not \`intern()\`.

## Concatenation

- \`a + b + c\` outside a loop is fine — one \`invokedynamic\`, one allocation.
- \`result += x\` **inside a loop** is O(n²). Use \`StringBuilder\`.
- \`StringBuilder\` starts at 16 chars and doubles. Pre-size it if you know
  roughly how big the result is.
- \`StringBuffer\` is the synchronised version and almost never the right answer.

## What an object costs

- 8-byte **mark word** (identity hash, GC age, lock state) + 4-byte class
  pointer + fields + padding to 8.
- \`new Object()\` = **16 bytes**. An \`Integer\` wrapping a 4-byte \`int\` = 16, plus
  a 4-byte reference to reach it.
- **Compressed oops** stop above a 32 GB heap, so a 33 GB heap holds *less* live
  data than a 31 GB one.

## Why allocation is cheap

- Each thread gets a **TLAB** — a private slab of Eden. \`new\` is a pointer bump,
  no synchronisation.
- **Escape analysis** plus **scalar replacement** can mean an object is never
  allocated at all.
- So the C++ instinct to avoid allocation is mostly wrong here. What costs is
  objects that **survive**.

## The boxing cache

- \`Integer.valueOf\` caches **-128 to 127**. So \`==\` works on small values and
  fails above 127 — a bug that passes every test.
- Comparing an \`Integer\` to an \`int\` **unboxes**, which throws
  \`NullPointerException\` when the reference is null. No \`null\` visible in the
  source.
- Same cache on \`Long\`, \`Short\`, \`Byte\` and \`Character\`.

## \`final\` and immutability

- \`final\` stops **reassignment**, not mutation. \`final List\` still accepts \`add\`.
- Real immutability: final fields, immutable field types, no setters, defensive
  copies both ways.
- A \`record\` gives you final fields, not deep immutability — copy in the compact
  constructor if it holds a mutable collection.
- \`final\` fields are **safely published**: see the reference, see the fields
  fully built, without synchronisation.
`,gr=`Most Java written today is Java 8 with a newer compiler. That is not a criticism —\r
it is what happens when a language keeps perfect backward compatibility and the\r
tutorials never catch up. But six months of releases since then have quietly\r
removed a lot of the ceremony people complain about, and Java 21 is the long-term\r
release where all of it landed together.\r
\r
This is the short list: the four language changes that actually change how you\r
write code, with a program you can run. Everything here is **final**, not\r
preview — no \`--enable-preview\`, no flags. You need JDK 21 or newer.\r
\r
The fifth big one, **virtual threads**, is a runtime change rather than a\r
language one and gets the whole of the **Advanced** page to itself.\r
\r
\`\`\`bash\r
java -version\r
\`\`\`\r
\r
If that says 17 or lower, get a build from [Adoptium](https://adoptium.net/) — it\r
is a zip, and it can sit alongside whatever you already have.\r
\r
## Records: the class you were going to write anyway\r
\r
A class whose entire job is to carry a few values used to cost you fifty lines —\r
fields, constructor, getters, \`equals\`, \`hashCode\`, \`toString\` — all of which\r
your IDE generated and none of which anyone read.\r
\r
\`\`\`java\r
record Point(int x, int y) {}\r
\`\`\`\r
\r
That is the whole declaration. You get:\r
\r
- \`private final\` fields\r
- a constructor taking every component\r
- accessors named \`x()\` and \`y()\` — note, *not* \`getX()\`\r
- \`equals\` and \`hashCode\` comparing every component\r
- a readable \`toString\`\r
\r
\`\`\`java\r
Point a = new Point(1, 2);\r
Point b = new Point(1, 2);\r
\r
a.equals(b)      // true  — value equality, for free\r
a.toString()     // "Point[x=1, y=2]"\r
\`\`\`\r
\r
Records are **immutable** and **final**. You cannot extend one and you cannot\r
reassign a component. That sounds restrictive until you notice how much of what\r
you pass around should never have been mutable in the first place: DTOs, API\r
responses, coordinates, config values, map keys.\r
\r
You can still add behaviour and validate in a compact constructor:\r
\r
\`\`\`java\r
record Rectangle(double width, double height) {\r
    Rectangle {                             // no parameter list, no assignments\r
        if (width <= 0 || height <= 0)\r
            throw new IllegalArgumentException("sides must be positive");\r
    }\r
\r
    double area() { return width * height; }\r
}\r
\`\`\`\r
\r
**When not to use one:** if the thing has identity rather than value — an entity\r
with a database ID, something with a lifecycle, anything you mutate — a record is\r
the wrong shape. JPA entities in particular need a no-arg constructor and mutable\r
fields, so they stay ordinary classes.\r
\r
## Sealed types: a closed set the compiler knows about\r
\r
An \`interface\` is open — anyone can implement it, so the compiler can never tell\r
you that you have handled every case. \`sealed\` closes it:\r
\r
\`\`\`java\r
sealed interface Shape permits Circle, Rectangle, Triangle {}\r
\r
record Circle(double radius) implements Shape {}\r
record Rectangle(double width, double height) implements Shape {}\r
record Triangle(double base, double height) implements Shape {}\r
\`\`\`\r
\r
Now the compiler knows there are exactly three shapes and nobody can add a\r
fourth. That fact is what makes the next section work: a \`switch\` over a sealed\r
type does not need a \`default\` branch, and **adding a fourth shape becomes a\r
compile error at every switch that does not handle it** — instead of a\r
\`NullPointerException\` in production six weeks later.\r
\r
Sealed interface plus records is the closest Java has to an algebraic data type,\r
and it is the right modelling tool for anything that is "one of a fixed set":\r
payment methods, parse results, state machine states, protocol messages.\r
\r
## Pattern matching: switch that returns something\r
\r
\`switch\` was a statement that fell through and returned nothing. Now it is an\r
expression that matches on type, binds a variable, and refuses to compile if you\r
missed a case.\r
\r
\`\`\`java Shapes.java\r
import java.util.List;\r
\r
public class Shapes {\r
\r
    sealed interface Shape permits Circle, Rectangle, Triangle {}\r
\r
    record Circle(double radius) implements Shape {}\r
    record Rectangle(double width, double height) implements Shape {}\r
    record Triangle(double base, double height) implements Shape {}\r
\r
    record Point(int x, int y) {}\r
    record Line(Point from, Point to) {}\r
\r
    // No default branch. The compiler knows the list is complete.\r
    static double area(Shape shape) {\r
        return switch (shape) {\r
            case Circle c    -> Math.PI * c.radius() * c.radius();\r
            case Rectangle r -> r.width() * r.height();\r
            case Triangle t  -> 0.5 * t.base() * t.height();\r
        };\r
    }\r
\r
    static String describe(Shape shape) {\r
        return switch (shape) {\r
            // 'when' adds a condition to a case\r
            case Circle c when c.radius() > 10 -> "a big circle";\r
            case Circle c                      -> "a circle of radius " + c.radius();\r
\r
            // a record pattern destructures the record inline\r
            case Rectangle(double w, double h) when w == h -> "a square of side " + w;\r
            case Rectangle(double w, double h)             -> "a " + w + " by " + h + " rectangle";\r
\r
            case Triangle t -> "a triangle";\r
        };\r
    }\r
\r
    public static void main(String[] args) {\r
        List<Shape> shapes = List.of(\r
                new Circle(2),\r
                new Circle(12),\r
                new Rectangle(3, 3),\r
                new Rectangle(4, 5),\r
                new Triangle(6, 2));\r
\r
        for (Shape s : shapes) {\r
            System.out.printf("%-28s area = %.2f%n", describe(s), area(s));\r
        }\r
\r
        Point a = new Point(1, 2);\r
        Point b = new Point(1, 2);\r
        System.out.println("a.equals(b) : " + a.equals(b));\r
        System.out.println("a           : " + a);\r
\r
        // Record patterns nest, so you can reach straight through the structure.\r
        Object value = new Line(new Point(0, 0), new Point(3, 4));\r
        if (value instanceof Line(Point(int x1, int y1), Point(int x2, int y2))) {\r
            double length = Math.hypot(x2 - x1, y2 - y1);\r
            System.out.printf("line from (%d,%d) to (%d,%d), length %.1f%n",\r
                    x1, y1, x2, y2, length);\r
        }\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-shapes\r
javac Shapes.java\r
java Shapes\r
\`\`\`\r
\r
\`\`\`output @run-shapes\r
a circle of radius 2.0       area = 12.57\r
a big circle                 area = 452.39\r
a square of side 3.0         area = 9.00\r
a 4.0 by 5.0 rectangle       area = 20.00\r
a triangle                   area = 6.00\r
a.equals(b) : true\r
a           : Point[x=1, y=2]\r
line from (0,0) to (3,4), length 5.0\r
\`\`\`\r
\r
Three things happened there that used to be impossible:\r
\r
- **\`switch\` returned a value**, with \`->\` arms that do not fall through, so no\r
  forgotten \`break\` can ever bite you again.\r
- **\`case Rectangle(double w, double h)\`** pulled the components out in the\r
  pattern itself. No cast, no accessor calls.\r
- **\`instanceof Line(Point(int x1, int y1), ...)\`** matched two levels deep and\r
  bound four variables in one line. Compare that to the cast-and-getter version\r
  you would have written in Java 8.\r
\r
The same pattern matching works on plain \`instanceof\`, which kills the oldest\r
piece of boilerplate in the language:\r
\r
\`\`\`java\r
// before\r
if (o instanceof String) {\r
    String s = (String) o;\r
    if (s.length() > 3) { ... }\r
}\r
\r
// now\r
if (o instanceof String s && s.length() > 3) { ... }\r
\`\`\`\r
\r
> \`switch\` on a nullable value still throws \`NullPointerException\` unless you\r
> write an explicit \`case null ->\`. That is deliberate — it keeps old switches\r
> behaving the way they always did.\r
\r
## Sequenced collections: first and last, finally\r
\r
A small one, but it removes a genuinely stupid asymmetry. Getting the last\r
element of a \`List\` was \`list.get(list.size() - 1)\`. Of a \`LinkedHashSet\`, it was\r
a loop. Java 21 gave every ordered collection a common interface:\r
\r
\`\`\`java\r
List<String> names = new ArrayList<>(List.of("ada", "grace", "alan"));\r
\r
names.getFirst();       // "ada"      — was names.get(0)\r
names.getLast();        // "alan"     — was names.get(names.size() - 1)\r
names.addFirst("linus");\r
names.removeLast();\r
names.reversed();       // a reversed VIEW, not a copy\r
\r
LinkedHashMap<String, Integer> scores = new LinkedHashMap<>();\r
scores.firstEntry();\r
scores.lastEntry();\r
\`\`\`\r
\r
\`reversed()\` returning a view rather than a copy is the part worth knowing —\r
iterating it costs nothing extra, and writes through it affect the original.\r
\r
## Text blocks, while we are here\r
\r
Older than Java 21 — final in 15 — but still unknown to a surprising number of\r
people, and it makes embedded JSON and SQL readable:\r
\r
\`\`\`java\r
String query = """\r
        SELECT id, name, email\r
        FROM users\r
        WHERE active = true\r
        ORDER BY name\r
        """;\r
\`\`\`\r
\r
The common leading whitespace is stripped based on the closing delimiter's\r
indentation, so the string does not carry your source indentation with it. End a\r
line with \`\\\` to join it to the next, and \`\\s\` keeps a trailing space that would\r
otherwise be trimmed.\r
\r
## When it goes wrong\r
\r
- **\`error: illegal start of type\` on a \`record\`** — you are compiling with an\r
  older \`--release\` or an older JDK. Check \`javac -version\`, and check your\r
  build's \`maven.compiler.release\` or \`sourceCompatibility\`.\r
- **\`switch\` demands a \`default\` you do not want** — the type is not \`sealed\`, so\r
  the compiler cannot prove the cases are exhaustive. Seal it, or accept the\r
  default.\r
- **"pattern matching in switch is a preview feature"** — you are on Java 17 to\r
  20. It was finalised in 21; upgrade rather than adding \`--enable-preview\`.\r
- **A record will not deserialise** — Jackson needs 2.12+ and, on some setups,\r
  \`-parameters\` passed to the compiler so it can see the component names.\r
- **The IDE underlines everything in red but it compiles** — the project language\r
  level is set separately from the JDK. In IntelliJ: **Project Structure →\r
  Project → Language level**.\r
\r
## Further reading\r
\r
- [JEP 395: Records](https://openjdk.org/jeps/395)\r
- [JEP 409: Sealed classes](https://openjdk.org/jeps/409)\r
- [JEP 441: Pattern matching for switch](https://openjdk.org/jeps/441)\r
- [JEP 440: Record patterns](https://openjdk.org/jeps/440)\r
- [JEP 444: Virtual threads](https://openjdk.org/jeps/444)\r
- [JEP 431: Sequenced collections](https://openjdk.org/jeps/431)\r
`,_r=`Records and pattern matching change how code reads. **Virtual threads** change\r
what shape a server can be — they are the reason a decade of reactive programming\r
became optional, and they are the single most asked-about thing in Java\r
interviews since Java 8's streams.\r
\r
This page is virtual threads in depth, the two APIs built on top of them, and\r
what has landed since 21.\r
\r
## The problem, stated properly\r
\r
A **platform thread** is a thin wrapper over an OS thread. It costs:\r
\r
- a stack reserved up front — around 1 MB by default, committed lazily\r
- kernel bookkeeping, and a system call to create\r
- a context switch through the kernel scheduler every time it yields\r
\r
So you pool them. A pool of 200 threads means **200 concurrent requests**,\r
whatever your hardware. And because a blocking call parks one of those expensive\r
threads doing nothing at all, throughput collapses long before the CPU is busy.\r
\r
The industry's answer was **asynchronous**: never block, return a \`Future\` or a\r
\`Mono\`, chain callbacks. It works, and the price is high:\r
\r
\`\`\`java\r
// The asynchronous version. Correct, and unreadable.\r
return findUser(id)\r
    .thenCompose(user -> loadOrders(user.id()))\r
    .thenCombine(loadPreferences(id), Report::new)\r
    .exceptionally(e -> fallbackReport(id));\r
\`\`\`\r
\r
You lose the call stack — an exception's stack trace shows the executor, not the\r
code that asked. You lose the debugger, because there is no single thread to step\r
through. You lose \`try\`/\`catch\`/\`finally\` and try-with-resources, because the\r
lexical block ends long before the work does. And it is viral: one async call\r
forces every caller to become async.\r
\r
## What a virtual thread actually is\r
\r
A virtual thread is a \`Thread\` whose stack lives on the **heap** as a\r
continuation, scheduled by the JVM onto a small pool of **carrier** threads —\r
platform threads, one per core by default, in a dedicated \`ForkJoinPool\`.\r
\r
\`\`\`text\r
    virtual threads (hundreds of thousands, on the heap)\r
    ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○ ○\r
        │       │           │\r
        ▼       ▼           ▼      mounted while running\r
    ┌───────┐┌───────┐┌───────┐┌───────┐\r
    │ CPU 0 ││ CPU 1 ││ CPU 2 ││ CPU 3 │   carrier threads (a handful)\r
    └───────┘└───────┘└───────┘└───────┘\r
\`\`\`\r
\r
When a virtual thread hits a blocking call, the JDK's I/O layer does not block\r
the carrier. It **unmounts** the virtual thread — copies its stack frames to the\r
heap — registers interest in the I/O event, and gives the carrier to another\r
virtual thread. When the event fires the continuation is mounted again, possibly\r
on a different carrier, and resumes on the line after the blocking call.\r
\r
The blocking call is still blocking *from the code's point of view*. Your stack\r
is intact, your \`finally\` runs, your debugger works. It is only the OS thread\r
that was freed.\r
\r
Which means the readable version is now the fast version:\r
\r
\`\`\`java\r
// Same work, sequential code. One virtual thread per request.\r
User user = findUser(id);\r
List<Order> orders = loadOrders(user.id());\r
Preferences prefs = loadPreferences(id);\r
return new Report(orders, prefs);\r
\`\`\`\r
\r
## How much cheaper\r
\r
\`\`\`java Scale.java\r
import java.util.concurrent.CountDownLatch;\r
import java.util.concurrent.atomic.AtomicInteger;\r
\r
public class Scale {\r
\r
    public static void main(String[] args) throws Exception {\r
        System.out.println("platform threads:");\r
        report(Thread.ofPlatform(), 10_000);\r
\r
        System.out.println();\r
        System.out.println("virtual threads:");\r
        report(Thread.ofVirtual(), 1_000_000);\r
    }\r
\r
    static void report(Thread.Builder builder, int count) throws Exception {\r
        AtomicInteger made = new AtomicInteger();\r
        CountDownLatch start = new CountDownLatch(1);\r
        CountDownLatch done = new CountDownLatch(count);\r
        long t0 = System.currentTimeMillis();\r
\r
        try {\r
            for (int i = 0; i < count; i++) {\r
                builder.start(() -> {\r
                    try {\r
                        start.await();          // every thread parks here\r
                    } catch (InterruptedException e) {\r
                        Thread.currentThread().interrupt();\r
                    }\r
                    done.countDown();\r
                });\r
                made.incrementAndGet();\r
            }\r
            System.out.printf("  created %,d in %,d ms%n", made.get(),\r
                    System.currentTimeMillis() - t0);\r
        } catch (OutOfMemoryError e) {\r
            System.out.printf("  died after %,d threads: %s%n", made.get(), e.getMessage());\r
        } finally {\r
            start.countDown();\r
        }\r
    }\r
}\r
\`\`\`\r
\r
\`\`\`bash @run-scale\r
javac Scale.java\r
java Scale\r
\`\`\`\r
\r
\`\`\`output @run-scale\r
platform threads:\r
  created 10,000 in 626 ms\r
\r
virtual threads:\r
  created 1,000,000 in 2,424 ms\r
\`\`\`\r
\r
Ten thousand platform threads took 626 ms — about 63 microseconds each, and that\r
machine would not have survived many more. A **hundred times as many** virtual\r
threads took four times as long, which is around 2.4 microseconds each.\r
\r
Try the platform run with 1,000,000 and it dies with\r
\`OutOfMemoryError: unable to create native thread\` — a message about native\r
memory, not the heap, because thread stacks are not heap-allocated.\r
\r
## Using them\r
\r
There are three ways in, and one of them is a mistake:\r
\r
\`\`\`java\r
// 1. An executor that makes a new virtual thread per task. The usual choice.\r
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {\r
    for (var task : tasks) executor.submit(task);\r
}   // close() blocks until every task finishes\r
\r
// 2. Directly, when you want to name it or hold the Thread.\r
Thread t = Thread.ofVirtual().name("importer-", 0).start(runnable);\r
t.join();\r
\r
// 3. WRONG. There is nothing to pool. This caps concurrency for no reason\r
//    and reintroduces the exact problem virtual threads remove.\r
Executors.newFixedThreadPool(200, Thread.ofVirtual().factory());\r
\`\`\`\r
\r
Note what \`newVirtualThreadPerTaskExecutor\` is **not**: it is not a pool, it has\r
no queue, and it never rejects. Every submitted task gets its own thread\r
immediately. Which also means it provides **no back-pressure** — see below.\r
\r
Enable them in a server with one property. Spring Boot 3.2+:\r
\r
\`\`\`text\r
spring.threads.virtual.enabled=true\r
\`\`\`\r
\r
## Pinning\r
\r
A virtual thread is **pinned** when it cannot be unmounted from its carrier. A\r
pinned thread that blocks takes a carrier thread out of circulation, and if\r
enough of them do it at once you deadlock a pool of \`availableProcessors()\`\r
threads.\r
\r
Two causes:\r
\r
- **Inside a \`synchronized\` block or method** — on Java 21. This was the big one:\r
  a \`synchronized\` method containing a database call pinned a carrier for the\r
  duration, which is exactly the case virtual threads exist to solve.\r
  **JEP 491 fixed this in Java 24**, and object monitors no longer pin.\r
- **Inside a native frame** — a JNI call, or a \`Object.wait()\` in some\r
  situations. Still pins, on every version.\r
\r
On Java 21 to 23 the guidance is to prefer \`ReentrantLock\`, which unmounts\r
correctly:\r
\r
\`\`\`java\r
// pins the carrier on 21-23\r
synchronized (this) { database.query(sql); }\r
\r
// unmounts correctly on every version\r
lock.lock();\r
try { database.query(sql); } finally { lock.unlock(); }\r
\`\`\`\r
\r
Find it before it finds you:\r
\r
\`\`\`bash\r
java -Djdk.tracePinnedThreads=full MyApp\r
\`\`\`\r
\r
That prints a stack trace whenever a virtual thread blocks while pinned. Run it\r
once against a real workload; the output is usually one library.\r
\r
## Where virtual threads do not help\r
\r
**CPU-bound work.** Virtual threads win when threads spend their time *waiting*.\r
For computation the limit is cores, and a platform thread pool sized to\r
\`availableProcessors()\` — or a parallel stream — remains correct. A million\r
virtual threads all doing arithmetic is a million threads fighting over the same\r
few carriers, plus scheduling overhead.\r
\r
**\`ThreadLocal\` at scale.** A million threads each holding a \`ThreadLocal\` value\r
is a million retained objects. Thread pools amortised that by reusing threads;\r
one-thread-per-task does not. This is what **scoped values** exist to fix.\r
\r
**Unbounded concurrency.** The executor never rejects, so ten thousand incoming\r
requests become ten thousand concurrent database calls, and the connection pool —\r
or the database — falls over. The limit did not disappear; it moved. Put a\r
\`Semaphore\` where the pool used to be:\r
\r
\`\`\`java\r
private final Semaphore dbSlots = new Semaphore(50);\r
\r
void query() throws InterruptedException {\r
    dbSlots.acquire();\r
    try { database.query(sql); } finally { dbSlots.release(); }\r
}\r
\`\`\`\r
\r
That is the honest version of what a thread pool was doing: not managing threads,\r
but limiting concurrency to a downstream resource. Virtual threads separate those\r
two jobs, and you now have to do the second one deliberately.\r
\r
## Structured concurrency\r
\r
One virtual thread per task makes a second problem obvious: a group of concurrent\r
subtasks has no lifetime. Submit two futures, and if one fails the other keeps\r
running; if the caller is cancelled, neither is.\r
\r
**Structured concurrency** gives a group of tasks the same block scope your code\r
already has:\r
\r
\`\`\`java\r
// Preview API — the exact shape has changed between releases.\r
try (var scope = StructuredTaskScope.open()) {\r
    Subtask<User>  user   = scope.fork(() -> findUser(id));\r
    Subtask<Orders> orders = scope.fork(() -> loadOrders(id));\r
\r
    scope.join();                       // wait for both\r
\r
    return new Report(user.get(), orders.get());\r
}   // leaving the block cancels anything still running\r
\`\`\`\r
\r
The guarantees are the point: both subtasks are cancelled if the block is left\r
for any reason, a failure in one can cancel the other, and the subtasks appear as\r
*children* of the calling thread in a thread dump — so a stack dump finally\r
reflects the actual shape of the work.\r
\r
**It is still a preview API** as of Java 25, and its API has changed across\r
several previews. Read it, understand the model, do not build production code on\r
it yet.\r
\r
## Scoped values\r
\r
\`ThreadLocal\` has three problems: it is mutable from anywhere, it has no defined\r
lifetime, and with one thread per task it retains one object per thread forever.\r
**Scoped values** (final in Java 25) fix all three by being immutable and\r
lexically bounded:\r
\r
\`\`\`java\r
private static final ScopedValue<User> CURRENT = ScopedValue.newInstance();\r
\r
ScopedValue.where(CURRENT, user).run(() -> {\r
    handleRequest();          // CURRENT.get() works anywhere below here\r
});                           // and is unbound again the moment this returns\r
\`\`\`\r
\r
The binding is visible to everything called inside the block, including subtasks\r
forked in a \`StructuredTaskScope\`, and nowhere else. There is no \`set\` — rebinding\r
means nesting another \`where\`, so the value cannot be changed underneath a caller.\r
\r
For request context — the authenticated user, a trace id, a tenant — this is what\r
you want, and it is the replacement \`ThreadLocal\` never had.\r
\r
## What else has landed since 21\r
\r
- **Compact source files and instance \`main\` methods** (final in **Java 25**). A\r
  single-file program no longer needs \`public class\` or \`String[] args\`:\r
\r
  \`\`\`java\r
  void main() {\r
      IO.println("Hello");\r
  }\r
  \`\`\`\r
\r
  This is a teaching change more than a production one, and it makes the first\r
  Java lesson dramatically shorter.\r
\r
- **Module import declarations** (final in **Java 25**) — \`import module\r
  java.base;\` pulls in everything a module exports, which pairs well with the\r
  above for scripts.\r
\r
- **Flexible constructor bodies** (final in **Java 25**) — statements are allowed\r
  before \`super()\`, so you can validate arguments before the superclass\r
  constructor runs, instead of smuggling the work into a static helper.\r
\r
- **Generational ZGC** — the default ZGC mode since Java 23, recovering much of\r
  the throughput ZGC previously traded away. See the\r
  [garbage collection advanced page](#/study/deep-gc/advanced).\r
\r
Java 21 remains the sensible baseline: it is LTS, everything on the Notes page is\r
final in it, and virtual threads work. Java 25 is the next LTS and is where the\r
\`synchronized\` pinning fix, scoped values and the compact source files all\r
arrive together.\r
\r
## When it goes wrong\r
\r
- **Virtual threads make no difference** — the workload is CPU-bound, or the\r
  bottleneck is a connection pool that still has 20 connections. Measure where\r
  the time goes before concluding anything.\r
- **Throughput collapses under load with no error** — pinning. Run with\r
  \`-Djdk.tracePinnedThreads=full\`; on Java 21-23 look for \`synchronized\` around\r
  I/O in your dependencies.\r
- **The database falls over after enabling virtual threads** — you removed the\r
  accidental concurrency limit the thread pool was providing. Add a \`Semaphore\`.\r
- **\`OutOfMemoryError: unable to create native thread\`** — these are *platform*\r
  threads. Thread stacks are native memory, so \`-Xmx\` is irrelevant; you have hit\r
  an OS or cgroup limit.\r
- **Heap full of \`ThreadLocal\` values** — one per task rather than one per pooled\r
  thread. Move to scoped values, or clear them explicitly.\r
- **\`StructuredTaskScope\` does not compile** — preview API. It needs\r
  \`--enable-preview\` at both compile and run time, and the API shape differs\r
  between 21, 24 and 25.\r
- **Thread dumps are useless with a million threads** — use\r
  \`jcmd <pid> Thread.dump_to_file -format=json dump.json\`, which is designed for\r
  virtual threads and groups them by the structured scope that created them.\r
\r
## Further reading\r
\r
- [JEP 444: Virtual Threads](https://openjdk.org/jeps/444)\r
- [JEP 491: Synchronize Virtual Threads without Pinning](https://openjdk.org/jeps/491)\r
- [JEP 505: Structured Concurrency](https://openjdk.org/jeps/505)\r
- [JEP 506: Scoped Values](https://openjdk.org/jeps/506)\r
- [Oracle's virtual threads guide](https://docs.oracle.com/en/java/javase/21/core/virtual-threads.html)\r
`,vr=`Fourteen questions on Java 21 and after. This is the set that separates people
who have read a release note from people who have shipped the feature — most of
the follow-ups are "and when would you *not* use it?"

The **Notes** cover the language changes. The **Advanced** page covers virtual
threads and everything built on them.

## Easy

### 1. What is a \`record\`, and what do you get for free?

A transparent carrier for a fixed set of values. \`record Point(int x, int y) {}\`
gives you private final fields, a canonical constructor, accessors named \`x()\`
and \`y()\` — not \`getX()\` — plus \`equals\`, \`hashCode\` and \`toString\` derived from
every component. Records are implicitly final and their fields cannot be
reassigned.

### 2. When should you not use a record?

When the thing has **identity** rather than value: an entity with a database id,
anything with a lifecycle, anything you mutate. JPA entities need a no-arg
constructor and mutable fields, so they stay ordinary classes. Records are also
only *shallowly* immutable — \`record Order(List<Item> items)\` still hands out a
mutable list unless the compact constructor copies it.

### 3. What is a text block for?

Multi-line string literals without escaping — embedded SQL, JSON, HTML. The
common leading whitespace is stripped based on the closing delimiter's
indentation, so the string does not carry your source indentation. \`\\\` at the end
of a line joins it to the next; \`\\s\` preserves a trailing space that would
otherwise be trimmed.

### 4. What did sequenced collections add?

A common interface over every ordered collection: \`getFirst\`, \`getLast\`,
\`addFirst\`, \`addLast\`, \`removeFirst\`, \`removeLast\` and \`reversed\`. Before Java 21
the last element of a \`List\` was \`get(size() - 1)\` and of a \`LinkedHashSet\` was a
loop. \`reversed()\` returns a **view**, not a copy, so iterating it is free and
writes through it affect the original.

## Medium

### 5. What do sealed types actually buy you?

A \`sealed interface\` names exactly which types may implement it, so the compiler
knows the list is complete. That makes a \`switch\` over the type **exhaustive**
without a \`default\` branch — and, more valuably, adding a new implementation
becomes a **compile error** at every switch that does not handle it. Without
sealing, the same omission is a runtime surprise. Sealed interface plus records
is Java's version of an algebraic data type.

### 6. What is the difference between \`switch\` as a statement and as an expression?

The statement form falls through between cases, returns nothing, and needs
\`break\`. The expression form uses \`->\` arms, does not fall through, produces a
value, and must be exhaustive — the compiler rejects it if a case is missing and
there is no \`default\`. Combined with sealed types, that turns a whole family of
runtime bugs into compile errors.

### 7. What is a record pattern?

Destructuring in the pattern itself. Instead of matching the type and then
calling accessors, you write \`case Rectangle(double w, double h) ->\` and the
components are bound as variables. They nest, so
\`if (o instanceof Line(Point(int x1, int y1), Point(int x2, int y2)))\` matches two
levels deep and binds four variables in one line, with no casts.

### 8. Why does \`switch\` on a \`null\` still throw?

Because that is what \`switch\` has always done, and changing it would silently
alter the behaviour of existing code. Pattern matching for switch is opt-in about
null: write an explicit \`case null ->\` and the switch handles it; leave it out
and you get \`NullPointerException\`, exactly as before.

### 9. What is a virtual thread, and how is it different from a platform thread?

A platform thread wraps an OS thread: roughly a megabyte of stack, a system call
to create, and a kernel context switch to schedule — which is why you pool them.
A **virtual thread** has its stack on the heap as a continuation and is scheduled
by the JVM onto a small pool of carrier threads. Creating one costs a couple of
microseconds. When it blocks, the JVM **unmounts** it from its carrier and runs
something else, so blocking no longer occupies an expensive resource.

### 10. If virtual threads are cheap, why not pool them?

Because pooling exists to amortise an expensive resource, and they are not one.
\`Executors.newFixedThreadPool(200, virtualFactory)\` caps concurrency at 200 for
no reason and reintroduces exactly the constraint virtual threads remove. The
model is one thread per task, created and discarded freely — which is what
\`newVirtualThreadPerTaskExecutor\` provides.

## Hard

### 11. When do virtual threads not help?

**CPU-bound work** — the limit is cores, not threads, and a platform pool sized
to \`availableProcessors()\` is still correct. **\`ThreadLocal\` at scale** — one
value per task instead of per pooled thread means a million retained objects,
which is what scoped values exist to fix. And **anywhere you needed the
back-pressure**: the executor never rejects, so the concurrency limit your thread
pool was accidentally providing is gone, and you have to reintroduce it
deliberately with a \`Semaphore\` in front of the constrained resource.

### 12. What is pinning?

A virtual thread is pinned when it cannot be unmounted from its carrier, so
blocking takes a carrier thread out of circulation. On Java 21 to 23 the big
cause was blocking inside a \`synchronized\` block — precisely the case virtual
threads exist to solve — and the advice was to prefer \`ReentrantLock\`, which
unmounts correctly. **JEP 491 removed the monitor case in Java 24**; native
frames still pin. Diagnose it with \`-Djdk.tracePinnedThreads=full\`, which prints
a stack trace every time it happens.

### 13. What problem does structured concurrency solve?

That a group of concurrent subtasks has no lifetime. Submit two futures and a
failure in one leaves the other running; cancel the caller and neither stops.
\`StructuredTaskScope\` gives the group the same block scope the code already has:
subtasks are cancelled when the block is left for any reason, a failure can
cancel its siblings, and they appear as **children** of the calling thread in a
thread dump. It is still a **preview** API as of Java 25 and its shape has
changed across previews, so understand the model but do not ship on it.

### 14. Why are scoped values better than \`ThreadLocal\` for request context?

Three reasons. They are **immutable** — there is no \`set\`, so a callee cannot
change a value underneath its caller; rebinding means nesting another
\`ScopedValue.where\`. They are **lexically bounded** — the binding exists for the
duration of the block and is gone afterwards, rather than lingering on the
thread. And they are **cheap under one-thread-per-task**, because the value lives
with the scope rather than being retained once per thread. They also propagate
correctly into subtasks forked in a structured scope, which \`ThreadLocal\` does
not.

## What to revise next

Questions 9 to 12 are one conversation, not four, and it usually ends at "so what
breaks when you turn them on?" — question 11 is the answer, and having a concrete
example of the connection-pool failure is worth more than the theory.

Question 5 tends to be followed by "show me" — be ready to write a sealed
interface, three records and an exhaustive switch on a whiteboard. It is about
fifteen lines, and it is the cleanest demonstration of modern Java there is.
`,yr='The chapter in a page. If a line here is news, the **Notes** or **Advanced**\npart is where it comes from.\n\n## Records\n\n- `record Point(int x, int y) {}` gives you final fields, a canonical\n  constructor, accessors named `x()` — **not** `getX()` — plus `equals`,\n  `hashCode` and `toString`.\n- Implicitly **final**, and components cannot be reassigned.\n- Validate in a **compact constructor**: `Point { if (x < 0) throw ...; }` — no\n  parameter list, no assignments.\n- Do not use one where **identity** matters rather than value: JPA entities,\n  anything with a lifecycle, anything you mutate.\n- Only *shallowly* immutable. A record holding a `List` still hands the list out.\n\n## Sealed types\n\n- `sealed interface Shape permits Circle, Rectangle, Triangle {}` — the compiler\n  now knows the complete list.\n- A `switch` over it is **exhaustive** with no `default`.\n- Adding a fourth implementation becomes a **compile error** at every switch that\n  does not handle it. Without sealing it is a runtime surprise.\n- Sealed interface + records is Java\'s algebraic data type.\n\n## Pattern matching\n\n- `switch` is now an **expression**: `->` arms, no fall-through, returns a value,\n  must be exhaustive.\n- `case Circle c when c.radius() > 10 ->` — a guard on a case.\n- **Record patterns** destructure inline: `case Rectangle(double w, double h) ->`.\n  They nest, so\n  `if (o instanceof Line(Point(int x1, int y1), Point(int x2, int y2)))` binds\n  four variables with no casts.\n- `instanceof` binds too: `if (o instanceof String s && s.length() > 3)`.\n- `switch` on `null` still throws unless you write `case null ->`. Deliberate.\n\n## Sequenced collections and text blocks\n\n- `getFirst`, `getLast`, `addFirst`, `addLast`, `removeFirst`, `removeLast`,\n  `reversed` — on every ordered collection.\n- `reversed()` is a **view**, not a copy.\n- Text blocks: `"""` … `"""`, common indentation stripped based on the closing\n  delimiter. `\\` joins lines, `\\s` keeps a trailing space.\n\n## Virtual threads\n\n- **Platform thread** = an OS thread. ~1 MB stack, a system call to create, so\n  you pool them, so 200 threads means 200 concurrent requests.\n- **Virtual thread** = stack on the heap as a continuation, scheduled by the JVM\n  onto a handful of **carrier** threads. Microseconds to create.\n- Blocking **unmounts** it from its carrier. Your stack, `finally` and debugger\n  all still work — only the OS thread is freed.\n- Measured: 10,000 platform threads in 626 ms; **1,000,000** virtual threads in\n  2,424 ms.\n- `Executors.newVirtualThreadPerTaskExecutor()` is the usual way in. `close()`\n  waits for everything.\n- **Do not pool them.** There is nothing to amortise.\n\n## What breaks when you turn them on\n\n- **CPU-bound work** gets no benefit. The limit is cores.\n- **`ThreadLocal`** becomes one value per task instead of per pooled thread —\n  a million retained objects. Use **scoped values**.\n- **No back-pressure.** The executor never rejects, so the concurrency limit your\n  pool was accidentally providing is gone. Put a `Semaphore` in front of the\n  database.\n- **Pinning** — blocking inside `synchronized` pinned the carrier on Java 21–23.\n  Fixed by JEP 491 in Java 24; native frames still pin. Find it with\n  `-Djdk.tracePinnedThreads=full`.\n\n## After 21\n\n- **Scoped values** (final in 25) — immutable, lexically bounded, propagate into\n  structured subtasks. The `ThreadLocal` replacement.\n- **Structured concurrency** — a group of tasks that fails and cancels together.\n  Still **preview** as of 25; understand it, do not ship on it.\n- **Compact source files and instance `main`** (final in 25).\n- **Generational ZGC** — the default ZGC mode since 23.\n- Java 21 is the sensible baseline; 25 is the next LTS and is where the pinning\n  fix and scoped values land together.\n',br={notes:{label:`Notes`,blurb:`The explanation itself.`},advanced:{label:`Advanced`,blurb:`The mechanisms underneath.`},revision:{label:`Quick revision`,blurb:`The chapter, compressed.`},interview:{label:`Interview`,blurb:`Questions, with full answers.`}},xr=[`notes`,`advanced`,`revision`,`interview`],Sr=[`Fundamentals`,`Classes and objects`,`Inheritance and polymorphism`,`Types and contracts`,`Errors and input`,`How it runs`,`What changed`],Cr=e=>{let t=Sr.indexOf(e);return t===-1?Sr.length:t},wr=[{slug:`deep-jvm`,title:`How the JVM runs your code`,tagline:`Class loading, bytecode and the JIT.`,summary:`Chapter 2 stops at "the JVM executes bytecode". This goes underneath it: how classes are found and loaded, what is actually inside a .class file, and how the JIT decides which methods are worth compiling to machine code.`,subject:`Java Advanced`,stage:`How it runs`,accent:`#38bdf8`,parts:{notes:cr,advanced:lr,revision:dr,interview:ur}},{slug:`deep-memory`,title:`Memory, in detail`,tagline:`What an object costs, and the boxing cache.`,summary:`Chapters 2, 12 and 21 give you stack, heap, the String Pool and autoboxing. This adds the numbers: what an object weighs in bytes, why allocation is nearly free, and why == on boxed integers works up to 127 and then stops.`,subject:`Java Advanced`,stage:`How it runs`,accent:`#34d399`,parts:{notes:fr,advanced:pr,revision:hr,interview:mr}},{slug:`deep-gc`,title:`Garbage collection`,tagline:`The one rule, and the log that proves it.`,summary:`Reachability is the only thing the collector cares about. Two runnable programs show an object being collected and the heap filling and emptying, and the advanced part covers how G1 really works and how to read a heap dump after the thing has fallen over.`,subject:`Java Advanced`,stage:`How it runs`,accent:`#f97316`,parts:{notes:ir,advanced:ar,revision:sr,interview:or}},{slug:`deep-modern-java`,title:`Modern Java, 21 and after`,tagline:`Records, sealed types, virtual threads.`,summary:`The course is written against Java as most people still write it. This is what changed: records, sealed types and pattern matching, then virtual threads in depth — including the part where turning them on knocks your database over.`,subject:`Java Advanced`,stage:`What changed`,accent:`#a78bfa`,parts:{notes:gr,advanced:_r,revision:yr,interview:vr}}],Tr=e=>{let t=xr.filter(t=>e.parts[t]).map((t,n)=>({id:t,...br[t],content:ee(e.parts[t]),minutes:S(e.parts[t]),route:n===0?`study/${e.slug}`:`study/${e.slug}/${t}`}));return{...e,parts:t,route:`study/${e.slug}`}},Er=[...rr,...wr].map(Tr).sort((e,t)=>Cr(e.stage)-Cr(t.stage)||(e.chapter??99)-(t.chapter??99)),Dr=e=>[e.title,e.tagline??``,e.summary,e.subject,e.stage].concat(e.parts.map(e=>e.content)).join(` `).toLowerCase(),Or=e=>e.parts.reduce((e,t)=>e+t.minutes,0),kr=[...new Set(Er.map(e=>e.subject))].sort(),Ar=e=>{let t=Er.filter(t=>t.subject===e);return Sr.map(e=>({stage:e,topics:t.filter(t=>t.stage===e)})).filter(e=>e.topics.length>0)},jr=e=>Er.find(t=>t.slug===e)??null,Mr=(e,t)=>e?.parts.find(e=>e.id===t)??e?.parts[0]??null,Nr=[{slug:`scrollbar`,name:`Scrollbar`,tagline:`Design a scrollbar, take the CSS.`,summary:`Pick the colours and sizes, watch a real scrollbar change as you do, and copy the CSS out. Includes the standards-based fallback for browsers that do not implement the WebKit pseudo-elements.`,tags:[`CSS`,`Generator`],status:`live`,accent:`#6baf8d`},{slug:`colour`,name:`Colour`,tagline:`Pick it, convert it, build a scale from it.`,summary:`A real picker with hue and alpha, input in any format you can paste, and output in HEX, RGB, HSL, HSB and OKLCH. Plus contrast ratios against black and white with the WCAG grades, a tint scale stepped through OKLCH, and the five standard harmonies.`,tags:[`Colour`,`CSS`,`Accessibility`],status:`live`,accent:`#8b6cff`},{slug:`gradient`,name:`Gradient`,tagline:`Linear, radial and conic, with real stops.`,summary:`Drag stops along the bar, add one anywhere by double-clicking, and choose the colour space the browser interpolates through — OKLCH avoids the grey dip sRGB puts halfway between two saturated colours. Copies as two declarations so older browsers still get something.`,tags:[`Colour`,`CSS`,`Generator`],status:`live`,accent:`#f472b6`},{slug:`mesh`,name:`Mesh gradient`,tagline:`Drag the colours around until it looks right.`,summary:`Layered radial gradients you position by dragging, with per-point spread and a base colour underneath. Copies as plain CSS, and exports a PNG painted from the same numbers on a canvas — so the image is as sharp as the size you pick rather than as sharp as your screen.`,tags:[`Colour`,`CSS`,`Export`],status:`live`,accent:`#ff0080`},{slug:`pdf-to-image`,name:`PDF to image`,tagline:`Pages out as PNGs, nothing uploaded.`,summary:`Drop a PDF in and get each page as a PNG at the resolution you choose. It runs in the tab, so the file never leaves the machine — which is the whole reason to have it here rather than using one of the sites that wants your document.`,tags:[`Files`,`Offline`],status:`upcoming`,accent:`#f97316`},{slug:`box-shadow`,name:`Box shadow`,tagline:`Layered shadows that look like light.`,summary:`Stack several shadows with independent offset, blur, spread and opacity, preview them on a surface you can recolour, and copy the result.`,tags:[`CSS`,`Generator`],status:`upcoming`,accent:`#38bdf8`},{slug:`base64`,name:`Base64 and data URIs`,tagline:`Encode a file into a stylesheet.`,summary:`Convert text or a small file to Base64 and back, and wrap it as a data URI with the right MIME type — for the times an icon is easier inlined than hosted.`,tags:[`Encoding`,`Offline`],status:`upcoming`,accent:`#34d399`},{slug:`cron`,name:`Cron reader`,tagline:`What that schedule actually means.`,summary:`Type a cron expression and read it back in English, with the next several run times listed so you can check it does what you meant before it does not.`,tags:[`Scheduling`],status:`upcoming`,accent:`#f472b6`}].map(e=>({...e,route:`tools/${e.slug}`})),Pr=e=>Nr.find(t=>t.slug===e&&t.status===`live`)??null,Fr=()=>{let e=[];for(let t of Er)for(let n of t.parts)e.push({id:n.route,kind:`Study`,title:t.title,detail:`${n.label} · ${t.subject}`,route:n.route,accent:t.accent,text:`${t.title} ${t.summary} ${n.label} ${t.subject} ${n.content}`.toLowerCase()});for(let t of de)e.push({id:t.route,kind:`Guide`,title:t.title,detail:t.category,route:t.route,accent:t.accent,text:`${t.title} ${t.summary} ${t.tags.join(` `)} ${t.content}`.toLowerCase()});for(let t of Nr)e.push({id:t.route,kind:`Tool`,title:t.name,detail:t.status===`live`?t.tagline:`Upcoming`,route:t.status===`live`?t.route:`tools`,accent:t.accent,text:`${t.name} ${t.tagline} ${t.summary} ${t.tags.join(` `)}`.toLowerCase()});for(let t of re)e.push({id:t.id,kind:`App`,title:t.name,detail:t.tagline,route:t.id,accent:t.accent,text:`${t.name} ${t.tagline} ${t.summary} ${t.tags.join(` `)}`.toLowerCase()});return e},Ir=(e,t)=>{let n=e.title.toLowerCase(),r=0;for(let i of t){if(!e.text.includes(i))return 0;n===i?r+=100:n.startsWith(i)?r+=50:n.includes(i)?r+=30:e.detail.toLowerCase().includes(i)?r+=10:r+=1}return r};function Lr({onNavigate:e}){let[t,n]=(0,_.useState)(!1),[r,i]=(0,_.useState)(``),[a,o]=(0,_.useState)(0),s=(0,_.useRef)(null),c=(0,_.useMemo)(Fr,[]),l=(0,_.useMemo)(()=>{let e=r.trim().toLowerCase().split(/\s+/).filter(Boolean);return e.length?c.map(t=>({row:t,score:Ir(t,e)})).filter(e=>e.score>0).sort((e,t)=>t.score-e.score).slice(0,12).map(e=>e.row):[]},[r,c]);(0,_.useEffect)(()=>{let e=e=>{(e.ctrlKey||e.metaKey)&&e.key.toLowerCase()===`k`&&(e.preventDefault(),n(!0)),e.key===`Escape`&&n(!1)};return window.addEventListener(`keydown`,e),()=>window.removeEventListener(`keydown`,e)},[]),(0,_.useEffect)(()=>{t?s.current?.focus():(i(``),o(0))},[t]),(0,_.useEffect)(()=>o(0),[r]);let u=t=>{t&&(n(!1),e(t.route))};return(0,N.jsxs)(N.Fragment,{children:[(0,N.jsxs)(`button`,{type:`button`,className:`global-search-trigger`,onClick:()=>n(!0),"aria-label":`Search everything`,children:[(0,N.jsx)(P,{name:`search`,size:15}),(0,N.jsx)(`span`,{className:`hide-sm`,children:`Search`}),(0,N.jsx)(`kbd`,{className:`hide-sm`,children:`Ctrl K`})]}),t&&(0,nr.createPortal)((0,N.jsx)(`div`,{className:`search-overlay`,onClick:()=>n(!1),role:`presentation`,children:(0,N.jsxs)(`div`,{className:`search-modal`,onClick:e=>e.stopPropagation(),role:`dialog`,"aria-modal":`true`,"aria-label":`Search everything`,children:[(0,N.jsxs)(`div`,{className:`search-modal-input`,children:[(0,N.jsx)(P,{name:`search`,size:17}),(0,N.jsx)(`input`,{ref:s,type:`text`,value:r,placeholder:`Search notes, questions, guides, apps, tools…`,onChange:e=>i(e.target.value),onKeyDown:e=>{e.key===`ArrowDown`?(e.preventDefault(),o(e=>Math.min(l.length-1,e+1))):e.key===`ArrowUp`?(e.preventDefault(),o(e=>Math.max(0,e-1))):e.key===`Enter`&&(e.preventDefault(),u(l[a]))}}),(0,N.jsx)(`kbd`,{children:`Esc`})]}),r.trim()!==``&&(0,N.jsxs)(`ul`,{className:`search-results`,children:[l.length===0&&(0,N.jsx)(`li`,{className:`search-empty`,children:`Nothing matches that.`}),l.map((e,t)=>(0,N.jsx)(`li`,{children:(0,N.jsxs)(`button`,{type:`button`,className:`search-result ${t===a?`is-active`:``}`,style:{"--accent":e.accent},onMouseEnter:()=>o(t),onClick:()=>u(e),children:[(0,N.jsx)(`span`,{className:`search-kind`,children:e.kind}),(0,N.jsxs)(`span`,{className:`search-text`,children:[(0,N.jsx)(`strong`,{children:e.title}),(0,N.jsx)(`span`,{className:`dim`,children:e.detail})]}),(0,N.jsx)(P,{name:`arrow`,size:14})]})},e.id))]})]})}),document.body)]})}var Rr={light:`sun`,dark:`moon`,system:`monitor`},zr={light:`Light`,dark:`Dark`,system:`System`},Br=[{id:`study`,label:`Study`,route:null},{id:`tools`,label:`Tools`,route:`tools`},{id:`apps`,label:`Apps`,route:`apps`},{id:`articles`,label:`Articles`,route:`articles`}];function Vr(e){let t=(0,_.useRef)(null),[n,r]=(0,_.useState)(null);return(0,_.useLayoutEffect)(()=>{let e=()=>{let e=t.current?.querySelector(`[data-active="true"]`);e&&r({"--tab-x":`${e.offsetLeft}px`,"--tab-w":`${e.offsetWidth}px`,"--tab-ready":1})};e();let n=typeof ResizeObserver<`u`?new ResizeObserver(e):null;return t.current&&n?.observe(t.current),document.fonts?.ready.then(e).catch(()=>{}),window.addEventListener(`resize`,e),()=>{n?.disconnect(),window.removeEventListener(`resize`,e)}},[e]),[t,n]}function Hr({theme:e,onCycleTheme:t,onSupport:n,tab:r,onNavigate:i,scrolled:a,hidden:o}){let[s,c]=Vr(r),[l,u]=(0,_.useState)(0);return(0,_.useEffect)(()=>u(e=>e+1),[e]),(0,N.jsxs)(`header`,{className:`site-header ${a?`is-scrolled`:``} ${o?`is-hidden`:``}`,children:[(0,N.jsxs)(`button`,{type:`button`,className:`brand`,onClick:()=>{i(null),window.scrollTo({top:0})},children:[(0,N.jsx)(`span`,{className:`brand-mark`,"aria-hidden":`true`,children:(0,N.jsx)(P,{name:`moon`,size:17})}),(0,N.jsxs)(`span`,{className:`brand-text`,children:[`After Hours`,(0,N.jsx)(`span`,{className:`brand-dot`,children:`.`})]})]}),(0,N.jsx)(`nav`,{className:`tabs`,"aria-label":`Sections`,ref:s,style:c??void 0,children:Br.map(e=>(0,N.jsx)(`button`,{type:`button`,className:`tab ${r===e.id?`is-active`:``}`,"data-active":r===e.id,onClick:()=>i(e.route),"aria-current":r===e.id?`page`:void 0,children:e.label},e.id))}),(0,N.jsxs)(`nav`,{className:`header-actions`,children:[(0,N.jsx)(Lr,{onNavigate:i}),(0,N.jsxs)(`a`,{className:`btn btn-ghost hide-sm`,href:`https://github.com/${te}`,target:`_blank`,rel:`noreferrer`,children:[(0,N.jsx)(P,{name:`github`,size:16}),(0,N.jsx)(`span`,{children:`GitHub`})]}),(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-coffee`,onClick:n,children:[(0,N.jsx)(P,{name:`coffee`,size:16}),(0,N.jsx)(`span`,{className:`hide-sm`,children:`Buy me a coffee`})]}),(0,N.jsx)(`button`,{type:`button`,className:`btn btn-icon`,onClick:t,title:`Theme: ${zr[e]} — click to change`,"aria-label":`Theme: ${zr[e]}. Click to change.`,children:(0,N.jsx)(`span`,{className:`theme-icon`,children:(0,N.jsx)(P,{name:Rr[e],size:17})},l)})]})]})}function Ur({total:e,platforms:t,guides:n,onArticles:r}){return(0,N.jsxs)(`section`,{className:`hero`,id:`top`,children:[(0,N.jsxs)(`p`,{className:`eyebrow`,children:[(0,N.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`Free, no accounts, no telemetry`]}),(0,N.jsxs)(`h1`,{children:[`Small desktop tools that`,(0,N.jsx)(`br`,{}),(0,N.jsx)(`span`,{className:`grad`,children:`do one thing well.`})]}),(0,N.jsx)(`p`,{className:`lede`,children:`Windows utilities built after hours — grab a build, free and with nothing to sign up for. The repositories are private, so ask if you want to read the source. Everything here runs entirely on your machine.`}),(0,N.jsx)(`ul`,{className:`hero-stats`,children:[[e,`apps`],[n,`guides`],[t,`platforms`],[0,`trackers`]].map(([e,t],n)=>(0,N.jsxs)(`li`,{style:{"--i":n},children:[(0,N.jsx)(`strong`,{children:e}),(0,N.jsx)(`span`,{children:t})]},t))}),(0,N.jsxs)(`p`,{className:`hero-aside`,children:[`There are also`,` `,(0,N.jsxs)(`button`,{type:`button`,className:`link-button`,onClick:r,children:[n,` written guides`]}),` `,`— Windows fixes and Raspberry Pi setups, start to finish, nothing to download.`]}),(0,N.jsxs)(`div`,{className:`hero-notes`,children:[(0,N.jsxs)(`span`,{children:[(0,N.jsx)(P,{name:`shield`,size:14}),` No telemetry`]}),(0,N.jsxs)(`span`,{children:[(0,N.jsx)(P,{name:`github`,size:14}),` Source on request`]}),(0,N.jsxs)(`span`,{children:[(0,N.jsx)(P,{name:`download`,size:14}),` Portable builds`]})]})]})}var Wr=(e,t,n)=>{if(!t.length)return!0;let r=n.get(e.slug);return t.every(e=>r.includes(e))};function Gr({topic:e,onOpen:t}){return(0,N.jsx)(`span`,{className:`part-links`,children:e.parts.map(e=>(0,N.jsx)(`button`,{type:`button`,className:`part-link is-${e.id}`,onClick:n=>{n.stopPropagation(),t(e.route)},children:e.label},e.id))})}function Kr({subject:e,terms:t,haystacks:n,onOpen:r}){let i=(0,_.useMemo)(()=>Ar(e).map(e=>({...e,topics:e.topics.filter(e=>Wr(e,t,n))})).filter(e=>e.topics.length>0),[e,t,n]),a=i.reduce((e,t)=>e+t.topics.length,0);if(a===0)return null;let o=0;return(0,N.jsxs)(`section`,{className:`syllabus`,"aria-label":`${e} contents`,children:[(0,N.jsxs)(`header`,{className:`syllabus-head`,children:[(0,N.jsx)(`h2`,{children:e}),(0,N.jsxs)(`span`,{className:`dim`,children:[a,` `,a===1?`topic`:`topics`,`, in order — notes, advanced and interview questions for each`]})]}),i.map(e=>(0,N.jsxs)(Ee,{className:`syllabus-stage`,variant:`blur`,children:[(0,N.jsxs)(`h3`,{children:[(0,N.jsx)(`span`,{className:`stage-dot`,"aria-hidden":`true`}),e.stage]}),(0,N.jsx)(`ol`,{children:e.topics.map((e,t)=>{o+=1;let n=e.chapter??o;return(0,N.jsx)(`li`,{style:{"--accent":e.accent,"--i":t},children:(0,N.jsxs)(`div`,{className:`syllabus-item`,children:[(0,N.jsx)(`span`,{className:`syllabus-number`,children:e.chapter===null?`••`:String(n).padStart(2,`0`)}),(0,N.jsxs)(`span`,{className:`syllabus-text`,children:[(0,N.jsx)(`button`,{type:`button`,className:`syllabus-title`,onClick:()=>r(e.route),children:e.title}),(0,N.jsx)(`span`,{className:`dim`,children:e.tagline??e.summary}),(0,N.jsx)(Gr,{topic:e,onOpen:r})]}),(0,N.jsxs)(`span`,{className:`syllabus-minutes dim`,children:[Or(e),` min`]})]})},e.slug)})})]},e.stage))]})}function qr({topic:e,onOpen:t}){let n=ve();return(0,N.jsxs)(`article`,{className:`card article-card`,style:{"--accent":e.accent},onClick:()=>t(e.route),...n,children:[(0,N.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,N.jsxs)(`header`,{className:`article-card-head`,children:[(0,N.jsxs)(`span`,{className:`badge badge-article`,children:[e.subject,` · `,e.stage]}),(0,N.jsxs)(`span`,{className:`dim`,children:[Or(e),` min`]})]}),(0,N.jsx)(`h3`,{children:(0,N.jsxs)(`button`,{type:`button`,className:`card-link`,onClick:n=>{n.stopPropagation(),t(e.route)},children:[e.chapter?`${e.chapter}. `:``,e.title]})}),e.tagline&&(0,N.jsx)(`p`,{className:`tagline`,children:e.tagline}),(0,N.jsx)(`p`,{className:`card-summary`,children:e.summary}),(0,N.jsx)(`footer`,{className:`card-foot`,children:(0,N.jsx)(Gr,{topic:e,onOpen:t})})]})}function Jr({query:e,onQuery:t,subject:n,onSubject:r,onOpen:i}){let a=(0,_.useMemo)(()=>e.trim().toLowerCase().split(/\s+/).filter(Boolean),[e]),o=(0,_.useMemo)(()=>new Map(Er.map(e=>[e.slug,Dr(e)])),[]),s=(0,_.useMemo)(()=>Er.filter(e=>Wr(e,a,o)),[a,o]);return(0,N.jsxs)(N.Fragment,{children:[(0,N.jsxs)(`section`,{className:`page-head`,children:[(0,N.jsxs)(`p`,{className:`eyebrow`,children:[(0,N.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`Java notes and interview prep`]}),(0,N.jsxs)(`h1`,{children:[`Learn it, revise it,`,(0,N.jsx)(`br`,{}),(0,N.jsx)(`span`,{className:`grad`,children:`then get asked about it.`})]}),(0,N.jsxs)(`p`,{className:`lede`,children:[`Every topic comes in three passes. `,(0,N.jsx)(`strong`,{children:`Notes`}),` explains it from nothing. `,(0,N.jsx)(`strong`,{children:`Advanced`}),` is the layer underneath — the mechanisms, the flags, the things you need when something is behaving strangely. `,(0,N.jsx)(`strong`,{children:`Interview`}),` is the same ground as questions with full answers, graded easy to hard. Jump straight to any of them from here, or switch between them once a topic is open.`]})]}),(0,N.jsxs)(`div`,{className:`toolbar`,children:[(0,N.jsx)(`div`,{className:`toolbar-row`,children:(0,N.jsxs)(`div`,{className:`search`,children:[(0,N.jsx)(P,{name:`search`,size:17,className:`search-icon`}),(0,N.jsx)(`input`,{type:`search`,value:e,placeholder:`Search topics, questions, code…`,onChange:e=>t(e.target.value),"aria-label":`Search study notes`})]})}),(0,N.jsxs)(`div`,{className:`chips`,children:[(0,N.jsx)(`button`,{type:`button`,className:`chip ${n?``:`is-active`}`,onClick:()=>r(null),children:`All`}),kr.map(e=>(0,N.jsx)(`button`,{type:`button`,className:`chip ${n===e?`is-active`:``}`,onClick:()=>r(n===e?null:e),"aria-pressed":n===e,children:e},e)),(0,N.jsxs)(`span`,{className:`chips-count`,children:[s.length,` `,s.length===1?`topic`:`topics`]})]})]}),s.length===0?(0,N.jsxs)(`div`,{className:`empty empty-in`,children:[(0,N.jsx)(P,{name:`search`,size:28}),(0,N.jsx)(`h3`,{children:`Nothing matches that`}),(0,N.jsx)(`p`,{children:`Try a different word, or clear the filter.`}),(0,N.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>{t(``),r(null)},children:`Clear filters`})]}):n?(0,N.jsx)(Kr,{subject:n,terms:a,haystacks:o,onOpen:i}):(0,N.jsx)(`div`,{className:`catalog catalog-grid`,children:s.map((e,t)=>(0,N.jsx)(Ee,{className:`reveal-cell`,delay:he(t),children:(0,N.jsx)(qr,{topic:e,onOpen:i})},e.slug))})]})}var Yr=e=>e.split(/\s+/).slice(0,2).map(e=>e[0]).join(``);function Xr({project:e,size:t=52}){let[n,r]=(0,_.useState)(!1),i={"--size":`${t}px`,"--accent":e.accent};return e.icon&&!n?(0,N.jsx)(`span`,{className:`app-icon`,style:i,children:(0,N.jsx)(`img`,{src:e.icon,alt:``,onError:()=>r(!0),loading:`lazy`})}):(0,N.jsx)(`span`,{className:`app-icon app-icon-mono`,style:i,"aria-hidden":`true`,children:Yr(e.name)})}function Zr({project:e,view:t,isFavorite:n,onToggleFavorite:r,onOpen:i}){let a=ve();return(0,N.jsxs)(`article`,{className:`card card-${t}`,style:{"--accent":e.accent},onClick:()=>i(e.id),...a,children:[(0,N.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,N.jsx)(`button`,{type:`button`,className:`fav ${n?`is-active`:``}`,onClick:t=>{t.stopPropagation(),r(e.id)},"aria-pressed":n,"aria-label":`${n?`Remove`:`Add`} ${e.name} ${n?`from`:`to`} favourites`,children:(0,N.jsx)(P,{name:`star`,size:15})}),(0,N.jsxs)(`header`,{className:`card-head`,children:[(0,N.jsx)(Xr,{project:e,size:t===`list`?44:54}),(0,N.jsxs)(`div`,{className:`card-title`,children:[(0,N.jsx)(`h3`,{children:(0,N.jsx)(`button`,{type:`button`,className:`card-link`,onClick:t=>{t.stopPropagation(),i(e.id)},children:e.name})}),(0,N.jsx)(`p`,{className:`tagline`,children:e.tagline})]})]}),(0,N.jsx)(`p`,{className:`card-summary`,children:e.summary}),(0,N.jsx)(`ul`,{className:`tag-row`,children:e.tags.slice(0,t===`list`?5:3).map(e=>(0,N.jsx)(`li`,{children:e},e))}),(0,N.jsxs)(`footer`,{className:`card-foot`,children:[(0,N.jsxs)(`div`,{className:`meta`,children:[(0,N.jsx)(`span`,{className:`badge badge-${e.status}`,children:ie[e.status]}),(0,N.jsxs)(`span`,{className:`dim`,children:[`v`,e.version]}),(0,N.jsx)(`span`,{className:`dim hide-sm`,children:e.size})]}),(0,N.jsxs)(`div`,{className:`card-actions`,children:[(0,N.jsxs)(`a`,{className:`btn btn-ghost btn-sm`,href:e.url,target:`_blank`,rel:`noreferrer`,onClick:e=>e.stopPropagation(),"aria-label":`${e.name} source on GitHub`,children:[(0,N.jsx)(P,{name:`github`,size:15}),(0,N.jsx)(`span`,{className:`hide-sm`,children:`Source`})]}),(0,N.jsxs)(`a`,{className:`btn btn-primary btn-sm`,href:e.downloadUrl,target:`_blank`,rel:`noreferrer`,onClick:e=>e.stopPropagation(),children:[(0,N.jsx)(P,{name:`download`,size:15}),(0,N.jsx)(`span`,{children:`Download`})]})]})]})]})}function Qr({value:e,label:t=`Copy`,className:n=``}){let[r,i]=(0,_.useState)(!1),a=(0,_.useRef)(null);return(0,_.useEffect)(()=>()=>clearTimeout(a.current),[]),(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-ghost ${r?`copy-pop is-copied`:``} ${n}`,onClick:async()=>{await Gn(e),i(!0),clearTimeout(a.current),a.current=setTimeout(()=>i(!1),2e3)},"aria-label":r?`Copied`:t||`Copy`,children:[(0,N.jsx)(P,{name:r?`check`:`copy`,size:15}),(0,N.jsx)(`span`,{children:r?`Copied`:t})]})}var $r=`a[href], button:not([disabled]), input, select, [tabindex]:not([tabindex="-1"])`,ei=240;function ti({onClose:e,labelledBy:t,className:n=``,style:r,children:i}){let a=(0,_.useRef)(null),o=(0,_.useRef)(null),s=(0,_.useRef)(!1),[c,l]=(0,_.useState)(`out`),u=(0,_.useCallback)(()=>{s.current||(s.current=!0,l(`out`),setTimeout(e,ei))},[e]);return(0,_.useEffect)(()=>{o.current??=document.activeElement,document.body.classList.add(`is-locked`),a.current?.focus();let e=requestAnimationFrame(()=>l(`in`)),t=e=>{if(e.key===`Escape`){u();return}if(e.key!==`Tab`)return;let t=a.current?.querySelectorAll($r);if(!t?.length)return;let n=t[0],r=t[t.length-1];e.shiftKey&&document.activeElement===n?(e.preventDefault(),r.focus()):!e.shiftKey&&document.activeElement===r&&(e.preventDefault(),n.focus())};return window.addEventListener(`keydown`,t),()=>{cancelAnimationFrame(e),window.removeEventListener(`keydown`,t),document.body.classList.remove(`is-locked`),o.current?.focus?.()}},[u]),(0,N.jsx)(`div`,{className:`overlay is-${c}`,onMouseDown:e=>e.target===e.currentTarget&&u(),children:(0,N.jsxs)(`div`,{className:`sheet ${n}`,style:r,role:`dialog`,"aria-modal":`true`,"aria-labelledby":t,tabIndex:-1,ref:a,children:[(0,N.jsx)(`button`,{type:`button`,className:`sheet-close btn btn-icon`,onClick:u,"aria-label":`Close`,children:(0,N.jsx)(P,{name:`close`,size:18})}),i]})})}function ni({project:e,onClose:t,isFavorite:n,onToggleFavorite:r}){return(0,N.jsx)(ti,{onClose:t,labelledBy:`sheet-title`,style:{"--accent":e.accent},children:(0,N.jsxs)(N.Fragment,{children:[(0,N.jsxs)(`div`,{className:`sheet-hero`,children:[(0,N.jsx)(Xr,{project:e,size:72}),(0,N.jsxs)(`div`,{children:[(0,N.jsxs)(`div`,{className:`sheet-badges`,children:[(0,N.jsx)(`span`,{className:`badge badge-${e.status}`,children:ie[e.status]}),(0,N.jsxs)(`span`,{className:`dim`,children:[`v`,e.version]}),(0,N.jsxs)(`span`,{className:`dim`,children:[`Updated `,xe(e.updated)]})]}),(0,N.jsx)(`h2`,{id:`sheet-title`,children:e.name}),(0,N.jsx)(`p`,{className:`tagline`,children:e.tagline})]})]}),(0,N.jsx)(`p`,{className:`sheet-summary`,children:e.summary}),(0,N.jsxs)(`div`,{className:`sheet-cta`,children:[(0,N.jsxs)(`a`,{className:`btn btn-primary`,href:e.downloadUrl,target:`_blank`,rel:`noreferrer`,children:[(0,N.jsx)(P,{name:`download`,size:16}),(0,N.jsx)(`span`,{children:`Download for Windows`})]}),(0,N.jsxs)(`a`,{className:`btn btn-ghost`,href:e.url,target:`_blank`,rel:`noreferrer`,children:[(0,N.jsx)(P,{name:`github`,size:16}),(0,N.jsx)(`span`,{children:`Repository`})]}),(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-ghost ${n?`is-active`:``}`,onClick:()=>r(e.id),"aria-pressed":n,children:[(0,N.jsx)(P,{name:`star`,size:16}),(0,N.jsx)(`span`,{children:n?`Saved`:`Save`})]})]}),(0,N.jsx)(`p`,{className:`hint`,children:`Downloads are hosted on MediaFire as a portable zip — unpack it anywhere and run the app, no installer. Windows SmartScreen may warn on first run because the binaries are unsigned.`}),(0,N.jsxs)(`p`,{className:`hint`,children:[`The repository itself is private, so the source and issue links open only for accounts that have been granted access.`,` `,(0,N.jsx)(`a`,{href:`https://github.com/${te}`,target:`_blank`,rel:`noreferrer`,children:`Ask for access`}),` `,`and mention which app you need.`]}),(0,N.jsxs)(`div`,{className:`sheet-grid`,children:[(0,N.jsxs)(`section`,{children:[(0,N.jsxs)(`h3`,{children:[(0,N.jsx)(P,{name:`bolt`,size:15}),` What it does`]}),(0,N.jsx)(`ul`,{className:`bullets`,children:e.highlights.map((e,t)=>(0,N.jsx)(`li`,{style:{"--i":t},children:e},e))})]}),(0,N.jsxs)(`div`,{className:`sheet-side`,children:[(0,N.jsxs)(`section`,{children:[(0,N.jsxs)(`h3`,{children:[(0,N.jsx)(P,{name:`monitor`,size:15}),` Requirements`]}),(0,N.jsx)(`ul`,{className:`bullets`,children:e.requirements.map((e,t)=>(0,N.jsx)(`li`,{style:{"--i":t},children:e},e))})]}),(0,N.jsxs)(`section`,{children:[(0,N.jsxs)(`h3`,{children:[(0,N.jsx)(P,{name:`shield`,size:15}),` Privacy`]}),(0,N.jsx)(`p`,{className:`muted`,children:e.privacy})]}),(0,N.jsxs)(`section`,{children:[(0,N.jsxs)(`h3`,{children:[(0,N.jsx)(P,{name:`terminal`,size:15}),` Build from source`]}),(0,N.jsxs)(`div`,{className:`code`,children:[(0,N.jsx)(`code`,{children:e.cloneCommand}),(0,N.jsx)(Qr,{value:e.cloneCommand,label:``,className:`btn-sm`})]}),(0,N.jsxs)(`p`,{className:`muted small`,children:[`Then `,(0,N.jsx)(`code`,{children:`npm install`}),` and `,(0,N.jsx)(`code`,{children:`npm start`}),`. Cloning needs repository access and a signed-in Git credential.`]})]})]})]}),(0,N.jsxs)(`footer`,{className:`sheet-foot`,children:[(0,N.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,N.jsx)(`li`,{children:e},e))}),(0,N.jsxs)(`div`,{className:`sheet-foot-links`,children:[(0,N.jsxs)(`a`,{href:e.issuesUrl,target:`_blank`,rel:`noreferrer`,children:[(0,N.jsx)(P,{name:`link`,size:14}),` Report an issue`]}),(0,N.jsxs)(`span`,{className:`dim`,children:[e.license,` licensed`]})]})]})]})})}function ri(){return ye(),(0,N.jsx)(`div`,{className:`scroll-progress`,"aria-hidden":`true`})}var ii=`itsadityakr27-8@okicici`,ai=`Aditya Kumar`,oi=`upi://pay?pa=${ii}&pn=${encodeURIComponent(ai)}&cu=INR`,si=`./upi-qr.svg`,ci=[49,99,199],li=e=>`${oi}&am=${e.toFixed(2)}`;function ui({onClose:e}){return(0,N.jsx)(ti,{onClose:e,labelledBy:`support-title`,className:`sheet-sm`,children:(0,N.jsxs)(`div`,{className:`support`,children:[(0,N.jsx)(`span`,{className:`support-mark`,"aria-hidden":`true`,children:(0,N.jsx)(P,{name:`coffee`,size:22})}),(0,N.jsx)(`h2`,{id:`support-title`,children:`Buy me a coffee`}),(0,N.jsx)(`p`,{className:`muted`,children:`If something here saved you some time, a coffee is a nice way to say so.`}),(0,N.jsxs)(`figure`,{className:`qr`,children:[(0,N.jsx)(`img`,{src:si,alt:`UPI QR code for ${ii}`,width:`220`,height:`220`}),(0,N.jsx)(`figcaption`,{children:`Scan with GPay, PhonePe, Paytm, BHIM — any UPI app`})]}),(0,N.jsxs)(`div`,{className:`upi-id`,children:[(0,N.jsx)(`code`,{children:ii}),(0,N.jsx)(Qr,{value:ii,label:`Copy UPI ID`,className:`btn-sm`})]}),(0,N.jsxs)(`p`,{className:`support-payee dim`,children:[`Payments go to `,ai]}),(0,N.jsxs)(`div`,{className:`support-amounts`,children:[ci.map(e=>(0,N.jsxs)(`a`,{className:`btn btn-ghost btn-sm`,href:li(e),children:[`₹`,e]},e)),(0,N.jsxs)(`a`,{className:`btn btn-primary btn-sm`,href:oi,children:[(0,N.jsx)(P,{name:`coffee`,size:15}),(0,N.jsx)(`span`,{children:`Open UPI app`})]})]})]})})}var di=[{id:`recent`,label:`Recently updated`},{id:`name`,label:`Name (A–Z)`},{id:`status`,label:`Stable first`}];function fi({query:e,onQuery:t,activeTags:n,onToggleTag:r,onClearTags:i,sort:a,onSort:o,view:s,onView:c,favoritesOnly:l,onFavoritesOnly:u,favoriteCount:d,count:f}){let p=(0,_.useRef)(null);return(0,_.useEffect)(()=>{let e=e=>{let n=/^(INPUT|TEXTAREA|SELECT)$/.test(e.target.tagName);e.key===`/`&&!n?(e.preventDefault(),p.current?.focus()):e.key===`Escape`&&e.target===p.current&&(t(``),p.current.blur())};return window.addEventListener(`keydown`,e),()=>window.removeEventListener(`keydown`,e)},[t]),(0,N.jsxs)(`div`,{className:`toolbar`,children:[(0,N.jsxs)(`div`,{className:`toolbar-row`,children:[(0,N.jsxs)(`div`,{className:`search`,children:[(0,N.jsx)(P,{name:`search`,size:17,className:`search-icon`}),(0,N.jsx)(`input`,{ref:p,type:`search`,value:e,placeholder:`Search apps, tags, features…`,onChange:e=>t(e.target.value),"aria-label":`Search apps`}),(0,N.jsx)(`kbd`,{className:`search-kbd`,children:`/`})]}),(0,N.jsxs)(`div`,{className:`toolbar-controls`,children:[(0,N.jsxs)(`label`,{className:`select`,children:[(0,N.jsx)(`span`,{className:`sr-only`,children:`Sort by`}),(0,N.jsx)(`select`,{value:a,onChange:e=>o(e.target.value),children:di.map(e=>(0,N.jsx)(`option`,{value:e.id,children:e.label},e.id))})]}),(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-icon ${l?`is-active`:``}`,onClick:()=>u(e=>!e),title:`Show favourites only`,"aria-pressed":l,children:[(0,N.jsx)(P,{name:`star`,size:16}),d>0&&(0,N.jsx)(`span`,{className:`pip`,children:d})]}),(0,N.jsx)(`div`,{className:`segmented`,role:`group`,"aria-label":`Layout`,children:[`grid`,`list`].map(e=>(0,N.jsx)(`button`,{type:`button`,className:s===e?`is-active`:``,onClick:()=>c(e),"aria-pressed":s===e,title:`${e[0].toUpperCase()}${e.slice(1)} view`,children:(0,N.jsx)(P,{name:e,size:15})},e))})]})]}),(0,N.jsxs)(`div`,{className:`chips`,children:[(0,N.jsx)(`button`,{type:`button`,className:`chip ${n.length===0?`is-active`:``}`,onClick:i,children:`All`}),E.map(e=>(0,N.jsx)(`button`,{type:`button`,className:`chip ${n.includes(e)?`is-active`:``}`,onClick:()=>r(e),"aria-pressed":n.includes(e),children:e},e)),(0,N.jsxs)(`span`,{className:`chips-count`,children:[f,` `,f===1?`app`:`apps`]})]})]})}function pi({topic:e,part:t,onNavigate:n}){let r=(0,_.useMemo)(()=>Hn(t.content),[t]),i=Er.findIndex(t=>t.slug===e.slug),a=Er[i-1]??null,o=Er[i+1]??null;(0,_.useEffect)(()=>{window.scrollTo({top:0})},[e.slug,t.id]);let s=(e,t)=>{e.preventDefault(),document.getElementById(t)?.scrollIntoView({behavior:`smooth`,block:`start`})};return(0,N.jsxs)(`article`,{className:`article topic`,style:{"--accent":e.accent},children:[(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm back`,onClick:()=>n(`study`),children:[(0,N.jsx)(P,{name:`arrow`,size:15,className:`flip`}),(0,N.jsx)(`span`,{children:`All study notes`})]}),(0,N.jsxs)(`header`,{className:`article-head`,children:[(0,N.jsxs)(`div`,{className:`article-meta`,children:[(0,N.jsx)(`span`,{className:`badge badge-article`,children:e.subject}),(0,N.jsx)(`span`,{className:`dim`,children:e.stage}),(0,N.jsxs)(`span`,{className:`dim`,children:[t.minutes,` min read`]})]}),(0,N.jsxs)(`h1`,{children:[e.chapter?(0,N.jsx)(`span`,{className:`chapter-number`,children:e.chapter}):null,e.title]}),(0,N.jsx)(`p`,{className:`lede`,children:e.summary})]}),(0,nr.createPortal)((0,N.jsxs)(`nav`,{className:`part-switch`,"aria-label":`Depth`,children:[(0,N.jsx)(`span`,{className:`part-switch-label dim`,children:e.title}),(0,N.jsx)(`div`,{className:`part-tabs`,children:e.parts.map(e=>(0,N.jsx)(`button`,{type:`button`,className:`part-tab ${e.id===t.id?`is-active`:``}`,onClick:()=>n(e.route),"aria-current":e.id===t.id?`page`:void 0,title:e.blurb,children:e.label},e.id))})]}),document.body),(0,N.jsxs)(`div`,{className:`article-body ${r.length>2?`has-toc`:``}`,children:[r.length>2&&(0,N.jsxs)(`aside`,{className:`article-toc`,"aria-label":`On this page`,children:[(0,N.jsx)(`h2`,{children:`On this page`}),(0,N.jsx)(`ol`,{children:r.map(e=>(0,N.jsx)(`li`,{children:(0,N.jsx)(`a`,{href:`#${e.id}`,onClick:t=>s(t,e.id),children:e.text})},e.id))})]}),(0,N.jsx)($n,{content:t.content,variant:t.id})]}),(0,N.jsxs)(`footer`,{className:`article-foot`,children:[(0,N.jsx)(`p`,{className:`hint`,children:`Written for this site rather than copied from a repository. Every program above was compiled and run before it was printed, and the output shown is what it produced — on one machine, with one JDK. Yours may differ in the numbers; it should not differ in the shape.`}),(0,N.jsxs)(`nav`,{className:`article-nav`,children:[a?(0,N.jsxs)(`button`,{type:`button`,className:`article-nav-link`,onClick:()=>n(a.route),children:[(0,N.jsx)(`span`,{className:`dim`,children:`Previous`}),(0,N.jsx)(`strong`,{children:a.title})]}):(0,N.jsx)(`span`,{}),o&&(0,N.jsxs)(`button`,{type:`button`,className:`article-nav-link is-next`,onClick:()=>n(o.route),children:[(0,N.jsx)(`span`,{className:`dim`,children:`Next`}),(0,N.jsx)(`strong`,{children:o.title})]})]})]})]})}var mi=(e,t=0,n=1)=>Math.min(n,Math.max(t,e)),hi=(e,t=2)=>Number(e.toFixed(t)),L=({r:e,g:t,b:n,a:r=1},i=!1)=>{let a=e=>Math.round(mi(e,0,255)).toString(16).padStart(2,`0`),o=`#${a(e)}${a(t)}${a(n)}`;return r<1||i?`${o}${a(r*255)}`:o},gi=e=>{let t=e.trim().replace(/^#/,``);if((t.length===3||t.length===4)&&(t=[...t].map(e=>e+e).join(``)),t.length!==6&&t.length!==8||!/^[0-9a-f]+$/i.test(t))return null;let n=e=>parseInt(t.slice(e,e+2),16);return{r:n(0),g:n(2),b:n(4),a:t.length===8?n(6)/255:1}};function _i({r:e,g:t,b:n,a:r=1}){let[i,a,o]=[e/255,t/255,n/255],s=Math.max(i,a,o),c=Math.min(i,a,o),l=s-c,u=(s+c)/2,d=0;l!==0&&(d=s===i?(a-o)/l%6:s===a?(o-i)/l+2:(i-a)/l+4,d*=60,d<0&&(d+=360));let f=l===0?0:l/(1-Math.abs(2*u-1));return{h:d,s:f*100,l:u*100,a:r}}function vi({h:e,s:t,l:n,a:r=1}){let i=t/100,a=n/100,o=(1-Math.abs(2*a-1))*i,s=(e%360+360)%360/60,c=o*(1-Math.abs(s%2-1)),[l,u,d]=s<1?[o,c,0]:s<2?[c,o,0]:s<3?[0,o,c]:s<4?[0,c,o]:s<5?[c,0,o]:[o,0,c],f=a-o/2;return{r:(l+f)*255,g:(u+f)*255,b:(d+f)*255,a:r}}function yi({r:e,g:t,b:n,a:r=1}){let[i,a,o]=[e/255,t/255,n/255],s=Math.max(i,a,o),c=s-Math.min(i,a,o),l=0;return c!==0&&(l=s===i?(a-o)/c%6:s===a?(o-i)/c+2:(i-a)/c+4,l*=60,l<0&&(l+=360)),{h:l,s:(s===0?0:c/s)*100,v:s*100,a:r}}function bi({h:e,s:t,v:n,a:r=1}){let i=t/100,a=n/100,o=a*i,s=(e%360+360)%360/60,c=o*(1-Math.abs(s%2-1)),[l,u,d]=s<1?[o,c,0]:s<2?[c,o,0]:s<3?[0,o,c]:s<4?[0,c,o]:s<5?[c,0,o]:[o,0,c],f=a-o;return{r:(l+f)*255,g:(u+f)*255,b:(d+f)*255,a:r}}var xi=e=>e<=.04045?e/12.92:((e+.055)/1.055)**2.4,Si=e=>e<=.0031308?e*12.92:1.055*e**(1/2.4)-.055;function Ci({r:e,g:t,b:n,a:r=1}){let i=xi(e/255),a=xi(t/255),o=xi(n/255),s=Math.cbrt(.4122214708*i+.5363325363*a+.0514459929*o),c=Math.cbrt(.2119034982*i+.6806995451*a+.1073969566*o),l=Math.cbrt(.0883024619*i+.2817188376*a+.6299787005*o),u=.2104542553*s+.793617785*c-.0040720468*l,d=1.9779984951*s-2.428592205*c+.4505937099*l,f=.0259040371*s+.7827717662*c-.808675766*l,p=Math.sqrt(d*d+f*f),m=Math.atan2(f,d)*180/Math.PI;return m<0&&(m+=360),{l:u*100,c:p,h:p<1e-6?0:m,a:r}}function wi({l:e,c:t,h:n,a:r=1}){let i=e/100,a=n*Math.PI/180,o=t*Math.cos(a),s=t*Math.sin(a),c=(i+.3963377774*o+.2158037573*s)**3,l=(i-.1055613458*o-.0638541728*s)**3,u=(i-.0894841775*o-1.291485548*s)**3,d=4.0767416621*c-3.3077115913*l+.2309699292*u,f=-1.2684380046*c+2.6097574011*l-.3413193965*u,p=-.0041960863*c-.7034186147*l+1.707614701*u;return{r:mi(Si(d),0,1)*255,g:mi(Si(f),0,1)*255,b:mi(Si(p),0,1)*255,a:r}}var Ti=({l:e,c:t,h:n})=>{let r=e/100,i=n*Math.PI/180,a=t*Math.cos(i),o=t*Math.sin(i),s=(r+.3963377774*a+.2158037573*o)**3,c=(r-.1055613458*a-.0638541728*o)**3,l=(r-.0894841775*a-1.291485548*o)**3;return[4.0767416621*s-3.3077115913*c+.2309699292*l,-1.2684380046*s+2.6097574011*c-.3413193965*l,-.0041960863*s-.7034186147*c+1.707614701*l].every(e=>e>=-1e-4&&e<=1.0001)},Ei=({r:e,g:t,b:n})=>.2126*xi(e/255)+.7152*xi(t/255)+.0722*xi(n/255);function Di(e,t){let n=Ei(e),r=Ei(t),[i,a]=n>r?[n,r]:[r,n];return(i+.05)/(a+.05)}var Oi=e=>({aaBody:e>=4.5,aaLarge:e>=3,aaaBody:e>=7,aaaLarge:e>=4.5}),ki={black:`#000000`,white:`#ffffff`,red:`#ff0000`,lime:`#00ff00`,blue:`#0000ff`,yellow:`#ffff00`,cyan:`#00ffff`,magenta:`#ff00ff`,silver:`#c0c0c0`,gray:`#808080`,grey:`#808080`,maroon:`#800000`,olive:`#808000`,green:`#008000`,purple:`#800080`,teal:`#008080`,navy:`#000080`,orange:`#ffa500`,pink:`#ffc0cb`,brown:`#a52a2a`,gold:`#ffd700`,indigo:`#4b0082`,violet:`#ee82ee`,tomato:`#ff6347`,salmon:`#fa8072`,crimson:`#dc143c`,coral:`#ff7f50`,khaki:`#f0e68c`,plum:`#dda0dd`,orchid:`#da70d6`,turquoise:`#40e0d0`,tan:`#d2b48c`,beige:`#f5f5dc`,ivory:`#fffff0`,lavender:`#e6e6fa`,transparent:`#00000000`},Ai=e=>[...e.matchAll(/-?[\d.]+/g)].map(e=>Number(e[0]));function ji(e){if(!e)return null;let t=String(e).trim().toLowerCase();if(ki[t])return gi(ki[t]);if(t.startsWith(`#`))return gi(t);if(t.startsWith(`rgb`)){let[e,n,r,i]=Ai(t);if([e,n,r].some(e=>e===void 0))return null;let a=(t.match(/%/g)??[]).length>=3;return{r:a?e/100*255:e,g:a?n/100*255:n,b:a?r/100*255:r,a:i===void 0?1:t.includes(`%`)&&i>1?i/100:i}}if(t.startsWith(`hsl`)){let[e,n,r,i]=Ai(t);return[e,n,r].some(e=>e===void 0)?null:vi({h:e,s:n,l:r,a:i===void 0?1:i>1?i/100:i})}if(t.startsWith(`hsb`)||t.startsWith(`hsv`)){let[e,n,r,i]=Ai(t);return[e,n,r].some(e=>e===void 0)?null:bi({h:e,s:n,v:r,a:i===void 0?1:i>1?i/100:i})}if(t.startsWith(`oklch`)){let[e,n,r,i]=Ai(t);return[e,n,r].some(e=>e===void 0)?null:wi({l:t.includes(`%`)||e>1?e:e*100,c:n,h:r,a:i===void 0?1:i>1?i/100:i})}return/^[0-9a-f]{3,8}$/.test(t)?gi(t):null}function Mi(e){let t=_i(e),n=yi(e),r=Ci(e),i=e=>Math.round(e),a=e.a??1;return[{label:`HEX`,value:L(e)},{label:`HEX 8`,value:L(e,!0)},{label:`RGB`,value:a<1?`rgb(${i(e.r)} ${i(e.g)} ${i(e.b)} / ${hi(a)})`:`rgb(${i(e.r)} ${i(e.g)} ${i(e.b)})`},{label:`HSL`,value:a<1?`hsl(${i(t.h)} ${i(t.s)}% ${i(t.l)}% / ${hi(a)})`:`hsl(${i(t.h)} ${i(t.s)}% ${i(t.l)}%)`},{label:`HSB`,value:`hsb(${i(n.h)}, ${i(n.s)}%, ${i(n.v)}%)`},{label:`OKLCH`,value:a<1?`oklch(${hi(r.l,1)}% ${hi(r.c,3)} ${hi(r.h,1)} / ${hi(a)})`:`oklch(${hi(r.l,1)}% ${hi(r.c,3)} ${hi(r.h,1)})`}]}function Ni(e,t,n){if(Ti({l:e,c:t,h:n}))return t;let r=0,i=t;for(let t=0;t<18;t++){let t=(r+i)/2;Ti({l:e,c:t,h:n})?r=t:i=t}return r}function Pi(e){let t=Ci(e),n=[96,91,83,73,63,53,45,37,29,21,13],r=[50,100,200,300,400,500,600,700,800,900,950];return n.map((e,i)=>{let a=i/(n.length-1),o=.45+.55*Math.sin(Math.PI*a),s=Ni(e,t.c*o,t.h);return{name:r[i],rgb:wi({l:e,c:s,h:t.h,a:1})}})}var R=(e,t)=>{let{l:n,c:r,h:i,a}=Ci(e),o=(i+t+360)%360;return wi({l:n,c:Ni(n,r,o),h:o,a})},z=e=>[{name:`Complementary`,colors:[e,R(e,180)]},{name:`Analogous`,colors:[R(e,-30),e,R(e,30)]},{name:`Triadic`,colors:[e,R(e,120),R(e,240)]},{name:`Split complementary`,colors:[e,R(e,150),R(e,210)]},{name:`Tetradic`,colors:[e,R(e,90),R(e,180),R(e,270)]}];function Fi(e,t,n){let r=Ci(e),i=Ci(t),a=i.h-r.h;return a>180&&(a-=360),a<-180&&(a+=360),wi({l:r.l+(i.l-r.l)*n,c:r.c+(i.c-r.c)*n,h:r.h+a*n,a:(e.a??1)+((t.a??1)-(e.a??1))*n})}var Ii=()=>({r:Math.floor(Math.random()*256),g:Math.floor(Math.random()*256),b:Math.floor(Math.random()*256),a:1}),Li=({r:e,g:t,b:n,a:r=1})=>`rgba(${Math.round(e)}, ${Math.round(t)}, ${Math.round(n)}, ${r})`;function Ri({label:e,value:t,wide:n=!1}){let[r,i]=(0,_.useState)(!1);return(0,N.jsxs)(`button`,{type:`button`,className:`copyable ${n?`is-wide`:``} ${r?`is-copied`:``}`,onClick:async()=>{await Gn(t),i(!0),setTimeout(()=>i(!1),1400)},title:`Copy ${t}`,children:[(0,N.jsx)(`span`,{className:`copyable-label`,children:e}),(0,N.jsx)(`span`,{className:`copyable-value`,children:t}),(0,N.jsx)(P,{name:r?`check`:`copy`,size:13})]})}function zi({hsv:e,onChange:t}){let n=(0,_.useRef)(null),r=(0,_.useRef)(!1),i=(0,_.useCallback)(r=>{let i=n.current?.getBoundingClientRect();if(!i)return;let a=Math.min(1,Math.max(0,(r.clientX-i.left)/i.width)),o=Math.min(1,Math.max(0,(r.clientY-i.top)/i.height));t({...e,s:a*100,v:(1-o)*100})},[e,t]);return(0,_.useEffect)(()=>{let e=e=>r.current&&i(e),t=()=>{r.current=!1};return window.addEventListener(`pointermove`,e),window.addEventListener(`pointerup`,t),()=>{window.removeEventListener(`pointermove`,e),window.removeEventListener(`pointerup`,t)}},[i]),(0,N.jsxs)(`div`,{className:`picker`,children:[(0,N.jsx)(`div`,{ref:n,className:`picker-area`,style:{"--hue":`hsl(${e.h} 100% 50%)`},tabIndex:0,role:`slider`,"aria-label":`Saturation and brightness`,"aria-valuetext":`saturation ${Math.round(e.s)}%, brightness ${Math.round(e.v)}%`,onPointerDown:e=>{r.current=!0,e.currentTarget.focus(),i(e)},onKeyDown:n=>{let r=n.shiftKey?10:1,i={ArrowRight:{s:Math.min(100,e.s+r)},ArrowLeft:{s:Math.max(0,e.s-r)},ArrowUp:{v:Math.min(100,e.v+r)},ArrowDown:{v:Math.max(0,e.v-r)}};i[n.key]&&(n.preventDefault(),t({...e,...i[n.key]}))},children:(0,N.jsx)(`span`,{className:`picker-dot`,style:{left:`${e.s}%`,top:`${100-e.v}%`,background:L(bi({...e,a:1}))}})}),(0,N.jsxs)(`label`,{className:`picker-slider hue`,children:[(0,N.jsx)(`span`,{className:`sr-only`,children:`Hue`}),(0,N.jsx)(`input`,{type:`range`,min:0,max:360,step:1,value:Math.round(e.h),onChange:n=>t({...e,h:Number(n.target.value)})})]}),(0,N.jsxs)(`label`,{className:`picker-slider alpha`,style:{"--solid":L(bi({...e,a:1}))},children:[(0,N.jsx)(`span`,{className:`sr-only`,children:`Alpha`}),(0,N.jsx)(`input`,{type:`range`,min:0,max:100,step:1,value:Math.round((e.a??1)*100),onChange:n=>t({...e,a:Number(n.target.value)/100})})]})]})}function Bi({label:e,ratio:t}){let n=Oi(t);return(0,N.jsxs)(`div`,{className:`contrast-row`,children:[(0,N.jsx)(`span`,{children:e}),(0,N.jsx)(`strong`,{children:t.toFixed(2)}),(0,N.jsxs)(`span`,{className:`contrast-badges`,children:[(0,N.jsx)(`em`,{className:n.aaBody?`ok`:`no`,children:`AA`}),(0,N.jsx)(`em`,{className:n.aaaBody?`ok`:`no`,children:`AAA`}),(0,N.jsx)(`em`,{className:n.aaLarge?`ok`:`no`,children:`AA large`})]})]})}function Vi(){let[e,t]=(0,_.useState)(()=>yi({r:139,g:108,b:255,a:1})),[n,r]=(0,_.useState)(`#8B6CFF`),[i,a]=(0,_.useState)(!1),o=(0,_.useMemo)(()=>bi(e),[e]),s=(0,_.useMemo)(()=>Ci(o),[o]),c=(0,_.useMemo)(()=>Mi(o),[o]),l=(0,_.useMemo)(()=>Pi(o),[o]),u=(0,_.useMemo)(()=>z(o),[o]),d=Di(o,{r:255,g:255,b:255}),f=Di(o,{r:0,g:0,b:0});(0,_.useEffect)(()=>r(L(o)),[o]);let p=e=>{r(e);let n=ji(e);n?(a(!1),t(yi(n))):a(e.trim()!==``)},m=e=>t(yi(e));return(0,N.jsxs)(`div`,{className:`tool tool-color`,children:[(0,N.jsxs)(`section`,{className:`tool-panel`,children:[(0,N.jsx)(`h2`,{children:`Pick`}),(0,N.jsx)(zi,{hsv:e,onChange:t}),(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{htmlFor:`color-input`,children:`Any format`}),(0,N.jsxs)(`div`,{className:`field-control`,children:[(0,N.jsx)(`input`,{id:`color-input`,className:`selector-input ${i?`is-bad`:``}`,type:`text`,value:n,spellCheck:`false`,onChange:e=>p(e.target.value)}),(0,N.jsx)(`button`,{type:`button`,className:`icon-button`,onClick:()=>m(Ii()),title:`Random colour`,"aria-label":`Random colour`,children:(0,N.jsx)(P,{name:`bolt`,size:15})})]})]}),(0,N.jsxs)(`p`,{className:`tool-note`,children:[`Paste anything: `,(0,N.jsx)(`code`,{children:`#8b6cff`}),`, `,(0,N.jsx)(`code`,{children:`rgb(139 108 255)`}),`,`,` `,(0,N.jsx)(`code`,{children:`hsl(252 100% 71%)`}),`, `,(0,N.jsx)(`code`,{children:`oklch(62% 0.23 288)`}),`, or a name like `,(0,N.jsx)(`code`,{children:`tomato`}),`.`,!Ti(s)&&(0,N.jsxs)(N.Fragment,{children:[` `,`This OKLCH value sits outside sRGB, so the swatch is the nearest colour a normal screen can show.`]})]})]}),(0,N.jsxs)(`section`,{className:`tool-panel`,children:[(0,N.jsx)(`h2`,{children:`Values`}),(0,N.jsx)(`div`,{className:`color-preview`,style:{"--c":Li(o)},"aria-label":`Preview of ${L(o)}`,children:(0,N.jsx)(`span`,{style:{color:d>f?`#fff`:`#000`},children:`Aa`})}),(0,N.jsx)(`div`,{className:`copyables`,children:c.map(e=>(0,N.jsx)(Ri,{label:e.label,value:e.value},e.label))}),(0,N.jsx)(`h2`,{children:`Contrast`}),(0,N.jsx)(Bi,{label:`On white`,ratio:d}),(0,N.jsx)(Bi,{label:`On black`,ratio:f}),(0,N.jsxs)(`p`,{className:`tool-note`,children:[`WCAG 2 wants `,(0,N.jsx)(`strong`,{children:`4.5`}),` for body text and `,(0,N.jsx)(`strong`,{children:`3`}),` `,`for text at 24px or 19px bold. AAA raises those to 7 and 4.5.`]})]}),(0,N.jsxs)(`section`,{className:`tool-panel is-full`,children:[(0,N.jsx)(`h2`,{children:`Scale`}),(0,N.jsx)(`p`,{className:`tool-note`,children:`Stepped through OKLCH with chroma easing off at both ends, then fitted to the sRGB gamut — so the pale and dark steps read as tints of the colour rather than as neon. Click a step to make it the colour.`}),(0,N.jsx)(`div`,{className:`swatch-scale`,children:l.map(e=>(0,N.jsxs)(`button`,{type:`button`,className:`scale-step`,style:{background:L(e.rgb)},onClick:()=>m(e.rgb),title:`${e.name} — ${L(e.rgb)}`,children:[(0,N.jsx)(`span`,{style:{color:Di(e.rgb,{r:0,g:0,b:0})>4.5?`#000`:`#fff`},children:e.name}),(0,N.jsx)(`small`,{style:{color:Di(e.rgb,{r:0,g:0,b:0})>4.5?`#000`:`#fff`},children:L(e.rgb)})]},e.name))}),(0,N.jsx)(Ri,{label:`Tokens`,value:l.map(e=>`--c-${e.name}: ${L(e.rgb)};`).join(`
`)})]}),(0,N.jsxs)(`section`,{className:`tool-panel is-full`,children:[(0,N.jsx)(`h2`,{children:`Harmonies`}),(0,N.jsx)(`p`,{className:`tool-note`,children:`Rotated in OKLCH rather than HSL, which keeps the perceived lightness and chroma steady across the set instead of letting the greens and yellows jump out.`}),(0,N.jsx)(`div`,{className:`harmonies`,children:u.map(e=>(0,N.jsxs)(`div`,{className:`harmony`,children:[(0,N.jsx)(`span`,{className:`dim`,children:e.name}),(0,N.jsx)(`div`,{className:`harmony-row`,children:e.colors.map((e,t)=>(0,N.jsx)(`button`,{type:`button`,style:{background:L(e)},onClick:()=>m(e),title:L(e),"aria-label":`Use ${L(e)}`,children:(0,N.jsx)(`span`,{children:L(e)})},t))})]},e.name))})]})]})}var Hi=[{id:`linear`,label:`Linear`},{id:`radial`,label:`Radial`},{id:`conic`,label:`Conic`}],Ui=[`circle`,`ellipse`],Wi=[`farthest-corner`,`farthest-side`,`closest-corner`,`closest-side`],Gi=[{id:`srgb`,label:`sRGB`},{id:`oklch`,label:`OKLCH`},{id:`oklab`,label:`OKLab`},{id:`hsl`,label:`HSL`}],Ki=[{name:`Dusk`,type:`linear`,angle:135,stops:[[`#8b6cff`,0],[`#f472b6`,55],[`#f97316`,100]]},{name:`Mint`,type:`linear`,angle:160,stops:[[`#0f766e`,0],[`#34d399`,100]]},{name:`Ember`,type:`radial`,angle:90,stops:[[`#fbbf24`,0],[`#dc2626`,70],[`#1c1917`,100]]},{name:`Steel`,type:`linear`,angle:180,stops:[[`#1e293b`,0],[`#64748b`,100]]},{name:`Wheel`,type:`conic`,angle:0,stops:[[`#ef4444`,0],[`#eab308`,25],[`#22c55e`,50],[`#3b82f6`,75],[`#ef4444`,100]]}],qi=({r:e,g:t,b:n,a:r=1})=>r<1?`rgb(${Math.round(e)} ${Math.round(t)} ${Math.round(n)} / ${Number(r.toFixed(2))})`:L({r:e,g:t,b:n}),Ji=1,Yi=(e,t)=>({id:Ji++,color:e,at:t}),Xi=e=>({type:e.type,angle:e.angle,shape:`circle`,extent:`farthest-corner`,cx:50,cy:50,space:`oklch`,repeating:!1,stops:e.stops.map(([e,t])=>Yi(ji(e),t))});function Zi(e){let t=[...e.stops].sort((e,t)=>e.at-t.at).map(e=>`${qi(e.color)} ${Math.round(e.at)}%`).join(`, `),n=`${e.repeating?`repeating-`:``}${e.type}-gradient`,r=e.type===`linear`?`${e.angle}deg`:e.type===`radial`?`${e.shape} ${e.extent} at ${e.cx}% ${e.cy}%`:`from ${e.angle}deg at ${e.cx}% ${e.cy}%`;return{modern:`${n}(${r}${e.space===`srgb`?``:` in ${e.space}`}, ${t})`,fallback:`${n}(${r}, ${t})`}}function Qi({gradient:e,selected:t,onSelect:n,onMove:r,onAdd:i}){let a=(0,_.useRef)(null),o=(0,_.useRef)(null),s=(0,_.useCallback)(e=>{let t=a.current?.getBoundingClientRect();return t?Math.min(100,Math.max(0,(e.clientX-t.left)/t.width*100)):0},[]);return(0,_.useEffect)(()=>{let e=e=>{o.current!==null&&r(o.current,s(e))},t=()=>{o.current=null};return window.addEventListener(`pointermove`,e),window.addEventListener(`pointerup`,t),()=>{window.removeEventListener(`pointermove`,e),window.removeEventListener(`pointerup`,t)}},[r,s]),(0,N.jsx)(`div`,{ref:a,className:`stop-track`,style:{"--bar":`linear-gradient(90deg, ${[...e.stops].sort((e,t)=>e.at-t.at).map(e=>`${qi(e.color)} ${e.at}%`).join(`, `)})`},onDoubleClick:e=>i(s(e)),title:`Double-click to add a stop`,children:e.stops.map(e=>(0,N.jsx)(`button`,{type:`button`,className:`stop-handle ${e.id===t?`is-selected`:``}`,style:{left:`${e.at}%`,"--c":qi(e.color)},onPointerDown:t=>{t.stopPropagation(),o.current=e.id,n(e.id)},"aria-label":`Stop at ${Math.round(e.at)} percent`},e.id))})}function $i(){let[e,t]=(0,_.useState)(()=>Xi(Ki[0])),[n,r]=(0,_.useState)(null),[i,a]=(0,_.useState)(!1),o=e.stops.find(e=>e.id===n)??e.stops[0],{modern:s,fallback:c}=(0,_.useMemo)(()=>Zi(e),[e]),l=`background-image: ${c};\nbackground-image: ${s};`,u=(0,_.useMemo)(()=>In(l,`css`),[l]),d=e=>t(t=>({...t,...e})),f=(0,_.useCallback)((e,n)=>t(t=>({...t,stops:t.stops.map(t=>t.id===e?{...t,at:n}:t)})),[]),p=(0,_.useCallback)(e=>t(t=>{let n=[...t.stops].sort((e,t)=>e.at-t.at),i=n.findLast?.(t=>t.at<=e)??n[0],a=n.find(t=>t.at>e)??n[n.length-1],o=a.at-i.at||1,s=Yi(Fi(i.color,a.color,(e-i.at)/o),e);return r(s.id),{...t,stops:[...t.stops,s]}}),[]),m=e=>t(t=>t.stops.length<=2?t:{...t,stops:t.stops.filter(t=>t.id!==e)}),h=(e,n)=>{let r=ji(n);r&&t(t=>({...t,stops:t.stops.map(t=>t.id===e?{...t,color:r}:t)}))},g=()=>t(e=>({...e,stops:e.stops.map(e=>({...e,color:Ii()}))})),v=async()=>{await Gn(l),a(!0),setTimeout(()=>a(!1),2e3)};return(0,N.jsxs)(`div`,{className:`tool tool-gradient`,children:[(0,N.jsxs)(`section`,{className:`tool-panel is-full`,children:[(0,N.jsx)(`h2`,{children:`Preview`}),(0,N.jsx)(`div`,{className:`gradient-preview`,style:{backgroundImage:s}}),(0,N.jsx)(`div`,{className:`gradient-preview is-text`,style:{backgroundImage:s},children:(0,N.jsx)(`span`,{children:`Gradient text`})})]}),(0,N.jsxs)(`section`,{className:`tool-panel`,children:[(0,N.jsx)(`h2`,{children:`Shape`}),(0,N.jsx)(`div`,{className:`segmented`,children:Hi.map(t=>(0,N.jsx)(`button`,{type:`button`,className:e.type===t.id?`is-active`:``,onClick:()=>d({type:t.id}),children:t.label},t.id))}),e.type!==`radial`&&(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{children:`Angle`}),(0,N.jsxs)(`div`,{className:`field-control size`,children:[(0,N.jsx)(`input`,{type:`range`,min:0,max:360,value:e.angle,onChange:e=>d({angle:Number(e.target.value)})}),(0,N.jsxs)(`span`,{className:`size-value`,children:[e.angle,`°`]})]})]}),e.type===`radial`&&(0,N.jsxs)(N.Fragment,{children:[(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{children:`Shape`}),(0,N.jsx)(`div`,{className:`segmented small`,children:Ui.map(t=>(0,N.jsx)(`button`,{type:`button`,className:e.shape===t?`is-active`:``,onClick:()=>d({shape:t}),children:t},t))})]}),(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{htmlFor:`grad-extent`,children:`Size`}),(0,N.jsx)(`div`,{className:`field-control`,children:(0,N.jsx)(`select`,{id:`grad-extent`,className:`selector-input`,value:e.extent,onChange:e=>d({extent:e.target.value}),children:Wi.map(e=>(0,N.jsx)(`option`,{value:e,children:e},e))})})]})]}),e.type!==`linear`&&(0,N.jsxs)(N.Fragment,{children:[(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{children:`Centre X`}),(0,N.jsxs)(`div`,{className:`field-control size`,children:[(0,N.jsx)(`input`,{type:`range`,min:0,max:100,value:e.cx,onChange:e=>d({cx:Number(e.target.value)})}),(0,N.jsxs)(`span`,{className:`size-value`,children:[e.cx,`%`]})]})]}),(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{children:`Centre Y`}),(0,N.jsxs)(`div`,{className:`field-control size`,children:[(0,N.jsx)(`input`,{type:`range`,min:0,max:100,value:e.cy,onChange:e=>d({cy:Number(e.target.value)})}),(0,N.jsxs)(`span`,{className:`size-value`,children:[e.cy,`%`]})]})]})]}),(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{htmlFor:`grad-space`,children:`Interpolate in`}),(0,N.jsx)(`div`,{className:`field-control`,children:(0,N.jsx)(`select`,{id:`grad-space`,className:`selector-input`,value:e.space,onChange:e=>d({space:e.target.value}),children:Gi.map(e=>(0,N.jsx)(`option`,{value:e.id,children:e.label},e.id))})})]}),(0,N.jsxs)(`label`,{className:`tool-check`,children:[(0,N.jsx)(`input`,{type:`checkbox`,checked:e.repeating,onChange:e=>d({repeating:e.target.checked})}),(0,N.jsx)(`span`,{children:`Repeating`})]}),(0,N.jsx)(`h2`,{children:`Presets`}),(0,N.jsx)(`div`,{className:`presets`,children:Ki.map(e=>(0,N.jsxs)(`button`,{type:`button`,className:`preset`,onClick:()=>{t(Xi(e)),r(null)},style:{"--swatch":e.stops[1]?.[0]??e.stops[0][0]},children:[(0,N.jsx)(`span`,{className:`preset-dot`,"aria-hidden":`true`}),e.name]},e.name))}),(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:g,children:[(0,N.jsx)(P,{name:`bolt`,size:14}),(0,N.jsx)(`span`,{children:`Shuffle colours`})]})]}),(0,N.jsxs)(`section`,{className:`tool-panel`,children:[(0,N.jsx)(`h2`,{children:`Stops`}),(0,N.jsx)(Qi,{gradient:e,selected:o?.id,onSelect:r,onMove:f,onAdd:p}),(0,N.jsx)(`p`,{className:`tool-note`,children:`Drag a handle to move it. Double-click the bar to add one — the new stop takes the colour the gradient already had at that point.`}),(0,N.jsx)(`ul`,{className:`stop-list`,children:[...e.stops].sort((e,t)=>e.at-t.at).map(t=>(0,N.jsxs)(`li`,{className:t.id===o?.id?`is-selected`:``,children:[(0,N.jsx)(`input`,{type:`color`,value:L({...t.color,a:1}),onChange:e=>h(t.id,e.target.value),"aria-label":`Stop colour`}),(0,N.jsx)(`input`,{className:`stop-hex`,type:`text`,value:qi(t.color),spellCheck:`false`,onChange:e=>h(t.id,e.target.value),onFocus:()=>r(t.id),"aria-label":`Stop colour value`}),(0,N.jsx)(`input`,{className:`stop-at`,type:`number`,min:0,max:100,value:Math.round(t.at),onChange:e=>f(t.id,Number(e.target.value)),"aria-label":`Stop position`}),(0,N.jsx)(`button`,{type:`button`,className:`icon-button`,onClick:()=>m(t.id),disabled:e.stops.length<=2,"aria-label":`Remove stop`,children:(0,N.jsx)(P,{name:`close`,size:13})})]},t.id))}),(0,N.jsxs)(`h2`,{children:[`CSS`,(0,N.jsxs)(`button`,{type:`button`,className:`code-action code-copy ${i?`is-copied`:``}`,onClick:v,children:[(0,N.jsx)(P,{name:i?`check`:`copy`,size:13}),i?`Copied`:`Copy`]})]}),(0,N.jsx)(`pre`,{className:`tool-code hljs`,children:(0,N.jsx)(`code`,{dangerouslySetInnerHTML:{__html:u}})}),(0,N.jsxs)(`p`,{className:`tool-note`,children:[`Two declarations on purpose. A browser that cannot parse`,` `,(0,N.jsxs)(`code`,{children:[`in `,e.space]}),` ignores the second line and keeps the first, so the gradient degrades to sRGB interpolation rather than disappearing. The second preview above is the same gradient clipped to text with `,(0,N.jsx)(`code`,{children:`background-clip: text`}),`.`]})]})]})}var ea=12,ta=[{label:`1920 × 1080`,w:1920,h:1080},{label:`1280 × 720`,w:1280,h:720},{label:`1200 × 630`,w:1200,h:630},{label:`1080 × 1080`,w:1080,h:1080},{label:`1080 × 1920`,w:1080,h:1920}],na=[`#ff0080`,`#7928ca`,`#00dfd8`,`#ff4d4d`,`#f9cb28`,`#4338ca`,`#10b981`],ra=1,ia=(e,t,n,r=70,i=70)=>({id:ra++,x:e,y:t,color:n,rx:r,ry:i}),aa=[{name:`Aurora`,base:`#1a0b2e`,points:[[18,22,`#7928ca`,85,80],[78,18,`#ff0080`,70,75],[30,82,`#00dfd8`,75,70],[88,76,`#4338ca`,80,85]]},{name:`Sunset`,base:`#1c1017`,points:[[12,78,`#f9cb28`,80,70],[55,30,`#ff4d4d`,75,80],[92,62,`#7928ca`,85,75]]},{name:`Deep`,base:`#04070f`,points:[[24,30,`#10b981`,70,65],[72,24,`#0ea5e9`,75,70],[50,90,`#4338ca`,90,80]]},{name:`Blush`,base:`#fdf2f8`,points:[[20,25,`#f9a8d4`,80,75],[80,30,`#c4b5fd`,75,80],[50,85,`#fde68a`,85,70]]}],oa=e=>({base:e.base,points:e.points.map(([e,t,n,r,i])=>ia(e,t,ji(n),r,i))}),B=e=>{let{r:t,g:n,b:r}=e.color,i=`${Math.round(t)}, ${Math.round(n)}, ${Math.round(r)}`;return`radial-gradient(ellipse ${Math.round(e.rx)}% ${Math.round(e.ry)}% at ${Math.round(e.x)}% ${Math.round(e.y)}%, rgba(${i}, 1) 0%, rgba(${i}, 0) 100%)`};function sa(e){let t=e.points.map(B);return[`.mesh {`,`  background-color: ${e.base};`,t.length?`  background-image:\n${t.map(e=>`    ${e}`).join(`,
`)};`:`  background-image: none;`,`}`].join(`
`)}function ca(e,t,n,r){let i=document.createElement(`canvas`);i.width=t,i.height=n;let a=i.getContext(`2d`);a.fillStyle=e.base,a.fillRect(0,0,t,n);for(let r of e.points){let{r:e,g:i,b:o}=r.color,s=`${Math.round(e)}, ${Math.round(i)}, ${Math.round(o)}`,c=r.rx/100*t,l=r.ry/100*n;if(c<=0||l<=0)continue;a.save(),a.translate(r.x/100*t,r.y/100*n),a.scale(c,l);let u=a.createRadialGradient(0,0,0,0,0,1);u.addColorStop(0,`rgba(${s}, 1)`),u.addColorStop(1,`rgba(${s}, 0)`),a.fillStyle=u;let d=Math.max(t/c,n/l)+2;a.fillRect(-d,-d,d*2,d*2),a.restore()}i.toBlob(e=>{if(!e)return;let t=URL.createObjectURL(e),n=document.createElement(`a`);n.href=t,n.download=r,document.body.appendChild(n),n.click(),n.remove(),setTimeout(()=>URL.revokeObjectURL(t),1e3)},`image/png`)}function la(){let[e,t]=(0,_.useState)(()=>oa(aa[0])),[n,r]=(0,_.useState)(null),[i,a]=(0,_.useState)(ta[0]),[o,s]=(0,_.useState)(!1),c=(0,_.useRef)(null),l=(0,_.useRef)(null),u=(0,_.useMemo)(()=>sa(e),[e]),d=(0,_.useMemo)(()=>In(u,`css`),[u]),f=(0,_.useMemo)(()=>({backgroundColor:e.base,backgroundImage:e.points.length?e.points.map(B).join(`, `):`none`}),[e]),p=(0,_.useCallback)((e,n,r)=>{let i=c.current?.getBoundingClientRect();if(!i)return;let a=Math.max(-15,Math.min(115,(n-i.left)/i.width*100)),o=Math.max(-15,Math.min(115,(r-i.top)/i.height*100));t(t=>({...t,points:t.points.map(t=>t.id===e?{...t,x:a,y:o}:t)}))},[]);(0,_.useEffect)(()=>{let e=e=>{l.current!==null&&(e.preventDefault(),p(l.current,e.clientX,e.clientY))},t=()=>{l.current=null};return window.addEventListener(`pointermove`,e,{passive:!1}),window.addEventListener(`pointerup`,t),()=>{window.removeEventListener(`pointermove`,e),window.removeEventListener(`pointerup`,t)}},[p]);let m=(e,n)=>t(t=>({...t,points:t.points.map(t=>t.id===e?{...t,...n}:t)})),h=()=>t(e=>{if(e.points.length>=ea)return e;let t=ji(na[e.points.length%na.length]),n=ia(20+Math.random()*60,20+Math.random()*60,t);return r(n.id),{...e,points:[...e.points,n]}}),g=e=>t(t=>({...t,points:t.points.filter(t=>t.id!==e)})),v=()=>t(e=>({...e,points:e.points.map(e=>({...e,x:10+Math.random()*80,y:10+Math.random()*80,color:Ii(),rx:55+Math.random()*45,ry:55+Math.random()*45}))})),y=async()=>{await Gn(u),s(!0),setTimeout(()=>s(!1),2e3)},b=e.points.find(e=>e.id===n)??null;return(0,N.jsxs)(`div`,{className:`tool tool-mesh`,children:[(0,N.jsxs)(`section`,{className:`tool-panel is-full mesh-stage`,children:[(0,N.jsxs)(`h2`,{children:[`Canvas`,(0,N.jsxs)(`span`,{className:`dim`,children:[i.label,` · `,e.points.length,`/`,ea,` points — drag them`]})]}),(0,N.jsxs)(`div`,{ref:c,className:`mesh-canvas`,style:{...f,"--ar":`${i.w} / ${i.h}`,"--arnum":i.w/i.h},onPointerDown:()=>r(null),children:[e.points.map(e=>(0,N.jsx)(`button`,{type:`button`,className:`mesh-point ${e.id===n?`is-selected`:``}`,style:{left:`${e.x}%`,top:`${e.y}%`,background:L(e.color)},onPointerDown:t=>{t.stopPropagation(),l.current=e.id,r(e.id)},onKeyDown:t=>{let n=t.shiftKey?5:1,r={ArrowLeft:{x:e.x-n},ArrowRight:{x:e.x+n},ArrowUp:{y:e.y-n},ArrowDown:{y:e.y+n}};r[t.key]&&(t.preventDefault(),m(e.id,r[t.key]))},"aria-label":`Point at ${Math.round(e.x)}, ${Math.round(e.y)}`},e.id)),e.points.length===0&&(0,N.jsx)(`p`,{className:`mesh-empty`,children:`Add a point to start`})]})]}),(0,N.jsxs)(`section`,{className:`tool-panel`,children:[(0,N.jsx)(`h2`,{children:`Mesh`}),(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{htmlFor:`mesh-base`,children:`Base colour`}),(0,N.jsxs)(`div`,{className:`field-control colour`,children:[(0,N.jsx)(`input`,{id:`mesh-base`,type:`color`,value:e.base,onChange:e=>t(t=>({...t,base:e.target.value}))}),(0,N.jsx)(`input`,{className:`colour-hex`,type:`text`,value:e.base,spellCheck:`false`,onChange:e=>{let n=ji(e.target.value);t(t=>({...t,base:n?L(n):e.target.value}))},"aria-label":`Base colour value`})]})]}),(0,N.jsxs)(`div`,{className:`mesh-actions`,children:[(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:h,disabled:e.points.length>=ea,children:[(0,N.jsx)(P,{name:`star`,size:14}),(0,N.jsx)(`span`,{children:`Add point`})]}),(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:v,children:[(0,N.jsx)(P,{name:`bolt`,size:14}),(0,N.jsx)(`span`,{children:`Shuffle`})]})]}),(0,N.jsx)(`h2`,{children:`Presets`}),(0,N.jsx)(`div`,{className:`presets`,children:aa.map(e=>(0,N.jsxs)(`button`,{type:`button`,className:`preset`,style:{"--swatch":e.points[0][2]},onClick:()=>{t(oa(e)),r(null)},children:[(0,N.jsx)(`span`,{className:`preset-dot`,"aria-hidden":`true`}),e.name]},e.name))}),(0,N.jsx)(`h2`,{children:`Export`}),(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{htmlFor:`mesh-size`,children:`Image size`}),(0,N.jsx)(`div`,{className:`field-control`,children:(0,N.jsx)(`select`,{id:`mesh-size`,className:`selector-input`,value:i.label,onChange:e=>a(ta.find(t=>t.label===e.target.value)),children:ta.map(e=>(0,N.jsx)(`option`,{value:e.label,children:e.label},e.label))})})]}),(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-primary`,onClick:()=>ca(e,i.w,i.h,`mesh-${i.w}x${i.h}.png`),children:[(0,N.jsx)(P,{name:`download`,size:15}),(0,N.jsx)(`span`,{children:`Download PNG`})]}),(0,N.jsx)(`p`,{className:`tool-note`,children:`The canvas above takes this shape, because every position and radius in a mesh is a percentage — the same points in a wider box make a different picture, so a preview that ignored the ratio would be lying.`}),(0,N.jsx)(`p`,{className:`tool-note`,children:`The PNG is painted on a canvas from the same numbers as the CSS, in this tab. Nothing is uploaded and no screenshot is taken, so the image is as sharp as the size you pick rather than as sharp as your screen.`})]}),(0,N.jsxs)(`section`,{className:`tool-panel`,children:[(0,N.jsx)(`h2`,{children:`Points`}),e.points.length===0&&(0,N.jsx)(`p`,{className:`tool-note`,children:`No points yet.`}),(0,N.jsx)(`ul`,{className:`mesh-list`,children:e.points.map((e,t)=>(0,N.jsxs)(`li`,{className:e.id===n?`is-selected`:``,children:[(0,N.jsxs)(`div`,{className:`mesh-list-head`,children:[(0,N.jsx)(`input`,{type:`color`,value:L(e.color),onChange:t=>m(e.id,{color:ji(t.target.value)}),"aria-label":`Point ${t+1} colour`}),(0,N.jsxs)(`button`,{type:`button`,className:`mesh-list-name`,onClick:()=>r(e.id),children:[`Point `,t+1,(0,N.jsx)(`span`,{className:`dim`,children:L(e.color)})]}),(0,N.jsx)(`button`,{type:`button`,className:`icon-button`,onClick:()=>g(e.id),"aria-label":`Remove point ${t+1}`,children:(0,N.jsx)(P,{name:`close`,size:13})})]}),e.id===n&&(0,N.jsxs)(`div`,{className:`mesh-list-body`,children:[(0,N.jsxs)(`label`,{children:[(0,N.jsx)(`span`,{children:`Spread across`}),(0,N.jsx)(`input`,{type:`range`,min:10,max:140,value:Math.round(e.rx),onChange:t=>m(e.id,{rx:Number(t.target.value)})}),(0,N.jsxs)(`em`,{children:[Math.round(e.rx),`%`]})]}),(0,N.jsxs)(`label`,{children:[(0,N.jsx)(`span`,{children:`Spread down`}),(0,N.jsx)(`input`,{type:`range`,min:10,max:140,value:Math.round(e.ry),onChange:t=>m(e.id,{ry:Number(t.target.value)})}),(0,N.jsxs)(`em`,{children:[Math.round(e.ry),`%`]})]})]})]},e.id))}),(0,N.jsxs)(`h2`,{children:[`CSS`,(0,N.jsxs)(`button`,{type:`button`,className:`code-action code-copy ${o?`is-copied`:``}`,onClick:y,children:[(0,N.jsx)(P,{name:o?`check`:`copy`,size:13}),o?`Copied`:`Copy`]})]}),(0,N.jsx)(`pre`,{className:`tool-code hljs`,children:(0,N.jsx)(`code`,{dangerouslySetInnerHTML:{__html:d}})}),b&&(0,N.jsxs)(`p`,{className:`tool-note`,children:[`Point `,e.points.indexOf(b)+1,` selected. Arrow keys nudge it while it has focus; hold shift for bigger steps.`]})]})]})}var ua={thumb:`#8b6cff`,track:`#1c1c26`,size:12,radius:8,borderWidth:0,borderColor:`#1c1c26`,selector:`body`},da=[{name:`Brand`,thumb:`#8b6cff`,track:`#1c1c26`,size:12,radius:8,borderWidth:0},{name:`Hairline`,thumb:`#6b6a80`,track:`transparent`,size:6,radius:999,borderWidth:0},{name:`Inset`,thumb:`#34d399`,track:`#0f1a16`,size:16,radius:999,borderWidth:4},{name:`Classic`,thumb:`#9aa0aa`,track:`#e6e6ee`,size:14,radius:0,borderWidth:0}],fa=e=>/^#[0-9a-f]{6}$/i.test(e)?e.toUpperCase():e;function pa(e,t=e.selector){let n=e.borderWidth>0;return`${t} {
  --sb-track-color: ${fa(e.track)};
  --sb-thumb-color: ${fa(e.thumb)};
  --sb-size: ${e.size}px;${n?`\n  --sb-thumb-border: ${fa(e.borderColor)};`:``}
}

${t}::-webkit-scrollbar {
  width: var(--sb-size);
}

${t}::-webkit-scrollbar-track {
  background: var(--sb-track-color);
  border-radius: ${e.radius}px;
}

${t}::-webkit-scrollbar-thumb {
  background: var(--sb-thumb-color);
  border-radius: ${e.radius}px;${n?`
  border: ${e.borderWidth}px solid var(--sb-thumb-border);
  background-clip: padding-box;`:``}
}

@supports not selector(::-webkit-scrollbar) {
  ${t} {
    scrollbar-color: var(--sb-thumb-color)
                     var(--sb-track-color);
    scrollbar-width: ${e.size<=8?`thin`:`auto`};
  }
}`}function ma({label:e,value:t,onChange:n}){return(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{htmlFor:`c-${e}`,children:e}),(0,N.jsxs)(`div`,{className:`field-control colour`,children:[(0,N.jsx)(`input`,{id:`c-${e}`,type:`color`,value:/^#[0-9a-f]{6}$/i.test(t)?t:`#000000`,onChange:e=>n(e.target.value),"aria-label":e}),(0,N.jsx)(`input`,{className:`colour-hex`,type:`text`,value:t,spellCheck:`false`,onChange:e=>n(e.target.value.trim()),"aria-label":`${e} value`})]})]})}function ha({label:e,value:t,onChange:n,min:r,max:i}){let a=e=>Math.min(i,Math.max(r,e));return(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{children:e}),(0,N.jsxs)(`div`,{className:`field-control size`,children:[(0,N.jsx)(`button`,{type:`button`,onClick:()=>n(a(t-1)),disabled:t<=r,children:`−`}),(0,N.jsx)(`input`,{type:`range`,min:r,max:i,value:t,onChange:e=>n(Number(e.target.value)),"aria-label":e}),(0,N.jsx)(`button`,{type:`button`,onClick:()=>n(a(t+1)),disabled:t>=i,children:`+`}),(0,N.jsxs)(`span`,{className:`size-value`,children:[t,`px`]})]})]})}function ga(){let[e,t]=(0,_.useState)(ua),[n,r]=(0,_.useState)(!1),i=e=>n=>t(t=>({...t,[e]:n})),a=(0,_.useMemo)(()=>pa(e),[e]),o=(0,_.useMemo)(()=>In(a,`css`),[a]),s=(0,_.useMemo)(()=>pa(e,`.sb-preview`),[e]);return(0,N.jsxs)(`div`,{className:`tool`,children:[(0,N.jsx)(`style`,{children:s}),(0,N.jsxs)(`section`,{className:`tool-panel`,children:[(0,N.jsx)(`h2`,{children:`Settings`}),(0,N.jsx)(`div`,{className:`presets`,children:da.map(e=>(0,N.jsxs)(`button`,{type:`button`,className:`preset`,onClick:()=>t(t=>({...t,...e,borderColor:e.track})),style:{"--swatch":e.thumb},children:[(0,N.jsx)(`span`,{className:`preset-dot`,"aria-hidden":`true`}),e.name]},e.name))}),(0,N.jsx)(ma,{label:`Thumb`,value:e.thumb,onChange:i(`thumb`)}),(0,N.jsx)(ma,{label:`Track`,value:e.track,onChange:i(`track`)}),(0,N.jsx)(ha,{label:`Width`,value:e.size,onChange:i(`size`),min:2,max:32}),(0,N.jsx)(ha,{label:`Corner radius`,value:e.radius,onChange:i(`radius`),min:0,max:20}),(0,N.jsx)(ha,{label:`Thumb border`,value:e.borderWidth,onChange:i(`borderWidth`),min:0,max:8}),e.borderWidth>0&&(0,N.jsx)(ma,{label:`Border colour`,value:e.borderColor,onChange:i(`borderColor`)}),(0,N.jsxs)(`div`,{className:`field`,children:[(0,N.jsx)(`label`,{htmlFor:`sb-selector`,children:`Applies to`}),(0,N.jsx)(`div`,{className:`field-control`,children:(0,N.jsx)(`input`,{id:`sb-selector`,className:`selector-input`,type:`text`,value:e.selector,spellCheck:`false`,onChange:e=>i(`selector`)(e.target.value||`body`)})})]}),(0,N.jsxs)(`p`,{className:`tool-note`,children:[(0,N.jsx)(`code`,{children:`body`}),` styles the page itself. A class such as`,` `,(0,N.jsx)(`code`,{children:`.panel`}),` styles one scrolling element — and a scrollbar only appears where `,(0,N.jsx)(`code`,{children:`overflow: auto`}),` meets content taller than the box.`]}),(0,N.jsx)(`button`,{type:`button`,className:`btn btn-ghost btn-sm`,onClick:()=>t(ua),children:`Reset`})]}),(0,N.jsxs)(`section`,{className:`tool-panel is-code`,children:[(0,N.jsxs)(`h2`,{children:[`CSS`,(0,N.jsxs)(`button`,{type:`button`,className:`code-action code-copy ${n?`is-copied`:``}`,onClick:async()=>{await Gn(a),r(!0),setTimeout(()=>r(!1),2e3)},children:[(0,N.jsx)(P,{name:n?`check`:`copy`,size:13}),n?`Copied`:`Copy`]})]}),(0,N.jsx)(`pre`,{className:`tool-code hljs`,children:(0,N.jsx)(`code`,{dangerouslySetInnerHTML:{__html:o}})})]}),(0,N.jsxs)(`section`,{className:`tool-panel`,children:[(0,N.jsx)(`h2`,{children:`Live`}),(0,N.jsxs)(`div`,{className:`sb-preview`,children:[(0,N.jsx)(`p`,{children:`This box is styled by the CSS beside it. Drag the bar, change a value, watch it move.`}),Array.from({length:18},(e,t)=>(0,N.jsxs)(`p`,{children:[`Line `,t+1,` — the `,(0,N.jsx)(`strong`,{children:`thumb`}),` is the part you drag, the`,` `,(0,N.jsx)(`strong`,{children:`track`}),` is the groove it runs in.`]},t))]}),(0,N.jsxs)(`p`,{className:`tool-note`,children:[`Firefox uses the `,(0,N.jsx)(`code`,{children:`@supports`}),` block at the bottom, which is why the corner radius and border have no effect there — the standard properties only expose colour and width.`]})]})]})}var _a={scrollbar:ga,colour:Vi,gradient:$i,mesh:la};function va({tool:e,onNavigate:t}){let n=_a[e.slug];return(0,_.useEffect)(()=>{window.scrollTo({top:0})},[e.slug]),(0,N.jsxs)(`article`,{className:`article`,style:{"--accent":e.accent},children:[(0,N.jsxs)(`button`,{type:`button`,className:`btn btn-ghost btn-sm back`,onClick:()=>t(`tools`),children:[(0,N.jsx)(P,{name:`arrow`,size:15,className:`flip`}),(0,N.jsx)(`span`,{children:`All tools`})]}),(0,N.jsxs)(`header`,{className:`article-head`,children:[(0,N.jsxs)(`div`,{className:`article-meta`,children:[(0,N.jsx)(`span`,{className:`badge badge-stable`,children:`Ready`}),(0,N.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,N.jsx)(`li`,{children:e},e))})]}),(0,N.jsx)(`h1`,{children:e.name}),(0,N.jsx)(`p`,{className:`lede`,children:e.summary})]}),n?(0,N.jsx)(n,{}):(0,N.jsx)(`p`,{className:`hint`,children:`This one is not built yet.`}),(0,N.jsx)(`footer`,{className:`article-foot`,children:(0,N.jsx)(`p`,{className:`hint`,children:`Everything above happens in this tab. Nothing is sent anywhere, nothing is stored, and closing the page leaves no trace of what you did with it.`})})]})}function ya({tool:e,onOpen:t}){let n=e.status===`live`,r=ve();return(0,N.jsxs)(`article`,{className:`card tool-card ${n?``:`is-upcoming`}`,style:{"--accent":e.accent},onClick:n?()=>t(e.route):void 0,...r,children:[(0,N.jsx)(`div`,{className:`card-glow`,"aria-hidden":`true`}),(0,N.jsxs)(`header`,{className:`article-card-head`,children:[(0,N.jsx)(`span`,{className:`badge ${n?`badge-stable`:`badge-wip`}`,children:n?`Ready`:`Upcoming`}),(0,N.jsx)(`ul`,{className:`tag-row`,children:e.tags.map(e=>(0,N.jsx)(`li`,{children:e},e))})]}),(0,N.jsx)(`h3`,{children:n?(0,N.jsx)(`button`,{type:`button`,className:`card-link`,onClick:n=>{n.stopPropagation(),t(e.route)},children:e.name}):e.name}),(0,N.jsx)(`p`,{className:`tagline`,children:e.tagline}),(0,N.jsx)(`p`,{className:`card-summary`,children:e.summary}),n&&(0,N.jsx)(`footer`,{className:`card-foot`,children:(0,N.jsxs)(`span`,{className:`article-more`,children:[`Open `,(0,N.jsx)(P,{name:`arrow`,size:15})]})})]})}function ba({onOpen:e}){let t=Nr.filter(e=>e.status===`live`),n=Nr.filter(e=>e.status!==`live`);return(0,N.jsxs)(N.Fragment,{children:[(0,N.jsxs)(`section`,{className:`page-head`,children:[(0,N.jsxs)(`p`,{className:`eyebrow`,children:[(0,N.jsx)(`span`,{className:`pulse`,"aria-hidden":`true`}),`Free tools, no sign up`]}),(0,N.jsxs)(`h1`,{children:[`Free tools that run`,(0,N.jsx)(`br`,{}),(0,N.jsx)(`span`,{className:`grad`,children:`in your browser.`})]}),(0,N.jsx)(`p`,{className:`lede`,children:`The same promise as the desktop apps, applied to the things that never needed a desktop app. Everything here runs entirely in your browser — no upload, no account, and nothing to install. Close the tab and there is no trace of it anywhere.`})]}),(0,N.jsx)(`div`,{className:`catalog catalog-grid`,children:t.map((t,n)=>(0,N.jsx)(Ee,{className:`reveal-cell`,delay:he(n),children:(0,N.jsx)(ya,{tool:t,onOpen:e})},t.slug))}),n.length>0&&(0,N.jsxs)(N.Fragment,{children:[(0,N.jsxs)(Ee,{as:`h2`,className:`section-heading`,variant:`left`,children:[`On the list`,(0,N.jsxs)(`span`,{className:`dim`,children:[n.length,` planned`]})]}),(0,N.jsx)(`div`,{className:`catalog catalog-grid`,children:n.map((t,n)=>(0,N.jsx)(Ee,{className:`reveal-cell`,delay:he(n),children:(0,N.jsx)(ya,{tool:t,onOpen:e})},t.slug))})]})]})}var xa=()=>decodeURIComponent(window.location.hash.replace(/^#\/?/,``));function Sa(){let[e,t]=(0,_.useState)(xa);return(0,_.useEffect)(()=>{let e=()=>t(xa());return window.addEventListener(`hashchange`,e),()=>window.removeEventListener(`hashchange`,e)},[]),[e,(0,_.useCallback)(e=>{e?window.location.hash=`/${e}`:window.location.hash&&history.replaceState(null,``,window.location.pathname+window.location.search),t(e??``)},[])]}var Ca=`afterhours:`,wa=(e,t)=>{try{let n=localStorage.getItem(Ca+e);return n===null?t:JSON.parse(n)}catch{return t}};function Ta(e,t){let[n,r]=(0,_.useState)(()=>wa(e,t));return(0,_.useEffect)(()=>{try{localStorage.setItem(Ca+e,JSON.stringify(n))}catch{}},[e,n]),[n,r]}var Ea=[`light`,`dark`,`system`],Da=()=>window.matchMedia?.(`(prefers-color-scheme: dark)`).matches??!1;function Oa(){let[e,t]=Ta(`theme`,`system`);return(0,_.useEffect)(()=>{let t=()=>{let t=e===`system`?Da()?`dark`:`light`:e;document.documentElement.dataset.theme=t};if(t(),e!==`system`)return;let n=window.matchMedia(`(prefers-color-scheme: dark)`);return n.addEventListener(`change`,t),()=>n.removeEventListener(`change`,t)},[e]),{theme:e,setTheme:t,cycle:()=>t(e=>Ea[(Ea.indexOf(e)+1)%Ea.length])}}var ka={stable:0,beta:1,wip:2},Aa=e=>[e.name,e.tagline,e.summary,...e.tags,...e.highlights,...e.platforms].join(` `).toLowerCase();function ja(){let{theme:e,cycle:t}=Oa(),[n,r]=Sa(),i=be(),[a,o]=Ta(`query`,``),[s,c]=Ta(`articleQuery`,``),[l,u]=Ta(`articleCategory`,null),[d,f]=Ta(`studyQuery`,``),[p,m]=Ta(`studySubject`,null),[h,g]=Ta(`tags`,[]),[v,y]=Ta(`sort`,`recent`),[b,x]=Ta(`view`,`grid`),[ee,S]=Ta(`favorites`,[]),[C,te]=Ta(`favoritesOnly`,!1),ne=(0,_.useCallback)(e=>g(t=>t.includes(e)?t.filter(t=>t!==e):[...t,e]),[g]),w=(0,_.useCallback)(e=>S(t=>t.includes(e)?t.filter(t=>t!==e):[...t,e]),[S]),T=(0,_.useMemo)(()=>{let e=a.trim().toLowerCase(),t=e?e.split(/\s+/):[],n=re.filter(e=>{if(C&&!ee.includes(e.id)||h.length&&!h.every(t=>e.tags.includes(t)))return!1;if(!t.length)return!0;let n=Aa(e);return t.every(e=>n.includes(e))}),r={name:(e,t)=>e.name.localeCompare(t.name),recent:(e,t)=>t.updated.localeCompare(e.updated),status:(e,t)=>ka[e.status]-ka[t.status]||e.name.localeCompare(t.name)};return[...n].sort(r[v]??r.recent)},[a,h,v,ee,C]),E=n.startsWith(`articles/`)?n.slice(9):null,ie=E?pe(E):null,D=n.startsWith(`study/`)?n.slice(6).split(`/`):null,O=D?jr(D[0]):null,k=O?Mr(O,D[1]):null,ae=n.startsWith(`tools/`)?n.slice(6):null,A=ae?Pr(ae):null,j=n===`articles`||E!==null,oe=!j&&(n===`tools`||ae!==null),se=!j&&(n===``||n===`study`||D!==null),ce=!j&&!se?re.find(e=>e.id===n)??null:null,le=n===`support`,ue=new Set(re.flatMap(e=>e.platforms)).size,M=oe?A?`tool:${A.slug}`:`tools`:se?O?`topic:${O.slug}`:`study`:j?ie?`article:${ie.slug}`:`articles`:`apps`;return(0,N.jsxs)(`div`,{className:`shell ${i.hidden?`header-hidden`:``}`,children:[(0,N.jsx)(`div`,{className:`bg-glow`,"aria-hidden":`true`}),(0,N.jsx)(ri,{}),(0,N.jsx)(Hr,{scrolled:i.scrolled,hidden:i.hidden,theme:e,onCycleTheme:t,onSupport:()=>r(`support`),tab:j?`articles`:oe?`tools`:se?`study`:`apps`,onNavigate:r}),(0,N.jsx)(`main`,{className:`page-transition`,children:oe?A?(0,N.jsx)(va,{tool:A,onNavigate:r}):(0,N.jsx)(ba,{onOpen:r}):se?O?(0,N.jsx)(pi,{topic:O,part:k,onNavigate:r}):(0,N.jsx)(Jr,{query:d,onQuery:f,subject:p,onSubject:m,onOpen:r}):j?ie?(0,N.jsx)(er,{article:ie,onNavigate:r}):(0,N.jsx)(Oe,{query:s,onQuery:c,category:l,onCategory:u,onOpen:r}):(0,N.jsxs)(N.Fragment,{children:[(0,N.jsx)(Ur,{total:re.length,platforms:ue,guides:de.length,onArticles:()=>r(`articles`)}),(0,N.jsx)(fi,{query:a,onQuery:o,activeTags:h,onToggleTag:ne,onClearTags:()=>g([]),sort:v,onSort:y,view:b,onView:x,favoritesOnly:C,onFavoritesOnly:te,favoriteCount:ee.length,count:T.length}),T.length>0?(0,N.jsx)(`div`,{className:`catalog catalog-${b}`,children:T.map((e,t)=>(0,N.jsx)(Ee,{className:`reveal-cell`,delay:he(t),children:(0,N.jsx)(Zr,{project:e,view:b,isFavorite:ee.includes(e.id),onToggleFavorite:w,onOpen:r})},e.id))}):(0,N.jsxs)(`div`,{className:`empty empty-in`,children:[(0,N.jsx)(P,{name:`search`,size:28}),(0,N.jsx)(`h3`,{children:`Nothing matches that`}),(0,N.jsx)(`p`,{children:C&&ee.length===0?`You have not saved any apps yet — tap the star on a card.`:`Try a different word, or clear the filters.`}),(0,N.jsx)(`button`,{type:`button`,className:`btn btn-ghost`,onClick:()=>{o(``),g([]),te(!1)},children:`Clear filters`})]})]})},M),(0,N.jsx)(tr,{onSupport:()=>r(`support`),articles:j}),(0,N.jsx)(ke,{raised:!!O}),ce&&(0,N.jsx)(ni,{project:ce,onClose:()=>r(`apps`),isFavorite:ee.includes(ce.id),onToggleFavorite:w}),le&&(0,N.jsx)(ui,{onClose:()=>r(`apps`)})]})}(0,v.createRoot)(document.getElementById(`root`)).render((0,N.jsx)(_.StrictMode,{children:(0,N.jsx)(ja,{})}));