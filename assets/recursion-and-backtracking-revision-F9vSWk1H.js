var e="The topic in a page. If a line here is news, the **Notes** part is where it\ncomes from.\n\n## Recursion\n\n- A **frame** per call holds its own parameters and locals. Frames pop in reverse\n  order, so work written after the recursive call happens on the way out.\n- Two conditions: a case that returns without recursing, and every call\n  **strictly closer** to it. Guard with `<=`, not `==`, or a negative argument\n  steps straight over the base case. Missing it is `StackOverflowError`, not a\n  hang.\n- Read it as a promise, not a trace: assume the smaller call works, then check\n  only the base case and the combine step.\n- Shape: stop, solve the smaller problem, combine.\n\n## Backtracking\n\n```java\nvoid go(int i, List<Integer> path) {\n    if (done(i)) { out.add(new ArrayList<>(path)); return; }\n    for (Option o : options(i)) {\n        apply(o);          // choose\n        go(i + 1, path);   // explore\n        undo(o);           // un-choose\n    }\n}\n```\n\n- `apply` and `undo` must be exact opposites. List add / remove last. Flag set /\n  clear. Cell overwritten / original written back — so save the original first.\n- Forgetting `undo` throws nothing. It carries one branch's debris into the next,\n  and the output is quietly wrong.\n- `out.add(path)` stores a reference to the list you keep mutating, so every\n  answer comes out identical, usually `[]`. Store `new ArrayList<>(path)`.\n\n## The templates\n\n| Want | Loop | Recurse with |\n|---|---|---|\n| Subsets | `for (i = start; i < n; i++)` | `i + 1` |\n| Combinations of size k | same, stop at `path.size() == k` | `i + 1` |\n| Combination sum, no reuse | same, stop at `left == 0` | `i + 1` |\n| Combination sum, reuse | same | `i` |\n| Permutations | `for (i = 0; i < n; i++)`, skip `used[i]` | next depth |\n| Permutations by swap | `for (i = k; i < n; i++)`, swap in and back | `k + 1` |\n\n- Subsets also go take-it or leave-it: add, recurse, remove, recurse.\n- `start` versus `0` in the loop is the whole difference between a subset and a\n  permutation. Passing `start` down instead of `i` or `i + 1` yields `[2,3]` and\n  `[3,2]` separately, and often recurses forever.\n\n## Duplicates\n\n![Which duplicate gets through the skip at one depth of the recursion](diagrams/recursion-and-backtracking-revision-duplicate-skip.jpg)\n\n```java\nArrays.sort(a);\nif (i > start && a[i] == a[i - 1]) continue;\n```\n\n- `i > start`, never `i > 0` — the first copy at this depth must get through.\n- Without the sort, equal values are not adjacent and the skip never fires.\n- Deduplicating the finished output with a `Set` does the exponential work first\n  and then discards it. Prune instead.\n\n## Pruning\n\n- `if (a[i] > left) break;` on a sorted array — everything right of it is worse.\n- N-Queens: three `boolean[]` — `col[c]`, `diag[r - c + n - 1]`, `anti[r + c]` —\n  make the conflict test O(1). Unpruned, `n = 12` never finishes.\n- Grid walks: overwrite the cell with `'#'`, recurse, write the saved character\n  back. The board is the visited set.\n- Test as early as you can. A check at the leaf prunes nothing.\n\n## Costs\n\n![What the limit on n tells you the search is going to be](diagrams/recursion-and-backtracking-revision-reading-n.jpg)\n\n| Search | Answers | Time |\n|---|---|---|\n| Subsets | 2ⁿ | O(n · 2ⁿ) — the `n` is the copy |\n| Permutations | n! | O(n · n!) |\n| N-Queens | varies | O(n!) worst case, far less once pruned |\n\n- Stack costs O(depth), and about 10,000 frames is a `StackOverflowError`.\n- 2²⁰ ≈ a million, so n ≈ 20 is the subset limit; 13! is six billion, so `n ≤ 10`\n  in the constraints means permutations. Output size is not charged against you.\n\n## The bugs and the API\n\n- No `undo`, an `undo` that is not the exact inverse, or `out.add(path)` without\n  the copy.\n- `i > 0` instead of `i > start`, or the skip without the sort.\n- `path.remove(2)` removes index 2, not the value 2.\n- `static` fields not reset, so the second test case inherits the first's state.\n\n| Want | Write |\n|---|---|\n| Snapshot the path | `new ArrayList<>(path)` |\n| Drop the last | `path.remove(path.size() - 1)` |\n| Flags | `boolean[] used = new boolean[n]` |\n| Undo a `StringBuilder` append | `sb.setLength(sb.length() - 1)` |\n| A row of n dots, one changed | `\".\".repeat(n)`, `sb.setCharAt(i, 'Q')` |\n";export{e as default};